# palimpsest 백서 — 코드를 근거로 삼는 프로젝트 온톨로지와 그 위의 에이전트

> **이 문서의 지위**
>
> - **성격**: 논거 문서(why). palimpsest가 왜 이렇게 설계되어야 하는지를 논증한다.
> - **권위 아님**: 사실·동작·계약의 권위는 코드와 ADR에 있다(헌장 §4-11). 이 문서는 그 위에 얹힌 배경 지도다. 충돌 시 ADR이 이긴다.
> - **자기제약**: 코드가 SoT인 동작을 여기서 이중화하지 않는다 — 이중화는 곧 drift다. 따라서 이 백서는 **구현 사실을 담지 않고**, 구현이 만족해야 할 **계약**만 담는다. 유일한 예외는 §11이며, 그 절은 스냅샷임을 명시한다.
> - **소비자**: palimpsest에 참여하는 사람과 에이전트. 방향 판단이 필요할 때 읽는다.
> - **갱신 주기**: 정체성·논거가 바뀔 때만. 슬라이스 단위 진척으로는 고치지 않는다.
> - **삭제 조건**: 여기의 논거가 전부 ADR과 코드로 흡수되어 이 문서가 요약 이상을 못 할 때.
> - **권위 출처**: `ADR-20260626-foundational-architecture`, `ADR-20260701-v1-ontology-recall-reframe`, `ADR-20260706-generative-curator-direction`, `ADR-20260712-palimpsest-identity-host-neutral-generative-curator`, `ADR-20260702-risk-designdecision-load-contract`, `ADR-20260713-multilang-depends-on-name-local-boundary`.
>   ⚠️ **이 인용들은 현재 이 저장소에 존재하지 않는다.** 선행 구현 저장소의 기록이며, 이 저장소로의 승계 범위는 미결이다(§11·§12-1). 승계가 끝나기 전까지 이 목록은 **가리킴이지 보증이 아니다** — 백서가 자기 논지의 예외가 되지 않도록 명시해 둔다.

---

## 1. 문제 — 프로젝트의 진실은 어디에 있는가

소프트웨어 프로젝트는 자기 자신에 대한 서술을 계속 만들어낸다. 기획서, 설계 문서, 아키텍처 다이어그램, 백서, README, 주석, 위키, 티켓. 이 서술들은 만들어지는 순간에는 참이다.

문제는 그다음이다. **코드는 계속 변하고, 서술은 변하지 않는다.** 동기화는 사람의 규율에 의존하며, 사람의 규율은 마감과 인수인계와 퇴사를 이기지 못한다.

여기서 흔한 오해를 정정해야 한다. 낡은 문서의 문제는 "쓸모없어지는 것"이 아니다. 쓸모없기만 하다면 무해하다. 실제 문제는 **낡은 문서가 거짓 신호가 된다**는 것이다. 독자는 그것이 낡았는지 모른 채 그것을 근거로 판단한다. 없는 문서보다 낡은 문서가 해롭다. 주석도 같은 운명이다 — 코드 옆에 있다는 이유로 더 신뢰받지만, 동기화 보장은 조금도 더 강하지 않다.

코드는 다르다. **코드는 설명이 아니라 동작 그 자체다.** 코드는 시스템이 무엇을 하는지 서술하지 않는다. 코드가 곧 시스템이 하는 일이다. 그러므로 코드는 자기 자신에 대해 낡을 수 없고, 틀릴 수 없다. 코드가 "틀렸다"고 말할 때 우리가 의미하는 것은 코드가 사실과 어긋난다는 뜻이 아니라 **코드가 의도와 어긋난다**는 뜻이다. 그 둘의 구분이 이 백서 전체의 축이다.

이것은 새로운 주장이 아니다. 이 작업공간의 행동 헌장 §4-11이 이미 같은 원칙을 담고 있다: 동작하는 코드베이스가 권위의 원본이고, 코드 변화에 자동 동기화되지 않는 문서는 권위가 아니다. 백서가 하는 일은 그 규범을 **시스템 설계 원리로 확장**하는 것이다.

### 1.1 그러나 코드는 말이 없다

코드가 유일하게 신뢰할 수 있는 진실이라는 것과, 코드가 충분하다는 것은 다르다.

코드는 **무엇을 하는가**를 남김없이 담는다. 그러나 **왜 그렇게 하는가**는 담지 않는다. 어떤 대안이 검토되고 왜 버려졌는지, 어떤 제약이 이 구조를 강요했는지, 이 함수의 이상해 보이는 분기가 어느 장애의 흉터인지 — 코드에 없다. 그 지식은 사람의 머리와, 낡아가는 문서와, 흩어진 대화에 있다.

그래서 문제 공간은 다음의 비대칭으로 정의된다.

|  | 코드 | 의도·결정 |
|---|---|---|
| 신뢰성 | 항상 참 | 낡는다 |
| 완전성 | "무엇"에 대해 완전 | "왜"를 유일하게 담음 |
| 기계 판독 | 가능 | 어려움 |
| 자동 갱신 | 자동 | 불가 |

한쪽만으로는 프로젝트를 이해할 수 없다. 코드만 보면 왜 그런지 모르고, 문서만 보면 지금 어떤지 모른다.

---

## 2. 무엇을 만드는가 — 전달 형태와 대상 범위

§1은 문제를 세웠다. 그러나 그 문제를 **어디서** 푸는지가 설계의 절반을 결정한다. 자기 코드베이스를 분석하는 도구와 임의의 남의 코드베이스를 분석하는 도구는 이름만 같은 다른 물건이다. 그러므로 형태를 먼저 못 박는다.

### 2.1 형태와 전제

palimpsest는 **사용자 프로젝트에 설치되는 Claude Code 플러그인**이다. 자기 자신의 온톨로지를 만드는 도구가 아니라, **설치된 그 프로젝트의 온톨로지를 만드는 도구**다.

대상 프로젝트에 거는 전제는 둘뿐이다.

- **git으로 관리된다** — 시간축(C6)과 이력이 전부 여기서 나온다. git이 없으면 결박도 신선도도 정의되지 않는다.
- **Claude Code를 쓴다** — §4의 "에이전트"가 추상적 존재가 아니라 실제 소비자로 존재한다.

그 밖에는 아무것도 가정하지 않는다. 언어, 프레임워크, 빌드 시스템, 저장소 개수, 프로젝트의 나이 — 전부 미지다. **이 "가정하지 않음"이 관용이 아니라 제약의 원천이다.** 아래 다섯 귀결이 그 제약이며, 이 백서의 여러 결론이 여기서 파생된다.

### 2.2 남의 프로젝트를 다룬다는 것의 다섯 귀결

**(1) 빌드할 수 없다.** 대상 프로젝트의 툴체인·의존성·비밀·환경 변수를 palimpsest는 갖지 못한다. 설령 현재 HEAD를 빌드할 수 있다 해도, 임의의 과거 커밋이 여전히 빌드된다는 보장은 없다 — 그리고 §7의 계보는 과거를 요구한다. 따라서 **빌드를 요구하는 정밀 분석은 주경로가 될 수 없다.** 정밀도가 모자라서가 아니라, 균일하게 돌지 않아서다. 이 귀결이 §12-2(C5 조달)의 판단 공간을 거의 다 닫는다.

**(2) 언어를 고를 수 없다.** 대상은 단일 언어가 아니고, 흔히 한 저장소 안에서도 여러 언어다. 추출 능력은 언어마다 다를 수밖에 없다 — 정적 타입 주석이 있는 언어와 없는 언어에서 같은 깊이의 의존 관계를 뽑을 수는 없다.

능력 차이 자체는 결함이 아니다. **결함은 그 차이를 숨기는 것이다.** 두 가지 규칙이 따라 나온다.

- **없는 것을 지어내지 않는다.** 타입 정보가 없는 언어에 타입 의존 엣지를 만들어 채워 넣으면, 그것은 사실층에 들어간 추론이다(P2 위반). 비어 있는 편이 옳다.
- **하한은 전역이 아니라 언어별로 표시된다.** "이 저장소에서 A 언어는 참조까지, B 언어는 구조까지"가 출력에 드러나야 한다. 그러지 않으면 §6.3의 거짓 안전이 언어 단위로 발생한다 — 한 언어를 못 읽는데 전부 읽은 것처럼 답하는 순간, 그 언어에 있는 결함은 "없다"고 보고된다.

**(3) 저장소 경계가 흔하다.** 멀티레포는 예외가 아니라 기본 가정 중 하나다. 그러므로 C4(경계 통과)의 대표 사례는 프로세스 경계가 아니라 **저장소 경계**이고, 결박 좌표는 커밋 하나가 아니라 **(저장소, 커밋)의 조합**이 된다. 신선도도 마찬가지로 조합 위에서 정의된다. 이것은 "나중에 확장할 축"이 아니라 처음부터 서 있어야 하는 축이다.

**(4) 프로젝트의 나이를 고를 수 없다.** → §2.3.

**(5) 남의 머신에서 돈다.** palimpsest는 서비스가 아니라 설치물이다. 설치 부담, 상주 비용, 최초 구축 시간이 전부 사용자의 것이다. 그러므로 **설치·운영 비용은 성능 최적화 항목이 아니라 1급 설계 제약**이며, 컨텍스트 비용(P9)과 같은 급이다. 다만 이것이 백서가 저장소 기술을 정한다는 뜻은 아니다 — git이 SoT이므로 그래프는 재구축 가능한 투영이고 교체 가능하다(P5). 백서가 고정하는 것은 **그 비용이 판단에 들어간다**는 사실뿐이다.

### 2.3 두 극단 — 오늘 시작한 프로젝트와 20년 된 프로젝트

둘 다 대상이다. 그런데 둘은 정반대의 콜드스타트 문제를 낸다.

**신생 프로젝트: 사실층이 비어 있고 의도가 코드를 선행한다.** 결정은 있는데 그것을 실현한 코드가 아직 없다. 그러면 결박할 좌표가 없다. 여기서 §3의 결박 모델이 한 상태를 더 가져야 한다는 것이 드러난다 — 결박은 있음/없음의 이분이 아니라 **보류(pending)**를 포함한다. 그리고 보류는 낡음과 다르다: **낡음은 좌표가 변한 것이고, 보류는 좌표가 아직 없는 것이다.** 둘을 같은 표시로 뭉개면 "아직 만들지 않았다"와 "만든 뒤 어긋났다"가 구별되지 않는다.

**성숙 프로젝트: 사실층은 즉시 풍부하고 의도층은 비어 있으며, 그 의도는 이미 유실됐다.** git 이력을 전부 읽으면 사실층은 첫날에 선다. 그러나 왜 그렇게 했는지는 기록된 적이 없다. 커밋 메시지·PR·이슈에 파편이 남아 있지만 그것은 결정의 기록이 아니라 **결정의 흔적**이다.

여기서 가장 위험한 유혹이 생긴다. 에이전트가 이력을 읽고 "이 결정은 아마 X였을 것"을 채워 의도층을 즉시 그럴듯하게 만드는 것. **그것이 정확히 §4가 금지하는 세탁이다.** 과거 의도의 발굴은 유용하지만 그 산물은 추론 파티션에 들어가고, **사람의 승인으로만 의도층으로 승격**된다. 이 규칙이 없으면 성숙 프로젝트에 설치한 첫날에 시스템 전체가 오염되고, 그 뒤로는 어디까지가 기록이고 어디부터가 재구성인지 아무도 모른다.

두 극단이 하나의 결론으로 모인다. **의도층은 채워지는 데 시간이 걸리고, 그동안 시스템은 비어 있음을 정직하게 보여야 한다.** 빈 의도층을 추측으로 메우는 제품이 첫인상은 좋다. 그 제품은 §1의 문제를 자동화한 것이다.

### 2.4 Claude Code는 매개체이지 근거가 아니다

플러그인이라는 형태는 **전달 경로**의 결정이지 코어의 정의가 아니다. 경계를 여기서 함께 적는다.

- **코어는 소비자-중립 표면을 노출한다.** 플러그인은 그 위의 얇은 어댑터다. 특정 호스트를 위한 특화 표면을 코어에 만들지 않는다.
- **코어는 특정 호스트의 개념을 내부화하지 않는다.** 호스트의 도메인 어휘로의 매핑은 어댑터의 책임이다.
- **코어는 특정 호스트의 산출물에 역방향으로 의존하지 않는다.** 외부 생산자의 사실을 받을 수는 있으나, 그것은 **출처 중립적 수용**이어야 한다 — palimpsest는 그 사실을 누가 만들었는지 알 필요가 없다.
- **"대상 프로젝트가 Claude Code를 쓴다"는 전제는 소비자에 대한 가정이지 코어의 의존이 아니다.** 이 둘의 구분이 무너지는 순간 — 코어가 호스트의 산출물 없이는 답을 못 내는 순간 — P7이 깨진다.

이 절을 명시하는 이유는 역사적이다. 이 프로젝트는 이미 한 번, 특정 소비자의 필요를 논하다가 코어를 그 소비자에 결합시키는 쪽으로 추론이 흘러간 적이 있다(`ADR-20260712` §맥락). **형태를 정할 때 경계를 같이 적어두지 않으면 같은 드리프트가 조용히 반복된다.**

---

## 3. 두 층 — 사실층과 의도층

palimpsest의 온톨로지는 두 층으로 나뉜다. 이 분리는 편의가 아니라 **신뢰성의 등급이 다르기 때문**이다.

**사실층 (아래층)** — 코드에서 결정론적으로 추출한 것. 무엇이 존재하고, 무엇이 무엇을 부르고, 어디가 진입점이고, 데이터가 어디로 흐르는가. 이 층의 성질:

- 코드가 바뀌면 재추출로 자동으로 바뀐다. **낡는다는 개념이 적용되지 않는다** — 재추출이 곧 정의이므로.
- 같은 커밋에서는 항상 같은 답이 나온다.
- 사람의 규율에 의존하지 않는다.

**의도층 (위층)** — 의사결정, 대안, 근거, 여정, 가치. 코드에 없으므로 사람과 조직이 공급한다. 이 층은 낡을 수 있다.

핵심 장치는 층의 분리 자체가 아니라 **결박(grounding)**이다.

> 의도층의 모든 항목은 사실층의 좌표에 결박된다. 그 좌표의 코드가 변하면, 기계는 그 결박이 stale임을 안다.

이것이 백서가 §1의 문제에 내놓는 답이다. **문서가 낡는 것은 막을 수 없다. 그러나 낡았다는 사실을 감지하는 것은 기계적으로 가능하다.** "이 결정은 어느 커밋 기준으로 쓰였고, 그 커밋 이후 결박된 코드가 바뀌었는가" — 이 질문은 결정론적으로 답할 수 있다. 낡음이 조용한 거짓 신호가 아니라 **표시된 상태**가 되는 순간, 낡은 문서는 다시 안전해진다.

결박은 세 상태를 갖는다. **살아 있음**(좌표가 현재 코드와 일치), **낡음**(좌표의 코드가 그 뒤 변함), **보류**(좌표가 아직 존재하지 않음 — §2.3의 신생 프로젝트). 세 번째를 두 번째와 뭉개면 "아직 안 만들었다"가 "어긋났다"로 읽힌다.

프로젝트 이름이 이것을 말한다. palimpsest는 지우고 다시 쓴 양피지이며, 지워진 이전 층이 그 아래로 비쳐 보이는 것이다. 표면의 코드 아래에 결정과 의도의 층이 쌓여 있으나 잘 드러나지 않는다. 그 층을 다시 읽을 수 있게 만드는 것이 이 시스템의 목적이다.

---

## 4. 역할 분담 — 사람·기계·에이전트

이 절의 세 **자리**는 §3의 두 **층**과 다른 축이다. §3은 *무엇이 기록되는가*(사실층·의도층)를 나누고, 이 절은 *누가 무엇을 지는가*를 나눈다. 두 축은 직교하며, 아래에서 "층"은 §3의 뜻으로만 쓴다.

결정론적 기계만으로 코드베이스에 의미를 부여하거나 서사적 결함을 발견하는 것은 불가능하다. "이 흐름이 사용자에게 무엇을 의미하는가", "이 두 경로의 차이가 실수인가 의도인가" — 이런 판단에는 도메인 이해와 맥락 추론이 필요하고, 그것은 규칙 엔진의 능력 밖이다.

그러나 그 역도 참이다. LLM 에이전트만으로는 정확성을 보증할 수 없다. 에이전트는 추측하고, 놓치고, 같은 질문에 매번 다르게 답한다. 코드베이스가 크면 전부 읽지 못하고, 읽지 못한 부분에 대해 그럴듯하게 말한다.

그리고 어느 쪽도 갖지 못하는 것이 하나 더 있다 — **결과를 지는 자리.** 이것은 능력의 문제가 아니라 구조의 문제이므로, 기계가 정밀해지거나 모델이 좋아져도 생기지 않는다.

palimpsest는 셋을 경쟁시키지 않고 자리를 나눈다.

| 자리 | 갖는 것 | 갖지 않는 것 |
|---|---|---|
| **사람** | 방향, 판정, 책임 | 전수 열람, 구현 대역폭 |
| **기계** | 협상 불가능한 기준 — §3 사실층, 그리고 승인되어 결박된 의도층 | 의미 부여, 우선순위 |
| **에이전트** | 구현·실행·조사, 의미 구성, 결박된 보고 | 자기 산물에 대한 판정, 승격 권한 |

**가운데 자리가 이 분담을 성립시킨다.** 사람과 에이전트가 직접 맞물리면 사람은 에이전트가 쓴 산문을 읽어서만 판정할 수 있고, 그 산문은 에이전트의 자기 증언이다. 기계가 양쪽이 함께 가리킬 수 있는 기준을 들고 있을 때에만 **판정이 열람량에 비례하지 않게 된다.**

이하에서 인용하는 외부 조사는 [`docs/research/2026-08-01-agentic-coding-verification-gap.md`](docs/research/2026-08-01-agentic-coding-verification-gap.md)이며 "조사 §x"로 표기한다. 이 조사를 포함한 연구 문서 전체는 [`docs/research/README.md`](docs/research/README.md)에 단원·장으로 재편되어 있다 — **그 문서들은 이 백서의 증거이지 요구사항이 아니며, 백서의 범위·능력 계약을 정하는 입력으로 쓰지 않는다.**

### 4.1 사람의 자리 — 책임이 앵커다

사람이 방향과 판정을 갖는 이유를 "에이전트가 그것을 못 하기 때문"이라고 적으면 안 된다. 그것은 능력 주장이고, **능력 주장 위에 세운 경계는 모델이 좋아질 때마다 이동한다.** 이 백서가 서는 근거는 하나다.

> 책임은 결과를 지는 주체가 있어야 성립하고, 그 주체는 시스템 안에서 만들어지지 않는다.

나머지는 여기서 파생된다.

- **방향** — 무엇이 할 가치가 있고 어떤 트레이드오프가 수용 가능한가. 에이전트도 착수는 한다. 갖지 못하는 것은 착수 능력이 아니라 **선호 순서의 소유**다.
- **판정** — 무엇을 받아들일 것인가. 여기에는 추론을 의도층으로 승격시키는 승인이 포함된다(§2.3, §10). 승인이 없으면 발굴된 의도는 추론으로 남는다.
- **자기 교정과 자발성** — 자기 산물의 결함을 스스로 찾아 되돌리는 것. 조사된 범위에서 에이전트의 자기평가는 확신이 오보정되어 있고, 더 무겁게는 **부족분을 내부에서 측정할 방법이 없다** — 재실행을 늘려도 분산이 드러날 뿐 편향은 드러나지 않는다(조사 §5.4~5.6). 관측되지 않는 부족분을 떠안는 자리가 곧 책임의 자리다.

**검토는 diff를 읽는 일이 아니다.** 이 절에서 가장 실무적인 구분이다. 검토가 "생성된 변경을 사람이 통독하는 것"을 뜻하면 그것은 동기 차단 단계가 되고, 전체 처리량의 상한이 사람의 열람 속도로 고정된다. 그리고 그 상한은 시간이 지날수록 **낮아진다** — 코드의 다수를 에이전트가 쓰면 저자성이 지식 지표로서 무효가 되므로, 심사석에 앉은 사람의 자격이 루프가 돌수록 얕아진다(조사 §4.1, §7.1). 통독을 검토의 정의로 삼으면 이 자리는 스스로 무너진다.

성립하는 정의는 하나다. **검토는 주장을 기계의 기준에 대조하는 일이다.** 대조가 가능하려면 주장이 좌표를 동반해야 하고(P3), 무엇을 보지 않았는지가 표시되어야 한다(P4). 사람이 감당하는 것은 코드의 양이 아니라 **판정의 양**이다.

### 4.2 기계의 계약

기계는 다음 네 가지를 보증한다. 이것이 에이전트가 기계의 출력에 기대어도 되는 근거이며, 동시에 사람의 검토가 통독 없이 성립하는 근거다.

1. **완전성** — 찾을 수 있는 것을 빠뜨리지 않는다. 놓친 것이 있으면 놓쳤다고 말한다. 찾을 수 있는 범위가 언어·저장소마다 다르면 그 차이도 함께 말한다(§2.2-2).
2. **결정론** — 같은 커밋에 대해 항상 같은 답을 낸다. 재실행이 답을 바꾸지 않는다.
3. **근거 결박** — 모든 답이 코드의 좌표를 동반한다. 좌표 없는 주장을 내지 않는다.
4. **정직한 공백** — 모르는 것을 모른다고 말한다. 추측으로 공백을 메우지 않는다.

이 넷은 사람에게도 같은 값을 갖는다. 2와 3이 없으면 대조가 재현되지 않고, 4가 없으면 **"문제 없음"과 "그 부분을 안 봤음"이 구별되지 않는다.**

### 4.3 에이전트의 자리

에이전트는 기계가 못 하는 것을 한다. 가설을 세우고, 서사를 구성하고, 우선순위를 매기고, 구현하고, 실행하고, 조사하고, 사람의 질문을 사람의 언어로 받는다. **내용에는 제약을 걸지 않는다** — 제약을 걸면 에이전트를 쓰는 이유가 사라진다.

제약은 전부 **형식**에 걸리며 네 가지다: 파티션 분리(P2), 좌표 동반(P3), 하한 표시(P4), 자기 판정 금지(P10). 넷 다 "무엇을 주장해도 좋으나 그 주장이 어떤 자격인지 감출 수 없다"는 한 가지 요구의 다른 얼굴이다.

**보고의 척추는 서술이 아니다.** 에이전트가 자기 실행에 대해 쓴 산문은 그 자체로 자기 증언이고, 표현된 확신은 내부 확신과 어긋난다 — 확신도는 사실과 추론을 가르는 대체물이 될 수 없다(조사 §5.1). 그러므로 보고는 두 부분으로 갈린다.

- **설명·서사·우선순위** — 에이전트의 자유. 추론 파티션에 남는다.
- **척추** — 무엇을 건드렸는가, 무엇을 확인했는가, **무엇을 보지 않았는가**. 셋 다 기계가 대조할 수 있는 좌표로 표현된다.

세 번째가 가장 무겁다. 에이전트는 기계가 넓게 잡은 것을 좁히는 일을 맡는데(P6, §6.3), **좁히면서 버린 것이 산출에 남지 않으면 거짓 안전이 제거되는 것이 아니라 관측 불가능한 자리로 옮겨갈 뿐이다.** 기계에만 P4를 걸고 에이전트에 걸지 않으면 §6.3의 논거가 한 칸 뒤에서 무효가 된다.

### 4.4 넘으면 안 되는 두 경계

**세탁 금지.** 에이전트의 산물이 기계의 사실로 승격되어서는 안 된다. 추론은 추론으로 표시되고, 출처와 공백과 확신도를 동반해야 한다. 사실과 추론의 파티션은 전체를 덮고 겹치지 않는다.

이 금지가 왜 결정적인가. **이것이 없으면 시스템이 자기 추측을 자기 근거로 삼는 고리가 닫힌다.** 한 번 세탁된 추론은 다음 회차에 사실로 읽히고, 그 위에 다시 추론이 쌓인다. 몇 회 반복되면 어디까지가 코드였는지 아무도 모른다. palimpsest가 해결하려던 문제 — 낡은 서술이 거짓 신호가 되는 것 — 를 더 나쁜 형태로 재생산하게 된다. 성숙 프로젝트의 과거 의도 발굴(§2.3)이 이 금지의 가장 큰 시험대다. 거기서는 세탁의 유인이 가장 크고, 세탁된 결과가 가장 그럴듯하다.

**자기 판정 금지.** 합성하는 주체가 자기 합성물의 참/거짓을 판정하면 자기-인증 고리가 닫힌다. 내용 판정은 생성 경로 밖에 둔다(P10).

단, 이 분리를 실제보다 강하게 읽으면 안 된다. **분리는 고리를 닫지 않을 뿐, 판정을 신뢰 가능하게 만들지 않는다.** 분리된 검증자도 자기 커버리지를 모르고, 검증자를 하나 더 붙이면 그 검증자를 볼 자리가 다시 비는 무한 후퇴가 된다(조사 §5.4~5.6). 따라서 **P4는 검증 산출에도 걸린다** — "찾은 반론"은 "반론의 전부"가 아니며, 그렇게 표시되어야 한다. 이 요구를 §5의 능력 계약이 어떻게 받을지는 미결이다(§12-11).

### 4.5 이 분담이 안전한 이유

- 에이전트가 틀려도 사실층은 오염되지 않는다. 틀린 추론은 추론 파티션 안에 격리되고 나중에 반증될 수 있다.
- 사실층이 넓을수록 에이전트가 추측할 필요가 줄어든다. **기계의 능력을 키우는 것이 곧 에이전트의 할루시네이션을 줄이는 일이다.** 이 둘은 상충하지 않고 같은 방향이다.
- **사람이 차단 단계에서 내려온다.** 판정이 통독이 아니라 대조이므로 루프의 처리량이 사람의 열람 속도에 묶이지 않는다. 사람이 빠지는 것이 아니다 — 책임과 방향은 그대로 두고, 대역폭을 요구하는 자리에서만 내려온다.
- 어디까지가 기계의 보증이고 어디부터가 에이전트의 판단인지가 **표시된다.** 이것은 사람이 언제나 그 둘을 구별해낸다는 주장이 아니라 산출물에 거는 형식 요건이다. 표시가 있어도 읽지 않으면 구별은 일어나지 않으며, 그 간극은 이 절이 닫지 못한다.

---

## 5. 기계가 제공해야 하는 것 — 능력 계약

에이전트가 프로젝트를 정확히 파악하려면 기계가 아래를 제공해야 한다. 각 항목은 구현 중립적으로 쓴다 — 어떻게 만족시킬지는 코드와 ADR의 몫이고, 여기서는 **무엇이 성립해야 하는가**만 정한다.

각 항목에 "이것이 없으면 에이전트가 무엇을 추측하게 되는가"를 붙인다. 그것이 그 능력의 존재 이유다.

### C1. 구조 — 무엇이 존재하는가

코드베이스에 어떤 단위가 있고 무엇이 무엇을 담는지. 파일, 모듈, 타입, 함수, 그리고 그것들의 포함 관계.

*없으면*: 에이전트가 이름 검색으로 코드를 찾는다. 검색어에 걸리지 않는 것은 존재하지 않는 것이 된다.

### C2. 참조 — 무엇이 무엇을 부르는가

호출, 임포트, 타입 의존. **정밀도가 중요하다.** 같은 이름의 모든 대상에 엣지를 거는 방식은 그래프를 채우지만 신호를 죽인다. 거짓 엣지는 없는 엣지보다 나쁘다 — 에이전트가 존재하지 않는 경로를 따라 추론하기 때문이다.

다언어에서는 여기에 경계 규칙이 추가된다. **이름 기반 해소는 언어 경계를 넘지 않아야 한다** — 한 언어의 이름이 다른 언어의 동명 대상에 붙으면 그것은 순수한 거짓 엣지다. 언어를 잇는 것은 이름의 우연한 일치가 아니라 명시적 임포트여야 한다.

*없으면*: 에이전트가 "아마 이걸 부를 것"이라고 가정한다.

### C3. 진입점 — 시스템은 바깥에서 어떻게 열리는가

HTTP 라우트, 메시지 컨슈머, 스케줄러, CLI 명령, 이벤트 핸들러. **여정은 진입점에서 시작하므로, 진입점 목록이 곧 가능한 서사의 목록이다.** 진입점을 하나 놓치면 그 진입점에서 시작하는 모든 서사가 시야에서 사라진다.

진입점은 프레임워크가 정의한다. 대상 프레임워크를 고를 수 없으므로(§2.1), 이 능력은 **프레임워크가 늘어날 때마다 자란다** — 그리고 아직 모르는 프레임워크의 진입점은 공백으로 표시된다.

*없으면*: 에이전트가 "사용자가 이 기능을 어떻게 시작하는가"에 답할 수 없다.

### C4. 경계 통과 — 프로세스·서비스·저장소를 건너는 흐름

프론트엔드가 부르는 백엔드, 서비스가 부르는 서비스, 큐를 건너는 메시지, 그리고 **다른 저장소에 있는 대상**. 실제 사용자 여정은 거의 항상 이런 경계를 넘는다. **경계에서 끊기는 추적은 여정을 추적하지 못한다.**

이 항목은 도구 선택에 강한 제약을 건다. 단일 빌드 단위를 데이터베이스로 삼는 분석 엔진은 이 경계를 구조적으로 넘지 못한다. 아무리 정밀해도 넘지 못한다 — 반대편이 DB 안에 없기 때문이다.

*없으면*: 에이전트가 각 서비스를 독립적으로 이해하고, 그 사이는 상상한다.

### C5. 경로 위의 지배관계 — 이 지점에 닿는 모든 경로가 저것을 통과하는가

특정 지점에 도달하는 경로의 집합과, 그 경로들이 어떤 검사·경계·가드를 통과하는지. 형식적으로:

> 진입점 E에서 지점 S로 가는 경로 중, 술어 P로 지배되지 않는 것이 존재하는가?

이것이 §6에서 다룰 서사적 결함 판정의 공통 형태다. 그리고 대부분의 코드 검색 도구가 제공하지 않는 것이다.

*없으면*: 에이전트가 "이 검사가 어딘가에서 이뤄질 것"이라고 가정한다. 보안 맥락에서 이 가정은 치명적이다.

### C6. 시간축 — 이 사실은 언제 기준인가

모든 사실이 어느 커밋 기준인지, 그리고 그 기준 이후 관련 코드가 변했는지. §3의 결박 감지가 여기에 의존한다. 멀티레포에서는 기준이 커밋 하나가 아니라 **(저장소, 커밋)의 조합**이며, 브랜치가 갈라지면 같은 심볼의 서로 다른 버전이 붕괴 없이 공존해야 한다.

*없으면*: 낡음이 표시되지 않고, 시스템 전체가 §1의 문제를 재생산한다.

### 횡단 요구 U. 균일성 — 빌드 없이, 임의의 커밋에서

C1~C6은 **대상 프로젝트를 빌드하지 않고, 이력상의 임의 커밋에 대해 같은 방식으로** 성립해야 한다(§2.2-1).

이것은 능력이 아니라 능력에 걸리는 조건이다. 그러나 조건이 능력을 결정한다. HEAD에서만 답할 수 있는 능력은 §7의 계보를 지탱하지 못하고, 빌드가 되는 프로젝트에서만 답할 수 있는 능력은 설치 대상의 일부에서 조용히 침묵한다. **균일하지 않은 능력은 "때때로 있는 능력"이고, 때때로 있는 능력에 기대어 판정을 내리면 그 판정은 언제 유효한지 알 수 없다.**

균일성을 포기하고 얻을 수 있는 것은 정밀도다. 그 교환은 금지되지 않지만, 주경로가 아니라 **보조로만** 허용되고, 그 산물은 적용 범위(어느 커밋, 어느 언어, 어느 저장소)를 명시적으로 달고 다녀야 한다.

### 계약 간 관계

C1~C2는 정적 구조, C3~C5는 동적 흐름, C6은 그 전부에 걸리는 시간 차원, U는 전부에 걸리는 적용 조건이다. **C5는 C2~C4 위에서만 성립한다** — 경로를 계산하려면 먼저 참조와 진입점과 경계 통과가 있어야 한다. 능력 확장의 순서가 여기서 강제된다.

---

## 6. 서사적 결함 — 왜 단위 테스트로 안 잡히는가

### 6.1 결함의 형태

사용자 여정 안에서만 드러나는 결함에는 공통 구조가 있다.

> **각 부품은 개별적으로 옳다. 조합에서만 틀리다.**

단위 테스트는 부품을 검사한다. 그러므로 모든 부품이 개별적으로 옳은 이 결함들에 대해, 단위 테스트는 **우연히 못 잡는 것이 아니라 구조적으로 볼 수 없다.** 테스트 커버리지를 100%로 올려도 달라지지 않는다.

통합 테스트도 부분적 답에 그친다. 통합 테스트는 누군가 상상한 경로만 검사하는데, 이런 결함은 정확히 **아무도 상상하지 못한 경로**에 산다.

### 6.2 판정 가능한 형태로의 환원

이 결함들을 정적으로 다루려면 서사를 술어로 번역해야 한다. 번역하면 거의 전부 C5의 형태로 수렴한다.

| 서사 | 정적 술어 |
|---|---|
| "다른 화면으로 남의 데이터가 보인다" | 같은 리소스에 닿는 진입점 중 소유권 가드로 지배되지 않는 경로가 있다 |
| "결제 안 했는데 배송이 나갔다" | 상태 전이 그래프에서 합법 전이 집합 밖의 도달 가능한 전이가 있다 |
| "롤백됐는데 알림은 이미 나갔다" | 되돌릴 수 없는 외부 효과가 트랜잭션 경계 안에 있다 |
| "재시도로 두 번 청구됐다" | 재시도 가능 진입점에서 닿는 상태 변경이 멱등 키 검사로 지배되지 않는다 |
| "중간에 실패했는데 앞 단계가 남았다" | 단계 k의 실패 경로가 단계 <k의 보상 지점에 도달하지 않는다 |
| "입력이 그대로 쿼리에 꽂힌다" | 신뢰 경계 밖 입력이 정화 없이 민감 지점에 도달한다 (경계를 건너서도) |
| "권한이 낮은 쪽으로 들어가면 뚫린다" | 낮은 권한 진입점에서 높은 권한 지점으로 가는 경로가 있다 |

첫 행이 특히 시사적이다. 판정 기준이 **절대적이지 않고 집합 내 비대칭**이다 — 같은 리소스에 닿는 열 개 경로 중 아홉이 가드를 통과하면, 나머지 하나가 결함이다. 이런 판정은 코드베이스 전체의 관계를 동시에 볼 수 있을 때만 가능하다. 파일 하나를 보는 리뷰어도, 함수 하나를 보는 테스트도 볼 수 없다.

### 6.3 완전성이 정밀도보다 앞선다

여기서 판정에 필요한 것이 무엇인지를 정확히 짚어야 한다. 직관과 반대이기 때문이다.

이 판정들에 필요한 것은 **정밀한 포인터 분석이 아니라 보수적으로 넓은 경로 집합**이다.

이유: 판정의 형태가 "모든 경로가 P를 통과하는가"이므로, **경로를 놓치면 통과하지 않는 경로를 못 보고 "안전하다"고 답한다.** 보안 맥락에서 거짓 안전(false-clean)은 최악의 실패 양식이다. 아무 답도 못 하는 것보다 나쁘다 — 사람이 확인을 그만두기 때문이다.

반대로 경로를 과다하게 잡으면 존재하지 않는 위험을 보고한다. 이것은 노이즈지만, 근거 좌표가 붙어 있으므로 **에이전트나 사람이 확인해서 걷어낼 수 있다.**

그러므로 순서가 정해진다.

> **기계는 완전성을 맡고, 정밀도(가지치기)는 근거를 보며 에이전트가 맡는다.**

이 순서가 뒤집히면 — 기계가 정밀도를 위해 불확실한 경로를 조용히 버리면 — 시스템이 조용히 거짓말하기 시작한다. 그리고 그 거짓말은 §4의 "정직한 공백" 계약 위반이므로, 사람이 감지할 방법이 없다.

같은 이유로 **완전히 sound한 정적 호출 그래프는 존재하지 않는다**는 사실을 시스템이 정면으로 인정해야 한다. 리플렉션, 의존성 주입, 동적 디스패치는 어떤 정적 분석기도 완전히 따라가지 못한다. 그러므로 기계의 답은 언제나 **하한(lower bound)**이며, 하한임이 출력에 표시되어야 한다. 이것을 숨기고 "전부 찾았다"고 말하는 순간 그 시스템은 신뢰할 수 없는 것이 된다.

그리고 임의의 프로젝트를 대상으로 하면 하한은 하나가 아니다. **언어마다, 프레임워크마다, 저장소마다 하한의 깊이가 다르다**(§2.2-2). 전역 하한 하나만 표시하면 그것은 가장 얕은 부분에서는 과장이고 가장 깊은 부분에서는 과소평가다. 그러므로 하한은 **어디에 대한 하한인지와 함께** 표시되어야 한다.

---

## 7. 그 위에 쌓이는 것 — 결정이 코드가 되는 경로

사실층은 시작점이지 목적지가 아니다. 목적지는 그 위에 쌓이는 층이다.

프로젝트가 진행되면서 사람과 팀과 조직과 외부 참여자가 결정을 만든다. 요구가 들어오고, 대안이 검토되고, 하나가 선택되고, 그것이 코드가 되고, 코드가 가치를 만든다. palimpsest는 이 사슬 전체를 1급으로 다룬다.

**결정 → 코드 → 여정 → 가치**

각 링크가 온톨로지의 엣지다.

- **결정 → 코드**: 어떤 결정이 어느 코드로 실현되었는가. 이것이 §3의 결박이며, 낡음 감지의 기반이다. 신생 프로젝트에서는 이 링크가 한동안 보류 상태로 존재한다(§2.3).
- **코드 → 여정**: 그 코드가 어느 사용자 여정 위에 있는가. C3~C5가 이것을 계산 가능하게 만든다.
- **여정 → 가치**: 그 여정이 누구에게 무엇을 하는가. 이 링크는 코드에 없다 — 사람이 공급한다.

### 버려진 것을 보존한다

채택된 결정만 남기지 않는다. 검토되고 버려진 대안, 중단된 접근, 대체된 이전 결정을 전부 보존한다.

이유는 회귀 방지다. **"이미 시도했고 왜 버렸는지"를 모르면 같은 벽에 다시 부딪힌다.** 최신 결정만 남은 저장소는 과거의 잘못된 결정으로 돌아가는 것을 막지 못한다. 오히려 "왜 이렇게 안 했지?"라는 질문에 아무도 답할 수 없게 만든다.

보존은 삭제가 아니라 **무효화**로 이뤄진다. 대체된 결정은 지워지지 않고 "언제부터 언제까지 유효했는가"를 달고 남는다. 이것이 palimpsest라는 이름과 정확히 맞는 지점이다. 지워진 층이 아래로 비친다.

### 계보

결정은 고립되지 않는다. 하나가 다른 것을 대체하고, 대체된 것이 또 그 앞의 것을 대체한다. 이 사슬이 계보(lineage)이며, "지금 이 구조가 왜 이런가"에 대한 유일한 완전한 답이다. 최신 결정 하나만으로는 그 답의 마지막 한 조각밖에 얻지 못한다.

계보가 과거를 요구하기 때문에, 사실층도 과거를 균일하게 답할 수 있어야 한다(횡단 요구 U). 계보의 한 마디가 3년 전 커밋을 가리키는데 그 커밋에 대해 아무것도 말할 수 없다면, 계보는 이름만 남는다.

---

## 8. 목표 산출 — 에이전트가 할 수 있어야 하는 것

앞의 모든 것은 수단이다. 목적은 프로젝트를 아는 에이전트다. 그 에이전트가 할 수 있어야 하는 일과, 각각이 요구하는 능력.

| 산출 | 질문의 형태 | 요구 능력 |
|---|---|---|
| **온보딩** | "이 기능은 어디서 시작해 어디로 흐르나" | C1·C3·C4 — 여정 재구성 |
| **컨설팅** | "여기를 바꾸면 무엇이 깨지나" | C2·C4·C6 — 영향 추적 |
| **큐레이션** | "왜 이렇게 되어 있나" | §7 계보 + §3 결박 |
| **감사** | "이 흐름에 구멍이 있나" | C5 — 서사 규칙 판정 |
| **중재** | "내 브랜치와 팀 브랜치 중 어느 쪽이 최신인가" | C6 — 브랜치 간 신선도 |
| **질의** | 임의의 프로젝트 질문 | 전부 + 점진 회상 |

이 표가 백서의 실용적 결론이다. 능력 계약(§5)은 추상적 완전성을 위해 있는 것이 아니라, **각각이 구체적인 산출을 가능하게 하기 때문에** 있다. 어떤 능력을 먼저 만들지는 어떤 산출을 먼저 원하는지가 결정한다.

한 가지 횡단 제약이 있다. 에이전트의 컨텍스트는 유한하고, 길어질수록 성능이 떨어진다. 그러므로 회상은 **한 번에 전부 싣는 방식이 아니라 필요한 시점에 필요한 부분만 가져오는 방식**이어야 한다. 지식 베이스를 아무리 잘 만들어도 그것을 통째로 밀어 넣는 순간 그 지식은 쓸모를 잃는다.

이 표의 산출들은 프로젝트의 성숙도에 따라 서로 다른 시점에 켜진다. 성숙 프로젝트에서는 온보딩·컨설팅·감사가 설치 직후 유용하고(사실층이 즉시 선다), 큐레이션은 의도층이 쌓이면서 자란다. 신생 프로젝트에서는 정확히 반대다. **첫날에 전부 유용한 척하지 않는 것이 이 표를 정직하게 쓰는 방법이다.**

---

## 9. 설계 원리

앞의 논거를 불변식으로 압축한다. 각 항목은 구현 판단이 갈릴 때의 결정 기준이다.

| # | 원리 | 근거 |
|---|---|---|
| **P1** | 코드가 근거의 원본이다. 코드와 분리되어 동기화되지 않는 문서는 배경 이해용이지 권위가 아니다. | §1, 헌장 §4-11 |
| **P2** | 사실과 추론을 분리한다. 파티션은 전체를 덮고 겹치지 않으며, 추론이 사실로 세탁되지 않는다. | §4, `ADR-20260702-risk-designdecision-load-contract` |
| **P3** | 근거 없는 답을 내지 않는다. 모든 주장이 코드 좌표를 동반한다. | §4, `ADR-20260626` §근거 |
| **P4** | 공백을 정직하게 표시한다. 모르는 것을 추측으로 메우지 않고, 하한은 하한이라고 말하며, 하한의 깊이가 언어·프레임워크·저장소마다 다르면 그 차이까지 표시한다. | §4, §6.3, §2.2 |
| **P5** | git이 SoT이고 그래프는 재구축 가능한 투영이다. 같은 커밋은 같은 그래프를 낳는다. | `ADR-20260626` §근거 |
| **P6** | 완전성이 정밀도보다 앞선다. 기계는 넓게 잡고, 좁히는 것은 근거를 보며 에이전트가 한다. | §6.3 |
| **P7** | 환경에 종속되지 않는다. 특정 소비자의 개념을 코어에 내부화하지 않고, 특정 호스트 산출물에 역방향으로 의존하지 않는다. 플러그인은 전달 형태이지 코어의 정의가 아니다. | §2.4, `ADR-20260712` §결정1 |
| **P8** | 생성형 큐레이션은 1급 능력이다. 단, 출처·공백·확신도로 사실과 분리한다. | `ADR-20260712` §결정2, `ADR-20260706` |
| **P9** | 한 번에 싣지 않고 점진적으로 회상한다. 컨텍스트 비용이 1급 제약이다. | §8, `ADR-20260626` §근거 |
| **P10** | 생성자와 검증자를 분리한다. 합성하는 주체가 자기 합성물을 판정하지 않는다. | §4, `ADR-20260706` §결정4 |
| **P11** | 대상 프로젝트를 빌드하지 않고 변형하지 않는다. 능력은 임의의 과거 커밋에서 균일하게 성립해야 하며, 균일하지 않은 정밀도는 보조이지 주경로가 아니다. | §2.2-1, §5 횡단요구 U |
| **P12** | 설치·운영 비용이 1급 제약이다. palimpsest는 사용자의 머신에서 돌고, 그 비용은 사용자가 낸다. | §2.2-5 |
| **P13** | 책임은 사람의 자리이며 이전되지 않는다. 방향·판정·자기 교정이 사람에게 있는 것은 능력 배분이 아니라 이 책임에서 파생된 것이고, 따라서 모델의 진보가 이 경계를 옮기지 않는다. | §4.1 |
| **P14** | 사람의 판정은 통독이 아니라 대조로 성립해야 한다. 사람이 감당하는 것은 코드의 양이 아니라 판정의 양이다. | §4.1, §4.2 |

P6과 P2가 함께 작동하는 방식에 주의할 것. 기계가 넓게 잡아 노이즈가 생기고(P6), 에이전트가 그것을 좁히지만, 좁힌 결과는 추론 파티션에 남는다(P2). 에이전트가 "이건 거짓 양성"이라고 판단해도 그 판단이 사실층의 엣지를 지우지 않는다. 판단이 틀렸을 때 복구 가능해야 하기 때문이다.

P11과 P6도 함께 읽어야 한다. 빌드 없는 분석은 빌드 있는 분석보다 덜 정밀하다. 그러나 P6에 따라 기계가 맡은 것은 정밀도가 아니라 완전성이고, **빌드 없는 분석의 약점은 정밀도이지 완전성이 아니다** — 넓게 잡는 것은 빌드 없이도 할 수 있다. 두 원리가 충돌하지 않고 맞물리는 지점이 여기다.

---

## 10. 이 백서가 주장하지 않는 것

정직성을 위해 명시한다. 아래는 palimpsest가 하지 못하거나 하지 않기로 한 것이다.

- **코드베이스의 완전 자동 이해를 주장하지 않는다.** 기계는 구조와 흐름을 제공할 뿐, 의미는 에이전트와 사람이 부여한다(§4).
- **책임의 이전을 주장하지 않는다.** 기계와 에이전트가 아무리 정확해져도 무엇을 받아들일지 결정하고 그 결과를 지는 자리는 사람에게 남는다. palimpsest가 낮추려는 것은 그 판정에 드는 비용이지 판정의 소재가 아니다(§4.1, P13).
- **검증자의 판정이 신뢰 가능하다고 주장하지 않는다.** 생성자와 검증자의 분리는 자기-인증 고리를 여는 것이지, 검증 결과를 보증하는 것이 아니다. 검증 산출도 하한이며 그렇게 표시된다(§4.4).
- **완전한 정적 호출 그래프를 주장하지 않는다.** 리플렉션·의존성 주입·동적 디스패치는 완전히 따라갈 수 없다. 기계의 답은 하한이며 그렇게 표시된다(§6.3).
- **모든 언어에 같은 깊이를 주장하지 않는다.** 능력은 언어·프레임워크마다 다르고, 그 차이는 감춰지지 않고 표시된다(§2.2-2).
- **빌드가 필요한 정밀도를 주장하지 않는다.** 대상 프로젝트를 빌드하지 않으므로, 타입 해소가 필요한 수준의 정밀도는 주경로에서 제공되지 않는다(§2.2-1, P11).
- **문서의 낡음을 없앤다고 주장하지 않는다.** 낡음은 감지될 뿐 방지되지 않는다(§3).
- **기록되지 않은 과거의 의도를 복원한다고 주장하지 않는다.** 이력에서 캐낸 의도는 추론이며, 사람의 승인 없이 의도층이 되지 않는다(§2.3).
- **에이전트의 판단을 사실로 승격하지 않는다.** 아무리 그럴듯해도 추론은 추론 파티션에 남는다(§4).
- **모든 서사적 결함을 잡는다고 주장하지 않는다.** §6.2의 술어로 환원되지 않는 결함이 있다. 환원되는 것만 기계가 판정하고, 나머지는 에이전트의 몫이다.
- **기존 도구의 대체를 주장하지 않는다.** 정밀 분석 엔진이 더 잘하는 영역이 있고, 그 산물은 출처 중립적으로 수용될 수 있다.

---

## 11. 현재 위치 — **이 절은 스냅샷이며 낡는다**

> ⚠️ **경고**: 이 절은 백서에서 유일하게 코드 사실을 언급하는 부분이며, 따라서 유일하게 낡는 부분이다. **여기의 내용과 코드가 다르면 코드가 맞다.** 이 절을 근거로 판단하지 말고 확인하라. 이 경고를 붙이는 것 자체가 §1~§3 논지의 시연이다.
>
> 작성 시점: 2026-08-01.

**이 저장소는 현재 이 백서 한 파일뿐이다.** 코드도 ADR도 없고, git 저장소도 아직 아니다. 즉 아래 서술은 **이 저장소의 상태가 아니라 선행 구현 저장소에서 관측된 것**이며, 그 자산의 승계 범위는 미결이다(§12-1).

선행 구현에서 사실층(§5)의 능력은 아래까지 서 있었다.

- **C1·C2 (구조·참조)**: 빌드 비의존 다언어 추출기 위에 구축됨(Java/Kotlin 계열 + ECMAScript 계열). 참조 정밀도는 이름 기반 과다 매칭에서 스코프 해소 쪽으로 개선 중 — 이것이 P6의 "넓게"와 거짓 엣지 사이의 실무적 경계선이다. 이름 기반 해소의 언어-로컬 경계와 "타입 정보 없는 언어에는 타입 엣지를 만들지 않는다"는 규칙이 결정으로 서 있다(`ADR-20260713-multilang-...`).
- **C3 (진입점)**: 웹 프레임워크의 라우팅·핸들러 구성이 1급 개념으로 들어옴.
- **C4 (경계 통과)**: 티어를 건너는 호출이 추론된 엣지로 표현됨. 추론이므로 P2에 따라 사실 파티션과 분리되어 있다. **저장소 경계는 미착수** — §2.2-3이 이것을 요구사항으로 올렸으므로 여기가 새로 벌어진 공백이다.
- **C5 (경로 위 지배관계)**: **가장 큰 공백.** 가드 개념은 온톨로지에 존재하나, "이 지점에 닿는 모든 경로가 저 가드를 통과하는가"를 답하려면 함수 내부의 제어흐름과 정의-사용 관계가 필요하고, 그 층이 아직 없다. §6의 판정 대부분이 이 공백에 막혀 있다.
- **C6 (시간축)**: 커밋 기반 신선도 + 전 이력 replay + 브랜치 스코프 노드 정체성으로 구축됨. **멀티레포 조합 좌표는 미착수.**

의도층(§7)은 결정·위험·요약의 적재 계약이 서 있고, 생성형 합성 경로가 격리된 형태로 회복되어 있었다(`ADR-20260706`).

**한편, §2가 새로 세운 요구는 어느 것도 실현된 적이 없다.** 선행 구현의 대상은 자기 자신과 단일 데모 코퍼스였고, 플러그인 형태·임의 사용자 프로젝트에의 설치·멀티레포·설치 비용은 모두 미검증이다. 그러므로 선행 구현의 결정들을 승계할 때는 **"단일 저장소·개발자 머신·고정 코퍼스"라는 전제 위에 서 있던 결정인지**를 각각 확인해야 한다 — 전제가 바뀐 결정은 자동으로 유효하지 않다.

능력 확장의 다음 병목은 여전히 C5다. §5의 "C5는 C2~C4 위에서만 성립한다"가 순서를 이미 강제했고, C2~C4가 (저장소 경계를 빼면) 서 있으므로 다음 차례가 됐다.

---

## 12. 열린 질문

백서가 결정하지 않는 것들. 각각은 별도의 조사나 결정을 필요로 한다.

1. **선행 자산의 승계 범위** — 선행 구현 저장소에 ADR 다수와 동작하는 구현이 있다. 그대로 상속할 것인가, 전제가 바뀐 것만 재작성할 것인가, 백서만 남기고 다시 세울 것인가. 판단 기준은 §11의 마지막 문단이다: 그 결정이 "단일 저장소·개발자 머신·고정 코퍼스" 전제 위에 서 있었다면 재론 대상이다. **이 질문이 열려 있는 동안 이 백서의 ADR 인용은 보증이 아니다**(문서 지위 블록 참조).
2. **C5의 조달 범위** — §2.2-1이 판단 공간을 이미 크게 좁혔다: 빌드 의존 엔진은 주경로가 될 수 없다. 인접 축인 정밀 참조(C2)도 같은 근거로 "palimpsest가 소유하는 빌드 비의존 추출"로 결정되어 있고, 빌드 의존 외부 엔진은 대체 불가한 니치(경계 넘는 오염 추적)를 위한 옵션 보조로만 유예되어 있다(`ADR-20260706` §결정6). 남은 질문은 **범위와 깊이**다 — 함수 내부 제어흐름·정의-사용 층을 어디까지 자체 소유할 것인가, 그리고 그 하한을 언어별로 어떻게 약속할 것인가.
3. **서사 규칙의 소유자** — §6.2의 술어 카탈로그를 코어가 고정할 것인가, 프로젝트별로 정의 가능하게 할 것인가. 고정은 단순하고, 확장 가능은 도메인 규칙을 담을 수 있다. 대상이 임의의 프로젝트라는 사실이 후자 쪽으로 무게를 옮긴다.
4. **가드·효과 어휘의 공급원** — "이 함수가 인가 검사다", "이 지점이 되돌릴 수 없는 외부 효과다"라는 라벨은 코드에서 완전히 자동 도출되지 않는다. 라벨을 사람이 승인해 고정하면 판정은 결정론을 유지하지만, 라벨 자체의 신선도 관리가 새 문제가 된다.
5. **게이트 오염(gate corruption)에 대한 자세** — 검증 장치를 검증 대상이 수정할 수 있으면, 게이트는 통과하기 쉬운 형태로 수렴한다. 실측된 형태는 assertion weakening이다 — 엄격한 동작 검사가 자명하게 만족되는 조건으로 바뀌고, 테스트는 통과하며, 결함은 남는다. 이것은 §4가 금지하는 세탁의 쌍둥이다: **세탁이 주장을 승격시킨다면 게이트 오염은 기준을 하강시키고, 결과는 같다** — 통과했다는 사실이 아무것도 보증하지 않게 된다.

   열린 질문은 이것이 palimpsest의 문제인가다. 사실층의 좌표는 협상 불가능하므로(어떤 심볼이 코드에 있는지 여부는 완화할 수 있는 어서션이 아니다) 사실층은 구조적으로 이 실패에 면역으로 보인다. 그러나 **의도층의 수용 기준, §12-4의 가드·효과 라벨, §12-3의 서사 규칙은 전부 사람이나 에이전트가 쓰는 것**이고, 따라서 완화될 수 있다. 그 층들에 대해 무엇을 보장할 것인가 — 아무것도 보장하지 않고 "판정의 근거만 제공한다"에 머무를 것인가, 아니면 **기준의 변경 이력 자체를 결박 대상**으로 삼을 것인가. 조사 근거: [`docs/research/2026-08-01-agentic-coding-verification-gap.md`](docs/research/2026-08-01-agentic-coding-verification-gap.md) §4.2.
6. **가치 링크의 표현** — §7의 "여정 → 가치"는 코드에 근거가 없다. 이 링크를 온톨로지에 어떻게 담으면서 P2(사실/추론 분리)를 지킬 것인가.
7. **멀티레포에서의 결박과 신선도** — 결박 좌표가 (저장소, 커밋) 조합이 될 때, 저장소들의 커밋이 서로 다른 시점일 때 "낡음"을 무엇으로 정의하는가. 한 저장소만 앞서 나간 상태는 낡은 것인가 정상인가.
8. **의도층 콜드스타트의 승격 절차** — 성숙 프로젝트에서 이력으로부터 발굴한 의도를 사람이 승인해 승격시키는 경로가 필요하다(§2.3). 그런데 승인 자체가 노동이면 아무도 하지 않고, 의도층은 영원히 비어 있게 된다. **승인 비용을 어디까지 낮출 수 있는가**가 이 시스템이 성숙 프로젝트에서 살아남는지를 가른다.
9. **저장소 기술의 설치 형태** — 그래프 저장소가 사용자 머신에서 어떤 형태로 서는가. git이 SoT이므로 그것은 교체 가능한 투영이지만(P5), "설치되는 물건"이라는 사실은 백서 차원의 제약이다(P12). 무엇을 요구하고 무엇을 동봉할 것인가.
10. **언어·프레임워크 확장 정책** — 새 언어를 언제, 어떤 깊이로 지원할 것인가. 그리고 지원하지 않는 언어가 섞인 프로젝트에 설치됐을 때 사용자에게 무엇을 약속하는가. 이것은 기능 로드맵이 아니라 **P4(정직한 공백)의 사용자 대면 형태**에 대한 질문이다.
11. **검증 산출의 하한 표시** — §4.4가 "검증자도 자기 커버리지를 모른다"를 받아들였으므로 검증 결과에도 P4가 걸린다. 그런데 §5의 능력 계약은 전부 *추출*에 대한 것이고 **판정에 대한 계약이 없다.** 검증자가 "무엇을 보지 않았는가"를 무엇에 기대어 말할 것인가 — 결박된 좌표 집합의 여집합으로 계산할 수 있는가, 아니면 검증자 자신의 신고에 머무르는가. 후자면 §4.3이 거부한 자기 증언이 검증 층에서 그대로 재현된다. 조사 근거: [`docs/research/2026-08-01-agentic-coding-verification-gap.md`](docs/research/2026-08-01-agentic-coding-verification-gap.md) §5.4~5.6.

---

## 부록: 용어

- **층 / 자리** — 백서는 두 축을 쓴다. **층**은 무엇이 기록되는가(§3: 사실층·의도층), **자리**는 누가 무엇을 지는가(§4: 사람·기계·에이전트)다. 두 축은 직교한다.
- **사실층** — 코드에서 결정론적으로 추출한 것. 낡지 않는다.
- **의도층** — 결정·근거·여정·가치. 코드에 없으며 사람이 공급한다. 낡을 수 있다.
- **결박(grounding)** — 의도층 항목이 사실층 좌표를 가리키는 것. 낡음 감지의 기반. 살아 있음·낡음·보류의 세 상태를 갖는다.
- **보류 결박(pending grounding)** — 결정은 있으나 그것을 실현한 코드가 아직 없어 좌표가 존재하지 않는 상태. 낡음과 구별된다.
- **세탁(laundering)** — 추론이 사실로 표시되어 파티션을 넘는 것. 금지.
- **서사적 결함** — 각 부품은 옳으나 조합에서 틀린 결함. 단위 테스트가 구조적으로 볼 수 없다.
- **지배(dominance)** — 어떤 지점에 도달하는 모든 경로가 특정 지점을 반드시 거치는 관계.
- **하한(lower bound)** — 정적 분석이 찾은 집합이 실제 집합의 부분집합일 수밖에 없음. 완전성 주장의 정직한 형태. 대상이 임의의 프로젝트이므로 하한은 하나가 아니라 언어·프레임워크·저장소별로 존재한다.
- **거짓 안전(false-clean)** — 위험이 있는데 없다고 답하는 것. 보안 맥락 최악의 실패 양식.
- **균일성(uniformity)** — 능력이 빌드 없이 임의의 과거 커밋에 대해 같은 방식으로 성립하는 성질. 균일하지 않은 능력은 언제 유효한지 알 수 없다.
- **콜드스타트** — 설치 직후 한쪽 층이 비어 있는 상태. 신생 프로젝트는 사실층이, 성숙 프로젝트는 의도층이 비어 있다.
