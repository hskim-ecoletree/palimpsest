# 음성 대조 — 2026-08-31

각 대조는 `git clone --shared`로 만든 별도 복제본에서 `target/debug/xtask check --root
<복제본>`을 실행했다. 원본 워킹트리는 바꾸지 않았다.

| 심은 결함 | rc | 관측 |
|---|---:|---|
| 역사 레코드의 기계 `요약` 한 칸 변조 | 0 | 끝난 회차 빚은 이 이슈의 계약대로 실패 유예하되 `갈린 칸 1 · 빠진 행 0`을 발화했다. |
| 역사 레코드 한 행 삭제 | 1 | `원 반환문의 항이 23 … 레코드는 22 행이다 (**빠진 행 1**)`로 칸 갈림과 다른 진단을 냈다. |
| 감사한 `IR1-05`의 `닫은커밋`을 다른 유효 SHA로 변조 | 1 | ``IR1-05` … 그 커밋이 `.github/workflows/ci.yml`를 안 만졌다`고 실패했다. |
| 종료 보고에 표준 검산 줄을 다시 삽입 | 1 | `종료 보고가 §9 표준 검산 줄을 담는다 — 원장은 둘이다`라고 실패했다. |
| 형식 이전 종료 회차의 정확한 상태 표지를 변조 | 1 | `종료 보고가 없는 회차가 2이다: 2026-08-18-round-protocol · 2026-08-31-legacy-round-debt`라고 실패했다. |

첫 대조의 rc가 0인 것은 #99가 물려받은 명시적 계약이다. 끝난 회차의 손 전사 차이는
앞 회차 판정을 자동으로 다시 열지 않고 **매 실행 수로 드러낸다**. 행 수가 달라지는
둘째 대조는 회차 레코드 자체가 불완전하므로 실패한다. 정상본은 둘 다 0이다.
