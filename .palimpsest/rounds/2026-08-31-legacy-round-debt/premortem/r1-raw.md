### #98 감사 근거가 레코드에 남지 않아, 틀린 SHA도 초록이 된다
- 어떻게 실패하나: 사람이 41행을 제대로 검토했더라도 `닫은커밋`만 고치고 행별 판단 근거를 보존하지 않으면, 경로를 우연히 만진 무관한 커밋도 `check_finding_closure`를 통과한다. 이후에는 왜 그 SHA가 처분 커밋인지 재검산할 수 없다.
- 어디가 걸리나: `xtask/src/main.rs:4393` · `check_finding_closure`; `.palimpsest/rounds/2026-08-22-agent-laziness/findings.jsonl`; `.palimpsest/rounds/2026-08-23-agent-laziness-behavior/findings.jsonl`
- 획득: 조회 — `pal query symbol.resolve/callers/reaches check_finding_closure`로 좌표를 찾고, 구현이 요구 경로의 변경 여부만 보며 처분 의미나 감사 근거는 읽지 않음을 코드에서 확인했다.
- 모집단: 회차기록
- 유효성: 추정
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 레코드 41행 전체에 걸린다. SHA만 바꾼 뒤에는 원래 판단 근거를 복원하기 어려우므로 행별 감사 근거와 SHA의 대응을 같은 변경에 남겨야 되돌릴 수 있다.

### #98의 「증거 없음」은 현 스키마에서 정직하게 닫을 값이 없다
- 어떻게 실패하나: 근거가 없는 행을 `열림`으로 돌리면 종료된 회차의 열린 발견이라 검사가 실패하고, `닫힘`으로 유지하면 근거 없는 완료 주장이며, `기각`으로 바꾸면 당시 유효했던 발견 자체를 사후에 뒤집는다. 별도 판정 구조 없이 감사대기 선언만 없애면 셋 중 하나를 거짓으로 기록하게 된다.
- 어디가 걸리나: `.claude/skills/round/bin/record.py`의 스키마 3 `상태`·`닫은커밋`; `xtask/src/main.rs:4476-4517` · `check_finding_closure`
- 획득: 조회 — `record.py --schema`에서 상태가 `열림|닫힘`뿐이고, 종료 회차의 열림과 보이지 않는 SHA가 각각 실패하는 구현을 직접 확인했다.
- 모집단: 규약
- 유효성: 참
- 해악도: 금지역
- 대상: 계획자신
- 얼마나 아픈가: 근거 없는 행이 하나만 있어도 A축 선언 제거와 기존 의미 보존을 동시에 달성할 수 없다. 「감사 결과 불명」을 닫힘 주장과 분리하는 최소 구조 또는 명시적 정책 판정이 필요하다.

### #99 재추출의 순번 ID가 과거 X-ID와 판단·닫힘 상태를 잘못 결합한다
- 어떻게 실패하나: 현 추출기는 원문에서 읽힌 순서대로 `PM<n>-01`, `IR<n>-01`을 새로 만든다. 과거 레코드에는 `PM1-X01`, `IR2-X01` 같은 별도 ID가 있으므로 단순 재생성이나 순번 결합을 하면 원 발견의 `처분`·`상태`·`닫은커밋`이 다른 추출 행에 붙거나 사라진다.
- 어디가 걸리나: `.claude/skills/round/bin/extract.py:204-228` · `main`; `.palimpsest/rounds/2026-08-19-finding-records/findings.jsonl`; `.palimpsest/rounds/2026-08-20-rust-extractor/findings.jsonl`
- 획득: 조회 — 추출기의 순번 ID 생성과 과거 `*-X*` ID 실물을 대조했다. 현재 `cargo xtask check`도 갈린 칸 1,934·빠진 행 596을 보고한다.
- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 끝난 회차 전사 전반에 걸리고, 잘못 합친 뒤에는 어느 판단이 어느 원문 항의 것인지 복구하기 어렵다. ID가 아니라 원문 항의 안정적인 정체성과 판단 필드를 별도로 대조해야 한다.

### #99의 카운터 0이 올바른 이주를 뜻하지 않을 수 있다
- 어떻게 실패하나: 레코드를 현 추출기의 현재 출력에 맞춰 통째로 다시 쓰면 갈린 칸·빠진 행 카운터는 0이 된다. 그러나 옛 반환 형식에서 추출기가 놓친 표나 잘못 정규화한 좌표도 그 출력 자체가 정본이 되어, 실제 발견이 사라진 채 검사가 초록일 수 있다.
- 어디가 걸리나: `.claude/skills/round/bin/extract.py:87-173` · `표들`·`독립리뷰`·`사전부검`; `xtask/src/main.rs:3852-3910` · 기계 칸 대조
- 획득: 조회 — 검사가 커밋 레코드를 현 추출기 산출과만 대조하고, 별도의 과거 형식 오라클은 사용하지 않음을 확인했다.
- 모집단: 자기장치
- 유효성: 추정
- 해악도: 금지역
- 대상: 계획자신
- 얼마나 아픈가: 596개 행 차이의 일부라도 오독이면 역사적 모집단과 유효성 지표가 영구 변한다. 당시 형식별 대표 원문에 대한 독립 음성 대조가 없으면 되돌리기 비용이 크다.

### 실제 종료 증거가 있는 회차를 「버려진 회차」로 소급 개칭한다
- 어떻게 실패하나: `2026-08-18-round-protocol`은 `state.md`가 종료를 선언할 뿐 아니라 완수 조건, 효과 출력, CI 성공 회차와 커밋까지 남긴다. 종료 보고가 없다는 이유로 회차 전체를 버림·접힘으로 표시하면 “보고가 없었다”를 “일을 끝내지 않았다”로 바꿔 과거 사실과 충돌한다.
- 어디가 걸리나: `.palimpsest/rounds/2026-08-18-round-protocol/state.md:3-36`; `.palimpsest/rounds/2026-08-18-round-protocol/intent.md:18-95`; `xtask/src/main.rs:4882-4913` · C7
- 획득: 조회 — 회차 문서와 해당 디렉터리의 Git 이력을 직접 대조했다. `76f5560`은 회차 종료 커밋, `2f70104`의 CI 성공은 문서에 기록돼 있다.
- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 회차 하나지만 프로젝트의 회차 규약이 태어난 기록이다. 기존 `folded.md`를 쓰면 특히 “소유자가 안 하기로 결정했다”는 다른 의미까지 소급 생성하므로 신뢰 손상이 크다.

### 새 「버려짐」 표시가 종료 판정 소비자 일부에만 연결된다
- 어떻게 실패하나: `abandoned.md` 같은 최소 구조를 새로 만들고 C7만 알아보게 하면, 원장 대조는 끝난 것으로 보지만 회차 레코드 검사나 발견 닫힘 검사는 여전히 진행 중으로 보거나 그 반대가 된다. 현재 공용 `기록이_확정됐나`는 `report.md|folded.md`를 보지만 `check_ledger_pair`는 같은 조건을 별도로 다시 적는다.
- 어디가 걸리나: `xtask/src/main.rs:35-42` · `기록이_확정됐나`; `xtask/src/main.rs:3834-3865` · `check_round_records`; `xtask/src/main.rs:4437-4439` · `check_finding_closure`; `xtask/src/main.rs:4931-4971` · `check_ledger_pair`
- 획득: 조회 — 그래프의 callers는 비어 있었고 교차 파일 해소 능력 부재가 표시됐다. 이후 직접 읽어 종료 판별이 공용 함수와 별도 조건으로 중복돼 있음을 확인했다.
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 최소 세 검사와 판정문이 걸린다. 일부만 고치면 새 회차를 여는 순간 C7 또는 열린 발견 검사가 빨개질 수 있으나, 공통 상태 판별로 모으면 되돌릴 수 있다.

### 유예 목록 제거를 위해 공통 빈-모집단 가드를 느슨하게 만든다
- 어떻게 실패하나: 세 유예 절을 삭제하면 `선언_목록`과 `check_declared_lists`의 하드코딩된 호출이 즉시 실패한다. 이를 빨리 초록으로 만들려고 “목록이 비어도 허용”으로 공통 함수를 완화하면, 여전히 필요한 `형식 이전` 같은 닫힌 예외 목록도 조용히 비울 수 있어 기존 판정 의미가 약해진다.
- 어디가 걸리나: `docs/gates/README.md:45-100`; `xtask/src/main.rs:4090-4144` · `선언_목록`·`check_declared_lists`; `xtask/src/main.rs:4398`; `xtask/src/main.rs:4860-4862`
- 획득: 조회 — 세 소비자가 해당 절을 무조건 읽고, 공통 파서가 빈 항목이나 없는 하한을 실패시키며, 검사 23이 네 목록 이름을 하드코딩함을 확인했다.
- 모집단: 자기장치
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: `cargo xtask check`가 즉시 빨개져 탐지는 쉽다. 다만 공통 가드를 완화하면 회귀가 초록으로 숨으므로 부채 목록의 제거와 역사적 예외 목록의 비어 있음 금지를 구분해야 한다.

## 내가 기각한 것

| 제목 | 어떻게 실패하나 | 어디가 걸리나 | 획득 | 모집단 | 유효성 | 해악도 | 대상 | 얼마나 아픈가 |
|---|---|---|---|---|---|---|---|---|
| Windows에서 한글 원 반환문 재추출이 로케일 때문에 깨진다 | Windows 기본 인코딩으로 원문·서브프로세스 출력을 읽어 `UnicodeDecodeError`가 난다고 의심했다 | `.claude/skills/round/bin/extract.py:44-51,211`; `xtask/src/main.rs:4147` | 조회 — 추출기는 파일을 UTF-8로 열고 `record.py` 출력도 `encoding="utf-8"`로 고정하며, xtask의 Python 실행자도 `PYTHONUTF8=1`을 설정함을 확인했다 | 저장소 | 거짓 | 미관 | 계획대상 | 현 경로에는 이미 방어가 있어 이번 계획이 새로 만들 실패가 아니다 |

새 범주: 과거 판정의 정체성·증거 보존
