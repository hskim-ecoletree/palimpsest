## 합격선 축

| 조건 | 판정 | 잰 수 | 근거 |
|---|---|---:|---|
| A1 | 통과 | 레코드 1269행 | `cargo xtask check`: 끝난 회차 `갈린 칸 0 · 빠진 행 0`, 검사 23/23 통과. |
| A2 | 통과 | 역사 레코드 980행 · 원문 그룹 52개 | `python3 xtask/tests/test_round_extract.py`가 네 역사 회차의 모든 원문 그룹을 실제 CLI 재이주 모드로 실행해 당시 ID와 사람 판단 칸을 보존하고 역사 기계 칸을 현재 추출값으로 맞췄다. 모호한 결합과 행 수 불일치는 각각 비정상 종료한다. 생산 호출은 `.claude/skills/round/bin/extract.py:413-426`에 있다. |
| A3 | 통과 | 음성 대조 2건 | `red/negative-controls.md`: 기계 칸 변조는 `갈린 칸 1 · 빠진 행 0`, 행 삭제는 `빠진 행 1`을 따로 냈다. 정상본은 둘 다 0이다. |
| B1 | 통과 | 41행 | `audit/issue-98.md`가 41행의 실제 처분 SHA와 좌표를 전수 열거한다. `cargo xtask check`의 닫힘 검사도 전체 541행에서 발화 0, 보이지 않는 SHA 0을 냈다. |
| B2 | 통과 | 감사 대기 0개 | `cargo xtask check`: 일반 닫힘 발화 0 · 감사 대기 발화 0, 선언 목록의 `A 축 감사 대기` 0개. |
| B3 | 통과 | 음성 대조 1건 | 감사한 `IR1-05`의 SHA를 다른 유효 SHA로 바꾸자 요구 좌표인 `.github/workflows/ci.yml`을 만지지 않았다는 진단과 함께 실패했다. |
| C1 | 통과 | 종료 보고 2곳 | `2026-08-18-completion-condition/report.md`와 `2026-08-19-finding-records/report.md`에서 셋째 원장이던 표준 검산 줄만 제거됐다. 게이트 판정과 의도 조건은 유지됐다. |
| C2 | 통과 | 역사 회차 1개 | `2026-08-18-round-protocol`만 정확한 옛 종료 상태 문구로 종료를 인정한다. 다른 회차와 비슷한 문구는 거부하는 시험이 통과했고, `report.md`나 `folded.md`를 소급 생성하지 않았다. |
| C3 | 통과 | 음성 대조 2건 | 표준 검산 줄 복원은 `원장은 둘이다`, 옛 종료 표지 훼손은 `종료 보고가 없는 회차가 2`라는 서로 다른 진단으로 각각 실패했다. |
| C4 | 통과 | 유예 선언 3종 모두 0개 | `cargo xtask check`: 선언 목록과 원장 둘 대조가 빈 빚 목록을 받아들이며 23/23 통과했다. 날짜 하한이 없거나 `형식 이전` 목록까지 비우면 실패하는 단위시험도 통과했다. |
| D1 | 통과 | 국소 1 · xtask 42 · 전체 검사 23 · CI 7개 작업 | 추출 회귀, `cargo test -p xtask`, `cargo xtask check`, `cargo test --workspace --all-targets`가 성공했다. 마지막 pushed SHA `ac0cef8`의 CI run `33404287032`에서 ubuntu·macOS·windows, 설치 작업과 양방향 산출물 호환 작업이 모두 성공했다. 규모 벤치 1개는 선언대로 제외됐다. |
| D2 | 통과 | 효과 수치 6축 | 실제 `cargo xtask check`: 손 전사 0, 갈린 칸 0, 빠진 행 0, 감사 대기 0, 검산 줄 유예 0, 보고 없음 유예 0. 검사 23/23 통과. |
| D3 | 반증 | 결박 3개 · 원장 18행 · 게이트 0 · 보고 0 · 열린 이슈 3개 | 기존 결박 15개는 모두 보존됐고 신규 결박 3개가 추가됐다. R1 발견도 구조화 원장에 기록됐다. 그러나 전용 게이트와 종료 보고가 아직 없고 #98·#99·#100은 모두 열려 있다. |

## 미측정 목록

없음 — A1부터 D3까지 모두 판정했다.

## 의도 축

### 빠진 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | 전용 게이트와 종료 보고를 아직 만들지 않았고 이슈 세 건도 닫지 않았다. | 원의도 | 참 | 실패 | D3 | `.palimpsest/rounds/2026-08-31-legacy-round-debt/intent.md:49` | 회차 디렉터리에 `report.md`가 없고 대응 게이트도 없다. `gh issue view`에서 #98·#99·#100이 모두 `OPEN`이다. |

### 요구되지 않은 것

없음.

### 있는데 틀린 것

없음. R1의 두 금지역은 모두 닫혔다.

- 역사 병합은 실제 CLI 경로에서 호출되며 52개 원문 그룹을 왕복한다.
- 결박 원장은 기존 15개를 모두 보존하고 신규 3개만 더했다. 누락과 중복은 0이다.

## 이번 라운드의 새 발견

없음.

## 자기 산출에 대한 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | 상태 문서의 남은 일 첫 항목이 이미 완료된 R1 발견 원장 기록까지 남은 일로 묶어 썼다. | 회차기록 | 참 | 거짓신호 | D3 | `.palimpsest/rounds/2026-08-31-legacy-round-debt/state.md:27` | `ac0cef8`에서 R1 발견 9행을 원장에 기록했으므로 남은 것은 R2 기록과 종료 산출뿐이다. |

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효 | 해악도 | 좌표 | 왜 아니었나 |
|---|---|---|---|---|---|---|
| 1 | R1 수정 뒤에도 역사 병합이 죽은 경로라는 의심 | 자기장치 | 거짓 | 미관 | `.claude/skills/round/bin/extract.py:413` | 넷째 인자인 기존 원장을 받으면 426행에서 `역사병합`을 호출한다. 네 역사 회차 980행의 실제 CLI 왕복도 통과했다. |
| 2 | 결박 복구가 기존 원장의 일부를 여전히 잃었다는 의심 | 저장소 | 거짓 | 미관 | `.palimpsest/intent/bindings.jsonl:1` | 착수본의 binding ID 15개가 현재 원장에 전부 있고 신규 3개만 추가됐다. 중복은 없다. |
| 3 | CI의 Node.js 20 폐기 예정 경고가 이번 구현 실패라는 의심 | 저장소 | 거짓 | 미관 | `.github/workflows/ci.yml` | GitHub가 해당 액션을 Node.js 24로 강제 실행했고 모든 작업이 성공했다. 이번 세 이슈의 조건이나 구현 결함을 반증하지 않는다. |

## 끝내도 되는가

아직 안 된다. 구현·회귀 검사·세 플랫폼 CI는 끝났고 R1의 금지역도 닫혔다. 전용 게이트와 종료 보고를 작성하고 상태 문구를 바로잡은 뒤, 구조화 원장에 이번 리뷰를 기록하고 #98·#99·#100을 닫으면 종료할 수 있다.
