## 합격선 축

| 조건 | 판정 | 잰 수 | 근거 |
|---|---|---:|---|
| A1 | 통과 | 레코드 1260행 | `cargo xtask check`: 끝난 회차 `갈린 칸 0 · 빠진 행 0`. |
| A2 | 반증 | 역사 레코드 980행 · 생산 호출자 0 | 착수 SHA와 비교해 ID 집합은 보존됐고 사람 칸 변경은 감사된 `닫은커밋` 20칸뿐이다. 그러나 `역사병합`은 정의와 단위시험 외 호출자가 없으며, 실제 `extract.py` 실행은 `PM1-S01`을 `PM1-01`로 재번호화하고 사람 판단 칸을 출력하지 않는다. |
| A3 | 통과 | 음성 대조 2건 | 격리 복제본 실측: 기계 칸 변조는 `rc=0 · 갈린 칸 1 · 빠진 행 0`, 행 삭제는 `rc=1 · 빠진 행 1`. |
| B1 | 통과 | 41행 | `audit/issue-98.md`가 41행을 열거한다. 착수 대비 사람 칸 변경 20건은 모두 `닫은커밋`이며 감사표와 일치한다. |
| B2 | 통과 | 529행 | `cargo xtask check`: 닫힘 발화 0 · 감사 대기 발화 0, 선언 항목 0. |
| B3 | 통과 | 음성 대조 1건 | `IR1-05`의 SHA를 `e3a0556`으로 바꾸자 `그 커밋이 .github/workflows/ci.yml를 안 만졌다`로 `rc=1`. |
| C1 | 통과 | 보고 2곳 | 두 종료 보고의 표준 판정·검산 줄이 제거됐고 관련 게이트 판정표와 의도 상자는 유지됐다. |
| C2 | 통과 | 역사 회차 1개 | `2026-08-18-round-protocol`은 정확한 종료 상태 문구와 당시 CI 표로만 종료로 분류한다. `report.md`나 `folded.md`를 소급 생성하지 않았다. |
| C3 | 통과 | 음성 대조 2건 | 검산 줄 삽입은 `종료 보고가 §9 표준 검산 줄을 담는다`, 종료 표지 훼손은 `종료 보고가 없는 회차가 2`로 각각 `rc=1`. |
| C4 | 통과 | 유예 목록 3개 | 세 목록 모두 0개. 선언 목록 검사와 원장 둘 대조가 `23/23` 안에서 통과했다. |
| D1 | 반증 | 국소 1 · xtask 23 · workspace 1 · CI 0 | `python3 xtask/tests/test_round_extract.py`, `cargo xtask check`, 재실행한 `cargo test --workspace --all-targets`는 성공했다. 그러나 HEAD `e3a0556`은 원격보다 6커밋 앞서 있고 해당 SHA의 GitHub Actions 실행은 0개다. |
| D2 | 통과 | 효과 수치 4축 | 실제 `cargo xtask check`: 손 전사 0, 갈린 칸 0, 빠진 행 0, 감사 대기·두 종료 유예 모두 0, 검사 23/23. |
| D3 | 반증 | 게이트 0 · 보고 0 · 원장 8행 열림 · 이슈 3개 열림 | 전용 게이트와 종료 보고가 없고 #98·#99·#100은 모두 OPEN이다. 결박 생성 과정은 기존 결박 15개를 삭제했다. |

## 미측정 목록

없음 — A1~D3 전부 판정했다.

## 의도 축

### 빠진 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | 마지막 pushed SHA의 세 플랫폼 CI가 없다. | 원의도 | 참 | 실패 | D1 | `.palimpsest/rounds/2026-08-31-legacy-round-debt/intent.md:47` | `git branch -vv`는 원격보다 6커밋 앞, `gh run list --commit e3a0556…`는 `[]`. |
| 2 | 게이트·종료 보고·이슈 종료가 아직 없다. | 원의도 | 참 | 실패 | D3 | `.palimpsest/rounds/2026-08-31-legacy-round-debt/intent.md:49` | 회차 디렉터리에 `report.md` 없음, 대응 게이트 0개, `gh issue view`에서 #98·#99·#100 모두 OPEN. |

### 요구되지 않은 것

없음.

### 있는데 틀린 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | 역사 ID·사람 판단을 보존한다는 병합 함수가 실제 재추출 경로에 연결되지 않았다. | 자기장치 | 참 | 금지역 | A2 | `.claude/skills/round/bin/extract.py:297` | `rg '역사병합\\('`의 호출은 단위시험 1곳뿐이다. CLI는 `.extract.py:393-409`에서 새 순번 ID와 기계 칸만 출력한다. 실제 옛 원문 재추출도 `PM1-S01` 대신 `PM1-01`을 냈다. 금지역 출처는 정책 파일과 의도 등록 목록이 없어 리뷰어 기본 목록의 ‘측정이 죽은 가지’. |
| 2 | 새 결박을 추가하면서 기존 결박 원장을 교체해 기존 15개를 잃었다. | 저장소 | 참 | 금지역 | D3 | `.palimpsest/intent/bindings.jsonl:1` | HEAD 파일은 header+15 bindings였으나 워킹트리는 header+새 binding 5개뿐이다. `git diff`에서 기존 15행 전부 삭제로 확인했다. 금지역 출처는 기본 목록의 ‘데이터 손실’. |

## 이번 라운드의 새 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | A2 보존 병합 장치가 생산 경로에서 죽은 가지다. | 자기장치 | 참 | 금지역 | A2 | `.claude/skills/round/bin/extract.py:297` | 정의·시험 외 호출자 0. 실제 CLI 재추출은 역사 ID와 사람 판단 칸을 보존하지 않는다. 자기장치지만 금지역 예외로 본 목록에 둔다. |
| 2 | 결박 작업이 기존 결박 15개를 삭제했다. | 저장소 | 참 | 금지역 | D3 | `.palimpsest/intent/bindings.jsonl:1` | `git diff`와 줄 수 대조: 기존 16줄에서 현재 6줄, 기존 binding 15개 전부 소실. |

## 자기 산출에 대한 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | 상태 문서가 이미 끝난 #99 재추출·#98 감사·#100 처분을 계속 “남은 것”으로 적는다. | 회차기록 | 참 | 거짓신호 | D3 | `.palimpsest/rounds/2026-08-31-legacy-round-debt/state.md:24` | 26~28행은 현 산출과 `cargo xtask check`의 0 수치에 반한다. |

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 아니었나 |
|---|---|---|---|---|---|---|
| 1 | `2026-08-18-round-protocol`의 종료 보고를 사후에 지어냈다는 의심 | 회차기록 | 거짓 | 미관 | `.palimpsest/rounds/2026-08-18-round-protocol/state.md:3` | `report.md`·`folded.md`는 없고, 보존된 정확한 종료 표지와 CI 기록만 소비한다. 표지를 훼손한 음성 대조도 실패했다. |
| 2 | workspace all-targets가 본질적으로 실패한다는 의심 | 저장소 | 거짓 | 미관 | `crates/pal-cli/tests/version_is_in_the_binary.rs:40` | 첫 실행 중 HEAD가 바뀌어 바이너리 SHA가 잠시 낡았다. HEAD 고정 후 전체 명령을 다시 실행해 전부 성공했다. |

## 끝내도 되는가

안 된다 — A2의 죽은 생산 경로와 결박 15개 데이터 손실이라는 금지역이 남고, D1·D3도 반증이다.
