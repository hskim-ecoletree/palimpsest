# 교대 상태 — legacy-round-debt

## 지금 단계

**단계**: 종료. 완수 조건 전부 닫힘 · 효과 관측 · CI 초록.

#98의 41행 감사, #99의 역사 재이주, #100의 두 종료 유예 처분과 음성 대조를 마쳤다.
두 차례 독립 리뷰의 발견을 모두 닫았고 전용 게이트·종료 보고·결박·구조화 원장이 선다.
구현 SHA `ac0cef8`의 CI run `33404287032`에서 ubuntu·macOS·windows와 양방향 산출물
호환 작업이 모두 성공했다.

## 착수 관측

- `cargo xtask check`는 23/23 통과하지만 끝난 회차 손 전사 2530칸
  (`갈린 칸 1934`·`빠진 행 596`)을 보고한다.
- 닫힘 검사는 일반 발화 0과 별도로 `감사 대기 발화 41`을 보고한다.
- 원장 둘 대조는 `검산 줄 유예 발화 2`와 `보고 없음 유예 발화 1`을 보고한다.
- 그래프는 `check_finding_closure`를 `xtask/src/main.rs:4393-4615`에서 찾았으나 호출자,
  cross-file resolution, effects, judgment는 답하지 못했다.

## 실패를 미리 막는 결정

- 재추출에서 역사적 식별자와 사람 판단 칸을 보존한다.
- #98 감사 근거를 경로 접촉만으로 대신하지 않는다.
- 2026-08-18 회차의 종료 증거를 먼저 감사하고 소급 종료 보고는 쓰지 않는다.
- 유예 목록 삭제만으로 얻은 초록은 음성 대조를 통과하지 못한다.

## 남지 않은 것

회차에 등록한 구현·감사·리뷰·효과·결박·검증·CI·이슈 처분을 모두 닫았다.

## 실패한 접근

- 처음 만든 역사 병합 함수가 실제 CLI 경로에 연결되지 않았다. 기존 findings를 넷째
  인자로 받는 재이주 모드를 연결하고 네 역사 회차의 모든 원문 그룹으로 검증했다.
- 기본 intent 저장소를 곧바로 export해 기존 결박 15개를 잃었다. 착수 원장에 새 결박
  세 개만 합친 임시 저장소에서 다시 export해 header+18개를 복구했다.
