I'll start by reading the plan and orienting in the repo.
Let me set up an isolated copy and start measuring the concrete claims.
원 저장소는 안 건드렸다(`git status` 깨끗 · HEAD=c573593). 격리 사본 `/tmp/premortem-tsv-1787106301/` 에서 실제 `xtask`/`pal` 바이너리를 세워 돌렸다.

---

### 안 B 의 「`xtask` 확장자 목록에 `tsv` 를 더한다」는 **아무것도 안 한다**
- 어떻게 실패하나: 안 B 항목 3 의 전반부를 그대로 수행 → 두 검사 모두 대상 파일을 **못 본다.** 검사 19 는 `.palimpsest/` 를 **디렉터리로** 빼므로 확장자를 더해도 모집단이 안 바뀐다. 죽은 링크 검사는 파일은 넣지만 마크다운 링크 문법만 뽑으므로 `근거인용` 열(산문)에서 링크 0 건이다. 회차는 「#71 이 지목한 사각을 검사 모집단에 넣었다」고 적게 되는데, 사실이 아니다.
- 어디가 걸리나: `xtask/src/main.rs:2444` (`모을_문서` 확장자) · `xtask/src/main.rs:2872` (`인용_모집단` 의 `const 밖: &[&str] = &["docs/", ".palimpsest/", "target/", ".git/", "corpus/"]`)
- 근거: **조회** — 격리 사본에서 두 곳을 실제로 패치하고 `./target/debug/xtask check` 를 돌렸다.
  - 기준선: `죽은 링크 부재 — 문서 181개 · 링크 388건` / `검사 19 — 파일 194개 · 인용 490곳`
  - `tsv` 추가 후: `문서 182개 · 링크 **388건**(불변)` / `파일 **194개**(불변)`
  - `findings.tsv` 를 심은 뒤: `문서 183개 · 링크 388건` — 여전히 링크 0 증가
- 해악도: **금지역** (「측정이 죽은 가지가 됨」 + 「사실이 아닌 것을 사실로 적음」 — 회차 자신의 완수 조건 문장이 거짓이 된다)
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(한 줄). 걸리는 곳 2. **단 안 B 는 이 한 줄이 항목 3 의 절반이므로, 실제로 필요한 것은 「새 검사」뿐이고 그것은 안 A 와 같다** — 두 안의 차이가 여기서 하나 줄어든다.

---

### `.tsv` 는 「갈래」가 아니다 — 이 저장소에 8 종의 서로 다른 스키마가 있다
- 어떻게 실패하나: 안 B 항목 1 이 *"`retro/02-classification.tsv` 와 **같은 갈래**의 TSV"* 를 전제하는데, 그런 갈래가 없다. 확장자로 모집단을 잡는 순간 `corpus/` 14 개(소유자가 **범위 밖 · 측정 장치**로 못박은 것)가 함께 들어오고, 그중 둘은 **헤더 행 자체가 없다**(`#` 로 시작하는 산문 로그). 그래서 검사는 **첫날부터 손으로 적은 예외 목록**을 달아야 하고, 그것은 이 저장소가 이미 이름 붙인 병이다 — `xtask/src/main.rs:2919` *"손으로 적은 예외 목록은 그때 조용히 샌다"*.
- 어디가 걸리나: `corpus/tasks/*.tsv`(12) · `corpus/golden/*.tsv`(2) · `.palimpsest/rounds/2026-08-18-completion-condition/retro/02-classification.tsv`(1)
- 근거: **조회** — 15 파일 전수의 헤더 열 수를 셌다: `1 · 3 · 5 · 5 · 5 · 7 · 7 · 9 · 9 · 9 · 11 · 11 · 11 · 11 · 1`. `f02-recall-sample.tsv`·`s0-reference-vector.tsv` 는 첫 줄이 `# …` 로 헤더가 없다. `근거` 라는 열 이름이 corpus 4 개 파일에 또 다른 뜻으로 존재한다.
- 해악도: 실패(검사가 즉시 빨개진다) → 예외 목록을 달면 **거짓신호**
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 15 파일이 걸린다. **안B만.**

---

### `02-classification.tsv` 는 계획 A 의 필드를 「대부분」 안 들고 있다 — 겹치는 것은 8 중 2
- 어떻게 실패하나: 안 B 의 근거(그리고 라운드 1 의 S17)가 *"계획 A 의 축을 대부분 들고 있다"* 인데, 열별로 실제 값을 세면 그렇지 않다. 이 전제 위에서 안 B 를 고르면 「이미 있는 것을 잇는다」고 믿고 실제로는 **새 스키마를 새로 세우게 된다** — 안 A 와 같은 일을, 형식만 나쁜 쪽으로.

  | 계획 A 필드 | TSV 에서 | 실측 |
  |---|---|---|
  | `모집단`/`출처`(독립리뷰/사전부검/인터뷰) | **없다** | 11 열 중 0 |
  | `유효성` | `축1_유효` | **참 109 · 거짓 0** — 분산 0. ⑧ 이 재려는 바로 그 축이 죽어 있다 |
  | `해악도` | `축6_깨짐` | ✅ enum 동일 (거짓신호 73 · 금지역 27 · 실패 6 · 미관 3) |
  | `처분` | `축3_산출`(장치/산문/삭제/없음) | 뜻이 다르다 — 「무엇을 낳았나」지 「메인이 어떻게 처분했나」가 아니다 |
  | `조건` | **없다** | |
  | `근거`(경로:줄) | `근거인용`(산문 인용) | **109 행 중 `경로:줄` 형태는 1 행**. 10 행은 `…` 로 **잘려 있다** |
  | `라운드` | `round` | ✅ |
  | `id` | `id` | ✅ (형식 다름: `R9-07`) |
- 어디가 걸리나: `.palimpsest/rounds/2026-08-18-completion-condition/retro/02-classification.tsv:1` (헤더) · `.palimpsest/rounds/2026-08-19-finding-records/premortem/r1-disposal.md:19` (S17 의 전제) · `plan-v2.md:40`
- 근거: **조회** — 열 1~11 을 `cut -f<n> | sort | uniq -c` 전수. `근거인용` 열에 `grep -cE '[…]+\.(rs|md|py|toml):[0-9]+'` = **1**, `grep -c '…'` = **10**.
- 해악도: **금지역** (계획의 선택 근거가 사실이 아닌 것 위에 선다)
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(전제 문장 하나). **이 한 건이 물음의 무게중심을 옮긴다.**

---

### 그 TSV 는 **한 커밋에 한 번 쓰인 회고 산출물**이지 append 되는 로그가 아니다
- 어떻게 실패하나: 안 B 는 「기존 TSV **갈래를 잇는다**」인데, 이을 갈래가 없다. 이 파일은 `559e0b8` 한 커밋에서 태어났고 **그 뒤 한 번도 안 만져졌다.** 45 커밋 전수를 사람(에이전트)이 읽어 여섯 축에 배정한 **1 회성 하이브리드 분류**이고, 회고 자신이 *"사람이 손으로 확정하는 순간 「손으로 베낀 거울」이 된다"* 고 적었다(`retro/axes.md:47`). 즉 **라운드마다 붙이는 운용 이력이 0 이다** — 안 B 는 검증된 관행을 잇는 것이 아니라 새 관행을 TSV 로 시작하는 것이다.
- 어디가 걸리나: `git log --follow -- …/02-classification.tsv` → 커밋 **1** 개
- 근거: **조회** — `git log --diff-filter=A` 로 최초 추가 커밋 확인, `--follow` 로 총 커밋 수 1 확인, 커밋 본문 전량 확인
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. **안B만.**

---

### 하나의 TSV 검사가 `02-classification.tsv` 위에서 첫날부터 빨개진다 — 그리고 그것을 피하면 #71 이 안 닫힌다
- 어떻게 실패하나: 안 B 항목 3 의 「열·enum·합계를 재는 검사」를 `.palimpsest/**/*.tsv` 에 걸면 모집단 2 개 중 **1 개가 RED**(11 열 vs 계약 8 열, 헤더 이름 전부 불일치). 고치는 길은 둘뿐이다 — ⓐ 110 행 소급 이전(**계획이 명시적으로 범위 밖**: `plan.md:76`), ⓑ 그 파일을 검사에서 제외. ⓑ 를 고르면 **#71 이 지목한 병이 그대로 남는다** — *"`retro/02-classification.tsv` 가 어느 검사의 모집단에도 안 들어"*(`plan-v2.md:11`). 회차는 #71 을 닫았다고 적고 지목된 파일은 여전히 사각에 있다.
- 어디가 걸리나: `plan.md:76`(범위 밖) ↔ `plan-v2.md:11`(#71 의 병) ↔ 안 B 항목 3
- 근거: **조회** — 격리 사본에 8 열 `findings.tsv` 를 심고 계약 대조를 실행: `RED: 1 / 2`
- 해악도: **금지역** (닫혔다고 적히지만 안 닫힌다)
- 대상: 계획대상
- 얼마나 아픈가: 되돌리기 비싸다 — ⓐ 는 범위 위반, ⓑ 는 원 의도 미달. **안B만이고, 안 B 의 가장 큰 구멍이다.**

---

### 안 A 는 `.jsonl` 선례를 근거로 들면서 그 선례의 규율(`schema_version`)을 안 지킨다
- 어떻게 실패하나: `plan.md:30-33` 이 형식 결정의 1 번 근거로 `crates/pal-cli/tests/intent_jsonl.rs` 를 든다. 그런데 그 파일이 CI 로 지키는 계약은 **머리 줄에 `schema_version` 이 있다**는 것이다(`intent_jsonl.rs:36`). 계획 A 의 필드 목록(`id·라운드·출처·모집단·유효성·해악도·처분·조건·근거·요약`)에는 버전이 없고, *"한 줄이 발견 하나"* 는 머리 줄과 충돌한다(쓰는 자가 「머리를 이미 썼는가」를 알아야 한다). 스키마가 다음 회차에 한 필드라도 바뀌면 **구 행과 신 행을 가를 표시가 없고**, 검사는 어느 쪽을 계약으로 볼지 모른다.
- 어디가 걸리나: `crates/pal-cli/tests/intent_jsonl.rs:4,36` · `plan.md:30-33` · `plan.md` 의 A 필드 목록
- 근거: **조회** — 해당 시험 전문을 읽고 `schema_version` assert 를 확인. 계획 필드 목록에 버전 없음 확인.
- 해악도: 거짓신호 → 두 번째 회차에서 실패
- 대상: 계획자신
- 얼마나 아픈가: 지금 고치면 한 줄. 나중이면 소급 이전. **안A만** (안 B 는 헤더 행이 있고 버전은 아예 없다 — 같은 병의 더 나쁜 형태다. 실질은 **둘다**)

---

### `findings.json` 은 RED · `findings.jsonl` 은 초록 — 한 글자가 MVP 트리거를 가른다
- 어떻게 실패하나: 격리 사본에 같은 내용을 두 이름으로 심었다. `.json` 은 sunset 검사 2 건을 즉시 터뜨리고, `.jsonl` 은 통과한다. 그런데 `docs/sunset.toml` 의 주석이 *"기계가 쓰는 것 = `.json` (**`pal` 이 쓰는** 회차 레코드)"* · *"`.palimpsest/rounds/*/*.json` 이 하나라도 생기면 트리거가 참이다"* 라고 적는다. **`plan.md:39-40` 은 이 어긋남을 알고 완화책을 명시했다** — *"그 사실을 `docs/sunset.toml` 에 적고 #68 에 남긴다 — **안 적으면 그것이 우회다**"*. **그런데 `plan-v2.md:21` 의 「확정된 것」이 *"sunset 트리거는 안 만진다. #68 에 관측만 남긴다"* 로 그 완화책을 지웠다.** v2 가 운용 계획이면 회차는 자기 계획이 「우회」라고 이름 붙인 것을 그대로 낸다.
- 어디가 걸리나: `xtask/src/main.rs:2713-2721`(`아래_전부` 가 `p.extension() == Some("json")` 로 비교) · `docs/sunset.toml:20-27` · `plan.md:39-40` ↔ `plan-v2.md:21`
- 근거: **조회** — 실제 바이너리 실행.
  - `findings.jsonl` → `ok sunset 선언 — 트리거 … 는 아직 0건`
  - `cp findings.jsonl findings.json` → `FAIL sunset 선언` · *"`docs/research` 를 지울 때가 됐다 — 트리거가 참이다"* 외 1 건
- 해악도: **금지역** (「측정이 죽은 가지가 됨」 — 검사가 *"그날이 오면 여기가 빨개진다"* 를 출력하는 동안 그날이 지나간다)
- 대상: 계획자신 (계획 문서 둘이 서로를 지운 자리)
- 얼마나 아픈가: 되돌릴 수 있다. **안A만** — 안 B 의 `.tsv` 는 「기계가 쓰는 것」이라 부를 근거가 약하고 쓰는 자도 없어 이 자리에 덜 닿는다.

---

### 안 B 의 「설치 표면에 안 나간다」는 비용이 계획이 적는 것보다 훨씬 작다 — 실측 델타는 `record.py` 하나
- 어떻게 실패하나: `plan-v2.md:44` 가 안 B 의 대가로 *"설치본은 이 장치를 못 받는다"* 를 적는다. 실측하면 **회차 규약 관련 payload 4 개 중 3 개가 `.md` 이고, 전부 이 저장소의 살아 있는 파일을 `include_str!` 로 그대로 싣는다** — 즉 이 회차가 SKILL.md·에이전트 정의 둘을 고치면 **안 A 든 안 B 든 그 변경이 설치본에 똑같이 나간다.** 그리고 `xtask` 검사는 **어느 안에서도 안 나간다**(PAYLOAD 에 없다). 그러므로 두 안의 설치 표면 차이는 **`record.py` 파일 하나**뿐이고, 안 B 는 *쓰는 자가 없는 설계*라 그 하나조차 필요 없다. 계획이 A 쪽에 실제보다 큰 이점을 계상하고 있다.
- 어디가 걸리나: `crates/pal-cli/src/install/layout.rs:68`(SKILL.md) · `:72`·`:76`(에이전트 둘) · `:83`(dashboard.py) — 넷 다 `include_str!("../../../../.claude/…")`
- 근거: **조회** — `pal install` 을 빈 저장소에서 실제로 돌렸다. 놓인 것 10 개 전량 확인, 그중 회차 규약 관련은 위 넷.
- 해악도: 거짓신호 (물음의 저울이 기울어 있다)
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. **둘다** — 물음 자체에 걸린다.

---

### 설치본의 `record.py` 는 0644 라 안 돌고, **그 사실을 아무 시험도 안 잡는다** (`dashboard.py` 가 이미 그 상태다)
- 어떻게 실패하나: 계획이 든 「걸리는 것」의 첫 줄은 맞다. 그런데 더 나쁜 것이 있다 — `dashboard.py` 는 **`layout.rs` 세 줄 말고는 저장소 어디에서도 안 불린다.** 시험 0 · CI 잡 0. 즉 설치본에 실린 스크립트가 **한 번도 실행된 적 없이** 「완료」로 잡혀 있다. `record.py` 는 **읽는 자가 아니라 쓰는 자**라 이 상태가 질적으로 다르다: 안 돌면 레코드가 안 쓰이고, 설치본에는 `xtask` 가 없으므로 **비었다는 것을 말해 줄 하한도 없다.** 회차는 「설치 표면 완료」를 적게 되고 실행 증거는 0 이다.
- 어디가 걸리나: `crates/pal-cli/src/install/layout.rs:83,84,150` — `dashboard` 문자열이 저장소 전체에서 이 3 곳뿐 · `.claude/skills/round/SKILL.md:96-98`(직접 실행 형태)
- 근거: **조회** — ① `pal install` 후 `ls -l` → `-rw-r--r-- dashboard.py`, 직접 실행 → `permission denied`. ② `grep -rn dashboard crates/ xtask/src .github/` → 3 히트, 전부 layout.rs. ③ 격리 사본에서 `record.py` 를 PAYLOAD+OWNED_FILES 에 실제로 더하고 `cargo test -p pal-cli` → **전량 초록(설치 시험 50/50 포함), 0 실패.** 장치가 「없다」를 못 잡는다.
- 해악도: **금지역** (측정이 죽은 가지 — 그리고 초록이 그것을 덮는다)
- 대상: 계획대상
- 얼마나 아픈가: 모드는 되돌릴 수 있으나 Windows 는 모드로 안 풀린다(`ADR-0023`). **안A만.**

---

### `처분` 어휘가 셋으로 갈렸고, 회차 자신의 첫 사용이 이미 규약의 넷을 어겼다
- 어떻게 실패하나: `SKILL.md:44` 가 처분을 **넷**으로 못박는다(계획 수정 · 탐지 수단 추가 · 완수 조건으로 전환 · 수용 가능한 이유 명시). 그런데 이 회차의 라운드 1 처분표가 실제로 쓴 값은 **다섯**이고 그중 `축소`·`완수 조건 정정` 은 넷에 없다. 계획 A 의 `처분` enum 은 **여섯**(정정/확대/축소/전환/범위밖/기각)이고 규약의 넷 중 어느 것과도 문자열이 안 맞는다. 그리고 분포가 **12/16 = 75% 가 「계획 수정」** — 첫 데이터셋부터 `축1_유효 = 참 109/거짓 0` 과 같은 **분산 붕괴**다. 라운드 1 의 처방(「대응표를 적는다」)은 ADR-0012 의 *"짝이 없으면 적지 않는다 · 꺼진 대조는 `–` 가 아니라 실패다"* 에 걸린다 — 여섯 중 넷은 대응 상대가 없다.
- 어디가 걸리나: `.claude/skills/round/SKILL.md:44` · `.palimpsest/rounds/2026-08-19-finding-records/premortem/r1-disposal.md`(처분 열) · `docs/adr/0012-a-single-truth-file-declares-only-what-has-a-counterpart-in-code.md`
- 근거: **조회** — r1-disposal.md 의 처분 열 값 전수: `계획 수정 12 · 탐지 수단 추가 1 · 축소 1 · 완수 조건으로 전환 1 · 완수 조건 정정 1`. SKILL.md:44 원문 대조.
- 해악도: 거짓신호 → ⑦⑧ 이 붙으면 **금지역**(분산 0 축을 「쟀다」고 출력)
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. **둘다** — 형식과 무관하다.

---

### 라운드 1 이 조건으로 건 「에이전트 원 반환문 보존」은 **선례가 0 회다**
- 어떻게 실패하나: `plan-v2.md:49-51` 이 합계 검산의 항등식 문제를 *"에이전트의 원 반환문을 파일로 보존하고 레코드 행 수와 대는 것이 조건"* 으로 닫는다. 그런데 저장소 전체에서 `## 내가 기각한 것` 을 담은 **보존된 에이전트 반환문이 0 건**이다 — 히트 5 개는 전부 (a) 에이전트 **정의** 파일 둘, (b) 계획·처분표가 그 절을 *언급*한 것이다. 이 회차 라운드 1 조차 보존된 것은 메인의 처분표(`r1-disposal.md`)지 사전부검의 원 반환문이 아니다. 즉 **가장 강한 교정책이 한 번도 수행된 적 없는 절차에 걸려 있고**, 그 절차를 안 하면 검산은 다시 자기가 쓴 것을 자기가 세는 항등식으로 돌아간다 — 조용히.
- 어디가 걸리나: `.claude/agents/pal-premortem-sweeper.md:93` · `.claude/agents/pal-independent-reviewer.md:154` (정의) · `.palimpsest/rounds/2026-08-19-finding-records/premortem/` (파일 1 개, 처분표)
- 근거: **조회** — `grep -rl "내가 기각한 것" .palimpsest/` → 2 파일(둘 다 계획/처분표) · `.claude/` → 2 파일(둘 다 정의). 보존된 반환문 0.
- 해악도: **금지역** (검산이 항등식으로 되돌아가는데 초록이다)
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(보존을 완수 조건으로 등록). **둘다.**

---

### TSV append 는 값 안의 탭·개행에서 **합계가 틀린다** — #71 이 요구한 바로 그 수에서
- 어떻게 실패하나: 에이전트가 반환문에서 값을 복사할 때 탭이 하나 섞이면 열이 밀려 **해악도 열에 「거짓」이 들어가고 「금지역」 합계가 줄어든다.** 개행이 섞이면 레코드 3 개가 **행 4 개**가 되어 총계가 부풀려진다. 기존 회고의 세는 법(`03-counts.md:3` — `awk -F'\t' 'NR>1{n[$c]++}'`)이 그대로 이 함정을 밟는다. JSON 은 두 문자를 형식이 이스케이프하므로 애초에 안 생긴다.
- 어디가 걸리나: `.palimpsest/rounds/2026-08-18-completion-condition/retro/03-counts.md:3` (세는 법) · 안 B 항목 2(*"쓰는 자를 안 만든다 — 에이전트·메인이 그대로 적는다"*)
- 근거: **조회** — `/tmp/pm-tsv-append-*/` 에 심어서 실행. 탭 삽입 → `금지역 1 · 거짓 1 · (빈값) 2`, 개행 삽입 → 레코드 3 → 행 4.
  ⚠ **정직하게 적는다: 엄격한 열 수(NF) 검사는 둘 다 잡는다**(NF=6, NF=3·3). 그러므로 「조용히」가 아니라 **CI 가 커밋 뒤에 빨개지고 사람이 손으로 이스케이프해 고친다**. 쓰는 자가 없다는 것이 이 비용을 매 라운드 반복시킨다.
- 해악도: 실패 (검사가 있으면) / **금지역** (검사가 열 수를 안 재면 합계가 조용히 틀린다)
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. **안B만.**

---

### 설치본에는 `.palimpsest/` 가 없어 ⑦⑧ 은 태어날 때부터 「못 셌다」이고, 그것을 볼 사람이 없다
- 어떻게 실패하나: `DIRS` 는 `.claude/*` 만 만든다. 갓 설치한 프로젝트에서 `dashboard.py` 를 돌리면 ①~⑥ 은 git 으로 답하지만 ⑦⑧ 은 읽을 파일이 없다. 그 자체는 규약대로(「0 이 아니라 못 셌다」) 정상이다. 문제는 **그 상태가 정상인지 고장인지 가릴 장치가 없다는 것** — 위에서 실측한 대로 `dashboard.py` 를 실행하는 시험·CI 잡이 0 이므로, 파일 경로가 틀렸든 파서가 깨졌든 설치본에서는 영영 「못 셌다」로 보이고 아무도 구별 못 한다. `layout.rs:80-82` 가 이미 같은 형태를 *"태어나면서 죽은 가지"* 로 이름 붙였다.
- 어디가 걸리나: `crates/pal-cli/src/install/layout.rs:151-163`(`DIRS`) · `.claude/skills/round/bin/dashboard.py:135`(「못 셌다」 관행)
- 근거: **조회** — 빈 저장소에 `pal install` 후 `ls -d .palimpsest` → `No such file or directory`. 설치된 `dashboard.py` 실행 → ①만 출력, 나머지 침묵.
- 해악도: **금지역** (측정이 죽은 가지)
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(음성 대조 시험 하나). **둘다.**

---

## 내가 기각한 것

- **「새 레코드 파일이 `.palimpsest/` 라서 계기판 ① 자기 비율을 기계적으로 부풀린다」** — 그럴듯했다. `dashboard.py:18` 의 `자기_접두` 에 `.palimpsest/` 가 있고, 회고가 ① 의 기준선 수열(`17·25·36·36·75·7·45·50·20·29·33·100·67·100·41`)을 등록해 뒀으니 오염이면 크게 걸린다. **실측: 최근 40 커밋 중 38 이 이미 `.palimpsest/` 를 만진다.** 레코드 파일 하나가 더해도 경로 비율이 몇 %p 움직일 뿐이고 「안 만진 라운드」는 40 중 2 뿐이다. **기각.**
- **「`record.py` 를 `PAYLOAD` 에 더하면 매니페스트 상한·설치 시험이 걸린다」** — 격리 사본에서 `PAYLOAD` + `OWNED_FILES` 에 실제로 더하고 `cargo test -p pal-cli` 전량 실행: **모두 초록**(설치 시험 50/50 포함, 0 실패). 상한 관련 시험은 이미 수를 **세도록** 고쳐져 있다(`install_hostile.rs:573` `지금_항목_수`). **기각.** ★ 다만 이 기각이 위의 「아무 시험도 안 잡는다」 시나리오의 근거가 됐다 — 아무것도 안 깨진다는 것은 아무것도 안 재고 있다는 뜻이기도 하다.
- **「`.tsv` 를 죽은 링크 검사에 넣으면 새 파일들이 들어와 빨개진다」** — 넣어 봤다. 문서 181 → 182, **링크 388 불변, 죽은 것 0**. 빨개지는 것이 없다. **기각** (그리고 그 무해함 자체가 「아무것도 안 한다」의 증거다).
- **「`findings.jsonl` 이 검사 19 모집단에 들어와 docstring 이 걸린다」** — `.palimpsest/` 가 검사 19 에서 **디렉터리로** 빠지므로 파일 수 194 불변. `record.py` 는 `.claude/` 라 들어오지만 그것은 이 계획이 만들 코드의 문제지 형식 선택의 차이가 아니다. **양안 무차별로 기각.**

---

## 어느 쪽이 덜 깨지는가 — 실측 위의 내 판단

**안 A 가 덜 깨진다.** 다만 계획이 적은 이유 때문이 아니다.

두 안의 차이라고 계획이 세운 것 중 **실측으로 살아남은 것이 거의 없다**:

| 계획이 적은 차이 | 실측 |
|---|---|
| 안 A 는 설치 3 목록·매니페스트·시험이 걸린다 | **0 실패.** 상한 시험은 이미 세도록 고쳐졌고 `DIRS` 에 `bin` 이 이미 있다 |
| 안 B 는 설치 표면에 안 나간다 | **거의 나간다.** SKILL.md·에이전트 둘이 `include_str!` 로 실린다. `xtask` 는 **어느 안에서도** 안 나간다. 실질 델타는 `record.py` 하나 |
| 안 B 는 확장자 한 줄로 검사에 든다 | **한 줄이 아무것도 안 한다.** 검사 19 는 디렉터리로 빼고, 죽은 링크는 링크가 0 이다. 안 B 도 **새 검사가 필요하고 그것은 안 A 와 같다** |
| 안 B 는 기존 갈래를 잇는다 | **잇을 갈래가 없다.** `.tsv` 는 이 저장소에서 8 종 스키마의 잡동사니고, 대상 파일은 커밋 1 회짜리 1 회성 회고 산출이다 |
| 안 B 는 이미 계획 A 의 축을 대부분 든다 | **8 중 2.** `모집단`·`처분`·`조건` 은 없고, `유효성` 은 있으나 **분산 0**, `근거` 는 이름만 같고 109 중 1 행만 `경로:줄` |

즉 **안 B 의 「싸다」가 대부분 환상**이고, 그것을 걷어내면 안 B 에는 **안 A 에 없는 고유 손실 둘**이 남는다:

1. **#71 이 지목한 파일을 못 닫는다.** 새 계약을 걸면 그 파일이 RED 이고, 피하려면 제외해야 하며, 소급 이전은 범위 밖이다. 「#71 을 닫았다」가 거짓이 된다.
2. **탭·개행 이스케이프 부담이 쓰는 자 없이 매 라운드 사람에게 남는다**, 그리고 `corpus/` 14 파일을 막을 손 예외 목록이 첫날부터 필요하다.

안 A 의 고유 손실은 **셋**인데 전부 **한 줄짜리 계획 수정으로 닫힌다**:

1. `.jsonl` 이 sunset 트리거를 비껴간다 — `plan.md:39-40` 에 이미 완화책이 있었고 **`plan-v2.md:21` 이 그것을 지웠다. 되살리면 닫힌다.**
2. `record.py` 가 0644 — 호출 형태를 `python3 <경로>` 로 적으면 닫힌다(그리고 그 수정은 **`dashboard.py` 의 기존 결함도 함께 닫는다** — 안 B 는 이 자리를 안 지나서 결함이 그대로 남는다).
3. `schema_version` 이 없다 — 선례가 이미 형태를 준다.

★ **가장 무거운 것은 어느 안에도 안 걸린다.** 「보존된 원 반환문 0 건」·「`dashboard.py` 를 실행하는 시험 0 개」·「설치본에 `.palimpsest/` 가 없다」·「처분 어휘 셋」은 **형식과 무관하다.** 두 안 중 무엇을 골라도 이 넷을 안 닫으면 회차는 초록으로 끝나고 장치는 죽은 가지가 된다. 물음이 A/B 에 걸려 있다는 계획의 전제 자체가, 실측하면 **가장 큰 위험의 위치를 가리키지 않는다.**

새 범주: **계획 개정이 앞 판의 완화책을 소리 없이 지우는 자리** (plan.md → plan-v2.md 의 「확정된 것」이 plan.md 가 명시적으로 「안 적으면 우회다」라고 건 조건을 삭제했다)
