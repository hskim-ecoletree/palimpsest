I'll start by reading the three plan versions and the surrounding context.
Now let me examine the actual repo surfaces the plan touches.
격리 사본 `/tmp/palim-premortem-1787107642/` 에서 심어 확인했다. 원 저장소는 안 건드렸다.

---

### v2 의 「당기는가」 측정 조건이 v3 에서 사라졌다 — 앞 라운드가 이름 붙인 그 형태가 또 있다
- 어떻게 실패하나: `plan-v2.md:64` 는 *"「당기는가」는 **소유자가 출력을 실제로 본 뒤에만** 잰다 — 안 보이면 「아니오」가 반증이 아니다"* 를 라운드 1 의 처분으로 걸었다. v3 는 그 문장을 안 옮겼다(`grep '본 뒤에만' plan-v3.md` → 0 건). v3:17-18 에 남은 것은 조작적 정의(`소유자가 설명을 요구했는가`)뿐이다. 이 회차는 **소유자가 계기판 ⑦⑧ 출력을 한 번도 안 볼 수도 있는** 구조다 — 계기판은 규약 §5 「검증 → 수정 착수」 자리에서 **에이전트가** 부르고(`SKILL.md:90-92`, `dashboard.py:5-6`), 소유자에게 보여지는 경로가 없다. 그러면 완수 조건 「당기는가」가 **「아니오」로 판정되고 그것이 계기판의 반증으로 기록된다** — 실제로는 안 보여 준 것이다.
- 어디가 걸리나: `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-08-19-finding-records/plan-v2.md:64` (사라진 조건) · `plan-v3.md:17-18` · `intent.md:14` (소유자가 잠근 완수 조건) · `.claude/skills/round/SKILL.md:90-92`
- 근거: **조회** — 세 판을 `grep '당기\|본 뒤에만\|반증이 아니'` 로 대조. v2 의 라운드 1 처분 12 항을 v3 에 1:1 로 맞춰 보니 **이 한 항만 대응이 없다**(나머지 11 은 v3:40·52·60·61-62·68·70-71·84·85·87 로 전부 살아 있다).
- 해악도: **금지역** — 「사실이 아닌 것을 사실로 적음」(`intent.md:50`). 안 보여 준 것을 「소유자가 설명을 안 요구했다」로 적으면 그것이 계기판의 판정이 된다.
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(한 줄 복원). 걸리는 곳은 게이트 `## 판정` 한 곳이지만 그 한 줄이 이 회차의 핵심 완수 조건이다.

---

### 합계 검산의 「원 반환문 항 수」는 기계로 셀 수 없고, 셀 수 있는 규칙으로 세면 이 회차의 유일한 데이터셋에서 어긋난다
- 어떻게 실패하나: v3:61-62 는 *"보존된 **원 반환문**의 항 수와 레코드 행 수"* 를 독립 원천으로 걸었다. 실제로 세어 봤다 — `^### ` 로 세면 r1=**18**, r2=**13**. 그런데 v3:44-45·85 는 `## 내가 기각한 것` 도 레코드 행으로 요구한다(r1 **5 불릿**, r2 **4 불릿**). 그러면 레코드 행은 23·17 이고 「항 수」는 18·13 이다 — **검산이 첫날부터 어긋난다.** 게다가 r2 에는 `## 어느 쪽이 덜 깨지는가` 라는 **표 5 행짜리 반증 절**이 더 있어(r2-raw.md:163) 어느 규칙으로도 안 세어진다. 두 파일의 절 구성이 **이미 서로 다르다**(r1: `##` 1 개 / r2: `##` 2 개).
- 어디가 걸리나: `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-08-19-finding-records/premortem/r1-raw.md` (### 18 · 기각 불릿 5) · `r2-raw.md` (### 13 · 기각 불릿 4 · `##` 절 2) · `plan-v3.md:61-62`
- 근거: **조회** — 격리 사본에서 카운터를 짜서 돌렸다(`/tmp/palim-premortem-1787107642/count.py`). 출력: `r1-raw.md | ###항: 18 | ##절: 1 | 기각불릿: 5` / `r2-raw.md | ###항: 13 | ##절: 2 | 기각불릿: 4`.
- 해악도: **실패** — 새 검사가 자기 회차의 데이터에서 빨개진다. 피하려고 규칙을 느슨하게 하면 **금지역**(측정이 죽은 가지)로 넘어간다.
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. ★ 추가로 **보존된 원 반환문이 사전부검 r1·r2 뿐이다** — v3:91 이 그렇게 적었다. 사전부검 **r3**(지금 이 반환), **독립 리뷰 5 라운드**, **인터뷰 4 라운드** 의 원 반환문은 보존 자리가 계획에 없다. `출처` enum 이 넷(독립리뷰/사전부검/인터뷰/실측)인데 **둘째 원천은 하나에만 있다.**

---

### 이 회차가 계획 F 를 적용하는 그 커밋이 자기가 쓴 `좌표` 를 낡게 만든다
- 어떻게 실패하나: v3:63 은 *"`좌표` 의 `경로:줄` 이 **실재하는지** 해소한다"* 를 새 검사에 건다. 그런데 이 회차가 담을 발견들의 좌표는 **이 회차가 고칠 파일들을 가리킨다.** 격리 사본에서 계획 F 를 실제로 적용했다(기각 절 → 표 · `모집단`·`유효성` 두 줄 추가 · `근거`→`획득`): `## 내가 기각한 것` 이 **93 → 95** 로 밀렸다. r2 의 레코드 좌표 `pal-premortem-sweeper.md:93` 은 이제 **빈 줄**을 가리킨다. 줄이 존재하기는 하므로 「줄 번호 ≤ 파일 줄 수」로 해소하면 **초록인 채 틀린 자리를 가리키고**(죽은 가지), 내용까지 대면 **그 커밋에서 빨개진다.** 같은 자리가 최소 넷 더 있다 — `pal-independent-reviewer.md:154`(계획 F 가 고친다) · `SKILL.md:44`·`:132`·`:355`(계획 B·H 가 고친다) · `dashboard.py:18`·`:38`·`:47`·`:135`(계획 D 가 고친다).
- 어디가 걸리나: `plan-v3.md:63` · `/Users/incognito/dev/projects/palimpsest/.claude/agents/pal-premortem-sweeper.md:93` · `pal-independent-reviewer.md:154` · `premortem/r2-raw.md`(좌표 인용부)
- 근거: **조회** — 격리 사본에 계획 F 를 심어 돌렸다. 심기 전 `grep -n '^## 내가 기각한 것'` → 93, 심은 뒤 → 95, `sed -n '93p'` → 빈 줄. 추가로 r1·r2 의 좌표 **37 개**를 전부 해소해 봤다: 지금은 37/37 이 살아 있고, **13 개가 경로 없는 파일명**(`layout.rs:161`·`dashboard.py:18`·`plan.md:76`…)이다. `git ls-files` 로 재니 `plan.md`·`main.rs`·`install.rs` 는 **후보가 둘**이라 파일명만으로는 해소가 모호하다.
- 해악도: **금지역** — 「측정이 죽은 가지가 됨」. 느슨하게 해소하면 검사가 초록인 채 아무것도 안 재고, 엄격하게 하면 append-only 로그가 **줄 번호 드리프트로 영구히 빨개진다.**
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 어렵다 — `.jsonl` 은 append-only 라 이미 쓴 좌표를 못 고친다. 걸리는 곳: 이 회차가 담을 레코드 전부.

---

### `라운드` 를 커밋 태그에서만 유도하면 대부분의 발견이 값을 못 받는다 — 그리고 기록 시점에 그 커밋이 아직 없다
- 어떻게 실패하나: v3:40 은 *"`라운드` 는 커밋 태그 `[R<n>]` 에서만 유도한다 — 원천을 둘로 안 만든다"* 다. 실측: 전체 **406 커밋 중 `[R<n>]` 은 13 개**(3.2%)이고, `(round)` 접두 커밋 **47 개가 태그 없이** 있다 — 착수 커밋(`0af61cb 회차 D 착수`), 실측 커밋(`5e83493 CI 가 잡았다`), 승격 커밋(`7fcb73f [승격]`)이 전부 무태그다. `출처` enum 에 **`인터뷰`·`실측`** 이 있는데 그 둘이 나오는 커밋에는 태그가 원리상 안 붙는다. ★ 더 결정적인 것: `record.py` 는 **커밋 전에** 줄을 붙인다. 지금 r2·r3 의 발견을 적으려 하면 HEAD 는 `44611af [R2]` 이고 R3 의 `[R3]` 커밋은 **아직 존재하지 않는다** — 유도하면 **한 칸 어긋난 라운드 번호**가 append-only 로 박힌다.
- 어디가 걸리나: `plan-v3.md:40` · `/Users/incognito/dev/projects/palimpsest/.claude/skills/round/bin/record.py`(만들 것)
- 근거: **조회** — `git log --format='%s' | wc -l` → 406, `| grep -cE '\[R[0-9]+\]'` → 13, `(round)` 커밋 중 무태그 → 47. 무태그 목록 12 건을 눈으로 확인했다.
- 해악도: **금지역** — 「사실이 아닌 것을 사실로 적음」. 되돌릴 수 없는 로그에 틀린 라운드가 들어간다. 그리고 계기판 ⑤ 라운드 칸과 대는 순간 두 수가 어긋난다.
- 대상: 계획자신
- 얼마나 아픈가: append-only 라 되돌리기 어렵다. 걸리는 곳: `record.py` 의 유도 로직 · 새 검사의 `라운드` 검증 · 계기판 ⑤.

---

### 이 회차가 `SKILL.md` 안의 손으로 베낀 거울 둘을 낡게 만드는데 계획 H 는 그 둘을 안 적는다
- 어떻게 실패하나: `SKILL.md:105` 가 *"**여섯 칸**이 나온다 — 자기 비율 · 미판정 잔액 · 진자 · 연쇄 깊이 · 라운드 · 승격"* 으로 계기판 칸 집합을 손으로 베껴 놓았다. 계획 D 가 ⑦⑧ 을 더하면 **여덟**이 된다. `SKILL.md:423` 이 *"**검사 19 개**는 파일을 읽어 판정할 뿐이라 세 OS 에서 같은 답을 낸다"* 로 검사 수를 베껴 놓았다. 계획 C 가 검사를 더하면 **20** 이 된다(`xtask/src/main.rs:537-557` 은 19 개이고 `total = checks.len()` 로 **세지만**, SKILL.md 는 벤 값이다). 계획 H 는 SKILL.md 에서 **CI 판별식 한 줄과 `dashboard.py` 호출 두 줄**만 고친다고 적었다 — 423 줄은 **판별식 바로 위 두 줄**인데도 안 든다.
- 어디가 걸리나: `/Users/incognito/dev/projects/palimpsest/.claude/skills/round/SKILL.md:105` · `:423` · `plan-v3.md:94-98` · `xtask/src/main.rs:537,558`
- 근거: **조회** — `grep -n '검사 19\|여섯 칸' SKILL.md`, `sed -n '537,558p' xtask/src/main.rs`(19 항 · `total = checks.len()`).
- 해악도: **거짓신호** — 아무것도 안 깨진다. 그러나 `SKILL.md` 는 `layout.rs:69` 로 **설치본에 통째로 실려 나가고**, 설치본 사용자는 *"여섯 칸이 나온다"* 를 읽고 여덟을 본다. 그리고 그 거울이 갈리는 것은 이 규약 자신이 `SKILL.md:419` 에서 *"검사의 모집단은 검사가 안다. 베끼지 말고 돌려라"* 로 진단한 바로 그 병이다.
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 두 줄.

---

### `^\.claude/` 는 빌드 입력 넷 중 하나만 닫는다 — 나머지 셋은 그대로 「안 기다린다」
- 어떻게 실패하나: v3:95-97 은 판별식에 `^\.claude/` 를 더한다. 실제로 `include_str!` 리터럴을 전부 뽑아 보니 `crates/`·`xtask/`·`Cargo.*`·`.github/` **밖의 빌드 입력이 넷**이다: `.claude/**`(4 파일) · `schema/graph.toml` · `surface/queries.toml` · `corpus/tasks/f03-normalize-seeds.ts`. 판별식을 고친 뒤에도 뒤 셋은 **"안 기다린다"** 를 받는다. `corpus/tasks/f03-normalize-seeds.ts` 는 `crates/pal-extract/tests/normalize_props.rs:26` 이 컴파일 타임에 삼키는 **시험 입력**이고, `cargo xtask check` 19 개 중 어느 것도 그 파일을 안 본다 — 즉 ① 이 초록이어도 CI 가 답할 것이 남는다. 판별식이 자기 문장(*"① 이 초록이면 CI 가 더 답할 것이 없다"*)에서 거짓이 된다.
- 어디가 걸리나: `/Users/incognito/dev/projects/palimpsest/.claude/skills/round/SKILL.md:427-429` · `crates/pal-extract/tests/normalize_props.rs:26` · `plan-v3.md:95-97`
- 근거: **조회** — `grep -rn 'include_str!' crates/ xtask/` 로 리터럴 15 개를 뽑아 정규화했고, 판별식 정규식을 8 개 경로에 실제로 돌려 표를 냈다(`schema/graph.toml` → 지금 「안 기다린다」 · H 적용 후에도 「안 기다린다」).
- 해악도: **거짓신호** — 종료 판정은 결국 「마지막 커밋 SHA 에 `conclusion=success`」로 재므로(`SKILL.md:431`) 못 닫히지는 않는다. 그러나 판별식이 **또 손으로 벤 거울**이 되고, 이 회차가 그것을 「고쳤다」고 적는다.
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 한 정규식.

---

### enum 을 「한 파일」에 두는데 계획 E 는 설치 델타를 `record.py` 하나라고 적는다 — 설치본 `record.py` 는 그 파일을 못 찾는다
- 어떻게 실패하나: v3:52 는 *"enum 을 **한 파일**에 두고 `record.py` 와 검사가 둘 다 읽는다"* 이고, v3:74 는 *"`layout.rs` 의 세 목록에 `record.py` 를 함께 더한다. **실질 델타는 이 파일 하나다**"* 다. 그 「한 파일」이 어디 사는지 계획에 없다. `record.py` 옆에 별 파일로 두면 **그 파일도 `PAYLOAD`·`OWNED_FILES`·`DIRS` 에 들어가야 한다** — `layout.rs` 는 이름으로 적는 명시 목록이고(`layout.rs:14-18` *"여기 적힌 이름 목록은 놓을 것"*), `Resource { path, body }` 에 와일드카드가 없다. 안 넣으면 설치본의 `record.py` 는 import/read 에서 죽고, 그것을 잡는 것은 계획 E 가 **새로 만들** 시험뿐이다.
- 어디가 걸리나: `plan-v3.md:52` vs `plan-v3.md:74` · `/Users/incognito/dev/projects/palimpsest/crates/pal-cli/src/install/layout.rs:20-24`(`Resource`) · `:26-89`(`PAYLOAD`) · `:146`
- 근거: **조회**(계획 두 줄 대조 · `layout.rs` 구조 확인) + **추정**(설치본에서 실제로 import 실패시켜 보지는 못했다 — `cargo build -p pal-cli` 를 격리 사본에서 안 돌렸다)
- 해악도: **실패** — 설치 왕복 시험이나 새 「실제로 도는지」 시험이 잡는다. 다만 **enum 을 `record.py` 안에 넣고 `xtask` 가 파이썬 소스를 정규식으로 긁는** 회피로 가면 그것은 「두 곳에 안 적는다」를 지킨 척하는 셋째 원천이 된다.
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳 3(설치 목록 셋) + 1(계획 문장).

---

### `python3 <경로>` 로 고쳐야 할 자리가 다섯인데 계획은 「두 줄」이라 적었다 — 그리고 사용자가 실제로 보는 줄이 빠졌다
- 어떻게 실패하나: v3:51·98 은 *"SKILL.md 의 `dashboard.py` 호출 **두 줄**도 함께 고친다"* 다. 실측으로 호출 형태를 적는 자리는 **다섯**이다: `SKILL.md:96`·`:98`(계획이 든 둘) · `dashboard.py:14`(docstring `사용: dashboard.py <착수커밋> …`) · `dashboard.py:84`(**범위가 비었을 때 사용자에게 출력하는 오류 메시지** — `⚠ 범위가 비었다 — 인자를 확인하라: dashboard.py <착수커밋> …`) · `NEXT-D-handoff.md:48`(추적됨). ★ `dashboard.py:14` 와 `:84` 는 `layout.rs:84` 의 `include_str!` 로 **설치본에 그대로 실려 나간다** — 0644 라 안 도는 형태를, 사람이 막힌 순간에 그대로 처방한다.
- 어디가 걸리나: `/Users/incognito/dev/projects/palimpsest/.claude/skills/round/bin/dashboard.py:14`·`:84` · `.claude/skills/round/SKILL.md:96`·`:98` · `/Users/incognito/dev/projects/palimpsest/NEXT-D-handoff.md:48`
- 근거: **조회** — `grep -rn 'dashboard.py' --include='*.md' --include='*.py'` 로 다섯 자리 확인, `git ls-files NEXT-D-handoff.md` 로 추적 확인.
- 해악도: **거짓신호** — 아무 검사도 안 깨진다(호출 형태를 재는 시험이 0 이다). 사람만 막힌다. 그리고 `record.py` 가 같은 docstring 관행을 따르면 **여섯째 자리**가 태어난다.
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 다섯 줄.

---

### `python3` 호출은 ADR-0023 을 근거로 들면서 Windows 에서 안 재고 정한다 — ADR 이 금지한 그 방식이다
- 어떻게 실패하나: v3:50-51 은 *"호출 형태는 `python3 <경로>` — … Windows 는 모드로도 안 풀린다(ADR-0023)"* 로 ADR 을 근거로 든다. 그런데 ADR-0023 의 결정은 *"플랫폼이 무언가를 못 볼 때 고를 축은 「양쪽이 할 수 있는 것」"* 이고, 맥락 절이 명시적으로 *"**적어 둔 주석은 아무것도 안 막는다**"* · *"실제로 그 플랫폼에서 돌려 보니 표시가 있던 자리 셋 중 둘이 뚫려 있었다"* 다. `python3` 라는 **이름**이 Windows PATH 에 서는지는 이 계획에서 안 쟀다 — CI 매트릭스에 `windows-latest` 가 있고(`ci.yml:75`, `:157`, `:206`) 워크플로에 **python 설정 스텝이 하나도 없다**(`grep -n python .github/workflows/*.yml` → 0 건). 계획 E 의 새 시험이 `python3` 를 spawn 하면 Windows 잡에서 어떻게 되는지가 **미지수인 채로** CI 에 들어간다. 그리고 저장소에는 이 물음을 답하는 자리가 이미 있다 — `install/exe.rs` 가 *"「이 자리가 실행될 수 있는가」… 분기는 **여기 한 번**이어야 한다 — 두 곳에서 각자 답하면 한쪽이 조용히 낡는다"* 로 선언했는데, 계획은 그 자리를 안 쓰고 **문서 문자열**로 답한다.
- 어디가 걸리나: `/Users/incognito/dev/projects/palimpsest/docs/adr/0023-consistent-method-and-result-across-platforms.md:18-25`·`:45-50` · `crates/pal-cli/src/install/exe.rs:1-15` · `.github/workflows/ci.yml:75,157,206` · `plan-v3.md:50-51,77`
- 근거: **조회**(ADR 본문 · `exe.rs` 모듈 문서 · CI 매트릭스 · python 스텝 부재) + **추정**(Windows 러너에서 `python3` 가 실제로 어떻게 도는지는 **못 물었다** — 이 PC 에 Windows 가 없다. 이것이 정확히 ADR 이 경고한 「안 재고 적는 자리」다)
- 해악도: **실패**(Windows 잡이 빨개지는 경우) 또는 **금지역**(시험이 `python3` 부재를 skip 으로 삼키면 그 시험은 세 OS 중 둘에서만 도는 **죽은 가지**가 되고, ADR-0023 의 「같은 방법·같은 결과」가 그 자리에서 깨진다)
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳: 새 시험 하나 + 호출 형태 다섯 자리.

---

### 「기각 절이 유일하게 구조가 없다」가 리뷰어에게는 거짓 — 구조 없는 절이 넷 더 있고 계획은 그것들을 안 든다
- 어떻게 실패하나: v3:85 는 *"`## 내가 기각한 것` 도 같은 표로 낸다. **지금은 유일하게 구조가 없는 절**이라 필수 필드를 못 채운다"* 다. `pal-independent-reviewer.md` 의 반환 형식을 실제로 세어 보면 구조 없는 절이 넷 더 있다: `### 빠진 것` · `### 요구되지 않은 것` · `### 있는데 틀린 것`(`:141-144`, 형식 지정 없음) · `## 미측정 목록`(`:138`). 이 넷은 전부 **발견을 담는 절**이고, `## 이번 라운드의 새 발견` 표(`:147`)에 안 들어간다. 그러면 시나리오 2 의 「원 반환문 항 수 ↔ 레코드 행 수」 검산이 독립 리뷰 반환에서는 **원리상 못 선다**. 그리고 `## 합격선 축` 표(`:135`)는 `통과·반증·대조 불가·미측정` 이라는 **다섯째 어휘**를 들고 있는데 v3 의 `처분` 여덟에도 `사전처분` 넷에도 대응이 없다.
- 어디가 걸리나: `/Users/incognito/dev/projects/palimpsest/.claude/agents/pal-independent-reviewer.md:134-144` · `:146-147` · `plan-v3.md:82-87`
- 근거: **조회** — `grep -n '^## ' pal-independent-reviewer.md | tail -8` 로 반환 형식 절 일곱을 세고 각 절의 형식 지정 유무를 읽었다.
- 해악도: **거짓신호** — 계획 문서가 사실이 아닌 것을 사실로 적었고(「유일하게」), 그 결과 **계획 F 의 범위가 조용히 좁아진다.** 고쳐 놓고 「구조 없는 절을 다 닫았다」고 믿게 된다.
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳: 리뷰어 정의 5 절 + 검산 규칙.

---

### `근거` 를 둘로 갈랐는데 레코드 스키마에는 한쪽만 들어 있다 — 그리고 다른 한쪽은 `유효성` 과 같은 것을 잰다
- 어떻게 실패하나: v3:37-39 는 *"`좌표`(경로:줄 또는 커밋)와 `획득`(조회/추정)으로 **가른다**"* 이고 계획 F:87 은 에이전트의 `근거` 를 `획득` 으로 개명한다. 그런데 v3:35-36 의 **레코드 필드 목록에 `획득` 이 없다** — `id·라운드·출처·모집단·유효성·해악도·처분·조건·좌표·요약` 열이다. 가른 둘 중 하나만 착지한다. 에이전트가 내는 `획득` 은 전사 단계에서 **버려지거나** `유효성` 에 흡수된다. 그리고 흡수하면 중복이다: `획득 ∈ {조회, 추정}`, `유효성 ∈ {참, 추정, 거짓}` 이고 두 어휘 모두 `추정` 을 같은 뜻으로 쓴다(`pal-premortem-sweeper.md:84`, `pal-independent-reviewer.md:63-67`) — `획득=추정 ⟺ 유효성=추정` 이라 **한 비트가 두 칸에 손으로 적히고 반드시 갈린다.** 사전부검의 제1 원칙(*"추정을 조회인 척하지 마라"*)이 걸린 축이 레코드에 자리가 없다.
- 어디가 걸리나: `plan-v3.md:35-39` vs `plan-v3.md:87` · `/Users/incognito/dev/projects/palimpsest/.claude/agents/pal-premortem-sweeper.md:84` · `.claude/agents/pal-independent-reviewer.md:63-67`
- 근거: **조회** — 계획 v3 의 필드 목록과 F 절을 대조하고, 두 에이전트의 두 어휘 정의표를 읽었다.
- 해악도: **금지역** — 「측정이 죽은 가지」. 개명만 하고 열을 안 만들면 개명이 아무것도 안 낳는다. 열을 만들면 두 칸이 같은 사실을 말하고 첫 데이터셋에서 어긋난다. ★ 이것은 「만들 필요가 없는 것을 만들고 있는가」에 해당한다 — **`유효성` 하나로 같은 답이 나온다.**
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 어렵다(append-only 스키마). 걸리는 곳: 스키마 · `record.py` · 새 검사 · 에이전트 둘.

---

### v2 의 「`처분` enum 과 규약 §5 의 대응표를 적는다」가 v3 에서 사라졌고, 그 결과 여덟이 자기들끼리 겹친다
- 어떻게 실패하나: `plan-v2.md:60` 은 라운드 1 의 처분으로 *"`처분` enum 과 규약 §5 의 **대응표를 적는다**"* 를 걸었다. v3 에 `대응표` 는 없다(`grep '대응표' plan-v3.md` → 0 건). 대신 v3:41 이 여덟을 **선언**한다. 대응표를 실제로 그려 보면 겹침이 드러난다 — `SKILL.md:141-145`: **축소** = *"등록된 퇴로면 그대로 쓴다. **아니면 승격**"*, **전환** = *"**승격**"*. 즉 `전환` 은 100% `승격` 이고 `축소` 는 부분집합이다. 한 칸에 한 값을 적으면 **둘 중 하나가 사라진다.** `막힘` 도 겹친다 — `SKILL.md:335-340` 의 §11 ② 표는 `금지역 → 막힘`, `실패 → 막힘` 으로 **`해악도` 에서 유도한다.** v3 는 `해악도` 를 별도 필드로 두면서 `막힘` 을 `처분` 값으로도 두어, v3:40 이 스스로 세운 *"원천을 둘로 안 만든다"* 를 어긴다. 그리고 §5:147 의 **`완화`**(*"재는 의도의 양이 줄었으면 완화다 … 이 자가 없으면 「테스트를 약하게 만들어 GREEN 을 얻는」 길이 정정으로 위장한다"*)는 여덟에 **없다** — 위장한 정정을 가리는 그 축이 레코드에서 사라진다.
- 어디가 걸리나: `plan-v2.md:60`(사라진 조건) · `plan-v3.md:41-43` · `/Users/incognito/dev/projects/palimpsest/.claude/skills/round/SKILL.md:141-145`·`:147`·`:335-340`
- 근거: **조회** — 세 판 `grep '대응표'` 대조 + 규약 §5·§11 본문을 읽고 대응표를 직접 그렸다.
- 해악도: **금지역** — 「측정이 죽은 가지」 + 「사실이 아닌 것을 사실로」. `완화` 가 `정정` 으로 기록되면 계기판 ⑧ 발견 유효성이 **약해진 검사를 개선으로 센다.**
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 어렵다(enum 을 늘리면 이미 쓴 행의 뜻이 바뀐다). 걸리는 곳: 스키마 · `record.py` · 새 검사 · 계기판 ⑧.

---

## 내가 기각한 것

- **「새 검사 모집단이 `corpus/` 의 tsv 12 개를 끌어온다」** — 계획 v3:55 의 모집단은 `.palimpsest/rounds/**` 로 한정된다. 실측: `find` 로 저장소 전체의 tsv/jsonl 을 뽑으니 tsv 15 개 중 **14 개가 `corpus/` 아래**이고 `.palimpsest/rounds/**` 에는 **`02-classification.tsv` 하나뿐**이다. 글롭 밖이다. **기각.**
- **「이 회차가 이미 만든 `premortem/*.md`·`red/*.txt` 가 모집단에 들어와 스키마 선언을 요구받는다」** — 확장자가 `.md`·`.txt` 라 `*.jsonl` + `*.tsv` 글롭에 안 걸린다. `.palimpsest/rounds/**` 아래 비-md 파일 20 개를 전부 세어 확인했다(txt 18 · tsv 1 · py 1). **기각.**
- **「`모집단이 비면 실패다` 하한이 이 저장소에서 즉시 발화한다」** — `02-classification.tsv` 가 이미 있어 모집단 ≥ 1 이다. **기각.**
- **「`.palimpsest/rounds/**/retro/detect-pendulum.py` 가 새 검사에 걸린다」** — `.py` 는 글롭 밖. **기각.**
- **「v3 가 `#68 에 관측만 남긴다` 를 안 지켰다」** — `gh issue view 68` 로 확인했다. 이 회차의 관측 코멘트가 **실제로 실려 있다**(*"관측 — 트리거가 나중에 죽은 가지가 된다 (회차 `2026-08-19-finding-records` 사전부검)"*, 격리 사본 실측표 포함). v3:104 의 *"관측은 실렸다"* 는 **참이다. 기각.**
- **(부분 기각) 「v3 의 수용 사유가 `sunset.toml` 을 잘못 인용한다」** — 적었다가 되물었다. v3:105 는 트리거 문장의 괄호(*"`pal` 이 쓰는 회차 레코드"*)를 근거로 든다. `docs/sunset.toml:17-22` 는 그 위에서 *"★ **이름에 안 묶이게 잡는다** … 그래서 이름 대신 **갈래**를 고정한다: 사람이 쓰는 것 = `.md` / 기계가 쓰는 것 = `.json`"* 이라 적었고, 이 축으로 보면 `findings.jsonl` 은 명백히 「기계가 쓰는 것」이다. **그러나** 같은 파일 `:15` 가 *"소유자가 정했다: **`pal` 이 자기 저장소에서 회차를 한 번 돌렸을 때**"* 라 적어 v3 의 읽기에도 근거가 있다. **어느 쪽 읽기가 옳은지 나는 못 정한다** — 이것은 소유자의 처분(「안 만지고 #68 에 관측만」)이 이미 덮은 자리다. 시나리오로 안 낸다. 다만 v3:105 의 「**일치한다**」는 두 읽기 중 하나를 고른 것이지 파일이 그렇게 말한 것이 아니므로, 게이트 `## 판정` 에 적을 때 **「선택한 읽기」임을 밝히는 편이 정직하다.**
- **「`SKILL.md` 편집이 좌표 `SKILL.md:44/132/355` 를 밀어낸다」** — 계획 B·H 의 편집은 **줄 수를 안 바꾸는 제자리 치환**(`python3 ` 접두 · 정규식에 `|^\.claude/` 추가)이라 밀리지 않는다. 실측으로 세 줄 다 지금도 인용 내용과 일치한다. **기각** — 다만 계획 F 의 에이전트 편집은 실제로 밀렸다(시나리오 3).

---

## ★ 메인이 볼 비율

시나리오 **11 건 전부가 `계획자신`** 이다. `계획대상`(#71·#72 가 지목한 병 자체)에 대한 새 시나리오는 **0 건**이다 — 그쪽은 앞 두 라운드가 이미 훑었고, v3 가 그것들을 흡수하면서 **흡수한 자리마다 새 선언면이 생겼다.** 이것이 규약 §2 가 적은 *"1 라운드가 계획을 고치면 고친 계획이 새 표면을 낸다"* 의 3 라운드째 관측이고, 상한 3 으로 닫는 것이 옳다는 증거다. 남은 것의 처분은 「무엇을 더 고칠까」가 아니라 **「고친 것이 또 무엇을 낳는가」로 판정해야 한다** — 위 11 중 **금지역 5**(시나리오 1·3·4·10·11)가 그 이유로 남는다.

새 범주: **판 사이의 완화책 증발 — 그리고 흡수된 완화책이 자기들끼리 모순되는 자리**(v2 의 라운드 1 처분 12 항 중 1 항 소실, 1 항이 「대응표를 그리자」에서 「여덟을 선언한다」로 바뀌며 겹침 발생, v3:52 ↔ v3:74 의 자기모순). 기존 시딩의 「요구 누락」과 달리 **원 요구가 아니라 앞 라운드가 건 방어가 사라지는 자리**다.
