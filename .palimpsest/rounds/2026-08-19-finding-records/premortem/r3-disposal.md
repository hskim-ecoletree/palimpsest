# 사전부검 라운드 3 — 처분표 (상한 3 · 닫는다)

> 시나리오 **12** · 기각 **7**. 원 반환문은 [`r3-raw.md`](r3-raw.md) 에 전문 그대로.
> ⚠ **사전부검 자신이 마지막 절에 「11 건」이라 적었다. 실제는 12 다.**

## ★ 계수 불일치가 이 회차에서 셋 중 둘 — 이것이 #71·#72 의 직접 증거다

| 누가 세었나 | 말한 수 | 원문의 `###` | 어긋남 |
|---|--:|--:|---|
| 메인 (라운드 1 보고) | 17 | **18** | ✗ |
| 메인 (라운드 2 보고) | 13 | 13 | ✓ |
| **사전부검 자신** (라운드 3 결론) | 11 | **12** | ✗ |

**사람도 에이전트도 자기 산출을 잘못 센다.** 회고가 *"합계 검산을 사람이 돌린다"* 를
병으로 지목한 이유가 회차 안에서 **세 번 중 두 번** 재현됐다.

## 상한에 닿았다 — 닫는 근거

라운드 3 의 **12 건 전부가 `계획자신`** 이고 `계획대상`(#71·#72 의 병 자체)에 대한 새
시나리오는 **0** 이다. 사전부검의 결론 문장 그대로 — *"v3 가 앞 것들을 흡수하면서
흡수한 자리마다 새 선언면이 생겼다."* 규약 §2 의 *"1 라운드가 계획을 고치면 고친 계획이
새 표면을 낸다"* 의 3 라운드째 관측이다. **상한 3 으로 닫고 남은 위험은 검증·실측이 잡는다.**

## 금지역 다섯 — 전부 계획 수정으로 닫는다

| # | 무엇 | 처분 |
|---|---|---|
| **R3-1** | **v2 의 「당기는가」 측정 조건이 v3 에서 사라졌다.** v2:64 의 *"소유자가 출력을 **실제로 본 뒤에만** 잰다 — 안 보이면 「아니오」가 반증이 아니다"* 가 v3 에 안 옮겨졌다. 계기판은 **에이전트가** 부르고 소유자에게 보여지는 경로가 없다 → 안 보여 준 것을 「설명을 안 요구했다」로 적으면 **거짓 판정** | **복원 + 실행.** v4 에 되살리고, **승인 자리에서 계기판 전체 출력을 소유자에게 실제로 보인다** |
| **R3-3** | **이 회차의 편집이 자기가 쓴 `좌표` 를 낡게 만든다.** 격리 실측: 계획 F 를 적용하니 `pal-premontem-sweeper.md` 의 `## 내가 기각한 것` 이 **93 → 95** 로 밀렸다. 느슨히 해소하면 죽은 가지, 엄격히 하면 append-only 로그가 **줄 드리프트로 영구히 빨개진다** | **계획 수정** — 좌표를 `경로`(필수)·`줄`(선택)·**`기준커밋`**(적을 때의 HEAD)로 나눈다. 검사는 **경로 실재만** 해소한다. 줄 드리프트는 기준커밋이 설명한다 |
| **R3-4** | **`라운드` 를 커밋 태그에서만 유도하면 대부분이 값을 못 받는다.** 실측: 406 커밋 중 `[R<n>]` 은 **13**(3.2%), `(round)` 무태그 **47**. 그리고 **기록 시점에 그 커밋이 아직 없다** — R3 발견을 적을 때 `[R3]` 커밋은 미래다 | **라운드 1 의 S12 처분을 뒤집는다** — 레코드가 `라운드` 를 **적고**, 계기판이 커밋 태그와 **댄다**. 원천 둘을 없애는 대신 **대는 장치**를 둔다 |
| **R3-11** | **`획득` 이 레코드 필드에 없고, 넣으면 `유효성` 과 같은 것을 잰다.** 두 어휘가 `추정` 을 같은 뜻으로 쓴다 — 한 비트가 두 칸에 손으로 적히고 반드시 갈린다 | **계획 수정 — `획득` 을 만들지 않는다.** `유효성` 하나로 같은 답이 나온다. 에이전트의 `근거` 개명만 한다(이름 충돌 해소) |
| **R3-12** | **v2 의 「대응표를 적는다」가 v3 에서 사라졌고 여덟이 겹친다.** `전환`=100% 승격 · `축소`⊃승격 · `막힘` 은 §11 ② 가 **해악도에서 유도**한다 · **`완화` 가 빠졌다** — 위장한 정정을 가리는 축이 사라진다 | **계획 수정 — 대응표를 실제로 그린다**(v4 §A). `막힘` 을 처분에서 빼고, `완화` 를 **`조건변경` 필드**로 세운다 |

## 나머지 일곱

| # | 무엇 | 해악도 | 처분 |
|---|---|---|---|
| R3-2 | **합계 검산의 「항 수」가 기계로 셀 수 없다.** r1 `###`18+기각5 · r2 13+4(+절 하나 더) · r3 12+7 — 절 구성이 파일마다 다르다. **그리고 보존된 원 반환문이 사전부검뿐**이고 독립 리뷰·인터뷰는 자리가 없다 | 실패 | **계획 수정** — 검산 규칙을 **`### 항 + 기각 불릿`** 으로 못박고, **네 출처 전부의 원 반환문을 보존**한다 |
| R3-5 | 이 회차가 `SKILL.md` 의 손으로 벤 거울 **둘**을 낡게 한다 — *"**여섯 칸**이 나온다"*(→여덟) · *"**검사 19 개**"*(→20) | 거짓신호 | **계획 수정** — H 에 더한다. ★ `SKILL.md` 는 설치본에 통째로 나간다 |
| R3-6 | **`^\.claude/` 는 빌드 입력 넷 중 하나만 닫는다.** 나머지 셋(`schema/graph.toml`·`surface/queries.toml`·`corpus/tasks/f03-normalize-seeds.ts`)은 그대로 「안 기다린다」 | 거짓신호 | **계획 수정** — 넷을 다 적되, **또 벤 거울임을 인정**하고 종료 판정은 *"마지막 커밋 SHA 에 `conclusion=success`"* 로 잰다는 규약 문장에 기댄다 |
| R3-7 | enum 「한 파일」의 자리가 설치 목록에 없다 — `layout.rs` 는 **이름으로 적는 명시 목록**이고 와일드카드가 없다 | 실패 | **계획 수정** — enum 을 **`record.py` 안에** 둔다. 별 파일이면 설치 3목록에 함께. 검사는 `record.py --schema` 를 불러 읽는다(정규식으로 안 긁는다) |
| R3-8 | `python3` 로 고칠 자리가 **다섯**인데 계획은 「두 줄」이라 적었다 — 특히 `dashboard.py:84` 는 **사람이 막힌 순간 보는 오류 메시지**이고 설치본에 그대로 나간다 | 거짓신호 | **계획 수정** — 다섯 자리 전부 |
| R3-9 | **`python3` 가 Windows 에서 서는지 안 재고 정했다.** CI 에 python 설정 스텝이 **0 건**이다. ADR-0023 이 금지한 「안 재고 적는」 형태 | 실패/금지역 | **계획 수정 — 재고 정한다.** 안 서면 `sys.executable` 을 안 쓰고 **`pal` 이 부르는 형태**나 다른 축으로 간다. ⚠ 이 판정을 안 하고 넘어가면 금지역이다 |
| R3-10 | 「기각 절이 유일하게 구조가 없다」가 **리뷰어에게는 거짓** — `### 빠진 것`·`### 요구되지 않은 것`·`### 있는데 틀린 것`·`## 미측정 목록` 넷이 더 있다. 그리고 `## 합격선 축` 이 **다섯째 어휘**(통과/반증/대조불가/미측정)를 든다 | 거짓신호 | **계획 수정** — F 의 범위를 다섯 절로 넓히고, 합격선 축은 **`처분` 이 아니라 별도 필드**로 둔다 |

## 기각한 것 — 실측이 기각했다 (7)

- **「모집단이 `corpus/` tsv 12 개를 끌어온다」** — 글롭이 `.palimpsest/rounds/**` 라 밖. tsv 15 중 **14 가 `corpus/`**, 글롭 안은 `02-classification.tsv` **하나**. 기각
- **「이 회차의 `premortem/*.md`·`red/*.txt` 가 모집단에 들어온다」** — 확장자가 안 걸린다(txt 18·tsv 1·py 1 전수 확인). 기각
- **「모집단 하한이 즉시 발화한다」** — `02-classification.tsv` 가 이미 있어 ≥1. 기각
- **「`detect-pendulum.py` 가 걸린다」** — `.py` 는 글롭 밖. 기각
- **「v3 가 `#68 에 관측만` 을 안 지켰다」** — `gh issue view 68` 로 확인, **관측이 실제로 실려 있다.** 기각
- **「`SKILL.md` 편집이 좌표 셋을 밀어낸다」** — 제자리 치환이라 안 밀린다. 기각
- **(부분 기각) 「v3 의 수용 사유가 `sunset.toml` 을 잘못 인용한다」** — `sunset.toml:17-22` 는
  **갈래**(기계=`.json`)로, `:15` 는 **쓰는 자**(`pal`)로 읽힌다. **두 읽기 다 근거가 있다.**
  ★ **처분: v4 와 게이트에 「선택한 읽기」임을 밝힌다** — 파일이 그렇게 말한 것이 아니다

## 새 범주

**판 사이의 완화책 증발** — 원 요구가 아니라 **앞 라운드가 건 방어**가 사라지는 자리.
v2 의 라운드 1 처분 12 항 중 **1 항 소실**(당기는가 측정 조건), **1 항이 변질**
(「대응표를 그리자」→「여덟을 선언한다」). 그리고 v3:52 ↔ v3:74 의 자기모순.
