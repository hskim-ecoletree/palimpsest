# 사전부검 라운드 1 — 처분표

> 시나리오 **17** · 기각 **5**. 격리 사본(`/tmp/premortem-20260819-1/repo`)에서 실측했고
> 원 저장소는 안 건드렸다. 기준선 `cargo xtask check` 19/19 · 설치 시험 50/50.
> ★ 남기지 않는다 — 넷 중 하나로 처분한다: 계획 수정 · 탐지 수단 · 완수 조건 전환 · 수용 사유 명시.

## 해악도 분포

| 해악도 | 수 |
|---|--:|
| **금지역** | **7** |
| 실패 | 1 |
| 거짓신호 | 9 |

## 승격으로 올린 것 — 물음 하나에 칸 하나 (§5)

| # | 시나리오 | 왜 승격인가 |
|---|---|---|
| **S17** | **만들 필요 없는 것을 만들고 있는가** — #71 이 지목한 사각은 `.tsv` **하나**이고, `retro/02-classification.tsv` 는 이미 11 열·110 줄의 기계 판독 파일로 계획 A 의 축을 대부분 들고 있다(`축1_유효`·`축2_대상`·`축6_깨짐`·`근거인용`). `xtask` 확장자 목록에 `tsv` 를 더하고 검사 하나를 놓으면 **새 형식·쓰기 스크립트·설치 3목록·매니페스트·uninstall 계약·실행 권한·플랫폼 대칭이 전부 안 생긴다.** | **축소**다. 소유자가 표면을 「스킬 `bin/` 스크립트」·「설치 표면에도 간다」로 골랐고, 이것은 그 선택을 깎는다 |
| **S1+S2** | **sunset 트리거(#68)를 이 회차가 만지는가.** `.jsonl` 을 고르면 트리거가 **영영 안 뜬다**(격리 실측: 파일 심고 `xtask check` → 19/19, sunset 은 「아직 0건」) → 금지역 「측정이 죽은 가지」. 그렇다고 트리거를 `.jsonl` 로 고치면 **그 자리에서 CI 가 빨개지고**(격리 실측: `FAIL sunset 선언` 둘) 범위 밖인 `docs/research` 3,015줄 삭제를 요구한다 | 계획의 `## 범위 밖` 이 #68 실행을 뺐다. **둘이 동시에 못 선다** — 범위를 건드리므로 소유자의 답이 필요하다 |

## 계획 수정으로 처분 (승인 불필요 — 의도를 향해 간다)

| # | 시나리오 | 해악도 | 처분 |
|---|---|---|---|
| S3 | 합계 검산이 **자기가 쓴 것을 자기가 세는 항등식**이 된다. 회고가 이미 겪었다 — *"나는 목표 수를 알고 쪼갰다 … 검산은 통과했으나 거울을 완전히 깨지는 못했다"* | 금지역 | **완수 조건으로 전환** — 검산은 **독립된 둘째 원천**(에이전트 원 반환문)을 대야 통과다 |
| S4 | 생존자 편향이 없어진 게 아니라 **전사 단계로 옮겨간다.** 발견을 내는 자와 기록·처분하는 자가 같고, 떨어뜨린 것을 `유효성=거짓` 으로 적을 유인이 없다 | 금지역 | **탐지 수단 추가** — 에이전트 원 반환문을 파일로 보존하고, 레코드 행 수와 원 반환문의 항 수를 **대는** 검사 |
| S6 | `## 내가 기각한 것` 이 **유일하게 구조가 없는 산문 절**인데 계획 A 는 그 줄에도 필수 필드 넷을 요구한다. 못 채우면 #72 의 ★ 가 조용히 빠지고, 억지로 채우면 「사실이 아닌 것을 사실로」 | 금지역 | **계획 수정** — 두 에이전트 정의의 기각 절도 **같은 표**로 내게 고친다 (`.claude/agents/pal-*.md` 둘 · 설치본이기도 하다) |
| S7 | 규약이 *"새 칸을 더하려면 회고의 수를 근거로 대야 하고 **원천이 산문이면 못 더한다**"* 고 못박았는데, ⑦⑧ 의 원천은 여전히 **사람(에이전트)의 판정**이다. 통과한 여섯은 전부 `git show`·`git diff`·`grep -c` 였다 | 금지역 | **계획 수정** — ⑦⑧ 출력에 *"이 둘의 원천은 기록된 판정이지 git 이 아니다"* 를 박는다. C6-c 충족을 주장하지 않는다 |
| S8 | **설치본의 `bin/*.py` 는 0644 라 실행이 안 된다**(격리 실측: `permission denied`). SKILL.md 는 두 경로 다 직접 실행하라고 적는다. **저장소 기존 결함**이고 이 회차가 처음 밟는다 | 금지역 | **계획 수정** — 호출 형태를 `python3 <경로>` 로 바꾸거나 모드를 세운다. Windows 는 모드로 안 풀린다(ADR-0023) |
| S9 | 새 검사가 **파일이 있는 회차만** 재서 옵트인이 된다. 이 저장소는 그 형태를 두 번 이름 붙였다 — *"모집단이 비면 실패다 — 0 건은 「안 부른다」가 아니라 「안 봤다」일 수 있다"* | 금지역 | **계획 수정** — 하한을 적는다. 모집단이 비면 실패 |
| S15 | 「당기는가」 조건이 **소유자가 출력을 봤는지**와 구별되지 않는다. `dashboard.py` 스스로 *"에이전트가 부른다. 사용자가 수시로 부르는 물건이 아니다"* 라고 적는다 | 금지역 | **완수 조건 정정** — *"⑦⑧ 을 포함한 전체 출력을 소유자에게 그대로 보인다"* 를 조건에 붙인다. 안 보이면 「아니오」가 반증이 아니다 |
| S5 | 사전부검 반환 형식에는 `모집단` 도 `유효성` 도 **없다**(대신 `대상` 2값·`근거` 2값). 그리고 **`근거` 는 이름이 같은데 뜻이 정반대**다 — 사전부검=획득 방법, 계획=`경로:줄` | 거짓신호 | **계획 수정** — 필드 이름을 가르고(`획득`/`출처`), 두 에이전트 정의에 `모집단`·`유효성` 을 더한다 |
| S10 | `findings.jsonl` 이 **기존 검사 어느 모집단에도 안 든다**(격리 실측: 문서 179 불변). #71 이 잡은 병이 새 파일에서 재현된다 | 거짓신호 | **계획 수정** — 새 검사가 `근거` 의 `경로:줄` 해소까지 잰다 |
| S11 | ⑦⑧ 은 **파일 전체**를 읽는데 ①~⑥ 은 커밋 범위다. 헤더는 한 범위를 주장한다. 그리고 인자를 둘만 주면 ⑦⑧ 이 조용히 「못 셌다」 | 거짓신호 | **계획 수정** — ⑦⑧ 에 범위 필터를 걸거나, 다른 범위임을 출력에 박는다 |
| S12 | 라운드 번호의 원천이 **둘**(커밋 `[R<n>]` ↔ 레코드 필드)이 되고 대는 장치가 없다. 규약이 이미 진단했다 — *"표기 규약이 회차 안에서 태어나면 갈린다"* | 거짓신호 | **계획 수정** — 레코드의 `라운드` 를 커밋 태그에서만 유도한다 |
| S13 | `처분` enum 이 규약 §5 와 다르다. 특히 규약은 **「안개는 적지 않는다」**고 명령하는데 #72 는 「기각한 것도 남는다」를 요구한다. `승격`·`막힘` 은 값이 없다 | 거짓신호 | **계획 수정** — 대응표를 적는다. 「안개」와 「기각」이 다른 것임을 스키마가 가른다 |
| S14 | 모집단 분리 (나)가 **이 회차 자신의 시연 데이터셋을 편향**시킨다. `자기장치` 가 0 이 되면 ⑦ 의 첫 수가 구조적으로 부풀려진다 — 회고의 `자기장치 22/108` 과 정반대 방향 | 거짓신호 | **계획 수정** — *"기록은 하되 이 회차에서 닫지 않는다"* 를 한 줄로 박는다. (나)는 **닫는 것**의 분할이지 **적는 것**의 분할이 아니다 |
| S16 | 스키마가 **두 벌**(`record.py` ↔ xtask 검사)이 되고 대는 장치가 없다. `layout.rs:57` 이 이미 *"사본을 두면 갈리고 두 벌을 대는 검사가 없다"* 를 적었다 | 거짓신호 | **계획 수정** — enum 을 한 파일에 두고 양쪽이 읽는다 |
| S18 | **`.claude/` 는 빌드 입력인데 규약의 CI 대기 판별식이 그것을 모른다.** 격리 실측: `touch .claude/skills/round/SKILL.md` → `Compiling pal-cli`. *"②의 목록은 빌드 입력이라 손으로 적어도 안 갈린다"* 는 **지금 거짓이다** | 거짓신호 | **계획 수정** — 판별식에 `^\.claude/` 를 더한다. 이 회차는 그 자리를 밟는다 |

## 기각한 것 — 실측이 기각했다

- **「`PAYLOAD` 에만 더하면 되돌릴 수 없다」(계획 E 의 ⚠)** — 격리 사본에서 실제로 해 봤더니
  **두 시험이 즉시 빨개졌다**(`놓는_것은_전부_되돌릴_수_있다` · `우리가_놓는_자리는_전부_통과한다`).
  **이미 장치가 지킨다.** 계획의 ⚠ 가 과장이었다 — 지운다.
- **「매니페스트 항목 상한 256」** — 실제 설치 후 세니 **28**, `record.py` 를 더해도 **30**.
  226 칸 남는다. 안 걸린다.
- **「`bin/` 디렉터리가 없어 설치가 멈춘다」** — `DIRS` 에 이미 있다(`layout.rs:161`).
- **「`findings.jsonl` 이 `.gitignore` 로 빠진다」** — `git check-ignore -v` rc=1. 안 걸린다.
- **「`record.py` 의 docstring 이 검사 19 를 깨뜨린다」** — 실재하지만 자기 신고형이고
  `cargo xtask check` 한 번에 즉시 드러난다.

## 새 범주 (사전부검이 시딩 밖에서 낸 것)

**에이전트 반환 형식과 저장 스키마의 계약 어긋남** — 같은 이름 다른 뜻(`근거`) ·
축의 값 개수 불일치(2값 ↔ 5값) · **구조 없는 절이 필수 필드를 가진 행으로 승격되는 자리**
(`## 내가 기각한 것`). 사람/에이전트가 산출을 넘기는 **경계**가 깨지는 자리다.
