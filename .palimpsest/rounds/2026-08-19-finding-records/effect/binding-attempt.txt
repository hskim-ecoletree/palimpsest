# 결박 시도 — §11 조건 4 (C10-d)

이 회차의 결정과 의도를 코드 좌표에 **결박**하고 그래프를 갱신하려 했다.
**문서가 아니라 산출이 무엇이 서는지 말한다.**

## pal ledger — 무엇을 결박할 수 있나
```

Snapshot  palimpsest@20fc5d6+worktree  (워킹트리)
저장소    선언 1 · 본 것 1
범위      매니페스트 없음 — 경로에서 유도했습니다 (선언이 아니라 추정)

워킹트리  20fc5d6 와 다른 파일 6개  ·  인덱스 신뢰 446 · 다시 잼 6

파일      452
  parsed               1
  partial              0
  unsupported        408  추출기 없음(로드맵)
  unrecognized        42  언어 미인식
  excluded             0
  binary               1
  generated            0

언어      Markdown          L0  결박 불가            215 파일
          Rust              L0  결박 불가            117 파일
          Python            L0  결박 불가             39 파일
          TOML              L0  결박 불가             28 파일
          Shell             L0  결박 불가              6 파일
          YAML              L0  결박 불가              2 파일
          JSON              L0  결박 불가              1 파일
          TypeScript        L2  identity: exact     1 파일

          ← 결박 불가 언어 7개 · 408 파일. 이 파일들에는 좌표가 없습니다

캐시      적중 415 · 빗나감 37

provider  (이 빌드에 provider 포트가 없습니다 — F21 미구축)
```

## pal narrative — 이 회차의 문서를 코드에 걸 수 있나
```

■ 서술물 인입
  문서 216 · 조각 2096 · 새 개체 187
  결박됨 0 · 후보 있음 7 · 미결박 2089

■ 무엇이 걸었나
  span                   7

■ 이 인입이 못 본 것
  이력 창 400 커밋 · 창 밖에서 마지막으로 바뀐 문서 0
  **그 문서들에는 「같은 커밋」 신호가 없습니다** — 없는 것이지 0 이 아닙니다.

  **아무것도 승인하지 않았습니다.** `inferred` 는 사람의 승인으로만
  `asserted` 가 됩니다 — `pal narrative approve <개체>`.

```
