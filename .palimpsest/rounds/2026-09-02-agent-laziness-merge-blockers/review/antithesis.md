# 반 — 독립 공격

R1은 직접 checkpoint, 빈 report, 축약 findings, 복원 불가 profile, 갈린 gate, 느슨한 doctor를
재현했다. R2는 원장 실행 경쟁, 선택 필드와 상태 모순, 숨은 Markdown heading, 갈린 external
store까지 추가로 공격했다. 각 재현은 교정 전 경계가 거짓 complete 또는 fail-open을 낼 수
있음을 보였고, 교정 뒤 같은 경로의 음성 대조로 다시 실행됐다.
