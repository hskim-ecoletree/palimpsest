# 교대 상태 — 결박 반경이 처음으로 쓰인다

> 회차 `2026-09-12-binding-radius-in-use` · 착수 커밋 `1276b8f`

★ **새 컨텍스트가 받는 것은 「`intent.md` 전문 + 이 파일의 요약」이다.** 직전 산출물을
시드로 받지 마라 — 의도가 변질되는 기제는 쪼개는 것 자체가 아니라 **다음 걸음의 입력이
원 목표가 아니라 직전 걸음의 산출물일 때**다.

## 지금 단계 — **완수 조건이 섰다. 소유자의 답 둘을 기다린다**

| 단계 | 상태 |
|---|---|
| 인터뷰 | **닫혔다** — 상한 3 · 3 라운드 소진 · 범주 다섯 전부 열렸다 |
| 사전부검 | **닫혔다** — 상한 2 · 2 라운드 소진 (R1 19 항 · R2 21 항) |
| 정반합 판 1 (㈂ 의 처분) | **닫혔다** — 상한 2 소진 · 소유자가 2026-09-13 에 답했다 |
| **완수 조건** | **섰다** — 44 조건 · 여덟 묶음(A~H) · 형식오류 0 (`pal round conditions --json`) |
| **승격 3·4** | **올렸다 · 답을 기다린다** — 아래 절 |
| 완수 조건 설계 평가 | 아직 (상한 2) — **승격 답이 온 뒤에 돈다.** 답이 `A3`·`E6` 를 바꿀 수 있다 |
| 승인 | 아직 |
| 루프 | 아직 — **코드는 한 줄도 안 바뀌었다** |

## ⚠ 이 회차가 지금 걸려 있는 병 — 다음 컨텍스트가 먼저 알아야 한다

**발견 64 행인데 산출물이 문서뿐이다.** 메모리 「발견이 완성을 밀어낸다」가 적은 형태이고,
이 회차가 지금 그 안에 있다. 재는 자리는 **전부 상한을 소진했다** — 인터뷰 3/3 ·
사전부검 2/2 · 정반합 2/2. **더 재지 마라. 남은 것은 쓰는 일이다.**

## 이 회차가 무엇인가 — 한 문단

결박 반경(`symbol`·`callers`·`closure:k`·`files:`)의 장치는 **이미 서 있다.** 순서표 §2 의
2 번이 2026-09-10 에 서면서 넓은 반경이 **빈 집합이 아니게 됐는데**, 저장소의 결박
**37 건이 전부 `symbol`** 이라 한 번도 안 쓰였다. 이 회차는 그것을 쓰이게 하고(㈀),
#58 의 「대조 불가」 둘을 처분하고(㈂-a · ㈂-b), 순서표의 `C6` 자리를 바로잡는다(㈄).

**없는 것은 하나다** — *"반경만 바꾸고 낡음 판정을 보존하는 경로."* `pal bind` 재호출은
`bound_at` 과 감시 다이제스트를 HEAD 로 재기준해 **지금 `stale` 인 7 건을 지운다.**

## 남은 일 — 순서대로

1. ~~완수 조건을 쓴다~~ **섰다.** `PM1-02`·`PM2-02`(완수조건전환) 가 `C2`·`A3` 으로 들어갔다.
2. **소유자의 답 둘을 받는다** — `intent.md` 의 `## 승격` 칸 **3·4**. 재료와 판정 초안을
   붙여서 올렸다. 답이 `A3`(기준 시점) 과 `E6`(CI 공백) 의 문면을 바꿀 수 있다.
3. `pal-condition-auditor` 로 **조건 설계 평가 2 라운드**. ⚠ 절만 뽑아 주고 **파일 경로를
   건네지 마라**(§3.5 의 격리 — `intent.md` 는 `## 개정`·`## 승격` 을 같은 파일에 싣는다).
   ⚠ `cargo xtask check` 의 남은 실패 하나가 `conditions-audit/r<n>-raw.md` 를 요구한다 —
   그것이 서면 28/28 로 돌아온다.
4. **승인** → 루프. **루프의 첫 일은 `B3-a`·`G3` 의 RED 를 넓히기 전에 남기는 것이다.**

## 완수 조건의 뼈대 — 여덟 묶음

| 묶음 | 무엇을 재나 | 계획 |
|---|---|---|
| **A** (11) | 보존 경로 — 여섯 필드 보존 · 기준 시점 · 앵커 출처 등록 | ㈀ 의 첫 일 |
| **B** (8) | 대상 37 이 근거를 대고 넓혀졌나 · 양성 축 셋 | ㈀ 의 본체 |
| **C** (4) | 넓힌 것이 무엇을 드러냈나 — 판정 분포 · `Undeterminable` 가림 | ㈀ 의 산출 |
| **D** (5) | 거짓 양성률을 `대조불가` 로 · 까닭의 좌표 | ㈂-a |
| **E** (6) | 반경별 벤치 · `[f09.4.pass]` ④ 는 안 닫는다 | ㈂-b |
| **F** (3) | 순서표 §2 3 번을 고치고 §1 은 안 고친다 | ㈄ |
| **G** (5) | 이슈 넷 · 회귀 넷 | 분할 |
| **H** (2) | 효과 — 넓힌 반경이 재검토를 불렀다 | §8 |

★ **음성 대조가 조건으로 서 있다** — `A1-a`·`A2-a`·`A3-a`·`B1-a`·`B6-a`·`C2-a`·`D1-a`.
전부 *"사본에서 판정이 뒤집히는가"* 를 묻는다.

## 소유자에게 올린 것 둘 — **`intent.md` 의 `## 승격` 칸 3·4**

⚠ **앞 판이 이 자리에 적은 「하나」의 전제가 뒤집혔다.** 앞 판은 *"새 감시 원소의 digest 는
넓히는 시점의 투영에서 읽히므로 넓힌 직후 새 원소는 반드시 `fresh`"* 를 **주어진 것**으로
두고 「순서로 푼다」를 처방했다. 그 전제는 **선택이지 사실이 아니다** — 37/37 의 `bound_at`
base 커밋 일곱이 **전부 실재해서**(`git cat-file -t`) HEAD 가 아닌 기준 시점이 있다.
그래서 승격 3 은 「증인을 어떻게 만드나」가 아니라 **「기준 시점을 어디로 잡나」**로 올렸다.

| 칸 | 물음 | 내 판정 초안 |
|---|---|---|
| **3** | 새 감시 원소의 **기준 시점** — HEAD(㈎) · `bound_at` 의 base 커밋(㈏) · 새 `Undeterminable` 사유(㈐) | **㈏.** ㈐ 는 `evaluate` 의 ②가 `Stale` 을 덮어 넓힘의 목적을 뒤집고 `[f09.2.pass]` 모집단을 움직인다. ㈎ 는 *"결정 뒤에 호출자가 변했나"* 를 영구히 못 묻게 한다 |
| **4** | ㈂-b 의 **「아무도 안 돌림」** — 손으로 돌려 게이트에 싣고 이슈로 세운다(㈎) · 이번에 `xtask test` 에 `--ignored` 축을 더한다(㈏) | **㈎.** `--ignored` 부재는 ㈂-b 가 만드는 것이 아니다 — `crates/pal-query/tests/bench.rs`(`[f05.3.pass]` ④)가 이미 같은 형태다. ㈏ 는 남의 기능의 CI 시간을 이 회차가 정하는 것 |

## 지금 선 것

- `main` · HEAD 는 이 회차의 마지막 커밋 · **미push** (push 는 한 번에 몰아서)
- `cargo xtask check` **27/28** — 남은 하나는 `conditions-audit/r<n>-raw.md` 부재이고
  **승인 앞 단계의 정상 상태**다 (`PM2-08` 이 25/28 로 적은 것이 지금 27/28 이다)
- 착수 기준선 `1276b8f` 의 CI 는 **초록**(잡 일곱 · 런 `34694615603`)
- 레코드 **64 행** — 사전부검 40 · 정반합 22 · 실측 2 / 유효 44 · 스스로 물린 것 16

## 실패한 접근 — **다음 컨텍스트가 같은 벽에 다시 부딪히지 않도록**

1. **`pal intent import` → `export` 순서로 대조하면 일이 지워진다.** `record` 가 id 가 같으면
   덮어쓰므로 옛 추적본이 넓힌 37 건을 되돌리고, `git diff` 가 **비어서** 「정상」으로 읽힌다.
   `intent.md` 의 `## 작업 규율` 2 가 뒤집힌 순서를 진다.
2. **정본 JSONL 을 손으로 고쳐 import 하는 길은 열려 있지만 쓰면 안 된다.** `import_jsonl` 이
   좌표 실재성도 digest 출처도 검사하지 않는다 — 위조한 감시 원소가 통과한다.
3. **순서표 §1 의 표를 고치려 하지 마라.** 측정 커밋이 행마다 달린 기록이고 표 바로 아래
   줄이 금한다. **아래에 얹는다.**
4. **관측 산출을 회차 디렉터리에 `.json` 으로 두지 마라** — `sunset` 트리거가 발화해
   `docs/research` 3,015 줄을 「지울 때가 됐다」로 만든다. `.tsv` 는 **첫 줄이 탭으로 가른
   열 이름**이어야 한다(산문 머리를 달면 빨개진다).
5. **판정을 「대조 불가」로 쓰지 마라** — 파서가 아는 토큰은 `대조불가`(붙여쓰기)다.

## 걸리는 것 — 값을 치르고 배운 것

1. **`rc` 는 판정이 아니다.** 파이프 끝이 `tail` 이면 실패한 실행도 `rc=0` 이다.
2. **검증 중에 커밋하지 마라.** 빌드 시점 커밋과 실행 시점 HEAD 가 갈린다.
3. **push 는 한 번.** CI 가 `cancel-in-progress: true` 다. 종료 판정은 **「회차의 마지막
   커밋 SHA 에 `conclusion=success`」**다.
4. 이 PC 에 **pyenv 도 rustup 도 없다**. 파이썬은 `python3`, Rust 는 1.94.1 고정.
5. `pal` 을 쓰려면 `PATH="$PWD/target/release:$PATH"` 가 필요하다. `target/` 은 저장소에 없다.
6. **서브에이전트 다수가 읽기 전용이라 파일을 못 쓴다.** 정반합 넷과 사전부검이 전문을
   반환문에 실었고 메인이 옮겼다 — 그 사실을 파일 머리에 적는다.
7. **코퍼스 클론 루트는 `~/dev/projects/boxwood` 를 인자로 넘겨야 선다.**

## 이 회차가 끝난 뒤 — **다음 지형이 필요하다**

⚠ **`docs/plan/03-shortest-path.md` §4 의 네 단계가 이 회차로 소진된다.** 0·1·2·3 단계가
전부 지났고 §5 가 「2 단계 직후」로 예고한 것이 이 회차다. 순서표 §2 의 남은 4·5·6 은
`00-goals.md` §2 기준으로 **P2·P3 이고 첫 릴리스 정의 밖**이다.

**그러므로 이 회차의 종료 보고는 「다음 회차가 받는 것」에 이슈 목록이 아니라
「무엇이 제품의 다음 경계인가」를 적어야 한다.** 그것을 정하는 것은 소유자다.
