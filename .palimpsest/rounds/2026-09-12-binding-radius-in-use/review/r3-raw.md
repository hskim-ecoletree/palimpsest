# 독립 리뷰 — 라운드 3 · 2026-09-12-binding-radius-in-use

> ⚠ **이 파일은 원 반환문이다. 메인이 요약하거나 고치지 않는다** — 합계 검산이 독립된
> 둘째 원천을 대야 하기 때문이다. **`pal-independent-reviewer` 가 냈고, 읽기 전용이라
> 파일을 못 써서 전문을 반환문에 실었고 메인이 옮겼다.** 판정도 순서도 문면도 그대로다.
>
> **리뷰어가 안 받은 것**: 대화 기록 · 메인의 사고 과정 · 왜 그렇게 만들었는지의 설명 ·
> 앞 라운드의 판정(시드로). `review/r1-raw.md`·`r2-raw.md` 는 존재를 확인했고 판정 근거로
> 인용하지 않았다 — 한 곳만 예외이고 그 사실을 리뷰어 자신이 적는다.
>
> ★★ **이 라운드가 잡은 금지역 셋은 전부 「앞 라운드의 정정이 만든 것」이다** — 합(合)이
> 삭제를 지시한 문장이 게이트 머리에 남아 있던 것 · 벤치를 다시 돌려 첫 실행의 인용이
> 고아가 된 것 · R2 의 처분으로 세운 검사의 모집단 선언이 거짓인 것. 규약 §11 ③ 이
> *"장치를 고칠 때마다 새 선언면이 생겨 다음 라운드의 먹이가 된다"* 로 적은 그 형태다.
>
> ★ **처분은 이 파일에 없다.** 처분은 `findings.jsonl` 의 레코드가 지고 §5 의 자로 메인이
> 정한다. 이 파일은 **무엇이 보고됐나**만 진다. 처분 커밋은 `f54e6a6` 이다.

---

## 머리 — 내가 받은 것 / 안 받은 것 / 실제로 돌린 것

**받은 것**: 저장소 파일 전부(`intent.md` 438 줄 · `docs/gates/binding-radius-in-use.md` 429 줄 · `approval.md` · `state.md` · `findings.jsonl` 194 행 · `widen/` · `bench/` · `effect/` · `observations/` · `dialectic/` · `premortem/` · `conditions-audit/` · `review/` · `crates/` · `xtask/` · `docs/` · `.claude/skills/round/SKILL.md` · `AGENTS.md`) · 커밋 범위 `1276b8f..e4176b3`(29 커밋).

**안 받은 것**: 대화 기록 · 메인의 사고 과정 · 왜 그렇게 만들었는지의 설명. `review/r1-raw.md`·`r2-raw.md` 는 존재를 확인했고 **판정 근거로 인용하지 않았다** — 아래 모든 판정은 내가 직접 재서 낸 것이다(단 한 곳, 「86.157 이 저장소 어디에 있나」를 전수 검색할 때 그 두 파일이 결과에 걸렸고 그 사실만 적었다).

**실제로 돌린 명령** (전 출력을 파일로 받았다. `tail` 로 자르지 않았다):

```
cargo xtask check                                   → rc=0 · 검사 29/29
cargo xtask test                                    → rc=0 · test result 줄 54 · passed 합 1065 · warning 0 · FAILED 0
pal query binding.status --json                     → 결박 39 · watch 합 193 · 37 부분집합 190
pal touch extract_with                              → ■ 이 좌표를 지켜보는 것 (2)
pal doctor --json                                   → 불변식 ⑦·⑧ 둘 다 not_built
pal query graph.dump --json                         → 노드 3372(전부 Symbol) · 엣지 6027 · Binding 노드 0
pal query --list
git diff 1276b8f..HEAD -- crates/pal-cli/src/main.rs · docs/plan/02-order.md · docs/gates/F09.md · .palimpsest/intent/bindings.jsonl
git merge-base --is-ancestor fd176a8 e16dc61        → 참
gh issue view 145 146 147 148 149 150 151 152       → 여덟 전부 OPEN
gh issue view 58 --comments | grep -c binding-radius-in-use   → 2
# 격리 사본(저장소 밖 scratchpad · tar 사본)
cargo xtask check --root <사본>   ① radius.rs:221 의 check_budget 호출 제거 → rc=1 · FAIL 결박을 저장하는 경로가 예산을 지난다
                                 ② 탐침 둘 심음 → 그 검사는 「ok · 3개」 그대로
cargo check --workspace --all-targets  (사본 · .rs 전량 touch 후)  → rc=0 · warning 0
cargo build --release --workspace      (사본 · .rs 전량 touch 후)  → rc=0 · warning 0
python3 로 조건 상자 50 ↔ 게이트 판정 표 52셀 전수 대조 · 표 블록 시작 위치 검사 · decisions.tsv 집계
```

**작업 트리**: 끝나고 `git status --porcelain` 이 **비어 있다**. HEAD 는 `e4176b347fddf9bc23bd93a30032c804b8a51875` 로 시작과 같다. 사본은 저장소 밖 scratchpad 에만 만들었고 저장소 파일은 한 글자도 안 고쳤다.

**금지역 목록의 출처**: `.claude/pal/policy.toml` 이 **없음을 확인했다**(`ls` 실패). 그래서 잠긴 의도 `intent.md:168-177` 이 등록한 목록 — 데이터_손실 · 측정이_죽은_가지 · 사실이_아닌_것을_사실로 · 인증 · 대표_취약점 — 을 썼다.

---

## 합격선 축

| 조건 | 판정 | 잰 수 | 근거 |
|---|---|---|---|
| A1 | 통과 | 37/37 | `cargo xtask test` rc=0 · `반경만_바뀌고_여섯이_보존된다` 가 스위트 안에 있고 초록. 산출 `observations/after-widen.txt` ⑦ 이 37/37 + 37/37 |
| A1-a | 통과 | 시험 1 | `pal_bind_재호출은_여섯을_안_보존하고_stale_을_지운다` (`crates/pal-cli/tests/radius_preserves_verdict.rs:167`) 실재 · 스위트 초록 |
| A2 | 통과 | 7/7 | **실측**: 착수 stale 7 id 전부가 지금도 stale(`1cf63ec5e0e9937a 39b92bd51bbc82c6 4065e80475cf1c72 49714573e0fa654c 91cdc3add647a3f7 cbd70e3e8dfca3ba e3baa46390859ef3`) |
| A2-a | 통과 | 시험 1 | 같은 시험이 `stale`→`fresh` 소멸을 붙든다 |
| A3 | 통과 | 시험 1 | `새_감시_원소의_기준은_bound_at_의_base_커밋이다`(`:110`) 초록 |
| A3-a | 통과 | 2 경로 | `기준을_head_로_읽으면_fresh_가_난다`(`:239`) 가 base→stale · HEAD→fresh 를 둘 다 assert. 갈린다 |
| A3-b | 통과 | 열 2 · 참 1 · 합 10 | **실측**: `decisions.tsv` 에 `대상digest차이`·`base에없던새원소` 둘 다 있다. 참 1 건(그 건은 안 넓혀졌다) · `base에없던새원소` 합 10 |
| A4 | 통과 | 시험 1 | `없는_결박을_지목하면_실패한다`(`:292`) 초록 |
| A5 | 통과 | 산출 1 | `effect/rerun-widen.txt` 읽음 — **내가 재실행하지 않았다** |
| A5-a | 통과 | 등록 4 · 생성 14 | **실측**: `cargo xtask check` 의 검사 12 초록 — 「`WatchEntry` 생성 자리 14개 · 등록된 자리 4개」. `radius.rs:209` 에 리터럴 실재 · 사유가 *"base 커밋 투영에서 읽어 만든다"*(`xtask/src/main.rs:2063`) |
| A6 | 통과 | 1건 · diff 0 | **실측**: `crates/pal-cli/src/main.rs:140` 에 `default_value = "symbol"` 그대로. `git diff 1276b8f..HEAD` 에 그 줄 **0 건** |
| B1 | 통과 | 37 행 · 14 열 | **실측**: 헤더 제외 37 행, 전 행 14 열, 열 이름이 조건 문면과 일치 |
| B1-a | 통과 | 18 ≠ 37 | `effect/negative-B1.md` 읽음 |
| **B2** | **미측정** | 0 | `dialectic/2-design.md:11` 이 대상을 `C3·H1·A3-b·B2-a` 로 적고 **`B2` 가 없다**(grep 으로 확인). 규약 §5 가 혼자 판정을 금한다 |
| B2-a | 통과 | 2 커밋 | **실측**: `git merge-base --is-ancestor fd176a8 e16dc61` = 참 |
| B3 | 통과 | 3/3 | **실측**: ⑴ symbol 19 · callers 18 ⑵ 감시 합 190 > 37 ⑶ `pal touch extract_with` → **(2)** |
| B3-a | 통과 | 37/37·37·30/7/0 | `observations/red-B3.txt` 가 착수값을 진다(넓히기 전 커밋 `ee93b35`) |
| **B4** | **반증** | 190 ≠ 193 | **실측**: 오라클 ⑴ 「`감시후` 열 합 = `binding.status --json` 의 `watch` 합」 — tsv 합 **190**, 명령 산출 합 **193**. ⑵ `callers`·`감시전==감시후` 행 수 = **7** 로 맞다. 아래 발견 20 |
| B5 | 통과 | 모집단 3 · rc=1 | **격리 사본에서 재현**: `radius.rs:221` 의 `check_budget` 호출을 지우니 `FAIL 결박을 저장하는 경로가 예산을 지난다` · `rc=1`. 지금은 초록(`bind.rs`·`narrative.rs`·`radius.rs`). ⚠ 모집단의 구멍은 발견 13 |
| B6 | 통과 | +20 −18 | **실측**: `git diff 1276b8f..HEAD -- .palimpsest/intent/bindings.jsonl` = 20 insert · 18 delete (비지 않았다) |
| B6-a | 통과 | 18→symbol | `effect/negative-B6.md` 읽음 |
| B7 | 통과 | ⑴ = 0 | **실측**: `판정==아니다 && 새반경!=옛반경` 행 **0**. ⑵ 모집단 0(영향받는다 18 이 전부 넓혀졌다) |
| B8 | 통과 | 112 | **실측**: `타파일새원소` 열 합 = **112** · 0 이 아니다 |
| C1 | 통과 | 30/7 → 25/12 · id 5 | **실측**: 37 부분집합 `fresh 25 · stale 12 · undeterminable 0`. 새 stale id 5 가 산출의 표와 **집합으로 일치** |
| C2 | 통과 | 0 | **실측**: `status.code.reason == watch_member_gone` = **0** · id 집합 비었다 |
| C2-a | 통과 | 시험 1 | `사라진_감시_원소가_변한_것을_가린다`(`crates/pal-core/src/binding.rs:1026`) 초록 |
| C3 | 통과 | 5 건 · 참 0 | `dialectic/2-synthesis.md` 가 다섯 전부 `아니다` 로 판정. 나는 §5 상 재판정할 수 없다 — 산출 읽음 |
| C4 | 통과 | 0 ≤ 5 | **실측**: undeterminable 이관 **0** ≤ 새 stale **5** |
| D1 | 통과 | 50개 | **실측**: `cargo xtask check` 의 「원장 둘 대조」가 `2026-09-12-binding-radius-in-use (50개)` 로 훑는다. ⑴ 의 좌표·표기 흠은 발견 17·18 |
| D1-a | 통과 | rc=1 | `effect/negative-D1.md` 읽음 · `xtask --root` 실재 확인(`xtask/src/main.rs:113-125`) |
| D2 | 통과 | grep 3/3 | **실측**: `이 코퍼스에서` 1 · `scripts/f09-verify.py:715` 1 · `scripts/f09-verify.py:709` 1 |
| D3 | 통과 | diff 0 · 1 · 2 | **실측**: `git diff … docs/gates/F09.md` **0 줄** · 새 게이트에 `docs/gates/F09.md` 3 건 · `gh issue view 58 --comments` 에 새 게이트 경로 2 건 |
| D4 | 통과 | 같은 절 | **실측**: `:150`·`:151` 이 같은 `###` 안, `:153` 이 양립 문장 |
| E1 | 통과 | 14 행 | **실측**: `bench/radius.tsv` 데이터 14 행 · `radius_bench.rs:243` 에 `#[ignore]` · 축 6+8 |
| E2 | 통과 | 열 4/4 | **실측**: `회차3최솟값ms`·`회차간분산`·`두규모의비`·`changed` 넷 다 있다. 산출 머리가 비를 재계산해 표와 같다 |
| E3 | 통과 | 114.892↔0.636 · 126.898↔0.658 | **실측**: `bench/radius.tsv`·`radius-run.txt` 에서 두 비가 1 을 건너뛴다. ⚠ 게이트가 인용한 수는 다르다 — 발견 15·16 |
| E4 | 통과 | 1 줄 | **실측**: 첫 줄이 탭으로 가른 열 이름 8 개, 산문 머리 없음 |
| E5 | 통과 | .json 0 | **실측**: 회차 디렉터리 `*.json` **0** · sunset 검사 초록 |
| E6 | 통과 | 0건 | **실측**: `grep -F -- '--ignored' .github/workflows/ci.yml xtask/src/main.rs` = **0**. 게이트 `## 범위 밖` 에 문장 있고 `## 효과` 에 벤치 출력 있다(수는 발견 15) |
| F1 | 통과 | 4 문자열 | **실측**: `git diff` 의 `§2 3 번` 행에 `F05`·`F13`·`F15`·`binding.rs:504` 다 있다 |
| F2 | 통과 | 삭제 0 | **실측**: `02-order.md` diff 의 `@@ -68,6` 훙크에 `-` 줄 없음 · `:53` 불변 |
| F3 | 통과 | 3 줄 불변 | **실측**: `:67-69` 가 diff 에 안 든다. 새 `###` 이 `:69` 뒤에 섰다 |
| F4 | 통과 | grep 0 | **실측**: `grep -F "파일 안의 관계만" docs/plan/02-order.md` = **0** · `:68` 은 diff 에서 안 바뀌었다 |
| G1 | 통과 | 4/4 OPEN | **실측**: ⓐ #145 · ⓑ #146 · ⓒ #147 · ⓓ #148 전부 OPEN. ⚠ #146·#147 번호가 게이트·`intent.md`·`state.md` 에 **0 건** — 발견 21 |
| G2 | 통과 | 29/29 · rc=0 | **실측**: `cargo xtask check` 29/29 · 착수 28 이상 |
| G3 | 통과 | 1065 ≥ 1047 | **실측**: `^test result:` 54 줄 · `passed` 합 **1065** · rc=0 · 전 출력에 `warning`·`error[E`·`panicked`·`FAILED` **0** |
| G4 | 통과 | warning 0 · 0 | **격리 사본에서 실측**: `.rs` 전량 touch 후 `cargo check --workspace --all-targets` 0 · `cargo build --release --workspace` 0 |
| **G5** | **미측정** | 미push 29 | **실측**: `git log origin/main..HEAD` = **29 커밋** · `origin/main` 은 아직 `1276b8f` |
| H1 | 반증 | 참인 재검토 0 / 5 | `dialectic/2-synthesis.md` 의 합(合)이 다섯 전부 `아니다` · 대조 둘도 `아니다`. 세 다리(문면 수정·재결박·이슈) 다 안 걸린다. ⚠ 게이트 `:15` 가 이것을 부정한다 — **발견 13** |
| H2 | 통과 | HEAD f1e8cdb | 산출 머리가 명령·HEAD·`cmp` 를 싣는다. 내가 지금 HEAD(`e4176b3`)에서 다시 돌려 **`■ (2)` 는 재현**됐고 스냅샷 줄만 다르다 — **같은 HEAD 로는 안 재봤다**(미측정 목록 2) |

**내가 센 검산** — 통과 **46** · 반증 **2**(`H1`·`B4`) · 대조불가 0 · 미측정 **2**(`B2`·`G5`) = **50**.
**게이트 `## 판정` 이 주장하는 값** — 통과 47 · 반증 1 · 대조불가 0 · 미측정 2 = 50.
**갈리는 자리는 `B4` 하나**다(게이트 통과 ↔ 내 반증).
조건 상자 50 개와 게이트 판정 표 50 개를 **전수 대조했고 ID 집합·판정이 전부 일치**한다(중복 0).

---

## 미측정 목록

| # | 안 잰 조건 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 못 쟀나 |
|---|---|---|---|---|---|---|
| 1 | `G5` — 마지막 커밋 SHA 의 `conclusion=success` | 회차기록 | 참 | 거짓신호 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:364` | 29 커밋이 미push 다. push 는 내 권한 밖이고 읽기 전용이다 |
| 2 | `H2` 의 「같은 HEAD 로 바이트 동일」 | 회차기록 | 추정 | 미관 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:369` | 산출의 HEAD 는 `f1e8cdb` 이고 지금은 `e4176b3` 다. 같은 HEAD 로 되돌리면 작업 트리를 만져야 해서 안 했다 |
| 3 | `A3-b` ⑴ `대상digest차이` 37 칸의 독립 재계산 | 회차기록 | 추정 | 금지역 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/widen/decisions.tsv:1` | base 커밋 투영을 37 번 다시 세워야 한다(`pal radius --dry-run` 37 회). 이번 라운드에 안 돌렸다 — 이 칸이 틀리면 새 `stale` 이 「그때의 값」으로 읽힌다 |
| 4 | 규약 §11 조건 4 의 「그래프를 갱신했다」 | 규약 | 참 | 거짓신호 | `.claude/skills/round/SKILL.md:815` | **실측**: `pal query graph.dump --json` 이 Symbol 3372 · 엣지 6027 만 싣고 **Binding 노드가 0** 이다. 결박의 그래프 반영을 잴 창이 이 빌드에 없다(불변식 ⑦ `not_built` · 소유자가 범위 밖으로 뺐다) |
| 5 | 규약 §11 조건 5 · §10 의 네 이름 | 규약 | 참 | 미관 | `.claude/skills/round/SKILL.md:780` | `report.md` 가 아직 없다(회차 미종료). 네 이름을 게이트·`intent.md`·`state.md`·`approval.md` 에서 grep 하니 **0 건**이라 지금 시점에 위반은 없다 |

---

## 의도 축

### 빠진 것

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 6 | 원문 24 행이 잠근 **㈀+㈁+㈂ 전부** 중 ㈁(불변식 ⑦)이 없고, 뺐다는 소유자 발화가 잠긴 인용 아홉에 **없다**. `pal doctor --json` 으로 확인: 불변식 ⑦ 은 지금도 `not_built`. 해제를 지는 것은 이 회차가 쓴 산문 셋뿐이다. 회차가 그 사실을 스스로 적어 두었으므로 은폐는 아니다 — 그러나 **원 의도의 1/3 이 회차 산출물로만 풀린 상태**는 남는다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:242` | 원의도 | 참 | 거짓신호 | 없음 |
| 7 | 원문 25 행 *"내가 근거를 대고 넓힌다"* 를 재는 `B2` 의 **판정 절차가 없다**. `dialectic/2-design.md:11` 을 grep 해 확인 — 대상이 `C3·H1·A3-b·B2-a` 이고 `B2` 가 빠졌다. 판 2 는 1 라운드로 닫혔고 상한 2 중 1 이 안 쓰였는데 그 라운드가 `B2` 를 안 받았다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:308` | 원의도 | 참 | 거짓신호 | B2 |

### 요구되지 않은 것

없음 — 새로 선 표면(`pal radius`·`--dry-run`·`Binding::with_radius`·`radius_preserves_verdict.rs`·`radius_bench.rs`·검사 12-나·이슈 여덟·결박 둘)을 전수로 훑었고 각각 ㈀·㈂-a·㈂-b·㈄·`G1`·규약 §11 조건 4 에 대응한다. ㈄ 는 소유자가 `approval.md` 에서 *"㈎ 전부 승인 — ㈄ 포함"* 으로 덮었고 실행 커밋 `8ceb8e9` 가 승인 표의 마지막 커밋 `5a5b8ea` 보다 뒤임을 `git log` 로 확인했다. 연결 안 된 표면은 0 이다 — `pal radius` 는 `crates/pal-cli/src/main.rs:581-582` 가 부르고, `with_radius` 는 `crates/pal-cli/src/radius.rs:216` 이 부른다.

### 있는데 틀린 것

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 8 | 원문 27 행이 잠근 *"**사람이** 결박문을 읽고 판정한다"* 를 수행한 것은 서브에이전트 둘이다. 회차가 게이트 `:301-315` 와 `intent.md:384-401` 에 그 치환을 명시했고 되돌릴 문(#149)도 세웠다 — **그러나 치환 자체는 닫히지 않았다.** 소유자도 다른 사람도 일곱 결박문을 읽지 않았고, 그러므로 `H1` 의 「반증」은 서브에이전트의 판정이다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:386` | 원의도 | 참 | 거짓신호 | H1 |

---

## 이번 라운드의 새 발견

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 9 | ★★ **게이트의 첫 문단이 정반합 합(合)이 삭제를 지시한 문장을 그대로 싣고, 같은 게이트가 그것을 부정한다.** `:15` 는 *"넓히지 않은 대조 하나가 「참인 재검토」였다"* 인데 ⑴ `dialectic/2-synthesis.md:143` 이 그 문장을 **지우라고 못 박았다**(*"대조 둘 중 45d8808585ee0d50 에는 「참인 재검토」가 붙는다 … 를 지운다"*) ⑵ 같은 표 `:55` 가 그 건을 **「★ 참 → 아니다로 바꾼다」**로 뒤집었다 ⑶ `dialectic/2-synthesis.md:67` 이 *"대조 둘도 아니다"* 로 닫았다 ⑷ **같은 게이트 `:282` 가 *"참인 재검토 0 건"* 이라 적어 `:15` 와 정면으로 부딪힌다**(1 대 0) ⑸ 그 대조는 **넓혀졌다** — `widen/decisions.tsv:8` 이 `45d8808585ee0d50` 을 `symbol→callers · 감시 1→11` 로 싣고 `observations/2-stale-packet.md:23` 도 같다. 「넓히지 않은」이 거짓이다. `H1` 이 반증인 근거를 그 문단이 스스로 무너뜨린다 | `docs/gates/binding-radius-in-use.md:15` | 회차기록 | 참 | **금지역** (사실이_아닌_것을_사실로) | H1 |
| 10 | `:16` 의 *"**§4** 가 그것을 진다"* 가 **없는 절을 가리킨다.** `grep -n '^## '` 로 확인 — 이 게이트의 `##` 절은 열 개이고 **번호가 하나도 없다**(R1 발견 #1 을 닫으면서 번호를 뗐다). 번호를 뗀 고침이 본문의 `§n` 참조를 같이 안 고쳐 낡음이 표시되지 않는 인용이 남았다 | `docs/gates/binding-radius-in-use.md:16` | 회차기록 | 참 | 거짓신호 | 없음 |
| 11 | ★★ **`## 효과` 가 산출 파일에 없는 수를 그 파일의 수라고 적는다.** `:372-374` 는 *"반경별 벤치를 손으로 돌린 전 출력이 `bench/radius-run.txt` 에 있다 — `changed 1` 에서 색인이 **86.157배** 빠르고 `changed 801` 에서 **0.572배** 다"* 인데, `86.157`·`0.572` 는 **`bench/` 아래 어느 파일에도 없다**. 전 저장소 검색에서 그 두 수가 나오는 곳은 이 게이트 두 줄과 앞 리뷰들의 원 반환문뿐이다. 두 번째 실행이 같은 파일을 덮어써서 첫 실행의 산출이 사라졌고, `radius-run.txt` 의 실제 값은 `114.892`/`0.636`(규모 2000)·`126.898`/`0.658`(규모 8000)이다. **규약 §8 의 효과 증거가 뒷받침 없는 수를 인용한다** | `docs/gates/binding-radius-in-use.md:374` | 회차기록 | 참 | **금지역** (사실이_아닌_것을_사실로) | E3 |
| 12 | ★★ **새로 세운 검사의 모집단 선언이 거짓이다 — 「결박을 정본에 쓰는 경로 전부」가 아니다.** `xtask/src/main.rs:2116` 과 `effect/negative-B5.md:22` 가 그렇게 적는데 ⑴ 훑는 디렉터리가 `pal-cli/src`·`pal-core/src`·`pal-query/src` 셋이라 **`crates/pal-intent/src` 를 안 본다** ⑵ 리터럴이 `intent.record(` 라 `crates/pal-intent/src/store.rs:714` 의 `self.record(&b)`(= `import_jsonl` 의 쓰기 경로)를 **원리상 못 본다.** 그 경로는 `check_budget` 을 안 지나고, **이 회차의 `## 작업 규율` 4 가 바로 그 문을 위험이라 적어 두었다.** `intent.md:213-216`. **격리 사본에 탐침 둘을 심어 확인**: `crates/pal-intent/src/probe_a.rs`(`intent.record(` · 예산 없음)와 `crates/pal-cli/src/probe_b.rs`(`store.record(` · 예산 없음)를 넣어도 그 검사는 **「ok — 결박을 쓰는 경로 3개 · 예산을 안 지나는 것 0개」 그대로**다. ⚠ 바로 위 검사 12(앵커)는 같은 파일에서 **6 디렉터리**를 훑는다(`:2068-2069`) — 비대칭이 같은 함수 옆에 있다. 「전부」를 지운 뒤 모집단을 실제로 넓혀야 한다 | `xtask/src/main.rs:2116` | 자기장치 | 참 | **금지역** (사실이_아닌_것을_사실로) | B5 |
| 13 | `:198` 의 코드블록이 *"손으로 돌린 전 출력"* 바로 아래에서 **첫 실행의 선형성** `3.84·3.87·4.02` 를 싣는다. `bench/radius.tsv` 와 `radius-run.txt` 의 값은 `3.46·3.59·3.60` 이다. `:187` 이 *"첫 실행(3.84·3.87·4.02)과 다르다"* 라 적은 **11 줄 뒤에** 그 첫 실행 값이 현재 산출처럼 다시 놓였다 | `docs/gates/binding-radius-in-use.md:198` | 회차기록 | 참 | 거짓신호 | E2 |
| 14 | `:206-207` 의 「재현」 블록도 첫 실행 수(`86.157` / `0.572`)를 쓴다. 그 블록은 `[f09.4.pass]` ④ 를 `대조불가` 로 두는 **까닭의 재현**이고, 판정을 지는 자리인데 산출과 수가 다르다. 방향(1 을 건너뛴다)은 두 실행에서 같으므로 판정은 안 움직인다 | `docs/gates/binding-radius-in-use.md:206` | 회차기록 | 참 | 거짓신호 | E3 |
| 15 | `:344` 가 *"§㈂-a 의 판정 표(이 게이트 `:138`)"* 라 적는데 그 표는 **`:148-151`** 이고 `:138` 은 산문 한 줄(*"그러므로 두 반경이 갈리는…"*)이다. `D1` 의 오라클이 새로 겨눈 바로 그 표를 가리키는 좌표가 틀렸다. 이 저장소가 `CA2-17`·`CA2-13` 과 R1 의 `rebind.rs:162` 로 **세 번 잡은 형태**다 | `docs/gates/binding-radius-in-use.md:344` | 회차기록 | 참 | 거짓신호 | D1 |
| 16 | `D1` ⑴ 이 요구하는 행(`:150`)의 판정 표기가 **`대조 불가`(띄어쓰기)**이고 까닭 좌표가 **`:715`** 뿐이다(파일명 없음). `intent.md:280-285` 가 표기의 정본을 세운 절이므로 같은 회차 안에서 두 표기가 함께 산다. ⚠ `intent.md:225-227` 의 규칙(*"표준 표와 조건 상자에는 붙여쓰기"*)이 이 표를 면제하므로 `D1` 을 뒤집지는 않는다 | `docs/gates/binding-radius-in-use.md:150` | 회차기록 | 참 | 미관 | D1 |
| 17 | ★ **`intent.md` 의 「합이 원 의도를 덮나」 표가 쪼개졌다 — 마지막 네 행이 표 밖이다.** `:384-401` 에 27 행 치환 산문이 들어가면서 `:402-405`(28 행 · 29 행 · 목적 기여 둘)가 **앞 줄이 산문인 채로 시작한다.** 파이썬으로 전 문서의 표 블록 시작을 검사했고 **이 저장소에서 그 형태는 `:402` 하나뿐**이다(게이트·`state.md`·`approval.md`·`widen/README.md` 는 0). GFM 은 단락 중간에서 표를 시작하지 않으므로 그 네 행은 파이프가 박힌 산문으로 렌더된다. 회차의 **자기 감사 표**이고, 리뷰 프롬프트가 실측 사고로 이름 붙인 *"치환이 절을 삼켰다"* 의 그 형태다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:402` | 회차기록 | 참 | 거짓신호 | 없음 |
| 18 | `state.md` 가 **한 라운드 낡았다.** `## 지금 단계` 제목이 *"독립 리뷰 R1 이 끝났고 R2 부터가 남았다"* 이고 표에 R2 행이 없으며 `4. **독립 리뷰 R2** ← **여기서부터다**` 로 적는데 **R2 는 닫혔다**(`review/r2-raw.md` · 커밋 `cf541a0`·`e4176b3`). 그리고 `## 지금 선 것` 이 *"레코드 **170** 행 — … 독립리뷰 **28**"* 인데 **실측 193 행 · 독립리뷰 51**(R2 의 23 행이 안 반영). 이 파일은 **머리에서 스스로 *"새 컨텍스트가 받는 것은 intent.md 전문 + 이 파일의 요약"* 이라 선언**하므로, 다음 컨텍스트가 R2 를 아직 안 돈 것으로 읽는다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/state.md:9` | 회차기록 | 참 | 거짓신호 | 없음 |
| 19 | `state.md:124` 가 *"레코드의 기계 칸을 손으로 적지 마라"* 를 실패한 접근으로 남겼는데, 위 18 의 낡은 수(170 · 28)가 **손으로 적은 칸**이다. 같은 파일 안에서 규율과 위반이 함께 있다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/state.md:139` | 회차기록 | 참 | 미관 | 없음 |
| 20 | ★ **`B4` 의 새 검산식도 항등식이 아니다 — `CA2-01` 이 고친 것과 같은 자리다.** 오라클은 *"`감시후` 열 합이 `binding.status --json` 의 `watch` 합과 같다"* 인데 **실측 190 ≠ 193** 이다. 어긋남의 원인은 이 회차가 규약 §11 조건 4 를 지키려 자기 결정 둘을 결박한 것(`3a3cb89428ed320b` watch 2 · `6c6e402d958fc5e1` watch 1)이고, 그것은 옳은 일이다. **틀린 것은 「명령 산출 전체와 37 행 합을 같다고 둔 산식」**이다. `docs/plan/02-order.md` 의 새 절은 두 모집단(190/193)을 이미 갈라 적었는데 `B4` 의 오라클만 안 갈렸다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:312` | 회차기록 | 참 | 거짓신호 | B4 |
| 21 | `G1` 이 세운 이슈 넷 중 **#146(sunset 트리거)·#147(extract.py 파서)의 번호가 게이트·`intent.md`·`state.md` 에 0 건**이다(grep 실측). 게이트 `## 범위 밖` 의 링크 목록은 #145·#148·#149·#150·#151·#152 여섯만 싣는다. 두 번호는 `findings.jsonl` 과 앞 리뷰 원 반환문에만 있어서 `G1` 의 *"번호가 산출에 남는다"* 는 **문면으로는 서지만** 게이트를 읽는 사람은 넷 중 둘을 못 찾는다 | `docs/gates/binding-radius-in-use.md:424` | 회차기록 | 참 | 거짓신호 | G1 |
| 22 | `intent.md:227` 이 판정 토큰의 정본을 `round_condition.rs:41-47` 로 적는데, 같은 문서 `:281-284` 의 ⟨`CA2-13`⟩ 정정이 정본을 **`crates/pal-intent/src/round_condition.rs:39-49`** 로 바꿨다. `enum Verdict` 는 실제로 `:39-49` 다(`:41-47` 은 변형 넷만). **정정이 `## 표기` 절까지 안 갔다** — 한 문서가 같은 정본을 두 좌표로 가리킨다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:227` | 회차기록 | 참 | 미관 | 없음 |

---

## 자기 산출에 대한 발견

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 23 | 새 검사의 술어가 **파일 단위**다 — 한 파일이 아무 곳에서든 `check_budget(` 을 부르면 그 파일의 모든 `intent.record(` 가 통과한다. 지금은 헛되지 않다(`narrative.rs:584` 의 `결박한다` 를 `:420`·`:505` 가 둘 다 지난다 — 읽어 확인했다). 그러나 앞으로 같은 파일에 예산을 안 지나는 쓰기 경로가 생기면 **검사가 조용히 통과한다** | `xtask/src/main.rs:2137` | 자기장치 | 추정 | 미관 | B5 |
| 24 | 두 번째 벤치 실행이 **첫 실행의 산출 파일을 덮어썼다.** 그래서 게이트가 인용하는 `86.157`/`0.572`/`3.84`/`3.87`/`4.02` 를 지는 산출이 저장소에 없다(발견 11·13·14 의 뿌리). 정정 자체는 재는 양을 늘렸지만 **옛 측정을 나란히 남기지 않아 그 측정을 인용한 문장들이 고아가 됐다** | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/bench/radius-run.txt:6` | 자기장치 | 참 | 거짓신호 | E1 |
| 25 | `bench/radius.tsv` 에 `E2` 오라클이 요구한 네 열 말고 다섯째 열 `색인이득배` 가 있고 `①②③`·`④-나` 행에서 `—` 다. 무해하지만 등록된 열 목록 밖이라 다음 회차가 스키마로 읽으면 어긋난다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/bench/radius.tsv:1` | 자기장치 | 참 | 미관 | E2 |

---

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 아니었나 |
|---|---|---|---|---|---|---|
| 26 | `D2` ⑴ 의 `grep -F` 가 실패한다 — 조건이 인용한 어구 *"이 코퍼스에서 두 반경이 같은 결박을 켰고"* 는 새 게이트에 **0 건** | 회차기록 | 거짓 | — | `docs/gates/binding-radius-in-use.md:127` | 게이트는 같은 까닭을 `docs/gates/F09.md:336-344` 에서 **축자 인용**하고 그 안에 `이 코퍼스에서` 가 든다(1 건). `F09.md:79` 와 `:336-344` 는 같은 진술의 두 자리이고 둘 다 실재함을 읽어서 확인했다. 오라클의 자연 독법으로 충족된다 |
| 27 | 승격 칸 1 의 소유자 답(*"표본기가 `f09-verify.py:715` 에서 그 축을 못 본다"*)이 `## 차선책` 의 정정(*"「구조적으로 못 본다」는 거짓이다"*)과 부딪힌다 | 회차기록 | 거짓 | — | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:434` | 정정이 무너뜨린 것은 **전역 주장**(표본기 전체가 그 축을 못 본다)이고, 소유자의 말은 **`:715` 한정**이다. `scripts/f09-verify.py:715` 의 갈래 필터가 `stale`·`orphaned` 만 담는 것은 참이고 `:709-711` 이 읽는 것과 양립한다. 부딪히지 않는다 |
| 28 | `A3-a` 가 음성 대조로 약하다 — 사본의 `pal radius` 를 고치지 않고 `pal bind` 를 대리 경로로 썼다 | 회차기록 | 거짓 | — | `crates/pal-cli/tests/radius_preserves_verdict.rs:239` | 시험 문서 주석 `:228-237` 이 치환을 표로 밝히고, 같은 픽스처에서 **두 판정이 실제로 갈린다**(base→`stale` · HEAD→`fresh`)를 둘 다 assert 한다. 기준 시점 선택이 산출에 차이를 만든다는 것을 재고 있다 |
| 29 | 게이트 §`ADR-0014` 의 *"값: 5/5 = 100%"* 가 같은 게이트의 `[f09.pass]` ⑤ `대조불가` 와 부딪힌다 | 회차기록 | 거짓 | — | `docs/gates/binding-radius-in-use.md:243` | `:157-160` 이 두 수의 모집단 차이를 적고 *"등록된 30% 상한을 구조가 먼저 깨뜨린다 … 그것도 ⑤ 를 `대조불가` 로 두는 까닭에 든다"* 로 잇는다. 100% 를 ⑤ 의 측정값으로 내세우지 않는다 |
| 30 | `pal radius --to symbol` 이 감시를 안 줄여 「`symbol` 이라 적혔는데 감시 78」인 결박이 남았다 | 저장소 | 거짓 | — | `crates/pal-core/src/binding.rs:433` | **실측**: 37 부분집합의 `symbol` 19 건이 **전부 watch 1** 이다. 그 상태의 결박이 이 회차에 0 건이다. `C4` 의 정정이 적은 위험은 실현되지 않았다 |
| 31 | 저장소 뿌리의 `NEXT-E-check-verifies-form-not-work.md`·`NEXT-E-prompt.md` 가 이 회차가 남긴 비계다 | 저장소 | 거짓 | — | `NEXT-E-prompt.md:1` | `git log` 로 확인 — `8f182d6`·`213d3ee`(#114 회차)가 만든 것이고 `1276b8f..HEAD` 범위 밖이다 |
| 32 | `pal query graph.dump` 가 결박을 안 실어 §11 조건 4 를 위반한다 | 저장소 | 거짓 | — | `crates/pal-query/src/lib.rs:849` | 불변식 ⑦(2층 결박 색인)이 `not_built` 이고 그것은 **소유자가 인터뷰 라운드 3 에서 범위 밖으로 뺀 것**이며 `G1` ⓐ(#145)가 이슈로 세웠다. 이 회차가 만든 결핍이 아니다 — 미측정 목록 4 로 옮겼다 |
| 33 | `findings.jsonl` 의 `정반합` 라운드 값이 판 번호와 라운드 번호를 한 칸에 섞는다(판 1 은 실제로 2 라운드 돌았는데 레코드는 R1·R2 가 판 1·판 2 다) | 회차기록 | 거짓 | — | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/findings.jsonl:1` | `cargo xtask check` 의 「회차 레코드」가 **정반합R1 22↔22 · 정반합R2 31↔31** 로 검산을 통과한다. 판마다 취합 보고를 하나 내는 관행이 저장소 전체(23 회차)에 걸쳐 일관되고 이 회차가 만든 규칙이 아니다 |

---

## 끝내도 되는가

**안 된다.**

본 목록에 **금지역 셋**이 남았다 — 발견 **9**(게이트 첫 문단이 정반합 합(合)이 삭제를 지시한 문장을 싣고 같은 게이트 `:282` 와 1 대 0 으로 부딪힌다 · 게다가 그 대조는 실제로 넓혀졌다) · **11**(`## 효과` 가 `bench/radius-run.txt` 에 없는 수 86.157/0.572 를 그 파일의 수라고 적는다) · **12**(새 검사의 모집단 선언 *"결박을 정본에 쓰는 경로 전부"* 가 거짓이고, 격리 사본의 탐침 둘이 초록으로 통과했다).

그리고 **반증 하나가 새로 섰다** — `B4` 의 검산식이 190 ≠ 193 으로 안 맞는다(발견 20). 규약 §11 의 해악 게이트는 금지역과 실패를 **교착**으로 정한다.

세 금지역은 셋 다 **한 문장·한 좌표·한 줄**로 닫힌다(`:15-16` 을 합(合)의 지시문으로 교체 · `:206`·`:374`·`:198` 의 수를 `radius-run.txt` 의 값으로 교체 · `xtask/src/main.rs:2116` 의 「전부」를 걷고 디렉터리·리터럴 모집단을 넓히기). 발견 20 은 `B4` 의 오라클을 「37 행 집합에 대해」로 한정하면 닫힌다.

⚠ 규약 §11 조건 2·6 도 아직 안 섰다 — 독립 리뷰 상한은 4 이고 이것이 R3 다. `G5` 는 29 커밋이 미push 라 원리상 아직 판정되지 않는다.