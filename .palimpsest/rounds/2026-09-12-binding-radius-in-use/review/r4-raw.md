# 독립 리뷰 — 라운드 4 · 2026-09-12-binding-radius-in-use

<!-- 이 파일은 원 반환문이다. 메인이 요약하거나 고치지 않는다 — 합계 검산이 독립된 둘째
     원천을 대야 하기 때문이다. `pal-independent-reviewer` 가 냈고, 읽기 전용이라 파일을
     못 써서 전문을 반환문에 실었고 메인이 옮겼다. 판정도 순서도 문면도 그대로다.
     메인이 옮기면서 한 것은 ① 이 제목과 주석을 단 것 ② 반환문 머리의 인사 한 줄을 뺀 것뿐이다.

     ⚠ 리뷰어가 안 받은 것: 대화 기록 · 메인의 사고 과정 · 왜 그렇게 만들었는지의 설명 ·
     앞 라운드들의 판정 시드. `review/r1-raw.md`·`r2-raw.md`·`r3-raw.md` 는 **읽지 않았다**고
     스스로 선언한다.

     ★★ 이것이 등록된 상한 4 의 마지막 라운드다. -->

> 판정 대상 HEAD `8a0aa67` · 2026-09-13 · 낸 자 `pal-independent-reviewer`

## 내가 받은 것 · 안 받은 것

**받았다:** 저장소 `/Users/incognito/dev/projects/palimpsest` 의 파일들 — 잠긴 의도 `​.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md` 전문(460 줄) · 게이트 `docs/gates/binding-radius-in-use.md`(574 줄) · 회차 산출물 전부(`approval.md`·`state.md`·`findings.jsonl`·`widen/`·`bench/`·`effect/`·`observations/`·`dialectic/`·`premortem/`·`conditions-audit/`) · `crates/` · `xtask/` · `docs/` · `scripts/` · `.claude/skills/round/` · 커밋 범위 `1276b8f..HEAD`(44 커밋).

**안 받았다:** 대화 기록 · 메인의 사고 과정 · 왜 그렇게 만들었는지의 설명 · 앞 라운드들의 판정 시드.

⚠ `review/r1-raw.md`·`r2-raw.md`·`r3-raw.md` 는 읽지 **않았다.** `dialectic/r3-raw.md`(취합 보고)와 `dialectic/3-*.md` 는 이 라운드의 판정 대상이라 읽었다. 아래 모든 판정은 내가 직접 재서 낸 것이고, 저 세 파일의 판정을 근거로 인용한 자리는 없다.

## 내가 실제로 돌린 것

```
cargo xtask check                                   → 검사 29/29 통과 · rc=0 (전 출력 파일로)
cargo xtask test                                    → test result 54 줄 · passed 1065 · failed 0 · rc=0
find . -name '*.rs' -not -path './target/*' | xargs touch
cargo build --release --workspace                   → Compiling 8 크레이트 · warning 0 · rc=0
find . -name '*.rs' ... | xargs touch ; cargo check --workspace --all-targets
                                                    → Checking 7 · warning 0 · rc=0 (3m45s 전량)
pal query binding.status --json                     → 결박 39 · radius symbol 19 callers 20 · watch 합 193
pal touch extract_with                              → 「■ 이 좌표를 지켜보는 것 (2)」
git merge-base --is-ancestor fd176a8 e16dc61        → rc=0
git merge-base --is-ancestor 3b9c59b 7d0ae43        → rc=0
git show 67e0640:.../bench/radius-run.txt | diff -  → bench/radius-run-first.txt 본문과 0 줄 차이
git show fd176a8:.../widen/decisions.tsv           → 지금과 바뀐 셀 1 (판정·근거 열은 0)
git show 1276b8f:crates/pal-core/src/binding.rs | sed -n 504p
gh api --method POST /markdown (mode=gfm)           → intent.md 승격 표·덮개 표·개정 행을 실제 렌더
python3 — decisions.tsv 전수 집계 · 패킷 대조 · P 참/거짓 집합 재계산 · 표 칸 수 스캔
gh issue view 145..154 / 58 --comments
```

**작업 트리:** `git status --porcelain` 이 **0 줄**이다(시작·끝 두 번 확인). HEAD `8a0aa67`. 파일을 한 글자도 고치지 않았고 커밋도 하지 않았다. ⚠ **다만 `G4` 의 등록된 오라클대로 `.rs` 전량의 mtime 을 두 번 갱신했다** — 내용 변경이 아니고 `git status` 는 비어 있다. 임시 파일은 세션 스크래치패드에만 썼다.

## 규약 §11 여섯 조건 · §10 네 금지 절 — 지금 어디 있나

| 조건 | 지금 |
|---|---|
| 1 판정을 커밋했다 | **섰다** — 게이트와 조건 상자 50 이 맞물리고 `원장 둘 대조` 가 이 회차를 `(50개)` 로 훑는다 |
| 2 독립 리뷰가 닫혔다 | **이 라운드가 상한 4 의 마지막**이다. 판단은 메인이 한다 |
| 3 효과 | **섰다** — `pal touch extract_with` 을 내가 다시 돌려 `(2)` 를 봤다 |
| 4 결박 + 그래프 갱신 | 앞 절반 **섰다**(결박 39 · 새 둘 다 `callers` · 회차의 두 결정). 뒤 절반은 게이트가 `## 범위 밖` 에서 원리상 못 잰다고 적는다 |
| 5 종료 보고에 네 이름이 없다 | **대조 불가** — `report.md` 가 없다. §10 이 그 자리를 `report.md` 로 못 박았으므로 네 금지 절(의도적으로 안 한 것 · 확인 못 한 것 · 추론 · 다음으로 넘기는 것)의 유무를 지금 잴 수 없다 |
| 6 push + CI 초록 | **안 섰다** — `origin/main..HEAD` 가 **44 커밋**이다. `G5` 가 그것을 진다 |

---

## 합격선 축

게이트 `## 판정` 이 주장하는 값은 **통과 47 · 반증 2 · 대조불가 0 · 미측정 1 = 50** 이고, **내가 잰 값도 같다.** 조건 ID 집합도 `intent.md` 의 체크박스 50 과 전수 일치했다(파이썬 집합 비교 · 누락 0 · 초과 0).

| 조건 | 판정 | 잰 수 | 근거 |
|---|---|---|---|
| A1 | 통과 | 시험 1 통과 | `cargo xtask test` 에서 `반경만_바뀌고_여섯이_보존된다 ... ok`. 시험 본문이 여섯 칸 바이트 동일 + 옛 쌍 부분집합 + **감시가 실제로 커졌다**는 반공전 assert 를 함께 진다 |
| A1-a | 통과 | 시험 1 통과 | `pal_bind_재호출은_여섯을_안_보존하고_stale_을_지운다 ... ok` |
| A2 | 통과 | 시험 1 통과 | 같은 시험 파일. 착수 `stale` 7 이 넓힌 뒤 12 안에 전부 든다(내 `binding.status` 집계로 재확인) |
| A2-a | 통과 | 시험 1 통과 | `A1-a` 와 같은 시험이 이 축도 진다 |
| A3 | 통과 | 시험 1 통과 | `새_감시_원소의_기준은_bound_at_의_base_커밋이다 ... ok` · 픽스처가 `stale` + `triggered_by` 1 + 대상 아님을 assert |
| A3-a | 통과 | 시험 1 통과 | `기준을_head_로_읽으면_fresh_가_난다 ... ok` |
| A3-b | 통과 | 37 행 전수 | `widen/recompute-A3b.tsv` 를 내가 집계 — 37/37 「같다」 · `대상digest차이 참` 1 · `base에없던새원소` 합 10, tsv 값과 재계산 값이 모든 행에서 일치 |
| A4 | 통과 | 시험 1 통과 | `없는_결박을_지목하면_실패한다 ... ok` |
| A5 | 통과 | 산출 이월 | `effect/rerun-widen.txt` 가 임시 저장소 재현과 바이트 동일을 적는다. **내가 재실행하지는 않았다** |
| A5-a | 통과 | 검사 1 초록 · 등록 4 | 검사 12 가 29/29 안에서 초록. `WATCH_ENTRY_SITES` 에 `crates/pal-cli/src/radius.rs` 가 사유 *"base 커밋 투영에서 읽어 만든다"* 로 등록됨. `radius.rs:209` 에 리터럴 실재 |
| A6 | 통과 | grep 1건 · diff 0건 | `crates/pal-cli/src/main.rs:140` 에 `default_value = "symbol"` 그대로. `git diff 1276b8f..HEAD` 에 그 줄 없음(diff 안의 `default_value` 는 `"."` 하나뿐) |
| B1 | 통과 | 37 행 · 14 열 | `head -1` 로 열 14 확인 · 데이터 행 37 |
| B1-a | 통과 | 산출 이월 | `effect/negative-B1.md` |
| **B2** | **반증** | 37 중 P 거짓 **22** | **내가 정본에서 전수 재계산해 판 3 의 수를 재현했다** — P 참 15 / P 거짓 22 · 안 넓힌 쪽 16 · **넓힌 쪽 6** · 호출자>0 인 24 행이 **12 대 12** · 호출자 0 인 13 행이 3 대 10 · 꼬리 7 행이 3 대 4 · 계층 S1 6/6 · S2 4/7 · S3 10/13 · S4 2/11. 게이트가 적은 다섯 수 전부가 `widen/decisions.tsv` 에서 선다 |
| B2-a | 통과 | 커밋 2 · rc=0 | `fd176a8`(표) 가 `e16dc61`(넓힘) 의 조상. 그리고 `fd176a8` 이후 바뀐 셀이 **1** 이고 `판정`·`근거` 열은 **0** — 금지역 ㉮ 가 지켜졌다 |
| B3 | 통과 | 3/3 축 | ⑴ `callers` 20 실재 ⑵ 감시 합 190 > 37 ⑶ `pal touch extract_with` 이 **(2)** — 내가 직접 돌렸다 |
| B3-a | 통과 | 관측 1 | `observations/red-B3.txt` 가 37/37 symbol · 합 37 · fresh 30 stale 7 · touch 0 을 넓히기 전에 진다 |
| B4 | 통과 | 190 = 190 · 7 | `감시후` 열 합 190 과 명령이 돌려준 그 37 건의 `watch` 합 190 이 같다(정본 전체는 193 — 두 모집단이 갈려 적혀 있다). `새반경==callers` 이고 `감시전==감시후` 인 행 **7** |
| B5 | 통과 | 모집단 4 · 통과 3 · 면제 1 | 검사 「결박을 저장하는 경로가 예산을 지난다」가 초록이고 **0 개를 훑지 않는다**. 39 × 78 = 3,042 로 상한 1,000,000 에 안 닿음. 음성 대조 rc=1 은 `effect/negative-B5.md` 이월 |
| B6 | 통과 | diff 20+/18- | `git diff 1276b8f..HEAD -- .palimpsest/intent/bindings.jsonl` 이 비어 있지 않다 |
| B6-a | 통과 | 산출 이월 | `effect/negative-B6.md` |
| B7 | 통과 | ⑴ 0 행 | `판정==아니다` 이면서 `새반경!=옛반경` 인 행 **0**(모집단 37 에서 실측). ⑵ 는 모집단 0 으로 공전 — 조건 문면이 그 사실을 이미 적는다 |
| B8 | 통과 | 112 | `타파일새원소` 합 112 · `observations/after-widen.txt:150` 에 실렸다 |
| C1 | 통과 | fresh 25 / stale 12 | 내 `binding.status` 집계가 그 37 건에서 fresh 25 · stale 12 · undeterminable 0 · orphaned 0. 바뀐 5 건의 id 집합이 `after-widen.txt` 표와 전수 일치 |
| C2 | 통과 | 0 | `status.code.reason == watch_member_gone` 집계 **0**. 0 이어도 적혀 있다 |
| C2-a | 통과 | 시험 1 통과 | `binding::tests::사라진_감시_원소가_변한_것을_가린다 ... ok` |
| C3 | 통과 | 5 건 판정 | 판 2 가 다섯을 결박문으로 판정해 전부 `아니다`. `A3-b` ⑴ 이 참인 행(`49714573e0fa654c`)은 **안 넓혀졌으므로** 「따로 표시」 모집단이 0 이고 그 사실이 정합이다(내가 확인) |
| C4 | 통과 | 0 ≤ 5 | undeterminable 로 옮겨간 것 0 · 새 stale 5. 내 집계로 재현 |
| D1 | 통과 | grep 1 · 검사 1 | 게이트 §⑤⑥ 양립 표에 `[f09.pass]` ⑤ 가 `대조불가` 로 실리고 **까닭의 좌표 `scripts/f09-verify.py:715` 가 같은 행**에 있다. 「원장 둘 대조」 초록 |
| D1-a | 통과 | 산출 이월 | `effect/negative-D1.md` |
| D2 | 통과 | grep 2/3 문자열 | `scripts/f09-verify.py:715`·`:709` 각 2 건이고 그 두 좌표가 실제로 맞다(내가 `sed` 로 확인). ⚠ **오라클 ⑴ 은 grep 으로 안 맞는다** — 게이트는 옛 까닭을 `F09.md:336-344` 에서 인용하고 조건이 지목한 `F09.md:79` 의 좌표·문구는 **0 건**이다. 본체(까닭 정정)가 섰으므로 통과로 두었고 그 어긋남은 아래 발견 8 로 올린다 |
| D3 | 통과 | diff 0줄 · grep 3 · 코멘트 2 | `git diff -- docs/gates/F09.md` 가 **0 줄** · 게이트에 `docs/gates/F09.md` 3 건 · `gh issue view 58 --comments` 에 새 게이트 경로 2 건 |
| D4 | 통과 | 같은 절 2건 | 게이트 `### ⑤ 와 ⑥ 이 양립하는 까닭` 표에 ⑤·⑥ 이 나란히 있고 양립 문장이 그 절에 있다 |
| E1 | 통과 | 14 행 · `#[ignore]` | `bench/radius.tsv` 데이터 행 14 = 6 + 8. `crates/pal-cli/tests/radius_bench.rs:242` 에 `#[ignore]`. 넷째 팔의 대리물 선언이 산출 머리에 있다 |
| E2 | 통과 | 비 7/7 재계산 | 최솟값·분산·두규모의비·changed 네 열 실재. 표의 비 일곱을 내가 나눗셈으로 다시 계산해 전부 일치(3.46·3.59·3.60·0.91·1.00·0.96·0.99) |
| E3 | 통과 | 1 을 건너뛴 비 2쌍 | 2000: 114.892 → 0.636 · 8000: 126.898 → 0.658. 색인이득배 네 값도 내 나눗셈과 일치 |
| E4 | 통과 | tsv 5/5 | 회차 디렉터리의 `.tsv` 다섯 전부 첫 줄이 탭으로 가른 열 이름 |
| E5 | 통과 | `.json` 0건 | `find ... -name '*.json'` 이 **0**. sunset 검사 초록 |
| E6 | 통과 | grep 0건 | `grep -F -- '--ignored' .github/workflows/ci.yml xtask/src/main.rs` 가 **rc=1 · 0 건**. 게이트 `## 범위 밖` 에 문장 있고 `## 효과` 에 벤치 출력 있다 |
| F1 | 통과 | grep 4/4 | `docs/plan/02-order.md:147`(옛 `:110`) 에 `F05`·`F13`·`F15`·`binding.rs:504` 문자열 전부 있고 `git diff` 에 그 줄이 든다. ⚠ 문자열은 맞고 **그 좌표가 가리키는 곳이 빈 줄이다** — 발견 5 |
| F2 | 통과 | diff hunk 3개 | `git diff 1276b8f..HEAD -- docs/plan/02-order.md` 의 hunk 가 `@@ -68`·`@@ -106`·`@@ -125` 셋이고 `:53` 이 어디에도 안 든다 |
| F3 | 통과 | 삽입 위치 확인 | 새 절 `### 그 뒤 무엇이 바뀌었나 — C6 행이 움직였다` 가 `:69` 뒤 · `### 실측이 갈린 자리 셋` 앞에 섰고 `:67-69` 는 문맥 줄로만 나온다 |
| F4 | 통과 | grep 0건 | `grep -F "파일 안의 관계만" docs/plan/02-order.md` 가 **0**. `:68` 은 diff 에 문맥 줄로만 든다 |
| G1 | 통과 | 이슈 4/4 OPEN | `gh issue view` 로 #145·#146·#147·#148 전부 OPEN 확인. 번호가 게이트·의도에 남았다 |
| G2 | 통과 | **29/29 · rc=0** | 내가 직접 돌렸다. 착수값 28 이상. 「원장 둘 대조」가 이 회차를 `(50개)` 로 훑는다 |
| G3 | 통과 | **passed 1065 · failed 0** | 내가 직접 돌렸다. `^test result:` 54 줄 합산 · 착수값 1047 이상 · rc=0 · 전 출력을 파일로 받아 `error[E`·`panicked` 스캔 0건 |
| G4 | 통과 | **warning 0 · 0** | mtime 전량 갱신 후 `cargo build --release --workspace`(8 크레이트 재컴파일) warning 0, 다시 갱신 후 `cargo check --workspace --all-targets`(7 크레이트) warning 0 |
| **G5** | **미측정** | 미push 44 | `git rev-list --count origin/main..HEAD` = **44**. HEAD 에 붙은 런이 없다 |
| **H1** | **반증** | 참인 재검토 0 | 판 2 가 다섯 + 대조 둘을 전부 `아니다`. 세 다리(결박문 고침·재결박·이슈) 중 어느 것도 「가리킨 결정」을 주체로 안 갖는다 — 게이트가 그 까닭을 진다 |
| H2 | 통과 | 머리 3필드 | `effect/touch-extract_with.txt` 머리에 명령 전문·HEAD `f1e8cdb`·종료값 0·재실행 대조가 적혀 있다. ⚠ **바이트 동일성은 내가 재현 못 했다** — HEAD 가 `8a0aa67` 로 옮겨져 같은 HEAD 가 아니다(내 재실행은 스냅샷 다이제스트·파일 수만 달랐다) |

### 판 3 자신에 대한 내 측정 — 이 라운드가 채운 자리

- **술어 P 가 문면으로 선다.** `dialectic/3-design.md:96` 의 ⒝ 가 *"**의존의 방향**(부르는 쪽이 변하면 이 결정이 어떻게 되나)이 문면에 서야 한다"* 이고, 합(合) R2 가 고정한 기준이 **그 문면 그대로**다. 설계문과 합(合) 둘이 쓴 기준이 같다.
- **여섯 행의 `P 거짓` 이 문면에 선다.** `45d8808585ee0d50`·`5b9331b398e8ff23`·`ce25c6f23f0a8992`·`ddb749e489242cab`·`fcd48a9033fd1fb0`·`fd4efc1eaea040c4` 의 `근거` 정본을 `widen/decisions.tsv` 에서 직접 꺼내 대조했다 — 인용이 **여섯 다 정본과 글자까지 같고**, 다섯은 관계가 「이 결정 → 부르는 쪽」이며 `fcd48a9033fd1fb0` 은 호출 경계가 문면에 항으로 **없다**. 판정이 문면에 선다.
- ★ **위조 대조 셋의 변별을 내가 채웠다 — 심판이 `미대조` 로 남긴 자리다.** 진짜 37 행의 `근거` 와 그 행 결박문 사이의 고유 토큰 겹침을 계산하면 **최솟값 0.27**, 위조 ㉠(측정값 재진술) **0.00**, 위조 ㉢(다른 결박의 근거 이식) **0.06** 이다. ㉢ 의 이식 문면에 든 `ExportSet`·`stitch_of`·`EXPORTS`·`export_digest`·`ledger`·`소비 쪽`·`모집단` **일곱이 전부** 대상 결박문에 없다. 위조 ㉡ 은 겹침 0.33 으로 이 기계 대조가 못 잡지만 술어 ⒜ 의 명시 제외 목록(심볼 이름·파일 경로)이 잡는다. **셋 다 `P 거짓` 이 독립으로 선다.**
- ★ **⒜·⒟ 의 37/37 「이월(R1)」도 내가 재보니 선다.** ⒜ — 37 행 전부가 자기 결박문과 25% 이상 고유 토큰을 공유한다(겹침 0 인 행 0). ⒟ — `판정==아니다` 19 행 중 `근거` 에 호출자 부재 어휘(`호출자`·`부르는 데가 없다`·`쓰이지 않`·`참조가 없`)를 쓴 행이 **0** 이고, `⟨호출자 0 …⟩` 꼬리는 `영향받는다` 7 행에만 붙어 있다.
- **판 3 의 재료 공급이 정직하다.** `observations/3-b2-packet.md` §2 의 37 행이 `decisions.tsv` 의 14 열과 **37/37 전 칸 일치**하고, §3 의 결박문 37 건이 `.palimpsest/intent/bindings.jsonl` 의 `note` 와 **37/37 문면 일치**한다. 패킷 커밋 `3b9c59b` 가 정(正) 커밋 `7d0ae43` 의 조상이다.
- **접기 규칙이 사전 등록되고 그대로 적용됐다.** 규칙 다섯 줄을 위에서 아래로 대면 2 번(`P 거짓` 하나라도)에서 `반증` 이 걸린다. 「부분 통과」가 안 쓰였다.
- **게이트가 갈림과 절차 공백 셋을 정직하게 적었다.** `## 판정` 의 표가 합(合) `반증` · 종료 판단 `미측정` · 소유자 `반증` 셋을 나란히 싣고, 그 아래 ★ 문장이 `미대조` · `이월(R1)` · *"상한 2 가 R2 의 합 산출물을 받을 반(反) 자리를 못 남겼다"* 셋을 이름으로 적는다.
- **R3 이 세운 것 둘도 섰다.** `bench/radius-run-first.txt` 의 본문이 `67e0640:bench/radius-run.txt` 와 **0 줄 차이**(충실한 되살림)이고, 예산 검사의 면제는 이름·사유·이슈(#153)가 붙어 있으며 모집단 4 중 3 을 실제로 재므로 **죽은 가지가 아니다.**

---

## 미측정 목록

| # | 안 잰 조건 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 못 쟀나 |
|---|---|---|---|---|---|---|
| 1 | `G5` — 회차 마지막 커밋 SHA 에 `conclusion=success` 런이 붙었다 | 회차기록 | 참 | 미관 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:383` | push 가 아직 없다. `git rev-list --count origin/main..HEAD` 가 44 이고 CI 는 `cancel-in-progress: true` 라 마지막 push 뒤에만 판정된다. 게이트의 미측정 선언과 같다 |

---

## 의도 축

### 빠진 것

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 2 | 원문 24 행이 잠근 「㈀ + ㈁ + ㈂ 전부」의 ㈁(불변식 ⑦)이 없고, 빠졌다는 사실을 지는 것이 이 회차가 쓴 편집자 산문 셋뿐이다 — 되받는 소유자 발화가 원장에 없다. 게이트가 스스로 *"원문 24 행이 잠근 집합의 1/3 이 회차 산출물로만 해제된 상태다"* 로 적으므로 은폐는 아니지만 **의도 축의 구멍으로 HEAD 에 남아 있다** | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:261` | 원의도 | 참 | 거짓신호 | 없음 |

### 요구되지 않은 것

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 3 | `bench/radius.tsv` 에 `E2` 가 등록한 네 열 밖의 다섯째 열 `색인이득배` 가 있다. 게이트가 미관으로 자진 신고했고 그 열이 `E3` 의 뒤집힘을 직접 싣는 자리이므로 해로움은 낮다 — 다음 회차가 이 파일을 스키마로 읽을 때만 걸린다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/bench/radius.tsv:1` | 자기장치 | 참 | 미관 | E2 |

### 있는데 틀린 것

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 4 | 원문 27 행이 잠근 *"사람이 결박문을 읽고 판정한다"* 를 실제로 수행한 것은 **서브에이전트 둘**이다. `widen/README.md` 는 판정자를 *"서브에이전트 하나"* 로, 판 2·판 3 은 각각 `pal-decision-synthesizer` 로 머리에 밝힌다 — 소유자도 다른 사람도 결박문을 읽지 않았다. 게이트가 치환을 적었으므로 정직하지만, 원문이 잠근 판정자와 실제 판정자가 다르다는 사실 자체는 HEAD 에 그대로 있다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:401` | 원의도 | 참 | 거짓신호 | C3 |

★ 원문 **25 행**(*"근거를 대고 넓힌다"*)과 **29 행**(효과의 증인)의 어긋남은 위 표에 다시 올리지 않았다 — **합격선 축이 `B2` · `H1` 을 각각 `반증` 으로 지고 있고 그것이 같은 사실이다.** 두 축에 같은 항을 두 번 세우면 셈이 부푼다.

---

## 이번 라운드의 새 발견

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 5 | **살아 있는 계획 문서가 빈 줄을 인용 출처로 적는다.** `docs/plan/02-order.md` §2 3 번 행이 *"`crates/pal-core/src/binding.rs:504` 가 「StaleDerived — 파생 입력을 주는 것이 이 빌드에 없다」로 적는다"* 라고 단정하는데, **지금 그 줄은 빈 줄**이고 인용문은 `:547` 에 있다. 까닭이 이 회차 안에 있다 — 착수 `1276b8f` 에서는 `:504` 가 그 인용 블록의 둘째 줄이었고, **이 회차의 커밋 `750e3ea` 가 같은 파일에 176 줄을 더해 좌표를 43 줄 밀어 놓았다.** 그 뒤 `8ceb8e9`(㈄)가 **이미 낡은 좌표**를 계획 문서에 새겼다. 그리고 `F1` 의 등록된 오라클이 `grep -F 'binding.rs:504'` 를 요구하므로 **그 거짓 좌표의 존재가 초록으로 인증된다.** 같은 좌표가 게이트 2 곳과 `intent.md` 5 곳에 있고, 그중 `intent.md:132`·`:428` 은 「불변식 ⑧ 은 원리상 못 연다」의 **유일한 사유 담지자**다. 이 저장소가 `CA2-17`·`CA2-13`·`rebind.rs:162` 로 세 번 잡은 형태의 **네 번째**이고, 처음으로 살아 있는 계획 문서에 들어갔다 | `docs/plan/02-order.md:147` | 원의도 | 참 | **금지역** (사실이_아닌_것을_사실로) | F1 |
| 6 | **`## 승격` 표의 「소유자의 답」 칸에 렌더되는 것이 메인의 「내 판정 초안」이다.** 승격 3·4·5·6 행이 **헤더 3 칸에 데이터 4 칸**을 넣었고, GFM 은 초과 칸을 버린다. GitHub 자신의 `/markdown` API 에 **실제 파일 여덟 줄을 그대로 넣어 렌더**해 확인했다 — 렌더된 표의 모든 행이 3 칸이고, 행 3·4·5·6 의 마지막 칸이 *"내 판정 초안 — …"* 이며 소유자의 답 문자열(`초안과 같다`·`반증 으로 잠근다`)은 **HTML 에 존재하지 않는다.** 그러므로 렌더로 읽는 사람에게는 **㈏ 기준시점 선택 · ㈎ 손으로 돌림 · ㈎ 다시 쓰지 않음 · `B2` 를 `반증` 으로 잠금** 네 소유자 결정이 전부 사라지고, 그 자리에 에이전트의 초안이 「소유자의 답」이라는 머리 아래 앉는다. `approval.md` 는 **의도 승인만** 싣고 승격 3~6 의 답을 안 싣는다. 이 회차의 R1 이 *"승인 산출물이 없는데 게이트가 승인을 받았다고 적었다"* 를 금지역으로 닫은 것과 **같은 병이 거울상으로** 돌아왔다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:459` | 회차기록 | 참 | **금지역** (사실이_아닌_것을_사실로) | 없음 |
| 7 | **㈄ 가 바이트로 보존한 §1 실측표의 한 행이 열을 한 칸씩 밀어 렌더한다.** `docs/plan/02-order.md:50`(`C3` 진입점 행)이 백틱 안의 `grep -cE '…'` 때문에 헤더 6 칸에 데이터 7 칸이 되고, GFM 이 초과 칸을 버리면서 「어느 언어에서」 칸에 grep 꼬리, 「무엇이 막나」 칸에 `전부`, **「측정 커밋」 칸에 `인식기 자체가 없다`** 가 들어간다. 착수 이전(2026-09-07 `1cfde9c`)부터 있던 결함이고 이 회차가 만든 것은 아니지만, **`F2` 가 그 표를 「바이트까지 그대로」 지키라고 잠근 표**이므로 다음 회차도 같은 문면을 읽는다. 같은 형태가 `docs/plan/disposal-map.md:182`·`:194` 에도 있다 | `docs/plan/02-order.md:50` | 저장소 | 참 | 거짓신호 | F2 |

---

## 자기 산출에 대한 발견

메인이 처리 방침을 정한다. 이 회차가 반드시 닫아야 하는 것이 아니다.

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 8 | `D2` 의 오라클 ⑴(*"`F09.md:79` 의 옛 까닭 인용 문구"*)이 **grep 으로 안 맞는다** — 게이트는 옛 까닭을 `docs/gates/F09.md:336-344` 에서 인용하고, 조건이 지목한 `F09.md:79` 라는 문자열도 그 줄의 문구(`표본이 symbol 과 같았다`·`이 코퍼스에서 두 반경이 같은 결박을 켰고`)도 게이트에 **0 건**이다. 문면을 엄격히 읽으면 *"셋 중 하나라도 없으면 불통과"* 라 `D2` 는 `반증` 이다. 본체(까닭의 정정)와 ⑵⑶ 은 실측으로 섰으므로 나는 `통과` 로 두었고 **그 갈림을 메인에게 남긴다** | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:357` | 회차기록 | 참 | 거짓신호 | D2 |
| 9 | `state.md` 의 단계 표가 게이트를 **「통과 47 · 반증 1 · 미측정 2」**로 적는데 같은 파일의 「지금 선 것」 절은 **「반증 2 · 미측정 1」**로 적는다. 한 파일이 같은 값을 두 값으로 진다 — 교대 문서는 새 컨텍스트가 받는 요약이라 앞 줄을 먼저 읽는다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/state.md:22` | 회차기록 | 참 | 거짓신호 | 없음 |
| 10 | `state.md` 의 레코드 수 **「193 행 — 정반합 53 · 독립리뷰 51」**이 낡았다. 내가 `findings.jsonl` 을 파이썬으로 세니 **데이터 265 행**(정반합 92 · 독립리뷰 84 · 조건평가 47 · 사전부검 40 · 실측 2)이다. 차이가 정확히 판 3 의 39 와 R3 의 33 이다. **그 줄 자신이 *"이 수를 손으로 적지 마라 — 이 줄이 두 번 낡았다"* 를 달고 세 번째로 낡았다** | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/state.md:86` | 회차기록 | 참 | 거짓신호 | 없음 |
| 11 | `## 개정` 의 2026-09-13 「조건 설계 평가 R1 …」 행이 표 칸 안에 **날 파이프**(`.filter(|b| b.target …)` 의 클로저 문법)를 담아 5 칸이 되고, GFM 이 초과 칸을 버려 **504 자 중 렌더되는 것이 154 자**다. `.filter(` 에서 문장이 끊기고 `CA1-06`·`GREEN 이 도달 불가` 가 렌더에서 **사라진다**(GitHub API 로 확인). 이 회차가 리뷰어들에게 *"표 칸에 파이프를 쓰지 마라"* 를 지시로 넘긴 바로 그 형태다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:441` | 회차기록 | 참 | 거짓신호 | 없음 |
| 12 | 「합이 원 의도를 덮나」 표가 **빈 줄 하나로 쪼개져** 아래 네 행(원문 28 행 · 29 행 · 목적 기여 둘)이 헤더 없는 블록이 된다. GitHub API 로 렌더하니 **표 1 개 · 행 6 개**만 나오고 `| 28 행 …` 부터는 `<p>` 안의 날 텍스트다. 자기 주장 표라 기계 영향은 없지만 **원문 다섯 줄의 덮임 판정이 표 밖으로 떨어진다** | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:402` | 회차기록 | 참 | 거짓신호 | 없음 |
| 13 | 한 문서가 같은 정본 줄을 **두 좌표로 가리키고 둘 다 틀렸다** — `B3` 의 `CA1-03` 주석은 `pal-query/src/lib.rs:849`, `## 개정` 의 같은 사실은 `:845` 라 적는데 `.filter(|b| b.target != symbol.id)` 는 **`:846`** 이다(`:849` 는 `Ok((here, watching))`). `CA2-13` 이 *"한 문서가 같은 정본을 두 좌표로 가리키고 있었다"* 로 이미 이름 붙인 형태의 재발 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:329` | 회차기록 | 참 | 거짓신호 | B3 |
| 14 | `G1` ⓑ 가 sunset 선언의 뜻을 적은 문장을 `docs/sunset.toml:23` 으로 지목하는데 그 문장은 **`:22`** 이고 `:23` 은 `#` 한 글자다(글롭 `:26` 은 맞다). `CA2-17` 이 이 좌표를 `:33` 에서 고칠 때 한 줄 어긋났고, **그 off-by-one 이 이슈 #146 본문 표에 그대로 실렸다** — `CA2-17` 이 *"틀린 좌표가 새 이슈 본문에 그대로 실릴 참이었다"* 로 경고한 바로 그 결과다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:379` | 회차기록 | 참 | 거짓신호 | G1 |
| 15 | 게이트와 `B5` 문면이 새 검사의 모집단을 **`intent.record(` 를 부르는 파일** · **「지금 모집단 셋」**으로 적는데, 구현은 `.record(&` 로 재고(주석이 스스로 *"수신자 이름으로 재면 `self.record(&b)` 가 조용히 빠진다"* 로 적는다) 검사 출력은 **「결박을 쓰는 경로 4개 · 예산을 지난다 3개 · 등록된 면제 1개」**다. 문서가 적은 모집단은 구현이 **일부러 버린 좁은 쪽**이라, 이 조건을 되짚는 사람이 grep 하면 다른 집합을 얻는다 | `docs/gates/binding-radius-in-use.md:97` | 회차기록 | 참 | 거짓신호 | B5 |
| 16 | 게이트 `## 범위 밖` 이 예산 하중을 **`37 × 78 = 2,886`** 으로 적는데 모집단을 안 적는다. `B5` 의 오라클이 지목한 `binding.status --json` 은 **39** 를 돌려주므로 그 명령으로 재면 **3,042** 다. 결론(상한 1,000,000 에 안 닿는다)은 안 움직이지만, 이 회차가 `B4`·`02-order` 에서 두 번 고친 **37 대 39 모집단 병의 세 번째 자리**이고 여기만 안 갈렸다 | `docs/gates/binding-radius-in-use.md:515` | 회차기록 | 참 | 미관 | B5 |
| 17 | 판 3 설계문이 **「설계의 일부다」**로 등록한 *"판이 끝난 뒤 메인이 재는 관측 하나 — 쏠림. `P 거짓` 행의 분포를 계층 S1~S4 에 대어 표로 적고 `observations/` 에 남긴다"* 가 **없다**(`grep -rn 쏠림 observations/` 가 0 건). 그 표는 합(合) 자신의 산출물 안에만 있어, 설계가 노린 「판정자와 다른 자가 재는 자기확인 방지 다리」가 안 섰다. ⚠ **내가 이 라운드에서 대신 쟀고 선다** — 호출자>0 24 행이 12 대 12, 계층 6/6·4/7·10/13·2/11 로 재현되고 판정 근거에 `호출자수` 를 든 문장이 없다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/3-design.md:292` | 회차기록 | 참 | 미관 | B2 |
| 18 | **취합 보고가 「셋이다. 묶지 않았다」로 올린 승격 물음이 원장에서 둘이 됐다.** 칸 1 → 승격 5, 칸 3 → 승격 6 인데 **칸 2(⒝ 의 하한을 「방향」으로 고정한 판정을 비준하는가)는 승격 5 의 답 ⑶ 으로 접혔다.** 그 칸의 갈래(「방향」 15 대 22 · 「항 더하기 관계」 20 대 17)가 「무엇을 올렸나」 열에 없어, **원장이 묻지 않은 물음에 대한 답만 싣는다.** 규약이 *"승격은 물음 하나에 칸 하나다 … 답 하나가 두 물음을 닫은 것처럼 보인다"* 로 금하고 심판이 *"합(合)의 두 칸과 묶지 마라"* 로 다시 적은 형태다. 게이트 산문이 비준을 따로 적어 실질은 남았다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:459` | 회차기록 | 참 | 거짓신호 | B2 |
| 19 | 취합 보고가 *"맞추지 않고 적는다 … 어느 쪽이 맞는지는 내가 정하지 않는다"* 로 올린 **「판 3 R1 의 채택 수가 15 와 14 로 갈린다」**가 게이트·`intent.md`·`state.md` 어디에도 없고(`grep 채택` 0 건) 레코드 항으로도 안 섰다. 판정값에 영향은 없지만 **명시로 미해소 상태로 올린 셈 어긋남이 처분 없이 취합 보고 안에만 남았다** | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/r3-raw.md:514` | 회차기록 | 참 | 미관 | 없음 |
| 20 | `report.md` 가 없다. 규약 §10 이 자리를 `report.md` 로 못 박고 *"종료 보고가 대화에만 살면 사후에 아무도 대조할 수 없다 — 독립 리뷰어는 대화 기록을 입력으로 안 받으므로 검사 대상이 검사자의 입력에서 구조적으로 빠진다"* 로 적었으므로, **§11 조건 5 는 지금 「통과」도 「반증」도 아니고 대조 불가다.** `state.md` 가 R4 뒤로 배치한 순서이므로 위반은 아니다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/state.md:64` | 회차기록 | 참 | 미관 | 없음 |

---

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 아니었나 |
|---|---|---|---|---|---|---|
| 21 | 게이트의 *"축이 가리키는 방향과 실측이 같다"* 가 합(合) 의 *"역상관도 무상관도 이 표본이 못 진다"* 와 부딪힌다 | 회차기록 | 거짓 | 거짓신호 | `docs/gates/binding-radius-in-use.md:285` | 두 문장의 쌍이 다르다. 합(合) 이 못 진다고 한 것은 **장치 신호 대 메모 진위**이고 게이트가 방향 일치를 적은 것은 **메모 넓이 대 메모 진위**다. 그리고 게이트 문장은 합(合) 자신의 발견 9(`dialectic/2-synthesis.md:127`)를 `n=1` 단서까지 그대로 옮긴 것이다 |
| 22 | 위조 대조 셋이 `미대조` 로 남았으므로 술어의 변별력이 안 선다 | 회차기록 | 거짓 | 금지역 | `docs/gates/binding-radius-in-use.md:416` | 내가 그 자리를 채웠고 변별이 선다. 진짜 37 행 최소 겹침 0.27 대 위조 ㉠ 0.00 · ㉢ 0.06, ㉢ 의 이식 토큰 일곱이 대상 결박문에 전부 없음, ㉡ 은 술어 ⒜ 의 명시 제외 목록이 잡는다 |
| 23 | ⒜·⒟ 의 37/37 이 이월값이라 근거 없이 서 있다 | 회차기록 | 거짓 | 거짓신호 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/3-synthesis-r2.md:100` | 기계 대조로 재현했다. ⒜ — 겹침 0 인 행 0 · 최솟값 0.27. ⒟ — `아니다` 19 행 중 호출자 부재를 근거로 쓴 행 0 · 꼬리는 `영향받는다` 7 행에만 |
| 24 | `bench/radius-run-first.txt` 가 사후에 「되살린」 파일이라 첫 실행의 값이 아닐 수 있다 | 회차기록 | 거짓 | 금지역 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/bench/radius-run-first.txt:1` | `git show 67e0640` 의 원 블롭과 본문 67 줄이 **0 줄 차이**다. 보존본임을 머리가 밝히고 판정을 지는 것이 두 번째 실행임도 적는다 |
| 25 | 판정 패킷이 결박문을 자르거나 고쳐 판 3 이 다른 것을 읽었다 | 회차기록 | 거짓 | 금지역 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/observations/3-b2-packet.md:98` | §2 의 37 행 14 열이 `decisions.tsv` 와 전 칸 일치 · §3 의 결박문 37 건이 정본 `note` 와 37/37 문면 일치 |
| 26 | `pal radius` 또는 `Binding::with_radius` 가 아무도 안 부르는 표면이다 | 저장소 | 거짓 | 실패 | `crates/pal-cli/src/main.rs:581` | 부르는 자리를 실제로 셌다. `main.rs:166` 이 서브커맨드를 선언하고 `:581` 이 `radius::run` 으로 디스패치하며 `radius.rs:216` 이 `with_radius` 를 부르고 단위 시험 셋이 더 부른다. 옛 `pal-cli/src/rebind.rs` 는 남아 있지 않다 |
| 27 | `## 효과` 의 둘째 항이 `cargo test … --ignored` 라 테스트 하네스가 클라이언트 역할을 한 것이다 | 회차기록 | 거짓 | 거짓신호 | `docs/gates/binding-radius-in-use.md:483` | §8 증인은 `pal touch extract_with` 이고 그것을 내가 릴리스 바이너리로 직접 돌려 `(2)` 를 봤다. 벤치 출력은 `E6` ⑶ 이 요구한 별개 산출이며 게이트가 *"그 출력이 `H1` 의 증인이 아니다"* 를 명시한다 |
| 28 | 판 3 뒤에 `widen/decisions.tsv` 의 `판정`·`근거` 가 고쳐졌다(금지역 ㉮) | 회차기록 | 거짓 | 금지역 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/widen/decisions.tsv:1` | `fd176a8` 판과 전 칸 비교했다. 바뀐 셀은 **1** 개(`49714573e0fa654c` 의 `대상digest차이` 거짓→참, R1 의 정정)이고 `판정`·`근거` 는 **0** 이다 |
| 29 | 설계가 등록한 반증 ⑤(*"S1 여섯 행의 ⒟ 에 한 줄도 없다"*)가 발화했는데 아무도 안 쟀다 | 회차기록 | 거짓 | 실패 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/3-design.md:286` | 합(合) R1 이 반론 17 의 처분에서 S1 여섯의 ⒟ 비대칭을 직접 다루고(`3-synthesis.md:89`) `:100` 이 ⒟ 37/37 의 근거를 적는다. 그리고 내가 기계로 ⒟ 를 다시 재서 섰다 |

---

## 끝내도 되는가

**안 된다 — 본 목록에 금지역 둘이 남았다.**

- **발견 5** — 살아 있는 계획 문서 `docs/plan/02-order.md:147` 이 빈 줄을 인용 출처로 단정하고, `F1` 의 오라클이 그 거짓 좌표를 초록으로 인증한다. 이 회차의 커밋이 스스로 만든 좌표 이동이다.
- **발견 6** — `## 승격` 3·4·5·6 의 「소유자의 답」 칸이 렌더에서 메인의 초안으로 바뀐다. `B2` 를 `반증` 으로 잠근 소유자 결정이 렌더된 원장에서 사라진다.

둘 다 문면 고침 한 번으로 닫히고(`:504`→`:547` · 승격 표 헤더를 4 칸으로), 나머지 열셋은 `자기 산출` 절에 있으므로 메인이 처리 방침을 정할 수 있다. 합격선 축은 게이트의 주장과 내 측정이 **통과 47 · 반증 2 · 대조불가 0 · 미측정 1** 로 완전히 일치하고, 판 3 의 수 다섯(22 · 15 · 6 · 16 · 12 대 12)은 정본에서 전부 섰다.