# 독립 리뷰 — 라운드 1 · 2026-09-12-binding-radius-in-use

> ⚠ **이 파일은 원 반환문이다. 메인이 요약하거나 고치지 않는다** — 합계 검산이 독립된
> 둘째 원천을 대야 하기 때문이다. **`pal-independent-reviewer` 가 냈고, 읽기 전용이라
> 파일을 못 써서 전문을 반환문에 실었고 메인이 옮겼다.** 옮기면서 바꾼 것은 없다 —
> 판정도 순서도 문면도 그대로다.
>
> **리뷰어가 안 받은 것**: 대화 기록 · 메인의 사고 과정 · 왜 그렇게 만들었는지의 설명 ·
> 앞 라운드 결과(이것이 R1 이다). 아래 첫 절이 리뷰어 자신의 격리 선언이다.
>
> ★ **처분은 이 파일에 없다.** 처분은 `findings.jsonl` 의 레코드가 지고 §5 의 자로
> 메인이 정한다. 이 파일은 **무엇이 보고됐나**만 진다.

---

## 내가 받은 것 / 안 받은 것 — 격리 선언

| 받았다 | 안 받았다 |
|---|---|
| `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md` 전문(405줄) · `docs/gates/binding-radius-in-use.md`(312줄) · 회차 산출물 전부 · 코드 · `git log 1276b8f..HEAD` · 규약 `.claude/skills/round/SKILL.md` · `AGENTS.md` | ❌ 대화 기록 · 메인의 사고 과정 · 왜 그렇게 만들었는지의 설명 · 앞 라운드 결과(이것이 독립 리뷰 **R1** 이다) |

**내가 실제로 돌린 것** — `cargo xtask check`(전 출력 파일로) · `cargo xtask test`(전 출력 파일로 · 54줄 · passed 1065) · `cargo check --workspace --all-targets`(격리 사본 · 새 `CARGO_TARGET_DIR`) · `pal query binding.status --json` · `pal radius <37건> --to callers --dry-run --json`(전수) · `pal touch extract_with` · `record.py gate` · `record.py conditions` · `record.py count` · `xtask --root <사본> check`(음성 대조 실제 심기) · `git show`/`ls-tree`/`merge-base` · `gh issue list`. **작업 트리는 한 글자도 안 고쳤다**(`git status --porcelain` 빈 출력).

**모집단 확인** — `git log --diff-filter=A 1276b8f..HEAD --name-only` 로 이 회차가 새로 만든 것을 확인했다: `crates/pal-cli/src/radius.rs` · `crates/pal-cli/tests/radius_preserves_verdict.rs` · `crates/pal-cli/tests/radius_bench.rs` · 회차 디렉터리 전부 · `docs/gates/binding-radius-in-use.md`.

**금지역 출처** — `.claude/pal/policy.toml` **없다**(확인). 그래서 `intent.md:158-167` 이 등록한 목록을 쓴다: 데이터_손실 · 측정이_죽은_가지 · 사실이_아닌_것을_사실로 · 인증 · 대표_취약점.

---

## 합격선 축

| 조건 | 판정 | 잰 수 | 근거 |
|---|---|---|---|
| A1 | 통과 | 37/37 | `cargo xtask test` — `반경만_바뀌고_여섯이_보존된다` ok. `observations/after-widen.txt` ⑦ 37/37 |
| A1-a | 통과 | 시험 1 | `pal_bind_재호출은_여섯을_안_보존하고_stale_을_지운다` ok. ⚠ 등록된 산출 `effect/negative-A1.md` 는 **없다**(자기 산출 발견 #16) |
| A2 | 통과 | 7/7 | 같은 시험 + `after-widen.txt` ⑦ 표 7행 전부 `True` |
| A2-a | 통과 | 시험 1 | 같은 시험의 `stale 7→6` 축 |
| A3 | 통과 | 시험 1 | `새_감시_원소의_기준은_bound_at_의_base_커밋이다` ok |
| A3-a | 통과 | 시험 1 | `기준을_head_로_읽으면_fresh_가_난다` ok — ⚠ **마지막 커밋 `d76114b` 에서 더해졌고** 게이트 §1 음성 대조 표에 안 실렸다 |
| A3-b | 통과 | 열 2 · 37행 | `decisions.tsv` 에 두 열이 섰다. ⚠ **한 칸이 사실과 다르다** — 발견 #10 |
| A4 | 통과 | 시험 1 | `없는_결박을_지목하면_실패한다` ok |
| A5 | 미측정 | — | `effect/rerun-widen.txt` 를 읽었을 뿐 **내가 재현을 안 돌렸다** |
| A5-a | 통과 | 자리 14 · 등록 4 | `cargo xtask check` 검사 12 초록. 등록 사유가 `"base 커밋 투영에서 읽어 만든다"`(`xtask/src/main.rs:2062`)로 `CA2-12` 가 요구한 문면이다 |
| A6 | 통과 | 1건 | `grep -n 'default_value = "symbol"' crates/pal-cli/src/main.rs` → `140:` 맞는다. `git diff 1276b8f..HEAD` 에 그 줄 없다(`default_value = "."` 하나만 든다) |
| B1 | 통과 | 37행 · 14열 | `csv.DictReader` 로 셌다 — 데이터 37행, 열 이름 14개가 문면과 같다 |
| B1-a | 미측정 | — | `effect/negative-B1.md` 를 읽었을 뿐 사본을 안 만들었다 |
| B2 | **미측정** | 판 0 | ⟨정반합⟩ 태그인데 **어느 판도 이 조건을 판정하지 않았다** — 판 1 은 ㈂, 판 2 는 `C3`·`H1`·`A3-b`(`dialectic/2-design.md:11`). 규약이 금해서 나 혼자 판정하지 않는다. 발견 #14 |
| B2-a | 통과 | 2 커밋 | `git merge-base --is-ancestor fd176a8 e16dc61` → 0 · 두 SHA 다르다 |
| B3 | 통과 | ⑴⑵⑶ 셋 | 라이브: 반경 `symbol` 19 · `callers` 18 / watch 합 **190** > 37 / `pal touch extract_with` → `■ 이 좌표를 지켜보는 것 (2)` |
| B3-a | 통과 | 착수값 4 | `observations/red-B3.txt` 가 `ee93b35`(넓힘 `e16dc61` 의 조상)에 섰다 |
| B4 | 통과 | 190 = 190 | `decisions.tsv` 감시후 합 190 ↔ `binding.status` watch 합 190(둘 다 내가 셌다). ⚠ 문면의 「`감시전==감시후` 행 수」는 실제 **26** 이고 산출은 7 을 적는다 — 발견 #19 |
| B5 | 통과 | 37×78=2886 | 라이브 최대 watch 78 · `PROVISIONAL_WATCH_PRODUCT_MAX` 1_000_000 (`budget.rs:330`) · `radius.rs:62` 가 `check_budget` 를 들인다 |
| B6 | 통과 | 18+/18− | `git diff --stat 1276b8f..HEAD -- .palimpsest/intent/bindings.jsonl` → 18 insertions, 18 deletions (비어 있지 않다) |
| B6-a | 미측정 | — | `effect/negative-B6.md` 를 읽었을 뿐 사본을 안 돌렸다 |
| B7 | 통과 | ⑴ 0건 | `판정==아니다 & 새반경!=옛반경` = **0**. ⚠ ⑵ 의 모집단이 **0** 이라 공전한다 — 발견 #20 |
| B8 | 통과 | 112 | `타파일새원소` 열 합 = 112 (내가 셌다) |
| C1 | 통과 | 5건 · id집합 | 라이브 `fresh` 25 · `stale` 12(착수 30/7) · 바뀐 5건 id 집합이 `after-widen.txt` ④ 에 있다 |
| C2 | 통과 | 0 | 라이브 `status.code.reason` 집계 = **빈 집합**(`watch_member_gone` 0) |
| C2-a | 통과 | 시험 1 | `binding::tests::사라진_감시_원소가_변한_것을_가린다` ok |
| C3 | 통과 | 7행 · 참 0 | 판 2 합(合)의 판정. **내가 뒤집힘의 근거를 독립 재현했다** — `git ls-tree daf15deb crates/pal-cli/src/` 에 `export.rs` 가 있고 그 시점 `label.rs` 머리가 이미 *"구조로 막힌다"* 를 적는다. 행 7 의 `아니다` 가 산출로 선다 |
| C4 | 통과 | 0 ≤ 5 | 라이브 undeterminable **0** · 새 stale **5** |
| D1 | **대조불가** | 훑은 조건 0 | ⑴ `record.py gate docs/gates/binding-radius-in-use.md` → `표준표: false` — 「원장 둘 대조」가 이 회차를 **형식 이전으로 빼고 한 조건도 안 훑었다** ⑵ 게이트 표준 표의 `대조불가` 행이 `—` 라 `[f09.pass]` ⑤ 가 그 행에 **없다**. ★ 실질(두 원장 일치)은 내가 손으로 확인했다 — ID 집합 대칭차 공집합 · 47/1/0/2. 발견 #11 |
| D1-a | 미측정 | — | 게이트도 미측정으로 적는다. ⚠ **그 까닭이 사실이 아니다** — 발견 #13 |
| D2 | 통과 | grep 3/3 | `이 코퍼스에서` 1 · `scripts/f09-verify.py:715` 1 · `:709` 1 · RED `observations/red-D2.txt` 있다 |
| D3 | 통과 | 3/3 | `git diff 1276b8f..HEAD -- docs/gates/F09.md` **0줄** · 게이트에 `docs/gates/F09.md` 3건 · `gh issue view 58 --comments` 에 새 게이트 경로 2건 |
| D4 | 통과 | 같은 절 | 게이트 `:124`·`:125` 가 ⑤⑥ 를 한 표에 두고 `:127` 이 양립 까닭을 적는다 |
| E1 | 통과 | 10행 | `radius_bench.rs` 가 저장소 안 `#[ignore]` 로 서고 넷째 팔이 대리물임을 산출 머리가 찍는다. ⚠ **축 곱이 문면과 다르다** — 발견 #18 |
| E2 | 통과 | 열 4 | `bench/radius.tsv` 에 최솟값·분산·두규모의비·changed 열이 있고 산출 머리의 선형성(3.84/3.87/4.02)이 표와 같다 |
| E3 | 통과 | 86.157 ↔ 0.572 | 두 비가 1 을 건너뛴다. 장치가 섰다 |
| E4 | 통과 | 첫 줄 | `head -1 radius.tsv` 가 탭으로 가른 열 이름이다 |
| E5 | 통과 | `.json` 0 | `find <회차> -name '*.json'` **0건** · sunset 검사 초록(트리거 0건) |
| E6 | 통과 | 3/3 | `grep -F -- '--ignored' .github/workflows/ci.yml xtask/src/main.rs` **0건** · 게이트 §9 에 문장 · §8 에 벤치 출력 |
| F1 | 통과 | 4/4 | `02-order.md:138` 에 `F05`·`F13`·`F15`·`binding.rs:504` 다 있고 diff 에 든다 |
| F2 | 통과 | 바이트 동일 | `git show 1276b8f:…\|sed -n 53p` 와 현재 `:53` 이 글자까지 같다 |
| F3 | 통과 | 3줄 불변 | `:67-69` 세 줄이 diff 에 변경으로 안 든다 · 새 `###` 절이 `:69` 뒤에 열렸다 |
| F4 | **반증** | 1건 ≠ 0건 | `grep -cF "파일 안의 관계만" docs/plan/02-order.md` → **1**(`:156`). 등록 오라클이 **0건**을 요구한다. 착수에 둘이었고(`:109`·`:128`) 하나만 고쳤다. 발견 #9 |
| G1 | 통과 | 4/4 | `gh issue list` 로 #145·#146·#147·#148 넷 다 OPEN 확인. 번호는 `47a0014` 커밋 메시지가 진다 |
| G2 | 통과 | 28/28 | `cargo xtask check` 전 출력 파일로 받았다 — `검사 28/28 통과` · `EXIT=0`. ⚠ **`report.md` 를 쓰는 순간 빨개진다** — 발견 #8 |
| G3 | 통과 | 1065 ≥ 1047 | `cargo xtask test` 전 출력 · `^test result:` 54줄 · passed 합 **1065** · `FAILED`/`panicked`/`^error` 0건. 게이트 기록은 1064 — 발견 #17 |
| G4 | 통과 | 경고 0 | 격리 사본에서 새 target 으로 `cargo check --workspace --all-targets` → `^warning` **0** · 재실행이 no-op(완주 확인). ⚠ release 축은 내가 안 돌렸다 |
| G5 | 미측정 | — | 미push. 게이트가 같이 적는다 |
| H1 | **반증** | 참인 재검토 0/5 | 판 2 합(合)의 판정이고 내가 근거를 재현했다(위 `C3`). 증인 ⑴⑵⑶ 의 좌표가 0건 |
| H2 | 통과 | 바이트 동일 | 측정 시점(`f1e8cdb`)의 `cmp` 기록. 나도 라이브로 `(2)` 를 재현했다 — 차이는 HEAD SHA 와 대장 수(파일 3 증가)뿐이고 감시 줄은 글자까지 같다 |

**검산** — 통과 41 · 반증 2 · 대조불가 1 · 미측정 6 = **50**

⚠ **게이트와 갈리는 자리 셋** — `F4`(통과→**반증**) · `D1`(통과→**대조불가**) · `B2`(통과→**미측정**). 나머지 차이(A5·B1-a·B6-a)는 *"회차가 안 쟀다"* 가 아니라 *"내가 안 쟀다"* 다.

---

## 미측정 목록

| # | 안 잰 조건 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 못 쟀나 |
|---|---|---|---|---|---|---|
| 1 | `A5` — 착수 상태에서 보존 경로를 다시 돌려 정본이 바이트까지 같나 | 원의도 | 참 | 거짓신호 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/effect/rerun-widen.txt` | 산출을 읽었을 뿐 임시 의도 저장소를 다시 세워 18건을 재실행하지 않았다. 한 라운드 안에 50 조건을 다 재현할 수 없었고 이것이 가장 비싼 넷에 든다 |
| 2 | `B1-a` — 넓힌 것만 담은 사본에서 `B1` 이 빨개지나 | 원의도 | 참 | 미관 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/effect/negative-B1.md` | 산출만 읽었다. 사본 생성이 결박을 만지는 일이라 읽기 전용 자리에서 안 했다 |
| 3 | `B6-a` — 사본에서 `import`→`export` 순서로 돌면 넓힘이 사라지나 | 원의도 | 참 | 미관 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/effect/negative-B6.md` | 같음. 그 대조가 정본 데이터를 되돌리는 경로라 재현을 안 걸었다 |
| 4 | `B2` — 근거가 결박문을 읽은 판정인가 | 규약 | 참 | 거짓신호 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:298` | ⟨정반합⟩ 조건이고 규약 §5 와 이 과업문이 *"혼자 판정하지 않는다"* 로 금했다. 그런데 **어느 판도 이것을 판정하지 않았다** — 그래서 판정자가 없다 |
| 5 | `D1-a` — 원장 둘 대조가 어긋난 사본에서 빨개지나 | 원의도 | 참 | 금지역 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:327` | **이 회차의 게이트로는 원리상 못 잰다** — 그 게이트의 표준 표를 파서가 못 읽어 검사가 아예 안 들어간다. 다른 회차로는 잴 수 있고 내가 쟀다(발견 #13) |
| 6 | `G5` — 마지막 커밋 SHA 에 `conclusion=success` | 원의도 | 참 | 거짓신호 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:354` | 아직 push 가 안 됐다. 원리상 종료 커밋 뒤에만 선다 |
| 7 | `G4` 의 release 축 — `.rs` mtime 갱신 후 `cargo build --release --workspace` | 원의도 | 참 | 미관 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:353` | `cargo check` 축만 격리 사본에서 새로 돌렸다(경고 0). release 빌드는 이 라운드 예산 밖이다 |

---

## 의도 축

### 빠진 것

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 1 | **㈁(불변식 ⑦)이 소유자의 잠긴 말에서 빠진 근거가 소유자의 말이 아니다.** `## 원문` 은 *"완성선을 어디에 두나? → **㈀ + ㈁ + ㈂ 전부**"* 를 소유자 인용으로 싣고, 그 절이 스스로 *"이 인용 안에는 소유자의 말만 있다 … 이 절은 다음 회차가 「소유자가 실제로 무엇을 요구했나」를 되짚는 유일한 자리다"* 라고 못 박는다. 그런데 **인용된 아홉 답 중 ㈁ 을 되받는 답이 하나도 없다** — 뺐다는 사실을 지는 것은 편집자 산문 셋(`:41` 편집자의 말 · `:128` 범위 밖 · `:385` 개정)뿐이고 셋 다 이 회차가 썼다. 소유자의 「전부」와 산출의 「빠졌다」 사이를 이을 소유자 발화가 원장에 없다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:27` | 원의도 | 참 | 거짓신호 | 없음(㈁ 은 조건이 없다 — `G1` ⓐ 가 이슈로만 진다) |
| 2 | **이 회차의 결정과 의도가 결박되지 않았다 — 규약 §11 조건 4.** 결박 축을 여는 회차인데 새 결박이 **0** 이다: `git show 1276b8f:.palimpsest/intent/bindings.jsonl \| grep -c ""` = 38(헤더+37), 현재도 38, 그리고 `grep -c "binding-radius-in-use" .palimpsest/intent/bindings.jsonl` = **0**. *"기준 시점은 `bound_at` 의 base 커밋이다"* · *"보존 불변식은 `with_radius` 한 자리다"* 같은 소유자 결정이 코드 좌표에 안 걸렸다. 문서에만 적힌 것은 낡음이 표시되지 않는다 | `.palimpsest/intent/bindings.jsonl` | 원의도 | 참 | 거짓신호 | 없음(조건 50 에 §11 ④ 를 재는 것이 없다) |

### 요구되지 않은 것

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 3 | **㈄(`F1`~`F4`)는 소유자의 「전부」에 없고, 회차가 스스로 건 잠금장치(*"승인이 그것을 덮는다. 승인 전에 `F` 를 실행하지 않는다"*)를 승인 기록 없이 지나갔다.** 실행은 `8ceb8e9` 이고 그 커밋 메시지가 *"승인이 덮었다(2026-09-13)"* 라 적는다. 그런데 **승인을 기록한 산출이 어디에도 없다** — `grep -rn "승인" <회차>/` 의 8개 자리 전부 「승인이 필요하다 / 승인 앞이다 / 승인이 여기서부터다」이고, `state.md:19` 는 지금도 `승인 \| 여기서부터다` 다. 그리고 시점이 더 나쁘다: 소유자의 마지막 기록 입력은 `c8bf780`(승격 3·4)인데 그 **뒤에** 조건이 44→45→50 으로 늘었다(`git show <커밋>:intent.md \| grep -c "^- \[[ x]\] \*\*"`). 규약 §4 는 *"승인은 의도가 잠길 때마다 한 번"* 이다. ★ 그런데 게이트가 *"33 개 걸림을 고친 뒤 **승인을 받았다**"* 로 단정한다 — **원장에 근거가 없는 사실 진술** | `docs/gates/binding-radius-in-use.md:21` | 원의도 | 참 | 금지역(사실이_아닌_것을_사실로) | `F1`·`F2`·`F3`·`F4` |

### 있는데 틀린 것

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 9 | **`F4` 가 자기 오라클로 반증인데 게이트가 통과로 적는다.** 등록 오라클 ⑴ 은 `grep -F "파일 안의 관계만" docs/plan/02-order.md` 가 **0건**인데 실측 **1건**(`:156`). 착수 시점에 그 문구는 **두 자리**에 있었고(`git show 1276b8f:docs/plan/02-order.md` → `:109`·`:128`) 회차는 `:109` 만 고쳤다. 남은 줄은 *"`pal touch` 는 **여전히** 「파일 안의 관계만입니다」를 찍는다"* — `F4` 가 겨눈 **바로 그 거짓 현재형**이고, `grep -rn "파일 안의 관계만" crates/` 는 **0건**이다. 회차는 이 위험을 알고 있었다(`8ceb8e9` 메시지: *"남기면 `F4` 의 오라클이 스스로 깨진다"*) — 그런데 이미 깨져 있었다 | `docs/plan/02-order.md:156` | 원의도 | 참 | 금지역(사실이_아닌_것을_사실로) | `F4` |
| 10 | **`widen/decisions.tsv` 의 `대상digest차이` 한 칸이 도구가 낸 값과 반대다.** 전수 대조했다 — `pal radius 49714573e0fa654c --to callers --dry-run --json` → `"대상digest차이":true`, TSV `:10` → `거짓`. 그리고 **같은 커밋(`fd176a8`)의 `widen/README.md` 가 *"37 건 전체에서 참인 것은 하나(`49714573e0fa654c`)"* 라 적는다** — 한 커밋 안에서 두 산출이 서로를 부정한다. `A3-b` 는 그 열을 *"안 적으면 새 `digest` 가 「그때의 값」으로 읽힌다 — 금지역 `사실이_아닌_것을_사실로`"* 라는 까닭으로 세웠다. 덜 적는 것보다 **거꾸로 적는 것**이 더 나쁘다. ⚠ 판정 영향은 없다(그 결박은 `아니다`·안 넓혔다) — 그러나 그 무해함이 문서에 적혀 있지 않다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/widen/decisions.tsv:10` | 원의도 | 참 | 금지역(사실이_아닌_것을_사실로) | `A3-b`·`B1` |
| 11 | **`D1` 의 등록 오라클이 이 회차를 한 조건도 안 훑았다 — 측정이 죽은 가지다.** `python3 .claude/skills/round/bin/record.py gate docs/gates/binding-radius-in-use.md` → `{"표준표": false, "판정": {}, "검산": null, "형식오류": []}`. 까닭은 절머리에 번호가 붙은 것이다 — 파서가 `## 판정` 으로 **시작하는** 헤딩만 보는데(`record.py:252`) 이 게이트는 `## 7. 판정` 이다. 그래서 `cargo xtask check` 의 「원장 둘 대조」가 이 회차를 **형식 이전**으로 분류한다(전 출력에 `형식 이전 … 2026-09-12-binding-radius-in-use (docs/gates/binding-radius-in-use.md)`). 게이트 검산 줄도 기계가 안 잰다. 그리고 오라클의 앞 절반도 안 선다 — 표준 표의 `대조불가` 행이 `—` 라 `[f09.pass]` ⑤ 가 거기 **없다**(있으면 검산 50 이 깨진다). ★ 실질은 내가 손으로 확인했다: `record.py conditions` 로 뽑은 50 조건의 판정 집합이 게이트 표와 **대칭차 공집합**이고 47/1/0/2 다. 갈린 것은 없고 **재는 기계가 없다** | `docs/gates/binding-radius-in-use.md:204` | 원의도 | 참 | 금지역(측정이_죽은_가지) | `D1`·`D1-a` |
| 13 | **`D1-a` 를 미측정으로 보낸 까닭이 사실이 아니고, 이 회차에서 잴 수 있었다.** 게이트 §7 은 *"「원장 둘 대조」의 뿌리가 **컴파일 시점에 박힌다**(`CARGO_MANIFEST_DIR`). 사본으로 `cd` 해도 원본을 재므로"* 라 적는다. **`xtask` 에 `--root <경로>` 가 있다**(`xtask/src/main.rs:114-125`) — 그 함수의 짝 `명령을_고른다` 는 주석으로 *"무시되는 인자는 「사본을 쟀다」를 문면으로만 참으로 만든다"* 까지 적어 두었다. 내가 격리 사본을 떠서 실제로 심었다: `xtask --root <사본> check` 가 사본을 읽고(`커밋 시각 없음` 문구가 사본임을 증명한다), 사본의 `2026-09-11-effect-confirmation/intent.md` 에서 `A1-b` 한 줄을 `통과`→`대조불가` 로 바꾸니 **`FAIL 원장 둘 대조 — 게이트는 「통과」, `intent.md` 는 「대조불가」다. 원장 둘이 갈렸다`** 가 났다. 규약 §10 의 가름 문장(*"이 회차에서 그것을 할 수 있었는가"*)으로 재면 **「원리상 못 잼」이 아니라 「안 잼」**이고 잔여다 | `docs/gates/binding-radius-in-use.md:239` | 원의도 | 참 | 금지역(사실이_아닌_것을_사실로) | `D1-a` |

---

## 이번 라운드의 새 발견

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 8 | **`report.md` 를 쓰는 순간 `cargo xtask check` 가 두 자리에서 빨개진다 — 지금 초록인 것은 회차가 아직 안 끝났기 때문이다.** ① `xtask/src/main.rs:6203` — `끝났나 && !형식이전_선언.contains(회차)` 면 *"**끝난 회차**인데 게이트에 표준 표가 없고 「형식 이전」 선언 목록에도 없다 — 표 헤더 한 글자로 조용히 검사 밖으로 나가지 않는다"* 를 push 한다. `끝났나` 는 `report.md` 존재로 켜진다(`:38-50`). ② `:6381` — *"하한 미충족: 끝난 회차 중 가장 최근인 `X` 가 이 검사 밖이다"*. 이 회차는 끝나는 순간 최근 회차가 되고 검사 밖이다. **면제로 도망갈 길도 닫혀 있다** — `docs/gates/README.md:85` 의 「형식 이전」 목록은 *"하한: 2026-08-19 이전에 연 회차만"* 이다. 푸는 값은 게이트 절머리 넷을 번호 없이(`## 합격선`·`## 판정`·`## 효과`·`## 범위 밖`) 되돌리는 것이고, 규약 `SKILL.md:736` 이 *"절 이름은 넷으로 고정한다 — 그래야 잰다"* · `:771` 이 *"새 회차는 반드시 이 형식으로 성립한다"* 로 이미 그것을 요구한다 | `docs/gates/binding-radius-in-use.md:18` | 규약 | 참 | 금지역(측정이_죽은_가지) · 종료 시 실패 | `G2`·`D1` |
| 14 | **⟨정반합⟩ 태그를 단 `B2` 를 어느 판도 판정하지 않았는데 게이트가 통과로 적는다.** 판 1 은 ㈂ 의 처분이고 판 2 의 설계문이 스스로 대상을 *"`C3`·`H1`·`A3-b`·`B2-a`"* 로 적는다(`dialectic/2-design.md:11`) — `B2` 가 없다. `grep -n "B2" dialectic/*.md` 의 결과 전부가 `B2-a` 이거나 그 해석 논의다. 규약 §5 는 *"의미론적 판정이 필요한 것"* 에만 정반합을 걸고 그 태그가 판정자를 지정한다. 판정자가 없는 조건이 통과로 원장에 들어가면 **「누가 판정했나」가 산출에 없는 통과**가 하나 생긴다. ⚠ 실질은 섰다 — `decisions.tsv` 37행에 `판정`·`근거` 가 있고 `widen/README.md` 가 판정자와 그가 안 받은 것을 적는다. 없는 것은 **판정 절차**다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:298` | 규약 | 참 | 거짓신호 | `B2` |

---

## 자기 산출에 대한 발견

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 15 | **게이트 §1 RED 표의 좌표가 어느 커밋에도 없던 자리다.** *"검사 12(앵커는 신고받지 않는다) \| 빨개졌다 — `crates/pal-cli/src/radius.rs:162`"* 라 적는데, `grep -n "WatchEntry {" crates/pal-cli/src/radius.rs` → **209** 이고 `:162` 는 `let 저장된_대상 = …` 줄이다. 실제 RED 는 `750e3ea` 의 **`crates/pal-cli/src/rebind.rs:162`** 였다(`git show 750e3ea:…/rebind.rs \| grep -n WatchEntry` → 162). **새 파일 이름에 옛 파일의 줄 번호를 붙였다** — `pal rebind`→`pal radius` 개명이 남긴 자취다. 이 저장소가 `CA2-17`(*"틀린 좌표가 새 이슈 본문에 그대로 실릴 참이었다"*)과 `CA2-13`(*"없는 파일을 정본으로 적었다"*)으로 두 번 잡은 형태와 같고, 게이트는 **동결 문서**라 사후에 못 고친다 | `docs/gates/binding-radius-in-use.md:34` | 회차기록 | 참 | 거짓신호 | `A5-a` |
| 16 | **`A1-a` 가 이름으로 지정한 산출 `effect/negative-A1.md` 가 없는데 통과로 적혔다.** `ls effect/` → `negative-B1.md`·`negative-B6.md`·`rerun-widen.txt`·`touch-extract_with*.txt` 다섯. `grep -rn "negative-A1"` 은 조건 문면 한 줄뿐이다. 실질은 더 강한 것으로 갈렸다(저장소 안 시험 `pal_bind_재호출은_여섯을_안_보존하고_stale_을_지운다` 가 통과하고 게이트 §1 이 그 선택을 적는다) — 그러나 **조건 문면이 안 고쳐졌고 개정에도 안 적혔다.** `A2-a` 의 *"같은 파일에 남긴다"* 도 같이 걸린다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:283` | 회차기록 | 참 | 거짓신호 | `A1-a`·`A2-a` |
| 17 | **`G3` 의 종료 측정이 마지막 커밋보다 앞서 있고, 그 뒤에 시험이 하나 늘었다.** `observations/final-G3.txt` 는 HEAD `69219be` 에서 `passed` 합 **1064** 를 적는다. 그런데 `git diff --stat 69219be..HEAD -- 'crates/**'` 가 `radius_preserves_verdict.rs \| 70 +` 를 내고, 그 70 줄이 **`A3-a` 의 음성 대조 시험**(`기준을_head_로_읽으면_fresh_가_난다`)이다 — 마지막 커밋 `d76114b`(게이트를 쓴 커밋)가 더했다. 내가 HEAD 에서 전 출력으로 다시 재니 **1065** 다. `G3` 판정은 살지만(1065 ≥ 1047) 게이트 `:216` 의 수는 HEAD 값이 아니다. 그리고 회차 스스로 `state.md:121` 에 *"검증 중에 커밋하지 마라 — 빌드 시점 커밋과 실행 시점 HEAD 가 갈린다"* 를 적어 두었다 | `docs/gates/binding-radius-in-use.md:216` | 회차기록 | 참 | 거짓신호 | `G3`·`A3-a` |
| 18 | **`E1` 의 축 곱이 산출과 다르고 게이트가 문면을 그대로 베낀다.** 게이트 `:141` 은 *"`#[ignore]` · 넷 × 두 규모 × 두 `changed` × 회차 3"* 이라 적는다. `bench/radius.tsv` 는 10행이고 실제로 돈 것은 **①②③ × 두 규모 × 회차 3(changed 축 `해당없음`)** + **④(가/나) × 규모 8000 **한 곳** × 두 changed × 회차 3** 이다. 소스가 그것을 확정한다 — `radius_bench.rs:278` 의 넷째 팔은 `크게` 하나만 받고, tsv 의 ④ 행 `두규모의비` 가 `—` 다. 4×2×2=16 이 아니라 10 이다. ⚠ 쓰임에는 구멍이 없다(`E2` 의 선형성은 ①②③ 의 두 규모가, `E3` 의 뒤집힘은 ④ 의 두 changed 가 진다) — **넓게 적힌 것이 문장뿐**이다 | `docs/gates/binding-radius-in-use.md:141` | 자기장치 | 참 | 거짓신호 | `E1` |
| 19 | **`B4` 가 문면으로 요구한 양과 산출이 적은 양이 다르다.** 문면은 *"`감시전 == 감시후` 인 행의 수를 **그대로 센 값**으로 적는다(도출하지 않는다)"* 인데, 내가 센 그 값은 **26**(안 넓힌 19 + 넓혔는데 안 커진 7)이고 산출은 **7** 을 적는다(`after-widen.txt` ⑧). 7 이 뜻 있는 수이고 조건의 제목(*"반경은 넓은데 감시가 안 큰 건수"*)이 그것을 가리키므로 **오라클 문장이 모집단을 잘못 적은 것**이다 — 안 넓힌 행은 정의상 전부 걸린다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:302` | 회차기록 | 참 | 미관 | `B4` |
| 20 | **`B7` ⑵ 의 모집단이 0 이라 공전한다.** ⑵ 는 *"`판정 == 영향받는다` 이면서 `새반경 == 옛반경` 인 행은 그 까닭이 `근거` 열에 적힌다"* 인데, 내가 세니 그 행이 **0** 이다(`영향받는다` 18 = 넓힌 18). ⑴(`판정==아니다 & 넓힘` = 0)은 모집단 37 에서 실제로 재고 있어 살아 있다. 절반이 항등식인 것이 산출에 안 적혔다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:306` | 회차기록 | 참 | 미관 | `B7` |
| 21 | **교대 파일 `state.md` 가 낡았고, 규약이 그것을 「새 컨텍스트가 받는 것」으로 지정한다.** 마지막 갱신이 `5a5b8ea`(코드가 바뀌기 전)다. 지금 거짓인 것 넷: *"지금 단계 — 조건 설계 평가가 닫혔다. **다음은 승인이다**"* · *"루프 \| 아직 — **코드는 한 줄도 안 바뀌었다**"*(실제 5 파일 · +1,194줄) · *"레코드 **111 행**"*(`record.py count` → **142**) · 묶음 표의 *"**B** (10)"*(실제 **12** — B1…B8 + 네 파생). 그리고 정반합 판 **2** 가 표에 없다. `SKILL.md:534` 가 *"새 컨텍스트에 주는 것은 「잠긴 의도 전문 + `state.md` 요약」이다"* 라 못 박으므로, 이 낡음은 다음 컨텍스트의 입력이 된다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/state.md:11` | 회차기록 | 참 | 거짓신호 | 없음 |

---

## 내가 기각한 것

| # | 무엇이 걸리나 | 좌표 | 모집단 | 유효성 | 해악도 | 조건 |
|---|---|---|---|---|---|---|
| 22 | *"`G1` 의 「번호가 산출에 남는다」가 2/4 다"* 라 적었다 — `grep -rn "#146\|#147" docs .palimpsest` 가 **0건**이고 게이트는 #145·#148 만 싣는다. **기각한다**: `git log -1 47a0014 --format=%B` 가 ⓐ**#145** ⓑ**#146** ⓒ**#147** ⓓ**#148** 넷을 라벨과 함께 싣는다. 커밋 메시지는 날짜가 붙고 못 고치는 산출이다. 남는 것은 추적성 취향 차이이고 조건 위반이 아니다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:350` | 회차기록 | 거짓 | 미관 | `G1` |
| 23 | *"`decisions.tsv` 의 `base에없던새원소` 가 도구와 두 행에서 갈린다"* 라 적었다 — 전수 대조에서 `4841643fa986aecb`(0↔1) · `a47cd21ca1fbc524`(0↔4). **기각한다**: 그 열은 **HEAD 에서 편 감시 집합**을 base 투영에 대는 값이고, TSV 는 `fd176a8` 에서 쟀는데 그 뒤 `radius_bench.rs`·`radius_preserves_verdict.rs` 가 들어와 HEAD 의 호출자가 늘었다. 측정 시점 차이이고 기록 오류가 아니다. **`대상digest차이` 는 이 변명이 안 통한다** — 저장값과 base 커밋만 쓰므로 HEAD 와 무관하다(그래서 #10 은 남겼다) | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/widen/decisions.tsv` | 회차기록 | 거짓 | 미관 | `A3-b` |
| 24 | *"`H1` 이 반증이므로 규약 §11 조건 3(`## 효과` 에 테스트가 아닌 것이 돌린 출력)이 안 채워졌다"* 라 적었다. **기각한다**: §8 이 *"틀린 답이어도 붙인다 — 틀렸다는 것이 그 회차가 얻은 가장 값진 것이다"* 로 그 갈림을 미리 닫았고, 조건 3 이 요구하는 것은 **판정값이 아니라 출력의 존재와 출처**다. `pal touch extract_with` 는 테스트도 CI 도 아니고 내가 라이브로 재현했다 — `■ 이 좌표를 지켜보는 것 (2)`. 두 축이 따로 선다 | `docs/gates/binding-radius-in-use.md:242` | 규약 | 거짓 | 미관 | `H1`·`H2` |
| 25 | *"`C4` 는 구현이 HEAD 에서 펴서 0 이 보장되므로 죽은 가지다"* 라 적었다. **기각한다**: RED 도달 가능성이 `C2-a` 의 단위 시험(`사라진_감시_원소가_변한_것을_가린다`, 통과)으로 따로 서 있고, 코퍼스 실측 callers 84 회가 모집단이 비지 않음을 진다. 그리고 산출이 그 인과를 스스로 적는다(`after-widen.txt` ⑤ *"0 이 나온 것은 우연이 아니라 설계다"*). 재는 명제가 「측정이 없다」가 아니라 「설계가 막았다」다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:315` | 원의도 | 거짓 | 미관 | `C4`·`C2-a` |
| 26 | *"게이트 §8 이 결박 하나만 싣는데 실제 출력은 둘이다"* 라 적었다. **기각한다**: 같은 절이 *"전문은 회차의 `effect/touch-extract_with.txt` 가 진다"* 로 발췌임을 명시하고, 그 파일에 둘 다 있다. `(0)`→`(2)` 의 수는 정확하다 | `docs/gates/binding-radius-in-use.md:244` | 회차기록 | 거짓 | 미관 | `H2` |

---

## 끝내도 되는가

**안 된다.** 본 목록에 금지역 **여섯**(#3·#9·#10·#11·#13·#8)이 남았고, 그중 #8 은 `report.md` 를 쓰는 순간 `cargo xtask check` 를 **실패**로 바꾼다(`xtask/src/main.rs:6203`·`:6381` 두 자리 · 면제 목록은 하한 2026-08-19 로 닫혀 있다). #11·#13 은 등록된 오라클 둘이 이 회차에 대해 **아무것도 안 쟀다**는 뜻이고 — 「원장 둘 대조」는 조건 0 개를 훑고 초록을 냈다 — #9·#10 은 **통과로 적힌 조건이 자기 오라클로 반증**이거나 **측정값을 거꾸로 적었다**. #3 은 소유자의 잠긴 범위 밖 산출이 승인 기록 없이 실행됐는데 게이트가 *"승인을 받았다"* 로 단정한다. 셋 다 이 회차가 스스로 등록한 금지역에 정면으로 닿는다.

★ **고침의 방향은 정정이다, 완화가 아니다**(★6 에 대한 답) — 조건 수가 44→45→50 으로만 갔고 ID 집합에서 **빠진 것이 하나도 없다**(`git show <커밋>:intent.md` 전수 대조). 개명(`pal rebind`→`pal radius`)은 남의 게이트 줄을 가리키던 이름을 고친 것이고, 합(合)의 열 항은 술어를 하나로 고정해 판정을 **뒤집은** 것이다 — 그 뒤집힘의 근거(`git ls-tree daf15deb` 에 `export.rs` 가 있다)를 내가 독립 재현했고 **선다.** 남은 것은 새 측정이 아니라 **이미 선 산출의 사실 정정 다섯과 절머리 넷의 이름**이다.

**주요 좌표** — `/Users/incognito/dev/projects/palimpsest/docs/gates/binding-radius-in-use.md`(`:18`·`:21`·`:34`·`:204`·`:216`·`:239`) · `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md`(`:27`·`:235`·`:283`·`:298`·`:302`·`:306`·`:327`) · `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-12-binding-radius-in-use/widen/decisions.tsv:10` · `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-12-binding-radius-in-use/state.md:11` · `/Users/incognito/dev/projects/palimpsest/docs/plan/02-order.md:156` · `/Users/incognito/dev/projects/palimpsest/xtask/src/main.rs`(`:114`·`:6203`·`:6381`) · `/Users/incognito/dev/projects/palimpsest/.claude/skills/round/bin/record.py:252`