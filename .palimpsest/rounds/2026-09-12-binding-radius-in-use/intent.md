# 잠긴 의도 — 결박 반경이 처음으로 쓰인다

> 회차 `2026-09-12-binding-radius-in-use` · 착수 커밋 `1276b8f`

## 원문

소유자가 2026-09-12 에 쓴 것 — **압축하지 않는다.**

> ## 착수 후보 셋 — 내 판정 초안을 함께 적는다
>
> ★ 내 추천은 (가)다. 근거: `AGENTS.md` 가 "빠르게 완성한다 → 처음의 의도에 도달한다 →
> 효과를 본다 → 그 뒤 계속 고친다" 로 순서를 못 박고, 효과는 앞 회차가 봤다.
> 그러므로 다음은 「고칠 것」이 아니라 순서표의 다음 완성이다.
>
> (가) ★ 순서표 §2 의 3 번 — 결박 반경과 낡음 전파 (C6)
>      순서표가 "2 가 갖춰져야 callers 반경이 뜻을 갖는다" 로 적었고 2 단계가 섰다(2026-09-10).
>      지금 반경 기본값이 symbol 이고 전파가 없어서 「낡았다」가 좁은 뜻만 진다.
>      03-shortest-path §5 가 이것을 「2 단계 직후」로 예고했다.
>
> 고르는 것은 소유자다. 다만 메뉴만 내밀지 말고, 인터뷰에서 각 갈래가 무엇을 바꾸는지
> 먼저 재서 가져와라 — 앞 회차가 그것을 안 해서 승격이 한 번 물렸다.

인터뷰 세 라운드가 잠근 것 아홉 — **상한 3 에 닿아 닫았다.** 범주 다섯(경계·의도·자율·
종료·재고)이 전부 한 번씩 열렸다.

> 어느 갈래를 여나? → **(가) 반경·전파 잇기**
> 완성선을 어디에 두나? → **㈀ + ㈁ + ㈂ 전부** ⚠ **㈁ 은 라운드 3 에서 빠졌다 — 아래 `## 개정`**
> 기존 결박 37 건을 어떻게 하나? → **내가 근거를 대고 넓힌다**
> 이 회차가 만드는 장치에 대한 발견은? → **(나) 분할 — 새 회차로**
> 넓혀서 `stale` 이 늘었을 때 무엇이 참·거짓을 가르나? → **사람이 결박문을 읽고 판정한다**
> 기본 반경을 옮기나? → **안 옮긴다 — 이번엔**
> §8 효과의 증인은? → **넓힌 반경이 실제 재검토를 불렀다**
> 라운드 상한 → **인터뷰 3 · 사전부검 2 · 조건평가 2 · 정반합 판마다 2 · 독립리뷰 4**
> 이것이 전부인가? → **없다 — 잠그러 가라**

## 목적 기여

[`00-goals.md`](../../../docs/plan/00-goals.md) §2 의 목표 **2(컨설팅 · P1)** — *"여기를
바꾸면 무엇이 깨지나"* — 그리고 §0.1 의 **(d) 나비효과 방지**.

**왜 지금 그것이 다른 것보다 먼저인가.** `00-goals.md` §0 이 *"파는 것"* 이라 적은 한
문장은 *"이 코드에 걸린 결정이 아직 유효한가에 기계가 답한다"* 이다. 지금 그 답은 **가장
좁은 뜻으로만** 선다 — 착수 시점 실측으로 저장소의 결박 **37 건이 전부 `symbol` 반경**
이고(`.palimpsest/intent/bindings.jsonl`), 그래서 *"유효한가"* 가 *"좌표 하나의 본문이
변했나"* 로만 답해진다. 순서표 §2 의 2 번(파일 경계를 넘는 참조 해소)이 2026-09-10 에
서면서 **넓은 반경이 빈 집합이 아니게 됐고**, 그 뜻을 넓히는 비용이 이제 조회 1 회다.

`AGENTS.md` 의 소유자 지시(*"소비 가능해지는 지점에 가장 가까운 것"*)로 재면 이 자리가
가장 가깝다 — **새 층을 세우는 것이 아니라 이미 선 층을 쓰는 일**이기 때문이다.

## 착수 시점 관측 — **잠그기 전에 실측한 것**

⚠ **이 절의 수는 착수 시점(`1276b8f`)의 것이고 회차가 끝나면 낡는다.** 재는 자리는
각 조건이 지목한 명령이다.

| 무엇 | 착수 시점 값 | 어떻게 쟀나 |
|---|---|---|
| 결박 수 · 반경 분포 | **37 건 · 전부 `symbol`** (`callers`·`closure`·`files` **0**) | `bindings.jsonl` 을 읽어 `radius` 를 셌다 |
| 감시 집합이 반경으로 커지나 | **커진다** — 같은 심볼에 `symbol` 1 · `callers` 2 · `closure:2` 3 | 임시 의도 저장소에 `pal bind` 세 번 |
| `pal touch` 의 `■ 이 좌표를 지켜보는 것` | **0** | `pal touch print_facts` |
| `pal doctor` 불변식 ⑦·⑧ | **둘 다 `not_built`** | `pal doctor --json` |
| `cargo xtask check` | **28/28** | 그대로 |
| `1276b8f` CI | **초록** · 잡 일곱 (런 `34694615603`) | `gh run view` |

## 이 회차가 안 하기로 정한 것과 그 까닭 — **원리상 못 하는 것을 먼저 가른다**

**`stale-derived`(불변식 ⑧ · 낡음 이어달리기)는 이 회차가 원리상 못 연다.** 저장소가
스스로 그 까닭을 적어 두었다:

- `crates/pal-cli/src/doctor.rs:19` — *"심볼은 파생물이 아니라 **이 스냅샷의 코드 자체**다
  … 결박은 감시 집합을 갖지만 그것은 *자기* 감시 집합이고 §6.4 가 말하는 **입력**이
  아니다. 그래서 **이 빌드에는 `stale-derived` 가 설 자리가 없고**, 그 사실이 불변식 ⑧ 의
  `not_built` 로 나온다."*
- `crates/pal-core/src/binding.rs:504` — *"`StaleDerived` — 결박의 **파생 입력을 주는
  것이 이 빌드에 없다.** 그리고 `NodeFreshness` 와 **합치지 않는다** — 모집단이 다르고,
  합치면 `[f22.4]` 불변식 8 의 모집단이 움직인다."*

**그러므로 순서표 §1 의 `C6` 행이 적은 *"낡음 전파 미구축"* 은 참인데 그 미구축의 소속이
`C6` 가 아니다** — 파생 노드 생산자(F05·F13·F15)가 서야 열린다. 순서표 §2 가 3 번에
「전파」를 함께 묶은 것은 §1 의 문면을 그대로 옮긴 결과이고, **그 어긋남을 이 회차가
정정한다**(㈄).

**「낡았다」를 넓히는 실제 길은 이어달리기가 아니라 반경이다** — `callers` 는 사실상 1 홉,
`closure:k` 는 k 홉이다. 그 장치가 서 있는데 한 번도 안 쓰였다.

## 계획 — 셋

⚠ **㈂ 의 모양은 정반합 판 1 과 소유자 결정(2026-09-13)이 확정했다.** 아래 `## 승격` 칸 1·2.

| | 무엇 | 왜 이 회차인가 |
|---|---|---|
| **㈀** | **반경이 처음으로 쓰이게 한다** — 결박 37 건을 근거를 대고 넓히고, `pal touch` 의 `■ 이 좌표를 지켜보는 것` 이 값을 갖는다 | 장치가 이미 서 있고 2 번이 서면서 빈 집합이 아니게 됐다. 이것이 이 회차의 본체다 |
| **㈂-a** | **`callers` 거짓 양성률을 「대조 불가」로 다시 적고 통과로 세지 않는다** — 그 사실과 **까닭의 좌표**를 함께 적는다 | 등록 실행체를 **두 코퍼스 × 두 반경**으로 돌렸고 네 산출의 표본 10 줄이 반경 간에 **글자까지 같다**. #58 이 2026-08-15 에 적은 실패가 재현됐고, 이번에는 **까닭이 좌표로 나왔다** |
| **㈂-b** | **반경별 비용 벤치를 등록된 형태로 저장소 안에 세워 잰다** — 넷(`symbol`·`callers`·`closure(2)`·증분 갱신) × 두 규모 × 회차 3 · `#[ignore]` · 저장소 안 실행체 | 소유자 결정(승격 칸 2). ⚠ **`[f09.4.pass]` ④ 는 「대조 불가」로 적는다** — 합격선이 `changed` 크기를 안 적어 부하 선택이 판정을 뒤집고, 그 곡선을 **이미 봤다** |
| **㈄** | **순서표의 `C6` 자리를 실측대로 바로잡는다** — ⚠ **§1 의 표는 안 고친다. 아래에 얹는다** | 위 절이 그 어긋남을 실측했다. 안 고치면 다음 회차가 같은 문면을 다시 읽는다. ⚠ §1 은 **측정 커밋 `19b5877` 이 행마다 달린 기록**이고 표 바로 아래 줄이 *"위 표는 안 고친다 … 바뀐 값을 여기 얹는다"* 로 금했다 — `C2` 가 2026-09-08 에 그 규칙대로 얹었고 ㈄ 도 그 형식을 따른다. **§2 의 3 번 행(`:110`)은 그 금지 밖이라 직접 고친다** |

## 차선책

⚠ **첫 항은 2026-09-13 에 소유자가 **내용으로** 직접 답했다.** 그 전까지 이 절은 메인이 쓴
초안이었고 소유자가 본 적이 없었다 — 종료 판단이 그 사실을 *"소유자가 안 본 문서에
소유자의 뜻은 없다"* 로 잡았다. **아래는 이제 소유자의 답이다.**

- **`callers` 가 두 반경에서 안 갈리면** — **거짓 양성률은 `대조불가` 로 다시 적고
  통과로 세지 않는다.** ★ 함께 적어야 참이 되는 것: 두 반경은 **`undeterminable` 축에서
  갈리는데** 그 차이가 **거짓 양성 표본에 안 실린다** — `scripts/f09-verify.py:715` 의 갈래
  필터가 표본 목록에 `stale`·`orphaned` 만 담기 때문이다. 「어디서도 안 갈렸다」가 아니라
  **「갈리는 축이 표본에 안 들어온다」**다.
  ⚠⚠ **앞 판은 이 자리를 *"표본기가 그 축을 구조적으로 못 본다"* 로 적었고 그것은 거짓이다**
  ⟨정정 2026-09-13 · 사전부검 R2⟩. 같은 루프의 `:709-711` 이 `undeterminable` 을 **읽어서**
  `관측된_사유` 에 담고, 그 값이 `[f09.pass]` ⑥ 의 하한 입력이다. **스크립트가 하는 일을
  안 한다고 적는 것**이 될 뻔했다 — 이 문장은 게이트와 `#58` 에 그대로 적힐 원인 진술이다.
- **결박 37 건 중 넓힐 근거가 서는 것이 소수면** — 넓힌 수를 합격선으로 삼지 않는다.
  **근거 없이 넓히는 것이 이 회차가 막아야 할 형태**이므로, 그때는 판정한 37 건 전부의
  근거를 산출로 남기고 넓힌 수는 그 결과로 적는다.
  ⚠ **원인을 사람 판단으로 적지 마라** — 사전부검 ⑦ 이 실측했다. 37 중 **13 이 호출자 0**
  이고 그것은 멤버 호출(`x.foo()`)을 **장치가 못 보는 것**이지 근거가 안 서는 것이 아니다.

## 범위 밖

- **불변식 ⑦(2층 결박 색인)** — `F05` 의 `projection-binding-index` 능력이고 순서표 밖이다.
  이 회차에서 뺀다(소유자 · 인터뷰 라운드 3). **이슈로 세운다.**
- **불변식 ⑧ · `stale-derived`** — 위 절이 적은 대로 **원리상 못 연다.**
- **기본 반경을 `symbol` 에서 옮기는 것** — 초석이고, 근거가 될 실측이 이 회차 안에서
  나오므로 재고 나서 다음이 정한다(소유자).
- **#119**(결박 대상이 심볼 한정) — 결박 **대상**의 축이고 이 회차는 **반경**의 축이다.
- **#57**(워킹트리 드리프트) — 다른 사건이다.
- **#135 의 ⓑ 잔량 · #133 · #131 · #136~#144** — 이번에 안 연다.
- **`PROVISIONAL_WATCH_PRODUCT_MAX` 의 값을 바꾸는 것** — `closure` 를 실제로 쓴 뒤에야
  실측할 근거가 생긴다. 다만 **예산이 실제로 걸리는지는 관측하고 적는다.**

## 상한

- 인터뷰 **3 라운드** — 소진했다(2026-09-12 · 라운드 3 에서 닫았다).
- 사전부검 **2 라운드**
- 완수 조건 설계 평가 **2 라운드**
- 완수 조건 검증 정반합 **판마다 2 라운드**
- 독립 리뷰 **4 라운드**

## 모집단 분리 — §11 ③

**(나) 분할을 고른다**(소유자). 이 회차가 만드는 장치(#58 의 표본기·벤치)에 대한 발견은
**별개 의도로 보고 이슈로 세운다.** 「나중에」가 아니라 다음 회차의 착수 대상이다.

## 금지역

`.claude/pal/policy.toml` 이 없다. 이 회차가 등록하는 목록은 규약 §11 의 **기본 다섯**이다 —
데이터_손실 · 측정이_죽은_가지 · 사실이_아닌_것을_사실로 · 인증 · 대표_취약점.

★ **이 회차에 특히 걸리는 둘을 지목한다.**
- **데이터_손실** — 결박 37 건은 추적 중인 `bindings.jsonl` 이 정본이고 `intent.redb` 는
  파생물이다. 반경을 바꾸는 것은 **저장된 데이터를 바꾸는 일**이라 되돌리기가 비싸다.
- **측정이_죽은_가지** — #58 이 *"두 반경의 표본이 **글자까지 같았다**"* 로 이미 한 번
  이 병에 걸렸다. 같은 형태가 다시 나면 그것은 통과가 아니다.

## 사전부검 R1 의 처리 방침 — **남기지 않는다**

원 반환문은 `premortem/r1-raw.md`, 레코드는 `findings.jsonl`(19 행 · 유효 11 · 스스로 물린 것 8).

| # | 무엇 | 처리 방침 |
|---|---|---|
| **①** | 반경을 바꾸는 명령이 없다 — `pal bind` 재호출이 `bound_at` 과 감시 다이제스트를 HEAD 로 재기준해 **지금 `stale` 인 7 건을 지운다**(격리 실측 `stale 7→6`) | **계획수정.** ㈀ 의 첫 일은 결박을 넓히는 것이 아니라 **반경만 바꾸고 낡음 판정을 보존하는 경로**를 세우는 것이다. 그것이 없으면 ㈀ 은 원리상 데이터_손실이다 |
| **②** | 감시 원소가 리네임되면 `Undeterminable` 이 `Stale` 을 **가린다**(`evaluate` 의 ②가 ③④ 앞에서 반환) | **완수 조건으로 전환.** 넓힌 반경에서 그 경로가 실제로 나는지 재고 적는다. ⚠ 코퍼스 실측이 이미 그것을 확증했다 — `watch_member_gone` 이 callers **84 회** · symbol **0 회** |
| **③④⑤⑥⑨⑩** | ㈂ 를 겨눈 여섯 | **정반합 판 1 이 처리했다** — 위 `## 승격` 칸 1·2. ⚠ 그중 **⑨ 는 반증됐다**(`bindings_watching` 과 `all` 은 둘 다 실체가 있고 살아 있는 호출 경로다) |
| **⑦** | 멤버 호출을 못 세므로 37 중 **13 이 호출자 0** — 넓혀도 감시가 안 커지고, 그중 셋은 `stale` 7 안에 있어 ①과 겹쳐 **순손실** | **계획수정.** 위 `## 차선책` 둘째 항이 그것을 진다 — 원인을 사람 판단으로 적지 않는다 |
| **⑧** | `repo_id` 가 **디렉터리 이름**이라 클론·워크트리에서 넓히면 덮이지 않고 **더해진다**(실측 37→38) | **계획수정.** 아래 `## 작업 규율` |
| **⑪** | 착수 근거 *"이미 선 층을 쓰는 일 · 비용은 조회 1 회"* 가 ㈂ 에 대해 거짓 | **계획수정.** 좌표 둘은 틀렸고(`bench.rs` 는 실재한다) 실질은 살아 있다 — 그 사실을 이 절이 진다. **㈀ 에 대해서는 그 근거가 참이고, ㈂ 에 대해서는 거짓이다** |

## 작업 규율 — 결박을 만질 때

⚠⚠ **이 절의 2 는 2026-09-13 에 뒤집혔다. 앞 판은 일을 지웠다** — 사전부검 R2 가 격리
저장소에서 재현했다. 아래 2 의 ⚠ 를 읽어라.

1. **결박을 만지는 일은 이 저장소 이름의 워킹트리에서만 한다.** 격리 클론·워크트리에서
   `pal bind` 를 부르지 않는다(R1 ⑧). 디렉터리 이름이 `repo_id` 로 들어가 **새 결박이 더해지고
   지우는 공개 API 가 없다.**
2. **결박을 만진 뒤에는 `pal intent export --out <추적본>` 을 **먼저** 하고 `git diff` 를 본다.**
   정본은 `.palimpsest/intent/bindings.jsonl` 이고 `intent.redb` 는 파생물이다.
   ⚠⚠ **`import` 를 먼저 부르지 마라 — 그것이 일을 지운다.** `IntentStore::record` 는
   id 가 같으면 **덮어쓰므로**(`store.rs:191`), 아직 옛 값인 추적본을 import 하면 넓힌 37 건이
   통째로 되돌아가고 이어진 export 가 그 되돌아간 값을 파일에 다시 쓴다. 그러면
   **`git diff` 가 비고, 그 빈 diff 가 「정상」으로 읽힌다** — 규율이 세운 유일한 검사기가
   **일이 지워졌을 때 가장 깨끗한 초록**을 낸다(사전부검 R2 가 격리 저장소에서 재현했다).
   `import` 는 **파생물을 잃었을 때 정본에서 되세우는 방향**으로만 쓴다.
3. **넓히기 전에 `pal query binding.status --json` 을 찍어 두고, 넓힌 뒤 다시 찍어 대조한다.**
   ⚠ **음성 축만으로는 부족하다** — `stale` 의 신원(id 집합)이 보존되는지는 **한 건도 안
   넓혔을 때 완벽하게 통과한다**(사전부검 R2). **양성 축을 함께 잰다**: ⑴ `radius` 분포가
   실제로 움직였나 ⑵ 감시 집합 크기의 합이 늘었나 ⑶ `pal touch` 의 `■ 이 좌표를 지켜보는 것`
   이 0 을 벗어났나. 셋 다 `binding.status --json` 과 `touch` 가 이미 싣는다.
4. **결박을 넓히는 경로는 코드로 세운다 — 정본 JSONL 을 손으로 고쳐 `import` 하지 않는다.**
   `import_jsonl` 은 **감시 원소의 좌표 실재성도 digest 의 출처도 검사하지 않는다**(사전부검
   R2 가 위조한 원소를 통과시켰다). 검사 12(앵커는 신고받지 않는다)는 Rust 소스의 리터럴
   자리만 세므로 **그 문을 원리상 못 본다.**
   ⚠ 코드 자리를 새로 만들면 검사 12 가 **등록되지 않은 자리**로 빨개진다. 푸는 값은
   `WATCH_ENTRY_SITES` 에 자리를 더하는 것인데, 등록된 셋의 사유는 전부 *"투영에서 읽는다"*
   이고 새 자리는 *"옛 결박에서 물려받는다"* 라 **넷째 종류**다. **그 사유를 등록에 적는다** —
   안 적으면 그 검사가 재는 명제가 「앵커는 전부 투영에서 온다」에서 「자리 수가 같다」로
   조용히 내려앉는다.

## 표기 — **파서가 아는 토큰을 쓴다**

계획과 차선책이 「대조 불가」(사이 띄어쓰기)로 쓰는 판정을 **표준 표와 조건 상자에는
`대조불가`(붙여쓰기)로 적는다.** 파서가 아는 값은 `통과 · 반증 · 대조불가 · 미측정` 넷뿐이고
(`record.py:151` · `round_condition.rs:41-47`), 셀 정규화는 `*` 와 백틱과 바깥 공백만 지운다.
띄어 쓰면 **그 행의 조건 ID 가 어느 판정에도 안 실린 채 검산이 어긋난다.**

## 완수 조건

⚠ **사전부검 R2 뒤에 쓴다.**

## 개정

| 언제 | 무엇을 | 왜 |
|---|---|---|
| 2026-09-12 | 완성선에서 **㈁(불변식 ⑦)을 뺐다** | 인터뷰 라운드 2 에서 소유자가 ㈀+㈁+㈂ 를 골랐는데, 그때 메인이 ㈁ 을 *"불변식 ⑦ 을 연다"* 로만 내고 **그것이 `F05` 소속 능력이라는 사실을 안 냈다.** 라운드 3 에서 그 사실을 내자 소유자가 **빼고 이슈로 세우는 쪽**을 골랐다. 축소가 아니라 **잘못된 정보 위에 선 선택을 바로잡은 것**이다 |
| 2026-09-13 | **㈂ 를 ㈂-a · ㈂-b 로 갈랐다** | 정반합 판 1. 두 절반의 처분이 갈렸으므로 한 칸으로 두면 규약 §5 가 금한 *"둘을 묶어 올려 답 없는 물음이 처리된 것처럼 사라지는"* 형태가 된다 |
| 2026-09-13 | `## 차선책` 첫 항을 **소유자의 답으로 대체했다** | 그 전까지 메인의 초안이었고 소유자가 본 적이 없다. 합(合)이 *"등록된 차선책이면 승격이 불필요"* 로 판정했는데 종료 판단이 그 전제를 무너뜨렸다 — *"소유자가 안 본 문서에 소유자의 뜻은 없다."* 그래서 **등록 여부를 따지는 대신 내용을 직접 물었다** |
| 2026-09-13 | 차선책 둘째 항에 **원인을 사람 판단으로 적지 말라**를 더했다 | 사전부검 ⑦ 의 처리 방침(계획수정). 37 중 13 이 호출자 0 인 원인은 장치의 한계이고, 그것을 *"근거가 서는 것이 24/37 이었다"* 로 적으면 원인이 뒤바뀐다 |

## 승격

| # | 무엇을 올렸나 | 소유자의 답 |
|---|---|---|
| **1** | **㈂ 의 `callers` 거짓 양성률 절반을 어떻게 하나.** 등록 실행체를 두 코퍼스 × 두 반경으로 돌렸고 네 산출의 표본 10 줄이 반경 간에 **글자까지 같다**(`ditto` stale 0 · `portal` stale 7). #58 이 적은 실패가 재현됐다 | ⟨2026-09-13⟩ **「대조 불가」로 다시 적고 통과로 안 센다.** 함께 적을 것 — 두 반경은 `undeterminable` 축에서 갈리는데 표본기가 `f09-verify.py:715` 에서 그 축을 못 본다 |
| **2** | **㈂ 의 반경별 비용 벤치 절반을 어떻게 하나.** 등록이 요구한 것은 넷 × 두 규모 × 회차 3 · `#[ignore]` · 저장소 안 실행체인데 선 것은 증분 하나 · 한 규모 · scratchpad 뿐이고, 합격선이 `changed` 크기를 안 적어 곡선(`k=1` 32.07× → `k=34` 0.926×)을 본 지금은 부하 선택이 판정을 뒤집는다 | ⟨2026-09-13⟩ **㈎ 세워서 재고, 부하 미규정은 게이트에 결함으로 적는다.** 그러므로 `[f09.4.pass]` ④ 는 「대조 불가」이고 장치는 선다 |
