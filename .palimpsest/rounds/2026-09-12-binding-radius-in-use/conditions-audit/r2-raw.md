# 완수 조건 설계 평가 — 라운드 2 · 2026-09-12-binding-radius-in-use

> ⚠ **이 파일은 원 반환문이다. 메인이 요약하거나 고치지 않는다** — 합계 검산이 독립된
> 둘째 원천을 대야 하기 때문이다. **감사자는 읽기 전용이라 파일을 못 써서 전문을
> 반환문에 실었고 메인이 옮겼다.** 옮기면서 바꾼 것은 하나뿐이다 — 전달 과정에서 HTML 로
> escape 된 `&lt;`·`&gt;`·`&amp;` 를 되돌렸다. **판정도 순서도 문면도 그대로다.**
> R1 과 달리 형식을 되돌려 받을 일이 없었다 — 개요 표 없이 `###` 항으로 왔다.

## 내가 받은 것과 안 받은 것

**받았다**: `audit-input-r2.md` 의 여섯 절(`## 원문` · `## 목적 기여` · `## 완수 조건` · `## 차선책` · `## 범위 밖` · `## 상한`) 하나뿐. 그리고 저장소를 직접 읽었다 — `crates/` · `xtask/` · `docs/` · `corpus/criteria.toml` · `scripts/f09-verify.py` · `.claude/skills/round/bin/` · `.palimpsest/intent/bindings.jsonl`.

**안 받았다**: 조건을 쓴 자의 설명 · 대화 기록 · 앞 라운드(R1)의 결과 · 사전부검 결과. **`.palimpsest/rounds/**` 는 열지 않았다** — `conditions-audit/r1-raw.md` 를 포함해 한 파일도 읽지 않았다. `⟨CA1-NN⟩`·`⟨PM1-NN⟩`·`⟨DL1-06⟩`·`⟨MS-01⟩` 표시가 가리키는 내용을 나는 모르고, 그 표시가 붙은 조건도 새로 판정했다.

**프롬프트에 섞여 온 것**: 없다. 다만 **받은 파일 안에 해설이 섞여 있다** — `## 원문` 24 행이 여섯 절 밖의 `## 개정` 을 가리킨다(아래 발견).

---

## 조건 갈래 분류

| 조건 | 갈래(결정론적/의미론적/조건 아님) | 지금 적힌 형태 | 같나 |
|---|---|---|---|
| `A1`·`A1-a`·`A2`·`A2-a`·`A3`·`A3-a` | 결정론적 | ⟨결정론적⟩ · 자동 시험 오라클 | 같다 |
| `A3-b` | 결정론적 | ⟨결정론적⟩ · `.tsv` 열 대조 | 같다 (재는 양이 틀렸다 — 발견) |
| `A4`·`A5`·`A5-a` | 결정론적 | ⟨결정론적⟩ | 같다 |
| `B1`·`B1-a`·`B3`·`B3-a`·`B5`·`B6`·`B6-a` | 결정론적 | ⟨결정론적⟩ | 같다 |
| `B2` | 의미론적(정반합) | ⟨정반합⟩ | 같다 |
| `B2-a` | 결정론적(절반) | ⟨결정론적⟩ | **절반만** — 커밋 순서는 기계, 「올렸다」는 산출이 없다 |
| `B4` | 결정론적 | ⟨결정론적⟩ | 같다 (산식이 항등식이 아니다 — 발견) |
| `C1`·`C2`·`C2-a` | 결정론적 | ⟨결정론적⟩ | 같다 (`C2` 의 문자열이 틀렸다 — 발견) |
| `C3` | 의미론적(정반합) | ⟨정반합⟩ | 같다 |
| `D1`·`D1-a` | 결정론적 | ⟨결정론적⟩ · `원장 둘 대조` 검사 | 같다 |
| `D2`·`D3`·`D4` | **의미론적(체크리스트)** | ⟨결정론적⟩ | **다르다** — 판정자가 사람의 문서 읽기다 |
| `E1`·`E2`·`E4`·`E5` | 결정론적 | ⟨결정론적⟩ | 같다 (`E1` 의 넷째 팔이 안 선다 — 발견) |
| `E3` | 결정론적 | ⟨결정론적⟩ | 같다 (딛는 산출이 `E1` 에 없다 — 발견) |
| `E6` | 의미론적(체크리스트) | ⟨결정론적⟩ | **다르다** |
| `F1`·`F3` | **의미론적(체크리스트)** | ⟨결정론적⟩ | **다르다** |
| `F2` | 결정론적 | ⟨결정론적⟩ · `git diff` | 같다 — **이 묶음에서 유일하게 기계가 답한다** |
| `G1` | 결정론적(약) | ⟨결정론적⟩ | 같다 |
| `G2`·`G3`·`G4`·`G5` | 결정론적 | ⟨결정론적⟩ | 같다 |
| `H1` | 의미론적(정반합) | ⟨정반합⟩ | 같다 (대체 증인이 축을 바꾼다 — 발견) |
| `H2` | 결정론적 | ⟨결정론적⟩ | 같다 |

---

## 발견

### `B4` 의 검산 산식이 항등식이 아니다 — 성립하는 것이 오히려 우연이다
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: 조건 `B4` (`.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md`) · `crates/pal-query/src/lib.rs:882`
- 어떻게 실패하나: `B4` 는 *"`감시전 == 감시후` 인 행의 수가 `watch` 합과 착수 값 37 의 차이로 검산된다"* 고 적었다. 착수 실측이 건당 감시 1 이므로, 안 넓힌 행 수를 `u`, 넓힌 행(`c = 37-u`)의 감시후 합을 `S` 라 하면 `Σwatch_after = u + S` 이고 우변은 `u + S - 37` 이다. 좌변 `u` 와 같아지려면 **`S = 37` 이어야 한다.** 그런데 넓힌 행은 감시후 ≥ 2 이므로 `S ≥ 2c`, 즉 `u ≥ 19` 이면서 넓힌 18 행의 감시 합이 정확히 37 인 칼날 위에서만 등식이 선다. `B4` 자신이 인용한 *"37 중 13 이 호출자 0"* 과 `B3` ⑵(*"감시 합이 37 보다 크다"*)를 함께 두면 **이 등식은 거의 확실히 깨진다.** 실행자가 등식을 못 맞추고 → ① 「반증」을 억지로 적거나 ② 산식을 사후에 고친다. 둘 다 조건이 아무것도 안 잰 것이다.
- 고치면 서는 것: `Σ감시후 = u + S` 의 전개를 열로 적고 `Σwatch_after` 와 대조하는 형태(행별 합 검산)

### `C2` 의 오라클 문자열이 JSON 값과 다르다 — 언제나 0 을 내고 통과한다
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: 조건 `C2`·`A2` · `crates/pal-core/src/binding.rs:439` · `crates/pal-core/src/binding.rs:467`
- 어떻게 실패하나: `C2` 의 오라클은 `status.code.reason == "watch-member-gone"` 집계다. 그런데 `UndeterminableReason` 은 `#[serde(rename_all = "snake_case")]` 이므로 **JSON 에 실리는 값은 `watch_member_gone`** 이다(하이픈 표기는 `name()`·`label.rs` 의 화면용이고, `docs/gates/F09.md:37` 도 `watch_member_gone` 으로 적었다). 그 조건대로 집계하면 **어떤 경우에도 0 이 나오고, `C2` 는 *"0 이어도 적는다"* 로 통과한다.** 그리고 `A2` 는 자기 모집단에서 뺄 것을 `C2` 의 그 판정으로 식별하므로, 제외 집합이 빈 채로 `A2` 가 「보존 경로가 깨졌다」로 빨개진다 — `A2` 가 막으려던 혼동이 그대로 돌아온다. `Undeterminable` 이 `Stale` 을 가리는 경로가 84 회 관측된 축인데, 그것을 세는 조건이 0 을 보고하고 아무도 안 이상해한다.

### 넓힘이 답을 **나쁘게** 만들었을 때 불통과를 내는 조건이 하나도 없다
- 모집단: 원의도
- 유효성: 참
- 해악도: 실패
- 어디가 걸리나: 조건 `A2`·`C1`·`C2` · `crates/pal-core/src/binding.rs:636-643`
- 어떻게 실패하나: 조건 자신이 적은 대로 `binding.rs:636-643` 의 ②가 ③④ 앞에서 반환하므로, 넓혀서 새로 든 감시 원소 하나가 안 보이면 **그 결박은 `Stale`/`Fresh` 라는 답 자체를 잃고 `Undeterminable` 이 된다.** 그리고 조건 자신이 *"코퍼스 실측 callers 84 회 · symbol 0 회"* 라고 적었다 — 즉 이 전환은 드문 사고가 아니라 **예상된 대량 발생**이다. 그런데 `A2` 는 그것을 모집단에서 **빼고**, `C1`·`C2` 는 **세어 적기만** 하고, `B3` 는 양성 축만 보고, 되돌림 기준(*"이 수가 N 이상이면 넓힘을 물린다"*)이 어디에도 없다. **37 중 스물이 「유효한가」에 답할 수 없게 되어도 조건 전부가 초록이다** — 목적 기여가 파는 것이라 적은 한 문장(*"이 코드에 걸린 결정이 아직 유효한가에 기계가 답한다"*)이 회차 뒤에 **더 적게** 답하는데, 그것을 잡는 자리가 없다. 넓힘이 제품의 핵심 답을 퇴행시키고, 게이트는 「반경이 처음으로 쓰였다」로 닫힌다.
- 고치면 서는 것: 판정 분포의 **퇴행 상한**(예: `undeterminable` 로 옮겨간 건수가 새로 `stale` 이 된 건수보다 많으면 불통과 또는 그 결박을 안 넓힌다)을 사전 등록

### `E1` 의 넷째 팔과 `E3` ⑴ 이 서로를 부정한다 — 없는 경로를 벤치한다
- 모집단: 자기장치
- 유효성: 참
- 해악도: 실패
- 어디가 걸리나: 조건 `E1`·`E3` · `crates/pal-cli/src/touch.rs:18`
- 어떻게 실패하나: `E1` 은 벤치의 넷을 `symbol` · `callers` · `closure(2)` · **증분 갱신**으로 적었다. 같은 묶음의 `E3` ⑴ 은 *"등록된 합격선이 「증분 갱신이 전체 재계산보다 빠르다」인데 **증분으로 상태를 갱신하는 경로가 이 빌드에 없다**"* 고 적었다. **둘이 동시에 참일 수 없다.** 저장소에서 「증분 갱신」이라는 낱말이 서는 자리는 `crates/pal-cli/src/touch.rs:18` 의 *"`WATCH` 색인이고 F09 가 증분 갱신을 **위해** 세웠다"* 뿐이다 — 색인은 있고 갱신 경로는 없다. 무엇을 재는지 **문면으로 안 갈린다.** 실행자가 넷째 팔을 임의의 대리물(색인 조회 대 전수 훑기 등)로 바꿔 재고, 그 치환이 어디에도 안 적힌다 — 「선 장치 · 안 닫힌 판정」을 막으려던 `E6` 의 취지가 `E1` 안에서 무너진다.

### `E3` 의 오라클이 딛는 `changed` 축이 `E1` 의 등록된 축에 없다
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: 조건 `E1`·`E3` · `crates/pal-query/tests/bench.rs:1`
- 어떻게 실패하나: `E3` 는 *"⑵ 의 뒤집힘이 **`E1` 의 산출로** 재현된다 — 같은 벤치를 작은 `changed` 와 큰 `changed` 로 돌려 비가 1 을 건너뛴다"* 로 RED 를 만든다. 그런데 `E1` 이 등록한 축은 **반경 넷 × 규모 둘 × 회차 셋**이고 `changed` 크기는 축에 없다. `E3` 가 요구하는 산출이 `E1` 을 다 돌려도 **생기지 않는다.** `E1` 이 초록인데 `E3` 를 닫을 산출이 없어 `E3` 가 「미측정」으로 남거나, `E1` 의 축을 말없이 늘려 `E2` 의 열 규격(비·분산·비)이 어느 축에 대한 것인지 갈린다.
- 고치면 서는 것: `E1` 의 축에 `changed` 크기(작게·크게)를 명시로 더하고 `bench/radius.tsv` 에 그 열을 세운다

### `A3-b` 가 「근사의 크기」라 부르며 재는 것은 다른 양이다
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: 조건 `A3`·`A3-b` · `crates/pal-core/src/repo.rs:231` · `.palimpsest/intent/bindings.jsonl:2`
- 어떻게 실패하나: `A3-b` 는 근사를 *"base 커밋은 「결박이 걸린 **커밋** 상태」이고 「결박한 순간의 워킹트리」가 아니다"* 로 정확히 적고, 그 **크기**를 ⑵ *"base 커밋에 없던 새 감시 원소의 건수"* 로 잰다. 그런데 그 근사의 크기는 **원소의 부재**가 아니라 **같은 원소의 내용 차이**다 — base 커밋의 본문과 결박 시점 워킹트리 본문이 다른 새 감시 원소의 수. 실측으로 `bindings.jsonl` 의 37/37 이 `TreeRef::Worktree` 이므로(기준 일곱 전부) **이 차이는 원리상 0 이 아니고**, 그 자리에서 나온 새 `digest` 는 뒤에 **「결정 뒤에 변한 것」이 아닌데 `Stale` 로 켜진다.** `A3-b` 가 재는 ⑵ 는 그 수를 한 건도 안 센다. 근사에서 나온 거짓 `stale` 이 `C3` 의 정반합에 섞여 들어가고 `H1` 의 증인이 그것으로 세워진다 — 사람이 결박문을 읽어도 「기준 시점 때문에 켜졌다」는 못 가린다.
- 고치면 서는 것: `widen/decisions.tsv` 에 **base 커밋 digest ≠ 결박 시점 워킹트리 digest 인 새 원소 수** 열을 세우고, `C3`·`H1` 에서 그 집합을 따로 표시

### `F3` 의 삽입 좌표 `:66` 이 `C2` 절의 결론 산문을 새 `C6` 절 아래로 밀어 넣는다
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `docs/plan/02-order.md:66` · `docs/plan/02-order.md:67-69`
- 어떻게 실패하나: `F3` 는 *"`:66` 뒤에 새 `###` 절(「`C6` 행이 움직였다」)을 열고"* 라고 적었다. 실측: `docs/plan/02-order.md:61-65` 가 `C2` 절의 표, `:66` 이 빈 줄, **`:67-69` 가 그 절의 결론 산문과 판정 링크**(*"막던 것이 풀렸다 … 판정: `docs/gates/rust-scope-references.md`"*)다. `:66` 뒤에 새 `###` 를 열면 **`C2` 절의 결론 세 줄이 `C6` 절 아래로 넘어간다** — `F3` 가 막으려던 것(「`C6` 의 정정이 `C2` 절 아래 앉는다」)의 정확한 거울상이 일어난다.
- 고치면 서는 것: 삽입 좌표를 `:69` 뒤(또는 `:71` 의 `### 실측이 갈린 자리 셋` 앞)로

### `F` 묶음이 여는 그 문서에, 이 회차의 전제를 **현재형으로 부정하는 줄**이 남고 어느 조건도 그것을 안 잰다
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `docs/plan/02-order.md:109` · `docs/plan/02-order.md:68`
- 어떻게 실패하나: 원문 13 행과 목적 기여가 이 회차의 근거로 든 것은 *"2 단계(파일 경계를 넘는 참조 해소)가 섰다(2026-09-10)"* 다. 그런데 `F1` 이 고치는 바로 그 표의 **바로 윗 행**이 지금 이렇게 적혀 있다 — `docs/plan/02-order.md:109`: *"1 이 갖춰져도 **파일 안에서만 답한다** — `pal touch` 가 **지금** 「파일 안의 관계만입니다」를 찍는다."* **저장소 전체에 그 문장은 이미 없다**(`grep "파일 안의 관계만" crates/` → 0 건). `:68` 도 *"파일 경계 넘기는 **그대로 미구축**"* 으로 남아 있고, 같은 문서 `:259` 는 이미 *"남은 자리는 §2 의 3 번 하나다"* 로 반대를 적어 **문서가 자기와 갈려 있다.** `F1`·`F2`·`F3` 는 3 번 행과 `C6` 행만 다루고 이 어긋남에는 아무 조건도 없다. 회차가 끝나면 순서표가 「2 번 미구축 · 3 번은 2 번에 매달림」이라 적은 채로 3 번의 산출을 싣는다 — `F1` 이 고치려던 병(*"측정이 거짓임을 드러낸 문서 한 줄"*)이 같은 표 안에 하나 더 남는다.

### 원문 28 행이 잠근 「기본 반경을 안 옮긴다」에 기계 가드가 없다
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-cli/src/main.rs:139`
- 어떻게 실패하나: `## 범위 밖` 이 *"기본 반경을 `symbol` 에서 옮기는 것"* 을 초석으로 빼 놓았고 원문 28 행이 *"안 옮긴다 — 이번엔"* 으로 소유자가 닫았다. 그런데 그것을 재는 조건이 없다. 한 줄 대조로 **결정론적으로 잴 수 있다** — `crates/pal-cli/src/main.rs:139` 의 `#[arg(long, default_value = "symbol")]`. 비교해 보라: 이 회차는 **문서 한 줄**(`F2` · §1 의 `C6` 행)에는 `git diff` 오라클을 붙였는데, **소유자가 명시로 잠근 코드 기본값**에는 아무것도 안 붙였다. 넓히는 경로를 만들면서 기본값을 같이 옮기는 것이 가장 편한 길이고(그러면 `B3` 의 양성 축 셋이 공짜로 움직인다), 그 이동을 잡는 조건이 없다.

### 「**근거를 대고** 넓힌다」의 근거 ↔ 행동 결박이 어느 조건에도 없다
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: 조건 `B1`·`B2`·`B3` (`.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md`)
- 어떻게 실패하나: 원문 25 행은 *"기존 결박 37 건을 어떻게 하나? → 내가 **근거를 대고** 넓힌다"* 이고, `## 차선책` 둘째 항은 *"**근거 없이 넓히는 것이 이 회차가 막아야 할 형태**"* 로 그것을 못 박는다. 그런데 `B1` 은 `근거` **열의 존재**만 요구하고, `B2` 는 *"근거가 결박문을 읽은 판정이다"* 만 요구하고, `B3` 는 **넓혀졌는지**만 본다. **`근거` 칸에 「이 결정은 호출자 변경에 영향받지 않는다」라고 적고도 `새반경 = callers` 로 넓힌 행이 있어도 조건 전부가 통과한다.** 반대 방향(영향받는다고 적고 안 넓힌 것)도 같다. 판정과 행동을 잇는 열도, 검산도 없다.
- 고치면 서는 것: `decisions.tsv` 에 `판정(영향받는다/아니다)` 열을 세우고 **`판정 == 아니다` 이면서 `새반경 != 옛반경` 인 행이 0** 을 결정론적 조건으로 (`B2` 의 정반합은 그 위에 얹는다)

### ⟨결정론적⟩ 태그가 붙었는데 판정자가 사람의 문서 읽기인 조건 다섯
- 모집단: 규약
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: 조건 `D2`·`D3`·`D4`·`F1`·`F3` · `xtask/src/main.rs:618`
- 어떻게 실패하나: `D2`·`D3`·`D4`·`F1`·`F3` 는 전부 ⟨결정론적⟩ 이고 오라클이 *"새 게이트가 ⑴ 인용하고 ⑵ 좌표를 적고 ⑶ 사실을 함께 적는다"* · *"한 줄로 적힌다"* · *"실측대로 고쳐졌다"* 형태다. 이것은 **체크리스트(의미론적 검증)**이고, 기계가 답하는 것이 하나도 없다. 같은 묶음의 `F2` 는 `git diff` 로, `D1` 은 `cargo xtask check` 의 「원장 둘 대조」로 실제 기계를 걸었다 — **할 수 있다는 것을 같은 문서가 두 번 보여 준다.** 특히 `D2` 는 ㈂-a 의 **본체**라고 스스로 적은 조건이다. 규약 §5 가 ⟨결정론적⟩ 을 기계 판정으로 보내므로 이 다섯은 **정반합도 기계도 안 거친 채** 「통과」가 된다. 태그가 정직하면 정반합이 붙고, 기계를 걸 수 있으면 `grep`/`xtask` 검사가 붙는다 — 지금은 둘 다 아니다.

### `A5-a` 의 RED 는 회차의 의도가 아니라 **검사 12** 를 재고, 등록할 「넷째 사유」가 `A3` 와 갈린다
- 모집단: 자기장치
- 유효성: 추정
- 해악도: 거짓신호
- 어디가 걸리나: `xtask/src/main.rs:2041-2050` · `xtask/src/main.rs:2074`
- 어떻게 실패하나: `A5-a` 는 `WatchEntry` 생성 자리를 `WATCH_ENTRY_SITES` **밖의 파일**에 일부러 두어 검사 12 를 빨갛게 만들고, 그 다음 *"셋과 다른 **넷째 종류** — 옛 결박에서 물려받는다"* 를 사유로 등록해 초록으로 만든다. 실측으로 보면 둘 다 어긋난다. ① `A3` 가 **새** 감시 원소의 `digest` 를 base 커밋 **투영에서 읽으라**고 못 박았으므로 그 자리의 사유는 등록된 셋과 **같은** *"투영에서 읽는다"* 다. ② 옛 원소는 `A1` 의 여섯째대로 **그대로 물려받으므로** 새 `WatchEntry {` 리터럴이 필요하지 않다(복제하면 검사에 안 걸린다). 즉 **빨감도 넷째 사유도 이 회차의 설계가 요구한 것이 아니라 조건이 요구해서 만들어지는 것**이고, 등록될 사유 문장은 `WATCH_ENTRY_SITES` 라는 **영구 등록처에 사실이 아닌 까닭**으로 남는다. 그리고 이 조건은 완수 조건이 **구현 파일 배치**를 지정한다.

### 판정 표기의 정본으로 지목한 파일이 **없는 파일**이다
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-intent/src/round_condition.rs:39-49` · `.claude/skills/round/bin/record.py:149`
- 어떻게 실패하나: 서두의 낱말 절이 *"파서가 아는 값은 넷뿐이고(`.claude/skills/round/bin/record.py:151` · `crates/pal-cli/src/round/condition.rs:41-47`)"* 라고 적었다. 앞의 좌표는 정확하다(`판정값 = [...]`). 뒤의 좌표는 **그 경로에 파일이 없다** — `crates/pal-cli/src/round/` 에는 `approval.rs`·`ledger.rs`·`mod.rs`·`status.rs`·`stop.rs`·`verify.rs` 뿐이다. 정본은 `crates/pal-intent/src/round_condition.rs:39-49` 의 `enum Verdict` 이고, 같은 `record.py` 의 주석이 *"조건 문법의 정본은 `crates/pal-intent/src/round_condition.rs` 다"* 로 이미 적고 있다. 이 절은 *"여섯 절 밖을 안 가리킨다"* 를 선언한 자리이고 조건 전부가 쓰는 낱말의 근거다. 다음 컨텍스트가 그 좌표를 열면 아무것도 없다.

### `## 원문` 이 원문만이 아니다 — 24 행이 여섯 절 밖의 `## 개정` 을 가리킨다
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: 조건 문서 `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md` 의 `## 원문` 24 행과 `## 완수 조건` 서두
- 어떻게 실패하나: `## 원문` 은 *"압축하지 않는다"* 로 소유자의 말을 그대로 싣는다고 선언한 절이다. 그런데 24 행의 인용 안에 **⚠ `㈁ 은 라운드 3 에서 빠졌다 — 아래 `## 개정``** 이라는 편집자의 주석이 박혀 있고, **`## 개정` 은 받은 여섯 절에 없다.** 그래서 문면만으로는 *"소유자가 ㈀+㈁+㈂ 전부라고 했다"* 와 *"㈁ 은 빠졌다"* 중 어느 쪽이 소유자의 말인지 안 갈린다. (완성선이 하나 빠진 근거 자체는 `## 범위 밖` 첫 항이 여섯 절 안에서 지므로 **덮이기는 한다** — 걸리는 것은 원문과 편집이 한 인용 안에 섞인 것이다.) 이 절은 다음 회차가 *"소유자가 실제로 무엇을 요구했나"* 를 되짚는 유일한 자리다. 편집 주석이 인용부호 안에 들어가면 그 구별이 영구히 사라진다.

### 목적 기여가 근거로 든 「파일 경계를 넘는 참조」가 실제로 쓰였는지 재는 조건이 없다
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: 조건 `B1` · `docs/plan/02-order.md:109`
- 어떻게 실패하나: 목적 기여는 *"순서표 §2 의 2 번(파일 경계를 넘는 참조 해소)이 2026-09-10 에 서면서 **넓은 반경이 빈 집합이 아니게 됐고**"* 를 이 회차가 지금인 이유로 든다. 그런데 `decisions.tsv` 의 열(`호출자수`·`감시전`·`감시후`)에는 **파일 축이 없고**, 새로 든 감시 원소가 대상과 **다른 파일**에 있는지를 묻는 조건이 하나도 없다. 전부 같은 파일 안의 호출자였다면 이 회차는 2 단계 전에도 가능했고 목적 기여의 전제가 안 선다 — 그 반증 가능성이 조건에서 빠져 있다. 열 하나 더하면 결정론적으로 닫힌다.

### `H1` 의 대체 증인이 원문 29 행과 **다른 것**을 잰다 — 「출력의 존재」로 되돌아간다
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: 조건 `H1`·`C3` · `docs/gates/F09.md:77`
- 어떻게 실패하나: `H1` 은 *"출력의 존재는 재검토가 아니다"* 를 못 박고 셋 중 하나의 좌표(결박문 수정 · 재결박 · 이슈)를 요구한다. 그런데 같은 조건의 ⚠ 가 *"`C3` 가 `대조불가` 면(새 `stale` 0 건) 증인이 **`C2` 의 수로 내려간다**"* 고 적었다. `C2` 의 수는 `undeterminable{watch_member_gone}` 의 **건수**다 — 그것은 재검토가 아니라 **장치가 낸 수**이고, 원문 29 행이 잠근 증인(*"넓힌 반경이 실제 재검토를 불렀다"*)과 축이 다르다. 게다가 위 발견대로 `C2` 의 오라클은 지금 언제나 0 을 낸다. **효과 조건이 「출력 하나」로 닫히는 길이 ⚠ 안에 남아 있다.**
- 고치면 서는 것: 새 `stale` 이 0 이면 효과는 `대조불가`로 적고 **증인을 대체하지 않는다** — 규약이 「대조 불가는 통과로 안 센다」를 이미 쓴다(`docs/gates/F09.md:77`)

### `G1` ⓑ 가 지목한 좌표가 트리거 선언이 아니다
- 모집단: 저장소
- 유효성: 참
- 해악도: 미관
- 어디가 걸리나: `docs/sunset.toml:26` · `xtask/src/main.rs:3009`
- 어떻게 실패하나: `G1` ⓑ 는 *"`docs/sunset.toml:33` — 선언은 「기계가 쓰는 것 = `.json`」인데 구현은 …"* 로 적었다. 실측: `:33` 은 검사 19 의 모집단에 관한 주석이고, 트리거 선언은 **`docs/sunset.toml:26`**(`트리거 = ".palimpsest/rounds/*/*.json"`), 인용한 문장은 `:23` 이다. 내용은 맞다(구현이 `xtask/src/main.rs:3009` 의 `아래_전부` 로 재귀하고 확장자만 맞힌다) — 좌표만 틀렸고 그 좌표가 새 이슈 본문에 그대로 실린다.

---

## 합이 의도를 덮나

`## 완수 조건` 끝의 자기 주장 표는 **근거로 안 봤다.** 아래는 원문을 다시 읽고 내가 댄 것이다.

| 원문이 잠근 것 | 덮이나 | 근거 |
|---|---|---|
| 23 행 *"(가) 반경·전파 잇기"* | **절반만 · 문면으로 덮인다** | 뒷 절반(전파)은 `## 범위 밖` 둘째 항이 `binding.rs:504` 인용으로 사유를 진다 — 순환 참조가 아니고 실측으로 확인했다 |
| 24 행 *"㈀ + ㈁ + ㈂ 전부"* | **덮인다(조건부)** | ㈁ 은 `## 범위 밖` 첫 항이 문면으로 진다. 다만 24 행 자체가 여섯 절 밖(`## 개정`)을 가리킨다 — 위 발견 |
| 25 행 *"내가 **근거를 대고** 넓힌다"* | ❌ **안 덮인다** | 근거의 **존재**만 재고 **근거 ↔ 행동**을 재지 않는다 — 위 발견 |
| 26 행 *"이 회차가 만드는 장치에 대한 발견은 분할"* | 덮인다 | `G1` |
| 27 행 *"사람이 결박문을 읽고 참·거짓을 가른다"* | 덮인다 | `C3`(정반합) |
| 28 행 *"기본 반경을 옮기나? → **안 옮긴다**"* | ❌ **안 덮인다** | 기계 가드가 없다(`main.rs:139`) — 위 발견 |
| 29 행 *"§8 효과의 증인은? → **넓힌 반경이 실제 재검토를 불렀다**"* | ❌ **⚠ 로 새어 나간다** | `H1` 의 대체 증인이 축을 바꾼다 — 위 발견 |
| 목적 기여 *"2 단계가 서면서 넓은 반경이 빈 집합이 아니게 됐다"* | ❌ **안 덮인다** | 파일 경계를 넘는 감시 원소가 실제로 들었는지 재는 조건이 없다 — 위 발견 |
| 목적 기여 *"「유효한가」에 기계가 답한다"* | ❌ **역방향이 안 덮인다** | 넓혀서 답을 **잃은** 경우에 불통과를 내는 조건이 없다 — 위 발견(해악도 「실패」) |

**가장 큰 빈 곳 하나를 고른다면**: 조건 전부가 「넓혀졌나 · 보존됐나 · 적혔나」를 재고, **「넓혀서 답이 좋아졌나」를 재는 조건이 하나도 없다.** `C1`·`C2` 는 분포와 건수를 *적으라*고만 하고 어느 값에서도 불통과가 안 난다.

## 의도보다 큰가

| 조건 | 원문이 요구했나 | 판정 |
|---|---|---|
| `F1`·`F2`·`F3` (㈄ 순서표 정정) | **아니다** — 조건 문서가 스스로 *"소유자의 「전부」에 없다. 이 회차가 더했다"* 로 적었다 | **의도보다 크다.** 다만 자진 표기했고 *"승인이 덮는다(아직 승인 전이다)"* 로 조건을 달았다 — 승인 전에 실행되면 그것은 조용한 확장이다. 그리고 이 확장이 위의 좌표 결함 둘(`F3` 의 `:66`, `:109` 방치)을 지고 있다 |
| `A5-a` (검사 12 를 일부러 빨갛게 만들고 넷째 사유를 등록) | 아니다 | **의도보다 크다** — 완수 조건이 **구현 파일 배치**와 **등록처에 남을 사유 문장**을 지정한다. 위 발견 |
| `B2-a` (커밋 SHA 를 올리는 절차) | 원문에 없다 | 크지만 **정당하다** — 25 행의 *"내가"* 가 문면으로 안 갈리는 것을 어느 쪽이어도 서게 만든다. 「참여 = 가시성 + 개입 가능성」과 맞다 |
| `E1`~`E6` · `D1`~`D4` | 24 행 ㈂-a·㈂-b 가 요구했다 | 안 크다 |

---

## 내가 기각한 것

### `B2-a` 의 오라클이 「소유자에게 올렸다」를 안 잰다
- 기각 사유: `git merge-base --is-ancestor` 는 *"판정을 보이는 자리에 먼저 세웠다"* 를 기계로 재고, 그것이 소유자의 요구(참여 = 가시성 + 개입 가능성)의 전부다. 「올렸다」는 메인의 보고 행위이고 완수 조건이 질 것이 아니다 — 승인 요청을 조건으로 만들면 그것이 책임 전가가 된다
- 모집단: 원의도
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: 조건 `B2-a` (`.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md`)

### 넓힘이 일회성 스크립트여도 전부 통과한다 — 제품 표면을 안 가린다
- 기각 사유: 처음엔 걸림으로 적었다. 물린 까닭은 원문 28 행이 *"기본 반경을 옮기나? → **안 옮긴다 — 이번엔**"* 으로 이 회차를 **측정 회차**로 못 박았고, `## 범위 밖` 이 표면 결정을 *"재고 나서 다음이 정한다"* 로 미룬 것이 소유자의 답이기 때문이다. 표면을 요구한 대목이 여섯 절에 없다. `A5`(재현성)가 *"손으로 고친 JSONL"* 은 이미 막는다
- 모집단: 원의도
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: 조건 `A5` · `crates/pal-intent/src/store.rs:670`

### `B3` ⑶ 의 GREEN 이 도달 불가다
- 기각 사유: 좌표를 직접 읽어 **도달 가능함을 확인했다.** `crates/pal-query/src/lib.rs:849` 의 `.filter(|b| b.target != symbol.id)` 는 *"자기 결박"* 만 뺀다. 새로 든 호출자 `C` 에 대해 `watching(C)` 가 돌려주는 결박의 `target` 은 `C` 가 아니므로 필터를 지난다. 그리고 착수 시점 37 건의 감시 집합이 전부 대상 자기 하나뿐이므로(실측) **어떤 호출자에 대해서도 그 줄이 지금 0 이다** — RED 도 보장된다. `pal touch` 의 출력 문구 `이 좌표를 지켜보는 것` 도 실재한다(`crates/pal-cli/src/touch.rs:290`)
- 모집단: 저장소
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: `crates/pal-query/src/lib.rs:849` · `crates/pal-cli/src/touch.rs:290`

### `G2` 의 「28 검사」가 틀렸다
- 기각 사유: 세어서 확인했다 — `xtask/src/main.rs:620` 의 `checks` 배열이 정확히 **28** 이고, `A5-a` 가 지목한 「검사 12」도 `xtask/src/main.rs:2026` 의 주석 번호와 맞다(실행 순서로는 13 번째다). `conditions-audit/r<n>-raw.md` 를 요구하는 검사도 실재한다(`xtask/src/main.rs:4496`)
- 모집단: 자기장치
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: `xtask/src/main.rs:620` · `xtask/src/main.rs:4496`

### `D` 묶음의 좌표가 틀렸다
- 기각 사유: 전부 줄 단위로 맞았다 — `corpus/criteria.toml:6840`(`false_positive_rate_callers_radius_percent = 30`) · `:7346`(`false_positive_split_by_identity_grade`) · `docs/gates/F09.md:35`·`:37`·`:77-81`·`:79-81`·`:85-88` · **`scripts/f09-verify.py:715`(갈래 필터)와 `:709-711`(`관측된_사유` 에 담는다) 둘 다 한 줄도 안 어긋났다.** `PM2-11` 이 정정한 *"표본기가 구조적으로 못 본다 → 표본에 안 들어온다"* 도 실측과 맞다
- 모집단: 저장소
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: `scripts/f09-verify.py:709-715` · `corpus/criteria.toml:6840`

### 「대상 37」의 모집단이 안 선다
- 기각 사유: 실측으로 정확히 섰다 — `.palimpsest/intent/bindings.jsonl` 은 머리 한 줄 + **결박 37**, `radius` 가 **37/37 `symbol`**, 감시 크기 합 **37**(건당 1), `bound_at` **37/37 `worktree`** · base 커밋 **일곱**. 서두 표의 값이 전부 재현됐다. 착수 커밋을 박아 사후 재정의도 막혔다
- 모집단: 저장소
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: `.palimpsest/intent/bindings.jsonl:1` · `crates/pal-core/src/repo.rs:228`
