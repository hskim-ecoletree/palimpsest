# 착수 관측 — 순서표가 이 회차의 전제를 **현재형으로 부정한다**  ⟨조건 `F4` 의 RED⟩

> 회차 `2026-09-12-binding-radius-in-use` · 2026-09-13 · 잰 HEAD `fd176a8`
>
> ⚠ **문면을 여기 보존한다.** 순서표 본문에는 그 문구를 안 남겼다 — 남기면 조건 `F4` 의
> 오라클(`grep -F` 가 0 건)이 **스스로 깨진다.** 실측의 자리는 이 파일이고, 순서표는
> 정정만 진다.

## ① 순서표 §2 의 2 번 행이 적고 있던 것 — 착수 커밋 `1276b8f`

⚠ 인용에서 마크다운 링크 하나(`00-goals.md` 를 가리키는 것)의 괄호를 뺐다 —
이 파일의 상대 경로에서는 그 링크가 안 서서 죽은 링크 검사가 발화한다. **그 밖의
문면은 그대로다.**

```
| **2** | **파일 경계를 넘는 참조 해소** | `C2` | 같은 행. 1 이 갖춰져도 파일 안에서만 답한다 — `pal touch` 가 지금 *"파일 안의 관계만입니다"* 를 찍는다. 「호출자 7 (exact 5 · candidate 2)」 줄(00-goals §1)이 이 자리다 |
```

## ② 그 문구가 코드에 있나 — **0 건**

```
$ grep -rn "파일 안의 관계만" crates/ ; echo "rc=$?"
rc=1  (1 = 0 건)
```

## ③ 지금 `pal touch` 가 그 자리에 찍는 것

```
508:            "  ※ **파일 간 해소를 이 답에서는 안 쟀습니다** — 0 건이라는 뜻이 아닙니다"
513:        "  ※ 파일 간 해소 — ⓐ `cross-file-import` {}/{} · ⓑ `path-resolution` {}/{} (선 것/짝)",
```

## ④ 같은 문서가 자기와 갈려 있었다

`docs/plan/02-order.md:68` 이 *"파일 경계 넘기는 **그대로 미구축**"* 으로 남아 있고,
같은 문서 `:259` 는 이미 *"남은 자리는 §2 의 3 번 하나다"* 로 **반대**를 적었다.

## ⑤ 왜 이것이 이 회차의 조건인가

원문 13 행과 `## 목적 기여` 가 이 회차가 **지금**인 근거로 든 것이 *"2 단계(파일 경계를
넘는 참조 해소)가 섰다(2026-09-10)"* 다. 그 표의 **바로 윗 행**이 그것을 현재형으로
부정하고 있었다 — 고치지 않으면 **회차가 끝난 뒤 순서표가 「2 번 미구축 · 3 번은 2 번에
매달림」이라 적은 채로 3 번의 산출을 싣는다.**

⚠ `:68` 은 **2026-09-08 회차의 결론 기록이라 덮지 않았다** — `C2` 절 아래에 날짜와 함께
정정을 얹었다(`F3` 이 세운 새 절 앞).
