# `B2` 판정 패킷 — 결박 37 건의 `판정`·`근거` 와 그 결박문 전문

> 회차 `2026-09-12-binding-radius-in-use` · 2026-09-13 · 판 **3** 의 입력
> 만든 자: **메인**. `dialectic/3-design.md` 의 §메인이 판을 시작하기 전에 할 것 셋 이 요구한 것이다.
>
> ★ **이 파일이 정(正)이 받는 유일한 자료다.** 설계문이 금지한 것은 여기 없다 —
> `intent.md` 전문 · `widen/README.md` · `docs/gates/binding-radius-in-use.md` ·
> `dialectic/` · `review/` · `conditions-audit/` · `premortem/` · 그리고 아래 두 결박문:
>
> ⚠⚠ **`3a3cb89428ed320b` · `6c6e402d958fc5e1` 의 결박문은 이 패킷에 없다.** 그 둘은 이
> 회차가 **자기 결정을 결박한 것**이고 그 `note` 안에 이 회차의 실측과 정반합 판 2 의
> 반(反)·합(合)이 한 일이 산문으로 적혀 있다 — 넘기면 격리가 깨진다(설계자가 지목했다).
> 정본은 **39 건**이고 이 패킷은 **37 건**을 담는다.

---

## 0. 기계가 이미 답한 것 — 판정에 안 넣는다

아래 열한 값은 **메인이 재서 여기 싣는다.** 설계자가 독립으로 잰 값과 **전부 같았다** —
두 자리가 같은 수를 냈다는 사실을 함께 적는다.

```
㈀1 행 · 열 : 37 · 14
㈀2 판정 분포 : {'아니다': 19, '영향받는다': 18}
㈀3 근거 빈 셀 : 0
㈀4 중복 문면 : 0 · 길이 min/중위/max : 42 74 191
㈀5 (판정,새반경) : {('아니다', 'symbol'): 19, ('영향받는다', 'callers'): 18}
㈀6 note 조달 : 37 / 37 · 길이 min/중위/max : 152 516 1378
㈀7 fd176a8 이후 바뀐 셀 : [('49714573e0fa654c', '대상digest차이')]
㈀8 S1(아니다·호출자0) 6 · S2(영향받는다·호출자0) 7 · S3(아니다·호출자>0) 13 · S4(영향받는다·호출자>0) 11
㈀9 「호출자 0」 꼬리 행 : 7 ['389ce29274d77a47', '5b9331b398e8ff23', '82d726bad31e77f6', 'a5b4ba095bf405f7', 'ddb749e489242cab', 'fcd48a9033fd1fb0', 'fd4efc1eaea040c4']
   그 행이 전부 영향받는다·callers·호출자0 : True
호출자 0 인 행 전체 : 13 {'영향받는다': 7, '아니다': 6}
대상digest차이=참 : ['49714573e0fa654c']
정본 총 결박 : 39
```

★ **`판정`·`근거` 두 열은 `fd176a8`(넓히기 전 커밋) 이후 한 셀도 안 변했다.** 바뀐 셀은
`49714573e0fa654c` 의 `대상digest차이` 하나뿐이다 — 그 불변성이 `B2-a` 가 「넓히기 전에
섰다」로 지는 증거이므로 **이 판의 결과로 그 두 열을 고치지 않는다**(설계문 금지역 ㉮).

## 1. 계층 넷 — 행마다 어느 계층인지 아래 표가 싣는다

| 계층 | 무엇 | 건수 |
|---|---|--:|
| **S1** | `아니다` · 호출자 0 | 6 |
| **S2** | `영향받는다` · 호출자 0 | 7 |
| **S3** | `아니다` · 호출자 > 0 | 13 |
| **S4** | `영향받는다` · 호출자 > 0 | 11 |

## 2. 37 행 — `decisions.tsv` 의 14 열 그대로

**바이트까지 같다** — `widen/decisions.tsv` 에서 그대로 옮겼다. 계층 칸만 메인이 더했다.

| 계층 | 결박id | 대상심볼 | 대상파일 | 옛반경 | 새반경 | 호출자수 | 감시전 | 감시후 | 타파일새원소 | 기준시점커밋 | 대상digest차이 | base에없던새원소 | 판정 | 근거 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| S3 | 1cc70cc19ef92186 | 89c2aab3184831be1555d51244ca187228f2b7b30e030c02f48487ef8998103a | crates/pal-core/src/envelope.rs | symbol | symbol | 17 | 1 | 1 | 0 | daf15debf4399f482d42c92e693646371ace64b6 | 거짓 | 0 | 아니다 | 낱말 「생략」을 elision 에 주고 fold 에는 「이관」을 준다는 표기 배정이고, 세 자리의 이름은 부르는 쪽과 무관하게 정해진다 |
| S4 | 1cf63ec5e0e9937a | 6b4389c2b18699084cbc6b2d3ff11b686bab5e15377049d54f858d7c704b7e4d | crates/pal-extract/src/rust.rs | symbol | callers | 1 | 1 | 2 | 0 | e9402bc63ab875ab933e7668098ce54240b80124 | 거짓 | 0 | 영향받는다 | ExportSet 이 담을 모집단을 ledger 의 stitch_of 가 최상위만 EXPORTS 로 옮기는 것에 맞춘 결정이라, 그 소비 쪽이 바뀌면 export_digest 와 EXPORTS 가 다른 모집단을 잰다 |
| S2 | 389ce29274d77a47 | b3429f4be3367c44c6ddfdddccc230e50c28a8612af8aef2ffb1b34e1ca5bfaf | crates/pal-core/src/catalog.rs | symbol | callers | 0 | 1 | 1 | 0 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 0 | 영향받는다 | 로드맵 26 을 다 적는 길과 파일을 코드에서 생성하는 길을 기각한 결정이라, 이 값을 읽는 대조가 파일을 생성하는 쪽으로 바뀌면 양방향 검사가 항등식이 된다 ⟨호출자 0 — 넓혀도 감시가 안 커진다. 멤버 호출(x.foo())을 장치가 못 보는 것이고 pal touch 가 그 문장을 스스로 찍는다. 근거가 안 선 것이 아니다⟩ |
| S1 | 39b92bd51bbc82c6 | 80cd88b02cc206949e89eb9a0cf7094a02cfb2e20173468f9c4c6ae37f118917 | crates/pal-core/src/scope.rs | symbol | symbol | 0 | 1 | 1 | 0 | e9402bc63ab875ab933e7668098ce54240b80124 | 거짓 | 0 | 아니다 | 보이는 것 중 가장 뒤를 고르고 「가장 앞선 것」 팔은 TDZ 전용으로 남긴다는 이 함수 본문의 해소 규칙 선택이다 |
| S4 | 3d9979946eb321de | 6f71aabf4b8aa59a0a496a310e8c31f720be605be26e2a16b8682d7de2147b1f | crates/pal-core/src/repo.rs | symbol | callers | 77 | 1 | 78 | 73 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 3 | 영향받는다 | 카탈로그의 인자 타입이 어댑터가 내는 스키마와 같은지 아무도 안 댄다는 미포착 범위를 적은 결정이라, 이 타입을 인자로 쓰는 바깥 표면이 늘거나 바뀌면 그 공백을 다시 재야 한다 |
| S3 | 4065e80475cf1c72 | bd26d1d8a3ee02466afc38a29580d36ab3f2593ac840d2260c9a6b2e37272c38 | xtask/src/main.rs | symbol | symbol | 1 | 1 | 1 | 0 | daf15debf4399f482d42c92e693646371ace64b6 | 거짓 | 0 | 아니다 | 활용형·낱말 경계·기계 표식 예외로 패턴을 짜고 검사 자신을 대상 밖에 둔다는 이 검사 내부의 수단 설계다 |
| S4 | 45d8808585ee0d50 | c6c07834dbf6e7ea5648280b5541b3a84aa0ec538d3ecfe77ec74ed5beb6c64a | crates/pal-cli/src/label.rs | symbol | callers | 10 | 1 | 11 | 0 | daf15debf4399f482d42c92e693646371ace64b6 | 거짓 | 2 | 영향받는다 | 병기를 조각 둘로 내고 고정폭 열에는 원 표기만 넣고 병기는 열 뒤에 붙이라는, 부르는 쪽의 조립 방식을 정하는 규칙이다 |
| S3 | 4841643fa986aecb | 15c1395c39a78013c7303fd25274f6e0c9a062beebb17bcd4a6ed761bc7fc69c | crates/pal-core/src/binding.rs | symbol | symbol | 18 | 1 | 1 | 0 | daf15debf4399f482d42c92e693646371ace64b6 | 거짓 | 0 | 아니다 | 넷 중 Live 만 Fresh 로 바꾸고 Current 는 안 쓴다는 변형 이름 선택이라 부르는 쪽이 바뀌어도 그 낱말 판단은 그대로다 |
| S3 | 49714573e0fa654c | 55373aa24b6c4a2013233431a9238ef785e0094fb505324b25d0b3b82a0f86a7 | xtask/src/main.rs | symbol | symbol | 1 | 1 | 1 | 0 | 5e673e7aeae4c596c1361ac8986c8f003cd464d4 | 참 | 0 | 아니다 | 검사 대상을 사전순 최대 하나가 아니라 선언 하한 이후 끝난 회차 전부로 잡는다는 이 선택자 자신의 모집단 규칙이다 |
| S2 | 5b9331b398e8ff23 | 7de16c48b5a51d40aeffa5766a516ef7cdb0c6ad1221640207c6e3c2e06b23e1 | crates/pal-store/src/projection.rs | symbol | callers | 0 | 1 | 1 | 0 | d53d67b247e53b5ea545ce57a4c3c1647c38fc98 | 거짓 | 0 | 영향받는다 | 형식 판 불일치를 오류가 아니라 값으로 내서 부르는 쪽이 「낡음」으로 산출하게 하고 관문을 open_read_only 하나로 못 박은 결정이다 ⟨호출자 0 — 넓혀도 감시가 안 커진다. 멤버 호출(x.foo())을 장치가 못 보는 것이고 pal touch 가 그 문장을 스스로 찍는다. 근거가 안 선 것이 아니다⟩ |
| S4 | 5e6bbbac89053565 | 93aebfd04620275ce8d8be7e62001e581c12bdda69ffb5e0ec6207ecdd7a99d7 | crates/pal-core/src/symbol.rs | symbol | callers | 24 | 1 | 25 | 23 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 1 | 영향받는다 | 종류 이름이 SymbolId::compute 의 성분으로 실리고 kind.name() 문자열을 쓰기에 뒤에 더해도 안전하다는 것이 근거라, 그 값을 쓰는 쪽이 바뀌면 접지 않는다는 판단을 다시 봐야 한다 |
| S3 | 6192ba6df5242980 | f53dcf5eb63e0439c1f1386276a60ad53159ae399aa8abbfca03492e5c4b292b | xtask/src/main.rs | symbol | symbol | 1 | 1 | 1 | 0 | daf15debf4399f482d42c92e693646371ace64b6 | 거짓 | 0 | 아니다 | 조건 설계 평가의 산출물 존재만 재고 평가의 질은 못 잰다고 밝힌 이 검사 자신의 강제 범위다 |
| S4 | 6968674d8aa1bc2f | 41e387f35da18f77b012a195bbfdbd515fec044a7a2f5e266c62aafbd2aa6819 | crates/pal-core/src/narrative.rs | symbol | callers | 5 | 1 | 6 | 1 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 0 | 영향받는다 | 거리 0 인 신호만 좌표를 확정할 수 있다는 타입 불변식이고, 넓힌 규칙이나 asserted 로 결박을 만드는 쪽이 생기면 F10 이 반증당한 형태가 재발한다 |
| S1 | 6c0e8ea521d0b2ac | 17f2ea80e931d7a47f1b78f368d107623d9c8b61368036be71a232ff20829a90 | crates/pal-core/src/doctor.rs | symbol | symbol | 0 | 1 | 1 | 0 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 0 | 아니다 | 빠뜨림만 잡고 틀린 선언은 못 잡는다는 이 검사 자신의 한계와 뒤집힐 조건을 적은 것이다 |
| S4 | 746a7761356136d8 | 55373aa24b6c4a2013233431a9238ef785e0094fb505324b25d0b3b82a0f86a7 | xtask/src/main.rs | symbol | callers | 1 | 1 | 2 | 0 | 07efe73aaa0a43cc6543f11d54f9df120f18a808 | 거짓 | 0 | 영향받는다 | 하한을 못 읽거나 대상이 비면 빈 벡터가 아니라 실패로 돌린다는 결정이고, 그 까닭이 부르는 쪽에 빈 모집단을 잡는 자가 없다는 사실이다 |
| S3 | 7793c31caa5c4bc2 | b8e5e7cb9a1b308656da40feac2d104f76d5578726b1f4200961b4495ece0bb0 | crates/pal-cli/src/ledger.rs | symbol | symbol | 4 | 1 | 1 | 0 | 58991b666e66700d4f00181b590ac3ce972d3463 | 거짓 | 0 | 아니다 | 정체성 등급의 합을 원인 넷으로 갈라 찍는다는 이 산출 블록 자체의 구성이다 |
| S1 | 819fb73f9caf7f22 | 2bf66b24410e4eed94a03bf7989b42009374393e8fe4411a2c62e84986f93d69 | crates/pal-extract/src/rust_scopes.rs | symbol | symbol | 0 | 1 | 1 | 0 | e9402bc63ab875ab933e7668098ce54240b80124 | 거짓 | 0 | 아니다 | 아이템의 순서 무관을 hoist_home 자리가 아니라 hoisted 표시로 표현한다는 이 함수 안의 표현 선택이다 |
| S4 | 822ebf9b8071c799 | 48a133c013b9264b9c774bae8b06cf6b68311e2865a19b085b64d6fd5827e12b | crates/pal-core/src/plan.rs | symbol | callers | 11 | 1 | 12 | 0 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 0 | 영향받는다 | 열거를 만드는 함수의 입력이 그 상태를 가질 수 있는가로 변형 추가와 별도 열거를 가르는 규칙이라, 그 값을 만드는 쪽의 입력이 바뀌면 다시 판단해야 한다 |
| S2 | 82d726bad31e77f6 | 475f5047c22a9e973101739c5bd9f638ee0996a5884e3767f078d66ff9dfb029 | crates/pal-core/src/envelope.rs | symbol | callers | 0 | 1 | 1 | 0 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 0 | 영향받는다 | 응답 시간을 봉투에 싣는 결정이 산출을 바이트로 대는 왕복 항등과 재구축 등가성을 깨뜨린 자리라, 그 값을 소비하는 쪽이 바뀌면 다시 봐야 한다 ⟨호출자 0 — 넓혀도 감시가 안 커진다. 멤버 호출(x.foo())을 장치가 못 보는 것이고 pal touch 가 그 문장을 스스로 찍는다. 근거가 안 선 것이 아니다⟩ |
| S4 | 8597d886cb7afc70 | fa4c34dbc53cad9661fed6adea0a1e3402904f1aceae5960bcd59beeedc6862d | xtask/src/main.rs | symbol | callers | 1 | 1 | 2 | 0 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 0 | 영향받는다 | 방향 4 의 스캔 대상을 이 상수 하나로 모으고 없는 디렉터리를 조용히 건너뛰지 않게 한 결정이라, 이것을 읽는 검사나 표면이 바뀌면 안 재어지는 표면이 생긴다 |
| S3 | 91cdc3add647a3f7 | f129649fd0fb2134a58581b31005e7ec9d5d86098ab2914cba3257cbc41cc888 | crates/pal-cli/src/touch.rs | symbol | symbol | 1 | 1 | 1 | 0 | e9402bc63ab875ab933e7668098ce54240b80124 | 거짓 | 0 | 아니다 | 호출이 아닌 것을 담고 호출인데 못 담는 것도 있다는 두 줄을 함께 찍어야 한다는 이 화면 자신의 산출문 규칙이다 |
| S3 | 969991a119d4c1ef | c9721e568bd411c336956a9468e77a3822191747b6e3ef753a91678ffd62ee35 | xtask/src/main.rs | symbol | symbol | 4 | 1 | 1 | 0 | 58991b666e66700d4f00181b590ac3ce972d3463 | 거짓 | 0 | 아니다 | 검사 하한을 사전순이 아니라 report.md 의 커밋 시각으로 고른다는 이 선택자 자신의 정렬 규칙이다 |
| S3 | a47cd21ca1fbc524 | 1274d02b8942ef34fbe4e7462ff2ca79d021ee4c1b0fd285e4c017f09ea7a181 | crates/pal-core/src/binding.rs | symbol | symbol | 10 | 1 | 1 | 0 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 0 | 아니다 | F12 §3.1 이 아니라 같은 문서 §4 의 「분류만 하고 평가하지 않는다」를 따른다는 이 열거의 분류 태도다 |
| S2 | a5b4ba095bf405f7 | db08e381145d997925da00791b8c96cb513ba53a13821407ce2e4cd0d7a1ce3a | xtask/src/main.rs | symbol | callers | 0 | 1 | 1 | 0 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 0 | 영향받는다 | 플랫폼 분기는 install::exe 한 곳에 살고 호출자는 묻기만 한다는 규약과 원리상 못 재는 것만 등록한다는 자격 규칙이라, 부르는 쪽이 자기 분기를 들이면 무너진다 ⟨호출자 0 — 넓혀도 감시가 안 커진다. 멤버 호출(x.foo())을 장치가 못 보는 것이고 pal touch 가 그 문장을 스스로 찍는다. 근거가 안 선 것이 아니다⟩ |
| S3 | b52057859bf405eb | bf93529eb4c5c5665b4884408ff61763e5066a7c6ed90ba8a32ac811e8f32fbc | xtask/src/main.rs | symbol | symbol | 10 | 1 | 1 | 0 | d53d67b247e53b5ea545ce57a4c3c1647c38fc98 | 거짓 | 0 | 아니다 | 행이 스스로 적은 처분자리 칸이 규칙 셋을 이기고 기각은 맨 먼저 걸러낸다는 이 순수 함수 안의 입력 우선순위다 |
| S3 | bd71eaa617e123bc | 41e387f35da18f77b012a195bbfdbd515fec044a7a2f5e266c62aafbd2aa6819 | crates/pal-core/src/narrative.rs | symbol | symbol | 5 | 1 | 1 | 0 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 0 | 아니다 | attached 에 적힌 명제와 실제 계산이 다르다는 것을 거짓 결박률 실측으로 적은 정정이고, 그 계산 사실은 부르는 쪽과 무관하다 |
| S4 | c201c01b6b0aff9a | 72bb7e9ea837a09cd0710e0f6e884bf3971ff26f38074022afd63b8e4ddc8ac5 | crates/pal-extract/src/parse.rs | symbol | callers | 4 | 1 | 5 | 3 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 1 | 영향받는다 | 부재가 아닌 것은 이 목록에 담지 않는다는 사용 금지 규칙이라, 여기에 무엇을 넣고 읽는 쪽이 바뀌면 종류가 다른 부재가 한 목록에 섞인다 |
| S1 | cbd70e3e8dfca3ba | f24c2c6201a391b3629d34d4fd320469019123f14a1b8c39a41e59c919b0c1ad | crates/pal-extract/src/rust_scopes.rs | symbol | symbol | 0 | 1 | 1 | 0 | e9402bc63ab875ab933e7668098ce54240b80124 | 거짓 | 0 | 아니다 | 가드와 or 갈래를 선언으로 묶지 않게 이 순회가 무엇을 거를지 고친 내부 구현이다 |
| S1 | cc2cee65c3c176ab | 08f9861c982032b24a414e6dbd993abb694cbdcea53cf49e2164332c10d3bd13 | crates/pal-extract/src/classify.rs | symbol | symbol | 0 | 1 | 1 | 0 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 0 | 아니다 | grade_of 의 팔을 언어마다 갈라 두어 한 언어를 올릴 때 다른 언어가 딸려 오르지 않게 한다는 이 함수 내부의 구조다 |
| S4 | ce25c6f23f0a8992 | c14d796f90a25db440c53774acc5b40beab0c506857eff84320a8a7c500b9da9 | crates/pal-cli/src/narrative.rs | symbol | callers | 2 | 1 | 3 | 0 | 58991b666e66700d4f00181b590ac3ce972d3463 | 거짓 | 0 | 영향받는다 | 읽기 표면과 쓰기 표면의 계약을 불리언이 아니라 타입으로 가른다는 결정이고, 부르는 자리에서 무엇이 참인지 읽혀야 한다는 것이 그 까닭이다 |
| S4 | d716d09163a6d0ab | 89c2aab3184831be1555d51244ca187228f2b7b30e030c02f48487ef8998103a | crates/pal-core/src/envelope.rs | symbol | callers | 17 | 1 | 18 | 12 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 3 | 영향받는다 | 같은 필드를 세는 소비자가 옮긴 것을 절단으로 읽으면 dropped() 가 거짓말이 된다는 이유로 대안 셋을 기각한 결정이다 |
| S2 | ddb749e489242cab | 4854875f1c9e3a783842953dbb7461f4f159d55b0dc542026d6beefefd03e750 | crates/pal-core/src/traverse.rs | symbol | callers | 0 | 1 | 1 | 0 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 0 | 영향받는다 | 실측 등급이 선언 상한을 넘지 못하고 모르면 지우지 않는다는 불변식이며, 그 값을 정규화가 읽어 정규형을 정한다는 것이 결정의 축이다 ⟨호출자 0 — 넓혀도 감시가 안 커진다. 멤버 호출(x.foo())을 장치가 못 보는 것이고 pal touch 가 그 문장을 스스로 찍는다. 근거가 안 선 것이 아니다⟩ |
| S1 | e3baa46390859ef3 | 484fd2669226dcb178549a2fe8791704604ffd5df7c9f09f2cd82b8d467ffc33 | crates/pal-core/src/cross_file.rs | symbol | symbol | 0 | 1 | 1 | 0 | d53d67b247e53b5ea545ce57a4c3c1647c38fc98 | 거짓 | 0 | 아니다 | 못 푼 참조의 걸음을 지나면서 적고 되짚어 만들지 않는다는, 이 함수가 자기 기록을 세우는 방식이다 |
| S3 | e64fd4f5378c5f20 | a23a6a391485b850b33bde9d2f26a8efe1cb88d0d4904f181e4081387974295b | xtask/src/main.rs | symbol | symbol | 1 | 1 | 1 | 0 | 5e673e7aeae4c596c1361ac8986c8f003cd464d4 | 거짓 | 0 | 아니다 | 완성 장면을 소제목 셋의 번호 붙은 절로 정의하고 그 형식과 장면 수 하한을 기계가 잰다는 이 검사의 판정 형식이다 |
| S3 | f4ba084c7a3838be | 3b1cfccff060320daef55cf54726139924c981c59d4e7f884c0a2749ad21b783 | crates/pal-cli/src/round/status.rs | symbol | symbol | 1 | 1 | 1 | 0 | daf15debf4399f482d42c92e693646371ace64b6 | 거짓 | 0 | 아니다 | 새 표기와 옛 표기를 둘 다 받는 별칭으로 종결 문서를 판정한다는, 이 검사가 받아들이는 문자열 집합의 결정이다 |
| S2 | fcd48a9033fd1fb0 | 6a77069c8591fd3a645fb3e1407b084cc8c0c6f84a13fb87e1122177015ca01b | crates/pal-store/src/projection.rs | symbol | callers | 0 | 1 | 1 | 0 | 9f0bf704464bec4920e3e226953c32640387623c | 거짓 | 0 | 영향받는다 | 그 값을 고칠 수 있는 사람이 우리가 할 수 있는 일의 범위를 넓히는가로 입력과 권한을 가르고 검증의 기준이 검증 대상 안에 있으면 안 된다는 신뢰 경계 규칙이다 ⟨호출자 0 — 넓혀도 감시가 안 커진다. 멤버 호출(x.foo())을 장치가 못 보는 것이고 pal touch 가 그 문장을 스스로 찍는다. 근거가 안 선 것이 아니다⟩ |
| S2 | fd4efc1eaea040c4 | 678dfa2b54b1eba48698e522c953d3b28b61c5ec480bf5cf42241611465cc7cc | crates/pal-core/src/binding.rs | symbol | callers | 0 | 1 | 1 | 0 | d53d67b247e53b5ea545ce57a4c3c1647c38fc98 | 거짓 | 0 | 영향받는다 | 빈 목록이 0 건인지 못 읽은 것인지를 이 자가 가르므로 읽는 표면이 그 값을 함께 실어야 하고 읽는 자리마다 음성 대조를 둔다는 결정이다 ⟨호출자 0 — 넓혀도 감시가 안 커진다. 멤버 호출(x.foo())을 장치가 못 보는 것이고 pal touch 가 그 문장을 스스로 찍는다. 근거가 안 선 것이 아니다⟩ |

⚠ **표 안에서 파이프 문자를 `／` 로 바꿨다** — 마크다운 표가 깨지기 때문이다. 원문에
파이프가 든 셀은 없었으므로 **이 치환은 아무 셀도 안 건드렸다.**

## 3. 결박문 37 건 — **전문이다. 자르지 않았다**

출처는 `pal intent export` 이고 `.palimpsest/intent/bindings.jsonl` 의 `note` 와 같다.

### `1cc70cc19ef92186` — S3 · `아니다` · 호출자 17 · 감시 1 → 1

**대상** `crates/pal-core/src/envelope.rs` · 심볼 `89c2aab3184831be…`

**`근거` 열의 문면** (판정 대상이다):

> 낱말 「생략」을 elision 에 주고 fold 에는 「이관」을 준다는 표기 배정이고, 세 자리의 이름은 부르는 쪽과 무관하게 정해진다

**결박문 `note` 전문**:

> ADR-0034 §2 — 세 뜻을 가른다. 「생략」은 `fold` 에 주지 않는다
>
> 소유자 1차 원문 31행 — *"절단, 예산 절단: elision … 일반적으로는 '축약'또는 '생략'
> 이라고 하고, 그런데 절단은 손상과 파괴에 더 가까운 느낌이라 문맥 상 의도와는 맞지 않아."*
>
> 그 낱말을 `elision` 에 배정했다. `fold` 에 주면 화면이 *"생략 N건이 다른 질의로
> 옮겨졌습니다 — 잘린 것이 아닙니다"* 가 되어 뒷절이 앞절을 부정한다.
>
> | 자리 | 개념 | 새 표기 |
> |---|---|---|
> | `Elision` | 값이 **없다.** 예산 상한에 걸려 안 봤다 | **생략(elision)** |
> | `Fold` | 값이 **있다.** 부피를 다른 질의로 옮겼다 | **이관(fold)** |
> | 회차의 문 | 안 하기로 정하고 사유를 남긴다 | **철회(folded)** |
>
> `envelope.rs` 가 *"접기 — 부피를 옮긴 것. 생략이 아니다"* 로 타입에서 이미 갈랐고
> `[f06].fold_is_not_elision` 이 시험으로 잠갔다. 화면이 그 가름을 따라간다.
>
> 회차 `2026-09-06-user-surface-vocabulary` · 이슈 #111

### `1cf63ec5e0e9937a` — S4 · `영향받는다` · 호출자 1 · 감시 1 → 2

**대상** `crates/pal-extract/src/rust.rs` · 심볼 `6b4389c2b1869908…`

**`근거` 열의 문면** (판정 대상이다):

> ExportSet 이 담을 모집단을 ledger 의 stitch_of 가 최상위만 EXPORTS 로 옮기는 것에 맞춘 결정이라, 그 소비 쪽이 바뀌면 export_digest 와 EXPORTS 가 다른 모집단을 잰다

**결박문 `note` 전문**:

> 회차 2026-09-07-rust-scope-references — `pub` 의 뜻은 「최상위 + 정확히 `pub`」 하나다
>
> `crates/pal-cli/src/ledger.rs` 의 `stitch_of` 가 `n.container.is_empty()` 로 **최상위만**
> EXPORTS 로 옮긴다. 그러므로 `ExportSet` 이 중첩 `pub` 을 담으면 `export_digest` 와
> EXPORTS 가 **서로 다른 모집단**을 재고, 그 어긋남은 화면 어디에도 안 나온다.
>
> `pub(crate)`·`pub(super)`·`pub(in …)` 도 안 담는다 — `ExportSet` 의 뜻이 *"밖에 노출하는
> 것"* 인데 크레이트 안 가시성은 그 뜻과 다르다.
>
> 이 규칙을 넓히면 `a13_pub_의_뜻은_최상위_pub_하나다` 가 빨개진다.
>
> 이슈 #130 · 조건 A13

### `389ce29274d77a47` — S2 · `영향받는다` · 호출자 0 · 감시 1 → 1

**대상** `crates/pal-core/src/catalog.rs` · 심볼 `b3429f4be3367c44…`

**`근거` 열의 문면** (판정 대상이다):

> 로드맵 26 을 다 적는 길과 파일을 코드에서 생성하는 길을 기각한 결정이라, 이 값을 읽는 대조가 파일을 생성하는 쪽으로 바뀌면 양방향 검사가 항등식이 된다 ⟨호출자 0 — 넓혀도 감시가 안 커진다. 멤버 호출(x.foo())을 장치가 못 보는 것이고 pal touch 가 그 문장을 스스로 찍는다. 근거가 안 선 것이 아니다⟩

**결박문 `note` 전문**:

> 왜 다른 셋이 아닌가
> | 대안 | 기각 이유 |
> |---|---|
> | 26 을 전부 적고 `introduced` 로 가른다 | 스무 개에 대해 방향 1 이 꺼진다. 그리고 로드맵이 상태처럼 읽힌다 — 계획 §7 의 넷째("같은 것을 두 곳에 적으면 그것이 곧 drift") |
> | 스무 개에 거주 불가 타입을 만든다 | 짝은 생기지만 없는 자리를 스무 개 만든 것이다. F05 가 `IMPORTED_BY`·`MODULE_MAP` 에서 안 한 그 선택이다 |
> | 파일을 코드에서 생성한다 | 양방향이 항등식이 되어 영원히 통과한다. 그러면 그 파일은 계약이 아니라 코드의 사본이고, 사본은 drift 를 못 잡는다 |

### `39b92bd51bbc82c6` — S1 · `아니다` · 호출자 0 · 감시 1 → 1

**대상** `crates/pal-core/src/scope.rs` · 심볼 `80cd88b02cc20694…`

**`근거` 열의 문면** (판정 대상이다):

> 보이는 것 중 가장 뒤를 고르고 「가장 앞선 것」 팔은 TDZ 전용으로 남긴다는 이 함수 본문의 해소 규칙 선택이다

**결박문 `note` 전문**:

> 회차 2026-09-07-rust-scope-references — Rust 는 「가장 뒤」 팔을 쓴다
>
> ADR-0027 §② 가 금지역으로 등록했던 것이 이 자리다 — *"Rust 의 shadowing 이
> `ScopeChain::resolve` 의 「가장 앞선 것」 팔을 **상시 경로**로 만드는 것"*.
>
> **그 팔을 안 쓰는 것으로 없앴다.** `ResolveRule::Shadowing` 은 보이는 것 중 가장 뒤를
> 고르고, 「가장 앞선 것」은 `ResolveRule::Tdz`(TypeScript) 전용으로 남았다. 두 규칙이
> 갈린 것을 시험 넷이 잠근다(`섀도잉_규칙은_보이는_것_중_가장_뒤를_쓴다` 외).
>
> **여기를 TypeScript 규칙으로 되돌리면** 이 저장소에서 선언 판정 214 건이 참조로
> 오분류되고, `cfg` 쌍둥이 31 건이 조용한 오답 엣지가 된다(V10 실측).
>
> 이슈 #130 · ADR-0027 「개정」 절

### `3d9979946eb321de` — S4 · `영향받는다` · 호출자 77 · 감시 1 → 78

**대상** `crates/pal-core/src/repo.rs` · 심볼 `6f71aabf4b8aa59a…`

**`근거` 열의 문면** (판정 대상이다):

> 카탈로그의 인자 타입이 어댑터가 내는 스키마와 같은지 아무도 안 댄다는 미포착 범위를 적은 결정이라, 이 타입을 인자로 쓰는 바깥 표면이 늘거나 바뀌면 그 공백을 다시 재야 한다

**결박문 `note` 전문**:

> ⚠ 이 결정이 안 덮는 것
> `.rs` 밖의 표면. 방향 4 는 Rust 소스만 훑는다. 플러그인 매니페스트
> (`plugin.json` · `commands/*.md`)에 질의 이름을 박아도 아무것도 울지 않는다.
> 지금은 손으로 댔고, 그 사실이 F06b 게이트 §판정-나-⑨ 에 있다.
> 인자의 타입. 이름과 필수 여부는 대조되지만 카탈로그의 `type`(`SymbolName` ·
> `RepoPath`)이 어댑터가 내는 스키마와 같은지는 아무도 안 댄다.
> 호스트가 하나라는 것. 세션은 클라이언트 하나로 쟀다. "MCP 를 지원한다" 는
> 그보다 넓은 문장이고 이 결정은 그것을 주장하지 않는다.

### `4065e80475cf1c72` — S3 · `아니다` · 호출자 1 · 감시 1 → 1

**대상** `xtask/src/main.rs` · 심볼 `bd26d1d8a3ee0246…`

**`근거` 열의 문면** (판정 대상이다):

> 활용형·낱말 경계·기계 표식 예외로 패턴을 짜고 검사 자신을 대상 밖에 둔다는 이 검사 내부의 수단 설계다

**결박문 `note` 전문**:

> #114 — 검출 수단은 사전형이 아니라 활용형으로 잰다. 그리고 자기를 안 잰다
>
> `G1` 이 등록한 정규식은 사전형(`켜다`·`접는다`)을 찾는데 이 저장소의 사전형은 **0 건**이고
> 실재하는 것은 활용형이다. 사전형으로 잠근 수단은 일부러 심은 문자열에만 발화하고,
> 그 발화는 실제 결함과 무관하다 — **자기가 만든 조건 위에서 발화 여부를 묻는 항등식**이다.
> 완수 조건 설계 평가 라운드 1 이 그것을 잡았다.
>
> 셋을 더했다 — ①활용형 ②경계(앞이 한글이면 다른 낱말이다. `지켜진`·`삼켜진다`·`보낸다`
> 가 위반으로 세어지면 합격선이 원리상 0 에 못 닿는다) ③기계 표식 예외 둘.
>
> ★ **검출 수단 자신도 대상 밖이다.** 패턴 표와 시험 픽스처가 그 낱말을 지고 있다.
> **경로가 아니라 파일 안의 표지로 갈랐다** — 경로로 가르면 `xtask` 전체가 빠지고,
> 그러면 이 회차가 고친 `xtask` 의 100 곳을 아무도 안 잰다.
>
> 교정 전 HEAD 사본에서 **931곳**을 관측했고 지금 워킹트리는 0 곳이다.
>
> 회차 `2026-09-06-user-surface-vocabulary` · 이슈 #114

### `45d8808585ee0d50` — S4 · `영향받는다` · 호출자 10 · 감시 1 → 11

**대상** `crates/pal-cli/src/label.rs` · 심볼 `c6c07834dbf6e7ea…`

**`근거` 열의 문면** (판정 대상이다):

> 병기를 조각 둘로 내고 고정폭 열에는 원 표기만 넣고 병기는 열 뒤에 붙이라는, 부르는 쪽의 조립 방식을 정하는 규칙이다

**결박문 `note` 전문**:

> ADR-0033 §3 — 병기는 조각 둘로 낸다. 그리고 코어 밖에 둔다
>
> `Label` 이 `원_표기`·`사용자_언어` 를 따로 낸다. 까닭 둘.
>
> **① 정렬.** `{:<N}` 은 char 수로 채우는데 한글은 터미널에서 두 열을 먹는다. 고정폭
> 열에는 원 표기만 넣고 병기는 열 뒤에 붙인다 — 두 조각이 **한 줄에 같이 있으면 그것이
> 병기**다. 붙어 있어야 하는 것이 아니다. `unicode-width` 의존은 안 들였다.
>
> **② 오염.** `pal-core` 의 `name()` 은 화면 문자열이 아니라 **와이어 토큰**이다.
> `export.rs` 가 Cypher 속성 값으로 쓰고, `pal-query` 가 `watch_grades` 의 키로 쓰고,
> 넷(`ResolutionGrade`·`Provenance`·`QueryName`·`schema::Cardinality`)은 자기 `parse` 의
> 열쇠다. 병기를 크레이트 경계 밖에 두면 그 둘이 **구조로** 막힌다 — `pal-core` 는 이
> 모듈을 볼 수 없다.
>
> 「해소 등급」은 이 빌드에서 사람 화면에 안 나간다(모집단 0). 그 자리는 `pal-core` 의
> `왕복_파서를_진_표시_함수` 가 대신 잠근다.
>
> 회차 `2026-09-06-user-surface-vocabulary` · 이슈 #110

### `4841643fa986aecb` — S3 · `아니다` · 호출자 18 · 감시 1 → 1

**대상** `crates/pal-core/src/binding.rs` · 심볼 `15c1395c39a78013…`

**`근거` 열의 문면** (판정 대상이다):

> 넷 중 Live 만 Fresh 로 바꾸고 Current 는 안 쓴다는 변형 이름 선택이라 부르는 쪽이 바뀌어도 그 낱말 판단은 그대로다

**결박문 `note` 전문**:

> ADR-0033 — 잘못 읽히는 낱말은 병기로 고쳐지지 않는다
>
> `Live` 를 `Fresh` 로 바꾸고 나머지 셋(`Stale`·`Orphaned`·`Undeterminable`)은 유지한다.
> 소유자 실측(2026-09-06) — *"'live' 는 최신, 신선한, 유효한이라는 뉘앙스가 약하기도 하고
> 한국어권 사용자들에게는 on-air 같은 느낌으로 받아들여짐."*
>
> 뜻이 약한 것은 사용자 언어 병기로 메울 수 있지만 **다른 뜻으로 읽히는 것은 병기해도
> 그 다른 뜻이 먼저 붙는다.** 그것이 넷 중 이것만 바꾼 근거다 — `undeterminable` 도
> 소유자 문장이 산술상 지목했으나 그것은 낯설 뿐 틀리게 읽히지 않는다.
>
> `Current` 는 기각했다 — `Lineage::Current` 와 **와이어에서** 충돌한다. 그 충돌을
> `binding.rs` 의 `두_축의_와이어` 모듈이 계속 잰다.
>
> 병기는 `pal-cli/src/label.rs` 가 진다. `pal-core` 밖에 두어야 `.name()` 이 Cypher 값과
> `parse` 열쇠로 나가는 자리가 오염되지 않는다.
>
> 회차 `2026-09-06-user-surface-vocabulary` · 이슈 #108 · #109 · #110

### `49714573e0fa654c` — S3 · `아니다` · 호출자 1 · 감시 1 → 1

**대상** `xtask/src/main.rs` · 심볼 `55373aa24b6c4a20…`

**`근거` 열의 문면** (판정 대상이다):

> 검사 대상을 사전순 최대 하나가 아니라 선언 하한 이후 끝난 회차 전부로 잡는다는 이 선택자 자신의 모집단 규칙이다

**결박문 `note` 전문**:

> 회차 2026-09-06-terrain-and-completion-scene 의 정정 — 「어색한 표현 부재」 검사의 대상은 사전순 최대 회차 하나가 아니라 docs/gates/README.md 의 선언이 정한 하한 이후에 끝난 회차 전부다. 앞 판의 사전순 선택은 같은 날짜의 형제 회차에 가려 먼저 끝난 회차를 영영 안 재는 죽은 가지였다(독립 리뷰 R1 · 금지역).

### `5b9331b398e8ff23` — S2 · `영향받는다` · 호출자 0 · 감시 1 → 1

**대상** `crates/pal-store/src/projection.rs` · 심볼 `7de16c48b5a51d40…`

**`근거` 열의 문면** (판정 대상이다):

> 형식 판 불일치를 오류가 아니라 값으로 내서 부르는 쪽이 「낡음」으로 산출하게 하고 관문을 open_read_only 하나로 못 박은 결정이다 ⟨호출자 0 — 넓혀도 감시가 안 커진다. 멤버 호출(x.foo())을 장치가 못 보는 것이고 pal touch 가 그 문장을 스스로 찍는다. 근거가 안 선 것이 아니다⟩

**결박문 `note` 전문**:

> 회차 2026-09-08-cross-file-references — **2 층 행의 형식 판은 오류가 아니라 값이다. 그리고 재는 자리는 하나다.**
>
> 저장이 postcard 라 자리 기반이고, 행의 모양이 바뀌면 옛 색인은 새 타입으로 못 읽힌다.
> 판 표시가 없으면 그 어긋남이 **원인 불명의 디코드 오류**로 나온다 — 주석에 구제를 적어
> 두는 것으로는 아무것도 안 막힌다(이 회차에서만 모양이 두 번 움직였다).
>
> ★ **이것이 값인 까닭** — 부르는 쪽이 「낡음」으로 산출해야 한다. 오류로만 내면 **옛
> 색인과 깨진 색인이 같은 말**이 된다. 그래서 `open_read_only` 가 이 값을 보고 거절하며
> **구제를 이름으로** 적는다.
>
> ★★ **읽는 표면마다 검사를 놓지 않는다** — 새 표면이 생길 때 빠지기 때문이다. 관문은
> `Projection::open_read_only` 하나이고, `pal-store/src` 안에서 `ReadOnlyDatabase::open`
> 이 **정확히 한 번** 나오는 것을 시험이 구조로 잰다. 그것이 `D4-a` 의 「전수」다.
>
> ⚠ **가르는 자를 `built_for` 로 두지 마라** — `rebuild` 가 그것을 빈 문자열로 갈아
> 끼워서 「세운 적 있다」와 「빈 값」이 같은 말이 된다. `META` 표가 있는가로 가른다.

### `5e6bbbac89053565` — S4 · `영향받는다` · 호출자 24 · 감시 1 → 25

**대상** `crates/pal-core/src/symbol.rs` · 심볼 `93aebfd04620275c…`

**`근거` 열의 문면** (판정 대상이다):

> 종류 이름이 SymbolId::compute 의 성분으로 실리고 kind.name() 문자열을 쓰기에 뒤에 더해도 안전하다는 것이 근거라, 그 값을 쓰는 쪽이 바뀌면 접지 않는다는 판단을 다시 봐야 한다

**결박문 `note` 전문**:

> ③ `SymbolKind` 를 접지 않고 늘린다
> 종류는 `SymbolId::compute` 의 성분이므로 정직한 이름이 곧 정직한 좌표다.
> `pal symbols` 가 `struct` 를 `class` 라 부르면 그것이 거짓이고, 그 거짓이 좌표에 실린다.
> ⚠ 처음에 적은 근거는 틀렸다. "접으면 충돌이 6.1% → 11.2% 로 는다" 라 적었는데
> 추출기가 선 뒤 재니 접어도 안 늘었다(cargo 380 에서 215 그대로). 그 수는
> 사전부검이 격리 스파이크로 잰 것이고 저장소에 안 남았다 — 독립 리뷰 R3 이 잡았다.
> 충돌 감소는 부수 효과였고 이 코퍼스에서는 0 이다. 지금은
> `--example coord_collisions` 가 그 축을 잰다.
> 변형을 뒤에 더하는 것은 기존 이름을 안 움직인다 — `kind.name()` 문자열을 쓰기
> 때문이고, F02-1 이 TypeScript 에서 이미 그 선례를 만들었다.

### `6192ba6df5242980` — S3 · `아니다` · 호출자 1 · 감시 1 → 1

**대상** `xtask/src/main.rs` · 심볼 `f53dcf5eb63e0439…`

**`근거` 열의 문면** (판정 대상이다):

> 조건 설계 평가의 산출물 존재만 재고 평가의 질은 못 잰다고 밝힌 이 검사 자신의 강제 범위다

**결박문 `note` 전문**:

> #113 — 완수 조건 설계 평가 단계가 무엇으로 강제되나
>
> 규약 §3.5 가 승인 앞에 세운 단계다. 문서에만 적으면 안 지켜진 전례가 이 저장소에 있으므로
> **산출물의 존재**를 여기서 잰다 — 하한 이후에 연 회차는 `conditions-audit/r<n>-raw.md` 가
> 있어야 한다.
>
> 하한은 `docs/gates/README.md` 의 선언이 진다. 코드에 손으로 베면 갈린다(#94 와 같은 병).
> ⚠ 그 선언은 **방향이 반대인 목록**이다 — 나머지 넷은 면제 목록이고 이것은 적용 목록이라
> 항목을 두지 않는다.
>
> ★★ **강제되지 않는 것: 평가의 질.** 감사자가 형식만 채우면 이 검사는 통과한다. 항 수와
> 레코드 수의 대조는 「회차 레코드」의 합계 검산이 따로 진다. 못 재는 것을 「강제된다」고
> 적으면 그것이 곧 거짓 신호라, 규약이 그 갈림을 명시적으로 적는다.
>
> 회차 `2026-09-06-user-surface-vocabulary` · 이슈 #113

### `6968674d8aa1bc2f` — S4 · `영향받는다` · 호출자 5 · 감시 1 → 6

**대상** `crates/pal-core/src/narrative.rs` · 심볼 `41e387f35da18f77…`

**`근거` 열의 문면** (판정 대상이다):

> 거리 0 인 신호만 좌표를 확정할 수 있다는 타입 불변식이고, 넓힌 규칙이나 asserted 로 결박을 만드는 쪽이 생기면 F10 이 반증당한 형태가 재발한다

**결박문 `note` 전문**:

> 맥락
> F12 §3.1 의 표가 계획 항목의 예상 좌표를 셋으로 적고 첫 줄에 이렇게 썼다:
> | 형태 | 예 | 해소 |
> |---|---|---|
> | 기존 심볼 | ``OrderService.cancel`` | 즉시 해소 → 결박 |
> 그 문서는 2026-08-11 에 쓰였고, [ADR-0015] 는 2026-08-15 에 나왔다.
> ADR-0015 가 F10 의 반증에서 나온 문장은 이것이다:
> 기계가 확인한 것은 이름의 유일성이지 주제의 일치가 아니다.
> "붙어 있음은 구문 트리의 사실이고, 유일함은 이름 공간의 사실이다" —
> 앞의 것만이 주장을 뒷받침한다.
> 실측이 그 근거였다 — `span` 신호 하나로 걸린 결박의 48.9% 가 엉뚱한 좌표였고,
> 그래서 `ConfirmingSignal` 이 거리 0 인 신호만 확정을 낼 수 있게 타입으로 막혔다.
> F12 의 좌표 후보는 넷 다 거리가 있다. 그리고 F12 는 F10 보다 더 넓게 뽑는다 —
> F10 은 경로를 펜스 안에서만, 식별자를 스팬 안에서만 보는데, ditto 의 계획 항목 575
> 중 백틱을 쓴 것이 5 건이라 그 규칙을 그대로 대면 후보가 코퍼스의 성질이 아니라
> 우리가 고른 규칙의 성질로 0 이 된다. 그래서 F12 는 본문 전체를 본다.
> ★ 넓힌 규칙 + `asserted` 결박 = F10 이 반증당한 형태의 재발이다.

### `6c0e8ea521d0b2ac` — S1 · `아니다` · 호출자 0 · 감시 1 → 1

**대상** `crates/pal-core/src/doctor.rs` · 심볼 `17f2ea80e931d7a4…`

**`근거` 열의 문면** (판정 대상이다):

> 빠뜨림만 잡고 틀린 선언은 못 잡는다는 이 검사 자신의 한계와 뒤집힐 조건을 적은 것이다

**결박문 `note` 전문**:

> 무엇이 이 결정을 뒤집는가
> 선언이 거짓말을 하기 시작하면. 공급 쪽이 "담을 수 없다" 고 적어 두면 그 검사는
> 영구히 돌지 않는다. 지금은 `coverage_gaps` 가 빠뜨림만 잡고 틀린 선언은 잡지
> 못한다 — 담을 수 있다고 적힌 라벨의 모집단이 0 인 경우가 그 자리다. F05 가 엣지를
> 저장하면 그 검사를 붙일 수 있다.
> 모집단 부재가 정상이 아니게 되면. P1 이 닫힐 무렵 다섯이 전부 실물에 서면 이
> 결정의 하중은 "부재를 표시한다" 에서 "부재가 남아 있으면 그것이 결함이다" 로
> 옮겨간다.

### `746a7761356136d8` — S4 · `영향받는다` · 호출자 1 · 감시 1 → 2

**대상** `xtask/src/main.rs` · 심볼 `55373aa24b6c4a20…`

**`근거` 열의 문면** (판정 대상이다):

> 하한을 못 읽거나 대상이 비면 빈 벡터가 아니라 실패로 돌린다는 결정이고, 그 까닭이 부르는 쪽에 빈 모집단을 잡는 자가 없다는 사실이다

**결박문 `note` 전문**:

> 회차 2026-09-06-terrain-and-completion-scene 의 정정(둘째) — 선택자가 하한을 못 읽거나 대상이 비면 실패로 돌린다. 앞 결박은 그 뒤 본문이 바뀌어 stale 로 표시됐고, 그것이 낡음 표시가 실제로 도는 증거다(독립 리뷰 R2·R4).

### `7793c31caa5c4bc2` — S3 · `아니다` · 호출자 4 · 감시 1 → 1

**대상** `crates/pal-cli/src/ledger.rs` · 심볼 `b8e5e7cb9a1b3086…`

**`근거` 열의 문면** (판정 대상이다):

> 정체성 등급의 합을 원인 넷으로 갈라 찍는다는 이 산출 블록 자체의 구성이다

**결박문 `note` 전문**:

> 회차 2026-09-11-effect-confirmation ⟨㉠ · #79⟩ — **정체성 등급을 원인별로 가른다.**
>
> 착수 시점의 `pal ledger` 에는 이 블록이 **없었다**(`observations/identity-before.txt`).
> 지금 산출한다 — `순서에 취약 20 · 등급이 낮음 3306 · 정확 6 · 불가 0 · 합 3332`.
>
> **왜 합만으로 안 되나** — `identity ordinal` 한 줄은 그 값이 **판별자 상한이 낮춘 것인지
> 추출기 등급이 낮은 것인지**를 안 가른다. 가르지 않으면 읽는 사람이 원인을 지어낸다.
>
> ⚠ **이 회차가 못 채운 것을 함께 적는다** — 화면이 셋을 가르게 됐지만 **2 층에 그 값이 없어**
> 심볼 한 줄의 원인은 여전히 원리상 못 고친다 ⟨`MS-09` · 종료 보고 `## 능력 부재`⟩.
> 그것은 잔여가 아니라 경계다 — 무엇이 갖춰지면 잴 수 있는지가 적혀 있다.
>
> ★ 그리고 낱말 하나 — 「워킹트리」가 표면 셋에서 서로 다른 것을 뜻하는데 어느 산출도 안
> 갈랐다 ⟨`MS-11`⟩. 이 파일의 주석이 이제 셋을 가른다.

### `819fb73f9caf7f22` — S1 · `아니다` · 호출자 0 · 감시 1 → 1

**대상** `crates/pal-extract/src/rust_scopes.rs` · 심볼 `2bf66b24410e4eed…`

**`근거` 열의 문면** (판정 대상이다):

> 아이템의 순서 무관을 hoist_home 자리가 아니라 hoisted 표시로 표현한다는 이 함수 안의 표현 선택이다

**결박문 `note` 전문**:

> 회차 2026-09-07-rust-scope-references — 아이템 호이스팅을 `hoist_home` 으로 표현하지 않는다
>
> 사전부검 R2 가 격리 사본에서 관측한 것: 「아이템 선언을 `hoist_home` 으로 놓는다」 변형의
> 엣지 집합이 「`impl` 이 스코프를 안 연다」 변형과 **비트 단위로 같았다.** `hoist_home` 이
> 가장 가까운 `Function`·`Module` 까지 올라가므로 `ScopeKind::Impl` 을 **건너뛰고**,
> 그래서 「`impl` 이 스코프를 연다」가 정확히 상쇄된다(가짜 78 · 누락 17).
>
> **Rust 의 「순서 무관」은 자리가 아니라 `ScopeBinding::hoisted` 로 표현한다.** 아이템은
> 자기가 선 스코프에 그대로 앉고 `hoisted = true` 만 받는다.
>
> 이 함수를 `hoist_home` 쪽으로 되돌리면 `V4` 가 `V3` 와 같은 집합을 산출하고, 그때
> 「완수의 증인」이 통째로 무의미해진다 — 변형 둘이 서로를 못 가른다.
>
> 이슈 #130 · 조건 A16 · V4

### `822ebf9b8071c799` — S4 · `영향받는다` · 호출자 11 · 감시 1 → 12

**대상** `crates/pal-core/src/plan.rs` · 심볼 `48a133c013b9264b…`

**`근거` 열의 문면** (판정 대상이다):

> 열거를 만드는 함수의 입력이 그 상태를 가질 수 있는가로 변형 추가와 별도 열거를 가르는 규칙이라, 그 값을 만드는 쪽의 입력이 바뀌면 다시 판단해야 한다

**결박문 `note` 전문**:

> 가르는 문장
> 그 열거를 만드는 함수의 입력이 이 상태를 가질 수 있는가.
> · 가질 수 있으면 — 변형을 더한다. `Undeterminable` 이 그랬다(F09)
> · 가질 수 없으면 — 자기 열거를 세운다. `PlanBindingState` 가 그것이다
> · 가질 수 없는데 더하면 — 아무도 못 만드는 이름(ADR-0012)이거나
> 한 열거가 두 모집단 위에 서는 것이고, 둘 다 이 저장소가 금한 형태다

### `82d726bad31e77f6` — S2 · `영향받는다` · 호출자 0 · 감시 1 → 1

**대상** `crates/pal-core/src/envelope.rs` · 심볼 `475f5047c22a9e97…`

**`근거` 열의 문면** (판정 대상이다):

> 응답 시간을 봉투에 싣는 결정이 산출을 바이트로 대는 왕복 항등과 재구축 등가성을 깨뜨린 자리라, 그 값을 소비하는 쪽이 바뀌면 다시 봐야 한다 ⟨호출자 0 — 넓혀도 감시가 안 커진다. 멤버 호출(x.foo())을 장치가 못 보는 것이고 pal touch 가 그 문장을 스스로 찍는다. 근거가 안 선 것이 아니다⟩

**결박문 `note` 전문**:

> 맥락
> F11 §6이 "응답 지연 — 대화 흐름을 끊지 않는가(목표: p95 < 500ms)" 를 검증 항목으로
> 적었고, `[f11.pass]` ⑦이 그 값을 질의 시간에 걸었다. 그런데 재려면 표면이 자기
> 시간을 낼 수 있어야 한다.
> 값은 이미 있었다. `pal-query::execute`가 `Instant` 하나로 재고
> `QueryLogEntry::duration_micros`에 넣는다. 그러나 그것은 2층 안에 있고 산출로 안
> 나갔다 — 그러면 "p95 < 500ms" 는 소비자가 확인할 수 없는 문장이다.
> 그래서 봉투에 실었다. `LogStatus::Recorded { duration_micros }`. 근거는 대칭이었다 —
> `Envelope::tokens`가 응답의 크기를 싣고 있으니 시간은 그 쌍이라는 것.
> 시험이 그것을 반증했다.
> intent_jsonl::내보내고_지우고_읽으면_값이_그대로다              ← 왕복 항등 (F05) · **깨졌다**
> rebuild_equivalence::이층을_지워도_일층과_의도로_같은_것이_선다  ← 재구축 등가성 (F04) · **같은 바이트를 댄다**
>
> 둘 다 `pal touch --json`의 산출을 바이트로 댄다. 왕복 항등이 그 자리에서 깨졌다 —
> `duration_micros` 가 712 와 512 로 갈렸다.
> ⚠ 재구축 등가성은 그 실행에서 우연히 통과했다. 두 값의 자릿수가 같았기 때문이고,
> 자릿수가 갈리면 `tokens.serialized_bytes`까지 함께 움직인다.
> ★ 「한 번 통과했다」가 「검사가 성립한다」를 뜻하지 않는다 — 이 결정이 겨냥한 것은
> 깨진 하나가 아니라 둘 다 시간에 대해 참이 아니게 된다는 사실이다.

### `8597d886cb7afc70` — S4 · `영향받는다` · 호출자 1 · 감시 1 → 2

**대상** `xtask/src/main.rs` · 심볼 `fa4c34dbc53cad96…`

**`근거` 열의 문면** (판정 대상이다):

> 방향 4 의 스캔 대상을 이 상수 하나로 모으고 없는 디렉터리를 조용히 건너뛰지 않게 한 결정이라, 이것을 읽는 검사나 표면이 바뀌면 안 재어지는 표면이 생긴다

**결박문 `note` 전문**:

> 결과
> MCP 툴은 질의가 늘면 따라 는다. 갈릴 방법이 없다.
> 어댑터 크레이트는 `pal-cli` 에 의존할 수 없다(의존 방향 규칙 3). 그 제약이 둘째를
> 강제한다 — 조립을 쓰려면 트레이트로 받는 수밖에 없다.
> 방향 4 의 스캔 대상이 상수 하나(`SURFACE_SOURCES`)로 모였고, 없는 디렉터리는
> 조용히 건너뛰지 않고 문제로 적힌다 — 스캔이 0 개를 훑고 통과하는 형태를 막는다.

### `91cdc3add647a3f7` — S3 · `아니다` · 호출자 1 · 감시 1 → 1

**대상** `crates/pal-cli/src/touch.rs` · 심볼 `f129649fd0fb2134…`

**`근거` 열의 문면** (판정 대상이다):

> 호출이 아닌 것을 담고 호출인데 못 담는 것도 있다는 두 줄을 함께 찍어야 한다는 이 화면 자신의 산출문 규칙이다

**결박문 `note` 전문**:

> 회차 2026-09-07-rust-scope-references — 이 라벨은 두 방향을 다 적어야 한다
>
> **호출이 아닌 것을 담는다** — 실측 엣지 4,209 중 타입 참조 1,197 · 호출·매크로 3,796 ·
> 그 밖 1,598. 스키마가 `[edge.REFERENCES]` 에서 *"`CALLS` 가 아니다 … `CALLS` 로 적으면
> 타입 참조가 호출로 둔갑한다"* 로 못 박았다.
>
> **호출인데 안 담는 것도 있다** — 효과 관측이 그것을 잡았다. `pal touch resolve_shadowing`
> 이 `호출자 0` 을 돌려주는데 바로 옆 `resolve_with` 이 `self.resolve_shadowing(…)` 으로
> 부른다. 멤버 호출은 `field_identifier` 라 이 층이 원리상 못 본다(실측 13,951 자리) ·
> 경로 호출도 꼬리라 못 본다(4,217 자리).
>
> **둘 중 하나만 적으면 0 이 「아무도 안 부른다」로 읽힌다.** 두 줄이 함께 서야 한다.
>
> 이슈 #130 · 조건 D8 · D2

### `969991a119d4c1ef` — S3 · `아니다` · 호출자 4 · 감시 1 → 1

**대상** `xtask/src/main.rs` · 심볼 `c9721e568bd411c3…`

**`근거` 열의 문면** (판정 대상이다):

> 검사 하한을 사전순이 아니라 report.md 의 커밋 시각으로 고른다는 이 선택자 자신의 정렬 규칙이다

**결박문 `note` 전문**:

> 회차 2026-09-11-effect-confirmation — **검사의 하한은 사전순이 아니라 커밋 시각이 고른다.**
>
> 「원장 둘 대조」가 *"끝난 회차 중 가장 최근 것이 검사에 들었는가"* 로 하한을 잡는데, 앞 판은
> 그 "가장 최근" 을 **회차 이름의 사전순**으로 골랐다. 회차 이름이 날짜로 시작하므로 대개
> 맞지만, **같은 날 회차가 둘이면 갈리고** 이름과 종료 시각이 어긋나는 회차에서 통째로 틀린다.
>
> **고른 자 — `report.md` 의 커밋 시각이다.** 회차가 끝난 시점을 이력이 진다.
> 시각이 없는 회차는 가장 최근으로 보고, 시각이 같으면 그때만 사전순 최대를 쓴다.
>
> ★ 시험 넷이 이 셋을 각각 고정한다 — 사전순이 아니라는 것 · 시각 부재 · 동시각 동률.
>
> ⚠ **이 결정이 `A5` 를 반증시킨 자리와 같은 계열이다** — 이 회차가 자기 장치를 고친 커밋이
> ㉡ 이 고칠 파일과 같은 파일에 있었고, 그 커밋이 `A5`·`A7` 의 「첫 코드 변경 커밋」으로
> 잡혔다. **고치는 손과 판정하는 손이 같은 파일에 있는 것 자체가 §11③ 이 이름 붙인 자리다** —
> 잠긴 의도가 착수 때 그것을 미리 적었고, 예고한 사고가 조건 둘을 실제로 깨뜨렸다.

### `a47cd21ca1fbc524` — S3 · `아니다` · 호출자 10 · 감시 1 → 1

**대상** `crates/pal-core/src/binding.rs` · 심볼 `1274d02b8942ef34…`

**`근거` 열의 문면** (판정 대상이다):

> F12 §3.1 이 아니라 같은 문서 §4 의 「분류만 하고 평가하지 않는다」를 따른다는 이 열거의 분류 태도다

**결박문 `note` 전문**:

> 왜 F12 §3.1 을 안 따르는가 — 어긋난 것은 문서다
> 문서가 그 줄을 쓸 때는 ADR-0015 가 없었다. 그리고 같은 문서의 §4 가 이미 반대
> 방향을 적어 두었다:
> `unplanned` 가 나쁜 것이 아니다. 판정이 아니라 관측이다.
> 분류만 하고 평가하지 않는다.
> 이 결정은 §4 를 §3.1 에 적용한 것이지 새 방향이 아니다. `UndeterminableReason` 의
> 머리가 "문서 §2.1 과 어긋나고, 어긋난 것은 문서다" 라고 적은 것과 같은 형태다.

### `a5b4ba095bf405f7` — S2 · `영향받는다` · 호출자 0 · 감시 1 → 1

**대상** `xtask/src/main.rs` · 심볼 `db08e381145d9979…`

**`근거` 열의 문면** (판정 대상이다):

> 플랫폼 분기는 install::exe 한 곳에 살고 호출자는 묻기만 한다는 규약과 원리상 못 재는 것만 등록한다는 자격 규칙이라, 부르는 쪽이 자기 분기를 들이면 무너진다 ⟨호출자 0 — 넓혀도 감시가 안 커진다. 멤버 호출(x.foo())을 장치가 못 보는 것이고 pal touch 가 그 문장을 스스로 찍는다. 근거가 안 선 것이 아니다⟩

**결박문 `note` 전문**:

> 결정
> 같은 상황에서 같은 방법과 같은 결과를 낸다. 플랫폼이 무언가를 못 볼 때 고를 축은
> **「볼 수 있는 쪽」이 아니라 「양쪽이 할 수 있는 것」**이다.
> 네 자리에서 같은 규칙이 선다:
> ★ 하드링크는 어디서나 끊고 쓴다. Windows 는 링크 수를 셀 수 없다
> (`number_of_links()` 는 unstable, raw FFI 는 `unsafe 금지` 게이트가 막는다). 그러니
> "양쪽 다 거부" 는 불가능하고, 가능한 통일은 "양쪽 다 끊는다" 뿐이다. ★ 그리고
> 그것은 타협이 아니다 — 첫 판의 거부 문구가 사람에게 적던 처방이 정확히 그것
> (`cp` 로 복사해 갈아끼우기)이고, 볼 수 없는 쪽에서는 쓰기 경로가 그 처방을 대신
> 수행한다. `[f24]` ⑦ 이 양쪽에서 서고 rc 도 최종 상태도 같다.
>
> ★ 같은 물음의 플랫폼 분기는 한 자리에 산다. "이 자리가 실행될 수 있는가" 를
> 묻는 곳이 둘이었고 둘 다 유닉스의 답만 알고 있었다. 유닉스에서 그 답을 정하는
> 것은 모드 비트이고 Windows 에서는 확장자다 — 축이 다를 뿐 같은 사건이다
> (파일은 있고 바이트도 우리 것인데 OS 가 못 띄우고 하네스가 그 실패를 삼킨다).
> 분기는 [`install::exe`] 한 곳이고 호출자는 묻기만 한다.
>
> ★ 공유되는 산출물에 기계 고유의 값을 안 싣는다. `settings.json` 과 매니페스트는
> 커밋되고 clone 과 함께 이동한다. 거기 절대 경로를 실으면 다른 기계에서 반드시
> 틀리고, 받는 쪽이 고치면 추적 파일이 바뀌어 핑퐁이 시작된다(실측). 그래서
> 훅 등록은 `PATH` 의 이름 하나이고, 두 산출물 다 있던 줄바꿈으로 되쓴다.
>
> ★ 못 재는 것은 초록으로 안 바꾼다. 그러나 「그냥 빨간 것」으로도 안 둔다.
> fixture 가 안 서는 자리는 여전히 시끄럽게 실패한다 — 통과로 만들면 그 자리는
> "아무것도 안 재면서 초록" 이 되고, 그것이 짝 없는 `cfg` 에 대해 이 저장소가
> 금지한 상태다. 대신 그 실패들을 까닭과 함께 등록하고, CI 는 "실패가 없는가" 가
> 아니라 "실패 집합이 등록 집합과 같은가" 를 묻는다(`cargo xtask test`).
> ★ 그리고 등록될 자격은 「원리상 못 잰다」뿐이다 (2026-08-17 · 개정). 첫 판은
> 자격을 안 적었고, 그래서 등록 다섯 중 **넷이 「아직 안 한 것」**이었다 — 다음 회차가
> 성질을 갈라 보니 fixture 를 `icacls`·개발자 모드·디렉터리로 세울 수 있었고, 하나는
> 제품을 고쳐서(위 「없앤 것」) 사라졌다. 지금 등록은 비어 있다.
> `등록은_원리상_불가능한_것만_담는다` 가 까닭에 「아직」·「나중에」·「미측정」이
> 들어오면 걸어 낸다 — 그것은 할 일이지 사실이 아니고, 목록에 넣는 순간 CI 가
> 그것을 초록으로 세어 준다.

### `b52057859bf405eb` — S3 · `아니다` · 호출자 10 · 감시 1 → 1

**대상** `xtask/src/main.rs` · 심볼 `bf93529eb4c5c566…`

**`근거` 열의 문면** (판정 대상이다):

> 행이 스스로 적은 처분자리 칸이 규칙 셋을 이기고 기각은 맨 먼저 걸러낸다는 이 순수 함수 안의 입력 우선순위다

**결박문 `note` 전문**:

> 회차 2026-09-08-cross-file-references — **원장의 `경로` 와 `처분자리` 는 다른 축이다.**
>
> `경로` 는 **발견이 관측된 자리**이고 그 값은 에이전트의 원 반환문이 정한다 — 합계 검산이
> 원 반환문과 글자로 대조하므로 사람이 못 고친다. 이 함수가 읽는 것은 **처분이 닿는
> 자리**다. 둘이 갈리는 발견에서 두 뜻은 **동시에 못 참이다.**
>
> 실측(2026-09-10): 열 행이 그 형태였고, `경로` 를 처분 자리로 고치면 합계 검산이
> 빨개졌고 그대로 두면 닫힘 검사가 빨개졌다. **초록으로 만드는 남은 길이 「닫은 것을 열린
> 채로 두는 것」뿐이었다** — 그것이 금지역 「측정이 죽은 가지」로 가는 문이다.
>
> ★ **행이 스스로 적은 `처분자리` 가 규칙 셋을 다 이긴다.** 규칙 셋은 *"이 출처·이 처분은
> 대개 어디에 닿는가"* 의 어림이고 그 칸은 **그 행에 대한 사실**이다. 다만 `기각` 은 안
> 이긴다 — 아무것도 안 고치는 처분은 무엇이 적혀 있든 못 잰다.

### `bd71eaa617e123bc` — S3 · `아니다` · 호출자 5 · 감시 1 → 1

**대상** `crates/pal-core/src/narrative.rs` · 심볼 `41e387f35da18f77…`

**`근거` 열의 문면** (판정 대상이다):

> attached 에 적힌 명제와 실제 계산이 다르다는 것을 거짓 결박률 실측으로 적은 정정이고, 그 계산 사실은 부르는 쪽과 무관하다

**결박문 `note` 전문**:

> 맥락
> ADR-0015가
> 「기계가 확인했다」는 무엇을 확인했는지 함께 적어야 한다고 정하고 셋을 요구했다 —
> ① 확인된 명제를 문장으로 ② 그것과 주장 사이의 거리를 ③ 그 거리를 합격선의 층화에.
> F10-5 가 그 요구를 여섯 신호에 각각 적용했다. `corpus/criteria.toml`
> `[f10.5].signal_ruling` 이 `attached` 에 이렇게 적었다 — 측정보다 먼저:
> | `attached` | "이 주석은 이 선언에 붙어 있다" | 거리 0 — 붙어 있음이 곧 「그것에 관하여」다 | 확정할 수 있다 |
> 그리고 그 분류 위에 설계 전체를 세웠다: 거리 0 인 신호만 좌표를 확정한다.
> `ConfirmingSignal` 이 그것을 타입으로 지고, 거리 있는 신호는 후보만 낸다.
> 실측이 그 명제를 반증했다. `attached` 로 걸린 결박 23 건 전수를 손으로
> 검토한 거짓 결박률이 26.1%(6/23)였고, 여섯이 전부 한 뿌리였다:
> 파일·모듈 머리 주석이 첫 선언에 붙었다        4
> 함수 묶음의 머리 주석이 첫 함수에 붙었다       1   (주제는 두 함수 뒤에 있다)
> 앞 함수의 꼬리 주석이 다음 선언에 붙었다       1
>
> 기계는 무엇을 확인했나. 구현은 이렇게 한다 — `Span.byte_end` 다음에 시작하는
> 선언을 찾아 붙인다. 그것은 참이다. 그런데 등록에 적힌 명제는 "이 주석은 이 선언에
> 붙어 있다" 였고, 둘은 다른 문장이다:
> 적힌 명제    "이 주석은 이 선언에 붙어 있다"
> 실제 계산    "이 주석 다음에 이 선언이 시작한다"
>
> 파일 머리 주석은 아무 선언에도 안 붙어 있다. `pad2` 는 자기 doc 주석을 따로
> 갖고 있는데도, 그 위의 파일 머리 주석(빈 줄로 갈린)이 걸렸다.

### `c201c01b6b0aff9a` — S4 · `영향받는다` · 호출자 4 · 감시 1 → 5

**대상** `crates/pal-extract/src/parse.rs` · 심볼 `72bb7e9ea837a09c…`

**`근거` 열의 문면** (판정 대상이다):

> 부재가 아닌 것은 이 목록에 담지 않는다는 사용 금지 규칙이라, 여기에 무엇을 넣고 읽는 쪽이 바뀌면 종류가 다른 부재가 한 목록에 섞인다

**결박문 `note` 전문**:

> 이 결정이 적용되지 않는 자리
> 부재가 아닌 것에는 쓰지 않는다. F02-2 가 「입자 부재」(조상 없는 익명 함수의 변경)를
> `recovery_sites` 에 담을지 판단하면서 안 담기로 했다 — 회복 지점은 "문법이 이 바이트를
> 못 읽었다" 이고 입자 부재는 바이트를 완벽히 읽었는데 담을 심볼이 없는 것이다.
> 종류가 다른 부재를 한 목록에 담으면 이 ADR 이 막으려는 것을 그대로 저지른다.
> 이유의 수가 늘어나는 것 자체가 관측 대상이다. 셋째 이유가 필요해지면 그때 "칸이어야
> 하는 것 아닌가" 를 다시 묻는다.

### `cbd70e3e8dfca3ba` — S1 · `아니다` · 호출자 0 · 감시 1 → 1

**대상** `crates/pal-extract/src/rust_scopes.rs` · 심볼 `f24c2c6201a391b3…`

**`근거` 열의 문면** (판정 대상이다):

> 가드와 or 갈래를 선언으로 묶지 않게 이 순회가 무엇을 거를지 고친 내부 구현이다

**결박문 `note` 전문**:

> 회차 2026-09-07-rust-scope-references — 패턴에서 가드와 or 갈래를 거른다
>
> 실행 중에 실측으로 잡은 둘이다. 둘 다 「선언이 아닌 것을 선언으로 묶는」 형태이고,
> 그 결과가 **가짜 엣지가 아니라 `RefResolution::Ambiguous`** 였다 — 같은 스코프에 동명
> 바인딩이 여럿 서면 해소를 거부하기 때문이다. 그래서 **엣지가 조용히 사라졌다.**
>
> - **`match_pattern` 은 가드를 담는다** — `b if b < K` 의 `b < K` 가 `condition` 필드에
>   있다. 안 거르면 가드 안의 **참조**가 선언으로 묶인다. 실측 62 자리.
> - **`A(x) | B(x)` 는 갈래마다 같은 이름을 묶는다**(Rust 가 그것을 요구한다). 전부 묶으면
>   동명 바인딩이 여럿 선다. 첫 갈래만 묶는다. 실측 11 자리.
>
> 둘을 고쳐 동명 아이템 재선언 자리가 **89 → 21** 로, 모호 참조가 **200 → 58** 로 줄었다.
> 남은 21 은 `cfg` 쌍둥이이거나 같은 이름의 `fn` 과 타입이다.
>
> 이슈 #130 · 조건 A14 · E3

### `cc2cee65c3c176ab` — S1 · `아니다` · 호출자 0 · 감시 1 → 1

**대상** `crates/pal-extract/src/classify.rs` · 심볼 `08f9861c982032b2…`

**`근거` 열의 문면** (판정 대상이다):

> grade_of 의 팔을 언어마다 갈라 두어 한 언어를 올릴 때 다른 언어가 딸려 오르지 않게 한다는 이 함수 내부의 구조다

**결박문 `note` 전문**:

> 결과
> 한 언어 안에서 등급이 갈린다. ditto 496 파일의 심볼 4,656 중 335 가 `ordinal` 로
> 내려앉는다(구조 분해 · TDZ). 그 심볼들에서는 이름이 안 지워진다
> 언어를 올릴 때 다른 언어가 딸려 오르지 않는다. `grade_of` 의 팔을 언어마다 갈라
> 두는 것이 이 결정의 딸린 규율이다 — F02-3 이 TypeScript 만 L2 로 올렸고 Kotlin 은 L1 ·
> `ordinal` 로 남았다. 합쳤으면 `f01-verify` 의 등급 음성 대조가 무의미해졌을 것이다
> `body_digest` 를 만드는 시점이 순회 밖으로 나간다. 등급은 파일 전체를 다 보아야
> 정해지므로, 선언을 만나는 그 자리에서 요약을 계산할 수 없다(`24207a3` 이 그 구조적
> 변경이다)
> 추출기 버전을 올려야 한다. 정규형이 바뀌면 옛 캐시가 옛 요약을 새 스키마로 되돌려
> 준다 — ADR-0004 의
> 키에 그 축이 이미 있다

### `ce25c6f23f0a8992` — S4 · `영향받는다` · 호출자 2 · 감시 1 → 3

**대상** `crates/pal-cli/src/narrative.rs` · 심볼 `c14d796f90a25db4…`

**`근거` 열의 문면** (판정 대상이다):

> 읽기 표면과 쓰기 표면의 계약을 불리언이 아니라 타입으로 가른다는 결정이고, 부르는 자리에서 무엇이 참인지 읽혀야 한다는 것이 그 까닭이다

**결박문 `note` 전문**:

> 회차 2026-09-11-effect-confirmation ⟨㉢ · #129⟩ — **읽기 표면과 쓰기 표면을 타입으로 가른다.**
>
> 광고된 질의 `pal query narrative.unbound` 가 **어떤 입력으로도 안 돌았다.** 두 표면이 같은
> `ingest` 를 부르는데 그것이 개체가 없으면 민팅해서 남겼고, 읽기 표면은 의도 저장소를
> 읽기로만 열기 때문이다. 착수 시점 관측이 `Error: 개체를 남기지 못했다 · rc=1` 이었다.
>
> **불리언으로 안 가른 까닭** — `ingest(…, true)` 는 부르는 자리에서 무엇이 참인지 안 읽힌다.
> 이 스위치가 가르는 것은 두 표면의 **계약**이다.
>
> ★ **그리고 0 이 아닌 값을 침묵으로 두지 않는다** — 목록에서 뺀 조각 수(`개체_없음`)를 함께
> 싣는다. 안 실으면 목록이 조용히 짧아지고 보는 사람은 그것을 "미결박이 그만큼뿐" 으로 읽는다.
>
> 이 회차의 `## 효과` 가 이 결정의 증인이다 — 같은 명령이 `rc=1` 에서 `rc=0` 이 됐고
> 새 줄 `이름이 아직 없어 뺀 조각 4733` 이 섰다.

### `d716d09163a6d0ab` — S4 · `영향받는다` · 호출자 17 · 감시 1 → 18

**대상** `crates/pal-core/src/envelope.rs` · 심볼 `89c2aab3184831be…`

**`근거` 열의 문면** (판정 대상이다):

> 같은 필드를 세는 소비자가 옮긴 것을 절단으로 읽으면 dropped() 가 거짓말이 된다는 이유로 대안 셋을 기각한 결정이다

**결박문 `note` 전문**:

> 왜 다른 셋이 아닌가
> | 대안 | 기각 이유 |
> |---|---|
> | `Elision` 에 사유를 하나 더한다 (`FoldedToOtherQuery`) | 같은 필드를 세는 소비자가 "절단 12 건" 을 읽는데 그중 열은 옮긴 것이다. `dropped()` 가 거짓말이 된다 |
> | 요약만 싣고 아무 말도 안 한다 (지금까지의 상태) | 정확히 이 제품이 고발하는 조용한 공백이다. 그리고 소비자가 어디서 전체를 얻는지 모른다 |
> | 접기를 옵션 플래그로 (`--full`) | F06 §6이 "옵션은 비게 된다" 로 이미 기각했다. 그리고 기본이 접힘이면 기본 경로가 침묵한다 |

### `ddb749e489242cab` — S2 · `영향받는다` · 호출자 0 · 감시 1 → 1

**대상** `crates/pal-core/src/traverse.rs` · 심볼 `4854875f1c9e3a78…`

**`근거` 열의 문면** (판정 대상이다):

> 실측 등급이 선언 상한을 넘지 못하고 모르면 지우지 않는다는 불변식이며, 그 값을 정규화가 읽어 정규형을 정한다는 것이 결정의 축이다 ⟨호출자 0 — 넓혀도 감시가 안 커진다. 멤버 호출(x.foo())을 장치가 못 보는 것이고 pal touch 가 그 문장을 스스로 찍는다. 근거가 안 선 것이 아니다⟩

**결박문 `note` 전문**:

> 결정
> 등급을 두 층으로 둔다. 언어 표는 선언 상한이고, 실제 등급은 심볼에 실린다.
> grade_of(Language) -> ExtractGrade     // 선언 상한 — 대장 머리가 읽는다
> Symbol { …, identity: IdentityGrade }  // 실측 — 정규화가 읽는다
>
> identity = min(측정값, grade_of(언어).identity())
>
> 그리고 그 값이 정규형을 정한다:
> | 심볼 등급 | 정규화 |
> |---|---|
> | `exact` | 그 심볼 안에서 선언된 심볼이 아닌 이름을 선언 순서의 자리 번호로 지운다 |
> | `ordinal` | 지우지 않는다 |
> 세 규칙이 함께 간다:
> 실측이 선언 상한을 넘지 못한다. 넘으면 언어 표가 심볼이 가진 것보다 낮은 값을
> 광고하고, 그러면 표가 뜻을 잃는다
> 모르면 지우지 않는다. 이름을 못 잡은 바인딩이 심볼 안에 하나라도 있으면 그 심볼은
> `ordinal` 이다 — 본문의 어떤 이름이 그것을 가리키는지 알 수 없기 때문이다
> 자리 번호는 구별된다. 전부 한 바이트로 지우면 `f(a, b)` 와 `f(a, a)` 가 같아진다.
> 어느 지역인지는 남기고 그 이름만 지운다

### `e3baa46390859ef3` — S1 · `아니다` · 호출자 0 · 감시 1 → 1

**대상** `crates/pal-core/src/cross_file.rs` · 심볼 `484fd2669226dcb1…`

**`근거` 열의 문면** (판정 대상이다):

> 못 푼 참조의 걸음을 지나면서 적고 되짚어 만들지 않는다는, 이 함수가 자기 기록을 세우는 방식이다

**결박문 `note` 전문**:

> 회차 2026-09-08-cross-file-references — **못 푼 참조의 걸음은 지나면서 적는다. 되짚어 만들지 않는다.**
>
> `attempts` 가 빈 배열이면 이 노드는 자리만 있던 때로 돌아간다 — 스키마 주석이 그것을
> 이름 붙였고(선행 구현의 `runs` 가 146 건 전부에서 비어 있었다) 그래서 `B2` 가 그 자리를
> 잰다. 되짚어 만든 걸음은 관측이 아니라 재구성이고, 재구성은 해소기가 **실제로 무엇을
> 봤는지**를 못 말한다.
>
> 그리고 **끊김은 「0」이 아니라 「유일하지 않다」다** — 마지막 걸음의 `found` 가 1 이
> 아닌 것이 끊긴 자리이고, 그 둘을 같게 보면 `Ambiguous` 가 사라진다. 시험이 그것을 잡아
> 단언을 한 번 고쳤다.

### `e64fd4f5378c5f20` — S3 · `아니다` · 호출자 1 · 감시 1 → 1

**대상** `xtask/src/main.rs` · 심볼 `a23a6a391485b850…`

**`근거` 열의 문면** (판정 대상이다):

> 완성 장면을 소제목 셋의 번호 붙은 절로 정의하고 그 형식과 장면 수 하한을 기계가 잰다는 이 검사의 판정 형식이다

**결박문 `note` 전문**:

> 회차 2026-09-06-terrain-and-completion-scene 의 결정 — 완성 장면은 「누가·무엇을 해서·무엇을 받는가」 소제목 셋을 가진 번호 붙은 절로 정의하고, 그 형식과 장면 수 하한 셋을 기계가 잰다(완수 조건 A1·A1-a). 손으로 훑고 통과라고 적는 것을 금한다. 음성 대조는 observations/negative-control-scenes.txt 에 있다.

### `f4ba084c7a3838be` — S3 · `아니다` · 호출자 1 · 감시 1 → 1

**대상** `crates/pal-cli/src/round/status.rs` · 심볼 `3b1cfccff060320d…`

**`근거` 열의 문면** (판정 대상이다):

> 새 표기와 옛 표기를 둘 다 받는 별칭으로 종결 문서를 판정한다는, 이 검사가 받아들이는 문자열 집합의 결정이다

**결박문 `note` 전문**:

> ADR-0034 §3 — 기계 계약은 별칭이다. 새 표기를 쓰고 옛 표기도 계속 읽는다
>
> `folded.md` 의 필수 절과 `state.md` 의 `## 지금 단계` 가 「접힘」 문자열을 요구했다.
> 그 문자열을 실제로 지는 파일 다섯이 **전부 지난 회차의 종결 기록**이다.
>
> - 고치면 **증거 위조**다 — 그 회차가 그때 쓴 문장이 아니게 된다.
> - 안 고치고 검사만 새 표기로 옮기면 **그 회차 전부가 빨개진다.**
>
> 그래서 별칭이다 — `## 왜 철회했나`·「철회」와 `## 왜 접었나`·「접힘」을 둘 다 받고,
> **새 회차는 새 표기로 쓴다.** 음성 대조는 `git clone` 사본에 `--root` 를 주고 한
> `folded.md` 의 제목을 **양쪽 표기가 아닌 것**으로 바꿔 관측했다.
>
> ⚠ 같은 별칭을 `findings.jsonl` 의 필수 필드 `처분` 에는 안 걸었다 — 제목은 한 곳이지만
> 필수 필드는 두 벌이 되면 `record.py count` 의 집계 축이 갈린다(ADR-0034 §4).
>
> 회차 `2026-09-06-user-surface-vocabulary` · 이슈 #111

### `fcd48a9033fd1fb0` — S2 · `영향받는다` · 호출자 0 · 감시 1 → 1

**대상** `crates/pal-store/src/projection.rs` · 심볼 `6a77069c8591fd3a…`

**`근거` 열의 문면** (판정 대상이다):

> 그 값을 고칠 수 있는 사람이 우리가 할 수 있는 일의 범위를 넓히는가로 입력과 권한을 가르고 검증의 기준이 검증 대상 안에 있으면 안 된다는 신뢰 경계 규칙이다 ⟨호출자 0 — 넓혀도 감시가 안 커진다. 멤버 호출(x.foo())을 장치가 못 보는 것이고 pal touch 가 그 문장을 스스로 찍는다. 근거가 안 선 것이 아니다⟩

**결박문 `note` 전문**:

> 가르는 문장
> 그 값을 고칠 수 있는 사람이, 그 값으로 우리가 할 수 있는 일의 범위를 넓힐 수 있는가.
> · 넓힐 수 없으면 — 입력이다. 매니페스트의 `files` 는 "이것들을 되돌려라" 라는
> 입력이고, 되돌릴 수 있는 자리는 그것과 무관하게 이미 정해져 있다
> · 넓힐 수 있으면 — 권한이고, 그 자리는 대상 안에 있으면 안 된다. 상한·실행
> 여부·argv 의 형태가 전부 그 자리다
> · ⚠ 「검증을 하나 더 붙이면 되지 않나」는 이 문장을 안 지난다 — 검증의 기준이
> 검증 대상 안에 있으면 기준이 아니다

### `fd4efc1eaea040c4` — S2 · `영향받는다` · 호출자 0 · 감시 1 → 1

**대상** `crates/pal-core/src/binding.rs` · 심볼 `678dfa2b54b1eba4…`

**`근거` 열의 문면** (판정 대상이다):

> 빈 목록이 0 건인지 못 읽은 것인지를 이 자가 가르므로 읽는 표면이 그 값을 함께 실어야 하고 읽는 자리마다 음성 대조를 둔다는 결정이다 ⟨호출자 0 — 넓혀도 감시가 안 커진다. 멤버 호출(x.foo())을 장치가 못 보는 것이고 pal touch 가 그 문장을 스스로 찍는다. 근거가 안 선 것이 아니다⟩

**결박문 `note` 전문**:

> 회차 2026-09-08-cross-file-references — **빈 목록이 「0 건」인지는 이 자가 가른다.**
>
> `pal touch` 의 화면은 앞 판에서 *"여기의 빈 목록은 정직하다 — 능력이 있고 값이 없는
> 것이다"* 라고 적고 언제나 「아직 없습니다」를 찍었다. **그 문장이 거짓인 경우가 있다.**
> 파생 저장소(`.palimpsest/intent.redb`)는 커밋된 정본에서 세우는 것이고 `.gitignore` 가
> 지운다 — **새로 받은 사람에게는 언제나 그 상태다.** 없으면 조회가 빈 목록을 돌려주고,
> 그 빈 목록은 *"아무것도 안 걸렸다"* 가 아니라 **"못 읽었다"** 다.
>
> ⚠ **효과 실행이 그 거짓을 잡았다**(2026-09-10) — `pal touch print_facts` 가
> 「걸린 것 (0) 아직 없습니다」를 찍었는데 `intent/bindings.jsonl` 에 그 심볼 다이제스트를
> 지켜보는 결박이 실재했다. **이 함수는 이미 있었고 답이 그 값을 안 싣고 있었다.**
> 그러므로 이 자리는 「만들 것」이 아니라 **「읽을 것」**이었다.
>
> ★ **읽는 자리마다 음성 대조를 같이 둔다** — 정본도 없으면 「아직 없습니다」가 참이고
> 그때는 그렇게 적는다. 한쪽만 재면 그 자는 부재를 안 가르고 **언제나**를 잰다.

---

## 4. 위조 대조 셋 — **대조라고 적어서 준다**

⚠⚠ **아래 셋은 진짜 판정이 아니다.** 결박문은 **진짜**이고 `근거` 만 갈아 끼웠다.
설계문 §음성 대조 가 레시피를 고정했고 메인이 그대로 만들었다. **37 행을 대체하지
않는다** — 평가 단위가 **37 + 3 = 40** 이다.

★ **맹검을 안 세운 까닭**(설계자의 판단): `decisions.tsv` 는 커밋돼 있고 정(正)은 저장소를
읽는다. 위조를 숨기면 즉시 알아내거나, 못 알아냈을 때 그것이 **위조를 못 가렸다는 뜻인지
정본을 안 봤다는 뜻인지 안 갈린다.** 이 대조가 재는 것은 판정자의 눈가림이 아니라
**술어의 변별력**이다 — 셋 중 하나라도 `P 참` 이면 술어가 변별하지 않는다.

| 대조 | 어느 행에 | 갈아 끼운 `근거` | 왜 이 레시피인가 |
|---|---|---|---|
| **㉠ 측정값 재진술** | `1cc70cc19ef92186` · S3 · `아니다` · 호출자 17 | 호출자가 17 이고 감시 집합이 1 에서 1 로 안 커진다 | 기계 열의 값만으로 만든 문장이다 — 결박문을 한 낱말도 안 쓴다. ⒜ 를 겨눈다 |
| **㉡ 좌표 재진술** | `cc2cee65c3c176ab` · S1 · `아니다` · 호출자 0 | `classify.rs` 의 `grade_of` 에 걸린 결정이다 | 대상 심볼 이름과 파일 경로만으로 만든 문장이다. ⒜ 와 ⒝ 를 함께 겨눈다 |
| **㉢ 다른 결박의 근거 이식** | `5e6bbbac89053565` · S4 · `영향받는다` · 호출자 24 | ExportSet 이 담을 모집단을 ledger 의 stitch_of 가 최상위만 EXPORTS 로 옮기는 것에 맞춘 결정이라, 그 소비 쪽이 바뀌면 export_digest 와 EXPORTS 가 다른 모집단을 잰다 | `1cf63ec5e0e9937a` 의 **실제 근거 문면을 그대로** 옮겼다. 문면 자체가 *"소비 쪽이 바뀌면…"* 이라 ⒝·⒞ 를 그럴듯하게 지나고 **⒜ 만이 그것을 잡는다** |

**셋의 결박문은 §3 의 해당 항이 그대로 진다** — 위조한 것은 `근거` 하나뿐이다.

## 5. 이 패킷이 안 담는 것

- `widen/README.md` — 특히 「누가 판정했고 무엇을 안 받았나」와 「판정이 측정값을 복사하지 않았다」 절
- `docs/gates/binding-radius-in-use.md` 전부
- `intent.md` 전문 — `B2` 의 문면 본체는 §6 이 싣고 **⟨⟩ 안의 주석은 지웠다**
- `dialectic/` · `review/` · `conditions-audit/` · `premortem/` 아래 전부
- `3a3cb89428ed320b` · `6c6e402d958fc5e1` 의 결박문

## 6. 잠긴 의도에서 온 발췌 셋 — **이것만이다**

### `B2` 의 문면 본체 (⟨⟩ 주석을 지웠다)

> **B2** ⟨정반합⟩ **근거가 결박문을 읽은 판정이다** — 결박 하나하나에
> *「이 결정이 호출자 변경에 영향받는가」* 를 답하고 그 답을 `판정` 열에 적는다.

### 원문 25 행 · 27 행 (소유자가 잠근 것)

> 기존 결박 37 건을 어떻게 하나? → **내가 근거를 대고 넓힌다**

> 넓혀서 `stale` 이 늘었을 때 무엇이 참·거짓을 가르나? → **사람이 결박문을 읽고 판정한다**

### `## 차선책` 둘째 항 — **⒟ 가 여기서 온다**

> 넓힐 근거가 서는 것이 소수면 → 넓힌 수를 합격선으로 삼지 않고 **37 건 전부의 근거**를
> 산출로 남긴다. ⚠ **원인을 사람 판단으로 적지 마라** — 37 중 **13 이 호출자 0** 이고
> 그것은 멤버 호출(`x.foo()`)을 **장치가 못 보는 것**이지 근거가 안 서는 것이 아니다.
> *"근거가 서는 것이 24/37 이었다"* 로 적으면 **원인이 뒤바뀐다.**
