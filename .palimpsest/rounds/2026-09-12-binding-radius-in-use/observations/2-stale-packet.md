# 정반합 판 2 의 재료 묶음 — 일곱 건

> 회차 `2026-09-12-binding-radius-in-use` · 2026-09-13 · 넓힘 커밋 `e16dc61`
> 설계문 `dialectic/2-design.md` 의 §「메인이 판을 시작하기 전에 할 것」 1 이 이 파일이다.
>
> ⚠⚠ **이 파일이 판의 입력 정본이다.** 세션 스크래치패드의 초벌을 쓰지 않는다 —
> 임시 경로라 세션이 끝나면 판정의 근거가 좌표를 잃고, 그 초벌에는 **넓힐 때의 판정과
> 근거가 섞여 있어** 설계가 정(正)에게 금한 것이 들어간다.
>
> ⚠ **이 파일에는 「넓힐 때의 판정」과 「그 근거」가 없다.** 일부러 뺐다 — 그것을 보고
> *"참인 재검토다"* 를 쓰면 검증이 아니라 **같은 근거의 재낭독**이다(설계문 §역할별 입력).

## 무엇이 기계로 재어졌나 — 이 표가 판정이 아니라 **재료**다

| 결박 | 대상 | 반경 | 감시 전→후 | 타파일 | 켠 것 | **그중 시험 항목** | 판정 |
|---|---|---|--:|--:|--:|--:|---|
| `3d9979946eb321de`  | `RepoPath` | `symbol`→`callers` | 1→78 | 73 | 4 | **4** | `stale` |
| `5e6bbbac89053565`  | `SymbolKind` | `symbol`→`callers` | 1→25 | 23 | 3 | **3** | `stale` |
| `8597d886cb7afc70`  | `SURFACE_SOURCES` | `symbol`→`callers` | 1→2 | 0 | 1 | **0** | `stale` |
| `c201c01b6b0aff9a`  | `recovery_sites` | `symbol`→`callers` | 1→5 | 3 | 1 | **0** | `stale` |
| `d716d09163a6d0ab`  | `Elision` | `symbol`→`callers` | 1→18 | 12 | 1 | **1** | `stale` |
| `822ebf9b8071c799` **대조** | `PlanBindingState` | `symbol`→`callers` | 1→12 | 0 | 0 | **0** | `fresh` |
| `45d8808585ee0d50` **대조** | `Label` | `symbol`→`callers` | 1→11 | 0 | 0 | **0** | `fresh` |

★ **대조 둘**(`822ebf9b8071c799` · `45d8808585ee0d50`)은 **같은 반경 확대로 감시가 11 개·
10 개 늘었는데 아무것도 안 켰다.** 숨기지 않고 대조라고 적어서 준다 — `stale` 의 뜻이
*"감시 원소가 변했다"* 이므로 맹검이 원리상 안 선다(설계문). **이 둘에 *"재검토되어야
한다"* 가 붙으면 다섯의 `참` 은 낡음에 대한 정보를 안 담는다.**

★★ **기계가 가른 것 하나** — 다섯 중 **둘은 생산 코드가 켰고 셋은 시험 항목이 켰다.**
이것이 설계문 §갈래 1 의 중심이다: *"시험 본문이 변한 것이 그 결정의 재검토를 부르나."*

| 켠 것이 | 결박 |
|---|---|
| **생산 코드** | `8597d886cb7afc70` · `c201c01b6b0aff9a` |
| **시험 항목** | `3d9979946eb321de`(4/4) · `5e6bbbac89053565`(3/3) · `d716d09163a6d0ab`(1/1) |

## 기준 시점 — 다섯과 대조 둘 전부

| 결박 | `기준시점커밋` | base→HEAD 커밋 수 | `대상digest차이` | `base에없던새원소` |
|---|---|--:|---|--:|
| `3d9979946eb321de` | `9f0bf704464b` | 541 | `거짓` | 3 |
| `5e6bbbac89053565` | `9f0bf704464b` | 541 | `거짓` | 1 |
| `8597d886cb7afc70` | `9f0bf704464b` | 541 | `거짓` | 0 |
| `c201c01b6b0aff9a` | `9f0bf704464b` | 541 | `거짓` | 1 |
| `d716d09163a6d0ab` | `9f0bf704464b` | 541 | `거짓` | 3 |
| `822ebf9b8071c799` | `9f0bf704464b` | 541 | `거짓` | 0 |
| `45d8808585ee0d50` | `daf15debf439` | 336 | `거짓` | 2 |

⚠ **`대상digest차이 = 거짓`이 보증하는 것은 대상 심볼 하나의 본문**이 base 커밋과 같다는
것이다. `stale` 을 켠 것은 **감시 원소(호출자)의 본문 변화**다. 그 관측이 감시 원소
수십 개로 **일반화되는지**는 판정이 답해야 한다(설계문 §갈래 2).

---

## 결박 `3d9979946eb321de`

- **대상**: `RepoPath` · `crates/pal-core/src/repo.rs`
- **반경**: `symbol` → `callers` · 감시 `1` → `78` · 타파일 새 원소 `73`
- **기준 시점**: `9f0bf704464b` — 그 커밋부터 HEAD 까지 **541 커밋**
- **`대상digest차이`**: `거짓` · **`base에없던새원소`**: `3`
- **지금 판정**: `stale`

### 무엇이 켰나 (4 개)

- `자리가_어긋나면_조용히_넘기지_않는다` · `crates/pal-core/src/projection.rs:505-517` · function · **시험 항목**
    - 그 범위를 바꾼 커밋 `213d3ee` — fix(#114): [R3·R4] 검출 수단의 활용형이 조건 문면보다 좁던 것을 넓히고 376곳을 고친다
- `동점이면_확정하지_않는다` · `crates/pal-core/src/narrative.rs:675-706` · function · **시험 항목**
    - 그 범위를 바꾼 커밋 `8d90151` — fix(#114): [R5] 검사가 증거를 고치라고 요구하던 것을 막고 「접다」 33곳을 고친다
    - 그 범위를 바꾼 커밋 `8f182d6` — feat(#114): 소유자가 지목한 표현 패턴 일곱을 저장소에서 없애고 검출 수단을 건다
- `뺀_신호의_날것은_아무것도_안_건다` · `crates/pal-core/src/narrative.rs:761-775` · function · **시험 항목**
    - 그 범위를 바꾼 커밋 `8f182d6` — feat(#114): 소유자가 지목한 표현 패턴 일곱을 저장소에서 없애고 검출 수단을 건다
- `동점을_약한_신호로_깨지_않는다` · `crates/pal-core/src/narrative.rs:709-734` · function · **시험 항목**
    - 그 범위를 바꾼 커밋 `8d90151` — fix(#114): [R5] 검사가 증거를 고치라고 요구하던 것을 막고 「접다」 33곳을 고친다
    - 그 범위를 바꾼 커밋 `8f182d6` — feat(#114): 소유자가 지목한 표현 패턴 일곱을 저장소에서 없애고 검출 수단을 건다

### 결박문 전문 — **정본에서 그대로**

```
⚠ 이 결정이 안 덮는 것
`.rs` 밖의 표면. 방향 4 는 Rust 소스만 훑는다. 플러그인 매니페스트
(`plugin.json` · `commands/*.md`)에 질의 이름을 박아도 아무것도 울지 않는다.
지금은 손으로 댔고, 그 사실이 F06b 게이트 §판정-나-⑨ 에 있다.
인자의 타입. 이름과 필수 여부는 대조되지만 카탈로그의 `type`(`SymbolName` ·
`RepoPath`)이 어댑터가 내는 스키마와 같은지는 아무도 안 댄다.
호스트가 하나라는 것. 세션은 클라이언트 하나로 쟀다. "MCP 를 지원한다" 는
그보다 넓은 문장이고 이 결정은 그것을 주장하지 않는다.
```

## 결박 `5e6bbbac89053565`

- **대상**: `SymbolKind` · `crates/pal-core/src/symbol.rs`
- **반경**: `symbol` → `callers` · 감시 `1` → `25` · 타파일 새 원소 `23`
- **기준 시점**: `9f0bf704464b` — 그 커밋부터 HEAD 까지 **541 커밋**
- **`대상digest차이`**: `거짓` · **`base에없던새원소`**: `1`
- **지금 판정**: `stale`

### 무엇이 켰나 (3 개)

- `판` · `crates/pal-core/src/projection.rs:575-605` · function · **시험 항목**
    - 그 범위를 바꾼 커밋 `be86499` — round(rust-scope): 지역이 자기 초기화식을 가리던 것을 고쳐 참 엣지 44 를 되찾는다
- `재귀는_선언_거르기에_안_걸린다` · `crates/pal-core/src/projection.rs:730-754` · function · **시험 항목**
    - 그 범위를 바꾼 커밋 `a1a4542` — round(cross-file-references): **`A2` 의 갈래 — 임포트 참조가 지역 변수와 같은 칸에 있었다**
    - 그 범위를 바꾼 커밋 `be86499` — round(rust-scope): 지역이 자기 초기화식을 가리던 것을 고쳐 참 엣지 44 를 되찾는다
- `겹치는_심볼에서는_가장_안쪽이_출발점이다` · `crates/pal-core/src/projection.rs:642-666` · function · **시험 항목**
    - 그 범위를 바꾼 커밋 `a1a4542` — round(cross-file-references): **`A2` 의 갈래 — 임포트 참조가 지역 변수와 같은 칸에 있었다**
    - 그 범위를 바꾼 커밋 `be86499` — round(rust-scope): 지역이 자기 초기화식을 가리던 것을 고쳐 참 엣지 44 를 되찾는다

### 결박문 전문 — **정본에서 그대로**

```
③ `SymbolKind` 를 접지 않고 늘린다
종류는 `SymbolId::compute` 의 성분이므로 정직한 이름이 곧 정직한 좌표다.
`pal symbols` 가 `struct` 를 `class` 라 부르면 그것이 거짓이고, 그 거짓이 좌표에 실린다.
⚠ 처음에 적은 근거는 틀렸다. "접으면 충돌이 6.1% → 11.2% 로 는다" 라 적었는데
추출기가 선 뒤 재니 접어도 안 늘었다(cargo 380 에서 215 그대로). 그 수는
사전부검이 격리 스파이크로 잰 것이고 저장소에 안 남았다 — 독립 리뷰 R3 이 잡았다.
충돌 감소는 부수 효과였고 이 코퍼스에서는 0 이다. 지금은
`--example coord_collisions` 가 그 축을 잰다.
변형을 뒤에 더하는 것은 기존 이름을 안 움직인다 — `kind.name()` 문자열을 쓰기
때문이고, F02-1 이 TypeScript 에서 이미 그 선례를 만들었다.
```

## 결박 `8597d886cb7afc70`

- **대상**: `SURFACE_SOURCES` · `xtask/src/main.rs`
- **반경**: `symbol` → `callers` · 감시 `1` → `2` · 타파일 새 원소 `0`
- **기준 시점**: `9f0bf704464b` — 그 커밋부터 HEAD 까지 **541 커밋**
- **`대상digest차이`**: `거짓` · **`base에없던새원소`**: `0`
- **지금 판정**: `stale`

### 무엇이 켰나 (1 개)

- `check_catalog` · `xtask/src/main.rs:1375-1528` · function · 생산 코드
    - 그 범위를 바꾼 커밋 `8f182d6` — feat(#114): 소유자가 지목한 표현 패턴 일곱을 저장소에서 없애고 검출 수단을 건다

### 결박문 전문 — **정본에서 그대로**

```
결과
MCP 툴은 질의가 늘면 따라 는다. 갈릴 방법이 없다.
어댑터 크레이트는 `pal-cli` 에 의존할 수 없다(의존 방향 규칙 3). 그 제약이 둘째를
강제한다 — 조립을 쓰려면 트레이트로 받는 수밖에 없다.
방향 4 의 스캔 대상이 상수 하나(`SURFACE_SOURCES`)로 모였고, 없는 디렉터리는
조용히 건너뛰지 않고 문제로 적힌다 — 스캔이 0 개를 훑고 통과하는 형태를 막는다.
```

## 결박 `c201c01b6b0aff9a`

- **대상**: `recovery_sites` · `crates/pal-extract/src/parse.rs`
- **반경**: `symbol` → `callers` · 감시 `1` → `5` · 타파일 새 원소 `3`
- **기준 시점**: `9f0bf704464b` — 그 커밋부터 HEAD 까지 **541 커밋**
- **`대상digest차이`**: `거짓` · **`base에없던새원소`**: `1`
- **지금 판정**: `stale`

### 무엇이 켰나 (1 개)

- `extract_detailed` · `crates/pal-extract/src/typescript.rs:86-106` · function · 생산 코드
    - 그 범위를 바꾼 커밋 `11b6162` — round(rust-scope): 뼈대를 언어 중립으로 가르고 Rust 표가 참조 엣지 4179 을 산출한다

### 결박문 전문 — **정본에서 그대로**

```
이 결정이 적용되지 않는 자리
부재가 아닌 것에는 쓰지 않는다. F02-2 가 「입자 부재」(조상 없는 익명 함수의 변경)를
`recovery_sites` 에 담을지 판단하면서 안 담기로 했다 — 회복 지점은 "문법이 이 바이트를
못 읽었다" 이고 입자 부재는 바이트를 완벽히 읽었는데 담을 심볼이 없는 것이다.
종류가 다른 부재를 한 목록에 담으면 이 ADR 이 막으려는 것을 그대로 저지른다.
이유의 수가 늘어나는 것 자체가 관측 대상이다. 셋째 이유가 필요해지면 그때 "칸이어야
하는 것 아닌가" 를 다시 묻는다.
```

## 결박 `d716d09163a6d0ab`

- **대상**: `Elision` · `crates/pal-core/src/envelope.rs`
- **반경**: `symbol` → `callers` · 감시 `1` → `18` · 타파일 새 원소 `12`
- **기준 시점**: `9f0bf704464b` — 그 커밋부터 HEAD 까지 **541 커밋**
- **`대상digest차이`**: `거짓` · **`base에없던새원소`**: `3`
- **지금 판정**: `stale`

### 무엇이 켰나 (1 개)

- `멈출_때_대기열에_남은_것이_건수에_들어간다` · `crates/pal-core/src/traverse.rs:254-273` · function · **시험 항목**
    - 그 범위를 바꾼 커밋 `de09dd4` — feat(#111): 회차 어휘 일곱을 개명하고 기계 계약을 별칭으로 연다

### 결박문 전문 — **정본에서 그대로**

```
왜 다른 셋이 아닌가
| 대안 | 기각 이유 |
|---|---|
| `Elision` 에 사유를 하나 더한다 (`FoldedToOtherQuery`) | 같은 필드를 세는 소비자가 "절단 12 건" 을 읽는데 그중 열은 옮긴 것이다. `dropped()` 가 거짓말이 된다 |
| 요약만 싣고 아무 말도 안 한다 (지금까지의 상태) | 정확히 이 제품이 고발하는 조용한 공백이다. 그리고 소비자가 어디서 전체를 얻는지 모른다 |
| 접기를 옵션 플래그로 (`--full`) | F06 §6이 "옵션은 비게 된다" 로 이미 기각했다. 그리고 기본이 접힘이면 기본 경로가 침묵한다 |
```

## 결박 `822ebf9b8071c799` ⟨**대조 — `C3` 의 산출이 아니다**⟩

- **대상**: `PlanBindingState` · `crates/pal-core/src/plan.rs`
- **반경**: `symbol` → `callers` · 감시 `1` → `12` · 타파일 새 원소 `0`
- **기준 시점**: `9f0bf704464b` — 그 커밋부터 HEAD 까지 **541 커밋**
- **`대상digest차이`**: `거짓` · **`base에없던새원소`**: `0`
- **지금 판정**: `fresh`

### 무엇이 켰나 — **아무것도 안 켰다**

감시가 늘었는데 새 원소 전부가 base 커밋 뒤에 안 변했다. **그것이 이 건이 대조인
까닭이다.**

### 결박문 전문 — **정본에서 그대로**

```
가르는 문장
그 열거를 만드는 함수의 입력이 이 상태를 가질 수 있는가.
· 가질 수 있으면 — 변형을 더한다. `Undeterminable` 이 그랬다(F09)
· 가질 수 없으면 — 자기 열거를 세운다. `PlanBindingState` 가 그것이다
· 가질 수 없는데 더하면 — 아무도 못 만드는 이름(ADR-0012)이거나
한 열거가 두 모집단 위에 서는 것이고, 둘 다 이 저장소가 금한 형태다
```

## 결박 `45d8808585ee0d50` ⟨**대조 — `C3` 의 산출이 아니다**⟩

- **대상**: `Label` · `crates/pal-cli/src/label.rs`
- **반경**: `symbol` → `callers` · 감시 `1` → `11` · 타파일 새 원소 `0`
- **기준 시점**: `daf15debf439` — 그 커밋부터 HEAD 까지 **336 커밋**
- **`대상digest차이`**: `거짓` · **`base에없던새원소`**: `2`
- **지금 판정**: `fresh`

### 무엇이 켰나 — **아무것도 안 켰다**

감시가 늘었는데 새 원소 전부가 base 커밋 뒤에 안 변했다. **그것이 이 건이 대조인
까닭이다.**

### 결박문 전문 — **정본에서 그대로**

```
ADR-0033 §3 — 병기는 조각 둘로 낸다. 그리고 코어 밖에 둔다

`Label` 이 `원_표기`·`사용자_언어` 를 따로 낸다. 까닭 둘.

**① 정렬.** `{:<N}` 은 char 수로 채우는데 한글은 터미널에서 두 열을 먹는다. 고정폭
열에는 원 표기만 넣고 병기는 열 뒤에 붙인다 — 두 조각이 **한 줄에 같이 있으면 그것이
병기**다. 붙어 있어야 하는 것이 아니다. `unicode-width` 의존은 안 들였다.

**② 오염.** `pal-core` 의 `name()` 은 화면 문자열이 아니라 **와이어 토큰**이다.
`export.rs` 가 Cypher 속성 값으로 쓰고, `pal-query` 가 `watch_grades` 의 키로 쓰고,
넷(`ResolutionGrade`·`Provenance`·`QueryName`·`schema::Cardinality`)은 자기 `parse` 의
열쇠다. 병기를 크레이트 경계 밖에 두면 그 둘이 **구조로** 막힌다 — `pal-core` 는 이
모듈을 볼 수 없다.

「해소 등급」은 이 빌드에서 사람 화면에 안 나간다(모집단 0). 그 자리는 `pal-core` 의
`왕복_파서를_진_표시_함수` 가 대신 잠근다.

회차 `2026-09-06-user-surface-vocabulary` · 이슈 #110
```

