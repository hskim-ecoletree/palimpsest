# 반경별 벤치 — **첫 실행의 전 출력** (보존본)

> 회차 `2026-09-12-binding-radius-in-use` · 2026-09-13 · **커밋 `67e0640` 의 산출이다**
>
> ★★ **왜 이 파일이 있나** ⟨2026-09-13 · 독립 리뷰 R3 발견 24⟩. 이 회차는 독립 리뷰 R2 의
> 판정(`E1` 반증 — 등록 16 중 10 만 돌았다)을 받아 **벤치를 고쳐 다시 돌렸고**, 그 두 번째
> 실행이 `bench/radius-run.txt` 를 **덮어썼다.** 그러자 게이트가 인용하던 첫 실행의 수
> (`86.157` · `0.572` · 선형성 `3.84`·`3.87`·`4.02`)를 **지는 산출이 저장소에서 사라졌다** —
> R3 이 그것을 금지역 `사실이_아닌_것을_사실로` 로 잡았다.
>
> **고침은 둘이다**: ⑴ 게이트의 인용을 두 번째 실행의 값으로 갈았다 ⑵ **첫 실행을 이
> 파일로 되살렸다**(`git show 67e0640:…`). 정정이 재는 양을 늘리면서 **옛 측정을 나란히
> 안 남기면 그 측정을 인용한 문장이 고아가 된다** — 그 형태를 여기 적어 둔다.
>
> ⚠ **판정을 지는 것은 이 파일이 아니라 `bench/radius-run.txt`(두 번째 실행)다.**
> 이것은 보존본이고, 두 실행의 방향이 같다는 사실(두 비가 1 을 건너뛴다)을 함께 보인다.

---

# 반경별 벤치 — **손으로 돌린 전 출력**  ⟨조건 `E1`·`E2`·`E3`·`E6`⟩

> 회차 `2026-09-12-binding-radius-in-use` · 2026-09-13
> 명령: `cargo test -p pal-cli --release --test radius_bench -- --ignored --nocapture`
> HEAD: `47a0014` · 종료값 **0**
>
> ⚠⚠ **CI 는 이것을 안 돌린다.** `cargo xtask test` 의 축이 `--all-targets` 와 `--doc`
> 둘뿐이고 `--ignored` 가 없다(`xtask/src/main.rs:384-387`). 소유자 결정 ⟨2026-09-13⟩ 이
> **㈎ — 이 회차가 손으로 돌려 수를 게이트에 싣고, CI 가 안 돌린다는 사실을
> `## 범위 밖` 에 명시하고, `--ignored` 축은 이슈로 세운다**(**#148**).
>
> **그러므로 「아무도 안 돌림」이 이 회차에 대해 거짓이다** — 이 파일이 수를 진다.
> 남는 것은 **회귀 탐지 공백** 하나이고 그것은 기록된 경계다.

## 전 출력

```

running 1 test

═══ F09 반경별 벤치 · `[f09.4.pass]` ④ ═══

  ⚠⚠ **넷째 팔은 대리물이다.** 등록된 합격선의 주어(*"증분으로 상태를 갱신하는
     파이프라인"*)가 이 빌드에 없다. 재는 것은 `WATCH` 색인 조회 대 전수 훑기이고,
     그래서 `[f09.4.pass]` ④ 는 이 벤치가 돌아도 **대조불가**다.

  ⚠ **합격선이 `changed` 크기를 안 적었다.** 그 값이 판정을 뒤집으므로 작은 것과
     큰 것을 둘 다 돌고 두 비가 1 을 건너뛰는지 적는다.

  회차 3 · 최솟값 · 규모 2000 → 8000 (4배) · 결박 200 건

  규모 2000 — 심볼 2000 · 파일 500
    ① symbol 반경                  min   37.750µs · max/min 1.09배 · 산출 500
    ② callers 반경                 min  583.333µs · max/min 1.04배 · 산출 2499
    ③ closure(2) 반경              min    2.202ms · max/min 1.03배 · 산출 4494

  규모 8000 — 심볼 8000 · 파일 2000
    ① symbol 반경                  min  145.042µs · max/min 1.01배 · 산출 2000
    ② callers 반경                 min    2.259ms · max/min 1.02배 · 산출 9999
    ③ closure(2) 반경              min    8.854ms · max/min 1.03배 · 산출 17994

  반경이 무엇을 늘리나 — 감시 집합 크기의 합
    규모 2000: {"① symbol 반경": 500, "② callers 반경": 2499, "③ closure(2) 반경": 4494}
    규모 8000: {"① symbol 반경": 2000, "② callers 반경": 9999, "③ closure(2) 반경": 17994}

  선형성 — 노드 4배에 대해
    ① symbol       3.84배   (선: 8배 이내 — 대상 수가 4 배다)
    ② callers      3.87배   (선: 8배 이내)
    ③ closure(2)   4.02배   (기록만 — 차수와 k 의 함수다)

  ④ WATCH 색인 대 전수 훑기 — 결박 200 건 · 감시된 좌표 801

    ④-가 WATCH 색인 조회 (changed 1)  min   18.625µs · max/min 6.27배 · 산출 1
    ④-나 전수 훑기      (changed 1)   min    1.605ms · max/min 1.70배 · 산출 1
    ④-가 WATCH 색인 조회 (changed 801) min    1.956ms · max/min 1.22배 · 산출 200
    ④-나 전수 훑기      (changed 801) min    1.118ms · max/min 1.18배 · 산출 200

  ④ 색인이 전수보다 몇 배 빠른가 — **`changed` 크기가 판정을 뒤집는다**
    changed     1 (닿는 결박    1) → 86.157배
    changed   801 (닿는 결박  200) → 0.572배
    두 비가 1 을 건너뛰나 — **그렇다**  ← 부하 선택이 합격선을 정한다. 게이트에 결함으로 적는다

test 반경별_벤치와_선형성 ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 1.62s

```
