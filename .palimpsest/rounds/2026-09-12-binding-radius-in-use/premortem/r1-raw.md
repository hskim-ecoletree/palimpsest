# 사전부검 R1 — 원 반환문

> 낸 자: `pal-premortem-sweeper` · 2026-09-12 · 회차 `2026-09-12-binding-radius-in-use`
> **받은 것**: `intent.md`(원 의도와 계획) · 저장소
> **안 받은 것**: 대화 기록 · 이 계획을 쓴 자의 설명 · 앞 라운드 결과(라운드 1 이라 없다)
>
> ⚠⚠ **한 자리에서 표기만 바꿨다 — 내용은 안 바꿨다.** 「내가 기각한 것」 표의 마지막
> 행 제목이 이스케이프된 파이프(`\|`)를 넉 장 담고 있어 `extract.py` 의 표 파서가 열을
> 밀어 읽었다(19 번째 행의 `유효성` 칸에 제목 조각이 들어갔다). 그래서 그 파이프들을
> 「세 칸이 전부 `—` 인 행」이라는 말로 바꿨다. **가리키는 대상도 판정도 그대로다.**
> 이것은 자기 장치(추출기)의 결함이고 이 회차의 모집단 분리 규칙에 따라 **이슈로 세운다.**

> ⚠ **이 파일은 원 반환문이다. 메인이 요약하거나 고치지 않는다** — 합계 검산이 독립된
> 둘째 원천을 대야 하기 때문이다.

---

착수 관측을 전부 다시 돌렸고(`pal query binding.status` · `pal query graph.dump --json` · `pal touch print_facts` · `pal doctor --json`), 격리된 의도 저장소(`/private/tmp/.../scratchpad/exp1/intent.redb`)와 격리 클론(`/tmp/binding-radius-1789219449`)에서 **실제로 반경을 넓혀 보고** 냈다. 워킹트리는 안 건드렸다.

먼저 이 회차의 계획이 안 적은 착수 사실 하나: **37 건 중 7 건이 지금 `stale` 이다**(`pal query binding.status` — fresh 30 · stale 7). 계획의 착수 관측표에 이 수가 없다. 아래 시나리오 여럿이 이 수 위에 선다.

---

### ① 반경을 넓히는 유일한 경로가 `bound_at` 과 감시 다이제스트를 HEAD 로 재기준한다 — 지금 `stale` 인 7 건이 조용히 `fresh` 가 된다

- **어떻게 실패하나**: 반경을 바꾸는 명령이 이 빌드에 **없다.** `pal --help` 의 17 명령 어디에도 rebind·widen 이 없고, 넓히는 길은 `pal bind <이름> --note <같은 조각> --radius callers` 를 다시 부르는 것뿐이다. 그런데 `bind::run` 은 `bound_at` 을 **지금 스냅샷**으로 새로 쓰고 `watch` 다이제스트를 **지금 투영**에서 새로 읽는다. 그래서 넓히는 순간 그 결박의 낡음 판정이 0 으로 되감긴다. 격리 저장소에서 실측: `4065e80475cf1c72`(`check_awkward_phrases`)를 `--radius callers` 로 다시 걸었더니 `bound_at` 이 `daf15de/tree 1107229…` → `1276b8f/tree b20885cc…`, 감시 다이제스트가 `0d39e24f…` → `931c843f…`(= 지금 본문)로 바뀌었고, **전체 판정이 `stale 7 · fresh 30` → `stale 6 · fresh 31` 로 움직였다.** 37 건 전부에 같은 일을 하면 지금 서 있는 낡음 신호 **7 건이 전부 사라진다.** 그리고 그 뒤 `pal intent export` 로 정본에 실리면, 파일은 *"이 결정들은 1276b8f 에 결박됐다"* 고 적는다 — **사실이 아니다.**
- **어디가 걸리나**: `crates/pal-cli/src/bind.rs:136`(`bound_at: report.ledger.snapshot.clone()`) · `crates/pal-cli/src/bind.rs:98-109`(감시 다이제스트를 지금 투영에서 읽음) · `crates/pal-cli/src/bind.rs:118`(`BindingId::derive` 로 같은 id 를 잡아 덮어씀) · `crates/pal-intent/src/store.rs:191` `record`
- **획득**: 조회 — `pal --help` · `bind.rs` 정독 + **실측**(격리 의도 저장소에 정본을 import → 재결박 → `pal intent export` → 필드 비교 → `binding.status` 재실행)
- **모집단**: 저장소
- **유효성**: 참
- **해악도**: **금지역** — `intent.md:136-138` 이 지목한 **데이터_손실**, 그리고 **사실이_아닌_것을_사실로**
- **대상**: 계획대상
- **얼마나 아픈가**: 파일 되돌리기는 **된다** — 원 `bindings.jsonl` 을 다시 import 하니 같은 id 위에 옛 `radius`·`watch`·`bound_at` 이 그대로 복원됐고 `stale 7` 이 돌아왔다(실측). **그러나 되돌리면 반경도 같이 되돌아간다.** 「넓히면서 낡음 판정을 보존한다」는 경로가 이 빌드에 없다. 걸리는 곳은 37 건 전부 · 명령 표면 1

---

### ② 감시 원소 하나가 사라지면 `Undeterminable` 이 `Stale` 을 **가린다** — 넓힌 반경이 낡음을 드러내는 게 아니라 덮는다

- **어떻게 실패하나**: `BindingStatus::evaluate` 는 ② 단계에서 `Undeterminable` 을 만나면 **③④ 에 가기 전에 반환한다.** 즉 감시 집합 안의 **호출자 하나라도 이름·파일이 바뀌면** `WatchMemberGone` 이 되고, **대상 본문이 실제로 변했다는 사실(`Stale{triggered_by}`)이 화면에서 사라진다.** 격리 클론에서 실측: `check_awkward_phrases` 를 `callers`(감시 2)로 걸고 ⓐ 대상 본문만 고쳤을 때 → `최신 상태 아님(stale) ← 1 개가 변했습니다`. ⓑ 이어서 호출자 `check` 를 `check_renamed_by_premortem` 으로 이름만 바꿨더니 → **`판정 불가(undeterminable) ← 감시 대상이 사라짐(watch-member-gone)`** 로 바뀌고 *"1 개가 변했습니다"* 가 **사라졌다.** 대상은 여전히 변해 있다. `symbol` 반경이었으면 `stale` 로 정확히 떴을 자리다. 이 저장소의 정체성 등급은 `ordinal` 이고 `SymbolId` 에 **파일 경로와 이름이 들어간다** — 리네임·파일 이동은 이 회차들이 늘 하는 일이다.
- **어디가 걸리나**: `crates/pal-core/src/binding.rs:613` `evaluate` · `:634`(gone → `WatchMemberGone`) · `:637-643`(②가 ③④ 앞에서 반환) · `crates/pal-core/src/coord.rs:111-128`(`SymbolId` 에 path·name 포함)
- **획득**: 조회 + **실측** — 격리 클론(`/tmp/binding-radius-1789219449`)에서 결박 → 대상 변형 → 호출자 리네임 → `pal query binding.status` 3 회
- **모집단**: 저장소
- **유효성**: 참
- **해악도**: **금지역** — **사실이_아닌_것을_사실로**(실제 낡은 것을 「판정 불가」로 적는다) 및 **측정이_죽은_가지**(§8 효과 증인 *"넓힌 반경이 실제 재검토를 불렀다"* 가 원리상 안 켜지는 경로가 생긴다)
- **대상**: 계획대상
- **얼마나 아픈가**: 되돌리려면 반경을 도로 좁혀야 한다. 걸리는 곳은 넓힌 결박 전부 — 실측으로 **감시 원소를 얻는 결박이 24 건**이고 그중 **7 건은 파일 간 호출자**를 가진다(파일 이동 한 번에 다 걸린다)

---

### ③ 사전 등록된 표본 20 의 절반(portal-backend · Kotlin)이 **구조적으로** `callers` == `symbol` 이다 — #58 이 걸린 병이 그대로 재현된다

- **어떻게 실패하나**: `[f09.4].sample_selection` 규칙 1 이 *"코퍼스 둘에서 각각 10 건, 합 20 건"* 으로 못 박았고 `ordinal` 축은 portal-backend(Kotlin)다. 그런데 **Kotlin 추출기는 스코프·임포트·엑스포트를 안 만든다** — `Capable::not_built`. 엣지가 0 이므로 `Projection::callers_of` 가 **빈 목록**을 돌려주고, `expand` 는 대상 하나만 담는다. 즉 Kotlin 쪽 10 건은 `callers` 로 걸어도 감시 집합이 1 이고 `symbol` 과 **글자까지 같다.** `corpus/criteria.toml:6978-6988`(`not_callers_false_positive`)이 #58 에서 적은 *"두 반경의 표본이 글자까지 같았다"* 가 **같은 원인으로 다시 난다.** 그리고 `criteria.toml:6713-6716` 이 그 형태를 막으려고 박은 하한(*"`callers` 반경에서 표본 중 하나 이상이 2 이상"*)은 Rust 쪽 10 건으로 통과해 버려서, **하한이 걸리지 않은 채 절반이 죽은 가지가 된다.**
- **어디가 걸리나**: `crates/pal-extract/src/kotlin.rs:157-161`(`kotlin-exports`·`kotlin-imports`·`kotlin-scopes` 전부 `not_built`) · `crates/pal-store/src/projection.rs:1079-1082`(읽기 실패·부재를 빈 목록으로) · `corpus/criteria.toml:732-735`(*"이 코퍼스에서 `facts` 가 `{callers: 0, callees: 0}`"* 이미 실측돼 있다) · `corpus/criteria.toml:7291-7313`
- **획득**: 조회 — `extractor_for` 와 `kotlin.rs` 의 `Capable` 자리 · `criteria.toml` 의 앞선 실측. ⚠ **portal-backend 에 직접 `pal` 을 돌려 0 을 재지는 않았다**
- **모집단**: 저장소
- **유효성**: 참(저장소가 이미 스스로 실측해 적어 둔 값 위에 선다)
- **해악도**: **금지역** — **측정이_죽은_가지**(`intent.md:138-139` 가 지목한 바로 그 형태)
- **대상**: 계획대상
- **얼마나 아픈가**: 되돌릴 것은 없지만 ㈂ 의 절반이 원리상 못 닫힌다. 걸리는 곳: 사전 등록 표본 20 중 10

---

### ④ 모집단을 「이 저장소로 먼저 → 안 갈리면 코퍼스로」로 정하는 것이 **사전 등록 규칙 위반**이라 ㈂ 가 통과해도 그 값이 통과의 근거가 못 된다

- **어떻게 실패하나**: `intent.md:96-99` 의 차선책이 *"`callers` 가 실제로 갈리는 표본을 이 저장소에서 못 만들면 … 코퍼스로 모집단을 넓힌다"* 라고 적었다. 그런데 등록된 규칙은 **코퍼스 둘이 1 순위이고 palimpsest 는 모집단이 아니며**, 마지막 줄이 *"**결과를 보고 이 규칙을 바꾸지 않는다.** 바꾸면 그 사실과 이유를 게이트에 적고, **바꾼 뒤의 값은 통과의 근거가 될 수 없다**"* 다. 계획이 적은 것은 정확히 **결과를 보고 모집단을 고르는 절차**다. 그러므로 ㈂ 가 어떤 수를 내든 `[f09.4.pass]` 의 통과로 셀 수 없고, 회차는 「닫았다」고 적게 된다.
- **어디가 걸리나**: `corpus/criteria.toml:7291-7313`(`[f09.4].sample_selection` 규칙 1·4 와 마지막 줄) · `corpus/criteria.toml:6403-6408`(*"결과를 보고 표본을 바꾸지 않는다"*) · `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:96-99`
- **획득**: 조회 — `criteria.toml` 의 `[f09.4]` 절 전문과 intent.md 차선책 대조
- **모집단**: 규약
- **유효성**: 참
- **해악도**: **금지역** — **사실이_아닌_것을_사실로**(등록된 합격선을 닫았다고 적는데 그 등록이 금지한 절차로 얻은 값이다)
- **대상**: 계획자신
- **얼마나 아픈가**: 지금 계획 문서 한 줄을 고치면 되돌릴 수 있다(아직 아무것도 안 쟀다). 걸리는 곳: ㈂ 전체

---

### ⑤ ㈀ 가 넓힐 대상을 **손으로 고르고** ㈂ 가 **그 손으로 고른 집합에서** 거짓 양성률을 잰다 — 측정 독립성이 없다

- **어떻게 실패하나**: 인터뷰가 잠근 답이 *"기존 결박 37 건을 **내가 근거를 대고 넓힌다**"* 이고 *"무엇이 참·거짓을 가르나 → **사람이 결박문을 읽고 판정한다**"* 다. 그리고 계획은 ㈀ 과 ㈂ 를 *"한 축"* 으로 묶었다. 그러면 **넓힐 것을 고르는 자 · 넓힌 결과를 판정하는 자 · 거짓 양성률을 적는 자가 같다.** 등록된 규칙이 그 재량을 정면으로 제거한 자리다 — 규칙 4 는 *"고르는 방법: 대장의 심볼을 `symbol_id` 사전순으로 정렬해 균등 간격으로 뽑는다 — **우리가 고르지 않는다**"* 이고, 규칙 3 의 근거는 *"고르는 사람이 판정하는 사람과 같으므로(R-18)"* 다. 실패 서사: 호출자가 시끄러운 결박은 *"근거가 안 선다"* 로 안 넓히고, 조용한 결박만 넓히면 거짓 양성률은 **무조건** 합격선 안에 든다. 그 수는 반경에 대해 아무것도 안 말한다.
- **어디가 걸리나**: `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:28-30`(잠긴 답 둘) · `intent.md:91`(*"㈂ 은 ㈀ 과 한 축이다"*) · `corpus/criteria.toml:7300-7307`(규칙 3·4) · `corpus/criteria.toml:6398-6400`
- **획득**: 조회 — 두 문서 대조
- **모집단**: 규약
- **유효성**: 참
- **해악도**: **금지역** — **측정이_죽은_가지**
- **대상**: 계획자신
- **얼마나 아픈가**: ㈀ 과 ㈂ 의 표본을 갈라 놓으면(㈂ 는 규칙 4 의 균등 간격으로 새로 뽑는다) 해소된다. 지금 고치는 비용은 계획 문서 한 절

---

### ⑥ `callers` 가 새로 더하는 감시 원소의 **49% 가 시험 함수**다 — 등록된 거짓 양성률 상한 30% 를 구조가 먼저 깨뜨린다

- **어떻게 실패하나**: 37 건 전부를 `callers` 로 넓혔을 때 새로 감시에 들어오는 심볼을 그래프에서 셌다 — **173 개**. 그중 **85 개(49.1%)가 `mod tests` 안이거나 `tests/` 아래의 시험 함수**다(예: `노드_상한만_낮추면_노드_상한만_걸린다` · `생략은_쌓인다` · `넉넉하면_생략이_0_이다`). 감시 원소를 얻는 결박 24 건 중 **12 건이 시험 함수 감시자를 최소 하나 얻는다.** 그러면 *"이 결정이 아직 유효한가"* 의 답이 **단위 시험 본문 한 줄 수정으로 `stale`** 이 된다 — 등록이 `callers` 거짓 양성률 상한을 **30%** 로 박았는데(`false_positive_rate_callers_radius_percent = 30`), 이 모집단에서는 그 상한을 넘기는 쪽이 자연스럽다. 그리고 **그 반증은 반경의 성질이 아니라 모집단 선택의 결과**인데, 게이트에는 *"`callers` 반경 거짓 양성률 반증"* 으로 적힌다.
- **어디가 걸리나**: `corpus/criteria.toml:6840-6855` · `crates/pal-core/src/radius.rs:132-164` `expand` · 실측 원천 `pal query graph.dump --json`
- **획득**: **실측** — `graph.dump` 의 역엣지로 37 대상의 호출자 집합을 만들고 `container` 에 `tests` 가 있거나 경로가 `/tests/` 인 것을 셌다
- **모집단**: 저장소
- **유효성**: 참
- **해악도**: **실패** — ㈂ 의 거짓 양성률이 등록된 합격선 밖으로 나가 반증으로 닫힌다
- **대상**: 계획대상
- **얼마나 아픈가**: 되돌릴 수 있다(반경을 좁힌다). 걸리는 곳: 결박 12 건 · 감시 원소 85 개

---

### ⑦ Rust 멤버 호출을 원리상 못 세므로 **37 중 13(35%)은 넓혀도 감시 집합이 안 커진다** — 산출에는 `callers` 라 적히고 감시는 그대로 1 이다

- **어떻게 실패하나**: `x.foo()` 는 `field_identifier` 라 참조 엣지가 안 되고, 이 저장소가 스스로 *"멤버 해소는 이 회차의 범위 밖"* 이라 적었다. 실측: 37 개 대상의 호출자 수 분포는 `0:13 · 1:9 · 2:1 · 4:3 · 5:2 · 9:1 · 10:2 · 11:1 · 17:3 · 24:1 · 77:1` — **13 건이 호출자 0** 이다. 그중 셋(`resolve_shadowing` · `패턴_순회` · `cross_file_edges`)은 **지금 `stale` 인 7 건 안에 있다.** 그 셋은 `--radius callers` 로 넓히면 ①에 의해 `stale` 이 지워지고, 그 대가로 **아무것도 안 얻는다**(감시 집합이 여전히 1). 그리고 `resolve_shadowing` 은 저장소가 이미 실측해 둔 자리다 — *"`호출자 0` 을 돌려주는데 바로 옆 `resolve_with` 이 `self.resolve_shadowing(…)` 으로 부른다."* 계획의 차선책(`intent.md:100-102`)은 이 사태를 *"넓힐 **근거**가 서는 것이 소수"* 라는 **사람 판단 문제**로 미리 적어 두었는데, 실측 원인은 **장치가 못 보는 것**이다. 그대로 가면 게이트에 *"근거가 서는 것이 24/37 이었다"* 로 적히고 원인이 뒤바뀐다.
- **어디가 걸리나**: `crates/pal-cli/src/touch.rs:489-497`(멤버 호출 미계수 · `resolve_shadowing` 실측 주석) · `crates/pal-core/src/radius.rs:137`(`Radius::Callers => out.extend(n.callers_of(target))`) · `crates/pal-store/src/projection.rs:1074-1078`(빈 목록이 *"덜 지켜본다"*) · `.palimpsest/intent/bindings.jsonl`
- **획득**: **실측** — `graph.dump` 역엣지로 37 대상의 호출자 수를 전수로 셌고, stale 7 건의 id 와 교차시켰다
- **모집단**: 저장소
- **유효성**: 참
- **해악도**: **거짓신호** — 안 깨지지만 `radius callers · 감시 1` 이 *"넓혔다"* 로 읽히고, 게이트의 원인 귀속이 틀린다
- **대상**: 계획대상
- **얼마나 아픈가**: 되돌릴 수 있다. 걸리는 곳: 결박 13 건(그중 stale 3 건은 ①과 겹쳐 **순손실**)

---

### ⑧ `repo_id` 가 **디렉터리 이름**이라 이름이 다른 클론·워크트리에서 넓히면 37 건이 덮이지 않고 **새 37 건이 더해진다** — 지우는 API 가 없다

- **어떻게 실패하나**: `.palimpsest/` 에 매니페스트가 없어서 `repo_id` 가 `repo_name(repo_path)` = **디렉터리 이름**으로 떨어진다. `SymbolId` 가 `repo_id` 를 해시에 넣고 `BindingId::derive(target, note)` 가 그 `target` 을 넣으므로, **디렉터리 이름이 다르면 같은 조각·같은 함수라도 다른 결박 id** 가 된다. 실측: `/tmp/binding-radius-1789219449` 로 클론해 정본 37 건을 import 한 뒤 같은 note·같은 함수로 `--radius callers` 를 걸었더니 → 좌표가 `binding-radius-1789219449@1276b8f+worktree#ce311d2ebc4b`(원본은 `#bd26d1d8a3ee`), 결박 id 가 `7dbe515c81a26e4b`(원본 `4065e80475cf1c72`), **개체가 새로 뽑히고**(`01M2AWMPX7GMF16CPNGBW00P8N` ≠ `01M1T18H3FXZYMPF4MAE4KRJQG`), **건수가 37 → 38.** 이 회차의 작업 관행(격리 사본·`git worktree`)과 정면으로 부딪친다. 37 건 전부에 하면 **74 건**이 되고, `pal-intent` 에는 **지우는 공개 API 가 없다**(R-21). 그리고 코드가 스스로 그 위험을 적어 두었다 — *"경로에서 유도한 이름은 저장소를 옮기면 바뀌고, 그러면 **결박이 가리키는 좌표가 통째로 흔들린다**."*
- **어디가 걸리나**: `crates/pal-cli/src/ledger.rs:117-122` · `crates/pal-core/src/coord.rs:111-128` · `crates/pal-core/src/binding.rs:29-36` `BindingId::derive` · `crates/pal-intent/src/store.rs:191` (`record` 만 있고 `remove` 없음) · `crates/pal-intent/src/store.rs:670`(import 는 **더하기**)
- **획득**: **실측** — 격리 클론에서 재현(37 → 38 · 새 id · 새 개체)
- **모집단**: 저장소
- **유효성**: 참
- **해악도**: **금지역** — **데이터_손실**(정본에 중복 결박이 실리고 제거 경로가 없다. 복구는 `intent.redb` 파일을 지우고 정본을 다시 import 하는 것뿐)
- **얼마나 아픈가**: `bindings.jsonl` 을 아직 안 커밋했으면 git 되돌리기로 회복. 커밋했으면 정본에서 손으로 지워야 한다. 걸리는 곳: 최대 37 건 × 2
- **대상**: 계획대상

---

### ⑨ 벤치의 등록된 합격선이 *"증분 갱신이 전체 재계산보다 빠르다"* 인데 **증분 갱신이 이 빌드에 없다** — ㈂ 의 ④ 는 원리상 못 닫는다

- **어떻게 실패하나**: `[f09.4.pass]` ④ `bench_grounds` 가 *"**합격선은 절대 시간이 아니라 이것이다**: 증분 갱신이 전체 재계산보다 빠르다. 안 빠르면 `WATCH` 색인이 아무 값도 안 낸 것"* 이라고 못 박았다. 그런데 `증분` 을 코드에서 찾으면 나오는 것은 **입구 주석 넷뿐**이다 — `bindings_watching` 이 *"증분 상태 갱신의 **입구**"* 라 적혀 있고, 증분으로 상태를 갱신하는 경로는 없다. `binding.status` 는 매번 전량 재계산한다. 그리고 `criteria.toml:4444` 계열의 「없다 (여덟)」 목록이 *"**증분 상태 갱신**"* 을 미구축으로 적어 두었다. 그러므로 ㈂ 가 ④ 를 「닫았다」고 적으려면 **합격선을 다시 써야 하고**, 그것은 *"재기 전에 확정한다"* 의 정면 위반이다.
- **어디가 걸리나**: `corpus/criteria.toml:7358-7373`(`bench_by_radius` · 합격선 문장) · `corpus/criteria.toml:6446-6448`(*"없다 (여덟)"* 에 「증분 상태 갱신」) · `crates/pal-intent/src/store.rs:232`(*"증분 상태 갱신의 입구"*) · `crates/pal-core/src/touch.rs:290`
- **획득**: 조회 — `grep -rn "증분" crates --include=*.rs`(입구 주석 넷뿐) + `criteria.toml` 의 ④ 전문
- **모집단**: 저장소
- **유효성**: 참
- **해악도**: **금지역** — **측정이_죽은_가지**(합격선을 재정의해 닫으면 그 벤치는 아무것도 안 잰다)
- **대상**: 계획대상
- **얼마나 아픈가**: 되돌릴 것 없음. 걸리는 곳: ㈂ 의 절반(벤치)

---

### ⑩ 비교의 기준선인 `symbol` 반경 거짓 양성률이 **반증인 채 서 있고**(42.9%) 그 재측정은 **대조 불가**로 끝났다 — `callers` 값을 해석할 자가 없다

- **어떻게 실패하나**: `[f09.pass]` 가 `callers` 값을 **`symbol` 값과 대 보게** 설계했다 — *"이 값이 `symbol` 반경의 값보다 낮게 나오면 그것은 **통과가 아니라 의심**이다."* 그런데 그 `symbol` 값은 **F09 게이트에서 42.9%(7 중 3)로 상한 10% 를 넘겨 반증**이고, [ADR-0014] 가 그것을 F10 으로 넘겼는데 **F10 이 15/20 으로 대조 불가**를 적고 닫았다(*"채우려고 편의 표본을 안 섞는다"*). 즉 ㈂ 가 `callers` 에서 어떤 수를 내도 **비교 대상이 없다.** 더 나쁜 것은 F10 이 실패한 그 경로가 **계획의 차선책이 제시한 바로 그 경로**(F09 표본 규칙 + 코퍼스)라는 점이다 — 계획은 이미 한 번 대조 불가로 끝난 길을 차선책으로 적었고, 그 사실을 안 적었다.
- **어디가 걸리나**: `docs/gates/F09.md:13` · `docs/gates/F09.md:34`(⑤ 42.9% 반증) · `docs/gates/F10.md:41`(⑨ 표본 15 · 대조 불가) · `docs/gates/F10.md:305-316`(*"F10 이 그것을 시도했고 못 닫았다"*) · `corpus/criteria.toml:6848-6855` · `corpus/criteria.toml:8169`
- **획득**: 조회 — 두 게이트의 판정표와 `criteria.toml` 의 `f09_remeasurement` 절
- **모집단**: 회차기록
- **유효성**: 참
- **해악도**: **금지역** — **사실이_아닌_것을_사실로**(기준선 없는 수를 *"#58 의 대조 불가를 닫았다"* 로 적게 된다)
- **대상**: 계획대상
- **얼마나 아픈가**: 되돌릴 것 없음. 지금 계획에 이 선행 사실을 적으면 ㈂ 의 범위가 정직해진다. 걸리는 곳: ㈂ 의 거짓 양성률 절반

---

### ⑪ *"새 층을 세우는 것이 아니라 이미 선 층을 쓰는 일"* 이 ㈂ 에는 거짓이다 — 이 저장소에 벤치 인프라가 **0** 이다

- **어떻게 실패하나**: `intent.md:48-49` 가 착수 근거를 *"소비 가능해지는 지점에 가장 가까운 것 … **새 층을 세우는 것이 아니라 이미 선 층을 쓰는 일**"* 로 적었고 `intent.md:46` 은 비용을 *"조회 1 회"* 로 적었다. 그 문장은 ㈀ 에 대해서는 참이지만 ㈂ 에 대해서는 거짓이다 — 워크스페이스 전체에 `criterion` 도 `[[bench]]` 도 `benches/` 도 없고 CI 워크플로 둘(`ci.yml`·`release.yml`)에 `bench` 문자열이 없다. 등록이 지정한 `crates/pal-query/tests/bench.rs` **파일 자체가 없다**. ㈂ 는 **표본기 + 벤치라는 새 장치를 처음부터 짓는 일**이다. 그리고 계획 자신이 그것을 인정한다 — `intent.md:127-128` 이 *"이 회차가 만드는 장치(#58 의 **표본기·벤치**)"* 라고 적었다. **착수 근거의 문면과 계획의 내용이 서로 부정한다.** 소유자가 (가)를 고른 근거가 *"순서표의 다음 완성"* 과 *"비용이 조회 1 회"* 였으므로, 이 어긋남은 **승격 대상**이다.
- **어디가 걸리나**: `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:46` · `:48-49` · `:91` · `:127-128` · `corpus/criteria.toml:7361-7366`(`crates/pal-query/tests/bench.rs` 지정) · `.github/workflows/`(잡 목록)
- **획득**: 조회 — `Cargo.toml` 전량에 `criterion`·`[[bench]]` 0 건 · `.github/workflows/*.yml` 에 `bench` 0 건 · `crates/pal-query/tests/bench.rs` 부재
- **모집단**: 원의도
- **유효성**: 참
- **해악도**: **거짓신호** — 아무것도 안 깨지지만 소유자가 *"조회 1 회짜리 일"* 로 믿고 고른 갈래가 실제로는 장치 둘을 새로 짓는 일이다
- **대상**: 계획자신
- **얼마나 아픈가**: 지금 승격하면 비용 0. 걸리는 곳: 착수 근거 절 1 · ㈂ 전체

---

## 내가 기각한 것

| 제목 | 어떻게 실패한다고 적었나 | 어디 | 획득 | 모집단 | 유효성 | 해악도 | 대상 | 왜 기각했나 |
|---|---|---|---|---|---|---|---|---|
| 넓힌 반경이 `PROVISIONAL_WATCH_PRODUCT_MAX` 에 걸려 저장이 거부된다 | 37 건 × 폐포 크기가 10⁶ 을 넘어 `pal bind` 가 멈춘다 | `crates/pal-core/src/budget.rs:330` · `crates/pal-core/src/radius.rs:175-181` | 실측 — `graph.dump` 역엣지로 37 대상의 k-홉 폐포를 전수 계산 | 저장소 | **거짓** | 실패 | 계획대상 | **안 걸린다.** `callers` 최대 감시 78 · `closure:2` 175 · k≥5 에서 **213 에 포화**(전체 심볼 3335). 최악의 곱이 **37 × 213 = 7,881** 로 상한의 **0.8%** 다. 계획의 *"예산이 실제로 걸리는지는 관측하고 적는다"* 는 「안 걸린다」로 닫힌다 |
| 감시자가 많아진 심볼에서 `pal touch` 가 `binding_max_exceeded` 로 결박을 잘라 화면이 조용히 생략한다 | 상한 10 을 넘겨 `■ 이 좌표를 지켜보는 것` 이 잘린다 | `crates/pal-core/src/budget.rs:356`(`PROVISIONAL_TOUCH_BINDING_MAX = 10`) · `crates/pal-query/src/lib.rs:928-930` | 실측 — 37 건 전부를 넓혔을 때 심볼당 감시자 수를 전수로 셈 | 저장소 | **거짓** | 거짓신호 | 계획대상 | **안 넘는다.** `callers` 에서 심볼당 최대 감시자 **3**(`check`), `closure:2` 에서도 최대 **8**. 상한 10 을 안 건드린다 |
| 넓혔다가 다시 좁히면 `WATCH` 색인에 옛 감시 원소가 남아 색인이 실체와 갈린다 | 좁힌 뒤에도 옛 감시자가 `bindings_watching` 에 잡힌다 | `crates/pal-intent/src/store.rs:191-230` | 조회 — `record` 본문 정독 | 저장소 | **거짓** | 금지역 | 계획대상 | `record` 가 **옛 감시를 먼저 읽고 새 집합에 없는 것을 `WATCH` 에서 지운다**(`:196-215`). 그 자리에 왜 지우는지가 주석으로 이미 적혀 있고 R-21 과의 관계까지 갈라 놓았다 |
| 37 건을 셸로 재결박할 때 note 안의 백틱이 명령 치환으로 터져 결박문이 손상된다 | 34/37 의 note 에 백틱이 있어 `--note "…"` 인라인 전개 시 실행된다 | `crates/pal-cli/src/bind.rs:46` · `.palimpsest/intent/bindings.jsonl` | 실측 — note 문자 통계(백틱 34 · `$` **0** · 역슬래시 **0** · 여러 줄 34) + `--note "$(cat note.txt)"` 로 재결박해 **id 가 `4065e80475cf1c72` 로 동일**함을 확인 | 저장소 | **거짓** | 금지역 | 계획대상 | 실제로 돌려 보니 파일 경유(`"$(cat …)"`)는 **두 번 전개되지 않아** 백틱이 안전하고 id 가 보존된다. `$` 와 역슬래시가 0 건이라 위험 표면이 더 좁다. 인라인 리터럴로 쓸 때만의 위험이고, 그건 계획이 아니라 실행 습관의 문제다 |
| 반경을 넓히면 2층에 `BOUND_TO` 엣지·`Binding` 노드가 늘어 `pal doctor` 불변식이 움직인다 | 감시 원소마다 엣지가 생겨 ①②③ 의 모집단이 흔들린다 | `crates/pal-cli/src/doctor.rs:257-287` | 조회 — doctor 의 그래프 조립 정독 + `pal doctor --json` | 저장소 | **거짓** | 실패 | 계획대상 | `BOUND_TO` 는 결박당 **하나**이고 끝점이 `b.target` 뿐이다. 감시 원소는 엣지가 안 된다. 결박 건수가 안 변하므로 ①②③ 의 모집단도 안 변하고, 계획이 범위 밖으로 뺀 ⑦ 은 `BindingIndex` 라벨이 없어 `not_built` 로 그대로 남는다 |
| 결박 노트 중에 `docs/plan/02-order.md` 를 인용한 것이 있어 ㈄ 의 정정이 그 결박을 낡게 한다 | 순서표 문면이 결박문에 복사돼 있다 | `.palimpsest/intent/bindings.jsonl` | 실측 — 37 note 전문에서 `02-order`·`순서표`·`C6`·`반경` 검색 | 저장소 | **거짓** | 거짓신호 | 계획대상 | **0 건.** 넷 중 어느 낱말도 note 에 없다. ㈄ 는 결박을 안 건드린다 |
| 착수 근거 *"2 번이 서면서 넓은 반경이 빈 집합이 아니게 됐다"* 가 실측과 다르다 | 파일 간 해소 없이도 대부분의 결박이 호출자를 얻으므로 인과가 과장됐다 | `intent.md:44-46` · `docs/plan/02-order.md:110` | 실측 — 새 감시자 225 자리를 같은 파일(100)/파일 간(125)으로 갈랐고, 결박 단위로도 셈 | 원의도 | **거짓** | 미관 | 계획자신 | 적었다가 확인하니 **과장이라 부를 수 없다.** 파일 간 감시자만 얻는 결박이 7 건이고, 같은 파일 호출자도 Rust 스코프 해소(2026-09-08)라는 **바로 앞 단계**의 산출이다. 「2 단계가 서야 뜻을 갖는다」는 진술은 대략 참이고, 날짜가 09-08 이냐 09-10 이냐의 차이는 이 회차의 판정을 안 움직인다 |
| `intent.md` 의 `## 승격` 표에 있는 자리 채우기 행(세 칸이 전부 `—` 인 행)이 계기판에 승격 1 건으로 헤아려진다 | 빈 행이 집계 모집단에 든다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/intent.md:153-155` | 조회 — `dashboard.py:181-187` · `record.py` · `extract.py` 정독 | 자기장치 | **거짓** | 거짓신호 | 계획자신 | ⑥ 승격은 **커밋 제목의 `[승격]` 표기**로 센다(`dashboard.py:185`). `intent.md` 의 승격 표를 읽는 파서가 없다. 자리 채우기 행은 집계에 안 든다 |

---

새 범주: **사전 등록된 합격선의 모집단이 이 저장소가 아닌데 계획이 이 저장소에서 재려 한다** — 「요구 누락」도 「범위가 좁아짐」도 아니고, *"합격선은 이미 등록돼 있다"* 가 참이면서 동시에 *"그 합격선은 여기에 대해 쓰인 것이 아니다"* 가 참인 자리다(시나리오 ③④⑥⑨⑩ 이 전부 이 범주에 든다)
