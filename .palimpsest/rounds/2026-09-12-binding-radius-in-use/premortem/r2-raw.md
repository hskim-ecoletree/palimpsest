# 사전부검 R2 — 원 반환문

> 낸 자: `pal-premortem-sweeper` · 2026-09-13 · 회차 `2026-09-12-binding-radius-in-use`
> **받은 것**: `intent.md`(라운드 1 뒤에 고쳐진 원 의도와 계획) · 저장소
> **안 받은 것**: 대화 기록 · 이 계획을 쓴 자의 설명 · 라운드 1 의 결과
>
> ⚠ **이 파일을 친 손은 메인이 띄운 전사 에이전트다 — 글은 사전부검의 것이고 한 글자도 안 고쳤다.**
> 그 자리가 파일을 안 써서 전문을 반환문에 실었다.

---

사전부검 라운드 2 — 회차 `2026-09-12-binding-radius-in-use`

측정에 쓴 것: `pal` 릴리스 바이너리(조회는 전부 scratchpad 의 임시 index/cache/intent 로 돌렸고 워킹트리·정본 결박은 안 건드렸다), `cargo xtask check` 실행 1 회, 코드 읽기.

---

### ㈀ 의 「보존 경로」를 세워도 `작업 규율` 2 가 그 결과를 지운다 — 그리고 그 규율의 검사기가 「정상」을 찍는다

- 어떻게 실패하나: 반경을 넓혀 `intent.redb` 에 앉힌 뒤, `작업 규율` 2 를 **적힌 순서대로** 실행한다 — `pal intent import .palimpsest/intent/bindings.jsonl` → `pal intent export --out .palimpsest/intent/bindings.jsonl` → `git diff`. 추적본은 아직 옛 상태(`symbol`)이고 `IntentStore::record` 는 **id 가 같으면 덮어쓴다**(`insert`). 그래서 import 가 37 건을 통째로 옛 값으로 되돌리고, 이어진 export 가 그 되돌아간 값을 파일에 다시 쓰고, **`git diff` 가 빈다.** 화면에는 *"바꿔치기가 아니라 더하기다 — 파일에 없던 결박은 그대로 남아 있습니다"* 만 뜬다. 규율이 세운 유일한 검사기(`git diff`)가 **일이 지워졌을 때 가장 깨끗한 초록**을 낸다.
- 어디가 걸리나: `intent.md:168` (작업 규율 2) · `crates/pal-intent/src/store.rs:191-230` (`record` = `t.insert`) · `crates/pal-intent/src/store.rs:717-724` (`import_jsonl` 이 `already_present` 를 세고도 `record` 를 부른다) · `crates/pal-cli/src/main.rs:631`
- 획득: **조회 + 격리 실측.** scratchpad 의 임시 의도 저장소에 정본 37 건을 import → 한 건의 `radius` 를 `callers`(감시 2)로 바꾼 파일을 import(= 「넓힘」) → export 로 확인(`callers`·감시 2) → **정본(옛 파일)을 import** → export → `callers` 가 `symbol`·감시 1 로 되돌아갔고 정렬 후 원본과 바이트로 같았다. 정본 `bindings.jsonl` 과 워킹트리는 안 건드렸다.
- 모집단: 자기장치
- 유효성: 참
- 해악도: **금지역** — 데이터_손실(넓힘과 새 감시 원소가 사라진다) · 측정이_죽은_가지(대조가 언제나 초록)
- 대상: **계획자신** — 이 규율은 R1 ⑧ 에 대한 대응으로 이번에 새로 생긴 절이다
- 얼마나 아픈가: 되돌릴 수 있다(다시 넓히면 된다). 다만 **그 사실을 알아차릴 자리가 없다** — 37 건 전부가 한 번에 걸리고, 순서만 뒤집으면(export 먼저) 사라진다

### 넓힐 때 새 감시 원소의 기준 시점을 「그때」로 잡을 방법이 없다 — 37/37 의 `bound_at` 이 워킹트리 트리다

- 어떻게 실패하나: `.palimpsest/intent/bindings.jsonl` 의 37 건이 **전부** `bound_at = worktree{base, tree_digest}` 이고 `bound_at_time = "worktree"` 다. 낡음은 `WatchEntry.digest`(결박 시점의 값) 대 지금 값의 비교이고(`evaluate`), 넓히면 **새로 들어오는 원소에는 그 시점의 digest 가 없다.** 그 트리는 git 에 없으므로 재투영이 불가능하다. 남는 선택지는 둘뿐이다 — ① 지금 값을 넣는다(그러면 새 원소는 **정의상 fresh**), ② 모른다고 적는다(그러면 `evaluate` 의 ②가 ③④ 앞에서 반환해 **결박 전체가 `undeterminable` 이 되어 낡음이 가려진다**). ①을 고르면 한 결박 안에 **기준 시점이 둘**(대상은 옛 워킹트리, 호출자들은 오늘)인데 산출에는 `bound_at` 이 **하나**만 실리고 화면은 *"반경 callers · 결박 좌표 `<옛 스냅샷>`"* 로 한 시점만 말한다.
- 어디가 걸리나: `.palimpsest/intent/bindings.jsonl`(37/37 이 `worktree`) · `crates/pal-core/src/binding.rs:619-651` (`evaluate`) · `crates/pal-core/src/binding.rs:48-52` (`WatchEntry` 에 시점 필드가 **없다**) · `crates/pal-cli/src/bind.rs:98-109`
- 획득: **조회** — `bindings.jsonl` 전수에서 `bound_at`·`bound_at_time` 을 세었고(7 개 서로 다른 워킹트리 트리, 전부 `worktree`), `evaluate` 와 `WatchEntry` 를 읽었다
- 모집단: 저장소
- 유효성: 참
- 해악도: **금지역** — 사실이_아닌_것을_사실로(결박 하나가 두 기준 시점을 갖는데 산출은 하나만 적는다)
- 대상: **계획자신** — R1 뒤에 ㈀ 의 첫 일로 새로 들어온 「반경만 바꾸고 낡음 판정을 보존하는 경로」가 정확히 이 자리에서 정의되지 않는다
- 얼마나 아픈가: 37 건 전부. `pal bind --at <base>` 로 기준을 다시 잡는 회피로는 `bound_at` 이 `worktree` → `committed` 로 **갈래가 바뀌어** 기준 시점이 통째로 재작성된다(그것도 되돌리기가 정본 파일 revert 뿐이다)

### 최소 표면(정본 JSONL 을 손으로 고쳐 import)을 고르면 「앵커는 신고받지 않는다」가 통째로 우회된다

- 어떻게 실패하나: ㈀ 의 첫 일을 **코드 없이** 하는 길이 실제로 있다 — 정본 JSONL 의 `radius` 와 `watch` 를 편집해 `pal intent import` 하면 된다(위 실측이 그 길로 넓혔다). 그런데 `import_jsonl` 은 **감시 원소의 좌표 실재성도, digest 의 출처도 검사하지 않는다.** 실측으로 `symbol` 이 `000…0` 인 감시 원소와 **아무 데서나 복사한 digest** 를 넣었더니 그대로 실렸고, `binding.status` 가 그 결박을 `fresh` 에서 `undeterminable{watch_member_gone}` 으로 바꿨다(37 → fresh 29 · stale 7 · undeterminable 1). 검사 12(앵커는 신고받지 않는다)는 **Rust 소스의 `WatchEntry {` 리터럴 자리 수**만 세므로 이 문을 원리상 못 본다.
- 어디가 걸리나: `crates/pal-intent/src/store.rs:664-760` (`import_jsonl` — 검증 없음) · `xtask/src/main.rs:2026-2085` (검사 12 의 모집단이 소스뿐) · `crates/pal-core/src/binding.rs:637-643`
- 획득: **조회 + 격리 실측** — 위조한 감시 원소가 import·export 왕복을 통과했고 상태 판정까지 바꿨다
- 모집단: 저장소
- 유효성: 참
- 해악도: **금지역** — 측정이_죽은_가지(회귀 방지 장치가 이 경로에 대해 아무것도 안 센다) · 사실이_아닌_것을_사실로(사람이 적은 값이 기계가 잰 앵커로 저장된다)
- 대상: 계획자신
- 얼마나 아픈가: 손으로 넓히면 37 건 전부가 이 문을 지난다. 되돌리기는 정본 revert + redb 재구축으로 가능하다

### 반대로 코드 경로를 세우면 검사 12 가 빨개지고, 등록해서 풀면 그 검사의 전제가 바뀐다

- 어떻게 실패하나: 「반경만 바꾸는」 명령을 새 파일(`pal-cli/src/rebind.rs` 등)에 세우면 거기서 `WatchEntry { .. }` 를 만들게 되고, 검사 12 는 **등록되지 않은 자리**를 보는 즉시 실패한다(`cargo xtask check` · CI 잡). 풀려면 `WATCH_ENTRY_SITES` 에 자리를 더해야 하는데, 등록된 셋의 사유는 전부 *"투영에서 읽는다"* 다. 새 자리의 사유는 *"옛 결박에서 물려받는다"* 로 **넷째 종류**이고, 그것을 목록에 넣는 순간 이 검사가 재는 명제가 「앵커는 전부 투영에서 온다」에서 「자리 수가 등록값과 같다」로 조용히 내려앉는다.
- 어디가 걸리나: `xtask/src/main.rs:2041-2051` (`WATCH_ENTRY_SITES` 셋) · `xtask/src/main.rs:2053-2085`
- 획득: **조회** — 검사 코드를 읽었고, 현재 값이 *"`WatchEntry` 생성 자리 3개 · 등록된 자리 3개"* 임을 `cargo xtask check` 출력으로 확인했다
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패(CI 빨강) → 푸는 방식에 따라 **거짓신호**
- 대상: 계획자신
- 얼마나 아픈가: 한 줄로 풀리지만, 푼 뒤의 검사가 무엇을 재는지는 아무 데도 안 적힌다

### ㈄ 가 고치려는 표에 *"위 표는 안 고친다"* 가 **바로 아래 줄**에 적혀 있다

- 어떻게 실패하나: ㈄ 는 *"순서표 §1·§2 의 `C6` 행을 실측대로 정정한다"* 로 적혔다. §1 은 `docs/plan/02-order.md:43` 의 **「실측표 · 측정 커밋 `19b5877`」** 이고, 행마다 마지막 열이 `19b5877` 이다. 표 바로 다음 절(55-58 줄)이 이렇게 적었다 — *"⚠ **위 표는 안 고친다.** 행마다 측정 커밋을 달아 둔 기록이고, 사후에 덮으면 무엇이 언제 움직였는지가 사라진다. 바뀐 값을 여기 얹는다."* 그리고 `C2` 행이 움직였을 때 실제로 그 규칙대로 **아래에 얹었다**(2026-09-08 · #130). ㈄ 를 문면대로 하면 `C6` 행의 「낡음 전파 미구축」이 덮이고, 그 행은 **`19b5877` 에서 그렇게 쟀다고 계속 주장하면서** 다른 회차의 판단을 싣게 된다.
- 어디가 걸리나: `docs/plan/02-order.md:43` · `:53`(C6 행) · `:55-58`(금지 문장) · `:59-68`(C2 가 따른 형식) · `intent.md:95`(㈄)
- 획득: **조회** — 두 자리를 직접 읽었다
- 모집단: 저장소
- 유효성: 참
- 해악도: **금지역** — 사실이_아닌_것을_사실로(측정 커밋이 달린 기록을 사후에 덮는다)
- 대상: **계획대상**
- 얼마나 아픈가: 한 행이지만 되돌리기가 git revert 뿐이고, 덮은 뒤에는 *"이 문장이 언제 참이었나"* 를 파일 안에서 되짚을 수 없다. §2 의 3 번 행(`:110`)은 이 금지의 밖이라 **㈄ 의 절반만 걸린다**

### 이 회차의 관측 산출물 하나가 sunset 트리거를 켰다 — 지금 `cargo xtask check` 가 빨갛고, 처방이 「문서 둘을 지워라」다

- 어떻게 실패하나: `docs/sunset.toml` 의 트리거는 `.palimpsest/rounds/*/*.json` 이고, 검사는 **깊이를 안 묶는다**(회차 디렉터리 아래 어디든 `.json` 하나면 참). 이 회차가 이미 `observations/m1_full.json` 을 썼다. 그래서 지금 `cargo xtask check` 가 *"`docs/research` 를 지울 때가 됐다 · `docs/evidence-map.md` 를 지울 때가 됐다"* 로 실패한다. 트리거의 뜻은 *"`pal` 이 회차 레코드를 `.json` 으로 쓰기 시작했다"* 였는데(`sunset.toml:17-25`), 발화시킨 것은 **사람이 손으로 둔 관측 덤프**다. 저장소 전 이력에서 회차 아래 추적된 `.json` 은 **0 건**이고(`.jsonl`·`.md`·`.tsv`·`.out` 만 썼다) 그래서 이 검사가 여태 초록이었다. ㈂-b 가 *"등록된 형태로 저장소 안에"* 산출을 더 세우면 같은 형태가 반복된다.
- 어디가 걸리나: `docs/sunset.toml:26`(트리거) · `xtask/src/main.rs:2848-2877`(깊이 안 묶음) · `xtask/src/main.rs:2879-2911` · `.palimpsest/rounds/2026-09-12-binding-radius-in-use/observations/m1_full.json`
- 획득: **조회 + 실행** — `cargo xtask check` 를 돌려 실패 문면을 그대로 받았고, `git ls-files` 로 추적된 `.json` 이 회차 전 이력에서 0 건임을 확인했다
- 모집단: 자기장치
- 유효성: 참
- 해악도: 실패(로컬 `cargo xtask check` 는 **지금** 빨갛다. 그 파일을 커밋하면 CI 잡도 같이 빨개진다) + **거짓신호**(처방이 `docs/research` 와 `docs/evidence-map.md` 삭제인데, `sunset.toml:42-43` 이 *"ditto 하네스 실패 관찰 … 1 차 사료라 지금 지우지 않는다"* 라고 적었다 — 따르면 1 차 사료 삭제다)
- 대상: **계획자신**
- 얼마나 아픈가: 파일 이름 하나로 풀린다(`.json` → `.jsonl`/`.out`). 안 풀고 따라가면 되돌리기가 매우 비싸다

### `observations/*.tsv` 가 회차 레코드 검사의 헤더 규칙을 이미 어겼고, ㈂-b 가 그 산출을 넷 × 두 규모로 늘린다

- 어떻게 실패하나: 회차 디렉터리 아래 `.tsv` 는 **첫 줄이 탭으로 가른 열 이름**이어야 한다. 이미 있는 `observations/bench.tsv` 의 첫 줄은 한국어 요약 문장(`결박 37 · 감시 열쇠(중복 제거) 34 · 감시 쌍 37`)이라 `cargo xtask check` 의 「회차 레코드」가 두 줄로 실패한다. ㈂-b 는 **넷(`symbol`·`callers`·`closure(2)`·증분) × 두 규모 × 회차 3** 의 산출을 저장소 안에 남기는 일이고, 같은 손버릇이면 같은 빨강이 반복된다.
- 어디가 걸리나: `.palimpsest/rounds/2026-09-12-binding-radius-in-use/observations/bench.tsv:1-2` · `xtask/src/main.rs:3894-3899`
- 획득: **조회 + 실행** — 검사 출력과 파일 첫 두 줄
- 모집단: 자기장치
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 헤더 한 줄. 다만 **지금 빨간 상태**라 아래 항의 병을 함께 만든다

### 「`cargo xtask check` 28/28」이 착수 관측에 등록됐는데 지금 25/28 이다 — 새 빨강이 안 보이는 나무가 된다

- 어떻게 실패하나: `intent.md:62` 가 착수 시점 값으로 **28/28** 을 등록했다. 지금 돌리면 **3 개가 실패**한다 — `sunset 선언`(위) · `회차 레코드`(위) · `완수 조건 설계 평가`(하한 `2026-09-06` 이후 회차는 `conditions-audit/r<n>-raw.md` 가 있어야 하는데 이 회차에 없다). 셋 중 둘은 **이 회차가 만든 파일** 때문이고 하나는 계획이 정한 순서(완수 조건을 사전부검 R2 뒤에 쓴다) 때문이다. 이 상태에서 ㈀·㈂-b 가 코드를 건드리면 **새로 켜진 빨강이 이미 켜진 빨강에 섞여** 구별되지 않는다. 착수 관측의 28/28 은 다른 조건들의 음성 대조 기준이기도 하다.
- 어디가 걸리나: `intent.md:62` · `xtask/src/main.rs:4497-4541`(검사 25) · `docs/gates/README.md:129-138`(하한 선언) · 이 회차 디렉터리에 `conditions-audit/` 없음
- 획득: **실행** — `cargo xtask check` 전체 출력(`Error: 3개 검사가 실패했다`)
- 모집단: 자기장치
- 유효성: 참
- 해악도: 실패 + 거짓신호(등록된 착수값이 **지금 참이 아니다**)
- 대상: 계획자신
- 얼마나 아픈가: 셋 다 싸게 풀린다. 안 풀면 이 회차 내내 「초록으로 돌아왔는가」를 못 잰다

### 「대조 불가」와 `대조불가` — 계획이 쓰는 표기가 표준 표 파서가 아는 토큰이 아니다

- 어떻게 실패하나: 계획은 ㈂-a·㈂-b·차선책·승격에서 일관되게 **「대조 불가」**(사이 띄어쓰기)로 쓴다. 회차 판정의 표준 표가 아는 값은 **`["통과", "반증", "대조불가", "미측정"]`** 넷이고, 셀 정규화는 `*` 와 백틱과 **바깥 공백만** 지운다. 그대로 표에 옮기면 *"표준 표에 판정 아닌 행이 있다: `대조 불가`"* 가 나고, 동시에 *"`대조불가` 행이 없다 — 넷을 다 적어야 합이 성립한다"* 도 난다. 조건 ID 가 그 행에 실려 있으면 **그 조건들이 어느 판정에도 안 실린 채로** 검산이 어긋난다.
- 어디가 걸리나: `.claude/skills/round/bin/record.py:151`(판정값) · `:222-223`(`_꾸밈없이`) · `:284-312` · `crates/pal-intent/src/round_condition.rs:41-47` · `intent.md:94`·`103`·`190`·`191`
- 획득: **조회** — 두 자리의 토큰 목록과 정규화 함수를 읽었다
- 모집단: 규약
- 유효성: 참
- 해악도: 실패(형식오류로 회차 레코드가 안 선다) — 표기를 고치면 끝이라 그 이상은 아니다
- 대상: 계획자신
- 얼마나 아픈가: 표기 하나. 다만 계획 문서 전체가 틀린 표기를 쓰므로 옮겨 적는 자리마다 반복된다

### ㈂-a 가 산출할 문장은 **이미 F09 게이트에 적혀 있다** — 새로운 것은 좌표 한 줄뿐이다

- 어떻게 실패하나: ㈂-a 는 *"`callers` 거짓 양성률을 「대조 불가」로 다시 적고 통과로 세지 않는다"* 다. 그런데 `docs/gates/F09.md:37` 이 2026-08-15 에 이미 그렇게 적었다 — `| ⑤ 거짓 양성률 callers | ≤ 30% | 대조 불가 — 표본이 symbol 과 같았다 (§4-라) | 대조 불가 |`. 즉 계획 넷 중 하나가 **이미 서 있는 기록의 재기술**이고, 실제로 새로 더해지는 것은 *"까닭의 좌표"* 한 줄이다. 계획표가 이것을 ㈀·㈄ 와 같은 무게의 칸으로 세우면 회차의 산출량이 실제보다 커 보인다.
- 어디가 걸리나: `docs/gates/F09.md:37` · `intent.md:93`(㈂-a) · `intent.md:190`(승격 1)
- 획득: **조회** — 게이트 §0 판정표를 읽었다
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호 — 아무것도 안 깨지지만 *"넷을 했다"* 가 사람을 잘못 믿게 한다
- 대상: 계획자신
- 얼마나 아픈가: 안 아프다. 다만 **더 작은 표면으로 같은 답이 나오는 자리**다 — 좌표 한 줄을 기존 행 아래 각주로 얹으면 끝난다

### 차선책 첫 항의 *"표본기가 그 축을 구조적으로 못 본다"* 가 같은 루프의 여섯 줄 위와 어긋난다

- 어떻게 실패하나: 소유자의 답으로 등록된 문장은 — *"두 반경은 `undeterminable` 축에서 갈리는데 등록 표본기가 `scripts/f09-verify.py:715` 의 갈래 필터로 **그 축을 구조적으로 못 본다**"* 다. 715 줄은 `if f in ("stale", "orphaned") and …` 로 **표본 목록**에서 `undeterminable` 을 빼는 것이 맞다. 그러나 **같은 루프의 710-711 줄**이 `if f == "undeterminable": 관측된_사유.add(...)` 로 그 축을 읽고 있고, 그 값이 `[f09.pass]` ⑥ 의 하한을 세우는 입력이다. 「구조적으로 못 본다」를 그대로 게이트와 `#58` 에 적으면 **스크립트가 하는 일을 안 한다고 적는 것**이 된다. 참인 문장은 *"거짓 양성 표본 목록이 `stale`·`orphaned` 만 담아서 그 축의 차이가 표본에 안 실린다"* 다.
- 어디가 걸리나: `scripts/f09-verify.py:709-711` vs `:715` · `intent.md:103-106` · `intent.md:190`
- 획득: **조회** — 해당 줄을 줄번호로 확인했다
- 모집단: 원의도(소유자가 내용으로 답한 문장)
- 유효성: 참
- 해악도: **금지역** — 사실이_아닌_것을_사실로(게이트와 이슈에 적히는 원인 진술이다)
- 대상: 계획자신
- 얼마나 아픈가: 문장 하나. 다만 이 회차가 **까닭의 좌표를 댔다**는 것이 ㈂-a 의 유일한 새 산출이라 그 한 문장이 산출의 전부다

### ⑥ 의 하한과 ㈂-a 가 같은 실행의 같은 다리에 얹혀 있다 — 한쪽은 「통과」로 세고 한쪽은 「통과로 안 센다」가 된다

- 어떻게 실패하나: `f09-verify.py` 는 `실_이력` 을 **네 번**(두 코퍼스 × 두 반경) 한 프로세스에서 부르고, `관측된_사유` 는 **모듈 전역 집합**이라 네 다리의 산출이 합쳐진다(`:81`·`:711`·`:813`). ⑥ 의 하한(*"사유 최소 둘이 실제로 산출"*)은 그 합집합에서 판정된다. 계획 자신이 R1 ② 처리에서 *"`watch_member_gone` 이 callers **84 회** · symbol **0 회**"* 를 적었다 — 그것이 맞다면 ⑥ 의 둘째 사유는 **callers 다리에서만** 왔고, ㈂-a 는 그 다리를 *"통과로 세지 않는다"* 로 적는다. 같은 실행의 같은 다리가 한 줄에서는 근거이고 다른 줄에서는 근거가 아니게 되는데, 게이트는 그 사실을 어디에도 안 적는다.
- 어디가 걸리나: `scripts/f09-verify.py:81`·`:711`·`:800-818` · `docs/gates/F09.md:38`(⑥ 하한 = `projection_stale`·`watch_member_gone`) · `intent.md:157`
- 획득: **조회(전역 집합의 공유)** + **추정(사유의 출처가 callers 다리라는 부분)** — 사유별 출처는 코퍼스 재실행이 필요해서 못 쟀다. 회차가 적은 84/0 을 그대로 믿지 않았다
- 모집단: 자기장치
- 유효성: 추정
- 해악도: 거짓신호 — 게이트 두 줄이 서로를 조용히 부정한다
- 대상: 계획대상
- 얼마나 아픈가: 재실행 한 번이면 갈린다(사유를 다리별로 나눠 세면 된다). 안 재면 ⑥ 이 무엇 위에 서 있는지 아무도 모른다

### `작업 규율` 3 의 음성 대조는 **아무 일도 안 했을 때 가장 잘 통과한다**

- 어떻게 실패하나: 규율 3 은 *"넓히기 전에 `pal query binding.status` 를 찍어 두고, 넓힌 뒤 다시 찍어 대조한다. `stale` 의 **신원**(id 집합)이 보존되는지가 ① 의 음성 대조다"* 다. 그 대조는 **넓힘이 일어났다는 것을 전혀 요구하지 않는다** — 넓히기가 조용히 실패했거나(예: 위 규율 2 가 되돌렸거나, import 가 판 검사에서 멈췄거나) 애초에 한 건도 안 넓혔으면 id 집합은 **완벽하게 보존되고 대조가 초록**이다. 양성 축(`radius` 분포가 실제로 움직였나 · 감시 집합 크기의 합이 늘었나 · `pal touch` 의 지켜보는 것이 0 을 벗어났나)이 규율에 없다.
- 어디가 걸리나: `intent.md:170-171` · 대조 가능한 실물: `pal query binding.status --json` 이 `radius`·`watch` 를 함께 싣는다(`crates/pal-query/src/lib.rs:780`·`:881`)
- 획득: **조회 + 실측** — 임시 저장소에서 `binding.status --json` 을 찍어 37 건에 `binding` id · `radius` · `watch` 가 실린다는 것과 현재 분포(fresh 30 · stale 7)를 확인했다
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호 — 규율이 「했다」를 안 재고 「안 망가졌다」만 잰다
- 대상: **계획자신**
- 얼마나 아픈가: 한 줄 더하면 끝난다. 다만 이 규율이 ① 에 대한 유일한 대조로 등록돼 있다

### §8 의 효과 증인(*"넓힌 반경이 실제 재검토를 불렀다"*)은 넓히는 시점에 원리상 관측될 수 없다

- 어떻게 실패하나: 새로 들어오는 감시 원소의 digest 는 **넓히는 시점의 투영에서** 읽힌다(`bind.rs:98-109`, 손으로 넣어도 같다). `evaluate` 는 그 값과 지금 값을 비교하므로 **넓힌 직후 새 원소는 반드시 일치**한다. 즉 넓힘 자체로는 `stale` 이 단 한 건도 안 늘어난다. §8 의 증인이 서려면 **넓힌 뒤에 호출자 코드가 바뀌는 커밋**이 필요하고, 이 회차 안에서 그것을 만들면 증인은 **회차가 스스로 만든 변경**이 된다. 넓히기를 회차 끝에 두면 증인이 아예 못 선다.
- 어디가 걸리나: `crates/pal-cli/src/bind.rs:98-109` · `crates/pal-core/src/binding.rs:619-651` · `intent.md:32`(§8 효과의 증인)
- 획득: **조회** — 두 경로를 읽었다. 넓힌 직후의 상태를 실제 37 건으로 돌려 보지는 않았다(보존 경로가 아직 없다)
- 모집단: 원의도
- 유효성: 참(기전) · 추정(회차 안에서 증인이 못 선다는 결론은 작업 순서에 달렸다)
- 해악도: 거짓신호 — 자기가 만든 변경으로 「효과」를 적으면 그것이 효과 확인이 아니다
- 대상: 계획대상
- 얼마나 아픈가: 순서로 풀린다(먼저 넓히고, 회차의 다른 커밋이 호출자를 건드리게 두고, 그 사실을 증인에 명시). 안 풀면 §8 이 자기 참조가 된다

### ㈂-b 는 「선 장치 · 안 닫힌 판정 · 아무도 안 돌림」 셋을 동시에 만든다

- 어떻게 실패하나: `[f09.4.pass]` 의 ④ 는 `bench_by_radius = true` 로 **등록된 참**이고, 그 합격선은 *"증분 갱신이 전체 재계산보다 빠르다"* 다. ㈂-b 는 그 벤치를 세우되 ④ 를 「대조 불가」로 적는다. 그러면 ㉠ `corpus/criteria.toml` 의 등록값은 `true` 인 채 남고 ㉡ 게이트에는 대조 불가가 적히고 ㉢ 실행체는 `#[ignore]` 라 **CI 가 절대 안 돌린다**(`.github/workflows/ci.yml` 에 `--ignored` 가 없다 — 확인했다). 셋의 합은 *"저장소 안에 서 있는데 아무 값도 안 내는 측정"* 이고, 등록값과 게이트 판정을 서로 대 보는 검사는 28 개 목록에 **없다**. 게다가 ④ 의 합격선은 `watch_placement` 결정(“증분이 안 빠르면 `WATCH` 색인 결정이 근거를 잃는다”)의 유일한 검증자다 — 그것이 영영 「대조 불가」로 남는다.
- 어디가 걸리나: `corpus/criteria.toml:7382-7396`(④ 등록) · `corpus/criteria.toml:6583-6616`(`watch_placement`) · `docs/gates/F09.md:492`(#58 로 넘긴 자리) · `crates/pal-query/tests/bench.rs:1-5` · `.github/workflows/ci.yml`(잡 목록에 `--ignored` 없음)
- 획득: **조회** — criteria·게이트·CI 워크플로·기존 벤치를 읽었고, `cargo xtask check` 의 28 개 검사 이름에 criteria↔게이트 대조가 없음을 출력으로 확인했다
- 모집단: 저장소
- 유효성: 참
- 해악도: **금지역** — 측정이_죽은_가지(`intent.md:147` 이 이 회차에 특히 걸린다고 스스로 지목한 둘 중 하나다)
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 다만 *"장치는 섰다"* 를 근거로 `#58` 을 닫으면 되돌리기가 이슈 이력까지 번진다 — 계획은 `#58` 의 처분을 안 적었다

---

## 내가 기각한 것

| 무엇을 물렸나 | 왜 아니었나 | 좌표 | 획득 | 모집단 | 유효성 | 해악도 | 대상 | 얼마나 아픈가 |
|---|---|---|---|---|---|---|---|---|
| 37 건 중 상당수가 이름 중복이라 `pal bind` 재호출로 못 넓힌다 | 실측: 37/37 의 대상 이름이 3,335 노드 안에서 **유일**하고 2층에 전부 있다. 이름 해소로 막히는 것은 0 건 | `pal query graph.dump` + `binding.status` 대조 · `crates/pal-cli/src/bind.rs:71-85` | 조회(실측) | 저장소 | 거짓 | 해당 없음 | 안 아프다 — 오히려 이 경로는 열려 있다 |
| `callers` 로 넓히면 대상이 감시에서 빠져 지금 `stale` 7 이 `fresh` 로 바뀐다 | `expand` 가 **언제나 대상을 먼저 넣는다**(`out.insert(target)`), 그리고 그 사실이 주석으로 *"빈 감시 집합은 만들 수 없다"* 로 적혀 있다 | `crates/pal-core/src/radius.rs:118-134` | 조회 | 저장소 | 거짓 | 해당 없음 | 안 아프다 |
| `pal intent import` 가 「더하기」라 파일에 없는 결박을 지운다 / 넓힘이 안 덮인다 | 둘 다 아니다 — 파일에 없는 결박은 **남고**(실측), 같은 id 는 **덮인다**(실측). 「더하기」는 집합 수준이지 레코드 수준이 아니다. 후자는 별개 시나리오로 세웠다 | `crates/pal-intent/src/store.rs:717-724` | 조회(실측) | 저장소 | 거짓 | 해당 없음 | 안 아프다 |
| ㈂-b 의 벤치를 기존 `pal-query/tests/bench.rs` 에 얹으면 검사 1(의존 방향)이 막는다 | 규칙 4 는 **`pal-store` → `pal-intent`** 만 금한다. `pal-query` 는 자유롭고 `pal-cli` 는 이미 `pal-intent` 를 dev 로 쓴다. 다만 `pal-store/tests` 에 넣으면 막힌다 — 검사가 `cargo metadata` 의 `kind:"dev"` 를 **안 가르기** 때문이고, 그때 진단문은 *"캐시 폐기 경로가 의도에 닿는다"* 라는 틀린 사유를 댄다 | `xtask/src/main.rs:803-806` · `cargo metadata` 실측(`pal-cli -> pal-intent dev` 가 목록에 실린다) | 조회(실측) | 저장소 | 거짓 | 해당 없음 | 안 아프다. 자리 선택이 잘못될 때만 깨어난다 |
| `observations/m1_full.json` 이 「회차 레코드」 검사의 `종류` 머리 줄 규칙에도 걸린다 | 안 걸렸다 — `cargo xtask check` 의 회차 레코드 실패는 `bench.tsv` 두 줄뿐이다. `.json` 은 그 규칙의 모집단이 아니다 | `cargo xtask check` 출력 · `xtask/src/main.rs:3878-3899` | 실행 | 자기장치 | 거짓 | 해당 없음 | 안 아프다 |
| `.palimpsest/intent.redb` 가 클론에서 오염되면 정본까지 오염된다 | `.gitignore:34` 가 **뿌리 기준**으로 `intent.redb` 를 지우므로 클론의 파생 저장소는 추적되지 않는다. 오염이 정본에 닿는 길은 **그 오염된 저장소에서 export 하는 것** 하나뿐이고, 그것은 작업 규율 2 의 순서 문제와 다른 사건이다 | `.gitignore:34` · `git check-ignore -v` | 조회(실측) | 저장소 | 거짓 | 해당 없음 | 안 아프다 |

새 범주: **회차 산출물이 저장소 검사의 모집단에 들어간다** — 관측 파일의 **이름과 첫 줄**이 `cargo xtask check` 의 판정을 바꾼다(sunset 트리거 `*.json` · 회차 레코드 TSV 헤더). 계획이 산출을 「저장소 안에 등록된 형태로」 세울수록 이 면이 커진다.