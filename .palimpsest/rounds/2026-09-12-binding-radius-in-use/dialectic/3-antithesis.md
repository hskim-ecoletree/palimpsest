# 반(反) — 판 3 의 반론표

<!-- 이 파일은 `pal-decision-opponent` 의 원 산출물이다. 메인이 옮겼고 고치지 않았다.
     반(反)이 받은 것과 안 받은 것은 아래 「선언」 절이 스스로 적는다. 메인이 옮기면서 한
     것은 ① 반환문 머리의 상태 한 줄을 이 제목으로 바꾼 것 ② 이 주석을 단 것뿐이다.

     ⚠ 반(反)이 안 받은 것: 패킷의 §0·§1·§4·§5·§6 · `3-design.md`(술어 전문은 프롬프트로
     받았다) · `intent.md` · `widen/README.md` · `docs/gates/binding-radius-in-use.md` ·
     `dialectic/` 의 다른 파일 · `review/` · `conditions-audit/` · `premortem/` ·
     대화 기록 · 메인의 사고 과정. -->

> 판 **3** · 회차 `2026-09-12-binding-radius-in-use` · 2026-09-13
> 낸 자: `pal-decision-opponent` (서브에이전트, 읽기 전용)

## ⚠ 선언 — 내가 받은 것과 안 받은 것

받은 것: `3-thesis.md` 전문 · 술어 P 와 딸린 세 규정(프롬프트 문면) · 패킷의 **§2 와 §3 만** · `.palimpsest/intent/bindings.jsonl` 의 `note` · 저장소 코드 · `git`.

**안 받았고 열지 않았다**: 패킷 §0·§1·§4·§5·§6 (§3 의 소제목이 계층·판정·호출자·감시를 스스로 싣고 있어 그것은 보였다. §4 위조 대조 표는 열지 않았다) · `dialectic/3-design.md` · `intent.md` · `widen/README.md` · `docs/gates/binding-radius-in-use.md` · 이 회차의 `review/`·`conditions-audit/`·`premortem/`·다른 `dialectic/` 파일 · 대화 기록 · 메인의 사고 과정.

프롬프트에 사고 과정이나 대화 기록이 섞여 오지 않았다. 초안이 인용한 `3-design.md:NN` 좌표들은 **열지 않았고 대조하지 않았다** — 그 인용의 진위는 내 반론에 안 들어간다.

---

## 초안이 서는 자리 — 쳐 봤고 안 무너졌다

1. **⒜ 의 인용 37/37 이 실재한다.** 초안이 행마다 단 인용을 `bindings.jsonl` 의 해당 `note` 와 **전수로** 대봤다. 37 행 전부에서 인용 문장이 그 결박의 `note` 안에 실제로 있다. 지어낸 인용도, 다른 결박에서 옮긴 인용도 **근거 대기 37 항에는 없다**(§발견 1 에는 있다 — 반론 1).
2. **기계 값 재현이 맞다.** 내가 다시 쟀다: 37 행 · 14 열 · `근거` 길이 min 42 / median 74 / max 191(문자) · 중복 0 · 빈 셀 0 · `(판정,새반경)` = (아니다,symbol) 19 · (영향받는다,callers) 18 · 다른 짝 0 · 꼬리 7 행 · **서로 다른 꼬리 1 종**(바이트 동일) · `fd176a8` 이후 바뀐 셀은 `49714573e0fa654c` 의 `대상digest차이` 하나(diff 가 ± 2 줄뿐). 초안의 표와 전부 같다.
3. **⒟ 가 안 무너진다.** `아니다`·호출자 0 여섯(`39b92bd51bbc82c6` `6c0e8ea521d0b2ac` `819fb73f9caf7f22` `cbd70e3e8dfca3ba` `cc2cee65c3c176ab` `e3baa46390859ef3`)의 `근거` 문면에 *"부르는 데가 없다 / 쓰이지 않는다 / 참조가 없다"* 류가 **한 행도 없다.** 37 행 전체에서도 호출자 부재를 `아니다` 의 근거로 쓴 행이 없다. 꼬리가 「호출자 0」을 적는 일곱은 전부 `영향받는다` 이고 문면이 *"근거가 안 선 것이 아니다"* 로 부정형이다.
4. **위조 대조 셋의 검출이 정본으로 선다.** 초안의 손잡이 5 를 실제로 쟀다 — `5e6bbbac89053565` 의 `note`(465 자)에 `ExportSet`·`stitch_of`·`export_digest`·`EXPORTS` 가 **넷 다 없다.** ㉢ 의 ⒜ 거짓은 무너지지 않는다. `1cc70cc19ef92186` 의 `note` 에 「호출자」·「감시」가 없어 ㉠ 의 ⒜ 거짓도 선다. ㉡ 은 `grade_of` 가 `note` 에 **있으나** 초안이 그 사실을 스스로 적고 까닭을 댔다.
5. **꼬리의 출처 판정이 맞다.** 꼬리의 *"멤버 호출은 … 장치가 못 본다"* 는 `91cdc3add647a3f7` 의 `note` 에 있고 꼬리를 진 일곱의 `note` 에는 없다. 초안의 「꼬리 ⒜ 거짓」이 정본으로 확인된다.
6. **작업 트리 주장이 참이다.** 나도 `git status --porcelain` 이 비어 있음을 확인했다.

---

## 반론

| # | 반론 | 좌표 | 유효성 | 해악도 |
|---|---|---|---|---|
| 1 | §발견 1 이 *"같은 결박문이"* 라며 인용한 문장은 `4841643fa986aecb` 의 결박문에 없고 **`45d8808585ee0d50` 의 결박문**에 있다 — ⒜ 가 이름 붙인 「다른 결박」을 초안이 저질렀다 | `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/3-thesis.md:248` (같은 오귀속이 `:326` 에 한 번 더) | 참 | 금지역 |
| 2 | §발견 3 의 *"기계 열이 전부 같고 판정이 갈렸다"* 가 거짓이다 — 짝 셋 전부에서 `새반경`·`감시후` 가 갈리고 한 짝은 아홉 기계 열 중 다섯이 다르다 | `3-thesis.md:328` | 참 | 금지역 |
| 3 | ⒝ 의 대안 읽기를 「지역성 인정」과 「명시 조건절 요구」 둘로 좁혀 거짓 딜레마를 세웠다 — **중간 읽기**(문면이 호출 경계를 낱말로 언급해야 ⒝ 가 선다)는 `아니다` 16 행만 죽이고 `영향받는다` 18 · `아니다` 3 을 살려 판정을 `반증` 으로 뒤집는다 | `3-thesis.md:44` | 참 | 거짓신호 |
| 4 | 16 행의 ⒞ 가 표에는 `참` 인데 §근거 대기에 **⒞ 에 대한 문장이 한 줄도 없다** — 안 잰 다리를 잰 것으로 세었다 | `3-thesis.md:139` (같은 형태가 `:143 :149 :155 :159 :163 :165 :173 :175 :177 :181 :187 :189 :197 :199 :201`) | 참 | 거짓신호 |
| 5 | 규정 3 의 「한 덩이 판정」을 **초안 자신이** 저질렀다 — 16 행의 ⒝ 근거가 글자까지 같은 두 낱말 *"⒝ — 지역성"* 이고 그것이 S1 6/6 을 통째로 덮는다 | `3-thesis.md:139` 이하 16 곳 | 참 | 거짓신호 |
| 6 | 손잡이 4 가 같은 문장 구조의 범위를 「S1 6/6」으로 축소했다 — 실측은 `아니다` **16/19**(S1 6/6 + S3 10/13)이고 두 계층에 걸친다 | `3-thesis.md:316` | 참 | 거짓신호 |
| 7 | 손잡이 3 이 엄격 ⒜ 읽기에서 떨어지는 행을 **둘**이라 적었으나 실측은 **최소 19** 다 — 지역성 꼬리표 16 종이 어느 결박문에도 없는 판정자 자작 문구다 | `3-thesis.md:315` | 참 | 거짓신호 |
| 8 | 손잡이 2 의 「조건절 없는 23 행」이 실은 **25** 다 — `1cc70cc19ef92186`·`bd71eaa617e123bc` 의 *"부르는 쪽과 무관하다"* 에는 조건절이 없는데 「명시 조건절 14」쪽으로 세었다 | `3-thesis.md:314` | 참 | 미관 |
| 9 | 코드 좌표가 한 리비전에서 나오지 않았다 — `39b92bd51bbc82c6` 의 `:325` 는 기준시점커밋 기준이고(HEAD 에서는 409, 325 는 `Scope::open` 의 본문) 나머지 아홉 행은 HEAD 기준이다. 어느 쪽인지 초안이 안 적었다 | `3-thesis.md:139` 대 `:171` | 참 | 거짓신호 |
| 10 | `b52057859bf405eb` 의 코드 좌표 셋이 **HEAD 에서도 기준시점커밋에서도 안 맞는다** — 인용한 주석은 5409 이고 「기각」 반환은 5419, 「처분자리」 갈림은 5425 다. 초안이 적은 5410·5413·5424 는 전부 주석 줄에 앉는다 | `3-thesis.md:181` | 참 | 거짓신호 |
| 11 | `49714573e0fa654c` 의 「코드 대조」가 그 행의 기준시점커밋에 없는 코드다 — `5e673e7` 의 `이_회차_종결문서` 는 `.rev().find(…)` 로 **사전순 최대 한 회차**를 고르는 판이고, 결박문이 「틀렸다」고 적은 바로 그 판이다. 그 행이 `대상digest차이 = 참` 인 유일한 행이라 이 어긋남이 가장 크게 남는다 | `3-thesis.md:149` | 참 | 거짓신호 |
| 12 | `e3baa46390859ef3` 의 좌표 넷 중 `:650`·`:687` 은 `attempts.push` 가 아니라 `a_attempts.push`·`b_attempts.push` 이고, 그 둘이 앉은 구간(630~700)에는 `for` 가 없다 — *"순회 안에서 일어난다"* 가 그 둘에는 안 선다 | `3-thesis.md:197` | 참 | 미관 |
| 13 | ⒞ 의 「지배 방향」 문제를 `45d8808585ee0d50` 한 행에서만 다뤘다 — 같은 형태가 `5b9331b398e8ff23`·`ce25c6f23f0a8992`·`fd4efc1eaea040c4` 셋에 더 있는데 그 셋은 ⒞ 논의 없이 `참` 이다 | `3-thesis.md:247` | 참 | 거짓신호 |
| 14 | 「기계 열을 복사하지 않았다」의 방어가 §발견 3 하나인데 그것이 거짓(반론 2)이라 방어가 비었다 — `판정` 은 `새반경` 과 19/19·18/18 로 완전 정합이고 `감시후 > 감시전` 인 11 행이 전부 `영향받는다` 다 | `3-thesis.md:274` | 참 | 거짓신호 |
| 15 | `6c0e8ea521d0b2ac` 의 `근거` 는 *"…한계와 뒤집힐 조건을 **적은 것이다**"* 로 결박문의 내용을 가리키는 서술이다 — 술어가 *"결박문의 요약이기만 하면 ⒝ 거짓"* 으로 명시 배제한 형태인데 초안은 「약」 표시도 없이 ⒝ 참으로 적었다 | `3-thesis.md:159` | 참 | 거짓신호 |
| 16 | ㈎ 가 ⒜ 의 배제 목록을 **닫힌 열거**로 읽은 것은 술어 문면과 안 맞는다(둘째 문장은 충분조건이고 하중은 첫 문장이다). 다만 *"전부"* 한 낱말이 결론을 살린다 — 그래서 초안이 손잡이 3 으로 내놓은 실패 경로는 **실제로 살아 있지 않고, 초안이 자기 취약성을 과대 진술했다** | `3-thesis.md:42` | 참 | 미관 |
| 17 | 꼬리 일곱이 「호출자 0」을 변호하는데, 그 변호가 정작 필요한 `아니다`·호출자 0 여섯(S1)에는 꼬리가 **없다**. 초안은 「꼬리 = S2 7/7」만 적고 이 비대칭을 안 적었다 | `3-thesis.md:217` | 추정 (`B7` ⑵ 문면을 못 봤다 — 꼬리가 넓힌 행에만 요구된 것일 수 있다) | 미관 |

---

## 표 밖의 근거 — 긴 인용과 실측

**반론 1 의 정본 대조.** 초안이 `4841643fa986aecb` 의 「같은 결박문」이라며 인용한 것은 *"`pal-core` 의 `name()` 은 화면 문자열이 아니라 와이어 토큰이다. `export.rs` 가 Cypher 속성 값으로 쓰고, `pal-query` 가 `watch_grades` 의 키로 쓰고, 넷은 자기 `parse` 의 열쇠다"* 다. `bindings.jsonl` 에서 두 `note` 를 열어 문자열 포함을 쟀다.

- `4841643fa986aecb` (624 자): `화면 문자열이 아니라` **없음** · `와이어 토큰` **없음** · `export.rs` **없음** · `pal-query` **없음** · `watch_grades` **없음**
- `45d8808585ee0d50` (661 자): 위 다섯 중 넷이 **있음** — 그 결박문 §② 「오염」 절이 출처다

`4841643fa986aecb` 의 `note` 가 호출자 소비를 적은 것은 사실이나 그 문장은 *"`pal-core` 밖에 두어야 `.name()` 이 Cypher 값과 `parse` 열쇠로 나가는 자리가 오염되지 않는다"* 이고 초안이 적은 문장이 아니다. **초안의 §발견 1 과 §⒞ 절은 다른 결박의 결박문 위에 서 있다.**

**반론 2 의 실측.** 초안이 「기계 열이 전부 같다」고 적은 짝 셋을 `widen/decisions.tsv` 에서 대봤다.

- `Elision` — `1cc70cc19ef92186` (symbol→**symbol**, 감시후 **1**, 타파일새원소 **0**, 기준시점 `daf15de`, base에없던새원소 **0**) 대 `d716d09163a6d0ab` (symbol→**callers**, 감시후 **18**, 타파일새원소 **12**, 기준시점 `9f0bf70`, base에없던새원소 **3**) — 아홉 기계 열 중 **다섯이 다르다**
- `이_회차_종결문서` — `49714573e0fa654c` (symbol, 감시후 1, `5e673e7`, digest차이 **참**) 대 `746a7761356136d8` (callers, 감시후 **2**, `07efe73`, digest차이 거짓) — **넷이 다르다**
- `ConfirmingSignal` — `bd71eaa617e123bc` (symbol, 감시후 1, 타파일새원소 0) 대 `6968674d8aa1bc2f` (callers, 감시후 **6**, 타파일새원소 **1**) — **셋이 다르다**

그러므로 *"판정이 기계 열을 복사했다면 이 셋은 갈릴 수 없다"* 는 성립하지 않는다. 오히려 세 짝 모두에서 `감시후` 가 `판정` 과 같은 방향으로 갈려 있어 **복사 가설을 지지하는 쪽**이다. 초안이 이것을 *"⒜ 의 방증"* 으로 읽은 것이 반론 14 의 구멍이다.

**반론 3 의 모집단.** `근거` 문면에 「부르는 쪽 / 읽는 쪽 / 소비 / 쓰는 쪽 / 만드는 쪽 / 호출자」 중 하나라도 있는 행을 셌다 — `영향받는다` **18/18** · `아니다` **3/19**(`1cc70cc19ef92186` `4841643fa986aecb` `bd71eaa617e123bc`). 즉 정본 자신이 두 무리를 밝게 가르고 있고, 초안의 ㈏ 는 그 가름을 지운다. 중간 읽기에서 죽는 것은 정확히 **지역성 꼬리표 16 행**이고, 초안이 ㈏ 에서 *"함께 ⒝ 거짓이 된다"* 며 방패로 쓴 `영향받는다` 7 행은 **한 행도 안 죽는다**. 그 7 행은 전부 호출 경계를 낱말로 부른다(*"부르는 쪽이 「낡음」으로 산출하게"* · *"부르는 자리에서 무엇이 참인지"* · *"읽는 표면이 그 값을 함께 실어야"* · *"그 값을 정규화가 읽어"* · *"그 값을 고칠 수 있는 사람이"* · *"부르는 쪽의 조립 방식을 정하는"* · *"부르는 쪽이 자기 분기를 들이면"*).

**반론 6 의 전수.** `아니다` 19 행 중 16 행이 *"…라는 이 X (자신의·자체의·내부의·안의) Y 다"* 로 끝난다 — S1 은 `39b92`(이 함수 본문의) `6c0e8`(이 검사 자신의) `819fb`(이 함수 안의) `cbd70`(내부 구현) `cc2ce`(이 함수 내부의) `e3baa`(이 함수가 자기 기록을) **6/6**, S3 은 `4065e8` `49714` `6192ba` `7793c3` `91cdc3` `969991` `a47cd2` `b52057` `e64fd4` `f4ba08` **10/13**. 남은 셋이 반론 3 의 그 셋이다. 여기에 S2 의 바이트 동일 꼬리 **7/7** 을 더하면 **네 계층 중 둘이 한 구조로 100% 덮인다.**

**반론 9~11 의 좌표 실측.** `grep -n` 으로 잰 값이다.
- `crates/pal-core/src/scope.rs` — `fn resolve_shadowing` 은 HEAD 에서 **409**, `is_none_or` 는 **448**. `git show e9402bc…:crates/pal-core/src/scope.rs` 에서는 **325**·**353**. 초안의 `:325` 는 기준시점커밋 기준이다.
- HEAD 기준이 맞는 좌표들: `doctor.rs` `coverage_gaps` **345**(기준시점 `9f0bf70` 에서는 334) · `rust_scopes.rs` `아이템_자리` **106**(기준시점 83) · `패턴_순회` **408**(기준시점 315) · `xtask/src/main.rs` `SURFACE_SOURCES` **1353**(기준시점 1090) · `cross_file.rs` **570**(기준시점 544) · 「부르는 쪽에 그런 자가 없다」 주석 **4857**(기준시점 4666).
- `xtask/src/main.rs` — 「★★ 기각을 맨 먼저 본다.」 **5409**, `return Err("기각")` **5419**, `if !처분자리.is_empty()` **5425**. 기준시점 `d53d67b` 에서는 5273·5283·5289. 초안의 5410·5413·5424 는 어느 쪽도 아니다.
- `git show 5e673e7…:xtask/src/main.rs` 의 `이_회차_종결문서`(4656 행)는 `Err(_) => return Vec::new()` · `.rev().find(|회차| … "report.md" …)` · `let mut out = vec![뿌리.join(최근)…]` 이고 `하한` 이라는 낱말이 그 함수에 없다. 초안이 인용한 `.filter(|회차| 회차.as_str() >= 하한.as_str())` 는 **HEAD 의 4879** 다.

---

## 내가 스스로 물린 것

- **「`scope.rs:325` 는 `resolve_shadowing` 이 아니다」를 반론으로 냈다가 물렸다.** 기준시점커밋 `e9402bc` 에서 325 가 정확히 `fn resolve_shadowing` 이다. 남긴 것은 「리비전을 안 적고 혼용했다」(반론 9)뿐이고, 「좌표가 틀렸다」는 취소한다.
- **「위조 ㉠ 의 ⒞ 는 실은 참이다」**(*"감시가 안 커진다"* + `판정 = 아니다` 는 같은 방향) — 내려고 했으나 초안이 `3-thesis.md:209` 의 ⚠ 에서 **스스로 적었다.** 새롭지 않아 물렸다.
- **「꼬리가 `영향받는다` 행에서 반대 방향으로 읽힌다」** — 초안이 `:234` 에서 스스로 적고 까닭을 댔다. 새롭지 않다.
- **「초안이 위조 셋을 잘못 짚었다」를 못 쟀다.** 패킷 §4 를 안 읽었으므로 내가 확인한 것은 *"초안이 위조라 적은 셋의 문면이 §2 의 정본 `근거` 와 다르다"* 와 *"㉢ 의 문면이 `1cf63ec5e0e9937a` 의 정본 `근거` 와 바이트로 같다"* 까지다. **위조가 정말 그 셋인지는 좌표를 못 댄다 — 「없음」.**
- **「16 행의 ⒝ 가 거짓이다」를 확정으로 내지 못했다.** 술어의 ⒝ 는 *"의존의 방향이 문면에 서야 한다"* 이고 지역성 꼬리표가 그것을 세우는지는 문면 해석이다. 그래서 반론 3 은 「초안이 중간 읽기를 안 다뤘다」까지를 참으로 냈고, **「판정이 뒤집힌다」는 추정으로 남긴다.**
- **`3-design.md` 의 인용(`:95` `:99` `:107` `:123` `:127` `:240` `:248` `:261` `:266-273` `:292-296` 등)이 그 파일에 실제로 그렇게 적혀 있는지 안 쟀다** — 금지 목록이라 열지 않았다. 초안 하중의 상당 부분이 그 인용 위에 서 있고, **그 자리를 재는 것은 내가 아니다.**