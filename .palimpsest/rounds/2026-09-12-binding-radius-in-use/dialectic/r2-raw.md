# 정반합 취합 — 2026-09-12-binding-radius-in-use 라운드 2 · 넓혀서 새로 `stale` 이 된 결박 다섯 각각에 대해, 그 결박문과 그것을 켠 것을 읽었을 때 이 결정은 지금 재검토되어야 하는가 — 그리고 그 재검토를 부른 것이 넓힌 반경인가

> 판 **2** · 라운드 **1/2**(R2 는 안 돌았다) · 낸 자 `pal-debate-reporter`(서브에이전트, 읽기 전용 — **이 파일은 메인이 옮겼다**)

## 내가 받은 것 / 안 받은 것

**받았다:** 다섯 산출물 전문 — `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/2-design.md` · `2-thesis.md`(⚠ 파일 끝 `## ⚠⚠ 이 초안은 합(合)이 수정했다` 절은 메인이 2026-09-13 에 얹은 **합의 수정 목록**이고 원 초안의 일부가 아니다 — 구별해 읽었다) · `2-antithesis.md` · `2-synthesis.md` · `2-referee.md`. 회차 이름과 라운드 번호. 원장 스키마(`.claude/skills/round/bin/record.py --schema` 의 enum 넷).

**안 받았다:** 대화 기록 · 메인의 사고 과정 · 이 판정을 실행할 계획(증인을 어떻게 세울지 · 어느 이슈를 열지). **판정을 다시 하지 않았다** — 아래 판정 문장·반론 문장·좌표는 원 산출물에서 그대로 옮긴 것이다.

**내가 저장소에서 직접 잰 것은 둘뿐이다:** ⑴ `dialectic/` 아래 파일 목록(`2-main-closes.md` 가 있는지) ⑵ ADR 파일 이름의 전체 형태(원 산출물이 `docs/adr/0024-…` 로 줄여 적은 자리를 좌표로 세우기 위해). 그 밖의 사실은 전부 원문 전사다.

## 판정

합(合) `2-synthesis.md` 의 판정 문장 그대로다.

> **합(合) — 술어를 하나로 고정하니 일곱이 전부 `아니다` 이고 `H1` 은 반증이다**

> **술어 P** — 「그 결박문이 이고 있는 **결정**이 지금 **미결**인가. 즉 ⒜ 그 결정이 아직 다시 정해져야 하는 상태이고, ⒝ 그렇게 만든 사실이 **결박 시점 이후에** 생겼는가.」 둘이 **함께** 참이어야 `참인 재검토` 다.

| 조건 | 판정 | 합(合)의 문면 그대로 |
|---|---|---|
| `C3` | **통과** | *"`C3` 는 「새로 `stale` 이 된 결박마다 참·거짓을 가른다」를 요구하고, 다섯 전부에 대해 건별 좌표와 함께 갈랐다. 새 `stale` 이 5 건이므로 `대조불가` 의 길은 닫혀 있다."* · **`참인 재검토` 건수: 0 건.** · **id 집합: `{}` (공집합).** |
| `H1` | **반증** | *"㉠ 귀속(넓힌 반경이 불렀다): 0 건. 그러므로 처방된 ⑴⑵⑶ 좌표의 **형태도 없다** — `H1` 의 증인은 이 판에서 하나도 서지 않는다."* · *"⚠ **`미측정` 이 아니다.** 재는 도구가 저장소에 있었고(`target/release/pal`), 일곱 결박문이 전부 읽혔고, 좌표가 전부 대졌다. 잴 수 있는 것을 재고 얻은 답이 「없다」다."* |
| `A3-b` | **통과** | *"`A3-b`: 통과(결정론적이므로 기계·메인이 닫는다). 초안의 각 행 §5 와 §갈래 2 의 결론을 이 표로 갈아야 한다."* · *"켤 수 있는 원소 전부(120/120)에 대해 base 는 충실한 대리다."* |

다섯 전부 `아니다`: `{3d9979946eb321de, 5e6bbbac89053565, 8597d886cb7afc70, c201c01b6b0aff9a, d716d09163a6d0ab}`. 대조 둘도 `아니다`: `{822ebf9b8071c799, 45d8808585ee0d50}`.

정(正)의 판정 절에서 **지워진 문장 둘**과 그 자리에 들어간 문장(합(合) §초안을 어떻게 고치나 1):

> 지운다 — *"대조 둘 중 `45d8808585ee0d50` 에는 「참인 재검토」가 붙는다"* · *"장치의 신호는 … 뒤집혀 있다"*.
> 대신 적는다 — **「대조 둘도 `아니다` 다. 이 표본에서는 장치의 신호와 결박문의 현재 진위 사이에 잴 것이 나오지 않았다 — 역상관도 무상관도 이 표본이 못 진다.」**

소유자에게: **안 올린다**(합(合)) · **없다**(종료 판단).

**⚠ 판정값 셋(`C3` 통과 · `H1` 반증 · `A3-b` 통과)을 `### ` 항으로 세우지 않았다.** 그것은 판정이고 발견이 아니다 — 항으로 세우면 원장의 발견 수에 판정이 섞이고, `## 내가 기각한 것` 에 넣으면 「헛것」으로 세어진다. 둘 다 틀린 자리이므로 이 `## 판정` 절의 표로만 둔다. 세지 않기로 한 사실을 여기 적는다.

## 반론 처리 — 전부

### 행 7 의 「참」과 행 1·3 의 「아니다」가 같은 술어를 반대로 쓴다 — 행 7 을 거짓으로 만드는 사실은 결박 시점에 이미 참이었다

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/2-thesis.md:110` · `crates/pal-core/src/binding.rs:138`
- 반(反)이 댄 좌표 전부: `2-thesis.md:35`·`:62`·`:110`·`:146` / `git ls-tree daf15debf439 crates/pal-cli/src/` / `crates/pal-core/src/binding.rs:138`
- ⚠ **등급이 바뀌었다.** 반(反)은 이 반론에 `금지역` 을 매겼고 합(合)은 **반론 자체는 채택하되 등급만 `거짓신호` 로 내렸다.** 위 해악도는 **합(合)의 것**이다. 합(合)의 까닭: *"설계문이 금지역을 셋으로 **미리 지목했고** 반론 1 은 그 셋 어디에도 안 닿는다 — ㉮: 초안의 근사 오류는 반대 방향이다(근사를 과대 보고했다) · ㉯: 초안은 `H1` 을 반증으로 적고 잔여 셋에 「`H1` 의 증인이 아니다」를 명시로 붙였다 · ㉰: 반론 11 의 기각이 그것을 닫았다. … 고쳐서 닫히는 것은 `거짓신호` 다"*
- 채택·근거(합(合) 그대로): *"사실이 내 손으로 재현됐다 — `git ls-tree daf15debf439` 에 `crates/pal-cli/src/export.rs` 가 있고, 그 시점 `label.rs` 머리가 이미 「구조로 막힌다」를 적고 있었다. 행 7 을 거짓으로 만든 사실은 결박 시점에 이미 참이다. 술어 P 로 일곱을 다시 대어 **행 7 을 `아니다` 로 뒤집었다**"*
- 합(合)이 그 뒤집힘에 붙인 문장: *"초안이 「결박 시점 뒤에 그 문장이 반증됐다」로 읽은 것은 실제로는 **반증의 발견**이었다 — `558d35c` 는 사실을 바꾼 것이 아니라 사실을 적었다."*
- 반(反)이 스스로 물러 적은 것: *"⚠ 한쪽으로 물러 적을 것: 행 1·3 의 거짓은 **전제 실패·공허**(없는 어댑터를 현재형으로 부름)이고 행 7 은 **직접 모순**이다. 강도가 같지는 않다. 그러나 강도 차이는 술어를 바꾸는 근거가 못 된다."*

### *"장치의 신호가 … 뒤집혀 있다"*(역상관)를 n=7·참 1 건에서 단언한다. 다른 대조 `822ebf9b` 는 `아니다` 다

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/2-thesis.md:24` · `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/2-thesis.md:97`
- 채택·근거(합(合) 그대로): *"반론 1 이 서면 「참」이 0 건이 되어 방향을 읽을 재료가 없어진다. 1/2 대 0/5 는 관측이 아니라 서사다"*
- 반(反)의 문장: *"**역상관은 지우고 「재료 부족」으로 적어야 한다.**"*

### 갈래 2 의 「보증 범위 1/78·1/25·1/2·1/5·1/18」과 `A3-b` 금지역 경고가 실측으로 뒤집힌다 — 켤 수 있는 원소에 대한 base 충실도는 다섯 행 전부 100% 다

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-cli/src/radius.rs:44-48` · `crates/pal-cli/src/radius.rs:57-59` · `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/2-thesis.md:122`
- 반(反)이 댄 좌표 전부: `2-thesis.md:43`·`:54`·`:65`·`:76`·`:88`·`:122` / `crates/pal-cli/src/radius.rs:30-59` / 재현 명령(본문)
- 채택·근거(합(合) 그대로): *"**내가 독립 재현했다** — base 트리에서 감시 원소를 되짚어 75/24/2/4/15 가 그대로 나왔고 미재현은 정확히 `base에없던새원소` 다. 행 3 은 `2/2` 다(`SURFACE_SOURCES`=`2bf2533a…`·`check_catalog`=`49f25e63…` 저장값 그대로)"*
- 합(合)이 이것으로 닫은 것: *"`A3-b`: 통과"* · *"**금지역 ㉮ 는 안 닿았다.** ㉮ 는 「근사가 켠 것을 「결정 뒤의 변화」로 적는 것」이고 초안의 오류는 **반대 방향**(근사를 실제보다 크게 적었다)이다. 사람을 잘못 믿게 하지만 사실을 없는 것으로 만들지 않는다 → `거짓신호`."*
- 반(反)의 문장: *"초안 §내가 확인 못 한 것 의 이 항목들은 「못 잰 것」이 아니라 「안 잰 것」이고, 재는 도구가 저장소에 빌드돼 있었다(`target/release/pal`)."*

### 「주석이 켰다」가 세 자리에 적혀 있는데 **주석은 원리상 결박을 못 켠다**

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-extract/src/parse.rs:170-173` · `crates/pal-core/src/coord.rs:90`
- 반(反)이 댄 좌표 전부: `2-thesis.md:40`·`:63`·`:148` / `crates/pal-extract/src/parse.rs:170-173` / 최소 재현
- 채택·근거(합(合) 그대로): *"`crates/pal-extract/src/parse.rs:170-173` 이 주석을 버리고 `crates/pal-core/src/coord.rs:90` 이 `BodyDigest` 를 「주석·공백·포매팅을 제거한 정규형」으로 정의한다. 그리고 `8f182d6` 의 `crates/pal-core/src/narrative.rs` hunk 를 내가 전수로 읽었다 — 그 함수 자리의 변화는 주석 한 줄이다"*
- 합(合)이 고친 분류: *"켠 것의 성질은 「주석·문자열」이 아니라 **「문자열 리터럴과 실코드」**다."* · 정정 대상 세 자리: 행 1 넷째 항의 `8f182d6` 주석 · 행 3 의 주석 셋 · R-c 의 *"주석·문자열이 켰다"*
- 반(反)의 문장: *"판정은 이 정정으로 안 뒤집힌다(문자열이 켰으므로). **틀리는 것은 근거의 인과다.**"*

### R-c 의 진단(「감도 경계가 tree-sitter 노드 경계다」)이 기제로 틀렸고, 그 성질은 **이미 등록된 결정**이다 — 새 관측으로 적으면 거짓신호다

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-extract/src/parse.rs:459-462` · `crates/pal-extract/src/parse.rs:119-120` · `crates/pal-core/src/coord.rs:90`
- 반(反)이 댄 좌표 전부: `2-thesis.md:148`·`:156` / `crates/pal-extract/src/parse.rs:113-120`·`:459-462` / `crates/pal-core/src/coord.rs:90`
- 채택·근거(합(合) 그대로): *"`crates/pal-extract/src/parse.rs:459-462` 가 「정규화 경로를 안 건드린다 … 의도한 동작이고(F03) … 닿으면 골든 넷이 움직이고 그것이 반증이다」로 **반증 조건까지 등록해 두었다.** 등록된 결정을 새 관측으로 원장에 올리면 발견 수가 허수가 된다"*
- 처분(합(合) §초안을 어떻게 고치나 6): *"**R-c 를 강등하고 다시 쓴다.** ★ 를 떼고 「새 관측」을 지운다 — **이미 등록된 결정(F03 · `parse.rs:459-462` · `coord.rs:90`)의 재진술**이라고 적고, 이슈로 여는 유일한 형태는 「이 결정을 뒤집자」이며 **그 재검토는 넓힌 반경이 부른 것이 아니다**를 붙인다."*

### `callers` 반경은 대상 타입 자신의 `impl` 멤버를 안 담는다 — 행 7 의 결정 ①이 말하는 `Label::병기` 가 감시 11 밖이다

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-core/src/radius.rs:132-137` · `crates/pal-cli/src/label.rs:54-59`
- 반(反)이 댄 좌표 전부: `crates/pal-core/src/radius.rs:132-137` / `crates/pal-cli/src/label.rs:54-59` / `bindings.jsonl` 줄 8
- ⚠ **반(反)이 이 반론에 유효성을 둘로 매겼다 — `참(사실) · 거짓(인과)`.** 위 유효성 `참` 은 **사실 부분**의 것이다. 인과 부분(*"이 누락이 행 7 을 놓치게 했다"*)은 반(反) 자신이 물렸고 합(合)도 기각했다 — 아래 `## 내가 기각한 것` 에 따로 세웠다.
- 채택·근거(합(合) 그대로): *"**내가 확인했다** — 행 7 의 감시 11 을 이름으로 되짚으니 `병기`·`정체성_등급_병기` 가 없다. 결박문 ①이 말하는 바로 그 함수가 감시 밖이다. 인과(「이것이 행 7 을 놓치게 했다」)는 반(反) 자신이 물렸고 나도 기각한다 — `병기` 본문은 불변이다. **사실만 잔여로 채택한다**"*
- 처분: 합(合)이 잔여 **R-d** 로 새로 세웠다 — *"귀속 ㉡, ⑶ 이슈, **`H1` 의 증인이 아니다.** 되돌림 판단의 재료로는 R-c 보다 무겁다."*

### 갈래 3 의 「켠 커밋은 **여덟**」이 일곱이다 — 열거도 일곱, 자기 산술도 4+3=7, 실측도 일곱

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/2-thesis.md:124`
- 채택·근거(합(合) 그대로): *"열거가 일곱이고 자기 분해도 4+3=7 이다. 어휘 정규화 비율이 4/8 이 아니라 **4/7** 이 되므로 갈래 3 이 보고하는 수가 바뀐다"*
- 합(合)이 고친 수: 켠 커밋 **일곱**(`213d3ee`·`8d90151`·`8f182d6`·`de09dd4`·`be86499`·`a1a4542`·`11b6162`) · 어휘 정규화·개명 **넷(4/7 · 57%)**

### **결정 문서가 결박 뒤에 움직인 것을 안 쟀다** — `ADR-0027` 이 켠 커밋 `11b6162` 로 개정됐다(§② + 상태 줄)

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `docs/adr/0027-the-instrument-must-reach-its-own-floor.md:3` · `docs/adr/0027-the-instrument-must-reach-its-own-floor.md:140-176`
- 반(反)이 댄 좌표 전부: `docs/adr/0027-the-instrument-must-reach-its-own-floor.md:3-4`·`:140-176` / `git log 9f0bf70..HEAD -- docs/adr/0027-…` / `2-thesis.md:124`
- 채택·근거(합(合) 그대로): *"`git log 9f0bf704464b..HEAD -- docs/adr/0027-…` 이 `11b6162` 하나를 내고 `:3` 의 상태 줄과 `:140-176` 의 개정 절이 실재한다(다른 넷은 0 커밋). 행 2 의 판정은 살지만 **행마다 다른 잣대**를 쓴 것이 맞고, 고치면 그 좌표가 행 2 의 근거를 **강화한다**"*
- 반(反)의 문장: *"무너지는 것은 근거의 완결성이다 … 그 자리를 안 적은 것은 행마다 다른 잣대다. 그리고 「문서 쪽은 안 쟀다」는 사실 자체가 `:174` 의 §내가 확인 못 한 것 에도 없다."*

### 이 판이 잰 수는 `ADR-0014` 가 이미 정의·소유한 거짓 양성률(5/5 = 100%)이고, 그 ADR 이 요구하는 셋 중 「입력 품질의 축」을 안 적었다

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `docs/adr/0014-precision-cannot-be-measured-apart-from-the-input-it-judges.md:11-13` · `docs/adr/0014-precision-cannot-be-measured-apart-from-the-input-it-judges.md:40-46`
- 반(反)이 댄 좌표 전부: `docs/adr/0014-…:8-33`·`:40-46` / `bindings.jsonl` 의 `promoted_by`(일곱 중 여섯 `by: span`) / `2-thesis.md:120`
- 채택·근거(합(合) 그대로): *"`docs/adr/0014-…:11-13` 의 정의가 P 의 ⒝ 와 같고 값은 `5/5` 다. `:40-46` 이 셋을 요구한다. **`promoted_by` 를 내가 직접 읽었다 — 일곱 중 여섯이 `{"proposal":{…,"by":"span"}}` 이고 메모가 ADR 절 전체다. 손으로 걸린 하나가 `45d8808585ee0d50` 이고 문장 단위다.** ★ **그리고 그 하나가 유일하게 주장이 실제로 거짓인 결박이다** — 축이 가리키는 방향과 같다(n=1, 측정은 아니다)"*
- 처분(합(合) §초안을 어떻게 고치나 9): *"**`ADR-0014` 요구 셋을 이 회차 산출에 적는다.** ① 이 표본은 **실물**이다(합성 아님) — 다만 여섯은 기계 승격된 ADR 절 전체이고 하나만 손으로 걸린 문장이다 ② 축은 **「메모의 넓이」** ③ **재판정 소유자**를 지목한다. 셋이 없으면 `5/5` 는 판정기의 값이 아니다."*
- `C3` 절에 합(合)이 붙인 문장: *"**이 수는 새 수가 아니다 — `docs/adr/0014-…:11-13` 이 소유한 거짓 양성률이고 값은 `5/5 = 100%` 다.**"*

### 손잡이 3 을 접은 근거(*"37 건을 무작위로 읽어도 같은 증인이 선다"*)가 **선택도를 0 으로 가정한다.** `callers` 18 건 중 5 건이 켰다 — 잴 수 있는 양이다

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/2-thesis.md:158` · `.palimpsest/intent/bindings.jsonl:6`
- 채택·근거(합(合) 그대로): *"`bindings.jsonl` 에 37 · `callers` 18 · 켠 것 5 이므로 선택도는 잴 수 있는 양이고 0 이 아니다. **결론(`H1` 반증)은 유지하되 근거를 갈았다** — 주의 배분 읽기는 선택도로 접는 것이 아니라 `H1` 의 문면(「가리킨 **결정**」)으로 접힌다"*
- 합(合)이 `H1` 절에 적은 것: *"남은 유일한 길은 「주의 배분」 읽기(넓혔기 때문에 이 일곱을 읽었다)인데, **초안이 그 길을 접은 근거는 무너졌고**(반론 10: 선택도가 0 이 아니다 — 결박 37 · `callers` 18 · 켠 것 5) **그 길 자체는 `H1` 의 문면에서 막힌다** … 그래서 결론은 같고 **근거를 갈았다.**"*

### ★ 귀속의 내용이 바뀐다 — 다섯이 `아니다` 인 「무관함의 진짜 축」은 반경 무관이 아니라 「메모의 넓이」이고 원인은 반경이 아니라 기계 승격이다

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `docs/adr/0014-precision-cannot-be-measured-apart-from-the-input-it-judges.md:40-46` · `.palimpsest/intent/bindings.jsonl:6` · `.palimpsest/intent/bindings.jsonl:21`
- 이것이 반론 9 가 낳은 것이고, **합(合)이 「귀속의 내용을 바꾼 것」으로 적었다**(§초안을 어떻게 고치나 8): *"**R-a·R-b 를 유지하되 R-b 를 승격한다.** 반론 9 의 축을 R-b 에 넣는다 — 다섯이 `아니다` 인 **무관함의 진짜 축은 「메모의 넓이」**(`ADR-0014` 가 이미 이름 붙였다)이고 원인은 반경이 아니라 **기계 승격이 ADR 절 전체를 메모로 삼는 것**이다(일곱 중 여섯이 `by: span`). ⚠ **이것이 귀속을 바꾼다** — 귀속값 ㉡ 은 그대로지만 ㉡ 의 **내용**이 「반경 무관」에서 **「메모 넓이 탓 · 승격 파이프라인 소관」**으로 바뀌고, 그래서 처방도 바뀐다: 반경 되돌림도 반경 이슈도 아니라 **승격의 메모 입자**다."*
- 반(反)의 원문(§9): *"★ **이것이 귀속을 바꾼다.** 다섯이 `아니다` 인 까닭으로 초안이 적은 것은 ㉡(반경 무관)인데, 무관한 **진짜 축**은 「메모가 ADR 절 전체라 넓다」이고 그 원인은 반경이 아니라 **기계 승격**이다. … 축을 안 적으면 `ADR-0014` 의 말대로 「무엇이 달라지면 값이 달라지는지 모르는 것」이 된다."*
- 합(合)이 이것을 소유자 결정으로도 안 올렸고 「메인이 판단할 자리」로 보이게 적었다: *"반론 9 가 낳은 처방(**승격이 ADR 절 전체를 메모로 삼는 것**)은 이 판의 물음 밖이고 발견·이슈로 나가는 것이 맞다. 그것을 결정으로 바꾸려면 `ADR-0014` 요구 ③(재판정 소유자)과 함께 별 자리에서 열려야 한다."*

### 잔여 R-a — `ADR-0024` §결과 두 항과 §안 덮는 것 두 항이 없는 크레이트를 현재형으로 부른다

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `docs/adr/0024-an-adapter-that-can-diverge-is-a-second-core.md:47-48` · `docs/adr/0024-an-adapter-that-can-diverge-is-a-second-core.md:59-62` · `xtask/src/main.rs:1363` · `docs/gates/inventory-disposal.md:256`
- 정(正)이 세우고 합(合)이 유지한 잔여다(*"R-a·R-b 를 유지하되 R-b 를 승격한다"*). 정(正)의 문면: *"결정 자체는 `docs/adr/0025-…:73` 이 「규칙은 남는다」로 이미 판정했고 그 판정이 이 네 항은 안 덮었다. 그리고 ADR-0024 의 상태 줄은 지금도 `**상태**: 채택 (2026-08-17)` 뿐이다(`docs/adr/0024-…:3`)"*
- 귀속 **㉡** · **`H1` 의 증인이 아니다.** 까닭(정(正)): *"원인 커밋 `4d8cece` 가 base 의 조상이다. 넓힌 반경은 이 산문을 가리키지 않았고 가리킬 수도 없었다"*
- 합(合)이 `H1` 절에서 다시 적은 것: *"R-a 는 동결 규약이 남긴 산문"*
- 합(合)이 행 1·3 의 처분에서 이 잔여로 합류시킨 것: 행 1 → *"없다 → 잔여 R-a(⑶)"* · 행 3 → *"없다 → R-a 합류"*

### 잔여 R-e ★ — 내용에서 유도한 id 를 진 결박의 메모를 `import_jsonl` 로 고치면 옛 id 를 그대로 두고 내용이 갈려 내용-주소 불변식이 조용히 깨진다

- 모집단: 저장소
- 유효성: 추정
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-intent/src/store.rs:710-714` · `crates/pal-core/src/binding.rs:17` · `crates/pal-core/src/binding.rs:29-36` · `crates/pal-core/src/binding.rs:280`
- 합(合)이 새로 세운 잔여다(§초안을 어떻게 고치나 7): *"**R-e** — 행 7 의 결박문 낡음. ⚠ **⑴ 「결박문을 고쳤다」가 공짜가 아니다**: `crates/pal-core/src/binding.rs:17`·`:29-36` 이 `BindingId` 를 `(대상, 메모)` 에서 유도하고 `:280` 이 그것을 쓴다. 메모를 고치면 **id 가 달라진다.** 반(反)이 찾은 `crates/pal-intent/src/store.rs:710-714` 의 덮어쓰기 경로는 **옛 id 를 그대로 두고 내용을 갈아** 내용-주소 불변식을 조용히 깬다(그것을 잡는 검사를 나는 못 찾았다). 그러므로 행 7 의 처방은 ⑶ **이슈**이고, 그 이슈가 물을 것은 「내용에서 유도한 id 를 진 결박의 메모를 어떻게 정정하나」다. 귀속 ㉡, **`H1` 의 증인이 아니다.**"*
- 왜 `추정` 인가 — 합(合) §내가 못 정한 것 에 같은 것이 한 줄로 남아 있다: *"**`import_jsonl` 경로가 내용-주소 불변식을 깨는 것을 잡는 검사가 정말 없는지.** `BindingId::derive` 의 호출부를 전수로 봤고 저장된 id 를 재유도해 대는 자리를 못 찾았지만, `cargo xtask check` 를 돌리지는 않았다. 있으면 R-e 의 해악도가 올라간다."*
- ⚠ 이 항의 재료(`import_jsonl` 이 같은 `id` 를 덮어쓴다)는 반(反)이 **기각**으로 적은 자리에서 나왔다 — 반(反)은 「데이터 손실을 부를 것이다」를 물렸고, 합(合)은 같은 사실을 **다른 해악**(불변식 붕괴)으로 다시 세웠다.

### 반(反)이 스스로 선언한 누출 둘이 어느 산출물에서도 처분되지 않았다

- 모집단: 규약
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/2-antithesis.md:13`
- 반(反)의 원문 그대로: *"⚠ **정직하게 적는 누출 둘.** ⑴ `grep -rn "FoldedToOtherQuery"` 가 `observations/2-stale-packet.md:192` **한 줄**을 화면에 올렸다 — ADR-0011 §왜-다른-셋이-아닌가 기각표의 첫 행이고 내가 이미 `docs/adr/0011-…:56` 에서 읽은 것과 같은 문자열이다. 그 파일을 열지는 않았다. ⑵ 같은 grep 이 **다른 회차**(`.palimpsest/rounds/2026-09-08-cross-file-references/observations/`)의 JSONL 두 줄을 올렸고, 거기서 `d716d09163a6d0ab` 가 그때 `radius: symbol`·감시 1 이었다는 사실을 얻었다. 금지 목록에 없는 회차이지만 재료로 썼으므로 적는다."*
- ⚠ **이 항은 합(合)의 산출이 아니다 — 취합 자리에서 집었다.** 합(合)의 채택·기각 표 열넷 어디에도 이 선언이 없고, 종료 판단은 본문을 안 받았으므로 다룰 수 없었다. 격리 선언이 재는 것이 이 자리에서 흐려지므로 적는다. **판정은 하지 않았다.**

### 종료 판단의 꼬리가 없는 파일을 가리킨다 — `2-main-closes.md` 가 저장소에 없다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/2-referee.md:52`
- 원문 그대로: *"> **메인의 기록** — 위 4 를 닫았다. [`2-main-closes.md`](2-main-closes.md) 가 그 둘의 판정을 진다."*
- 잰 것: `dialectic/` 아래 파일 목록에 `2-main-closes.md` 가 없다(`1-*` 여덟 · `2-antithesis.md` · `2-design.md` · `2-referee.md` · `2-synthesis.md` · `2-thesis.md` · `r1-raw.md`). 그리고 회차 디렉터리 전체에서 그 이름을 부르는 자리는 이 한 줄뿐이다.
- 그래서 종료 판단이 「무엇을 안 끝냈나」 4 로 남긴 것 — *"**합(合)이 남긴 「메인이 판단할 자리 둘」이 미결이다.** … **메인이 닫아야 하고, 닫기 전에는 이 회차가 끝난 것이 아니다.**"* — 이 닫혔다고 적힌 채로 그 판정을 지는 파일이 없다.
- ⚠ 이 항도 합(合)의 산출이 아니다 — 취합 자리의 계수 대조에서 나왔다. 그 줄이 **저장소 안 문서를 마크다운 링크로 적은 자리**이기도 하다(`#134`).

## 기각의 원천 — 어디서 몇이 왔나

⚠ **이 표가 `## 내가 기각한 것` 절 **앞**에 있다.** 그 절 안에 두면 계수기가 표의 데이터
행을 기각 항으로 세어 **추출기와 다섯이 갈린다**(실측: 36 ↔ 31). 같은 파일을 두 자가
세므로 표는 그 절 밖에 둔다.

**합(合)의 기각 표는 네 행이고, 그중 반론 단위는 하나뿐이다.** 나머지 셋은 반론 1 의
**등급** · 반론 6 의 **인과 부분** · 초안의 **「541 커밋」**이다. 아래 절이 그것과 반(反)이
스스로 물린 것, 반(反)의 `## 내가 기각한 것` 열넷을 전부 센다.

| 기각의 원천 | 수 |
|---|--:|
| 합(合)의 기각 표 행 | 4 |
| 그중 반론 단위 | 1 |
| 반(反)이 반론 안에서 스스로 물린 것 | 2 (반론 11 전체 · 반론 6 의 인과) |
| 반(反)의 `## 내가 기각한 것` 표 행 | 14 (그중 하나는 「부분 기각」) |
| 아래 `### ` 항 수(같은 것을 두 원천이 기각한 셋을 하나로 묶었다) | 16 |

## 내가 기각한 것

### 다섯 행의 4·6·7 항이 같은 문면으로 반복된다 — 한 덩이 판정의 흔적

- 모집단: 자기장치
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/2-thesis.md:42` · `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/2-thesis.md:44`
- **세 원천이 같은 것을 기각했다 — 하나로 묶었다.** 반(反)의 반론 11(스스로 `유효성 거짓` 으로 매겼다) · 반(反)의 `## 내가 기각한 것` 마지막 행 · 합(合)의 기각 표 첫 행.
- 반(反) 그대로: *"**그런데 3·4 항의 좌표는 행마다 다르고 내가 전부 독립 재현했다** — 행마다 다른 파일·다른 커밋·다른 심볼이다. 그러므로 반복되는 것은 **판정이 아니라 틀**이고, 이 반론은 **유효성 거짓**이다. 다섯을 한 덩이로 판정한 흔적은 못 찾았다."*
- 합(合) 그대로: *"반(反) 자신이 물렸고 나도 확인했다 — 행마다 다른 파일·커밋·심볼이 근거로 붙어 있고, 반(反)이 그 좌표를 전수 독립 재현했으며 나도 행 1·2·3·5·7 의 핵심 좌표를 따로 열었다. **반복되는 것은 판정이 아니라 틀이다.**"*
- 이 기각이 닫은 것: 설계문이 미리 지목한 금지역 **㉰**(다섯을 한 덩이로 판정해 건별 판정이 없는 것).

### 반론 1 에 반(反)이 매긴 `금지역` 등급

- 모집단: 자기장치
- 유효성: 거짓
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/2-antithesis.md:44` · `crates/pal-core/src/binding.rs:138`
- ⚠ **반론 자체는 채택됐다 — 기각된 것은 등급뿐이다.** 합(合) 그대로: *"**등급만 내린다(반론 자체는 채택했다).** 설계문이 금지역을 셋으로 **미리 지목했고** 반론 1 은 그 셋 어디에도 안 닿는다 … 그리고 술어는 `C3` 의 문면과 코드(`crates/pal-core/src/binding.rs:138`·`:46`)와 저장소 규약 셋으로 **근거를 대어 고를 수 있었다** — 재료가 모자라 못 정하는 갈림이 아니다. 고쳐서 닫히는 것은 `거짓신호` 다"*
- **이 한 건이 판의 끝을 정했다.** 종료 판단 그대로: *"**등급이 내려간 한 건**(반(反) `금지역` → 합(合) `거짓신호`)은 이 표에서 `거짓신호` 로 셌다. … **이 판의 끝이 그 한 건의 등급에 달려 있었다**는 사실은 기록으로 남긴다. 그 건이 `금지역` 이었다면 답은 「한 라운드 더」였다."*

### 반론 6 의 인과 부분 — *"이 누락이 행 7 을 놓치게 했다"*

- 모집단: 저장소
- 유효성: 거짓
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-cli/src/label.rs:54-59` · `crates/pal-core/src/radius.rs:132-137`
- 두 원천이 기각했다 — 반(反)이 반론 6 안에서 스스로 물렸고 합(合)이 기각 표에 다시 적었다.
- 반(反) 그대로: *"⚠ **내가 스스로 물리는 부분:** 이것이 행 7 의 침묵 **원인은 아니다** — `병기` 의 본문은 base..HEAD 에서 안 변했다. 그러므로 「이 누락이 행 7 을 놓치게 했다」는 **거짓**이다."*
- 합(合) 그대로: *"반(反) 자신이 물렸고 근거가 맞다 … 사실만 채택하고 인과는 버린다"*

### 초안의 *"base..HEAD 541 커밋"* 이 틀렸다

- 모집단: 자기장치
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/2-thesis.md:124`
- 두 원천이 기각했다 — 반(反)의 `## 내가 기각한 것` 표와 합(合)의 기각 표.
- 반(反) 그대로: *"`git rev-list 9f0bf70..47a0014 --count` = 541 — 재료 잠근 시점의 값"* · 본문(§7): *"`:124` 의 「base..HEAD 541 커밋」은 `47a0014` 시점의 값과 일치하므로 **틀린 것이 아니다**(지금은 545) — 그 부분은 기각한다."*
- 합(合) 그대로: *"나도 기각을 확인한다 — 재료를 잠근 시점의 값이고, 설계문은 540 으로 적었다. 수가 다른 것은 시점 차이이고 틀린 것이 아니다"*

### `///`·`//!` 가 감시 `digest` 안일 것이다(초안 손잡이 1)

- 모집단: 저장소
- 유효성: 거짓
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-extract/src/parse.rs:170-173` · `crates/pal-cli/src/label.rs:200`
- 반(反)의 실측 그대로: *"스팬 밖이고(`잔여_사유` 스팬은 `pub const fn` 줄에서 시작), 안이었어도 `parse.rs:171` 이 버린다"* → **기각 — 초안이 옳다**
- 정(正)이 이것을 가장 값나가는 손잡이로 내놨다: *"**`///`·`//!` 가 감시 `digest` 안이라면 R-c 와 대조 행 7 의 판정이 함께 무너진다.**"*

### 감시 11 이 초안이 센 것이 아닐 것이다(손잡이 2)

- 모집단: 자기장치
- 유효성: 거짓
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-cli/src/label.rs:54-59` · `crates/pal-cli/src/label.rs:200`
- 반(反)의 실측 그대로: *"11 해시 전부 `label.rs` 심볼로 되짚었다. `1 + 10 = 11` 맞다"* → **기각**
- 되짚은 이름 열하나: `Label`·`신선도`·`파일_상태`·`정체성_등급`·`대기_사유`·`미해소_사유`·`추출_등급`·`가까움`·`판정_불가_사유`·`잔여_사유`·`두_조각이_안_뒤바뀐다`

### `8f182d6` 이 `check_catalog` 범위에 더 많은 hunk 를 넣었을 것이다(손잡이 6)

- 모집단: 저장소
- 유효성: 거짓
- 해악도: 거짓신호
- 어디가 걸리나: `xtask/src/main.rs:1373`
- 반(反)의 실측 그대로: *"파일을 만진 50 커밋 전수 digest 추적 → 전이는 `8f182d6` 한 번"* → **기각**

### `8597` 의 문구 *"조용히 꺼진다→멎는다"* 를 계약으로 보는 시험·게이트가 있을 것이다(손잡이 5)

- 모집단: 저장소
- 유효성: 거짓
- 해악도: 거짓신호
- 어디가 걸리나: `xtask/src/main.rs:1478`
- 반(反)의 실측 그대로: *"`xtask/src/main.rs:1478` 의 `problems.push` 문면이고 그 문자열을 단정하는 자리를 못 찾았다"* → **기각**
- 정(正)이 이 자리를 *"가장 물릴 자리"* 로 지목했다: *"그 문구를 계약으로 보는 시험이나 게이트가 있다면 이 행은 참이 된다."*

### #114 의 `접다→뭉개다` 개명이 `ADR-0027 §③` 제목을 바꿔 앵커를 깼을 것이다

- 모집단: 저장소
- 유효성: 거짓
- 해악도: 거짓신호
- 어디가 걸리나: `docs/adr/0027-the-instrument-must-reach-its-own-floor.md:3`
- 반(反)의 실측 그대로: *"제목이 지금도 `### ③ SymbolKind 를 접지 않고 늘린다`"* → **기각**

### 다섯의 subject ADR 이 결박 뒤에 움직였을 것이다

- 모집단: 저장소
- 유효성: 거짓
- 해악도: 거짓신호
- 어디가 걸리나: `docs/adr/0024-an-adapter-that-can-diverge-is-a-second-core.md:3` · `docs/adr/0005-absence-carries-its-kind.md:3` · `docs/adr/0011-moved-volume-and-unseen-volume-are-different-fields.md:3`
- 반(反)의 실측 그대로: *"다섯 중 넷(0024·0005·0011·0021)은 base..HEAD 에서 **0 커밋**"* → **부분 기각**(0027 만 남아 반론 8)
- ⚠ 열넷 중 유일하게 「부분 기각」인 행이다. 살아남은 쪽이 위 `## 반론 처리` 의 `ADR-0027` 항이다.

### `base에없던새원소` 가 장치의 결함일 것이다

- 모집단: 자기장치
- 유효성: 거짓
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-cli/src/radius.rs:57-59`
- 반(反)의 실측 그대로: *"`crates/pal-cli/src/radius.rs:57-59` 가 설계로 적고 건수를 산출에 싣는다"* → **기각**
- 이 기각이 반론 3 과 `A3-b` 통과의 바닥이 됐다 — 미재현 수가 정확히 `base에없던새원소` 이고 그 원소들은 **원리상 `fresh`** 다.

### 행 7 의 대체 방어(`crates/pal-cli/tests/export_ascii.rs`)는 「있다」뿐이고 안 막을 것이다

- 모집단: 저장소
- 유효성: 거짓
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-cli/tests/export_ascii.rs:1` · `crates/pal-cli/src/label.rs:18-30`
- 반(反)의 실측 그대로: *"속성 키 셋을 걷고 하한 둘(`CREATE` 존재 · 값 ≥ 3)과 음성 대조를 진다. **실제로 막는다**"* → **기각 — 초안보다 강하게 선다**
- 합(合)이 행 7 을 뒤집을 때 이 기각을 근거로 썼다: *"대체 방어 `crates/pal-cli/tests/export_ascii.rs` 가 **실제로 막는다**"*

### ⑴ 결박문 수정은 `pal bind` 재호출밖에 길이 없어 데이터 손실을 부를 것이다

- 모집단: 저장소
- 유효성: 거짓
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-intent/src/store.rs:710-714`
- 반(反)의 실측 그대로: *"`crates/pal-intent/src/store.rs:710-714` 의 `import_jsonl` 이 같은 `id` 를 **덮어쓴다**. `export → 수정 → import` 로 `bound_at`·감시를 보존해 고칠 수 있다"* → **기각**
- ⚠ **합(合)이 같은 사실을 반대 방향의 해악으로 다시 세웠다** — 위 `## 반론 처리` 의 잔여 R-e 항이 그것이다. 기각된 것은 「데이터 손실」이고, 새로 선 것은 「내용-주소 불변식이 조용히 깨진다」다.

### 초안이 인용한 결박문 문면이 `bindings.jsonl` 과 다를 것이다

- 모집단: 회차기록
- 유효성: 거짓
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/intent/bindings.jsonl:8`
- 반(反)의 실측 그대로: *"일곱 `note` 를 직접 파싱해 대었다. 행 7 의 ② 인용은 바이트로 같다"* → **기각**
- 정(正)도 같은 대조를 스스로 적었다: *"재료 묶음의 결박문 일곱은 `.palimpsest/intent/bindings.jsonl` 의 `note` 필드와 **바이트로 같다**"*

### 행 1·5 의 HEAD 좌표가 안 맞을 것이다

- 모집단: 회차기록
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: `crates/pal-cli/src/label.rs:194` · `crates/pal-core/src/envelope.rs:190`
- 반(反)의 실측 그대로: *"`envelope.rs:190`·`:222` · `plan.rs:409` · `binding.rs:484`·`:536` · `file_graph.rs:315` · `parse.rs:389` · `label.rs:18`·`:26`·`:30` 전부 적힌 그대로(`label.rs:194` 만 한 줄 어긋남 — 문서 주석은 195 부터)"* → **기각**
- ⚠ 기각이지만 **한 줄 어긋남이 실재한다**고 원문이 적었다. 그 어긋남을 반(反)이 반론으로 세우지 않았고 합(合)도 다루지 않았다.

### `recovery_sites` 호출부가 셋이 아닐 것이다

- 모집단: 저장소
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: `crates/pal-extract/src/parse.rs:412` · `crates/pal-extract/src/parse.rs:360`
- 반(反)의 실측 그대로: *"넷이다 — 넷째는 `crates/pal-extract/src/parse.rs:412`, `#[cfg(test)]` 안의 시험 헬퍼다. **생산자 셋**이라는 초안의 문장은 맞다"* → **기각**

## 계수 — 두 원천 대조

| 잰 것 | 반(反) | 합(合) | 같나 |
|---|--:|--:|---|
| 반론에 붙은 번호 | **11**(1~11) | 채택 **10** + 기각 표 **4 행** = **14 행** | **갈린다** — 합(合)의 기각 표 4 행 중 반론 단위는 하나(11)뿐이고, 나머지 셋은 반론 1 의 **등급** · 반론 6 의 **인과 부분** · 초안의 **「541 커밋」**이다 |
| 반론 단위로 다시 센 채택+기각 | 11 | 채택 10 + 기각 1 = **11** | 같다 |
| 살아남은 것 | — | **10** | 종료 판단의 해악도 표(`거짓신호 10`)와 같다 |
| 해악도 `금지역` | **1**(반론 1) | **0** | **갈린다** — 합(合)이 반론 1 의 등급을 `거짓신호` 로 내렸다 |
| 해악도 `거짓신호` | **9**(반론 2~10) | **10** | **갈린다** — 반론 1 이 내려와 합쳐진 결과다 |
| 해악도 `미관` | **1**(반론 11) | **1**(그 하나가 유일하게 기각됐다) | 같다 |
| 반(反)의 `## 내가 기각한 것` 행 | **14**(그중 하나는 「부분 기각」) | 그중 둘을 합(合)이 자기 기각 표에 다시 적었다 | — |
| 새 `stale` 모집단 | 5 | 5 | 같다 |
| `참인 재검토` | 정(正) **1**(대조 행 7) | **0** | **갈린다** — 합(合)이 행 7 을 뒤집었다. 이것이 이 판의 값이다 |
| 켠 커밋 | **일곱** | **일곱** | 같다 · 정(正)과 갈린다(정(正)은 **여덟**이라 적고 괄호 안은 일곱이다) |
| 어휘 정규화 비율 | 4/7(57%) | 4/7(57%) | 같다 · 정(正)의 4/8 과 갈린다 |
| base 재현 수(감시 원소) | 75 · 24 · 2 · 4 · 15 | 75/78 · 24/25 · 2/2 · 4/5 · 15/18 | 같다 — 두 자리가 독립으로 재현했다 |
| base..HEAD 커밋 수 | 541(재료 잠근 `47a0014` 시점) · 지금 545 | 541 은 시점 차이이고 틀린 것이 아니다 · 설계문은 **540** | 셋이 다르고 **세 원천이 그 차이의 까닭을 각각 적었다** |
| 심볼·요약 수(같은 base 121 파일) | base **2,626 심볼** · HEAD **3,363 심볼** | **body 요약 2,548 개** | **갈린다** — 두 수가 같은 것을 재는지 어느 원문도 적지 않았다. 맞추지 않는다 |
| 결박 총수 · `callers` · 켠 것 | 37 · 18 · 5 | 37 · 18 · 5 | 같다 |
| 설계문 §반증 여섯 중 발화 | (안 받았다) | **0** | 종료 판단이 받은 수(0)와 같다 |
| 쓴 라운드 / 상한 | — | — | **1 / 2** — 종료 판단이 *"상한이 한 라운드 남았는데 안 쓴다"* 로 적었다 |
| 잔여 | — | **다섯**(R-a·R-b·R-c·R-d·R-e) | 정(正)은 **셋**이었다 — 합(合)이 둘을 더하고 R-c 를 강등하고 R-b 를 승격했다 |

## 종료 판단

`2-referee.md` 그대로 옮긴다. 이 자리는 판정 내용을 안 다룬다고 그 문서가 스스로 적었다.

> **끝났다.**
>
> 살아남은 반론 열 건이 **전부 `거짓신호`** 다. … 라운드를 더 도는 조건은 **살아남은 것에 `금지역` 이나 `실패` 가 있을 때**다. 둘 다 0 이다.
>
> 명시로 적는다 — **상한이 한 라운드 남았는데 안 쓴다.** 이것은 아껴서가 아니다. … R2 를 부르는 방아쇠는 「상한이 남았다」가 아니라 「금지역·실패가 살아남았다」다. 방아쇠가 안 당겨졌다.

| 해악도 | 수 |
|---|--:|
| 금지역 | 0 |
| 실패 | 0 |
| 거짓신호 | 10 |
| 미관 | 0 |

> **판정값으로 안 갈랐다.** 채택/기각의 비율(10 채택 · 4 기각)도 근거로 안 썼다. 가른 것은 위 표 한 개다.
> **「새 발견이 0」은 안 썼다.** 이 판은 dry 로 끝난 게 아니라 **해악도 상한이 `거짓신호` 에서 멈춰서** 끝났다.

**무엇을 안 끝냈나 — 다섯을 그대로 옮긴다.**

1. *"**`거짓신호` 열 건은 사라진 것이 아니라 판정문으로 옮겨진 것이다.** … 열 건이 판정문에 실제로 적혔는지는 합(合)의 산출물이 지고, 이 종료 판단이 보증하지 않는다."*
2. *"**반증 조건 여섯이 0 발화한 것을 「정(正)이 옳다」로 읽지 않았다.** … 검출기의 민감도 문제인지 정(正)의 건강함인지는 미결이고, 이 판단은 그것에 의존하지 않는다."*
3. *"**R2 를 안 돌았으므로 R1 의 합(合) 산출물 자체는 반(反)을 한 번도 안 받았다.** … 그 역할은 `독립 리뷰` 로 넘어간다."*
4. *"**합(合)이 남긴 「메인이 판단할 자리 둘」이 미결이다.** … **메인이 닫아야 하고, 닫기 전에는 이 회차가 끝난 것이 아니다.**"* ⚠ 그 파일(`2-main-closes.md`)이 저장소에 없다 — 위 `## 반론 처리` 의 마지막 항이 그것이다.
5. *"**등급이 내려간 한 건**(반(反) `금지역` → 합(合) `거짓신호`)은 이 표에서 `거짓신호` 로 셌다. … **이 판의 끝이 그 한 건의 등급에 달려 있었다**는 사실은 기록으로 남긴다. 그 건이 `금지역` 이었다면 답은 「한 라운드 더」였다."*

**합(合)이 못 정한 것 넷 — `### ` 항으로 세우지 않았다.** 스키마가 「안개」를 *"레코드에 안 들어온다"* 로 가르므로, 항으로 세우면 없는 레코드를 지어내는 것이고 기각 절에 넣으면 헛것으로 세어진다. 문장으로만 옮긴다.

- *"**선택도 — 「주의 배분」 읽기의 값.** … **그 수가 정하는 것은 다른 것이다 — 반경을 되돌릴지.**"*
- *"**`5e6bbbac89053565` 의 좌표 충돌 수.** `crates/pal-extract/examples/coord_collisions.rs` 를 돌리지 않았다(초안도 안 돌렸다). … 판정은 안 바뀐다."*
- *"**일치도.** `widen/decisions.tsv` 를 안 받았으므로 내 판정이 넓힐 때 판정과 맞는지 **모른다.** 그리고 대기 전에 **두 판정의 술어를 나란히 적어야** 그 비교가 뜻을 갖는다."*
- *"**`import_jsonl` 경로가 내용-주소 불변식을 깨는 것을 잡는 검사가 정말 없는지.**"* ⚠ 이 하나만은 잔여 R-e 로 **단언된 쪽이 따로 있어서** 위에 `유효성=추정` 으로 항을 세웠다.

## 소유자에게 올릴 물음

**없다.**

두 자리가 같은 답을 냈고, 근거가 다르다는 사실까지 옮긴다.

- 합(合): *"**안 올린다.** 살아남은 반론의 가장 무거운 것이 **`거짓신호`** 다 … 고르니 일곱이 일관되게 `아니다` 로 닫히고 `C3`·`H1`·`A3-b` 가 전부 판정값을 얻는다."*
- 종료 판단: *"**없다.** … 합(合)의 「안 올린다」와 결과가 같으나, 근거는 합(合)의 산출이 아니라 위 해악도 표다."*

⚠ **소유자에게 안 올라가지만 미결로 남은 것 둘** — 합(合)이 *"메인이 판단할 자리"* 로 보이게 적었고 종료 판단이 *"닫기 전에는 이 회차가 끝난 것이 아니다"* 로 받았다. 그대로 옮긴다.

1. *"설계문 스스로 남긴 갈림 — 「`C3` 의 「판정자는 `B2-a` 와 같은 자로 적는다」를 「같은 에이전트가 판정해야 한다」로 읽는다면 이 설계와 부딪힌다」. 그 해석은 설계자도 정하지 않았다."*
2. *"반론 9 가 낳은 처방(**승격이 ADR 절 전체를 메모로 삼는 것**)은 이 판의 물음 밖이고 발견·이슈로 나가는 것이 맞다. 그것을 결정으로 바꾸려면 `ADR-0014` 요구 ③(재판정 소유자)과 함께 별 자리에서 열려야 한다."*

---

> ## ⚠⚠ 메인의 기록 — 이 파일을 옮기면서
>
> **원 반환문이다. 요약하거나 판정을 고치지 않았다.** 옮기면서 한 것은 셋이다 —
> ⑴ 전달 과정의 HTML escape 를 되돌렸다 ⑵ 반환문이 전문을 감싸고 있던 바깥
> 코드펜스를 벗겼다(그것을 남기면 계수기와 추출기가 이 파일에서 **아무 항도 못 센다**)
> ⑶ `어디가 걸리나` 의 좌표 목록이 긴 항에서 뒤쪽 좌표 일부를 줄였다 — **첫 좌표는 안
> 건드렸다**(추출기가 첫 좌표를 `경로` 로 읽는다) ⑷ **불릿 하나의 머리말을 바꿨다** —
> `- 유효성을 \`추정\` 으로 적은 근거` 가 `- 유효성:` 라벨로 읽혀 그 항의 `유효성` 칸에
> 문장 전체가 들어갔다(추출기는 알려진 라벨의 **접두**로 가른다). `- 왜 \`추정\` 인가` 로
> 바꿨다 — **내용은 그대로다.** ⑸ `## 내가 기각한 것` 의 요약 표를 **그 절 앞의 새 절로**
> 옮겼다 — 절 안에 두면 계수기가 표의 데이터 행 다섯을 기각 항으로 세어 추출기와
> **36 ↔ 31** 로 갈렸다(실측). **표의 내용도 절의 항도 안 바뀌었다.**
>
> **★ 그리고 마지막 항의 사실이 그 사이에 바뀌었다 — 그 사실을 적는다.**
>
> `## 반론 처리` 의 마지막 항(*"`2-main-closes.md` 가 저장소에 없다"*)은 **취합이 잰
> 시점에 참이었다.** 그 판정을 지는 파일을 메인이 커밋 **`05527b1`** 에서 세웠고, 취합은
> 그보다 앞서 재고 있었다. **지금은 `dialectic/2-main-closes.md` 가 있다** — 종료 판단이
> §4 로 남긴 둘(`C3` 의 판정자 해석 · 반론 9 의 처방)을 근거와 함께 닫는다.
>
> 그러므로 그 항은 **유효한 발견이고 이미 닫혔다** — 레코드의 `닫은커밋` 이 `05527b1` 을
> 진다. ⚠ **항을 지우지 않는다**: 취합이 잰 것이 그때 참이었다는 사실이 남아야
> 「무엇이 언제 닫혔는지」가 사후에 대조된다.
>
> 그리고 취합이 **합(合)의 산출이 아닌 항 둘을 스스로 세웠다**(반(反)의 누출 선언 미처분 ·
> 이 마지막 항). 취합이 *"뺄지는 메인이 판단할 자리다"* 로 남겼고 — **안 뺀다.** 둘 다
> 좌표가 서고 유효성이 참이며, 취합 자리가 계수 대조에서 집은 것을 버리면 그 자리가
> 재는 것이 없어진다.
