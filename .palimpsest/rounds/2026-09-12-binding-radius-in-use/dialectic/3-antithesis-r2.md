# 반(反) · 라운드 2 — 판 3 의 반론표

<!-- 이 파일은 `pal-decision-opponent` 의 원 산출물이다(라운드 2). 메인이 옮겼고 고치지
     않았다. 이번 대상은 **R1 의 합(合) 판정문**이다 — 정(正)의 초안이 아니다.
     반(反)이 받은 것과 안 받은 것은 아래 「선언」 절이 스스로 적는다.
     메인이 옮기면서 한 것은 이 제목과 이 주석을 단 것뿐이다. -->

> 판 **3** · 라운드 **2** · 회차 `2026-09-12-binding-radius-in-use` · 2026-09-13
> 낸 자: `pal-decision-opponent` (서브에이전트, 읽기 전용)

## ⚠ 선언 — 내가 받은 것 · 안 받은 것

받은 것: 프롬프트가 실은 술어 P 정본·딸린 규정 셋·접기 규칙·금지역 다섯, 그리고 **R1 의 합(合) 판정문 전문**. 내가 직접 열어 본 것: `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/3-synthesis.md` 전문 · 같은 회차 `observations/3-b2-packet.md` 의 **§2·§3 만** · `dialectic/3-thesis.md`(합이 인용한 「참 (약)」 정의를 대려고 :90~:229 구간) · `git status --porcelain` · `git rev-parse HEAD`.

**한 줄도 안 열었다**: 패킷 §0·§1·§4·§5·§6 · `dialectic/3-design.md` · `dialectic/3-referee.md` · `dialectic/3-antithesis.md`(R1 반) · `intent.md` · `widen/README.md` · `docs/gates/binding-radius-in-use.md` · `review/` · `conditions-audit/` · `premortem/` · 대화 기록 · 메인의 사고 과정. **프롬프트에 사고 과정이나 대화 기록이 섞여 오지 않았다.**

작업 트리: `git status --porcelain` 이 **0 줄**(시작·끝 두 번). HEAD `c8738d5`. 한 글자도 안 고쳤다.

**좌표 약어** — `합:NN` = `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/3-synthesis.md:NN` · `패킷:NN` = `.palimpsest/rounds/2026-09-12-binding-radius-in-use/observations/3-b2-packet.md:NN` · `정:NN` = `.palimpsest/rounds/2026-09-12-binding-radius-in-use/dialectic/3-thesis.md:NN`.

---

## 초안이 서는 자리

**㊀ 이 섰다 — 합의 ⒝ 읽기는 보탠 것이 아니다.** ⒝ 의 셋째 절이 문면으로 *"의존의 방향(부르는 쪽이 변하면 이 결정이 어떻게 되나)이 문면에 서야 한다"* 다. 「방향」은 두 항의 관계이고 괄호가 그 방향을 명시한다. 「두 항이 다 문면에 있어야」는 술어 문면에서 나온다. 못 깼다.

**16 행의 인용이 전수로 정본이다.** 합:151~166 의 16 항을 패킷 §2 의 `근거` 셀과 기계로 대조했다(16/16 이 정본 문말의 그대로다). **뜻을 바꾼 자름은 없다.** 머리를 `…` 로 줄인 것은 전부이나, 줄인 머리가 판정을 바꾸는 행은 아래 반론 10·11 로만 남는다.

**㊃ 의 인용 셋이 실재한다.** `패킷:424` 에 *"공급 쪽이 「담을 수 없다」 고 적어 두면 그 검사는 영구히 돌지 않는다"* 가 글자로 있다 · `f4ba084c7a3838be` 결박문에 *"「접힘」 문자열을 요구했다"* 와 *"새 회차는 새 표기로 쓴다"* 가 있다(`패킷:908` 아래) · `b52057859bf405eb` 의 *"이 순수 함수 안의 입력 우선순위"* 는 `패킷:678` 의 `근거` 정본이다. **인용 위조는 없다.**

**산술과 분포가 맞다.** S1 6 · S2 7 · S3 13 · S4 11 = 37, `symbol` 19 · `callers` 18, `P 거짓` 16 이 전부 `symbol`·`아니다` — 패킷 §2 에서 내가 다시 셌고 전부 맞다.

**㊄ 의 「위조 둘」은 틀리지 않았다.** 합:56 과 합:212 의 주어는 ⒝ 다(*"⒝ 거짓이 되는 단위"* · *"⒝ 가 … 자동 참이 되어 대조 둘만 잡는다"*). ㉢ 은 합 자신의 표에서 ⒝ 참·⒜ 거짓(합:143)이므로 ⒝ 에 한정한 말이고 모순이 없다. **여기는 안 무너졌다.**

**★ 판정값을 뒤집을 근거는 없다.** 16 행에서 ⒝ 를 되살릴 행을 찾았으나 하나도 없었고(아래 「물린 것」), `6c0e8ea521d0b2ac` 한 행이 **술어 문면에 직접 결박된 독립 근거**(*"결박문의 요약이기만 하면 ⒝ 는 거짓"*)로 `P 거짓` 이다 — 그 근거 *"…이 검사 자신의 한계와 뒤집힐 조건을 **적은 것이다**"*(`패킷:419`)는 결박문의 두 소제목과 첫 문장을 옮긴 것뿐이다. 접기 규칙은 **한 행으로 `반증`** 이다. 아래 반론이 전부 채택돼도 `B2` = `반증` 은 선다.

---

## 반론

| # | 반론 | 좌표 | 유효성 | 해악도 |
|---|---|---|---|---|
| 1 | 「이 넷은 지역성조차 주장하지 않는다 · 「안/밖」의 소재 표시가 없다」가 `6c0e8ea521d0b2ac` 에서 정본에 반한다 — 그 근거는 「이 검사 **자신의** 한계」이고 「자신의」는 합이 지역성 서술로 분류한 넷과 같은 낱말이다 | 합:58 | 참 | 금지역 |
| 2 | 그 보조 방어(「어느 읽기에서도 통과는 안 선다」)의 남은 셋은 소유격 소재 표시를 지고 있어 초안의 ㈏ 아래에서는 ⒝ 참이 된다 — 방어가 넷이 아니라 실질 한 행(`6c0e` 의 「요약」)으로 준다 | 합:58 | 추정 | 거짓신호 |
| 3 | ★ 한 문서에 비등가한 ⒝ 기준 둘이 있다 — §핵 ㊀ 과 행별 근거 16 항은 **방향**을 요구하고 처방 2 는 **항 + 관계**만 요구한다. 넓힌 18 중 7 행이 정확히 그 틈에 앉는다 | 합:250 | 참 | 거짓신호 |
| 4 | ★ 초안은 「참 (약)」 7 행을 **㈏ 가 지는 행**이라고 스스로 정의했는데, 합은 ㈏ 를 철회하고도 그 7 행의 ⒝ 를 참으로 옮겼다 — 철회한 읽기에 매달린 값이다 | 정:125 | 참 | 거짓신호 |
| 5 | 「`영향받는다` 18 행은 18/18 이 호출 경계를 항으로 부른다」가 `fcd48a9033fd1fb0` 에서 거짓이고, 합 자신이 뒤에서 그것을 인정한다 | 합:49 | 참 | 거짓신호 |
| 6 | 「「참 (약)」 여섯」이 일곱이다 — 합 자신의 표 7 행과 초안의 「7 행이고」가 둘 다 일곱이다 | 합:289 | 참 | 미관 |
| 7 | 자기 표찰 시험을 비대칭으로 댔다 — `6c0e` 는 「…적은 것이다」로 죽였는데 `ce25c6f23f0a8992` 는 문면이 글자로 「…이 그 까닭이다」인데 살렸다. 술어는 「까닭이기만」을 명시 배제한다 | 패킷:86 | 참 | 거짓신호 |
| 8 | `b52057859bf405eb` 의 행별 근거가 스스로 「문면이 세우는 방향은 오히려 바깥에서 안으로다」라 적는다 — 방향이 서면 ⒝ 는 답이 있는 것이고 실패는 ⒞ 로 옮겨간다. 「방향이 없어서 거짓」과 그 행에서 어긋난다 | 합:161 | 참 | 거짓신호 |
| 9 | ㊁ 는 그 16 행에서 「⒞ 는 반증 불가가 된다」고 적었는데 표는 같은 16 행의 ⒞ 에 확정값 「거짓」을 넣었다 — 반증 불가라고 선언한 다리에 값을 준 것이고, 정직한 값은 「해당없음」이다 | 합:47 | 참 | 거짓신호 |
| 10 | ㊃ 의 반례 셋 중 둘이 축이 다르다 — `f4ba` 의 바깥은 지난 회차 **파일**이고 `6c0e` 의 「공급 쪽」은 **선언을 적는 쪽**이다. ⒝ 가 이름 붙인 축은 「호출자 변경」이다 | 합:52 | 참 | 미관 |
| 11 | 행별 근거가 **결박문 내용**을 ⒝ 의 근거로 끌어 쓰는데, 같은 사실을 「범위 밖」 절은 「답이 옳았나 = 범위 밖」으로 보냈다 — 같은 재료를 두 자리에서 다르게 쓴다 | 합:155 | 참 | 미관 |
| 12 | ㊁ 의 「지문」이 과잉결정이다 — 16 행에 ⒞ 문장이 없는 것은 합이 채택한 반론 5(같은 문장 구조로 덮기)로도 똑같이 설명된다. 순환의 증거로만 읽을 근거가 없다 | 합:47 | 추정 | 미관 |
| 13 | ㊄ 의 ㉲ 적용은 「자동 참」이 아니라 「37 행에서 전부 참으로 관측」이다 — ㈏ 아래에서도 ⒝ 는 위조 둘에서 실제로 거짓이 되므로 「자동」은 추론이다 | 합:56 | 추정 | 미관 |

---

## 표 밖 항 — 긴 인용과 전수

**항 ⑴ (반론 1·2 의 정본).** 합:58 이 「지역성조차 주장하지 않는다」로 묶은 넷의 `근거` 문말은 각각 이렇다 — `6c0e8ea521d0b2ac` *"…이 검사 **자신의** 한계와 뒤집힐 조건을 적은 것이다"*(`패킷:419`) · `a47cd21ca1fbc524` *"…이 열거의 분류 태도다"*(`패킷:615`) · `e64fd4f5378c5f20` *"…이 검사의 판정 형식이다"*(`패킷:896`) · `f4ba084c7a3838be` *"…이 검사가 받아들이는 문자열 집합의 결정이다"*(`패킷:908`). 「자신의」는 합이 지역성 서술로 분류한 `49714573e0fa654c`(*"이 선택자 자신의"*) · `6192ba6df5242980`(*"이 검사 자신의"*) · `91cdc3add647a3f7`(*"이 화면 자신의"*) · `969991a119d4c1ef`(*"이 선택자 자신의"*)와 **글자까지 같은 낱말**이다. 그러므로 `6c0e` 에 대한 「소재 표시가 없다」는 정본에 반한다(반론 1 = 참). 남은 셋은 소유격뿐이어서 「소유격은 안/밖 표시가 아니다」라는 방어가 가능하다 — 그래서 반론 2 는 **추정**으로 낸다. ⚠ 다만 `6c0e` 의 괄호 근거(「결박문의 요약이기만」)는 **술어 문면에 결박돼 살아남고**, 접기 규칙은 한 행으로 `반증` 이므로 **이 반론은 판정값을 뒤집지 않는다.** 바뀌는 것은 「어느 읽기에서도」의 **폭**이다 — 넷이 아니라 하나다.

**항 ⑵ (반론 3·4·5 의 7 행 전수).** 합:45 의 ㊀ 기준(*"부르는 쪽이 변하면 이 결정이 어떻게 되나"* 가 문면에 서야)을 넓힌 18 행에 그대로 대면 아래 7 행이 떨어진다. 나는 이 일곱을 **합의 「약」 표시를 안 보고** 문면만으로 골랐고, 골라 놓고 대조하니 합의 「참 (약)」 7 행(합:110·113·118·133·135·139·140)과 **정확히 같은 집합**이었다.

- `45d8808585ee0d50`(`패킷:63`) *"…부르는 쪽의 조립 방식을 정하는 규칙이다"* — 방향이 결정→부르는 쪽이다. 조건절 없음.
- `5b9331b398e8ff23`(`패킷:66`) *"…부르는 쪽이 「낡음」으로 산출하게 하고 관문을 `open_read_only` 하나로 못 박은 결정이다"* — 같은 역방향.
- `746a7761356136d8`(`패킷:71`) *"그 까닭이 부르는 쪽에 빈 모집단을 잡는 자가 없다는 사실이다"* — 문면이 스스로 「까닭」이다.
- `ce25c6f23f0a8992`(`패킷:86`) *"…부르는 자리에서 무엇이 참인지 읽혀야 한다는 것이 **그 까닭이다**"* — 술어가 명시 배제한 형태를 글자로 진다.
- `ddb749e489242cab`(`패킷:88`) *"그 값을 정규화가 읽어 정규형을 정한다는 것이 결정의 축이다"* — 역방향, 조건절 없음.
- `fcd48a9033fd1fb0`(`패킷:92`) *"그 값을 고칠 수 있는 사람이 …"* — **호출 경계가 문면에 아예 없다.** 합:289 가 이것을 인정한다(*"「부르는 쪽」이라는 낱말이 없고"*), 그래서 합:49 의 「18/18」은 그 문서 안에서 이미 반증돼 있다.
- `fd4efc1eaea040c4`(`패킷:93`) *"읽는 표면이 그 값을 함께 실어야 하고 …"* — 의무가 결정→표면 방향이다.

그리고 초안이 그 표시를 정의한 문장이 정:125 다 — *"「참 (약)」 = ⒝ 가 **명시된 조건절 없이** 의무·경계·지배의 형태로만 선 행이다. **7 행이고 §㈏ 가 그 읽기를 진다.**"* 합은 처방 2 에서 **㈏ 를 철회**하라고 적었으면서(합:250) 그 7 행의 값은 *"초안의 표기를 그대로 옮겼다"*(합:145)로 남겼다. **철회한 읽기가 지던 값이 판정 표에 그대로 남아 있다.** 결과로 합:178 의 「넓힌 행 `P 거짓` = 0」과 합:181 의 ★ 문장(*"넓힌 18 행은 한 행도 안 죽는다"*)은 **잰 값이 아니라 기준을 갈아 쓴 값**이다. 한 기준을 끝까지 대면 분포는 21/16 이 아니라 14/23 이고 그 23 중 7 이 **넓힌 쪽**이다. ⚠ 합:289 가 *"더 엄격히 읽으면 P 거짓이 넓힌 18 행으로도 번진다 … 바뀌는 것은 갈래별 분포다"* 로 이 가능성을 **선언했다** — 그러므로 새로운 것은 ㉮ 「그 수가 셋이 아니라 일곱이고 초안이 이미 ㈏ 에 결박해 둔 그 일곱이라는 것」과 ㉯ 「★ 를 붙인 합:181 은 안 감싸고 「내가 못 정한 것」 아래에서만 감쌌다는 배치」다. 원장과 커밋 제목이 인용하는 문장은 합:181 이다.

**항 ⑶ (반론 8 의 두 문장).** 합:161 — *"순수성은 호출자 무관을 뜻하지 않고 … **문면이 세우는 방향은 오히려 바깥에서 안으로다**."* 합:168 — *"⚠ **어긋난 방향이 아니라 방향이 없어서 거짓**이다."* 한 행에서 둘은 같이 못 선다. 방향이 「바깥에서 안으로」 선다면 ⒝ 의 답은 **있고** 그 답은 `판정 = 아니다` 와 반대이므로 실패는 ⒞ 다. P 는 논리곱이라 값은 같지만, 기록되는 **실패의 종류**가 「물음에 답하지 않았다」에서 「근거와 판정이 어긋난다」로 바뀐다 — 합이 범위 밖으로 보낸 그 물음에 한 발을 들이는 자리다. 같은 구조가 `49714573e0fa654c`(*"선언 하한 이후 끝난 회차 전부"*) · `969991a119d4c1ef`(*"`report.md` 의 커밋 시각"*) · `f4ba084c7a3838be` 에도 있다.

**항 ⑷ (반론 9).** 술어 ⒞ 의 문면은 *"⒝ 의 답이 같은 행 `판정` 열의 값과 같은 방향이다"* 다. ⒝ 의 답이 없는 행에서 이 명제는 **어긋남이 아니라 미정의**다. 합은 그것을 「거짓」으로 채우고(합:107 등 16 행) 그 근거를 초안이 위조 ㉠㉡ 에 쓴 관행에서 가져왔다(합:168) — 술어 정본이 아닌 **초안의 관행**이 값의 출처다. 결과 표에서 16 행이 다리 **둘**을 잃은 것으로 읽히고, 합 자신이 ㊁ 에서 그 행의 ⒞ 를 *"반증 불가"* 라 부른 것과 같이 못 선다. 「해당없음」이나 「미측정」이 정직한 값이고, P 는 안 변한다.

---

## 내가 스스로 물린 것

- **「합의 ⒝ 읽기가 술어 밖에서 왔다」를 세우려다 못 세웠다.** ⒝ 의 셋째 절이 괄호로 방향을 명시하고 *"문면에 서야 한다"* 로 못 박는다. 합의 읽기는 보탠 것이 아니라 문면이다. **㊀ 은 안 무너졌다.**
- **16 행 중 ⒝ 를 참으로 되살릴 행을 찾았고 하나도 없었다.** 되살림 후보 넷(`b52057859bf405eb`·`49714573e0fa654c`·`969991a119d4c1ef`·`f4ba084c7a3838be`)은 전부 바깥을 **데이터 축**으로만 세워서, ⒝ 를 참으로 읽으면 그 답이 `판정 = 아니다` 와 반대가 되어 ⒞ 에서 죽는다. **`P 참` 으로 가는 길이 없다 — 판정값을 뒤집을 근거는 없다.**
- **「⒜ 37/37 참이 합의 독립 측정이 아니다」를 냈다가 물렸다.** 합:291 이 *"내가 독립으로 다시 잰 것은 일곱이다"* 로 스스로 선언했다. 새롭지 않다. 그리고 ⒜ 를 더 엄격히 재도 `P 거짓` 만 늘어 판정값은 그대로다.
- **금지역 ㉱ 위반을 걸려 했으나 못 걸었다.** 합은 행별 근거 16 항을 실제로 적었고, 나머지 21 행은 초안의 항을 이월했음을 합:291 에 적었다. 「행별 근거 없이 적었다」가 안 선다.
- **㉮·㉯ 침범을 찾았으나 없다.** `widen/decisions.tsv` 와 `.palimpsest/intent/bindings.jsonl` 은 작업 트리에서 변경 0 이고, 합은 처방 전부를 `3-thesis.md` 산문으로 한정했다(합:260). `git status --porcelain` 0 줄.
- **해악도를 올리고 싶었으나 내린 자리** — 반론 3·4 는 원장에 갈 ★ 문장을 흔들지만 합:289 가 같은 가능성을 선언해 두었으므로 「사실이 아닌 것을 사실로」로 올리지 않고 `거짓신호` 로 뒀다. 반론 1 만 `금지역` 이다 — 그 문장은 어디에서도 단서가 붙지 않았고 정본이 글자로 반증한다.