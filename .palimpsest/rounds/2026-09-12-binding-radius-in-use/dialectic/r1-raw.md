# 정반합 취합 — 2026-09-12-binding-radius-in-use 라운드 1 · ㈂(#58 의 「대조 불가」 둘)를 이 회차의 완성선에서 빼는가, 뺀다면 둘 중 얼마를 빼며 그 처분은 규약 §5 의 넷 중 무엇인가

> ⚠⚠ **이 파일을 친 손은 메인이 띄운 전사 에이전트다 — 글은 `pal-debate-reporter` 의 것이고 한 글자도 안 고쳤다.** 그 자리가 읽기 전용이라 파일을 못 썼다.

> 낸 자리: `pal-debate-reporter`. **읽기 전용이다 — 판정을 다시 하지 않았다.**
> 받은 것 — 설계문(`1-design.md`) · 정(`1-thesis.md`) · 반 둘(`1-antithesis.md`·`1-antithesis-r2.md`) · 합 둘(`1-synthesis.md`·`1-synthesis-r2.md`) · 종료 판단 둘(`1-referee.md`·`1-referee-r2.md`) · 라운드 사이 측정 셋(`observations/r2-exact-axis.md`·`r2-bench-ratio.md`·`r2-issue-63.md`)의 **전문** · 회차 이름 · 발견 원장 스키마.
> 안 받은 것 — 대화 기록 · 메인의 사고 과정 · 이 판정을 실행할 계획.
> 아래 판정 문장 · 반론 문장 · 채택/기각 근거 · 좌표는 **원문 그대로**다. 줄이지 않았다.
> 판 번호는 **1**(출처 `정반합` 의 라운드 1). 라운드는 **2 를 다 썼다**(상한 소진).

**모집단을 매긴 규칙 — 내가 정했고, 그래서 적는다.** 반론이 치는 대상이 이 판의 산출물 문장(`1-thesis.md`·`1-synthesis.md`)이면 `회차기록`, 등록된 합격선(`corpus/criteria.toml`)·게이트 문서(`docs/gates/F09.md`)·코드(`crates/`)의 사실이면 `저장소`, 재는 장치(`scripts/f09-verify.py`)의 성질이면 `자기장치`, `intent.md` 의 문면이면 `원의도`, `.claude/skills/round/SKILL.md` 의 문면이면 `규약`. **유효성·해악도는 내가 안 매겼다** — 반(反)이 매기고 합(合)이 고친 값을 그대로 옮겼고, 둘이 갈린 자리는 항마다 ⟨⟩ 로 적었다.

**정본이 바뀐 자리 하나 — 이 판은 「라운드 1 의 합」을 라운드 2 의 초안으로 삼았다.** 설계문 `1-design.md:175` 가 *"**2 라운드에서 정(正)을 다시 부르지 않는다.** 정은 앞 라운드의 반론을 안 받으므로 같은 입력에 같은 초안이 나온다. 대신 **합(合)의 판정문을 초안으로 삼아 반(反)을 새로 부른다**"* 로 미리 정했다. 그래서 라운드 2 의 「초안」은 `1-thesis.md` 가 아니라 `1-synthesis.md` 다.

---

## 판정

합(合) 라운드 2 가 산출한 것 그대로 ⟨`1-synthesis-r2.md:27-47`⟩:

> ## 판정
>
> **수정**
>
> ### 물음에 대한 답 — **두 절반 각각**
>
> **⑴ `callers` 반경 거짓 양성률 — 이 회차의 완성선에서 「닫는다」를 뺀다. 처분은 「축소」이고, 등록된 차선책이 그대로 적용되므로 승격하지 않는다.**
>
> 근거는 `intent.md` 「차선책」 첫 항의 조건 ③ 이 **발화했다**는 것이고, 그 발화를 **등록 실행체 자신의 산출**이 세운다:
>
> - `ditto(exact)` — 두 반경의 표본 10 줄이 글자까지 같다(`stale 0`). R2 와 반(反)이 각각 돌려 일치.
> - `portal(ordinal)` — 두 반경의 표본 10 줄이 글자까지 같다(`stale 7`). 반(反)과 **내가** 각각 돌려 일치.
> - 상한 없는 전수 궤적에서도 `stale`∪`orphaned` 의 첫 전이 집합이 두 반경에서 id·커밋까지 같다. `callers` 가 더한 감시 원소는 이 창에서 `stale` 을 하나도 더 켜지 않았다.
>
> 차선책의 문면이 이 경우의 행위를 이미 지정한다 — *"거짓 양성률은 「대조 불가」로 다시 적고 … 그것을 통과로 세지 않는다."* 규약 §5 는 축소를 *"등록된 차선책이면 그대로 쓴다. 아니면 승격"* 으로 가른다. **초안의 「승격한다」를 여기서 뒤집는다.** 초안이 승격 근거로 쓴 둘 — 「조건 ③ 이 절반만 섰다」(라운드 1 합) · 「`exact` 축은 미측정」(라운드 1 합의 기각 #11) — 은 **라운드 1 뒤에 난 측정으로 둘 다 소멸했다.**
>
> ⚠ **함께 적어야 참이 되는 단서 둘**(안 적으면 이 값이 거짓이 된다):
> - 두 반경은 **`undeterminable` 축에서 갈린다**(전수 궤적 1/20 · `a88539ce` · `watch_member_gone` · 첫 갈림 커밋 `a0db9e1a`). 등록 표본기는 `scripts/f09-verify.py:715` 의 갈래 필터로 **그 축을 구조적으로 못 본다.** 갈린 곳이 있는데 표본기가 못 보는 것이지, 아무 데서도 안 갈린 것이 아니다.
> - 이 저장소(palimpsest)에서는 `callers` 가 실제로 갈린다(결박 37 중 감시가 커지는 것 24 · 새 감시 심볼 173). 그런데 `corpus/criteria.toml` 의 `[f09.4].sample_selection` 규칙 1 이 모집단을 **코퍼스 둘**로 못 박았고 같은 절이 *"결과를 보고 이 규칙을 바꾸지 않는다 … 바꾼 뒤의 값은 통과의 근거가 될 수 없다"* 로 재등록 경로를 미리 닫았다. **그래서 「이 저장소로 재면 된다」는 길이 아니다.**
>
> **⑵ 반경별 비용 벤치 — 내가 안 정한다. 이 절반만 소유자에게 올린다.** 아래 「소유자에게」.

**값이 세 자리에서 움직였다 — 정(正) ↔ 합(合) R1 ↔ 합(合) R2**

| 잰 것 | 정(正) ⟨`1-thesis.md:22-32`⟩ | 합(合) R1 ⟨`1-synthesis.md:26`·`:67`⟩ | 합(合) R2 ⟨`1-synthesis-r2.md:29`·`:33`·`:47`⟩ |
|---|---|---|---|
| 거짓 양성률 절반 | **뺀다**(`#58` 로) | 「수정」 — 값은 안 고정하고 **소유자에게 올린다** | **「닫는다」를 뺀다 · 축소 · 등록된 차선책 적용 · 승격 없음** |
| 벤치 절반 | **둔다** | 같은 물음에 안 담김 | **내가 안 정한다 — 이 절반만 소유자에게 올린다** |
| 처분 라벨 | **축소**(자 = 「#58 이 남긴 대조 불가 둘 중 이 회차가 닫는 수」 = 2 → 1) · **승격 필요** | 승격(소유자에게) | ⑴ 축소·승격 없음 / ⑵ 소유자에게 |
| 소유자에게 올리는 절반 | 거짓 양성률 | 거짓 양성률 | **벤치** ⟨*"올린다 — 그러나 초안이 올리려던 것과 다른 절반을 올린다"*⟩ |

---

## 반론 처리 — 전부 (22 항)

### 판 1 · 라운드 1 · #1 — 초안이 *"등록된 선정 규칙 4 를 그대로 적용했다"* 며 게이트에 남기겠다고 한 문장이 **등록의 실행체와 다른 절차**다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `scripts/f09-verify.py:265-296`(`심볼_고르기`, `파일_안에서=`) · `:660-676`(`변경된` 산출 + `결박_걸기`) · `:58`(`MIN_BINDINGS=20`) · `:73`(`HISTORY_WINDOW=120`) ↔ `1-thesis.md:148-160`·`:318`
- 반론 원문: *"초안이 *"등록된 선정 규칙 4 를 그대로 적용했다"* 며 게이트에 남기겠다고 한 문장이 **등록의 실행체와 다른 절차**다 — 등록된 표본기는 대장 전량이 아니라 **창 안에서 바뀐 파일의 심볼**만, **이름이 유일한 것**만, `간격=len(유일)//40` 으로 후보 40 을 뽑아 그중 걸리는 20 에 건다"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:32`⟩: **채택.** *"`scripts/f09-verify.py:265-296`·`:660-676` 을 직접 읽었다. 등록 실행체는 대장 전량이 아니라 **창 안에서 바뀐 파일의 심볼**을 **이름이 유일한 것**만, `간격=len(유일)//40` 으로 뽑는다. 초안이 돌린 것은 핀의 대장 전량 균등간격이다. 결론(portal 0)은 `edges 0` 때문에 안 움직이지만, **「등록 규칙을 그대로 돌렸다」는 출처 진술이 거짓**이고 그 문장이 게이트와 `#58` 에 적힐 예정이었다"* — 라운드 2 의 반(反)도 이 자리를 다시 확인하고 *"채택 #1 이 옳다 … 고침 3 은 선다"* 로 무너뜨리지 못했다 ⟨`1-antithesis-r2.md:29`⟩

### 판 1 · 라운드 1 · #2 — **`exact`(ditto) 축은 등록 모집단에서 살아 있는데 초안은 두 축 전부에 대해 「이번에도 대조 불가」를 적는다**

- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역 ⟨반·합 같다 — 반(反)이 *"사실이_아닌_것을_사실로 — 안 잰 축을 「대조 불가」로 적는다"* 로 매겼다⟩
- 어디가 걸리나: 반(反)의 명령 산출 · `scripts/f09-verify.py:801-804`(코퍼스마다 따로 부른다) ↔ `1-thesis.md:318`(게이트 문장 머리)
- 반론 원문: *"**`exact`(ditto) 축은 등록 모집단에서 살아 있는데 초안은 두 축 전부에 대해 「이번에도 대조 불가」를 적는다** — 내가 등록 절차대로 돌렸다: `START=d6ee4ad65cb8`(핀에서 비머지 120 뒤), 바뀐 `.ts` **576** 파일, 그 안의 심볼 **2204**, 유일·균등 후보 40 중 앞 20 의 호출자 = `[0,0,1,2,0,2,0,0,0,0,2,0,0,0,2,2,0,0,0,1]` → **≥1 이 7 · ≥2 가 5**. 결박 시점 그래프도 직접 떴다(`nodes 5239 · edges 4487`). 즉 `exact` 축에서 `callers` 감시 집합은 `symbol` 과 **갈린다.** 초안은 그 축을 한 번도 안 시도하고 판정을 적는다"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:33`⟩: **채택 · 금지역.** *"내가 등록 절차로 직접 돌렸다(위 표). 시작 커밋의 2층에 엣지가 있고 표본에 호출자 ≥1·≥2 가 둘 다 있다. **`callers` 감시 집합이 `symbol` 과 갈리는 표본이 `exact` 축에 실재한다.** 그런데 초안의 「기록될 문장」(`1-thesis.md:318`)은 머리를 *"`callers` 는 이번에도 대조 불가다"* 로 열고 근거를 `ordinal` 축에서만 댄다. **안 잰 것을 잰 것으로 적는 형태**이고, 이 회차가 스스로 등록한 금지역(`intent.md:133` 사실이_아닌_것을_사실로)에 정면으로 든다"* — ⚠ **이 항이 라운드 2 에서 뒤집혔다.** 라운드 2 반론 #1 이 등록 실행체 산출로 *"「못 잰다고 적을 근거」는 등록 실행체 자신의 산출로 실재한다"* 를 세웠고 합(合) R2 가 그것을 금지역으로 채택했다

### 판 1 · 라운드 1 · #3 — **벤치의 등록 합격선이 구성상 항진명제**이고 초안은 그것을 안 열어 봤다

- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역 ⟨반·합 같다 — 반(反)이 *"측정이_죽은_가지 — `intent.md:138` 이 지목한 그 형태"* 로 매겼다⟩
- 어디가 걸리나: `crates/pal-intent/src/store.rs:241-270` · `:369-386` · `corpus/criteria.toml:7361-7371`(`bench_grounds` — 재는 것 넷만 적고 부하 규정 없음) ↔ `1-thesis.md:321`·`:466`
- 반론 원문: *"**벤치의 등록 합격선이 구성상 항진명제**이고 초안은 그것을 안 열어 봤다 — `bindings_watching(changed)` 은 `changed` 만 멀티맵 역조회하고, `all()` 은 `BINDING` 전수 훑기 + 정렬이다. `changed` 집합 크기가 **등록 어디에도 안 적혀 있으므로** 1 로 잡으면 *"증분이 전체보다 빠르다"* 가 **잴 것 없이 참**이다. 초안은 *"두 함수가 실재한다"* 에서 *"닫을 수 있다"* 로 바로 갔다"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:34`⟩: **채택 · 금지역.** *"`corpus/criteria.toml` 의 `bench_grounds` 전문을 열었다 — 재는 것 넷·두 규모·회차 3 은 적혀 있는데 **`changed` 집합 크기가 어디에도 없다.** `crates/pal-intent/src/store.rs:241-270` 은 `changed` 만 역조회하고 `:369-386` 은 `BINDING` 전수 훑기 + 정렬이다. `changed` 를 1 로 잡으면 *"증분이 전체보다 빠르다"* 가 **잴 것 없이 참**이다. 초안은 *"두 함수가 실재한다"* 에서 *"닫을 수 있다"* 로 바로 갔다(`1-thesis.md:321`). 닫으면 **측정이 죽은 가지**를 닫았다고 적게 된다"* — 라운드 사이 측정 `observations/r2-bench-ratio.md:30` 이 그 자리의 실제 값을 냈다(`changed`=1 에서 **32.20×**)

### 판 1 · 라운드 1 · #4 — **「죽은 가지가 된다」를 떠받치는 하한이 등록된 합격선이 아니다**

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `corpus/criteria.toml:6712-6716` · `:6832-6836`(*"등급별로 갈라 잰다 … 뭉치면"*) · `scripts/f09-verify.py:680-688` ↔ `1-thesis.md:162`·`:318`
- 반론 원문: *"**「죽은 가지가 된다」를 떠받치는 하한이 등록된 합격선이 아니다** — 그 문장은 `control_off_modes` 안의 **후보**이고 스스로 *"게이트에 열다섯째로 적을지는 판정 시점에 정한다"* 고 적었다. `docs/gates/F09.md` 는 그것을 안 채택했고(`grep 열다섯` → `:402` 한 건, 무관), `실_이력` 이 거는 하한 둘은 (결박 ≥ 20)·(결박 직후 전부 fresh)뿐이라 **감시 집합 크기 하한은 구현이 없다.** 게다가 등록은 등급 축을 갈라 적으라고 못 박았으므로 「ditto 7 건이 채워서 가려진다」는 뭉치기 자체가 안 일어난다"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:35`⟩: **채택.** *"절반 맞고, **내가 연 것은 반(反)보다 세다.** `corpus/criteria.toml:6712-6716` 은 *"게이트에 열다섯째로 적을지는 판정 시점에 정한다"* 는 **후보**가 맞다. 다만 같은 취지의 음성 대조가 `:7019-7026` 에 **등록돼 있다**(*"`callers` 가 `symbol` 보다 커야 한다 — 표본 중 하나 이상에서. 전부 같으면 … 엣지가 안 붙은 것이다"*). 그리고 `false_positive_split_by_identity_grade = true` 와 `split_grounds` 가 **등급 축을 갈라 적으라고 못 박았다.** 갈라 적으면 초안이 두려워한 *"ditto 7 건이 하한을 채워 ordinal 20 건이 통째로 죽은 가지가 된다"* 는 **일어나지 않는다** — 갈라 적는 순간 `ordinal` 축에서 대조가 스스로 멈춘다. **초안이 「빼야 한다」를 지탱하려고 댄 가장 센 근거가 여기서 무너진다"* — ⚠ 이 채택의 **결정타 부분이 라운드 2 반론 #3 에서 거짓으로 판정됐다**(합 R2 가 *"그래서 초안이 「초안의 가장 센 근거가 여기서 무너진다」고 쓴 것은 과장이다 — 무너지지 않는다"* 로 채택)

### 판 1 · 라운드 1 · #5 — **초안이 ⑪ 을 「반증」으로 세면서 자기 산출이 ⑪ 의 정의를 재현한 것을 안 센다**

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `premortem/r1-raw.md:158-159` · 반(反)이 돌린 세 명령 ↔ `1-thesis.md:195-197`·`:200`·`:346`·`:353`
- 반론 원문: *"**초안이 ⑪ 을 「반증」으로 세면서 자기 산출이 ⑪ 의 정의를 재현한 것을 안 센다** — ⑪ 은 *"벤치 인프라 0"* 을 네 조회로 정의했고(`criterion`·`[[bench]]`·`benches/`·CI), 초안의 M6 이 그 넷을 다시 돌려 **전부 0 건**을 얻었다. 나도 재현했다(`grep -rn "criterion\|\[\[bench\]\]" --include=Cargo.toml .` → rc=1 · `grep -rn bench .github/workflows/` → rc=1 · `find … -name benches` → 0 건). 그런데 초안은 *"둘 다 거짓이다"* 로 적고 유효 일곱을 다섯으로 줄여 그 차이를 판정 근거로 쓴다"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:36`⟩: **채택.** *"`criterion`·`[[bench]]`·`benches/`·CI 넷이 전부 0 건인 것은 초안의 M6 자신이 냈다. 그런데 초안은 *"둘 다 거짓이다"* 로 적고 유효 일곱을 다섯으로 줄인 뒤 **그 축소분을 판정 근거로 썼다**(`1-thesis.md:346`·`:353`)"*

### 판 1 · 라운드 1 · #6 — **`crates/pal-query/tests/bench.rs` 는 F09 의 넷을 안 잰다 — F05 의 넷을 잰다.**

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `crates/pal-query/tests/bench.rs:1`·`:171-209` · `corpus/criteria.toml:4874-4877` · `corpus/criteria.toml:7367-7368` · `crates/pal-query/Cargo.toml:9-13` · `cargo test -p pal-query --release --test bench -- --ignored` 산출 ↔ `1-thesis.md:200-207`
- 반론 원문: *"**`crates/pal-query/tests/bench.rs` 는 F09 의 넷을 안 잰다 — F05 의 넷을 잰다.** 파일 머리가 *"4종 벤치 + 선형성 — `[f05.3.pass]` ④"* 이고, 내가 돌린 산출도 `① 심볼 조회 ② 1홉 역방향 ③ 3홉 BFS ④ 전체 재구축` 이다. `[f09.4.pass]` 가 요구한 넷은 `symbol·callers·closure(2)·증분 갱신` 이고, 초안이 「파일 경로를 지정한 등록」이라 지목한 자리도 열어 보면 **F05 의 4종을 위한 지정**이다. 거기에 `pal-query` 는 `pal-intent` 의존이 없다. 그러므로 ⑪ 의 실질(*"표본기 + 벤치라는 새 장치를 처음부터 짓는 일"*)은 좌표 둘이 틀렸어도 **살아 있고**, 초안은 그것을 「벤치를 남기는 근거」로 뒤집어 쓴다"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:37`⟩: **채택.** *"직접 열었다. 파일 머리가 *"4종 벤치 + 선형성 — `[f05.3.pass]` ④"* 이고 `:171-209` 의 단언이 `심볼 조회·1홉 역방향·3홉 BFS·전체 재구축`이다. `[f09.4.pass]` 가 요구한 넷(`symbol·callers·closure(2)·증분 갱신`)과 **겹치는 항이 없다.** `crates/pal-query/Cargo.toml` 에 `pal-intent` 의존이 없어 그 자리에 들어가지도 못한다. **초안이 「벤치를 남기는 근거」로 쓴 물건이 다른 게이트의 물건**이다. ⑪ 의 좌표 둘이 틀린 것은 맞지만 ⑪ 의 실질(*"새 장치를 짓는 일"*)은 살아 있다"*

### 판 1 · 라운드 1 · #7 — **㉳(등록된 차선책의 문면 그 자체)를 떨어뜨린 근거가 「비용」이다**

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨합이 *"해악도는 반(反)의 것을 유지한다 — 다만 이 항은 해악도와 무관하게 「철회·축소」 급소에 걸린다"* 를 덧붙였다⟩
- 어디가 걸리나: `1-thesis.md:373`·`:379` · `.claude/skills/round/SKILL.md:324`·`:469`
- 반론 원문: *"**㉳(등록된 차선책의 문면 그 자체)를 떨어뜨린 근거가 「비용」이다** — 초안은 ㉳ 의 결과 문장이 자기 값과 *"거의 같다"* 고 인정하고서 *"비용이 다르다 … 다섯 번째로 … 같은 문장을 적는다"* 로 뺐다. 규약은 「어렵다·시간이 없다」류를 이월 사유로 못 박아 배제했고, 하필 그 칸이 **등록된 차선책에 가장 가까워 승격이 필요 없던 칸**이다. 즉 이 초안은 승격이 필요 없는 길을 노력 사유로 버리고 승격이 필요한 길을 골랐다"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:38`⟩: **채택.** *"`1-thesis.md:379` 가 *"비용이 다르다 … 다섯 번째로 … 같은 문장을 적는다"* 로 뺐다. 규약(`.claude/skills/round/SKILL.md`)이 **이월 사유가 아닌 넷**으로 *어렵다·시간이 없다·컨텍스트가 모자란다·생각보다 넓다* 를 못 박았다. 반론 2·4 가 *"결론은 어차피 같다"* 부분을 무너뜨리고 나면 **남는 것은 비용뿐**이고, 그것은 규약이 배제한 사유다. 게다가 ㉳ 가 **승격이 필요 없던 유일한 칸**이다"*

### 판 1 · 라운드 1 · #8 — **「축소」 라벨을 고르면서 ㈁ 선례를 안 쓴 이유를 한 줄도 안 댔고, 내 실측이 선례와 같은 형태를 가리킨다**

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `crates/pal-extract/src/kotlin.rs:157-161` · `intent.md:149` ↔ `1-thesis.md:14`(*"읽었다 / 안 썼다"* 만 적고 근거 없음)
- 반론 원문: *"**「축소」 라벨을 고르면서 ㈁ 선례를 안 쓴 이유를 한 줄도 안 댔고, 내 실측이 선례와 같은 형태를 가리킨다** — `ordinal` 축이 못 재는 원인은 이 회차의 범위가 아니라 `F02` 의 능력 부재(`kotlin-exports`·`kotlin-imports`·`kotlin-scopes` 가 `not_built`)다. ㈁ 이 `F05` 소속이라 빠졌을 때 소유자 개정은 그것을 *"축소가 아니라 정정"* 으로 라벨했다. 같은 자를 대면 이 값도 **정정** 후보이고, 그러면 승격 부담과 원장 표기가 달라진다"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:39`⟩: **채택.** *"`1-thesis.md:14` 는 *"읽었다 / 안 썼다"* 만 적고 근거 칸이 비었다. 설계문이 **성립 주장도 불성립 주장도 근거가 필요하다**고 미리 걸어 둔 자리다. 그리고 선례의 실제 작동은 「라벨을 정정으로 바꾼다」가 아니라 **「빠진 사실을 소유자에게 내고 소유자가 고른다」**(`intent.md:149`)이다 — `ordinal` 축이 `F02` 의 Kotlin 능력 부재로 못 재는 축이라는 사실은 `intent.md` 의 착수 관측표에 **없다**"*

### 판 1 · 라운드 1 · #9 — **행선지를 `#58` 로 잡은 것이 소유자가 고른 (나) 분할의 문면과 어긋난다**

- 모집단: 원의도
- 유효성: 거짓 ⟨반(反)은 「참」으로 냈고 합(合)이 *"문면 충돌이 없다"* 로 기각했다 — 갈렸다⟩
- 해악도: 거짓신호 ⟨반(反)이 매긴 것. 합(合)은 기각했으므로 해악도를 안 냈다⟩
- 어디가 걸리나: `intent.md:127-128` ↔ `1-thesis.md:322`
- 반론 원문: *"**행선지를 `#58` 로 잡은 것이 소유자가 고른 (나) 분할의 문면과 어긋난다** — `intent.md:127` 의 괄호가 *"이 회차가 만드는 장치(**#58 의 표본기·벤치**)"* 라고 `#58` 을 이름으로 지목했다. 초안은 *"만든 장치가 아니라 못 만든 측정이라 `#58` 이 정본"* 이라며 그 분할 밖으로 뺐는데, 문면은 `#58` 의 표본기·벤치를 **분할 안**으로 적었다"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:47`⟩: **기각.** *"`intent.md:127-128` 의 문장은 *"이 회차가 만드는 장치(#58 의 표본기·벤치)**에 대한 발견**은 별개 의도로 보고 이슈로 세운다"* 다. 모집단이 **「장치에 대한 발견」**이지 **「완성선 항목의 행선지」**가 아니다. 둘은 다른 모집단이고, 설계문이 *"뺀 것이 #58 로 돌아간다를 **행선지로 적는 것**은 이 판 안"* 이라고 따로 열어 두었다. 문면 충돌이 없다"* — 라운드 2 의 반(反)도 *"기각 #9 가 옳다"* 로 동의했다 ⟨`1-antithesis-r2.md:32`⟩

### 판 1 · 라운드 1 · #10 — **자가 §5 가 지정한 자가 아니다**

- 모집단: 규약
- 유효성: 추정
- 해악도: 미관 ⟨반·합 같다⟩
- 어디가 걸리나: `.claude/skills/round/SKILL.md:329` · `gh issue view 58`(*"우선순위 P2 · 의존 없음 · 규모 S"*) ↔ `1-thesis.md:424-432`
- 반론 원문: *"**자가 §5 가 지정한 자가 아니다** — 규약의 자는 *"그 검사가 재는 의도의 양"* 인데 초안은 **항목 수**(닫는 「대조 불가」 수)로 셌다. `#58` 은 벤치 절반을 **규모 S · P2 · 의존 없음**으로 적었고 거짓 양성률 절반은 네 회차가 못 넘은 벽이다. 두 절반을 1:1 로 세면 2→1 이 **재는 의도의 양이 절반으로 줄었다**를 뜻하지 않는다"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:40`⟩: **채택.** *"규약의 자는 *"그 검사가 재는 의도의 양"* 인데 초안은 **항목 수**로 셌다. 방향(줄었다)은 안 바뀌지만, 두 절반을 1:1 로 세는 것이 근거 없이 들어갔다"* — 합(合) R2 가 *"고침 8(자)은 살리되 이번 판정이 **두 절반 각각에 처분을 붙이므로** 「항목 수」 자는 안 쓴다"* 로 처리했다 ⟨`1-synthesis-r2.md:81`⟩

### 판 1 · 라운드 1 · #11 — **차선책 조건 ③ 이 「절반만 섰다」가 아니라 「미충족 확정」이다**

- 모집단: 원의도
- 유효성: 추정
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `intent.md:96-99` · 반론 2 의 산출 ↔ `1-thesis.md:442`·`:454`
- 반론 원문: *"**차선책 조건 ③ 이 「절반만 섰다」가 아니라 「미충족 확정」이다** — 반론 2 의 수가 나온 이상 *"그래도 안 갈리면"* 은 발화하지 않았다. 초안은 조건 ③ 을 미결로 남긴 채 차선책의 **결론만** 채택했고, 그 사실이 승격 물음의 문면을 바꾼다(지금 문면은 *"portal 20/20"* 만 붙이고 ditto 축을 안 붙인다)"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:41`·`:48`⟩: **몫 채택 · 몫 기각.** 채택 — *"핵심은 채택한다 — **조건 ③(*"그래도 안 갈리면"*)이 발화하지 않았으므로 차선책의 결론을 원용할 수 없다.** 다만 「미충족 **확정**」이라는 문면은 안 받는다"*. 기각 — *"반(反) 스스로 *"두 반경의 **판정**(stale/fresh)이 갈리는지는 나도 안 쟀다"* 고 물렸고, 내 실측도 **감시 집합이 갈린다**까지만 세운다. 그러므로 조건 ③ 의 상태는 「미충족 확정」이 아니라 **미측정**이다. 초안의 「절반만 섰다」도 틀렸고 반(反)의 「확정」도 틀렸다 — 정확한 말은 *"`ordinal` 축은 미충족 확정 · `exact` 축은 미측정"* 이다"* — ⚠ **이 항의 채택분이 라운드 2 에서 뒤집혔다**(라운드 2 반론 #7)

### 판 1 · 라운드 2 · #1 — **고침 1·2 의 핵심 문장이 등록 실행체의 산출로 반증된다**

- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역 ⟨반·합 같다 — 반(反)이 *"사실이_아닌_것을_사실로 · 측정이_죽은_가지"* 로 매겼다⟩
- 어디가 걸리나: `1-synthesis.md:54`·`:55` ↔ 반(反)의 실행: `f09-verify.py` 의 `실_이력(tmp, DITTO, DITTO_PIN, ".ts", "ditto(exact)", "symbol"/"callers", …)` → `표본 10 · stale 0 · orphaned 10` 두 번 동일 · `관측된_사유 []`
- 반론 원문: *"**고침 1·2 의 핵심 문장이 등록 실행체의 산출로 반증된다** — 합(合)은 *"`exact` 축 — **못 잰다고 적을 근거가 없다.** 등록 절차로 뽑은 표본에 호출자가 붙는다"* 로 적고 *"「이번에도 대조 불가」로 두 축을 덮는 문장을 지운다"* 를 지시했다. 내가 등록 실행체를 그대로 돌렸다 → `ditto(exact)/symbol` 과 `/callers` 의 표본 10 줄이 **결박 id·구간·갈래·경로·커밋 제목까지 전부 같고 `stale 0`** 이다. 즉 **못 잰다고 적을 근거는 등록 실행체 자신의 산출로 실재한다.** 합(合)은 *"감시 집합이 갈린다"*(참, 20→27)에서 *"표본이 갈린다"*(거짓)로 건너뛰었다 — **채택 #2 에서 초안에게 금지역으로 매긴 바로 그 형태**(안 잰 것을 잰 것으로)를 반대 방향으로 저질렀고, 그 문장이 게이트와 `#58` 에 적힐 예정이다"*
- 채택·기각과 근거 ⟨합 `1-synthesis-r2.md:53`⟩: **채택 · 금지역.** *"등록 실행체를 두 팀(R2·반)이 각각 돌려 `표본 10 · stale 0 · orphaned 10` 이 글자까지 같았다. 「못 잰다고 적을 근거」는 등록 실행체 자신의 산출로 **실재한다.** 초안이 라운드 1 에서 초안에게 금지역으로 매긴 형태(안 잰 것을 잰 것으로)를 **반대 방향으로** 저지른 것이고, 그 문장이 게이트와 `#58` 에 적힐 예정이었다"*

### 판 1 · 라운드 2 · #2 — **고침 1 의 *"`ordinal` 축은 이 회차에서 **원리상 못 잰다**"* 가 실행체 산출과 어긋나고, 그 문장이 소유자 한 문장의 전제로 박혔다**

- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역 ⟨반·합 같다 — 반(反)이 *"사실이_아닌_것을_사실로 — 소유자 결정의 입력이다"* 로 매겼다⟩
- 어디가 걸리나: `1-synthesis.md:53`·`:73` ↔ 반(反)의 portal 실행 산출 · `docs/gates/F09.md:34`·`:173-191`(`4106a5a6`·`0949191a`·`a044bc6a`·`4d7c2dfe`·`8d5edfa7`·`bc556eb7`·`e63feb66` 일곱 줄)
- 반론 원문: *"**고침 1 의 *"`ordinal` 축은 이 회차에서 **원리상 못 잰다**"* 가 실행체 산출과 어긋나고, 그 문장이 소유자 한 문장의 전제로 박혔다** — 내가 `실_이력(portal(ordinal), symbol/callers)` 를 돌렸다 → **둘 다 `ok` · `표본 10 · stale 7 · orphaned 3`.** 그리고 그 표본은 `docs/gates/F09.md:173-191` 에 **이미 결박마다 한 줄로 판정돼 기록돼 있다**(거짓 양성 3/7 = 42.9% · 반증). 못 재는 것은 「`ordinal` 축」이 아니라 **「`callers` 를 `symbol` 과 독립으로」** 뿐이다. 합(合)의 한 문장은 *"`ordinal` 이 `F02` 의 Kotlin 능력 부재로 이 회차에서 원리상 못 재는 상태에서"* 를 소유자에게 **사실로 제시**한다 — `ordinal` 축 ⑤ 의 값이 이미 게이트에 적혀 있다는 사실을 빼고"*
- 채택·기각과 근거 ⟨합 `1-synthesis-r2.md:54`⟩: **채택 · 금지역.** *"**내가 직접 돌려 반증했다**(위 표). `portal/symbol`·`portal/callers` 둘 다 `ok · 표본 10 · stale 7`, 그 일곱은 `docs/gates/F09.md` 가 이미 42.9% 로 판정해 둔 바로 그 일곱이다. 못 재는 것은 축이 아니라 **독립성**이다. 소유자 결정의 입력에 거짓 사실을 넣는 것이므로 가장 무겁다"*

### 판 1 · 라운드 2 · #3 — **채택 #4 의 결정타 — *"갈라 적는 순간 `ordinal` 축에서 대조가 스스로 멈춘다"* — 가 거짓이다.**

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호 ⟨**갈렸다** — 반(反)은 **금지역**(*"측정이_죽은_가지 — 등록이 「막는다」고 적은 자리가 안 막고 통과를 냈다"*)으로 매겼고 합(合)이 *"금지역에서 내렸다 — 기각표에 근거"* 로 낮췄다⟩
- 어디가 걸리나: `1-synthesis.md:35` ↔ `corpus/criteria.toml:7024-7026` · `:6928-6930` · `scripts/f09-verify.py:756` · 반(反)의 portal 감시 합계 산출(`symbol 20 · callers 20 · closure:2 20`) · 반(反)의 portal `실_이력` 산출(`ok`)
- 반론 원문: *"**채택 #4 의 결정타 — *"갈라 적는 순간 `ordinal` 축에서 대조가 스스로 멈춘다"* — 가 거짓이다.** 합(合)이 연 `corpus/criteria.toml:7019-7026` 은 실재하지만 **그 하한은 이 경우에 원리상 못 걸린다**: 문면이 *"감시 집합 크기의 합이 **결박 수 미만**이면 멈춘다"* 인데 내가 잰 portal 값이 `symbol 20 · callers 20 · closure:2 20 / 결박 20` 이라 `20 < 20` 이 거짓이다. 그리고 *"`callers` 가 `symbol` 보다 커야 한다"* 를 실제로 검사하는 곳은 ⑦ `반경별` 하나뿐이고 그 함수는 **ditto 에서만 돈다**(`scripts/f09-verify.py:756`). 실측이 그것을 확증한다 — `edges 0` 인 portal 에서 `⑤ portal(ordinal)/callers` 가 **`ok` 로 나왔다.** 이것은 `corpus/criteria.toml:6928-6930` 이 *"넷 다 하한으로 막는다"* 고 등록한 넷 중 셋째(**「엣지 0 건인데 `callers` 반경이 통과」**)가 **구현이 없는 채로 방금 발생한 것**이다. 합(合)은 이 자리에서 *"초안이 「빼야 한다」를 지탱하려고 댄 가장 센 근거가 여기서 무너진다"* 고 적었는데, 무너지는 것은 합(合) 쪽이다"*
- 채택·기각과 근거 ⟨합 `1-synthesis-r2.md:55`·`:68`⟩: **사실 부분 채택 · 해악도 기각.** 채택 — *"직접 확인했다. `corpus/criteria.toml` 의 하한 문면은 *"감시 집합 크기의 합이 결박 수 미만이면 멈춘다"* 인데 portal 은 `symbol 20 · callers 20 · closure:2 20 / 결박 20` 이라 안 걸린다. *"`callers` 가 `symbol` 보다 커야 한다"* 를 실제로 검사하는 곳은 `scripts/f09-verify.py` 의 `반경별()` 하나뿐이고 그 함수는 **`DITTO` 에서만 돈다**(`사본(tmp, "radius", DITTO, DITTO_PIN)`). 그래서 초안이 *"초안의 가장 센 근거가 여기서 무너진다"* 고 쓴 것은 과장이다 — 무너지지 않는다"*. 해악도 기각 — *"**`ok` 는 판정이 아니다.** 그 `ok` 문자열 자신이 *"거짓 양성 판정은 게이트에 목록으로"* 라고 적고, `scripts/f09-verify.py` 의 `실_이력()` 독스트링이 *"거짓 양성의 판정은 이 스크립트가 안 한다"* 로 못 박았다. 그리고 그 자리의 **실제 판정은 이미 내려져 있고 「대조 불가」다**(`docs/gates/F09.md` §0 의 ⑤ `callers` 행). 그러므로 죽은 가지가 **원장에 통과로 적힌 적이 없다.** 사실 부분(하한이 안 걸린다 · ⑦ 이 ditto 전용이다)은 채택했고 해악도만 내렸다"*

### 판 1 · 라운드 2 · #4 — **고침 5 를 지금 실행하면 「값을 보고 합격선을 고르는 일」이 된다**

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호 ⟨**갈렸다** — 반(反)은 **금지역**(*"측정이_죽은_가지"*)으로 매겼고 합(合)이 *"금지역에서 내렸다 — 기각표에 근거"* 로 낮췄다⟩
- 어디가 걸리나: `1-synthesis.md:58` ↔ `corpus/criteria.toml:6400-6402` · `:7305-7309`(`sample_selection` 끝문단) · `:7370-7371`(합격선 문장) · 반(反)의 벤치 산출(`k=31 1.008 · k=32 0.976 · k=34 0.926`, 전 k 에서 회차 표준편차 ≤ 2.2k ns)
- 반론 원문: *"**고침 5 를 지금 실행하면 「값을 보고 합격선을 고르는 일」이 된다** — 합(合)은 *"`changed` 집합 크기를 최소 둘(예: **전체의 일부 · 전체**)로 못 박는다 … 규약상 정정(자유)이다"* 로 적었다. 내가 곡선을 다시 냈다: **k=31 에서 1.008× · k=32 에서 0.978× · k=W=34 에서 0.926×.** 즉 등록 합격선(*"증분 갱신이 전체 재계산보다 빠르다"*)은 **「전체」를 부하로 잡으면 실패한다.** 그러므로 고침 5 가 말한 두 부하 중 어느 쪽을 적느냐가 **통과/반증을 직접 결정**하고, 그 선택이 이 곡선을 **본 뒤에** 이뤄진다. `corpus/criteria.toml:6400-6402` 가 *"합격선을 재기 전에 확정한다 … **값을 보고 정하면 게이트가 아니다**"* 를 이 기능의 **가장 무거운 자리**로 등록했고 `[f09.4].sample_selection` 이 *"결과를 보고 이 규칙을 바꾸지 않는다. 바꾸면 … **바꾼 뒤의 값은 통과의 근거가 될 수 없다**"* 로 못 박았다. 합(合)의 「정정(자유)」 라벨은 이 조항을 안 통과한다"*
- 채택·기각과 근거 ⟨합 `1-synthesis-r2.md:56`·`:69`⟩: **라벨 부분 채택 · 해악도와 좌표 기각.** 채택 — *"라벨 부분만 채택한다. 부하를 `changed`=1 로 잡으면 초안 자신이 채택 #3 에서 *"잴 것 없이 참"* 이라고 인정한 항진명제가 되고, 그때 그 변경은 **재는 의도의 양을 줄이는 것(완화=축소)**이다. 즉 라벨이 부하 선택에 **의존**하는데 초안은 무조건 「정정(자유)」로 적었다. 규약 §5 의 자(*"고친 뒤 그 검사가 재는 의도의 양"*)가 답을 안 낸 채 라벨이 먼저 붙었다"*. 기각 — *"**좌표를 넓혀 읽었다.** `corpus/criteria.toml` 의 *"값을 보고 정하면 게이트가 아니다"* 는 문면이 **거짓 양성률과 `Undeterminable` 비율**을 지목하고, `sample_selection` 끝문단은 **⑤ 표본 선정 규칙**을 지목한다. 둘 다 ④ 벤치의 부하를 안 건다. 게다가 초안의 고침 5 는 부하 **둘(일부·전체)**을 요구하는데, 곡선상 「전체」는 실패 쪽이므로 **그 지시로는 통과를 제조할 수 없다.** 라벨 문제만 채택했다"*

### 판 1 · 라운드 2 · #5 — **㈂ 의 「둘 중 얼마」에서 벤치 절반이 소유자 한 문장에서 사라졌다**

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `1-synthesis.md:73` ↔ `.claude/skills/round/SKILL.md:464` · 판정할 물음의 문면(*"둘 중 얼마를 빼며"*)
- 반론 원문: *"**㈂ 의 「둘 중 얼마」에서 벤치 절반이 소유자 한 문장에서 사라졌다** — 판정할 물음은 *"둘 중 얼마를 빼며"* 인데 합(合)의 `## 소유자에게` 한 문장은 **거짓 양성률 절반만** 담고 벤치 절반의 처분을 안 담는다. 규약이 *"★ 승격은 물음 하나에 칸 하나다. 둘을 묶어 올리면 … **답 없는 물음이 처리된 것처럼 사라진다**(실측: 그렇게 한 번 사라졌다)"* 로 이 형태를 미리 적었다. 지금은 반대 방향의 같은 사고다 — 칸 하나를 올리면서 **다른 물음 하나를 안 올린 채 「둔다」로 남긴다**"*
- 채택·기각과 근거 ⟨합 `1-synthesis-r2.md:57`⟩: **채택.** *"판정할 물음이 *"둘 중 얼마를"* 인데 초안의 승격 물음은 거짓 양성률만 담았다. 규약이 *"승격은 물음 하나에 칸 하나 … 답 없는 물음이 처리된 것처럼 사라진다(실측)"* 로 이 형태를 미리 적었다. 이번 판정은 **두 절반 각각에 처분을 붙여** 이것을 닫는다"*

### 판 1 · 라운드 2 · #6 — **벤치 절반을 「닫는다」로 가져가면 등록된 것의 1/4 · 1/2 만 잰 것이고, 실행체가 저장소 밖이다**

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `1-synthesis.md:58` ↔ `corpus/criteria.toml:7360-7371` · `observations/r2-bench-ratio.md:14`·`:77-79` · 반(反)의 실행체(같은 문제: scratchpad standalone crate) · `find crates -name "bench*.rs"` → 1 건
- 반론 원문: *"**벤치 절반을 「닫는다」로 가져가면 등록된 것의 1/4 · 1/2 만 잰 것이고, 실행체가 저장소 밖이다** — `bench_grounds` 가 요구한 것은 **재는 것 넷**(`symbol`·`callers`·`closure(2)`·증분 갱신) × **두 규모** × 회차 3 이고, 형태는 *"`#[ignore]` 로 두고 `cargo test --release -- --ignored` 로 돌린다"* 이다. 재어진 것은 **증분 갱신 하나 · N=37 한 규모**뿐이고(R2 스스로 *"N=37 하나에서만 쟀다 … 두 규모의 선형성은 이 저장소 하나로는 못 낸다"* 고 적었다), **반경별 넷은 R2 도 나도 한 번도 안 돌렸다.** 그리고 두 실행체가 둘 다 scratchpad 의 standalone crate라 **저장소에서 재현할 수 없다** — `find crates -name "bench*.rs"` 는 `crates/pal-query/tests/bench.rs`(F05 의 것) 하나만 낸다. 합(合)의 고침 5 는 부하만 말하고 이 셋을 하나도 말하지 않는다"*
- 채택·기각과 근거 ⟨합 `1-synthesis-r2.md:58`·`:70`⟩: **채택(한 갈래 기각).** 채택 — *"직접 확인했다. `corpus/criteria.toml` 의 `bench_grounds` 는 **재는 것 넷**(`symbol`·`callers`·`closure(2)`·증분 갱신) × **두 규모** × 회차 3 을 요구하고 형태를 *"`#[ignore]` 로 두고 `cargo test --release -- --ignored`"* 로 못 박았다. 지금 선 것은 증분 하나 · 한 규모이고 실행체 둘이 다 scratchpad 다. `find crates -name "bench*"` 는 `crates/pal-query/tests/bench.rs`(F05 의 것) 하나만 낸다. **「닫는다」를 지금 적으면 안 잰 것을 잰 것으로 세는 것이다"*. 기각 — *"「두 규모는 이 저장소 하나로는 못 낸다」 … **선례가 반증한다.** `crates/pal-query/tests/bench.rs` 는 `[f05.3.pass]` ④ 를 **합성 그래프 두 규모**(작게 2000 · 배수 4)로 냈고 코퍼스를 안 썼다. 같은 형태가 `IntentStore` 에도 열려 있다. **규모는 이월 사유가 아니다** — 규약이 배제한 「어렵다·넓다」에 해당한다"*

### 판 1 · 라운드 2 · #7 — **등록된 차선책의 조건 ③ 이 이제 발화했고, 그러면 규약상 승격이 필요 없다**

- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `1-synthesis.md:41`·`:48`·`:67` ↔ `intent.md:96-99`(라운드 1 이 *"나도 안 쟀다"* 로 물러선 자리) · `.claude/skills/round/SKILL.md:325` · 반(反)의 ditto·portal 실행 산출 넷
- 반론 원문: *"**등록된 차선책의 조건 ③ 이 이제 발화했고, 그러면 규약상 승격이 필요 없다** — `intent.md:96-99` 는 *"이 저장소에서 못 만들면 → **코퍼스로 모집단을 넓힌다** → **그래도 안 갈리면** 거짓 양성률은 「대조 불가」로 다시 적고 비용 벤치만 닫는다 — 그것을 통과로 세지 않는다"* 다. 내가 **코퍼스 둘 다**에서 등록 실행체를 돌렸고 **양쪽 다 두 반경의 표본이 글자까지 같다.** 합(合)은 채택 #11 에서 *"조건 ③ 이 발화하지 않았으므로 차선책의 결론을 원용할 수 없다"* · 기각 #11 에서 *"`exact` 축은 **미측정**"* 으로 적고 그 위에서 **「올린다」**를 냈다. 규약은 축소를 *"**등록된 차선책이면 그대로 쓴다.** 아니면 승격"* 으로 갈랐다 — 발화한 이상 이 승격은 규약이 요구한 것이 아니고, 소유자가 `intent.md` 에서 이미 답한 것을 다시 묻는 것이 된다"*
- 채택·기각과 근거 ⟨합 `1-synthesis-r2.md:59`⟩: **채택 — 이 판의 축.** *"**이 판의 축이다.** 조건 ③ 의 전제(두 코퍼스 모두에서 두 반경의 표본이 안 갈린다)를 등록 실행체 산출로 세웠고 그중 portal 절반은 **내가 직접 돌렸다.** 규약 §5 가 *"등록된 차선책이면 그대로 쓴다"* 로 갈랐으므로, 이 절반의 승격은 소유자가 `intent.md` 에서 이미 답한 것을 다시 묻는 것이 된다"* — ⚠ **종료 판단자가 이 채택의 전제에 값이 없다고 적었다**(`1-referee-r2.md:56`: *"`## 차선책` 절은 메인 초안이고, 소유자는 본 적이 없고, §4 승인이 아직이다. 그러면 처분 ⑴ 은 **아직 참도 거짓도 아닌 전제 위에 서 있다**"*)

### 판 1 · 라운드 2 · #8 — **「교착으로 안 올린 이유」가 이제 죽었는데 문이 다시 열리지 않았다**

- 모집단: 규약
- 유효성: 거짓 ⟨반(反)은 「참」으로 냈고 합(合)이 *"규약의 교착 조건 둘 다 안 선다"* 로 기각했다 — 갈렸다⟩
- 해악도: 거짓신호 ⟨반(反)이 스스로 금지역에서 내려 매긴 것: *"반론 8 을 「금지역」으로 올리려 했다. 교착을 축소로 부르는 것이 **사실이 아닌 것을 사실로**인지 **라벨 다툼**인지 내가 못 가른다 … `거짓신호` 로 낮춰 매겼다"*. 합(合)은 기각했으므로 해악도를 안 냈다⟩
- 어디가 걸리나: `1-synthesis.md:80` ↔ `.claude/skills/round/SKILL.md:469` · `observations/r2-issue-63.md:9`(`#63` 제목) · `docs/gates/F09.md:336-341` · 반(反)의 ditto·portal 실행 산출
- 반론 원문: *"**「교착으로 안 올린 이유」가 이제 죽었는데 문이 다시 열리지 않았다** — 합(合)의 `## 내가 못 정한 것` 4 는 *"내 실측이 `exact` 축에 **아직 안 써 본 길**을 보였으므로, 지금은 「같은 실패 3 회」가 아니라 「이 회차가 아직 안 해 본 것」이다 — 그래서 교착으로 올리지 않았다"* 였다. 그 길을 R2 와 내가 각각 가 봤고 **`#58`/`docs/gates/F09.md:336-341` 이 적은 것과 글자까지 같은 실패**가 났다(*"두 반경의 표본이 글자까지 같았다"*). 규약의 조건은 *"같은 실패가 **3 회** 반복되거나 … **축소로 위장해 완료를 선언하지 않는다**"* 이고 `#63` 본문이 이미 *"세 회차 연속 대조 불가"* 를 제목에 달았다. 즉 합(合)이 교착 문을 안 연 유일한 근거가 측정으로 사라졌는데 처분(축소/승격)은 그대로다"*
- 채택·기각과 근거 ⟨합 `1-synthesis-r2.md:71`⟩: **기각.** *"규약의 교착 조건 둘 다 안 선다. ㉠ *"같은 실패 3 회"* — `#63` 이 세는 세 회차(F10·F10-5·F10-6)는 `f10-verify.py` 의 **다른 표본기**이고 `#63` 자신이 *"세 번의 이유가 전부 달랐다"* 고 적었다. 이 회차의 `f09-verify.py` ⑤ 는 `#63` 이 적은 *"켜진 것 0"* 과 달리 **14 건이 켜졌다**(R2 §2.3). 「두 반경의 표본이 같다」는 실패 자체는 F09 원판과 이 회차로 **두 번**이다. ㉡ *"재현은 되는데 원인을 못 댄다"* — 원인이 좌표로 나왔다(`crates/pal-extract/src/kotlin.rs` 의 `not_built` 로 portal 엣지 0 · ditto 는 호출자가 변한 것이 아니라 사라졌다 · `scripts/f09-verify.py:715` 의 갈래 필터). 규약이 교착 조항으로 실제 막으려는 것(*"축소로 위장해 완료를 선언하지 않는다"*)은 이 판정이 **「통과로 세지 않는다」를 그대로 적음으로써** 이미 막는다"*

### 판 1 · 라운드 2 · #9 — **`exact` 축 표본 10 은 사건 열이 아니라 사건 하나다 — 그 사실이 합(合)의 어느 칸에도 없다**

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `1-synthesis.md:52`·`:54` ↔ 반(反)의 궤적(첫 전이 14 건 중 13 건이 `6f298c8e`, `stale` 은 `ced475c1` 하나) · `scripts/f09-verify.py:715`·`:725` · `corpus/criteria.toml:6928-6930`
- 반론 원문: *"**`exact` 축 표본 10 은 사건 열이 아니라 사건 하나다 — 그 사실이 합(合)의 어느 칸에도 없다** — 내 궤적에서 두 반경 모두 **13 건이 커밋 `6f298c8e` 하나에서 한꺼번에 켜졌고**(`chore(rebuild): 옛 src 계층 테스트 전부 제거 — tests/`), 표본기는 그중 앞 10 을 담고 `:725` 에서 멈춘다. 그래서 등록 표본 10 은 **한 커밋의 한 행위(테스트 디렉터리 통째 삭제)**를 열 번 센 것이고 `stale` 이 0 이다. 거짓 양성률의 분모(=`stale`)가 0 이면 *"거짓 양성 0 / 0% ≤ 10%·30%"* 가 **잴 것 없이 참**이 된다. `corpus/criteria.toml:6928-6930` 의 「통과인데 아무것도 안 움직인 경우」 넷에 **이 형태(「`stale` 0 인데 ⑤ 가 통과」)가 없다.** 합(合)은 `exact` 축을 판정의 단위로 올리면서 그 축의 표본이 이 상태라는 것을 안 적는다"*
- 채택·기각과 근거 ⟨합 `1-synthesis-r2.md:60`⟩: **채택.** *"R2 궤적이 첫 전이 13 건이 커밋 `6f298c8e`(테스트 디렉터리 통째 삭제) 하나에서 켜졌음을 보였고, 표본기는 `:725` 에서 멈춘다. 분모(`stale`)가 0 이면 *"거짓 양성 0%"* 는 **잴 것 없이 참**이다. `corpus/criteria.toml` 의 「통과인데 아무것도 안 움직인 경우」 넷에 **이 형태가 없다.** 이 사실을 안 적고 `exact` 축을 판정 단위로 올리면 다음 회차가 그 값을 통과로 읽는다"*

### 판 1 · 라운드 2 · #10 — **합(合)의 ditto 수 금지 근거(*"아무도 재현하지 못했다"*)가 이제 거짓이다**

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨**갈렸다** — 반(反)은 **미관**으로 매겼고 합(合)이 *"**미관에서 올렸다**"* 로 올렸다⟩
- 어디가 걸리나: `1-synthesis.md:22` ↔ `observations/m1_registered.out` · 반(反)의 동일 산출
- 반론 원문: *"**합(合)의 ditto 수 금지 근거(*"아무도 재현하지 못했다"*)가 이제 거짓이다** — *"정·반·합 셋이 서로 다른 수를 냈다 … **구체적 값은 아무도 재현하지 못했다.** 그러므로 ditto 쪽 특정 수를 게이트나 `#58` 에 적는 것은 지금 금지한다"*. 셋이 갈린 까닭은 셋 다 **등록 실행체를 안 돌리고 각자 근사 절차를 돌렸기 때문**이고(채택 #1 이 그것을 스스로 밝혔다), 등록 실행체를 돌린 두 실행(R2·나)은 **10 줄 전부가 글자까지 같다.** 금지 조항이 살아 있으면 이 회차는 **자기가 재현한 값을 못 적는 상태**로 남는다"*
- 채택·기각과 근거 ⟨합 `1-synthesis-r2.md:61`⟩: **채택 · 해악도 상향.** *"셋이 갈린 까닭은 셋 다 등록 실행체가 아니라 각자 근사 절차를 돌렸기 때문이고(초안 자신의 채택 #1 이 밝혔다), 등록 실행체를 돌린 실행들은 전부 일치한다. **미관에서 올렸다** — 금지가 살아 있으면 이 회차의 처분(「대조 불가로 다시 적는다」)이 **자기 근거 없이** 원장에 적히고, 그것이 「상자를 근거 없이 채운다」의 형태다"*

### 판 1 · 라운드 2 · #11 — **`ordinal` 축 `callers` 를 「못 잰다」로 적으면, 같은 표본을 판정했을 때 나오는 값이 `30%` 상한 밖이라는 사실이 안 적힌다**

- 모집단: 저장소
- 유효성: 추정 ⟨반(反)이 *"판정은 사람이 하는 것이고 내가 안 했다 — 표본 동일성만 산출이다"* 로 스스로 물렸다⟩
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `docs/gates/F09.md:184-191` ↔ 반(反)의 `portal/callers` 산출 10 줄 ↔ `1-synthesis.md:53`·`:73`
- 반론 원문: *"**`ordinal` 축 `callers` 를 「못 잰다」로 적으면, 같은 표본을 판정했을 때 나오는 값이 `30%` 상한 밖이라는 사실이 안 적힌다** — `docs/gates/F09.md:184-191` 이 그 열 줄에 대해 이미 **거짓 양성 3/7 = 42.9%** 를 결박마다 한 줄로 판정했고, 내가 돌린 `portal/callers` 표본은 그 열 줄과 **글자까지 같다.** 같은 표본에 같은 판정 규칙을 대면 `callers` 값도 42.9% 이고 상한 30% 밖이다. 나는 **판정하지 않으므로 이것을 값으로 세우지 않는다** — 그러나 합(合)의 문면(*"못 잰다"*)은 이 계산이 가능하다는 사실 자체를 소유자에게서 가린다"*
- 채택·기각과 근거 ⟨합 `1-synthesis-r2.md:62`·`:72`⟩: **사실 부분 채택 · 추정 부분 기각.** 채택 — *"표본 동일성은 **내가 재현했다** — `portal/callers` 열 줄이 `docs/gates/F09.md` §4-가 의 일곱을 포함해 `symbol` 과 같다. 그러므로 *"이 축은 값을 못 낸다"* 가 아니라 *"이 축의 `callers` 값은 `symbol` 과 같은 값이고 독립이 아니다"* 가 참이다"*. 기각 — *"반(反) 스스로 「추정」으로 물렸고 나도 세우지 않는다. `docs/gates/F09.md` §4-라 가 이유를 적었다 — 두 값이 **독립이 아니므로** `callers` 의 고유 위험에 걸린 30% 상한에 그 값을 댈 수 없다. 표본 동일성(사실)만 채택했다"*

---

## 내가 기각한 것

**없음.** 나는 취합 자리이고 판정을 다시 하지 않았다 — 내가 기각한 반론은 하나도 **없음**이다. 위 22 항은 반(反) 두 표의 **행 전수**(11 + 11)이고, 채택·기각은 전부 합(合) 둘이 낸 것을 그대로 옮겼다.

**항으로 안 실은 것 — 셋.** 합(合) R2 의 고침 7 이 *"게이트·`#58` 에 새로 적을 사실 셋"* 으로 지정하고 *"셋 다 **장치에 대한 발견**이므로 `intent.md` 의 (나) 분할대로 이슈/`#58` 로 간다"* 고 처분까지 적었지만, **해악도를 안 매겼다.** 축 넷(모집단·유효성·해악도·어디가 걸리나)이 안 차므로 항으로 못 낸다. 내가 해악도를 채우면 그것이 곧 수 맞추기다 — 안 채운다. 메인이 해악도를 매기면 항이 셋 는다. ⟨전부 `1-synthesis-r2.md:82`⟩

1. **ⓐ** — *"두 반경은 `undeterminable` 축에서 갈리고 표본기가 `:715` 에서 그 축을 못 본다"* · 좌표 `scripts/f09-verify.py:715` · `observations/r2-exact-axis.md:123-125`
2. **ⓑ** — *"`exact` 축 표본은 `stale` 이 0 이라 거짓 양성률의 분모가 비었고 등록된 「통과인데 안 움직인 넷」에 **그 형태가 없다**(다섯째 후보)"* · 좌표 `corpus/criteria.toml:6928-6930`
3. **ⓒ** — *"「엣지 0 건인데 `callers` 반경이 통과」를 막는 하한이 `⑤` 경로에 **구현돼 있지 않다**(⑦ `반경별()` 은 ditto 전용)"* · 좌표 `scripts/f09-verify.py:756` · `corpus/criteria.toml:6928-6930`

**항으로 안 실은 사실 둘 더.**

1. 합(合) 둘이 남긴 `## 내가 못 정한 것` — R1 넷 · R2 셋 — 은 **해악도가 없고 발견이 아니라 미결 목록**이다. 종료 판단자가 그 중 둘을 설계문 ④ 양성 끝 조건의 빈칸으로 세었다(`1-referee-r2.md:50`). 항으로 안 냈다.
2. 라운드 사이 측정 셋(`observations/r2-exact-axis.md`·`r2-bench-ratio.md`·`r2-issue-63.md`)은 스스로 *"판정하지 않는다. 수와 산출만 적는다"* 로 열고 해악도를 안 매긴다. **출처가 `정반합` 이 아니라 실측**이고, 스키마의 `합계검산.면제출처` 에 `실측` 이 들어 있다. 항으로 안 냈다.

---

## 계수 — 두 원천 대조

**반론 행 수 — 반(反)이 붙인 번호 ↔ 합(合)의 처분 표 행 수**

| 잰 것 | 반(反) | 합(合) | 같나 |
|---|---|---|---|
| 라운드 1 반론 | 11 (#1~#11) | **12** = 채택 10 + 기각 2 | **갈린다** — `#11` 이 채택 표와 기각 표에 **둘 다** 걸려 있다(핵심 채택 · 「미충족 확정」 문면 기각) |
| 라운드 2 반론 | 11 (#1~#11) | **15** = 채택 10 + 기각 5 | **갈린다** — 몫 기각 넷(`#3(해악도)` · `#4(해악도·좌표)` · `#6(한 갈래)` · `#11(추정 부분)`)이 행을 늘렸고, 전부 기각은 `#8` 하나다 |
| **합계** | **22** | **27 행** (채택 20 + 기각 7) | **갈린다** — 몫 기각 5 개가 행을 늘렸다. 반론 **id** 로 세면 22 로 같다 |

**채택·기각**

| 잰 것 | 수 | 어디서 나왔나 |
|---|---|---|
| 제기된 반론 | 22 | 반 두 표의 행 |
| 전부 또는 좁혀 채택 | 20 | 합 두 채택 표 |
| 전부 기각 | 2 | 라운드 1 `#9` · 라운드 2 `#8` |
| 몫만 기각(본체는 채택) | 5 몫 | 라운드 1 `#11` · 라운드 2 `#3`·`#4`·`#6`·`#11` |
| 헛것 비율(전부 기각 / 제기) | **2/22 = 9.1%** | 위 두 줄 |

**해악도 — 반(反)이 스스로 매긴 것 ↔ 합(合)이 낸 것**

| 라운드 | 반(反) 매김(제기 전수) | 합(合) 살아남음 | 갈린 자리 |
|---|---|---|---|
| 1 | 금지역 2 · 실패 0 · 거짓신호 8 · 미관 1 (=11) | 금지역 2 · 실패 0 · 거짓신호 7 · 미관 1 (=10) | `#9` 가 기각돼 빠졌다. 나머지는 반(反)의 값을 그대로 뒀다 |
| 2 | 금지역 4 · 실패 0 · 거짓신호 6 · 미관 1 (=11) | 금지역 2 · 실패 0 · 거짓신호 8 · 미관 0 (=10) | **`#3` 금지역 → 거짓신호** · **`#4` 금지역 → 거짓신호** · **`#10` 미관 → 거짓신호**(합이 *"미관에서 올렸다"*) · `#8` 기각 |
| **합계** | 금지역 6 · 실패 0 · 거짓신호 14 · 미관 2 (=22) | **금지역 4 · 실패 0 · 거짓신호 15 · 미관 1 (=20)** | 세 자리에서 갈렸다 |

**합(合) ↔ 종료 판단(referee) 대조** — referee 가 받은 분포와 합의 채택 표를 대었다.

| 라운드 | 합의 채택 표 | referee 가 적은 분포 | 같나 |
|---|---|---|---|
| 1 | 금 2 · 실 0 · 거 7 · 미 1 (=10) | 금 2 · 실 0 · 거 7 · 미 1 (=10) | 같다 |
| 2 | 금 2 · 실 0 · 거 8 · 미 0 (=10) | 금 2 · 실 0 · 거 8 · 미 0 (=10) | 같다 |

**반증 #9(좌표 되풀이) — 종료 판단자의 계수와 반(反)의 자기 신고가 갈렸다**

| 잰 것 | 반(反) R2 의 자기 신고 | 종료 판단자의 실측 | 같나 |
|---|---|---|---|
| 라운드 1 과 정확히 되풀이된 좌표 | **하나뿐**(`intent.md:96-99`) ⟨`1-antithesis-r2.md:8`⟩ | **2 건**(`intent.md:96-99` · `.claude/skills/round/SKILL.md:469`) + 구간이 겹치는 1 블록(`corpus/criteria.toml` 의 `bench_grounds`) | **갈린다** |
| 좌표 원자 수 | 안 셌다 | 라운드 1 **37** · 라운드 2 **35** | — |
| 친 대상 교집합 | 안 셌다 | **0** — 친 대상이 통째로 갈렸다 | — |
| 되풀이율 | 안 셌다 | **원자 35 중 3 (≈9%)** | — |

종료 판단자가 이 갈림을 스스로 적었다 ⟨`1-referee-r2.md:40`⟩: *"**정확히는 둘이다.** … 자기 되풀이를 **과소** 신고한 것이므로 진척 주장에 유리하게 틀린 쪽이 아니라 불리하게 틀린 쪽이고, 세 건이어도 9% 라 #9 의 결론은 안 바뀐다. **다만 자기 신고 수치가 실측과 어긋난 사실 자체는 기록해 둔다.**"*

**이 보고가 내는 항의 수 = 22.** `### ` 로 시작하는 줄도 22 다(코드펜스 없음 · `## 내가 기각한 것` 아래에 `- ` 불릿도 표 행도 없다). 그것이 원장에 실릴 레코드 수다.

---

## 종료 판단 — `pal-debate-referee` 가 낸 것 그대로

**라운드 1/2** ⟨`1-referee.md:10`⟩

> **한 라운드 더 돈다.**

근거 ⟨`1-referee.md:21`⟩: *"설계문 `## 상한과 끝 조건` 의 **② 해악 게이트** 첫 행에 그대로 걸린다 — *"금지역·실패가 있고 상한이 남았다 → 한 라운드 더."* 금지역이 **2** 이고 상한은 **2 라운드**, 쓴 것은 **1** 이다. 상한이 한 라운드 남았으므로 「소유자에게 올린다」는 아직 아니다."*
그리고 ⟨`1-referee.md:25`⟩: *"「소유자에게 올린다」를 지금 내지 않은 이유는 하나 더 있다. 남은 넷 중 **소유자만 답할 수 있는 물음이 없다.** … 상한이 남았는데 올리면 남은 한 라운드를 버리는 것이고, 그것은 소유자 결정 *"독립 리뷰 상한을 다 쓴다"* 와 어긋난다."*

**라운드 2/2** ⟨`1-referee-r2.md:7`⟩

> **소유자에게 올린다.**

근거 ⟨`1-referee-r2.md:18`⟩: *"**게이트가 그대로 읽힌다.** `1-design.md` 의 ② 해악 게이트는 세 줄뿐이고, 그중 걸리는 줄은 하나다 — *"금지역·실패가 있고 **상한을 다 썼다** → 소유자에게 올린다."* 금지역 2 가 살아남았고 상한 2 를 다 썼다. 다른 두 줄은 성립하지 않는다: 「한 라운드 더」는 상한이 남았을 때만이고(안 남았다), 「끝났다」는 **거짓신호·미관만** 남았을 때만이다(금지역 2 가 남았다). 기각 5 건은 게이트의 입력이 아니다 — 살아남은 것만 센다."*

**세 경로가 같은 곳을 가리켰다** ⟨`1-referee-r2.md:44`·`:50`·`:58`⟩:

> ### #10 — 「끝났다」를 냈는데 처분이 축소·전환인가 → **발효 안 함(내가 「끝났다」를 안 냈다).**
> 처분은 두 절반 모두 #10 이 지목한 모양이다 — ⑴ **축소**(거짓 양성률: 축소·승격 없음) ⑵ **전환**(벤치: 소유자에게 올린다).

> ### ④ 양성 끝 조건 셋 — **독립적으로 「끝났다」를 막는다**
> 합이 **「못 정한 것」 셋**을 남겼고, 그중 둘이 ④ 의 칸에 그대로 꽂힌다 — 벤치 부하의 현실적 크기(칸 1 「기록될 문장이 참인가」)와 `#58` 로 보낸 절반이 실제로 집히는가(칸 2 「행선지」·칸 3 「효과의 증인」). **해악 게이트가 없었어도 ④ 만으로 「끝났다」는 못 나온다.**

> 메인이 댄 사실은 그 전제에 값을 매기는 것이 아니라 **전제가 아직 정해지지 않았음**을 확정한다 — `## 차선책` 절은 메인 초안이고, 소유자는 본 적이 없고, §4 승인이 아직이다. 그러면 처분 ⑴ 은 **아직 참도 거짓도 아닌 전제 위에 서 있다.** … 설계문의 셋째 답 조건이 *"소유자만 답할 수 있는 물음이 남았다"* 이므로, 해악 게이트와 별개로 **이 사실 하나만으로도 답은 「소유자에게 올린다」다.** 세 경로(게이트 · ④ · 이 사실)가 전부 같은 곳을 가리킨다.

**상한에 대해 종료 판단자가 적은 것** ⟨`1-referee-r2.md:75`⟩: *"**상한을 안 늘렸다.** 상한 2 는 `intent.md:122` 가 지고 있고 설계문이 *"종료 판단자는 이것을 늘리지 않는다"* 로 못 박았다. **모자랐다고 본다 — 금지역 2 와 「못 정한 것」 셋이 상한 안에서 안 닫혔다.** 그것을 적어서 올리는 것이 내가 할 수 있는 전부다."*

---

## 소유자에게 올릴 물음

**물음 하나에 칸 하나다.** 두 원천이 서로 다른 수의 칸을 냈다 — 합(合) R2 가 **1 칸**, 종료 판단자가 **4 칸**이다. **맞추지 않았다.**

**합(合) 라운드 2 가 낸 것** ⟨`1-synthesis-r2.md:92`⟩

| # | 낸 자리 | 물음 |
|---|---|---|
| 합-1 | 합(合) R2 | **㈂ 의 「반경별 비용 벤치」 절반을 — 등록이 요구한 것(재는 것 넷 `symbol`·`callers`·`closure(2)`·증분 갱신 × 두 규모 × 회차 3 · `#[ignore]` 형태 · 저장소 안 실행체)이 아직 하나(증분) · 한 규모 · scratchpad 실행체뿐이고, 합격선 *"증분이 전체보다 빠르다"* 가 `changed` 크기를 안 적어 곡선의 교차점(감시 열쇠의 약 91%)을 이미 본 지금은 부하 선택이 통과/반증을 직접 뒤집는 상태에서 — ㈎ 이 회차에서 등록된 형태 그대로 저장소 안에 세워 재고 부하 미규정은 게이트에 결함으로 적는가, ㈏ 완성선에서 빼 `#58` 로 보내는가?** |

**종료 판단자가 낸 것** ⟨`1-referee-r2.md:66-71`⟩ — *"**물음 하나에 칸 하나다. 아래 넷은 묶이지 않는다 — 답 하나가 둘을 닫을 수 없다.**"*

| # | 낸 자리 | 물음 |
|---|---|---|
| 심판-1 | 종료 판단자 | `intent.md` 의 `## 차선책` 절은 메인이 초안한 것이고 당신은 본 적이 없는데, 그것을 규약 §3 이 말하는 **「등록된 차선책」으로 인정하는가** — 즉 그것을 쓰는 데 승인이 필요 없는가? |
| 심판-2 | 종료 판단자 | 물음 1 이 「아니다」이면, 합의 처분 ⑴(거짓 양성률을 축소하고 승격 없이 간다)은 **승격 절차를 밟아야 하는 축소로 바뀌는데**, 그 절차를 지금 밟는가 아니면 이 회차의 완성선에서 그 절반을 빼는가? |
| 심판-3 | 종료 판단자 | 살아남은 금지역 ⑵ 가 *"`ordinal` 축을 「원리상 못 잰다」로 적은 거짓 문장이 당신에게 간 한 문장의 전제에 박혔다"* 인데, 그 한 문장을 **정정한 것으로 다시 받겠는가**? |
| 심판-4 | 종료 판단자 | 벤치 절반을 소유자 판단으로 올린 건에 대해, **벤치 부하의 현실적 크기가 아직 안 정해진 채로** 그 절반의 행선지를 정하겠는가, 아니면 크기를 먼저 재고 정하겠는가? |

⚠ **칸 수가 갈린 사실을 적는다.** 합(合) R2 는 벤치 절반 **하나만** 올렸고(거짓 양성률 절반은 *"소유자가 `intent.md` 의 차선책에서 이미 답한 것"* 이라 안 올렸다), 종료 판단자는 **그 「이미 답한 것」의 전제 자체**를 물음 1·2 로 올렸다. 종료 판단자의 물음 4 와 합(合)의 물음 합-1 은 같은 절반(벤치)을 다른 각으로 묻는다. **합쳐서 몇 칸인지는 내가 안 정했다** — 그것은 판정이고 나는 취합 자리다.
