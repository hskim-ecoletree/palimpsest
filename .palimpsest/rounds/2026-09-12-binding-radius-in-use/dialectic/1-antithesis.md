# 반(反) — ㈂(#58 의 「대조 불가」 둘)의 처분

> `pal-decision-opponent` 가 냈다. 회차 `2026-09-12-binding-radius-in-use` · **판 1 · 라운드 1** ·
> 내가 돌린 커밋 `e05ca36`(`git rev-parse HEAD`; 초안의 기준 `1276b8f` 과의 차이는
> `.palimpsest/` 다섯 파일뿐 — `git show --stat e05ca36`).
>
> **받은 것**: 초안 전문(`dialectic/1-thesis.md`) · `intent.md` · `premortem/r1-raw.md` ·
> 좌표 목록 · 저장소.
> **안 받은 것**: 대화 기록 · 메인의 사고 과정 · 설계문(`dialectic/1-design.md`) ·
> 앞 라운드의 반론 · 다른 회차의 결론. 프롬프트에 사고 과정이나 대화 기록이 섞여 오지 않았다.
> **나는 판정하지 않는다.** 초안이 댄 수는 전부 내가 다시 돌렸고, 아래 표의 좌표는 내 실행 산출이다.
>
> ⚠⚠ **이 파일을 친 손은 메인이다 — 글은 반(反)의 것이고 한 글자도 안 고쳤다.**
> 반(反) 자리의 에이전트가 읽기 전용이라 파일을 못 썼고, 자기 반환문 끝에
> *"합(合)이 `dialectic/1-antithesis.md` 에 그대로 옮기면 된다"* 고 적었다. 규약 §5 가
> **저작은 강제되지 않는다**고 미리 적어 둔 자리이므로, 옮긴 사실을 여기 밝힌다.

## 초안이 서는 자리

무너뜨리지 못한 것 다섯.

- **portal-backend 의 엣지 0.** 내가 다시 돌렸다 — `pal query graph.dump --repo ~/dev/projects/boxwood/portal-backend` → `nodes 1340 · edges 0`. 초안의 수와 한 자리도 안 갈린다. 엣지가 0 이므로 **어떤 표본을 뽑아도** `ordinal` 축에서 `callers` == `symbol` 이다. 이 결론은 아래 반론 1 이 표본 절차를 무너뜨려도 **안 무너진다.**
- **ditto 의 수.** `--at aded7ce7f88f` → `nodes 4578 · edges 4601`, 균등간격 20 의 호출자 수가 `[1,1,1,0,1,0,2,2,1,2,2,1,1,0,1,1,2,0,2,3]` 로 **글자까지 같다.** 호출자 0 인 심볼 1413/4578 도 같다.
- **결박 37 · 전부 `symbol`.** `.palimpsest/intent/bindings.jsonl` 을 직접 세었다 — 38 행 중 1 행은 `{"kind":"header"}` 이고 나머지 37 이 전부 `radius=symbol`.
- **`bench.rs` 는 실재하고 초록이다.** `cargo test -p pal-query --release --test bench -- --ignored --nocapture` → `test result: ok. 1 passed`. 사전부검 ⑪ 의 *"파일 자체가 없다"* 는 거짓이 맞다(초안이 안 돌린 자리를 내가 돌려 확인했다).
- **사전부검 ⑨ 의 좌표 반박은 선다.** `crates/pal-intent/src/store.rs:241-270` 과 `:369-386` 은 둘 다 몸통이 있고, `crates/pal-cli/src/touch.rs` · `query.rs` · `doctor.rs` 가 부른다. ⑨ 가 댄 `corpus/criteria.toml:6446` 계열의 「없다 (여덟)」은 **F09 착수 전 재고 목록**이라(같은 목록에 *"거짓 양성률 합격선 · 반경별 벤치"* 도 들어 있다) 현재 빌드의 부재 근거로 못 쓴다. 이 자리에서는 초안이 이겼다.

## 반론

| # | 반론 | 좌표 | 유효성 | 해악도 |
|---|---|---|---|---|
| 1 | 초안이 *"등록된 선정 규칙 4 를 그대로 적용했다"* 며 게이트에 남기겠다고 한 문장이 **등록의 실행체와 다른 절차**다 — 등록된 표본기는 대장 전량이 아니라 **창 안에서 바뀐 파일의 심볼**만, **이름이 유일한 것**만, `간격=len(유일)//40` 으로 후보 40 을 뽑아 그중 걸리는 20 에 건다 | `scripts/f09-verify.py:265-296`(`심볼_고르기`, `파일_안에서=`) · `:660-676`(`변경된` 산출 + `결박_걸기`) · `:58`(`MIN_BINDINGS=20`) · `:73`(`HISTORY_WINDOW=120`) ↔ `1-thesis.md:148-160`·`:318` | 참 | 거짓신호 |
| 2 | **`exact`(ditto) 축은 등록 모집단에서 살아 있는데 초안은 두 축 전부에 대해 「이번에도 대조 불가」를 적는다** — 내가 등록 절차대로 돌렸다: `START=d6ee4ad65cb8`(핀에서 비머지 120 뒤), 바뀐 `.ts` **576** 파일, 그 안의 심볼 **2204**, 유일·균등 후보 40 중 앞 20 의 호출자 = `[0,0,1,2,0,2,0,0,0,0,2,0,0,0,2,2,0,0,0,1]` → **≥1 이 7 · ≥2 가 5**. 결박 시점 그래프도 직접 떴다(`nodes 5239 · edges 4487`). 즉 `exact` 축에서 `callers` 감시 집합은 `symbol` 과 **갈린다.** 초안은 그 축을 한 번도 안 시도하고 판정을 적는다 | 위 명령 산출 · `scripts/f09-verify.py:801-804`(코퍼스마다 따로 부른다) ↔ `1-thesis.md:318`(게이트 문장 머리) | 참 | 금지역 (사실이_아닌_것을_사실로 — 안 잰 축을 「대조 불가」로 적는다) |
| 3 | **벤치의 등록 합격선이 구성상 항진명제**이고 초안은 그것을 안 열어 봤다 — `bindings_watching(changed)` 은 `changed` 만 멀티맵 역조회하고, `all()` 은 `BINDING` 전수 훑기 + 정렬이다. `changed` 집합 크기가 **등록 어디에도 안 적혀 있으므로** 1 로 잡으면 *"증분이 전체보다 빠르다"* 가 **잴 것 없이 참**이다. 초안은 *"두 함수가 실재한다"* 에서 *"닫을 수 있다"* 로 바로 갔다 | `crates/pal-intent/src/store.rs:241-270` · `:369-386` · `corpus/criteria.toml:7361-7371`(`bench_grounds` — 재는 것 넷만 적고 부하 규정 없음) ↔ `1-thesis.md:321`·`:466` | 참 | 금지역 (측정이_죽은_가지 — `intent.md:138` 이 지목한 그 형태) |
| 4 | **「죽은 가지가 된다」를 떠받치는 하한이 등록된 합격선이 아니다** — 그 문장은 `control_off_modes` 안의 **후보**이고 스스로 *"게이트에 열다섯째로 적을지는 판정 시점에 정한다"* 고 적었다. `docs/gates/F09.md` 는 그것을 안 채택했고(`grep 열다섯` → `:402` 한 건, 무관), `실_이력` 이 거는 하한 둘은 (결박 ≥ 20)·(결박 직후 전부 fresh)뿐이라 **감시 집합 크기 하한은 구현이 없다.** 게다가 등록은 등급 축을 갈라 적으라고 못 박았으므로 「ditto 7 건이 채워서 가려진다」는 뭉치기 자체가 안 일어난다 | `corpus/criteria.toml:6712-6716` · `:6832-6836`(*"등급별로 갈라 잰다 … 뭉치면"*) · `scripts/f09-verify.py:680-688` ↔ `1-thesis.md:162`·`:318` | 참 | 거짓신호 |
| 5 | **초안이 ⑪ 을 「반증」으로 세면서 자기 산출이 ⑪ 의 정의를 재현한 것을 안 센다** — ⑪ 은 *"벤치 인프라 0"* 을 네 조회로 정의했고(`criterion`·`[[bench]]`·`benches/`·CI), 초안의 M6 이 그 넷을 다시 돌려 **전부 0 건**을 얻었다. 나도 재현했다(`grep -rn "criterion\|\[\[bench\]\]" --include=Cargo.toml .` → rc=1 · `grep -rn bench .github/workflows/` → rc=1 · `find … -name benches` → 0 건). 그런데 초안은 *"둘 다 거짓이다"* 로 적고 유효 일곱을 다섯으로 줄여 그 차이를 판정 근거로 쓴다 | `premortem/r1-raw.md:158-159` · 위 세 명령 · `1-thesis.md:195-197` ↔ `:200`·`:346`·`:353` | 참 | 거짓신호 |
| 6 | **`crates/pal-query/tests/bench.rs` 는 F09 의 넷을 안 잰다 — F05 의 넷을 잰다.** 파일 머리가 *"4종 벤치 + 선형성 — `[f05.3.pass]` ④"* 이고, 내가 돌린 산출도 `① 심볼 조회 ② 1홉 역방향 ③ 3홉 BFS ④ 전체 재구축` 이다. `[f09.4.pass]` 가 요구한 넷은 `symbol·callers·closure(2)·증분 갱신` 이고, 초안이 「파일 경로를 지정한 등록」이라 지목한 자리도 열어 보면 **F05 의 4종을 위한 지정**이다. 거기에 `pal-query` 는 `pal-intent` 의존이 없다. 그러므로 ⑪ 의 실질(*"표본기 + 벤치라는 새 장치를 처음부터 짓는 일"*)은 좌표 둘이 틀렸어도 **살아 있고**, 초안은 그것을 「벤치를 남기는 근거」로 뒤집어 쓴다 | `crates/pal-query/tests/bench.rs:1`·`:171-209` · `corpus/criteria.toml:4874-4877` · `corpus/criteria.toml:7367-7368` · `crates/pal-query/Cargo.toml:9-13` · `cargo test -p pal-query --release --test bench -- --ignored` 산출 ↔ `1-thesis.md:200-207` | 참 | 거짓신호 |
| 7 | **㉳(등록된 차선책의 문면 그 자체)를 떨어뜨린 근거가 「비용」이다** — 초안은 ㉳ 의 결과 문장이 자기 값과 *"거의 같다"* 고 인정하고서 *"비용이 다르다 … 다섯 번째로 … 같은 문장을 적는다"* 로 뺐다. 규약은 「어렵다·시간이 없다」류를 이월 사유로 못 박아 배제했고, 하필 그 칸이 **등록된 차선책에 가장 가까워 승격이 필요 없던 칸**이다. 즉 이 초안은 승격이 필요 없는 길을 노력 사유로 버리고 승격이 필요한 길을 골랐다 | `1-thesis.md:373`·`:379` · `.claude/skills/round/SKILL.md:324`·`:469` | 참 | 거짓신호 |
| 8 | **「축소」 라벨을 고르면서 ㈁ 선례를 안 쓴 이유를 한 줄도 안 댔고, 내 실측이 선례와 같은 형태를 가리킨다** — `ordinal` 축이 못 재는 원인은 이 회차의 범위가 아니라 `F02` 의 능력 부재(`kotlin-exports`·`kotlin-imports`·`kotlin-scopes` 가 `not_built`)다. ㈁ 이 `F05` 소속이라 빠졌을 때 소유자 개정은 그것을 *"축소가 아니라 정정"* 으로 라벨했다. 같은 자를 대면 이 값도 **정정** 후보이고, 그러면 승격 부담과 원장 표기가 달라진다 | `crates/pal-extract/src/kotlin.rs:157-161` · `intent.md:149` ↔ `1-thesis.md:14`(*"읽었다 / 안 썼다"* 만 적고 근거 없음) | 참 | 거짓신호 |
| 9 | **행선지를 `#58` 로 잡은 것이 소유자가 고른 (나) 분할의 문면과 어긋난다** — `intent.md:127` 의 괄호가 *"이 회차가 만드는 장치(**#58 의 표본기·벤치**)"* 라고 `#58` 을 이름으로 지목했다. 초안은 *"만든 장치가 아니라 못 만든 측정이라 `#58` 이 정본"* 이라며 그 분할 밖으로 뺐는데, 문면은 `#58` 의 표본기·벤치를 **분할 안**으로 적었다 | `intent.md:127-128` ↔ `1-thesis.md:322` | 참 | 거짓신호 |
| 10 | **자가 §5 가 지정한 자가 아니다** — 규약의 자는 *"그 검사가 재는 의도의 양"* 인데 초안은 **항목 수**(닫는 「대조 불가」 수)로 셌다. `#58` 은 벤치 절반을 **규모 S · P2 · 의존 없음**으로 적었고 거짓 양성률 절반은 네 회차가 못 넘은 벽이다. 두 절반을 1:1 로 세면 2→1 이 **재는 의도의 양이 절반으로 줄었다**를 뜻하지 않는다 | `.claude/skills/round/SKILL.md:329` · `gh issue view 58`(*"우선순위 P2 · 의존 없음 · 규모 S"*) ↔ `1-thesis.md:424-432` | 추정 | 미관 |
| 11 | **차선책 조건 ③ 이 「절반만 섰다」가 아니라 「미충족 확정」이다** — 반론 2 의 수가 나온 이상 *"그래도 안 갈리면"* 은 발화하지 않았다. 초안은 조건 ③ 을 미결로 남긴 채 차선책의 **결론만** 채택했고, 그 사실이 승격 물음의 문면을 바꾼다(지금 문면은 *"portal 20/20"* 만 붙이고 ditto 축을 안 붙인다) | `intent.md:96-99` · 반론 2 의 산출 ↔ `1-thesis.md:442`·`:454` | 추정 | 거짓신호 |

## 내가 스스로 물린 것

- **「표본기가 F09 뒤에 방향을 고쳤으니 `#58` 의 대조 불가가 낡았다」** 로 치려 했다. `git log -S"결박은 과거에 걸고 앞으로 온다" -- scripts/f09-verify.py` → `02bc6ff 2026-08-15`, **스크립트를 만든 커밋과 같다.** F09 기록보다 뒤가 아니다. 철회.
- **사전부검 ⑨ 의 좌표(`corpus/criteria.toml:6446`)로 초안의 M7 을 치려 했다.** 열어 보니 그 목록은 F09 **착수 전 재고**이고 같은 줄에 *"거짓 양성률 합격선 · 반경별 벤치"* 도 들어 있다 — 현재 빌드의 부재 근거가 못 된다. 초안이 이 자리에서 옳다. 철회.
- **portal 축도 등록 표본으로는 갈릴 수 있다**로 치려 했다. `edges 0` 이라 어떤 표본에서도 호출자가 0 이다. 철회.
- **반론 2 가 보인 것은 「감시 집합이 갈린다」까지다.** 두 반경의 **판정**(stale/fresh)이 갈리는지는 나도 안 쟀다 — `scripts/f09-verify.py` 전량 실행(prettier 변형 + 코퍼스 사본)은 나도 안 돌렸다. 그래서 반론 2 는 *"exact 축에서 잴 것이 있다"* 까지만 서고 *"재면 갈린다"* 까지는 안 선다.
- **초안의 M2 집계(13 · 24 · 173 · 85 · 12)는 다시 안 돌렸다.** 그래서 어느 반론도 그 수 위에 안 세웠다.
- **초안이 기준 커밋을 `1276b8f` 로 적고 워킹트리를 `?? …/2026-09-12-binding-radius-in-use/` 로 적은 것**과 지금 상태(`e05ca36` · 미추적은 `1-thesis.md` 하나)의 차이를 반론으로 세우려 했으나, `git show --stat e05ca36` 이 `.palimpsest/` 다섯 파일만 건드렸음을 보였다. 수가 안 움직인다. 철회.
