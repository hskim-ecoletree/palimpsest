# 사전부검 라운드 3 — 처분표 (시나리오 18 · 기각 6 · 상한 소진)

원 반환문: `r3-raw.md`. 대상은 계획 **판 3**. 처분 결과는 **판 4**.

| # | 시나리오 | 해악도 | 처분 |
|---|---|---|---|
| 1 | `intent.md`→게이트 링크가 회차 내내 죽은 링크 검사를 빨갛게 한다 | 실패 | **해소** — #13 채택으로 intent→gate 방향 자체가 사라진다 |
| 2 | 조기 return 을 걷으면 ③④⑤⑥ 이 **거짓 0** 을 내고 ⑦⑧ 이 중복 · 시험이 못 본다 | 거짓신호 | **계획수정** — 칸마다 「못 셌다」를 내게 하고 `발견칸` 중복 호출을 없앤다. 시험도 고친다 |
| 3 | §9 「넷의 합」 삭제 근거가 **거짓** — 비동결 게이트가 이미 둘 | **금지역** | **계획수정** — **§9 를 안 지운다.** *"원문 수치와 함께"* → *"조건 ID 와 함께"* 로만 정정하고 합 검산은 ID 목록 길이로 |
| 4 | 강제가 피측정자 옵트인에 달렸고 하한 1 은 과거 둘로 **영구 충족** | **금지역** | **계획수정** — #13 로 옵트인 축이 줄고, 하한을 *"가장 최근 회차가 검사에 들었는가"* 로 바꾼다 |
| 5 | 「묶음 행 + ID 열」이 두 게이트 어디에도 안 맞는다 | 실패 | **계획수정** — `## 판정` 에 **표준 표 하나**(판정 → ID 목록)를 위에 두고 기존 묶음·근거 표는 **아래 그대로 남긴다.** 파서는 표준 표만 읽는다 |
| 6 | 수 칸과 ID 목록이 한 행 안의 원장 둘 — 자기 위반 | 거짓신호 | **계획수정** — 표준 표에 **수 칸을 안 둔다.** 세면 나온다 |
| 7 | 근거 칸의 명령을 아무것도 안 읽는다 — ③ 에 합격선·음성대조 0 | 거짓신호 | **계획수정** — *"재현 가능하게 적혔다"* 주장을 **걷는다.** 조건은 *"근거 칸에 명령이 적혔다"* 로 정직하게 낮추고, 옳은 명령인지는 독립 리뷰가 진다 |
| 8 | `## 효과` 의 `cargo xtask check` 를 **CI 가 돈다** — §8 재위반 | 거짓신호 | **계획수정** — 효과는 **격리 사본에 어긋남을 심어 21 번째를 발화시킨 출력** + **이 회차 `intent.md` 에 계기판을 직접 댄 출력**. 둘 다 CI 가 안 돌린다 |
| 9 | Stop 관측에 **양성 대조가 없다** — 「안 먹는다」와 「설정이 틀렸다」가 같은 화면 | **금지역** | **완수조건전환** — 같은 설정으로 `SubagentStop` 이 **먼저 먹는지** 본다. 안 먹으면 관측 자체가 「못 태웠다」 |
| 10 | 조건·태그 파서가 `dashboard.py` 와 파이썬 안에서 두 벌 — C2-b 재발 | 거짓신호 | **계획수정** — 파서를 `record.py` **안**에 두고 `dashboard.py` 가 **`import` 해서** 쓴다 |
| 11 | 열림 축의 과거 **434 행 기본값**이 미정 — 어느 쪽도 죽은 가지거나 거짓 | **금지역** | **계획수정(넷째 갈래)** — **소급하지 않는다.** 과거 파일은 스키마 2 로 남고 3 은 이 회차 이후부터. 계기판은 스키마 2 회차에 **「형식 이전」**을 낸다 |
| 12 | 손으로 벤 「20」이 **두 자리** — `ci.yml:111` 이 남는다 | 거짓신호 | **계획수정** — 둘 다 |
| 13 | ★ 게이트→intent 링크가 **이미 둘에 있다** — 더 작은 표면 | 미관 | **채택** — **frontmatter 형식 2 축을 버린다.** `rust-extractor.md` 에 링크 한 줄. #1·#14 가 함께 사라진다 |
| 14 | 짝 링크가 여럿일 때 어느 것이 짝인지 규칙이 없다 | 실패(추정) | **해소** — #13 채택 |
| 15 | §9 「게이트가 코드보다 작다」가 이 회차 모양에서 원리상 못 선다 | 거짓신호 | **계획수정** — §9 의 명령을 `xtask/`·`.claude/**/bin/` 까지 세도록 고친다. **범위 안이다** |
| 16 | 파서를 새 파일로 놓으면 `PAYLOAD` 에 안 들고 아무도 안 잰다 | 거짓신호 | **계획수정** — #10 과 같은 처분(`record.py` 안). 새 파일 0 |
| 17 | 스키마 3 이 서로 다른 두 종류에 버전 하나를 강요 | 거짓신호(추정) | **계획수정** — **종류별 버전.** 예외표는 행 형식이 안 바뀌므로 2 로 남는다 |
| 18 | Stop 관측 산출을 `.json` 으로 놓으면 sunset 트리거 발화 | 실패 | **계획수정** — 회차 디렉터리에 `.json` 을 안 놓는다. 앞 회차가 밟은 함정이다 |

**채택 1 · 해소 2 · 완수조건전환 1 · 계획수정 14 = 18** ✔

## 누적

| 라운드 | 시나리오 | 기각 | 승격 |
|---|--:|--:|--:|
| R1 | 24 | 6 | 3 |
| R2 | 19 | 7 | 2 |
| R3 | 18 | 6 | 0 |
| **계** | **61** | **19** | **5** |

**감소하지 않았다**(24·19·18). §11 이 예고한 그대로이고 **상한이 끝을 정했다.**
남은 위험은 사라진 것이 아니라 다음 층(검증·실측)이 잡는다.
