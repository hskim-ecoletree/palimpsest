# 사전부검 라운드 1 — 원 반환문 (`pal-premortem-sweeper`, 2026-08-22)

> §7 이 요구하는 보존이다. **합계 검산의 독립된 둘째 원천** — 처분표(`r1-disposal.md`)를
> 쓴 손이 자기 수를 세면 그것은 검산이 아니라 항등식이다.
> 하네스가 지시형 패턴(`settings-json`)을 감지해 제어 태그를 중화했다(`<` → `&lt;`).

## 어떻게 조사했나

`pal` 은 PATH 에 없고(`target/debug/pal` 바이너리만 있다) 이 계획의 대상이 대부분
마크다운·규약면이라 **그래프에 안 물었다.** 아래 `조회` 는 전부 파일 직접 읽기 + grep +
격리 프로토타입 실행(`/tmp/premortem-21check-20260822/probe.py`)이다.
**그래프를 못 쓴 사실을 여기 적는다 — 그것이 §6 이 요구하는 「능력 부재」 표기다.**

**시나리오 24 건 · 기각 6 건.**

---

### 1. 모집단 규칙이 발단 게이트를 뺀다 — `rust-extractor.md` 는 `intent.md` 를 안 가리킨다
- 어떻게 실패하나: 계획 ③ 의 모집단은 *"`.palimpsest/rounds/<slug>/intent.md` 를 가리키는 게이트만"*. `docs/gates/rust-extractor.md` 는 `:4` 에서 **회차 디렉터리**를 가리키고 `:55` 의 `` (`intent.md`) `` 는 코드 스팬이지 링크가 아니다. 프로토타입을 돌리니 이 게이트가 **모집단에서 통째로 빠졌다.** 회차의 RED 를 세운 바로 그 게이트가 21 번째 검사에 안 들어간다.
- 어디가 걸리나: `docs/gates/rust-extractor.md:4`·`:55` · `plan.md:64-65`
- 획득: 조회 — `grep -n 'intent.md' docs/gates/*.md` + 프로토타입 실행 결과에 `rust-extractor.md` 가 안 뜸
- 모집단: 회차기록 · 유효성: 참 · 해악도: 금지역(측정이 죽은 가지) · 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(한 줄 링크 추가). 걸리는 곳 1. **다만 고치는 방법이 「게이트에 링크를 더한다」라 모집단이 손으로 조정되는 것과 같아진다.**

### 2. 모집단에 계획이 안 옮긴 게이트 둘이 든다 — 조건 78 개
- 어떻게 실패하나: `intent.md` 를 실제로 가리키는 게이트는 **셋**이다 — `round-completion-condition.md:4`(조건 41) · `round-finding-records.md:9`(46) · `inventory-disposal.md:12`(37, 평문). 계획은 **46+44=90 만** 전사한다. 검사가 서는 순간 옮기지 않은 두 회차(41+37=78)가 형식 오류로 빨개지거나, 초록으로 만들려면 모집단을 손으로 베어야 한다 — 계획 스스로 *"모집단을 손으로 안 벤다"* 고 적었다.
- 어디가 걸리나: `plan.md:64-65`·`plan.md:112-122` · `docs/gates/round-completion-condition.md:4` · `docs/gates/inventory-disposal.md:12`
- 획득: 조회 — `grep -l 'palimpsest/rounds' docs/gates/*.md` → 4 건 · 절 넷 완비 게이트도 정확히 같은 4 건
- 모집단: 저장소 · 유효성: 참 · 해악도: 실패(CI 가 빨개진다) · 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. **걸리는 곳 2 게이트 · 조건 78 · 회차 2.** 계획의 「안 하는 것」이 이 둘을 면제하지 않는다 — 그 절은 「옛 게이트 35 개」만 면제하고 이 둘은 새 형식 게이트다.

### 3. `inventory-disposal` 회차엔 조건 ID 가 하나도 없다 — 37/37
- 어떻게 실패하나: 그 회차의 완수 조건 37 개는 전부 `- [x] **MCP 자산이 0 이다** — …` 꼴이고 **ID 가 없다.** 프로토타입 파서가 37 줄 중 0 개를 파싱했다. ⑤ 의 엄격 파서는 이 회차를 재현 불가능한 형식 오류로 본다. 초록으로 만드는 유일한 길은 **닫힌 회차의 잠긴 의도에 2026-08-22 가 ID 를 새로 붙이는 것**이고, 그러면 그 회차가 무엇에 대해 통과했는지의 사전 등록성이 깨진다(`docs/gates/README.md:10-14` 가 금지한 자와 같은 종).
- 어디가 걸리나: `.palimpsest/rounds/2026-08-18-inventory-disposal/intent.md:43-146` · `docs/gates/inventory-disposal.md:86-89`
- 획득: 조회 — 프로토타입 출력 `조건 줄 37 · ID 파싱됨 0 · ID 없는 줄 37`
- 모집단: 저장소 · 유효성: 참 · 해악도: 금지역(사실이 아닌 것을 사실로) · 대상: 계획대상
- 얼마나 아픈가: 되돌리기 비싸다(닫힌 회차 개작은 git 이력이 남을 뿐 문서 자격은 안 돌아온다). 걸리는 곳 1 회차 · 37 조건.

### 4. `## 판정` 절의 ID 정규식은 잡음이 지배한다
- 어떻게 실패하나: 계획 ③ 은 *"게이트 `## 판정` 의 ID 집합"* 을 잰다고만 적는다. 그 절을 ID 정규식으로 훑으면 실제로 나오는 것: `inventory-disposal` → **`F04`~`F18`·`C2`~`C6`·`P` 계열 31 개**(F 번호 대응표·이슈 표에서 온 것) · `round-finding-records` → **`R8`·`R10`~`R12`(정정 라운드 번호)·`S01`(사전부검 시나리오 ID)** · `round-completion-condition` → **`P1`·`P2`·`P3`(진자 지표)**. 전부 조건 ID 와 문법이 같다. 잡음을 걸러내려면 「새 형식으로 쓴 표만 읽는다」로 좁혀야 하고, 그러면 **검사는 우리가 다시 쓴 게이트만 보게 되어 통과가 자기충족**이 된다.
- 어디가 걸리나: `plan.md:66-67` · `docs/gates/inventory-disposal.md:86-226` · `docs/gates/round-finding-records.md:38-114`
- 획득: 조회 — 프로토타입의 `게이트에만:` 목록 세 줄
- 모집단: 저장소 · 유효성: 참 · 해악도: 금지역(측정이 죽은 가지 — 자기가 만든 형식만 재는 검사) · 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳 3 게이트.

### 5. 전사한 두 회차에서 21 번째 검사는 항등식이다 — RED 도 음성 대조도 없다
- 어떻게 실패하나: 90 개 상자와 두 게이트의 ID 표를 **같은 커밋에서 같은 손이** 하나의 원천(게이트 판정 표 역산)에서 써 넣는다. 그러면 「두 원장이 어긋나는가」는 원리상 어긋날 수 없다 — 계획 ③ 이 *"둘을 남기고 대는 것이 독립된 두 원천을 만든다"* 고 주장한 것이 이 회차에 한해 거짓이다. 계획 어디에도 **이 검사의 RED 관측**도 **음성 대조**도 등록돼 있지 않다. `plan.md` 전문에 「음성 대조」라는 낱말이 0 회다.
- 어디가 걸리나: `plan.md:58-69` · 규약 `SKILL.md:67-71`(셋을 함께 단다)·`:246`(항등식)
- 획득: 조회 — `grep -c '음성 대조' plan.md` = 0 · 계획 ⑦ 이 전사 원천을 게이트 역산으로 명시
- 모집단: 계획자신 → 자기장치 · 유효성: 참 · 해악도: 금지역(측정이 죽은 가지) · 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(격리 사본에 어긋남을 심어 발화 확인). 걸리는 곳 1 검사.

### 6. 게이트에 43 개 ID 를 써 넣으면 2026-08-22 의 추론이 2026-08-20 의 판정으로 박제된다
- 어떻게 실패하나: `rust-extractor.md:56-62` 의 판정 표는 **수 넷과 `B3` 하나**만 적는다. 어느 43 개가 통과인지는 그 회차가 적은 적이 없고, 계획은 그것을 *"나머지 43 통과"* 로 **역산**한다. 그 43 개 ID 목록을 게이트 `## 판정` 에 적는 순간, 문서는 「2026-08-20 회차가 이 43 개를 각각 재고 통과로 판정했다」고 말하게 된다. 실제로는 같은 문서 `:85-89` 가 *"독립 리뷰 R1 이 내 판정을 뒤집었다 … 40/1/2/1 을 냈다"* 라고 적고, `round-finding-records.md:40-43` 은 *"C2-b 를 반증으로 냈고 닫았다 — ★ 그 수정을 다시 잰 리뷰는 없다"* 라고 적는다. **다시 안 잰 통과가 ID 로 못 박히면 그 유보가 사라진다.**
- 어디가 걸리나: `docs/gates/rust-extractor.md:56-62`·`:85-89` · `docs/gates/round-finding-records.md:40-43` · `plan.md:121-122`
- 획득: 조회 — 두 게이트 판정 절 정독
- 모집단: 저장소 · 유효성: 참 · 해악도: 금지역(사실이 아닌 것을 사실로) · 대상: 계획대상
- 얼마나 아픈가: 되돌리기 중간(각 ID 옆에 「2026-08-22 전사」 표기를 달면 완화된다). 걸리는 곳 2 게이트 · 조건 90.

### 7. 판정 표에서 수를 빼면 §9 의 「넷의 합 = 등록 수, 검산하고 적는다」와 부딪힌다
- 어떻게 실패하나: 계획 ② 는 수를 지우고 ID 만 남긴다. 그런데 `SKILL.md:283-285` 는 *"통과·반증·대조불가·미측정. **원문 수치와 함께.** ★ 넷의 합이 등록된 조건 수와 같아야 한다 — 검산하고 적는다"* 이고, 그 문장은 **실측된 오계수에서 태어났다**(`round-completion-condition.md:31-35`: 30/9 로 적었다가 38/2/1/0 으로 정정, 같은 커밋 안에서 9 와 11 이 공존). 규약을 안 고치면 두 규칙이 서로를 어기고, 고치면 **모집단 밖 35 개 게이트에는 아무 규칙도 안 남는다.**
- 어디가 걸리나: `.claude/skills/round/SKILL.md:281-287` · `plan.md:40-56`
- 획득: 조회 — SKILL §9 원문 · 게이트 `round-completion-condition.md:31-35`
- 모집단: 규약 · 유효성: 참 · 해악도: 거짓신호 · 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳 규약 1 절 · 게이트 35.

### 8. `findings.jsonl` 에 「열림」 축이 없다 — 훅의 「열린 발견」은 원리상 못 읽힌다
- 어떻게 실패하나: 계획 ⑥ 은 *"**열린 발견** 중 해악도가 금지역·실패인 것이 있으면 회차는 막힘"* 이고 *"이미 있는 `findings.jsonl` 의 해악도 enum 을 읽기만 한다 — 새 선언면이 안 생긴다"* 고 주장한다. 그런데 스키마에 열림/닫힘 축이 **없다**. `REQUIRED` 는 `id·라운드·출처·모집단·유효성·해악도·처분·경로·요약` 이고 `처분` 은 항상 여섯 값 중 하나로 **이미 처분된 상태**만 표현한다. 「열림」을 읽으려면 필드를 더해야 하고 그것은 깨지는 변경이라 `SCHEMA_VERSION` 3 이며 — `record.py:47-50` 이 적듯 **버전 1 로 정당하게 쓰인 레코드가 스키마 1 에서 전부 실패한 전례**가 있다.
- 어디가 걸리나: `.claude/skills/round/bin/record.py:53-72`·`:80` · `plan.md:102-106`
- 획득: 조회 — `record.py` ENUM·REQUIRED 정독
- 모집단: 저장소 · 유효성: 참 · 해악도: 금지역(측정이 죽은 가지 — 훅의 판정 입력이 존재하지 않는다) · 대상: 계획자신
- 얼마나 아픈가: 되돌리기 중간. 걸리는 곳 `record.py` · `xtask` 위임 · 기존 레코드 두 파일(합 431 행).

### 9. 해악도만 읽으면 두 회차 다 언제나 막힘이다 — 훅이 영영 안 막는다
- 어떻게 실패하나: 8 을 우회해 「기록된 모든 금지역·실패 발견」으로 읽으면, 실측 분포가 이렇다 — `2026-08-19-finding-records`: 금지역 **54** · 실패 **15**, `2026-08-20-rust-extractor`: 금지역 **36** · 실패 **11**. 발견 레코드를 쓰는 회차는 **첫 라운드부터 언제나 「막힘」**이 되고, 「막힌 회차는 차단하지 않는다」에 따라 Stop 훅은 **한 번도 안 막는다.** NEXT-C 가 등록한 음성 대조(*"훅이 한 번도 안 막으면 너무 느슨하다"*)가 그대로 발화한다.
- 어디가 걸리나: `.palimpsest/rounds/2026-08-{19,20}-*/findings.jsonl` · `plan.md:102-103`
- 획득: 조회 — 두 `findings.jsonl` 을 파이썬으로 집계
- 모집단: 저장소 · 유효성: 참 · 해악도: 금지역(만든 즉시 안 도는 장치) · 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳 훅 정책 1.

### 10. `stop_hook_active` 조기 통과가 Stop 에도 걸린다 — 차단은 세션당 한 번뿐
- 어떻게 실패하나: `policy.rs:57-59` 는 **EVENTS 안의 모든 사건**에 대해 `stop_hook_active == true` 면 무조건 `Pass` 한다. `"Stop"` 을 EVENTS 에 더하면 이 규칙을 그대로 상속한다. 실측 주석(`:25`)대로 2 회차부터 `true` 가 들어오므로, 에이전트는 **한 번 막힌 뒤 다시 멈추면 무조건 통과**한다. 계획 ⑥ 의 「진행 인지형 자기 상한 — N 회 막히면 해제」는 N≥2 인 순간 도달 불가능한 죽은 코드가 된다.
- 어디가 걸리나: `crates/pal-cli/src/hook/policy.rs:57-60`·`:86-91` · `plan.md:96`
- 획득: 조회 — `decide` 본문과 시험 `반복_회차는_같은_페이로드에서도_통과한다`
- 모집단: 저장소 · 유효성: 참 · 해악도: 금지역(자기 상한 로직이 도달 불가) · 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳 1 함수 · 시험 4.

### 11. `EVENTS` 는 등록면과 판정면을 겸한다 — 「만들되 안 켠다」가 원리상 불가능하다
- 어떻게 실패하나: `policy.rs:33-36` 이 못 박는다 — *"**등록도 이 목록으로 한다** — 판정하는 자리와 등록하는 자리가 갈리면 등록만 남고 판정이 사라진 훅이 조용히 돈다."* 그리고 `layout.rs:278` 이 `HOOK_EVENTS = crate::hook::EVENTS` 다. 소유자가 잠근 「만들되 점등은 별도 승인」을 만족하려면 셋 중 하나뿐인데 전부 나쁘다: (a) EVENTS 에 `Stop` 을 더하고 정책은 늘 Pass → **점등된 죽은 가지**, (b) EVENTS 에 안 더하고 코드만 둔다 → **부르는 자 0 인 죽은 코드**, (c) 등록면과 판정면을 가른다 → **그 파일이 명시적으로 금지한 형태**.
- 어디가 걸리나: `crates/pal-cli/src/hook/policy.rs:32-36` · `crates/pal-cli/src/install/layout.rs:278` · `plan.md:93`
- 획득: 조회 — 두 상수와 그 위 주석
- 모집단: 규약 · 유효성: 참 · 해악도: 금지역(어느 갈래를 골라도) · 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. **걸리는 곳: 소유자 승인 자리 하나가 원리상 못 서는 형태다** — 승격으로 올려야 할 물음이지 구현으로 넘길 물음이 아니다.

### 12. `decide` 는 순수 함수다 — 회차·파일시스템·git 이 필요한 정책은 서명을 바꾼다
- 어떻게 실패하나: `pub fn decide(event: &str, payload: &Value) -> Decision` 이고 파일도 프로세스도 안 건드린다. 계획의 Stop 정책은 ① 어느 회차인지 알아야 하고 ② `intent.md` 를 읽어야 하고 ③ `findings.jsonl` 을 읽어야 하고 ④ 「판정 커밋」을 찾으려면 git 을 불러야 한다. 서명이 바뀌면 `hook.rs:64` 와 시험 넷이 함께 움직인다. 그리고 **하네스의 cwd 가 저장소 루트라는 보장이 없다** — 못 찾으면 `hook.rs` 규약대로 조용히 통과하므로, 가장 필요한 상황에서 훅이 아무것도 안 한다.
- 어디가 걸리나: `crates/pal-cli/src/hook/policy.rs:52-67` · `crates/pal-cli/src/hook.rs:64-73`·`:21-24`
- 획득: 조회 — 서명·호출부·모듈 머리 주석
- 모집단: 저장소 · 유효성: 참 · 해악도: 실패 + 거짓신호 · 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳 2 파일 · 시험 4 · 격리 시험 가능성 상실.

### 13. Stop 의 차단 프로토콜은 실측된 적이 없다
- 어떻게 실패하나: `hook.rs:14-20` 은 *"**`SubagentStop` 의** 차단은 넷 중 하나로 전달되고, 우리가 쓰는 것은 `exit 0` + 표준출력 `{"decision":"block",…}` 이다"* 라고 **사건을 명시해** 적었다. `Stop` 에 대해 같은 형태가 먹는지는 이 저장소에 관측이 없다. 안 먹으면 훅은 표준오류에 「차단」이라 적으면서 실제로는 아무것도 안 막고, `pal doctor` 도 「등록된 명령이 돈다」만 보므로 못 가른다.
- 어디가 걸리나: `crates/pal-cli/src/hook.rs:9-20`·`:26-32`
- 획득: 조회 — 모듈 주석의 실측 범위 + `grep -rn 'SubagentStop'` 이 시험 41 자리 전부를 그 사건으로만 덮는 것 확인. **Stop 페이로드는 못 돌려 봤다**(하네스 없이 재현 불가)
- 모집단: 저장소 · 유효성: **추정** · 해악도: 금지역(「차단했다」고 적고 안 막는다) · 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. **완수 조건으로 전환할 값이 크다** — 「실제 Stop 에서 차단이 먹는 것을 관측했다」.

### 14. 「그 회차의 판정 커밋」에 기계 표기가 없다 — 새 표기가 태어난다
- 어떻게 실패하나: 계획 ⑥ 의 정책은 *"그 회차의 판정 커밋이 없으면 차단"* 인데, 저장소가 아는 커밋 제목 표기는 **`[R<n>]` 과 `[승격]` 둘뿐**이다(`SKILL.md:122-127`). 「판정 커밋」을 기계로 알려면 셋째 표기를 만들어야 하고, `SKILL.md:129-133` 이 정확히 그 자리를 경고한다 — *"표기 규약이 회차 안에서 태어나면 갈린다"*(실측: 연쇄 깊이 13→11). 계획 ⑥ 의 *"새 선언면이 안 생긴다"* 가 이 지점에서 거짓이 된다.
- 어디가 걸리나: `.claude/skills/round/SKILL.md:122-133` · `plan.md:95`·`:105-106`
- 획득: 조회 — SKILL 표기 표 · `dashboard.py:52-67`(읽는 표기가 `[R<n>]` 하나)
- 모집단: 규약 · 유효성: 참 · 해악도: 거짓신호 · 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳 규약 1 · 훅 1 · 계기판 1.

### 15. Stop 등록이 전 설치본으로 번지는데 시험은 전부 `"SubagentStop"` 리터럴이다
- 어떻게 실패하나: `HOOK_EVENTS = crate::hook::EVENTS` 라 `Stop` 을 더하면 **`pal install`·`update` 를 돌린 모든 프로젝트**가 Stop 훅을 받는다. 그런데 `install_hooks.rs` 의 27 자리, `hook.rs` 의 14 자리 — **41 개가 전부 문자열 `"SubagentStop"`** 이고, `HOOK_EVENTS` 를 도는 반복문은 시험에 **하나도 없다**. 그래서 Stop 에 대한 **멱등성 · `uninstall` 복원(S2==S0) · 남의 훅 보존 · 옛 형태 이주**가 전부 미관측으로 출하된다. `install_hooks.rs:471-475` 는 `m["settings"]["hooks"][0]` 처럼 **인덱스 0** 을 박아 두어 EVENTS 순서에도 매여 있다.
- 어디가 걸리나: `crates/pal-cli/tests/install_hooks.rs`(27 자리)·`crates/pal-cli/tests/hook.rs`(14 자리) · `crates/pal-cli/src/install/layout.rs:278`
- 획득: 조회 — `grep -c 'SubagentStop'` · `grep -n 'HOOK_EVENTS\|for .*event' tests/` = 0
- 모집단: 저장소 · 유효성: 참 · 해악도: 금지역(데이터 손실 — `uninstall` 이 남의 설정에 우리 등록을 남기거나 남의 것을 지운다) · 대상: 계획대상
- 얼마나 아픈가: **되돌리기 비싸다(남의 프로젝트 설정에 남는다).** 걸리는 곳 시험 2 파일 · 설치 표면 전부.

### 16. `verify.py` 가 설치 표면 셋에 안 들어가면 태어나면서 죽은 가지다
- 어떻게 실패하나: 계획은 `verify.py` 를 `record.py` 옆에 놓는다고만 적고 **`PAYLOAD`·`OWNED_FILES`·`DIRS` 를 자리 배치 표에 안 적었다.** `layout.rs:146-149` 가 그 사고를 이미 기록한다 — *"`PAYLOAD` 에 더하면서 여기 안 더하면 놓기는 하는데 `update`·`uninstall` 이 거부한다 — 실측 2026-08-19: 되돌릴 수 없는 상태가 됐다."* 그리고 `SKILL.md` 는 `include_str!` 로 설치본에 그대로 실리므로, 규약이 `verify.py` 를 부르는데 설치본에 물건이 없으면 「태어나면서 죽은 가지」다.
- 어디가 걸리나: `crates/pal-cli/src/install/layout.rs:27-95`·`:141-172` · `plan.md:149-156`
- 획득: 조회 — `PAYLOAD`/`OWNED_FILES`/`DIRS` 목록 전수 + 계획의 자리 배치 표
- 모집단: 저장소 · 유효성: 참 · 해악도: 금지역 (다만 `layout.rs` 자체 시험이 절반은 잡는다) · 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳 상수 3 · 시험 2.

### 17. `verify.py` 는 마크다운에 적힌 임의 명령을 실행한다 — 기본 금지역 「대표 취약점」
- 어떻게 실패하나: `CHECK: <셸 명령>` 을 `intent.md` 에서 읽어 실행한다. `intent.md` 는 에이전트가 쓰고 브랜치·PR 로 들어오는 파일이고, `verify.py` 는 **설치본에도 실려 남의 저장소에서 남의 `intent.md` 를 태운다.** `SKILL.md:389` 의 기본 금지역이 *"대표_취약점 = **주입** · 경로 탈출 · 역직렬화 · 비밀 노출"* 을 든다. 계획 전문에 신뢰 경계·샌드박스·허용 명령 목록에 대한 문장이 **한 줄도 없다.** `xtask` 가 이미 `python3 record.py check` 를 서브프로세스로 부르므로(`main.rs:3320-3325`) 「임의 명령은 CI 밖」이라는 방벽도 완전하지 않다.
- 어디가 걸리나: `plan.md:71-79` · `.claude/skills/round/SKILL.md:382-390` · `xtask/src/main.rs:3320-3325`
- 획득: 조회 — 계획 ④ 전문 · 기본 금지역 목록 · `grep -n '주입\|신뢰\|샌드박스' plan.md` = 0
- 모집단: **원의도** · 유효성: 참 · 해악도: 금지역(대표 취약점 — 주입) · 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(수용 사유를 명시하는 것도 처분이다). 걸리는 곳 새 파일 1 · 설치 표면.

### 18. `EXPECT` 를 절대값에서 정규식으로 바꾸는 것은 §5 의 「완화」다
- 어떻게 실패하나: 계획 ④ 는 *"`EXPECT` 는 절대값이 아니라 관계·상태로 쓴다 — `EXPECT: /Rust\s+L1/` 가 답이다"* 로 규율을 등록한다. `SKILL.md:154` 는 *"고친 뒤 그 검사가 재는 의도의 양이 **늘었으면 정정, 줄었으면 완화**다. 완화는 축소라 올린다. 이 자가 없으면 「테스트를 약하게 만들어 GREEN 을 얻는」 길이 정정으로 위장한다"* 이다. `117 파일` → `/Rust\s+L1/` 은 **재는 양이 준다**(파일 수를 안 잰다). 승격 사유인데 계획은 규율 등록으로 다룬다. 그리고 `EXPECT: /./` 나 `EXPECT: 확인` 을 막는 것이 아무것도 없다 — NEXT-C:170-171 이 실측한 자기충족 형태 그대로다.
- 어디가 걸리나: `plan.md:81-82` · `.claude/skills/round/SKILL.md:152-154` · `NEXT-C-agent-laziness.md:170-171`
- 획득: 조회 — 규약 §5 정정 문단 · 계획 ④
- 모집단: 규약 · 유효성: 참 · 해악도: 금지역(약해진 검사가 통과로 위장) · 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. **걸리는 곳: 앞으로 쓰이는 모든 `EXPECT`.** 음성 대조를 함께 등록하지 않으면 원리상 관측 불가.

### 19. 들여쓴 조건 상자가 계기판 ② 에 안 보인다 — ④ 의 형식이 그 형태를 부른다
- 어떻게 실패하나: `dashboard.py:125-126` 의 정규식은 `^- \[ \]` 로 **들여쓰기를 허용하지 않는다.** 격리 사본에서 확인했다 — `- [x] A1 … / '  - [ ] A1-a 하위'` 를 넣으니 **열림 0 · 닫힘 1**. 계획 ④ 의 `CHECK:`/`EXPECT:` 는 조건 아래 **들여쓴 줄**로 붙으므로, 하위 조건을 들여쓰는 습관이 곧 생긴다. 그러면 미측정 하위 조건이 ② 에서 **보이지 않고**, 지금 닫으려는 금지역이 한 겹 아래에서 그대로 재발한다. 오늘은 다섯 회차 전부 들여쓴 상자가 0 이라 아직 안 터졌다.
- 어디가 걸리나: `.claude/skills/round/bin/dashboard.py:125-126` · `plan.md:75-79`
- 획득: 조회 — 격리 사본에 심어 재현 · `grep -cE '^ +- \[ \]'` 로 현 상태 0 확인
- 모집단: 저장소 · 유효성: 참 · 해악도: 금지역(미판정이 0 으로 보인다) · 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(정규식 한 줄). 걸리는 곳 `dashboard.py` 1 · 21 번째 검사의 파서 1.

### 20. `SKILL.md:455` 의 「검사 20 개」가 21 번째로 거짓이 된다
- 어떻게 실패하나: 규약이 CI 판별식 자리에 *"검사 20 개는 파일을 읽어 판정할 뿐이라 세 OS 에서 같은 답을 낸다. ⚠ 이 수도 손으로 벤 거울이다 — 세는 자리는 `xtask` 의 `checks.len()` 이다"* 라고 적는다. 21 번째를 더하는 순간 그 수가 낡는다. 자기 인지를 이미 붙여 놨으므로 해악은 작지만, **이 회차의 주제가 「수를 안 적는다」인데 자기 규약 안의 손으로 벤 수를 그대로 두면 그 자체가 표본이 된다.**
- 어디가 걸리나: `.claude/skills/round/SKILL.md:455` · `xtask/src/main.rs:559`
- 획득: 조회 — `grep -rn '검사 20'`
- 모집단: 규약 · 유효성: 참 · 해악도: 거짓신호 · 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳 1 줄.

### 21. §11③ (나) 가 아무것도 안 가른다 — 이 회차의 장치는 전부 금지역 예외로 본 모집단에 들어온다
- 어떻게 실패하나: `.claude/pal/policy.toml` 이 **없고**(실측) 계획도 금지역 목록을 등록하지 않으므로 **기본 다섯**이 정본이다. 그중 「측정이 죽은 가지」가 있고, 이 회차가 만드는 넷은 **전부 측정 장치**다. §11③ 의 예외(*"자기 장치에 대한 발견이 금지역에 닿으면 본 모집단이다. 미룰 수 없다"*)가 사실상 모든 자기 장치 발견에 걸린다 → 계획 `:158-161` 의 「별도 목록」이 비고, §11 의 음성 대조 *"「별도 목록」이 매 회차 비어 있으면 — 모집단 분리가 아무것도 안 가르고 있다"* 가 발화한다. 위 시나리오 1·4·5·8·9·10·11·16 이 전부 이 부류다.
- 어디가 걸리나: `.claude/skills/round/SKILL.md:400-439` · `plan.md:158-161` · `.claude/pal/` 부재
- 획득: 조회 — `ls .claude/pal/` → No such file · 계획에 금지역 등록 절 없음
- 모집단: 규약 · 유효성: 참 · 해악도: 실패(해악 게이트가 상한에서 「막힘」을 내고 회차가 안 닫힌다) · 대상: 계획자신
- 얼마나 아픈가: 되돌리기 중간. **처분 방법이 셋뿐이다** — 잠긴 의도가 금지역 목록을 좁혀 등록하거나, 장치 수를 줄이거나, 「막힘」으로 끝날 것을 착수 시점에 받아들이거나.

### 22. 「만들 것 넷」인데 하위 절이 여덟이다 — 별도 목록의 경계가 안 정해진다
- 어떻게 실패하나: `plan.md:23` 이 `## 만들 것 넷` 인데 ①~⑧ 여덟 절이 달린다. §11③ 의 「이 회차가 만든 장치」가 무엇인지가 판정 모집단을 가르는 축인데, 그 축의 원소 수가 문서 안에서 넷과 여덟으로 갈린다. 종료 시 「별도 목록」이 무엇을 담아야 하는지 대조할 원문이 없다.
- 어디가 걸리나: `plan.md:23-131`
- 획득: 조회 — 절 제목 셈
- 모집단: 회차기록 · 유효성: 참 · 해악도: 거짓신호 · 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳 1 줄.

### 23. 더 작은 표면이 같은 답을 낸다 — 계기판 ② 가 게이트를 읽으면 장치 넷이 필요 없다
- 어떻게 실패하나: 금지역의 정의는 *"판정 원장이 두 자리에 있고 **한 자리가 죽었다**"* 이고 거짓 신호를 내는 것은 **계기판 ② 하나**다. 최소 처분은 `dashboard.py:122-129` 가 그 회차의 게이트 `## 판정` 을 찾아 읽고, 못 찾으면 「못 셌다」를 내는 것이다(⑥⑦⑧ 이 이미 쓰는 어법). ~15 줄이고 **새 선언면이 0** 이다. 계획은 이 길을 §7 항등식으로 물리치는데(`:60-62`), §7 이 금지한 것은 **게이트를 `intent.md` 에서 렌더링하는 것**(쓰는 방향)이고 **계기판이 게이트를 읽는 것**(읽는 방향)은 그 논변에 안 걸린다. **논변이 반대 방향에 잘못 적용됐다.**
- 어디가 걸리나: `.claude/skills/round/bin/dashboard.py:122-129` · `.claude/skills/round/SKILL.md:244-247` · `plan.md:58-62`
- 획득: 조회 — §7 원문의 문맥(합계 검산의 둘째 원천)과 계획 ③ 의 인용 대조 · `dashboard.py` ② 구현 정독
- 모집단: **원의도** · 유효성: **추정** — 소유자가 인터뷰에서 잠근 것은 「ID 를 적는다」와 「집합 같음」이라 **더 작은 표면이 소유자의 잠금을 만족하는지는 내가 못 판정한다**
- 해악도: 거짓신호 · 대상: 계획자신
- 얼마나 아픈가: 되돌리기 비싸다 — 장치 넷을 만든 뒤에는 못 되돌린다. **착수 전에 던져야 하는 물음이다.**

### 24. 게이트 없는 회차는 게이트 구동 모집단에 원리상 안 보인다
- 어떻게 실패하나: `2026-08-18-round-protocol` 은 완수 조건 7 개(`[x]` 7, ID 없음)를 갖고 **`docs/gates/` 에 짝이 없다.** 모집단을 게이트에서 출발시키면 「원장이 두 자리인데 한 자리가 죽었다」의 형제 — **「원장이 아예 하나도 없다」** — 를 검사가 못 본다. 회차 디렉터리에서 출발했으면 잡혔을 것이다.
- 어디가 걸리나: `.palimpsest/rounds/2026-08-18-round-protocol/intent.md:20-26` · `plan.md:64-65`
- 획득: 조회 — `grep -rln 'round-protocol' docs/` → 게이트 없음 · 그 intent 의 조건 7 줄
- 모집단: 저장소 · 유효성: 참 · 해악도: 거짓신호 · 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(양방향으로 대면 된다). 걸리는 곳 1 회차 · 검사 1.

---

## 내가 기각한 것 (6)

| # | 제목 | 왜 아니었나 | 어디가 걸리나 | 모집단 | 유효성 | 해악도 |
|---|---|---|---|---|---|---|
| R1 | RED 수치가 낡았거나 틀렸다 | 넷 다 실측 재현 — 열린 상자 **90** · 계기판 ② **44/44** · `EVENTS` 하나 · `CHECK:` **0** · 절 넷 완비 **4** | `plan.md:12-18` | 회차기록 | 거짓 | 미관 |
| R2 | 전사 역산이 틀렸다 (finding-records 46) | 묶음 표 합 6+4+6+5+3+3+5+3+0+6+1+2 = **44 통과**, 대조불가 2 = `C9-a`·`C9-b`, `intent.md` 묶음별 수와 **12/12 일치** | `docs/gates/round-finding-records.md:51-63` | 회차기록 | 거짓 | 미관 |
| R3 | 게이트 둘을 고치는 것이 동결 규약 위반이다 | `README.md:3` 이 동결 범위를 **`F*`·`G*`·`S*`·`preflight`** 로 명시하고 `xtask` 의 `동결된_판정_문서인가`(`main.rs:2365-2372`)가 같은 접두로 가른다. 둘은 그 밖이다 | `docs/gates/README.md:1-8` | 규약 | 거짓 | 미관 |
| R4 | 상한에 「소진」이라는 반증된 말을 다시 썼다 | `rust-extractor/intent.md:4` 가 *"인터뷰 4 소진"* 을 이미 쓴다. 여기서 「소진」은 반증된 종료 조건이 아니라 「상한을 다 썼다」다 | `plan.md:165` | 규약 | 거짓 | 미관 |
| R5 | `- [x] A1 … · 통과` 태그가 계기판 ② 정규식을 깨뜨린다 | 격리 사본에서 심어 봤다 — `^- \[[xX]\]` 는 접두만 보므로 뒤에 무엇이 붙어도 센다. **안 깨진다** | `dashboard.py:125-126` | 자기장치 | 거짓 | 미관 |
| R6 | 21 번째 검사가 마크다운을 처음 읽어 CI 판별식이 갈린다 | 죽은 링크·sunset·「사라진 문서 인용」 셋이 이미 마크다운과 `.palimpsest/` 를 읽고 `SKILL.md:441-451` 이 그 정정을 적어 놨다 | `SKILL.md:441-451` | 규약 | 거짓 | 미관 |

---

## 새 범주

**부분 점등 불가 — 등록면과 판정면을 겸하는 상수 위에 「만들되 안 켠다」를 얹으려는 자리**
(시나리오 11). 시딩 목록의 「통합·되돌리기」와도 「stub」과도 다르다. 켜도 죽은 가지,
안 켜도 죽은 코드, 가르면 그 파일이 명시적으로 금지한 형태 — 세 갈래가 전부 막히는
구조라서 **구현 단계가 아니라 승인 단계에서 처분해야 한다.**
