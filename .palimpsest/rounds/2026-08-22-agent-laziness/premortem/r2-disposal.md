# 사전부검 라운드 2 — 처분표 (시나리오 19 · 기각 7)

원 반환문: `r2-raw.md`. 대상은 계획 **판 2**. 처분 결과는 **판 3**.

| # | 시나리오 | 해악도 | 처분 |
|---|---|---|---|
| 1 | 계기판 ② 가 빈 커밋 범위에서 아예 안 뜬다(조기 return) · 그것을 재는 시험이 언제나 빈 범위 | 거짓신호 | **계획수정** — 2026-08-19 가 ⑦⑧ 에만 고친 것을 ②③④⑤⑥ 에도 고친다. 시험의 착수 커밋도 고친다 |
| 2 | 들여쓰기를 `:125` 만 고치면 분모가 깨진다(`2/3`) — 짝 `:126` 이 남는다 | 거짓신호 | **계획수정** — 둘 다. 자리 배치에 `:125-126` 으로 적는다 |
| 3 | 계기판 ② 가 코드펜스와 `## 범위 밖` 의 상자를 센다 — 이 회차가 형식을 예시로 적는 순간 RED 가 거짓 | **금지역** | **계획수정** — 파서가 펜스와 절을 안다. **본 모집단**(이 회차의 수치를 거짓으로 만든다) |
| 4 | 이주 모집단이 431 이 아니라 **432 행 · 파일 넷 · 559 줄** · 게이트 39 가 아니라 **38** | **금지역** | **계획수정** — 실측으로 정정하고 **계획에서 수를 걷는다.** 예외표 둘의 버전 표류를 대는 장치가 없다는 것도 함께 |
| 5 | 설치본에서 `record.py add` 가 버전 다른 파일에 조용히 덧붙인다(rc=0) | 금지역 | **계획수정** — `add` 가 머리 줄 버전을 본다. 퇴로(v2 유지)를 쓰면 이 위험은 안 생긴다 |
| 6 | 「열림」 축을 **쓰는 자가 없고 닫는 자도 미정** | 금지역 | **계획수정** — 소비자를 계기판에 세우고 **닫는 자와 시점**을 명시한다 |
| 7 | `verify.py` 허용 목록이 방어가 아니다 — `git -c alias.pwn` 재현 | **금지역** | **해소** — 승격 4 = **G**. `verify.py` 를 안 만든다 |
| 8 | `CHECK` 실행자를 안 만들면 같은 답이 나온다 | 거짓신호 | **채택** — 승격 4 = **G** |
| 9 | `verify.py` 가 「실제로 돈다」를 재는 자리가 없다 | 금지역 | **해소** — 승격 4 = **G** |
| 10 | 체크박스/ID 파서가 Rust·파이썬 두 벌 — C2-b 재발 | 거짓신호 | **계획수정** — `xtask` 가 파이썬 원천에 물어보는 **기존 위임 패턴**(`main.rs:3200-3220`)을 쓴다 |
| 11 | 게이트 짝 찾기 규칙이 5 중 2 만 맞고, frontmatter 경로는 죽은 링크 검사가 안 본다 | 거짓신호 | **계획수정** — **마크다운 링크로 적는다.** 기존 `check_dead_links` 가 공짜로 재준다 |
| 12 | 「지정된 셀」 실측 — 표가 둘 · ID 칸이 3열 산문에 `ⓑ⑤` · finding-records 는 개별 ID 가 없다 | 실패→금지역 | **계획수정** — **묶음 행을 유지하고 ID 열만 더한다**(정보 소실 0) · ⓑ 표는 다른 모집단이라 대조 밖 · *"유일한 계수 자리"* → *"유일한 판정 자리"* |
| 13 | 줄바꿈 조건 **10/90** 에 태그 자리가 없고 `⟨전사⟩` 와 `· 통과` 의 순서가 미정 | 실패 | **계획수정** — 태그는 **첫 줄 끝**에 고정하고 순서를 `· <판정> ⟨전사 …⟩` 로 못 박는다 |
| 14 | frontmatter 옵트인에 하한이 없다 — 저장소가 세 번 놓은 방어의 짝이 없다 | **금지역** | **계획수정** — 하한을 등록한다(형식 2 회차가 0 이면 `bail!`) |
| 15 | 이 회차 게이트가 끝까지 없다 — 검사가 내내 빨갛거나 무력하거나 | 실패/금지역 | **계획수정** — 「게이트 없음」은 **보고**이고, 회차가 `report.md` 를 가지면 **실패**로 승격한다(#74 가 이미 같은 축을 지적했다) |
| 16 | Stop 관측이 세션을 가둘 수 있고 「관측했다」의 합격선이 없다 · `.claude/settings.json` 이 없다 | 실패 | **계획수정 + 완수조건전환** — 관측 프로토콜을 적고 `stop_hook_active` 가드를 필수로, 합격선을 「먹는다/안 먹는다/못 태웠다」 셋으로 사전 등록 |
| 17 | `## 효과` 자리를 Stop 관측에 배정 — §8 은 **「그 회차의 산출을」** 돌린 출력을 요구 | **금지역** | **계획수정** — `## 효과` 는 21 번째 검사·계기판·전사를 돌린 출력. Stop 관측은 별도 절 |
| 18 | 금지역 좁힘이 사후 해석에 열려 있다 — 피발견자가 모집단을 정한다 | 거짓신호 | **계획수정** — 가르는 축을 **하나**로 고정하고 **양방향** 음성 대조를 등록한다 |
| 19 | ⑨ 가 원 의도의 후반절을 통째로 미룬다 | 거짓신호 | **해소** — 승격 5 = **A**. 「가져올 것 N 개를 근거와 함께 판정으로 적는다」 |

**해소 3 · 채택 1 · 계획수정 15 = 19** ✔
