# E9 — 음성 대조 다섯 (격리 사본 `<격리 사본>`) · 2026-08-23

기준선: **검사 21/21 통과**. 아래는 그 위에 하나씩 흠집을 낸 것이다.

### ① 게이트 표에서 조건 ID `A1` 을 지웠다 (검산도 함께 맞췄다)
```
원장 둘 대조: 2026-08-20-rust-extractor: `intent.md` 의 `A1` 가 게이트 표준 표에 없다 — 넷 중 어디에도 안 적히면 그 조건은 조용히 안 세어진다
```

### ② 게이트에서 `A1` 을 통과 → 반증으로 옮겼다
```
원장 둘 대조: 2026-08-20-rust-extractor: `A1` — 게이트는 「반증」, `intent.md` 는 「통과」다. 원장 둘이 갈렸다
```

### ③ `intent.md` 의 `A1` 상자를 끄고 태그를 지웠다
```
원장 둘 대조: 2026-08-20-rust-extractor: `A1` — 게이트는 「통과」, `intent.md` 는 「미측정」다. 원장 둘이 갈렸다
    2026-08-20-rust-extractor: `A1` — 게이트는 「통과」인데 상자가 안 켜져 있다. 상자 켜짐 = 판정이 났다는 뜻이다
```

### ④ 최근 끝난 회차의 게이트 표준 표 헤더를 깨서 「형식 이전」으로 만들었다
```
원장 둘 대조: 하한 미충족: 끝난 회차 중 가장 최근인 `2026-08-20-rust-extractor` 가 이 검사 밖이다 — 표준 표를 세우거나 게이트를 짝지어야 한다. 전역 개수 하한은 과거로 영구 충족되므로 하한을 여기 건다
```

### ⑤ 검산 줄만 고쳐 적힌 수와 ID 목록 길이를 어긋냈다
```
원장 둘 대조: 형식 오류 · docs/gates/rust-extractor.md: 검산이 `통과 42` 이라 적었는데 ID 목록은 43 개다 — 수는 세면 나온다. 캐시를 두지 않는다
    형식 오류 · docs/gates/rust-extractor.md: 검산 합이 43 인데 적힌 ID 는 44 개다
    하한 미충족: 끝난 회차 중 가장 최근인 `2026-08-20-rust-extractor` 가 이 검사 밖이다 — 표준 표를 세우거나 게이트를 짝지어야 한다. 전역 개수 하한은 과거로 영구 충족되므로 하한을 여기 건다
```

