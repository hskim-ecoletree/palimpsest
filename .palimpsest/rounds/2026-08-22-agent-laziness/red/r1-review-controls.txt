# 독립 리뷰 R1 이 잡은 구멍의 음성 대조 (격리 사본) · 2026-08-23

## S4 — **0 개를 훑고 초록**이 가능했다 (금지역: 측정이 죽은 가지)

리뷰어가 격리에서 재현했다: 표준 표 넷을 `—` 로 두고 검산을 `0 … = 0` 으로 적으면
집합 대조가 **공집합끼리** 맞아 `## 완수 조건` 절이 사라져도 **21/21 통과**였다.

고친 뒤 같은 흠집을 다시 냈다:

```
$ (게이트 표준 표 넷을 `—` 로 · intent 의 `## 완수 조건` 절 이름을 바꿈)
조건 0

  FAIL  원장 둘 대조
원장 둘 대조: 형식 오류 · .palimpsest/rounds/2026-08-20-rust-extractor/intent.md: `## 완수 조건` 에 상자가 하나도 없다 — 게이트 `docs/gates/rust-extractor.md` 는 표준 표를 세웠는데 **댈 것이 없다.** 0 건은 「안 부른다」가 아니라 「안 봤다」다
    하한 미충족: 끝난 회차 중 가장 최근인 `2026-08-20-rust-extractor` 가 이 검사 밖이다 — 표준 표를 세우거나 게이트를 짝지어야 한다. 전역 개수 하한은 과거로 영구 충족되므로 하한을 여기 건다
```

**발화한다.** 「모집단이 비면 실패다」가 이 검사에도 선다 —
회차 레코드 검사가 이미 쓰던 규율과 같은 자다.
