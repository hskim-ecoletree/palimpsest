# 상태 — 사용자 표면 어휘·표현 정리와 회차 절차 교정

## 지금 단계

**정반합 진행 중 — 합(合) 셋이 아직 안 돌았다.** 정(正) 셋과 반(反) 셋이 나왔고,
사전부검 라운드 1 이 끝났다. 다음 걸음은 `pal-decision-synthesizer` 셋을 띄우는 것이다.

## 잠긴 의도

`intent.md` 전문을 읽어라. 조건 36개(A1~H3) · 형식 오류 0 · 켜진 상자 0.
`pal round conditions --file <intent.md> --json` 이 세는 자리다.

## 착수 커밋 · 브랜치

착수 `b165445` · 브랜치 `docs/overview-user-feedback` (소유자 결정: 현재 브랜치에 이어 붙인다)
회차 커밋 `f91bea2` → `ffc5010` → `3f05598`

## 상한

사전부검 **2** (1 씀) · 독립 리뷰 **5** (0 씀) · 정반합 판정 건마다 **1**

## 모집단 분리

**(나)** — 장치를 만들되 그것에 대한 발견은 별개 의도로 새 회차를 연다.
`E`(#112)·`F`(#113) 가 장치를 만드는 것 자체가 산출물이라 (가) 가 원리상 불가능하다.

## 지금까지 난 것

| 산출 | 자리 |
|---|---|
| 정(正) 셋 | `dialectic/a1-thesis.md` · `d1-thesis.md` · `g1-thesis.md` |
| 반(反) 셋 | `dialectic/a1-antithesis.md`(반론 16) · `d1-antithesis.md`(17) · `g1-antithesis.md`(15) |
| 소유자 1차 원문 | `dialectic/owner-source-9eec834.txt` — `git show 9eec834:temp-comment.md` 사본 |
| 사전부검 R1 | `premortem/r1-raw.md` — 시나리오 14 · 스스로 기각 5 |

**합(合)은 아직 없다. 발견 레코드(`findings.jsonl`)도 아직 없다** — 처분이 정해져야
`닫은커밋` 을 적을 수 있기 때문이다.

## 실패한 접근 — 다음 컨텍스트가 같은 벽에 안 부딪히도록

| 무엇 | 왜 실패했나 |
|---|---|
| **`Live → Fresh` 로 가면서 「소유자가 `undeterminable` 은 언급 안 했다」고 적은 것** | 소유자 문장 *"4개 중 stale, orphaned 을 제외하고는"* 이 산술상 둘을 지목한다 |
| **`docs/overview.md` 의 새 표기를 「소유자가 읽고 문제 안 삼았다」로 승격한 것** | 그 다섯 낱말은 직전 커밋 `a4a5231` 이 처음 쓴 것이다. 소유자가 읽은 판은 `9eec834` 다 |
| **「생략」을 `fold` 에 배정한 것** | 소유자는 그 낱말을 `elision`(현 「절단」)에 주라고 적었다. 정반대다 |
| **#114 에서 회차 기록을 대상 밖으로 뺀 것** | 발생 수의 63% 를 덜어낸다. 파일 수만 세고 발생 수를 안 쟀다 |
| **정반합 산출물을 `dialectic/` 에 두면서 상대 링크를 그대로 옮긴 것** | 한 겹 깊어져 「죽은 링크 부재」가 깨졌다. `../../../../` 이어야 한다 |
| **소유자 원문 사본을 `.md` 로 저장한 것** | 원문의 상대 링크가 죽은 링크가 된다. `.txt` 로 바꿔도 검사가 본다 — 보존 고지를 붙이고 경로만 고쳤다 |

## 다음 걸음

1. **합(合) 셋** — `pal-decision-synthesizer` 에 초안+반론표를 넘긴다. 메인이 겸하지 않는다.
2. **사전부검이 요구한 의도 개정** — 아래 넷은 조건 문면을 고쳐야 한다.
   - `B4`·`D4`·`G3` 의 **「23/23」** — `C2-a`·`G2-a` 가 검사를 늘리면 `checks.len()` 이 24 가 되어 셋이 문면 그대로 반증된다. `ci.yml` 이 *"검사 수는 여기 안 적는다"* 를 명시적으로 금지한다
   - `C2-a` — 「JSON 에 한국어 금지」를 문면대로 구현하면 결박 16건의 `note`(사용자가 손으로 쓴 본문)를 금지하는 검사가 된다. **enum 토큰 필드로 범위를 좁혀야** 한다
   - `B2-a` — 심을 자리가 원리상 없다. **양성 대조를 붙여야** 침묵이 정보가 된다
   - `A2` — 「근거에 적는다」로 닫히는데 실제 충돌은 와이어 위에 있다. 직렬화 토큰이 겹치지 않는지 재는 조건이 필요하다
3. **사전부검 R2** (상한 2 중 남은 1)
4. **발견 레코드** — 정반합 반론 48건 + 사전부검 14건을 처분하고 `findings.jsonl` 에 적는다.
   처분을 먼저 커밋하고 레코드가 그 SHA 를 받아 뒤따른다
5. 그 뒤 실행 — `A` → `B` → `C` · `D` → `E` → `F` · 마지막에 `G`

## 반론이 찾은 것 중 코드 수정이 필요한 것

| 좌표 | 무엇 |
|---|---|
| `crates/pal-cli/tests/touch_recall.rs:198,206` | `["code"]["state"]` 를 읽는데 실제 태그는 `"freshness"` 다. 비교가 항등이라 아무것도 안 잰다 |
| `xtask/src/main.rs:1936` | `check_no_regeneration` 의 하한이 `"CodeFreshness::Stale"` 문자열이다. 개명하면 죽는다 |
| `xtask/src/main.rs:4642-4644` | 「처분」 파서 계약의 **셋째** 자리. `D2` 가 둘만 이름 댔다 |
| `crates/pal-core/src/cascade.rs:56-57,74` | `NodeFreshness::Live` 가 별개 열거로 있고 `pal doctor` 가 `"live"` 를 찍는다 |
| `surface/queries.toml:99` · `catalog.rs:184` | 「바깥 오라클」이 사용자 화면에 나가고 바이트 대조를 지난다 |
| `corpus/criteria.toml:6781,7125,10291,10634` | `live` 가 기계 판독 키로 들어 있다 |
| `crates/pal-cli/src/export.rs:206,249-253` | `.name()` 병기가 Cypher 값으로 샌다 |
| `crates/pal-core/src/doctor.rs:130` · `catalog.rs:185,187` | 사용자가 보는 화면 문자열이 doc 주석이 아니라 `match` 팔에 있다 |
