# 상태 — 사용자 표면 어휘·표현 정리와 회차 절차 교정

## 지금 단계

**종료 직전.** 이슈 일곱의 산출이 다 섰고 `cargo xtask check` 26/26 · `cargo test` 전량
통과다. 남은 것은 독립 리뷰 · 게이트 · 종료 보고 · push.

## 잠긴 의도

`intent.md` 전문을 읽어라. **조건 51개**(A1~H3) · 형식 오류 0.
세는 자리는 `PAL_BIN=<빌드된 pal> cargo run -q -p pal-cli -- round conditions --file <intent.md> --json` 이다.
⚠ `pal` 은 이 PC 의 PATH 에 없다. `cargo run` 으로 부르거나 `PAL_BIN` 을 준다 —
`dashboard.py`·`record.py` 는 `PAL_BIN` 을 읽는다.

## 착수 커밋 · 브랜치

착수 `b165445` · 브랜치 `docs/overview-user-feedback` · **push 0 회**(`cancel-in-progress: true`)

## 상한

사전부검 **2** (다 썼다) · 독립 리뷰 **5** · 정반합 판정 건마다 **1**
(`A1`·`D1`·`G1` 다 썼고 넷째 「검증 중 확장 판단」이 `G4` 물음에 쓰이고 있다)

## 소유자가 답한 것 — 다시 묻지 마라

| 물음 | 답 |
|---|---|
| `Live → Fresh` 의 범위 | **`CodeFreshness` 넷만.** `NodeFreshness`·`pal doctor`·`corpus/criteria.toml` 은 범위 밖 |
| 회차 어휘 넷 개명 | **넷 다 개명한다** |
| 「절단」·「봉투」 | **이 회차에서 함께 닫는다** |
| 문장 등록 결함 | **`cut-the-bullshit` 이 맡는다.** 현상·설계 문서를 내는 것이 `G4` |

## 이 회차가 낸 것

| 산출 | 자리 |
|---|---|
| ADR 둘 | `docs/adr/0033-…`(상태 어휘) · `0034-…`(회차 어휘) |
| 새 검사 셋 | 「기계 토큰에 한국어 금지」·「완수 조건 설계 평가」·「어색한 표현 부재」 (23 → 26) |
| 새 에이전트 다섯 | `pal-debate-designer`·`pal-decision-proposer`·`pal-debate-referee`·`pal-debate-reporter`·`pal-condition-auditor` |
| 규약 새 절 | `SKILL.md` §3.5(완수 조건 설계 평가) · §5 여섯 역할 |
| 병기 모듈 | `crates/pal-cli/src/label.rs` |
| 관측 기록 | `observations/` 여섯 |
| 새 이슈 셋 | #116(상태 넷으로 충분한가) · #117(내부 용어 일반) · #118(과하게 복잡한 표현) |
| 결박 | 16 → 22. `pal intent export` 로 JSONL 정본을 다시 냈다 |

## 실패한 접근 — 다음 컨텍스트가 같은 벽에 안 부딪히도록

| 무엇 | 왜 실패했나 |
|---|---|
| **「`KO-SENTENCE-REGISTER.md` 를 그 저장소에 커밋 안 했다」고 적어 둔 것** | 낡았다. 커밋 `e7b3684` 로 커밋됐고 push 까지 됐다. **토론 설계자가 초안을 안 받는 자리에서 그 전제를 잡았다** |
| **새로 만든 에이전트 정의를 같은 세션에서 `Agent` 로 부르려던 것** | 레지스트리는 **세션 시작에** 읽힌다. 그 뒤에 만든 정의는 안 들어온다. 일반 서브에이전트에게 정의 파일을 전문 읽히고 그대로 행동하게 하면 격리는 유지된다 |
| **`센다` 를 일괄로 `잰다` 로 바꾼 것** | 두 뜻이다 — 판정(잰다)과 개수(헤아린다). 한쪽으로 몰면 「깨진 것을 잰다」가 된다 |
| **`봉투` 를 일괄로 `응답 묶음` 으로 바꾼 것** | 조사가 갈린다(`봉투는`→`응답 묶음은`) 그리고 **Rust 식별자에 공백이 들어가 컴파일이 깨진다** |
| **`G1` 이 등록한 사전형 정규식을 그대로 검사로 옮기려던 것** | 사전형(`켜다`·`접는다`)은 이 저장소에 0 건이다. 활용형으로 재야 하고, 경계가 없으면 `지켜진`·`보낸다` 가 위반으로 세어져 합격선이 원리상 0 에 못 닿는다 |
| **검출 수단을 경로로 자기 제외 하려던 것** | `xtask` 전체가 빠지고 이 회차가 고친 `xtask` 100 곳을 아무도 안 잰다. **파일 안의 표지로 갈라야 한다** |
| **감사자 반환문의 기각 절을 최상위 불릿으로 받은 것** | 추출기는 `### ` 항만 읽고 합계 검산은 불릿도 센다 — 「원문 14 ↔ 레코드 21」이 난다 |
| **`pal bind` 뒤에 커밋하면 결박이 남는 줄 안 것** | `intent.redb` 는 `.gitignore` 에 있다. `pal intent export --out .palimpsest/intent/bindings.jsonl` 을 돌려야 정본이 갱신된다 |

## 다음 걸음

1. **독립 리뷰** — 상한 5 를 다 쓴다(소유자 결정). 커밋 제목에 `[R<n>]` 를 단다 —
   안 달면 계기판 ④⑤ 가 「못 셌다」를 낸다.
2. **정반합 마무리** — `G4` 물음의 반(反)·합(合)·종료 판단·취합 보고. `PM2-16` 이 그것으로 닫힌다.
3. **게이트** `docs/gates/user-surface-vocabulary.md` — `## 판정` 은 수가 아니라 조건 ID 를
   적고 `intent.md` 상자와 양방향 집합 같음이어야 한다.
4. **종료 보고** `report.md` — 「의도적으로 안 한 것」·「확인 못 한 것」·「추론」·「다음으로
   넘기는 것」이 없어야 한다.
5. **이슈** — `C3`(`pal touch` 출력을 #110 에 붙인다) · `H3`(#108~#114 를 닫는다).
6. **push 는 한 번만** — `cancel-in-progress: true` 다.
