# 독립 리뷰 R1 — 회차 `2026-09-06-user-surface-vocabulary`

`pal-independent-reviewer` 원 반환문. 2026-09-06.

**측정 기준 커밋** `218982a` (착수 `b165445`). ⚠ 리뷰 중 저장소가 움직였다 — 시작 시 HEAD 는 `dbf4158` 였고 지금은 `218982a` 이며 워킹트리에 미커밋 수정 9 파일이 있다.

**금지역 출처**: `.claude/pal/policy.toml` 없음 · 잠긴 의도에 등록 목록 없음 → **`SKILL.md` 의 기본 다섯**을 쓴다.

**모집단 확인**: `git log --diff-filter=A b165445..HEAD --name-only` 실행 — 이 회차가 새로 만든 것은 에이전트 정의 다섯 · `crates/pal-cli/src/label.rs` · ADR 둘 · 회차 디렉터리 전부. `xtask/src/main.rs` 의 검사 24·25·26 은 수정으로 들어왔다(`자기장치`).

---

## 합격선 축

| 조건 | 판정 | 잰 수 | 근거 |
|---|---|---|---|
| A1 | 통과 | ADR-0033 §1 · 정반합 산출 3 파일 | `Live → Fresh`, 셋 유지 |
| A2 | 통과 | ADR-0033 §2 | *"두 축은 같은 낱말을 쓰지 않는다"* |
| A2-a | 통과 | 시험 2 통과 · RED 1 실패 관측 | `두_축의_와이어::*` ok · `observations/a2a-red.txt` |
| A3 | 통과 | ADR-0033 §3 | |
| A4 | 통과 | ADR 137행 · §1·§2·§3 존재 | |
| B1 | 통과 | `CodeFreshness::Live` **0건** | 남은 `NodeFreshness::Live` 5곳은 `## 범위 밖` |
| B2 | 통과 | 별칭 0 · 저장 0건 | |
| B2-a | 통과 | 양성 대조 시험 통과 | `저장된_옛_표기` |
| B3 | 통과 | **928 passed · 0 failed · rc=0** | 전 출력 검사 — `FAILED`·`panicked`·`error[E` 0건 |
| B4 | 통과 | **26/26 · rc=0** | |
| C1 | 통과 (모집단 주석) | `label.rs` 함수 7 · 호출 **9곳** · `ResolutionGrade` 모집단 0 | |
| **C1-a** | **반증** | 병기 주입 후에도 **26/26 rc=0** | 아래 발견 3·6 |
| C1-b | 통과 | 왕복 시험 4 통과 | |
| C1-c | 통과 | RED 2 실패 관측 | `observations/c1c-red.txt` |
| C2 | 통과 | `--json` 4518바이트 · 병기 문자열 **0건** | |
| C2-a | 통과 (모집단 좁음) | 파일 39 · 토큰 212 · 한국어 0 · 시험 4 | |
| C2-b | 통과 | 위반 셋 다 닫힘 | |
| **C3** | **반증** | 이슈 #110 **코멘트 0건** | 108~114 전부 0 |
| D1 | 통과 | ADR-0034 §1 표 일곱 | |
| D2 | 통과 / **게이트 기재 미측정** | 지금 실측 **1523**(ADR 은 1502) · 예외표 89/123 | 문면이 *"판정 시점에 다시 세고 그 값을 게이트에 적는다"* 인데 게이트가 없다 |
| D3 | 통과 | `pal touch` 출력에 `절단`·`접힘` 0건 | |
| D3-b | 통과 | 별칭 성립 | `contains("철회") \|\| contains("접힘")` |
| D3-a | 통과 | ① 통과 ② **발화** ③ 통과 | `git clone` + `--root` |
| D4 | 통과 | 26/26 rc=0 | |
| D4-a | 통과 | `round_stop` 23 passed | |
| D5 | 통과 | ADR-0034 가 이슈 인용 배제 명시 | |
| D6 | 통과 / **모집단 게이트 기재 미측정** | 퇴로·접힘·오라클·막힘·절단·봉투 = **전부 0** | `처분` 35/6/4 는 필드 유지를 명시 선언 |
| E1 | 통과 | 에이전트 정의 **6** | |
| E2 | 통과 | 표에 「받는 것」/「안 받는 것」 6행 | |
| E3 | 통과 | 강제/비강제 둘 다 적힘 | |
| E3-a | 통과 | 인용 처리 명시 | |
| E4 | 통과 | `PAYLOAD` 5 + `OWNED_FILES` 5 | |
| E4-a | 통과 | 출처 enum·절·칸·검산 | `조건평가R1 21↔21` |
| **E5** | **미측정** | 6역할 판이 **앞 둘만** 돌았다 | `-antithesis`·`-synthesis`·`-referee`·`r<n>-raw.md` **0** · 3역할 대조 없음 |
| E6 | 통과 | `놓는_것은_전부_되돌릴_수_있다` ok | |
| F1~F4·F6·F7 | 통과 | §3.5 존재 · 세 갈래 표 · 상한 · 평가자 갈림 · 강제 · rc=0 | |
| F5 | 통과 / 게이트 부착 미측정 | 조건 50행 분류 · 발견 14 · 기각 7 | |
| G1 | 통과 / **구현과 어긋남** | 목록 확정 | 검출 수단과 ADR-0034 가 `docs/research/**` 를 더했다 — 발견 1·5 |
| **G2** | **반증** | 검사는 0곳이나 **`docs/research` 20곳** 이 모집단 밖 | |
| G2-a | 통과 (구멍 있음) | 일곱 패턴 각각 발화 시험 4 통과 · 931 baseline | 자기 제외 표지 구멍은 자기 산출 절 |
| G3 | 통과 | 26/26 rc=0 · 928 passed | |
| G4 | 통과 / **결박 없음** | `KO-SENTENCE-REGISTER.md` 커밋 `e7b3684` 실재 | 이 저장소에 좌표 결박 0건 |
| **H1** | **부분 통과 / ⓒ 미측정** | ⓐ 0·0·0 ⓑ `최신 상태(fresh)` ✓ ⓒ 게이트 없음 | |
| H4 | 통과 | #108~#114 **일곱 전부** · 커밋 26개 전부 번호 보유 | |
| **H2** | **미측정** | HEAD 에 대한 런 **없음** | |
| **H3** | **반증** | #108~#114 **일곱 전부 OPEN** | |

**음성 대조**: 여섯 다 등록됐고 **여섯 다 실제 발화가 관측됐다.**

---

## 미측정 목록

| # | 안 잰 조건 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 못 쟀나 |
|---|---|---|---|---|---|---|
| 1 | `E5` 효과 — 6역할 판 하나를 끝까지 돌린 출력 | 원의도 | 참 | 거짓신호 | `dialectic/g4-*.md` | 판이 앞 둘에서 멈춰 있다 |
| 2 | `H1` ⓒ — 출력이 게이트 `## 효과` 에 붙었나 | 회차기록 | 참 | 거짓신호 | `docs/gates/` | 게이트 문서가 아직 없다 |
| 3 | `D2` 판정 시점 모집단을 게이트에 적었나 | 회차기록 | 참 | 거짓신호 | `intent.md` | 같음. 지금 값은 1523 |
| 4 | `D6` 모집단 실측을 게이트에 적었나 | 회차기록 | 참 | 거짓신호 | `intent.md` | 같음 |
| 5 | `H2` 마지막 SHA 의 `conclusion=success` | 원의도 | 참 | 거짓신호 | `intent.md` | 아직 push 하지 않았다 |
| 6 | 개정 R4 가 요구한 「이슈 116·117·118 번호를 게이트에 적는다」 | 회차기록 | 참 | 거짓신호 | `intent.md` | 게이트 없음. 이슈 셋은 실재 확인함 |
| 7 | 종료 보고의 네 이름 | 회차기록 | 참 | 거짓신호 | `SKILL.md` | `report.md` 가 아직 없다 |

---

## 의도 축

### 빠진 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | **`docs/research` 20곳이 교정도 검출도 안 됐다.** `G1` 의 정반합 판정이 *"`docs/research/**` 를 **동결로 빼지 않는다** … 반 14번은 `research` 를 **넣는 것**으로 해소한다(25곳)"* 로 **명시적으로 대상에 넣었다.** 그런데 검출 수단이 그 디렉터리를 이름으로 통째 건너뛴다. 합격선 「0곳」이 등록된 모집단보다 20 작다 | 원의도 | 참 | 금지역 | G1·G2·G2-a | `xtask/src/main.rs:4609` · `g1-synthesis.md:99,196` | 격리 사본에서 `"research"` 만 배제 목록에서 빼고 `cargo xtask check` → `FAIL … 20곳 남았다`. 원본은 `ok … 0곳` |
| 2 | **`G4` 의 산출물이 이 저장소 어디에도 결박돼 있지 않다.** 문서는 실재하나 그 좌표가 `intent.md`·ADR·`docs/**` 중 어디에도 없다 | 원의도 | 참 | 거짓신호 | G4 | `docs/` 전역 | `grep -rn "KO-SENTENCE-REGISTER" docs/ intent.md` → **0건** |
| 3 | **`C1-a` 를 재는 자가 없다.** 「병기가 기계 출력으로 새지 않는다」에 대응하는 시험도 검사도 0개다 | 원의도 | 참 | 거짓신호 | C1-a | `crates/pal-cli/src/export.rs` | `grep -rn "병기" export.rs tests/` → 0건 |
| 4 | **이 회차의 종결 문서가 교정 표기를 쓰는지 재는 자리가 없다.** 검출 수단은 `docs/gates` 를 건너뛰고 `.palimpsest` 를 뿌리에 안 담는다 — 요구된 바로 그 두 자리가 원리상 대상 밖이다 | 원의도 | 참 | 거짓신호 | G2 | `xtask/src/main.rs:4505,4609` | 코드 전수 확인 |

### 요구되지 않은 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 5 | **ADR-0034 `## 범위 밖` 이 `docs/research/**` 를 동결로 등록했다** — 잠긴 의도의 `G1` 에 없고, ADR 이 근거로 건 `g1-synthesis.md` 는 **정반대로 판정했다.** 동결 목록이 정반합 판정 없이 한 항목 넓어졌다 | 원의도 | 참 | 금지역 | D5·G1 | `docs/adr/0034…md:119` · `g1-synthesis.md:196` | 두 문장 대조 |

### 있는데 틀린 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 6 | **`label.rs` 가 「구조로 막힌다」고 적은 것이 `C1-a` 가 이름 댄 좌표에 대해 거짓이다.** ①(기계 출력 오염)이 지목한 `crate::export` 는 **`label` 과 같은 크레이트의 형제 모듈**이라 아무것도 안 막는다 | 원의도 | 참 | 금지역 | C1-a | `label.rs:18-19` · `export.rs:206` | 격리 사본에서 치환 후 `cargo check -p pal-cli` **exit 0** · `cargo xtask check` **26/26 통과** |
| 7 | **`pal plan` 화면이 존재하지 않는 변형 이름을 찍는다** — `pending→live`. `PlanBindingState` 에 `Live` 변형이 없다 | 저장소 | 참 | 거짓신호 | 없음 | `crates/pal-cli/src/plan.rs:311` | ⚠ 워킹트리에서 수정 중 — 메인이 이미 잡은 것으로 보인다 |
| 8 | **`SKILL.md` §3.5 가 감사자에게 주라는 절 이름이 이 회차 의도에 없다.** §3.5 는 `## 차선책` 을 주라고 적는데 잠긴 의도는 `## 퇴로` 를 쓴다 | 규약 | 참 | 거짓신호 | F1·D6 | `SKILL.md:168` · `intent.md:173` | `grep -n "^## 퇴로\|^## 차선책"` |

---

## 이번 라운드의 새 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 9 | **`어색한 표현 부재` 의 자기 제외 표지가 전역 뮤트 스위치다.** 대상 안 어느 파일이든 표지 한 줄만 넣으면 그 파일의 나머지가 통째로 검출에서 빠진다 | 자기장치 | 참 | 금지역 | G2·G2-a | `xtask/src/main.rs:4514,4544-4556` | 격리 사본 `AGENTS.md` 머리에 표지를, 끝에 `하네스 없이도 혼자 선다.` 를 심고 → `ok … 남은 것 0곳`(줄 81710 · 원본 81812) |
| 10 | **`C3` 반증 확정** — `pal touch` 출력이 이슈 #110 에 안 붙었다 | 원의도 | 참 | 거짓신호 | C3 | `intent.md:123` | `gh issue view` 전부 코멘트 0 |
| 11 | **`H3` 반증 확정** — 에픽과 하위가 여덟 다 OPEN 이다 | 원의도 | 참 | 거짓신호 | H3 | `intent.md:171` | `gh issue list --state all` |

---

## 자기 산출에 대한 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 12 | **`기계 토큰에 한국어 금지` 의 모집단이 3 크레이트뿐이고 그 한정 근거가 어디에도 없다.** 대상 밖에 실제 한국어 기계 토큰이 있다 — `pal-intent/src/round_condition.rs` 의 `#[serde(rename="통과"/"반증"/…)]` | 자기장치 | 참 | 거짓신호 | C2-a | `xtask/src/main.rs:857` · `round_condition.rs:41-90` | 코드 대조. 게이트 파서 계약이라 의도적 면제일 개연성이 높으나 **어디에도 안 적혀 있다** |
| 13 | **개정 R4 의 수가 자기 본문과 어긋난다** — *"넷 중 **셋**은 안 받는다"* 라 적고 다음 문장이 ①③ **둘만** 든다 | 회차기록 | 참 | 거짓신호 | 없음 | `intent.md:334-338` | 원문 대조 |
| 14 | **`h1-touch.txt` 의 스냅샷이 `dbf4158` 인데 회차의 마지막 커밋은 다르다.** 게이트가 이 출력을 그대로 실으면 종료 SHA 와 증거 SHA 가 갈린다 | 회차기록 | 참 | 미관 | H1 | `observations/h1-touch.txt:1` | 파일 머리 대조 |
| 15 | **검출 수단이 경로가 아니라 디렉터리 이름으로 배제한다** — `adr`·`gates` 라는 이름의 디렉터리가 `crates/` 어디에 생겨도 조용히 대상 밖이 된다 | 자기장치 | 추정 | 미관 | G2-a | `xtask/src/main.rs:4609` | 코드 읽음. 지금 그런 디렉터리는 없어 실측으로 재현하지 않았다 |

---

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 아니었나 |
|---|---|---|---|---|---|---|
| R1 | 「`label` 병기가 `pal-query` 의 `watch_grades` 키를 오염시킬 수 있다」 | 원의도 | 거짓 | 미관 | `pal-query/src/lib.rs:758` | `pal-query` 는 `pal-cli::label` 을 **볼 수 없다**. 이 경로는 실제로 크레이트 경계가 막는다 |
| R2 | 「`label.rs` 가 아무도 안 부르는 표면이다」 | 원의도 | 거짓 | 미관 | `pal-cli/src/*.rs` | `grep -rn "label::"` → **9 호출자**. 실행 출력에서 병기를 눈으로 확인했다 |
| R3 | 「`H4` 커밋 규율이 안 지켜졌다」 | 원의도 | 거짓 | 미관 | `git log b165445..HEAD` | 커밋 26개 **전부** 제목에 이슈 번호가 있고 일곱이 다 덮인다 |
| R4 | 「`D6` 이 「처분」을 안 옮겨 미완이다」 | 원의도 | 거짓 | 미관 | `SKILL.md:650` | *"레코드 필드 `처분` 은 옛 이름이고 그대로 둔다"* 로 명시 결정. 실행 출력은 전부 옮겨졌다 |
| R5 | 「26개 중 0개를 훑고 초록인 검사가 있다」 | 자기장치 | 거짓 | 미관 | `xtask check` 전 출력 | 26줄 전부 모집단 수를 낸다 |
| R6 | 「`B1` 개명이 안 끝났다」 | 원의도 | 거짓 | 미관 | `cascade.rs:74` | 남은 것은 `NodeFreshness::Live` 와 `pal doctor` 불변식 문장뿐이고 **둘 다 소유자 결정으로 잠갔다** |

---

## 끝내도 되는가

**안 된다** — 본 목록에 **금지역 넷**(발견 1·5·6·9)과 **반증 넷**(`C1-a`·`C3`·`G2`·`H3`)이
남았다. 특히 발견 1·5 는 같은 뿌리다: `G1` 의 정반합이 `docs/research` 를 **대상에 넣는다**고
판정했는데 ADR 과 검출 수단이 그것을 동결로 적어 **20곳이 「0곳」으로 집계되고 있다.**
