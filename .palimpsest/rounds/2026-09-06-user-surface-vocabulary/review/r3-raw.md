# 독립 리뷰 R3 — 회차 `2026-09-06-user-surface-vocabulary`

`pal-independent-reviewer` 원 반환문. 2026-09-06. 기준 커밋 `47dbdd0` 의 격리 클론.
⚠ 리뷰 중 메인이 워킹트리를 움직였다 — 그 어긋남을 리뷰어가 스스로 적었고 그대로 둔다.

> ⚠ **좌표 칸을 기계가 읽을 수 있게 정규화했다** — 글로브(`0033-*.md`)·줄임표(`docs/gates/…`)·
> 중괄호 묶음(`{a1,d1,g1}`)을 실제 경로 하나로 폈고, 조건 칸의 여러 ID(`B4·D4·G3`)는 첫
> 하나만 남겼으며 괄호 주석(`없음(§11 종료 조건 2)`)은 지웠다. 처분이 실제로 내린 자리와
> 좌표가 갈린 행 하나(넷째 왕복 시험)는 그 자리로 옮겼다. **문장은 하나도 안 바꿨다.**
> 추출기는 열을 이름으로 읽고 원장의 `경로` 는 커밋이 만졌는지로 대조되므로, 글로브가
> 남으면 그 행이 원리상 대조 불가가 된다.


독립 리뷰 반환문 (읽기 전용 · 파일 수정·커밋 없음)

기준 커밋: **`47dbdd0`**. ⚠ 리뷰 도중 워킹트리가 움직였다 — `a3eda27` 이 얹혔고 15 파일이 수정 중이며, 그 상태에서 `cargo test` 가 **컴파일에 실패한다**(`crates/pal-cli/src/doctor.rs:465` 이 `ResidualReason::ALL` 을 부르는데 `crates/pal-core/src/judgment.rs` 의 미커밋 편집이 그 항목을 옮기는 중). 아래 모든 측정은 `47dbdd0` 의 격리 클론에서 다시 냈다.

금지역 목록의 출처: `.claude/pal/policy.toml` **없음** → 잠긴 의도가 목록으로 등록한 것도 없음(개정 표의 태그뿐) → **지시문의 기본 다섯**을 썼다.

---

## 합격선 축

| 조건 | 판정 | 잰 수 | 근거 |
|---|---|---|---|
| A1 | 통과 | ADR 1편 · 정반합 산출 3 | `docs/adr/0033-a-word-that-reads-wrong-is-not-fixed-by-a-gloss.mdmd §1` · `dialectic/a1-{thesis,antithesis,synthesis}.md` |
| A2 | 통과 | ADR 절 1 | `0033 §2` 「두 축은 같은 낱말을 쓰지 않는다」 |
| A2-a | 통과 | 시험 2 · RED 1 | `binding.rs:733 두_축의_와이어` · `observations/a2a-red.txt` (rename="current" 로 FAILED 관측) |
| A3 | 통과 | 형식 3 | `0033 §3` 괄호 소문자 · 열 뒤 병기 · 영어권 생략은 규칙만 |
| A4 | 통과 | ADR 1 | `0033` |
| B1 | 통과 | `CodeFreshness::Live` 0 · `Fresh` 16 · scripts 3 | `grep -rn CodeFreshness::Live crates xtask scripts` → 0 (`cascade.rs:74` 는 `NodeFreshness`, 범위 밖) |
| B2 | 통과 | 저장 0 / 23행 · 참조 0 | `.palimpsest/intent/bindings.jsonl` 에 `freshness` 0건 · `pal-store`·`pal-intent` 의 `CodeFreshness` 참조 0 |
| B2-a | 통과 | 시험 2 (양·음) | `binding.rs:700 옛_토큰은_읽기에서_거부된다` + 새 토큰 읽힘 |
| B3 | 통과 | rc=0 | 격리 클론 `cargo test --workspace` rc=0, 실패 0 |
| **B4** | **반증** | **25/26 · rc=1** | HEAD 에서 `cargo xtask check` → `FAIL 원장 둘 대조`. 상자 54 를 켜고 `· 통과 ⟨전사…⟩` 를 붙인 사본에서는 **26/26 · rc=0** 을 실측했다 |
| C1 | 통과 | label 함수 7 · 호출 9 | `label.rs` 의 일곱이 전부 `doctor.rs:410`·`ledger.rs:610,628,629`·`touch.rs:320,348,358`·`query.rs:355,367` 에서 불린다(0 인 것 없음) |
| C1-a | 통과 | 속성 3/3 | `export.rs` 의 열거 유래 값은 `kind`·`identity`·`grade` 셋뿐이고 `tests/export_ascii.rs` 가 그 셋을 다 잰다 |
| C1-b | 통과 | 자리 4 · 한국어 0 | `graph.rs:190,63` · `query_log.rs:137` · `schema.rs:172` 의 `name()` 전부 영어 |
| C1-c | 통과 | 왕복 시험 3 + 음성 1 | `graph.rs:330 왕복_파서를_진_표시_함수` · `c1c-red.txt`. ⚠ 넷째 자리 미포함 → 자기산출 #5 |
| C2 | 통과 | 3 명령 · 한국어 0 | `pal touch --json` · `pal ledger --json` · `pal doctor --json` 에서 한국어 값 0 (`pal query --json` 의 카탈로그 요약은 개정 R4 가 대상 밖) |
| C2-a | 통과 | 파일 39 · 토큰 212 · 0건 | `cargo xtask check` 의 「기계 토큰에 한국어 금지」 |
| C2-b | 통과 | 위반 3/3 | `ledger.rs:144 nul-byte` · `IdentityGrade unavailable` · `NearKind spelling/substring` |
| C3 | 통과 | 이슈 댓글 2 | `gh issue view 110 --comments` 에 출력과 결정이 붙어 있다 |
| D1 | 통과 | 일곱 | `0034 §1` |
| **D2** | **반증** | **게이트 1502 ↔ 실측 1551 / 1564** | 그 수를 쓴 커밋 `558d35c` 에서 1551, HEAD 에서 1564(14 파일 합). 예외표 89 는 일치. D2 문면이 *"판정 시점에 다시 세고 그 값을 게이트에 적는다"* 를 못 박았다 |
| D3 | 통과 | 넷 다 | HEAD 재실행 `pal touch` 에 「생략」·「이관」, 「절단」·「접힘」 0 |
| D3-b | 통과 | 별칭 2 | `status.rs:613,649` 가 옛·새 표기를 둘 다 읽는다 |
| D3-a | 통과 | 관측 1 | `d3a-negative-control.txt` (`git clone` + `--root`, 사본이 사본을 재는지 먼저 확인) |
| **D4** | **반증** | rc=1 | B4 와 같은 실행 |
| D4-a | 통과 | rc=0 | 격리 클론 `cargo test` |
| D5 | 통과 | 절 1 | `0034 §「이름의 근거로 이슈 인용을 쓰지 않는다」` |
| **D6** | **반증** | **잔존 좌표 5** | `AGENTS.md:27,32`(막힘·접힘) · `scripts/frontier.sh:62`(실행 출력) · `docs/plan/README.md:8,104,106,108,110`(오라클) · `NEXT-B-plan-for-harness.md:67`(퇴로) · `docs/plan/disposal-map.md:335`(퇴로 인용). 게이트에 **개정 R4 가 요구한 모집단 실측이 없다** |
| E1 | 통과 | 에이전트 6 | `.claude/agents/` 에 다섯 신설 + 기존 `pal-decision-opponent`·`-synthesizer` |
| E2 | 통과 | 표 행 6 · 「안 받는 것」 열 1 | `SKILL.md:405-412` |
| E3 | 통과 | 절 1 + 비강제 선언 1 | `SKILL.md:423-438` |
| E3-a | 통과 | 문단 1 | `SKILL.md:417-422` |
| E4 | 통과 | `include_str!` 5 · `OWNED_FILES` 5 | `install/layout.rs:95-115,194-198` |
| E4-a | 통과 | enum 2 · 짝 2 · 검산 2 | `record.py:70` · `xtask:3574-3575` · 검사 출력 `정반합R1 13↔13 · 조건평가R1 21↔21` |
| E5 | 통과 | 산출 6 | `dialectic/g4-{design,thesis,antithesis,synthesis,referee}.md` + `r1-raw.md`, 갈림 둘 기술 |
| E6 | 통과 | rc=0 | `cargo test` |
| F1 | 통과 | 절 1 | `SKILL.md:160` (승인 앞) |
| F2 | 통과 | 갈래 3 + 겹침 문단 1 | `SKILL.md:184-203` |
| F3 | 통과 | 2 라운드 | `SKILL.md:213` |
| F4 | 통과 | 절 1 | `SKILL.md:219` |
| F5 | 통과 | 반환문 1(26KB) · 레코드 21 | `conditions-audit/r1-raw.md` · 검사 「완수 조건 설계 평가」 대상 1 · 반환문 1 |
| F6 | 통과 | 절 1 | `SKILL.md:224-235` |
| F7 | 통과 | rc=0 | `cargo test` |
| G1 | 통과 | 산출 3 · 코드 결박 1 | `dialectic/g1-*.md` · `xtask:4526 동결_경로` |
| G2 | 통과 | 패턴 7 · 파일 246 · 줄 85384 · 0곳 | `cargo xtask check` |
| G2-a | 통과 | 시험 4 · RED 931→939 | `xtask:4661-4718` · `g2a-red.txt` |
| **G3** | **반증** | rc=1 | `cargo xtask check` (같은 실행). `cargo test` 는 rc=0 |
| G4-b | 통과 | 216행 · 절 5 · 4.1~4.4 = 159·172·180·189 | 외부 저장소 실측 전량 일치. `merge-base --is-ancestor e7b3684 origin/main` rc=0 |
| G4-c | 통과 | 관측 3/3 | 재측정: 문서 커밋 예 · `한국어-10` 은 그 문서에만 · `checklists/ko.md` 마지막은 한국어-9 |
| G4 | 통과 | 절 5 · palimpsest 0건 | `KO-SENTENCE-REGISTER.md` |
| H1 | 통과 | ⓐ0건 ⓑ`최신 상태(fresh)` ⓒ있음 | HEAD 재실행: `절단`·`접힘`·`가 폅니다` grep 0. ⚠ 게이트 인용 블록의 스냅샷이 링크한 「전문」과 다르다 → 자기산출 #6 |
| H4 | 통과 | 7/7 | `git log --format=%s b165445..HEAD` → #108×1 #109×5 #110×3 #111×1 #112×5 #113×1 #114×2 |
| **H2** | **반증** | **런 1건 · headSha=b165445** | `gh run list --branch docs/overview-user-feedback` 결과가 착수 커밋(2026-09-05, 회차 착수 **전**) 하나뿐. `git rev-parse origin/docs/overview-user-feedback` = `b1654455…` — 회차의 33 커밋이 push된 적이 없다 |
| H3 | 통과 | 7/7 CLOSED | `gh issue view 108~114` |
| H3-a | 통과 | 줄 1 | 게이트 73-76행 |

**검산 — 통과 48 · 반증 6 · 대조 불가 0 · 미측정 0 = 54**

음성 대조: `A2-a`·`B2-a`·`C1-c`·`C2-a`·`D3-a`·`G2-a`·`F` 일곱 자리에 등록됐고, 그중 다섯은 관측 파일이 실물로 남아 있으며 RED 가 실제로 빨갰던 것이 파일 안에 찍혀 있다(`FAILED` 문자열 확인).

---

## 미측정 목록

| # | 안 잰 조건 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 못 쟀나 |
|---|---|---|---|---|---|---|
| 1 | `E5` 의 「기존 3역할 절차와 갈린다」 재현 | 자기장치 | 참 | 미관 | `docs/gates/user-surface-vocabulary.md:261-268` | 앞 절차를 다시 돌리면 앞 결과가 컨텍스트에 들어와 격리가 깨진다 — 원리상 재현 불가 |
| 2 | `F5` 감사 반환문의 **질** | 자기장치 | 참 | 미관 | `.claude/skills/round/SKILL.md:233-235` | 규약이 「강제되지 않는 것」으로 명시 선언했다. 내가 대체 판정을 세울 근거가 없다 |
| 3 | `A1`·`D1`·`G1` 정반합 **판정 내용**의 타당성 | 회차기록 | 참 | 미관 | `.palimpsest/rounds/2026-09-06-user-surface-vocabulary/dialectic/a1-synthesis.md` | 산출물 존재와 판정문만 읽었고 근거 사슬을 재검증하지 않았다 |
| 4 | `G2` 가 고친 931곳의 **의미 보존** | 저장소 | 참 | 거짓신호 | `xtask/src/main.rs:4494-4501` | 검사는 낱말만 잰다. 931곳을 사람 눈으로 다시 읽는 일을 이 라운드에 안 했다 |
| 5 | `C1` 의 「**전부**」 — `println!` 을 지나는 열거 전수 | 저장소 | 참 | 거짓신호 | `crates/pal-cli/src/label.rs:40` | `label` 이 덮는 일곱 갈래 밖에 화면 상태값이 더 있는지 전수로 세지 않았다(`.name()` 인쇄 자리 14곳만 눈으로 봤다) |

---

## 의도 축

### 빠진 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 6 | `D6` 의 개명이 **저장소의 첫 지시문**에 안 닿았다 — `AGENTS.md` 가 회차의 나가는 문을 *"종료 · 막힘 · 접힘"* 으로 적고 `SKILL.md` 는 *"종료 · 교착 · 철회(folded)"* 로 적는다. 규약 둘이 같은 문장에서 갈린다 | 저장소 | 참 | 거짓신호 | D6 | `AGENTS.md:27,32` ↔ `.claude/skills/round/SKILL.md:254` | 47dbdd0 클론에서 `grep -rn "막힘\|접힘" AGENTS.md` → `27:★★ **그리고 나가는 문은 셋이다 — 종료 · 막힘 · 접힘.**` · `32:…그것은 접힘이 아니라…` |
| 7 | `D6` 이 요구한 「**실행 출력으로 찍는 자리**」가 남았다 — `AGENTS.md:14` 가 첫 명령으로 지시하는 스크립트가 사용자 화면에 `막힘 N건` 을 찍는다 | 저장소 | 참 | 거짓신호 | D6 | `scripts/frontier.sh:56,62` | `sed -n '55,63p' scripts/frontier.sh` → `echo "── 막혀 있음 ───…"` · `echo "… · 막힘 ${#blocked[@]}건"` |
| 8 | 낡은 인용 — `docs/plan/disposal-map.md` 가 `SKILL.md:188` 을 *"…음성 대조 · **퇴로**를 함께."* 로 인용하는데 그 줄은 지금 *"…음성 대조 · **차선책**을 함께."* 다(`SKILL.md:737`) | 저장소 | 참 | 거짓신호 | D6 | `docs/plan/disposal-map.md:335` | 47dbdd0 클론에서 두 줄을 나란히 뽑아 대조. `docs/plan/README.md:8,104,106,108,110`(오라클) · `NEXT-B-plan-for-harness.md:67`(퇴로)도 같은 갈래 |

### 요구되지 않은 것

없음 — 이 회차의 산출물 중 잠긴 의도가 안 요구한 것을 못 찾았다.

### 있는데 틀린 것

없음 — 만들어진 것이 의도와 **다르게 동작하는** 자리는 못 찾았다(빠진 것과 판정의 거짓은 위·아래 절이 진다).

---

## 이번 라운드의 새 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 9 | **`H2` 를 「통과」로 적었는데 그 CI 런이 존재하지 않는다.** 이 브랜치의 런은 **하나뿐**이고 그 `headSha` 는 착수 커밋 `b165445`(2026-09-05T15:41:51Z — 회차 착수 **전**)다. 원격 브랜치는 아직 `b165445` 라 회차의 33 커밋이 **push된 적이 없다.** 게이트는 획득 표에 *"브랜치 머리 SHA 에 붙은 `pull_request` 런"* 이라고 적는데 그 SHA 를 안 적어 문면만으로는 반증도 안 된다 | 회차기록 | 참 | **금지역** | H2 | `docs/gates/user-surface-vocabulary.md:64,199` | `gh run list --branch docs/overview-user-feedback --limit 10 --json headSha,conclusion,event` → `[{"conclusion":"success","headSha":"b1654455aa74de34825dda9950f98880c70ad744","event":"pull_request"}]` · `git rev-parse origin/docs/overview-user-feedback` → `b1654455aa74de34825dda9950f98880c70ad744` |
| 10 | **종료 보고가 「독립 리뷰 5/5」를 적는데 산출물은 하나다.** `review/` 에 `r1-raw.md` 하나, 원장에 `출처=독립리뷰` 는 **라운드 1(28건)** 뿐이다. `§11 ①` 상한 소모가 회차를 닫을지의 근거이므로 이 수가 틀리면 종료 판단의 입력이 틀린다 | 회차기록 | 참 | **금지역** | 없음 | `.palimpsest/rounds/2026-09-06-user-surface-vocabulary/report.md:30` | `ls review/` → `r1-raw.md` 하나 · `findings.jsonl` 집계 → `('독립리뷰', 1) 28` 외 없음 · 검사 출력에도 `독립리뷰R1 28↔28` 하나뿐 |

---

## 자기 산출에 대한 발견

메인이 처분한다. 이 회차가 반드시 닫아야 하는 것이 아니다.

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 11 | 게이트 `## 합격선` 이 **개정 R5 이전 상태로 굳었다** — *"완수 조건 **51개**(`A1`~`H3`)"* · *"개정 **넷**(R1·R2·R3·R4)"*. 실제는 **54개(`A1`~`H3-a`) · 개정 다섯**이고, 빠진 셋이 정확히 R5 가 신설한 `H3-a`·`G4-b`·`G4-c` 다. 같은 문서 69행의 검산은 **54** 라 문서가 자기 안에서 갈린다 | 회차기록 | 참 | 거짓신호 | 없음 | `docs/gates/user-surface-vocabulary.md:14` ↔ `:64,69` | `grep -c '^- \[ \] \*\*' intent.md` → **54** · `intent.md` 의 `### R1`~`### R5` **다섯** |
| 12 | 게이트의 「결정론 / 결정론이 아님」 분류가 **54 중 36 만 덮는다.** 18개가 어느 목록에도 없다 — `A2 A3 A4 B1 C1 C1-a C1-b C2 C2-b C3 D3 D3-b D6 E3-a E4-a G4-b G4-c H3-a`. §3.5 가 *"두 값이 어긋나면 그것 자체가 걸림"* 이라고 못 박은 축인데 그 절반이 값을 안 받았다 | 회차기록 | 참 | 거짓신호 | 없음 | `docs/gates/user-surface-vocabulary.md:17-22` | 두 목록을 파싱해 조건 54 와 차집합: 결정론 17 · 비결정론 19 · 합 36 |
| 13 | 게이트의 `D2` 실측 **1502** 가 **어느 시점에도 안 맞는다.** 그 수를 쓴 커밋 `558d35c` 에서 **1551**, HEAD 에서 **1564**. 조건 문면이 *"수는 회차가 진행되면 낡는다 — 판정 시점에 다시 세고 그 값을 게이트에 적는다"* 로 이 실패를 미리 이름 댔다 | 회차기록 | 참 | 거짓신호 | D2 | `docs/gates/user-surface-vocabulary.md:153-154` | `git archive <sha> \| … "처분" 키 행 세기` → `558d35c: 1551` · `47dbdd0: 1564` · `grep -rc '"처분"' .palimpsest/rounds/*/findings.jsonl` 합 = 1564 (14 파일) |
| 14 | 종료 보고의 **「금지역 아홉과 실패 열넷」이 원장과 안 맞는다.** 실측 금지역 **10** · 실패 **21**. 「기각 제외」·「유효=참」·둘의 교집합 어느 필터로도 9/14 가 안 나온다 | 회차기록 | 참 | 거짓신호 | 없음 | `report.md:35` | `findings.jsonl` 102 데이터 행 집계: 전체 `{실패 21, 금지역 10, 거짓신호 47, 미관 24}` · 기각 제외 `{실패 21, 금지역 10, …}` · 유효=참 `{실패 21, 금지역 10, …}` |
| 15 | `C1-c` 의 왕복 시험이 `C1-b` 가 **이름 댄 넷 중 셋**만 덮는다 — `schema::Cardinality` 가 빠졌다. 그 자리도 `name()` ↔ `parse()` 왕복이고 `parse` 가 `Option` 이라, 뒤에 병기를 얹으면 `C1-b` 가 「금지역」으로 태그한 그 조용한 죽음이 그대로 난다 | 자기장치 | 참 | 거짓신호 | C1-c | `crates/pal-core/src/schema.rs:170-186` vs `crates/pal-core/src/graph.rs:330-382` | 시험 모듈에 `해소_등급`·`출처`·`질의_이름` 셋만 있다. `schema.rs:181 fn parse(raw)` 는 같은 꼴이고 시험이 없다 |
| 16 | `H1` 효과 블록이 **링크한 「전문」과 다른 실행**이다. 게이트 블록은 `palimpsest@daf15de+worktree` 인데 *"전문"* 이라고 가리킨 `observations/h1-touch.txt` 는 `dbf4158+worktree` 다. 게이트에 실린 그 실행의 저장된 산출물이 없고, 근거 절의 줄 다섯(`결박 불가 언어`·`워킹트리`·`재구축`·`질의 로그`·`크기`)이 생략 표시 없이 빠졌다. ⚠ **내용 자체는 HEAD 에서 내가 독립으로 재현했다**(ⓐⓑ 성립) — 해악은 출처뿐이다 | 회차기록 | 참 | 거짓신호 | H1 | `docs/gates/user-surface-vocabulary.md:213,222,236` ↔ `observations/h1-touch.txt:1,32` | 두 파일의 스냅샷 문자열 대조 · HEAD 재실행 `pal touch SymbolKind` → `palimpsest@47dbdd0+worktree`, `절단\|접힘\|가 폅니다` grep **0** |
| 17 | `E5` 의 효과가 **설치된 에이전트 정의를 지나지 않았다.** 산출물 셋이 머리에 *"이 세션의 레지스트리가 새 정의를 안 실어서 일반 서브에이전트에게 정의 파일을 전문 읽혔다"* 고 스스로 적는데, **게이트의 `## 효과` 절에는 그 사실이 없다.** 격리 자체는 각 산출물이 선언했으므로 `E1`·`E5` 는 서지만, 뒤에 읽는 사람이 「새 에이전트 정의가 실제로 디스패치돼 돌았다」로 읽게 된다 | 자기장치 | 참 | 거짓신호 | E5 | `dialectic/g4-design.md:5-8` · `g4-thesis.md:5-7` · `g4-referee.md:5-8` ↔ 게이트 `:256-271` | 세 파일 머리 8줄 · 게이트 `## 효과` 의 `E5` 절 전문에 「레지스트리」·「일반 서브에이전트」 0건 |

---

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 아니었나 |
|---|---|---|---|---|---|---|
| 18 | 「`intent.md` 안에 `## 승격` 과 `### 승격 — 소유자가 정할 것 하나` 가 둘 다 있어 게이트 파서가 절을 잘못 자른다」 | 회차기록 | 거짓 | 미관 | `intent.md:364,389` | 상자 54 를 켠 격리 사본에서 `cargo xtask check` 가 **26/26 · rc=0** 이었고 「원장 둘 대조」가 이 회차 54개를 정상으로 읽었다. 헤딩 중복도 ID 중복도 0 |
| 19 | 「어색한 표현 부재 검사의 `검출-수단-자기-제외` 표지를 아무 파일에나 넣으면 그 파일이 통째로 빠진다」 | 자기장치 | 거짓 | 금지역(이었다면) | `xtask/src/main.rs:4562` | 코드가 `상대_경로(root,&f) == "xtask/src/main.rs"` 로 **파일 하나에만** 표지를 듣는다. 그리고 `자기_제외` 상수가 있는 줄 4543 은 두 표지를 한 줄에 지는데, 시작→끝이 같은 줄에서 상쇄돼 누출이 없다 |
| 20 | 「기계 토큰 검사의 모집단이 세 크레이트뿐이라 `pal-cli`·`pal-intent`·`pal-extract` 의 토큰을 안 본다」 | 자기장치 | 거짓 | 거짓신호(이었다면) | `xtask/src/main.rs:862` | 저장소 전수로 `fn name(` 이 세 크레이트 **밖에 0개**이고, 밖의 한국어 serde 토큰은 `pal-intent/src/round_condition.rs:41-90`(게이트 파서 계약, 한국어가 정본) 하나뿐이라 넣으면 그 계약을 금지하는 검사가 된다 |
| 21 | 「`pal query --json` 에 한국어가 나가므로 `C2` 가 반증이다」 | 저장소 | 거짓 | 거짓신호(이었다면) | `crates/pal-core/src/catalog.rs` | 개정 R4 가 카탈로그 요약문을 **명시적으로 대상 밖**으로 뒀다(*"enum 토큰이 아니라 사람이 읽는 설명문"*). `C2` 가 재는 것은 **병기**이고 그것은 0건이다 |
| 22 | 「`F5` 의 효과가 `cargo xtask check` 한 줄이라 「테스트도 CI 도 아닌 것」이 아니다」 | 회차기록 | 거짓 | 미관(이었다면) | 게이트 `:273-281` | 그 절이 실제 산출물 `conditions-audit/r1-raw.md`(26KB · 발견 14 · 기각 7)를 링크하고, 개정 R4 가 그 반환문 때문에 조건 열넷을 고쳤다. 검사 줄은 존재 확인이고 효과는 반환문이다 |
| 23 | 「「완수 조건 설계 평가」 검사가 자기를 만든 회차 하나만 재므로 죽은 가지다」 | 자기장치 | 거짓 | 금지역(이었다면) | `xtask/src/main.rs:4423-4470` | 하한 이후 회차가 늘면 모집단이 자동으로 는다. 그리고 발화 경로가 실제로 관측됐다(게이트 47행 — 산출물이 없을 때 빨갰다가 넣으니 초록). 「0개를 훑고 초록」이 아니라 「1개를 훑고 초록」이다 |

---

## 끝내도 되는가

**안 된다** — 본 목록에 **금지역 둘**(#9 `H2` 통과 판정에 대응하는 CI 런이 없고 브랜치가 push조차 안 됐다 · #10 종료 보고의 독립 리뷰 소모 5 가 산출물 1 과 갈린다)과 **합격선 축 반증 여섯**(`B4`·`D2`·`D4`·`D6`·`G3`·`H2`)이 남았다.

그중 `B4`·`D4`·`G3` 은 상자 채우기 한 걸음으로 닫히는 것을 격리 사본에서 실측했다(26/26 · rc=0). `D2`·`D6`·`H2` 는 그것과 별개다.