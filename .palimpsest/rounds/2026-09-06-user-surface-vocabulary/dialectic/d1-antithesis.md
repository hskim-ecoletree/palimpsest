# 반(反) — `D1` 회차 어휘 판정 초안에 대한 반론

`pal-decision-opponent` 원 반환문. 2026-09-06.

## 초안이 서는 자리

- **`pal round status` 의 사람용 출력이 전부 영어** — `round/status.rs:753-760` 이 `round:`·`verification:`·`terminal:`·`completion:`·`open harmful findings:` 다섯 줄만 낸다.
- **`findings.jsonl` 의 `"처분"` 1462행** — 직접 셌다.
- **「오라클」이 두 뜻이라 회차 쪽만 바꾼다** — `query_log.rs:54`·`pal-git/src/lib.rs:164` 는 test oracle 이고 다른 개념이다.
- **`REQUIRED`·`known_fields` 좌표** — `record.py:99` · `status.rs:466,469` 전부 실재한다.

## 반론

| # | 반론 | 좌표 | 유효성 | 해악도 |
|---|---|---|---|---|
| 1 | **D1 근거 1 이 사실이 아니다.** 「처리 방침·대안 경로·철회·차단·판정 명령」은 소유자가 읽은 표기가 아니라 **에이전트가 소유자 의견에 대한 답으로 쓴 표기**다. 소유자가 읽은 판(`9eec834:docs/overview.md` §8.8)은 「퇴로 / 처분 넷 / 막힘 / 접힘 / 오라클(oracle)」이었다. `git log -S` 로 다섯 낱말 전부가 `a4a5231` 단 한 커밋에서 처음 등장함을 확인했다 | `git log -S "처리 방침" -- docs/overview.md` → `a4a5231` · `git show 9eec834:docs/overview.md` | **참** | **금지역** |
| 2 | **D1 근거 2 가 자기를 무너뜨린다.** 「#111 후보는 소유자가 읽은 적 없고 고르면 근거가 어감뿐」이라는 논변이 overview 의 다섯에 **똑같이** 적용된다 | `d1-thesis.md:63-66` ↔ `a4a5231` | **참** | 거짓신호 |
| 3 | **D3 이 소유자 지시를 정확히 뒤집는다.** 소유자 원문은 **「생략」을 elision(=절단)에 배정**했다 — *"절단, 예산 절단: elision … 일반적으로는 '축약'또는 '생략' 이라고 하고, 그런데 절단은 손상과 파괴에 더 가까운 느낌이라 문맥 상 의도와는 맞지 않아."* 초안은 그 낱말을 **fold** 에 주고, 소유자가 거부한 「절단」은 그대로 둔다 | `git show 9eec834:temp-comment.md` | **참** | **금지역** |
| 4 | **D3 의 전제가 원문 오독이다.** 초안은 `evidence.rs:29` 를 *"질의 결과의 elision"* 이라 적었는데 코드가 명시적으로 **그 반대**를 못 박는다 — `// 접기 — **부피를 옮긴 것. 절단이 아니다** … [f06].fold_is_not_elision`. `Elision` 은 절단 기록이고 `Fold` 는 다른 필드다 | `pal-core/src/envelope.rs:239,244-256` · `evidence.rs:9-12` | **참** | **금지역** |
| 5 | **D3 이 만드는 문장이 자기모순이다.** 제안대로면 화면이 `생략 1027건이 다른 질의로 옮겨졌습니다 — 잘린 것이 아닙니다` 가 된다. 「생략」은 「잘린 것」쪽 낱말이라 뒷절이 앞절을 부정한다 | `evidence.rs:29` · `tests/envelope_two_layer.rs:247-250` | **참** | 거짓신호 |
| 6 | **D2 근거 2 의 인용문이 그 좌표에 없다.** 초안은 `SKILL.md §9` 를 링크하며 *"동결된 문서를 건드리면 안 된다"* 를 **직접 인용**했다. §9 전문에 그 문장이 없다 | `.claude/skills/round/SKILL.md:587-646` · `d1-thesis.md:81-83` | **참** | **금지역** |
| 7 | **「처분」의 원문이 저장소 안에 없다.** 소유자 의견의 유일한 1차 산출물(`temp-comment.md`)에는 `퇴로`·`처분`·`막힘`·`오라클` 이 **한 번도 안 나온다.** 소유자가 실제로 지목한 회차 어휘는 **「접다」 하나뿐**이다 | `git show 9eec834:temp-comment.md` · 이슈 #111 | **참**(부재 확인) | 거짓신호 |
| 8 | **D3 이 「접힘」의 기계 계약 둘을 안 셌다.** ①`status.rs:640` 이 `state.md` 본문에서 **문자열 `"접힘"`** 을 `contains` 로 찾고 없으면 에러를 낸다 ②`xtask/src/main.rs:5039` 가 `folded.md` 에서 **`## 왜 접었나`** 를 `contains` 로 찾는다 | `round/status.rs:640,642` · `xtask/src/main.rs:5039-5042` | **참** | **실패** |
| 9 | **D2 근거 1 의 일반화가 반증된다.** `record.py count` 는 사람이 읽는 표에 **축 이름 「처분」을 그대로 찍고**, `check` 는 stderr 에 `필수 필드 \`처분\` 가 없다` 를 낸다. 그 스크립트는 `pal install` 이 남의 프로젝트로 복사한다 | `record.py:787-791,634` · `install/layout.rs:103-104,169` | **참** | 거짓신호 |
| 10 | **이 저장소의 「표면」 정의가 D2 의 경계를 부정한다.** `docs/overview.md:991` — *"표면(surface) \| **사용자·에이전트**가 닿는 자리"*. 회차를 도는 에이전트가 `findings.jsonl` 을 손으로 쓴다 | `docs/overview.md:991` · `record.py:99,120-131` | **참** | 거짓신호 |
| 11 | **「차단」이 이미 다른 개념으로 점유돼 있다.** `pal` 의 훅은 `{"decision":"block"}` 을 「차단」으로 부르고 시험 이름까지 그렇다. `budget.rs:188-190` 의 「연속 차단 상한」도 별개다 | `tests/hook.rs:109` · `tests/install_hooks.rs:269,308` · `budget.rs:188-190` | **참** | 거짓신호 |
| 12 | **「대안 경로」가 「우회로」와 뭉친다.** §5 는 퇴로와 승격(잠금으로 되돌아가는 **우회로**)을 **서로 대립시켜** 정의한다 | `SKILL.md:175,245` | **참** | 거짓신호 |
| 13 | **「처리 방침」이 동사와 단계 이름 자리에서 안 선다.** 「처분」은 루프의 **단계 이름**이자 동사(`그 발견을 처분한 커밋`)로 쓰인다. 「처리 방침한 커밋」은 성립하지 않는다 | `SKILL.md:170` · `record.py:399` | **참** | 거짓신호 |
| 14 | **D2 의 선택지 셋이 소유자가 지정한 형식을 빠뜨렸다.** 소유자는 *"용어 중 **상태나 코드성 데이터는 영어를 우선으로** 하고, 사용자 표면에 나갈 때 사용자 언어와 같이 표기"* 라 적었다. `처분` 은 JSONL enum — 정확히 「코드성 데이터」다. **`disposition`/`disposal` 로 바꾸고 한국어를 병기**하는 넷째 안이 소유자 지시에서 곧장 나온다. 영어 원 표기는 이미 있다 — `disposal-overrides.jsonl` | `git show 9eec834:temp-comment.md` · `SKILL.md:545` | **참** | **금지역** |
| 15 | **D1 이 병기 형식을 `A3` 보다 먼저 정해 버린다.** 승격하려는 표기는 **「처리 방침 (스킬 용어: 처분)」— 한국어(한국어)** 다. 소유자는 *"한글/영어 혼용하지 말고 통일해"* 와 *「최신 상태 아님(Staled)」* 을 요구했다 | `docs/overview.md:1000` · `intent.md` §A3 | **참** | 거짓신호 |
| 16 | **초안이 만지려는 그 파일에 소유자가 실제로 지목한 결함이 남는다.** 소유자가 `evidence.rs` 계열에서 이름으로 짚은 것은 **「절단」과 「봉투」**다. 초안은 「접힘」 줄만 (반대 방향으로) 고치고 「절단」은 그대로 둔다 | `git show 9eec834:temp-comment.md` · `evidence.rs:9,29,51-54` | **참** | 거짓신호 |
| 17 | **1차 원문이 저장소에서 사라졌는데 초안이 그것을 안 짚었다.** `temp-comment.md` 는 `1718447` 에서 추적에서 빠졌고 워킹트리에도 없다. 유일한 소유자 근거는 `git show 9eec834:temp-comment.md` 로만 닿는다 | `git show --stat 1718447` | **참** | 거짓신호 |

## 대립역이 스스로 물린 것

| 냈다가 물린 것 | 왜 |
|---|---|
| 「오라클」 개명이 `pal.round.oracle.v1` 해시 도메인을 깬다 | 초안이 영어 `oracle` 을 안 건드린다고 명시했다. 다만 초안의 실측 표가 그것을 *"코드 이름이다"* 로만 적은 것은 부정확하다 |
| `## 퇴로` 절 제목이 기계 계약이다 | 근거 없음. `grep -rn "퇴로" crates/ xtask/` 가 0건 |
| 「처분」 전량 이주가 `disposal-overrides.jsonl` 예외표를 깬다 | 모집단이 0이다. 그 파일이 저장소에 없다 |
| 영구 이중 어휘가 새 병을 만든다 | 초안이 이미 비용으로 적었다 |

**형식 고지** — 프롬프트의 *"소유자 원문이 `docs/instructions/` 아래에 있는지"* 라는 안내는 **틀렸다**. 거기 없고 `git show 9eec834:temp-comment.md` 에 있다.
