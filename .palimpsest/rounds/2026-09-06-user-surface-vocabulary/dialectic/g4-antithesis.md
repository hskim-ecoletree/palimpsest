# 반(反) — `G4` 초안을 무너뜨리려 시도한다

`pal-decision-opponent` 원 반환문. 2026-09-06.

> **입력 오염 점검.** 사고 과정·대화 기록은 안 섞여 왔다. 지시대로 `state.md` 는 안 열었다.
> 프롬프트가 준 기계 측정값을 확정으로 받지 않고 여섯을 다시 돌렸고, 어긋난 것은 없었다.

## 초안이 서는 자리 — 무너뜨리지 못한 것

- **기계 측정 여섯을 다시 돌렸고 초안과 어긋난 것이 없다.** `e7b3684` 는
  `KO-SENTENCE-REGISTER.md` 에 대한 유일한 커밋이고(216행),
  `git merge-base --is-ancestor e7b3684 origin/main` 이 참이라 push 됐고,
  `## 4. 교정한 낱말과 표현` 이 있고, `SKILL.md:852` 가 실제로 거짓신호 행이며,
  `#107`~`#114` 는 여덟 다 OPEN 이다.
- **㉰ — `PM2-16` 이 겨눈 자리가 `G4` 가 아니라 `H3` 이라는 읽기.** 원문을 열어 대조했다.
  인용이 정확하고, 그 반환문은 `G4` 에 무엇을 더하라고 적지 않았다.
- **㉲ 의 실측 전부.** `한국어-10` 은 그 저장소에서 `KO-SENTENCE-REGISTER.md` 안에서만
  나타나고, `gh issue list --state all` 이 `[]` 를 내고, `checklists/ko.md` 는 아홉 항목이며
  플러그인 사본과 `diff` 가 없다. 넷 다 재현됐다.
- **㉯ 의 ⑥ 어긋남과 그 해석**(*"거짓신호는 아직 발생하지 않았고 #114 를 닫는 순간에
  발생한다"*)은 성립한다.

## 반론

| # | 반론 | 좌표 | 유효성 | 해악도 |
|---|---|---|---|---|
| 1 | **㉣ 를 빠뜨리는 근거 ②가 사실이 아니다 — 그 이슈는 이미 섰다.** 초안은 *"「상태 넷으로 충분한가」… **그 이슈도 아직 안 섰다**"* 라고 적었다. 초안이 전문으로 받은 그 줄이 같은 문장 안에서 *"섰다 — #116"* 이라고 적고 있고, 조회하면 #116 은 2026-09-06T00:03:07Z 에 서서 OPEN 이다. 「가장 센 경쟁자」를 빠뜨린 근거 둘 중 하나가 조회로 반증된다 | `intent.md:184` · `gh issue view 116` → `OPEN` · `findings.jsonl`(`CA1-13` · 닫힘 · `7c1f7f4`) | 참 | 금지역 |
| 2 | **㉣ 를 빠뜨리는 근거 ①은 「별도 목록 밖」까지만 증명하고 「이슈를 세우면 안 된다」를 증명하지 않는데, 이 회차 자신이 그 밖의 것으로 이슈 셋을 세웠다.** `SKILL.md:898-899` 가 별도 목록을 「자기 장치·회차 기록」으로 좁히는 것은 맞다. 그러나 이 회차는 **모집단이 `규약`·`원의도`인 항목**을 §11 ③ (나)의 분할로 부르며 #116·#117·#118 을 세웠고, #117·#118 본문이 스스로 *"…가 **분할**한 것이다"* 라고 적는다. 초안이 스스로 적은 역전 조건이 이 회차의 실물로 충족된다 | `SKILL.md:898-899,903-910` · `gh issue view 117`·`118` | 참 | 거짓신호 |
| 3 | **이 회차는 「분할했다고 적었는데 이슈가 안 섰다」를 문장이 아니라 이슈로 닫은 전례를 이미 갖고 있다.** `CA1-13` 이 같은 모양을 냈고 처분은 「종료 문서에 적는다」가 아니라 **#116 을 실제로 세우고 `intent.md` 에 링크를 박는 것**이었다. 초안의 *"셋 다 **기록만** 요구한다"* 는 같은 회차가 같은 결함에 내린 처리 방침과 어긋난다 | `findings.jsonl`(`CA1-13`) · `intent.md:184` · `git show 7c1f7f4 --stat` | 참 | 거짓신호 |
| 4 | **`G4-b` 는 지정한 자리에 놓을 수 없고, 놓으면 `cargo xtask check` 가 빨개진다.** 초안은 *"게이트의 `## 판정` **표에서 `G4` 행**이 착지 좌표를 함께 적는다"* 를 요구한다. 그 표는 행이 조건이 아니라 판정 넷이고 두 열이다. 파서가 세 자리에서 죽인다 — `표준 표에 판정 아닌 행이 있다` · `세 칸 이상이다 — 표준 표는 두 열이다` · `조건 ID 꼴이 아니다`(`cut-the-bullshit`·`e7b3684` 는 `^[A-Z][0-9]+(-[a-z])?$` 를 안 지난다) | `record.py:196-198,252,273-277,286-289,294-295` · `SKILL.md:736-755` · `xtask/src/main.rs:643,5273-5312` | 참 | 실패 |
| 5 | **`G4-c` 가 종료 보고에 없는 절을 지목하고, 있는 절의 정의와 부딪힌다.** §10 의 종료 보고 절은 다섯이고 `## 효과` 는 그중에 없다 — 그것은 **게이트** 절이다. 그리고 §10 이 `## 범위 밖` 을 *"착수 때 정한 것. **회차 중에 새로 생기지 않는다**"* 로 못 박았는데 「판정 시점에 다시 잰 타 저장소 수령 상태」는 착수 때 정한 것이 아니다. §11 의 음성 대조가 이 모양을 *"종료 보고에 그 넷 아닌 **새 이름의 목록**이 생긴다"* 로 부른다 | `SKILL.md:786-793` · `:704-707,729-733` · `:962` | 참 | 거짓신호 |
| 6 | **새 조건 셋이 판정 수단 없는 서술형인데, 이 회차는 바로 그 결함을 이미 한 번 닫았고 그것을 잴 라운드도 안 썼다.** §3.5 는 *"결정론적 검증이 가능하다 → **무조건 시험을 작성한다**"* 이고, 초안은 *"셋 다 문면 대조로 잰다"* 라고만 적을 뿐 시험도 의미론적 검증 방법도 등록하지 않는다. `CA1-12` 가 같은 결함을 냈고 정정으로 닫혔다. §3.5 의 상한은 2 라운드인데 `conditions-audit/` 에는 `r1-raw.md` 하나뿐이라 **평가자가 조건 작성자와 갈리는 자리가 한 칸 비어 있다** | `SKILL.md:176-190,197-203,205-213` · `findings.jsonl`(`CA1-12`) · `ls conditions-audit/` | 참 | 거짓신호 |
| 7 | **새 조건 ID 셋을 원장에 넣는 것은 §5 ③ 의 「편입」이므로 확대다 — 「정정」 딱지가 설계문이 ㉢ 에만 건 관문을 우회한다.** `H3-a`·`G4-b`·`G4-c` 는 기존 문면의 수정이 아니라 원장 둘에 들어가는 **새 원소 셋**이고 검산 합을 셋 올린다. 초안 스스로 ㉵ 에서 **확대의 시험**을 돌리면서 판정 이름은 정정으로 붙였다 | `SKILL.md:314-315,319,321,366-371` · `record.py:70-78,120-131,308-319,353-358` · `g4-design.md` 승격 배선 ㉢ 행 | 참 | 거짓신호 |
| 8 | **㉳ 은 순환이다 — `처분` 은 이 판의 입력이 아니라 출력이다.** 초안은 `findings.jsonl` 의 `처분=정정` 을 판정 근거로 쓴다. 그 필드의 정의가 *"처분은 **무엇을 하기로 했나**"* 이고 `사전처분=계획수정` 이 그것이 사전부검이 적은 **계획**임을 말한다. 설계문이 ㉠~㉣ 중 하나를 고르라고 세운 판의 대상이 바로 그 값이다. 덧붙여 ㉡ 을 채택하면 같은 행의 `"조건변경": "없음"`·`"조건": "없음"` 이 거짓이 된다 | `findings.jsonl`(`PM2-16`) · `record.py:87-90,70-78,99-104` | 참 | 거짓신호 |
| 9 | **㉮ 의 넷째 행은 설계문이 판 밖으로 뺀 조건 판정이고, 근거가 문서의 자기 선언뿐이다.** 설계문이 *"`G4` 의 「palimpsest 도메인 용어는 뺀다」 조항이 충족됐나 — 그것은 조건 판정(§5 ①)이고 이 판은 확장 판단(§5 ②)이다"* 라고 못 박았다. 산문 자기 선언은 측정이 아니다. 그리고 그 행이 *"`G4` 의 문면 셋은 이미 충족됐다"* 라는 결론의 하중을 진다 | `g4-design.md` 「이 판이 안 다루는 것」 4행 · `KO-SENTENCE-REGISTER.md:6-8,157` | 참 | 거짓신호 |
| 10 | **㉮ 가 댄 좌표 다섯 중 넷이 실물과 안 맞는다.** 초안은 `:155,157,181,190,197` 을 적었다. 실제로 `## 4` 는 155 이고 `### 4.1`=159 · `4.2`=172 · `4.3`=180 · `4.4`=189 다. 157·181·190·197 은 각각 한 줄 문장·표 구분행·빈 줄·인용문 안이다 | `grep -n '^### ' KO-SENTENCE-REGISTER.md` → `159,172,180,189` | 참 | 미관 |
| 11 | **설계문이 「인용으로 지나야 한다」고 못 박은 좌표 다섯 중 2번을 초안이 한 번도 인용하지 않았다.** 좌표 2 는 `## 승격` 4번의 **소유자 답 원문**이다. 초안에는 「승격 4번」이라는 이름만 세 번 나오고 원문은 없다. 그 원문은 *"cut-the-bullshit **에서 처리하도록 하려고 하니** 현상이나 설계가 **가능하다면** 설계까지 별도 md 파일로 만들어줘"* 이고, 「처리하도록 하려고 하니」는 **수령처의 처리**를 전제로 문서를 시킨 것이다. 초안의 *"셋 다 기록만 요구한다"* 독법과 ㉣ 사이의 저울이 정확히 그 문장 위에 있다 | `g4-design.md` 좌표 2번 · `intent.md:355` · `g4-thesis.md`(원문 인용 0회) | 참 | 거짓신호 |
| 12 | **`H3-a` 가 넣으라는 문장이 #114 의 실제 완료 조건과 어긋난다.** 초안 문면은 *"#114 의 닫힘은 `G2` 의 패턴 교정과 `G4` 의 문서 제출을 뜻하고"* 인데, #114 본문의 완료 조건 넷은 `G1`·`G2`·`G3`·`G2-a` 에 대응한다. **`G4` 는 #114 의 완료 조건이 아니다** — 개정 R1 이 이 회차에 신설한 확대다 | `gh issue view 114` · `intent.md:160-164,247` | 참 | 거짓신호 |
| 13 | **소유자가 「이해할 수 있는 한국인은 없다」고 단언한 그 표현이 이 저장소에 실물로 남는데, 그것을 잴 조건이 원리상 없다.** `docs/adr/0024-…:40` 이 *"그 표면에 대해 검사가 **꺼진 채로 돈다**"* 를 갖고 있다. `docs/adr/**` 는 `G1` 이 동결했고 `G2` 의 아홉 뿌리에도 없다. 초안은 이 항을 *"`G2`·`PM2-17` 의 자리다"* 로 넘겼는데, 넘긴 그 자리가 정의상 이 문장을 못 잰다 | `docs/adr/0024-an-adapter-that-can-diverge-is-a-second-core.md:40` · `intent.md:160-161` · `owner-source-9eec834.txt:33` | 참 | 거짓신호 |

## 내가 스스로 물린 것

- **「`KO-SENTENCE-REGISTER.md` 에 palimpsest 도메인 용어가 남았다」를 세우려 했다.** 걷었다.
  「음성 대조」는 그 저장소 자신의 어휘로 이미 여러 파일에 있다. 수입된 용어가 아니다.
- **「#118 이 `G4` 의 넘김을 이미 수령했으므로 ㉲ 가 무너진다」를 세우려 했다.** 걷었다.
  #118 은 palimpsest 이슈이고 갈래가 다르며 `한국어-10` 을 등록하지 않는다.
- **「`G4-b` 가 타 저장소 경로를 링크로 적으면 죽은 링크 검사가 빨개진다」를 세우려 했다.**
  걷었다. 초안이 「링크로 적는다」고 쓰지 않았고 좌표를 못 댄다.
- **「독립 리뷰 5 라운드가 0 회 돌았으므로 종료가 더 필요하다」를 세우려 했다.** 걷었다.
  설계문이 그것을 판 밖으로 뺐다. 초안을 무너뜨리는 반론이 아니라 다른 물음이다.
