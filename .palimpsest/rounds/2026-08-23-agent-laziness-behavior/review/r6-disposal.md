# 독립 리뷰 R6 — 처분표

> 원 반환문: [`r6-raw.md`](r6-raw.md) · **남기지 않는다.**

리뷰어가 스물하나를 냈고 **여덟을 스스로 기각했다.**

★ **이 라운드는 재현에 시간을 다 썼고 게이트가 세운 것 대부분이 실물로 섰다** —
`O1` 서른여섯 칸 재채점(소수점까지 같음) · 브리핑 발췌 여섯 구간이 사전 등록 시점
`SKILL.md` 와 줄 단위 완전 일치 · 전사 서른여섯의 모델·도구 목록·`agentId`·`SendMessage` 0 ·
**`findings.jsonl` 92 행이 다섯 원 반환문과 행 순서까지 어긋남 0.**
**그래서 기각이 여덟이다.**

## ★★ 이 라운드의 성격 — 반대 방향의 병

앞 다섯 라운드는 **내가 자기를 좋게 적은 것**을 잡았다.
**이 라운드는 내가 자기를 나쁘게 적은 것을 잡았다.**

| 조건 | 앞 판 | 지금 | 까닭 |
|---|---|---|---|
| **`A10`** | 반증 | **통과** | 조건문은 *"해시를 적고 `git diff` 를 첨부했다"* 이고 **둘 다 서 있다.** 거짓이었던 것은 *"한 자도 안 바뀌었다"* 라는 **산문**이고 **그 문장을 지는 조건은 없다** |
| **`B6`** | 반증 | **통과** | 원천은 `rules.log` 에 못박혔고, 짝짓기가 지금 **두 원천**으로 선다 |
| **`C2`** | 대조불가 | **통과** | 리뷰어가 `O2` **서른여섯 칸을 손으로 세어** 자동 계수와 맞췄다 — 댈 자리가 있었다 |

⚠ **과잉 자백은 축소만큼 나쁘다.** 축소는 나쁜 것을 감추고, 과잉 자백은
**멀쩡한 것을 못 믿게 만든다.** 이 회차는 **양쪽을 다 저질렀다.**

## 처분 — 금지역 하나

| 무엇 | 처분 |
|---|---|
| ★ **`a2-oracle-negative-control.log` 가 아직 「다섯 축이 전부 발화한다 · `A2` 는 통과」로 끝난다.** 게이트가 그 문서를 **「전문」으로 가리키면서** 아래에서 그 결론을 반증으로 뒤집는다. **같은 회차가 `control-saturation.log` 에는 정정 블록을 붙였으면서 여기엔 안 붙였다** | **정정.** 정정 블록을 붙였다 — *"발화한 것은 셋이다"* |

## 처분 — 나머지

| 무엇 | 처분 |
|---|---|
| **`plan.md` 가 아직 버려진 과제(표현식 계산기·CSV 정규화기)를 적는다.** 과제를 갈아치운 커밋이 `plan.md` 에서 **모델 한 줄만** 고쳤다 | **정정.** 표를 갈고 **왜 갈렸는지**를 함께 적었다 |
| **판정문의 「파일럿 다섯」이 보존 관측 넷과 어긋난다** — `pb` 가 관측표에 없다 | **정정.** `pb` 를 실었다 |
| 정정문이 대는 `SKILL.md:331` 이 실제로는 다른 줄이다(내 §7 기입이 줄을 밀었다) | **정정** |
| ★ **등록된 음성 대조 열둘 중 넷만 걸었고, 안 건 여덟에 `A7`·`A8`·`C6`·`C8` 의 것이 있는데 넷 다 통과로 닫혔다** | **정정.** 게이트에 적었다 — **그 넷의 통과는 다른 셋보다 약하다** |
| ★ **진행 원장이 여섯 라운드째 승격 안 된 채다** | **범위밖 · 그러나 기록한다.** 실험은 끝났으므로 지금 올려도 측정이 안 바뀐다. **「승격했어야 했는데 안 했다」를 종료 보고가 진다** |
| 기각 여덟 | **기각** |

## 판정이 또 바뀌었다

**통과 31 · 반증 5 · 대조불가 1 · 미측정 2 = 39.**

⚠ 내가 쓴 검산 줄이 **또 틀렸다**(`반증 4` 라 적었는데 ID 는 다섯) — **게이트 파서가 잡았다.**
**산문에 수를 적으면 틀린다**는 것을 이 회차가 세 번째로 보였다.
