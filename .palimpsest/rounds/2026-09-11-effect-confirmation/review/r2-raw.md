# 독립 리뷰 라운드 2 — 회차 `2026-09-11-effect-confirmation`

> 잰 날 2026-09-12 · HEAD `1b2ead6` · 착수 `8604d62`
> 읽지 않은 것: `review/r1-raw.md`(규약대로). 원장의 `IR1-*` 레코드는 중복 계수를 피하려고 읽었다.
> 돌린 것: `cargo xtask check`(28/28) · `cargo test --workspace`(rc=0) ·
> `cargo check --workspace --all-targets`(mtime 전량 갱신 후 3m13s · 경고 0) ·
> `cargo build --release --workspace`(경고 0) · `pal ledger` · `pal query narrative.unbound` ·
> `pal doctor` · `pal symbols /dev/null` · `git merge-base --is-ancestor` 열둘 · `gh issue view` 일곱.

## 합격선 축

⚠ 아래 「잰 수」는 **내가 이 라운드에 실제로 돌려 얻은 값**이다. 「전사」라 적은 것은 회차의
보존 산출을 읽어 자기정합만 확인한 것이고 내가 재현하지 못한 것은 `## 미측정 목록` 에 있다.

| 조건 | 판정 | 잰 수 | 근거 |
|---|---|---|---|
| A1 | 통과 | 3/3 `rc=0` · SHA 3 쌍 모두 다름 | `git merge-base --is-ancestor a74737b 14f0f92`·`77dc977 88dbe13`·`22cf488 9d94f34` 전부 `rc=0` |
| A1-b | 통과 | 3/3 해시 일치 | `git rev-parse <봉인>:…/plan/<이슈>-pre.md` = `e6f401a0…`·`9b0f3a5a…`·`00c807cd…` 가 `touch/<이슈>.txt` 머리의 `사전등록-blob` 과 같다 |
| A1-c | 통과 | 3/3 **EXACT** (5249·7549·6464 바이트) | `judge-prompt.md` 의 `## ① 사전 등록 전문` 절에서 구분선을 떼어 `git cat-file blob` 과 바이트 비교했다 |
| A1-a | 통과 ⟨전사⟩ | 사본 4 · 뒤집힘 4 | `effect/negative-A1.md` — `neg1` `A1` · `neg2` `A1-b` · `neg3` `A1` · `neg4` `A1-c` 가 각각 FAIL |
| A2 | 통과 | 3/3 머리에 넷(㉢ 은 셋 + 「원리상 없다」) | `touch/{126,79,129}.txt:3-12` |
| A2-b | 통과 | 3/3 sha256 일치 | 덧붙임 앞까지 자른 본문 해시가 `rerun-*.txt` 가 선언한 값과 같다. ⚠ **문서가 적은 `diff` 명령 그대로는 ㉠·㉢ 에서 `rc=1`** — 아래 새 발견 3 |
| A2-c | 통과 | ㉢ 산출 둘째 줄이 후보 3 건 | `touch/129.txt:16` · 머리가 셋째 항의 부재를 적는다 |
| A3 | 통과 | 3/3 좌표절에 `파일:줄` 5·6·8 건 | `plan/{126,79,129}-pre.md` 의 「바꿀 좌표」 절 |
| A3-a | 통과 ⟨전사⟩ | 사본 1 · `FAIL A3 — 좌표절 1 · 파일:줄 0 건` | `effect/negative-A3.md:18` |
| A4 | 통과 | 3/3 에 §5.8 표 전문(4 행) | `plan/129-pre.md` §2.3 · `plan/126-pre.md` · `plan/79-pre.md` |
| **A5** | **반증** | ㉡ `rc=1` · ㉠㉢ `rc=0` | `git merge-base --is-ancestor 14f0f92 8644835` → **`rc=1`**. ㉡ 의 사전 등록 목록은 `xtask/src/main.rs` 이고 `8604d62..HEAD` 에서 그 파일을 건드린 첫 커밋이 `8644835`(22:07)다. 게이트와 같은 값 |
| A5-b | 통과 ⟨전사⟩ | 앵커 2/3 · ㉢ 대조불가 | `effect/126-delta.md` §3 `1f184642…`→`e18b2faa…`. 봉인 시점 본문 해시를 내가 다시 안 냈다 |
| A5-d | 통과 ⟨전사⟩ | ㉡ 움직임 1 · ㉠ 움직임 1 · ㉢ 대조불가 | `effect/{126,79}-delta.md` §3 |
| A5-e | 통과 | ⓐ ㉢ 에 `body` 줄 없음 · ⓑ 사전 등록에 기재 | `touch/129.txt` 전문에 `fun · … body` 줄 0 건 |
| A5-c | **미측정** | — | 게이트 `:74` — 판 2 가 라운드 1 에서 미측정으로 닫았다. `intent.md:196` 상자 안 켜짐 |
| A5-a | 통과 ⟨전사⟩ | 사본 2 · 뒤집힘 2 | `effect/negative-A5.md:16`(`FAIL A5`)·`:33`(`body 7058bb9c2fcf`→`305e595b853e`) |
| A6 | 통과 | 보존본 3(심볼마다 1) | `ls touch/` = `126.txt` `79.txt` `129.txt` |
| **A7** | **반증** ⟨게이트와 갈린다⟩ | ㉡ `rc=1` · ㉠㉢ `rc=0` | `git merge-base --is-ancestor 3637ea9 8644835` → **`rc=1`**. A7 문면이 *"커밋 순서는 `A1`·`A5` 와 **같은 자**로 판정한다"* 라 적고 A5 의 자는 *"(그 목록의 파일을 건드린 첫 커밋)"* 이다. 게이트는 A5 에서 완화 독해를 명시적으로 **거부**해 놓고(`:50`) A7 에만 그 독해를 썼다 — 아래 의도 축 1 |
| A7-a | 통과 ⟨전사⟩ | 사본 2 · 뒤집힘 2 | `effect/negative-A7.md` — `neg6`·`neg7` 이 각각 `FAIL A7` |
| B1 | 통과 ⟨정반합·전사⟩ | 코드 변경 커밋 3(`398d233`·`9f993cc`·`1abc0e9`) | `git log --reverse 8604d62..HEAD -- <각 목록 파일>` |
| B1-a | 통과 ⟨전사⟩ | `rc=1` · 의도 저장소 있음 | `observations/red-129.txt`. 고쳐진 뒤라 내가 RED 를 다시 못 잰다 |
| B1-c | 통과 | 시험 3 자리 — `xtask/src/main.rs:6449-6491`(3) · `crates/pal-cli/src/ledger.rs:861·885·923`(3) · `crates/pal-cli/tests/narrative_read_only.rs` | `cargo test --workspace` 가 전부 돌았다(rc=0) |
| B1-d | 통과 | **`rc=0`** · 27350 줄 산출 | `./target/release/pal query narrative.unbound` 를 내가 HEAD 에서 다시 돌렸다 |
| B1-e | 통과 | 3/3 사전 등록에 시험↔RED 짝 | `plan/{126,79,129}-pre.md` 의 「시험과 그것이 재는 RED」 절 |
| B2 | 통과 | **28/28** · `rc=0` | `cargo xtask check` 전 출력 32 줄 — 실패 0 |
| B3 | 통과 | **passed 합 1047** ≥ 1028 · `test result:` 49 줄 · FAILED 0 · `rc=0` | `cargo test --workspace` 전 출력 |
| B4 | 통과 | 경고 **0** | `.rs` 전량 mtime 갱신 후 `cargo check --workspace --all-targets`(3m13s 전량 재컴파일) `grep -c '^warning'` = 0 · `cargo build --release --workspace` 도 0 |
| B5 | 통과 | `순서에 취약 20 · 등급이 낮음 3309 · 정확 6 · 불가 0 · 합 3335` | `./target/release/pal ledger` 를 내가 HEAD 에서 다시 돌렸다 — 명령이 그 수를 낸다 |
| B5-a | 통과 | 모집단 정의 1 절 | `plan/79-pre.md` §4 「모집단 — 분모가 무엇인가」 |
| B6 | 통과 | 갈래 = 커밋 시각 · 「커밋 0」 처분이 코드·판정문·시험에 | `xtask/src/main.rs:6427`(`unwrap_or(i64::MAX)`) · `:6356`(근거 문자열) · `:6551-6552`(시험) |
| **C1** | **반증** | 13 행 중 원인 끝이 touch 인 것 **3** | `effect/{126,79,129}-delta.md` §2 전수. 게이트와 같은 값 |
| C2 | **미측정** | — | 소유자 `intent.md` `## 승격` 칸 2. 상자 안 켜짐 |
| C2-b | **미측정** | — | 같은 칸 |
| C2-a | 통과 ⟨전사⟩ | ⓐ 유효 3·무효 2 · ⓑ 유효 **0**·무효 2 | `effect/negative-C2.md` · `effect/126-judge-{1,2}.md` |
| **C3** | **반증** | 프롬프트 판 **4** ↔ 명단 **5** | `grep -c '판 [0-9]' effect/judge-prompt.md` 의 판 머리가 1·2·3·4 뿐이고 `judge-roster.md` 5 행 · 반환문 파일 5 개다 |
| C3-a | 통과 | 명단 5 행 · 「N 건인지 확인해라」 0 건 | `effect/judge-roster.md` · `judge-prompt.md` 의 `## 물음` 절 |
| C3-b | 통과 | 반환문 **5** = 명단 **5** | `effect/{126-judge-1,126-judge-2,79-judge-1,129-judge-1,129-judge-2}.md` |
| **C4** | **반증** | 근거 절 붙은 셀 **5** · 안 붙은 **8** = 13 | 게이트 `:296-341` 표를 전수로 셌다 — 「해당 없음」만 있는 셀 8 |
| C5 | 통과 | 3/3 delta 존재 · ㉢ 의 「0」도 기재 | `effect/129-delta.md:33` |
| D1 | 통과 | 정정 2 행 + 잰 명령 2 | `pal ledger`(심볼 3335·Rust L1 141 파일) · `pal symbols /dev/null` 가 **다섯**을 오류문에 싣는다 — 내가 둘 다 다시 돌렸다 |
| D2 | 통과 ⟨정반합·전사⟩ | 규범형 문장 1(소유자 2026-09-12) | 게이트 `:127-205` · `intent.md` `## 승격` 칸 15 |
| D3 | 통과 | 갈래 ⓐ 값 있음 · ⓑ 값 없음 | `pal ledger` 머리 = `저장소 선언 1 · 본 것 1` |
| D4 | 통과 | `#66` **CLOSED** `2026-09-12T06:05:04Z` | `gh issue view 66` |
| D4-a | 통과 | 5 자리 — 안 고침 4 · 고침 1 | `scripts/f12-verify.py:556-570` 이 실제로 고쳐져 있다 · `docs/gates/F12.md:312`·`:398` · `corpus/criteria.toml:10530`·`:10886` 는 그대로 |
| E1 | 통과 | 절 4 · 합 41+4+0+5 = **50** · `intent.md` 조건 ID 집합과 **집합 같음**(차집합 양쪽 0) | 게이트 `:27-36` 을 파싱해 `intent.md` 의 체크박스 50 과 대조했다 |
| E2 | 통과 | 명령 4 · 방향 13/13 | `effect/e2-run.txt`(180 줄) · 게이트 `:296-341` |
| **E4** | **반증** ⟨게이트와 갈린다⟩ | 열린 발견 **1**(`IR1-05`) | `findings.jsonl` 306 행 중 `상태=열림` 1. 게이트는 통과로 싣는다 — 아래 새 발견 1 |
| E5 | **미측정** | 회차 마지막 커밋 없음 · `origin/main` 은 `72bd281`(18 커밋 뒤) | `gh run list` 에 `1b2ead6` 런 0 건 |
| E6 | 통과 | 2 문서 갱신 | `docs/plan/03-shortest-path.md:139` · `docs/plan/02-order.md:252-259` |

**내 검산** — 통과 **39** · 반증 **6** · 대조불가 **0** · 미측정 **5** = **50**.
게이트(통과 41 · 반증 4)와 **두 자리에서 갈린다** — `A7` 과 `E4`.

## 미측정 목록

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | 음성 대조 다섯(`A1-a` `A3-a` `A5-a` `A7-a` `C2-a`)의 격리 사본 아홉을 내가 재실행하지 못했다 — 판정을 낸 `judge.sh` 가 저장소에 없다(`find . -name judge.sh` 빈 출력). 사양 여섯 줄은 인라인돼 있어 자기정합만 확인했다 | 원의도 | 참 | 미관 | A1-a·A3-a·A5-a·A7-a·C2-a | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/negative-A1.md:13-20` | `find . -name 'judge.sh' -not -path './target/*'` → 빈 출력 |
| 2 | `A5-b`·`A5-d` 의 앵커를 내가 다시 내지 못했다 — 봉인 커밋 시점에 `pal touch --at` 을 다시 돌려 `body <hash>` 를 재계산하지 않았다 | 원의도 | 참 | 미관 | A5-b·A5-d | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/126-delta.md` §3 | 안 돌렸다 — 전사만 읽었다 |
| 3 | `B1-a` 의 RED 를 내가 못 쟀다 — `#129` 가 이미 고쳐져 `pal query narrative.unbound` 가 `rc=0` 이다. 원리상 못 잰다 | 원의도 | 참 | 미관 | B1-a | `.palimpsest/rounds/2026-09-11-effect-confirmation/observations/red-129.txt` | `./target/release/pal query narrative.unbound` → `rc=0` |
| 4 | `E5` 를 못 쟀다 — 회차의 마지막 커밋이 아직 없고 `1b2ead6` 에 붙은 런이 0 건이다 | 원의도 | 참 | 미관 | E5 | 없음 | `gh run list --limit 5` 의 최신 `headSha` 가 `72bd281` |
| 5 | `A5-c`·`C2`·`C2-b`·`E3` 는 회차가 미측정으로 닫은 것이라 내가 판정을 새로 세우지 않았다 — ⟨정반합⟩ 이거나 소유자 결정이다 | 원의도 | 참 | 미관 | A5-c·C2·C2-b·E3 | `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:196` | `intent.md` 의 안 켜진 상자 다섯과 게이트 미측정 표가 같다 |

## 의도 축

### 빠진 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 6 | 원문의 합격선 본체가 **안 재어진 채로 회차가 닫히려 한다** — 원문은 *"그 산출을 보고 달라진 변경 **1 건 이상**, 좌표와 함께"* 인데 그것을 지는 `C2`·`C2-b` 가 둘 다 미측정이다. 실질(귀속 후보 3 · 판정자 유효 3)은 있으나 **조건으로 잰 것은 없다**. 소유자 칸 2 가 정한 것이라 규약 위반은 아니지만, **회차가 「효과 확인」을 주장할 수 있는 근거가 조건 축에는 0 이다** | 원의도 | 참 | 거짓신호 | C2·C2-b | `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:237-238` | 게이트 `:71-77` 의 미측정 표 · `intent.md` 상자 `[ ]` 둘 |
| 7 | 종료 보고 `report.md` 가 아직 없어 **네 이름**(의도적으로 안 한 것 · 확인 못 한 것 · 추론 · 넘기는 것)이 하나도 안 섰다. ⚠ **재발** — 원장 `IR1-05` 가 같은 것을 냈고 지금도 **열린 채**다(회차의 유일한 열린 발견) | 원의도 | 참 | 거짓신호 | E3 | `.palimpsest/rounds/2026-09-11-effect-confirmation/` | `test -f …/report.md` → 없음 · `findings.jsonl` 의 `IR1-05` `상태=열림` |

### 요구되지 않은 것

없음 — 착수 이후 회차 디렉터리 밖에 **새로 선 파일은 둘**(`docs/gates/effect-confirmation.md` · `crates/pal-cli/tests/narrative_read_only.rs`)이고 둘 다 조건이 요구한 것이다. 인터뷰가 잠근 *"(가) 장치를 안 만든다"* 도 지켜졌다 — 판정 스크립트가 커밋되지 않았다.

### 있는데 틀린 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 8 | ★★ **`A7` 이 `A5` 와 같은 자로 안 재어졌고 게이트가 그것을 「통과」로 싣는다.** `A7` 문면은 *"커밋 순서는 `A1`·`A5` 와 **같은 자**로 판정한다"* 이고 `A5` 의 자는 괄호로 못 박은 *"(그 목록의 파일을 건드린 첫 커밋)"* 이다. 그 자로 ㉡ 을 재면 readnote `3637ea9`(22:30)가 첫 코드 변경 커밋 `8644835`(22:07)의 **조상이 아니다**. 게이트는 바로 그 완화 독해(*"그 자리의 **작업**으로 건드린 첫 커밋"*)를 `A5` 에서 **명시적으로 거부**하면서 *"여기서만 다르게 읽지 않는다"* 라 적어 놓고, `A7` 에는 그 독해를 썼다. **`A5` 를 뒤집은 자가 `A7` 에 전파되지 않았다** | 원의도 | 참 | **금지역** ⟨사실이 아닌 것을 사실로⟩ | A7 | `docs/gates/effect-confirmation.md:31` · `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:199` | `git merge-base --is-ancestor 3637ea9 8644835` → **`rc=1`** · `… f60df1d 9f993cc` → `rc=0` · `… 138c782 1abc0e9` → `rc=0`. 그리고 `grep -rn "8644835"` 가 회차 문서 여덟에 적중하는데 **`A7` 을 다시 잰 자리는 하나도 없다** |

## 이번 라운드의 새 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 9 | **게이트가 `E4` 를 통과로 싣는데 지금 원장에 열린 발견이 1 건 있다.** `IR1-05`(`E3` · 거짓신호)가 `상태=열림` 이고 `닫은커밋` 이 `null` 이다. 판정이 선 커밋(`39b0fa3`)에서는 참이었으나 그 뒤 `1b2ead6` 이 독립 리뷰 R1 을 적재하면서 다시 1 이 됐고 **판정 표가 안 따라갔다**. ⚠ `cargo xtask check` 의 「발견이 닫혔나」는 *"열림 1행 (끝난 회차 0)"* 으로 통과하므로 **기계가 이것을 안 잡는다** | 원의도 | 참 | 거짓신호 | E4 | `docs/gates/effect-confirmation.md:31` | `python3` 로 `findings.jsonl` 306 행을 세어 `Counter({'닫힘':304,'열림':1})` · `cargo xtask check` 의 「발견이 닫혔나」 줄 |
| 10 | **`C1` 의 증거 문서가 HEAD 이력 밖의 커밋을 인과 사슬의 한 끝으로 싣는다.** `effect/126-delta.md:4` 가 readnote 를 `9be7699` 라 적는데 그 커밋은 `HEAD` 의 조상이 아니다(고쳐 쓰기 전의 판이고 실물은 `3637ea9`). 두 커밋의 트리는 같지만 **새 클론에서는 `9be7699` 가 아예 없어** 그 줄로는 `A7` 의 순서를 아무도 못 되짚는다. ㉠·㉢ 의 delta 는 살아 있는 SHA 를 쓴다 | 원의도 | 참 | 거짓신호 | C1·A7 | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/126-delta.md:4` | 회차 문서와 게이트의 7~12 자리 hex 90 개를 전수로 훑어 `git cat-file -t` 가 commit 인데 `merge-base --is-ancestor … HEAD` 가 `rc=1` 인 것 둘을 찾았다 — `9be7699` · `6e26be5` |
| 11 | **`A2-b` 가 문서로 적은 재실행 대조 명령이 ㉠·㉢ 에서 `rc=1` 을 낸다.** `effect/rerun-126.txt:6-8` 의 `diff <(sed -n '/^--- 전 출력/,$p' …) …` 을 그대로 돌리면 ㉡ 만 빈 diff 이고 ㉠·㉢ 은 다르다 — 뒤에 붙인 `--- ⟨덧붙임⟩` 절이 마커 **뒤**에 있어 함께 딸려 나오기 때문이다. `rerun-79.txt:4` 는 *"명령은 `effect/rerun-126.txt` 머리와 같다"* 로 그 명령을 그대로 승계한다. ⚠ **선언된 sha256 은 맞다**(덧붙임을 자르면 정확히 일치) — 깨진 것은 재현 절차이지 사실이 아니다 | 원의도 | 참 | 거짓신호 | A2-b | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/rerun-126.txt:6-8` · `effect/rerun-79.txt:4` | `diff <(sed -n '/^--- 전 출력/,$p' touch/79.txt \| tail -n +2) <(sed -n … rerun-79.txt \| tail -n +2)` → `rc=1`; ㉢ 도 `rc=1`; 덧붙임 앞까지 자른 sha256 은 `1fdf18fb…`·`0766ec2f…` 로 선언값과 일치 |
| 12 | **계획 문서 둘 중 한쪽만 `C2` 미측정을 싣는다.** `docs/plan/02-order.md:259` 가 3 단계를 취소선 + *"**닫혔다 2026-09-12**"* 로 적고 *"어긋남은 해소됐다"* 까지 쓰는데, 그 단계의 합격선 본체가 미측정이라는 말이 **한 줄도 없다**. 형제 문서 `03-shortest-path.md:139` 는 `IR1-08` 의 처분으로 그 경고를 싣는다 — **같은 형태가 처분자리 밖에 남았다**(`IR1-08` 의 `처분자리` 는 `docs/plan/03-shortest-path.md` 하나다) | 원의도 | 참 | 거짓신호 | E6·C2 | `docs/plan/02-order.md:259` | `grep -n "effect-confirmation" docs/plan/02-order.md` · `IR1-08` 레코드의 `처분자리` 값 |
| 13 | **원 의도의 합격선이 실제로 기대는 귀속은 하나이고 그것이 「touch 가 틀려서」 난 것이다.** 원인 끝이 touch 인 차이 셋 중 — ㉡㉰ 의 결과 좌표 `xtask/src/main.rs:6407` 은 **근거만 바뀐 자리**라 코드에 흔적이 없고(`grep -c "선언 순서" xtask/src/main.rs` = **0**), ㉡㉱ 의 `xtask/src/main.rs:643` 은 변경 커밋 `398d233` 의 hunk(`@@ -6334` · `@@ -6389`) **밖**이라 안 바뀐 줄이며, 실제로 바뀐 줄을 가리키는 것은 ㉠㉱ 의 `crates/pal-cli/src/defect.rs:326`(`@@ -319,7 +319,11` 안) 하나뿐이다. 그리고 그 하나의 방향은 게이트가 스스로 **「나빠졌다」**로 적는다 — touch 가 호출자를 3 이라 해서 사람이 `grep` 을 돌렸다. ⟨`DL1-23` 의 연장 — 그 발견은 ㉡ 둘만 다뤘고 **셋을 합쳐 원 의도 합격선에 댄 줄은 없다**⟩ | 원의도 | 참 | 거짓신호 | C2·C4 | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/126-delta.md:38-39` · `effect/79-delta.md:38` | `git diff 398d233^ 398d233 -- xtask/src/main.rs \| grep "^@@"` → `@@ -6334,23` · `@@ -6389,6` · `git diff 9f993cc^ 9f993cc -- crates/pal-cli/src/defect.rs \| grep "^@@"` → `@@ -319,7 +319,11` |
| 14 | **`docs/plan/03-shortest-path.md` §4 의 표가 끊겨 3 단계 행이 표로 렌더링되지 않는다.** `:126-130` 이 헤더+행 셋이고 `:131-138` 에 빈 줄과 산문 일곱 줄이 들어간 뒤 `:139` 가 다시 `**3**` 행(세로줄로 시작하는 줄) 이라 GFM 에서는 **머리 없는 한 줄**이 되어 파이프가 그대로 보인다. 끊은 문단은 착수 전(`9e043346` 2026-09-10)에 들어왔으나 **이 회차가 `:139` 를 다시 썼고**(`6a31780`) 고치지 않았다 — `E6` 이 가리키는 바로 그 행이다 | 저장소 | 참 | 미관 | E6 | `docs/plan/03-shortest-path.md:131-139` | `git blame -L 127,139 docs/plan/03-shortest-path.md` → `:131-138` 은 `9e043346`, `:139` 는 `6a317803` |

## 자기 산출에 대한 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 15 | `state.md` 의 「지금 수」 표가 **또** 낡았다 — *"원장 **283 행**"*(지금 306) · *"**열린 발견 0** ⟨`E4` 통과⟩"*(지금 1) · *"독립 리뷰 **1/3**"*(지금 2/3 가 돌고 있다). `IR1-12` 가 같은 표의 같은 병을 한 번 고쳤고 **한 커밋 만에 되돌아왔다** | 회차기록 | 참 | 거짓신호 | 없음 | `.palimpsest/rounds/2026-09-11-effect-confirmation/state.md:144-149` | `wc -l findings.jsonl` = 306 · `상태=열림` 1건 |
| 16 | `state.md:192`·`:193` 이 인용한 커밋 `6e26be5`(판 4 R3 의 반 · 판 5 R2 의 정)가 **HEAD 이력 밖**이다. 실물은 `2ce869b` 다 | 회차기록 | 참 | 미관 | 없음 | `.palimpsest/rounds/2026-09-11-effect-confirmation/state.md:192` | `git merge-base --is-ancestor 6e26be5 HEAD` → `rc=1` · `git log -1 --format=%s 6e26be5` 가 `2ce869b` 와 같은 제목 |
| 17 | 게이트 `## 효과` 의 `pal doctor` 행이 *"못 잰 것을 **못 잤다고** 적는다"* 라 적는다 — 오타이고 저장소의 「어색한 표현 부재」 검사가 못 잡는다(패턴 8 개에 없다). 내가 `pal doctor` 를 다시 돌린 값은 **잔여 4 · 빨강 1 · ok 1 · rc 0** 으로 그 행의 수 자체는 맞다 | 회차기록 | 참 | 미관 | E2 | `docs/gates/effect-confirmation.md:111` | `./target/release/pal doctor` → `잔여 1·2·3·6 · 빨강 4 · ok 5` · `rc=0` |

## 내가 기각한 것

| # | 요약 | 경로 | 모집단 | 유효성 | 해악도 |
|---|---|---|---|---|---|
| 18 | `A4` 가 ㉢ 에서 §5.8 표를 안 실었다(`grep -c "메인이 이미 오염됐다" plan/129-pre.md` = 0) → **아니다.** `plan/129-pre.md` §2.3 이 제목을 *"§5.8 표 전문"* 으로 달고 4 행 표를 바이트 그대로 싣는다. 내 grep 이 헤딩 문구만 찾았다 | `.palimpsest/rounds/2026-09-11-effect-confirmation/plan/129-pre.md` | 원의도 | 거짓 | 미관 |
| 19 | 음성 대조 판정 스크립트가 저장소에 없어 `A1-a`~`C2-a` 가 원리상 재현 불가다 → **아니다.** 인터뷰가 *"(가) 안 만든다 — 일회성으로 돌리고 수만 남긴다"* 로 잠갔고 판정 규칙 여섯 줄이 `negative-A1.md:13-20` 에 인라인돼 있으며 사본마다 PASS/FAIL 전 출력이 보존돼 있다 | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/negative-A1.md` | 원의도 | 거짓 | 미관 |
| 20 | `A2-b` 가 반증이다 — `touch/79.txt` 마커 뒤 바이트의 sha256 이 선언값과 다르다 → **아니다.** 선언값은 `--- ⟨덧붙임⟩` 앞까지의 본문 해시이고 그렇게 자르면 `1fdf18fb…` 로 정확히 일치한다. 깨진 것은 **명령 문면**뿐이고 그것은 따로 냈다(새 발견 3) | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/rerun-79.txt` | 원의도 | 거짓 | 미관 |
| 21 | `pal ledger` 가 지금 심볼 **3335** 를 내는데 게이트는 **3332** 를 싣는다 → **아니다.** 게이트가 잰 HEAD(`9e092e3`)를 명기했고 그 뒤 커밋이 심볼을 늘린 것은 정상 드리프트다. `IR1-16` 도 같은 결론이었고 나도 독립으로 같은 값에 닿았다 | `docs/gates/effect-confirmation.md:119` | 저장소 | 거짓 | 미관 |
| 22 | `B4` 가 릴리스 축에서 안 재어졌다 → **아니다.** `.rs` 전량 mtime 을 갱신한 뒤 `cargo check --workspace --all-targets` 가 3 분 13 초 전량 재컴파일로 경고 **0**, `cargo build --release --workspace` 도 **0** 이다. 내가 둘 다 돌렸다 | `crates/` `xtask/` | 원의도 | 거짓 | 미관 |

## 이 반환문이 낸 항의 총수

5 + 2 + 1 + 6 + 3 + 5 = 22

## 끝내도 되는가

**안 된다.** 본 목록에 **금지역 1**(항 8 — `A7` 이 `A5` 와 같은 자로 안 재어진 채 「통과」로
실려 있다)이 남았고, 그것은 이 회차가 `## 금지역` 에 등록한 *「사실이 아닌 것을 사실로」* 에
직접 든다. 고치는 길은 둘 중 하나이고 **둘 다 한 커밋 안에서 닫을 수 있다** — ⓐ `A7` 을
반증으로 다시 적고 무엇이 안 섰는지를 `A5` 옆에 나란히 적는다 · ⓑ `A5`·`A7` 을 **한 자**로
다시 읽어 둘 다 통과로 적고 `PM3-15` 를 어떻게 지키는지를 게이트에 적는다. **지금처럼 둘을
다르게 읽는 것만 안 된다.**

그 밖에 **실패는 0** 이다(`cargo xtask check` 28/28 · `cargo test` 1047 passed · 경고 0).
`E4`(항 9)는 원장 한 줄이 열려 있는 동안 판정 표가 앞서 간 것이고, `E3`·`E5` 와 함께
**종료 절차가 닫으면 자연히 해소된다** — 따로 라운드를 쓸 자리가 아니다.

⚠ **내 판정은 게이트와 두 자리에서 갈린다**(`A7` · `E4`). 어느 쪽이 옳은지는 내가 정하지
않는다 — 나는 문면과 실측을 댔고 판정은 게이트가 진다.
