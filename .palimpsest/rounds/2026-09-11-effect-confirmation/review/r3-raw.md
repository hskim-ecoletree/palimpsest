# 독립 리뷰 라운드 3/3 — 효과 확인

> 회차 `2026-09-11-effect-confirmation` · 잰 날 2026-09-12 · HEAD `6fadb98` · 착수 `8604d62`
> 안 본 것: `review/r1-raw.md` · `review/r2-raw.md`(과업문이 금했다). 원장의 `IR1-*`·`IR2-*`
> 요약만 중복 계수를 피하려고 읽었다.
> 전 출력을 파일로 받아 쟀다 — `cargo xtask check`(28/28) · `cargo test --workspace`(rc=0) ·
> `cargo check --workspace --all-targets`(mtime 전량 갱신 뒤 3분 14초 전량 재컴파일).

## 합격선 축

| 조건 | 판정 | 잰 수 | 근거 |
|---|---|---|---|
| A1 | 통과 | 3/3 | `git merge-base --is-ancestor` — (77dc977,88dbe13)=0 · (a74737b,14f0f92)=0 · (22cf488,9d94f34)=0 · 세 쌍 다 SHA 다름 |
| A1-b | 통과 | 3/3 | `git rev-parse <봉인>:<plan>` 이 touch 머리의 `사전등록-blob` 과 일치 — 9b0f3a5a… · e6f401a0… · 00c807cd… |
| A1-c | 통과 | 1/3 실측 | `judge-prompt.md` 판 1 의 ① 절(120 줄)을 `git cat-file blob e6f401a0…` 와 `diff` — 구분선 한 줄 빼면 차이 0. 판 2·3·4 는 안 쟀다 |
| A1-a | 통과 | 사본 4/4 | `effect/negative-A1.md` 가 네 사본의 FAIL 을 싣는다. 사본 재실행은 못 했다 ⟨미측정 목록 1⟩ |
| A2 | 통과 | 3/3 | `touch/79.txt:3-12` · `touch/126.txt:3-13` · `touch/129.txt:3-13` 머리에 명령·HEAD·심볼ID·blob |
| A2-b | 통과 | 3/3 문서 | `effect/rerun-{79,126,129}.txt` 존재. 바이트 동일 재계산은 안 했다 ⟨미측정 목록 3⟩ |
| A2-c | 통과 | 1/1 | `touch/129.txt:6-8` 이 심볼 ID 가 원리상 없음을 적고 전 출력을 보존 |
| A3 | 통과 | 3/3 | 사전 등록의 「바꿀 좌표」 절 — `plan/79-pre.md:116` 6행 · `plan/126-pre.md:77` 5행 · `plan/129-pre.md:108` 9행 |
| A3-a | 통과 | 사본 1/1 | `effect/negative-A3.md:18` 이 `FAIL A3 — 좌표절 1 · 파일:줄 0 건` |
| A4 | 통과 | 3/3 | §5.8 표 행 29·18·25 · 「읽은 것」「돌린 것」 절이 셋 다 있다 |
| A5 | 반증 | ㉡ rc=1 | `git merge-base --is-ancestor 14f0f92 8644835` → rc=1. 사전 등록 목록의 `xtask/src/main.rs` 를 `8604d62..HEAD` 에서 처음 건드린 커밋이 `8644835`(22:07)이고 touch 는 22:27 이다. ㉠·㉢ 은 rc=0 |
| A5-b | 통과 | 2/3 문서 | `observations/e1-measure.md:118-120` 이 두 앵커를 싣고 ㉢ 은 `A5-e` ⓐ 로 대조불가. 재계산 못 함 ⟨미측정 목록 2⟩ |
| A5-d | 통과 | 3/3 문서 | 같은 문서의 표 — `nodes_of` 7058bb9c→4ee8c2a6 · `check_ledger_pair` 1f184642→e18b2faa · `write` 는 변경 대상 아님 |
| A5-e | 통과 | 3/3 | 세 사전 등록의 앵커 절(`79-pre.md:141` · `126-pre.md:106` · `129-pre.md:135`)이 ⓐⓑ 를 미리 적는다 |
| A5-c | 미측정 | 0 | 회차가 미측정으로 닫았다 ⟨정반합 판 2⟩. 나도 새로 안 세웠다 |
| A5-a | 통과 | 사본 2/2 | `effect/negative-A5.md:15-16`(축 ⓐ FAIL) · `:36`(축 ⓑ body 갈림). 사본 재실행 못 함 ⟨미측정 목록 1⟩ |
| A6 | 통과 | 3/3 | 사전 등록 §1 이 심볼을 먼저 적고 보존본은 심볼마다 하나(`touch/` 에 파일 셋) |
| A7 | 반증 | ㉡ rc=1 | `git merge-base --is-ancestor 3637ea9 8644835` → rc=1. ㉠(f60df1d→9f993cc)·㉢(138c782→1abc0e9) 은 rc=0 |
| A7-a | 통과 | 사본 2/2 | `effect/negative-A7.md:17`·`:28` 이 두 축의 FAIL 을 싣는다. ⚠ 그 대조가 덮는 축은 아래 「새 발견 1」이 진다 |
| B1 | 통과 | 3/3 | ⟨정반합⟩ 판정을 안 뒤집는다. `#79`·`#126`·`#129` 가 `gh issue view` 에서 셋 다 CLOSED |
| B1-a | 통과 | 1/1 | `observations/red-129.txt` 가 `rc=1` 과 `Error: 개체를 남기지 못했다` 를 싣고 `intent.redb` 1351680 바이트를 함께 적는다 |
| B1-c | 통과 | 3/3 | `crates/pal-cli/tests/narrative_read_only.rs` 신설 · `xtask/src/main.rs:6456` 시험 모듈 · `ledger.rs` 시험. `cargo test --workspace` rc=0 |
| B1-d | 통과 | 1/1 | 내가 돌렸다 — `./target/release/pal query narrative.unbound` rc=0 · 새 줄 `이름이 아직 없어 뺀 조각` 이 산출된다 |
| B1-e | 통과 | 3/3 | 사전 등록의 「시험과 그것이 재는 RED」 절이 셋 다 있다 |
| B2 | 통과 | 28/28 | 내가 돌렸다 — 전 출력 보존 · `검사 28/28 통과` · rc=0 · 하한 28 충족 |
| B3 | 통과 | 1047 | 내가 돌렸다 — `^test result:` 49 줄의 `passed` 합 1047 ≥ 1028 · `FAILED`·`panicked`·`error[E` 0건 |
| B4 | 통과 | 경고 0 | `.rs` 전량 mtime 갱신 후 `cargo check --workspace --all-targets` 가 3분 14초 전량 재컴파일로 경고 0. 릴리스 축은 안 쟀다 ⟨미측정 목록 5⟩ |
| B5 | 통과 | 3335 | 내가 돌렸다 — `pal ledger` 가 `순서에 취약 20 · 등급이 낮음 3309 · 정확 6 · 불가 0 · 합 3335` 를 명령으로 낸다 |
| B5-a | 통과 | 1/1 | `plan/79-pre.md:94` 「모집단 — 분모가 무엇인가」가 봉인 안에 있다 |
| B6 | 통과 | 1/1 | `plan/126-pre.md:59` 가 커밋 시각 갈래를 고르고 `xtask/src/main.rs:6427-6450` 이 `None` 처분을 코드에, `:6456-6500` 이 시험을 싣는다 |
| C1 | 반증 | 3/13 | 세 delta 의 차이 행 4+6+3=13 인데 원인 끝이 touch 좌표인 행은 3(`126-delta` 2 · `79-delta` 1). 소유자 칸 3 이 모집단을 「전부」로 넓혔다 |
| C2 | 미측정 | 0 | 소유자 칸 2 가 미측정으로 닫았다. ⟨정반합⟩ 이라 혼자 안 세운다 |
| C2-b | 미측정 | 0 | 같음 |
| C2-a | 통과 | 사본 2/2 | `effect/negative-C2.md` + `judge-roster.md` 2 행(미끼만 준 판에서 유효 0) |
| C3 | 반증 | 판 4/5 | `grep '보존본 · 판' effect/judge-prompt.md` 가 판 1·2·3·4 만 낸다. `judge-roster.md` 5 행과 `129-judge-2.md:5` 가 「판 5」를 부르는데 그 판이 없다 |
| C3-a | 통과 | 5/5 | `judge-roster.md` 가 다섯을 전수로 싣고 버린 반환문이 없다고 적는다 |
| C3-b | 통과 | 5=5 | `effect/*-judge-*.md` 파일 5개 = roster 5행 |
| C4 | 반증 | 8/13 | 게이트 `:310-323` 의 마지막 칸을 세었다 — 「해당 없음」 10 중 근거 절 없는 맨 것이 8 |
| C5 | 통과 | 3/3 | `effect/{79,126,129}-delta.md` 셋 다 있고 ㉢ 이 「차이 0」을 그대로 적는다 |
| D1 | 통과 | 2/2 | 게이트 `:125-128` 이 두 전제의 지금 값과 잰 명령을 싣고 `gh issue view 66` 의 댓글이 같은 내용을 싣는다 |
| D2 | 통과 | 1/1 | ⟨정반합⟩ · 소유자 칸 15 의 규범형 문장. 안 뒤집는다 |
| D3 | 통과 | 2/2 | 게이트 `:219-220` 이 갈래마다 명령과 값을 댄다 |
| D4 | 통과 | 1/1 | `gh issue view 66` → CLOSED |
| D4-a | 통과 | 5/5 | 게이트 `:285-290` 이 다섯 자리의 처분을 따로 적고 `scripts/f12-verify.py:557-570` 이 실제로 고쳐졌다(`python3 -m py_compile` OK) |
| E1 | 통과 | 50=50 | 게이트 판정 표의 ID 집합과 `intent.md` 체크박스 50 개가 **집합 같음**(차집합 양쪽 0) · 40+5+0+5=50 · 절 이름 넷뿐 |
| E2 | 통과 | 4행 | 게이트 `## 효과` 가 사람이 셸에서 부른 `pal` 산출 넷을 싣고 방향 13/13 |
| E3 | 미측정 | 0 | `report.md` 가 아직 없다. ⟨정반합⟩ 이라 혼자 안 세운다 |
| **E4** | **반증** | 열림 3 | 내가 쟀다 — `findings.jsonl` 의 `상태=열림` 이 **3**(`IR1-05`·`IR2-07`·`IR2-09`). 게이트는 통과로 싣는다. 판정이 선 `39b0fa3` 에서는 참이었다 |
| E5 | 미측정 | 0 | 회차의 마지막 커밋이 아직 없다 |
| E6 | 통과 | 2/2 | `docs/plan/03-shortest-path.md:130` 3단계 행과 `docs/plan/02-order.md:259` 가 둘 다 이 회차의 결과와 `C2` 미측정을 싣는다 |

**내 검산** — 통과 39 · 반증 6 · 대조불가 0 · 미측정 5 = 50.
게이트와 갈리는 자리는 **`E4` 하나**(게이트 통과 ↔ 내 반증)다.

## 미측정 목록

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | 음성 대조 다섯의 격리 사본 아홉을 내가 재실행하지 못했다 — 판정을 낸 `judge.sh` 가 저장소에 없다 | 원의도 | 참 | 미관 | A1-a·A3-a·A5-a·A7-a·C2-a | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/negative-A1.md:12` | `find . -name judge.sh` 빈 출력. 인터뷰가 *"(가) 안 만든다 — 일회성으로 돌리고 수만 남긴다"* 로 잠갔다 |
| 2 | `A5-b`·`A5-d` 의 앵커를 내가 다시 계산하지 않았다 — 봉인 시점에 `pal touch --at` 을 다시 안 돌렸다 | 원의도 | 참 | 미관 | A5-b·A5-d | `.palimpsest/rounds/2026-09-11-effect-confirmation/observations/e1-measure.md:118` | 문서의 값만 대조했다 |
| 3 | `A2-b` 의 재실행 바이트 동일을 내가 재계산하지 않았다 | 원의도 | 참 | 미관 | A2-b | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/rerun-79.txt:1` | 파일 존재만 확인했다 |
| 4 | `B1-a` 의 RED 를 내가 못 쟀다 — 이미 고쳐져 `pal query narrative.unbound` 가 rc=0 이다 | 원의도 | 참 | 미관 | B1-a | `.palimpsest/rounds/2026-09-11-effect-confirmation/observations/red-129.txt:1` | 내가 돌린 값이 rc=0. 원리상 재현 불가 |
| 5 | `B4` 의 릴리스 축을 내가 안 돌렸다 — `cargo check` 축만 쟀다 | 원의도 | 참 | 미관 | B4 | `crates/` | `cargo check --workspace --all-targets` 3분 14초 전량 재컴파일 · 경고 0. `cargo build --release --workspace` 는 안 돌렸다 |
| 6 | `A5-c`·`C2`·`C2-b`·`E3` 는 ⟨정반합⟩ 이거나 회차가 미측정으로 닫은 것이라 내가 판정을 새로 안 세웠다 | 원의도 | 참 | 미관 | A5-c·C2·C2-b·E3 | `docs/gates/effect-confirmation.md:80` | 과업문이 *"⟨정반합⟩ 조건은 혼자 판정하지 않는다"* 로 못 박았다 |
| 7 | `E5` 를 못 쟀다 — 회차의 마지막 커밋이 아직 없다 | 원의도 | 참 | 미관 | E5 | `docs/gates/effect-confirmation.md:85` | `gh run list` 를 댈 대상 커밋이 없다 |

## 의도 축

### 빠진 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 8 | ⚠ **재발** — 종료 보고 `report.md` 가 아직 없어 네 이름(의도적으로 안 한 것 · 확인 못 한 것 · 추론 · 넘기는 것)이 하나도 안 섰다. 원장 `IR1-05`·`IR2-07` 이 같은 것을 냈고 둘 다 아직 `상태=열림` 이다 | 원의도 | 참 | 거짓신호 | E3 | `.palimpsest/rounds/2026-09-11-effect-confirmation/` | `ls` 에 `report.md` 없음 · `python3` 로 원장을 읽어 열린 셋을 확인 |
| 9 | 원 의도가 잠근 주장 범위(*"「이 저장소에서 섬」까지만 주장하고 R-19 를 범위 밖에 적는다"*)가 **밖으로 나가는 문서에 안 실렸다.** `03-shortest-path.md` §4 가 3 단계의 효과 결과를 세 방향으로 싣는데 자기 저장소 자기적용 편향도 「사전 등록을 쓰는 손과 변경을 쓰는 손이 같다」도 한 글자가 없다 | 원의도 | 참 | 거짓신호 | E6 | `docs/plan/03-shortest-path.md:141` | `grep -n 'R-19\|자기 저장소\|같은 저장소\|편향' docs/plan/03-shortest-path.md` → 소유자 규범 문장 두 줄만 적중, 편향 0 건 |
| 10 | 게이트가 「원 의도의 합격선이 실제로 기대는 것」을 적으면서 **그 셋 중 둘이 `A5`·`A7` 이 반증된 자리(㉡)에 산다는 것**을 안 적는다. ㉡㉰·㉡㉱ 의 순서 조건이 문면대로는 안 섰고, 코드 흔적이 남은 하나(㉠㉱)만 순서 조건이 통과다 | 원의도 | 참 | 거짓신호 | A5·A7·C2 | `docs/gates/effect-confirmation.md:528` | `effect/126-delta.md:26`·`:27` 이 ㉡ 소속 · `git merge-base --is-ancestor 14f0f92 8644835` rc=1 · `3637ea9 8644835` rc=1 |

### 요구되지 않은 것

없음 — `git log --diff-filter=A 8604d62..HEAD --name-only` 가 회차 디렉터리 밖에 낸 새 파일은
`crates/pal-cli/tests/narrative_read_only.rs` 와 `docs/gates/effect-confirmation.md` 둘뿐이고,
고친 코드 열넷은 전부 재는 자리 셋이나 원장에 실린 발견으로 되짚힌다. 인터뷰가 잠근
*"장치를 안 만든다"* 도 지켜졌다.

### 있는데 틀린 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 11 | ⚠ **재발** — 게이트가 `E4` 를 **통과**로 싣는데 지금 원장의 열린 발견이 **3** 이다(`IR1-05`·`IR2-07`·`IR2-09`). 판정이 선 `39b0fa3` 에서는 참이었고 그 뒤 두 라운드가 발견을 실으면서 거짓이 됐다. 판정 표에 **잰 커밋이 안 적혀** 있어 읽는 사람이 그 구별을 못 한다 | 원의도 | 참 | 거짓신호 | E4 | `docs/gates/effect-confirmation.md:31` | `python3` 로 `findings.jsonl` 을 읽어 `상태` 계수 — 닫힘 324 · 열림 3 · 머리 1. `cargo xtask check` 의 「발견이 닫혔나」도 `열림 3행` 을 찍는다 |

## 이번 라운드의 새 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 12 | ★★ **`A7-a` 의 음성 대조가 `A7` 이 실제로 깨진 축을 안 덮는데 그 사실이 어디에도 안 적혔다.** 판정 스크립트가 `<CHANGE>` 를 **인자로 받고** 사본마다 변경 커밋이 하나뿐이라, *"어느 커밋을 첫 변경 커밋으로 뽑나"* 라는 축은 두 사본에서 **원리상 안 발화한다** — `A7` 을 반증시킨 바로 그 축이다. 게이트는 같은 사각을 `A5-a` 에 대해서만 ⚠ 로 달았고(`:62-66`), 자기가 *"한 조건을 뒤집으면 그 조건의 자를 인용하는 조건을 전수로 다시 재야 한다"* 라 적어 놓고 **그 규칙을 음성 대조에는 안 돌렸다**. 그대로 두면 「A7 의 경계는 대조로 검증됐다」가 거짓으로 선다 | 자기장치 | 참 | 금지역 | A7-a | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/negative-A7.md:44` | `negative-A1.md:14-19` 의 규칙 블록이 `A5`·`A7` 을 둘 다 `$CHANGE` 로 잰다 · `negative-A7.md:48-59` 가 사본마다 `change=` 를 하나씩만 싣는다 · 게이트 `:62-66` 은 `A5-a` 만 적고 `grep -n 'A7-a' docs/gates/effect-confirmation.md` 는 판정 표 한 줄뿐이다 |

## 자기 산출에 대한 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 13 | `E1` 의 실측 원본이 뒤집힌 값을 정오표 없이 그대로 싣는다 — `observations/e1-measure.md:140` 이 *"제안: `A5` 통과"*, `:154` 가 *"제안: `A7` 통과"*, `:451` 이 *"통과 33 · 반증 1 = 34"* 이고, `:13` 의 좌표 표가 ㉡ 의 첫 변경 커밋을 **`398d233`** 으로 적는다. 그 파일은 `c402d15` 이후 한 번도 안 고쳐졌고 게이트에서 되짚는 링크도 없다. 회차 자신이 칸 11 에서 *"정정문을 옆에 붙이고 원문은 안 고친다"* 를 규칙으로 세웠는데 그 정정문이 이 파일 옆에 없다 | 회차기록 | 참 | 거짓신호 | A5·A7·E1 | `.palimpsest/rounds/2026-09-11-effect-confirmation/observations/e1-measure.md:140` | `git log -- observations/e1-measure.md` 가 `c402d15` 한 줄 · `grep -rn 'e1-measure'` 가 회차 안 다른 문서 한 곳뿐 |
| 14 | ⚠ **재발(3 회째)** — `state.md` 의 「지금 수」 표가 또 낡았다: *"원장 283 행"*(지금 327) · *"열린 발견 0 ⟨`E4` 통과⟩"*(지금 3) · 이슈 행이 `#140`·`#141` 만 적고 **`#142` 가 없다**(`d44e8dc` 가 세웠고 게이트는 싣는다). 원장 `IR1-12`·`IR2-15` 가 같은 표를 이미 두 번 잡았다 | 회차기록 | 참 | 거짓신호 | 없음 | `.palimpsest/rounds/2026-09-11-effect-confirmation/state.md:145` | `wc -l findings.jsonl` 328(머리 1 + 레코드 327) · 열림 3 · `gh issue list` 가 `#142` OPEN |
| 15 | 게이트와 이슈 `#140` 이 **고칠 문면의 수를 다르게 적는다** — 게이트 `:560` 은 *"조건 문면 **넷**"* 이고 이슈 본문의 서두는 *"그 뿌리가 **조건 문면 셋**"*, `## 닫는 조건` 첫 줄도 *"**세 문면**이 고쳐지고"* 다. 넷째(`DL1-25` · 판정자가 `N` 을 검증 안 한 것)는 본문 ④ 에만 있고 닫는 조건의 수에는 안 들어갔다 — 다음 회차가 닫는 조건을 자로 쓰면 넷째가 조용히 빠진다 | 회차기록 | 참 | 거짓신호 | 없음 | `docs/gates/effect-confirmation.md:560` | `gh issue view 140 --json body` 전문 — 서두 *"조건 문면 셋"* · `## 닫는 조건` *"세 문면이 고쳐지고"* · 본문 항은 ①②③④ 넷 |

## 내가 기각한 것

| # | 요약 | 경로 | 모집단 | 유효성 | 해악도 |
|---|---|---|---|---|---|
| 16 | ㉢ 의 판정자가 「유효 1」을 냈는데 delta 는 「차이 0」이라 두 산출이 모순이다 → 아니다. `129-judge-2.md:57-60` 과 게이트가 *"「유효 1」은 「귀속된 차이 1」이 아니다"* 를 스스로 갈라 적는다. `C2` 가 묻는 것은 **차이 중** 귀속이고 ㉢ 의 차이 셋은 전부 컴파일러·검사가 원인이다 | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/129-judge-2.md:57` | 원의도 | 거짓 | 미관 |
| 17 | 게이트 `## 효과` 의 「이 회차의 산출이 실제로 돌았다」 표 넷 중 `pal doctor` 행이 이 회차 산출을 안 지난다 → 아니다. `crates/pal-cli/src/doctor.rs:136` 이 `ledger::compute` 를 부르고 그 경로에 이 회차가 세운 `IdentityTally` 가 있다. `pal symbols` 행은 자기 칸에 *"`#66` 전제 ② 를 재는 자리"* 라 적어 용도를 밝힌다 | `crates/pal-cli/src/doctor.rs:136` | 원의도 | 거짓 | 미관 |
| 18 | 게이트가 싣는 수가 지금 값과 다르다(심볼 3332↔3335 · 결박 불가 812↔849 파일 · 뺀 조각 4733↔226) → 아니다. 잰 HEAD `9e092e3` 를 `## 효과` 머리가 명기했고 그 뒤 커밋이 값을 움직인 정상 드리프트다. 원장 `IR1-16`·`IR2-21` 이 같은 형태를 이미 기각했다 | `docs/gates/effect-confirmation.md:110` | 저장소 | 거짓 | 미관 |
| 19 | `D4-a` 가 고친 `scripts/f12-verify.py` 를 어느 검사도 안 돌리므로 깨진 채 실릴 수 있다 → 아니다. `python3 -m py_compile scripts/f12-verify.py` 가 OK 를 내고 바뀐 것은 f-string 문면뿐이다. 「검사가 안 돌린다」는 사실 자체는 게이트가 이미 적는다 | `scripts/f12-verify.py:557` | 저장소 | 거짓 | 미관 |
| 20 | `C3-b` 의 *"`judge-roster.md` 의 수와 파일 수가 같다"* 가 안 맞는다(㉠ 은 판정 파일이 하나뿐이다) → 아니다. roster 5 행 ↔ `effect/*-judge-*.md` 5 개로 같다. ㉠ 이 하나인 것은 띄운 인스턴스가 하나라서다 | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/judge-roster.md:7` | 원의도 | 거짓 | 미관 |
| 21 | 이 회차가 코드 좌표에 결박을 안 남겨 결정이 문서에만 산다 → 아니다. 소유자 칸 1 의 셈법이 `crates/pal-cli/src/ledger.rs:336-338` 의 doc 주석에 `intent.md` `## 승격` 칸 1 인용과 함께 박혔고, `xtask/src/main.rs:6427-6450` 과 `crates/pal-git/src/lib.rs:196-206` 도 발견 ID 를 좌표에 싣는다 | `crates/pal-cli/src/ledger.rs:336` | 저장소 | 거짓 | 미관 |

## 이 반환문이 낸 항의 총수

7 + 3 + 0 + 1 + 1 + 3 + 6 = 21

## 끝내도 되는가

**메인이 정한다 — 재료를 적는다.**

본 목록에 남은 것: **금지역 1**(새 발견 12 · `A7-a` 의 음성 대조가 `A7` 이 깨진 축을 안 덮는다) ·
**실패 0** · **거짓신호 4**(항 8·9·10·11). 자기 산출 셋은 전부 거짓신호다.

★ **상한의 마지막 라운드다.** 이 라운드가 낸 금지역 하나는 **문서 한 문단으로 닫히는
종류**다 — 사본을 다시 돌리라는 것이 아니라, 게이트가 `A5-a` 에 대해 이미 적은 ⚠ 를
`A7-a` 에도 적고 그 사각을 `#140` 계열로 넘기면 된다. 되돌릴 수 없는 것이 아니다.

★ **`E4` 는 종료 절차가 자기를 닫는 자리다** — 열린 셋 중 둘(`IR1-05`·`IR2-07`)은
`report.md` 가 서면 닫히고 `IR2-09` 는 그 뒤 `E4` 를 다시 재면 닫힌다. 다만 **게이트가
`E4` 를 통과로 싣는 채로 회차가 끝나면 그 줄은 거짓으로 굳는다** — 판정 표에 잰 커밋을
적거나 마지막에 다시 재는 것 중 하나는 있어야 한다.

⚠ **내가 못 판 자리를 다시 적는다** — 음성 대조 아홉 사본과 앵커 재계산은 못 돌렸다
(미측정 목록 1·2·3). 그래서 「대조가 정말 빨개지나」는 이 라운드가 **문서로만** 봤다.
