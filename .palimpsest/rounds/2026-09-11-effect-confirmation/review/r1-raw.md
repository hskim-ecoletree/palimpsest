# 독립 리뷰 라운드 1 — 원문

> 회차 `2026-09-11-effect-confirmation` · 잰 날 2026-09-12 · HEAD `39b0fa3`
> **읽기 전용이다.** 이 파일 하나만 쓴다.

돌린 것 — `cargo xtask check`(28/28 · rc=0) · `cargo test --workspace`(`^test result:` 49 줄 ·
`passed` 합 **1047** · rc=0) · `.rs` 전량 mtime 갱신 후 `cargo check --workspace --all-targets`
(경고 **0**) 와 `cargo build --release --workspace`(경고 **0**) · `git merge-base --is-ancestor`
계열 · `git log --reverse --format=%H 8604d62..HEAD -- <파일>` · `git cat-file blob` 바이트 대조 ·
`./target/release/pal ledger` · `gh issue view` · `findings.jsonl` 전수 파싱.

---

## 합격선 축

「잰 것」은 **내가 이번 라운드에 직접 돌려 확인한 것**이다. 「문면」은 회차 산출물의 기록을
읽고 좌표만 확인한 것이다.

| 조건 | 판정 | 잰 것 | 근거 |
|---|---|---|---|
| `A1` | 통과 | 3/3 | `merge-base --is-ancestor`(77dc977→88dbe13 · a74737b→14f0f92 · 22cf488→9d94f34) 전부 rc=0 · SHA 다름 |
| `A1-b` | 통과 | 3/3 | `git rev-parse <봉인>:<plan>` = `9b0f3a5a…`·`e6f401a0…`·`00c807cd…` 가 `touch/*.txt` 머리의 `사전등록-blob` 과 같다. 지금 워킹트리 blob 도 같다 |
| `A1-c` | 통과 | 4/4 | `judge-prompt.md` 의 `## ①` 절 네 개를 구분선 사이에서 떼어 봉인 blob 과 **바이트로** 비교 — 네 판 전부 exact match |
| `A1-a` | 통과 | 문면 | `effect/negative-A1.md` 가 사본 넷(neg1~neg4)에서 `A1`·`A1-b`·`A1-c` 가 각각 뒤집힌 전 출력을 싣는다. 사본을 재실행하지는 않았다 |
| `A2` | 통과 | 3/3 | 세 `touch/*.txt` 머리에 명령·HEAD·심볼 ID·blob 이 있다(㉢ 은 `A2-c` 갈래) |
| `A2-b` | 통과 | 문면 | `effect/rerun-{79,126,129}.txt` 셋이 마커 뒤 본문의 바이트 동일을 적는다 |
| `A2-c` | 통과 | 좌표 | `touch/129.txt:6-8` 이 「심볼 ID 원리상 없다」와 근거를 적는다 |
| `A3` | 통과 | 3/3 | `plan/126-pre.md` §4 · `79-pre.md` §5 · `129-pre.md` §4 가 각각 `파일:줄` 표를 싣는다 |
| `A3-a` | 통과 | 문면 | `effect/negative-A3.md` — 좌표 줄 열하나를 지운 `neg8` 에서 `FAIL A3` |
| `A4` | 통과 | 3/3 | 세 사전 등록 전부에 §5.8 표 4 행이 전문으로 들어 있다(`observations/red.md` §5.8 과 대조) |
| **`A5`** | **반증** | 3/3 | 아래 **발견 1**. 조건 문면의 사상 규칙(*"그 목록의 파일을 건드린 첫 커밋"*)을 그대로 적용하면 ㉡ 의 첫 커밋은 `8644835`(xtask/src/main.rs)이고 `merge-base --is-ancestor 14f0f92 8644835` = **rc=1** 이다. 실측은 `git log <touch>..HEAD` 로 범위를 좁혀 쟀는데 그 범위에서는 조상 관계가 **구성상 언제나 참**이다 |
| `A5-b` | 통과 | 문면 | `observations/e1-measure.md` 가 `--at 77dc977`·`--at a74737b` 로 `body 7058bb9c2fcf`·`1f1846428f55` 를 재현 |
| `A5-d` | 통과 | 문면 | 같은 곳 — 봉인 시점 ↔ 지금이 둘 다 움직였다. ㉢ 은 대조불가로 적혔다 |
| `A5-e` | 통과 | 좌표 | 세 사전 등록의 앵커 절이 ⓐⓑ 를 미리 적는다 |
| `A5-c` | **갈린다** | 부분 | `observations/a5c-usage-census.md`(93 줄 전수 · 갈래 여섯)가 **실재**하고 게이트 `:460-465` 가 *"더러운 트리에서도 「일치」를 찍는다"* 를 적는다. 그러나 조건이 요구한 **팔 ③**(*"`--at` 없이 부른 **모든** 산출에서 구조적으로 참"* 의 코드 독해 논증)은 서지 않았다. ⟨정반합⟩ 이라 판정은 정반합이 한다 |
| `A5-a` | 통과 | 문면 | `effect/negative-A5.md` — ⓐ `neg5` 에서 `FAIL A5` · ⓑ `neg9` 에서 `body` 가 `7058bb…`→`305e59…`. ⚠ **대조가 실측 절차를 안 덮는다** — 아래 발견 4 |
| `A6` | 통과 | 좌표 | 세 자리 각각 보존본 하나 |
| `A7` | 통과 | 6/6 | `merge-base` 여섯(touch→readnote · readnote→변경) 전부 rc=0 · SHA 다름 |
| `A7-a` | 통과 | 문면 | `effect/negative-A7.md` — `neg6`·`neg7` 두 경계가 각각 `FAIL A7` |
| `B1` | 갈린다 | 문면 | 세 자리에 코드 변경 커밋이 있다(`398d233`·`9f993cc`·`1abc0e9`). *"이슈 본문이 요구한 것을 만족한다"* 는 ⟨정반합⟩ 이고 `CA2-15` 가 스스로 *"기계가 못 판정한다"* 를 적는다. ⚠ 셋 다 **아직 OPEN** 이다(발견 5) |
| `B1-a` | 통과 | 좌표 | `observations/red-129.txt` — `rc=1` · `intent.redb` 1351680 바이트 존재 |
| `B1-c` | 통과 | 좌표 | `xtask/src/main.rs:6456-6551` 시험 넷 · `crates/pal-cli/tests/narrative_read_only.rs` · `ledger.rs` 시험 셋 |
| `B1-d` | 통과 | 문면 | `effect/green-129.txt` · `e2-run.txt` 가 `rc=0` |
| `B1-e` | 통과 | 좌표 | 세 사전 등록 §5 가 시험 이름 ↔ RED 를 짝지운다 |
| `B2` | 통과 | **잼** | `cargo xtask check` → `검사 28/28 통과` · rc=0. 하한 28 충족 |
| `B3` | 통과 | **잼** | `cargo test --workspace` rc=0 · `^test result:` **49 줄** · `passed` 합 **1047** ≥ 1028 |
| `B4` | 통과 | **잼** | mtime 전량 갱신 후 `cargo check --workspace --all-targets` 경고 **0** · `cargo build --release --workspace` 경고 **0** |
| `B5` | 통과 | **잼** | `./target/release/pal ledger` 가 `정체성 심볼 3335 / 순서에 취약 20 · 등급이 낮음 3309 · 정확 6 · 불가 0 · 검산 …= 3335` 를 **명령으로** 낸다 |
| `B5-a` | 통과 | 좌표 | `plan/79-pre.md` §4 가 분모를 봉인 전에 적었다 |
| `B6` | 통과 | 좌표 | `plan/126-pre.md` §3 이 커밋 시각 갈래를 고르고 「커밋 0」 처분을 적었다. 코드 `xtask/src/main.rs:6427-6449` · 판정문 `:6356` · 시험 `:6525-6551` |
| `C1` | 반증 | 13/13 | 세 delta 의 차이 13 행 중 원인 끝이 `touch/…:N` 인 것은 **3**(126㉰`:17` · 126㉱`:24` · 79㉱`:23`). 게이트가 적은 대로다 |
| `C2` | 미측정 | — | ⟨정반합⟩ · 소유자 칸 2 |
| `C2-b` | 미측정 | — | ⟨정반합⟩ · 소유자 칸 2 |
| `C2-a` | 통과 | 문면 | `effect/negative-C2.md` 두 축 — ⓐ 미끼 ⓓⓔ 를 무효로 잡고 진짜 셋은 통과 · ⓑ 미끼만 준 사본에서 **유효 0** |
| `C3` | 반증 | **잼** | `judge-prompt.md` 의 `## ①` 절이 **넷**(줄 52·482·1154·1571)이고 `judge-roster.md` 5 행이 *"판 5"* 를 부른다 — 그 판이 파일에 없다 |
| `C3-a` | 통과 | 좌표 | `judge-roster.md` 5 행 · 반환문 버린 것 0 을 명기 |
| `C3-b` | 통과 | **잼** | `effect/*-judge-*.md` **5 개** = 명단 5 행 |
| `C4` | 반증 | 13/13 | 게이트 `## 효과` 표의 「참이었나」 칸 중 맨 「해당 없음」 **8** · 절이 붙은 것 **5**. 손으로 세어 게이트 검산(5/8)과 같았다 |
| `C5` | 통과 | 3/3 | ㉠ 6 · ㉡ 4 · ㉢ 3 이 전부 delta 로 섰고 ㉢ 은 0 을 0 으로 적었다 |
| `D1` | 통과 | 부분 | 게이트 `:93-96` 이 정정문과 잰 명령을 싣는다. `pal ledger` 를 다시 돌리니 지금 값은 **심볼 3335 · parsed 142** 로 드리프트했다(게이트는 3332) |
| `D2` | 통과 | 문면 | ⟨정반합⟩ · 소유자 칸 15 의 규범형 문장. 갈리는 자리 없음 |
| `D3` | 통과 | **잼** | `pal ledger` 가 ⓐ 에 값(`Rust L1 141 파일`)을 내고 ⓑ 에 `저장소 선언 1 · 본 것 1` 을 낸다 |
| `D4` | 통과 | **잼** | `gh issue view 66` → `state=CLOSED` · `closedAt=2026-09-12T06:05:04Z` |
| `D4-a` | 통과 | **잼** | 다섯 자리 실재 · `scripts/f12-verify.py:556-570` 이 실제로 고쳐졌다(`2d47da6`) |
| `E1` | 통과 | **잼** | 게이트 `## 판정` 42+3+0+5=**50** · `intent.md` 상자 **50** · 두 ID 집합 `diff` 가 **빈 출력**(집합 같음) · 중복 0. `intent.md` 의 상자 주석 판정값도 게이트 칸과 50/50 일치 |
| `E2` | 통과 | 문면 | `effect/e2-run.txt` — 사람이 셸에서 `pal` 넷을 직접 부른 전 출력 · 종료값 포함. ⚠ 한 명령의 전 출력은 안 보존됐다(발견 6) |
| `E3` | 미측정 | **잼** | `report.md` 가 회차 디렉터리에 **없다**(`ls`) |
| `E4` | 통과 | **잼** | `findings.jsonl` 284 행 = 헤더 1 + 발견 **283** · `상태` 전부 `닫힘` · `닫은커밋` 없는 닫힘 **0** · 닫은커밋 SHA 전부 실존(`git cat-file -e`) |
| `E5` | 미측정 | **잼** | `gh run list --commit 39b0fa3` 이 **빈 출력** |
| `E6` | 통과 | 좌표 | `docs/plan/03-shortest-path.md:139`·`:147-163` · `docs/plan/02-order.md:252-259` 가 갱신됐다. ⚠ **그 내용이 틀렸다** — 발견 2 |

### 요약

- 통과 **41** · 반증 **4**(`A5` 추가) · 대조불가 0 · 미측정 **3**(`C2`·`C2-b`·`E3`·`E5` 중
  `E3`·`E5`·`C2`·`C2-b` = 4) · 갈린다 **2**(`A5-c`·`B1`)
  → 정확히: 통과 41 · 반증 4 · 미측정 4(`C2`·`C2-b`·`E3`·`E5`) · 갈린다 2 = **51**? 아니다 —
  `A5-c` 가 게이트의 미측정 다섯 중 하나이므로 **통과 41 · 반증 4 · 미측정 4 · 갈린다 2 = 50** 이다.
- 게이트와 **갈린 자리는 하나** — `A5`(게이트 통과 ↔ 내 판정 반증).
- **「미측정을 통과로 적은 자리」는 없다.** 게이트의 미측정 다섯은 다섯 다 실제로 안 재어졌다.

### 음성 대조 다섯의 자기 항등식 검사

다섯 다 *"이 검사가 실패할 수 있는 길"* 을 실제로 보였다 — `negative-A1.md` 는 인자를 잘못 준
첫 실행에서 여섯이 전부 `FAIL` 이 났다는 것까지 적고, `neg2`·`neg8` 에서는 노린 축 밖의 조건도
함께 빨개졌으며, `neg6`·`neg7` 에서는 **노린 축 하나만** 빨개졌다. 항등식이 아니다.
⚠ 다만 `A5` 축의 대조는 **실측이 쓴 절차를 안 덮는다** — 발견 4.

---

## 미측정 목록

**이번 라운드에 내가 직접 안 잰 것**이다 — 회차 산출물의 기록을 읽고 좌표만 확인했다.

| # | 안 잰 조건 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 못 쟀나 |
|---|---|---|---|---|---|---|
| 1 | `A1-a` `A3-a` `A5-a` `A7-a` `C2-a` — 격리 사본 아홉을 **재실행 안 했다** | 원의도 | 참 | 미관 | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/negative-A1.md` | 사본이 임시 클론이라 지금 없다. 기록된 전 출력만 읽었다 |
| 2 | `A5-b` `A5-d` — `pal touch --at <봉인>` 을 다시 안 돌렸다 | 원의도 | 참 | 미관 | `.palimpsest/rounds/2026-09-11-effect-confirmation/observations/e1-measure.md` | 스크래치 인덱스가 필요해 저장소 상태를 건드릴 위험이 있었다 |
| 3 | `A2-b` — `rerun-*.txt` 의 바이트 동일을 재계산 안 했다 | 원의도 | 참 | 미관 | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/rerun-126.txt` | 마커 분리 규칙이 파일 안에만 있어 재현 비용이 컸다 |
| 4 | `B1` `A5-c` `C2` `C2-b` `C4` `D2` `E3` — ⟨정반합⟩ 조건의 최종 판정 | 원의도 | 참 | 미관 | `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:196` | 규약상 독립 리뷰 혼자 안 판정한다 |

---

## 의도 축

### 빠진 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 5 | ⟨본문 「발견 7」⟩ 종료 보고가 없어 네 이름(안 한 것·확인 못 한 것·추론·넘기는 것)이 하나도 안 섰다 — 이 회차가 낸 잔여 다섯이 한 자리에 안 모인다 | 원의도 | 참 | 거짓신호 | `E3` | `.palimpsest/rounds/2026-09-11-effect-confirmation/` (report.md 부재) | `ls …/report.md` → `No such file or directory` |
| 6 | ⟨본문 「발견 6」⟩ `E2` 의 가장 큰 근거가 사후 복원 불가다 — `pal query narrative.unbound` 전 출력 466960 바이트 중 머리 40 줄만 보존되고 나머지는 sha256 만 있다. 그 해시를 댈 대상이 저장소에 없다 | 원의도 | 참 | 거짓신호 | `E2` | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/e2-run.txt:7-13` | 파일 본문이 `…⟨8510 줄 줄임⟩` 에서 끊긴다 |

**아래는 같은 둘의 근거 전문이다 — 표와 본문이 같은 항이고 수는 둘이다.**

#### 발견 7 — 종료 보고가 없어 네 이름이 하나도 안 섰다

`report.md` 가 회차 디렉터리에 없다. 그러므로 **의도적으로 안 한 것 · 확인 못 한 것 ·
추론(확인 아님) · 다음으로 넘기는 것** 네 이름 중 어느 것도 저장소에 안 섰다.
`E3` 이 그 자리를 미측정으로 잡고 있으므로 은폐는 아니지만, 이 회차가 낸 **잔여**(백틱 참조
사각 · `#140` 네 항 · `A5-c` 팔 ③ · 세 이슈 미종결 · `C4` 의 여덟 셀)를 한 자리에 모으는
문서가 없다. 흩어진 채로 넘어가면 다음 회차가 전수로 못 받는다.

- 모집단: 원의도
- 유효성: 참 — `ls .palimpsest/rounds/2026-09-11-effect-confirmation/report.md` 가 `No such file`
- 해악도: 거짓신호
- 어디가 걸리나: `E3`(`intent.md:260`) · 원문의 *"어디서 멈추고 올리나"*

#### 발견 6 — `E2` 의 가장 큰 근거가 사후 복원 불가다

`effect/e2-run.txt:7-13` 이 스스로 적는다 — `pal query narrative.unbound` 의 전 출력은
**8550 줄 · 466960 바이트**인데 파일에는 **머리 40 줄**만 있고 나머지는 `sha256
d64c509d6d8b…` 로만 진다. 그 sha 가 가리키는 바이트가 저장소에 없으므로 **그 해시를 대조할
대상이 없다.** `C3` 이 반증된 까닭(*"판정자가 읽은 바이트가 사후에 복원된다"* 가 깨졌다)과
같은 형태가 `E2` 의 첫 행에도 있다. 파일은 그 사실을 정직하게 적고 *"판정에 쓰이는 자리는
머리 여섯 줄"* 로 한정했으므로 거짓 주장은 아니다.

- 모집단: 원의도
- 유효성: 참 — `wc` 로 파일을 세었고 본문이 40 줄 뒤에서 `…⟨8510 줄 줄임⟩` 으로 끊긴다
- 해악도: 거짓신호
- 어디가 걸리나: `E2`(`intent.md:259`)

### 요구되지 않은 것

없음 — 코드 변경 전량(`xtask/src/main.rs` · `ledger.rs` · `defect.rs` · `narrative.rs` ·
`query.rs` · `pal-query/src/lib.rs` · `touch.rs` · `pal-git/src/lib.rs` ·
`scripts/f12-verify.py` · 시험 둘)을 커밋별로 훑었고 전부 세 사전 등록의 좌표표 · `D4-a` ·
정반합 처분(`e44a87e`·`06e8d63`·`017e5f1`·`644e2e6`) 안에 든다. 아무도 안 부르는 표면도
없었다 — `IdentityTally`(9) · `최근에_끝난`(호출 `:6350`) · `종료_커밋_시각`(호출 `:6350`·`:6356`) ·
`민팅`(16) · `개체_없음`(6) · `narrative_unminted`(4) · `Ingested::비어_있다`(호출 `query.rs:159`)
전부 제품 경로에서 불린다.

### 있는데 틀린 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 7 | ⟨본문 「발견 1」⟩ ★★ `A5` 의 사상 규칙이 실측 단계에서 「touch 커밋 **뒤**」로 좁혀졌고, 그 범위에서는 조상 관계가 구성상 언제나 참이라 검사가 원리상 못 빨개진다. 문면대로 재면 ㉡ 이 반증이고 게이트는 통과로 싣는다 | 원의도 | 참 | 금지역 | `A5` | `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:192` · `.palimpsest/rounds/2026-09-11-effect-confirmation/observations/e1-measure.md` `### A5` 절 · `.palimpsest/rounds/2026-09-11-effect-confirmation/plan/126-pre.md:53-57` · `docs/gates/effect-confirmation.md:31` | `git log --reverse --format=%H 8604d62..HEAD -- xtask/src/main.rs \| head -1` → `8644835…` · `git merge-base --is-ancestor 14f0f92 8644835; echo rc=$?` → `rc=1` (㉠·㉢ 은 rc=0) |
| 8 | ⟨본문 「발견 2」⟩ ★★ 계획 문서가 원 의도의 합격선을 *"섰다 2026-09-12"* 로 적는데 게이트는 그 본체 `C2` 를 *"「안 쟀다」이지 「됐다」가 아니다"* 로 적는다. 같은 줄의 *"㉠ `#79` 는 0"* 은 `effect/79-delta.md:23-26` 이 자기모순으로 판정해 정정한 문장이다 | 원의도 | 참 | 금지역 | `E6`·`C2`·`C1` | `docs/plan/03-shortest-path.md:139` | `git blame -L 139,139` → `f331f2f`(00:18) · `C2`→미측정은 `06e8d63`(11:22) · 게이트 `:34`·`:58`·`:286` 과 `effect/79-delta.md:23`·`:38` 대조 |
| 9 | ⟨본문 「발견 4」⟩ `A5-a` 의 음성 대조가 실측이 쓴 절차를 안 덮는다 — 대조 스크립트는 변경 커밋을 인자로 받고, 실측은 `git log <touch>..HEAD` 로 스스로 뽑는다. 그래서 대조가 뒤집은 축이 실측 축이 아니다 | 자기장치 | 참 | 금지역 | `A5-a`·`A5` | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/negative-A1.md:14-21` · `.palimpsest/rounds/2026-09-11-effect-confirmation/observations/e1-measure.md` `### A5` 절 | 두 문서의 절차 문면 대조 · 범위 연산자의 성질은 7 에서 재현 |
| 10 | ⟨본문 「발견 3」 · **이번 라운드의 새 발견**⟩ ★ 이 회차가 실증한 검사 사각(죽은 링크 검사가 백틱 코드 참조를 원리상 못 본다)이 원장에도 이슈에도 안 실렸다 — 게이트 산문 한 줄이 전부다. `E4` 는 `findings.jsonl` 만 세므로 기계가 다음 회차로 안 넘긴다 | 저장소 | 참 | 금지역 | 없음 | `docs/gates/effect-confirmation.md:142-143` · `xtask/src/main.rs` 「죽은 링크 부재」 검사 | `findings.jsonl` 283 행 전수 파싱 → 「죽은 링크·마크다운 링크·코드 참조」 적중 **0** · `gh issue list --state open --limit 60` 전수 확인(`#87` 은 반대 방향 · `#134` 는 다른 자리) |
| 11 | ⟨본문 「발견 5」 · **이번 라운드의 새 발견**⟩ 재는 자리 셋의 이슈가 셋 다 아직 OPEN 이다. `B1`·`B5`·`B6`·`B1-d` 가 통과이고 차선책 2 가 안 열렸으므로 문면이 가리키는 갈래는 「닫는다」인데 안 닫혔다 | 저장소 | 참 | 거짓신호 | `B1`·`B5` | `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:272-277` | `gh issue view 79/126/129` → 셋 다 `{"closedAt":null,"state":"OPEN"}` |

**아래는 같은 다섯의 근거 전문이다 — 표와 본문이 같은 항이고 수는 다섯이다.**
⚠ **10·11 은 「이번 라운드의 새 발견」이다** — 본문이 이 절 안에 이어져 있을 뿐이다.

#### 발견 1 ★★ — `A5` 의 사상 규칙이 측정 단계에서 바뀌어 항등식이 됐고, 문면대로 재면 ㉡ 이 반증이다

조건 문면(`intent.md:192`):

> `touch/<이슈>.txt` 커밋이 **그 자리의 첫 코드 변경 커밋의 조상이고 두 SHA 가 다르다.**
> 재는 자리와 커밋의 사상은 **사전 등록의 `파일:줄` 목록**이 진다(그 목록의 파일을 건드린 첫 커밋)
> ⟨`PM3-15` — 대상 선정을 산문으로 남기면 ⟨결정론적⟩ 태그가 거짓이 된다⟩

실측 기록(`observations/e1-measure.md` `### A5` 절)은 이렇게 적는다:

> 그 목록의 파일을 **touch 커밋 뒤** 처음 건드린 커밋을 `git log <touch>..HEAD -- <파일들>` 로 뽑았다

**「touch 커밋 뒤」는 조건 문면에 없다.** 그리고 그 한정을 넣는 순간 `git log <touch>..HEAD`
가 내는 커밋은 **정의상 touch 의 자손**이므로 `merge-base --is-ancestor <touch> <변경>` 은
**구성상 언제나 rc=0** 이다. 이 회차가 등록한 금지역 **「측정이 죽은 가지」** 그대로다.

문면 그대로 재면 ㉡ 이 뒤집힌다:

```
$ git log --reverse --format=%H 8604d62..HEAD -- xtask/src/main.rs | head -1
8644835435cf6dc59acd01e797268e2859ebbe71
$ git merge-base --is-ancestor 14f0f92 8644835 ; echo rc=$?
rc=1                          ← touch 가 첫 변경의 조상이 아니다
```

㉠·㉢ 은 문면 그대로 재도 통과한다(`9f993cc`·`1abc0e9` 가 각각 touch 뒤다). **㉡ 하나가
반증**이고, 조건이 *"재는 자리 각각"* 을 걸므로 `A5` 는 반증이다.

★ **회차가 이 사실을 이미 문서로 갖고 있었다.** `plan/126-pre.md:53-57` §2.4 가 스스로 적는다 —
*"같은 파일을 이 세션이 이미 고쳤다 — `8644835` 가 …"*. 봉인 자신이 A5 를 깨는 사실을 싣고
있었는데 어느 판정도 그것을 `A5` 로 연결하지 않았다. `grep -rn 8644835` 의 적중 7 건 중
`A5` 를 부르는 것은 **0** 이다.

★ **「그 커밋은 #126 작업이 아니다」로는 못 구한다.** 그것이 바로 `PM3-15` 가 금지한
「산문으로 대상 선정」이고, 그래서 문면이 기계 규칙을 못 박았다.

- 모집단: 원의도
- 유효성: 참 — 위 두 명령의 rc 로 재현했다
- 해악도: **금지역** — 「측정이 죽은 가지」(실측 절차가 원리상 못 빨개진다) 와
  「사실이 아닌 것을 사실로」(게이트 `:31` 이 `A5` 를 통과 칸에 싣는다) 둘에 동시에 든다
- 어디가 걸리나: `A5` · `A5-a` · 게이트 `docs/gates/effect-confirmation.md:31` ·
  `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:192` ·
  `.palimpsest/rounds/2026-09-11-effect-confirmation/observations/e1-measure.md` `### A5` 절

#### 발견 2 ★★ — 계획 문서가 원 의도의 합격선을 「섰다」로 적는데 게이트는 그 본체를 「안 쟀다」로 적고, 같은 줄의 수는 회차가 스스로 정정한 거짓이다

`docs/plan/03-shortest-path.md:139` 의 합격선 칸:

> 그 산출을 보고 달라진 변경 **1 건 이상**, 좌표와 함께 ⟨**섰다 2026-09-12** — 재는 자리 셋 중
> **㉡ `#126` 에서 둘**. 새 에이전트가 귀속 **유효 3 · 무효 2** … **㉠ `#79` 는 0** 이고 …⟩

둘이 틀렸다.

**⑴ 「섰다」** — 이 문장이 그대로 옮긴 것이 `C2`(*"그 차이 중 touch 산출에 귀속되는 것이
1 건 이상"* ← **합격선 본체**)이고, 게이트 `:34`·`:53`·`:58` 은 그것을 **미측정**으로 적으며
절 제목이 *"미측정 다섯 — 「안 쟀다」이지 「됐다」가 아니다"* 다. `intent.md` `## 승격` 칸 2 의
합도 *"내 판정은 0 이 아니라 「안 쟀다」다"* 를 명기한다. **게이트는 「안 쟀다」, 계획 문서는
「됐다」.**

**⑵ 「㉠ `#79` 는 0」** — 게이트 `## 효과` 표의 ㉠㉱ 행이 *"원인이 touch 인가 = **그렇다**
⟨`touch/79.txt:23`⟩"* 이고 `effect/79-delta.md:38` 이 같은 것을 적는다. 더구나 같은 파일
`:23-26` 이 **이 문장을 이름으로 정정한다**:

> ⚠ ⟨2026-09-12 정정⟩ 이 머리글은 *"넷이고 **어느 것도 touch 가 원인이 아니다**"* 였는데,
> 같은 표 ㉱ 의 원인 칸이 **`touch/79.txt:23`** 이라 **자기모순**이었다

계획 문서의 그 줄은 **정정 전 문장을 그대로 들고 있다.**

시각이 이것을 못 박는다 — 계획 줄은 `f331f2f`(2026-09-12 00:18)에 섰고, `C2`→미측정은
`06e8d63`(11:22), `79-delta` 정정은 같은 날 그 뒤다. **`E6` 을 한 번 집행하고 그 뒤의
정정 넷을 계획 문서에 안 되먹였다.**

★ 이 자리가 무거운 까닭 — 이 줄은 **다음 회차가 무엇이 남았는지 읽는 첫 자리**이고
(`02-order.md:252-259` 가 여기를 가리킨다), 「합격선이 섰다」와 「㉠ 은 0」 둘 다 다음
회차의 착수 판단을 바꾼다.

- 모집단: 원의도
- 유효성: 참 — `git blame -L 139,139` = `f331f2f` · `git log -S` 로 `06e8d63` 확인 ·
  게이트 `:286` 과 `effect/79-delta.md:23`·`:38` 대조
- 해악도: **금지역** — 「사실이 아닌 것을 사실로」
- 어디가 걸리나: `E6`(`intent.md:263`) · `C2` · `C1` ·
  `docs/plan/03-shortest-path.md:139`

#### 발견 4 — `A5` 의 음성 대조가 실측이 쓴 절차를 안 덮는다

`effect/negative-A1.md:14-21` 이 싣는 판정 스크립트는

```
judge.sh <REPO> <ISSUE> <SEAL> <TOUCH> <READNOTE> <CHANGE> <프롬프트TIP>
  A5    is-ancestor $TOUCH $CHANGE 가 0 이고  $TOUCH != $CHANGE
```

곧 **변경 커밋을 인자로 받는다.** 그런데 실측(`e1-measure.md`)은 변경 커밋을
`git log <touch>..HEAD` 로 **스스로 뽑는다.** 대조가 뒤집은 것은 *"사람이 넘긴 CHANGE 가
touch 앞이면 빨개지는가"* 이고, 실측이 쓴 경로는 *"범위에서 뽑은 CHANGE"* 라 **그 범위가
곧 정답을 보증한다.** 두 절차가 다르므로 `A5-a` 가 `A5` 의 실측 축을 **원리상 못 친다** —
음성 대조 자신이 아니라 **대조와 실측의 불일치**가 항등식을 만든 자리다.

`neg5`(봉인→변경→touch)를 **실측 절차로** 재면 `git log <touch>..HEAD -- <파일>` 이 그
변경을 아예 안 집으므로 `FAIL` 이 아니라 「변경 커밋 없음」이 난다 — 조건에 그 갈래의 판정이
없다.

- 모집단: 자기장치(`judge.sh` · `e1-measure.md`) — **금지역에 닿으므로 본 목록이다**
- 유효성: 참 — 두 문서의 절차 문면을 나란히 읽었고, 범위 연산자의 성질은 위 발견 1 에서 재현했다
- 해악도: **금지역** — 「측정이 죽은 가지」
- 어디가 걸리나: `A5-a`(`intent.md:197`) · `A5`

#### 발견 3 ★ — 이 회차가 실증한 검사 사각(백틱 코드 참조)이 원장에도 이슈에도 안 실렸다

게이트 `:142-143` 이 적는다:

> ⚠ `cargo xtask check` 의 「죽은 링크 부재」가 이것을 원리상 못 잡는다 — 그 검사는
> 마크다운 링크(`[…](…)`)를 재고 이 자리는 **백틱 코드 참조**였다. 그 사실을 여기 적는다.

그 사각이 이 회차에서 **실제로 금지역 사고를 냈다** — 게이트가 `docs/adr/0027-rust-first-class.md`
라는 없는 파일을 두 자리에서 권위 재료로 걸어 두었고, 아무도 못 열어 *"어느 것도 규범형으로
말하지 않는다"* 라는 전칭 부정이 안 재어진 채 사실로 앉았다(`intent.md:700-705` 가 스스로
*"등록 금지역 둘에 동시에 든다"* 로 판정한다).

그런데 **그 사각 자체의 처분은 게이트 산문 한 줄이 전부다.**

- `findings.jsonl` 283 행에 「죽은 링크」·「마크다운 링크」·「코드 참조」를 요약으로 담은
  행이 **0** 이다(전수 파싱).
- 열린 이슈 전수에도 없다 — `#87` 은 **반대 방향**(코드 스팬 안의 `](…` 를 링크로 **오인**)
  이고 `#134` 는 리뷰어 파일의 뿌리 기준 링크 문제다. 「백틱 참조를 **못 본다**」는 자리는
  아무도 안 진다.
- 규약(`SKILL.md` 계열)이 저장소 안 문서를 **백틱 코드 표기**로 적으라고 요구하므로
  (`#134`), 이 저장소는 **검사가 원리상 못 보는 표기로 저장소 내부 참조를 적도록 지시**한다.
  그 지시가 서 있는 한 같은 사고가 되풀이된다.

`E4`(열린 발견 0)는 `findings.jsonl` 만 센다. 원장에 안 실린 것은 **기계가 다음 회차로 안
넘긴다.**

- 모집단: 저장소
- 유효성: 참 — `findings.jsonl` 전수 파싱 적중 0 · `gh issue list --state open --limit 60`
  전수 확인 · 게이트 `:142-143` 문면
- 해악도: **금지역** — 「측정이 죽은 가지」
- 어디가 걸리나: 어느 조건에도 안 걸린다 — **의도 축의 발견**이다.
  좌표는 `docs/gates/effect-confirmation.md:142-143` · `xtask/src/main.rs` 의 「죽은 링크 부재」 검사

#### 발견 5 — 재는 자리 셋의 이슈가 셋 다 아직 OPEN 이다

```
$ gh issue view 79  → {"closedAt":null,"state":"OPEN"}
$ gh issue view 126 → {"closedAt":null,"state":"OPEN"}
$ gh issue view 129 → {"closedAt":null,"state":"OPEN"}
```

세 자리 다 코드 변경·시험·GREEN 이 섰고 `B1`·`B5`·`B6`·`B1-d` 가 통과로 적혔다.
차선책 2 는 *"`B5` 의 산출 경로가 안 서면 `#79` 를 **안 닫고**"* 이고 게이트는 그 문이
**안 열렸다**고 적으므로, 문면이 가리키는 갈래는 「닫는다」다. 그런데 안 닫혔다.
`./scripts/frontier.sh` 는 이 셋을 **아직 할 일**로 계속 낸다.

- 모집단: 저장소
- 유효성: 참 — `gh issue view` 셋
- 해악도: 거짓신호
- 어디가 걸리나: `B1` · `B5` · 차선책 2(`intent.md:272-277`)

---

## 자기 산출에 대한 발견 ⟨모집단 `회차기록` · 메인이 처리 방침을 정한다⟩

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 12 | ⟨본문 「발견 8」⟩ `state.md` 의 「남은 것」 표가 닫힌 둘을 열린 것으로 적는다 — *"게이트 50 개 전부 미측정 칸"* · *"`E1` … 지금 0/50"* · *"`E4` … 지금 65"*. 실제는 42/3/0/5 이고 열림 0 이다 | 회차기록 | 참 | 거짓신호 | 없음 | `.palimpsest/rounds/2026-09-11-effect-confirmation/state.md:157`·`:189`·`:190` | 게이트 `:31-36` · `findings.jsonl` 전수 파싱과 대조 |
| 13 | ⟨본문 「발견 9」⟩ 원장 `MS-19`(금지역·닫힘)가 근거 좌표로 `docs/adr/0027-rust-first-class.md` 를 든다 — 이 회차가 금지역으로 판정한 바로 그 죽은 경로다 | 회차기록 | 참 | 거짓신호 | `E4` | `.palimpsest/rounds/2026-09-11-effect-confirmation/findings.jsonl` `MS-19` 행 | `ls docs/adr/` 전수 · `grep -rn "0027-rust-first-class"` — 실재는 `docs/adr/0027-the-instrument-must-reach-its-own-floor.md:129-136` |
| 14 | ⟨본문 「발견 10」⟩ `judge-roster.md` 끝줄이 낡았다 — *"이 표는 아직 자라는 중이다 — ㉠·㉢ 의 판이 끝나면 행이 는다"* 인데 다섯 행이 다 섰다 | 회차기록 | 참 | 미관 | `C3-a` | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/judge-roster.md` 끝줄 | `ls effect/*judge-*.md` → 5 개 |

**아래는 같은 셋의 근거 전문이다 — 표와 본문이 같은 항이고 수는 셋이다.**

#### 발견 8 — `state.md` 의 「남은 것」 표가 닫힌 둘을 열린 것으로 적는다

`state.md:157` 이 *"게이트 `## 판정` | 50 개 전부 `미측정` 칸"*, `:189` 가
*"**E** — `E1` … ⚠ **지금 0/50 이다**"*, `:190` 이 *"**G** — `E4` … **지금 65**"* 를 적는다.
`E1` 은 `b441adf` 가, `E4` 는 `39b0fa3` 가 이미 닫았고 지금 게이트는 42/3/0/5 이며 원장의
열림은 0 이다. `:140` 이 *"지금 수 ⟨`f75b4b0` + 미커밋⟩"* 으로 핀을 적지만, **E·G 행은
취소선 없이 남은 것 표에 있어** 이어받는 기계가 둘을 할 일로 읽는다.

- 모집단: 회차기록 · 유효성: 참 · 해악도: 거짓신호
- 어디가 걸리나: 어느 조건에도 안 걸린다. 좌표 `.palimpsest/rounds/2026-09-11-effect-confirmation/state.md:157`·`:189`·`:190`

#### 발견 9 — 원장 `MS-19` 가 근거 좌표로 없는 파일을 든다

`findings.jsonl` 의 `MS-19`(해악도 `금지역` · 처분 `정정` · 닫은커밋 `c807f32`)가 요약에
*"⑴ `docs/adr/0027-rust-first-class.md` 의 「대안과 왜 아닌가」 표가 … 이미 기각했다"* 를
적는다. 그 파일은 **저장소에 없다**(실재는
`docs/adr/0027-the-instrument-must-reach-its-own-floor.md:129-136`). 이 회차가 **금지역으로
판정한 바로 그 죽은 경로**가 회차 원장의 근거 칸에 그대로 남아 있다.

- 모집단: 회차기록 · 유효성: 참 · 해악도: 거짓신호
- 어디가 걸리나: `E4`. 좌표 `.palimpsest/rounds/2026-09-11-effect-confirmation/findings.jsonl` `MS-19` 행

#### 발견 10 — `judge-roster.md` 의 끝줄이 낡았다

*"⚠ **이 표는 아직 자라는 중이다** — ㉠·㉢ 의 판이 끝나면 행이 는다."* 표는 이미 다섯 행이고
㉠·㉢ 의 판이 다 끝났다(`79-judge-1` · `129-judge-1` · `129-judge-2`).

- 모집단: 회차기록 · 유효성: 참 · 해악도: 미관
- 어디가 걸리나: `C3-a`. 좌표 `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/judge-roster.md` 끝줄

---

## 내가 기각한 것

| # | 요약 | 경로 | 모집단 | 유효성 | 해악도 |
|---|---|---|---|---|---|
| 15 | 원장 283 행 중 133 행의 `경로`·136 행의 `처분자리` 가 저장소 뿌리에 없는 경로다 → **아니다.** 회차 뿌리 기준 표기이고 `cargo xtask check` 의 「회차 레코드」가 2638 행을 재어 좌표 면제 2 행만 내고 통과한다 | `.palimpsest/rounds/2026-09-11-effect-confirmation/findings.jsonl` | 회차기록 | 거짓 | 미관 |
| 16 | 게이트 `#66` 정정 표의 「심볼 3332 · 파일 142」가 지금 `pal ledger` 값 3335 와 다르다 → **아니다.** 잰 명령과 HEAD(`9e092e3`)가 문서에 명기돼 있고, 그 뒤 커밋이 심볼을 셋 늘린 것은 정상 드리프트다 | `docs/gates/effect-confirmation.md:95` | 저장소 | 거짓 | 미관 |
| 17 | `B3` 의 셈법이 「48 줄」이라 적었는데 지금 49 줄이라 회귀 조건이 갈린다 → **아니다.** 조건은 *"`passed` 합산 ≥ 1028"* 이고 괄호 안 줄 수는 착수 시점 설명이다. 합 1047 로 통과 | `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:216` | 원의도 | 거짓 | 미관 |
| 18 | `A1-c` 가 전사만 있고 안 재어졌다 → **아니다.** `judge-prompt.md` 의 `## ①` 네 절을 구분선 사이에서 떼어 봉인 blob 과 바이트 비교했고 4/4 exact match 다 | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/judge-prompt.md:52`·`:482`·`:1154`·`:1571` | 원의도 | 거짓 | 미관 |
| 19 | `D4-a` 가 등록한 좌표 `scripts/f12-verify.py:559-560` 이 실물과 안 맞는다 → **아니다.** 착수 시점 좌표이고 게이트가 `:556-570` 으로 갱신해 실었으며 그 자리가 실제로 고쳐졌다 | `scripts/f12-verify.py:556-570` | 저장소 | 거짓 | 미관 |
| 20 | 소유자의 규범형 문장이 코드·ADR 어디에도 결박 안 됐다 → **아니다.** `docs/plan/03-shortest-path.md:150-153` 이 소유자 귀속과 날짜와 함께 싣고 `ADR-0027:14` 가 같은 「바닥」을 `[f11.pass]` ⑧ 으로 묶는다. 두 문장이 안 부딪힌다 | `docs/plan/03-shortest-path.md:150-153` | 저장소 | 거짓 | 미관 |
| 21 | 이 회차가 코드 결박을 하나도 안 늘렸다(`bindings.jsonl` 35 → 35) → **아니다.** `intent.md:110-114` 가 세 심볼 결박 0 을 착수에 못 박고 목표 1 을 원리상 밖으로 선언했으며 `#69` 를 범위 밖에 등록했다 | `.palimpsest/intent/bindings.jsonl` | 저장소 | 거짓 | 미관 |
| 22 | `neg9` 산출의 `워킹트리 일치` 가 더러운 트리에서 찍히므로 `A5-b` 가 오염을 못 잡는다 → **아니다.** 그 산출이 바로 `A5-b` 가 `body` 해시로 오염을 **잡은** 자리이고, 「일치」 줄 문제는 `A5-c`·`## 범위 밖` 이 이미 등록해 둔 것이다 | `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/negative-A5.md` | 원의도 | 거짓 | 미관 |

---

## 이 반환문이 낸 항의 총수

**4 + 2 + 3 + 2 + 3 + 8 = 22.**
미측정 목록 4 · 빠진 것 2 · 있는데 틀린 것 3 · 이번 라운드의 새 발견 2 · 자기 산출 3 · 기각 8.
그중 유효성 `거짓` 은 **8**(전부 기각)이다. `## 합격선 축` 의 50 행은 **조건 판정**이고 항이 아니다.

---

## 끝내도 되는가

**안 된다.** 본 목록에 금지역 **넷**(발견 1·2·3·4)이 남았다. 그중 둘(발견 1·4)은 같은
축(`A5` 의 사상 규칙)이고, 발견 2 는 다음 회차의 착수 문서에 앉아 있다.
