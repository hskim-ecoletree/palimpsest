# 반(反) — 판 4 · `C4` ⟨`pal-decision-opponent`⟩

> 아래는 에이전트의 **반환문 전문**이다. 부르는 쪽이 한 글자도 안 고쳤다.
> 이 대립역은 지시로 `effect/*-judge-*.md` 다섯을 **전부 읽었다** — 초안이 못 본 자리다.

---

프롬프트에 대화 기록·메인의 사고 과정은 섞여 오지 않았다(초안 파일 + 조건 문면 + 증거 목록뿐). 지시대로 `effect/*-judge-*.md` 다섯을 전부 읽었다. 새 실행은 안 했다 — `git show`/`git grep`/`sed` 만 썼다.

## 초안이 서는 자리

- **「방향」 기입 0 은 실제로 섰다.** 핀 `58d21c9` 에서 `git grep "나아졌다\|나빠졌다\|모른다"` 를 다시 돌렸고, 세 delta·세 readnote·다섯 judge 어디에도 **차이 행에 붙은 방향 라벨이 없다**. 이 사실은 내 반론 어느 것도 안 건드린다.
- **초안이 지목한 첫 뒤집기 자리(`effect/*-judge-*.md` 다섯)는 안 열린다 — 내가 다 읽었다.** 다섯은 전부 `C2-b` 의 서식(`인용한 줄` · `담은 사실` · `사전 등록에 있나` · `§5.8 표에 있나` · `판정 근거`)만 채우고 **방향도 「참이었나」도 라벨로 안 적는다**. 판정자가 답한 것은 「그 사실이 **새로운가**」이지 「**참인가**」가 아니다(`effect/126-judge-1.md:22-27` · `effect/79-judge-1.md:18-23` · `effect/129-judge-1.md:37-51` · `effect/129-judge-2.md:35-41` · `effect/126-judge-2.md:23-35`). 설계도 같은 자리를 못 박았다 — `dialectic/4-design.md:133` *"판정자 원문(귀속 여부는 `C4` 의 술어가 아니다)"*.
- **`effect/79-delta.md:28` 의 결과 좌표 어긋남 지적이 맞다.** `git show 9f993cc:crates/pal-cli/src/ledger.rs` 에서 「그룹마다 첫 선언은 빠집니다」 줄은 `:661`(주석 `:658-660`)이고 delta 가 적은 `:653-656` 이 아니다.
- **핀 「움직이는 표적」 반론은 바이트 기준으로는 안 선다.** `05f07ea..HEAD` 에서 회차 디렉터리에 더해진 것은 `dialectic/` 의 preflight·thesis 다섯 파일뿐이고 delta·readnote·touch·게이트 바이트는 **한 줄도 안 변했다**.

## 반론

| # | 대상 | 반론 | 좌표 | 유효성 | 해악도 | 이 반론이 틀렸다면 무엇으로 아는가 |
|---|---|---|---|---|---|---|
| R1 | `C4` | 초안의 ★뼈대(§5)가 **사실이 아니다** — 「`호출자 1` 이 참이었나」는 이미 회차에 적혀 있다. `effect/126-readnote.md:14` 가 *"그 뒤 `grep -n "check_ledger_pair" xtask/src/main.rs` 로 확인했다 — `:643` 의 검사 등록 표 한 자리뿐이다"* 로 **명령·좌표·결론**을 적었고, 그것은 변경 커밋 `398d233` **전**에 커밋된 `9be7699` 다(`effect/126-delta.md:4`). 초안은 *"확인한 것은 delta 가 아니라 나다"*(`dialectic/4-thesis.md:92`) · *"기록이 저장소 어디에도 안 섰다"*(`:82`) 를 적는다 | `effect/126-readnote.md:14` ↔ `dialectic/4-thesis.md:82`·`:92` | **참** | **금지역** | `126-readnote.md:14` 가 ㉡㉱ 의 원인 줄(`touch/126.txt:24`)이 **아닌** 다른 줄을 확인한 것이면, 또는 readnote 가 변경 커밋 뒤에 커밋됐으면 |
| R2 | `C4` | 초안은 **readnote 셋을 한 번도 안 다뤘다** — `dialectic/4-thesis.md` 전문에 `readnote` 문자열이 **0 회**다. 그런데 셋은 「touch 가 말한 것이 참이었나」를 근거와 함께 적는다: `effect/79-readnote.md:20-41`(「호출자 3」이 **틀렸다** · `pal query symbol.callers` 산출 3 줄 · `defect.rs:322`) · `effect/126-readnote.md:21-33`(`identity ordinal` 이 왜 참인지 · `coord.rs:246-266`·`ledger.rs:334`) · `effect/129-readnote.md:33-35`(*"㉢ 에서 touch 는 **틀린 말을 하지 않았다** — 후보 셋은 실재하고 좌표도 맞다"*). 「실질 2/11」은 **적어도 3** 이고 ㉢ 자리에는 **자리 전체에 걸린 답**이 있다 | `dialectic/4-thesis.md`(`readnote` 0 회) · `effect/79-readnote.md:20`·`effect/126-readnote.md:21`·`effect/129-readnote.md:33` | **참** | **금지역** | `C4` 의 「적혔다」가 `effect/*-delta.md` 안으로 **한정**된다는 문면이 있으면. 초안 스스로 `:128` 에서 *"기록 위치를 한정하지 않으므로"* 라 적었으므로, 그 근거로는 안 된다 |
| R3 | `C4`·`C1` | **모집단 11 은 `C1` 을 잘라 인용해서 나왔다.** 초안은 `C1` 을 *"사전 등록 ↔ 실제 변경의 차이 목록"*(`4-thesis.md:44`)까지만 옮겼는데, 원문은 이어서 ***"차이마다 좌표 두 끝이 붙었다 — `touch/<이슈>.txt:N` 이 원인이고 `파일:줄` 이 결과다"*** 다. 즉 `C1` 이 부르는 「차이」는 **원인이 touch 인 것**이고, 11 행 중 8 행은 원인 칸에 스스로 *"touch 아님"*·*"컴파일러"*·*"저장소 검사"* 를 적는다. 모집단은 **3**(㉡㉰·㉡㉱·㉠㉱)이고 그 셋은 R1·R2 로 「참이었나」가 **3/3** 이다 | `intent.md:231` ↔ `dialectic/4-thesis.md:44` · `effect/126-delta.md:24-27` · `effect/79-delta.md:27-30` · `effect/129-delta.md:29-31` | **참** | **거짓신호** | `C1` 의 뒷 절이 「차이의 정의」가 아니라 「기입 형식의 요구」일 뿐이라는 읽기가 서면. 그때도 `79-delta.md:27-29`·`129-delta.md:29-31` 은 그 형식을 **못 지킨 행**이 되므로 모집단은 여전히 11 이 아니다 |
| R4 | `C4` | 초안이 **자기가 「해당 없음」이라 적은 행을 실패 분모에 넣었다.** ⓒ 칸에 ㉢ 세 행은 `해당 없음`, 다섯 행은 `판정 불능` 이라 적어 놓고(`4-thesis.md:61-67`) 같은 행들을 「0/11」의 분모로 세었다(`:69`). 원인이 touch 가 아닌 행에는 「touch 가 말한 것」이라는 **대상 자체가 없으므로** 술어가 공허하다 — 초안이 §7 에서 스스로 금지한 「측정이 죽은 가지」와 같은 형태다 | `dialectic/4-thesis.md:61-67` ↔ `:69` · `:109` | **참** | **거짓신호** | 원인이 도구인 차이에도 「touch 가 그 자리에 대해 말한 것」이 실재한다고 좌표로 보이면 |
| R5 | `C4` | **「라벨」 요건은 초안이 새로 만든 것이다.** 판정문 `:20` 은 *"**라벨된** 기입도 0 건"* 으로 조건을 세우는데, `intent.md:238` 은 *"근거와 함께 적혔다"* 이고 `dialectic/4-design.md:90-91` 의 판정 대상도 ⓐ 붙었나 ⓑ 근거 ⓒ 어긋남뿐이다. 어디에도 라벨 형식 요구가 없다. 라벨을 요건으로 올리는 순간 R1·R2 의 산문 기입이 부당하게 탈락한다 | `dialectic/4-thesis.md:20` ↔ `intent.md:238` · `dialectic/4-design.md:90-92` | **참** | **거짓신호** | 「적혔다」가 정형 라벨을 뜻한다는 문면이 회차 어딘가에 서면 |
| R6 | `C4` | **§3 의 「전수 확인」이 사실과 다르다.** *"훑으면 나오는 곳은 **넷뿐**"*(`:73-78`) 인데, 핀 `58d21c9` 에서 같은 grep 은 **12 곳**을 낸다 — `1-design.md:92`·`2-design.md:92`·`3-design.md:92`·`4-design.md:92`·`preflight.md:243`·`:283`·`:285`·`126-judge-2.md:6`·`judge-prompt.md:1108` 이 빠졌다. ⚠ 초안은 **`4-design.md:92` 를 자기 `:130` 에서 인용한다** — 자기가 인용한 줄을 자기 전수 목록에서 뺐다. (결론은 안 바뀐다. 바뀌는 것은 「전수로 쟀다」의 신뢰도다) | `git grep -n "나아졌다\|나빠졌다\|모른다" 58d21c9 -- .palimpsest/rounds/2026-09-11-effect-confirmation` (12 hit) ↔ `dialectic/4-thesis.md:73-78`·`:130` | **참** | **거짓신호** | 초안의 「넷」이 raw match 가 아니라 「기입 후보」의 수였다는 읽기. 그러나 문장은 *"훑으면 나오는 곳"* 이다 |
| R7 | `C4` ⓒ | **어긋남 판정을 증거를 안 보고 내렸고, 실제로 어긋난 자리가 있다.** 초안은 ㉠㉱ 에 *"어긋남 없음(내가 대조함)"* 이라 적었는데(`4-thesis.md:64`), 같은 회차의 `effect/79-judge-1.md:21` 은 같은 touch 줄을 두고 *"grep 의 「호출 자리 4」와 touch 의 「참조 엣지 기준 호출자 3」은 **잣대와 값이 다른 별개의 사실**"* 이라 읽는다 — `effect/79-readnote.md:20`·`effect/79-delta.md:94` 의 *"「호출자 3」이 **틀렸다** … 실제는 4"* 와 **정면으로 갈린다**. `4-design.md:90-91` 의 ⓒ 가 잡으라고 세운 바로 그 형태인데, 초안은 다섯을 안 읽어 못 봤다 | `effect/79-judge-1.md:21` ↔ `effect/79-readnote.md:30-41` · `effect/79-delta.md:94` ↔ `dialectic/4-thesis.md:64` | **참** | **거짓신호** | 두 문장이 양립한다고 보면(판정자는 **신규성**만 말했고 참·거짓은 안 말했다) 어긋남이 아니다. 그러면 이 회차에 **ⓒ 를 판정할 근거가 한 자리도 없다**는 것이 대신 선다 |
| R8 | `C4` | **§4 가 회차의 순서를 거꾸로 읽었다.** *"게이트 `## 효과` 가 비었으니 그리로 갔을 가능성도 닫힌다"*(`:86`) 인데, 방향이 실릴 자리를 지정한 것은 `E2`(`intent.md:254` — *"`## 효과` 에 … 차이마다 방향이 나란히 적혔다"*)이고 그 작업은 `state.md:105` 의 **6 번**(게이트·효과), 지금 도는 정반합은 **4 번**(`state.md:103`)이다. `intent.md:123` 도 *"게이트·종료 문서는 그 뒤에 쓴다"* 로 못 박는다. **아직 안 쓸 차례인 자리의 공백**을 반증의 근거로 셌다 — 초안이 「가장 센 대안」이라 부른 **미측정**에 초안이 못 댄 좌표가 이것이다 | `intent.md:254`·`intent.md:123` · `state.md:103`·`:105` ↔ `dialectic/4-thesis.md:86`·`:119` | **참** | **거짓신호** | `C4` 가 `E2` 와 **다른 자리**를 요구한다는 문면이 있으면. 문면은 위치를 안 한정하므로(초안 `:128`), 「아직 안 쓸 차례」는 그대로 남는다 |
| R9 | `C1`·`C2` | **`effect/79-delta.md` 가 자기모순이고 초안이 그것을 안 적었다.** 머리글 `:23` 은 *"차이 — 넷이고 **어느 것도 touch 가 원인이 아니다**"* 인데 같은 표 `:30` 의 원인 칸은 **`touch/79.txt:23`** 이다. 초안은 ㉠㉱ 를 touch 원인으로 쓰면서(`:64`·`:90`) 이 모순을 안 짚었다 — 그런데 모집단을 「귀속 차이」로 셀지 말지가 바로 이 한 줄에 걸린다 | `effect/79-delta.md:23` ↔ `:30` (preflight 에도 그대로 실림 — `dialectic/preflight.md:307`·`:311`) | **참** | **거짓신호** | 머리글의 「원인」이 「계획을 바꾼 원인」이고 표의 「원인」이 「그 행이 생긴 원인」이라 층위가 다르다는 읽기가 서면 |
| R10 | `C4` | 초안의 검산 인용이 한 자리 틀렸다 — *"`preflight.md:298-311` … 그 산출도 **11 줄**이다"*(`:48`). 그 범위는 **14 줄**이고 그중 3 줄은 `## 2. 차이` **절 머리글**이다(행은 11) | `dialectic/preflight.md:298-311` ↔ `dialectic/4-thesis.md:48` | **참** | **미관** | 초안이 「줄」로 「표 행」만 셌다고 읽으면 수는 맞는다. 그래도 인용된 범위는 14 줄이다 |

## 내가 스스로 물린 것

- **「자기지시 읽기(초안 자신이 기록하는 자리다)」를 반론으로 못 올린다.** `dialectic/4-design.md:40` 이 *"`C4` 는 **이미 적힌 기록을 보는 조건**이다"* 로 명시하고, `:92-93` 이 방향 값의 옳고 그름을 판정 대상에서 뺀다. 설계 문면이 초안 편이다 — 좌표로 반박이 안 선다.
- **「핀이 움직였다」를 반론으로 못 올린다.** `git diff --stat 05f07ea..HEAD` 가 delta·readnote·touch·게이트에 **0 변경**을 낸다. `4-design.md:242` 의 ⑧ 은 안 걸린다. (순서 논점은 바이트가 아니라 일정이라 R8 로 따로 냈다.)
- **「`## 6. 발견` 절이나 `79-delta.md:37-40` 경계 항목을 모집단에 넣어야 한다」를 못 올린다.** R3 이 모집단을 **좁히는** 쪽이라 같은 반론자가 넓히는 쪽을 동시에 주장할 근거가 없다. 초안이 그 둘을 뺀 근거(`:50-51`)는 좌표로 서 있다.
- **judge 다섯에서 방향·참이었나를 찾으려 했고 못 찾았다.** 다섯 전문을 읽었다 — 없다. 초안이 지목한 첫 뒤집기 손잡이는 **닫힌다**고 적는다.
