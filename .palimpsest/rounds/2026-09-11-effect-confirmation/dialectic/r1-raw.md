# 정반합 취합 — 2026-09-11-effect-confirmation 라운드 1 · 조건 다섯(`B1`·`A5-c`·`C2`·`C2-b`·`C4`)이 실제로 충족됐나

> 낸 자리: `pal-debate-reporter`. **읽기 전용이다 — 판정을 다시 하지 않았다.**
> 받은 것 — 설계문 · 정 넷 · 반 넷 · 합 넷 · 종료 판단 · preflight 의 전문.
> 안 받은 것 — 대화 기록 · 메인의 사고 과정 · 이 판정을 실행할 계획.
> 아래 판정 문장 · 반론 문장 · 근거 좌표는 **원문 그대로**다. 줄이지 않았다.

**설계문이 네 자리에 같은 바이트로 놓였다는 사실 — 실측했다.**
`dialectic/1-design.md` · `2-design.md` · `3-design.md` · `4-design.md` 넷의 sha256 이 전부
`829871c1eabcb1d24d21e7b9f04be47b8a7d5afbca4ccad9e9439b287121703c` 다(각 20910 바이트 · 268 줄).
설계문 자신이 요구한 기입이다 — `1-design.md:216-218`: *"**네 자리에 같은 바이트로 저장하고**(`dialectic/1-design.md` · `2-design.md` · `3-design.md` · `4-design.md`), **넷이 sha256 이 같다는 사실을 `r<n>-raw.md` 에 적는다.** 사본인 것을 안 적으면 그것이 곧 거짓 신호다."*

---

## 판정 — 합(合)이 산출한 것 그대로

**판 1 · `B1`** ⟨`1-synthesis.md:9`·`:15`⟩

> **수정** ← 게이트가 아니다. 아래 「소유자에게」가 게이트다
> **값: 통과**

**판 2 · `A5-c`** ⟨`2-synthesis.md:10`·`:55`⟩

> **수정**
> **`A5-c` = `미측정`**

**판 3 · `C2`·`C2-b`** ⟨`3-synthesis.md:11`·`:17-18`⟩

> **수정** — `C2`·`C2-b` 를 둘 다 **통과에서 미측정으로** 내린다. (게이트는 아래 「소유자에게」다)
> | 조건 | 판정 |
> | **`C2`** | **미측정** |
> | **`C2-b`** | **미측정** |

**판 4 · `C4`** ⟨`4-synthesis.md:11`·`:13`⟩

> **수정** — 초안의 판정값 `반증` 을 **`미측정`** 으로 바꾼다. ← 게이트가 아니다. 아래 「소유자에게」가 게이트다
> ① 조건 판정 — `C4` = **미측정**

**정(正)이 낸 값과의 대조 — 넷 중 셋이 뒤집혔다**

| 판 | 조건 | 정(正) | 합(合) | 움직였나 |
|---|---|---|---|---|
| 1 | `B1` | 통과 ⟨`1-thesis.md:164`⟩ | 통과 | 안 움직였다 |
| 2 | `A5-c` | 반증 ⟨`2-thesis.md:19`·`:140`⟩ | 미측정 | 움직였다 |
| 3 | `C2` | 통과 ⟨`3-thesis.md:105`⟩ | 미측정 | 움직였다 |
| 3 | `C2-b` | 통과 ⟨`3-thesis.md:106`⟩ | 미측정 | 움직였다 |
| 4 | `C4` | 반증 ⟨`4-thesis.md:20`⟩ | 미측정 | 움직였다 |

---

## 반론 처리 — 전부 (36 항)

### 판 1 · R1 — `#79` 가 인용한 그 문장 — *"약한 것과 취약한 것이 같은 화면이 된다"* — 이 고친 뒤에도 그대로 참이다

- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역 ⟨반·합 같다⟩
- 어디가 걸리나: `crates/pal-cli/src/ledger.rs:663`(화면 문구) · `:661`(코드 스스로 *"첫 선언은 빠집니다"*) · `:876`(시험이 *"첫 `dup` 과 `solo` 는 순서 위험이 없다"* 를 **불변식으로 고정**) · `crates/pal-core/src/coord.rs:258-265` · `observations/identity-after.txt:34-36`
- 출처 `정반합` · 처분 `확대` · 승격됨 `예` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"중복 그룹의 **첫 선언**(`ordinal == 0`)은 순서를 바꾸면 정체성이 맞바뀌는데(`SymbolId` 성분이 `Discriminator(kind, ordinal)` 다), tally 는 그것을 「등급이 낮음」에 넣고 화면은 **"순서 위험은 없고"** 라고 찍는다. 곧 「그냥 L1」과 「순서에 취약한 첫 선언」이 **여전히 같은 글자(등급이 낮음 3309)** 다 — 갈라진 것은 초과분뿐이다"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:37`⟩: **채택.** *"**코드로 직접 쟀다. 참이다.** `SymbolId::compute` 가 `discriminator.ordinal` 을 해시에 넣는다(`crates/pal-core/src/coord.rs:126-127`). `nodes_of` 는 등장 순서로 `ordinal` 을 준다(`ledger.rs:385-391`). 따라서 같은 (체인·이름·종류) 둘의 선언 순서를 바꾸면 **첫 선언의 `SymbolId` 가 움직인다.** ★ **이 저장소의 통과 중인 시험이 그것을 이미 증명하고 있다** … 그 문장들은 전부 `git log -S"순서 위험은 없고"` 로 확인한 대로 **이 회차의 `9f993cc` 가 새로 만든 것**이다"*

### 판 1 · R2 — 초안의 근거 2(*"같은 술어다"*)는 `ordinal` 의 생성 규칙을 안 봤다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `.palimpsest/rounds/2026-08-20-rust-extractor/premortem/r2-raw.md:76`(*"자기 저장소는 117 파일 2,778 선언 중 44 건(1.6%)"*) · `crates/pal-cli/src/ledger.rs:279-294`(`container_chains` — `s.name` 만 씀) · `git log -S"container_chains" -- crates/pal-cli/src/ledger.rs` = `5d20d4e` 하나 · `observations/identity-after.txt:34`
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"`#79` 의 「464」는 `ordinal>0` 의 값이지만 같은 출처가 **자기 저장소는 117 파일 2,778 선언 중 44 건(1.6%)** 이라고 함께 쟀다. 지금 값은 3,325 중 **10(0.30%)** — 선언이 늘었는데 5 배 줄었고, 그 사이 `container_chains` 는 **한 줄도 안 바뀌었다** … 회차 어디에도 이 4 배 어긋남을 잇는 자리가 없다"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:38`⟩: **채택.** *"인용 원문을 열어 확인했다 … ⚠ **다만 반이 든 설명 하나는 내가 반증했다** — 반은 *"그 사이 추출기가 안 바뀌었다"* 는 뉘앙스로 `container_chains` 만 짚었는데, `git log --since=2026-08-20 -- crates/pal-extract/src/rust.rs` 는 **여러 커밋**을 낸다. 모집단(2,778 선언 ↔ 3,325 심볼)과 계수기(임시 스크립트 ↔ `nodes_of`)가 다르므로 어긋남 자체는 설명 가능하다. **그래도 아무도 그 조정을 적지 않았다**는 사실은 남는다"*

### 판 1 · R3 — 「커밋 시각」을 실제로 읽는 유일한 함수가 시험에 한 번도 안 걸린다

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `xtask/src/main.rs:6434-6448`(함수 전문 · 시험 참조 0) · `:6450-6490`(시험 셋 전부 주입 클로저) · `:6422-6428`(`unwrap_or(i64::MAX)`) · `:6358`
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"`종료_커밋_시각` 은 `git` 이 어떤 이유로든(비-git 트리·PATH·권한·`--` 경로 오타) 실패하면 조용히 `None` 을 내고, 그러면 **모든 회차가 `i64::MAX` 로 묶여 판정이 통째로 사전순 최대로 되돌아간다** — `#126` 이 *"「최근」의 자가 아니다"* 라 못 박은 바로 그 규칙으로."*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:39`⟩: **채택.** *"`grep -rn "종료_커밋_시각" xtask crates` 를 내가 다시 돌렸다 — 정의(`xtask/src/main.rs:6434`)와 호출 둘(`:6350`·`:6356`)뿐이고 시험 참조가 **0** 이다 … **`B1`·`B6` 을 깨지는 않는다**"*

### 판 1 · R4 — 초안의 ★(*"이 고침이 실제로 다른 회차를 집는다"*)가 과장이다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `observations/ledger-pair-before.txt` ↔ `observations/ledger-pair-after.txt` · 반이 돌린 `for d in .palimpsest/rounds/*/; do git log -1 --format=%ct -- "$d/report.md"; done` 전량(최대 = 1789040144 = cross-file-references)
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"실측한 회차 18 개의 `%ct` 전량에서 **커밋 시각 최대와 사전순 최대가 같은 회차**(`2026-09-08-cross-file-references`)다 … 즉 이 저장소 이력에서 두 규칙이 **다른 회차를 집는 입력은 없다**"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:40`⟩: **채택.** *"**내가 직접 재현했다.** … 초안의 그 한 문장은 사실이 아니다 — 그리고 내 산출은 한 글자도 안 고치고 원장에 실리므로 이것은 「사실이 아닌 것을 사실로」의 씨앗이다. ⚠ **회차 산출물 자신은 결백하다** — `effect/126-delta.md:63-72` 는 *"뽑힌 회차가 같다"* 를 먼저 적고 … **압축한 것은 초안이다**"*

### 판 1 · R5 — 초안의 근거 3(*"「어떤 입력으로도」를 시험이 받친다"*)에서 둘째 입력이 공허하다

- 모집단: 저장소
- 유효성: 거짓 ⟨반은 「참」으로 냈다 · 합: *"부분만 참이고 결론이 틀렸다"*⟩
- 해악도: 거짓신호 ⟨반이 매긴 값 · 합: *"해악도 `거짓신호` 도 과대 평가라 미관으로도 안 싣는다"*⟩
- 어디가 걸리나: `crates/pal-cli/tests/narrative_read_only.rs:86-96` · `crates/pal-cli/src/narrative.rs:238-244` · `observations/red-129.txt`
- 출처 `정반합` · 처분 `기각` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `닫힘`
- 반론 원문: *"`읽기_경로는_의도_저장소를_안_불린다` 의 후반(`pal narrative` 로 세운 **뒤**)은, 그 시점에 모든 조각의 개체가 이미 있어 `민팅::안한다` 분기(`개체_없음 += 1; continue`)가 **한 번도 실행되지 않는다.**"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:49`⟩: **기각.** *"**부분만 참이고 결론이 틀렸다.** 반의 사실 주장(후반에서 `개체_없음` 분기가 안 돈다)은 맞다 … 그러나 ⑴ 그 시험의 **선언된 목적은 민팅 분기가 아니라 「읽기가 쓰지 않는다」**이고 … ⑵ 고친 분기 자체는 **첫 시험**(`:51`, `intent.redb` 없는 새 저장소)이 돌리며 … ⑶ 반이 든 *"`B1-a` 가 요구한 상태와 다르다"* 는 어긋났다 … 곧 「있을 때」 입력은 이미 재어져 있다."*

### 판 1 · R6 — 「정확」 버킷의 화면 문구도 사실이 아닐 수 있다

- 모집단: 저장소
- 유효성: 참
- 해악도: 미관 ⟨반·합 같다⟩
- 어디가 걸리나: `crates/pal-cli/src/ledger.rs:664` · `:337-350`(가르는 규칙) · `crates/pal-core/src/coord.rs:258-265`
- 출처 `정반합` · 처분 `확대` · 승격됨 `예` ⟨합: *"R1 과 같은 처분에 묶는다"*⟩ · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"`정확` 의 술어는 `ordinal == 0 && grade == Exact` 인데 화면은 *"이름으로 유일하고"* 라 찍는다. `ordinal == 0` 은 **유일**이 아니라 **그룹의 첫째**다"*
- 채택·기각과 근거 ⟨합 `1-synthesis.md:41`⟩: **채택.** *"R1 과 뿌리가 같다 … L2 언어에서 중복 그룹이 서면 `SymbolNode.identity` 가 **데이터로** `Exact` 가 되어 … 화면 문구보다 더 나쁜 거짓이 대장에 실린다. 지금 이 저장소에 그 실례가 없어 실해가 없다(`정확 6` · TypeScript 1 파일)"*

### 판 2 · O1 — 팔 ③ 이 「미도래」다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `.claude/skills/round/SKILL.md:523` · `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:123`·`:159` · `state.md:30`
- 출처 `정반합` · 처분 `전환` ⟨판정값을 반증→미측정으로 움직인 반론⟩ · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `닫힘`
- 반론 원문: *"규약이 게이트 문서를 *"「다 했다」를 기록하는 자리"* 로 못 박고, 회차가 `**㉡ 을 가장 먼저 닫고, 게이트·종료 문서는 그 뒤에 쓴다**` · `초록을 만들려고 종료 문서를 앞당겨 쓰지 않는다` 로 그 순서를 스스로 잠갔다. 초안은 이 이음매를 「가장 약하다」고 이름만 적고 **이 좌표들을 하나도 안 댔다**"*
- 채택·기각과 근거 ⟨합 `2-synthesis.md:18`⟩: **채택.** *"**직접 열어 확인했고, 반이 댄 것보다 강한 좌표가 있다.** `dialectic/2-design.md:265-266` 이 … **이 판이 안 다루는 것**에 게이트 집필을 명시적으로 넣었다 … ⚠ 반이 댄 `SKILL.md:523` 은 **철회 절의 문장**이라 단독으로는 약하다 — 그 좌표는 빼고 위 넷으로 세운다"*

### 판 2 · O2 — 게이트가 `A5-c` 를 `미측정` 칸에 넣은 것은 기계가 강제한 유일한 값이다

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `xtask/src/main.rs:6307-6314` · `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:191` · `docs/gates/effect-confirmation.md:27`
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `닫힘`
- 반론 원문: *"`xtask` 가 *"상자를 채웠다 = 판정이 났다. 안 채웠다 = 미측정"* 으로 양방향을 걸고, `intent.md:191` 의 `A5-c` 상자는 `- [ ]` 다. 초안이 「부재의 증거」로 읽은 칸은 판정이 나기 전까지 다른 값이 될 수 없다"*
- 채택·기각과 근거 ⟨합 `2-synthesis.md:19`⟩: **채택.** *"`let 채워야 = 게 != "미측정";` 가 게이트 값과 `intent.md` 상자를 **양방향**으로 묶는다 … 그러므로 초안이 「부재의 증거」로 읽은 `docs/gates/effect-confirmation.md:27` 은 **아무것도 증언하지 않는다**"*

### 판 2 · O3 — 규약이 못 박은 게이트 네 절 어디에도 「그 사실」이 갈 칸이 없다

- 모집단: 규약
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `.claude/skills/round/SKILL.md:734-741`·`:767` · `docs/gates/effect-confirmation.md:5`·`:9-10`
- 출처 `정반합` · 처분 `축소` ⟨앞몫 채택 · 「문면 결함 → 승격 후보」 몫 기각⟩ · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `닫힘`
- 반론 원문: *"그러므로 초안의 *"어느 절도 안 적는다"*(`:98`)는 **위반의 증거가 아니라 조건 문면이 갈 자리를 안 지정했다는 증거**다. ⚠ 자인 — `SKILL.md:767` 이 「근거 표를 위에 더한다」를 허용하므로 원리상 불가능은 아니다. 그래서 이것은 반증이 아니라 **문면 결함(승격 후보)** 이다"*
- 채택·기각과 근거 ⟨합 `2-synthesis.md:20`·`:35`⟩: **앞몫 채택** — *"**조건 문면이 갈 자리를 안 지정한 것은 사실이다**"*. **뒷몫(승격 후보) 기각** — *"**칸이 실재한다.** `SKILL.md:767` … 게다가 `2-design.md:245-246` 이 … **근거 기입을 이미 요구한다.** 조건 문면을 안 고쳐도 「그 사실」은 그 근거 표에 들어간다. **승격은 불필요하고, 조건 문면 개정은 `state.md:38-40` 에 따라 소유자 재승인을 부르므로 근거 없이 부르면 안 된다**"*

### 판 2 · O4 — 팔 ① 이 스스로 「용법 전수」라 부르는데 전수가 아니다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/preflight.md:94-230` 대 `dialectic/2-thesis.md:27-46`
- 출처 `정반합` · 처분 `확대` ⟨합 확장 판단: *"이번에 늘린다"*⟩ · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"preflight 의 grep 산출은 자기참조를 뺀 적중이 **93 줄**인데, 초안의 ⓐ~ⓔ 가 짚은 좌표는 **36 개**다. 빠진 자리 예: `dialectic/{1,2,3,4}-design.md:23`·`:45`·`:66`·`:78`·`:104`·`:131`·`:260`(28) · `conditions-audit/r1-raw.md:179` · `r2-raw.md:44`·`:200` · `premortem/r2-raw.md:31`·`:101`·`:151` · `r3-raw.md:77`·`:147`·`:187` · `intent.md:182`·`:188`·`:192`·`:338` · `observations/red.md:4` · `issue-66-now.txt:51` · `premortem-artifacts/README.md:21` · `effect/green-129.txt:3` · `129-readnote.md:26` · `judge-prompt.md:185`·`:574`·`:649`·`:1287`·`:1733`"*
- 채택·기각과 근거 ⟨합 `2-synthesis.md:21`⟩: **채택.** *"직접 셌다 … ★ **팔 ③ 이 미도래로 내려가면 팔 ① 이 판정을 지는 유일한 팔이 되므로, 이 공백의 무게가 초안이 매긴 것보다 커진다**"*

### 판 2 · O5 — 초안이 ⓐ(*"「쓴 것」이 아니다"*)에 넣은 `nodes_of.txt:44`·`nodes_of2.txt:44` 는 실제로 하중을 진다 · 기준이 비대칭이다

- 모집단: 회차기록
- 유효성: 참(비대칭 몫) / 거짓(하중 몫) ⟨합: *"초안이 적은 *"이 주장은 하중을 진다"* 쪽이 과장이다"*⟩
- 해악도: **반 `거짓신호` ↔ 합 `미관` — 갈린다**
- 어디가 걸리나: `observations/premortem-artifacts/README.md:21` · `nodes_of.txt:44` · `nodes_of2.txt:44` · `git log -1 868c27a` · `dialectic/2-thesis.md:30`·`:43`
- 출처 `정반합` · 처분 `축소` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `닫힘`
- 반론 원문: *"그 둘의 표지가 *"**같은 HEAD·같은 워킹트리**에서 두 번 돌린 산출이 바이트로 동일하다"* 이고, 회차 커밋이 그 둘을 *"`A2-b`(재실행 대조)를 세운 근거"* 라 적는다. 초안은 `effect/rerun-*.txt:3` 의 **같은 문장 형식**은 ⓓ(「쓴 것일 수 있다」)로 올렸다 — **기준이 비대칭이다**"*
- 채택·기각과 근거 ⟨합 `2-synthesis.md:22`·`:36`⟩: **비대칭 몫 채택** — *"비대칭은 실재한다"*. **하중 몫 기각** — *"**열어 보니 하중은 딴 데 있다.** `effect/rerun-79.txt:6-7` · `rerun-126.txt:11-12` · `rerun-129.txt:5-6` 이 **`본문 sha256` 과 `재실행 sha256` 의 일치**를 싣고 … `A2-b`(`intent.md:182`)가 요구한 *"`diff` 가 바이트로 동일"* 은 **그 해시 쌍이 지지, 「깨끗」이라는 머리 문장이 아니다.**"*

### 판 2 · O6 — 설계문 §반증 ⑧ 을 한 문서 안에서 갈라 적용했다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `dialectic/2-thesis.md:11`·`:27`·`:91`·`:135-136` · `dialectic/2-design.md:245-246` · `git log --oneline -3`
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `닫힘`
- 반론 원문: *"초안은 `preflight.md:232-271` 을 *"핀 뒤에 생긴 바이트"* 라며 증거에서 뺐는데(`:91`), `preflight.md` 는 **파일 전체가** 핀(`05f07ea`) 뒤 커밋(`58d21c9`)에서 처음 생겼다 — 초안 자신이 `:11` 에서 그렇게 적었다. 같은 잣대면 팔 ① 의 전수(`:27`)와 「확인 못 한 것」 5·6 의 유일한 근거(`:135-136`)도 함께 빠진다"*
- 채택·기각과 근거 ⟨합 `2-synthesis.md:23`⟩: **채택.** *"★ **다만 해소 방향이 반대다** — `2-design.md:73-74` 가 preflight 를 *"메인이 발동 전에 돌려 정과 반에게 같은 바이트로 준다"* 로 **설계된 입력**으로 못 박았으므로, §반증 ⑧ 의 *"판정 중 생긴 파일 인용"* 에 안 걸린다. **뺀 쪽이 틀렸다**"*

### 판 2 · O7 — 금지역 인용이 문면보다 넓다(해악도 부풀림)

- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:88`·`:191` 대 `dialectic/2-thesis.md:113`
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `닫힘`
- 반론 원문: *"초안 `:113` 은 *"항등식임을 알고도 게이트에 안 적은 상태로 닫는 것은 금지역에 닿는다"* 라 적는데, 그 자리가 적은 것은 *"**효과 측정이** 항등식이면 「측정이 죽은 가지」"* 다 … 「게이트에 안 적음」을 금지역으로 올리는 다리가 없다"*
- 채택·기각과 근거 ⟨합 `2-synthesis.md:24`⟩: **채택.** *"`intent.md:90` 원문은 *"**효과 측정이** 항등식이면 「측정이 죽은 가지」"* 다. 그리고 `intent.md:310-314` 가 `MS-06` 을 다루며 *"**`A5-c` 의 「워킹트리 일치」와 같은 부류** — 제품의 표시 결함이고 이 회차는 관측만 한다"* 로 **이 부류의 처분을 이미 확정**했다"*

### 판 2 · O8 — 팔 ③ 의 유일한 측정 행위가 *"전문(42 줄) 읽었다"* 인데 그 파일은 41 줄이다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관 ⟨반·합 같다⟩
- 어디가 걸리나: `docs/gates/effect-confirmation.md`(`wc -l` = 41) 대 `dialectic/2-thesis.md:19`·`:95`
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"팔 ③ 의 **유일한 측정 행위**가 *"전문(42 줄) 읽었다"* 인데 그 파일은 **41 줄**이다(개행으로 끝난다). 두 번 적었다"*
- 채택·기각과 근거 ⟨합 `2-synthesis.md:25`⟩: **채택.** *"`wc -l docs/gates/effect-confirmation.md` 를 돌렸고 `cat -n` 전문도 41 에서 끝난다. 초안이 두 자리에서 틀렸다"*

### 판 2 · O9 — ⓐ·ⓒ 의 셈이 자기 목록과 안 맞는다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관 ⟨반·합 같다⟩
- 어디가 걸리나: `dialectic/2-thesis.md:29-30`·`:37-38`
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"ⓐ 를 *"17 자리"* 라 적고 **20 개**를 나열했고(touch·rerun 6 + judge-prompt 4 + premortem-artifacts 10), ⓒ 를 *"(7)"* 이라 적고 **8 개**를 나열했다"*
- 채택·기각과 근거 ⟨합 `2-synthesis.md:26`⟩: **채택.** *"초안 `:29-30`·`:37-38` 의 나열을 세어 대조했고 라벨과 갈린다"*

### 판 2 · O10 — ⓓ 가 셋이 아니다 — `A2-b` 의 전제를 건 조건 문면 좌표를 안 짚었다

- 모집단: 원의도
- 유효성: 참
- 해악도: 미관 ⟨반·합 같다⟩
- 어디가 걸리나: `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:182` 대 `dialectic/2-thesis.md:43`
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"초안이 *"`A2-b` 가 「같은 워킹트리」를 전제로 걸므로 이 주장은 하중을 진다"* 라 적으면서, 그 전제를 실제로 건 **조건 문면 자체**를 안 짚었다"*
- 채택·기각과 근거 ⟨합 `2-synthesis.md:27`⟩: **채택.** *"`intent.md:182` 가 *"같은 HEAD·같은 워킹트리에서"* 를 박아 둔 자리임을 확인했다"*

### 판 3 · R1 — 초안의 가장 무거운 문단(손잡이 1)이 든 좌표 셋이 전부 한 줄씩 어긋난다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `observations/premortem-artifacts/check_ledger_pair.txt:3`·`:10` · `observations/premortem-artifacts/README.md:23` ↔ `dialectic/3-thesis.md:84`
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"`check_ledger_pair.txt:2` 는 심볼 ID 줄이고 `fun … identity ordinal` 은 `:3` 이며, `:9` 는 `■ 이 심볼이 하는 것` 이고 `호출자 1 · 피호출자 12` 는 `:10` 이다. `README.md:24` 는 `identity_ceiling.txt` 행이고 `호출자 1` 을 적은 것은 `:23` 이다"*
- 채택·기각과 근거 ⟨합 `3-synthesis.md:29`⟩: **채택.** *"직접 열어 확인했다 … 반이 옳다"*. ⚠ 합의 지시: *"이 초안이 원장에 그대로 실릴 것이라면 **고치지 말고 정오표를 옆에 붙여라** — 판정문의 사후 수정은 증거 위조다"*(`3-synthesis.md:48`)

### 판 3 · R2 — ㉡ⓑ 도 오염돼 있고 readnote 가 같은 파일을 ⓒ 의 오염원으로만 공시했다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역 ⟨반·합 같다⟩
- 어디가 걸리나: `observations/premortem-artifacts/clone-before.txt:3` · `effect/126-readnote.md:67` ↔ `dialectic/3-thesis.md:85`
- 출처 `정반합` · 처분 `확대` · 승격됨 `예` · 조건변경 `강화` ⟨합: *"고치면 **재는 양이 늘어난다**(완화가 아니다)"*⟩ · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"`clone-before.txt:3` 과 `check_ledger_pair.txt:3` 이 `identity ordinal` 을 글자 그대로 담는다. 더 아픈 것은 `effect/126-readnote.md:67` 이 **같은 파일을 ⓒ 의 오염원으로만 공시**하고 **같은 줄이 담은 ⓑ 는 안 적었다**는 것이다. 배제 집합을 「메인이 이미 안 것」으로 읽으면 분자는 초안이 적은 1 이 아니라 **0** 이다"*
- 채택·기각과 근거 ⟨합 `3-synthesis.md:30`⟩: **채택.** *"같은 줄이 낳은 두 사실 중 하나만 공시한 비대칭이 실재한다. ⚠ 반이 **덜 말했다** — ⓐ·ⓒ 는 파일에만 있는 것이 아니라 `premortem/r2-raw.md:51`·`:31` 에 **메인의 산문으로** 봉인 여섯 시간 전에 적혀 있다"*

### 판 3 · R3 — 형식 미달은 ㉡ⓑ 하나가 아니라 일곱 중 여섯이다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역 ⟨반·합 같다⟩
- 어디가 걸리나: `effect/judge-prompt.md:28-29` · `effect/126-judge-1.md:32-33`·`:39-40` ↔ `dialectic/3-thesis.md:16`·`:26`
- 출처 `정반합` · 처분 `확대` · 승격됨 `예` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"판정자 반환 형식(`effect/judge-prompt.md:28-29`)이 *"있다(**인용**) | 없다"* 라 **「없다」쪽엔 인용을 아예 안 요구한다** — 유효 판정은 구조상 인용을 못 낳는다 … 초안의 *"일곱 전부에 규정 형식의 내용 시험이 적혔다"* 는 **다섯 칸이 채워졌다**를 **두 문서 인용으로 보였다**로 바꿔 부른 것이다"*
- 채택·기각과 근거 ⟨합 `3-synthesis.md:31`⟩: **채택.** *"초안 스스로 `dialectic/3-thesis.md:43` 에서 *"성립은 내 독립 확인(위 grep)으로 선다"* 라 적어 계기가 아니라 판정자가 아닌 자가 메웠음을 자백한다"*

### 판 3 · R4 — `C2-b` 는 증거가 아니라 메인의 자르기를 잰다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역 ⟨반·합 같다⟩
- 어디가 걸리나: `effect/126-judge-1.md:30`·`:37`·`:44` · `effect/judge-prompt.md:413-418`
- 출처 `정반합` · 처분 `확대` ⟨합: *"좁혀서 채택한다"*⟩ · 승격됨 `예` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"`touch/126.txt:17` **한 줄**이 `effect/126-judge-1.md:30`(ⓑ 유효)·`:37`(ⓒ 유효)·`:44`(ⓓ 무효) 로 **인용한 줄이 세 번 다 글자까지 같은데 판정이 갈린다.** 가른 것은 그 줄이 아니라 메인이 `effect/judge-prompt.md:413-418` 에 **미리 써 준 「주장하는 사실」**이다"*
- 채택·기각과 근거 ⟨합 `3-synthesis.md:32`⟩: **좁혀서 채택.** *"갈린다는 것 자체는 `C2-a` 가 의도한 음성 대조가 **먹힌** 증거이지 결함이 아니다. 살아남는 것은 *"분자를 혼자 지는 자리의 유효성을 메인의 자르기가 정한다"* 이고 … 그 절은 사전 등록↔변경만 덮고 **사전 등록↔판정자 프롬프트의 문항 작성**은 안 덮는다. 그러므로 선언된 범위 밖으로 처분할 수 없다"*

### 판 3 · R5 — §5.8 의 「전수」는 내용이 빠진 게 아니라 시간상 불가능하다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역 ⟨반·합 같다⟩
- 어디가 걸리나: `observations/red.md:231` · `git log --format='%h %ci' -- observations/red.md` → `7b4ddaa 2026-09-11 10:43:43` ↔ `dialectic/3-thesis.md:80`
- 출처 `정반합` · 처분 `확대` · 승격됨 `예` · 조건변경 `강화` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"`observations/red.md:231` 이 스스로 *"사전부검 **라운드 1** 이"* 라 적고, 그 파일은 `7b4ddaa`(2026-09-11 10:43:43)에 한 번 커밋된 뒤 **고쳐진 적이 없다** … 배제 오라클이 **오염의 3 분의 1 만 덮도록 구성돼 있다**"*
- 채택·기각과 근거 ⟨합 `3-synthesis.md:33`⟩: **채택.** *"배제 오라클이 **구성상 전수가 아니다.** 초안은 이것을 *"새는 사실 하나"*(손잡이 1)로 다뤘는데 실제는 오라클의 정의역 결함이다"*

### 판 3 · R6 — 초안의 세는 규칙이 ㉡ 과 ㉠ 에 비대칭으로 걸렸다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `effect/79-delta.md:37-40` ↔ `effect/126-delta.md:26` ↔ `dialectic/3-thesis.md:57`·`:87`
- 출처 `정반합` · 처분 `확대` · 승격됨 `예` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"같은 자를 대면 합은 3 이거나 1 이고, 초안이 적은 **2 는 어느 읽기에서도 안 나온다**"*
- 채택·기각과 근거 ⟨합 `3-synthesis.md:34`⟩: **채택.** *"같은 범주(근거가 바뀌었다)가 한쪽은 표 밖·한쪽은 표 안이다. 초안의 수는 어느 일관된 자에서도 안 나온다"*

### 판 3 · R7 — 분자 둘의 「결과 좌표」가 둘 다 「안 바뀐 자리」다

- 모집단: 회차기록
- 유효성: 참
- 해악도: **반 `거짓신호` ↔ 합 `실패` — 갈린다** ⟨합: *"⚠ **해악도를 반이 매긴 것보다 올린다**"*⟩
- 어디가 걸리나: `effect/126-delta.md:27` · `plan/126-pre.md:82` ↔ `dialectic/3-thesis.md:62`
- 출처 `정반합` · 처분 `확대` · 승격됨 `예` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"`effect/126-delta.md:27` 이 ㉱ 의 결과를 *"`:643`(**안 건드린** 자리)"* 라 스스로 적고, ㉰ 의 `:6407` 은 `plan/126-pre.md:82` 가 봉인 전에 *"`check_ledger_pair` **뒤** (새 자리)"* 로 이미 지정한 바로 그 자리다 … 차이의 결과 칸에서 무변화를 확인하는 것은 확증이 아니다"*
- 채택·기각과 근거 ⟨합 `3-synthesis.md:35`⟩: **채택.** *"이것은 보고 문구의 흠이 아니라 **합격선 본체가 재려는 것**(`intent.md:11` *"달라진 변경"*)이 산출물 층위에 하나도 안 섰을 가능성이고, `C2` 를 미측정으로 내리는 두 근거 중 하나다"*

### 판 3 · R8 — 초안이 음성 대조가 「실행됐다」고 기댄 근거가 같은 회차에서 반증돼 있다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `effect/126-judge-2.md:51-52` ↔ `effect/79-judge-1.md:70-72` · `effect/129-judge-2.md:31` ↔ `dialectic/3-thesis.md:91`
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"`effect/126-judge-2.md:51-52` 는 되불러 준 수 넷이 같은 것을 *"두 인스턴스가 같은 바이트를 받았다는 **기계 대조**"* 라 적는데, `effect/79-judge-1.md:70-72` 가 같은 회차에서 *"판정자의 계수와 인용이 **근사**"*(③ 345↔337 · `+` 232↔233)임을 실측하고 `MS-10` 으로 싣는다"*
- 채택·기각과 근거 ⟨합 `3-synthesis.md:36`⟩: **채택.** *"회차가 자기 문서에서 서로 반대를 말한다"*

### 판 3 · R9 — `C2-b` 가 요구한 「인용한 `touch/<이슈>.txt:N` 의 줄」에서 판정자가 `N` 을 검증한 적이 한 번도 없다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `effect/126-judge-1.md:23`·`:30`·`:37` · `effect/judge-prompt.md:413`·`:417` ↔ `dialectic/3-thesis.md:38` ⟨합은 `judge-prompt.md:409`·`:413`·`:417` 로 적었다⟩
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"초안이 찾은 ㉠ⓔ 의 오류는 **고립된 실수가 아니라 검증된 적 없는 모집단의 표본**이다 — 초안은 *"미끼라 분자에 안 들어간다"* 로 처분하고 모집단을 안 물었다"*
- 채택·기각과 근거 ⟨합 `3-synthesis.md:37`⟩: **채택.** *"`C2-b` 문면은 *"인용한 `touch/<이슈>.txt:N` 의 줄"* 이라 N 결박이 조건의 일부인데 계기가 그것을 안 쟀다"*

### 판 3 · R10 — 초안은 자기가 안 연 문서 때문에 여유의 크기를 못 잰 채 통과를 냈다

- 모집단: 회차기록
- 유효성: 참 ⟨합은 사실 관계를 부정하지 않았다 — 중복과 설계 격리를 근거로 기각했다⟩
- 해악도: 거짓신호 ⟨반이 매긴 값 · 합은 별도 값을 안 냈다⟩
- 어디가 걸리나: `premortem/r2-raw.md:58` ↔ `dialectic/3-thesis.md:101`
- 출처 `정반합` · 처분 `기각` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `닫힘`
- 반론 원문: *"`premortem/r2-raw.md:58` 이 이미 *"`C2` 가 0 이면 차선책 1 이 발동해 「반증」으로 회차가 닫힌다 … **잘못된 반증은 되돌리기가 가장 비싸다**"* 라 적어 두었다. R2 와 겹치면 여유 2 가 아니라 **여유 0 일 수 있는 판정**을 여유 2 로 보고한 것이 된다"*
- 채택·기각과 근거 ⟨합 `3-synthesis.md:43`⟩: **기각.** *"초안이 `## 차선책` 을 안 연 것은 **설계가 그렇게 시켰기 때문**이다 … 설계가 명시로 막은 것을 안 본 것은 초안의 결함이 아니다(그 격리가 초안의 값이었다). 남는 실질 — *"여유가 0 일 수 있다"* — 은 R2·R6 이 이미 근거와 함께 지고 있어 거기서 채택했다. 별도 반론으로 세우면 같은 것이 두 번 세어진다"*

### 판 4 · R1 — 초안의 ★뼈대(§5)가 사실이 아니다 — 「`호출자 1` 이 참이었나」는 이미 회차에 적혀 있다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역 ⟨반·합 같다⟩
- 어디가 걸리나: `effect/126-readnote.md:14` ↔ `dialectic/4-thesis.md:82`·`:92`
- 출처 `정반합` · 처분 `확대` ⟨합: *"이번에 늘린다(원장 등재)"*⟩ · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"`effect/126-readnote.md:14` 가 *"그 뒤 `grep -n "check_ledger_pair" xtask/src/main.rs` 로 확인했다 — `:643` 의 검사 등록 표 한 자리뿐이다"* 로 **명령·좌표·결론**을 적었고, 그것은 변경 커밋 `398d233` **전**에 커밋된 `9be7699` 다. 초안은 *"확인한 것은 delta 가 아니라 나다"*(`dialectic/4-thesis.md:92`) · *"기록이 저장소 어디에도 안 섰다"*(`:82`) 를 적는다"*
- 채택·기각과 근거 ⟨합 `4-synthesis.md:28`⟩: **채택.** *"**직접 열어 확인했다** … R1 이 스스로 건 반증 조건 둘이 다 닫힌다. 회차가 `intent.md:87-90` 에 등록한 **「사실이 아닌 것을 사실로」** 에 드는 형태다"*

### 판 4 · R2 — 초안은 readnote 셋을 한 번도 안 다뤘다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역 ⟨반·합 같다⟩
- 어디가 걸리나: `dialectic/4-thesis.md`(`readnote` 0 회) · `effect/79-readnote.md:20`·`effect/126-readnote.md:21`·`effect/129-readnote.md:33`
- 출처 `정반합` · 처분 `확대` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"셋은 「touch 가 말한 것이 참이었나」를 근거와 함께 적는다: `effect/79-readnote.md:20-41`(「호출자 3」이 **틀렸다** · `pal query symbol.callers` 산출 3 줄 · `defect.rs:322`) · `effect/126-readnote.md:21-33` … · `effect/129-readnote.md:33-35` … 「실질 2/11」은 **적어도 3** 이고 ㉢ 자리에는 **자리 전체에 걸린 답**이 있다"*
- 채택·기각과 근거 ⟨합 `4-synthesis.md:29`⟩: **채택.** *"`grep -c readnote dialectic/4-thesis.md` = 0 을 확인했다. 초안 스스로 `4-thesis.md:128` 에서 *"`C4` 문면은 기록 위치를 한정하지 않으므로"* 라 적었으므로 「delta 안만 봤다」는 변명이 자기 문장으로 막힌다"*

### 판 4 · R3 — 모집단 11 은 `C1` 을 잘라 인용해서 나왔다

- 모집단: 원의도
- 유효성: 참(앞절) / 거짓(뒷절 *"모집단은 3 이다"*) ⟨합: *"「3 이다」를 확정으로 받지 않고 「안 정해진다」로만 받는다"*⟩
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `intent.md:231` ↔ `dialectic/4-thesis.md:44` · `effect/126-delta.md:24-27` · `effect/79-delta.md:27-30` · `effect/129-delta.md:29-31`
- 출처 `정반합` · 처분 `축소` ⟨앞절 채택 · 뒷절 기각⟩ · 승격됨 `예` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"원문은 이어서 ***"차이마다 좌표 두 끝이 붙었다 — `touch/<이슈>.txt:N` 이 원인이고 `파일:줄` 이 결과다"*** 다 … 모집단은 **3**(㉡㉰·㉡㉱·㉠㉱)이고 그 셋은 R1·R2 로 「참이었나」가 **3/3** 이다"*
- 채택·기각과 근거 ⟨합 `4-synthesis.md:30`·`:43`⟩: **앞절 채택** — *"초안이 11 을 확정으로 적은 것은 근거 초과다"*. **뒷절 기각** — *"`intent.md:232` 의 `C2` 가 ***"그 차이 중** touch 산출에 귀속되는 것"*** 으로 부분집합을 따로 부른다 … 그러므로 `C1` 의 뒷절은 **모집단의 정의가 아니라 기입 형식의 요구**라는 읽기가 더 세게 선다 … **다만 그 읽기를 세우면 여덟 행이 `C1` 의 형식을 못 지킨 행이 되므로, 문면은 어느 쪽으로도 깨끗하게 안 선다.**"*

### 판 4 · R4 — 초안이 자기가 「해당 없음」이라 적은 행을 실패 분모에 넣었다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `dialectic/4-thesis.md:61-67` ↔ `:69` · `:109`
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"원인이 touch 가 아닌 행에는 「touch 가 말한 것」이라는 **대상 자체가 없으므로** 술어가 공허하다 — 초안이 §7 에서 스스로 금지한 「측정이 죽은 가지」와 같은 형태다"*
- 채택·기각과 근거 ⟨합 `4-synthesis.md:31`⟩: **채택.** *"이것은 초안이 `:109` 에서 스스로 *"공허참으로 통과시키면 … 「측정이 죽은 가지」"* 라 금지한 것의 **거울상**이다 — 공허한 축으로 **불통과**를 만들었다"*

### 판 4 · R5 — 「라벨」 요건은 초안이 새로 만든 것이다

- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `dialectic/4-thesis.md:20` ↔ `intent.md:238` · `dialectic/4-design.md:90-92`
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"판정문 `:20` 은 *"**라벨된** 기입도 0 건"* 으로 조건을 세우는데, `intent.md:238` 은 *"근거와 함께 적혔다"* 이고 `dialectic/4-design.md:90-91` 의 판정 대상도 ⓐ 붙었나 ⓑ 근거 ⓒ 어긋남뿐이다. 어디에도 라벨 형식 요구가 없다"*
- 채택·기각과 근거 ⟨합 `4-synthesis.md:32`⟩: **채택.** *"라벨 형식 요구는 어디에도 없다. 초안 `:20` 의 *"**라벨된** 기입도 0 건"* 이 산문 기입을 부당하게 탈락시킨다"*

### 판 4 · R6 — §3 의 「전수 확인」이 사실과 다르다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `git grep -n "나아졌다\|나빠졌다\|모른다" 58d21c9 -- .palimpsest/rounds/2026-09-11-effect-confirmation` (12 hit) ↔ `dialectic/4-thesis.md:73-78`·`:130`
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"*"훑으면 나오는 곳은 **넷뿐**"*(`:73-78`) 인데, 핀 `58d21c9` 에서 같은 grep 은 **12 곳**을 낸다 … ⚠ 초안은 **`4-design.md:92` 를 자기 `:130` 에서 인용한다** — 자기가 인용한 줄을 자기 전수 목록에서 뺐다"*
- 채택·기각과 근거 ⟨합 `4-synthesis.md:33`⟩: **채택.** *"**내가 다시 돌렸다** — 열 파일에 걸쳐 나온다 … 결론은 안 바뀌지만 **「전수로 쟀다」의 신뢰도가 깨진다**"* ⚠ 반은 **12 곳**, 합은 **열 파일**이라 적었다 — 세는 단위가 갈린다.

### 판 4 · R7 — 어긋남 판정을 증거를 안 보고 내렸고, 실제로 어긋난 자리가 있다

- 모집단: 회차기록
- 유효성: 참(앞을 안 보고 채웠다) / 거짓(*"정면으로 갈린다"* 몫) ⟨합: *"**원문을 열어 봤더니 갈리지 않는다**"*⟩
- 해악도: **반 `거짓신호` ↔ 합 `미관` — 갈린다**
- 어디가 걸리나: `effect/79-judge-1.md:21` ↔ `effect/79-readnote.md:30-41` · `effect/79-delta.md:94` ↔ `dialectic/4-thesis.md:64`·`:138`
- 출처 `정반합` · 처분 `축소` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"초안은 ㉠㉱ 에 *"어긋남 없음(내가 대조함)"* 이라 적었는데(`4-thesis.md:64`), 같은 회차의 `effect/79-judge-1.md:21` 은 같은 touch 줄을 두고 *"grep 의 「호출 자리 4」와 touch 의 「참조 엣지 기준 호출자 3」은 **잣대와 값이 다른 별개의 사실**"* 이라 읽는다 — … **정면으로 갈린다**"*
- 채택·기각과 근거 ⟨합 `4-synthesis.md:34`·`:44`⟩: **축소 채택** — *"초안은 `effect/*-judge-*.md` 를 **안 읽었다고 `:138` 에 스스로 적는다.** 안 본 자료로 ⓒ 를 초록칠한 것이다"*. **앞절 기각** — *"`effect/79-readnote.md:39-41` 이 스스로 *"⚠ **touch 가 거짓말을 한 것은 아니다**…"* 라 적는다. 판정자의 *"잣대와 값이 다른 별개의 사실"* 과 **같은 말**이다 … 이것은 반이 `high`(거짓신호)로 올린 것 중 **원문 오독**에 해당한다"*

### 판 4 · R8 — §4 가 회차의 순서를 거꾸로 읽었다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다 — 합이 **해악도 상향을 명시적으로 기각**했다⟩
- 어디가 걸리나: `intent.md:254`·`intent.md:123` · `state.md:103`·`:105` ↔ `dialectic/4-thesis.md:86`·`:119`
- 출처 `정반합` · 처분 `전환` ⟨합: *"이 반론이 **판정값을 움직인다** — 반증에서 미측정으로"*⟩ · 승격됨 `예` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"방향이 실릴 자리를 지정한 것은 `E2`(`intent.md:254`)이고 그 작업은 `state.md:105` 의 **6 번**(게이트·효과), 지금 도는 정반합은 **4 번**(`state.md:103`)이다 … **아직 안 쓸 차례인 자리의 공백**을 반증의 근거로 셌다"*
- 채택·기각과 근거 ⟨합 `4-synthesis.md:35`·`:45`⟩: **채택**(해악도 상향은 기각) — *"게이트 `## 효과` 는 **아직 안 쓸 차례**이고, 초안은 그 공백을 반증의 기둥 하나로 썼다"* / *"초안의 **추론**이 틀렸을 뿐, 회차가 금지역에 들어간 것은 아니다"*

### 판 4 · R9 — `effect/79-delta.md` 가 자기모순이고 초안이 그것을 안 적었다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합 같다⟩
- 어디가 걸리나: `effect/79-delta.md:23` ↔ `:30` (preflight 에도 그대로 실림 — `dialectic/preflight.md:307`·`:311`)
- 출처 `정반합` · 처분 `확대` ⟨합: *"이번에 늘린다(원장 등재 · `C1` 귀속)"* + `C1` 모집단 승격에 묶임⟩ · 승격됨 `예` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"머리글 `:23` 은 *"차이 — 넷이고 **어느 것도 touch 가 원인이 아니다**"* 인데 같은 표 `:30` 의 원인 칸은 **`touch/79.txt:23`** 이다 … 모집단을 「귀속 차이」로 셀지 말지가 바로 이 한 줄에 걸린다"*
- 채택·기각과 근거 ⟨합 `4-synthesis.md:36`⟩: **채택.** *"직접 확인했다. 모집단을 어떻게 셀지가 이 한 줄에 걸리는데 초안이 그것을 안 짚었다. `C1` 산출물의 결함이라 원장으로 간다"*

### 판 4 · R10 — 초안의 검산 인용이 한 자리 틀렸다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관 ⟨반·합 같다⟩
- 어디가 걸리나: `dialectic/preflight.md:298-311` ↔ `dialectic/4-thesis.md:48`
- 출처 `정반합` · 처분 `정정` · 승격됨 `아니오` · 조건변경 `없음` · 사전처분 `해당없음` · 상태 `열림`
- 반론 원문: *"*"`preflight.md:298-311` … 그 산출도 **11 줄**이다"*(`:48`). 그 범위는 **14 줄**이고 그중 3 줄은 `## 2. 차이` **절 머리글**이다(행은 11)"*
- 채택·기각과 근거 ⟨합 `4-synthesis.md:37`⟩: **채택.** *"열네 줄이고 그중 셋(`:298`·`:302`·`:307`)이 `## 2. 차이` 절 머리글이다. 초안의 산술은 맞고 낱말이 틀렸다"*

---

## 내가 기각한 것

**없음.** 나는 취합 자리이고 판정을 다시 하지 않았다 — 내가 기각한 반론은 하나도 **없음**이다. 위 36 항은 반(反) 네 표의 **행 전수**이고, 채택·기각은 전부 합(合)이 낸 것을 그대로 옮겼다.

**항으로 안 실은 것 — 넷.** 합(合)이 *"발견으로 원장에"* 라 지정했지만 **해악도를 안 매겼다.** 축 넷(모집단·유효성·해악도·어디가 걸리나)이 안 차므로 항으로 못 낸다. 내가 해악도를 채우면 그것이 곧 수 맞추기다 — 안 채운다. 메인이 해악도를 매기면 항이 넷 는다.

1. **`crates/pal-git/src/lib.rs:193-197` 의 문서 주석이 약속한 것을 코드가 구조적으로 못 준다** ⟨판 2 합 `2-synthesis.md:81`⟩ — *"주석은 *"대장을 계산하는 동안 사용자가 파일을 고칠 수 있고, 그러면 요약이 달라진다"* 를 **이 함수가 잡는 위험**으로 적는데, `--at` 없는 경로에서 비교 두 변이 같은 필드라 그 검사는 원리상 발화할 수 없다 … **`PM2-04`(더러운 워킹트리에 `일치`)와 다른 관측이다**"* · 처분 *"안 늘린다(조건) · 발견으로 원장에"*
2. **`pal ledger` 의 「워킹트리」 줄** ⟨판 2 합 `2-synthesis.md:79`⟩ — *"초안 `:89` 가 *"두 종류의 워킹트리 줄이 섞여 있는데 어느 산출도 안 가른다"* 라 적은 것은 옳다 — **관측으로 원장에 남긴다**"* · 좌표 `crates/pal-cli/src/ledger.rs:691-700` · `observations/identity-after.txt:8`
3. **`C2` 와 `C2-b` 의 판정값이 같이 움직였다** ⟨판 3 합 `3-synthesis.md:63`⟩ — 처분 *"승격(발견)"* · *"`dialectic/3-design.md:238` 이 이 사건을 미리 지목하고 *"두 조건이 실은 한 조건이고, 그러면 `C2-a` 의 ⓑ 축이 재는 것도 없다. **발견으로 올린다**"* 로 처분을 적어 두었다"*
4. **`effect/negative-C2.md` 부재** ⟨판 3 합 `3-synthesis.md:64`⟩ — *"⚠ 사실 관계만 적는다 — 회차 전체에 `negative-*` 이름의 파일이 **하나도 없고**(`find` 산출 빈 출력), 실질은 `effect/126-judge-1.md`(ⓐ 축)와 `effect/126-judge-2.md`(ⓑ 축 · 유효 0)에 남았다"*

**항으로 안 실은 사실 하나 더** — `dialectic/r1-raw.md` 가 **이미 저장소에 있다**(79889 바이트 · 685 줄 · 2026-09-12 01:20). 나는 그것을 읽지도 쓰지도 않았고 위 산출은 다섯 산출물 원문만으로 세웠다. 그 파일과 이 보고가 갈리면 어느 쪽이 원장에 실릴지는 메인이 정할 일이다.

---

## 계수 — 두 원천 대조

**반론 행 수 — 반(反)이 붙인 번호 ↔ 합(合)의 처분 표 행 수**

| 잰 것 | 반(反) | 합(合) | 같나 |
|---|---|---|---|
| 판 1 반론 | 6 (R1~R6) | 6 = 채택 5 + 기각 1 | 같다 |
| 판 2 반론 | 10 (O1~O10) | **12** = 채택 10 + 기각 2 | **갈린다** — 기각 2 행은 `O3 의 「문면 결함 → 승격 후보」 몫` · `O5 의 하중 주장`으로, 같은 id 가 채택 표와 기각 표에 **둘 다** 걸려 있다 |
| 판 3 반론 | 10 (R1~R10) | 10 = 채택 9 + 기각 1 | 같다 |
| 판 4 반론 | 10 (R1~R10) | **13** = 채택 10 + 기각 3 | **갈린다** — 기각 3 행은 `R3 의 뒷절` · `R7 의 앞절` · `R8 의 해악도 상향`으로 몫 기각이다 |
| **합계** | **36** | **41 행** (채택 34 + 기각 7) | **갈린다** — 몫 기각 5 개가 행을 늘렸다. 반론 **id** 로 세면 36 으로 같다 |

**채택·기각**

| 잰 것 | 수 | 어디서 나왔나 |
|---|---|---|
| 제기된 반론 | 36 | 반 네 표의 행 |
| 전부 또는 좁혀 채택 | 34 | 합 네 채택 표 |
| 전부 기각 | 2 | 판 1 R5 · 판 3 R10 |
| 몫만 기각(본체는 채택) | 5 몫 | 판 2 O3·O5 · 판 4 R3·R7·R8 |
| 헛것 비율(전부 기각 / 제기) | **2/36 = 5.6%** | 위 두 줄 |

**해악도 — 반(反)이 스스로 매긴 것 ↔ 합(合)이 낸 것**

| 판 | 반(反) 매김(제기 전수) | 합(合) 살아남음 | 갈린 자리 |
|---|---|---|---|
| 1 | 금지역 1 · 실패 0 · 거짓신호 4 · 미관 1 (=6) | 금지역 1 · 실패 0 · 거짓신호 3 · 미관 1 (=5) | R5 가 기각돼 빠졌다 |
| 2 | 금지역 0 · 실패 0 · 거짓신호 7 · 미관 3 (=10) | 금지역 0 · 실패 0 · 거짓신호 6 · 미관 4 (=10) | **O5 거짓신호 → 미관** |
| 3 | 금지역 4 · 실패 0 · 거짓신호 6 · 미관 0 (=10) | 금지역 4 · 실패 1 · 거짓신호 4 · 미관 0 (=9) | **R7 거짓신호 → 실패**(합이 *"해악도를 반이 매긴 것보다 올린다"* 라 적었다) · R10 기각 |
| 4 | 금지역 2 · 실패 0 · 거짓신호 7 · 미관 1 (=10) | 금지역 2 · 실패 0 · 거짓신호 6 · 미관 2 (=10) | **R7 거짓신호 → 미관** · R8 상향 요구를 합이 기각 |
| **합계** | 금지역 7 · 실패 0 · 거짓신호 24 · 미관 5 (=36) | **금지역 7 · 실패 1 · 거짓신호 19 · 미관 7 (=34)** | 세 자리에서 갈렸다 |

**합(合) ↔ 종료 판단(referee) 대조** — referee 가 받은 세 칸 분포와 합의 채택 표를 대었다.

| 판 | 합의 채택 표 | referee 가 적은 분포 | 같나 |
|---|---|---|---|
| 1 | 금 1 · 실 0 · 거 3 · 미 1 (=5) | 금 1 · 실 0 · 거 3 · 미 1 (=5) | 같다 |
| 2 | 금 0 · 실 0 · 거 6 · 미 4 (=10) | 금 0 · 실 0 · 거 6 · 미 4 (=10) | 같다 |
| 3 | 금 4 · 실 1 · 거 4 · 미 0 (=9) | 금 4 · 실 1 · 거 4 · 미 0 (=9) | 같다 |
| 4 | 금 2 · 실 0 · 거 6 · 미 2 (=10) | 금 2 · 실 0 · 거 6 · 미 2 (=10) | 같다 |

**이 보고가 내는 항의 수 = 36.** `### ` 로 시작하는 줄도 36 이다(코드펜스 없음). 그것이 원장에 실릴 레코드 수다.

---

## 합(合)이 낸 확장 판단 — 그대로 옮긴다

**판 1** ⟨`1-synthesis.md:72-79`⟩

| 나온 것 | 판단 | 사유 — *"원 의도의 완결에 필요한가"* |
|---|---|---|
| R1-a **거짓 문장 제거** — `ledger.rs:663`·`:307`·`:323` 의 *"순서 위험은 없(고)다"* 와 `:876` 의 단언 메시지에서 순서 위험 주장을 뺀다 | **이번에 늘린다** | **필요하다.** 원 의도는 「재는 자리 셋에서 효과를 잰다」이고, 그 산출물이 **거짓을 사실로 찍는 채로는 회차가 닫히지 않는다**(`intent.md:87-88` 금지역). 새 측면이 아니라 이 회차가 만든 것의 뒷정리다. 기계적이고 수를 안 움직인다 |
| R1-b **셈법 변경** — 그룹 크기를 보고 첫 선언도 `순서에_취약` 으로 센다(`nodes_of` 2 패스) | **승격** | **새 측면이다.** 값이 `순서에 취약 10` 에서 움직이고, 그러면 `B5`·`B5-a` 가 사전 등록한 분모 해석과 `#79` 의 「그 수」 정의가 함께 바뀐다. ⚠ **조건 문면을 내가 고치지 않는다** — 소유자가 정할 자리다 |
| R6 「정확」 문구 + `identity_ceiling` 이 그룹 중복을 못 보는 것 | **R1 과 같은 처분에 묶는다** (문구는 R1-a, 의미는 R1-b) | 뿌리가 하나인데 따로 처분하면 한쪽만 고쳐 놓고 닫힌다. ⚠ 단 `identity_ceiling` 의 의미 자체는 `0c9b83f`(S2) 산이라 **이 회차가 만든 것이 아니다** — 그 구별을 처분에 적어야 귀속이 안 틀어진다 |
| R2 자기 저장소 수치 4 배 어긋남(44 → 10) | **안 늘린다 · 원장 등재** | `B5-a` 가 *"`#79` 본문의 464·7,156 은 안 쓴다"* 를 봉인 전에 적고 분모를 스스로 정의했으므로(`plan/79-pre.md:94-115`) 원 의도의 완결에 필요하지 않다. 다만 **조정 없이 남기면 다음 회차가 「R-16 이 거의 해소됐다」로 오독한다** — 발견으로 싣는다 |
| R3 `종료_커밋_시각` 무시험 | **안 늘린다 · 원장 등재** | `#126` 도 `B6` 도 이 함수의 시험을 요구하지 않고(`B6` 이 요구한 「커밋 0 인 회차의 처분」은 이미 재어진다), 원 의도는 「효과 측정」이지 커버리지 확장이 아니다 |
| R4 초안의 ★ 과장 | **이번에 늘린다** (코드 아님 — 초안 문장 교정) | 내 산출과 초안은 **한 글자도 안 고치고 원장으로 간다.** 거짓 문장을 실은 채 옮기면 그것이 곧 이 회차의 금지역이다 |

**판 2** ⟨`2-synthesis.md:76-82`⟩

| 나온 것 | 판단 | 사유 |
|---|---|---|
| O4 — 팔 ① 전수 미완 | **이번에 늘린다** | 팔 ③ 이 미도래로 내려가 팔 ① 이 판정을 지는 축이 됐다. 원 의도(*"증거로 안 쓴다"*)의 **완결에 필요한 것**이지 새 측면이 아니다 |
| ⓓ2 — `touch/79.txt:9`·`touch/129.txt:11` 의 「워킹트리 깨끗」 출처 미기재 | **이번에 늘린다** | `touch/126.txt:9` 가 같은 회차에서 출처를 명기한 **선례**를 이미 세웠다. 기입 균일화는 새 측정이 아니라 기록 정리다. 새 실행은 필요 없다(출처가 안 되살려지면 「되살릴 수 없음」을 적는 것이 곧 답이다) |
| O3 — 게이트에 「그 사실」이 갈 칸 | **안 늘린다 · 승격 아님** | `SKILL.md:767` 이 근거 표 추가를 허용하고 `2-design.md:245-246` 이 그것을 이미 요구한다. 조건 문면 개정은 `state.md:38-40` 상 소유자 재승인을 부르는데, 부를 근거가 없다 |
| `pal ledger` 의 「워킹트리」 줄(`crates/pal-cli/src/ledger.rs:691-700` · `observations/identity-after.txt:8` 이 `다른 파일 2개`) | **안 늘린다** | 조건 문면이 *"**`pal touch` 의** 「워킹트리 일치」 줄"* 로 표면을 명시했다. 넓히면 **새로운 측면**이고 조건을 사후에 키우는 것이다. 다만 초안 `:89` 가 *"두 종류의 워킹트리 줄이 섞여 있는데 어느 산출도 안 가른다"* 라 적은 것은 옳다 — **관측으로 원장에 남긴다** |
| 읽기 B(명제의 참) | **안 늘린다** | `2-design.md:109-110` 이 *"`PM2-04` 는 앞쪽을 관측했다"* 로 읽기 A 를 골라 놨다 |
| **★ 새 발견 — `crates/pal-git/src/lib.rs:193-197` 의 문서 주석이 약속한 것을 코드가 구조적으로 못 준다** | **안 늘린다(조건) · 발견으로 원장에** | 직접 열어 확인했다 … **`PM2-04`(더러운 워킹트리에 `일치`)와 다른 관측이다** — 그쪽은 화면 문자열, 이쪽은 **주석이 문서화한 보증의 부재**다. 회차 처분은 `intent.md:310-314`·`2-design.md:260-262` 에 따라 **관측만** |
| 초안의 §반증 ⑧ 적용 오류(O6) | **안 늘린다 · 이 판은 유효하다** | `2-design.md:73-74` 가 preflight 를 설계된 공유 입력으로 못 박으므로 *"판정 중 생긴 파일 인용"* 에 안 걸린다. **§반증 ⑧ 은 발화하지 않았고 판 2 를 무효로 돌리지 않는다** — 메인이 이것을 근거로 판을 다시 돌리지 않도록 적어 둔다 |

**판 3** ⟨`3-synthesis.md:59-66`⟩

| 토론에서 나온 것 | 처분 | 사유 — *"원 의도의 완결에 필요한가"* |
|---|---|---|
| **`C2-b` 의 배제 집합이 「봉인 전 메인 손에 있던 것」을 안 덮는다**(R5 + `premortem/r2-raw.md:51`·`:31`) | **승격** | 원 의도는 *"touch 가 **처음** 말해 준 것"* 이다. 배제 집합이 문면상 두 문서로 못 박혀 있어 메인이 자기 산문으로 먼저 적은 사실이 통과한다. 조건 문면이 의도를 못 잰다 — 고치면 **재는 양이 늘어난다**(완화가 아니다). 그래도 착수에 잠긴 조건이라 메인이 혼자 못 바꾼다 |
| **`C2-b` 의 「두 문서의 인용」을 계기가 원리상 못 낳는다**(R3) | **승격** | 조건과 그 조건을 재는 도구가 어긋났다. 이번 회차 안에서 판정자를 다시 띄워 메우면 `C3-a`(*"띄운 인스턴스 전수"*)와 `A6` 계열의 「좋은 것만 남기기」 금지에 정면으로 걸린다. 소유자가 정할 일이다 |
| **귀속 사실의 자르기를 메인이 판정자 프롬프트에 미리 써 준다**(R4) | **승격** | `intent.md` `## 범위 밖` 이 선언한 편향은 사전 등록↔변경의 같은 손이고, 이것은 **사전 등록↔판정 문항**의 같은 손이다. 선언된 범위 밖이 안 덮으므로 회차 중에 조용히 범위 밖으로 밀 수 없다 |
| **「근거만 바뀐 것」을 차이로 세는가 — 셈 규칙이 봉인 안에 없다**(R6·R7) | **승격** | 원 의도의 합격선(*"달라진 변경 1 건 이상"*)이 세는 대상을 못 박아야 완결된다. ⚠ 메인이 지금 규칙을 **사후에** 정하면 그것은 정정이 아니라 판정을 착지시키는 것이다 — `C5` 가 막으려던 「집합을 사후에 정하기」와 같은 형태다 |
| **`C2` 와 `C2-b` 의 판정값이 같이 움직였다** | **승격(발견)** | `dialectic/3-design.md:238` 이 이 사건을 미리 지목하고 *"두 조건이 실은 한 조건이고, 그러면 `C2-a` 의 ⓑ 축이 재는 것도 없다. **발견으로 올린다**"* 로 처분을 적어 두었다 |
| **`effect/negative-C2.md` 부재**(초안 손잡이 4) | **안 늘린다 — 발견으로만 적는다** | `C2-a` 는 ⟨결정론적⟩이고 `dialectic/3-design.md` 의 `## 이 판이 안 다루는 것` 이 기계에 준 자리다. ⚠ 사실 관계만 적는다 — 회차 전체에 `negative-*` 이름의 파일이 **하나도 없고**(`find` 산출 빈 출력), 실질은 `effect/126-judge-1.md`(ⓐ 축)와 `effect/126-judge-2.md`(ⓑ 축 · 유효 0)에 남았다 |
| **판정자의 「기계 대조」 주장**(R8) · **초안 좌표 오류**(R1) · **N 미검증**(R9) | **안 늘린다** | 기록 정정으로 닫힌다. 새 측정을 안 요구하고, 원 의도의 완결에 새로 필요한 것이 없다 |
| **㉡ⓐ 의 「호출자 1」이 참인가**(초안 손잡이 5 · 반의 자기 기각 넷째) | **안 늘린다** | `intent.md` 가 참·거짓을 `C4` 에 배정했고 `## 범위 밖` 이 「호출자 N」의 미해소 몫(`MS-08`)을 **관측만 한다**로 못 박았다. 이 판의 값을 안 바꾼다 |

**판 4** ⟨`4-synthesis.md:63-70`⟩

| 나온 것 | 처분 | 사유 |
|---|---|---|
| R1·R2 — readnote 셋이 증거다 | **이번에 늘린다** | 초안의 결론을 뒤집는 증거이고 좌표가 실재한다. 별도 작업이 아니라 이 판의 재작성 안에서 끝난다 |
| R1 — 초안이 사실 아닌 것을 사실로 적었다 | **이번에 늘린다**(원장 등재) | `intent.md:83-93` 이 등록한 금지역에 직접 든다. `findings.jsonl` 에 `해악도=금지역` 으로 싣는다 |
| R4·R5·R6·R10 — 초안 자체의 결함 | **이번에 늘린다** | 전부 초안 문면 수정으로 닫힌다 |
| R9 — `effect/79-delta.md` 자기모순 | **이번에 늘린다**(원장 등재 · `C1` 귀속) | `C1` 산출물의 결함이고 좌표가 둘 다 실재한다. `C4` 의 문면은 안 건드린다 |
| R7 — ⓒ 를 판정할 근거가 회차에 없다 | **이번에 늘린다**(원장 등재 · 해악도 `미관`) | `4-design.md:90-91` 이 ⓒ 를 판정 대상에 세웠는데 회차에 그 입력이 없다는 것은 기록할 값이다. 새 측정은 안 한다 |
| R3·R9 — `C1` 의 「차이」 모집단이 문면으로 안 정해진다 | **승격** | `C1` 문면(`intent.md:231`)의 두 절이 서로 다른 모집단을 부르고, 그 갈림이 `C4`·`C2`·`C5` 셋의 판정값을 같이 움직인다. **조건 문면을 고쳐야 풀리므로 여기서 안 고친다** |
| R8 — `C4` 가 `E2` 앞에서 판정된다 | **승격** | 회차의 `## 순서`(`intent.md:123`)와 `state.md` 「남은 것」이 방향 기입을 6 번에 두고, `C4` 판정은 4 번이다. **조건이 요구하는 산출물이 아직 안 쓸 차례인데 그 조건을 먼저 판정하는 순서**는 이 판이 못 고친다 |
| 방향 기입의 **존재**가 합격선인가 | **승격** | `intent.md:238` 은 *"방향은 합격선이 **아니다**"* 이고 `intent.md:296`(범위 밖)도 *"방향은 `C4` 로 기록하되 합격선이 아니다"* 인데, `dialectic/4-design.md:90-91` 은 ⓐ 를 *"차이 전수에 **두 기입**이 붙었나"* 로 세워 존재를 판정 대상에 넣는다. **이 갈림이 `통과`(존재 면제)와 `미측정`(존재 요구) 사이를 가른다.** 설계문이 조건 문면보다 세게 읽히는 자리라 내가 못 정한다 |

**승격 계수** — 「승격」으로 처분된 것: 판 1 `R1-b` 하나 · 판 2 **0** · 판 3 **다섯**(배제 집합 정의역 · 「두 문서의 인용」 계기 · 자르기 주체 · 셈 규칙 · `C2`↔`C2-b` 동일성) · 판 4 **셋**(`C1` 모집단 · `E2` 앞 판정 순서 · 방향 기입 존재의 합격선 여부) = **아홉.**

---

## 설계문 `## 무엇이 이 판을 반증하나` 여덟 항목 — 라운드 1 점검 결과

설계가 못 박은 기입이다 ⟨`1-design.md:224-225`: *"취합 보고가 라운드마다 이 여덟을 **점검하고 그 결과를 적는다.**"*⟩

**① 반론표에 `파일:줄` 인용이 0 건인 라운드가 있다 → 안 걸렸다.**
네 반론표 **36 행 전부**에 좌표 칸이 찼다. 판 2 O1 은 좌표 하나(`state.md:30`)가 초안의 금지 문서임을 **반 스스로 밝히고** *"그 좌표만으로는 물지 않겠다"* 라 적었다(`2-antithesis.md:11`) — 나머지 셋(`SKILL.md:523` · `intent.md:123`·`:159`)으로 섰다.

**② 정의 판정문이 `effect/126-judge-1.md` 와 같은 문장을 3 개 이상 연속으로 공유한다 → 안 걸렸다. 실측했다.**
네 thesis 전문과 `effect/126-judge-1.md` 를 문장 단위로 대조했다 — **공유 문장 0 개**(연속은 물론 단독도 0). 문자 단위 최장 공통 부분열은 판 1 21 자 · 판 2 24 자 · 판 3 **40 자**(`" · identity ordinal · body 1f1846428f55`" — 산출 파일의 인용 줄) · 판 4 21 자다. 전부 **좌표·산출 인용**이고 문장이 아니다. 더하여 판 3 정이 스스로 *"★ **판정자의 「유효 1」을 분자로 안 썼다**"*(`3-thesis.md:66`) 라 적고 유효 라벨 일곱을 독립 grep 으로 다시 확인했다 — 설계가 요구한 *"판정자의 라벨은 자료이지 권위가 아니다"* 가 실행됐다. **`C3` 이 두 번 세어진 자리는 없다.**

**③ 판 넷이 전부 라운드 1 에서 「전원 통과 · 살아남은 반론 0」 → 안 걸렸다.**
살아남은 반론 **34 건**이고 통과는 판 1 하나뿐이다(나머지 넷은 미측정). 판 2·3·4 는 정의 판정값을 뒤집었다. 이 조항이 요구하는 *"「무엇을 보고 안 걸렸나」를 조건당 세 자리 이상"* 기입은 **발동하지 않는다.**

**④ 판 2 의 산출에 `crates/pal-cli/src/ledger.rs` · `crates/pal-git/src/lib.rs` · `crates/pal-cli/src/touch.rs` 중 둘 이하만 인용됐다 → 안 걸렸다. 실측했다.**

| 판 2 산출 | `ledger.rs` | `pal-git/lib.rs` | `touch.rs` | 셋 다 |
|---|---|---|---|---|
| `2-thesis.md` | 2 회 | 3 회 | 4 회 | **예** |
| `2-antithesis.md` | 1 회 | 1 회 | 1 회 | **예** |
| `2-synthesis.md` | 3 회 | 2 회 | 1 회 | **예** |

세 산출 **전부** 셋을 다 인용했다. 정은 다섯 자리 사슬을 열어 *"`self.tree_digest == self.tree_digest` — **항등이다. 값이 무엇이든 참이다.**"*(`2-thesis.md:73`) 로 세웠고, 반은 *"반례를 못 찾았다"*·*"**코드로는 못 무너뜨렸다**"*(`2-antithesis.md:15`·`:41`) 로 적었으며, 합은 `crates/pal-git/src/lib.rs:193-197` 에서 **새 발견**을 냈다. **코드 독해 논증이 실제로 돌았다.**

**⑤ 판 4 의 반론 과반이 「방향 값이 틀렸다」류다 → 안 걸렸다.**
판 4 반론 10 건 중 방향 값의 옳고 그름을 다투는 것은 **0 건**이다. 반이 스스로 물린 자리(`4-antithesis.md:34`)가 그 경계선을 인용한다 — *"`dialectic/4-design.md:40` 이 *"`C4` 는 **이미 적힌 기록을 보는 조건**이다"* 로 명시하고, `:92-93` 이 방향 값의 옳고 그름을 판정 대상에서 뺀다. 설계 문면이 초안 편이다."* **설계의 경계선이 먹혔다.**

**⑥ 판 3 에서 `C2` 와 `C2-b` 가 같은 근거만 대고 두 판정값이 언제나 같이 움직인다 → 걸렸다. 발견으로 올린다.**
합이 스스로 적었다 ⟨`3-synthesis.md:23`⟩: *"★ **설계문 `dialectic/3-design.md:238` 의 발동 조건 6 이 걸렸다** — `C2` 와 `C2-b` 의 판정값이 같이 움직였다. 설계가 *"두 조건이 실은 한 조건이고 … **발견으로 올린다**"* 라 적어 둔 자리다."* 실제로 대조했다 — 정에서 둘 다 **통과**, 합에서 둘 다 **미측정**으로 같이 움직였고, 근거도 겹친다: `C2` 의 근거 ⑴ 이 *"분자의 유효성 필터가 `C2-b` 인데 그것이 미측정이다"* 라 **`C2-b` 를 직접 물고**, R2·R5(배제 오라클)가 `C2-b` 를 무너뜨리면서 같은 손으로 `C2` 의 분자를 무너뜨린다. 설계가 적어 둔 귀결도 그대로 옮긴다 — *"그러면 `C2-a` 의 ⓑ 축이 재는 것도 없다."* 합의 처분은 **승격(발견)** 이다.

**⑦ 라운드 2 의 반론이 라운드 1 의 것과 8 할 이상 같은 주장이다 → 점검 불가.**
이것은 라운드 1 이다. 비교 대상이 없다. **라운드 2 취합에서 점검해야 한다.**

**⑧ 판정문이 기준 핀 밖의 바이트에 기댔다 → 발화 여부가 갈렸다. 갈린 채로 적는다.**
기준 핀은 preflight 가 박은 `05f07ea995d534ec319426db2b301e1310b8a446` 다(`preflight.md:8-16`).

- **판 2 정이 스스로 하나 적었다**(`2-thesis.md:11`): *"⚠ **딱 하나 어긋난다** — preflight 가 자기 자신에 대해 적은 `004949f56444…` 는 지금 파일(`ececd3f8dc67…`)과 **다르다** … 증거 파일이 아니라 preflight 자신의 자기기술이므로 내 근거는 안 흔들리지만, 설계문 §반증 ⑧ 이 sha 불일치를 판 무효 사유로 걸어 뒀으니 적어 둔다."* 증거 파일 **28 개는 전부 일치**한다고 같은 줄이 적는다.
- **판 2 반 O6 가 preflight 파일 전체가 핀 뒤 바이트임을 지적했고**, **합이 닫았다**(`2-synthesis.md:82`): *"`2-design.md:73-74` 가 preflight 를 설계된 공유 입력으로 못 박으므로 *"판정 중 생긴 파일 인용"* 에 안 걸린다. **§반증 ⑧ 은 발화하지 않았고 판 2 를 무효로 돌리지 않는다** — 메인이 이것을 근거로 판을 다시 돌리지 않도록 적어 둔다."*
- **판 4 반이 스스로 물렸다**(`4-antithesis.md:35`): *"`git diff --stat 05f07ea..HEAD` 가 delta·readnote·touch·게이트에 **0 변경**을 낸다. `4-design.md:242` 의 ⑧ 은 안 걸린다."*
- ⚠ **그러나 내가 대조해 적어야 하는 사실 하나** — 역할들이 잰 HEAD 가 **핀이 아니다.** 판 2 정은 *"지금 HEAD 는 `58d21c9`"*, 판 2 반은 *"HEAD 가 `58d21c9` 에서 `c75d086` 으로 움직였지만"*, 판 4 반은 *"핀 `58d21c9` 에서 `git grep` 을 다시 돌렸다"*(`4-antithesis.md:12`·`:26` — **반이 `58d21c9` 를 「핀」이라 부른다**) 라 적는다. 즉 **판정 중에 HEAD 가 두 번 움직였고, 판 4 의 전수 검산(R6 의 12 곳)은 핀 `05f07ea` 가 아니라 `58d21c9` 에서 나왔다.** 바이트 기준으로 증거 파일이 안 변했다는 것은 판 2 정·판 4 반이 각각 확인했으므로 ⑧ 이 「발화했다」고 이 자리에서 선언하지 않는다 — **⑧ 의 발화 여부는 합 둘이 「안 걸렸다」로 냈고, 핀 이름의 어긋남은 그대로 적는다.**

**★ 그리고 하나 더 — 판정값이 게이트 표에 실릴 때 살아남은 반론의 해악도가 같이 안 실리면 → 아직 점검할 수 없다.**
게이트 `docs/gates/effect-confirmation.md` 는 판 2 합의 판정으로 **미도래**다 — `## 판정` 표가 50 개 전부 `미측정` 이고 `## 효과` 는 *"(아직 안 붙었다)"* 다(`2-thesis.md:99`). 이 조항은 **게이트 집필 시점에 점검해야 한다.** 판 2 합이 그 기입을 「통과」의 선결로 이미 명시했다(`2-synthesis.md:68` ⑶ — *"그 사실을 … `## 판정` **표준 표 위 근거 표**(`SKILL.md:767` · `2-design.md:245-246`)에 적고, 같은 커밋에서 `intent.md:191` 의 상자를 채울 것"*).

---

## 종료 판단 — `pal-debate-referee` 가 낸 것 그대로

| 판 | 답 | 근거 |
|---|---|---|
| 판 1 | **한 라운드 더.** | *"R1 이 금지역으로 살아남았으므로 설계문의 둘째 줄(「금지역·실패가 하나라도 → 라운드 2」)이 그대로 걸린다. 상한이 1 남았으니 R1 만 의제로 도는 마지막 라운드다 — 거짓신호 셋과 미관 하나는 라운드 2 의 의제가 아니라 메인이 초안에서 처리할 몫이다."* |
| 판 2 | **끝났다.** | *"설계문의 끝 조건은 「라운드 1 뒤 살아남은 반론이 전부 거짓신호·미관이면 종료」이고, 살아남은 10 건이 정확히 그 분포다(금지역 0·실패 0). 라운드 2 는 「금지역·실패 반론만 의제로」 도는 라운드이므로 의제가 비어 있어 돌 근거가 없다 — 메인이 채택분을 초안에 반영하고 진행한다."* |
| 판 3 | **한 라운드 더.** | *"금지역 넷(R2·R3·R4·R5)에 실패 하나(R7)가 얹혀 세 판 중 가장 무겁다 — 종료가 아니라 라운드 2 다. ⚠ 상한이 2 뿐이라 **이 다섯을 한 라운드에 다 소화해야 하고**, 못 닫은 것이 남으면 그 시점에 「닫혔다」가 아니라 소유자에게 올라간다."* |
| 판 4 | **한 라운드 더.** | *"R1·R2 가 금지역으로 살아남았다. 살아남은 열 중 여덟이 거짓신호·미관이라 부피는 크지만 **부피는 끝 조건이 아니다** — 금지역 둘이 라운드 2 를 강제한다."* |

**회차 전체로 따라오는 것** ⟨`r1-referee.md:71` 전문⟩:

> 세 판 전부 라운드 2 를 돈다. 회차 라운드 셈은 판 2 를 빼고 **3/8** 이고, 판 2 가 끝나지 않았다면 이 회차의 정반합은 아직 최대 5 라운드를 더 쓸 수 있다 — 판 3 의 무게(금지역 4 + 실패 1)를 보면 상한을 아낄 자리가 아니다 ⟨*"리뷰 상한은 아끼는 것이 아니다"*⟩. 셋 다 **마지막 라운드**로 들어가므로, 라운드 2 의 의제는 금지역·실패 **일곱 건**(판1 R1 · 판3 R2·R3·R4·R5·R7 · 판4 R1·R2)으로만 묶어야 한다. 거짓신호 열셋과 미관 셋을 같은 라운드에 섞으면 상한 한 칸이 해악도 낮은 것에 먹히고, 그 결과 금지역이 안 닫힌 채 상한이 소진되어 **세 판이 동시에 소유자 승격으로 떨어진다**. 라운드 2 뒤에도 금지역·실패가 남는 판은 「닫혔다」로 적지 말고 그 판만 소유자에게 올려라 — 상한 소진은 완료 선언이 아니다.

**내가 검산한 것** — referee 가 센 「금지역·실패 일곱 건」은 판1 R1(1) + 판3 R2·R3·R4·R5·R7(5) + 판4 R1·R2(2) = **8 건**이다. **referee 의 「일곱」과 그 자신이 나열한 id 여덟이 갈린다.** 나열한 id 를 옮기고 수는 맞추지 않는다. 같은 문단의 「거짓신호 열셋과 미관 셋」은 판 1·3·4 만 세면 거짓신호 3+4+6=13 · 미관 1+0+2=3 으로 **맞는다.**

**소유자에게 올리라고 합이 낸 것** — 판 1 **올린다** · 판 2 **안 올린다** · 판 3 **올린다** · 판 4 **올린다**. referee 는 `## 소유자에게 올린다면 — 물음 한 문장` 에 *"⟨해당 없음 — 세 판 모두 상한이 남았다.⟩"* 와 *"해당 없음"* 을 적었다 — **referee 의 「해당 없음」과 합 셋의 「올린다」가 갈린다.** referee 는 설계상 판정문 본문과 합의 조건 판정값을 안 받았으므로 이 갈림은 격리의 귀결이다. 맞추지 않고 둘 다 적는다.

---

## 소유자에게 올릴 물음 — 합이 낸 것 그대로, 물음 하나에 칸 하나

**칸 1 ⟨판 1 · `B1` · 합 `1-synthesis.md:66`⟩**
㉠ 의 새 산출이 찍는 *"순서 위험은 없고"* 가 중복 그룹 첫 선언에 대해 거짓이고 `ledger.rs:876` 의 시험이 그 거짓을 불변식으로 못 박았는데 — **이 회차 안에서 문구와 시험을 걷어낼 것인가, 아니면 수 자체를 그룹 단위로 다시 세어 `#79` 의 닫는 조건을 재정의할 것인가**(후자는 사전 등록한 분모와 `B5` 의 실측값을 움직인다).

**칸 2 ⟨판 3 · `C2`·`C2-b` · 합 `3-synthesis.md:72`⟩**
**`C2`·`C2-b` 를 미측정으로 닫고 조건 문면 셋(배제 집합의 정의역 · 「두 문서의 인용」의 계기 · 사실 자르기의 주체)을 다음 회차로 승격할 것인가, 아니면 이번 회차 안에서 그 셋을 고쳐 다시 잴 것인가.**
⚠ 합이 함께 적은 것: *"`intent.md` `## 차선책` 1 은 *"`C2` 가 0 이면 반증으로 적고 멈춘다"* 인데 **내 판정은 0 이 아니라 「안 쟀다」다.** 차선책 1 은 안 열린다."*

**칸 3 ⟨판 4 · `C4` · 합 `4-synthesis.md:76`⟩**
**`C1` 의 「차이」 모집단을 「사전 등록 ↔ 실제 변경 전부」로 할 것인가 「원인이 touch 인 것」으로 할 것인가**, 그리고 그에 딸린 둘 — **방향 기입의 존재가 `C4` 의 합격선인가**(`intent.md:238` ↔ `dialectic/4-design.md:90-91`), **`C4` 판정을 `E2` 뒤로 미룰 것인가**(`intent.md:123` · `state.md` 「남은 것」 4 ↔ 6).

**칸 4 ⟨취합 자리가 내는 것 — 위 「내가 기각한 것」에서⟩**
합(合)이 *"발견으로 원장에"* 라 지정한 넷에 **해악도가 안 붙어 있다.** 내가 붙이면 그것은 수 맞추기다. **그 넷의 해악도를 누가 매기는가 — 메인인가, 라운드 2 의 합인가, 소유자인가.** 안 매기면 그 넷은 원장에 안 실린다.

---

**주요 파일 (절대 경로)**

- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/1-design.md` ⟨`2`·`3`·`4-design.md` 와 sha256 동일: `829871c1eabcb1d24d21e7b9f04be47b8a7d5afbca4ccad9e9439b287121703c`⟩
- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/{1,2,3,4}-thesis.md`
- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/{1,2,3,4}-antithesis.md`
- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/{1,2,3,4}-synthesis.md`
- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/r1-referee.md`
- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/preflight.md`
- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/r1-raw.md` ⟨**이미 존재한다** — 685 줄 · 79889 바이트. 나는 안 읽었다⟩
