# 정반합 취합 — 2026-09-11-effect-confirmation 라운드 1 · ㉠ `#79` · ㉡ `#126` · ㉢ `#129` 세 자리 위에서 잠긴 의도가 ⟨정반합⟩으로 태그한 조건 다섯이 통과·반증·대조 불가·미측정 중 무엇인가

> 낸 자리: `pal-debate-reporter`. **읽기 전용이다.**
> **받은 것** — 설계문 · 정 넷 · 반 넷 · 합 넷 · 종료 판단 · preflight 의 전문. 회차 이름. `record.py --schema`.
> **안 받은 것** — 대화 기록 · 메인의 사고 과정 · 이 판정을 실행할 계획.
> 아래 판정 문장·반론 문장·근거 좌표는 **원문 그대로**다. 줄이지 않았다.

## 설계문이 네 자리에 같은 바이트로 있다

규약이 *"판 하나가 산출물 여섯을 남긴다"* 로 `dialectic/<판>-design.md` 를 요구하는데 설계문 하나가 판 넷을 진다. 네 파일이 **사본**이고 sha256 이 같다:

```
829871c1eabcb1d24d21e7b9f04be47b8a7d5afbca4ccad9e9439b287121703c  dialectic/1-design.md
829871c1eabcb1d24d21e7b9f04be47b8a7d5afbca4ccad9e9439b287121703c  dialectic/2-design.md
829871c1eabcb1d24d21e7b9f04be47b8a7d5afbca4ccad9e9439b287121703c  dialectic/3-design.md
829871c1eabcb1d24d21e7b9f04be47b8a7d5afbca4ccad9e9439b287121703c  dialectic/4-design.md
```

설계문 자신이 `1-design.md:216-218` 에서 *"넷이 sha256 이 같다는 사실을 `r<n>-raw.md` 에 적는다. 사본인 것을 안 적으면 그것이 곧 거짓 신호다"* 라고 요구했다. 적었다.

---

## 판정 — 합(合)이 낸 문장 그대로

### 판 1 · `B1`

**합의 판정 머리:** `**수정** ← 게이트가 아니다. 아래 「소유자에게」가 게이트다`

**조건 판정값: 통과**

> **값: 통과**
>
> **「요구한 것」의 읽기 — 내가 고른 것과 그 결정적 근거.** 정이 든 두 읽기 중 **닫는 조건 문장**을 택한다. 결정적인 것은 정도 반도 안 쓴 사실이다: **`#79` 본문이 스스로 센 「순서에 취약한 464 건」이 괄호로 `ordinal>0` 이라고 적혀 있다**(`dialectic/preflight.md:352`). 곧 이슈 본문 자신이 「순서에 취약」을 `ordinal>0` 과 동일시했고, 구현의 버킷 ①이 바로 그 술어다. **이슈가 자기 수로 부정한 것을 그 이슈의 요구로 읽을 수는 없다.** 그래서 R1 이 드러낸 개념적 공백은 「이슈의 요구 미달」이 아니라 **이슈 자신의 셈법이 불완전했다**는 새 사실이다.
>
> **이 판정을 뒤집는 것 (한 줄)** — `#79` 본문의 「464(`ordinal>0`)」가 **저자가 의도한 정의가 아니라 당시 스크립트의 한계였다**는 것이 서면(예: `2026-08-20-rust-extractor/premortem/r2-raw.md:76` 의 계수기가 그룹 첫 선언을 일부러 뺀 것이 아니라 못 센 것이었다면), 「요구한 것」은 그룹 전체를 가르는 것이 되고 ㉠ 이 무너져 `B1` 은 **반증**이 된다.

⚠ 정(正)의 초안은 `통과` 였고 합이 값을 안 바꿨다. 바꾼 것은 근거와 「소유자에게」다.

### 판 2 · `A5-c`

**합의 판정 머리:** `**수정**`

**조건 판정값: `A5-c` = `미측정`**

> **판정값을 `반증` → `미측정` 으로 내린다.**
>
> **이 판정을 뒤집는 것** — 회차가 게이트 집필 단계에 들어갔다는 기록(게이트의 `## 판정` 이 `미측정` 아닌 값을 하나라도 갖거나 `state.md ## 지금 단계` 가 「게이트」로 바뀜)이 서면, 그 뒤에도 「그 사실」이 게이트에 없는 것은 **미도래가 아니라 반증**이다.
>
> **「통과」가 되려면 무엇이 있어야 하나 (메인의 할 일)** — ⑴ 팔 ① 의 전수를 preflight 적중 전량으로 덮고 ⓓ2 두 머리의 출처를 명기(또는 「되살릴 수 없음」을 명기)할 것 ⑵ 팔 ② 의 코드 사슬 다섯 자리를 회차 산출물에 좌표와 함께 고정할 것 ⑶ 그 사실을 `docs/gates/effect-confirmation.md` 의 `## 판정` **표준 표 위 근거 표**(`SKILL.md:767` · `2-design.md:245-246`)에 적고, 같은 커밋에서 `intent.md:191` 의 상자를 채울 것.

⚠ 정(正)의 초안은 `반증` 이었다. **합이 값을 뒤집었다.**

### 판 3 · `C2` · `C2-b`

**합의 판정 머리:** `**수정** — `C2`·`C2-b` 를 둘 다 **통과에서 미측정으로** 내린다. (게이트는 아래 「소유자에게」다)`

**조건 판정값: `C2` = 미측정 · `C2-b` = 미측정**

> **뒤집는 것 — `C2`:** 「차이」의 셈 규칙을 ㉠·㉡ 에 **같은 자로** 못 박은 문서가 봉인 안에 존재함이 보이고, 그 자 아래에서 산출물 층위의 차이가 하나라도 서면 통과로 뒤집힌다.
> **뒤집는 것 — `C2-b`:** 배제 집합을 「봉인 전에 메인 손에 있던 회차 문서 전량」으로 읽어도 살아남는 귀속이 하나 이상 있고, 그 귀속에 대해 두 문서 인용이 **판정자 원문 안에** 적혀 있음이 보이면 통과로 뒤집힌다.
>
> ★ **설계문 `dialectic/3-design.md:238` 의 발동 조건 6 이 걸렸다** — `C2` 와 `C2-b` 의 판정값이 같이 움직였다. 설계가 *"두 조건이 실은 한 조건이고 … **발견으로 올린다**"* 라 적어 둔 자리다.

⚠ 정(正)의 초안은 `C2` 통과 · `C2-b` 통과였다. **합이 둘 다 뒤집었다.**

### 판 4 · `C4`

**합의 판정 머리:** `**수정** — 초안의 판정값 `반증` 을 **`미측정`** 으로 바꾼다. ← 게이트가 아니다. 아래 「소유자에게」가 게이트다`

**조건 판정값: `C4` = 미측정**

> **이 판정을 뒤집는 것** — `E2` 의 `## 효과` 가 쓰인 뒤에도 차이마다 방향이 나란히 안 서면 그때 **반증**이고, 반대로 서면 **통과**다. 지금 당장 뒤집으려면 원인이 도구인 여덟 행에도 「touch 가 그 자리에 대해 말한 것」이 실재함을 좌표로 보여야 한다.

⚠ 정(正)의 초안은 `반증` 이었다. **합이 값을 뒤집었다.**

---

## 반론 처리 — 전부

> 아래 각 `### ` 항이 원장 레코드 하나다. **출처는 전부 `정반합`.** `라운드` 는 1.
> 「어디가 걸리나」는 반(反)이 낸 좌표 칸을 그대로 옮긴 것이다.

### [판1·R1] `#79` 가 인용한 그 문장 — *"약한 것과 취약한 것이 같은 화면이 된다"* — 이 고친 뒤에도 그대로 참이다

- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역
- 어디가 걸리나: `crates/pal-cli/src/ledger.rs:663`(화면 문구) · `:661`(코드 스스로 *"첫 선언은 빠집니다"*) · `:876`(시험이 *"첫 `dup` 과 `solo` 는 순서 위험이 없다"* 를 **불변식으로 고정**) · `crates/pal-core/src/coord.rs:258-265` · `observations/identity-after.txt:34-36`
- 조건: `B1` · 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"**코드로 직접 쟀다. 참이다.** `SymbolId::compute` 가 `discriminator.ordinal` 을 해시에 넣는다(`crates/pal-core/src/coord.rs:126-127`). `nodes_of` 는 등장 순서로 `ordinal` 을 준다(`ledger.rs:385-391`). 따라서 같은 (체인·이름·종류) 둘의 선언 순서를 바꾸면 **첫 선언의 `SymbolId` 가 움직인다.** ★ **이 저장소의 통과 중인 시험이 그것을 이미 증명하고 있다** — `ledger.rs` 의 `체인이_없으면_순서가_정체성을_흔든다` 가 `assert_eq!(원래[1].id, 지금[1].id)` 를 거는데, 자리 1 은 `ordinal==0` 인 `m` 이고 뒤바꾼 뒤 그 자리에 앉은 심볼이 **다른 심볼**이다. … 그 문장들은 전부 `git log -S"순서 위험은 없고"` 로 확인한 대로 **이 회차의 `9f993cc` 가 새로 만든 것**이다"*

### [판1·R2] 초안의 근거 2(*"같은 술어다"*)는 `ordinal` 의 생성 규칙을 안 봤다 — 자기 저장소 수치가 44 → 10 으로 조정 없이 4 배 어긋난다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-08-20-rust-extractor/premortem/r2-raw.md:76`(*"자기 저장소는 117 파일 2,778 선언 중 44 건(1.6%)"*) · `crates/pal-cli/src/ledger.rs:279-294`(`container_chains` — `s.name` 만 씀) · `git log -S"container_chains" -- crates/pal-cli/src/ledger.rs` = `5d20d4e` 하나 · `observations/identity-after.txt:34`
- 조건: `B1` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택(안 늘린다 · 원장 등재).** 합의 원문 — *"인용 원문을 열어 확인했다 … ⚠ **다만 반이 든 설명 하나는 내가 반증했다** — 반은 *"그 사이 추출기가 안 바뀌었다"* 는 뉘앙스로 `container_chains` 만 짚었는데, `git log --since=2026-08-20 -- crates/pal-extract/src/rust.rs` 는 **여러 커밋**을 낸다. 모집단(2,778 선언 ↔ 3,325 심볼)과 계수기(임시 스크립트 ↔ `nodes_of`)가 다르므로 어긋남 자체는 설명 가능하다. **그래도 아무도 그 조정을 적지 않았다**는 사실은 남는다"* · 확장 판단 사유 — *"**조정 없이 남기면 다음 회차가 「R-16 이 거의 해소됐다」로 오독한다** — 발견으로 싣는다"*

### [판1·R3] 「커밋 시각」을 실제로 읽는 유일한 함수가 시험에 한 번도 안 걸린다

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `xtask/src/main.rs:6434-6448`(함수 전문 · 시험 참조 0) · `:6450-6490`(시험 셋 전부 주입 클로저) · `:6422-6428`(`unwrap_or(i64::MAX)`) · `:6358`
- 조건: `B1` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 탐지수단 · 상태: 열림
- 채택·기각과 근거: **채택(안 늘린다 · 원장 등재).** 합의 원문 — *"`grep -rn "종료_커밋_시각" xtask crates` 를 내가 다시 돌렸다 — 정의(`xtask/src/main.rs:6434`)와 호출 둘(`:6350`·`:6356`)뿐이고 시험 참조가 **0** 이다. … `git` 실패 시 `None` → `i64::MAX` 로 전 회차가 묶여 규칙이 사전순으로 되돌아가는 경로가 안 재어진다. **`B1`·`B6` 을 깨지는 않는다**"*

### [판1·R4] 초안의 ★(*"이 고침이 실제로 다른 회차를 집는다"*)가 과장이다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `observations/ledger-pair-before.txt` ↔ `observations/ledger-pair-after.txt` · 반이 돌린 `for d in .palimpsest/rounds/*/; do git log -1 --format=%ct -- "$d/report.md"; done` 전량(최대 = 1789040144 = cross-file-references)
- 조건: `B1` · 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택(이번에 늘린다 — 코드 아님 · 초안 문장 교정).** 합의 원문 — *"**내가 직접 재현했다.** … 두 규칙이 이 저장소 이력에서 **다른 회차를 집는 입력이 없다.** 초안의 그 한 문장은 사실이 아니다 — 그리고 내 산출은 한 글자도 안 고치고 원장에 실리므로 이것은 「사실이 아닌 것을 사실로」의 씨앗이다. ⚠ **회차 산출물 자신은 결백하다** — `effect/126-delta.md:63-72` 는 *"뽑힌 회차가 같다"* 를 먼저 적고 갈리는 짝을 *"그 둘이 마지막이던 시점에"* 로 한정했다. **압축한 것은 초안이다**"*

### [판1·R6] 「정확」 버킷의 화면 문구(*"이름으로 유일하고"*)도 사실이 아닐 수 있다

- 모집단: 저장소
- 유효성: 참
- 해악도: 미관
- 어디가 걸리나: `crates/pal-cli/src/ledger.rs:664` · `:337-350`(가르는 규칙) · `crates/pal-core/src/coord.rs:258-265`
- 조건: `B1` · 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"R1 과 뿌리가 같다. `ordinal==0` 은 유일이 아니라 그룹의 첫째다. L2 언어에서 중복 그룹이 서면 `SymbolNode.identity` 가 **데이터로** `Exact` 가 되어(`ledger.rs:412` 의 `min` 이 L2 에선 안 깎는다) 화면 문구보다 더 나쁜 거짓이 대장에 실린다. 지금 이 저장소에 그 실례가 없어 실해가 없다(`정확 6` · TypeScript 1 파일)"* · 확장 판단에서 **R1 과 같은 처분에 묶는다**(문구는 R1-a, 의미는 R1-b)

### [판2·O1] 팔 ③ 이 「미도래」다 — 회차가 게이트 집필을 판정 뒤로 스스로 잠갔다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.claude/skills/round/SKILL.md:523` · `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:123`·`:159` · `state.md:30`
- 조건: `A5-c` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택. 이 반론이 판정값을 뒤집었다.** 합의 원문 — *"**직접 열어 확인했고, 반이 댄 것보다 강한 좌표가 있다.** `dialectic/2-design.md:265-266` 이 *"차선책 발동 여부 · 이슈 닫기 · **게이트 문서 집필** · `#66` 처분. **판정값의 귀결**이고 메인과 소유자의 몫이다"* 로 **이 판이 안 다루는 것**에 게이트 집필을 명시적으로 넣었다 … ⚠ 반이 댄 `SKILL.md:523` 은 **철회 절의 문장**이라 단독으로는 약하다 — 그 좌표는 빼고 위 넷으로 세운다"*

### [판2·O2] 게이트가 `A5-c` 를 `미측정` 칸에 넣은 것은 기계가 강제한 유일한 값이지 「기입 부재」의 증거가 아니다

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `xtask/src/main.rs:6307-6314` · `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:191` · `docs/gates/effect-confirmation.md:27`
- 조건: `A5-c` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"`xtask/src/main.rs:6307-6314` 를 열었다 — `let 채워야 = 게 != "미측정";` 가 게이트 값과 `intent.md` 상자를 **양방향**으로 묶는다. `intent.md:191` 의 `A5-c` 상자가 `- [ ]` 인 한 게이트는 `미측정` 외의 값을 못 가진다. 그러므로 초안이 「부재의 증거」로 읽은 `docs/gates/effect-confirmation.md:27` 은 **아무것도 증언하지 않는다**"*

### [판2·O3] 규약이 못 박은 게이트 네 절 어디에도 「그 사실」이 갈 칸이 없다

- 모집단: 규약
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.claude/skills/round/SKILL.md:734-741`·`:767` · `docs/gates/effect-confirmation.md:5`·`:9-10`
- 조건: `A5-c` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택(사실 인정 몫만).** 합의 원문 — *"`.claude/skills/round/SKILL.md:737-741` 의 네 절 정의를 열었고, 게이트 자신의 `## 범위 밖`(`docs/gates/effect-confirmation.md:40-41`)이 *"잠긴 의도의 `## 범위 밖` 이 진다. **여기 옮겨 적지 않는다**"* 로 그 칸마저 닫았다. `E1`(`intent.md:253`)도 「그 사실」을 안 요구한다. **조건 문면이 갈 자리를 안 지정한 것은 사실이다**"* — ⚠ 「문면 결함 → 승격 후보」 몫은 **기각**됐다(아래 `## 내가 기각한 것`)

### [판2·O4] 초안이 스스로 「용법 전수」라 부른 것이 전수가 아니다 — 적중 93 줄 대 짚은 좌표 36 개

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/preflight.md:94-230` 대 `dialectic/2-thesis.md:27-46`
- 조건: `A5-c` · 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택(이번에 늘린다).** 합의 원문 — *"직접 셌다 — preflight 적중 줄 중 자기참조(`dialectic/preflight.md:` 로 시작하는 줄)를 뺀 나머지가 초안이 ⓐ~ⓔ 로 가른 좌표보다 훨씬 많다. … ★ **팔 ③ 이 미도래로 내려가면 팔 ① 이 판정을 지는 유일한 팔이 되므로, 이 공백의 무게가 초안이 매긴 것보다 커진다**"* · 확장 판단 사유 — *"원 의도(*"증거로 안 쓴다"*)의 **완결에 필요한 것**이지 새 측면이 아니다"*

### [판2·O5] 초안이 ⓐ 에 넣은 `nodes_of.txt:44`·`nodes_of2.txt:44` 와 ⓓ 의 `effect/rerun-*.txt:3` 이 같은 문장 형식인데 기준이 비대칭이다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관
- 어디가 걸리나: `.palimpsest/rounds/2026-09-11-effect-confirmation/observations/premortem-artifacts/README.md:21` · `nodes_of.txt:44` · `nodes_of2.txt:44` · `git log -1 868c27a` · `dialectic/2-thesis.md:30`·`:43`
- 조건: `A5-c` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **비대칭 지적만 채택.** 합의 원문 — *"`observations/premortem-artifacts/README.md:21` 을 열었다 … `effect/rerun-*.txt:3` 과 **같은 문장 형식**인데 초안은 앞을 ⓐ, 뒤를 ⓓ 로 갈랐다. 비대칭은 실재한다"* — ⚠ 「하중을 진다」 몫은 **기각**됐다(아래)

### [판2·O6] 설계문 §반증 ⑧ 을 한 문서 안에서 갈라 적용했다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `dialectic/2-thesis.md:11`·`:27`·`:91`·`:135-136` · `dialectic/2-design.md:245-246` · `git log --oneline -3`
- 조건: `A5-c` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택하되 해소 방향을 뒤집었다.** 합의 원문 — *"초안은 `dialectic/preflight.md:232-271` 을 *"핀 뒤 바이트"* 라며 뺐으면서 같은 파일 `:94-230` 은 전수의 근거로 썼다. ★ **다만 해소 방향이 반대다** — `2-design.md:73-74` 가 preflight 를 *"메인이 발동 전에 돌려 정과 반에게 같은 바이트로 준다"* 로 **설계된 입력**으로 못 박았으므로, §반증 ⑧ 의 *"판정 중 생긴 파일 인용"* 에 안 걸린다. **뺀 쪽이 틀렸다**"* · 확장 판단 — *"**§반증 ⑧ 은 발화하지 않았고 판 2 를 무효로 돌리지 않는다** — 메인이 이것을 근거로 판을 다시 돌리지 않도록 적어 둔다"*

### [판2·O7] 금지역 인용이 문면보다 넓다 — 해악도 부풀림

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:88`·`:191` 대 `dialectic/2-thesis.md:113`
- 조건: `A5-c` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"`intent.md:90` 원문은 *"**효과 측정이** 항등식이면 「측정이 죽은 가지」"* 다. 그리고 `intent.md:310-314` 가 `MS-06` 을 다루며 *"**`A5-c` 의 「워킹트리 일치」와 같은 부류** — 제품의 표시 결함이고 이 회차는 관측만 한다"* 로 **이 부류의 처분을 이미 확정**했다. … 「게이트에 안 적음」을 금지역으로 올리는 다리가 없다"*

### [판2·O8] 팔 ③ 의 유일한 측정 행위가 *"전문(42 줄) 읽었다"* 인데 그 파일은 41 줄이다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관
- 어디가 걸리나: `docs/gates/effect-confirmation.md`(`wc -l` = 41) 대 `dialectic/2-thesis.md:19`·`:95`
- 조건: `A5-c` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"`wc -l docs/gates/effect-confirmation.md` 를 돌렸고 `cat -n` 전문도 41 에서 끝난다. 초안이 두 자리에서 틀렸다"* ⟨취합자 검산: `wc -l` 을 다시 돌려 **41** 을 얻었다. 같은 파일에 「워킹트리」 적중 **0**⟩

### [판2·O9] ⓐ·ⓒ 의 셈이 자기 목록과 안 맞는다 — *"17 자리"* 라 적고 20 개, *"(7)"* 이라 적고 8 개

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관
- 어디가 걸리나: `dialectic/2-thesis.md:29-30`·`:37-38`
- 조건: `A5-c` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"초안 `:29-30`·`:37-38` 의 나열을 세어 대조했고 라벨과 갈린다"*

### [판2·O10] ⓓ 가 셋이 아니다 — `A2-b` 의 전제를 인용하면서 그 조건 문면 좌표를 안 짚었다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관
- 어디가 걸리나: `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md:182` 대 `dialectic/2-thesis.md:43`
- 조건: `A5-c` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"`intent.md:182` 가 *"같은 HEAD·같은 워킹트리에서"* 를 박아 둔 자리임을 확인했다"*

### [판2·합] ★ 새 발견 — `crates/pal-git/src/lib.rs:193-197` 의 문서 주석이 약속한 것을 코드가 구조적으로 못 준다

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-git/src/lib.rs:193-197` · `crates/pal-cli/src/ledger.rs:86` · `:186`
- 조건: `A5-c` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **반론이 아니다 — 합(合)이 직접 낸 발견이고 스스로 「발견으로 원장에」라 처분했다.** 합의 원문 — *"직접 열어 확인했다. 주석은 *"대장을 계산하는 동안 사용자가 파일을 고칠 수 있고, 그러면 요약이 달라진다"* 를 **이 함수가 잡는 위험**으로 적는데, `--at` 없는 경로에서 비교 두 변이 같은 필드라 그 검사는 원리상 발화할 수 없다. … **`PM2-04`(더러운 워킹트리에 `일치`)와 다른 관측이다** — 그쪽은 화면 문자열, 이쪽은 **주석이 문서화한 보증의 부재**다. 회차 처분은 `intent.md:310-314`·`2-design.md:260-262` 에 따라 **관측만**"*

### [판3·R1] 초안의 가장 무거운 문단(손잡이 1)이 든 좌표 셋이 전부 한 줄씩 어긋난다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `observations/premortem-artifacts/check_ledger_pair.txt:3`·`:10` · `observations/premortem-artifacts/README.md:23` ↔ `dialectic/3-thesis.md:84`
- 조건: `C2-b` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"직접 열어 확인했다 — `observations/premortem-artifacts/check_ledger_pair.txt:3` 이 `fun · … identity ordinal · body 1f1846428f55` 이고 `:10` 이 `호출자 1 · 피호출자 12` 다(`:2` 는 심볼 ID 줄, `:9` 는 `■ 이 심볼이 하는 것`). `README.md:23` 이 `check_ledger_pair.txt` 행이고 `:24` 는 `identity_ceiling.txt` 행이다. 반이 옳다"* · ⚠ 합의 지시 — *"이 초안이 원장에 그대로 실릴 것이라면 **고치지 말고 정오표를 옆에 붙여라** — 판정문의 사후 수정은 증거 위조다"*

### [판3·R2] ㉡ⓑ 도 오염돼 있고 readnote 가 같은 파일을 ⓒ 의 오염원으로만 공시했다 — 배제 집합을 넓혀 읽으면 분자는 1 이 아니라 0

- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역
- 어디가 걸리나: `observations/premortem-artifacts/clone-before.txt:3` · `effect/126-readnote.md:67` ↔ `dialectic/3-thesis.md:85`
- 조건: `C2`·`C2-b` · 처분: 전환 · 승격됨: 예 · 조건변경: 강화 · 사전처분: 완수조건전환 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"`observations/premortem-artifacts/clone-before.txt:3` 이 `identity ordinal` 과 `body 1f1846428f55` 를 **한 줄에** 담는다. 그런데 `effect/126-readnote.md:67` 은 그 파일을 ⓒ 의 오염원으로만 적고 `:66`(ⓑ 행)엔 *"둘 다 **없다**"* 만 적는다. 같은 줄이 낳은 두 사실 중 하나만 공시한 비대칭이 실재한다. ⚠ 반이 **덜 말했다** — ⓐ·ⓒ 는 파일에만 있는 것이 아니라 `premortem/r2-raw.md:51`·`:31` 에 **메인의 산문으로** 봉인 여섯 시간 전에 적혀 있다"*

### [판3·R3] 형식 미달은 ㉡ⓑ 하나가 아니라 일곱 중 여섯이다 — 판정자 반환 형식이 유효 판정에서 인용을 원리상 못 낳는다

- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역
- 어디가 걸리나: `effect/judge-prompt.md:28-29` · `effect/126-judge-1.md:32-33`·`:39-40` ↔ `dialectic/3-thesis.md:16`·`:26`
- 조건: `C2-b` · 처분: 전환 · 승격됨: 예 · 조건변경: 없음 · 사전처분: 완수조건전환 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"`effect/judge-prompt.md:28-29` 를 열어 확인했다. `effect/126-judge-1.md:32-33`·`:39-40` 이 맨 *"없다"* 이고, `:25`(ⓐ)는 §5.8 만 인용한다. 초안이 *"일곱 전부에 규정 형식의 내용 시험이 적혔다"* 라 적은 것은 **다섯 칸이 채워졌다**를 **두 문서 인용으로 보였다**로 바꿔 부른 것이 맞다. 초안 스스로 `dialectic/3-thesis.md:43` 에서 *"성립은 내 독립 확인(위 grep)으로 선다"* 라 적어 계기가 아니라 판정자가 아닌 자가 메웠음을 자백한다"*

### [판3·R4] `C2-b` 는 증거가 아니라 메인의 자르기를 잰다 — 글자까지 같은 한 줄이 세 번 다르게 판정된다

- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역
- 어디가 걸리나: `effect/126-judge-1.md:30`·`:37`·`:44` · `effect/judge-prompt.md:413-418`
- 조건: `C2-b` · 처분: 전환 · 승격됨: 예 · 조건변경: 없음 · 사전처분: 완수조건전환 · 상태: 열림
- 채택·기각과 근거: **좁혀서 채택.** 합의 원문 — *"⚠ **좁혀서 채택한다** — 갈린다는 것 자체는 `C2-a` 가 의도한 음성 대조가 **먹힌** 증거이지 결함이 아니다. 살아남는 것은 *"분자를 혼자 지는 자리의 유효성을 메인의 자르기가 정한다"* 이고, 이것은 `intent.md` `## 범위 밖` 이 선언한 편향(*"사전 등록을 쓰는 손과 실제 변경을 쓰는 손이 같다"*)과 **다른 자유도**다 — 그 절은 사전 등록↔변경만 덮고 **사전 등록↔판정자 프롬프트의 문항 작성**은 안 덮는다. 그러므로 선언된 범위 밖으로 처분할 수 없다"*

### [판3·R5] §5.8 의 「전수」는 내용이 빠진 게 아니라 시간상 불가능하다 — 배제 오라클의 정의역 결함

- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역
- 어디가 걸리나: `observations/red.md:231` · `git log --format='%h %ci' -- observations/red.md` → `7b4ddaa 2026-09-11 10:43:43` ↔ `dialectic/3-thesis.md:80`
- 조건: `C2-b` · 처분: 전환 · 승격됨: 예 · 조건변경: 강화 · 사전처분: 완수조건전환 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"`observations/red.md:231` 과 `git log` 를 직접 돌려 확인했다(커밋 `7b4ddaa` 하나 · 10:43:43). 사전부검 R2·R3 은 `fbec3dc` 16:17:50 이고 산출물은 `868c27a` 20:18:52 다. 배제 오라클이 **구성상 전수가 아니다.** 초안은 이것을 *"새는 사실 하나"*(손잡이 1)로 다뤘는데 실제는 오라클의 정의역 결함이다"*

### [판3·R6] 초안의 세는 규칙이 ㉡ 과 ㉠ 에 비대칭으로 걸렸다 — 2 는 어느 읽기에서도 안 나온다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `effect/79-delta.md:37-40` ↔ `effect/126-delta.md:26` ↔ `dialectic/3-thesis.md:57`·`:87`
- 조건: `C2` · 처분: 전환 · 승격됨: 예 · 조건변경: 없음 · 사전처분: 완수조건전환 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"`effect/79-delta.md:37-40` 과 `effect/126-delta.md:26` 을 나란히 열었다. 같은 범주(근거가 바뀌었다)가 한쪽은 표 밖·한쪽은 표 안이다. 초안의 수는 어느 일관된 자에서도 안 나온다"* · 확장 판단(R6·R7 묶음) — *"⚠ 메인이 지금 규칙을 **사후에** 정하면 그것은 정정이 아니라 판정을 착지시키는 것이다 — `C5` 가 막으려던 「집합을 사후에 정하기」와 같은 형태다"*

### [판3·R7] 분자 둘의 「결과 좌표」가 둘 다 「안 바뀐 자리」다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 실패 ⟨★ 합이 반이 매긴 `거짓신호` 에서 **올렸다**⟩
- 어디가 걸리나: `effect/126-delta.md:27` · `plan/126-pre.md:82` ↔ `dialectic/3-thesis.md:62`
- 조건: `C2` · 처분: 전환 · 승격됨: 예 · 조건변경: 없음 · 사전처분: 완수조건전환 · 상태: 열림
- 채택·기각과 근거: **채택 + 해악도 상향.** 합의 원문 — *"`effect/126-delta.md:27` 이 `:643` 을 *"**안 건드린** 자리"* 라 스스로 적고, `plan/126-pre.md` §4 의 좌표 목록(`:81-85`)에 `:643` 이 **없다.** `:6407` 은 `plan/126-pre.md:82` 가 봉인 전에 지정한 바로 그 자리다. ⚠ **해악도를 반이 매긴 것보다 올린다** — 이것은 보고 문구의 흠이 아니라 **합격선 본체가 재려는 것**(`intent.md:11` *"달라진 변경"*)이 산출물 층위에 하나도 안 섰을 가능성이고, `C2` 를 미측정으로 내리는 두 근거 중 하나다"*

### [판3·R8] 초안이 음성 대조가 「실행됐다」고 기댄 근거가 같은 회차에서 반증돼 있다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `effect/126-judge-2.md:51-52` ↔ `effect/79-judge-1.md:70-72` · `effect/129-judge-2.md:31` ↔ `dialectic/3-thesis.md:91`
- 조건: `C2-b` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택(안 늘린다 — 기록 정정으로 닫힌다).** 합의 원문 — *"`effect/126-judge-2.md:51-52` 가 되불러 준 수 넷의 일치를 *"같은 바이트를 받았다는 **기계 대조**"* 라 부르는데, `effect/79-judge-1.md:70-72` 가 같은 회차에서 *"판정자의 계수와 인용이 **근사**"* 임을 실측하고 *"이 대조로는 「바이트 동일」을 못 세운다"* 라 적는다. 회차가 자기 문서에서 서로 반대를 말한다"*

### [판3·R9] `C2-b` 가 요구한 「인용한 `touch/<이슈>.txt:N` 의 줄」에서 판정자가 `N` 을 검증한 적이 한 번도 없다

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `effect/126-judge-1.md:23`·`:30`·`:37` · `effect/judge-prompt.md:413`·`:417` ↔ `dialectic/3-thesis.md:38`
- 조건: `C2-b` · 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 탐지수단 · 상태: 열림
- 채택·기각과 근거: **채택(안 늘린다 — 기록 정정으로 닫힌다).** 합의 원문 — *"`effect/126-judge-1.md:23`·`:30`·`:37` 의 「인용한 줄」에 줄 번호가 없고, 번호는 `effect/judge-prompt.md:409`·`:413`·`:417` 에서 메인이 괄호로 붙였다. `C2-b` 문면은 *"인용한 `touch/<이슈>.txt:N` 의 줄"* 이라 N 결박이 조건의 일부인데 계기가 그것을 안 쟀다. 유일하게 누가 열어 본 자리(ⓔ)에서 오류가 나왔다는 것이 모집단 표본으로 읽힌다"*

### [판3·합] `C2` 와 `C2-b` 의 판정값이 같이 움직였다 — 설계문 §반증 ⑥ 이 발화했다

- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `dialectic/3-design.md:238` · `intent.md:232`·`:233`·`:234`
- 조건: `C2`·`C2-b` · 처분: 전환 · 승격됨: 예 · 조건변경: 없음 · 사전처분: 완수조건전환 · 상태: 열림
- 채택·기각과 근거: **반론이 아니다 — 설계가 미리 지목하고 합이 「승격(발견)」으로 처분했다.** 합의 원문 — *"`dialectic/3-design.md:238` 이 이 사건을 미리 지목하고 *"두 조건이 실은 한 조건이고, 그러면 `C2-a` 의 ⓑ 축이 재는 것도 없다. **발견으로 올린다**"* 로 처분을 적어 두었다"*

### [판4·R1] 초안의 ★뼈대(§5)가 사실이 아니다 — 「`호출자 1` 이 참이었나」는 이미 회차에 적혀 있다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역
- 어디가 걸리나: `effect/126-readnote.md:14` ↔ `dialectic/4-thesis.md:82`·`:92`
- 조건: `C4` · 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택. 이 반론이 판정값을 뒤집었다.** 합의 원문 — *"**직접 열어 확인했다.** `effect/126-readnote.md:14` 가 초안이 `4-thesis.md:92` 에서 자기가 했다는 그 `grep` 을 **똑같이** 적었고, 명령·좌표(`:643`)·결론이 다 있다. 그리고 `git log` 로 `9be7699`(readnote) 가 `398d233`(변경) **앞**임을 확인했다 — R1 이 스스로 건 반증 조건 둘이 다 닫힌다. 회차가 `intent.md:87-90` 에 등록한 **「사실이 아닌 것을 사실로」** 에 드는 형태다"*

### [판4·R2] 초안은 readnote 셋을 한 번도 안 다뤘다 — 전문에 `readnote` 문자열이 0 회다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역
- 어디가 걸리나: `dialectic/4-thesis.md`(`readnote` 0 회) · `effect/79-readnote.md:20`·`effect/126-readnote.md:21`·`effect/129-readnote.md:33`
- 조건: `C4` · 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"`grep -c readnote dialectic/4-thesis.md` = 0 을 확인했다. 초안 스스로 `4-thesis.md:128` 에서 *"`C4` 문면은 기록 위치를 한정하지 않으므로"* 라 적었으므로 「delta 안만 봤다」는 변명이 자기 문장으로 막힌다. 초안은 저장소 읽기를 받았고 `4-design.md` 는 readnote 를 **금지하지 않았다**(그 낱말이 설계문에 아예 없다)"*

### [판4·R3] 모집단 11 은 `C1` 을 잘라 인용해서 나왔다

- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `intent.md:231` ↔ `dialectic/4-thesis.md:44` · `effect/126-delta.md:24-27` · `effect/79-delta.md:27-30` · `effect/129-delta.md:29-31`
- 조건: `C4`·`C1` · 처분: 전환 · 승격됨: 예 · 조건변경: 없음 · 사전처분: 완수조건전환 · 상태: 열림
- 채택·기각과 근거: **앞절만 채택.** 합의 원문 — *"`intent.md:231` 전문을 열어 확인했다 — 초안은 `4-thesis.md:44` 에서 앞절만 옮겼고 *"`touch/<이슈>.txt:N` 이 원인이고"* 를 안 옮겼다. ⚠ **다만 「모집단은 3 이다」까지는 안 받는다**(아래 기각 참조). 받는 것은 **「모집단이 문면으로 안 정해진다」**이고, 초안이 11 을 확정으로 적은 것은 근거 초과다"* — 뒷절은 **기각**됐다(아래)

### [판4·R4] 초안이 자기가 「해당 없음」이라 적은 행을 실패 분모에 넣었다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `dialectic/4-thesis.md:61-67` ↔ `:69` · `:109`
- 조건: `C4` · 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"초안 표(`4-thesis.md:61-67`)와 합계 줄(`:69`)을 대조해 확인했다. 원인이 컴파일러·저장소 검사·시험인 행에는 **「touch 가 말한 것」이라는 대상 자체가 없다.** 이것은 초안이 `:109` 에서 스스로 *"공허참으로 통과시키면 … 「측정이 죽은 가지」"* 라 금지한 것의 **거울상**이다 — 공허한 축으로 **불통과**를 만들었다"*

### [판4·R5] 「라벨」 요건은 초안이 새로 만든 것이다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `dialectic/4-thesis.md:20` ↔ `intent.md:238` · `dialectic/4-design.md:90-92`
- 조건: `C4` · 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"`intent.md:238` 은 *"근거와 함께 적혔다"* 뿐이고, `dialectic/4-design.md:90-92` 의 판정 대상도 ⓐⓑⓒ 뿐이다. 라벨 형식 요구는 어디에도 없다. 초안 `:20` 의 *"**라벨된** 기입도 0 건"* 이 산문 기입을 부당하게 탈락시킨다"*

### [판4·R6] §3 의 「전수 확인」이 사실과 다르다 — 「넷뿐」이 아니라 12 곳이다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `git grep -n "나아졌다\|나빠졌다\|모른다" 58d21c9 -- .palimpsest/rounds/2026-09-11-effect-confirmation` (12 hit) ↔ `dialectic/4-thesis.md:73-78`·`:130`
- 조건: `C4` · 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"`git grep -n "나아졌다\|나빠졌다\|모른다" 58d21c9 -- .palimpsest/rounds/2026-09-11-effect-confirmation` 을 **내가 다시 돌렸다** — 열 파일에 걸쳐 나온다. 초안은 *"나오는 곳은 넷뿐"* 이라 적었고, 자기가 `:130` 에서 인용한 `dialectic/4-design.md:92` 를 자기 전수 목록에서 뺐다. 결론은 안 바뀌지만 **「전수로 쟀다」의 신뢰도가 깨진다"* ⚠ **반은 「12 곳」, 합은 「열 파일」이라 적었다 — 두 수가 갈린다. 맞추지 않고 그대로 옮긴다.**

### [판4·R7] 초안이 ⓒ 「어긋남 없음」을 판정자 원문을 안 보고 적었다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관 ⟨★ 합이 반이 매긴 `거짓신호` 에서 **내렸다** — 축소 채택⟩
- 어디가 걸리나: `effect/79-judge-1.md:21` ↔ `effect/79-readnote.md:30-41` · `effect/79-delta.md:94` ↔ `dialectic/4-thesis.md:64`
- 조건: `C4` · 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **축소 채택.** 합의 원문 — *"초안 `:64` 는 ㉠㉱ 에 *"어긋남 없음(내가 대조함)"* 이라 적었는데, 초안은 `effect/*-judge-*.md` 를 **안 읽었다고 `:138` 에 스스로 적는다.** 안 본 자료로 ⓒ 를 초록칠한 것이다. ⚠ **「정면으로 갈린다」는 부분은 기각한다**"* · 확장 판단 — *"`4-design.md:90-91` 이 ⓒ 를 판정 대상에 세웠는데 회차에 그 입력이 없다는 것은 기록할 값이다"*

### [판4·R8] §4 가 회차의 순서를 거꾸로 읽었다 — 아직 안 쓸 차례인 자리의 공백을 반증의 근거로 셌다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `intent.md:254`·`intent.md:123` · `state.md:103`·`:105` ↔ `dialectic/4-thesis.md:86`·`:119`
- 조건: `C4` · 처분: 전환 · 승격됨: 예 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림
- 채택·기각과 근거: **채택. 이 반론이 판정값을 움직였다.** 합의 원문 — *"`intent.md:123`·`:254` 와 `state.md` 「남은 것」 4·6 을 열어 확인했다. 게이트 `## 효과` 는 **아직 안 쓸 차례**이고, 초안은 그 공백을 반증의 기둥 하나로 썼다(`:86`). 이 반론이 **판정값을 움직인다** — 반증에서 미측정으로"* · 확장 판단 — **승격**(*"조건이 요구하는 산출물이 아직 안 쓸 차례인데 그 조건을 먼저 판정하는 순서는 이 판이 못 고친다"*)

### [판4·R9] `effect/79-delta.md` 가 자기모순이고 초안이 그것을 안 적었다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `effect/79-delta.md:23` ↔ `:30` (preflight 에도 그대로 실림 — `dialectic/preflight.md:307`·`:311`)
- 조건: `C4`·`C1` · 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택(이번에 늘린다 · 원장 등재 · `C1` 귀속).** 합의 원문 — *"`:23` 머리글 *"어느 것도 touch 가 원인이 아니다"* ↔ `:30` 원인 칸 `touch/79.txt:23` 을 직접 확인했다. 모집단을 어떻게 셀지가 이 한 줄에 걸리는데 초안이 그것을 안 짚었다. `C1` 산출물의 결함이라 원장으로 간다"* ⟨취합자 주: 판 3 의 정(正)도 같은 자기모순을 「손잡이 2」로 독립 발견했다 — `dialectic/3-thesis.md:87`⟩

### [판4·R10] 검산 인용이 한 자리 틀렸다 — `preflight.md:298-311` 은 11 줄이 아니라 14 줄이다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관
- 어디가 걸리나: `dialectic/preflight.md:298-311` ↔ `dialectic/4-thesis.md:48`
- 조건: `C4` · 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **채택.** 합의 원문 — *"`dialectic/preflight.md:298-311` 을 줄 번호로 찍어 확인했다 — 열네 줄이고 그중 셋(`:298`·`:302`·`:307`)이 `## 2. 차이` 절 머리글이다. 초안의 산술은 맞고 낱말이 틀렸다"*

---

## 내가 기각한 것

> ⚠ 이 절은 비어 있지 않다. **합(合)이 근거를 달아 기각한 것 일곱**이고, 그 일곱이 「발견의 몇 %가 헛것인가」를 세는 유일한 모집단이다.
> 나는 기각을 다시 판정하지 않았다 — 합이 낸 것을 옮겼다.

### [판1·R5 · 전량 기각] `읽기_경로는_의도_저장소를_안_불린다` 의 둘째 입력이 공허하다

- 모집단: 회차기록
- 유효성: 거짓 ⟨합: *"부분만 참이고 결론이 틀렸다"*⟩
- 해악도: 미관 ⟨합: *"해악도 `거짓신호` 도 과대 평가라 미관으로도 안 싣는다"*. 반이 매긴 것은 `거짓신호`⟩
- 어디가 걸리나: `crates/pal-cli/tests/narrative_read_only.rs:86-96` · `crates/pal-cli/src/narrative.rs:238-244` · `observations/red-129.txt`
- 조건: `B1` · 처분: 기각 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **기각.** 합의 원문 — *"**부분만 참이고 결론이 틀렸다.** 반의 사실 주장(후반에서 `개체_없음` 분기가 안 돈다)은 맞다 … 그러나 ⑴ 그 시험의 **선언된 목적은 민팅 분기가 아니라 「읽기가 쓰지 않는다」**이고(`crates/pal-cli/tests/narrative_read_only.rs:67-69` 주석), 바이트 동일 단언은 쓰기로 여는 우회로를 잡는 **음성 대조**로 실제 기능한다(`effect/129-delta.md:55-66` 의 표 ②가 그것을 실측했다). ⑵ 고친 분기 자체는 **첫 시험**(`:51`, `intent.redb` 없는 새 저장소)이 돌리며 *"이름이 아직 없어 뺀 조각"* 을 단언한다. ⑶ 반이 든 *"`B1-a` 가 요구한 상태와 다르다"* 는 어긋났다 — `B1-a`·`B1-d` 는 시험이 아니라 **작업 저장소 산출**(`observations/red-129.txt` · `effect/green-129.txt`)로 지고, 그쪽은 `intent.redb` 가 **있는** 상태다"*

### [판2·O3 뒷몫 · 부분 기각] 「문면 결함 → 승격 후보」 — 조건 문면이 갈 자리를 안 지정했으니 문면 개정으로 올려야 한다

- 모집단: 규약
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: `.claude/skills/round/SKILL.md:767` · `dialectic/2-design.md:245-246` · `state.md:38-40`
- 조건: `A5-c` · 처분: 기각 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **기각.** 합의 원문 — *"**칸이 실재한다.** `.claude/skills/round/SKILL.md:767` — *"기존 묶음 표·근거 표를 지우지 않는다. 표준 표를 **위에 더한다** — 근거가 사라지면 판정이 무엇에 근거했는지가 사라진다."* 게다가 `2-design.md:245-246` 이 *"이 네 판의 판정값이 게이트 표에 실릴 때 **살아남은 반론의 해악도가 같이 안 실리면** 규약이 금지한 방식으로 쓰인 것"* 이라 **근거 기입을 이미 요구한다.** … **승격은 불필요하고, 조건 문면 개정은 `state.md:38-40` 에 따라 소유자 재승인을 부르므로 근거 없이 부르면 안 된다**"*

### [판2·O5 하중 몫 · 부분 기각] `nodes_of.txt:44`·`rerun-*.txt:3` 이 `A2-b` 를 실제로 받친다

- 모집단: 회차기록
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: `effect/rerun-79.txt:6-7` · `effect/rerun-126.txt:11-12` · `effect/rerun-129.txt:5-6` · `intent.md:182` ↔ `dialectic/2-thesis.md:43`
- 조건: `A5-c` · 처분: 기각 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **기각.** 합의 원문 — *"**열어 보니 하중은 딴 데 있다.** `effect/rerun-79.txt:6-7` · `rerun-126.txt:11-12` · `rerun-129.txt:5-6` 이 **`본문 sha256` 과 `재실행 sha256` 의 일치**를 싣고, `rerun-126.txt:7-8` 은 재현 `diff` 명령까지 적는다. `A2-b`(`intent.md:182`)가 요구한 *"`diff` 가 바이트로 동일"* 은 **그 해시 쌍이 지지, 「깨끗」이라는 머리 문장이 아니다.** 그러므로 「워킹트리」 문장이 무너져도 `A2-b` 의 측정은 안 죽는다 — **초안이 적은 *"이 주장은 하중을 진다"*(`2-thesis.md:43`) 쪽이 과장이다.** 비대칭 지적만 남기고 하중 주장은 뺀다"*

### [판3·R10 · 전량 기각] 초안은 자기가 안 연 문서 때문에 여유의 크기를 못 잰 채 통과를 냈다

- 모집단: 회차기록
- 유효성: 거짓
- 해악도: 미관 ⟨반이 매긴 것은 `거짓신호`⟩
- 어디가 걸리나: `premortem/r2-raw.md:58` ↔ `dialectic/3-thesis.md:101` · `dialectic/3-design.md:117`·`:139-141`
- 조건: `C2` · 처분: 기각 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **기각.** 합의 원문 — *"초안이 `## 차선책` 을 안 연 것은 **설계가 그렇게 시켰기 때문**이다 — `dialectic/3-design.md:117` 이 네 판 공통 비공개에 *"이 판정을 실행할 계획 · 판정값이 어디로 실릴지"* 를 넣고, `:139-141` 이 *"⚠ **라운드 1 의 정에게는 `## 차선책` 과 `## 범위 밖` 을 안 준다** … 판정을 착지시킨다"* 로 그 격리를 목적과 함께 못 박는다. 설계가 명시로 막은 것을 안 본 것은 초안의 결함이 아니다(그 격리가 초안의 값이었다). 남는 실질 — *"여유가 0 일 수 있다"* — 은 R2·R6 이 이미 근거와 함께 지고 있어 거기서 채택했다. 별도 반론으로 세우면 같은 것이 두 번 세어진다"*

### [판4·R3 뒷절 · 부분 기각] *"모집단은 3 이고 그 셋은 「참이었나」가 3/3 이다"*

- 모집단: 원의도
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: `intent.md:232` · `intent.md:231` ↔ `dialectic/4-antithesis.md` R3
- 조건: `C4`·`C1` · 처분: 기각 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **기각.** 합의 원문 — *"`intent.md:232` 의 `C2` 가 ***"그 차이 중** touch 산출에 귀속되는 것"*** 으로 부분집합을 따로 부른다. `C1` 의 「차이」가 이미 touch 원인으로 좁혀져 있으면 `C2` 의 「그 차이 중」이 잉여가 된다. 그러므로 `C1` 의 뒷절은 **모집단의 정의가 아니라 기입 형식의 요구**라는 읽기가 더 세게 선다 — 이것은 R3 이 자기 반증 조건으로 직접 적어 둔 갈래다. **다만 그 읽기를 세우면 여덟 행이 `C1` 의 형식을 못 지킨 행이 되므로, 문면은 어느 쪽으로도 깨끗하게 안 선다.** 그래서 「3 이다」를 확정으로 받지 않고 「안 정해진다」로만 받는다"*

### [판4·R7 앞절 · 부분 기각] *"`effect/79-judge-1.md:21` 과 readnote 가 정면으로 갈린다"*

- 모집단: 회차기록
- 유효성: 거짓 ⟨합: *"**원문 오독**에 해당한다"*⟩
- 해악도: 미관
- 어디가 걸리나: `effect/79-readnote.md:39-41` · `effect/79-readnote.md:26-28` ↔ `effect/79-judge-1.md:21`
- 조건: `C4` · 처분: 기각 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **기각.** 합의 원문 — *"**원문을 열어 봤더니 갈리지 않는다.** `effect/79-readnote.md:39-41` 이 스스로 *"⚠ **touch 가 거짓말을 한 것은 아니다** — 같은 화면의 `:26` 이 해소율을 싣고 `symbol.callers` 의 근거도 `범위 미해소 169` 를 적는다. 그러나 「호출자 3」 옆에는 그 표시가 없다"* 라 적는다. 판정자의 *"잣대와 값이 다른 별개의 사실"* 과 **같은 말**이다. 게다가 `effect/79-readnote.md:26-28` 의 `pal query symbol.callers` 산출이 실제로 **셋**을 내므로 「참조 엣지 기준 3」은 그 잣대에서 참이다. 이것은 반이 `high`(거짓신호)로 올린 것 중 **원문 오독**에 해당한다"*

### [판4·R8 해악도 상향 몫 · 부분 기각] R8 의 해악도를 올려야 한다

- 모집단: 회차기록
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: `dialectic/4-antithesis.md` R8 해악도 칸 ↔ `dialectic/4-synthesis.md` 기각표
- 조건: `C4` · 처분: 기각 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 열림
- 채택·기각과 근거: **기각.** 합의 원문 — *"(반이 매긴 `거짓신호` 를 그대로 둔다) 초안의 **추론**이 틀렸을 뿐, 회차가 금지역에 들어간 것은 아니다. 순서 문제 자체는 아래 승격으로 올린다"*

### 표 밖에 적는 것 — 반(反)이 스스로 물려 반론표에 안 실은 것들

합이 처분한 적 없고 반론표에도 안 들어갔으므로 **원장 레코드로 안 낸다.** 다만 이 라운드에서 「좌표를 못 대서 안 냈다」가 몇 건인지는 세어 둔다: 판 1 다섯 · 판 2 다섯 · 판 3 넷 · 판 4 셋 = **열일곱**. 그중 반 스스로 「유효성 거짓」이라 명시한 것은 판 1 의 둘(「커밋 시각 동률」·「`--json identity` 산출 부재」) · 판 2 의 하나(`pal ledger` 의 「워킹트리」 줄) · 판 3 의 하나(`effect/negative-C2.md` 부재)다.

---

## 계수 — 두 원천 대조

### ① 반(反)이 붙인 번호 ↔ 합(合)의 처분 표 행 수

| 잰 것 | 반(反) | 합(合) | 같나 |
|---|---|---|---|
| 판 1 반론 수 | 6 (`R1`~`R6`) | 채택 5 + 기각 1 = **6** | **같다** |
| 판 2 반론 수 | 10 (`O1`~`O10`) | 채택 10 + 기각 2 = **12** | **갈린다** — 기각 2 행은 채택된 `O3`·`O5` 의 **부분 몫**이라 같은 반론이 두 표에 걸친다. 반론 개체 수로는 10, 처분 행 수로는 12 다. 맞추지 않는다 |
| 판 3 반론 수 | 10 (`R1`~`R10`) | 채택 9 + 기각 1 = **10** | **같다** |
| 판 4 반론 수 | 10 (`R1`~`R10`) | 채택 10 + 기각 3 = **13** | **갈린다** — 기각 3 행은 채택된 `R3`·`R7`·`R8` 의 **부분 몫**이다. 반론 개체 수로는 10, 처분 행 수로는 13 다. 맞추지 않는다 |
| **합계** | **36** | 채택 **34** + 기각 **7** = **41** | 개체 36 · 처분 행 41 |

### ② 합(合)의 채택 해악도 ↔ 종료 판단(`r1-referee.md`)이 받은 분포

| 판 | 잰 것 | 합(合)의 채택표에서 센 것 | `r1-referee.md` 가 적은 것 | 같나 |
|---|---|---|---|---|
| 판 1 | 금지역·실패·거짓신호·미관 | 1 · 0 · 3 · 1 (계 5) | 1 · 0 · 3 · 1 (계 5) | **같다** |
| 판 2 | 〃 | 0 · 0 · 6 · 4 (계 10) | 0 · 0 · 6 · 4 (계 10) | **같다** |
| 판 3 | 〃 | 4 · 1 · 4 · 0 (계 9) | 4 · 1 · 4 · 0 (계 9) | **같다** |
| 판 4 | 〃 | 2 · 0 · 6 · 2 (계 10) | 2 · 0 · 6 · 2 (계 10) | **같다** |
| **회차 합** | 〃 | **7 · 1 · 19 · 7 = 34** | 심판이 회차 전체로 적은 것: 금지역·실패 **일곱 건**(판1 R1 · 판3 R2·R3·R4·R5·R7 · 판4 R1·R2) | ⚠ **갈린다** — 나열된 `id` 는 **여덟 개**인데 「일곱 건」이라 적혔다. 내가 센 금지역 7 + 실패 1 = **8** 이다. 맞추지 않고 갈렸다고 적는다 |

⚠ 위 마지막 행이 이 라운드에서 **유일하게 확인된 계수 어긋남**이다. 심판의 문장 원문: *"라운드 2 의 의제는 금지역·실패 **일곱 건**(판1 R1 · 판3 R2·R3·R4·R5·R7 · 판4 R1·R2)으로만 묶어야 한다."* 괄호 안을 세면 1 + 5 + 2 = **8** 이다. 같은 문단이 이어서 적은 *"거짓신호 열셋과 미관 셋"* 도 내 셈(거짓신호 19 · 미관 7)과 갈리는데, 심판은 **판 2 를 뺀 세 판**만 셌다 — 판 1·3·4 의 거짓신호 3+4+6 = **13**, 미관 1+0+2 = **3** 이므로 그 둘은 맞는다. 그러면 금지역·실패도 판 1·3·4 로 세어야 하고 그 값은 **8** 이다.

### ③ 판정값과 그 이동

| 판 | 조건 | 정(正)의 값 | 합(合)의 값 | 움직였나 |
|---|---|---|---|---|
| 판 1 | `B1` | 통과 | **통과** | 안 움직였다 |
| 판 2 | `A5-c` | 반증 | **미측정** | 움직였다 (`O1` + `O2`) |
| 판 3 | `C2` | 통과 | **미측정** | 움직였다 |
| 판 3 | `C2-b` | 통과 | **미측정** | 움직였다 |
| 판 4 | `C4` | 반증 | **미측정** | 움직였다 (`R8` + `R1`·`R2`) |

**넷 중 셋에서 반론이 판정값을 뒤집었다. 조건 다섯 중 통과 1 · 미측정 4 · 반증 0 · 대조 불가 0.**

### ④ 반(反)이 매긴 해악도를 합(合)이 바꾼 자리

| 반론 | 반(反) | 합(合) | 방향 |
|---|---|---|---|
| 판3 `R7` | 거짓신호 | **실패** | 올림 — *"⚠ **해악도를 반이 매긴 것보다 올린다**"* |
| 판4 `R7` | 거짓신호 | **미관** | 내림(축소 채택) |
| 판1 `R5` | 거짓신호 | (기각 · *"과대 평가라 미관으로도 안 싣는다"*) | 기각 |
| 판4 `R8` | 거짓신호 | 거짓신호 (상향 요구를 기각) | 유지 |

---

## 합(合)의 확장 판단 — 그대로 옮긴다

> ★ 「나온 것 / 판단 / 사유」 세 칸을 원문 그대로 옮겼다. 사유가 길면 그것은 원 산출물이 길다는 사실이다.

### 판 1

| 나온 것 | 판단 | 사유 — *"원 의도의 완결에 필요한가"* |
|---|---|---|
| R1-a **거짓 문장 제거** — `ledger.rs:663`·`:307`·`:323` 의 *"순서 위험은 없(고)다"* 와 `:876` 의 단언 메시지에서 순서 위험 주장을 뺀다 | **이번에 늘린다** | **필요하다.** 원 의도는 「재는 자리 셋에서 효과를 잰다」이고, 그 산출물이 **거짓을 사실로 찍는 채로는 회차가 닫히지 않는다**(`intent.md:87-88` 금지역). 새 측면이 아니라 이 회차가 만든 것의 뒷정리다. 기계적이고 수를 안 움직인다 |
| R1-b **셈법 변경** — 그룹 크기를 보고 첫 선언도 `순서에_취약` 으로 센다(`nodes_of` 2 패스) | **승격** | **새 측면이다.** 값이 `순서에 취약 10` 에서 움직이고, 그러면 `B5`·`B5-a` 가 사전 등록한 분모 해석과 `#79` 의 「그 수」 정의가 함께 바뀐다. ⚠ **조건 문면을 내가 고치지 않는다** — 소유자가 정할 자리다 |
| R6 「정확」 문구 + `identity_ceiling` 이 그룹 중복을 못 보는 것 | **R1 과 같은 처분에 묶는다** (문구는 R1-a, 의미는 R1-b) | 뿌리가 하나인데 따로 처분하면 한쪽만 고쳐 놓고 닫힌다. ⚠ 단 `identity_ceiling` 의 의미 자체는 `0c9b83f`(S2) 산이라 **이 회차가 만든 것이 아니다** — 그 구별을 처분에 적어야 귀속이 안 틀어진다 |
| R2 자기 저장소 수치 4 배 어긋남(44 → 10) | **안 늘린다 · 원장 등재** | `B5-a` 가 *"`#79` 본문의 464·7,156 은 안 쓴다"* 를 봉인 전에 적고 분모를 스스로 정의했으므로(`plan/79-pre.md:94-115`) 원 의도의 완결에 필요하지 않다. 다만 **조정 없이 남기면 다음 회차가 「R-16 이 거의 해소됐다」로 오독한다** — 발견으로 싣는다 |
| R3 `종료_커밋_시각` 무시험 | **안 늘린다 · 원장 등재** | `#126` 도 `B6` 도 이 함수의 시험을 요구하지 않고(`B6` 이 요구한 「커밋 0 인 회차의 처분」은 이미 재어진다), 원 의도는 「효과 측정」이지 커버리지 확장이 아니다 |
| R4 초안의 ★ 과장 | **이번에 늘린다** (코드 아님 — 초안 문장 교정) | 내 산출과 초안은 **한 글자도 안 고치고 원장으로 간다.** 거짓 문장을 실은 채 옮기면 그것이 곧 이 회차의 금지역이다 |

### 판 2

| 나온 것 | 판단 | 사유 |
|---|---|---|
| O4 — 팔 ① 전수 미완 | **이번에 늘린다** | 팔 ③ 이 미도래로 내려가 팔 ① 이 판정을 지는 축이 됐다. 원 의도(*"증거로 안 쓴다"*)의 **완결에 필요한 것**이지 새 측면이 아니다 |
| ⓓ2 — `touch/79.txt:9`·`touch/129.txt:11` 의 「워킹트리 깨끗」 출처 미기재 | **이번에 늘린다** | `touch/126.txt:9` 가 같은 회차에서 출처를 명기한 **선례**를 이미 세웠다. 기입 균일화는 새 측정이 아니라 기록 정리다. 새 실행은 필요 없다(출처가 안 되살려지면 「되살릴 수 없음」을 적는 것이 곧 답이다) |
| O3 — 게이트에 「그 사실」이 갈 칸 | **안 늘린다 · 승격 아님** | `SKILL.md:767` 이 근거 표 추가를 허용하고 `2-design.md:245-246` 이 그것을 이미 요구한다. 조건 문면 개정은 `state.md:38-40` 상 소유자 재승인을 부르는데, 부를 근거가 없다 |
| `pal ledger` 의 「워킹트리」 줄(`crates/pal-cli/src/ledger.rs:691-700` · `observations/identity-after.txt:8` 이 `다른 파일 2개`) | **안 늘린다** | 조건 문면이 *"**`pal touch` 의** 「워킹트리 일치」 줄"* 로 표면을 명시했다. 넓히면 **새로운 측면**이고 조건을 사후에 키우는 것이다. 다만 초안 `:89` 가 *"두 종류의 워킹트리 줄이 섞여 있는데 어느 산출도 안 가른다"* 라 적은 것은 옳다 — **관측으로 원장에 남긴다** |
| 읽기 B(명제의 참) | **안 늘린다** | `2-design.md:109-110` 이 *"`PM2-04` 는 앞쪽을 관측했다"* 로 읽기 A 를 골라 놨다 |
| **★ 새 발견 — `crates/pal-git/src/lib.rs:193-197` 의 문서 주석이 약속한 것을 코드가 구조적으로 못 준다** | **안 늘린다(조건) · 발견으로 원장에** | (원문은 위 해당 항에 전문으로 실었다) |
| 초안의 §반증 ⑧ 적용 오류(O6) | **안 늘린다 · 이 판은 유효하다** | `2-design.md:73-74` 가 preflight 를 설계된 공유 입력으로 못 박으므로 *"판정 중 생긴 파일 인용"* 에 안 걸린다. **§반증 ⑧ 은 발화하지 않았고 판 2 를 무효로 돌리지 않는다** — 메인이 이것을 근거로 판을 다시 돌리지 않도록 적어 둔다 |

### 판 3

| 토론에서 나온 것 | 처분 | 사유 — *"원 의도의 완결에 필요한가"* |
|---|---|---|
| **`C2-b` 의 배제 집합이 「봉인 전 메인 손에 있던 것」을 안 덮는다**(R5 + `premortem/r2-raw.md:51`·`:31`) | **승격** | 원 의도는 *"touch 가 **처음** 말해 준 것"* 이다. 배제 집합이 문면상 두 문서로 못 박혀 있어 메인이 자기 산문으로 먼저 적은 사실이 통과한다. 조건 문면이 의도를 못 잰다 — 고치면 **재는 양이 늘어난다**(완화가 아니다). 그래도 착수에 잠긴 조건이라 메인이 혼자 못 바꾼다 |
| **`C2-b` 의 「두 문서의 인용」을 계기가 원리상 못 낳는다**(R3) | **승격** | 조건과 그 조건을 재는 도구가 어긋났다. 이번 회차 안에서 판정자를 다시 띄워 메우면 `C3-a`(*"띄운 인스턴스 전수"*)와 `A6` 계열의 「좋은 것만 남기기」 금지에 정면으로 걸린다. 소유자가 정할 일이다 |
| **귀속 사실의 자르기를 메인이 판정자 프롬프트에 미리 써 준다**(R4) | **승격** | `intent.md` `## 범위 밖` 이 선언한 편향은 사전 등록↔변경의 같은 손이고, 이것은 **사전 등록↔판정 문항**의 같은 손이다. 선언된 범위 밖이 안 덮으므로 회차 중에 조용히 범위 밖으로 밀 수 없다 |
| **「근거만 바뀐 것」을 차이로 세는가 — 셈 규칙이 봉인 안에 없다**(R6·R7) | **승격** | 원 의도의 합격선(*"달라진 변경 1 건 이상"*)이 세는 대상을 못 박아야 완결된다. ⚠ 메인이 지금 규칙을 **사후에** 정하면 그것은 정정이 아니라 판정을 착지시키는 것이다 — `C5` 가 막으려던 「집합을 사후에 정하기」와 같은 형태다 |
| **`C2` 와 `C2-b` 의 판정값이 같이 움직였다** | **승격(발견)** | `dialectic/3-design.md:238` 이 이 사건을 미리 지목하고 *"두 조건이 실은 한 조건이고, 그러면 `C2-a` 의 ⓑ 축이 재는 것도 없다. **발견으로 올린다**"* 로 처분을 적어 두었다 |
| **`effect/negative-C2.md` 부재**(초안 손잡이 4) | **안 늘린다 — 발견으로만 적는다** | `C2-a` 는 ⟨결정론적⟩이고 `dialectic/3-design.md` 의 `## 이 판이 안 다루는 것` 이 기계에 준 자리다. ⚠ 사실 관계만 적는다 — 회차 전체에 `negative-*` 이름의 파일이 **하나도 없고**(`find` 산출 빈 출력), 실질은 `effect/126-judge-1.md`(ⓐ 축)와 `effect/126-judge-2.md`(ⓑ 축 · 유효 0)에 남았다 |
| **판정자의 「기계 대조」 주장**(R8) · **초안 좌표 오류**(R1) · **N 미검증**(R9) | **안 늘린다** | 기록 정정으로 닫힌다. 새 측정을 안 요구하고, 원 의도의 완결에 새로 필요한 것이 없다 |
| **㉡ⓐ 의 「호출자 1」이 참인가**(초안 손잡이 5 · 반의 자기 기각 넷째) | **안 늘린다** | `intent.md` 가 참·거짓을 `C4` 에 배정했고 `## 범위 밖` 이 「호출자 N」의 미해소 몫(`MS-08`)을 **관측만 한다**로 못 박았다. 이 판의 값을 안 바꾼다 |

### 판 4

| 나온 것 | 처분 | 사유 |
|---|---|---|
| R1·R2 — readnote 셋이 증거다 | **이번에 늘린다** | 초안의 결론을 뒤집는 증거이고 좌표가 실재한다. 별도 작업이 아니라 이 판의 재작성 안에서 끝난다 |
| R1 — 초안이 사실 아닌 것을 사실로 적었다 | **이번에 늘린다**(원장 등재) | `intent.md:83-93` 이 등록한 금지역에 직접 든다. `findings.jsonl` 에 `해악도=금지역` 으로 싣는다 |
| R4·R5·R6·R10 — 초안 자체의 결함 | **이번에 늘린다** | 전부 초안 문면 수정으로 닫힌다 |
| R9 — `effect/79-delta.md` 자기모순 | **이번에 늘린다**(원장 등재 · `C1` 귀속) | `C1` 산출물의 결함이고 좌표가 둘 다 실재한다. `C4` 의 문면은 안 건드린다 |
| R7 — ⓒ 를 판정할 근거가 회차에 없다 | **이번에 늘린다**(원장 등재 · 해악도 `미관`) | `4-design.md:90-91` 이 ⓒ 를 판정 대상에 세웠는데 회차에 그 입력이 없다는 것은 기록할 값이다. 새 측정은 안 한다 |
| R3·R9 — `C1` 의 「차이」 모집단이 문면으로 안 정해진다 | **승격** | `C1` 문면(`intent.md:231`)의 두 절이 서로 다른 모집단을 부르고, 그 갈림이 `C4`·`C2`·`C5` 셋의 판정값을 같이 움직인다. **조건 문면을 고쳐야 풀리므로 여기서 안 고친다** |
| R8 — `C4` 가 `E2` 앞에서 판정된다 | **승격** | 회차의 `## 순서`(`intent.md:123`)와 `state.md` 「남은 것」이 방향 기입을 6 번에 두고, `C4` 판정은 4 번이다. **조건이 요구하는 산출물이 아직 안 쓸 차례인데 그 조건을 먼저 판정하는 순서**는 이 판이 못 고친다 |
| 방향 기입의 **존재**가 합격선인가 | **승격** | `intent.md:238` 은 *"방향은 합격선이 **아니다**"* 이고 `intent.md:296`(범위 밖)도 *"방향은 `C4` 로 기록하되 합격선이 아니다"* 인데, `dialectic/4-design.md:90-91` 은 ⓐ 를 *"차이 전수에 **두 기입**이 붙었나"* 로 세워 존재를 판정 대상에 넣는다. **이 갈림이 `통과`(존재 면제)와 `미측정`(존재 요구) 사이를 가른다.** 설계문이 조건 문면보다 세게 읽히는 자리라 내가 못 정한다 |

**확장 판단 요약 계수** — 「이번에 늘린다」 **10** · 「안 늘린다」 **10** · 「승격」 **9** · 「같은 처분에 묶는다」 **1**. ⟨판1: 늘린다 2 · 안 늘린다 2 · 승격 1 · 묶음 1 / 판2: 늘린다 2 · 안 늘린다 5 / 판3: 승격 5 · 안 늘린다 3 / 판4: 늘린다 5 · 승격 3⟩

---

## 설계문 `## 무엇이 이 판을 반증하나` 여덟 항목 점검 — 라운드 1

> 설계문 `1-design.md:224-225` 이 *"취합 보고가 라운드마다 이 여덟을 **점검하고 그 결과를 적는다**"* 로 못 박았다. ②③④⑥ 은 실제로 대조했다.

| # | 설계문이 적은 반증 조건 | 라운드 1 점검 결과 |
|---|---|---|
| ① | **반론표에 `파일:줄` 인용이 0 건인 라운드가 있다** → 반이 산출물을 안 읽고 초안의 문면만 다퉜다 | **발화 안 함.** 네 판 36 반론 전부 좌표 칸이 채워져 있다. 최소 자리도 두 좌표 이상이다(예: 판2 `O8` = `docs/gates/effect-confirmation.md` 대 `dialectic/2-thesis.md:19`·`:95`). 0 건인 판 **없음** |
| ② | **정의 판정문이 `effect/126-judge-1.md` 와 같은 문장을 3 개 이상 연속으로 공유한다** → 정이 판정자를 베꼈다 | **발화 안 함 — 실제로 대조했다.** 네 정(`1~4-thesis.md`)과 `effect/126-judge-1.md` 의 10 자 초과 줄을 연속 일치로 훑었다. 최장 연속 공유는 **판 2·판 3 에서 1 줄**이고 그 한 줄은 마크다운 표 구분선 `\|---\|---\|---\|---\|` 다. 판 1·판 4 는 **0 줄**이다. **실질 문장 공유 0** |
| ③ | **판 넷이 전부 라운드 1 에서 「전원 통과 · 살아남은 반론 0」** | **발화 안 함 — 정반대다.** 조건 다섯 중 **통과 1**(`B1`) · 미측정 4. 살아남은 반론 **34 건**(금지역 7 · 실패 1 · 거짓신호 19 · 미관 7). 그러므로 「무엇을 보고 안 걸렸나」를 조건당 세 자리 이상 적어야 하는 부가 의무는 **발생하지 않는다** |
| ④ | **판 2 의 산출에 `crates/pal-cli/src/ledger.rs` · `crates/pal-git/src/lib.rs` · `crates/pal-cli/src/touch.rs` 중 둘 이하만 인용됐다** → 문면 논증이었다 | **발화 안 함 — 실제로 셌다.** `2-thesis.md` : `ledger.rs` **2** · `pal-git/src/lib.rs` **3** · `touch.rs` **4** — 셋 다 인용. `2-antithesis.md` : 1 · 1 · 1 — 셋 다 인용. `2-synthesis.md` : 3 · 2 · 1 — 셋 다 인용. **세 자리 전부가 세 파일을 다 댔다.** 판 2 의 정은 여기에 더해 `crates/pal-cli/src/main.rs:562-564` · `crates/pal-core/src/repo.rs:293-295` · `crates/pal-core/src/ledger.rs:385-391` 까지 사슬로 열었다 |
| ⑤ | **판 4 의 반론 과반이 「방향 값이 틀렸다」류다** → 이 설계의 경계선이 안 먹혔다 | **발화 안 함.** 판 4 반론 10 건 중 방향 **값**의 옳고 그름을 다투는 것은 **0 건**이다. `R1`·`R2` 는 「참이었나」의 기입 존재, `R3` 은 모집단, `R4` 는 분모, `R5` 는 라벨 요건, `R6` 은 전수 주장, `R7` 은 ⓒ 근거, `R8` 은 순서, `R9` 는 delta 자기모순, `R10` 은 인용 줄 수다. 반은 스스로 *"`:92-93` 이 방향 값의 옳고 그름을 판정 대상에서 뺀다. 설계 문면이 초안 편이다 — 좌표로 반박이 안 선다"* 라 적고 물러섰다. **경계선이 먹혔다** |
| ⑥ | **판 3 에서 `C2` 와 `C2-b` 가 같은 근거만 대고 두 판정값이 언제나 같이 움직인다** → 두 조건이 실은 한 조건이다. **발견으로 올린다** | ★ **발화했다.** 정(正)은 둘 다 `통과`, 합(合)은 둘 다 `미측정` — **두 지점에서 두 값이 같이 움직였다.** 합 스스로 적었다: *"★ **설계문 `dialectic/3-design.md:238` 의 발동 조건 6 이 걸렸다** — `C2` 와 `C2-b` 의 판정값이 같이 움직였다."* ⚠ 다만 **근거는 완전히 같지 않다** — `C2` 의 근거 ⑵⑶(셈 규칙 비대칭 · 결과 좌표 무변화)은 `C2-b` 근거에 없고, `C2-b` 의 근거 ⑴⑵(인용 형식 · 오라클 정의역)은 `C2` 근거에 없다. 조건문의 앞쪽(*"같은 근거만 대고"*)은 **거짓**, 뒤쪽(*"두 판정값이 같이 움직인다"*)은 **참**이다. 설계가 요구한 대로 **발견으로 올렸다**(위 `[판3·합]` 항) |
| ⑦ | **라운드 2 의 반론이 라운드 1 의 것과 8 할 이상 같은 주장이다** → 정체다. 교착 절차로 | **점검 불가 — 라운드 2 가 아직 안 돌았다.** 판 1·3·4 가 라운드 2 를 돌면 그때 점검한다. 판 2 는 라운드 2 가 없으므로 이 항목이 **영구히 발화하지 않는다** |
| ⑧ | **판정문이 기준 핀 밖의 바이트에 기댔다**(증거 `sha256` 불일치, 또는 판정 중 생긴 파일 인용) → 그 판은 무효이고 다시 돌린다 | **발화 안 함 — 그러나 한 자리가 비어 있다.** 핀은 `05f07ea` 이고 정 넷이 독립으로 검산했다. `05f07ea..58d21c9` 의 유일한 차이는 `dialectic/preflight.md` 한 파일 추가다(판 1·2·4 의 정이 각각 확인). 판 4 의 반은 *"`05f07ea..HEAD` 에서 … delta·readnote·touch·게이트 바이트는 **한 줄도 안 변했다**"* 를 재확인했다. ⚠ **한 자리** — `preflight.md` 자신의 sha256 이 목록의 `004949f5…` 와 디스크의 `ececd3f8…` 로 **다르다**(자기참조). 판 1·2 의 정이 둘 다 자발적으로 적었고 *"핀의 자기검산이 한 자리 비어 있다"* 로 남겼다. 판 2 의 `O6` 가 이 조항의 적용을 다퉜고, **합이 *"§반증 ⑧ 은 발화하지 않았고 판 2 를 무효로 돌리지 않는다"* 로 닫았다**(`2-design.md:73-74` — preflight 는 설계된 공유 입력이다) |
| ★ | **네 판의 판정값이 게이트 표에 실릴 때 살아남은 반론의 해악도가 같이 안 실리면** 규약이 금지한 방식으로 쓰인 것이다 | **아직 점검할 시점이 아니다.** `docs/gates/effect-confirmation.md` 는 41 줄이고 `## 판정` 표가 50 개 전부 `미측정` 이다(취합자가 `wc -l` 로 다시 재었다: **41**). 판 2 의 합이 게이트 집필을 *"판정값의 귀결"* 로 미도래 처리했다. **메인이 게이트에 옮길 때 이 보고의 해악도 분포를 같이 실어야 이 조항이 안 걸린다** — 이 문장이 그 경고다 |

---

## 종료 판단 — `r1-referee.md` 원문

### 판 2 — **끝났다**

> 설계문의 끝 조건은 「라운드 1 뒤 살아남은 반론이 전부 거짓신호·미관이면 종료」이고, 살아남은 10 건이 정확히 그 분포다(금지역 0·실패 0). 라운드 2 는 「금지역·실패 반론만 의제로」 도는 라운드이므로 의제가 비어 있어 돌 근거가 없다 — 메인이 채택분을 초안에 반영하고 진행한다.
>
> ## 소유자에게 올린다면 — 물음 한 문장
> 해당 없음.

⟨해악도 분포: 금지역 0 · 실패 0 · 거짓신호 6 · 미관 4⟩

### 판 1 — **한 라운드 더** (1/2)

> R1 이 금지역으로 살아남았으므로 설계문의 둘째 줄(「금지역·실패가 하나라도 → 라운드 2」)이 그대로 걸린다. 상한이 1 남았으니 R1 만 의제로 도는 마지막 라운드다 — 거짓신호 셋과 미관 하나는 라운드 2 의 의제가 아니라 메인이 초안에서 처리할 몫이다.

⟨해악도 분포: 금지역 1 · 실패 0 · 거짓신호 3 · 미관 1⟩

### 판 3 — **한 라운드 더** (1/2)

> 금지역 넷(R2·R3·R4·R5)에 실패 하나(R7)가 얹혀 세 판 중 가장 무겁다 — 종료가 아니라 라운드 2 다. ⚠ 상한이 2 뿐이라 **이 다섯을 한 라운드에 다 소화해야 하고**, 못 닫은 것이 남으면 그 시점에 「닫혔다」가 아니라 소유자에게 올라간다.

⟨해악도 분포: 금지역 4 · 실패 1 · 거짓신호 4 · 미관 0⟩

### 판 4 — **한 라운드 더** (1/2)

> R1·R2 가 금지역으로 살아남았다. 살아남은 열 중 여덟이 거짓신호·미관이라 부피는 크지만 **부피는 끝 조건이 아니다** — 금지역 둘이 라운드 2 를 강제한다.

⟨해악도 분포: 금지역 2 · 실패 0 · 거짓신호 6 · 미관 2⟩

### 회차 전체로 따라오는 것 (원문)

> 세 판 전부 라운드 2 를 돈다. 회차 라운드 셈은 판 2 를 빼고 **3/8** 이고, 판 2 가 끝나지 않았다면 이 회차의 정반합은 아직 최대 5 라운드를 더 쓸 수 있다 — 판 3 의 무게(금지역 4 + 실패 1)를 보면 상한을 아낄 자리가 아니다 ⟨*"리뷰 상한은 아끼는 것이 아니다"*⟩. 셋 다 **마지막 라운드**로 들어가므로, 라운드 2 의 의제는 금지역·실패 **일곱 건**(판1 R1 · 판3 R2·R3·R4·R5·R7 · 판4 R1·R2)으로만 묶어야 한다. 거짓신호 열셋과 미관 셋을 같은 라운드에 섞으면 상한 한 칸이 해악도 낮은 것에 먹히고, 그 결과 금지역이 안 닫힌 채 상한이 소진되어 **세 판이 동시에 소유자 승격으로 떨어진다**. 라운드 2 뒤에도 금지역·실패가 남는 판은 「닫혔다」로 적지 말고 그 판만 소유자에게 올려라 — 상한 소진은 완료 선언이 아니다.

⚠ **취합자 검산:** 위 문장의 *"일곱 건"* 과 괄호 안 `id` **여덟 개**가 갈린다(계수표 ② 참조). 나는 맞추지 않는다. 라운드 2 의 의제를 짤 때 **여덟**을 쓸지 **일곱**을 쓸지는 메인이 정할 자리다.

---

## 소유자에게 올릴 물음

> 합(合) 셋이 「올린다」를, 판 2 의 합만 「안 올린다」를 냈다. 물음 하나에 칸 하나다. **원문 그대로.**

| # | 낸 판 | 물음 |
|---|---|---|
| 1 | 판 1 (`B1`) | ㉠ 의 새 산출이 찍는 *"순서 위험은 없고"* 가 중복 그룹 첫 선언에 대해 거짓이고 `ledger.rs:876` 의 시험이 그 거짓을 불변식으로 못 박았는데 — **이 회차 안에서 문구와 시험을 걷어낼 것인가, 아니면 수 자체를 그룹 단위로 다시 세어 `#79` 의 닫는 조건을 재정의할 것인가**(후자는 사전 등록한 분모와 `B5` 의 실측값을 움직인다) |
| 2 | 판 3 (`C2`·`C2-b`) | **`C2`·`C2-b` 를 미측정으로 닫고 조건 문면 셋(배제 집합의 정의역 · 「두 문서의 인용」의 계기 · 사실 자르기의 주체)을 다음 회차로 승격할 것인가, 아니면 이번 회차 안에서 그 셋을 고쳐 다시 잴 것인가** |
| 3 | 판 4 (`C4`) | **`C1` 의 「차이」 모집단을 「사전 등록 ↔ 실제 변경 전부」로 할 것인가 「원인이 touch 인 것」으로 할 것인가** |
| 4 | 판 4 (`C4`) | **방향 기입의 존재가 `C4` 의 합격선인가**(`intent.md:238` ↔ `dialectic/4-design.md:90-91`) |
| 5 | 판 4 (`C4`) | **`C4` 판정을 `E2` 뒤로 미룰 것인가**(`intent.md:123` · `state.md` 「남은 것」 4 ↔ 6) |
| 6 | 취합자 | ⚠ **이것은 합이 낸 것이 아니다 — 내가 계수 대조에서 발견한 것이다.** 종료 판단이 라운드 2 의 의제를 *"금지역·실패 **일곱 건**"* 이라 적고 괄호에 **여덟 개**의 `id` 를 나열했다. 라운드 2 의 의제는 **여덟**인가 **일곱**인가 |

**판 2 의 합은 「안 올린다」를 냈다** — 원문: *"살아남은 반론 중 가장 무거운 것이 **거짓신호**이고, 금지역·실패는 하나도 안 남았다. 금지역 후보였던 셋을 전부 좌표로 닫았다 … 남은 것은 전부 **메인이 초안을 고치고 게이트 집필 단계에서 마저 하면 되는 일**이고, 조건 문면을 안 건드리므로 `state.md:38-40` 의 재승인 조항도 안 걸린다."*

---

## 합(合)이 「내가 못 정한 것」으로 남긴 것 — 원장에 안 싣되 옮겨 둔다

> 발견이 아니라 **판정의 구멍**이다. 항으로 안 냈다. 넷 중 셋이 「1 차 원문·원 산출 파일을 못 봤다」로 같은 모양이다.

- **판 1** — ⑴ `#79`·`#126`·`#129` 의 1 차 원문을 원격에서 못 봤다(*"**내 `B1` 판정의 결정적 근거가 그 인라인의 「464 건(`ordinal>0`)」이라는 괄호 하나**이므로, 그 괄호가 원문에 없거나 다르면 판정이 흔들린다"*) ⑵ `순서에 취약 10` 이 그룹 몇 개에서 나온 것인지 못 쟀다 ⑶ `effect/79-delta.md`·`effect/129-delta.md` 의 음성 대조 표가 실제 실행 산출인지 못 봤다
- **판 2** — ⑴ ⓓ2 두 머리(`touch/79.txt:9` · `touch/129.txt:11`)의 「워킹트리 깨끗」이 무엇에서 왔는가(*"둘 다 없으면 **「출처를 못 되살린다」를 그대로 적는 것이 답**"*) ⑵ 팔 ① 의 전수를 합이 완성하지 않았다(*"그 분류가 나오기 전에는 `A5-c` 는 팔 ③ 과 무관하게 `통과` 로 못 간다"*)
- **판 3** — ⑴ `C2-b` 가 「미측정」인가 「반증」인가(*"어느 읽기도 **통과는 아니다**"*) ⑵ ㉡ⓑ 가 봉인 전에 메인에게 「사실로」 등록됐는가(*"없으면 이것은 원리상 못 정한다"*) ⑶ 분자 둘이 산출물 층위에서 정말 0 인가(*"그래서 이것을 지금 정하면 정정이 아니라 착지다"*)
- **판 4** — ⑴ 방향 기입의 존재가 합격선인지 ⑵ `C1` 모집단 ⑶ `effect/126-judge-2.md`·`129-judge-1.md`·`129-judge-2.md` 의 ⓒ 관련 내용(*"반의 진술을 그대로 받은 자리이므로 적어 둔다"*)

---

## 이 보고가 낸 항의 수

**`### ` 항 43 개** = 채택 반론 **34** + 기각 **7** + 합(合)이 직접 낸 발견 **2**.
그 밖의 표(확장 판단 · 계수 · 반증 여덟 점검 · 종료 판단 · 소유자 물음 · 못 정한 것)는 **발견이 아니다** — `### ` 를 안 썼다.

⚠ **메인이 실을 레코드 수는 43 이다.** 이 수가 `findings.jsonl` 의 라운드 1 정반합 레코드 수와 갈리면 `cargo xtask check` 의 합계 검산이 빨개진다.

---

**주요 파일 (절대 경로)**

- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/` — `1~4-design.md`(사본 넷) · `1~4-thesis.md` · `1~4-antithesis.md` · `1~4-synthesis.md` · `r1-referee.md` · `preflight.md`
- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/findings.jsonl`
- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md`
- `/Users/incognito/dev/projects/palimpsest/docs/gates/effect-confirmation.md`
- `/Users/incognito/dev/projects/palimpsest/.claude/skills/round/bin/record.py`
