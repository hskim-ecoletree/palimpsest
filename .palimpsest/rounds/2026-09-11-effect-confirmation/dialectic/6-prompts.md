# 메인의 과업문 — 판 6 ⟨`E3`⟩

> 회차 `2026-09-11-effect-confirmation` · 설계문 `dialectic/6-design.md` 의
> `## 역할별 입력` 이 요구한 자리다 — *"메인은 과업문을 쓰기 전에 이 절을 위에서 아래로
> 한 번 읽고, **보낸 과업문 전문을 `dialectic/6-prompts.md` 로 보존한다.**"*

★★ **이 자리가 이 회차에서 처음 선다.** 판 4 의 합이 `## 내가 못 정한 것` 첫 줄로 지목했다 —
*"정·반에게 간 프롬프트 전문이 회차기록에 보존돼야 정할 수 있다."* 이 회차에서 **「안 주는 것」
칸이 세 번 넘겨졌고**, 그때마다 **에이전트가 자기 산출 머리에 적어서만** 드러났다.
그 자리가 없으면 이 계열의 이탈은 기계에도 사람에게도 안 보인다.

⚠ **취합 보고가 이 파일을 받아 설계문의 「안 주는 것」 칸과 대조한다** ⟨설계문 §6⟩.
그러므로 이 파일은 **보낸 그대로**여야 한다 — 다듬으면 대조가 헛것이 된다.

---

## 1. 설계 — `pal-debate-designer`

⚠ 설계문이 서기 **전에** 보낸 것이라 설계문의 「안 주는 것」 칸을 못 지났다. 그 칸을 만든 것이
이 과업문이다.

````
정반합 **판 6** 을 설계한다. 회차는 `2026-09-11-effect-confirmation` 이다.

산출은 `dialectic/6-design.md` 하나뿐이다. 다른 파일은 안 고치고 커밋도 안 한다.

## 판정할 물음 — 한 문장
**완수 조건 `E3` 이 충족됐나** — "종료 보고 `report.md` 가 섰고 네 이름이 없다".

## 받는 것
- 잠긴 의도 `intent.md` 전문 · 좌표(규약 §10 절) · 기준 핀 `dialectic/6-preflight.md` · 저장소 읽기

## 안 받는 것
- 초안(없다) · 대화 기록 · 앞 판의 결과(`dialectic/` 의 판 1~5 산출물과 `r*-raw.md`)
- ⚠ 예외 둘 — `4-design.md`·`5-design.md` 는 설계문의 **형식**을 보기 위해 열어도 된다(판정 내용은 보지 마라)
- `report.md` 자신 — 열지 마라. 읽으면 설계가 아니라 판정이 된다.

## 정할 것
① 무엇을 판정하나 ② 역할마다 주는 것/안 주는 것 ③ 몇 라운드(상한 2 · 늘리지 마라)
④ 무엇이 끝을 정하나(해악도이지 `verdict` 가 아니다) ⑤ 무엇이 이 판을 반증하나

## 이 판의 어려운 자리
`E3` 은 문자열 검색으로 끝나면 안 된다. 규약 §10 이 네 이름을 금하는 까닭은 그 이름들이
「할 수 있었는데 안 한 것」(잔여)과 「원리상 못 하는 것」(경계)을 한 칸에 섞기 때문이다.
그러므로 물어야 하는 것은 둘이다 — ⓐ 네 이름이 문면에 없나(기계적) ·
ⓑ **다른 이름으로 같은 일을 하고 있지 않나**(규약이 적은 음성 대조가 정확히 그것이다).
⚠ 설계가 ⓐ·ⓑ 중 무엇을 판정값으로 삼는지 못 박아라.
````

## 2. 정(正) — `pal-decision-proposer` ⟨라운드 1⟩

````
정반합 **판 6 · 라운드 1/2 의 정(正)** 이다. 초안을 낸다.

저장소 뿌리: `/Users/incognito/dev/projects/palimpsest`
회차 디렉터리: `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/` (아래 `.palimpsest/rounds/2026-09-11-effect-confirmation`)

산출은 `.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/6-thesis.md` **하나뿐**이다. 다른 파일은 안 고치고 커밋도 안 한다.

## 판정할 물음

**완수 조건 `E3` 이 충족됐나** — *"종료 보고 `report.md` 가 섰고 **네 이름이 없다**"*.

## 받는 것 — 이것이 전부다

1. **설계문** `.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/6-design.md` **전문.** ⚠ **먼저 읽어라** — 네 임무·판정 단위·집계 규칙·근거 규칙이 거기 있다.
2. **기준 핀 + 기계 사실** `.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/6-preflight.md` **전량**(핀 여섯 + preflight ①~⑩).
3. **`.palimpsest/rounds/2026-09-11-effect-confirmation/report.md` 전문** — 판정 대상이다.
4. **규약** `.claude/skills/round/SKILL.md` 의 **§10 전문**(`:778-802`) · `:350-356` · `:967-971`.
5. **잠긴 의도의 발췌** `.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/6-intent-excerpt.md` — `E3` 행 · `## 상한` · `## 금지역` · `## 범위 밖`.
6. **게이트** `docs/gates/effect-confirmation.md` **전문**.
7. 저장소 읽기 · `git`·`grep`·`shasum`·`ls`.

## ⚠⚠ 안 받는 것 — **열지 마라**

- `.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md` **원본** — 위 5 의 발췌만 받는다. ⚠ 특히 **`## 승격` 본문 · `## 차선책` · `## 개정` · `## 순서`** 를 열지 마라.
- `.palimpsest/rounds/2026-09-11-effect-confirmation/findings.jsonl` — **주지 않는다.** 이미 매긴 처분이 네 판단을 미리 정한다.
- `.palimpsest/rounds/2026-09-11-effect-confirmation/state.md` — **주지 않는다.**
- `.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/` 의 `1-*.md` · `2-*.md` · `3-*.md` · `4-*.md` · `5-*.md`(설계문 포함) · `r1-raw.md` · `r2-raw.md` · `r3-raw.md` · `r4-raw.md` · `r1-referee.md` · `r2-referee.md` · `r3-referee.md` · `5-referee.md` · `r1-reporter-run1.md` · `preflight.md` · `preflight-r3.md` · `5-preflight.md` · `5-preflight-r2.md` — **전부 열지 마라.** 이 판의 핀은 `6-preflight.md` **하나**다.
- `.palimpsest/rounds/2026-09-11-effect-confirmation/review/` 아래 전량 — **열지 마라.**
- 메인의 초안 — **없다.** 메인의 입장도 없다.
- `report.md` 를 고칠 권한 — **없다.** 고치지 마라.

★ **네가 위 금지를 어기게 되면 그 사실을 산출 머리에 적어라.** 이 회차에서 같은 칸이 세 번 넘겨졌고, 그때마다 에이전트가 자기 산출 머리에 적어서 드러났다.

## 못 박는 것 다섯 ⟨설계문 §2⟩

1. **항 전수 표**를 낸다 — `report.md:N` 좌표 · 항 요지 · 값(경계/잔여/판정 불가) · 근거 좌표. **표에 안 실린 항이 있으면 그것이 첫 반론감이다.**
2. 항마다 규약의 **가르는 문장**(`SKILL.md:784` — *"「이 회차에서 그것을 할 수 있었는가.」 할 수 있었으면 한다"*)을 **명시적으로 묻고 답을 적는다.**
3. **규약 밖 새 이름 절**(preflight ⑤ 가 하나를 냈다)에 대해 *"이것이 `SKILL.md:969` 의 음성 대조 발화인가"* 를 **한 항으로 따로** 답한다.
4. **ⓐ 의 결과**(preflight ②·③)를 **사실로 받는다** — 다시 세지 말고, 그 값에 `E3` 판정을 기대지도 마라.
5. **ⓑ 의 값과 `E3` 의 판정값을 한 문단에 섞지 마라.**

## 새 실행 금지

빌드·시험·`pal` 호출·`cargo xtask check`·`gh` **쓰기**를 하지 마라. `git`·`grep`·`shasum`·`ls`·읽기와 `gh` **읽기**는 된다. *"돌려 봐야 갈린다"* 로 적을 것은 그 사실과 명령을 판정문에 옮겨라.

## 산출 형식

설계문이 요구하는 절을 둬라. 최소한 — 머리에 **무엇을 받고 무엇을 안 받았는지** · `## 항 전수 표` · `## 규약 밖 새 이름` · `## ⓑ 집계` · `## `E3` 의 판정값`(+ 근거 좌표 + 「이 판정을 뒤집는 것」 한 줄 + **그 값이 ⓑ 에서 왔다는 한 줄**) · `## 내가 못 정한 것`.
````

## 3. 반(反) — `pal-decision-opponent` ⟨라운드 1⟩

````
정반합 **판 6 · 라운드 1/2 의 반(反)** 이다. 초안을 무너뜨리려 시도한다.

산출은 `R/dialectic/6-antithesis.md` 하나뿐이다. **네가 직접 `Bash` 히어독으로 써라.**

## 받는 것
① 초안 전문 `R/dialectic/6-thesis.md` ② `R/report.md` 전문 ③ `R/dialectic/6-preflight.md` 전량
④ 규약 `SKILL.md:778-802`·`:348-358`·`:960-975` ⑤ 설계문 발췌 둘 `R/dialectic/6-design-for-antithesis.md`
⑥ 잠긴 의도 발췌 `R/dialectic/6-intent-for-antithesis.md` ⑦ 저장소 읽기

## ⚠⚠ 안 받는 것 — 열지 마라
- ⚠⚠ `R/intent.md` — 주지 마라(⑥ 의 발췌만). **이 회차에서 바로 이 칸이 세 번 넘겨졌다.**
- ⚠⚠ `R/findings.jsonl` — 주지 마라.
- `R/dialectic/6-design.md` 원본 — 열지 마라. 특히 `## 상한과 끝 조건` 은 종료 판단의 것이다.
- `R/state.md` · `R/review/` 전량 · `dialectic/` 의 `1-*`~`5-*` · `r1~r4-raw.md` · `*-referee.md` ·
  `preflight.md`·`preflight-r3.md`·`5-preflight*.md` · `r1-reporter-run1.md` — 전부.
- 대화 기록 · 메인의 사고 과정 · `report.md` 를 고칠 권한.
★ 금지를 어기게 되면 산출 머리에 적어라.

## 임무 여섯 (0 건이면 0 건이라고)
① 초안의 항 전수 표에서 빠진 항(직접 세어 대조) ② 경계 옷을 입은 잔여
③ 새 이름의 목록(`SKILL.md:969`)을 초안이 음성 대조로 안 읽고 지나간 자리
④ 선례 정당화와 절차 압력이 초안에 섞였나 — 문장 좌표로
⑤ 「다음 회차가 받는 것」의 항이 이슈로 섰나 — preflight ⑦ 과 초안의 주장이 갈리는 자리
⑥ ★★ 반대 방향도 — 초안이 「잔여」로 매긴 항 중 실은 경계인 것. 한쪽만 치면 반증 기계가 된다.
★ 초안이 스스로 적은 손잡이 여섯·못 정한 것 여섯을 그대로 받지 말고 좌표에서 다시 재라.

## 새 실행 금지
빌드·시험·`pal`·`cargo xtask check`·`gh` 쓰기 금지. 돌려 봐야 갈리는 것은 반론으로 적어라.

## 반환 형식
반론마다 여섯 — `id` · 대상 · 주장 · **근거 좌표(필수)** · **해악도(스스로 매긴다)** ·
"이 반론이 틀렸다면 무엇으로 아는가". 좌표 없는 반론은 표에 안 싣고 `## 내가 스스로 물린 것` 에.
머리에 받은 것/안 받은 것과 핀 재대조를 적어라.
````

⚠ **위 둘은 요지가 아니라 보낸 과업문이다.** 다만 발동문이 길어 **경로 표기를 `R` 로 줄여** 적었다 — 실제로 보낸 것은 `.palimpsest/rounds/2026-09-11-effect-confirmation/` 로 풀린 절대경로다. 그 치환 말고는 바이트가 같다.

## 4. 합(合) — `pal-decision-synthesizer` ⟨라운드 1⟩

받은 것 — 초안 전문 · 반론표 전문 · **설계문 전문** · `report.md` 전문 · `6-preflight.md` ·
규약 `SKILL.md:778-802`·`:348-358`·`:960-975`·`:550-570` · `6-intent-for-synthesis.md`
(`E3` 행 · `## 상한` · `## 금지역` · `## 범위 밖` · `## 차선책`) · **원장 스키마의 축 아홉**(값 목록) · 저장소 읽기.

**안 준 것** — `intent.md` 원본(특히 `## 승격` 본문) · `findings.jsonl` · 앞 판 다섯의 산출 ·
`state.md` · `review/` · `report.md` 를 고칠 권한 · **종료 판단의 재량**.

⚠ **함께 준 것 하나** — 반(反)이 합에게 남긴 말(*"초안이 「이 판정은 한 항 위에 서 있다」라
적은 것은 자기 감도표와 어긋난다"*)을 **그대로 옮겨 주고** *"그 말이 맞는지 네가 좌표에서 재라"*
를 붙였다. **반의 판단을 합에게 전달한 것**이고, 설계문이 합에게 반론표 전문을 주므로 격리
이탈은 아니다. 다만 **어느 반론을 먼저 보라는 지목**이라 그 사실을 여기 적는다.

## 5. 종료 판단 — `pal-debate-referee` ⟨라운드 1⟩

받은 것 — 설계문의 `## 상한과 끝 조건` 절 **바이트 그대로**(조항 ㉮ 포함) · 쓴 라운드 수(1/2) ·
살아남은 반론의 **해악도 분포만**(금지역 0 · 실패 1 · 거짓신호 10 · 미관 1) ·
그 실패 반론이 **무엇에 대한 것인지 한 줄**(조항 ㉮ 의 발화 주장) ·
**메인이 적은 사실 하나** — *"그 핀 밖 바이트(`6-prompts.md`)를 만든 것은 메인이고 만든 시각이
라운드 1 이 도는 중이다. 즉 판정 중에 나무가 움직였다. 다만 `report.md` 와 핀 여섯의 sha256 은
정·반·합 셋이 각각 다시 대어 전부 일치했다."*

**안 준 것** — ⚠⚠ **판정문 본문 셋** · **조건의 판정값** · `report.md` · `intent.md` ·
`findings.jsonl` · `state.md` · `dialectic/` 의 다른 파일 · `review/` · 대화 기록.

★ **⑤ 를 메인이 자진해서 적었다.** 그것이 없으면 종료 판단은 조항 ㉮ 의 두 갈래 중 하나를
원리상 못 본다 — 핀 여섯은 전부 일치하므로 「발동 시점과 종료 시점이 갈렸나」 축은 초록이고,
갈린 것은 「판정문이 핀 **밖** 바이트에 기댔나」 축뿐이기 때문이다.
