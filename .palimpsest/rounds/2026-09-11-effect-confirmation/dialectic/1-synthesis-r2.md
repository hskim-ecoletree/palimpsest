# 합(合) — 판 1 · 라운드 2/2 · 완수 조건 `B1`

> 낸 자리: `pal-decision-synthesizer`. **초안을 쓴 자가 아니다.**
> 받은 것 — 초안 · 반론표 · 설계문 · preflight · `intent.md` 전문(`## 차선책`·`## 범위 밖`·`## 승격` 포함) · 저장소 읽기.
> **새 실행은 하나도 안 했다** — `git`·`grep`·읽기만 썼다. 파일을 안 고쳤고 커밋 안 했다.
> **기준 핀** — 초안의 핀은 `fc961d3` 이고 지금 `HEAD = 07e9653` 이다. `git diff --stat fc961d3..HEAD -- crates/ xtask/` 가 **빈 출력**임을 내가 직접 확인했고, 그 사이 `intent.md` 의 변경은 `## 승격` 칸 2 에 `#140` 한 줄이 붙은 것뿐(`B1` 문면 불변)이다. 아래 좌표는 전부 HEAD 에서 내가 열어 실측했다.

## 판정

**수정**

### 조건 판정값 — `B1` 은 **통과**

세 자리 각각에 HEAD 의 조상인 코드 변경 커밋이 있고(`git merge-base --is-ancestor` 로 `9f993cc`·`398d233`·`1abc0e9`·`e44a87e`·`06e8d63` 전수 확인), 셋 다 그 이슈 본문의 **닫는 조건**을 만족한다. 근거는 산문이 아니라 내가 직접 연 코드다:

| 자리 | 이슈가 닫으라고 적은 것 | HEAD 에서 내가 연 자리 |
|---|---|---|
| ㉠ `#79` | *"닫으려면 그 수를 내는 **산출 경로**가 필요하다"*(`dialectic/preflight.md`) | `crates/pal-cli/src/main.rs:672` → `crates/pal-cli/src/ledger.rs:816` → `print_identity`(`:683-708`) 가 네 버킷과 검산을 찍는다. 가르는 술어는 `:366-370` 한 자리이고 그룹 크기는 `:416-419` 의 1 패스가 만든다. 그 열쇠(`:418`)와 `ordinal` 을 주는 `seen` 의 열쇠(`:425`)가 문자 그대로 같아 「그룹이 여럿」 ⊇ 「`ordinal>0`」이 성립한다 |
| ㉡ `#126` | *"「최근」이 판정에 안 쓰이면 낱말을 빼거나, 쓰인다면 **커밋 시각**으로 고른다"* | `xtask/src/main.rs:6344` 가 갈래를 선언하고 `:6350`→`최근에_끝난`(`:6422-6427`)→`종료_커밋_시각`(`:6434-6446`, `git log -1 --format=%ct`). 판정문이 무엇으로 골랐는지도 적는다(`:6356-6359`) |
| ㉢ `#129` | *"광고된 질의 하나가 어떤 입력으로도 안 돈다"* | `crates/pal-cli/src/query.rs:150-157` 이 `민팅::안한다` 를 넘기고 `crates/pal-cli/src/narrative.rs:238-244` 가 그 갈래에서 개체를 안 만든다. RED(`observations/red-129.txt`) ↔ GREEN(`effect/green-129.txt:6` — 종료값 0 · 표준오류 0 바이트) |

**이 판정을 뒤집는 것 한 줄** — `#79` 의 1 차 원문에 *"닫으려면 그 수를 내는 산출 경로가 필요하다"* 가 **없거나 다르면** ㉠ 의 근거가 통째로 무너지고 `B1` 은 **대조 불가**로 간다. 재는 법: `gh issue view 79 --json body` 를 `dialectic/preflight.md` 의 `#79` 인라인 블록과 `diff`. ⚠ **나도 안 돌렸다**(새 실행 금지).

### 초안 ⑦ 은 `B1` 의 값을 **안 움직인다 — 처분만 받는다**

`crates/pal-cli/src/ledger.rs:310-320` 이 같은 주석 아래(`:328-345`)와 코드(`:366-370`)에 정면으로 어긋나는 것을 **내가 직접 열어 확인했다** — `:315` *"이 버킷은 「이름이 유일한 것」과 「그룹의 첫 선언」을 함께 담는다"* 는 HEAD 에서 거짓이고, `:317-320` *"셈법을 그룹 단위로 바꾸는 것은 … **이 자리에서 안 한다** — 정반합 판 1 이 그것을 소유자 승격으로 올렸다"* 는 `intent.md` `## 승격` 칸 1 이 답했고 `06e8d63` 이 집행한 것을 부정한다. `git blame -L 310,320` 은 전부 `e44a87e`, `-L 328,345` 는 `06e8d63` 15 줄 — **뒷정리가 쓴 문단을 승격 집행이 안 걷어낸 것**이 맞다.

그래도 `B1` 이 재는 것은 *"이슈 본문이 요구한 것"* 이고 `#79` 가 요구한 것은 **산출 경로**다. doc 주석은 그 경로도 그 산출값도 안 건드린다. 그러므로 값은 **통과**로 두고, ⑦ 은 **회차 금지역 「사실이 아닌 것을 사실로」에 드는 발견**으로 원장에 싣고 이 회차 안에 고친다(아래 `## 확장 판단`).

## 채택한 반론

| # | 반론 | 왜 채택했나 | 해악도 |
|---|---|---|---|
| **AT2-03** | 초안 ① 의 귀속이 `git blame` 과 어긋난다 — 화면·시험이 `e44a87e` 산이라 적었는데 그 구간에 `e44a87e` 가 0 줄이고, 근거로 댄 `git blame -L 297,345` 는 690~974 줄에 원리상 안 닿는다 | **내가 직접 재서 반의 수치가 그대로 맞았다** — `-L 690,705` 는 `9f993cc` 10 줄 + `06e8d63` 6 줄, `-L 905,915` 는 9 + 2, `-L 944,974` 는 `9f993cc` 28 + `06e8d63` 2 + `5d20d4e0` 1 줄이고 **세 구간 전부 `e44a87e` 0 줄**이다. 초안 ⑦ 이 코드에 대해 고발한 것과 **같은 형태**(추정을 조회인 척함)를 판정문 자신이 저질렀고, 이 문서는 바이트 그대로 원장에 남는다 | 거짓신호 ⟨반이 매긴 값 그대로. 「올린다」로 안 올리는 까닭 — 문장 하나를 바로잡는 일이라 소유자 결정이 필요한 자리가 없다⟩ |
| **AT2-06** | 초안 ② 의 「줄 번호 일치」가 ③ 의 산출에 원리상 안 닿는다 — `negative-B5-group.txt` 는 가르는 자리를 **끈** 나무의 `cargo test` 산출이고 `identity-group.txt` 는 24 초 뒤 `./target/release/pal ledger` 의 **다른 바이너리** 산출이다 | **두 파일 머리를 내가 열어 확인했다** — `effect/negative-B5-group.txt:3-5`(`if false && 그룹_크기 > 1` · `cargo test`) vs `observations/identity-group.txt:3-5`(`./target/release/pal ledger` · `02:12:34Z` · **워킹트리**). 줄 번호 일치가 받치는 것은 **끈 나무의 시험 파일 세 줄**뿐이고, 릴리스 바이너리의 빌드 SHA·해시는 어느 산출에도 없다. 초안 ② 의 *"커밋된 바이트를 가리키고 있다는 것을 이 줄 번호 일치가 받친다"* 는 **성립하지 않는다** | 거짓신호 ⟨반이 매긴 값 그대로. `B1` 은 코드 경로의 실재로 서지 산출 파일로 서지 않으므로 판정값은 안 흔들린다⟩ |
| **AT2-04** | ㉡ 의 「최근」에 **사전순이 아직 살아 있다** — 커밋 시각이 없는 회차가 둘 이상인 가지에서 `max_by_key` 의 튜플 둘째가 가르고, 그때 판정문은 `커밋 시각 없음 — 가장 최근으로 본다` 만 적고 사전순이 동점을 갈랐다는 사실은 안 적는다 | **코드를 열어 확인했다** — `xtask/src/main.rs:6425` 가 `(시각(회차).unwrap_or(i64::MAX), (*회차).clone())` 이고, `:6415-6416` 이 스스로 *"시각 없는 회차가 여럿이면 그중 **사전순 최대**"* 라 적으며, 시험 `시각이_같으면_사전순_최대를_고른다`(`:6483-6489`)가 `|_| None` 갈래를 **의도로 못 박는다.** 판정문 문구(`:6356-6359`)에는 그 사실이 없다. 초안 ④ 는 이 가지를 한 번도 안 연다 | 거짓신호 ⟨반이 매긴 값 그대로⟩ ⚠ **다만 `B1` 이 아니라 `B6`(⟨결정론적⟩) 에 닿는 발견으로 받는다** — 설계문 `## 이 판이 안 다루는 것` 이 결정론적 조건의 산출에 대한 반론을 *"발견이지 이 판의 판정값이 아니다"* 로 못 박았다. `#126` 이 친 것은 **1 차 자**가 사전순인 것이고 그 자는 커밋 시각으로 바뀌었으므로 ㉡ 의 `B1` 은 안 깨진다 |
| **AT2-05** | 초안 ③ 이 `#79` 의 산문을 택하면서 같은 문장의 괄호·수치(`464` · `7,156`)가 **이슈 자신의 자기모순**이 된다는 사실을 판정문에 한 줄도 안 싣는다 | **좌표가 실재한다** — 이슈 인라인의 `464`·`7,156` 과 실측 `20`·`3306`(`observations/identity-group.txt`)은 자릿수가 다르고, 회차는 이미 `intent.md` 의 `B5-a` 와 `effect/79-delta.md:98-101` 에 그 어긋남을 적어 뒀다. 판정문만 그 교차 참조가 비어 있다 | **미관** ⟨반의 `거짓신호` 에서 **내림**. 까닭 — 초안은 *"이슈의 수를 재생산했다"* 를 **한 번도 주장하지 않는다**(③ 은 산문 정의에만 기댄다). 그리고 그 사실은 회차 안 두 자리에 이미 실려 있어 읽는 이가 틀리게 믿을 경로가 없다. 빠진 것은 **교차 참조**이지 거짓 주장이 아니다⟩ |
| **AT2-07** | 초안이 적은 「`e44a87e`·`06e8d63` 이 `ledger.rs` 를 101 줄 고쳤다」가 틀렸다 | **세 명령을 내가 다시 돌려 반의 수치가 그대로 맞았다** — `06e8d63` 단독 54+47=101 · `e44a87e` 단독 34+5=39. 101 은 합산이 아니라 `06e8d63` 단독값이다 | 미관 ⟨반이 매긴 값 그대로. 「대안이 빠지는 근거」 칸의 방증 수치라 어느 판정도 이것에 안 걸린다⟩ |

## 기각한 반론

| # | 반론 | 왜 기각했나 (근거) |
|---|---|---|
| **AT2-01** | `t.합() != report.symbols.len()`(`ledger.rs:705-707`)이 **도달 불가능한 가지**라 회차 금지역 「측정이 죽은 가지」에 든다 | **반의 관측은 맞지만 결론이 틀렸다 — 원문(코드) 오독이 `금지역` 으로 올라온 자리다.** 그 검산이 스스로 선언하는 방어 대상은 *"버킷이 **겹치거나 빠진** 것"*(`ledger.rs:681-682`·`:706`)이고, **그 결함 부류에서 이 가지는 실제로 발화한다** — `ledger.rs:369` 의 `return` 을 지우면 `그룹_크기 > 1` 인 심볼이 `순서에_취약` 과 등급 버킷에 **둘 다** 더해져 `합() = n + 중복수 ≠ n` 이 되고 `:705-707` 과 시험 `:929` 가 빨개진다. `헤아린다`(`:366-376`)의 `match` 는 `IdentityGrade` 세 변종(`crates/pal-core/src/ledger.rs:94-101`)을 남김없이 덮으므로 **지금 안 빨개지는 것은 「아무것도 안 재서」가 아니라 「불변식이 지금 참이라서」**다. 이 기준을 받으면 참인 프로그램의 모든 `assert!(불변식)` 이 금지역이 된다. ⚠ 그리고 반이 제시한 반증 명령(`그룹_크기 > 99999999` 로 **버킷을 옮기기만** 하는 변이)은 합을 보존하므로 **원리상 이 축을 못 건드린다** — 돌려도 아무것도 안 갈린다. 회차 금지역 문면은 *"**효과 측정**이 항등식이면"*(`intent.md:90`)인데 ㉠ 의 효과 측정은 `effect/negative-B5-group.txt` 이고 그것은 **셋이 빨개졌다.** ⟨잔여 하나는 인정한다 — 아래 `## 초안을 어떻게 고치나` ⑥⟩ |
| **AT2-02** | `#79` 가 이름으로 지목한 「같은 글자」는 안 갈렸다 — `identity: discriminator.identity_ceiling().min(s.identity)`(`ledger.rs:447`)가 그대로라 `pal ledger --symbols`·`pal touch` 소비자는 여전히 심볼 단위로 못 가른다 | **관측은 참이고(내가 `:447`·`:449-451` 을 열어 확인했다) 반이 `## 범위 밖`·`## 차선책` 을 못 받은 탓에 선 반론이다 — 근거를 대어 기각한다.** ⑴ `#79` 본문이 **닫는 조건을 자기 입으로 한 문장으로 못 박았다** — *"닫으려면 그 수를 내는 산출 경로가 필요하다"*. 심볼 단위 판별이 아니라 **수를 내는 경로**다. ⑵ 회차가 그 읽기를 **측정 전에** 잠갔다 — `intent.md` 의 `B5` 가 그 문장을 글자 그대로 옮겨 적었고, `## 차선책` 2 의 ★ 가 *"스키마를 먼저 건드리지 마라 … `SymbolNode`·`schema/graph.toml` 로 가는 것은 **만들 필요가 없는 것을 만드는 자리**"* 로 반대 경로를 막았다. ⑶ 반이 지적한 잔여는 **`## 범위 밖` 에 이미 이름 붙어 등록돼 있다** — *"심볼 한 줄의 `identity ordinal` 이 원인을 안 가르는 것 ⟨`MS-09`⟩ … 2층에 그 값이 없어 갈래 ⓐ(저장한다) 없이는 **원리상 못 고친다** — 사전 등록 `plan/79-pre.md` §3 에 미리 적고 왔다."* 사후 면제가 아니라 **사전 등록된 범위 밖**이다 |

## 초안을 어떻게 고치나

① **①의 귀속 문장을 실측으로 바꾼다** ⟨AT2-03⟩ — *"화면·시험은 `e44a87e`·`06e8d63` 산이다"* 를 지우고 `git blame -L 690,705` · `-L 905,915` · `-L 944,974` 의 실제 분포(`9f993cc` 다수 · `06e8d63` 소수 · `5d20d4e0` 한 줄 · `e44a87e` 0 줄)로 다시 쓴다. 근거로 댄 명령도 `-L 297,345` 가 아니라 각 구간의 blame 으로 바꾼다.

② **②의 적극적 주장을 걷어낸다** ⟨AT2-06⟩ — *"산출이 … 커밋된 바이트를 가리키고 있다는 것을 이 줄 번호 일치가 받친다"* 를 삭제하고, 그 자리에 **줄 번호 일치가 받치는 것은 끈 나무의 시험 파일 세 줄뿐**이라고 적는다. `identity-group.txt` 가 HEAD 의 바이트에서 났는지는 **미측정**이고, 가르는 명령은 `cargo build --release && ./target/release/pal ledger` 로 `순서에 취약` 이 20 인지 보는 것이다 — **초안도 나도 안 돌렸다.**

③ **④에 사전순 가지를 적는다** ⟨AT2-04⟩ — `xtask/src/main.rs:6425` 의 튜플 둘째가 시각 없는 회차 동점을 사전순으로 가르고 `:6415-6416` 이 그것을 의도로 적으며 `:6483-6489` 의 시험이 고정한다는 것, 그리고 **판정문(`:6356-6359`)에는 그 사실이 없다**는 것을 나란히 적는다. 이것은 `B1` 이 아니라 **`B6` 에 닿는 발견**임을 같은 줄에 밝힌다.

④ **③에 이슈 수치의 자기모순을 한 줄 넣는다** ⟨AT2-05⟩ — `#79` 의 `464`·`7,156` 은 산문 정의와 같은 문장 안에 있고 실측 `20`·`3306` 과 자릿수가 다르며, 회차는 그 사실을 `intent.md` 의 `B5-a` 와 `effect/79-delta.md:98-101` 에 이미 실었다는 교차 참조.

⑤ **「101 줄」을 고친다** ⟨AT2-07⟩ — `06e8d63` 단독 101 · `e44a87e` 단독 39.

⑥ **AT2-01 의 잔여를 발견으로 적는다** — 반의 「죽은 가지」 주장은 기각하되, **음성 대조 `effect/negative-B5-group.txt` 가 가르는 술어 축만 껐고 「합 = 분모」 축은 한 번도 안 껐다**는 것은 참이다(그 산출에서 `ledger.rs:929` 는 **안 빨개졌고** `:933` 만 빨개졌다). 그 축은 **미측정**이라고 적고 가르는 명령을 함께 적는다 — `ledger.rs:369` 의 `return` 을 지운 사본에서 `cargo test -p pal-cli ledger::tests::합이_분모와_같다` 가 `:929` 에서 빨개지는지. ⚠ **아무도 안 돌렸다.**

⑦ **좌표 셋을 되잡는다** ⟨내가 찾은 것 — 반의 「전수 대조했고 어긋난 좌표가 하나도 없다」가 이 자리에서 틀렸다⟩ — 초안의 `ledger.rs:358-361`(가르는 술어)은 실제 **`:366-370`** 이고, `:413-418`(그룹 1 패스)은 **`:416-419`**, `:421`(`seen` 열쇠)은 **`:425`** 다. `crates/pal-core/src/coord.rs:126-127` 은 종류 이름 줄이고 `ordinal` 이 해시에 드는 줄은 **`:128`** 이다. 하중을 지는 좌표(`:697-701` 화면 · `:911-912`·`:933`·`:972-973` 단언 · `:683-708` · `:816` · `:47`)는 전부 정확하다. ★ 이 교정은 사소하지 않다 — `#79` 가 막으려 한 것이 *"다음 회차가 못 되짚는다"* 이고, 바이트 그대로 보존되는 판정문의 좌표가 어긋나면 그 자리를 판정문 자신이 만든다.

## 확장 판단

기본값은 **안 늘린다**. 뒤집는 유일한 시험은 자기 완결성 — *"안 하면 다음 회차에 짐이 되거나 의도가 변질되는가."*

| 나온 것 | 판단 | 사유 |
|---|---|---|
| **초안 ⑦ — `ledger.rs:310-320` 의 거짓 문단 셋** | **이번에 늘린다** | 자기 완결성 시험을 **통과한다.** ⓐ 이 회차가 이 회차의 승격 집행 커밋으로 만든 거짓이고, ⓑ 회차 스스로 선언한 금지역 「사실이 아닌 것을 사실로」에 들며, ⓒ **다음 회차가 이 파일을 열면 코드가 「그룹 단위로 안 바꿨다」고 말한다** — 소유자가 답한 승격 칸 1 을 부정하는 문장이 제품 코드에 남는다. ⚠ 「어렵다·시간이 없다」는 사유가 아니고, 이 건에는 그런 사유조차 없다(주석 문단 삭제·재작성). 함께 고칠 것 둘 — `ledger.rs:966` *"같은 이름이 둘이면 **둘째가** ①이다"*(바로 아래 `:972` 의 단언이 **전원 2** 를 요구한다) · `ledger.rs:949` *"판별자 상한은 `Exact` 이므로"*(`:364-365` 가 *"`identity_ceiling` 을 안 받는다 — 그것이 앞 판의 거짓이었다"* 로 스스로 부정한 규칙이다) |
| ⑦ 을 고치면 `effect/79-delta.md` 에 차이 한 행이 는다 | **함께 늘린다** | `C1` 이 *"사전 등록 ↔ 실제 변경의 차이 **전수**"* 를 요구하고 `## 승격` 칸 3 이 모집단을 「전부」로 못 박았다. 고치고 delta 를 안 늘리면 `C1` 이 조용히 반쪽이 된다. **원인 끝은 `touch/79.txt:N` 이 아니라 정반합이다** — 칸 1 이 세운 기입 형식을 그대로 쓴다 |
| AT2-04 의 `B6` 잔여(판정문이 사전순 동점을 안 적는다) | **안 늘린다 — 발견으로 올린다** | `B6` 은 ⟨결정론적⟩이고 설계문이 *"기계가 판정한다 … 그 산출이 틀렸다는 반론은 **발견**이지 이 판의 판정값이 아니다"* 로 못 박았다. `B6` 이 요구한 **처분**(*"가장 최근으로 본다"*)은 코드(`:6415-6416`)와 판정문(`:6357`) 양쪽에 있다. 안 적힌 것은 그 처분 **안의** 동점 규칙이다. 원장에 싣고 `B6` 의 닫힘은 기계·메인이 판정한다 |
| AT2-02 의 심볼 단위 잔여 | **안 늘린다** | `## 범위 밖` 의 `MS-09` 로 **사전 등록돼 있고**(`plan/79-pre.md` §3) 2층 스키마 없이는 원리상 못 고친다. `## 차선책` 2 가 그 경로를 명시로 막았다. 이번에 늘리면 회차가 스스로 뺀 것을 되집는 것이고 **의도가 변질된다** |
| AT2-01 의 「합 = 분모」 축 음성 대조 미측정 | **안 늘린다 — 발견으로 올린다** | 그 축의 대조는 `B5` 의 음성 대조가 요구한 자(*"가르는 자리를 끄면 빨개지는가"*)를 이미 만족했다(셋이 빨개졌다). 축을 하나 더 다는 것은 **이 회차가 만든 장치를 이 회차가 다시 먹이로 삼는 것**이고 규약 §11③ 이 그것을 15 라운드의 실제 원인으로 적는다. 미측정이라는 사실과 가르는 명령을 적는 것으로 닫는다 |
| 판정문 좌표 교정(위 ⑦) | **안 늘린다 — 초안 수정으로 흡수** | 조건도 코드도 안 건드린다 |

**조건 문면은 한 글자도 안 고친다.** 설계문 `## 이 판이 안 다루는 것` 이 문면 개정을 금하고(*"고친 뒤 재는 양이 줄면 그것은 정정이 아니라 완화이고 승격이다"*), `B1` 의 문면이 의도를 못 잰다는 것은 이 라운드에서 안 드러났다 — `CA2-15` 가 경고한 *"해석이라 기계가 못 판정한다"* 는 정확했고, 그래서 이 판이 돌았고, 갈래는 이슈 본문 자신의 닫는 문장으로 갈렸다.

## 소유자에게

**안 올린다.**

살아남은 반론의 해악도가 **거짓신호 셋 · 미관 둘**이고 **금지역·실패가 없다.** 반이 `금지역` 으로 매긴 `AT2-01` 은 근거를 대어 기각했다 — 그 검산 가지는 자기가 선언한 결함 부류(버킷 겹침·빠짐)에서 실제로 발화하고(`ledger.rs:369` 의 `return` 삭제 → `:705-707`·`:929` 빨개짐), 반의 반증 명령은 합을 보존하는 변이라 그 축에 원리상 안 닿는다. 게이트 표대로 **거짓신호·미관뿐이면 메인이 초안을 고쳐서 진행한다.**

초안 ⑦ 도 안 올린다 — **소유자가 정할 것이 남아 있지 않다.** 실질 물음(「셈법을 그룹 단위로 바꿀 것인가」)은 소유자가 `## 승격` 칸 1 에서 이미 답했고, 남은 것은 그 답을 부정하는 옛 주석 문단을 걷는 **집행**이다. 메인이 혼자 정할 수 있는 일을 올리는 것은 참여가 아니라 책임 전가다.

## 내가 못 정한 것

- **이슈 본문 셋의 1 차 원문.** 전부 `dialectic/preflight.md` 의 인라인 바이트로만 읽었다. 원격 조회를 안 했다(새 실행 금지). ㉠ 의 판정 전체가 *"닫으려면 그 수를 내는 산출 경로가 필요하다"* 라는 한 문장에 걸려 있으므로, **그 한 문장이 원문에 그대로 있는지**가 이 판정의 단 하나의 급소다. 정할 수 있으려면 `gh issue view 79 --json body` 의 바이트와 인라인 블록의 `diff` 가 있어야 한다.
- **`observations/identity-group.txt` 가 HEAD 의 바이트에서 난 산출인가.** 머리가 스스로 *"HEAD `7abb859` · 코드는 **워킹트리**"* 라 적고 그 워킹트리가 `06e8d63` 으로 커밋됐는지는 아무 기록도 안 짓는다. 내가 확인한 간접 증거는 `git log 06e8d63..HEAD -- crates/pal-cli/src/ledger.rs` 가 **빈 출력**이라는 것과 `7abb859` 가 `06e8d63` 의 부모라는 것뿐이다. 정할 수 있으려면 HEAD 에서 `cargo build --release && ./target/release/pal ledger` 를 다시 돌린 산출이 있어야 한다. **`B1` 은 이것에 안 걸리지만 `B5` 는 걸릴 수 있다** — 그 판정은 내 몫이 아니다.
- **그룹 크기 분포(전부 2 인가 · 그룹이 10 개인가).** `effect/79-delta.md:104-107` 이 그렇게 적고 산술 정합(`3309 − 10 + 7 = 3306`)은 맞지만 **유일해가 아니다.** 가르는 명령: `./target/release/pal ledger --symbols | jq -r '[.container|join("."),.name,.kind]|@tsv' | sort | uniq -c | awk '$1>1'`. **아무도 안 돌렸다.**
- **AT2-04 의 모집단이 공집합인가.** `report.md` 가 커밋 안 된 회차가 동시에 둘 이상 설 수 있는지를 구조적으로 확인 못 했다. 정할 수 있으려면 회차 둘을 세우고 `report.md` 를 커밋하지 않은 채 `cargo xtask check` 의 「원장 둘 대조」 판정문을 읽어야 한다. **안 돌렸다.**
- **`findings.jsonl` 에 ⑦·AT2-03·AT2-04·AT2-06 이 이미 등재됐는지.** 설계문이 합에게 스키마 축 아홉만 주고 처분 표는 안 준다. 등재 여부와 중복은 취합 보고와 메인의 몫이다.

---

**관련 파일(절대경로)**
`/Users/incognito/dev/projects/palimpsest/crates/pal-cli/src/ledger.rs` ·
`/Users/incognito/dev/projects/palimpsest/crates/pal-core/src/ledger.rs` ·
`/Users/incognito/dev/projects/palimpsest/crates/pal-core/src/coord.rs` ·
`/Users/incognito/dev/projects/palimpsest/crates/pal-cli/src/query.rs` ·
`/Users/incognito/dev/projects/palimpsest/crates/pal-cli/src/narrative.rs` ·
`/Users/incognito/dev/projects/palimpsest/xtask/src/main.rs` ·
`/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/`
