# 반(反) — 판 5 · 라운드 1/2 · 완수 조건 `D2` ⟨`pal-decision-opponent`⟩

> 낸 자리: `pal-decision-opponent`. **받은 것은 초안 전문과 preflight 전량, `D2` 문면,
> 설계문의 두 절(`## 무엇이 근거로 들 수 있고 무엇이 못 드는가` · `## 세 값의 경계`)뿐이다.**

## 받은 것과 연 것 — 먼저 적는다

- **프롬프트에 대화 기록·메인의 사고 과정은 안 섞여 왔다.** 초안 전문 · `dialectic/5-preflight.md` 전량 · `D2` 문면 · 설계문의 두 절만 받았다.
- **안 열었다** — `intent.md` 의 `## 차선책`·`## 승격` · `state.md` · `findings.jsonl` · `dialectic/[1-4]-*` · `r1-*` · `r2-*`.
- **새 실행(빌드·시험·`pal`·`gh`)을 안 했다.** 쓴 것은 `git`·`grep`·`sed`·`shasum` 뿐이다.
- ⚠ **preflight 밖에서 추가로 연 것**: `crates/pal-core/src/language.rs` · `crates/pal-extract/src/` 파일 목록 · `docs/gates/F11-touch.md` · `docs/gates/F10.md:465` · `docs/sunset.toml` · `.palimpsest/rounds/2026-09-11-effect-confirmation/observations/red.md`(⚠ preflight 의 sha256 표 **밖**이다 — 그것을 좌표로 쓴 자리에 표시했다).

---

## 초안이 서는 자리

무너뜨리지 못한 것 셋을 먼저 적는다.

1. **답 값과 조건 판정을 두 칸으로 분리한 것** — 설계문 `dialectic/5-design.md:19-28` 의 요구를 글자대로 지켰다. 여기서 깰 것이 없다.
2. **ⓐ-1 의 자기 제한** — `docs/adr/0027-…:133` 이 혼자 내는 것이 *"⑧ 을 요구로 유지한다"* 까지라고 초안이 **스스로 잘랐다**(`5-thesis.md:94-99`). 이 자리는 반이 칠 것이 없다. 아래 반론은 **그 간극을 메운다고 든 ⓐ-2~ⓐ-6** 을 친다.
3. **ⓒ 계열을 근거로 안 센 것** — `5-thesis.md:157-162`. 「Rust 추출기를 실제로 세웠다」를 규범 근거로 안 들었고, 그 절제가 실제로 지켜졌다(ⓐ-3 은 예외이고 아래 `AT5-04` 로 친다).

---

## ★ 이 판 고유의 임무 다섯 — 훑은 결과

| # | 임무 | 결과 |
|---|---|---|
| 1 | **is → ought 도약** | **2 건** — `AT5-04`(ⓐ-3 이 서술문에서 규범을 뽑는다) · `AT5-02`(초안이 선언한 다리 두 짝 중 ⓐ-4 가 자기 좌표에서 무너져, 남는 것이 ⓑ(i) 의 *"섰다"* 뿐이 된다) |
| 2 | **물음 치환** | **1 건** — `AT5-03`. ⓐ-1·ⓐ-2 가 전부 ⑧ 에 대한 진술이고 ⑧ 의 정의는 `corpus/criteria.toml:10039` 의 *"`pal touch` 가 결박을 하나 이상 낸다"* — **「결박이 서나」(결과) 축**이다. 초안이 「틀렸다면」 2 로 미리 표시한 자리이고, **재 보니 선다** |
| 3 | **ⓐ 계열이 그 명제를 실제로 말하나** | **여섯 중 직접 말하는 것 0.** ⓐ-1 = 초안 자백(`:94-99`) · ⓐ-2 = `AT5-03` · ⓐ-3 = `AT5-04` · ⓐ-4 = `AT5-02`(발동 조건 미충족) · ⓐ-5 = 전제 조건 논증(초안 자백 `:265-266`) · ⓐ-6 = 초안 자백(`:138-140`). **초안이 스스로 「직접 진술이 아니다」라 적은 것이 셋이고, 나머지 셋 중 둘이 좌표에서 깨진다** |
| 4 | **반대 방향 근거 넷 중 안 다룬 것** | 넷은 형식상 전부 다뤘다. 그러나 **다섯째가 있다** — `AT5-05`(같은 소유자 문서의 §5). 그리고 `00-goals.md:316` 처리는 `AT5-06`·`AT5-07` 로 무너진다 |
| 5 | **절차 압력** | **2 건** — `AT5-10`(대안표 「아니오」 행이 *"D3 에 일이 남는다"* 를 기각 사유 칸에 넣었다) · `AT5-11`(`intent.md:32` 은 **처분** 계획이지 **권한** 진술이 아니다). 초안이 스스로 표시한 자리(`:238-241`)는 **표시만으로 해소 안 된다** |

---

## 반론

| id | 대상 | 주장 | 근거 좌표 | 유효성 | 해악도 |
|---|---|---|---|---|---|
| **AT5-01** | 둘 다 | **기준 핀이 이미 깨진 채로 이 판이 돌았다.** preflight 의 sha256 표 22 건 중 **4 건이 지금 불일치**하고(`intent.md`·`touch/79.txt`·`touch/129.txt`·`effect/79-delta.md`), 그중 `intent.md` 는 초안이 ②의 근거로 든 파일이다. 설계문 반증 **12** 가 문면대로 발화한다 | `dialectic/5-preflight.md:20-45`(핀 표) · `dialectic/5-design.md:344-345` · 돌린 명령 `shasum -a 256 -c`(핀 표 22 건 → FAILED 4) · `git rev-parse HEAD` = `e14157c` ≠ 핀 `a1493b5` · `git log a1493b5..HEAD` 5 건 | **참** | 거짓신호 |
| **AT5-02** | 둘 다 | **ⓐ-4 가 무너진다 — `ADR-0017` 의 규칙이 ⑧ 에 대해 발동하지 않는다.** 그 규칙의 발동 조건은 *"도달 가능한 최대 < 등록된 하한"* 인데 ⑧ 은 **하한 1 을 넘어 통과**였다. 초안이 근거로 댄 바로 그 줄이 *"하한 1은 넘지만"* 이라 적는다 | `docs/adr/0017-…:44-46`(발동 조건) · `docs/gates/F11-touch.md:52`(⑧ 실측 1 · **통과**) · `docs/gates/F11-touch.md:323` *"하한 1은 넘지만 그 1이 무엇 위에 섰는지를 적는다"* · `docs/adr/0027-…:14` *"그 하한은 넘었다"* · 초안 `5-thesis.md:117-125`·`:154-155` | **참** | 거짓신호 |
| **AT5-03** | 답 값 | **ⓐ-2 가 규범을 못 낸다 — 「하한 1」은 이 저장소의 상투 문구다.** 같은 문장이 합격선 일곱 자리에 그대로 있고, ⑧ 의 근거문 자신이 *"이것은 ①의 근거가 아니다"* 로 증거력을 스스로 뺀다. *"등록된 합격선은 「그래야 한다」의 문법"* 이라는 초안의 전제가 여기서 깨진다 | `corpus/criteria.toml:10044` *"하한 1 은 이 저장소의 하한 관례다"* · `:10043` · `:10039` · 같은 문장 `corpus/criteria.toml:8957`·`9139`·`9505`·`9541`·`9871`·`9913`·`9977` · 초안 `5-thesis.md:101-107` | **참** | 거짓신호 |
| **AT5-04** | 답 값 | **ⓐ-3 은 ⓒ(드러난 선호)를 ⓐ 로 갈아입힌 것이다.** 초안이 든 「사유 문장」 둘이 전부 **서술문**이고, 그 절에 있는 유일한 명령문은 *"Rust 를 다섯째 1급 언어로 올린다"* 다. 「막혔다」에서 「막히면 안 된다」로 건너뛴 자리다 | `docs/instructions/2026-08-20-owner-direction.md:30`·`:39-40`(서술) · 같은 문서 `:12`·`docs/adr/0027-…:36`(유일한 명령문) · `dialectic/5-design.md:113`(ⓒ 규칙) · 초안 `5-thesis.md:109-115` | **참** | 거짓신호 |
| **AT5-05** | 답 값 | **같은 소유자 문서가 반대쪽을 말하는데 초안이 그 절을 안 들었다.** §5 는 *"자기 저장소 117 파일만으로는 R-19 의 편향을 못 피하고 추출기가 palimpsest 의 코딩 습관에만 맞춰질 수 있다"* 로 **외부 코퍼스를 고른** 판단이다. ⓐ 계열이 한 방향으로 만장일치라는 인상이 선택 인용으로 만들어졌다 | `docs/instructions/2026-08-20-owner-direction.md:16`(§5 판단 행) · `:105-106` · 초안이 인용한 자리 `5-thesis.md:109-115`(§1 만) | **참** | 거짓신호 |
| **AT5-06** | 답 값 | **`00-goals.md:316` 은 낡은 줄이 아니라 채택된 ADR 이 재확인한 줄이고, 채점 순위가 ⑧ 보다 위다.** `ADR-0025` 가 비목표 표에서 한 줄만 빼며 *"같은 표의 나머지는 그대로다"* 라 못 박았고(2026-08-18, ADR-0027 이틀 전), `00-goals.md:3` 은 자기를 *"모든 기능 문서의 채점 기준"* 이라 선언한다. 초안의 「목적/요구」 구분선은 **저장소 어느 문서에도 없는 초안의 조어**다(초안 자백 `:259-262`) | `docs/adr/0025-…:34` · `docs/plan/00-goals.md:3` · `:316` · 초안 `5-thesis.md:172-179` | **참** | 거짓신호 |
| **AT5-07** | 답 값 | **초안 안에서 모순한다.** `:176` 은 *"실사용 = 첫 릴리스 정의"* 를 끌어와 **이 저장소를 그 첫 자리**라 부르는데, 같은 초안의 구속 문장 후반절과 R-19 가 *"자기 저장소에서 난 값을 합격의 근거로 쓰지 않는다"* 를 못 박는다. 릴리스 판정은 합격 판단이다 — 한 문단에서 금지한 것을 다른 문단에서 쓴다 | 초안 `5-thesis.md:176` ↔ `5-thesis.md:56-57` · `docs/plan/00-risks.md:410` · `corpus/criteria.toml:10043` | **참** | 거짓신호 |
| **AT5-08** | 조건 판정 | **②의 「이 회차의 측정을 근거로 댔다」가 실질에서 안 선다.** 초안 자신이 *"측정은 규범을 안 낳는다"* 라 적었고, 다리로 세운 ⓐ-2·ⓐ-4 는 회차 **이전**의 문서다 — 이 회차 측정을 전부 빼도 「예」가 그대로 선다. 게다가 잠긴 의도가 **큐레이션 축은 이 표본 위에서 원리상 안 재어진다**고 미리 적었다(이 판의 명제가 바로 큐레이션이다) | `intent.md:244`(조건 문면) · `intent.md:104-108` *"목표 1(큐레이션 …)은 이 표본 위에서 원리상 안 재어진다"* · 초안 `5-thesis.md:147`·`:154-155` | **참** | 거짓신호 |
| **AT5-09** | 조건 판정 | **읽기 ⓐ 를 고른 논증이 비약이다.** *"판정 기구를 지정한 조건이 그 기구의 산출을 답의 자리에서 빼면 조건이 자기를 못 채운다"* 는 **판정 기구**와 **답이 서는 자리**를 한 낱말로 묶는다. ⓐ′ 에서도 정반합이 판정하고 답만 게이트에 실린다 — 자기 모순이 안 난다. 그러면 ②를 통과로 미는 유일한 논거가 사라진다 | 초안 `5-thesis.md:31-33`·`:39-41` · `docs/gates/effect-confirmation.md:27`(D2 가 지금 `미측정` 칸에 있다) · `dialectic/5-preflight.md:118-133`(`## 효과` 에 `D2` 절 없음) | **참** | 거짓신호 |
| **AT5-10** | 답 값 | **절차 압력이 대안표에 남았다.** 「아니오」의 기각 칸이 *"「아니오」를 고르면 … 남고 그것은 `D3` 의 일"* 을 사유로 싣는데, 설계문은 그 잔여를 **기각 사유가 아니라 단지 적을 것**이라고 미리 정했다. 「남는 일」이 값 선택의 근거가 된 자리다 | 초안 `5-thesis.md:234` · `dialectic/5-design.md:169-171` · `dialectic/5-design.md:117`(금지 ①) | **참** | 거짓신호 |
| **AT5-11** | 답 값 | **「아직」을 배제한 유일한 좌표가 권한 진술이 아니다.** `intent.md:32` 가 답하는 물음은 *"`#66` 을 어떻게 **처분**하나"* — 절차 처분 계획이지 *"규범을 누가 정하나"* 가 아니다. 소유자가 이 회차에 직접 쓴 원문은 **효과 확인 단계만** 열라 하고 물음 1 의 답을 위임하지 않는다. 저장소 다섯 자리는 지금도 *"소유자에게 묻는 중"* 을 현재형으로 적는다 | `intent.md:32`(물음이 「처분」이다) · `intent.md:8-19`(소유자 2026-09-11 원문) · `dialectic/5-preflight.md:284-313`(다섯 자리) · `dialectic/5-design.md:177`(전형 ①) · 초안 `5-thesis.md:235`·`:267-270` | **참** | 거짓신호 |
| **AT5-12** | 답 값 | **구속 한 문장이 새 구속을 안 낳는다 — 「예」의 요건 3 이 형식만 찬다.** 전반절은 채택된 `ADR-0017` 의 규칙을 그대로 옮긴 것이고 후반절은 R-19·⑧ 근거문이 이미 지고 있다. 새것이 있다면 그것은 **`ADR-0017` 의 적용 범위를 「등록된 합격선」에서 「`pal` 이 못 서는 모든 자리」로 넓힌 것**인데, 그 확장을 받치는 ⓐ 좌표가 없다 | 초안 `5-thesis.md:54-57` · `docs/adr/0017-…:36`·`:44-46` · `docs/plan/00-risks.md:410` · `corpus/criteria.toml:10043` · `dialectic/5-design.md:162-163` | **참** | 거짓신호 |
| **AT5-13** | 답 값 | **「예」의 면제 절이 소유자가 세운 물음 1 의 틀을 비켜간다.** `#66` 둘째 댓글은 물음 1 을 *"Markdown 도 함께 걸린다 — 문서 결박(`#69`)과 같은 벽"* 위에 세웠는데, 초안의 *"무엇은 안 해도 되나"* 가 **자기 저장소 커버리지를 통째로 면제**한다. 미결박의 큰 몫이 Markdown 577 파일이므로, 이 「예」는 소유자가 물은 벽에 대해 「아니오」와 구별되는 귀결을 안 낸다 | `dialectic/5-preflight.md:109-111`(소유자 댓글) · 초안 `5-thesis.md:60-61` · `observations/issue-66-now.txt:18`(Markdown 577) | **참** | 거짓신호 |
| **AT5-14** | 둘 다(부수 발견) | **부수 발견 ③ 이 오독이다 — 「추출기 둘」과 「enum 넷→다섯」은 다른 것을 센다.** 이 회차의 관측 파일이 이미 갈라 놓았다: *"오늘 HEAD 의 코드가 실제로 가진 추출기 **3** — `Capable::Present` 가 Kotlin·TypeScript·Rust"*. 그러므로 Rust 이전의 추출기는 **둘**이고 `ADR-0027:22` 는 맞다. **채택된 ADR 이 낡았거나 틀렸다는 진술이 거짓인 채로 원장에 실린다** — 회차가 스스로 금지역으로 등록한 「사실이 아닌 것을 사실로」다 | `observations/red.md:184`(⚠ preflight sha 표 **밖** 파일) · `crates/pal-extract/src/`(`kotlin.rs`·`typescript.rs`·`rust.rs`) · `crates/pal-core/src/language.rs:24-30` · `docs/gates/F11-touch.md:316-317` *"추출기는 셋이다"* · `intent.md:86-88`(금지역) · 초안 `5-thesis.md:205-209`·`:313-316` | **참** | 금지역 |
| **AT5-15** | 답 값(근거) | **초안이 든 「783 파일」이 이 회차의 게이트 값과 다르다.** 게이트 `D1` 은 같은 축을 **812 파일**로 적는다. 초안은 두 관측의 HEAD 가 다른 것을 알면서 *"내가 든 값은 두 자리에서 같다"* 라 적었는데, 783 은 같지 않다 | 초안 `5-thesis.md:150`·`:290-293` · `observations/issue-66-now.txt:28`(783 · HEAD `49a6b6b`) · `docs/gates/effect-confirmation.md:56`(812 · HEAD `9e092e3`) | **참** | 미관 |
| **AT5-16** | 답 값(근거) | **좌표가 한 줄 어긋났다.** 초안이 *"결박 ≥ 1"* 의 자리를 `corpus/criteria.toml:10038` 이라 적었는데 실제는 `:10039` 다. 판정문이 원장으로 옮겨지면 고칠 수 없는 좌표다 | 초안 `5-thesis.md:103` · `corpus/criteria.toml:10039` | **참** | 미관 |

---

## "이 반론이 틀렸다면 무엇으로 아는가"

- **AT5-01** — 합이 `shasum -a 256 -c`(preflight 핀 표)를 다시 돌려 22/22 OK 가 나오면 틀렸다. ⚠ 내가 확인한 완화 사실도 같이 적는다: `intent.md` 의 드리프트는 전부 **`:451` 이후**(커밋 `30488ae` 의 `@@ -451`, 워킹트리의 `@@ -486`)라 초안이 인용한 `:32`·`:36`·`:104-116`·`:244` 는 **바이트가 같다**. 그래도 핀 문면(*"정·반 둘 다 같은 핀에서 읽는다"*)은 거짓이다. ★ 합이 조항 12 를 **두 번째로** 면제하면 그때는 `intent.md:86-88` 의 「측정이 죽은 가지」로 올라간다(칸 6 이 *"조항 8 이 이 회차에서 한 번도 이빨을 안 썼다"* 를 이미 적었다).
- **AT5-02** — `ADR-0017` 안에 *"하한을 넘었더라도 도달 가능한 최대의 **질**이 낮으면 같은 처방을 낸다"* 는 취지의 줄이 있으면 틀렸다. 내가 `docs/adr/0017-…` 전문에서 못 찾았다. 반대 자료 하나를 숨기지 않는다 — `docs/adr/0027-…:28` 이 *"[ADR-0017] 이 이름 붙인 「자가 짧다」가 이 저장소 자신에게 일어난 것"* 이라 적는다. 그러나 그것은 **이름의 원용**이고 `:44-46` 규칙의 **발동 조건 충족**이 아니다.
- **AT5-03** — `corpus/criteria.toml` 의 ⑧ 절이 다른 합격선과 달리 *"이 하한은 관례가 아니라 제품 요구다"* 를 적고 있으면 틀렸다. `:10034-10045` 전체를 읽었고 없다.
- **AT5-04** — `docs/instructions/` 어딘가에 *"자기적용이 막히면 안 된다"* 계열의 **명령문**이 있으면 틀렸다. `grep -rn '자기적용' docs/instructions/` 가 낸 네 자리를 다 읽었고 전부 서술이다.
- **AT5-05** — §5 가 *"자기 저장소로도 충분하지만 보태서 고른다"* 로 읽히면 약해진다. `:105-106` 은 *"못 피하고"* 라 적는다.
- **AT5-06** — `ADR-0025` 이후 `00-goals.md:316` 을 다시 뺀 채택 문서가 있으면 틀렸다. `git log -- docs/plan/00-goals.md` 로 갈린다(내가 안 돌렸다).
- **AT5-07** — 초안의 구속 후반절이 「합격」에 릴리스 정의를 안 포함한다고 읽히면 약해진다. 그때는 「합격」의 범위를 초안이 좌표로 적어야 한다.
- **AT5-08** — 정이 *"이 측정 하나를 빼면 답 값이 뒤집힌다"* 를 좌표 하나로 대면 이 반론은 죽는다. 지금 초안의 ⓑ 절(`:142-155`)은 반대로 *"측정은 규범을 안 낳는다"* 를 적는다.
- **AT5-09** — `D2` 문면에 *"답은 정반합 산출물에 선다"* 가 있으면 틀렸다. `intent.md:244` 전문에 없다(그 부재가 초안의 부수 발견 ① 이다).
- **AT5-10** — 설계문이 잔여 업무를 기각 사유로 허용했으면 틀렸다. `:169-171` 은 *"남긴다고 적기만 한다"* 다.
- **AT5-11** — `intent.md` 의 `## 승인` 절이 이 잠금을 **소유자 서명**으로 못 박고 있으면 약해진다. ⚠ **나는 그 절을 안 열었다**(받은 입력 밖이다). **열어 봐야 갈린다** — `sed -n '/^## 승인/,/^## /p' .palimpsest/rounds/2026-09-11-effect-confirmation/intent.md`.
- **AT5-12** — *"`pal` 이 못 서는 자리 전부"* 로의 확장을 받치는 ⓐ 좌표를 정이 대면 틀렸다.
- **AT5-13** — 초안이 *"커버리지 면제는 Markdown 을 뺀다는 뜻이 아니다"* 를 좌표와 함께 적으면 약해진다.
- **AT5-14** — `Capable::Present` 가 Rust 이전에 셋 이상이었음을 보이는 커밋이 있으면 틀렸다. `git show a1493b5:crates/pal-extract/src/extractor.rs` 와 `observations/red.md:176-191` 이 반대를 적는다. ⚠ 근거 하나(`red.md`)가 **핀 표 밖**이므로, 합은 핀 안 좌표(`docs/gates/F11-touch.md:316-317`)로 같은 사실을 다시 받을 수 있다.
- **AT5-15** — 783 과 812 가 다른 축을 세는 수이면 틀렸다(둘 다 *"결박 불가 언어 7 개"* 옆의 파일 수다).
- **AT5-16** — 없다. `sed -n '10039p' corpus/criteria.toml` 이 판정한다.

---

## 돌려 봐야 갈리는 것

1. **`#66` 이 지금 열려 있나** — 초안이 *"직접 못 쟀다"* 고 적었다(`5-thesis.md:275-279`). 나도 못 쟀다(`gh` 금지). `gh issue view 66 --json state,closedAt`. ⚠ 그리고 `docs/adr/0027-…:3` 이 *"[#66] 종료 시점에 발행"* 이라 적힌 채 **채택**돼 있어, 이슈가 열려 있으면 절차 기록이 이미 어긋나 있다.
2. **핀 재검** — `shasum -a 256 -c`(preflight `:23-44`) · `git rev-parse HEAD`. 내가 돌렸고 **4 건 FAILED**다(`AT5-01`).
3. **`00-goals.md:316` 의 생존** — `git log --oneline -- docs/plan/00-goals.md`.

---

## 내가 스스로 물린 것 — 세웠다가 근거를 못 댄 것

1. **"ⓐ-5(`AGENTS.md:7`)는 「쓰나」 축이라 물음 치환이다"** — 세웠다가 접었다. `AGENTS.md:7` 은 그래프 **사용**을 지시하지만 그 지시가 성립하려면 색인이 있어야 한다는 초안의 전제 조건 논증이 형식상 틀리지 않는다. **좌표로 깰 수 없었다.**
2. **"ⓐ-6(`docs/sunset.toml:15`)은 문서 폐기 트리거일 뿐 제품 규범이 아니다"** — 초안이 `:138-140` 에서 **이미 같은 간극을 자백**했다. 새롭지 않아 뺀다.
3. **"②가 자기 지시적이다"** — 초안이 `:78-79` 에서 먼저 적었다. 새롭지 않아 뺀다(내가 새로 댄 것은 `AT5-09` 의 비약 하나뿐이다).
4. **"`docs/sunset.toml` 의 MVP 트리거가 이미 참이면 `sunset.toml` 이 자기 검사를 빨갛게 한다"** — `.palimpsest/rounds/*/*.json` 존재 여부를 안 셌고(새 실행 금지 범위를 넓게 잡았다), 이 판의 명제와도 멀다. **좌표 없이 세우지 않는다.**
5. **"「예」가 `intent.md:36`(「이 저장소에서 섬」까지만 주장한다)을 넘는다"** — 구속 문장이 **다음 회차**를 묶는다는 점에서 넘는다고 볼 여지가 있으나, `:36` 이 제한하는 것은 **주장의 범위**이지 **규범의 시간축**이라고 읽을 좌표를 못 댔다. 뺀다.
