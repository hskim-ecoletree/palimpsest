# 반(反) — 완수 조건 `E3` ⟨판 6 · 라운드 1⟩

> **받은 것** — 초안 전문 `dialectic/6-thesis.md` · 판정 대상 `report.md` 전문 · 기준 핀+기계 사실
> `dialectic/6-preflight.md` 전량 · 설계 발췌 `dialectic/6-design-for-antithesis.md` · 잠긴 의도 발췌
> `dialectic/6-intent-for-antithesis.md` · `.claude/skills/round/SKILL.md` · 저장소 읽기(`git`·`grep`·
> `shasum`·`ls`·`gh` 읽기).
> **안 받았다** — 메인의 사고 과정·대화 기록(**과업문에 섞여 오지 않았다. 0 건**) · `intent.md` 원본 ·
> `findings.jsonl` · `state.md` · `dialectic/6-design.md` 원본 · `dialectic/6-prompts.md` ·
> `dialectic/` 의 `1-*`~`5-*`·`r1`~`r4-raw`·`*-referee`·앞 preflight 들 · `review/` 전량 ·
> `report.md` 를 고칠 권한.
>
> **금지 이탈 — 없다.** 위 「열지 마라」 목록의 어느 파일도 열지 않았다. `6-prompts.md` 는 `git status`
> 에 이름으로만 잡혔고 **열지 않았다.**
> ⚠ **두 가지를 스스로 신고한다.** ⑴ `SKILL.md` 를 과업문이 지정한 세 구간(`:778-802`·`:348-358`·
> `:960-975`) **밖**에서도 `grep`·`sed` 로 읽었다(`:545-575`, 특히 **`:560`**). 그 파일은 **핀 안**이고
> 과업문이 `grep` 을 허락했으므로 핀 밖 바이트가 아니다. ⑵ `docs/gates/effect-confirmation.md` 를
> `:55-59`·`:335-339`·`:408-412`·`:514-518` 만 열었다 — **핀 안 파일**이고(`6-preflight.md:54`) 초안이
> 근거로 쓴 좌표를 대조하기 위해서다.
>
> **새 실행 — 0 건.** 빌드·시험·`pal`·`cargo xtask check`·`gh` 쓰기를 하지 않았다.

## 핀 여섯을 다시 댔다 ⟨조항 ㉮⟩

```
1f8c8c50acdf5f5387271d4e275a3f534ae9a4f6dfe3bc4794c90cddd3a21aa7  report.md            ✓ 일치
4fa4c2509349bbc51c2afe017c945c33050aae86926f66c0f6db8a8bc2e08759  SKILL.md             ✓ 일치
d306d950143e5a49d0b73f670098ee48207d9c43babbafc89bf6a3476bbe65d0  effect-confirmation  ✓ 일치
intent.md · state.md · findings.jsonl — 안 열었으므로 해시를 대지 않았다(연 것만 댄다)
git diff HEAD -- report.md → 공백. 판정 중에 안 고쳐졌다 ⟨설계 반증 13⟩
```

---

## 임무 여섯 — 명시적 결과

| # | 임무 | 결과 |
|---|---|---|
| 1 | **전수 표에서 빠진 항** | **3 건** — `report.md:71-72`·`:108`·`:136`. 단 **초안 자신의 기준(문단도 항)** 아래서만 빠진 것이다. 반대 기준에서는 **초안이 8 건을 더 넣은 것**이다 → `A1`·`A2` |
| 2 | **경계 옷을 입은 잔여** | **3 건** — 항 4(`:131`, 요건 ⑵ 가 「없다」) · 항 16(`:81`) · 항 19(`:94`, 요건을 「해당 없음」으로 면제) → `A8`·`A9` |
| 3 | **새 이름의 음성 대조** | 초안은 `§2` 에서 **발화를 적었다**(설계 반증 3 통과). 다만 근거 3(*"회차 고유의 기록 요구"*)에 좌표가 없다 — 핀 안 좌표가 실재한다(`dialectic/6-intent-for-antithesis.md:17` = `intent.md:666` *"종료 보고의 하네스 실패 항이 이 둘을 진다"*). **초안에 유리한 방향의 미인용**이므로 반론으로 안 올린다 |
| 4 | **선례 정당화 · 절차 압력** | **절차 압력 0 건.** `E5`·「회차가 안 끝난다」·「고치면 된다」가 근거 자리에 없다. `6-thesis.md:104` 의 *"하나라도 나타나면 회차가 안 끝난 것이다"* 는 `SKILL.md:780` **인용**이지 압력이 아니다. **선례 정당화 0 건**(근거 자리). 경계 1 건 — `6-thesis.md:139` 가 저장소 선례(`docs/gates/effect-confirmation.md:373-380`)를 인용하나, 자기 판정의 **약점을 세우는 손잡이 절**이고 값의 근거가 아니다 |
| 5 | **「다음 회차가 받는 것」의 이슈** | **preflight ⑦ 과 초안이 안 갈린다.** `gh issue list` 로 다시 댔다 — `#140`·`#141`·`#142` 셋 다 `OPEN`, 그 위로 **`#143` 은 없다**. 즉 `report.md:116-117` 의 *"이슈로 안 섰다"* 는 **지금도 참**이다. 초안의 사실 주장은 선다. 갈리는 것은 **그 사실의 값**이다 → `A1`·`A6` |
| 6 | **반대 방향(잔여→경계)** | **4 건 + 1** — `A3`(능력 부재 넷) · `A4`(항 8) · `A5`(항 9) · `A6`(항 21) · `A12`(항 5, 근거만) |

---

## 초안이 서는 자리 — 무너뜨리지 못했다

1. **항 전수 표가 실재하고 값이 빠진 칸이 없다.** 스물한 항 전부에 값이 붙었다 ⟨설계 반증 6·7 통과⟩.
2. **가르는 문장이 10 회 등장한다**(`grep -c "할 수 있었" 6-thesis.md` → `10`) ⟨설계 반증 8 통과⟩.
3. **ⓐ/ⓑ 를 갈랐다** — `6-thesis.md:108` 이 *"문면은 초록이다 … 이 반증은 오직 실질에서"* 로 적는다
   ⟨설계 반증 10 통과⟩. preflight ③ 이 빨개진 사실도 `:11` 에 실렸다 ⟨설계 반증 9 통과⟩.
4. **`report.md` 를 실제로 열었다** — 스물한 좌표를 바이트로 대조했고 **전부 맞는다** ⟨설계 반증 1 통과⟩.
5. **인용한 핀 안 좌표가 실재한다.** 내가 대조한 넷 다 그렇게 적혀 있다 —
   `docs/gates/effect-confirmation.md:57`(*"되돌릴 수 없다 … 증거 위조다"*) · `:337`(*"⑶ 의 호출자
   목록을 `grep` 이 지었다"*) · `:410-411`(*"판정이 난 뒤 증거를 고치면 판정의 근거가 움직인다"*) ·
   `:516`(*"이 회차의 재는 자리 밖"*). **인용 위조는 0 건이다.**
6. **시각 주장이 선다.** `#142` = `2026-09-12T07:59:50Z`, `c746038` = `2026-09-12T09:20:43Z`.
   차이 **1 시간 20 분 53 초**(초안은 *"1 시간 21 분"* — 반올림이고 어긋남이 아니다).
7. **판정값 자체는 내 반론 열둘을 다 먹여도 안 뒤집힌다.** 아래 `## 값 감도` 를 보라.

---

## 반론

| id | 대상 | 주장 | 근거 좌표 | 유효성 | 해악도 | 이 반론이 틀렸다면 무엇으로 아는가 |
|---|---|---|---|---|---|---|
| **A1** | ⓑ 집계 + 항 13·21 | **판정 단위가 설계·기계와 어긋난다.** 설계는 단위를 *"최상위 불릿·행 전수"* 로 못 박는데 `report.md` 에는 **최상위 불릿이 0 개**다(`grep -n "^[-*] " report.md` → 0 건). 남는 것은 표 행뿐이고, 초안의 21 항 중 **여덟은 문단**이다(`:81`·`:83`·`:87`·`:94`·`:97`·`:101`·`:116`·`:144`). 그리고 **핀 안 preflight 가 문단을 항에서 이미 뺐다** — ⑥ 이 `## 범위 밖`(산문 2 줄)을 **0 항**으로, ⑦ 이 `## 능력 부재`(표 3 행+문단 1)를 **3 항**으로 냈다. 기계의 조작적 정의가 「행」이다. 그 정의로 세면 단위는 **13 항**이고 **잔여의 기둥 둘(`:116`·`:101`)이 단위에서 빠진다** | `dialectic/6-design-for-antithesis.md:14` · `dialectic/6-preflight.md:121`·`:138` · `report.md` 의 `^[-*] ` = 0 건 · `6-thesis.md:42`(초안이 기계의 3 을 4 로 덮는 자리) | **참** | 거짓신호 | 설계문 원본(`6-design.md`, **내가 못 연다**)이 「행」을 문단까지 포함하도록 정의한 문장을 다른 데 갖고 있으면. 또는 preflight ⑥·⑦ 의 계수기가 문단을 세도록 작성돼 있었는데 우연히 0·3 이 나온 것이면 — **이것은 돌려 봐야 갈린다**: preflight 를 지은 스크립트로 `## 하네스가 설계대로 안 돈 것` 의 항 수를 뽑아 8 이 나오는지 2 가 나오는지 본다 |
| **A2** | ⓑ 집계 | **초안 자신의 기준(문단도 항)을 그대로 적용하면 전수 표가 전수가 아니다.** 문단 세 개가 표에 없다 — `report.md:71-72`(★★ *"이 절이 이 회차의 절반이다 … 소유자가 칸 14 에서 … 그 대가를 여기 적는다"*, **새 이름 절이 우회가 아님을 보이는 유일한 문단**) · `:108`(*"셋 다 이슈로 섰고"*) · `:136`(*"셋이고 셋 다 이 회차가 관측만 했다"*). 초안은 `:81`(*"둘 다 메인에게서 나온 것이다"* — 한 문장 귀속)을 항으로 세면서 `:71-72` 를 안 셌다. **머리글이라 뺐다면 `:81` 도 항이 아니다** | `report.md:71-72`·`:108`·`:136` ↔ `6-thesis.md:59-68`(새 이름 절 표에 `:71` 없음)·`:34-39`(능력 부재 표에 `:136` 없음)·`:46-51`(`:108` 없음) | **참** | 거짓신호 | `6-design.md` 원본이 *"절의 머리글 문단은 항이 아니다"* 를 명시하면. 그러면 `A1` 쪽만 남는다 |
| **A3** | 항 6·7·8·9 ⟨반대 방향⟩ | **「만들 것」 전환 부재는 문자열의 부재일 뿐 요건의 부재가 아니다.** 규약은 `SKILL.md:560` 에서 *"그래프가 못 답하면 「능력 부재」로 내고, 모델 추정이 그 자리를 메운 사실을 적는다. **그 목록이 다음에 「만들 것」이다**"* 라 적는다 — **목록 자체가 전환**이고 별도 포인터를 요구하는 문장이 규약에 없다. `:801` 의 `→ 「만들 것」` 은 같은 사상(寫像)의 축약이다. 그리고 `report.md:136` 의 *"셋 다 이 회차가 관측만 했다"* 는 **`:560` 이 적으라고 한 바로 그 사실**이다 — 준수이지 회피가 아니다. 초안의 요건 ⑵ ✗ 는 `grep "만들 것" → 0 건` 하나에 걸려 있고, 그것은 설계가 반증 2 에서 이름 붙인 **문자열 검색으로 끝내는 형태**다 | `SKILL.md:560` · `SKILL.md:801` · `report.md:136` · `6-thesis.md:32`(근거가 grep 0 건)·`:41`(*"네 항 전부가 같은 한 가지로 걸린다"*) | **참** | 거짓신호 | `SKILL.md` 어딘가가 *"능력 부재 항마다 만들 것을 이슈/포인터로 건다"* 를 적고 있으면. 나는 `grep "만들 것" SKILL.md` 로 `:560`·`:801` 둘뿐임을 확인했다 |
| **A4** | 항 8(`report.md:142`) ⟨반대 방향⟩ | **항 자신이 「원리상」이라 적는데 초안이 잔여로 매겼다.** 셀의 마지막 문장은 *"2 층에 그 값이 없어 **원리상 못 고친다**"* 다. 가르는 문장의 「그것」은 **항이 이름한 일**(원인을 가르게 하는 것)이지 「이슈 세우기」가 아닌데, 초안은 답을 *"그렇다 — 같은 회차가 이슈 셋을 세웠고"* 로 적었다. **「이슈로 섰다」는 `## 다음 회차가 받는 것` 의 요건**이고 설계 요건표가 `## 능력 부재` 에 안 건 것이다. 다른 절의 요건을 수입해 가르는 문장의 답을 만들었다 | `report.md:142` · `6-thesis.md:38`(답이 *"그렇다 — 같은 근거"*, 그 근거가 `:36` 의 이슈 셋) · `dialectic/6-design-for-antithesis.md:25`(능력 부재 요건에 이슈가 없다) | **참** | 거짓신호 | `report.md:142` 의 *"원리상 못 고친다"* 가 2 층 설계에 대한 것이지 회차가 할 수 있었던 일에 대한 것이 아니라고 읽히면. 그 경우 이 항은 `## 원리상 못 잰 것` 에 잘못 앉은 것이 되어 **다른 종류의 발견**이 된다 |
| **A5** | 항 9(`report.md:144`) ⟨반대 방향⟩ | **초안이 「재는 자리 밖」을 인용하고도 「할 수 있었다」로 답했다.** 핀 안 게이트가 `docs/gates/effect-confirmation.md:516` 에서 *"고치는 자리는 표면 어휘 쪽이라 **이 회차의 재는 자리 밖**이다"* 라 적고, 항은 이미 한 몫(*"`ledger.rs` 의 주석이 이제 셋을 가른다"*)을 싣는다. 한 몫은 했고 남은 몫은 범위 밖인데 값이 **잔여(넷 중 가장 약하다)** 다 — 초안이 그 인용을 **자기 값에 반대되는 자리에** 붙였다 | `docs/gates/effect-confirmation.md:516`(내가 바이트로 대조했다) · `report.md:145` · `6-thesis.md:39` | **참** | 거짓신호 | 「재는 자리 밖」이 **잠긴 의도의 범위 밖**이 아니라 **이 회차가 고르지 않은 것**을 뜻하면. 그러면 (가) 선택으로 돌아가고 `A6` 의 판단이 이 항에도 적용된다 |
| **A6** | 항 21(`report.md:101-102`) ⟨반대 방향 · **초안의 기둥 둘 중 하나**⟩ | **착수 시점에 잠근 범위 선택을 잔여로 세면 `## 범위 밖` 이라는 이름 자체가 붕괴한다.** 항의 사유는 *"회차가 **착수 시점에** (가)를 골랐다"* 이고, 규약은 `SKILL.md:798` 에서 `## 범위 밖` 을 **「착수 때 정한 것」**으로 못 박아 **허락**한다. §10 이 겨냥한 병은 `:782` 의 *"섞이면 앞의 것이 뒤의 것 **외양을 입는다**"* 인데, 이 항은 선택·시점·대가를 **명시**한다 — 외양을 입은 것이 아니라 벗긴 것이다. 초안의 독해대로면 모든 회차의 `## 범위 밖` 전량이 잔여가 되고, 규약이 다섯째 이름을 둔 까닭이 사라진다 | `report.md:101-102` · `SKILL.md:798` · `SKILL.md:782` · `6-thesis.md:70`(*"이 항의 문면은 「의도적으로 안 한 것」의 정의 그 자체다"*) | **참** | 거짓신호 | 잠긴 의도의 (가) 선택이 `## 범위 밖` 이 아니라 `## 순서`·`## 승격` 에서 **회차 중에** 내려진 것이면. 나는 `intent.md` 를 못 열어 **그 시점을 못 댔다** — 핀 안에 들어와야 갈린다: `intent.md` 에서 (가)/(나) 선택 문장의 줄 번호와 그것이 `## 범위 밖` 안인지 |
| **A7** | 항 21 의 근거 구조 | **항 21 의 「그렇다」가 핀 밖 바이트에만 걸린다 — 초안이 스스로 물은 분리가 안 선다.** 초안은 1 차 근거(핀 안)와 보조 관측(핀 밖)을 갈랐다고 적지만, **핀 안 근거는 「이슈를 세울 수 있었다」만 세운다.** 항이 이름한 것은 **자리(장치)** 이고, `report.md:117` 이 *"이슈로 세우는 것이 **첫 걸음**"* 이라 적어 **자리 ≠ 이슈**를 못 박는다. *"그 자리가 실제로 섰다"* 를 대는 유일한 바이트는 핀 밖 `dialectic/6-prompts.md` 의 존재다. 설계 반증 4(*"판정문이 기준 핀 밖의 바이트에 기댄다 → 조항 ㉮"*)가 겨누는 자리다. ⚠ **규칙 문서 인용은 이 반론에 안 걸린다** — `6-design.md` 를 규칙으로 인용하는 것은 증거 바이트 의존이 아니다. `6-prompts.md` 는 **세계에 대한 사실**로 쓰였다 | `6-thesis.md:70-72`(1 차 근거와 보조 관측) · `6-thesis.md:7`(*"넷째의 **존재**만 … 보조 관측으로 쓴다"*) · `report.md:117` · `dialectic/6-design-for-antithesis.md:64` | **참** | **금지역** | `report.md:101` 의 *"그 자리"* 가 「이슈」로 읽히면 — 그러면 핀 안 근거만으로 선다. `:117` 이 둘을 가르므로 나는 안 읽힌다고 본다. ⚠ **해악도 자기 점검** — 값을 안 바꾸고(잔여 6 으로 줄 뿐) 판의 유효성만 건드린다. 설계가 이 자리를 **명시적 무효 조항**으로 적어 뒀기에 `금지역` 으로 매겼다. 「거짓신호」로 내리는 판단도 정당하다 |
| **A8** | 항 4(`report.md:131`) | **설계 요건표가 이미 잔여로 못 박은 칸을 초안이 규약 한 줄로 덮었다.** 설계는 경계 요건을 *"**전부** 채워야 한다"* 로, 잔여를 *"가르는 문장에 「할 수 있었다」가 나오**거나**, 위 요건을 **하나라도** 못 채운 채 그 절에 앉아 있다"* 로 적는다 — **OR 이다.** 이 항은 요건 ⑵ 칸이 **「없다」**이므로 가르는 문장의 답과 **무관하게** 잔여다. 초안은 `SKILL.md:784` 를 정본 삼아 OR 을 덮어썼고, 그것은 설계가 `:39-41` 에서 금한 **조건 문면의 축소**와 같은 동작이다(그 조항은 합을 겨누지만 축소 자체는 같다) | `dialectic/6-design-for-antithesis.md:25`·`:26`·`:39-41` · `SKILL.md:800` · `report.md:131` · `6-thesis.md:28`(*"나는 `SKILL.md:784` 의 가르는 문장을 정본으로 삼아 경계로 매긴다"*) | **참** | 거짓신호 | 설계가 요건표보다 `SKILL.md:784` 를 상위로 두는 문장을 `6-design.md` 원본에 갖고 있으면. 발췌(`:45` *"정본은 규약 §10 이다"*)가 그럴 여지를 남긴다 — 다만 `:800` 자신이 *"조건이 없으면 그것은 경계가 아니라 잔여다"* 라 적으므로 규약을 정본 삼아도 잔여다 |
| **A9** | 항 16(`:81`)·19(`:94`) | **요건을 「해당 없음」으로 면제하고 경계를 줬다.** 새 이름 절의 경계 요건은 *"그 이름이 네 이름 중 하나의 **우회가 아님**을 **항의 내용으로** 보인다"* 하나뿐인데, 초안은 두 항의 판정 칸을 **「해당 없음(귀속 기록)」·「해당 없음(인용)」**으로 채웠다. 요건을 안 잰 것이고, 설계 `:26` 에 따르면 *"요건을 하나라도 못 채운 채 그 절에 앉아 있다"* → **잔여**다. ⚠ 초안은 `:141` 에서 **17·20 만** 약하다고 적었다 — **16·19 는 안 집었다** | `6-thesis.md:63`·`:66`(판정 칸 = 「해당 없음」) · `dialectic/6-design-for-antithesis.md:25`·`:26` · `6-thesis.md:141`(17·20 만 지목) | **참** | 거짓신호 | 「우회가 아님」을 **절 단위로** 한 번 보이면 그 절의 모든 항이 충족한다고 읽히면. 그런데 설계 `:12` 가 *"절 단위 총론은 판정이 아니다"* 로 그 길을 막는다 |
| **A10** | 초안의 자기 요약 | **`6-thesis.md:121` 의 *"이 판정은 `report.md:116-117` 한 항 위에 서 있다"* 가 자기 감도표와 어긋난다.** 바로 위 표(`:116-119`)는 **`:116` 하나 + 「위 전부」**(능력 부재 넷 · `:132` · `:101`)가 **동시에** 무너져야 0 이 된다고 적는다. 한 항이 아니라 **일곱이 동시에** 무너져야 한다. 이 한 문장이 합(合)에게 *"한 칸만 뒤집으면 통과"* 로 읽힐 자리를 만든다 — 판정값을 바꾸는 오독을 초대한다 | `6-thesis.md:119`(*"`:116` 하나가 뒤집힌다 **+ 위 전부**"*) ↔ `6-thesis.md:121` | **참** | 거짓신호 | 「서 있다」가 *"가장 짧은 길"* 의 수사로만 읽히면. 그러나 같은 줄이 `그러므로` 로 시작해 감도표의 **결론**임을 주장한다 |
| **A11** | ⓑ 판정 단위의 경계 | **가장 강한 「확인 못 한 것」 실례가 판정 단위 밖에 앉아 있고, 초안이 그것을 미정으로 남겼다.** `report.md:26-27` 이 미측정 다섯을 *"「안 쟀다」이지 「됐다」가 아니다"* 로 싣는다. 그중 **`E3`·`E5` 만** *"종료 절차가 재는 자리"* 이고, **`A5-c`(판 2 가 라운드 1 에서 닫았다) · `C2`·`C2-b`(소유자 칸 2)** 셋은 항 자신이 **다른 사유**를 적는다 — 종료 절차의 원리가 아니다. 규약 `:780` 은 네 이름이 **종료 보고에** 없어야 한다고 적지 **세 절에만** 이라 적지 않는다. 초안은 `:147` 에서 *"내가 안 정했다"* 로 열어 뒀다 — **미측정을 미측정으로 남긴 칸이 판정문 안에 있다** | `report.md:26-27` · `SKILL.md:780` · `dialectic/6-design-for-antithesis.md:17-19`(판정 단위가 `## 남지 않은 것` 을 뺀다) · `6-thesis.md:147` | **참** | 거짓신호 | 설계의 판정 단위가 **합의된 경계**이고 합이 그것을 못 넓히므로, 이 반론은 **값을 못 움직인다.** 「다음 판이 받는 것」이지 이 판의 반론이 아니라고 읽으면 틀린다 |
| **A12** | 항 5(`report.md:132`) ⟨반대 방향·약함⟩ | **「한 줄을 `grep` 이 잡았다」에서 「전 줄에 대해 할 수 있었다」로 간 n=1 귀납이다.** 셀이 적는 것은 *"㉠ 에서 touch 가 틀렸다는 것은 `grep` 이 잡았지 **이 회차의 장치가 잡지 않았다**"* 이고, 요건 ⑵ 가 요구하는 것은 **각 줄에 대한 독립 오라클**이다. 우연한 한 건의 적발은 오라클을 세울 수 있었음의 근거가 아니다 | `report.md:132` · `6-thesis.md:26`(*"회차 안에서 한 줄에 대해 실제로 했다"*) | **참** | 미관 | **값은 안 움직인다** — 초안은 요건 ⑴ ✗(*"안 쟀다 ≠ 원리상 못 잰다"*)로도 같은 잔여에 닿는다. 내가 치는 것은 근거 하나뿐이다. 이 점을 스스로 적는다 |

---

## 값 감도 — 내 반론을 **전부** 먹이면 `E3` 는 어떻게 되나

| 먹이는 것 | 남는 잔여 | `E3` |
|---|---|---|
| `A1`(단위=행)만 | `:132`·`:140`·`:141`·`:142` = **4** | **반증 유지** |
| `A1` + `A3`(능력 부재 넷이 경계) | `:132` = **1** | **반증 유지** |
| `A1`+`A3`+`A12`(항 5 의 근거만 무너짐, 요건 ⑴ 은 남음) | **1** | **반증 유지** |
| `A8`·`A9` 를 더하면(잔여 증가 방향) | **3 이상** | **반증 유지** |
| `A1`+`A3` + **항 5 가 경계로 완전히 뒤집힘** | **0** | **통과** — 이 길 하나뿐이다 |

★ **반의 결론은 「반증을 뒤집어라」가 아니다.** 내가 세운 열둘 중 **열하나는 값을 안 움직인다.**
값이 걸린 자리는 **단 하나** — `report.md:132`(항 5)가 경계로 완전히 뒤집히고 동시에 `A1`·`A3` 이
서는 경우다. 그리고 **`A12` 는 그 자리를 내가 못 세운다고 스스로 적었다**(요건 ⑴ 이 남는다).
⚠ 그러므로 **초안이 지목한 기둥(`report.md:116-117`)은 기둥이 아니다.** 진짜 기둥은
**`report.md:132`·`:140`·`:141`·`:142`** 넷이고, 초안은 그 넷을 *"가장 약하다"* 쪽으로 적었다 —
**초안이 자기 판정의 무게가 어디 실렸는지를 틀리게 적었다**(`A10`).

---

## 내가 스스로 물린 것 ⟨세웠다가 좌표를 못 댄 것⟩

1. **`6-thesis.md:72` 의 `6-design.md:162` 인용을 대조 못 했다.** `6-design.md` 원본이 금지 목록에
   있다. 발췌 `dialectic/6-design-for-antithesis.md:86-88` 이 `6-prompts.md` 를 반증 조건으로
   언급하므로 **인용은 그럴듯하다.** 위조 주장을 세울 근거가 없어 **접었다.**
2. **「1 시간 21 분」이 실제로 1 시간 20 분 53 초다.** 반올림이고 해악도가 미관에도 못 미쳐 표에
   안 실었다.
3. **항 10·11·12 의 요건 ⑴(*"이 회차의 의도 밖 물음"*)을 못 쟀다.** 근거가 `intent.md` 의 (가)
   선택과 소유자 칸 2 인데 **둘 다 핀 안에 안 들어왔다.** 잠긴 의도 발췌(`:20`)에
   *"답 — 「미측정으로 닫고 문면 셋을 승격」"* 이 보이나 **어느 칸인지 매핑이 없다.** 초안의 ✓ 가
   틀렸다고 주장할 좌표를 못 댔다.
4. **`report.md:113`·`:114` 가 항 21 과 **같은 사유**(*"회차가 (가)를 골랐다"*)를 적는데 값이
   갈린다**(경계 / 잔여)는 어긋남을 세우려 했다. 그러나 설계 요건표가 두 절에 **다른 요건**을
   걸므로(이슈 유무) 초안의 비대칭은 **규칙대로**다. **접었다** — 대신 `A6` 에서 항 21 쪽만 쳤다.
5. **`6-thesis.md:85` 의 *"선례를 근거로 쓴 자리는 이 초안에 없다"* 를 반증하려 했다.**
   `:139` 가 저장소 선례를 인용하나 **손잡이 절**이고 값의 근거가 아니다. **접었다**(임무 4 에
   경계 1 건으로만 적었다).
6. **`## 남지 않은 것` 의 「반증 다섯」 표(`report.md:18-24`)에 「누가 만들었나」 칸이 있는 것을**
   네 이름의 우회로 세우려 했다. 판정 단위 밖이고 §10 의 어느 이름에도 안 걸린다. **접었다.**
7. **메인의 사고 과정이 과업문에 섞여 왔는지** — 안 섞였다. 세울 것이 없다.
