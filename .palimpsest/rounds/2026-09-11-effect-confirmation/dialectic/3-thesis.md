# 정(正) — 판 3 · `C2`·`C2-b` ⟨`pal-decision-proposer`⟩

> 아래는 에이전트의 **반환문 전문**이다. 부르는 쪽이 한 글자도 안 고쳤다.

---

# 정(正) — `C2-b` 의 내용 시험을 통과한 채로 touch 에 귀속되는 차이가 1 건 이상 서 있나

> 받은 것 — 설계문 `dialectic/3-design.md`(판 3 만) · `dialectic/preflight.md` · 조건 문면 · 저장소.
> **메인의 입장은 안 왔다.** 프롬프트에 대화 기록·초안·다른 판의 결과가 섞여 오지 **않았다**(확인함).
> 회차의 `state.md` 와 `intent.md` 의 `## 차선책`·`## 범위 밖` 은 **안 열었다.**
> 아래 좌표의 뿌리는 전부 `.palimpsest/rounds/2026-09-11-effect-confirmation/` 다.

## 판정

**`C2-b` 는 통과이고(제출된 귀속 일곱 전부에 규정 형식의 내용 시험이 적혔고, 유효 라벨 일곱을 사전 등록 전문과 §5.8 표에 직접 대어 다시 확인하니 문면대로 전부 성립한다), `C2` 도 통과다(귀속된 차이가 ㉠ 0 · ㉡ **2** · ㉢ 0 으로 합 **2** 이고 하한 1 을 넘는다).**

## 근거 — 좌표와 함께

### 1. 무엇이 「귀속」이고 무엇이 「내용 시험」인가 — 세는 대상을 먼저 고정한다

- **귀속(후보)** 은 readnote 가 제출한다 — `effect/126-readnote.md:63-67`(ⓐⓑⓒ) · `effect/79-readnote.md` 의 `## 4`(ⓐⓑⓒ) · `effect/129-readnote.md` 의 `## 3`(ⓐ). **일곱**이다.
- **내용 시험**은 판정자 프롬프트가 강제한 다섯 칸(`effect/judge-prompt.md:25-30`)으로 판정자 원문에 적힌다.
- **미끼 ⓓ·ⓔ 는 귀속이 아니다** — `C2-a` 의 음성 대조용이고 `effect/judge-roster.md:8-12` 가 그렇게 적는다.

### 2. `C2-b` — 형식: 일곱 전부에 적혔다. 좌표 하나가 틀렸다

| 자리 | 후보 | 인용 좌표 | 내가 연 실물 | 내용 시험이 적힌 자리 |
|---|---|---|---|---|
| ㉡ | ⓐ | `touch/126.txt:24` | `호출자 1 · 피호출자 12` **맞다** | `effect/126-judge-1.md:22-27` |
| ㉡ | ⓑ | `touch/126.txt:17` | `fun · … identity ordinal · body 1f1846428f55` **맞다** | `effect/126-judge-1.md:29-34` |
| ㉡ | ⓒ | `touch/126.txt:17` | 같은 줄 **맞다** | `effect/126-judge-1.md:36-41` |
| ㉠ | ⓐ | `touch/79.txt:23` | `호출자 3 · 피호출자 1` **맞다** | `effect/79-judge-1.md:18-23` |
| ㉠ | ⓑ | `touch/79.txt:36-46` | `내가 모르는 것 / 9 건` 블록 **맞다** | `effect/79-judge-1.md:25-30` |
| ㉠ | ⓒ | `touch/79.txt:56` | `2층  심볼 3315 색인됨` **맞다** | `effect/79-judge-1.md:32-37` |
| ㉢ | ⓐ | `touch/129.txt:21`·`:23` | `fun write … manifest.rs:373` · `… projection.rs:251` **맞다** | `effect/129-judge-1.md:37-51` · `effect/129-judge-2.md:35-41` |

★ **어긋남 하나** — 미끼 ㉠ⓔ 의 인용 좌표가 `effect/judge-prompt.md:1098` 에서 `:19-20` 인데, `touch/79.txt` 의 그 두 줄은 **`:18-19`** 다(`:20` 은 `■ 이 좌표를 지켜보는 것 (0)`). ㉡ 의 같은 후보 좌표(`touch/126.txt:19-20`, 이쪽은 맞다)를 그대로 옮긴 자리다. **미끼라 분자에 안 들어가지만 좌표 오류는 사실이다.**

### 3. `C2-b` — 내용: 유효 라벨 일곱을 내가 다시 확인했다

- **㉡ⓐ**(호출자 1) — `plan/126-pre.md` 에서 「호출자」는 두 곳뿐이고(`:49` 는 §5.8 사본의 **`identity_ceiling`** 행 · `:82` 는 *"호출자가 `report.md` 존재로 걸러 넘긴다"* 로 **다른 뜻**), `check_ledger_pair` 의 호출자 수는 없다. `observations/red.md:239` 의 `check_ledger_pair` 행은 결박 0 · 46 줄 · 같은 파일 결박 8 · 한 칸 옆만 적는다. → **문면대로 유효.**
- **㉡ⓑ**(정체성 표시가 `ordinal`) — `grep -c ordinal plan/126-pre.md` 가 **0** 이다. §5.8 표(`observations/red.md:235-240`)에 정체성 항목이 없다. → **유효.** ⚠ 다만 판정자 문면(`effect/126-judge-1.md:32-34`)은 *"없다/없다"* 만 적고 **두 문서에서 한 글자도 인용하지 않는다** — 조건 문면이 요구한 *"두 문서의 인용으로 보인다"* 를 글자대로 보면 이 칸은 비어 있고, **성립은 내 독립 확인(위 grep)으로 선다.**
- **㉡ⓒ**(`body 1f1846428f55`) — `plan/126-pre.md:109`·`:112` 는 `body <hash>` 를 **자리표시로만** 적고 값이 없다. → 유효. 단 readnote 스스로 `effect/126-readnote.md:67` 에서 *"약하다"* 고 미리 적었다.
- **㉠ⓐ**(호출자 3) — `plan/79-pre.md:152-153` 이 아는 것은 *"`grep` 으로 이미 넷"* 이고 **touch 의 수는 없다.** §5.8 의 `nodes_of` 행은 결박·지켜보는 것·52 줄뿐. → 유효(둘이 「같은 사실」인가에서 가장 무른 칸이다 — 잣대가 다르고 값도 다르다).
- **㉠ⓑ**(미해소 아홉) — `plan/79-pre.md` 에 `SymbolNode`·`Discriminator` 는 **정의 설명**으로만 나오고(`:40`·`:36-38`) *"이 함수의 미해소 참조 아홉"* 은 없다. → 유효.
- **㉠ⓒ**(3315) — `plan/79-pre.md:100` 이 *"`2층 심볼 3308 색인됨`"* 을 **인용해 두었다.** 판정자는 *"3315 와 3308 은 다른 수"* 로 유효를 냈는데(`effect/79-judge-1.md:35-37`), **같은 회차의 `effect/79-delta.md:85` 는 3308·3315·3325 를 「실행마다 움직이는 같은 분모」로 다룬다.** → **라벨과 내 확인이 갈린다.** 「값이 다르면 다른 사실」 읽기에서만 유효다. **분자에 안 들어가므로 판정값은 안 바뀐다.**
- **㉢ⓐ**(`write` 후보 좌표 둘) — `plan/129-pre.md` 는 `store.rs:163-172` 하나만 적고 나머지 둘의 좌표가 없다. §5.8 은 이 심볼을 아예 안 다룬다. → 유효.

### 4. `C2` — 「새 사실」이 아니라 **차이**를 센다. 세 자리를 갈라서 셌다

delta 의 「차이」 표를 직접 읽고 **원인 좌표가 `touch/<이슈>.txt:N` 인 행**만 셌다.

| 자리 | 차이 수 | touch 에 귀속된 행 | 좌표 |
|---|---|---|---|
| ㉠ `#79` | 4 | **0** | `effect/79-delta.md:23` 이 표 제목에서 *"어느 것도 touch 가 원인이 아니다"* 라고 스스로 적는다 |
| ㉡ `#126` | 4 | **2** — ㉰·㉱ | `effect/126-delta.md:26`(㉰ · 원인 `touch/126.txt:17`) · `:27`(㉱ · 원인 `touch/126.txt:24`) |
| ㉢ `#129` | 3 | **0** | `effect/129-delta.md:33` — *"이 자리의 차이는 0 이다 … 셋 다 도구가 시킨 것"* |
| **합** | 11 | **2** | |

- 분자의 둘은 `C2-b` 를 통과한 귀속에 **정확히 물려 있다** — ㉰ ← ㉡ⓑ · ㉱ ← ㉡ⓐ.
- 그 둘의 **결과 좌표를 실물로 열어 확인했다**: `xtask/src/main.rs:6407`(`398d233` 기준)은 새 함수의 문서 주석 머리로 `check_ledger_pair` 가 끝나는 `:6405` 바로 뒤다 · `:643` 은 `("원장 둘 대조", check_ledger_pair(root)),` 로 **안 건드린 검사 등록 표**가 맞다.
- **인과가 변경 커밋보다 앞선 문서에 적혀 있다** — `effect/126-readnote.md:45-51` 이 두 귀속의 인과를 그대로 적고, 그 커밋 `3637ea9` 는 touch 커밋 `14f0f92` 뒤·변경 커밋 `398d233` 앞이다(`A7` 순서).
- **하한 1 이 자리마다가 아니라 전체에 걸린다**는 것은 `intent.md:114` 가 *"하한 1 을 실제로 지는 것은 ㉠·㉡ 둘이다"* 로 착수 시점에 적었다.

★ **판정자의 「유효 1」을 분자로 안 썼다** — ㉢ 이 그 함정의 실물이다. `effect/129-judge-2.md:56-59` 스스로 *"「유효 1」은 「귀속된 차이 1」이 아니다"* 라 적는다. 내 셈은 그 구별을 따랐다.

## 대안과 그것이 빠지는 근거

| 대안 | 왜 안 고르나 |
|---|---|
| **㉮ 지금 그대로 — `C2` 통과 · `C2-b` 통과** ← 고른 것 | 아래 셋이 다 빠지고 남는 자리 |
| ㉯ `C2` 통과(합 **3**) — ㉠㉱ 도 센다 | `effect/79-delta.md:30` 의 원인 칸은 `touch/79.txt:23` 을 **명시**해 `C1` 의 「원인 좌표」 형식을 충족한다. 그런데 같은 파일 `:23` 의 표 제목이 *"어느 것도 touch 가 원인이 아니다"* 다 — **파일이 자기모순이다.** 나는 서사 쪽을 골랐다(touch 가 **덜 말해서** grep 이 목록을 지었으니 작업을 바꾼 것은 touch 가 아니다). **판정값은 이 읽기에 안 흔들린다** — 어느 쪽이든 `C2 ≥ 1`. |
| ㉰ `C2` 반증(0) — ㉡㉰㉱ 가 「차이」가 아니다 | `effect/126-delta.md:30-31` 이 스스로 *"계획 자체는 안 바뀌었다 … 바뀐 것은 **근거와 생략된 걸음**"* 이라 적는다. 「보기 좋다」라는 **바뀌기 전 상태가 봉인된 `plan/126-pre.md` 에 없다** — §4 ⑵ 는 배치만 적고 까닭이 없다. 그러면 차이의 앞쪽 끝이 **봉인 밖의 자기 증언**이다. **그래도 안 고른다**: `C1` 의 문면은 차이를 「산출물의 차이」로 한정하지 않고, `A7` 순서로 커밋된 `effect/126-readnote.md:45-51` 이 변경 **전에** 그 전이를 적었다. ⚠ 이것이 이 초안의 가장 무른 관절이다. |
| ㉱ `C2-b` 반증 — ㉡ⓐ·ⓒ 는 사전부검이 이미 말했다 | **사실이다(아래 §틀렸다면).** 그러나 `C2-b` 의 문면이 배제 집합을 *"`plan/<이슈>-pre.md` 전문에도 `observations/red.md` §5.8 표에도"* **둘로 못 박았고**, 두 문서 어디에도 없는 것이 맞다. 설계문 `dialectic/3-design.md:267-268` 이 *"문면이 의도를 못 잰다는 것이 드러나면 **고치지 않고** ②확장 판단으로 올린다"* 로 이 길을 막아 두었다. → **판정은 통과, 문제는 확장 판단으로 올린다.** |

## 이 초안이 틀렸다면 무엇이 먼저 드러나나

**★ 손잡이 1 — §5.8 이 오염 「전수」가 아니다. 내가 실물을 찾았다.**
`observations/red.md:231-233` 은 *"숨기지 않고 여기 전수로 적는다"* 라고 선언하는데, **봉인보다 먼저 커밋된** `premortem/r2-raw.md:51`(커밋 `fbec3dc` · 2026-09-11 16:17:50 / 봉인 `a74737b` · 22:24:00)이 이렇게 적는다:

> `check_ledger_pair` ↔ 같은 파일의 `check_intent_untouched` 로 `C2-b` 를 실제로 돌렸다. … **판정에 쓸 수 있는 것은 `호출자 1 · 피호출자 12` 하나뿐**이다

즉 **㉡ⓐ 가 담은 사실을 메인이 사전 등록을 쓰기 여섯 시간 전에 자기 손으로 적었다.** 같은 사실이 `observations/premortem-artifacts/README.md:24` 에도, 산출 원본 `observations/premortem-artifacts/check_ledger_pair.txt:9` 에도 있다. 그리고 같은 파일 `:2` 에 **`fun · xtask/src/main.rs:5851 · identity ordinal · body 1f1846428f55`** — **ⓑ 와 ⓒ 가 담은 사실 둘이 한 줄에 같이 있다**(ⓒ 의 값은 `premortem/r2-raw.md:31` 이 본문에 인용까지 했다).
→ `C2-b` 의 배제 집합을 *"메인이 이미 안 것"* 으로 읽으면 **㉡ⓐ·ⓒ 는 무효**이고, ㉱ 가 떨어져 **`C2` 의 분자가 2 → 1(ⓑ 뿐)** 로 준다. **여기서 ⓑ 까지 무너지면 `C2` 는 0 이고 차선책 1 이 열린다.** ⓑ 만 살아남는 까닭은 얇다 — 그 사실을 **문장으로 적은** 회차 문서가 없을 뿐, 산출 원본은 메인 손에 있었다.

**손잡이 2** — `effect/79-delta.md` 가 표 제목(`:23`)과 행 ㉱(`:30`)에서 서로 반대를 말한다. 반이 행 쪽을 세우면 합이 3 이 되고, 반대로 「touch 가 덜 말한 것도 귀속이 아니다」를 ㉡ 에 확장 적용하면 분자가 흔들린다.

**손잡이 3** — ㉡ⓑ 의 내용 시험(`effect/126-judge-1.md:32-34`)에 **두 문서 인용이 0 건**이다. `C2-b` 의 *"두 문서의 인용으로 보인다"* 를 형식 요건으로 읽으면, **`C2` 의 분자를 혼자 지는 그 칸이 형식 미달**이다.

**손잡이 4** — `effect/negative-C2.md` 가 **없다**(`dialectic/preflight.md:290-293` 의 `ls` 실패 산출). 음성 대조 자체는 실행됐고 `effect/126-judge-1.md`(ⓐ 축 · ⓓⓔ 를 무효로 잡음)와 `effect/126-judge-2.md:38`(ⓑ 축 · 유효 0)에 남았지만, **조건이 지정한 파일 이름이 안 섰다.** 이것은 `C2-a`(⟨결정론적⟩)의 것이라 내가 판정하지 않는다 — 다만 *"`C2-b` 가 항등식이 아니다"* 의 근거가 **이름이 다른 파일에 있다**는 사실은 적어 둔다.

**손잡이 5** — ㉡ⓐ 가 담은 「호출자 1」은 **참인지 안 재어졌다.** 같은 회차가 ㉠ 에서 「호출자 3」이 실제 4 였음을 실측했다(`effect/79-readnote.md` §2 · `MS-08`). 같은 결함이 ㉡ 에도 있으면 ㉱ 는 **거짓 사실에 귀속된 차이**다. ⟨참·거짓은 `C4` 의 것이므로 이 판은 값을 안 낸다.⟩

## 내가 확인 못 한 것

- **판정자에게 실제로 전달된 바이트** — `effect/79-judge-1.md:58-78` 이 스스로 *"되불러 주기는 「대체로 같은 것을 받았다」까지만 말한다"* 라 적는다(③ 줄 수 337 대 파일 345). 나도 `touch/126.txt` 60 줄 · `touch/79.txt` 65 줄인데 판정자는 59 · 64 라 적은 것을 확인했다. **한 줄 차이의 원인을 못 짚었다.**
- **판정자가 정말 새 인스턴스·도구 0 회였는지** — `effect/judge-roster.md:25-26` 의 자기 서술뿐이고 기계 증거를 못 봤다.
- **㉡ 의 「호출자 1」의 참 거짓** — 새 실행이 금지돼 `pal query symbol.callers check_ledger_pair` 를 안 돌렸다.
- **`premortem/r2-raw.md:51` 을 쓴 시점에 메인이 `identity ordinal` 을 「사실로」 등록했는지** — 문장이 없다. 산출 파일에 줄이 있었다는 것까지만 확인했다.
- `intent.md` 의 `## 차선책`·`## 범위 밖`·`state.md` — **설계가 막아서 안 열었다.** 그러므로 *"`C2` 가 0 이면 무슨 일이 생기나"* 를 모른 채 세었다.

---

`C2: 통과 — touch 산출에 귀속된 차이가 ㉠ 0 · ㉡ 2 · ㉢ 0 으로 합 2 이고 하한 1 을 넘는다(effect/126-delta.md:26-27).`
`C2-b: 통과 — 제출된 귀속 일곱 전부에 규정 형식의 내용 시험이 적혔고, 유효 라벨 일곱이 plan/<이슈>-pre.md 전문과 observations/red.md §5.8 표에 대어 문면대로 성립한다.`

**이 판정을 뒤집는 것 — `C2`:** `effect/126-delta.md:26-27` 의 ㉰·㉱ 가 「사전 등록 ↔ 실제 변경의 차이」가 아니라고 판정되면(둘 다 산출물이 아니라 **근거와 안 밟은 걸음**이고, 바뀌기 전 상태가 봉인 문서에 없다), 분자가 0 이 되어 반증으로 뒤집힌다.
**이 판정을 뒤집는 것 — `C2-b`:** `C2-b` 의 배제 집합을 「메인이 이미 안 것」으로 읽으면 `premortem/r2-raw.md:51`·`:31` 과 `observations/premortem-artifacts/check_ledger_pair.txt:2,9` 가 ㉡ⓐ·ⓒ 를 무효로 만들고, §5.8 의 *"전수로 적는다"* 선언이 거짓이 되어 조건의 오라클 자체가 깨진다.
