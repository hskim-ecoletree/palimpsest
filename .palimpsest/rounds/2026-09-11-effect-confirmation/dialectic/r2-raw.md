# 정반합 취합 — 2026-09-11-effect-confirmation 라운드 2 · 판 1(`B1`)과 판 3(`C2`·`C2-b`)이 실제로 충족됐나

> 낸 자리: `pal-debate-reporter`. **읽기 전용** — 파일을 안 고쳤고 커밋 안 했다.
> 받은 것 — 설계문 · 정 · 반 · 합 · 종료 판단 다섯 산출물 전문 · 회차 이름 · 원장 스키마(schema 3).
> **안 받은 것** — 대화 기록 · 메인의 사고 과정 · 이 판정을 실행할 계획.
> 내가 쓴 도구 — `git` · `shasum`/`hashlib` · `grep` · 읽기. 새 빌드·시험·`pal` 호출은 0 이다.
> **설계문 사본** — `dialectic/1-design.md` · `2-design.md` · `3-design.md` · `4-design.md` 넷의 sha256 이 `829871c1eabcb1d24d21e7b9f04be47b8a7d5afbca4ccad9e9439b287121703c` 로 **같다**(내가 `shasum -a 256` 으로 직접 쟀다). 설계문의 운영 각주가 이 사실을 여기 적으라고 요구한 자리다. `dialectic/5-design.md` 는 다른 바이트이고 이 라운드의 입력이 아니다.

## 판정 — 합(合)이 낸 문장 그대로

#### 판 1 · `B1`

`1-synthesis-r2.md` 의 판정 절 머리는 **수정** 이고, 조건 판정값은 **`B1` 은 통과** 다. 합의 문장:

> 세 자리 각각에 HEAD 의 조상인 코드 변경 커밋이 있고(`git merge-base --is-ancestor` 로 `9f993cc`·`398d233`·`1abc0e9`·`e44a87e`·`06e8d63` 전수 확인), 셋 다 그 이슈 본문의 **닫는 조건**을 만족한다. 근거는 산문이 아니라 내가 직접 연 코드다

> **이 판정을 뒤집는 것 한 줄** — `#79` 의 1 차 원문에 *"닫으려면 그 수를 내는 산출 경로가 필요하다"* 가 **없거나 다르면** ㉠ 의 근거가 통째로 무너지고 `B1` 은 **대조 불가**로 간다. 재는 법: `gh issue view 79 --json body` 를 `dialectic/preflight.md` 의 `#79` 인라인 블록과 `diff`. ⚠ **나도 안 돌렸다**(새 실행 금지).

> 그래도 `B1` 이 재는 것은 *"이슈 본문이 요구한 것"* 이고 `#79` 가 요구한 것은 **산출 경로**다. doc 주석은 그 경로도 그 산출값도 안 건드린다. 그러므로 값은 **통과**로 두고, ⑦ 은 **회차 금지역 「사실이 아닌 것을 사실로」에 드는 발견**으로 원장에 싣고 이 회차 안에 고친다

#### 판 3 · `C2`·`C2-b`

`3-synthesis-r2.md` 의 판정 절 머리는 **수정** 이고, 조건 판정값은 **`C2` = 미측정 · `C2-b` = 미측정** 이다. 합의 표를 그대로 옮긴다:

| 조건 | 판정값 | 근거 좌표 | 이 판정을 뒤집는 것 |
|---|---|---|---|
| **`C2`** | **미측정** | 문면(`intent.md:232`)대로 세면 원인 칸이 `touch/<이슈>.txt:N` 인 행은 **셋**이다 — `effect/126-delta.md:26`·`:27` · `effect/79-delta.md:38`. 소유자가 `intent.md:428` 에서 *"`C2` 의 「그 차이 중 touch 산출에 귀속되는 것」이 부분집합을 따로 부른다"* 로 그 읽기를 못 박았으므로 **「달라진 변경」으로 0 을 만드는 길은 닫혔다.** 그런데 그 셋이 **유효한** 귀속인지는 `C2-b` 가 정하고 `C2-b` 가 미측정이므로, **유효 귀속의 수가 안 재졌다** | `C2-b` 가 통과로 확정되면 `C2` 는 즉시 **통과**(3 ≥ 1)다. 반대로 배제 집합의 정의역(#140 ①)이 「회차 디렉터리 전체」로 넓혀지면 ㉠ⓐ·ⓑ 가 떨어지고 `C2` 는 다시 갈린다 |
| **`C2-b`** | **미측정** | 「내용 시험이 적혔다」는 **7/7 서 있다** — readnote 넷째 칸(`effect/79-readnote.md:63-67` · `126-readnote.md:63-67` · `129-readnote.md:39-41`)과 판정자 절. 「두 문서의 인용」은 **고르지 않다**: 양쪽 다 원문 인용 **3**(`79-judge-1.md:21-22` ⓐ · `:28-29` ⓑ · `129-judge-2.md:39-40`) · 한쪽만 **2**(`79-judge-1.md:35-36` · `126-judge-1.md:25-26`) · 양쪽 맨 「없다」 **2**(`126-judge-1.md:32-33` · `:39-40`). 그런데 배제 집합의 정의역이 `plan/<이슈>-pre.md` + `observations/red.md` §5.8 **둘뿐**이라(`intent.md:233`), 봉인 2 시간 43 분 전에 회차에 들어온 `observations/premortem-artifacts/nodes_of.txt:10`(`호출자 3 · 피호출자 1` — **글자까지 같다**)을 **구성상 못 덮는다.** 시험이 적혔으나 그 시험이 재도록 정의된 것이 재려는 것과 다르다 | *"맨 「없다」도 인용이다"* 가 서면 **통과**. 반대로 *"「귀속마다」는 전수 요건이다"* 를 글자대로 집행하면 `126-judge-1.md:32-33`·`:39-40` 둘 때문에 **반증** |

> ⚠ **★ 이 표는 독립 판정이 아니다 — 소유자가 `intent.md:412` 에서 이미 「미측정으로 닫는다」를 골랐고 `:414` 가 그 값을 게이트 `## 판정` 의 `미측정` 칸에 넣으라 적었다.** 이 판이 새로 한 것은 **값이 아니라 근거**다(아래 `A6`). 값은 **기록**이고 근거는 **판정**이다 — 그 구별이 판정문에 적혀야 한다.

---

## 반론 처리 — 전부

### 판 1 · AT2-01 — 이 회차가 세운 「검산」이 항등식이라 원리상 못 빨개진다

- 모집단: 저장소
- 유효성: 거짓 ⟨반은 「참」으로 냈고, 합이 *"반의 관측은 맞지만 결론이 틀렸다 — 원문(코드) 오독이 `금지역` 으로 올라온 자리다"* 로 뒤집었다. **두 원천이 갈렸고 나는 합의 값을 적되 반의 값을 같이 남긴다**⟩
- 해악도: 금지역 ⟨반이 스스로 매긴 값. 합은 기각하며 값을 안 다시 매겼고 종료 판단 표에도 안 실렸다⟩
- 어디가 걸리나: `crates/pal-cli/src/ledger.rs:705-707` · `:423-454` · `:221` · `:225` · `:681-682` · `:929` · `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/negative-B5-group.txt:30` · `intent.md:87` · `:90`
- 처분: 기각 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 닫힘 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **기각.** 합의 문장 그대로 — *"그 검산이 스스로 선언하는 방어 대상은 「버킷이 **겹치거나 빠진** 것」(`ledger.rs:681-682`·`:706`)이고, **그 결함 부류에서 이 가지는 실제로 발화한다** — `ledger.rs:369` 의 `return` 을 지우면 `그룹_크기 > 1` 인 심볼이 `순서에_취약` 과 등급 버킷에 **둘 다** 더해져 `합() = n + 중복수 ≠ n` 이 되고 `:705-707` 과 시험 `:929` 가 빨개진다. … 이 기준을 받으면 참인 프로그램의 모든 `assert!(불변식)` 이 금지역이 된다. ⚠ 그리고 반이 제시한 반증 명령(`그룹_크기 > 99999999` 로 **버킷을 옮기기만** 하는 변이)은 합을 보존하므로 **원리상 이 축을 못 건드린다** — 돌려도 아무것도 안 갈린다."*

### 판 1 · AT2-02 — `#79` 가 이름으로 지목한 「같은 글자」는 안 갈렸다, 갈린 것은 집계뿐이다

- 모집단: 저장소
- 유효성: 참 ⟨합: *"관측은 참이고(내가 `:447`·`:449-451` 을 열어 확인했다)"*⟩
- 해악도: 거짓신호 ⟨반이 매긴 값. 합은 기각하며 다시 안 매겼다⟩
- 어디가 걸리나: `crates/pal-cli/src/ledger.rs:447` · `:449-451` · `crates/pal-core/src/touch.rs:35-57` · `crates/pal-cli/src/ledger.rs:642-651` · `dialectic/preflight.md:342` · `:355-356` · `intent.md:205`
- 처분: 기각 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 닫힘 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **기각.** 합의 문장 — *"반이 `## 범위 밖`·`## 차선책` 을 못 받은 탓에 선 반론이다 — 근거를 대어 기각한다. ⑴ `#79` 본문이 **닫는 조건을 자기 입으로 한 문장으로 못 박았다** … ⑵ 회차가 그 읽기를 **측정 전에** 잠갔다 … ⑶ 반이 지적한 잔여는 **`## 범위 밖` 에 이미 이름 붙어 등록돼 있다** ⟨`MS-09`⟩ … 사후 면제가 아니라 **사전 등록된 범위 밖**이다"*

### 판 1 · AT2-03 — 초안 ① 의 귀속이 `git blame` 과 어긋난다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반이 매긴 값 그대로. 합: *"「올린다」로 안 올리는 까닭 — 문장 하나를 바로잡는 일이라 소유자 결정이 필요한 자리가 없다"*⟩
- 어디가 걸리나: `crates/pal-cli/src/ledger.rs` 에 대해 `git blame -L 690,705` · `-L 905,915` · `-L 944,974` · `dialectic/1-thesis-r2.md:31`
- 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **채택.** *"**내가 직접 재서 반의 수치가 그대로 맞았다** — `-L 690,705` 는 `9f993cc` 10 줄 + `06e8d63` 6 줄, `-L 905,915` 는 9 + 2, `-L 944,974` 는 `9f993cc` 28 + `06e8d63` 2 + `5d20d4e0` 1 줄이고 **세 구간 전부 `e44a87e` 0 줄**이다. 초안 ⑦ 이 코드에 대해 고발한 것과 **같은 형태**(추정을 조회인 척함)를 판정문 자신이 저질렀고, 이 문서는 바이트 그대로 원장에 남는다."* 처분 자리는 합의 「초안을 어떻게 고치나」 ① 이다.

### 판 1 · AT2-04 — 사전순이 「최근」의 자로 아직 살아 있다(커밋 시각이 없는 회차가 둘 이상인 가지에서)

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호 ⟨반이 매긴 값 그대로⟩
- 어디가 걸리나: `xtask/src/main.rs:6425` · `:6415-6416` · `:6356-6359` · `:6487` · `dialectic/preflight.md:338-339` · `intent.md:215`
- 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **채택.** 합의 문장 — *"⚠ **다만 `B1` 이 아니라 `B6`(⟨결정론적⟩) 에 닿는 발견으로 받는다** — 설계문 `## 이 판이 안 다루는 것` 이 결정론적 조건의 산출에 대한 반론을 「발견이지 이 판의 판정값이 아니다」로 못 박았다. `#126` 이 친 것은 **1 차 자**가 사전순인 것이고 그 자는 커밋 시각으로 바뀌었으므로 ㉡ 의 `B1` 은 안 깨진다."* 확장 판단에서는 *"**안 늘린다 — 발견으로 올린다** … 원장에 싣고 `B6` 의 닫힘은 기계·메인이 판정한다"*.
- ⚠ **내가 잰 것** — 이 항은 라운드 1 의 `R1`-계열 `R3`(`DL1-03`)과 **같은 코드 자리·같은 귀결·같은 미기재**를 친다. 아래 `DL2-25` 를 보라. 원장에 두 번 세어질 위험이 있다.

### 판 1 · AT2-05 — 초안 ③ 은 `#79` 를 스스로에게 맞세워 읽으면서 그 사실을 안 적는다

- 모집단: 회차기록
- 유효성: 참
- 해악도: **미관** ⟨⚠ **두 원천이 갈렸다** — 반은 `거짓신호`, 합이 `미관` 으로 **내렸다.** 합의 사유: *"초안은 「이슈의 수를 재생산했다」를 **한 번도 주장하지 않는다** … 빠진 것은 **교차 참조**이지 거짓 주장이 아니다"*⟩
- 어디가 걸리나: `dialectic/preflight.md:352-353` · `:355-356` · `observations/identity-group.txt:38` · `:40` · `intent.md:214` · `effect/79-delta.md:98-101`
- 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **채택.** *"**좌표가 실재한다** — 이슈 인라인의 `464`·`7,156` 과 실측 `20`·`3306`(`observations/identity-group.txt`)은 자릿수가 다르고, 회차는 이미 `intent.md` 의 `B5-a` 와 `effect/79-delta.md:98-101` 에 그 어긋남을 적어 뒀다. 판정문만 그 교차 참조가 비어 있다."*

### 판 1 · AT2-06 — 초안 ② 가 대는 「줄 번호 일치」는 ③ 의 산출에 원리상 안 닿는다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반이 매긴 값 그대로. 합: *"`B1` 은 코드 경로의 실재로 서지 산출 파일로 서지 않으므로 판정값은 안 흔들린다"*⟩
- 어디가 걸리나: `effect/negative-B5-group.txt:3-5` · `:10-21` · `observations/identity-group.txt:3` · `:4-5` · `dialectic/1-thesis-r2.md:35`
- 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 탐지수단 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **채택.** *"**두 파일 머리를 내가 열어 확인했다** … 줄 번호 일치가 받치는 것은 **끈 나무의 시험 파일 세 줄**뿐이고, 릴리스 바이너리의 빌드 SHA·해시는 어느 산출에도 없다. 초안 ② 의 「커밋된 바이트를 가리키고 있다는 것을 이 줄 번호 일치가 받친다」는 **성립하지 않는다**."* 가르는 명령은 `cargo build --release && ./target/release/pal ledger` 로 `순서에 취약` 이 20 인지이고 **초안도 합도 안 돌렸다.**

### 판 1 · AT2-07 — 초안의 「101 줄」이 틀렸다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관 ⟨반이 매긴 값 그대로⟩
- 어디가 걸리나: `dialectic/1-thesis-r2.md:81` · `git diff --shortstat 06e8d63~1 06e8d63 -- crates/pal-cli/src/ledger.rs` → 54/47 · `e44a87e~1 e44a87e` → 34/5 · `af981d1 HEAD` → 68/32
- 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **채택.** *"**세 명령을 내가 다시 돌려 반의 수치가 그대로 맞았다** — `06e8d63` 단독 54+47=101 · `e44a87e` 단독 34+5=39. 101 은 합산이 아니라 `06e8d63` 단독값이다."*

### 판 1 · 초안 ⑦ — `crates/pal-cli/src/ledger.rs:310-320` 의 거짓 문단 셋

- 모집단: 저장소
- 유효성: 참 ⟨합: *"내가 직접 열어 확인했다"*⟩
- 해악도: 금지역 ⟨회차 자신의 문면 — `intent.md:87-88` *"사실이 아닌 것을 사실로"*. ⚠ 이것은 **반론이 아니라 정(正)이 낸 것**이라 종료 판단의 「살아남은 반론」 분포에 안 들어갔다⟩
- 어디가 걸리나: `crates/pal-cli/src/ledger.rs:310-320`(`:311`·`:315`·`:317-320`) · 함께 고칠 둘 `ledger.rs:966` · `ledger.rs:949` · 대조 자리 `:328-345` · `:366-370` · `intent.md` `## 승격` 칸 1
- 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: 합의 확장 판단 — **「이번에 늘린다」.** *"자기 완결성 시험을 **통과한다.** ⓐ 이 회차가 이 회차의 승격 집행 커밋으로 만든 거짓이고, ⓑ 회차 스스로 선언한 금지역 「사실이 아닌 것을 사실로」에 들며, ⓒ **다음 회차가 이 파일을 열면 코드가 「그룹 단위로 안 바꿨다」고 말한다** — 소유자가 답한 승격 칸 1 을 부정하는 문장이 제품 코드에 남는다. ⚠ 「어렵다·시간이 없다」는 사유가 아니고, 이 건에는 그런 사유조차 없다(주석 문단 삭제·재작성)."* 소유자에게는 **안 올렸다** — *"실질 물음(「셈법을 그룹 단위로 바꿀 것인가」)은 소유자가 `## 승격` 칸 1 에서 이미 답했고, 남은 것은 그 답을 부정하는 옛 주석 문단을 걷는 **집행**이다."*

### 판 1 · ⑦ 을 고치면 `effect/79-delta.md` 에 차이 한 행이 는다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨합이 해악도를 따로 안 매겼다 — 내가 합의 사유 *"고치고 delta 를 안 늘리면 `C1` 이 조용히 반쪽이 된다"* 에서 읽어 매긴 값이고, **합의 원문에는 없는 값이다**⟩
- 어디가 걸리나: `effect/79-delta.md` · `intent.md` 의 `C1` 문면 · `## 승격` 칸 3
- 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: 합의 확장 판단 — **「함께 늘린다」.** *"`C1` 이 「사전 등록 ↔ 실제 변경의 차이 **전수**」를 요구하고 `## 승격` 칸 3 이 모집단을 「전부」로 못 박았다. 고치고 delta 를 안 늘리면 `C1` 이 조용히 반쪽이 된다. **원인 끝은 `touch/79.txt:N` 이 아니라 정반합이다** — 칸 1 이 세운 기입 형식을 그대로 쓴다."*

### 판 1 · AT2-01 의 잔여 — 음성 대조가 「합 = 분모」 축을 한 번도 안 껐다

- 모집단: 회차기록
- 유효성: 참 ⟨합: *"반의 「죽은 가지」 주장은 기각하되 … 는 것은 참이다"*⟩
- 해악도: 거짓신호 ⟨합이 따로 안 매겼다. 「미측정이라는 사실과 가르는 명령을 적는 것으로 닫는다」로 처분했다 — 값은 **내가 매긴 것이고 합의 원문에는 없다**⟩
- 어디가 걸리나: `effect/negative-B5-group.txt`(그 산출에서 `crates/pal-cli/src/ledger.rs:929` 는 **안 빨개졌고** `:933` 만 빨개졌다) · 가르는 명령은 `ledger.rs:369` 의 `return` 을 지운 사본에서 `cargo test -p pal-cli ledger::tests::합이_분모와_같다`
- 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 탐지수단 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: 합의 확장 판단 — **「안 늘린다 — 발견으로 올린다」.** *"그 축의 대조는 `B5` 의 음성 대조가 요구한 자(「가르는 자리를 끄면 빨개지는가」)를 이미 만족했다(셋이 빨개졌다). 축을 하나 더 다는 것은 **이 회차가 만든 장치를 이 회차가 다시 먹이로 삼는 것**이고 규약 §11③ 이 그것을 15 라운드의 실제 원인으로 적는다."* ⚠ **아무도 안 돌렸다.**

### 판 1 · 합이 찾은 것 — 초안의 좌표 셋이 어긋났고 반의 「전수 대조했고 어긋난 좌표가 하나도 없다」가 그 자리에서 틀렸다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨합이 따로 안 매겼다. ★ 로 *"이 교정은 사소하지 않다"* 라 적었다 — 값은 **내가 매긴 것이다**⟩
- 어디가 걸리나: 초안의 `ledger.rs:358-361` → 실제 `:366-370` · 초안 `:413-418` → 실제 `:416-419` · 초안 `:421` → 실제 `:425` · 초안 `crates/pal-core/src/coord.rs:126-127` → `ordinal` 이 해시에 드는 줄은 `:128` · 반의 주장은 `dialectic/1-antithesis-r2.md:13`
- 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: 합의 「초안을 어떻게 고치나」 ⑦ — *"★ 이 교정은 사소하지 않다 — `#79` 가 막으려 한 것이 「다음 회차가 못 되짚는다」이고, 바이트 그대로 보존되는 판정문의 좌표가 어긋나면 그 자리를 판정문 자신이 만든다."* 확장 판단은 **「안 늘린다 — 초안 수정으로 흡수」**(*"조건도 코드도 안 건드린다"*).

### 판 3 · A1 — 초안의 *"계기가 두 문서의 인용을 원리상 못 낳는다"* 가 거짓, 같은 계기가 실제로 낳았다

- 모집단: 회차기록
- 유효성: 참
- 해악도: **금지역** ⟨반·합·종료 판단 세 원천이 같은 값⟩
- 어디가 걸리나: `effect/79-judge-1.md:21-22` · `effect/129-judge-2.md:39-40` ↔ 초안이 든 `effect/79-judge-1.md:29` · `effect/judge-prompt.md:28-29` · `intent.md:87-91` · `intent.md:415`(`#140` ②) · `dialectic/3-thesis-r2.md:115`
- 처분: 정정 · 승격됨: **예** · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **채택.** *"**내가 직접 열어 확인했다.** `effect/79-judge-1.md:21` 은 사전 등록 §2.2·§8 을 **원문 인용**으로 대고 `:22` 는 §5.8 행 내용(`결박 0 · 지켜보는 것 0 · 산출이 52 줄`)을 댄다. `129-judge-2.md:39-40` 도 같다. … ★ 그리고 이 거짓 명제는 이미 `9e092e3` 의 커밋 본문과 **`#140` ②** 로 저장소에 실렸다 — 초안이 그것을 되풀이했다. `intent.md:87-91` 의 「사실이 아닌 것을 사실로」에 정면으로 걸린다. **초안 자신이 `:115` 에서 「그것을 부수면 값이 뒤집힌다」라 적었으므로 `C2-b` 판정의 유일한 받침이 부서졌다**"*
- ★ **소유자가 답했다** — `intent.md` `## 승격` 칸 7: **「이번 회차에서 고친다」**(원문은 아래 `## 종료 판단` 에 그대로 옮겼다).

### 판 3 · A2 — 초안이 「내용 시험이 적혔다」를 판정자 반환문 한 곳에서만 찾았고 readnote 의 네 번째 칸을 한 번도 안 봤다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨반·합·종료 판단 세 원천이 같은 값⟩
- 어디가 걸리나: `effect/79-readnote.md:63-67` · `effect/126-readnote.md:63-67` · `effect/129-readnote.md:39`(합은 `:39-41` 로 적는다 — ⚠ **두 원천의 좌표가 갈린다**) · `intent.md:234`
- 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **채택.** *"확인했다 — `79-readnote.md:63-67` · `126-readnote.md:63-67` · `129-readnote.md:39-41` 이 귀속 **전수 7 건**에 `사전 등록·§5.8 에 있나` 칸을 달고 있다. `C2-b` 문면은 **누가 적는지를 안 정한다**(주체를 정한 것은 `C2-a` 의 「심는 자와 채점하는 자가 다르다」, 그리고 그것은 **무효 판정의 주체**다 — `intent.md:234`). 그러므로 「적혔다」의 모집단에 readnote 가 들어간다. 초안은 그 자리를 안 열고 「0 개」를 셌다."*

### 판 3 · A3 — 판정 대상은 조건 문면인데 초안은 `## 원문` 의 낱말로 조건을 바꿔 읽었다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨세 원천 같은 값⟩
- 어디가 걸리나: 조건 문면 `intent.md:232` ↔ 초안이 쓴 `intent.md:11-12` · 소유자 칸 3 `intent.md:428`
- 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **채택.** *"`intent.md:232` 의 글자는 「그 차이 중 touch 산출에 귀속되는 것이 1 건 이상」이고 「달라진 변경」이 아니다. 그리고 소유자가 `:428` 에서 그 읽기를 이름으로 못 박았다. 초안의 §4 「두 읽기가 3 과 0 으로 갈린다」는 **이미 닫힌 갈래를 다시 연 것**이고, 원장에 존재하지 않는 대립을 남긴다."*

### 판 3 · A4 — *"칸 3 은 `C1` 의 모집단만 정했다"* 가 자기 인용 구간 안의 `:428` 로 반박된다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨세 원천 같은 값⟩
- 어디가 걸리나: `intent.md:428` · 합이 직접 확인한 사실 자리 `xtask/src/main.rs:6407` ← `plan/126-pre.md:82` ⑵ · `xtask/src/main.rs:643` ← `effect/126-delta.md:27` · `defect.rs:326` ← `plan/79-pre.md:122` ⑶ 이 `defect.rs:322` 를 지정 · `effect/79-delta.md:15`
- 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **범위를 좁혀 채택.** *"⚠ **범위를 좁혀 채택한다** — `:428` 은 `C2` 의 **합격선 읽기**(귀속으로 센다)를 닫지만, R7 이 실제로 가리킨 **사실**(분자 셋의 결과 좌표가 봉인이 미리 지정했거나 「안 건드린」 자리다)까지 닫지는 않는다. 내가 그 사실을 직접 확인했다 … **판정값에서는 닫히고 발견으로는 산다**."* 합의 「초안을 어떻게 고치나」 4 — *"결과 좌표 셋이 봉인 지정 자리라는 관측은 **발견**으로 옮긴다(`C4`·`D2` 의 입력이다)."*

### 판 3 · A5 — 초안 §1 이 세운 사실이 반증 조항 8 의 발동 조건인데 초안이 조항 8 을 한 번도 안 불렀다

- 모집단: 회차기록
- 유효성: 참
- 해악도: **금지역** ⟨세 원천 같은 값⟩
- 어디가 걸리나: `dialectic/3-design.md:242-243` ↔ 초안이 부른 `dialectic/3-design.md:238` · 초안이 쓴 `effect/79-delta.md:23-31`·`:39` · 핀 `dialectic/preflight.md:28`·`:27`·`:37` · `dialectic/3-thesis-r2.md:43`
- 처분: 정정 · 승격됨: **예** · 조건변경: 없음 · 사전처분: 수용사유 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **채택.** *"**조항 8 이 발화했다고 판정한다.** 둘 다 내가 기계로 확인했다 — ⑴ `shasum -a 256` 이 `touch/79.txt`(`c53ded18…`) · `touch/129.txt`(`15a2acd9…`) · `effect/79-delta.md`(`fa3b9237…`)를 핀(`preflight.md:28`·`:27`·`:37`)과 다르게 낸다. ⑵ **초안이 그 핀 밖 바이트에 기댔다** … ⚠ 감경 사유도 적는다 — 초안이 드리프트를 **숨기지 않고 §1 로 먼저 보고했고** 핀 시점 바이트에서 좌표가 다 맞는다는 것도 확인했다."*
- ★ **소유자가 답했다** — `intent.md` `## 승격` 칸 6: **「집행 면제 · 드리프트를 기록으로 남긴다」**(원문은 아래 `## 종료 판단` 에 그대로 옮겼다).
- ⚠ **내가 잰 것** — 핀 대비 드리프트는 **셋이 아니라 다섯**이다. 아래 `DL2-23` 을 보라.

### 판 3 · A6 — 초안이 `intent.md:412` 를 잘못 인용했고 그 잘못이 판정의 성격을 감춘다

- 모집단: 회차기록
- 유효성: 참
- 해악도: **금지역** ⟨세 원천 같은 값⟩
- 어디가 걸리나: `intent.md:412` · `:410` · `:414` · `:408-410`
- 처분: 정정 · 승격됨: **예** · 조건변경: 없음 · 사전처분: 계획수정 · 상태: **열림** ⟨★ 소유자가 답한 칸 6·7 중 **어느 것도 `A6` 이 아니다** — 아래 `## 소유자에게 올릴 물음` 을 보라⟩ · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **채택.** *"**바이트로 확인했다.** `intent.md:412` 의 실제 글자는 「**답 — 「미측정으로 닫고 문면 셋을 승격」.**」 이고, 초안이 `:412` 로 돌린 「이번 회차 안에서 그 셋을 고쳐 다시 재지 않는다」는 **물음의 둘째 갈래**(`:410`)다. ★ **네가 정하라 한 것 — 소유자의 답은 「절차 선택」이 아니라 「판정값의 확정」이다.** 근거 둘: ⑴ 물음(`:408-410`)이 「미측정으로 닫고 … 승격할 것인가」로 **값의 낱말을 담아 물었고** 답이 그 낱말을 그대로 골랐다. ⑵ `:414` 가 「게이트 `## 판정` 의 `미측정` 칸에 `C2`·`C2-b` 가 들어가고」로 **집행을 지시한다.** 그러므로 이 판의 값 산출은 **판정이 아니라 기록**이고, 그렇게 안 읽히게 쓴 초안은 원장에 「독립 판정 둘」을 더 얹는다 — 「측정이 죽은 가지」다."*

### 판 3 · A7 — 초안이 올린 「승격 네 번째 항」이 갈 자리가 없다

- 모집단: 회차기록
- 유효성: **거짓** ⟨반은 「참」으로 냈고 합이 *"「이미 집행됐다」는 「넷째가 못 들어간다」를 안 함의한다"* 로 기각했다. **두 원천이 갈렸다**⟩
- 해악도: 거짓신호 ⟨반이 매긴 값. 합은 기각하며 다시 안 매겼고 종료 판단 표에도 안 실렸다⟩
- 어디가 걸리나: `intent.md:415` · `git diff fc961d3..HEAD -- …/intent.md` · `dialectic/3-antithesis-r2.md:45`
- 처분: 기각 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 닫힘 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **기각.** *"승격은 기존 이슈에 항을 더하거나 새 이슈를 세워도 되고, 반 스스로 `3-antithesis-r2.md:45` 에서 「나는 `#140` 본문을 안 봤다」라 적어 자기 전제를 못 세웠다. 초안이 `fc961d3` 에서 잤다는 것도 확인은 된다(`git show fc961d3:…/intent.md` 의 `:415` 에는 `#140` 이 없다)만, 그것은 **초안을 탓할 근거가 아니라 면책 사유**다. ★ 넷째 항은 **다른 근거로** 떨어진다 — `A3`·`A4` 대로 `intent.md:428` 이 「달라진 변경」 셈 규칙을 이미 닫았으므로 승격할 것이 남지 않는다."*

### 판 3 · A8 — 초안이 「내가 전수 재검으로 새로 세웠다」로 내놓은 오염 중 둘은 회차가 봉인 직후 스스로 이미 적어 둔 것이다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨세 원천 같은 값. ⚠ 반이 자백한 것 — *"A7·A8·A9 를 처음 `금지역` 으로 매겼다가 … `거짓신호` 로 내렸다"*⟩
- 어디가 걸리나: `effect/126-readnote.md:67` · `effect/79-readnote.md:67`
- 처분: 축소 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **범위를 좁혀 채택.** *"⚠ **범위를 좁혀 채택한다** — 초안이 **실제로 새로 세운 것**은 `nodes_of.txt:10` 으로 ㉠ⓐ·㉠ⓑ 까지 오염이 갔다는 확장이고 그것은 readnote 에 없다. A8 은 ㉠ⓒ·㉡ⓒ 두 행의 「내가 새로 세웠다」만 깎는다."*

### 판 3 · A9 — 초안이 `effect/79-judge-1.md:23` 을 「세상에 대해 거짓인 문장」이라 적고 덧붙임을 요구한 것이 과하다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 ⟨세 원천 같은 값⟩
- 어디가 걸리나: `effect/79-judge-1.md:23` · `observations/red.md:229` · `observations/premortem-artifacts/README.md:1` · `observations/premortem-artifacts/nodes_of.txt:2`
- 처분: 축소 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **채택.** *"**두 시점 다 `pal touch` 가 낸 값**이다. 판정자 문장의 앞절(「사전 등록이 미리 안 것은 「grep 호출 자리 4」뿐」)이 보이듯 그것은 **도구 귀속**이지 회차 내 시간 순서가 아니다. `C3-a` 가 못 고치게 한 원문에 「거짓」 딱지를 다는 것은 기록을 더럽힌다. ⚠ 오염 사실 자체(메인이 봉인 전에 그 바이트를 쥐고 있었다)는 **그대로 산다** — 그것은 판정자의 문장이 아니라 **배제 집합의 정의역** 문제다."*

### 판 3 · A10 — 분모가 초안 안에서 안 정해진다(§3 일곱 · §4 여섯 · 명단 여덟)

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관 ⟨세 원천 같은 값⟩
- 어디가 걸리나: `effect/judge-roster.md:9-13` ↔ 초안 §3 표 · §4 표
- 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **채택.** *"확인했다 — §3 표는 **일곱 행**인데 §4·「대안」은 「여섯」으로 적고, `judge-roster.md:9-13` 의 유효 합계는 3+0+3+1+1 = **여덟**(2 번·5 번이 대조·재실행이라 중복). 판정값에 안 걸리나 **원장이 이 수를 옮겨 적으면 검산이 어긋난다**."* 합의 「초안을 어떻게 고치나」 9 — *"분모를 일곱으로 통일한다."*

### 판 3 · A11 — 초안의 §2 가 통째로 라운드 1 의 반론 다섯을 다시 세는 절인데 반증 조항 7 을 한 번도 안 부른다

- 모집단: 회차기록
- 유효성: 참 ⟨반이 매긴 값. **합은 유효성도 처분도 안 매겼다**⟩
- 해악도: 거짓신호 ⟨반이 스스로 매긴 값. ⚠ **합이 채택·기각을 안 매겨 종료 판단의 해악도 분포 표에 안 실렸다** — 종료 판단: *"해악도가 없는 반론은 이 판단에 들어오지 못한다 — 즉 판 3 은 *분류되지 않은 반론 하나를 남긴 채* 상한을 소진했다"*⟩
- 어디가 걸리나: `dialectic/3-design.md:240-241` · `dialectic/3-antithesis.md` ↔ `dialectic/3-antithesis-r2.md`
- 처분: 기각 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 탐지수단 · 상태: 닫힘 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: 합의 문장 그대로 — *"**`A11`** — 채택·기각을 안 매긴다. 「취합 보고(`pal-debate-reporter`)가 잰다.」 반증 조항 7(`3-design.md:240-241`)은 `dialectic/3-antithesis.md` 와 나란히 세야 하고, 반도 나도 그 파일을 안 받았고 안 열었다."* **★ 내가 쟀다 — 아래 `## 반증 여덟 항목 점검` ⑦. 결과는 「조항 7 은 안 걸린다」이므로 A11 이 가리킨 귀결(교착 절차)은 발동하지 않는다. 그래서 처분을 「기각」으로 적는다** — ⚠ 다만 **A11 이 가리킨 절차 공백(조항 7 을 아무도 안 불렀다)은 참이었고, 그것을 처음 부른 것이 A11 이다.**

### 판 3 · 반증 조항 6 이 두 라운드 연속으로 걸렸다

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호 ⟨합이 따로 안 매겼다 — *"발견으로 올린다"* 까지만 적었다. 값은 **내가 매긴 것이다**⟩
- 어디가 걸리나: `dialectic/3-design.md:238` · `dialectic/3-synthesis.md`(라운드 1 `C2` 미측정 · `C2-b` 미측정) · `dialectic/3-synthesis-r2.md:19`(라운드 2 `C2` 미측정 · `C2-b` 미측정)
- 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 탐지수단 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: 합의 문장 — *"⚠ **반증 조항 6 이 두 라운드 연속으로 걸렸다**(`dialectic/3-design.md:238` — `C2`·`C2-b` 가 또 같이 움직였다). 다만 이번 라운드는 **둘이 같은 근거로 움직인 것이 아니라 `C2` 가 `C2-b` 에 종속한다**는 것을 좌표로 세웠다 — 조항 6 의 「실은 한 조건」이 **참일 수 있다**는 쪽으로 증거가 하나 늘었다. 발견으로 올린다."* **내가 대조했다** — 두 라운드 다 두 값이 `미측정`·`미측정` 로 같다. 「언제나 같이 움직인다」는 걸리고 「같은 근거만 댄다」는 라운드 2 에서 안 걸린다.

### 내가 쟀다 — 핀 대비 드리프트가 셋이 아니라 다섯이다

- 모집단: 회차기록
- 유효성: 참 ⟨내가 `hashlib.sha256` 으로 `preflight.md` 의 40 개 핀 전수를 `05f07ea` · `fc961d3` · `07e9653` · `30488ae` · 현재 바이트에 대어 쟀다⟩
- 해악도: 거짓신호
- 어디가 걸리나: `dialectic/preflight.md` 의 sha256 목록 40 개 중 `fc961d3`(라운드 2 정이 선언한 핀)에서 어긋난 것 — `touch/79.txt` · `touch/129.txt` · `effect/79-delta.md` **(합이 센 셋)** + **`intent.md` · `findings.jsonl` (아무도 안 센 둘)**. 그리고 `dialectic/preflight.md` 자신은 `05f07ea` 에 추적 안 됐고 `docs/gates/effect-confirmation.md` 는 `05f07ea`·`fc961d3`·`30488ae` 어디에도 커밋 안 돼 있다
- 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 탐지수단 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: 합은 *"⑴ `shasum -a 256` 이 `touch/79.txt` · `touch/129.txt` · `effect/79-delta.md` 를 핀과 다르게 낸다"* 로 **셋**을 셌다. 내가 전수를 대니 **다섯**이다. `intent.md` 는 `## 승격` 칸 1~5(`06e8d63`)·칸 6~7(`30488ae`)이 들어가 움직였고, `findings.jsonl` 은 라운드 1 의 36 레코드가 들어가 움직였다. **두 파일 다 라운드 2 의 판정이 직접 인용한 증거다.** 소유자 칸 6 이 면제한 것은 「판을 다시 도는 것」이고 이 두 파일은 그 칸의 근거 목록에 **안 들어 있다.**

### 내가 쟀다 — 판 1 의 라운드 2 도 핀 밖 바이트에 기댔는데 조항 8 을 한 번도 안 불렀다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `git show 05f07ea:…/intent.md` 에는 `## 승격` 이 `:379` 에 **머리만** 있고 칸이 하나도 없다. 칸 1~5 는 `fc961d3:…/intent.md:386-443` 에 처음 선다. 그런데 판 1 의 정은 `intent.md:393-399`(칸 1)를 ③ 의 근거로 대고, 판 1 의 합은 `## 승격` 칸 1 을 초안 ⑦ 의 판정 근거로, 칸 3 을 확장 판단의 근거로 댄다 — `dialectic/1-thesis-r2.md:45` · `dialectic/1-synthesis-r2.md:26` · `:70`. 설계문 `dialectic/1-design.md:242-243` 의 조항 8 은 *"판정문이 기준 핀 밖의 바이트에 기댔다 … 그 판은 무효이고 다시 돌린다"* 다
- 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 탐지수단 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: **판 1 의 어느 산출물도 조항 8 을 부르지 않았다**(내가 `1-thesis-r2.md`·`1-antithesis-r2.md`·`1-synthesis-r2.md` 전문에서 「조항 8」·「반증 조항」 문자열을 찾았고 없다). 판 3 만 불렀고 소유자 칸 6 이 판 3 에 대해 면제했다. 칸 6 은 *"집행하면 판 1·2·4 도 같은 핀 위에 있으므로 범위를 함께 정해야 한다"* 를 합의 물음 안에 담았으나 **답의 범위가 판 1 을 이름으로 덮는지는 칸 6 문면에 없다.**

### 내가 쟀다 — 판 1 의 `AT2-04` 는 라운드 1 `R3`(`DL1-03`)의 재발이다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: 라운드 1 `dialectic/1-antithesis.md:26`(R3) ↔ 라운드 2 `dialectic/1-antithesis-r2.md:28`(AT2-04). 공유 좌표 — `xtask/src/main.rs` 의 `unwrap_or(i64::MAX)` 자리(R3 `:6422-6428` ↔ AT2-04 `:6425`) · 판정문 문구(R3 `:6358` ↔ AT2-04 `:6356-6359`). 원장 `DL1-03` 이 라운드 1 에서 이미 `정정`·`거짓신호`·`열림` 으로 실렸다
- 처분: 확대 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 탐지수단 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: 두 반론의 **발동 조건이 다르다**(R3 = `git` 이 실패해 전 회차가 `i64::MAX` 로 묶임 · AT2-04 = 커밋 시각 없는 회차가 둘 이상) 그러나 **귀결과 미기재 지적이 같다** — 「사전순이 「최근」의 자로 되돌아가고 판정문이 그 사실을 안 적는다」. **원장에 두 레코드로 실리면 같은 결함이 두 번 세어진다.** 세는 것은 내 자리가 아니고, 이 사실을 적는 것이 내 자리다.

### 내가 쟀다 — 반(反) 판 3 이 받은 과업문에 사실이 아닌 것이 하나 있었다

- 모집단: 회차기록
- 유효성: 참 ⟨반이 `3-antithesis-r2.md:11` 에 스스로 적었고 내가 `git diff --stat fc961d3..2178154~1` 로 재확인했다 — 움직인 것은 일곱이고 `scripts/f12-verify.py` 는 없다. 그 파일의 마지막 변경은 `2d47da6` 으로 `fc961d3` 보다 앞선다⟩
- 해악도: 거짓신호 ⟨회차 금지역 문면 `intent.md:87-88` 「사실이 아닌 것을 사실로」에 닿으나, 반이 즉시 잡아 반환문에 적었고 판정값에 안 걸렸다. **합은 이 항을 처분 표에 안 넣었다**⟩
- 어디가 걸리나: `dialectic/3-antithesis-r2.md:11` · `git diff --stat fc961d3..HEAD`
- 처분: 정정 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 계획수정 · 상태: 열림 · 출처: 정반합 · 라운드: 2
- 채택·기각과 근거: 반의 문장 그대로 — *"⚠ 내가 받은 과업문 자체에 한 군데 어긋남이 있다 — 「움직인 것은 `docs/gates/effect-confirmation.md` · `scripts/f12-verify.py` · `dialectic/` 과 `state.md`」 라 적혔는데, `git diff --stat fc961d3..HEAD` 에 `scripts/f12-verify.py` 는 **없다**."* **합의 채택·기각 표 어디에도 이 항이 없다** — 반론표 밖에 적혔기 때문이다. 커밋 `a1493b5` 가 이 사실을 따로 적었다.

## 내가 기각한 것

| 요약 | 경로 | 모집단 | 유효성 | 해악도 |
|---|---|---|---|---|
| 「합 판 1 의 채택 표 해악도 분포와 종료 판단 표가 갈린다」 — 세워서 대조했고 **일치했다.** 합의 채택 다섯은 `AT2-03` 거짓신호 · `AT2-06` 거짓신호 · `AT2-04` 거짓신호 · `AT2-05` 미관 · `AT2-07` 미관이고 종료 판단 표는 거짓신호 3 · 미관 2 다. 기각 | `dialectic/1-synthesis-r2.md:34-38` ↔ `dialectic/r2-referee.md:19-24` | 회차기록 | 거짓 | 미관 |
| 「A11 은 헛것이다 — 조항 7 이 안 걸렸으므로」 — 세웠다가 물렀다. 조항 7 이 안 걸린 것은 **내가 재고 나서** 안 것이고, 라운드 2 의 어느 자리도 그 조항을 안 불렀다는 절차 공백은 A11 이 처음 짚었다. 「결과가 음성이었다」가 「물음이 헛것이었다」를 안 함의한다. 기각 | `dialectic/3-antithesis-r2.md:35` · `dialectic/3-design.md:240-241` | 자기장치 | 거짓 | 미관 |

사람이 정하는 칸은 두 행 다 같다 — 처분: 기각 · 승격됨: 아니오 · 조건변경: 없음 · 사전처분: 해당없음 · 상태: 닫힘 · 출처: 정반합 · 라운드: 2.

## 계수 — 두 원천 대조

| 잰 것 | 반(反) | 합(合) | 종료 판단 | 같나 |
|---|---|---|---|---|
| 판 1 · 붙인 반론 번호 수 | **7**(`AT2-01`~`AT2-07`) | **7**(채택 5 + 기각 2) | — | **같다** |
| 판 1 · 채택 | — | **5**(`AT2-03`·`AT2-06`·`AT2-04`·`AT2-05`·`AT2-07`) | **5** | **같다** |
| 판 1 · 기각 | — | **2**(`AT2-01`·`AT2-02`) | 표 밖 | **같다** |
| 판 1 · 해악도 분포(살아남은 것) | 자기 매김 금지역 1 · 거짓신호 5 · 미관 1 | 거짓신호 3 · 미관 2 | 금지역 0 · 실패 0 · 거짓신호 3 · 미관 2 | **합 ↔ 종료 판단 같다 · 반 ↔ 합 갈린다**(반의 금지역 1 은 기각됐고 `AT2-05` 는 거짓신호→미관으로 내려갔다) |
| 판 1 · 반이 스스로 물린 것 | **5** | 처분 표에 안 실림 | — | ⚠ **갈린다 — 합이 안 셌다** |
| 판 3 · 붙인 반론 번호 수 | **11**(`A1`~`A11`) | **10**(채택 9 + 기각 1) | 9(표에 든 것) | ⚠ **갈린다 — 차이는 `A11` 하나다.** 합이 채택·기각을 안 매겼고 종료 판단이 *"해악도가 없는 반론은 이 판단에 들어오지 못한다"* 로 표 밖에 뒀다 |
| 판 3 · 채택 | — | **9**(`A1`·`A5`·`A6`·`A2`·`A3`·`A4`·`A8`·`A9`·`A10`) | **9** | **같다** |
| 판 3 · 기각 | — | **1**(`A7`) | 표 밖 | **같다** |
| 판 3 · 해악도 분포(살아남은 것) | 자기 매김 금지역 3 · 거짓신호 7 · 미관 1 (계 11) | 금지역 3 · 거짓신호 5 · 미관 1 (계 9) | 금지역 3 · 실패 0 · 거짓신호 5 · 미관 1 (계 9) | **합 ↔ 종료 판단 같다 · 반 ↔ 합 갈린다**(반의 거짓신호 7 중 `A7` 은 기각 · `A11` 은 미분류) |
| 판 3 · 반이 스스로 물린 것 | **6** | 처분 표에 안 실림 | — | ⚠ **갈린다 — 합이 안 셌다** |
| 판 3 · `C2-b` 의 분모 | 초안 §3 **7** ↔ §4 **6** ↔ `judge-roster.md` **8** | **7 로 통일하라**(합 「고치나」 9) | — | ⚠ **세 원천이 갈렸다 — `A10` 이 그것이다** |
| 두 판 · 조건 문면을 바꾸나 | — | **안 바꾼다**(둘 다) | — | **같다** · `intent.md` `## 승격` 의 *"`## 완수 조건` 의 상자 50 개는 문면도 수도 그대로다"* 와도 같다 |
| 라운드 2 발견 항 총수(이 보고) | — | — | — | `### ` 항 **27** + `## 내가 기각한 것` 표의 데이터 행 **2** = **29**. 라운드 1 은 `DL1-01`~`DL1-36` 으로 **36** 이었다 |

## 반증 여덟 항목 점검 — 라운드 2 에 대해

#### ① 반론표에 `파일:줄` 인용이 0 건인 라운드가 있다

**안 걸린다.** 내가 정규식으로 셌다 — `1-antithesis-r2.md` 좌표 토큰 **91**(고유 86), `3-antithesis-r2.md` **57**(고유 44). 견줄 값으로 라운드 1 은 `1-antithesis.md` 27 · `3-antithesis.md` 107 이다.

#### ② 정의 판정문이 `effect/126-judge-1.md` 와 같은 문장을 3 개 이상 연속 공유한다

**안 걸린다.** 두 초안(`1-thesis-r2.md`·`3-thesis-r2.md`)을 판정자 원문 넷(`126-judge-1.md`·`79-judge-1.md`·`129-judge-2.md`·`126-judge-2.md`) 전부에 대어 18 자 이상 문장을 대조했다. 최대 연속 공유는 **1**(`1-thesis-r2.md` ↔ `79-judge-1.md`)이고 나머지는 **0** 이다.

#### ③ 판 넷이 전부 라운드 1 에서 「전원 통과 · 살아남은 반론 0」

**안 걸린다.** 라운드 1 에서 판 1 은 금지역 1 을 남겼고 판 3 은 금지역 4 · 실패 1 을 남겼다(종료 판단이 그 수를 적는다). 원장 `DL1-01`~`DL1-36` 이 그 반론 36 건이다.

#### ④ 판 2 의 산출에 `ledger.rs`·`pal-git/lib.rs`·`touch.rs` 중 둘 이하만 인용됐다

**이 라운드에는 점검 대상이 없다.** 종료 판단이 *"판 2 는 라운드 1 에서 끝났고"* 라 적었고 라운드 2 에 판 2 의 산출물이 없다.

#### ⑤ 판 4 의 반론 과반이 「방향 값이 틀렸다」류다

**이 라운드에는 점검 대상이 없다.** 종료 판단이 *"판 4 는 `E2` 뒤로 미뤄졌다(소유자 승격 칸 5)"* 라 적었고, `intent.md` `## 승격` 칸 5 의 답이 *"「`E2` 뒤로 미뤄 판정한다」"* 다.

#### ⑥ 판 3 에서 `C2` 와 `C2-b` 가 같은 근거만 대고 두 판정값이 언제나 같이 움직인다 ★ 실제로 대조했다

**절반 걸린다.** 값은 라운드 1 에서 `C2` 미측정 · `C2-b` 미측정(`3-synthesis.md`), 라운드 2 에서도 `C2` 미측정 · `C2-b` 미측정(`3-synthesis-r2.md:19`) — **두 라운드 연속으로 한 번도 안 갈렸다.** 근거는 라운드 2 에서 갈렸다: `C2` 의 근거는 「원인 칸이 touch 인 행 셋 + `intent.md:428` 로 읽기가 잠김 + `C2-b` 에 종속」이고 `C2-b` 의 근거는 「배제 집합의 정의역이 `nodes_of.txt:10` 을 못 덮는다」다. 합이 같은 판단을 냈다 — *"둘이 같은 근거로 움직인 것이 아니라 `C2` 가 `C2-b` 에 종속한다는 것을 좌표로 세웠다 — 조항 6 의 「실은 한 조건」이 참일 수 있다는 쪽으로 증거가 하나 늘었다."* 발견으로 실었다(위 `### 판 3 · 반증 조항 6 이 두 라운드 연속으로 걸렸다`).

#### ⑦ 라운드 2 의 반론이 라운드 1 의 것과 8 할 이상 같은 주장이다 ★ 합이 못 재고 나에게 넘긴 자리 · 내가 쟀다

**두 판 다 안 걸린다.** 잰 법 — 라운드 2 반론 하나하나를 라운드 1 반론 전수와 주장 단위로 대고, 보조로 인용 좌표의 파일 겹침을 기계로 셌다.

**판 3** — `dialectic/3-antithesis.md`(R1~R10, **10** 건) ↔ `dialectic/3-antithesis-r2.md`(A1~A11, **11** 건).

| 라운드 2 | 라운드 1 에 같은 주장이 있나 | 판단 |
|---|---|---|
| A1 | `R3` 과 **주제 같고 결론이 정반대** — R3 은 *"계기가 유효 판정에서 인용을 원리상 못 낳는다"*(채택·금지역), A1 은 *"같은 계기가 실제로 낳았다"* | 같은 주장 아님 |
| A2 | 없음 — readnote 넷째 칸을 「적혔다」의 모집단으로 부른 자리가 라운드 1 에 없다 | 새롭다 |
| A3 | 없음 | 새롭다 |
| A4 | 없음 — 근거인 `intent.md:428` 은 라운드 1 **뒤에** 생긴 바이트다 | 새롭다 |
| A5 | 없음 — 라운드 1 반론표에 기준 핀 논의가 0 건이다 | 새롭다 |
| A6 | 없음 — 근거인 `intent.md:412` 는 라운드 1 뒤에 생겼다 | 새롭다 |
| A7 | 없음 — `#140` 은 `9e092e3` 이 라운드 1 뒤에 세웠다 | 새롭다 |
| A8 | `R2` 와 **주제 같고 방향 반대** — R2 는 *"readnote 가 ⓑ 를 안 적었다"*, A8 은 *"readnote 가 ㉠ⓒ·㉡ⓒ 를 이미 적었다"* | 같은 주장 아님 |
| A9 | 없음 | 새롭다 |
| A10 | `R6` 과 **인접**(둘 다 수) 그러나 R6 은 ㉠/㉡ 셈 규칙 비대칭, A10 은 초안 내부 분모 7/6/8 불일치 | 같은 주장 아님 |
| A11 | 없음 — 조항 7 자체를 부른 첫 자리다 | 새롭다 |

**같은 주장 0 / 11 = 0.0 할.** 넉넉히 「주제가 겹치는 것」까지 세어도 3 / 11 = **2.7 할**. 8 할에 한참 못 미친다 → **조항 7 안 걸린다 · 교착 절차로 안 간다.** 기계 보조 대조(인용 파일 겹침)도 같은 방향이다 — A3·A4·A6·A7·A10·A11 은 라운드 1 반론과 **공유하는 근거 파일이 하나도 없다.**

**판 1** — `dialectic/1-antithesis.md`(R1~R6, **6** 건) ↔ `dialectic/1-antithesis-r2.md`(AT2-01~AT2-07, **7** 건).

| 라운드 2 | 라운드 1 에 같은 주장이 있나 | 판단 |
|---|---|---|
| AT2-01 | 없음 — 라운드 1 의 반은 *"`ledger.rs:220-225` 의 「합이 분모와 같다」를 코드로 재검증하지 못했다 … 좌표를 못 대므로 반론으로 안 냈다"* 로 **스스로 물렸다** | 새롭다 |
| AT2-02 | `R1` 과 **부분 겹침** — 둘 다 *"「같은 글자」가 여전히 같다"* 를 친다. 그러나 R1 의 대상은 tally 버킷·화면·시험(`e44a87e`·`06e8d63` 가 닫았다)이고 AT2-02 의 대상은 2 층 `SymbolNode.identity` 값이다 | 부분 겹침 |
| AT2-03 | 없음 — 라운드 2 초안에만 있는 문장을 친다 | 새롭다 |
| AT2-04 | **`R3` 과 같은 주장이다** — 같은 코드 자리(`unwrap_or(i64::MAX)`) · 같은 귀결(사전순으로 되돌아감) · 같은 미기재 지적(판정문이 안 적는다). 발동 조건만 다르다 | **같다** |
| AT2-05 | `R2` 와 **부분 겹침** — 둘 다 *"이슈/출처의 수치가 실측과 안 맞는다"*. R2 는 다른 회차의 `44 건(1.6%)`, AT2-05 는 `464`·`7,156` ↔ `20`·`3306` 의 자기모순 | 부분 겹침 |
| AT2-06 | 없음 | 새롭다 |
| AT2-07 | 없음 | 새롭다 |

**같은 주장 1 / 7 = 1.4 할.** 부분 겹침까지 세면 3 / 7 = **4.3 할**. 8 할 미만 → **조항 7 안 걸린다.** ⚠ 다만 `AT2-04` 의 재발 자체는 발견으로 실었다(`DL2-25` 자리).

#### ⑧ 판정문이 기준 핀 밖의 바이트에 기댔다 ★ 실제로 대조했다

**두 판 다 걸린다.** `dialectic/preflight.md` 가 박은 핀은 `05f07ea` 이고 그 목록은 40 개다. 내가 그 40 개를 다섯 시점에 대어 쟀다.

| 시점 | 핀 목록 40 개 중 어긋난 것 |
|---|---|
| `05f07ea`(핀 자신) | 0 ⟨`dialectic/preflight.md` 는 그 시점에 추적 안 됨 · `docs/gates/effect-confirmation.md` 는 어느 시점에도 커밋 안 됨⟩ |
| `fc961d3`(판 1 의 정이 선언한 자기 핀) | **5** — `intent.md` · `findings.jsonl` · `touch/129.txt` · `touch/79.txt` · `effect/79-delta.md` |
| `07e9653`(판 1 의 합이 읽은 HEAD) | **5** — 같음 |
| `30488ae`(현재 HEAD) | **5** — 같음 |

- **판 3** — 합이 조항 8 발화를 **선언했다.** 다만 센 것은 **셋**이고 실제는 **다섯**이다(`intent.md`·`findings.jsonl` 이 빠졌다). 소유자 칸 6 이 **집행 면제**로 답했다.
- **판 1** — **아무도 조항 8 을 안 불렀다.** 그런데 판 1 의 정도 합도 `intent.md` 의 `## 승격` 칸(`fc961d3:…/intent.md:386-443`)에 기대어 값과 확장 판단을 세웠고, 그 칸들은 `05f07ea:…/intent.md` 에 **존재하지 않는다**(그 시점에는 `## 승격` 머리 한 줄뿐이다). 발견으로 실었다.
- 라운드 2 는 **새 preflight 를 안 냈다.** 그래서 「이 라운드의 핀이 무엇인가」가 두 판에서 갈린다 — 판 1 은 `fc961d3` 을 자기 핀으로 선언했고, 판 3 은 `05f07ea` 를 핀으로 잡고 드리프트를 보고했다. **이 갈림 자체를 적는다.**

#### ★ 그리고 하나 더 — 판정값이 게이트 표에 실릴 때 살아남은 반론의 해악도가 같이 안 실리면

**아직 발화 전이다.** 내가 지금 `docs/gates/effect-confirmation.md` 를 열어 보니 `## 판정` 표(`:27`)가 `A1`~`E6` **전량을 `미측정` 칸에 두고 있고** 판 1 의 `B1` 통과가 아직 안 실렸다. 해악도 열도 없다. 그 표에 `B1` 을 옮길 때 살아남은 반론의 해악도(거짓신호 3 · 미관 2)를 같이 싣지 않으면 그때 이 조항이 걸린다.

## 확장 판단 — 합이 낸 것 그대로

#### 판 1

> 기본값은 **안 늘린다**. 뒤집는 유일한 시험은 자기 완결성 — *"안 하면 다음 회차에 짐이 되거나 의도가 변질되는가."*

| 나온 것 | 판단 | 사유 |
|---|---|---|
| **초안 ⑦ — `ledger.rs:310-320` 의 거짓 문단 셋** | **이번에 늘린다** | 자기 완결성 시험을 **통과한다.** ⓐ 이 회차가 이 회차의 승격 집행 커밋으로 만든 거짓이고, ⓑ 회차 스스로 선언한 금지역 「사실이 아닌 것을 사실로」에 들며, ⓒ **다음 회차가 이 파일을 열면 코드가 「그룹 단위로 안 바꿨다」고 말한다** — 소유자가 답한 승격 칸 1 을 부정하는 문장이 제품 코드에 남는다. ⚠ 「어렵다·시간이 없다」는 사유가 아니고, 이 건에는 그런 사유조차 없다(주석 문단 삭제·재작성). 함께 고칠 것 둘 — `ledger.rs:966` *"같은 이름이 둘이면 **둘째가** ①이다"*(바로 아래 `:972` 의 단언이 **전원 2** 를 요구한다) · `ledger.rs:949` *"판별자 상한은 `Exact` 이므로"*(`:364-365` 가 *"`identity_ceiling` 을 안 받는다 — 그것이 앞 판의 거짓이었다"* 로 스스로 부정한 규칙이다) |
| ⑦ 을 고치면 `effect/79-delta.md` 에 차이 한 행이 는다 | **함께 늘린다** | `C1` 이 *"사전 등록 ↔ 실제 변경의 차이 **전수**"* 를 요구하고 `## 승격` 칸 3 이 모집단을 「전부」로 못 박았다. 고치고 delta 를 안 늘리면 `C1` 이 조용히 반쪽이 된다. **원인 끝은 `touch/79.txt:N` 이 아니라 정반합이다** — 칸 1 이 세운 기입 형식을 그대로 쓴다 |
| AT2-04 의 `B6` 잔여(판정문이 사전순 동점을 안 적는다) | **안 늘린다 — 발견으로 올린다** | `B6` 은 ⟨결정론적⟩이고 설계문이 *"기계가 판정한다 … 그 산출이 틀렸다는 반론은 **발견**이지 이 판의 판정값이 아니다"* 로 못 박았다. `B6` 이 요구한 **처분**(*"가장 최근으로 본다"*)은 코드(`:6415-6416`)와 판정문(`:6357`) 양쪽에 있다. 안 적힌 것은 그 처분 **안의** 동점 규칙이다. 원장에 싣고 `B6` 의 닫힘은 기계·메인이 판정한다 |
| AT2-02 의 심볼 단위 잔여 | **안 늘린다** | `## 범위 밖` 의 `MS-09` 로 **사전 등록돼 있고**(`plan/79-pre.md` §3) 2층 스키마 없이는 원리상 못 고친다. `## 차선책` 2 가 그 경로를 명시로 막았다. 이번에 늘리면 회차가 스스로 뺀 것을 되집는 것이고 **의도가 변질된다** |
| AT2-01 의 「합 = 분모」 축 음성 대조 미측정 | **안 늘린다 — 발견으로 올린다** | 그 축의 대조는 `B5` 의 음성 대조가 요구한 자(*"가르는 자리를 끄면 빨개지는가"*)를 이미 만족했다(셋이 빨개졌다). 축을 하나 더 다는 것은 **이 회차가 만든 장치를 이 회차가 다시 먹이로 삼는 것**이고 규약 §11③ 이 그것을 15 라운드의 실제 원인으로 적는다. 미측정이라는 사실과 가르는 명령을 적는 것으로 닫는다 |
| 판정문 좌표 교정(위 ⑦) | **안 늘린다 — 초안 수정으로 흡수** | 조건도 코드도 안 건드린다 |

> **조건 문면은 한 글자도 안 고친다.** 설계문 `## 이 판이 안 다루는 것` 이 문면 개정을 금하고(*"고친 뒤 재는 양이 줄면 그것은 정정이 아니라 완화이고 승격이다"*), `B1` 의 문면이 의도를 못 잰다는 것은 이 라운드에서 안 드러났다 — `CA2-15` 가 경고한 *"해석이라 기계가 못 판정한다"* 는 정확했고, 그래서 이 판이 돌았고, 갈래는 이슈 본문 자신의 닫는 문장으로 갈렸다.

#### 판 3

> **안 늘린다.** 살아남은 아홉이 전부 `C2`·`C2-b` 의 판정과 그 기재 안에 있고, 새 역할·새 판·새 측정을 부르는 것이 하나도 없다. 「어렵다·시간이 없다」로 미룬 것은 없다.
>
> **단 하나는 「이번에 늘린다」로 낸다 — `#140` ② 의 문면 정정**(위 10). 사유는 규정된 그 하나다: *원 의도의 완결에 필요한가* → **필요하다.** 안 고치면 다음 회차가 *"계기가 원리상 못 낳는다"* 를 참으로 받고 고칠 것이 없는 것을 고치러 간다 — 의도가 그 자리에서 변질된다. 비용은 이슈 본문 한 단락이고 이 회차 안에서 끝난다.
>
> `C2-a` 의 `effect/negative-C2.md` 부재는 **이 판이 안 정한다** — ⟨결정론적⟩이라 기계가 판정한다(`3-design.md` *"이 판이 안 다루는 것"*). 실질은 `126-judge-1.md`(미끼 ⓓⓔ 무효)와 `126-judge-2.md`(유효 0)에 있다는 사실만 넘긴다.

## 종료 판단

#### 판 1 · 라운드 2/2 — **끝났다**

> 걸리는 줄은 끝 조건 표의 **첫째 줄** — *"전부 거짓신호·미관 → 종료. 메인이 초안을 고쳐 진행"* 이다. 살아남은 다섯 건이 모두 거짓신호·미관이고 금지역·실패는 하나도 없다.
>
> 셋째 줄(*"라운드 2 뒤에도 금지역·실패가 남는다 → 소유자에게 올린다"*)은 **안 걸린다.** 남은 금지역·실패가 0 이므로 상한 소진이 승격 사유가 되지 않는다. 상한에 닿은 것 자체는 완료도 승격도 아니고, 여기서는 해악도가 종료를 가른다.
>
> 「한 라운드 더」도 안 고른다. 상한 2 를 다 썼고, 그것을 넘길 근거인 금지역·실패가 없다. 교착 조건(같은 실패 3 회 반복, 또는 재현되는데 원인 못 댐)도 성립하지 않는다 — 살아남은 실패가 0 이다.
>
> 라운드 1 의 금지역 1 건이 라운드 2 에서 0 으로 내려간 것은 **분포가 실제로 움직였다**는 뜻이다. … 소유자에게 올린다면 — 물음 한 문장: **해당 없음. 승격 사유가 성립하지 않는다.**

#### 판 3 · 라운드 2/2 — **소유자에게 올린다**

> 걸리는 줄은 끝 조건 표의 **셋째 줄** — *"라운드 2 뒤에도 금지역·실패가 남는다 → 소유자에게 올린다. 「닫혔다」로 안 적는다"*.
>
> - 상한은 잠긴 의도가 잠근 **2 라운드**이고 판 3 은 2/2 를 다 썼다. 상한에 닿은 것은 완료가 아니므로 **「끝났다」로 안 적는다**. 「한 라운드 더」는 상한을 넘는 것이라 이 자리에서 못 고른다.
> - 라운드 1 (금지역 4 · 실패 1) → 라운드 2 (금지역 3 · 실패 0) 로 줄었으나 **0 이 아니다**. 감소 추세는 끝 조건이 아니다 — 끝 조건은 금지역·실패의 **부재**다.
> - **교착은 아니라고 적는다.** 같은 실패가 3 회 반복된 것이 아니고 재현-원인 불명도 아니다. 상한 소진 상태에서 금지역이 살아남은 것이지, 진행이 멈춘 것이 아니다.
> - ⚠ `A11` 은 채택·기각이 안 매겨져 위 표에 없다. **해악도가 없는 반론은 이 판단에 들어오지 못한다** — 즉 판 3 은 *분류되지 않은 반론 하나를 남긴 채* 상한을 소진했다. 이 사실 자체를 아래 칸 하나로 올린다.
>
> | # | 물음 |
> |---|---|
> | 1 | `A1` 의 금지역을 그대로 둔 채 판 3 을 진행시킬 것인가, 아니면 그 조건을 다시 설계할 것인가? |
> | 2 | `A5` 의 금지역을 그대로 둔 채 판 3 을 진행시킬 것인가, 아니면 그 조건을 다시 설계할 것인가? |
> | 3 | `A6` 의 금지역을 그대로 둔 채 판 3 을 진행시킬 것인가, 아니면 그 조건을 다시 설계할 것인가? |
> | 4 | 합이 해악도를 안 매기고 「취합 보고가 잰다」로 넘긴 `A11` 을, 취합 보고 전에 소유자가 직접 잴 것인가? |

#### ★ 소유자가 이미 답했다 — `intent.md` 의 `## 승격` 칸 6·7 원문 그대로

> #### 칸 6 ⟨판 3 라운드 2 · 합 `dialectic/3-synthesis-r2.md`⟩
>
> **물음** — 설계문의 **반증 조항 8** 이 발화했다(`dialectic/3-design.md:242-243` —
> *"그 판은 **무효이고 다시 돌린다**"*). 합이 기계로 둘을 확인했다: ⑴ `touch/79.txt` ·
> `touch/129.txt` · `effect/79-delta.md` 셋의 sha256 이 기준 핀 `05f07ea` 와 다르다
> ⑵ 초안이 **핀 뒤에 생긴 바이트**(`effect/79-delta.md:23-31`·`:39` — `06e8d63` 가 넣은 줄)에
> 기대어 값을 세웠다. 조항 8 을 집행할 것인가.
>
> **답 — 「집행 면제 · 드리프트를 기록으로 남긴다」.**
>
> 다시 돌려도 값은 안 움직인다 — `C2`·`C2-b` 는 칸 2 로 이미 확정됐고, 밀린 것은 본문이 아니라
> 머리에 일곱 줄이 들어간 것이다(핀 시점 바이트에서 좌표가 전부 맞았음을 정·반·합 셋이 각각
> 확인했다). ⚠ **대가를 적는다 — 조항 8 이 이 회차에서 한 번도 이빨을 안 썼다.**
> 장치를 세워 놓고 발화했을 때 면제하는 것이 이 저장소가 반복해 온 형태이고, 그 사실을
> 여기 남긴다.
>
> ★ **면제는 「안 고친다」가 아니다.** 줄 밀림 자체는 2026-09-12 에 **고쳤다** — ⓓ2 덧붙임을
> 머리 끝에서 **전 출력 뒤**로 옮겼다. 핀 대비 앞 65 줄(㉠)·38 줄(㉢)이 다시 **바이트로
> 동일**하고 `touch/79.txt:23`·`:36`·`:56` 과 `touch/129.txt:21`·`:23` 이 핀의 그 줄을 가리킨다.
> 면제한 것은 **판을 다시 도는 것**이지 결함을 두는 것이 아니다.
>
> #### 칸 7 ⟨판 3 라운드 2 · 합 `dialectic/3-synthesis-r2.md`⟩
>
> **물음** — 소유자가 이미 승인해 `#140` ② 로 올린 문장이 **반증됐다.** 그 문장은
> *"`C2-b` 가 요구한 「두 문서의 인용」을 판정자 반환 형식이 **원리상 못 낳는다**"* 인데,
> 합이 실물 반례 둘을 찾았다(`effect/79-judge-1.md:21-22` · `129-judge-2.md:39-40` 가 두 문서를
> 원문 인용으로 댄다). `#140` ② 의 문면을 이번 회차에서 고칠 것인가.
>
> **답 — 「이번 회차에서 고친다」.**
>
> 안 고치면 다음 회차가 거짓 전제 위에서 시작해 **고칠 것이 없는 것을 고치러 간다.**
> 살아남는 관측으로 바꿔 적는다 — *"반환 형식이 「없다」 쪽에 인용을 **요구하지 않아**
> 귀속마다 고르지 않게 나왔다"*(양쪽 원문 인용 3 · 한쪽 2 · 맨 「없다」 2).
> 조건 문면을 안 건드리므로 재승인을 안 부른다.

⚠ **내가 잰 대조** — 종료 판단이 낸 물음 넷 중 소유자 칸이 대응하는 것은 **둘**이다: 칸 7 ↔ 물음 1(`A1`) · 칸 6 ↔ 물음 2(`A5`). **물음 3(`A6`)과 물음 4(`A11`)에는 대응하는 칸이 없다.** 그리고 소유자가 *"이 **일곱**이 조건 문면을 바꾸나 — **안 바꾼다** … 칸 6 은 **절차 면제**이고 칸 7 은 **이슈 본문**이다. `## 완수 조건` 의 상자 50 개는 문면도 수도 그대로다 — **`36f0507` 의 조건 문면이 계속 선다.**"* 라 적었다.

## 소유자에게 올릴 물음

물음 하나에 칸 하나다. 아래 셋은 **아직 답이 없는 자리**이고, 나는 판정을 안 한다 — 갈렸다는 것만 적는다.

| # | 물음 |
|---|---|
| 1 | 종료 판단의 **물음 3** — `A6`(초안이 `intent.md:412` 를 오인용해 「소유자가 이미 값을 정했다」는 사실을 감췄다 · **금지역**)의 금지역을 그대로 둔 채 판 3 을 진행시킬 것인가, 아니면 그 조건을 다시 설계할 것인가? 칸 6·7 은 이 물음에 답하지 않았다 |
| 2 | 종료 판단의 **물음 4** — `A11` 은 이제 답이 있다(취합 보고가 쟀다: 판 3 겹침 **0.0 할** · 판 1 겹침 **1.4 할** · 둘 다 8 할 미만이라 **교착 절차로 안 간다**). 이 측정을 `A11` 의 처분으로 받아 「기각·닫힘」으로 원장에 실을 것인가, 아니면 소유자가 직접 다시 잴 것인가? |
| 3 | 칸 6 이 면제한 반증 조항 8 의 **범위와 계수**가 둘 다 실제보다 좁다. ⑴ 칸 6 이 센 드리프트는 **셋**인데 핀 목록 40 개를 전수로 대면 **다섯**이다(`intent.md`·`findings.jsonl` 이 더 있고, 둘 다 라운드 2 의 판정이 직접 인용한 증거다). ⑵ **판 1 의 라운드 2 도 핀 밖 바이트(`intent.md` 의 `## 승격` 칸 1·3 — `05f07ea` 에 없던 줄)에 기대어 값과 확장 판단을 세웠는데 판 1 의 어느 산출물도 조항 8 을 부르지 않았다.** 칸 6 의 면제를 이 다섯과 판 1 까지 덮는 것으로 읽을 것인가, 아니면 판 1 에 대해 따로 정할 것인가? |

---

**관련 파일(절대경로)**

- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/1-design.md` · `3-design.md`(같은 바이트) · `preflight.md`
- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/1-thesis-r2.md` · `1-antithesis-r2.md` · `1-synthesis-r2.md` · `1-antithesis.md`
- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/3-thesis-r2.md` · `3-antithesis-r2.md` · `3-synthesis-r2.md` · `3-antithesis.md` · `3-synthesis.md`
- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/dialectic/r2-referee.md`
- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md` · `findings.jsonl`
- `/Users/incognito/dev/projects/palimpsest/docs/gates/effect-confirmation.md`
