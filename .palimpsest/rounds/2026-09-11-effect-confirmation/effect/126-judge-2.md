# 귀속 판정 원문 — ㉡ `#126` · 인스턴스 2 ⟨`C3-b`⟩ · **`C2-a` 의 ⓑ 축**

> 판정자: **새 인스턴스**(`general-purpose` · sonnet · 도구 사용 0 회).
> 프롬프트는 `effect/judge-prompt.md` 의 **판 3** 이고, 넷은 판 1 과 **같은 바이트**이며
> 후보만 **무효로 잡힐 둘(ⓓ·ⓔ)** 로 줄였다.
> ★ **판정자는 이것이 대조용이라는 것을 모른다** — 프롬프트 본문이 판 1 과 같다.
> ⚠ 전달 과정에서 이스케이프된 `&lt;`·`&gt;`·`&amp;` 세 글자만 원문 글자로 되돌렸다.
> 표의 백틱 앞 역슬래시도 전달 이스케이프라 걷었다 — 그 밖에는 한 글자도 안 고쳤다.

---

## 받은 것

| 절 | 첫 줄 | 마지막 줄 | 줄 수 |
|---|---|---|---|
| ① 사전 등록 | `# 사전 등록 — ㉡ `#126`` | `  **차이 0 이 나오면 0 으로 적는다** ⟨`C5`⟩.` | 120 |
| ② touch 산출 | `# `pal touch` 산출 — ㉡ `#126`` | `  능력      ledger.snapshot · symbol.resolve · symbol.contains · symbol.callers · symbol.reaches · graph.dump · binding.status · narrative.unbound · binding.touch · plan.deviation · symbol.references · 미구축 F13 · F15` | 59 |
| ③ diff | `diff --git a/xtask/src/main.rs b/xtask/src/main.rs` | ` /// 축 아홉(`출처`·`모집단`·`유효성`·`해악도`·`처분`·`승격됨`·`조건변경`·`사전처분`·`상태`)의` | 144 |
| ④ §5.8 표 | `### 5.8 ⚠ 메인이 이미 오염됐다 — 사전부검이 `pal touch` 를 먼저 돌렸다` | `사전부검이 먼저 말했다.` | 17 |

## 판정

### ⓓ — 무효
- 인용한 줄: `  fun · xtask/src/main.rs:5934 · identity ordinal · body 1f1846428f55`
- 담은 사실: `check_ledger_pair` 의 선언 자리가 `xtask/src/main.rs:5934` 다.
- 사전 등록에 있나: 있다(인용) — "**`check_ledger_pair`** 하나. `xtask/src/main.rs:5934` 의 함수다." (§1) 및 "`:5934` `fn check_ledger_pair(root: &Path) -> Result<String>` — 검사 본체의 머리" (§2.1)
- §5.8 표에 있나: 없다 — 표는 결박 수·산출 줄 수·동일 줄 비교만 적고 선언 좌표는 안 적는다.
- 판정 근거: 이 사실은 §5.8 표에는 없지만 사전 등록 전문 §1·§2.1 이 이미 명시적으로 적어 두었다. ①에 있으므로(⑤5.8 여부와 무관하게) 무효다.

### ⓔ — 무효
- 인용한 줄: `■ 이 좌표에 걸린 것 (0)` / `  아직 없습니다.`
- 담은 사실: `check_ledger_pair` 좌표에 걸린 결박이 0 이다.
- 사전 등록에 있나: 있다(인용) — §2.3 이 §5.8 표를 전문 인용하며 "`check_ledger_pair` | 결박 **0** · 산출이 46 줄 · …" 를 그대로 싣고, §7 도 "touch 산출은 **46 줄** 안팎이고 결박 **0** 이다(§5.8 에서 이미 안다)" 라 적는다.
- §5.8 표에 있나: 있다(인용) — "`check_ledger_pair` | 결박 **0** · 산출이 46 줄 · 같은 파일 …"
- 판정 근거: 같은 사실이 §5.8 원표뿐 아니라 사전 등록 §2.3(§5.8 전문 인용)·§7(명시적 선언)에 이중으로 이미 있다. 인용한 두 줄이 담은 "결박 0"은 부분 없이 전부가 이미 알려져 있었으므로 무효다.

## 합계

유효 0 · 무효 2

---

## 부르는 쪽이 적는 것 — 이 판이 무엇을 세웠나

★ **`C2` 가 항등식이 아니다.** 무효로 잡힐 것만 제출한 사본에서 **유효 0** 이 났고,
그러면 `C2`(*"귀속되는 것이 1 건 이상"*)는 **그 사본에서 빨개진다.** 판 1 에서 유효 3 이
난 것이 구성상 보장된 값이 아니라는 뜻이다 ⟨`C2-a` ⓑ 축⟩.

★ **`C2-b` 도 항등식이 아니다** — 판 1 이 같은 둘을 **무효로 잡았다** ⟨`C2-a` ⓐ 축⟩.

⚠ **되불러 준 값 넷이 판 1 과 같다**(120 · 59 · 144 · 17). 두 인스턴스가 **같은 바이트**를
받았다는 기계 대조다.
