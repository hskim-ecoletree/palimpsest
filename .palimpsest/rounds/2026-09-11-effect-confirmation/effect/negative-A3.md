# 음성 대조 — `A3` ⟨`A3-a`⟩

> 회차 `2026-09-11-effect-confirmation` · 잰 날 2026-09-12 · 재는 자리 **㉠ `#79`**
> 조건 문면: *"좌표 줄을 지운 사본이 A3 판정을 **통과하는지** 재고 산출을
> `effect/negative-A3.md` 로 남긴다. **통과하면 A3 은 아무것도 안 잰다**"*

★ **이 대조는 방향이 반대다.** 나머지 넷은 *"빨개지는가"* 를 묻고 이것은 *"초록이 되는가"*
를 묻는다. **초록이면 불통과**다.

## 사본

`neg8` — `plan/79-pre.md` 에서 `파일:줄` 꼴 좌표를 담은 줄 **열한 개**를 지우고 봉인했다.
나머지 커밋 순서는 원본과 같다. 검사와 격리 조건은 `effect/negative-A1.md` 가 진다.

## 판정 — **안 통과했다**

```
FAIL  A3  — 좌표절 1 · 파일:줄 0 건
```

절 제목(`## 5. 바꿀 좌표와 각 자리에 무엇을 쓰나 ⟨A3⟩`)은 남았는데 **그 아래의 좌표가
0 건**이라 검사가 빨개졌다. 조건 문면이 요구한 것은 *"바꿀 `파일:줄` 목록과 각 자리에
무엇을 쓸 것인가"* 이고, **절 이름만으로는 안 채워진다**는 것을 이 사본이 보인다.

★ **딸려 나온 것** — 같은 사본에서 `A1-b`·`A1-c` 도 빨개졌다. 봉인 blob 이 원본과 달라져서다
(`선언 9b0f3a5a… ↔ 실제 ccc6be8f…`). **A3 축과 무관한 발화**이고 그대로 적는다.

## `A3-a` 의 판정 — **통과**

좌표를 지운 사본이 `A3` 을 **못 통과했다.** 그러므로 `A3` 은 아무것도 안 재는 것이 아니다.

## 전 출력

```
### 사본 neg8 — seal=6eec9a152ed7 touch=70121132c376 readnote=ffae041a8e96 change=1b9c6e356389
PASS  A1
FAIL  A1-b  — 선언=9b0f3a5a87f60f02e43daf9e2fb49455a5c31466 실제=ccc6be8fd680d5c016321ffc306229baa0a3e32c
FAIL  A1-c  — 인라인 ↔ blob 이 11 줄 다르다
FAIL  A3  — 좌표절 1 · 파일:줄 0 건
PASS  A5
PASS  A7
```
