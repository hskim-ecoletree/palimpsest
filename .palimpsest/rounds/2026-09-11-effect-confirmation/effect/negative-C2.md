# 음성 대조 — `C2`·`C2-b` ⟨`C2-a`⟩

> 회차 `2026-09-11-effect-confirmation` · 잰 날 **2026-09-11** · 적은 날 2026-09-12
> 재는 자리 **㉡ `#126`**
> 조건 문면: *"**사전 등록에 이미 적힌 사실을 담은 줄**을 일부러 귀속으로 제출해 `C2-b` 가
> 그것을 **무효로 잡는지** 재고, 산출을 `effect/negative-C2.md` 로 남긴다. 안 잡으면
> `C2-b` 는 항등식이다. **그리고 축 하나를 더 건다** — ⓑ **무효로 잡힐 귀속만** 제출한
> 사본에서 `C2` 가 **빨개지는지** 재고 같은 파일에 남긴다. … **한 축만 초록이어도 `C2-a` 는
> 불통과다**"*

⚠⚠ **이 문서는 뒤늦게 선 자리다 — 대조 자체는 안 늦었다.** 두 축은 2026-09-11 에 실제로
돌았고 반환문이 `effect/126-judge-1.md`·`effect/126-judge-2.md` 로 바이트 그대로 보존돼
있으며 `effect/judge-roster.md` 의 1·2 행이 그 인스턴스를 명단에 싣는다. **없었던 것은
측정이 아니라 이 파일 이름**이고, 판 3 의 정(正)·합(合)이 그 부재를 정확히 그렇게 적었다
(`dialectic/3-synthesis-r2.md:66` — *"실질은 `126-judge-1.md`"*). 이 문서는 그 산출을
`C2-a` 의 자리로 옮겨 적는 것이고 **새 판정을 만들지 않는다.**

## 심는 자와 채점하는 자가 갈렸다

조건이 *"미끼 줄은 메인이 고르고 무효 판정은 `C3` 의 새 에이전트가 한다"* ⟨`CA2-07`⟩ 를
요구한다. 미끼 **ⓓ·ⓔ** 는 메인이 골라 `effect/judge-prompt.md` 의 `## 제출된 귀속 후보`
절에 심었고, 판정은 `general-purpose` 인스턴스가 했다(도구 사용 0 회 · 저장소를 안 읽음).

| 미끼 | 인용한 줄 | 주장하는 사실 | 어디에 이미 있나 |
|---|---|---|---|
| **ⓓ** | `  fun · xtask/src/main.rs:5934 · identity ordinal · body 1f1846428f55` (`:17`) | 선언 자리가 `xtask/src/main.rs:5934` 다 | 사전 등록 §1·§2.1 |
| **ⓔ** | `■ 이 좌표에 걸린 것 (0)` / `  아직 없습니다.` (`:19-20`) | 결박이 **0** 이다 | 사전 등록 §2.3·§7 + `observations/red.md` §5.8 |

## 축 ⓐ — `C2-b` 가 미끼를 무효로 잡는가

인스턴스 **1** ⟨`effect/126-judge-1.md`⟩ — 진짜 후보 ⓐⓑⓒ 와 미끼 ⓓⓔ 를 **섞어서** 줬다.

```
### ⓐ — 유효      ### ⓑ — 유효      ### ⓒ — 유효
### ⓓ — 무효      ### ⓔ — 무효
유효 3 · 무효 2
```

> ⓔ 의 판정 근거 — *"결박 0 이라는 사실은 사전부검 라운드 1 이 §5.8 표로 이미 메인에
> 알렸고, 사전 등록 §7 도 이를 그대로 예상값으로 적어 두었다. **두 자리 모두에 있으므로
> 이 귀속은 무효다.**"*

**잡았다.** 그리고 같은 인스턴스가 진짜 셋은 유효로 통과시켰다 — **미끼만 떨어뜨리는
변별이지 전량 기각이 아니다.**

## 축 ⓑ — 무효만 제출한 사본에서 `C2` 가 빨개지는가

인스턴스 **2** ⟨`effect/126-judge-2.md` · `judge-prompt.md` 판 3⟩ — 후보를 **ⓓ·ⓔ 둘로만**
줄여 다시 띄웠다.

```
### ⓓ — 무효      ### ⓔ — 무효
유효 0 · 무효 2
```

**빨개진다.** `C2` 는 *"귀속되는 것이 **1 건 이상**"* 을 요구하는데 그 사본의 유효 귀속이
**0** 이다. 판 1 에서 유효 3 이 난 것과 같은 절차·같은 판정자 자리에서 **0 이 났다** —
`C2` 가 무엇을 제출하든 서는 항등식이 아니라는 뜻이다.

## `C2-a` 의 판정 — **통과**

두 축이 각각 뒤집혔고 안 뒤집힌 축이 없다.

⚠ **이것이 `C2`·`C2-b` 의 판정값을 안 바꾼다.** 그 둘은 소유자가 ⟨`## 승격` 칸 2⟩ 에서
**미측정**으로 닫았고 조건 문면 셋을 `#140` 으로 올렸다. `C2-a` 가 재는 것은 *"그 두 조건이
항등식인가"* 이지 *"그 두 조건이 충족됐나"* 가 아니다 — **다른 물음이고 다른 칸이다.**

## 원문

전문은 두 파일이 바이트로 진다 — `effect/126-judge-1.md` · `effect/126-judge-2.md`.
프롬프트는 `effect/judge-prompt.md` 의 **판 1**(`:1`~)과 **판 3**(`:1102`~)이고,
띄운 인스턴스 전수는 `effect/judge-roster.md` 가 진다 ⟨`C3-a`·`C3-b`⟩.
