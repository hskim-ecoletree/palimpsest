# 귀속 판정 원문 — ㉠ `#79` · 인스턴스 1 ⟨`C3-b`⟩

> 판정자: 새 에이전트(`general-purpose` · sonnet · **도구 사용 0 회**).
> 프롬프트는 `effect/judge-prompt.md` 의 **판 2** 이고 넷을 인라인으로 실었다.
> ⚠ 전달 과정에서 이스케이프된 `&lt;`·`&gt;`·`&amp;` 만 원문 글자로 되돌렸다.

---

## 받은 것

| 절 | 첫 줄 | 마지막 줄 | 줄 수 |
|---|---|---|---|
| ① 사전 등록 | `# 사전 등록 — ㉠ #79` | `- 새로 알게 될 것이 없으면 **차이 0 으로 적는다** ⟨C5⟩.` | 154 |
| ② touch 산출 | `# pal touch 산출 — ㉠ #79` | `  능력      ledger.snapshot · … · 미구축 F13 · F15` | 64 |
| ③ diff | `diff --git a/crates/pal-cli/src/defect.rs b/crates/pal-cli/src/defect.rs` | `     }` | **337** |
| ④ §5.8 표 | `### 5.8 ⚠ 메인이 이미 오염됐다 — 사전부검이 pal touch 를 먼저 돌렸다` | `사전부검이 먼저 말했다.` | 17 |

### ⓐ — 유효
- 인용한 줄: `  호출자 3 · 피호출자 1`
- 담은 사실: `nodes_of` 의 (참조 엣지 기준) 호출자가 3, 피호출자가 1이다.
- 사전 등록에 있나: 없다 — 사전 등록 §2.2는 `grep -rn "nodes_of"` 로 **호출 자리 넷**(defect.rs:322 · ledger.rs:212 · 시험 둘)을 세웠고, §8은 *"touch 가 그보다 더 말해 줄 것이 있는지가 이 자리의 물음이다"* 라 적어 답을 모른 채 남겼다. grep의 "호출 자리 4"와 touch의 "참조 엣지 기준 호출자 3"은 잣대와 값이 다른 별개의 사실이다.
- §5.8 표에 있나: 없다 — `nodes_of` 행은 `결박 0 · 지켜보는 것 0 · 산출이 52 줄` 만 적었고 호출자 수는 없다.
- 판정 근거: 사전 등록이 미리 안 것은 "grep 호출 자리 4"뿐이고 "참조 엣지 호출자 3"은 touch가 처음 낸 값이다. 두 표에 모두 없으므로 유효.

### ⓑ — 유효
- 인용한 줄: `■ 내가 모르는 것` / `9 건` 이하 `SymbolNode` · `Discriminator` 를 포함한 목록 (`:36-46`)
- 담은 사실: `nodes_of` 의 미해소 참조가 9건이고 그중 `SymbolNode`·`Discriminator` 가 있다.
- 사전 등록에 있나: 없다 — 사전 등록은 `SymbolNode`·`Discriminator` 를 필드·판별자 정의를 설명하며 언급하지만(§2.1), "이 함수의 미해소 참조 9건" 같은 사실은 어디에도 없다. `no_symbol_at_crate_root`·`outside_repo` 같은 해소 실패 사유 분류도 사전 등록에 없다.
- §5.8 표에 있나: 없다 — 표는 결박·지켜보는 것·산출 줄 수만 다룬다.
- 판정 근거: 같은 낱말(`SymbolNode`, `Discriminator`)의 등장만으로는 같은 사실이 아니다. "미해소 참조 9건"이라는 사실 자체가 두 문서 어디에도 없으므로 유효.

### ⓒ — 유효
- 인용한 줄: `  2층       심볼 3315 색인됨`
- 담은 사실: 이 실행에서 2층에 색인된 심볼 수가 3315다.
- 사전 등록에 있나: 부분적으로만 — 사전 등록 §4가 "`pal touch` 의 근거 줄이 `2층 심볼 3308 색인됨` 이라 적었다"를 인용하지만, 이는 **3308**이라는 다른 수이고 §4 스스로 그 수를 "다른 시점·다른 코퍼스의 것"이라 못박는다. "2층 심볼 수를 안다"는 범주는 있으나 "3315"라는 값 자체는 없다.
- §5.8 표에 있나: 없다.
- 판정 근거: 3315와 3308은 같은 사실이 아니라 다른 수치이며, 사전 등록도 3308을 "다른 시점의 것"이라 스스로 무효화했다. 값 부분이 없으므로 유효.

### ⓓ — 무효
- 인용한 줄: `  fun · crates/pal-cli/src/ledger.rs:301 · identity ordinal · body 7058bb9c2fcf`
- 담은 사실: `nodes_of` 의 선언 자리가 `crates/pal-cli/src/ledger.rs:301` 이다.
- 사전 등록에 있나: 있다(인용) — §1 「심볼 선정」: *"**`nodes_of`** 하나. `crates/pal-cli/src/ledger.rs:301` 의 함수이고 `#79` 가 이름으로 지목한 자리다"*.
- §5.8 표에 있나: 없다(다만 이미 ①에서 걸린다).
- 판정 근거: 좌표(`ledger.rs:301`)는 사전 등록이 touch를 돌리기 전부터 심볼 선정 근거로 이미 적어 둔 사실이다. 하나라도 걸리면 무효이므로 무효.

### ⓔ — 무효
- 인용한 줄: `■ 이 좌표에 걸린 것 (0)` / `  아직 없습니다.`
- 담은 사실: `nodes_of` 에 걸린 결박이 0건이다.
- 사전 등록에 있나: 있다(인용) — §2.3이 §5.8 표를 그대로 옮겨 적었고 그 표의 `nodes_of` 행: *"결박 **0** · 지켜보는 것 **0** · 산출이 52 줄"*.
- §5.8 표에 있나: 있다(인용) — 같은 문구.
- 판정 근거: 사전 등록 자신이 "★ 여기 적힌 사실은 `C2` 의 귀속에서 빠진다"고 못박은 바로 그 표의 항목이다. 두 자리 모두에 명시돼 있으므로 무효.

## 합계
유효 3 · 무효 2

---

## ⚠ 부르는 쪽이 적는 것 — **되불러 준 값 하나가 안 맞았다**

③ 의 줄 수를 판정자가 **337** 이라 했고 파일은 **345** 다. 어느 쪽이 틀렸는지 가리려고
**같은 인스턴스에 셋을 더 물었다**(판정은 다시 하지 말라고 못 박았다):

| 물은 것 | 판정자 | 기계 | 같나 |
|---|---|---|---|
| `@@` 줄 수와 내용 | **13** 줄 · 범위가 `-319,7 +319,11` … `-821,8 +1040,8` 로 순서까지 | **13** 줄 · 같은 범위 | **같다** ⚠ 셋째 `@@` 의 꼬리 문맥만 다르게 옮겨 적었다(`pub struct LedgerReport {` ↔ 실제 `use serde::Serialize;`) |
| `+` 줄 수 | 233 | **232** | 1 차이 |
| `-` 줄 수 | 9 | **9** | 같다 |
| `diff --git` 줄 | 2 줄 · 내용 일치 | 2 줄 | **같다** |

★★ **그러므로 이 대조로는 「바이트 동일」을 못 세운다.** 판정자의 계수와 인용이 **근사**임이
드러났다 — 줄 수를 9 만큼, `+` 를 1 만큼 틀렸고 `@@` 꼬리 하나를 다르게 옮겼다.
**되불러 주기는 「대체로 같은 것을 받았다」까지만 말한다.**

**무엇이 기계로 선 것과 안 선 것을 가른다:**
- **섰다** — `A1-c`: `effect/judge-prompt.md` 의 ① 절 ↔ 봉인 blob `9b0f3a5…` 이
  `git cat-file blob | diff -` 로 **바이트 같음**(12,229 바이트). 그것이 이 조건의 오라클이다.
- **안 섰다** — 판정자에게 **전달된** 바이트가 그 파일과 같은지. 전달은 사람이 옮긴 것이고
  그 자리에 기계 대조가 없다. **이 한계를 발견으로 싣는다** ⟨`MS-10`⟩.
