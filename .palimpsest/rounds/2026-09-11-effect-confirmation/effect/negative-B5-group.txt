# 음성 대조 — ㉠  의 셈법을 그룹 단위로 바꾼 뒤
#
# 잰 때: 2026-09-12T02:12:10Z · HEAD 7abb859 · 코드는 워킹트리
# 끈 자리: crates/pal-cli/src/ledger.rs · IdentityTally::헤아린다 의 `if 그룹_크기 > 1` 을
#          `if false && 그룹_크기 > 1` 로 바꿔 **가르는 자리를 껐다**.
# 물음:   가르는 자리를 끄면 시험이 정말 빨개지는가 — 항등식이 아닌가.
# 답:     **셋이 빨개진다.** 아래가 그 산출 전문이다.
#
--- 전 출력 (여기서부터 바이트 그대로 · 위는 머리) ---
test ledger::tests::최상위_선언은_체인이_빈다 ... ok
test ledger::tests::재선언은_한_좌표로_뭉개지지_않고_후보로_남는다 ... ok
test ledger::tests::같은_컨테이너의_오버로드는_여전히_순서로_갈린다 ... ok
test ledger::tests::불변식_e_컨테이너가_같은_이름을_가른다 ... ok
test ledger::tests::컨테이너가_다르면_ordinal_이_다시_0_이다 ... ok
test ledger::tests::불변식_f_컨테이너_순서를_바꿔도_정체성이_그대로다 ... ok
test ledger::tests::불변식_g_파일을_옮기면_정체성만_바뀐다 ... ok
test ledger::tests::체인이_없으면_순서가_정체성을_흔든다 ... ok
test ledger::tests::포함_관계에_순환이_있어도_멈춘다 ... ok
test ledger::tests::순서로_가린_것과_등급이_낮은_것이_갈린다 ... FAILED
test ledger::tests::check_ledger_pair_는_순서에_취약하지_않다 ... FAILED
test ledger::tests::합이_분모와_같다 ... FAILED
thread 'ledger::tests::순서로_가린_것과_등급이_낮은_것이_갈린다' (29595827) panicked at crates/pal-cli/src/ledger.rs:911:9:
assertion `left == right` failed: `dup` 둘 다 — 첫 선언도 순서가 바뀌면 정체성이 맞바뀐다
  left: 0
 right: 2
thread 'ledger::tests::check_ledger_pair_는_순서에_취약하지_않다' (29595822) panicked at crates/pal-cli/src/ledger.rs:972:9:
assertion `left == right` failed: 같은 이름 둘이면 첫 선언까지 전원이 ①이다
  left: 0
 right: 2
thread 'ledger::tests::합이_분모와_같다' (29595833) panicked at crates/pal-cli/src/ledger.rs:933:9:
assertion `left == right` failed
  left: 0
 right: 2
test result: FAILED. 16 passed; 3 failed; 0 ignored; 0 measured; 81 filtered out; finished in 0.01s
