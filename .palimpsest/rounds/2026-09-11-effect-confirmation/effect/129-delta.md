# 사전 등록 ↔ 실제 변경 — ㉢ `#129` ⟨`C1`⟩

> 봉인 `22cf488`(`plan/129-pre.md`) → touch `9d94f34`(`touch/129.txt`) →
> readnote `138c782`(`effect/129-readnote.md`) → **변경 `1abc0e9`**
> ⚠ 줄 번호는 변경 커밋 `1abc0e9` 에서 실측했다.
> ⚠ 방향 판정은 `C4` ⟨정반합⟩ 이 진다.

## 1. 계획대로 간 것 — 좌표 아홉

| 사전 등록 §4 | 실제 | 같나 |
|---|---|---|
| ⑴ `enum 민팅 { 한다, 안한다 }` | `narrative.rs:82` | **같다** |
| ⑵ `ingest(…, 민팅)` | `narrative.rs:176-182` | **같다** |
| ⑶ 안한다면 `keep_entity` 를 안 부르고 `개체_없음 += 1` · `continue` | `narrative.rs:237-245` | **같다** |
| ⑷ `Ingested.개체_없음` | `narrative.rs:106` | **같다**(차이 ㉮) |
| ⑸ 질의가 `민팅::안한다` 를 넘기고 수를 올린다 | `query.rs:149-160`(인입) · `:188`(올림) | **같다** |
| ⑹ `QueryCtx.narrative_unminted` · `QueryResult::Narrative::unminted` | `pal-query/src/lib.rs:335` · `:227` · `:499` | **같다** |
| ⑺ `print_narrative` 가 적는다 | `query.rs:493-506` | **같다** |
| ⑻ `pal narrative` 는 `민팅::한다` | `narrative.rs:145` | **같다** |
| ⑼ 시험 | `crates/pal-cli/tests/narrative_read_only.rs` | **같다**(차이 ㉯) |

**안 건드린 것도 계획대로다** — `store.rs` 의 `write()` 거부 규칙 · `pal narrative` 의 행동 ·
`EntityId` 생성 방식 · `ElisionReason` enum.

## 2. 차이 — 셋이고 **어느 것도 touch 가 원인이 아니다**

| # | 차이 | 원인 | 결과 좌표 |
|---|---|---|---|
| ㉮ | `Ingested::비어_있다()` 를 새로 세웠다 | **컴파일러** — 질의 경로가 인입을 안 부를 때 `Ingested` 하나가 필요했다. `Vec::new()` 를 흩으면 *"안 물었다"* 와 *"물었는데 0"* 이 같은 글자가 된다 | `narrative.rs:110-124` |
| ㉯ | `touch.rs` 의 `QueryCtx` 에 `narrative_unminted: 0` 을 더했다 | **컴파일러**(`E0063`) — `pal touch` 는 인입을 안 부른다 | `touch.rs:153` |
| ㉰ | 세는 낱말이 `센다` → `헤아린다` | **저장소 검사** — 「어색한 표현 부재」가 `narrative.rs:85` 를 잡았다. ㉠ 에 이어 두 번째다 | `narrative.rs:85` |

★★ **이 자리의 차이는 0 이다** — 위 셋은 전부 **도구가 시킨 것**이고 touch 산출에 귀속되는
것이 **하나도 없다.** `effect/129-readnote.md` §2 가 예고한 그대로다.

## 3. 앵커 ⟨`A5-b`·`A5-d`·`A5-e`⟩ — **대조불가**

- 답이 **후보 목록**이라 `fun · … body <hash>` 줄이 **없다** ⟨`A5-e` ⓐ⟩.
- touch 를 건 `write`(`crates/pal-intent/src/store.rs:163`)는 **변경 대상이 아니다**
  ⟨`A5-d` 뒤쪽 갈래⟩ — 그 거부 규칙이 옳고, 고친 것은 **부르는 쪽**이다.
- **둘 다 사전 등록 §6 에 미리 적고 왔다.** 그러므로 `A5-b` 는 이 자리에서 **「대조불가」**다.
  판정은 게이트가 진다.

## 4. RED ↔ GREEN ⟨`B1-a`·`B1-d`⟩

| | 산출 | 값 |
|---|---|---|
| RED | `observations/red-129.txt` | `rc=1` · *"의도 저장소 트랜잭션이 실패했다: 읽기로 연 의도 저장소에 쓰려 했다"* · `intent.redb` **있음**(1,351,680 바이트) |
| GREEN | `effect/green-129.txt` | **`rc=0`** · 표준오류 **0 바이트** · 표준출력 466,960 바이트 · `미결박 1705` · `이름이 아직 없어 뺀 조각 4186` |

⚠ **잴 때 한 번 속을 뻔했다** — zsh 의 MULTIOS 가 `2>&1 1>/dev/null` 을 뒤집어 표준출력을
표준오류처럼 보여 줬다. **파일 둘로 갈라 받아** 다시 쟀다. 규약이 *"`rc` 는 판정이 아니다"* ·
*"전 출력을 파일로 받고 거기서 잰다"* 로 적은 자리의 사촌이다.

## 5. 음성 대조 — **둘을 갈라 보는 것이 이 짝의 값이다**

| 끈 것 | `narrative_unbound_는_읽기로도_돈다` | `읽기_경로는_의도_저장소를_안_불린다` |
|---|---|---|
| ① 민팅 분기(`안한다` 가지) | **FAILED** | **FAILED** |
| ② 질의를 **쓰기로 연다**(버린 갈래 ⓐ) | **ok** ← 통과한다 | **FAILED** — *"질의가 의도 저장소를 만들었다"* |
| 되돌린 뒤 | ok | ok |

★★ **②가 이 짝의 존재 이유다.** 첫 시험만 두면 **한 줄짜리 우회로**(읽기 → 쓰기)가 초록이
되고, 그 우회로는 `query.rs:109` 이 적어 둔 계약을 깨면서 **읽기가 `intent.redb` 를
불리게** 만든다. 사전 등록 §5 에 그 까닭을 미리 적고 왔다.

## 6. 이 자리에서 나온 발견

**없다.** 새 발견 0 · 귀속 후보 1(그리고 그 후보에 붙은 차이가 0).
⚠ **0 을 0 으로 적는 것이 이 자리의 산출이다** — `intent.md ## 재는 자리 셋` 이 ㉢ 을
**음성 방향의 표본**으로 고른 값이 여기서 실현됐다.
