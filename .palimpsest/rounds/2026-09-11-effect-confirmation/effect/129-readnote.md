# 「봤다」 — ㉢ `#129` ⟨`A7`⟩

> 산출: `touch/129.txt`(HEAD `22cf488`) · **후보 목록이라 심볼 ID 가 없다** ⟨`A2-c`⟩
> **touch 커밋 뒤 · 코드 변경 전**에 커밋된다.

## 1. 읽은 줄과 알게 된 것

| 좌표 | 읽은 것 | 알게 된 것 |
|---|---|---|
| `:19` | `` `write` 의 후보가 3건입니다. 하나를 고르지 않습니다. `` | 사전 등록 §1 의 예상대로 **후보 목록**이다. 그러므로 `A2` 의 머리 넷 중 **셋째(심볼 ID)가 원리상 없고** ⟨`A2-c`⟩, `A5-b` 의 앵커도 **원리상 못 선다** ⟨`A5-e` ⓐ⟩ |
| `:21` | `fun write crates/pal-cli/src/install/manifest.rs:373` | ★ **새로 안 것.** 사전 등록은 `store.rs:163` 하나만 적었다 |
| `:22` | `fun write crates/pal-intent/src/store.rs:163` | 내가 이미 읽은 자리다(`#129` 의 뿌리) |
| `:23` | `fun write crates/pal-store/src/projection.rs:251` | ★ **새로 안 것.** 2층의 쓰기다 |
| `:27` | `대장 parsed 141 · … / 1273 파일` · `2층 심볼 3325` | ㉠ 의 tally 가 낸 분모와 같은 수(3325)다 — 그 뒤로 `.rs` 를 안 고쳤으니 맞다 |
| `:30` | `워킹트리 일치` | `A5-c` 가 잡아 둔 자리 — 증거로 쓰지 않는다 |
| `:36` | `크기 약 538 토큰 이상 (잰 것: 2152 바이트)` | 본문은 **1201 바이트**다. `MS-06` 의 **셋째 관측**이고 방향이 같다 |

## 2. 계획이 바뀌나 ⟨`C1`·`C4` 의 입력⟩

★★ **안 바뀐다. 이 자리의 차이는 0 이다.**

- `:21`·`:23` 이 준 두 자리(`manifest.rs:373` · `projection.rs:251`)는 **내 계획이 안
  건드리는 파일**이다. §4 의 좌표 아홉 중 어느 것도 그 둘과 안 겹친다.
- `:19` 가 준 「후보 목록」은 사전 등록 §1·§6·§7 이 **미리 적어 둔 것**이다. 예상이 맞은
  것이지 새로 알게 된 것이 아니다.
- 나머지 줄은 전부 근거(스냅숏·대장·워킹트리)이고 이미 아는 값이거나 `MS-06`·`A5-c` 로
  이미 처분된 자리다.

**이것이 이 회차가 ㉢ 을 고른 까닭이다** — `intent.md ## 재는 자리 셋` 이 ㉢ 을
*"「touch 가 쓸모없었다」가 나올 수 있는 **음성 방향의 표본**"* 으로 적었고, 실제로 그렇게
나왔다. **0 을 0 으로 적는다** ⟨`C5`⟩.

⚠ **0 이라는 것과 「touch 가 틀렸다」는 다르다.** ㉢ 에서 touch 는 **틀린 말을 하지 않았다** —
후보 셋은 실재하고 좌표도 맞다. 준 것이 **내 물음에 안 닿았을** 뿐이다. ㉠ 의 `MS-08`
(호출자 하나를 빠뜨림)과 **다른 종류**이고, 게이트가 그 둘을 안 섞어야 한다.

## 3. 귀속 후보 ⟨`C2`·`C2-b` 로 넘긴다⟩

| # | 좌표 | 담은 사실 | 사전 등록·§5.8 에 있나 |
|---|---|---|---|
| ⓐ | `touch/129.txt:21`·`:23` | `write` 라는 이름의 함수가 **셋**이고 그중 둘이 `install/manifest.rs:373`·`pal-store/src/projection.rs:251` 이다 | 둘 다 **없다**. ⚠ **다만 이 사실이 변경을 하나도 안 바꿨다** — `C2` 는 *"차이 중 touch 에 귀속되는 것"* 을 묻는데 **이 자리에는 차이가 0 이라 귀속될 차이가 없다.** 후보로 제출하되 **그 사실을 함께 적는다** |

★ **`C2` 의 하한 1 건은 ㉠·㉡ 이 진다** — `intent.md` 가 *"하한 1 을 실제로 지는 것은
㉠·㉡ 둘이다"* 라고 착수 시점에 이미 적었다(`## 재는 자리 셋`). 여기서 억지로 귀속을
만들지 않는다.
