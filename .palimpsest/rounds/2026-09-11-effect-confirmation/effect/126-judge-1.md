# 귀속 판정 원문 — ㉡ `#126` · 인스턴스 1 ⟨`C3-b`⟩

> 판정자: 새 에이전트(`general-purpose` · sonnet). 프롬프트는 `effect/judge-prompt.md`
> 의 **판 1** 이고 넷을 인라인으로 실었다. 저장소를 읽지 말라고 못 박았고 도구 사용 0 회다
> (`tool_uses: 0`).
> ⚠ **요약이 아니라 반환문 전문이다.** 전달 과정에서 `&lt;`·`&gt;`·`&amp;` 로 이스케이프된
> 세 글자만 원문 글자(`<`·`>`·`&`)로 되돌렸다 — 그 밖에는 한 글자도 안 고쳤다.

---

## 받은 것

| 절 | 첫 줄 | 마지막 줄 | 줄 수 |
|---|---|---|---|
| ① 사전 등록 | `# 사전 등록 — ㉡ `#126`` | `  **차이 0 이 나오면 0 으로 적는다** ⟨`C5`⟩.` | 120 |
| ② touch 산출 | `# `pal touch` 산출 — ㉡ `#126`` | `  능력      ledger.snapshot · symbol.resolve · symbol.contains · symbol.callers · symbol.reaches · graph.dump · binding.status · narrative.unbound · binding.touch · plan.deviation · symbol.references · 미구축 F13 · F15` | 59 |
| ③ diff | `diff --git a/xtask/src/main.rs b/xtask/src/main.rs` | `` /// 축 아홉(`출처`·`모집단`·`유효성`·`해악도`·`처분`·`승격됨`·`조건변경`·`사전처분`·`상태`)의 `` | 144 |
| ④ §5.8 표 | `### 5.8 ⚠ 메인이 이미 오염됐다 — 사전부검이 `pal touch` 를 먼저 돌렸다` | `사전부검이 먼저 말했다.` | 17 |

---

### ⓐ — 유효
- 인용한 줄: `  호출자 1 · 피호출자 12`
- 담은 사실: `check_ledger_pair` 의 호출자 수가 1이다.
- 사전 등록에 있나: 없다
- §5.8 표에 있나: 없다 — 표에 「호출자 0」이 적힌 것은 `identity_ceiling` 행이지 `check_ledger_pair` 행이 아니다. `check_ledger_pair` 행은 「결박 0 · 산출 46줄 · 결박 8건 · 한 칸 옆」만 말한다.
- 판정 근거: `check_ledger_pair` 의 caller 수치는 사전 등록 §2.1(줄 번호·rev/find 로직)에도, §5.8 표의 해당 행에도 없다. touch 가 처음 준 새 사실이므로 유효하다.

### ⓑ — 유효
- 인용한 줄: `  fun · xtask/src/main.rs:5934 · identity ordinal · body 1f1846428f55`
- 담은 사실: 이 심볼의 정체성 표시가 `ordinal` 이다.
- 사전 등록에 있나: 없다
- §5.8 표에 있나: 없다
- 판정 근거: 사전 등록 §6 은 `body <hash>` 가 변경 전후로 달라질 것을 예상할 뿐 정체성 종류(`ordinal`)는 어디서도 언급하지 않는다. §5.8 표도 결박·산출 줄 수·한 칸 옆 여부만 적고 identity 종류는 안 적는다.

### ⓒ — 유효
- 인용한 줄: `  fun · xtask/src/main.rs:5934 · identity ordinal · body 1f1846428f55`
- 담은 사실: 본문 앵커 값이 `body 1f1846428f55` 다.
- 사전 등록에 있나: 없다
- §5.8 표에 있나: 없다
- 판정 근거: 사전 등록 §6 은 해시가 "달라질 것"이라는 예상만 적었지 구체적 해시 값을 몰랐다(touch 를 돌리기 전이므로 알 수 없다). §5.8 표에도 해시 값은 없다.

### ⓓ — 무효
- 인용한 줄: `  fun · xtask/src/main.rs:5934 · identity ordinal · body 1f1846428f55`
- 담은 사실: `check_ledger_pair` 의 선언 자리가 `xtask/src/main.rs:5934` 다.
- 사전 등록에 있나: 있다(인용) — "`check_ledger_pair`** 하나. `xtask/src/main.rs:5934` 의 함수다." (§1) 및 "`:5934` `fn check_ledger_pair(root: &Path) -> Result<String>` — 검사 본체의 머리" (§2.1)
- §5.8 표에 있나: 없다(표는 좌표를 적지 않는다)
- 판정 근거: 좌표 `:5934` 는 사전 등록 §1·§2.1 이 이미 명시적으로 적어 두었다. touch 가 같은 줄을 다시 확인해 줬을 뿐 새 사실이 아니다.

### ⓔ — 무효
- 인용한 줄: `■ 이 좌표에 걸린 것 (0)` / `  아직 없습니다.`
- 담은 사실: 이 심볼에 걸린 결박이 0건이다.
- 사전 등록에 있나: 있다(인용) — "touch 산출은 **46 줄** 안팎이고 결박 **0** 이다(§5.8 에서 이미 안다)." (§7) 및 §2.3 이 옮겨 적은 §5.8 표 내용
- §5.8 표에 있나: 있다(인용) — "`check_ledger_pair` | 결박 **0** · 산출이 46 줄 · 같은 파일 … 결박 **8** 건이 있고 그중 하나가 **한 칸 옆**이다"
- 판정 근거: 결박 0 이라는 사실은 사전부검 라운드 1 이 §5.8 표로 이미 메인에 알렸고, 사전 등록 §7 도 이를 그대로 예상값으로 적어 두었다. 두 자리 모두에 있으므로 이 귀속은 무효다.

## 합계
유효 3 · 무효 2
