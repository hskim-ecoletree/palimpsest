# 사전 등록 ↔ 실제 변경 — ㉡ `#126` ⟨`C1`⟩

> 봉인 `a74737b`(`plan/126-pre.md`) → touch `14f0f92`(`touch/126.txt`) →
> readnote `9be7699`(`effect/126-readnote.md`) → **변경 `398d233`**
> ⚠ **`9be7699` 는 이 이력에 없다** ⟨2026-09-12 정정 · 독립 리뷰 R2⟩ — 버려진 커밋이고
> `git merge-base --is-ancestor 9be7699 HEAD` 가 `rc=1` 이다. 파일 이력의 정본은 **`3637ea9`** 다.
> **위 줄은 안 고친다** — 그때 그렇게 적혔다는 것이 기록이고, 이 줄이 정정이다.
> 차이마다 **좌표 두 끝**을 붙인다 — `touch/126.txt:N` 이 원인이고 `파일:줄` 이 결과다.
> ⚠ **줄 번호는 변경 커밋 `398d233` 에서 잰 것이다** — 뒤에 같은 파일을 또 고치면 밀린다.
> ⚠ **방향 판정(나아졌다/나빠졌다/모른다)과 「touch 가 말한 것이 참이었나」는 `C4` 가
> 지고 그것은 ⟨정반합⟩이다.** 이 파일은 차이와 근거만 싣고 스스로 채점하지 않는다.

## 1. 계획대로 간 것 — 좌표 다섯

| 사전 등록 §4 | 실제 | 같나 |
|---|---|---|
| ⑴ `최근_끝난` 선택을 함수 호출로 | `xtask/src/main.rs:6345-6350` — `끝난` 을 만들고 `최근에_끝난(&끝난, …)` 을 부른다 | **같다**(아래 차이 ㉮ 하나) |
| ⑵ `최근에_끝난(회차들, 시각)` 을 `check_ledger_pair` **뒤**에 | `xtask/src/main.rs:6407-6427` | **같다** |
| ⑶ `종료_커밋_시각(root, 회차)` | `xtask/src/main.rs:6429-6447` | **같다**(차이 ㉯) |
| ⑷ 판정문에 무엇으로 골랐는지 | `:6354-6359`(근거 문자열) · `:6361`(통과) · `:6363-6368`(하한 미충족) | **같다** |
| ⑸ 시험 셋 | `xtask/src/main.rs:6449-6491` · 이름 셋 다 사전 등록 §5 의 것 | **같다** |

## 2. 차이 — 넷이고 원인이 둘로 갈린다

| # | 차이 | 원인 좌표 | 결과 좌표 |
|---|---|---|---|
| ㉮ | 사전 등록은 `최근에_끝난(&회차들, …)` 이라 적었는데 실제는 **`&끝난`**(`report.md` 로 먼저 거른 목록)을 넘긴다 | **touch 아님** — 사전 등록 §4 ⑵ 가 이미 *"호출자가 `report.md` 존재로 걸러 넘긴다"* 라고 적었다. ⑴ 의 표기가 그것과 어긋났던 것이고 실제는 ⑵ 를 따랐다 | `xtask/src/main.rs:6345-6350` |
| ㉯ | `%ct` 를 고른 근거(`%at` 은 rebase 가 옛 값을 들고 다닌다)를 코드 주석에 적었다 — 사전 등록엔 `%ct` 만 있고 까닭이 없었다 | **touch 아님** — 함수를 쓰다 나온 것 | `xtask/src/main.rs:6432-6433` |
| ㉰ | **새 함수를 `check_ledger_pair` 뒤에 두는 선택의 근거가 바뀌었다** — 「보기 좋다」 → 「이 심볼의 정체성이 선언 순서에 안 걸린다는 것을 확인했다」 | **`touch/126.txt:17`** (`identity ordinal`) → 따라가서 `crates/pal-core/src/coord.rs:246-266` 과 `crates/pal-cli/src/ledger.rs:334` | `xtask/src/main.rs:6407`(둔 자리 자체) |
| ㉱ | **호출자를 찾는 걸음이 없어졌다** — 등록 표를 안 건드리기로 확정했다 | **`touch/126.txt:24`** (`호출자 1 · 피호출자 12`) | `xtask/src/main.rs:643`(**안 건드린** 자리) |

★ **㉰㉱ 가 이 자리의 산출이다.** 넷 중 둘이 touch 산출을 원인으로 갖는다.
⚠ **그리고 계획 자체는 안 바뀌었다** — 좌표 다섯과 시험 셋이 그대로 섰다. 바뀐 것은
**근거와 생략된 걸음**이다. 그 구별을 여기 적어 둔다 ⟨`C4` 가 판정할 입력⟩.

## 3. 앵커 ⟨`A5-b`·`A5-d`⟩

| 시점 | `body` | 출처 |
|---|---|---|
| 봉인 `a74737b` | **`1f1846428f55`** | `touch/126.txt:17` |
| 변경 `398d233` | **`e18b2faabec2`** | 변경 뒤 같은 명령을 다시 돌려 잰 값 |

**움직였다.** touch 를 건 심볼이 변경 대상이므로 `A5-d` 의 앞쪽 갈래가 걸리고, 앵커가
서 있다. 심볼 ID 는 **`#076728deab4f` 로 안 바뀌었다**(정체성은 그대로 · 트리 참조만
`a74737b` → `398d233`).

## 4. 음성 대조 — 끄면 빨개진다

`최근에_끝난` 의 본문을 `끝난.iter().max()`(사전순)로 되돌려 `cargo test -p xtask 최근_끝난`:

    test 시각이_같으면_사전순_최대를_고른다 ... ok
    test 커밋_시각이_없는_회차는_가장_최근으로_본다 ... FAILED
    test 최근에_끝난_것은_사전순이_아니라_커밋_시각으로_고른다 ... FAILED
    test result: FAILED. 1 passed; 2 failed

셋째가 초록인 것은 **정상**이다 — 그 시험은 동률에서 사전순을 요구하므로 끈 판에서도 참이다.
되돌린 뒤 셋 다 초록.

## 5. 검사 산출 대조 ⟨`## 순서` 가 요구한 둘⟩

`observations/ledger-pair-before.txt` ↔ `-after.txt`:

    before: 최근 끝난 회차 `2026-09-08-cross-file-references` 가 검사에 들었다
    after : 최근 끝난 회차 `2026-09-08-cross-file-references` 가 검사에 들었다 (종료 커밋 시각 1789040144)

**뽑힌 회차가 같다** — `PM2-13` 이 예고한 자리다. 지금 저장소에서 사전순 최대와 커밋 시각
최대가 같기 때문이고, 그래서 차이는 더한 근거 문구뿐이다.

★ **그런데 같은 날짜 짝에서는 실제로 갈린다.** 끝난 회차들의 종료 커밋 시각을 재면:

    1788662558  2026-09-06-user-surface-vocabulary
    1788732455  2026-09-06-terrain-and-completion-scene

커밋 시각 최대는 `terrain…` 인데 **사전순 최대는 `user…`** 다(`t` < `u`). 그 둘이 마지막이던
시점에 이 검사는 **틀린 회차를 집고 있었다.** 합성 입력이 아니라 이 저장소의 이력이다.
⚠ 두 회차가 다 검사 대상이라 그때도 판정은 안 뒤집혔다 — `#126` 본문이 적은 그대로다.

## 6. 이 자리에서 나온 발견 — 원장으로 간다

`touch/126.txt:58` 의 `크기 약 1027 토큰 **이상** (잰 것: 4108 바이트)` 가 본문 **3346
바이트**와 다르고, `crates/pal-cli/src/evidence.rs:53-55` 의 주석은 *"이 화면처럼 들여쓴
산출은 그보다 크다"* 라 적는다 — **실측은 반대다.** 이 회차는 `pal` 의 표시를 안 고치므로
(`A5-c` 와 같은 자리) **원장에 싣고 `## 범위 밖` 에 한 줄을 남긴다.**
