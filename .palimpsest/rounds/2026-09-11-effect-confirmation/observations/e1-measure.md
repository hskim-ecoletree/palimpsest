# `E1` 실측 — 등록 조건 서른넷의 관측 기록

> 잰 것: 하위 에이전트 · 2026-09-12 · HEAD `0e1a876`
> **이 파일은 판정하지 않는다** — 관측 사실과 제안 판정만 싣는다. 판정은 메인이 한다.
> 재지 않은 것: `A1-a` `A3-a` `A5-a` `A7-a` `C2-a`(메인 담당) ·
> `A5-c` `B1` `C2` `C2-b` `C4` `D2` `D4` `E1` `E3` `E4` `E5`(정반합·승격·종료 절차가 짐)

---

## 0. 좌표 재확인 — `git log` 로 다시 쟀다

| 자리 | 봉인 | touch | readnote | 첫 변경 |
|---|---|---|---|---|
| ㉡ `#126` | `a74737b` 22:24:00 | `14f0f92` 22:27:32 | `3637ea9` 22:30:44 | `398d233` 22:46:43 |
| ㉠ `#79` | `77dc977` 23:01:34 | `88dbe13` 23:02:57 | `f60df1d` 23:04:04 | `9f993cc` 23:20:53 |
| ㉢ `#129` | `22cf488` 23:32:25 | `9d94f34` 23:33:09 | `138c782` 23:34:06 | `1abc0e9` 23:49:53 |

⚠ **`effect/126-delta.md:3` 이 readnote 를 `9be7699` 로 적는다.** 그 객체는 실재하지만
`git merge-base --is-ancestor 9be7699 HEAD` 가 **1**(조상이 아님)이다 — 같은 메시지·같은
분 단위 시각의 **버려진 커밋**이고, 파일 이력이 내는 정본은 `3637ea9` 다.
조건 문면이 이 SHA 를 안 쓰므로 어느 판정에도 안 들어간다. **기록으로만 남긴다.**

⚠ `touch/79.txt` · `touch/129.txt` 는 봉인 뒤 두 번 더 커밋됐다(`7abb859` · `30488ae`).
`git show` 로 확인한 결과 **둘 다 머리/꼬리의 덧붙임뿐이고 `--- 전 출력 ---` 뒤 본문
바이트는 한 줄도 안 바뀌었다.** `touch/129.txt` 의 `138c782` 수정도 머리 한 줄
(백틱 → 낫표)이다.

---

## 1. A 절 — 절차

### `A1` · `A1-b` · `A1-c`

`git merge-base --is-ancestor <봉인> <touch>` 종료값과 `<봉인> != <touch>`:

    anc(a74737b,14f0f92)=0  differ=YES
    anc(77dc977,88dbe13)=0  differ=YES
    anc(22cf488,9d94f34)=0  differ=YES

`A1-b` — `git rev-parse <봉인커밋>:<plan/N-pre.md>` ↔ `touch/<이슈>.txt` 머리의 `사전등록-blob`:

| 자리 | rev-parse | 머리에 적힌 값 | 같나 |
|---|---|---|---|
| ㉡ | `e6f401a05f1bb1cee563c49f8becdaf988997dfc` | 같음 | **같다** |
| ㉠ | `9b0f3a5a87f60f02e43daf9e2fb49455a5c31466` | 같음 | **같다** |
| ㉢ | `00c807cd4ecda4ca54ef22a6ffdc0e5e85e54f24` | 같음 | **같다** |

세 파일의 **지금 워킹트리 blob 도 같은 해시**다 — 봉인 뒤 안 고쳤다.

`A1-c` — `judge-prompt.md` 의 `## ① 사전 등록 전문` 네 판을 잘라
`git cat-file blob <해시> | diff -` 로 댔다:

    판1-① 126-pre (54-173)    : IDENTICAL
    판2-① 79-pre  (484-637)   : IDENTICAL
    판3-① 126-pre (1156-1275) : IDENTICAL
    판4-① 129-pre (1573-1719) : IDENTICAL

**제안: `A1` 통과 · `A1-b` 통과 · `A1-c` 통과.**

### `A2` · `A2-b` · `A2-c`

`A2` 머리 넷:

| 자리 | 부른 명령 | HEAD SHA | 심볼 ID | 사전등록-blob |
|---|---|---|---|---|
| ㉡ | `./target/release/pal touch check_ledger_pair` | `a74737b0…` | `palimpsest@a74737b+worktree#076728deab4f` | 있다 |
| ㉠ | `./target/release/pal touch nodes_of` | `77dc9777…` | `palimpsest@77dc977+worktree#a9dd338ebdd7` | 있다 |
| ㉢ | `./target/release/pal touch write` | `22cf4889…` | **원리상 없다** ⟨`A2-c`⟩ | 있다 |

⚠ ㉢ 은 넷 중 셋뿐이다. `A2-c` 가 그 갈래를 미리 파 두었고 머리가 그 사실과 근거
(`Snapshot palimpsest@22cf488+worktree` 에 `#<hex>` 가 없다)를 적는다.
**「넷이 적혔다」를 글자 그대로 읽으면 ㉢ 이 안 채워지고, `A2-c` 와 함께 읽으면 채워진다.**

`A2-b` — `touch/<이슈>.txt` 의 봉인 당시 판과 `effect/rerun-<이슈>.txt` 에서
`--- 전 출력 ---` 뒤만 떼어 `diff`:

    126 (14f0f92 판) : IDENTICAL  46 줄  sha256 5c9ad8d7f150ccfc…  (머리의 주장과 같음)
    79  (88dbe13 판) : IDENTICAL  52 줄  sha256 1fdf18fb3c0730e0…  (같음)
    129 (138c782 판) : IDENTICAL  21 줄  sha256 0766ec2faa24ca44…  (같음)

**제안: `A2` 통과(㉢ 은 `A2-c` 경유) · `A2-b` 통과 · `A2-c` 통과.**

### `A3` · `A4` · `A6`

`A3` — 봉인된 blob 안에 좌표 표가 있다:
`plan/126-pre.md` §4 다섯 줄 · `plan/79-pre.md` §5 여섯 줄 · `plan/129-pre.md` §4 아홉 줄.
전부 `파일:줄` 과 「무엇을 쓰나」가 짝으로 붙어 있다.

`A4` — 셋 다 §2.1 읽은 것(경로와 줄 범위) · §2.2 돌린 것(`gh issue view` · `grep` · 실행) ·
§2.3 §5.8 표를 싣는다. 세 pre 의 §5.8 표를 `observations/red.md:235-240` 과 `diff` 한 결과
**셋 다 IDENTICAL**.

`A6` — 심볼 선정이 봉인 안에 있다(㉡ `check_ledger_pair` · ㉠ `nodes_of` · ㉢ `write`),
보존된 산출은 심볼마다 **하나**다. 뒤의 두 커밋은 머리·꼬리 덧붙임이라 산출을 안 늘린다.

**제안: `A3` 통과 · `A4` 통과 · `A6` 통과.**

### `A5` · `A5-b` · `A5-d` · `A5-e`

`A5` — 사상은 사전 등록의 `파일:줄` 목록이 진다. 그 목록의 파일을 touch 커밋 뒤 처음
건드린 커밋을 `git log <touch>..HEAD -- <파일들>` 로 뽑았다:

| 자리 | 사전 등록이 지목한 파일 | 첫 변경 커밋 | anc(touch,변경) | SHA 다름 |
|---|---|---|---|---|
| ㉡ | `xtask/src/main.rs` | `398d233` | 0 | YES |
| ㉠ | `crates/pal-cli/src/ledger.rs` · `defect.rs` | `9f993cc` | 0 | YES |
| ㉢ | `narrative.rs` · `query.rs` · `pal-query/src/lib.rs` | `1abc0e9` | 0 | YES |

★ **`A5-b` 를 직접 쟀다** — `--index`·`--cache-dir` 를 스크래치로 돌려 저장소 상태를
안 건드리고 봉인 커밋에서 같은 심볼을 다시 만졌다:

    $ pal touch nodes_of --at 77dc977
      fun · crates/pal-cli/src/ledger.rs:301 · identity ordinal · body 7058bb9c2fcf
    $ pal touch check_ledger_pair --at a74737b
      fun · xtask/src/main.rs:5934 · identity ordinal · body 1f1846428f55
    $ pal touch write --at 22cf488
      `write` 의 후보가 3건입니다. 하나를 고르지 않습니다.      ← `body` 줄 자체가 없다

`touch/79.txt:17` 이 적은 값이 `7058bb9c2fcf` 이고 `touch/126.txt:17` 이 적은 값이
`1f1846428f55` 다. **둘 다 봉인 커밋 시점의 본문에서 나온 값이다 — 기계로 확인됐다.**
㉢ 은 `A5-e` ⓐ 대로 앵커가 원리상 안 선다.

`A5-d` — 지금 HEAD(`0e1a876`)에서 다시 만졌다:

| 심볼 | 봉인 시점 | 지금 | 변경 대상인가 |
|---|---|---|---|
| `nodes_of` | `7058bb9c2fcf` | **`4ee8c2a643ae`** | 그렇다 — **움직였다** |
| `check_ledger_pair` | `1f1846428f55` | **`e18b2faabec2`** | 그렇다 — **움직였다** |
| `write` | (없음) | (없음) | **아니다** — `129-pre.md` §6 이 까닭을 적고 `129-delta.md:41` 이 「대조불가」로 판정했다 |

⚠ `effect/126-delta.md:39` 가 변경 뒤 값으로 적은 `e18b2faabec2` 가 **지금 값과 같다** —
그 파일의 그 함수는 그 뒤로 안 바뀌었다. `79-delta.md:63` 의 `305e595b853e` 는 지금
`4ee8c2a643ae` 로 또 움직였다(`ledger.rs` 가 그 뒤 세 번 더 고쳐졌다). 둘 다 「다르다」는
요구는 만족한다.

`A5-e` — 못 서는 자리 둘이 **봉인 안에** 미리 적혀 있다: ⓐ 후보 목록(`129-pre.md` §6 ·
`126-pre.md` §6 · `79-pre.md` §7 이 각각 예상으로) · ⓑ 공백·주석 불변(세 pre 의 같은 절).
ⓐ 는 위 실행이 재현했다. ⓑ 는 내가 새로 안 돌렸다 — 조건이 요구하는 것은 「미리 적는다」다.

**제안: `A5` 통과 · `A5-b` 통과 · `A5-d` 통과 · `A5-e` 통과.**

### `A7`

`git merge-base --is-ancestor` 종료값 여섯, 전부 0 이고 SHA 가 다 다르다:

    touch→readnote : (14f0f92,3637ea9)=0  (88dbe13,f60df1d)=0  (9d94f34,138c782)=0
    readnote→변경  : (3637ea9,398d233)=0  (f60df1d,9f993cc)=0  (138c782,1abc0e9)=0

세 readnote 다 「읽은 줄 ↔ 알게 된 것」 표를 싣고 좌표를 붙인다
(㉡ 10 행 · ㉠ 9 행 · ㉢ 7 행). ㉢ 은 `:19` 같은 짧은 꼴을 쓰고 머리가
*"산출: `touch/129.txt`"* 로 파일을 고정한다 — ㉡ 은 머리에 *"좌표는 전부
`touch/126.txt:N` 이다"* 라 명기했다.

**제안: `A7` 통과.**

---

## 2. B 절 — 실제 작업

### `B1-a` · `B1-d`

`observations/red-129.txt` — `.palimpsest/intent.redb` 가 **1351680 바이트로 있는 상태**에서
`Error: 개체를 남기지 못했다 / 읽기로 연 의도 저장소에 쓰려 했다` · `rc=1`.
그 파일은 지금도 같은 크기·같은 날짜다.

`B1-d` — `effect/green-129.txt` 가 rc 0 · 표준오류 0 바이트를 적는다. **다시 쟀다**:

    $ ./target/release/pal query narrative.unbound
    rc=0 · stdout 466960 바이트 · stderr 0 바이트
    ■ narrative.unbound / 결박됨 37 · 후보 있음 647 · 미결박 1705
    .palimpsest/intent.redb md5 = 7c3d6e58a07618fd7db49c4c771b274e  (실행 전후 동일)

**제안: `B1-a` 통과 · `B1-d` 통과.**

### `B1-c` · `B1-e`

사전 등록이 짝지은 시험 여덟이 전부 코드에 있다:

| 시험 | 자리 |
|---|---|
| `최근에_끝난_것은_사전순이_아니라_커밋_시각으로_고른다` | `xtask/src/main.rs:6460` |
| `커밋_시각이_없는_회차는_가장_최근으로_본다` | `xtask/src/main.rs:6475` |
| `시각이_같으면_사전순_최대를_고른다` | `xtask/src/main.rs:6484` |
| `순서로_가린_것과_등급이_낮은_것이_갈린다` | `crates/pal-cli/src/ledger.rs:893` |
| `합이_분모와_같다` | `crates/pal-cli/src/ledger.rs:917` |
| `check_ledger_pair_는_순서에_취약하지_않다` | `crates/pal-cli/src/ledger.rs:955` |
| `narrative_unbound_는_읽기로도_돈다` | `crates/pal-cli/tests/narrative_read_only.rs:51` |
| `읽기_경로는_의도_저장소를_안_불린다` | `crates/pal-cli/tests/narrative_read_only.rs:73` |

짝(시험 ↔ RED)은 `126-pre.md` §5 · `79-pre.md` §6 · `129-pre.md` §5 의 표가 진다 —
**셋 다 봉인 blob 안**이다.

**제안: `B1-c` 통과 · `B1-e` 통과.**

### `B2`

    $ cargo xtask check
    …
    검사 28/28 통과
    B2_RC=0

**제안: `B2` 통과** (초록 · 28 ≥ 28).

### `B3`

전 출력을 파일로 받아 `^test result:` 줄의 `passed` 를 합산했다:

    $ cargo test --workspace   (전 출력 84295 바이트)
    ^test result: 줄 49 개 · Doc-tests 묶음 6 개
    passed 합계 = 1046
    `[1-9]* failed` 인 줄 = 0
    B3_RC=0

⚠ 문면은 *"48 줄"* 이라 적는데 지금은 **49 줄**이다 — 시험 바이너리가 하나 늘었다.
문면이 합격선으로 못 박은 것은 **합 1028 이상**이고 1046 이 그 위다.

**제안: `B3` 통과.**

### `B4`

    $ find . -name '*.rs' -not -path './target/*' -exec touch {} +
    $ cargo build --release --workspace
      Compiling 8 크레이트 전부 · Finished in 14.27s · warning 0 줄 · rc 0
    $ find . -name '*.rs' -not -path './target/*' -exec touch {} +
    $ cargo check --workspace --all-targets
      Checking 8 크레이트 전부 · Finished in 3m 08s · warning 0 줄 · rc 0

둘 다 mtime 을 새로 찍은 뒤라 **증분이 아니다**(각각 여덟 크레이트를 다시 컴파일했다).

**제안: `B4` 통과.**

### `B5` · `B5-a`

`pal ledger` 를 지금 돌린 산출:

    정체성    심볼 3334
      순서에 취약              20
      등급이 낮음            3308
      정확                    6
      불가                    0
              ← 검산 20 + 3308 + 6 + 0 = 3334 · 2층에 들어가는 심볼 3334

**「순서에 취약」과 「등급이 낮음」이 갈린 수가 명령으로 난다.** 손으로 센 수가 아니다.

`B5-a` — `plan/79-pre.md` §4(봉인 blob `9b0f3a5a…` 안)가
*"분모 = 그 실행에서 2층에 들어가는 심볼 전량"* 이라 적고 `#79` 본문의 「464 · 7,156」을
**안 쓴다**는 것까지 적는다.

⚠ **버킷 ① 의 술어는 봉인 뒤 바뀌었다** — `ordinal > 0` → 「그룹 크기 > 1」(소유자 결정 ·
`79-delta.md` ㉲). **분모의 정의는 안 바뀌었다.** `B5-a` 가 요구하는 것이 분모냐
버킷 술어까지냐는 문면 해석이다.

**제안: `B5` 통과 · `B5-a` 통과(버킷 술어 변경은 메인이 볼 자리).**

### `B6`

`plan/126-pre.md` §3(봉인 안) — **커밋 시각 갈래**를 골랐고 까닭을 적었다
(*"뽑힌 회차가 `검사안` 에 없으면 `problems` 에 들어가고 `bail!` 된다. 그 낱말은 판정에 쓰인다"*).
그래서 뒷문장이 걸리고, 셋이 다 섰다:

- **코드** — `xtask/src/main.rs:6422-6427` `최근에_끝난` 이
  `시각(회차).unwrap_or(i64::MAX)` 로 **커밋 시각 없음을 가장 최근으로** 본다.
- **판정문** — `:6358` `None => "커밋 시각 없음 — 가장 최근으로 본다"`,
  `:6361`·`:6368` 이 그 근거를 통과·미충족 두 메시지에 다 싣는다.
- **시험** — `:6475` `커밋_시각이_없는_회차는_가장_최근으로_본다`.

**제안: `B6` 통과.**

---

## 3. C 절 — 차이

### `C1` · `C5`

세 자리 다 `effect/<이슈>-delta.md` 가 섰다:

| 자리 | §1 계획대로 | §2 차이 | 0 인가 |
|---|---|---|---|
| ㉡ | 좌표 다섯 | **넷** (㉮㉯㉰㉱) | 아니다 |
| ㉠ | 좌표 여섯 | **여섯** (㉮~㉳) | 아니다 |
| ㉢ | 좌표 아홉 | **셋** (㉮㉯㉰) · 귀속 차이 **0** | **0 을 0 으로 적었다** |

차이마다 **원인 칸**과 **결과 좌표 칸**이 붙어 있다.

⚠ **`C1` 문면은 *"`touch/<이슈>.txt:N` 이 원인이고 `파일:줄` 이 결과"* 라 적는다.**
실제로 원인 칸이 touch 좌표인 행은 **셋**뿐이고(`touch/126.txt:17` · `:24` ·
`touch/79.txt:23`), 나머지 열 행의 원인은 저장소 검사·컴파일러·정반합이다.
소유자가 `## 승격` 칸 3 에서 모집단을 **「사전 등록 ↔ 실제 변경 전부」**로 넓혔으므로
그 열 행은 touch 좌표를 원리상 못 가진다. **두 끝 중 앞 끝이 비는 행이 10/13 이다.**

**제안: `C1` 통과 · `C5` 통과.** (`C1` 의 「두 끝」 해석은 메인이 볼 자리)

### `C3` — ★ **반증 제안**

`judge-prompt.md` 는 **판 넷**을 싣는다(`:1` 판1 ㉡ · `:432` 판2 ㉠ · `:1102` 판3 ㉡ ·
`:1522` 판4 ㉢). 네 판 모두 넷이 바이트로 그대로다 — 기계로 확인했다:

    ① 사전 등록 : 네 판 다 IDENTICAL (위 `A1-c`)
    ② touch 산출 : 판1/판3 = touch/126.txt@14f0f92 · 판2 = touch/79.txt@88dbe13
                   판4 = touch/129.txt@138c782 — 네 판 다 IDENTICAL
    ③ diff       : 판1/판3 = `git diff a74737b..398d233 -- xtask/src/main.rs`
                   판2 = `git diff 77dc977..9f993cc -- crates/`
                   판4 = `git diff 22cf488..1abc0e9 -- crates/` — 네 판 다 IDENTICAL
    ④ §5.8 표    : 네 판 다 red.md:229-245 와 IDENTICAL

**그런데 띄운 인스턴스는 다섯이다.** `judge-roster.md` 의 5 번 행과
`129-judge-2.md:5` 가 프롬프트를 **「`judge-prompt.md` 판 5」**라 부르는데
**그 판이 파일에 없다.** `grep '판 [0-9]'` 가 판 1·2·3·4 만 낸다.

그리고 4 번 인스턴스가 받은 ③ 은 **네 파일 중 앞의 둘**이었다 — `129-judge-1.md:3-8` 이
스스로 적는다:

> ⚠⚠ **이 판의 ③ 은 잘려 있었다 — 부르는 쪽의 잘못이다.**
> `C3` 이 *"넷 다 바이트로 그대로 싣는다 — **자르거나 요약하면**…"* 을 못 박았는데,
> 이 인스턴스에 준 ③ 은 **네 파일 중 앞의 둘**(`narrative.rs` · `query.rs`)뿐이다.
> … **조건은 문면대로 못 지켰다.**

`grep '앞의 두 파일'` 이 `judge-prompt.md` 에서 **0 건**이다 — 즉 파일의 「판 4」는
잘린 판이 아니라 **전 diff 판**이고, **잘린 프롬프트의 전문은 어디에도 보존돼 있지 않다.**

그러므로 `C3` 의 두 요구가 각각 안 섰다:
1. *"넷 다 바이트로 그대로 싣는다"* — 인스턴스 4 의 ③ 이 잘렸다(회차 스스로 인정).
2. *"프롬프트 전문을 `effect/judge-prompt.md` 로 보존한다"* — 다섯 중 **넷**만 있다.

⚠ **메인이 볼 자리**: `C3` 을 **인스턴스마다** 읽으면 반증이고, **「귀속 판정이 선
프롬프트」 전체의 성질**로 읽으면 인스턴스 5 가 그 자리를 메우므로 다르게 갈 수 있다.
다만 2 번(전문 보존)은 후자로 읽어도 **다섯째 프롬프트가 파일에 없다**는 사실이 남는다.

**제안: `C3` 반증.**

### `C3-a` · `C3-b`

`C3-a` — 프롬프트 본문(`═ 여기서부터 프롬프트 ═` 와 `═ 프롬프트 끝 ═` 사이)에
*"귀속이 N 건인지 확인해라"* 꼴이 **없다**. 물음은
*"제출된 **귀속 후보**마다, 그 후보가 인용한 줄이 담은 **사실**이 ① 사전 등록 전문에도
없고 ② §5.8 표에도 없는가"* 하나다. `C2`(1 건 이상)를 말하는 줄은 `:1106-1107` 에 있는데
그것은 **판 3 의 머리말**이고 `:1110` 의 프롬프트 시작 마커 **바깥**이다.
명단은 다섯을 적고 다섯의 반환문이 다 있다(아래).

`C3-b` — `judge-roster.md` 의 행 **5** ↔ 파일 **5**:

    effect/126-judge-1.md  effect/126-judge-2.md  effect/79-judge-1.md
    effect/129-judge-1.md  effect/129-judge-2.md

각각 「받은 것」 절 + 후보별 판정 + `## 합계` 를 싣는 **반환문 전문**이다.
요약본이 아니고, 4 번(잘린 ③)의 반환문도 **안 버렸다**.

**제안: `C3-a` 통과 · `C3-b` 통과.**

---

## 4. D 절

### `D1`

정정문이 **저장소 안에** 선다 — `docs/gates/effect-confirmation.md:56-68`
`### #66 의 낡은 전제 둘 — 정정문 ⟨D1⟩`. 지금 값과 잰 명령이 칸으로 갈려 있다.
같은 표가 이슈 `#66` 의 2026-09-12T05:02 댓글 `### ①` 에도 있다.

**둘째 전제를 직접 다시 쟀다**:

    $ ./target/release/pal symbols /dev/null   (rc=1 · 인자 오류 경로)
    Error: 확장자 `.` 를 언어로 알지 못한다 — 아는 것은
           Kotlin · Java · JavaScript · TypeScript · Rust 다섯 이다

**넷이 아니라 다섯이다** — 게이트가 적은 값과 같다.

**제안: `D1` 통과.**

### `D3`

게이트 `:149-177` 이 갈래마다 명령과 값을 적는다. 둘 다 다시 돌렸다:

| 갈래 | 잰 명령 | 지금 값 | 값이 나오나 |
|---|---|---|---|
| ⓐ Rust 추출기 | `pal ledger` | `Rust L1 ordinal 141 파일` · `정체성 심볼 3334` | **난다** |
| ⓑ 다른 저장소의 실사용 | `pal ledger` 머리 | `저장소 선언 1 · 본 것 1` · `Snapshot palimpsest@0e1a876+worktree` | **안 난다**(저장소가 하나이고 자기 자신) |

⚠ 게이트가 적은 심볼 수는 **3332**(HEAD `9e092e3` 시점)이고 지금은 **3334** 다 —
그 뒤 `.rs` 가 늘었다. **조건이 요구하는 것은 「명령이 값을 낸다」이고 그 수 자체가
합격선이 아니다.** 게이트가 *"답을 문면에 담지 않는다"* 를 명기한다.

**제안: `D3` 통과.**

### `D4-a`

다섯 좌표가 **전부 실재한다** — `sed -n` 으로 직접 열어 확인:

| 좌표 | 지금 그 줄이 무엇을 말하나 |
|---|---|
| `corpus/criteria.toml:10530` | *"이 자리의 처분은 [#66] 이 소유자에게 묻고 있다"* |
| `corpus/criteria.toml:10886` | *"「이 도구가 자기 계획을 잰다」는 **Rust 추출기가 선 뒤**의 문장이고, 그 처분은 [#66] 이 …"* |
| `docs/gates/F12.md:312` | *"처분은 이 게이트가 정하지 않는다 — [#66] 이 소유자에게 묻고 있다"* |
| `docs/gates/F12.md:398` | *"자기 적용이 구조적으로 못 선다 … **[#66]**(소유자에게 묻는 중)"* |
| `scripts/f12-verify.py:556-563` | 정정 주석 여섯 줄 + 고쳐진 `skip(…)` 문면 |

처분은 게이트 `:215-237` 이 **좌표마다 한 행씩** 적는다. **한 규칙이 아니다** — 가르는 자가
명시돼 있다(*"이 문장이 **판정의 기록**인가, **지금 돌아가는 주장**인가"*):

- `docs/gates/F12.md:312`·`:398` · `corpus/criteria.toml:10530`·`:10886` → **안 고친다** +
  *"어디가 거짓이 되나"* 를 행마다 다르게 적는다(넷의 거짓 사유가 서로 다르다).
- `scripts/f12-verify.py` → ★ **고쳤다**. `git show` 로 확인했다 — 앞 판의
  *"추출기는 Kotlin·Java·JS·TS 넷뿐이다"* 가 *"F12 판정 시점에는 … 넷이라 … 합산하지
  않았다. ⚠ **그 전제는 지금 거짓이다** — Rust 추출기가 들어와 **다섯**이다"* 로 바뀌었다.
- 그리고 *"`B2` 의 28 검사가 `scripts/f12-verify.py` 를 안 돌린다"* 를 함께 적는다.

**제안: `D4-a` 통과.**

---

## 5. E 절

### `E2`

게이트 `## 효과`(`:41-55`):

⑴ *"**돌린 것**: 사람(메인)이 셸에서 `pal` 을 직접 불렀다 — `cargo test` 도
`cargo xtask check` 도 CI 도 아니다."* 산출은 `effect/e2-run.txt`(180 줄)이고 명령 넷이
종료값과 함께 들어 있다 — `:16` `pal query narrative.unbound`(rc 0 · *"RED 가 GREEN 이
됐다"*) · `:62` `pal ledger`(rc 0) · `:107` `pal symbols /dev/null`(rc 1 · 정상 경로) ·
`:111` `pal doctor`(rc 0).

⑵ 차이 표(`:238-272`)가 **열세 행**이고 행마다 **방향** 칸과 「touch 가 말한 것이
참이었나」 칸이 **나란히** 있다. 검산이 적혀 있다 — *"방향 기입 13/13 · 「참이었나」 기입
13/13"*.

⚠ `e2-run.txt` 는 `narrative.unbound` 의 전 출력(8550 줄 · 466960 바이트)을 **머리 40 줄**로
줄였고 그 사실과 sha256 을 머리에 적는다. 나머지 셋은 전 출력이다. **`E2` 문면은 전 출력을
요구하지 않는다**(그 요구는 `C3` 쪽이다).

**제안: `E2` 통과.**

### `E6`

- `docs/plan/03-shortest-path.md` §4 의 **3 단계 행** — *"⟨**섰다 2026-09-12** — 재는 자리
  셋 중 **㉡ `#126` 에서 둘**. 새 에이전트가 귀속 **유효 3 · 무효 2** … ㉠ `#79` 는 **0**
  … ㉢ `#129` 도 **0** … 판정은 [게이트](../../../../docs/gates/effect-confirmation.md)⟩"*.
  그 아래 ★★ 세 문단이 이 회차의 세 방향과 `#66` 종료까지 싣는다.
- `docs/plan/02-order.md:252-259` — *"⚠ **2026-09-12 에 한쪽이 닫혔다** … 아래 표의 둘째
  행(3 단계 효과 확인)을 회차 `2026-09-11-effect-confirmation` 이 실행했다"* 와
  표 행의 취소선 + *"**닫혔다 2026-09-12**"*.
- 커밋: `f331f2f`(뼈대) → `8966f5b`(*"계획 문서 둘이 이 회차의 결과를 받는다 ⟨`E6`⟩"*) →
  `cc703dd`(소유자 규범형 문장 반영).

**제안: `E6` 통과.**

---

## 6. 제안 판정의 계수

**통과 33 · 반증 1(`C3`) · 대조불가 0 · 미측정 0 = 34**
