# 교대 상태 — 효과 확인

> 회차 `2026-09-11-effect-confirmation` · 착수 커밋 `8604d62`

★ **새 컨텍스트가 받는 것은 「`intent.md` 전문 + 이 파일의 요약」이다.** 직전 산출물을
시드로 받지 마라 — 의도가 변질되는 기제는 쪼개는 것 자체가 아니라 **다음 걸음의 입력이
원 목표가 아니라 직전 걸음의 산출물일 때**다.

## 지금 단계

★★ **소유자 승격에서 멈췄다.** 정반합 판 6(`E3`)이 상한 2/2 를 다 쓰고 **「소유자에게
올린다」**로 끝났고, 물음 **여섯**이 `intent.md` `## 승격` **칸 16~21** 에 서 있다.
**소유자의 답 없이는 회차가 안 닫힌다** — 규약 §5 의 게이트가 *"살아남은 반론이 금지역·실패에
닿으면 소유자에게 올린다"* 로 못 박고, 판 6 의 설계가 *"「닫혔다」로 안 적는다"* 를 더했다.

⚠⚠ **`cargo xtask check` 가 27/28 이다 — 그것이 사실이다.** 「발견이 닫혔나」가 빨갛고
그 진단이 *"끝난 회차에 열린 발견이 있으면 **그 회차가 안 끝난 것**이다"* 다. `report.md` 는
섰는데 열린 발견이 **30**(전부 `DL5-*`)이다. **기계가 옳게 말하고 있다 — 고장이 아니다.**

### 지나온 것 ⟨이 세션⟩

음성 대조 다섯(`f75b4b0`) → 결정론 조건 34 실측 → 라운드 3 취합(`c402d15`) →
판 5 R1 지연 취합(`ff4f6cb`) → 처분 집행(`644e2e6`·`47357a4`) → 원장 닫기(`9fa4a13`·`39b0fa3`) →
`E1` 판정 표(`b441adf`) → **독립 리뷰 3/3**(`6a31780`·`1b2ead6`·`f50f0e1`·`6fadb98`·`a5142c8`·`e4253fe`) →
종료 보고 `report.md`(`c746038`) → **정반합 판 6 라운드 1·2**(`9f57859`·`422365b`) → 판 6 취합 37 항.

### 정본이 어디인가 — **수를 이 파일에서 읽지 마라**

이 표가 회차 중에 **세 번** 낡았고 독립 리뷰가 세 번 다 잡았다.

| 잰 것 | 정본 |
|---|---|
| 검사 | `cargo xtask check` |
| 원장 | `python3 .claude/skills/round/bin/record.py count <회차>` |
| 조건 판정 | 게이트 `docs/gates/effect-confirmation.md` 의 `## 판정` ↔ `intent.md` 의 상자 |
| 소유자에게 올린 물음 | `intent.md` `## 승격` 칸 16~21 |
| 이슈 | `gh issue view <번호>` |

## ⚠⚠ 다음 세션이 먼저 할 일 — **소유자의 답을 받는 것**

**칸 16~21 여섯에 답이 오기 전에는 판 6 을 다시 돌지도, 회차를 닫지도 마라.**
여섯의 요지는 이렇다(전문은 `intent.md` `## 승격`):

| 칸 | 물음 |
|---|---|
| 16 | 메인이 만든 **핀 밖 발췌 넷**을 여섯 자리가 받은 것이 조항 ㉮ 인가 — 라운드 1·2 가 **무효**인가 |
| 17 | 조항 ㉮ 의 **「핀 여섯」** ↔ 실제 **「핀 아홉」** — 오기인가 무효 사유인가 |
| 18 | 조항 ㉮ 에 없는 **「핀 밖」의 정의**를 이 회차가 박나 다음 회차가 박나 |
| 19 | 무효라면 **상한 2/2** 와 부딪힌다 — 라운드를 열 것인가, 접고 넘길 것인가 |
| 20 | 합이 **「금지역」을 「실패」로 내린** 강등을 추인하는가 |
| 21 | 라운드 2 의 핀이 **종료 뒤에** 갈렸다(메인이 과업문을 덧붙였다) — 핀 이탈인가 기록 추가인가 |

★★ **여섯 중 다섯이 메인이 낸 것이다.** 칸 20 만 에이전트의 판단이다.

### 답이 온 뒤의 순서

1. **답을 `## 승격` 칸에 적는다**(원문 그대로 · 물음 하나에 답 하나).
2. **`DL5-*` 열린 30 을 처분한다** — 답이 정한 대로. 닫는 커밋이 각 행의 `처분자리` 를 만져야 하고 `닫은커밋` 없는 닫힘은 형식 오류다.
3. `cargo xtask check` **28/28** 을 되찾는다.
4. **결박** — `pal bind` 로 이 회차의 결정과 의도를 결박하고 그래프를 갱신한다 ⟨§11 조건 4⟩. ⚠ **아직 한 번도 안 했다.**
5. **push 한 번** — `a8df04f` 이후 이 회차는 **push 를 한 번도 안 했다**(`cancel-in-progress: true`).
6. **CI 초록**(`E5`) — 판정은 **「회차의 마지막 커밋 SHA 에 `conclusion=success` 런이 붙었다」**로 잰다. 그 사실을 적는 커밋이 회차의 마지막 커밋이 된다(앞 회차 `8604d62` 와 같은 형태).

## 완수 조건 — **50/50 이 판정됐다**

**통과 40 · 반증 6 · 대조불가 0 · 미측정 4.** 게이트 `## 판정` 이 정본이다.

- **반증 여섯** — `A5`·`A7`·`C1`·`C3`·`C4`·`E3`. **반증은 실패가 아니다** ⟨규약 §9⟩.
  여섯 다 「거짓을 실었다」가 아니라 **「문면이 지정한 것이 안 섰다」**이고, **넷의 뿌리가
  이 회차가 스스로 만든 것**이다.
- **미측정 넷** — `A5-c`(판 2) · `C2`·`C2-b`(소유자 칸 2 · 문면 넷은 `#140`) · `E5`(CI).
  **「안 쟀다」이지 「됐다」가 아니다.**

★★ **독립 리뷰가 판정 둘을 뒤집었다** — `A5`(R1) · `A7`(R2). 둘 다 **메인의 실측이 문면보다
무른 자를 썼다.** 둘째는 첫째를 고치고도 **같은 자를 인용하는 조건을 전수로 안 다시 잰 것**이다.

## ⚠⚠ 이 회차에서 **메인이 낸 것** — 계열이 여덟이다

`MS-21` 이 이름 붙인 자리이고 회차 내내 자랐다. **형태가 아니라 자리가 문제다.**

| # | 무엇 | 어디서 드러났나 |
|---|---|---|
| 1 | 판 3 의 반에게 준 과업문에 **사실이 아닌 것** 하나 | 반이 자기 산출 머리에 적었다 ⟨`DL2-27`⟩ |
| 2 | 판 4 R3 의 반에게 **설계문이 안 주기로 한 입력**을 줬다 | 반이 적었다 ⟨칸 14⟩ |
| 3 | 판 5 R2 의 정에게 같은 종류를 줬다 | 정이 적었다 ⟨`DL3-40`⟩ |
| 4 | 독립 리뷰 R1 에 **반환 형식을 틀리게 지시**했다 | 리뷰어가 적었다 ⟨`#142` 의 셋째 실례⟩ |
| 5 | `A5`·`A7` 의 실측에 **문면보다 무른 자**를 썼다 | 독립 리뷰 R1·R2 ⟨판정 둘이 뒤집혔다⟩ |
| 6 | 판 6 라운드 1 이 **도는 중에** `6-prompts.md` 를 만들었다 | 반이 적고 합이 **실패**로 살렸다 ⟨칸 16⟩ |
| 7 | 격리용 **발췌 넷을 핀에 안 넣었다** — 라운드 1 의 합이 하나에 **여덟 자리**에서 기댔다 | 라운드 2 의 정이 찾았다 ⟨칸 16⟩ |
| 8 | 라운드 2 의 핀이 **종료 뒤에** 갈렸다 | 취합 보고가 쟀다 ⟨칸 21⟩ |

★★ **여덟 중 여섯이 「에이전트가 자기 산출 머리에 적어서」만 드러났다.** 그 자리를 세우려고
`dialectic/6-prompts.md` 를 만들었고 — 판 4 의 합이 지목하고 판 6 의 설계가 요구한 자리다 —
**그 파일 자신이 6·8 을 만들었다.** 그리고 취합 보고가 실측했다:
`grep -rn '6-prompts|과업문' xtask/ crates/` → **0 건. 그 파일을 읽는 기계 검사가 없다.**

## 이 회차가 세운 이슈

`#140`(조건 문면 **넷**) · `#141`(백틱 참조 사각) · `#142`(추출기 ↔ 계수기가 다른 수를 낸다).
**닫은 것** — `#66`·`#79`·`#126`·`#129`.

## 다른 기계에서 이어받을 때

★ **이 회차는 `2026-09-11` 에 기계를 옮겼다.** 옮기기 전에 잰 것은 아래가 전부다.

**먼저 세워야 하는 것 셋** — 안 세우고 재면 조건이 **다른 것을 잰다.**

1. **`cargo build --release`** — 완수 조건이 `./target/release/pal` 을 이름으로 부른다.
   `target/` 은 통째로 무시되므로 저장소에 안 실렸다.
2. **`pal doctor` → `pal narrative`** — `.palimpsest/intent.redb` 를 다시 만든다. 정본은
   추적 중인 `.palimpsest/intent/bindings.jsonl` 이고 `redb` 는 파생물이라 `.gitignore` 가
   막는다. ⚠ **`B1-a` 는 그 파일이 「있는데도」 실패하는 것을 요구한다** — 안 만들고 재면
   「파일이 없어서 난 실패」가 되어 **모집단이 달라진다.**
3. **`cargo xtask check` 를 먼저 돌려 28/28 을 확인해라.** 그것이 이 회차의 회귀 기준선이다.
   ★ **앞 판이 「`corpus/manifest.toml` 의 외부 저장소 핀 때문에 갈릴 수 있다 · 추정이다」로
   적은 자리는 실측으로 닫혔다** — 착수 커밋 `8604d62` 의 CI 가 **세 OS 에서 갓 클론한 나무**로
   `cargo xtask check` 와 `cargo xtask test` 를 통과시켰고, `868c27a` 에서도 「회차 레코드」
   하나만 죽고 나머지 **27 이 전부 초록**이었다. 그 핀은 이 둘을 안 깬다. ⚠ 남는 것은 새 기계의
   **네트워크·캐시**이고 그것은 다른 물음이다.

**갈리는 것 둘** — 기계를 옮기면 값이 바뀐다.

- **`grep` 구현.** 옮기기 전 기계는 ugrep 7.8.4 였고 BSD `/usr/bin/grep` 과 **빈 줄 넷에서
  갈렸다**(8 ↔ 4). Linux 의 GNU grep 은 셋째 값을 낼 수 있다. `grep -f` 가 들어가는 판정을
  다시 돌리면 **어느 grep 인지 먼저 적어라.**
- **격리 사본의 디렉터리 이름.** `RepoId` 의 성분이라 `palimpsest` 이외의 이름으로 뜨면
  심볼 id 가 통째로 어긋난다 ⟨`PM1-19`⟩.

**증거는 저장소에 실었다.** `observations/premortem-artifacts/` — 사전부검·조건평가가 시스템
temp 에 남긴 산출 열여섯과 격리 사본의 탐침 `diff` 다. 무엇이 무엇을 받치는지는 그 안의
`README.md` 가 적는다. ⚠ **전부 HEAD `1a01057` 에서 떴다.**

**안 실린 것.** `.claude/settings.json` 은 `.gitignore` 가 막는다(플러그인 선언 하나뿐이고
사용자 레벨 경로로 풀린다). 앞 기계의 격리 사본 `/tmp/pm-1789102884/` 은 버렸다 — 탐침
두 줄은 위 `clone-probe.diff` 가 진다.

## 이 회차가 하는 일 한 문장

이 저장소에서 `pal touch` 를 **실제 작업 앞에** 부르고, 그 산출을 본 뒤의 변경이 달라졌는지
잰다. 실제 작업은 이슈 **`#79` · `#126` · `#129`** 셋이다.

## 남은 것

### 먼저 — 지금 수 ⟨종료 직전 · 2026-09-12⟩

⚠⚠ **이 표가 회차 중에 세 번 낡았고 독립 리뷰가 세 번 다 잡았다.** 정본은 이 표가 아니라
`cargo xtask check` · `record.py count` · 게이트 `## 판정` 이다. **수를 읽기 전에 그 셋을 돌려라.**

| 잰 것 | 어디가 정본인가 |
|---|---|
| 검사 | `cargo xtask check` |
| 원장 | `python3 .claude/skills/round/bin/record.py count <회차>` |
| 조건 판정 | 게이트 `docs/gates/effect-confirmation.md` 의 `## 판정` 과 `intent.md` 의 상자 — **둘이 갈리면 「원장 둘 대조」가 빨개진다** |
| 이슈 | `gh issue view <번호>` |

★ **이 회차의 산출 한 줄** — 조건 50 개가 전부 판정됐고 **반증 다섯** 중 넷의 뿌리가
**이 회차가 스스로 만든 것**이다. 종료 보고 `report.md` 가 전부를 진다.

⚠ **이 파일이 인용하는 커밋 중 이력 밖인 것이 있다** — `6e26be5`(판 4 R3 의 반 · 판 5 R2 의 정)가
`git merge-base --is-ancestor 6e26be5 HEAD` 에서 `rc=1` 이다. 버려진 커밋이고, 그 산출물
자신은 `dialectic/` 에 바이트로 서 있다.

<details><summary>그 앞의 수 ⟨`cc703dd`⟩</summary>

| 잰 것 | 값 |
|---|---|
| `cargo xtask check` | **28/28** · 워킹트리 clean · ⚠ **CI 는 아직 안 봤다** — 이 회차는 `a8c0df4` 이후 **push 를 안 했다**(`cancel-in-progress: true` 라 한 번에 민다) |
| 원장 | **204 행** — 사전부검 72 · 정반합 65 · 조건평가 47 · 실측 **20** |
| **열린 발견** | ★ **55** — 금지역 **11** · 거짓신호 35 · 미관 9 |
| **조건 상자** | ★ **0 / 50 이 켜졌다** — `E1` 이 통째로 안 섰다 |
| 게이트 `## 판정` | 50 개 전부 `미측정` 칸 |
| `#66` | **CLOSED** ⟨2026-09-12T06:05:04Z⟩ — 댓글 둘(`5643614829` `D1` · `5644071744` 닫는 것) |
| 정반합 | **판 다섯 · 라운드 셋 전부 닫혔다.** 소유자 승격 **열다섯** |

★★ **핀은 이제 안 걸린다** — 판 4 R3 과 판 5 R2 가 둘 다 닫혔으므로
`dialectic/preflight-r3.md` · `5-preflight-r2.md` 는 **기록**이지 구속이 아니다.
`intent.md` · `findings.jsonl` · 게이트를 자유롭게 만져도 조항 8 이 안 걸린다.
⚠ **다음에 판을 새로 열면 핀을 다시 박아라** — 칸 9 의 교훈이고 라운드 3 이 집행했다.

★★ **`E4` 가 「열린 발견 0 · 열린 금지역·실패 0」을 요구한다.** 지금 55 건이 열려 있고
그중 **금지역이 열하나**다. **`r3-raw.md` 가 실리면 더 는다.** 이것이 남은 일의
**가장 큰 덩어리**이고, 닫는 커밋이 각 행의 `처분자리` 를 실제로 만져야 한다
(`발견이 닫혔나` 검사가 그것을 잰다). ⚠ **닫힘인데 `닫은커밋` 이 없으면 형식 오류다.**

</details>

| | 무엇 |
|---|---|
| 1 | ~~소유자 승인~~ **끝났다** ⟨2026-09-11⟩ — 조건 50 을 그대로 승인받았다. ⚠ **함께 올릴 것**: 조건 설계 평가는 상한 2 를 다 썼고 감사자가 마지막으로 본 것은 **조건 46 시점**이다. `A7`·`A7-a`·`A5-d`·`A5-e`·`B1-d`·`B1-e` 여섯과, 기록 정합이 고친 **넷**(`A1-a`·`A5-a`·`A5-b`·`C2-a`)도 마찬가지다. **조건 46 이후 문면이 바뀐 기존 조건은 13**(`A1-c` `A5-b` `A5-c` `B1` `B1-a` `B6` `C2-a` `C2-b` `C3` `C3-a` `C3-b` + `A1-a` `A5-a`)이고 새로 선 여섯을 합치면 **19** 가 감사자가 못 본 문면이다. ★ **`A7-a` 는 고쳤다** ⟨승인 앞 정정 · `MS-05`⟩ — 자기 판정값(「한 축만 초록이어도 불통과」)을 달고 `A7` 의 **앞쪽** 경계를 뒤집는 축 ⓑ 를 더했다. 조건 수는 **50 그대로**다. **그래도 감사를 한 번도 안 거친 줄이다** — 승인과 함께 이 한 줄을 같이 보아라 |
| 2 | ~~자리마다: 조사 → 사전 등록 → touch → 변경 → 차이~~ **끝났다** ⟨2026-09-11⟩ |
| 3 | ~~**`C3` 귀속 판정**~~ **끝났다** ⟨2026-09-11⟩ — 판정자 일곱 · 전수 보존 |
| 4 | ~~정반합 라운드 2 — 판 1·3~~ **끝났다** ⟨2026-09-12⟩ — 판 1 **종료**(`B1` 통과) · 판 3 **소유자 승격**(`C2`·`C2-b` 미측정 · 금지역 셋 · 소유자가 칸 6·7 로 답했다). 판 2 는 라운드 1 에서 끝났다. **남은 것: 취합 보고 `r2-raw.md` → 원장 적재** |
| ~~**A**~~ | ~~판 4 의 합·종료 판단~~ **끝났다** ⟨2026-09-12⟩ — `C4` = **반증** · 종료 판단도 **소유자에게 올린다**(금지역 1 · `A4`). `r2-referee.md` 가 판 1·3·4 셋을 진다 |
| ~~**A′**~~ | **끝났다** ⟨2026-09-12⟩ — 소유자가 **「집행한다」**를 골랐다. `intent.md` `## 승격` **칸 12** 가 전문을 지고 `## 상한` 이 초과를 진다. ★★ **판 4 라운드 2 는 무효다** — 그 산출물(`4-thesis-r2.md`·`4-antithesis-r2.md`·`4-synthesis-r2.md`)은 **바이트로 남기되 판정으로 안 쓴다**. 메인이 핀 40 개를 전수로 다시 재서 칸 9 의 「다섯」이 실제로 **여섯**임을 확인했다(자기 핀 제외) |
| ~~**B**~~ | **끝났다** ⟨`c807f32`·`8457ac1`⟩ — 게이트 정정 **여섯**(다섯이 아니다 · 여섯째는 `## 합격선` 의 차선책 줄이고 **메인이 찾았다**)과 세운 절 셋(`### #66 의 물음 1` · `### #66 을 안 닫는다` · `### 정반합 산출물 — 정오표와 성격`). 원장에 `MS-15`~`MS-20` 으로 실었다 |
| ~~**B′**~~ | ★★ **판 4 라운드 3 이 닫혔다** ⟨`029d1a6`⟩ — 핀 `42b24eb` · 정 `288771d`(`C4` 반증) · 반 `6e26be5`(반론 15) · 합 `3befbb5`(**`C4` = 반증** · 채택 13 · 기각 3) · 종료 판단 `029d1a6`(**소유자에게 올린다**). ★ **값은 라운드 2 와 같은데 근거가 다르다** — 라운드 2 가 든 셋 중 둘을 `c807f32` 가 없앴고, 지금 반증은 **문면 하나**에만 걸려 있다: 조건 `C4` 의 *"근거와 함께"* + 게이트 `:182` 가 그 칸에만 붙인 「· 근거」 + 칸 12 의 문면 동결. **여덟 행의 「해당 없음」은 사실로 전부 참이다**(반이 전수 `grep` 으로 확인) — 반증의 내용은 「거짓을 실었다」가 아니라 **「근거를 안 실었다」**다 |
| ~~**C**~~ | ★★ **판 5 라운드 2 가 닫혔다** ⟨`0771f75`⟩ — 핀 `06d8835` · 정 `6e26be5` · 반 `3befbb5`(반론 13 · 금지역 1) · 합 `b268bec`(**`AT5-14` 거짓신호로 내림 · `D2` 미측정**) · 종료 판단 `0771f75`(**소유자에게 올린다**). ★★ **합이 이 회차에서 가장 무거운 것을 찾았다** — 게이트가 `D2` 절에 **자기 반증 검사를 등록해 놓고 한 번도 안 돌렸다.** 재료 둘째의 경로(`docs/adr/0027-rust-first-class.md`)가 **없는 파일**이라 아무도 실재 문서를 안 열었고, 그래서 *"어느 것도 규범형으로 말하지 않는다"* 라는 전칭 부정이 안 재어진 채 사실로 적혔다. ⚠ 백틱 참조라 「죽은 링크 부재」가 **원리상 못 본다** |
| ~~**A″**~~ | **끝났다** ⟨`cc703dd`⟩ — 소유자가 **칸 13·14·15** 를 답했다. 전문은 `intent.md` `## 승격`. ★★ **칸 15 가 `D2` 를 통과시키고 `#66` 을 닫았다** |
| ~~**F**~~·~~**F′**~~ | **끝났다** ⟨`cc703dd`⟩ — ★ **`D4` 는 반증이 아니라 통과다.** 칸 15 가 칸 10 을 거뒀다. `#66` 이 **CLOSED** 이고 댓글 둘(`D1` 의 것 + 닫는 것)이 실렸으며 게이트가 그 URL 둘을 싣는다. 차선책 4 는 **안 열린다** |
| ~~**E6**~~ | **끝났다** ⟨`8966f5b`·`cc703dd`⟩ — 계획 문서 둘이 이제 *"전제였던 `#66` 도 이 회차가 닫았다"* 와 소유자의 규범형 문장을 싣는다 |
| ~~칸 7~~ | **끝났다** — `#140` ② 의 문면이 이미 살아남는 관측으로 고쳐져 있다 |
| ~~**D**~~ | **끝났다** — `dialectic/r3-raw.md` **49 항**(`### ` 42 + 기각 표 7) → `DL3-01`~`DL3-49` · 실측 넷(`MS-21`~`MS-24`). ★★ 취합이 구멍 하나를 찾았다 — **판 5 라운드 1 의 취합 보고가 한 번도 안 났고 그 라운드의 `AT5-01`~`AT5-16` 열여섯이 원장에 없다.** `r4-raw.md` 로 메운다(`r<n>` 은 취합의 순번이지 판의 라운드가 아니다). 앞 판의 과업문: **라운드 3 취합 보고** — 판 4 R3 + 판 5 R2 를 묶어 `dialectic/r3-raw.md` 로. **항 수 = 레코드 수**. ⚠ `## 내가 기각한 것` 절은 **표**로 받아라(불릿은 추출기가 안 읽는다 — `MS-14`). **이것이 다음 걸음이다** — 판 5 R2 의 반이 원장에 `AT5` 가 0 건임을 재어 적었고, 합이 *"갈래 ⑴ 은 취합 보고 커밋의 일"* 로 판정했다 |
| **E** | **`E1` — 게이트 `## 판정` 표를 채운다.** 판정 넷의 합 = **50** 이고 `intent.md` 의 조건 ID 집합과 **집합 같음**이어야 한다. **같은 커밋에서 `intent.md` 의 상자도 채운다** — 원장 둘이 갈리면 빨개진다. ⚠ **지금 0/50 이다** |
| **G** | ★★ **`E4`** — 원장의 **열린 발견을 0 으로** 만든다. **지금 65**(`DL3` 열림 10 이 더해졌다). ⟨감사 산출 `observations/g-disposal-audit.md`⟩ **실행됨 19 · 부분 26 · 안 됨 10.** 「부분」 26 은 정정 내용이 합(合) 문서에 커밋돼 있고 빠진 것이 **독자가 초안을 만나는 자리의 정오표**뿐이라, 게이트 `### 정반합 산출물 — 정오표와 성격` 에 **표 하나**로 싣고 닫는다(⚠ `DL1-17` 은 합의 지시 자체가 「정오표를 붙여라」라 그 길로만 닫힌다). 「안 됨」 10 은 실제로 처분한다 — `MS-10` 주석 · `MS-11` 「워킹트리」 세 뜻 · `MS-12`+`DL2-23` 조항 6 · `DL1-02` 수 조정 · `DL1-03` `종료_커밋_시각` 시험 · `DL1-25` · `DL2-04` 근거 문면 · `DL2-10` 「합=분모」 음성 대조 · `DL2-26`. 앞 판의 수: **55**(금지역 11 · 거짓신호 35 · 미관 9)이고 **D 가 실리면 는다**. 닫는 커밋이 `처분자리` 를 만져야 하고 `닫은커밋` 없는 닫힘은 형식 오류다. ⚠ **대부분은 이미 처분이 실행됐고 기장만 안 된 것**이다 — 예: `DL2-08`(ledger.rs 의 거짓 문단 셋)은 ㉳ 가 걷었고, `DL2-12`(`#140` ② 의 거짓)는 칸 7 이 고쳤고, `DL2-17`(`intent.md:412` 오인용)은 칸 8 이 게이트에 성격을 적었고, `DL2-16`(조항 8 을 안 불렀다)은 칸 12 가 집행했다. **한 건씩 처분이 어디까지 섰는지 확인하고 닫아라** |
| **H** | **독립 리뷰 3 라운드**(상한 · `pal-independent-reviewer`) → **`E3` 정반합**(종료 보고에 네 이름이 없다) → 종료 보고 `report.md` → **결박**(`pal bind`) → 이슈 닫기 → CI 초록(`E5`) |
| 8 | ~~다음 회차로 올릴 것~~ **이슈가 섰다** ⟨2026-09-12⟩ — `#140`(`ready-for-agent`). `C2`·`C2-b` 의 조건 문면 셋. **이 회차는 그 셋을 안 고친다** |
| 9 | ~~해악도가 안 붙은 넷~~ **끝났다** ⟨2026-09-12⟩ — 좌표에서 다시 재어 `MS-10`~`MS-13` 으로 실었다(`출처=실측` · 라운드 5 · 정반합 36 항의 검산을 안 건드린다) |

## ~~⚠ 지금 미뤄 둔 수리~~ — **셋 다 끝났다** ⟨2026-09-12⟩

① 줄 밀림 — **고쳤다.** 덧붙임을 전 출력 뒤로 옮겼고 핀 대비 앞 65 줄(㉠)·38 줄(㉢)이
다시 **바이트로 동일**하다. ② CI 벽시계 — **고쳤다**(손자 20 초 · 판정선 8 초).
★ **흔들림이라는 것이 실측으로 확인됐다** — `fc961d3` 의 실패한 잡만 다시 돌렸더니
**같은 커밋·같은 바이트에서 초록**이 났다(런 `34667591686` 이 `failure` → `success`).
코드가 아니라 **기계 속도**가 판정을 뒤집은 것이고, 그래서 상수가 아니라 **간격**을 고쳤다.
③ 내 과업문의 거짓 — **취합 보고가 `DL2-27` 로 실었다**(내가 따로 안 실었다).
그리고 두 기계가 갈리던 자리는 `MS-14` 로 실었다. 아래는 그때의 기록이다.

<details><summary>그때 적어 둔 것</summary>

## ⚠ 미뤄 뒀던 수리 — **정반합이 끝나기 전에 손대면 기준 핀이 또 깨진다**

**① `touch/79.txt`·`touch/129.txt` 의 줄이 +7 밀렸다.** ⟨2026-09-12 · 판 3 라운드 2 의 정(正)이
잡았다⟩ ⓓ2 처분(「워킹트리 깨끗」의 출처를 못 되살린다)을 **머리 끝 · 전 출력 마커 앞**에
일곱 줄로 넣었더니 그 아래가 전부 밀렸다. 실측: `touch/79.txt:23` 이 핀 `05f07ea` 에서
`호출자 3 · 피호출자 1` 이었는데 지금은 `:30` 이다.
**`C2-b` 는 `touch/<이슈>.txt:N` 의 `N` 을 조건의 성분으로 묶은 유일한 조건**이라 이 밀림이
그 조건 위에 직접 얹힌다. `touch/126.txt` 는 안 밀렸다(그 파일은 원래 출처를 적고 있었다).

**고칠 방법은 정해져 있다 — 덧붙임 일곱 줄을 파일 *맨 끝*으로 옮긴다.** 그러면 머리도
본문도 한 줄도 안 밀리고 ⓓ2 의 기입은 남는다. ★ **판 3 의 반(反)·합(合)이 끝난 뒤에 한다** —
지금 고치면 정(正)의 §1 이 가리키는 바이트가 사라져 반(反)이 그 주장을 못 잰다.

**② CI 의 Windows 시험 하나가 벽시계에 걸려 빨개졌다.** `fc961d3` 에서 `windows-latest` 만
실패했다(ubuntu·macos 초록). `crates/pal-cli/tests/round_approve_verify.rs:898` 의
`assert!(started.elapsed() < Duration::from_secs(4))` 이고, 재는 것은 *"detached 손자를
안 기다리고 돌아오나"* 인데 **기계 속도에 걸린 상수**라 느린 러너에서 흔들린다.
`ledger.rs` 의 셈법 변경과 무관하다(그 시험은 `round approve/verify` 계열이다).
★ **판 1 의 반(反)이 끝난 뒤에 고친다** — 그 반에게 *"`crates/` 는 `fc961d3` 이후 한 바이트도
안 바뀌었다"* 를 못 박아 줬다.

**③ 내가 판 3 의 반(反)에게 준 과업문에 거짓이 하나 있었다.** ⟨2026-09-12⟩
*"움직인 것은 `docs/gates/effect-confirmation.md` · `scripts/f12-verify.py` · `dialectic/` 과
`state.md` 다"* 라 적었는데 **`scripts/f12-verify.py` 는 안 움직였다** — 그 파일의 마지막
커밋은 `2d47da6` 이고 `fc961d3` 보다 앞이다. 반이 자기 산출 머리에서 그것을 잡아 적었다.
**회차의 금지역 「사실이 아닌 것을 사실로」에 드는 형태이고, 낸 것은 에이전트가 아니라
메인이다.** 판이 닫히면 `출처=실측` 으로 원장에 싣는다(정반합 항 수 검산을 안 건드린다).

</details>

## ~~소유자에게 올릴 칸들~~ — **셋 다 답받았다** ⟨`cc703dd`⟩

**칸 13 · 14 · 15 의 전문은 `intent.md` `## 승격` 이 진다 — 여기 두 번 안 적는다.**
답만 한 줄씩: **13** 안 덮는다 → `C4` **반증** · **14** 격리 이탈은 **유효로 확정**(설계문에
격리 이탈 무효 조항이 **없다** — 집행은 집행이 아니라 확장) · **15** 소유자가
**「자기 저장소 큐레이션은 바닥까지만 제품 요구다」**를 썼다 → `D2` **통과** · `D4` **통과**.

<details><summary>올릴 때 적어 둔 것</summary>

### 소유자에게 올릴 칸들 ⟨2026-09-12 · 판 4 R3 이 둘을 냈다⟩

**규약 §5 — 승격은 물음 하나에 칸 하나다.** 묶어 올리면 답 하나가 두 물음을 닫은 것처럼
보이고 답 없는 물음이 처리된 것처럼 사라진다.

### 칸 13 ⟨판 4 R3 · 합 `dialectic/4-synthesis-r3.md`⟩ — `C4` 의 값이 여기 걸렸다

**물음** — 칸 4 의 *"존재는 합격선, 값은 아니다"* 가 `C4` 문면(`intent.md:243`)의
**「근거와 함께」**까지 덮나, 아니면 **방향 칸의 값**만 덮나.

- **덮는다** → `C4` **통과**. 근거 없는 여덟 셀은 `MS-18` 과 같은 **정정** 거리로 내려앉는다.
- **안 덮는다** → 합의 **반증**이 그대로 선다.

⚠ **함께 정할 것 둘** — ⑴ 안 덮는다면 「근거」의 문턱이 **셀 안의 근거 절**인가(그러면
`C4` 는 서식 검사가 된다) **행 단위 근거**인가. ⑵ **칸 3·4 의 자기 좌표가 둘 다 죽었다** —
칸 3 은 *"`C1` 문면(`intent.md:231`)"* 이라 적는데 `:231` 은 §5.8 산문이고 `C1` 은 `:236` ·
칸 4 는 *"`intent.md:238`"* 이라 적는데 `:238` 은 **`C2-b`** 이고 `C4` 는 `:243` 이다.

★ **합이 든 근거** — `:180`(*"기입의 존재가 합격선이다"*)과 `MS-17` 은 **같은 손이 같은
커밋(`c807f32`)에서 쓴 같은 의역**이라 독립 권위가 아니고, 칸 4 원문은 *"**방향** 기입의
존재"* 를 묻고 *"조건 문면은 안 바뀐다"* 로 끝난다. **도구는 조건 문면을 못 줄인다.**

### 칸 14 ⟨판 4 R3 · 종료 판단 `dialectic/r3-referee.md`⟩ — 격리 이탈의 처분

**물음** — 판 4 에서 하네스가 설계대로 안 돈 것이 **두 번째**다(첫째는 조항 8 · 소유자가
집행을 골랐다). 이번 이탈 — **설계가 반(反)에게 안 주기로 한 입력을 메인의 과업문이 준 것** —
에 대해서도 **조항 8 과 같은 무효·재실행을 집행할 것인가**, 아니면 **합이 오염 없음을 보인
근거를 받아 라운드 3 의 산출을 유효로 확정할 것인가.**

★ **합이 오염 없음을 보인 방법** — 그 입력에 기댄 반론 둘(R1·R3)을 **문면으로** 기각하고
하나(R4)를 **원장 밖 `git show` 로 다시 세웠다.** 종료 판단은 반대편 재료를 댄다 —
*"라운드를 한 번 더 도는 것은 **같은 하네스로 같은 결함을 다시 겪는 것**이지 그 실패를
닫는 행위가 아니다."*

⚠ **집행하면 라운드 4 이고 상한은 이미 3/2 다.**

### 칸 15 — **판 5 의 합이 내면 여기 선다** ⟨대기⟩

</details>

## ⚠⚠ 내가 낸 것 — 과업문이 설계문을 두 번 어겼다 ⟨2026-09-12⟩

**둘 다 에이전트가 잡아 자기 산출 머리에 적었다.** 판이 닫히면 **원장에 `출처=실측` 으로
싣는다**(지금은 `findings.jsonl` 이 핀 안이라 못 만진다).

| 어디 | 설계문이 안 주기로 한 것 | 내가 준 것 | 무엇이 됐나 |
|---|---|---|---|
| **판 4 R3 의 반(反)** | `dialectic/4-design.md:151` — `intent.md` 의 `## 차선책`·`## 범위 밖` · `findings.jsonl` 의 **이미 매긴 처분** | `intent.md` **전문** · `findings.jsonl` | 반이 **R1·R3·R4** 를 그 필드(`해악도`·`처분`·`닫은커밋`)로 세웠다. 반이 그 사실을 「규약 위반으로 스스로 적어 두는 것」 절에 적고 **감가·기각을 합에게 넘겼다** |
| **판 5 R2 의 정(正)** | `dialectic/5-design.md:223-224` — *"라운드 1 의 **반론표 원문은 안 받는다**"* | `dialectic/5-antithesis.md` | 정이 **전문을 안 열고** `grep` 적중 줄 셋만 봤다고 적었다 |

★★ **소유자가 칸 14 로 답했다** ⟨`cc703dd`⟩ — **유효로 확정하고 대가를 적는다.**
까닭은 **설계문에 격리 이탈에 대한 무효 조항이 없다**는 것이다(조항 8 은 **핀 이탈**에 대한
것이라, 집행하는 것은 조항의 **집행**이 아니라 **확장**이다). **대가 둘을 종료 보고가 진다** —
⑴ 판 4 에서 하네스가 설계대로 안 돈 것이 **두 번째**다 ⑵ **메인의 과업문을 보존하는 자리가
회차기록에 없다**(판 4 의 합이 `## 내가 못 정한 것` 첫 줄로 적었다).
⚠ **원장에 `출처=실측` 으로 싣는 것은 아직 안 했다 — `D` 와 함께 해라.**

★★ **이것이 `DL2-27` 과 같은 계열이다** — *"내가 판 3 의 반(反)에게 준 과업문에 거짓이
하나 있었다."* 그때는 **사실이 틀린 것**이었고 이번은 **격리 경계를 넘긴 것**이다.
같은 자리(메인의 과업문)에서 두 회 났으므로 **형태가 아니라 자리가 문제다.**

⚠ **되돌릴 수 없다.** 준 것을 안 준 것으로 못 만든다. 할 수 있는 것은 ⑴ 그 사실을
산출물 머리에 박고 ⑵ 합에게 명시로 넘기고 ⑶ 원장에 싣는 것이고, 셋 다 했다.
★ **반이 함께 적은 것** — *"초안도 같은 파일을 근거로 썼다(`4-thesis-r3.md` 가
`findings.jsonl:127`~`:129` 를 인용한다). **한쪽만 격리가 깨진 것인지 양쪽 다인지는
합이 판단할 일이다.**"*

## ~~게이트에 남은 거짓과 공백~~ — **여섯이었고 여섯 다 고쳤다** ⟨`c807f32` · 2026-09-12⟩

★★ **다섯이 아니라 여섯이었다.** 앞 다섯은 전부 `## 효과` 에서 나왔고 **`## 합격선` 은
판 4 의 정도 판 5 의 정도 안 봤다** — 그 절이 *"차선책 … 하나도 안 썼다"* 로 적는데
칸 10 이 차선책 4 를 발동했다. **어느 절이 아무에게도 안 읽혔는지가 `MS-20` 의 값이다.**

원장에 `MS-15`~`MS-20`(금지역 넷 · 거짓신호 둘). 세운 절 셋도 같은 커밋이다 —
`### #66 의 물음 1`⟨`D2`⟩ · `### #66 을 안 닫는다`⟨`D4` = 반증⟩ ·
`### 정반합 산출물 — 정오표와 성격`⟨칸 8·11·12⟩. 아래는 그때의 기록이다.

<details><summary>고치기 전에 적어 둔 것</summary>

### 게이트에 남은 거짓과 공백 — **다섯. 전부 내가 만든 것이다**

**넷은 `## 효과` 에 있다** ⟨판 4 라운드 2 의 정(正)이 잡았다 · 2026-09-12⟩

1. ★ **`effect/negative-79.txt` 는 저장소에 없다.** `docs/gates/effect-confirmation.md:112`
   이 ㉠㉰ 의 방향 근거로 그 파일을 댄다. 내가 다시 쟀다 — `ls` 에 없고
   `git log --all -- '*negative-79*'` 가 **빈 출력**이며 `git check-ignore` 도 exit 1 이다.
   **있지도 않은 산출물을 근거로 세웠다** — 회차가 `intent.md:87-88` 에 등록한 금지역
   「사실이 아닌 것을 사실로」다. 실물 기록은 `effect/79-delta.md:70-88` §4 에 있다.
2. **모집단이 12 가 아니라 13 이다.** `effect/79-delta.md:23` 이 스스로 **여섯**이라 적는데
   게이트 `:101`·`:119` 는 ㉠ 을 **5** 로 세고 합을 **12** 로 적는다. 행 **㉳** 가
   `0aabb1c`(게이트 집필 11:33) **뒤**인 `017e5f1`(12:03)에 들어왔고 게이트가 그 앞에서 멈췄다.
3. **㉳ 가 방향·「참이었나」를 하나도 못 받았다.** `grep -rn "㉳"` 적중이
   `effect/79-delta.md:31`·`:40` 둘뿐이다. ⓐ 축(차이 전수에 두 기입)이 12/13 에서 깨진다.
   ⚠ 그리고 `:117`(㉢㉰)의 방향 칸은 근거 절 없이 `**나아졌다**` 뿐이다.

**다섯째는 `### #66 의 물음 2` 절에 있다** ⟨판 5 라운드 1 의 정(正) 부수 발견 ②⟩

**게이트가 갈래 선택을 `D2` 에 맡기는데 그 자리가 셋으로 갈린다.** ⟨판 5 라운드 1 의 정(正)
부수 발견 ②⟩ — `docs/gates/effect-confirmation.md` 의 `## 효과` `### #66 의 물음 2` 절이
*"어느 갈래를 고를지는 `D2` 의 정반합이 판정한다"* 라 적는데,
⑴ `docs/adr/0027-…:133` 이 2026-08-20 에 갈래 ⓑ 를 **이미 기각**했고
⑵ 설계문 `dialectic/5-design.md` 의 `## 이 판이 안 다루는 것` 이 갈래 물음을 **`D3` 의 것**이라
못 박았다. **내가 쓴 문장이 틀렸다.** → `D3` 로 고친다.

### 그리고 게이트에 **더할** 것 둘

- **`D2` 절** — `## 효과` 에 `D1`·`D3`·`D4-a` 는 있고 **`D2` 가 없다**(판 5 의 정이
  preflight 로 기계 확인). 답 값 **「아직 못 답한다」** · 「아직」이 요구하는 둘(*무엇이
  있으면 답하나* = 명제를 규범형으로 적는 ⓐ 문장 하나 · *누가 낼 수 있나* = 소유자) ·
  「이 판정을 뒤집는 것」을 싣는다. ⚠ **`D2` 문면은 안 고친다**(고치면 완화이고 승격이다).
- **`AT5-14` 의 정정문** ⟨칸 11⟩ — *"`ADR-0027:22` 의 「Kotlin·TypeScript 둘뿐」은 **맞다**.
  「추출기 셋」(`git ls-tree a1493b5 crates/pal-extract/src/`)과 「`Language` enum variant
  다섯」(`crates/pal-core/src/language.rs:24-30`)은 **다른 것을 센다**"*.
  원문(`dialectic/5-thesis.md`)은 **한 글자도 안 고친다** — `C3-a` 가 바이트 보존을 요구한다.

★ **다섯을 한 커밋에 고치고 `effect/79-delta.md` 에 차이 행이 느는지 본다** —
`C1` 이 전수를 요구하고 칸 3 이 모집단을 「전부」로 못 박았다(원인 끝은 **정반합**이다).

</details>

## 취합 보고가 두 판본을 냈다 — 어느 것을 실었나 ⟨2026-09-12⟩

`r2-raw.md` 는 **`| 요약 | 경로 | 모집단 | 유효성 | 해악도 |`** 머리를 쓴 판본이다.
보고자가 그 뒤에 첫 칸을 `| 기각한 것 |` 로 바꾼 판본을 하나 더 냈는데 **안 실었다** —
두 기계가 갈리던 까닭이 보고자의 열 이름이 아니라 **`record.py` 의 별칭에 「요약」이라는
낱말 자신이 빠져 있던 것**이었고, 그것을 고치자 실린 판본이 그대로 29/29 로 선다.
**보고자가 스키마의 칸 이름을 그대로 쓴 것이 옳았다** — 고칠 자리가 하네스였다.

## ~~소유자에게 물을 것 하나~~ — **답받았다: 「집행한다」** ⟨2026-09-12⟩

★★ **답은 `intent.md` `## 승격` 칸 12 가 진다 — 여기 두 번 안 적는다.** 아래는 물음이
무엇이었는지의 기록이고, 그 아래 ⑴⑵ 두 줄은 **여전히 유효한 재료**다.

<details><summary>올릴 때 적어 둔 것</summary>

### 소유자에게 물을 것 하나 — 판 4 의 합이 올렸다 ⟨2026-09-12⟩

⚠ **종료 판단도 같은 물음을 냈다**(`r2-referee.md` 판 4 절) —
*"금지역 `A4` 를 해소하지 않은 채 판 4 의 완수 조건 판정을 확정해도 되는가, 아니면 `A4`
해소를 판정 확정의 선결로 두는가? **이것이 이 승격의 유일한 물음이다** — 나머지 여섯은
거짓신호·미관이므로 소유자 결정 없이 메인이 초안 수정으로 처리한다."*

**`A4` = 금지역.** 설계 반증 조항 8(`dialectic/4-design.md:242-243` — *"그 판은 **무효이고
다시 돌린다**"*)이 판 4 라운드 2 에 대해 **문자 그대로** 발화했다 —
`dialectic/preflight.md:60` 이 게이트 파일을 **이름으로** 핀에 싣는데(`306ad7ec…`)
지금 `fb7e5a13…` 이다.

★★ **그리고 합이 핀 40 개를 전수로 다시 재서 칸 9 의 전제가 틀렸음을 찾았다.**
칸 9 는 면제 범위를 **다섯**으로 이름 붙였는데 실제로 어긋나는 것은 **일곱**이고,
그중 `dialectic/preflight.md` 는 **구조상 만족 불가능한 자기 핀**(자기 해시를 자기 안에
적을 수 없다)이라 빼면 **여섯**이다. **빠진 하나가 하필 `C4` 가 재는 게이트 파일이다.**

> **소유자가 정할 것** — 조항 8 을 이번엔 **집행할 것인가**(상한 2/2 를 다 썼으므로
> 라운드를 더 주는 결정이 따라온다), 아니면 **면제하고 대가를 한 번 더 적을 것인가.**

곁들여 소유자가 알아야 할 것 둘 ⟨합의 문장 그대로⟩:
⑴ 그래도 **`C4` 의 값은 어느 핀에서 재도 반증이다** — `effect/79-delta.md:23` 의 「여섯」과
게이트의 「12」는 핀 안팎 어디서 봐도 갈린다.
⑵ **`C4` 를 통과로 되돌리는 길은 문서 한 행이다** — 게이트 `## 효과` 표에 ㉳ 행을 더하고
`:112`·`:117`·`:119` 를 고치면 닫힌다. 그것은 `E2` 의 재작성이고 **메인의 자리**다.

</details>

★ **⑵ 가 곧 B 다.** 그리고 ⑵ 가 참이라는 것이 **조항 8 을 집행하는 근거**가 됐다 —
반증의 근거가 메인이 이제 고칠 자리에 있으므로, 고치기 전의 판정을 확정하면 원장이
**없어진 거짓을 반증으로** 적게 된다. 소유자가 그 길을 안 골랐다.

## 발동문의 본보기 — **판은 다 닫혔다. 형식만 남긴다**

⚠⚠ **이 절의 발동문은 판 4 라운드 **2** 의 것이고 그 라운드는 조항 8 로 **무효**다.
다시 띄우지 마라.** 남겨 두는 까닭은 **형식** 하나다 — 무엇을 주고 무엇을 안 주는지,
「무겁게 볼 자리」를 어떻게 지목하는지, 금지를 어떻게 거는지.

★★ **라운드 3 에서 배운 것을 먼저 적는다 — 다음에 판을 열면 이것부터 고쳐라:**

1. **설계문의 「안 주는 것」 칸을 발동문 쓰기 전에 열어라.** 라운드 3 에서 **두 번** 넘겼다
   (`4-design.md:151` · `5-design.md:223-224`). 둘 다 에이전트가 잡았고 소유자가 칸 14 로
   **유효 확정 + 대가 기록**을 정했다. **같은 자리에서 세 번 나면 그것은 형태가 아니라 자리다.**
2. **핀을 라운드마다 다시 박아라** — `dialectic/preflight-r3.md` · `5-preflight-r2.md` 가
   본보기다. **자기 파일을 자기 핀 목록에 넣지 마라**(구조상 만족 불가능하고 칸 9 의 셈을 흐린다).
3. **과업문을 보존할 자리가 없다** — 판 4 의 합이 그것을 `## 내가 못 정한 것` 첫 줄로 적었다.
   다음 회차가 그 자리를 세우면 이 계열의 이탈이 **기계에 보이게** 된다.

<details><summary>형식의 본보기 — 판 4 라운드 2 의 발동문(무효)</summary>

```
정반합 판 4 · 라운드 2/2 의 합(合)이다. 판정한다.
산출은 dialectic/4-synthesis-r2.md 로 바이트 그대로 보존된다.

판정 대상 — 완수 조건 C4 의 문면 전문:
- [ ] C4 ⟨정반합⟩ 차이마다 방향(나아졌다 / 나빠졌다 / 모른다)과 「touch 가 말한 것이
  참이었나」가 근거와 함께 적혔다. ⚠ 방향은 합격선이 아니다
모집단을 정하는 딸린 조건:
- [ ] C1 ⟨결정론적⟩ 재는 자리 각각에 사전 등록 ↔ 실제 변경의 차이 목록이
  effect/<이슈>-delta.md 로 섰고, 차이마다 좌표 두 끝이 붙었다

받는 것 — 초안 dialectic/4-thesis-r2.md ⟨C4 = 반증⟩ · 반론표 dialectic/4-antithesis-r2.md
⟨반론 7 건 A1~A7 · 스스로 물린 것 5 · 「규약 위반으로 스스로 적어 두는 것」 절⟩ ·
설계문 dialectic/4-design.md · preflight dialectic/preflight.md ·
intent.md 전문(## 차선책·## 범위 밖·## 금지역·## 상한·## 승격 포함 — 칸 3·4·5·6·9 가 전제다) ·
docs/gates/effect-confirmation.md 전문 · effect/*-delta.md 셋 · effect/*-readnote.md 셋 ·
저장소 읽기 · git·grep·shasum.

네가 정할 것 넷 — ① C4 의 판정값(넷 중 하나 + 근거 좌표 + 「이 판정을 뒤집는 것」 한 줄)
② 반론마다 채택·기각(근거 필수 · 반이 매긴 해악도를 그대로 받을지 올릴지 내릴지도)
③ 확장 판단(기본값 안 늘린다 · 뒤집는 시험은 자기 완결성 하나) ④ 소유자에게 올릴 것.

⚠⚠ 무겁게 볼 자리 셋:
- A1 — 설계문 :252-255 가 ⟨결정론적⟩ 조건(E1·E2 포함)의 산출을 "사실로 받는다. 그 산출이
  틀렸다는 반론은 발견이지 이 판의 판정값이 아니다" 로 못 박았다. 초안이 든 사유 셋 중
  「게이트가 12 라 적었다」가 E2 의 산술이라면 그것은 C4 의 판정값이 아니라 발견으로 가야
  한다. ★ C4 가 실제로 무는 것이 무엇인지 네가 가려라.
- A2 — 죽은 좌표(effect/negative-79.txt)의 해악도가 금지역인가 거짓신호인가. 반이 같은
  게이트의 윗절을 근거로 든다 — 이 회차가 D4-a 에서 금지역을 "돌아가는 코드가 거짓을
  주장하는 자리" 로 한정하고 동결 문서·합격선 파일의 거짓은 "안 고친다 — 거짓이 되는
  자리를 적는 것이 처분" 으로 처리했다. ⚠ 반이 "최종 확정은 cargo xtask check 를 돌려
  봐야 갈린다 — 나는 안 돌렸다" 라 적었다. 너도 돌리지 마라 — 그 사실과 명령을 옮겨라.
- A4 — preflight 의 핀 목록이 게이트 파일을 이름으로 싣는다(preflight.md:60 · 306ad7ec…
  ↔ 지금 fb7e5a13…). 그러면 조항 8 의 첫 방아쇠가 문자 그대로 걸린다. 초안이 그것을
  스스로 면제했는데 면제는 소유자의 자리라는 것이다. ★ 소유자 칸 9(intent.md)의 면제가
  「파일 다섯의 목록」인지 「사유의 일반화」인지 네가 읽어라.

⚠ 반이 스스로 적은 오염 하나 — 저장소 전수 grep 을 걸다가 state.md 의 네 줄이 물렸고
본문은 안 열었으며 어느 반론의 근거로도 안 썼다고 적었다. 반이 그 사실을 넘긴 까닭은
"합이 「초안의 사유가 새로운가」를 잴 때 이 오염을 알고 있어야 하기 때문" 이다.
★ 그것을 알고 판단해라 — 다만 state.md 는 너도 열지 마라.

⚠ 새 실행 금지 — 빌드·시험·pal 호출·cargo xtask check 를 하지 마라.
git·grep·shasum·ls·읽기는 된다. 반이 "돌려 봐야 갈린다" 로 적은 것은 그 사실과 명령을
판정문에 옮겨라.

반환 형식 — 설계문 ## 역할별 입력 의 합(合) 절이 요구하는 형식. 채택 표와 기각 표를
나누고 채택 표에는 해악도를 실어라(종료 판단이 그 분포만 받는다).
```

그 뒤 **종료 판단**(`pal-debate-referee`)을 띄운다 — 주는 것은 **설계문의 상한·끝 조건 절 ·
살아남은 반론의 해악도 분포 · 쓴 라운드 수(2/2)** 뿐이고 **판정문 본문·판정값은 안 준다.**
산출은 `dialectic/r2-referee.md` 에 판 4 절로 덧붙인다.

</details>

## 실패한 접근 — 다시 밟지 마라

| 무엇 | 왜 안 되나 |
|---|---|
| **`#102` 를 표본으로 쓰기** | 본문에 *"답해야 할 것"* 이 넷이고 전부 설계 결정이다. 회차를 삼킨다. `#79` 로 바꿨다 |
| **`C2-a` 를 「판정자에게 touch 산출을 안 준다」로 세우기** | 안 주면 `touch/<이슈>.txt:N` 을 원리상 못 대므로 귀속 0 이 **구성상 보장된다** — 항등식이다. **미끼(다른 심볼의 touch 산출)** 로 세웠다. ⚠ **그 미끼도 그 뒤 버려졌다** — 사전부검 R2·R3 가 「같은 파일 두 심볼의 차이가 원리상 다섯 가지뿐」임을 보였고, 지금 `C2-a` 의 미끼는 **「사전 등록에 이미 적힌 사실을 담은 줄」**이다 |
| **눈가림을 설계하기** | 규약 §7 이 실측으로 못 박았다 — *"처치가 행동을 바꾸면 그 행동이 곧 팔을 말한다."* 시도하지 않는다. 사전 등록이 그 자리를 대신한다 |
| **`cargo test` 의 출력으로 컴파일 경고를 재기** | 전량 캐시 적중이라 `Compiling` 줄이 0 이었다. 그 0 은 측정치가 아니다. `.rs` 전량 mtime 갱신 후 따로 쟀다 |
| **조건 본문을 고치고 `## 개정` 에 그 커밋에서 안 적기** | `834ba43` 이 hunk 여섯으로 조건을 크게 고치면서 개정 표에 **0 행**을 더했고, 다음 커밋 `6aa7622` 가 소급해 메웠다. 한 커밋 동안 「무엇을 왜 바꿨는지」가 파일 안에 없었다 — `PM2-11`(*고쳤다고 적고 안 고침*)의 **반대 방향**이다 |
| **앞 세션이 인용한 수를 그대로 믿기** | ⓐ·ⓑ 분모가 갈렸다(5767↔5760 · 1062↔1061). 결박도 34↔30 으로 갈렸다. **다시 쟀다** |
| **격리 사본을 아무 이름으로 뜨기** | 저장소 **디렉터리 이름이 `RepoId` 의 성분**이라 심볼 id 가 통째로 갈린다 — 사본에서 `repo@…#bc51ced…`, 원본에서 `palimpsest@…#a9dd338…`. 음성 대조 산출을 원본과 나란히 대려면 **사본 디렉터리 이름을 `palimpsest` 로** 둔다 ⟨`PM1-19`⟩ |
| **`cargo xtask check` 의 「완수 조건 설계 평가」 실패를 결함으로 읽기** | 그것은 `conditions-audit/r<n>-raw.md` 가 **아직 없어서** 난다. §3.5 를 돌리면 꺼진다. ⚠ **지금은 꺼져 있다** — `conditions-audit/r1-raw.md`·`r2-raw.md` 가 섰고 그 검사가 `대상 회차 5개 · 평가 반환문 5개` 로 초록이다. 회귀 기준선은 착수 시점과 같은 **28/28** 이다 |

## 걸리는 자리 — 앞 회차가 값을 치르고 배운 것

1. **`report.md` 를 쓰는 순간 검사 기준이 올라간다** — 「발견이 닫혔나」가 열린 발견 *전부*를
   요구하고 「어색한 표현 부재」의 모집단에 이 회차 문서와 게이트가 들어온다.
   **종료 문서를 쓰기 전에 발견을 다 닫고 표현을 훑어라.**
2. **원장의 `경로` 와 `처분자리` 는 다른 축이다** — 앞은 관측된 자리, 뒤는 처분이 닿은 자리.
3. **선언보다 값을 먼저 세워라** — 주석·사유만 고치면 독립 리뷰가 다시 잡는다.
4. **음성 대조 자신이 항등식일 수 있다** — 세운 뒤 **끄면 정말 빨개지는지 돌려 봐라.**
5. **`#[test]` 와 함수 사이에 문서 주석을 끼우지 마라** — 그 시험이 속성을 잃고 한 번도 안
   돈다. `cargo test` 는 초록이고 **빌드 경고만** 난다. 경고를 함께 봐라.
6. **벤치를 잴 때는 기계를 비워라** — 서브에이전트가 같은 기계를 쓰면 `f04-verify.py` 의
   ⑨ 가 흔들린다.
7. **`state.md` 의 신선도를 재는 검사가 없다** — `cargo xtask check` 28 개 중 이 파일이
   회차의 실제 단계와 맞는지 보는 것이 하나도 없다. 그래서 네 걸음 뒤처진 채로 28/28 이
   초록이었다. **이 회차의 범위 밖이라 안 만든다** — 다만 여기 적어 다음 회차가 집게 한다.
8. **원장 `CA2-21` 은 실측으로 닫혔다** — 독립 검증이 `cargo test --workspace` 를 완주시켜
   `test result:` **48 줄 · passed 합 1028 · Doc-tests 6 묶음 · 실패 0** 을 얻었다. `B3` 의
   셈법은 문면 그대로 맞고 *"안 맞는 것 아닌가"* 는 **거짓**이다 — 기각이 참이었다.
   ⚠ **레코드의 `유효성` 은 `추정` 그대로 둔다** — 남의 판정 필드를 사후에 고치는 자리가
   규약에 없다. 그래서 **기각을 `유효성` 으로 세면 9, `처분` 으로 세면 10** 이 그대로다.
9. **기록 정합은 다섯 판에서 닫았다** — 독립 검증이 6·6·5·9·6 을 냈고 **네 판에서
   「앞 판의 수정이 새로 넣은 줄」이 다시 걸렸다.** 두 형태가 반복됐다: ⓐ **범위를 좁혀
   고치면 고친 자리만 깨끗해진다**(세 번) · ⓑ **줄 번호로 자기 표를 가리키면 표가 길어질
   때마다 밀린다**(세 행). ⓑ 는 ⟨라운드 태그⟩ + 행 이름 참조로 바꿔 막았다.
   **넘긴 것 셋 중 하나는 닫혔다** — `A7-a` 의 자기 판정값은 ⟨승인 앞 정정⟩ 이 달았다
   ⟨`MS-05`⟩. 남은 둘은 그대로다: `CA2-21` 의 `유효성` 필드 · 「조건 열둘」의 실수(감사자
   반환문의 수라 표시만 달았다).
10. **무시 대상을 좌표로 적으면 로컬만 초록이다** — 원장의 `경로` 가 `.gitignore` 가 막는
   파생물(`index.redb`·`intent.redb`·`cache/`·`target/`)을 가리키면, **그 파일이 있는 기계에서는
   검사가 통과하고 갓 클론한 나무에서는 실패한다.** 같은 커밋이 기계마다 다른 판정을 받는
   자리이고 ADR-0023 이 금지한 형태다. 지금은 검사가 면제하고 수를 싣지만, **그 면제는
   추적 중인 좌표를 삼키지 않는다**(`git check-ignore` 는 추적 중인 파일을 무시로 안 본다 ·
   음성 대조 네 자리가 `xtask` 시험에 박혔다). ⟨`MS-04`⟩
11. ★★ **백틱 코드 참조는 「죽은 링크 부재」가 원리상 못 잡는다** ⟨2026-09-12 · 판 5 R2⟩ —
   그 검사는 마크다운 링크(`[…](…)`)만 잰다. 게이트가 `docs/adr/0027-rust-first-class.md`
   (**없는 파일**)를 백틱으로 적어 두어 **아무도 그 문서를 안 열었고**, 그 위에 세운 전칭
   부정이 한 번도 안 재어진 채 사실로 적혔다. **경로를 근거로 댈 거면 마크다운 링크로 적어라** —
   그러면 검사가 잡는다. 이 회차가 `E6` 에서 실제로 그렇게 잡혔다.
12. ★ **판정문에 「이 판정을 뒤집는 것」을 적었으면 그 검사를 실제로 돌려라** ⟨같은 곳⟩ —
   적어 두기만 하면 **「측정이 죽은 가지」**다. 이 회차는 그것을 두 자리에 적었고 한 자리는
   안 돌렸다. **돌린 자리(「아직」이 요구하는 둘)는 실제로 값을 냈다** — 소유자의 문장이
   적힌 그대로 그 자리를 닫았다. 장치가 값을 낸 실례와 안 낸 실례가 한 회차 안에 다 있다.
13. **전칭 부정(「어느 것도 …않는다」)을 적을 때는 모집단 전수를 열었는지 먼저 확인해라** —
   셋 중 하나의 경로가 죽어 있으면 그 문장은 **둘만 보고 셋을 말한 것**이다.
14. **CI 는 `cancel-in-progress: true` 다** — 커밋을 여럿 내되 **push 는 한 번** 한다.
   종료 판정은 **회차의 마지막 커밋 SHA 에 `conclusion=success` 런이 붙었나**로 잰다.
