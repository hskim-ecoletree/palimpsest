# 잠긴 의도 — 효과 확인

> 회차 `2026-09-11-effect-confirmation` · 착수 커밋 `8604d62`

## 원문

소유자가 2026-09-11 에 쓴 것 — **압축하지 않는다.**

> [이 회차에서 열 것 — 최단 경로 §4 의 3 단계「효과 확인」]
> 전제는 `#66` 이다. 그 문서가 적는다: *"이 저장소에서 `pal touch` 를 실제 작업 앞에 부르고,
> 그 산출을 본 뒤의 변경이 달라졌는지 잰다"* · 합격선은 *"그 산출을 보고 달라진 변경 1 건
> 이상, 좌표와 함께"*. 그리고 *"이 단계는 한 번도 실행된 적이 없다"*.
>
> ⚠ 이것은 **내(소유자)가 고른 것이다.** 순서표 §2 의 다음은 3 번(결박 반경과 낡음 전파)
> 이고 최단 경로의 다음은 3 단계라 둘이 갈렸고, 위임 조건이 성립하지 않아 내가 정했다.
> 그 사실을 잠긴 의도의 `## 목적 기여` 에 적어라.
>
> [이번엔 열지 마라 — 다음 회차의 몫이다]
> `#136`(엣지가 ⓐ/ⓑ 갈래와 참조 좌표를 안 싣는다) · `#137`(조건 파서가 줄 단위) ·
> `#138`(F04 벤치가 선의 경계) · `#139`(심볼이 가시성을 안 싣는다) ·
> `#135` 의 남은 ⓑ 몫 · `#131`(Kotlin) · 정확도 잔여 `#133 #78 #77 #57 #63~#65 #58`.

인터뷰 세 라운드가 잠근 것 열하나 — **상한 3 에 닿아 닫았다.** 범주 다섯(경계·의도·자율·
종료·재고)이 전부 한 번씩 열렸다.

> 「실제 작업」을 무엇으로 잠그나? → **프론티어의 열린 이슈를 골라 실제로 고친다**
> 인과를 무엇이 증언하나? → **사전 등록 — 보기 전 계획을 커밋으로 봉인한다**
> 몇 건을 재나? → **하한 1 을 지키되 상한을 3 으로 잠근다**
> 장치를 만드나? → **(가) 안 만든다 — 일회성으로 돌리고 수만 남긴다**
> 어느 이슈 위에서 재나? → **`#102` · `#126` · `#129`** ⚠ **`#102` 는 착수 전에 `#79` 로 바뀌었다 — 아래 `## 개정`**
> 「달라진 변경」의 방향을 재나? → **문면대로 「달라졌다」만 재고, 방향은 따로 적는다**
> `#66` 을 어떻게 처분하나? → **이 회차가 닫는다 — 두 물음에 답하고 낡은 전제를 정정한다**
> 어디서 멈추고 올리나? → **세 자리를 다 재고도 차이가 0 이면 멈춘다**
> 사전 등록을 쓰기 전에 얼마나 조사하나? → **평소대로 조사하고, 무엇을 봤는지 기록한다**
> 라운드 상한 → **인터뷰 3 · 사전부검 2 · 조건평가 2 · 정반합 판마다 2 · 독립리뷰 3**
> 이 회차가 무엇까지 주장하나? → **「이 저장소에서 섬」까지만 주장하고 R-19 를 범위 밖에 적는다**

## 목적 기여

[`00-goals.md`](../../../docs/plan/00-goals.md) §2 의 목표 **1(큐레이션)** 과 **2(컨설팅)** —
둘 다 **P1** 이고 그 문서가 *"첫 릴리스의 정의: 1과 2가 **실사용에서 값을 내면** 제품이다"*
로 잠근다. 이 회차가 재는 것이 바로 그 「실사용에서 값을 내나」다.

그리고 §0.1 의 잠긴 문장이 이 회차의 자다:

> **역사·결정을 알게 하는 것은 목적이 아니라 수단이다. 상위 가치는 그 결과로 더 나은
> 코드가 나오는 것이다.**

**왜 지금 그것이 다른 것보다 먼저인가** — `AGENTS.md` 의 소유자 지시가 판정자다
(*"이 순서가 다른 모든 우선순위를 이긴다"*):

> **빠르게 완성한다 → 처음의 의도에 도달한다 → 효과를 본다 → 그 뒤 계속 고친다.**

1·2 단계가 섰으므로(`#130` 2026-09-08 · `#135` 2026-09-10) **「효과를 본다」가 다음 자리**다.
`03-shortest-path.md` §4 의 3 단계가 그 자리이고 그 칸이 스스로 *"이 단계는 한 번도 실행된
적이 없다"* 를 적는다.

★★ **이 자리는 소유자가 골랐다 — 메인이 아니다.** 소유자 원문 그대로:

> ⚠ 이것은 **내(소유자)가 고른 것이다.** 순서표 §2 의 다음은 3 번(결박 반경과 낡음 전파)
> 이고 최단 경로의 다음은 3 단계라 둘이 갈렸고, 위임 조건이 성립하지 않아 내가 정했다.

위임 조건은 `02-order.md` 가 적는다 — *"다음에 첫 자리가 비면 순서표와 최단 경로가 **같은
자리를 가리키는 한** 메인이 지목하고 소유자는 보고를 받는다."* 둘이 갈렸으므로 그 조건이
성립하지 않았고, 그래서 소유자가 정했다.

## 상한

- 인터뷰 **3 라운드** — 소진했다(2026-09-11 · 라운드 3 에서 닫았다).
- 사전부검 **2 라운드**
- 완수 조건 설계 평가 **2 라운드**
- 완수 조건 검증 정반합 **판마다 2 라운드**
- 독립 리뷰 **3 라운드**

⚠ 앞 회차는 독립 리뷰가 4 였다. 이 회차는 산출이 코드보다 **측정 기록** 쪽이라 3 으로
줄였다 — 상한은 손실 제한이지 안전 보증이 아니다(§11 ①).

## 금지역

**`.claude/pal/policy.toml` 이 없다** — `.claude/pal/` 디렉터리 자체가 없고
`find . -name 'policy.toml' -not -path './target/*'` 가 빈 출력이다(착수 관측 §5.3).
그러므로 규약의 **기본 다섯**을 쓴다 — 데이터 손실 · **측정이 죽은 가지** ·
**사실이 아닌 것을 사실로** · 인증 · 대표 취약점.

★ 이 회차는 가운데 둘에 특히 가깝다. 효과 측정이 항등식이면 「측정이 죽은 가지」이고,
귀속을 근거 없이 적으면 「사실이 아닌 것을 사실로」다.

⚠ **회차 중에 금지역을 늘리지 않는다.**

## 재는 자리 셋

| # | 이슈 | 무엇 | 왜 이 자리인가 |
|---|---|---|---|
| ㉠ | **`#79`** | `identity_ceiling` 이 `min` 에 삼켜져 「순서에 취약한 것」과 「그냥 L1 인 것」이 같은 글자가 된다 | 제품 코드(`crates/pal-cli/src/ledger.rs:334`)이고 파일을 넘는 자리라 **역방향 색인이 쓰이는** 표본 |
| ㉡ | **`#126`** | 「최근 끝난 회차」를 사전순으로 고르는 자리가 하나 더 있다 — `check_ledger_pair` | **중복된 규칙의 둘째 자리**라 *"여기를 바꾸면 무엇이 깨지나"* 그 자체 |
| ㉢ | **`#129`** | `pal query narrative.unbound` 가 읽기 트랜잭션에서 쓰려 해 실패한다 | **단일 자리**라 *"touch 가 쓸모없었다"* 가 나올 수 있는 **음성 방향의 표본** |

★ **셋의 모양을 일부러 다르게 골랐다.** 하나만 맞춰 둔 표본이면 결과가 구성상 정해진다.

⚠⚠ **세 심볼 전부 결박이 0 이다** ⟨사전부검 R1 · `PM1-02`⟩. `nodes_of` · `identity_ceiling` ·
`check_ledger_pair` 어느 것도 결박 34 건의 대상이 아니다. **그러므로 이 회차가 재는 것은
`00-goals.md` §2 의 목표 2(컨설팅 — *"여기를 바꾸면 무엇이 깨지나"*)이고, 목표 1(큐레이션 —
*"왜 이렇게 되어 있나"*)은 이 표본 위에서 원리상 안 재어진다.** 표본을 안 바꾸고 축을
명시한다 — 소유자가 고른 자리이고, 컨설팅 축은 P1 이라 그 자체로 목적 기여가 선다.

⚠ **이 구별은 「못 읽었다」와 다르다.** 작업 저장소에서 결박 0 은 **진짜 0** 이다 —
격리 사본에서는 같은 명령이 *"못 읽었습니다 — 「0 건」이 아닙니다"* 를 내므로 갈린다.

## 순서 — 못 박는다 ⟨사전부검 R1 · `PM1-17`⟩

**㉡ 을 가장 먼저 닫고, 게이트·종료 문서는 그 뒤에 쓴다.**

까닭: ㉡ 의 대상 `check_ledger_pair` 가 **「원장 둘 대조」 검사 그 자체**이고, 그 검사가
`E1`·`E4`·`B2` 를 판정한다. `report.md` 를 쓰는 순간 하한이 이 회차로 옮겨 오고 **그때
판정하는 코드가 이 회차가 방금 고친 코드**가 된다. 규약 §11③ 은 자기 장치를 본 모집단에서
빼되 *"금지역에 닿으면 본 모집단이다. 미룰 수 없다"* 로 예외를 둔다 — 여기가 그 예외다.

**그래서 ㉡ 을 고치기 전과 후에 `cargo xtask check` 를 각각 돌려 두 산출을 보존한다**
(`observations/ledger-pair-before.txt` · `-after.txt`). 그것이 「고친 검사가 자기를
통과시켰나」를 사후에 대는 유일한 길이다.

## 완수 조건

### A — 절차가 실제로 섰나 ⟨결정론적⟩

- [ ] **A1** 재는 자리 각각에 사전 등록 `plan/<이슈>-pre.md` 가 있고, **그 파일을 실은 커밋이 그 자리의 `touch/<이슈>.txt` 를 실은 커밋보다 앞선다** — `git log --format=%H --reverse -- <경로>` 로 판정한다
- [ ] **A1-b** **봉인이 내용까지 잠근다** — `touch/<이슈>.txt` 머리에 **그 시점 `plan/<이슈>-pre.md` 의 blob 해시**(`git rev-parse HEAD:<경로>`)가 적혔고, 그 해시가 **봉인 커밋의 blob 과 같다.** ⟨`PM1-05` — 순서만 잠그면 빈 껍데기를 먼저 커밋하고 touch 를 본 뒤 채우는 길이 A1·A3 을 둘 다 통과한다. 격리 저장소에서 실제로 심어 재현됐다⟩
- [ ] **A1-a** ⟨A1·A1-b 의 음성 대조⟩ 순서를 뒤집은 사본과 **내용을 사후에 채운 사본** 둘에서 판정이 각각 **뒤집히는지** 재고, 산출을 **`effect/negative-A1.md`** 로 남긴다. 안 뒤집히면 A1·A1-b 는 항등식이다
- [ ] **A2** 재는 자리 각각에 `pal touch <심볼>` 의 **전 출력**이 `touch/<이슈>.txt` 로 보존됐고, 머리에 **부른 명령 전문 · 그때의 HEAD SHA · A1-b 의 blob 해시**가 적혔다
- [ ] **A3** 재는 자리 각각의 사전 등록이 **좌표를 적었다** — 바꿀 `파일:줄` 목록과 각 자리에 무엇을 쓸 것인가. 「`#79` 를 고친다」 같은 한 줄은 A3 을 못 채운다
- [ ] **A3-a** ⟨A3 의 음성 대조⟩ 좌표 줄을 지운 사본이 A3 판정을 **통과하는지** 재고 산출을 **`effect/negative-A3.md`** 로 남긴다. 통과하면 A3 은 아무것도 안 잰다
- [ ] **A4** 재는 자리 각각의 사전 등록에 **touch 를 부르기 전에 무엇을 봤는지**가 적혔다 — 읽은 파일 경로 · 돌린 검색 · 본 이슈 · **그리고 「사전부검이 흘린 것」**(착수 관측 §5.8 의 표)

### B — 실제 작업이 실제로 섰나 ⟨결정론적⟩

- [ ] **B1** 재는 자리 각각에 **코드 변경 커밋**이 있고, 그 이슈 본문이 요구한 것을 만족한다
- [ ] **B1-a** **㉢ 의 RED 를 관측했다** — `pal query narrative.unbound` 의 실패는 **`.palimpsest/intent.redb` 가 없을 때만** 난다. 작업 저장소에는 그 파일이 있어 고치기 전에도 초록이다. **그 파일이 없는 자리에서 RED 를 관측하고 산출을 `observations/red-129.txt` 로 남긴다**, 그리고 고침을 재는 시험도 같은 조건에서 돈다 ⟨`PM1-04`⟩
- [ ] **B2** `cargo xtask check` 가 초록이다 ⟨회귀 조건 — 착수 시점 **28/28**⟩
- [ ] **B3** `cargo test --workspace` 가 초록이고 **통과 수가 착수 시점 1028 이상**이다 ⟨회귀 조건⟩. 줄면 시험을 지운 것이다
- [ ] **B4** 컴파일 경고가 **0** 이다 ⟨회귀 조건 — 착수 시점 0⟩. `.rs` 전량 mtime 갱신 후 `cargo build --release --workspace` 와 `cargo check --workspace --all-targets` **각각**으로 잰다 — 증분 빌드는 경고를 다시 안 뱉는다
- [ ] **B5** `#79` 가 요구한 것을 만족한다 — *"닫으려면 그 수를 내는 산출 경로가 필요하다. 손으로 센 수를 판정 표에 실으면 다음 회차가 그것을 못 되짚는다."* 「순서에 취약한 것」과 「그냥 L1 인 것」을 가르는 수가 **명령으로** 난다
- [ ] **B5-a** **B5 의 모집단 정의가 사전 등록에 먼저 적혔다** — 무엇을 분모로 삼는지. ⟨`PM1-21` — 안 적으면 새 명령이 내는 **어떤 수든** B5 를 채운다. `#79` 본문의 「464 · 7,156」은 합이 7,620 이고 지금 스냅샷의 좌표 3,306 과 안 맞는다 — 모집단이 다르다는 뜻이고 그것이 정의돼야 한다⟩
- [ ] **B6** ㉡ 의 고침이 **커밋이 0 인 회차**(예: 착수 시점의 이 회차 자신)를 어떻게 처분하는지 코드와 판정문에 명시돼 있다 ⟨`PM1-16` — 「커밋 시각」으로 고치면 미커밋 항목이 미정의가 되고, 고치는 손이 자기 입력을 만들고 있다⟩

★ **B2·B3·B4 는 RED 가 없다 — 착수 때 이미 초록이다.** 그래서 **회귀 조건**이라고 적었다.
착수 시점 값을 나란히 적어 「0 개를 훑고 통과하는 검사」가 되는 것을 막는다.

### C — 차이를 쟀나 ⟨결정론적이 아니다 → 정반합⟩

- [ ] **C1** 재는 자리 각각에 **사전 등록 ↔ 실제 변경**의 차이 목록이 `effect/<이슈>-delta.md` 로 섰고, 차이마다 **좌표 두 끝**이 붙었다 — touch 산출의 어느 줄(`touch/<이슈>.txt:N`)이 원인이고 어느 `파일:줄` 이 결과인가
- [ ] **C2** 그 차이 중 **touch 산출에 귀속되는 것이 1 건 이상**이고, **귀속된 줄이 「심볼 고유 줄」이다** ← **합격선 본체**
- [ ] **C2-b** **「심볼 고유 줄」이 먼저 기계로 정의됐다** — 진짜 산출과 미끼 산출을 `comm` 으로 대어 **공통 줄을 뺀 나머지**가 그것이고, 그 목록이 `effect/<이슈>-specific-lines.txt` 로 **판정 전에** 섰다 ⟨`PM1-07` — 두 산출이 **38/52 줄 동일**하다. 공통 줄에 귀속하면 미끼에도 귀속이 나므로 그 귀속은 심볼에 대해 아무것도 안 말한다⟩
- [ ] **C2-a** ⟨C2 의 음성 대조⟩ 미끼 팔의 귀속이 **미끼의 심볼 고유 줄에 대해 0** 이다. 산출은 `effect/negative-C2.md`. 미끼에도 고유 귀속이 나면 판정이 확증편향이다
- [ ] **C2-c** **미끼는 같은 파일 안의 다른 심볼**이고, 그 선정 규칙이 **사전 등록에 함께 봉인**됐다 ⟨`PM1-06` — `A2` 가 머리에 명령 전문을 적게 하므로 미끼의 심볼 이름이 한 줄에 드러난다. 다른 파일의 심볼을 쓰면 판정자가 머리만 읽고 무관함을 알아 **구성상 귀속 0** 이 된다. 같은 파일이면 그 길이 막힌다⟩
- [ ] **C3** 귀속 판정을 한 것은 **새 에이전트**이고 사전등록·touch 산출(또는 미끼)·실제 변경·「사전부검이 흘린 것」 넷만 받았다. **프롬프트에 답을 안 담았다** — 프롬프트 전문을 `effect/judge-prompt.md` 로 보존한다
- [ ] **C3-a** **팔마다 판정자를 따로 띄웠고 인스턴스를 섞지 않았다** — 진짜 팔과 미끼 팔에 **각각 새 인스턴스**를 쓰고, 몇 개를 띄웠는지 `effect/judge-roster.md` 에 적는다 ⟨`PM1-08` — 같은 인스턴스에 둘을 주면 둘째가 오염되고, 팔마다 하나면 「미끼가 정보를 안 실었다」와 「이 판정자가 더 엄했다」를 원리상 못 가른다⟩
- [ ] **C4** 차이마다 **방향**(나아졌다 / 나빠졌다 / 모른다)과 **「touch 가 말한 것이 참이었나」**가 근거와 함께 적혔다. ⚠ 방향은 합격선이 **아니다** ⟨`PM1-03`·`PM1-20` — `pal touch identity_ceiling` 의 **호출자 0 은 거짓 음성**이다(`x.foo()` 를 안 센다). 방향을 안 재면 **touch 가 잘못 이끈 변경도 C2 를 채운다**⟩
- [ ] **C5** **차이가 0 인 자리도 그대로 적혔다.** 재는 자리 전부 기록되고 0 을 뺀 자리가 없다

★ **`C2-a` 를 「판정자에게 touch 산출을 안 준다」로 세우지 않았다.** 안 주면 판정자가
`touch/<이슈>.txt:N` 을 원리상 못 대므로 귀속 0 이 **구성상 보장된다** — 그것은 음성
대조가 아니라 항등식이다(규약 §7 이 이 저장소에서 두 번 잡은 형태).

★★ **그리고 `C2-b`·`C2-c` 가 미끼를 실패할 수 있게 만든다.** 미끼는 **같은 파일의 다른
심볼**이라 머리로 안 갈리고, 귀속은 **공통 줄을 뺀 고유 줄**에만 센다. 그러면 두 방향이
다 열린다 — 미끼에서 고유 귀속이 나면 확증편향이 드러나고, 진짜에서 고유 귀속이 하나도
안 나면 `C2` 가 반증된다.

### D — `#66` 의 두 물음 ⟨D1 결정론적 · D2 정반합 · D3 선언⟩

- [ ] **D1** `#66` 본문의 낡은 전제 둘 — 「결박 가능한 좌표 7 개 · 파일 1 개」 · 「추출기는 Kotlin·Java·JavaScript·TypeScript 넷뿐」 — 을 **실측값으로 정정했다.** 지금 값과 **무슨 명령으로 쟀는지** 함께
- [ ] **D2** `#66` 의 물음 1 — *"이 도구가 자기 자신을 큐레이션할 수 있어야 하는가"* — 에 답했다. ⚠ **이것은 규범 명제라 측정이 혼자 못 답한다** — 이 회차의 측정을 **근거로** 대고 판정은 정반합이 한다 ⟨`PM1-14`⟩
- [ ] **D3** `#66` 의 물음 2 — *"그렇다면 **Rust 추출기**인가, 아니면 ⑧을 **다른 저장소의 실사용**으로 다시 정의하는가"* — 에 **선언으로** 답했다. ⚠ **측정이 아니다** — 앞 갈래는 `#130`·ADR-0027 로 **이미 섰고**, 뒤 갈래는 이 회차가 `## 범위 밖` 에 스스로 뺐다. 그 사실을 적는 것이 이 조건이다 ⟨`PM1-14`⟩
- [ ] **D4** `#66` 을 닫았다
- [ ] **D4-a** **`#66` 을 현재형으로 인용하는 자리의 처분이 적혔다** — `corpus/criteria.toml:10530`·`:10886` · `docs/gates/F12.md:312`·`:398` · `scripts/f12-verify.py:559-560`. ⚠ 어느 검사도 이슈 상태를 안 본다. **동결 문서(닫힌 게이트)와 합격선 파일은 안 고친다** — 어디가 거짓이 되는지 적는 것이 처분이다 ⟨`PM1-13`⟩

### E — 종료 ⟨결정론적⟩

- [ ] **E1** 게이트 `docs/gates/effect-confirmation.md` 가 절 이름 넷으로 섰고, 표준 표의 **검산이 맞다** — 넷의 합 = 등록 조건 수 · `intent.md` 의 조건 ID 집합과 **집합 같음**
- [ ] **E2** `## 효과` 에 **테스트도 CI 도 아닌 것이 이 회차의 산출을 돌린 출력**이 붙었고, **차이마다 방향이 나란히 적혔다** ⟨`PM1-20`⟩
- [ ] **E3** 종료 보고 `report.md` 가 섰고 **네 이름이 없다**
- [ ] **E4** `findings.jsonl` 의 **열린 발견이 0** 이고 열린 금지역·실패가 없다
- [ ] **E5** 회차의 **마지막 커밋 SHA** 에 `conclusion=success` 런이 붙었다
- [ ] **E6** **`docs/plan/03-shortest-path.md` §4 의 3 단계 행과 `docs/plan/02-order.md` 의 대응 줄이 이 회차의 결과로 갱신됐다** ⟨`PM1-12` — 안 하면 회차가 끝나도 그 문서가 계속 *"이 단계는 한 번도 실행된 적이 없다"* 를 적는다. 28 개 검사 중 이것을 잡는 것이 없다⟩

## 차선책

1. **`C2` 가 0 이면** — **반증으로 적고 멈춘다.** 더 재지 않는다. 인터뷰 2 의 답이 그렇게
   잠갔고, 규약 §9 가 *"반증은 실패가 아니다"* 를 못 박는다. 그 반증이 이 단계가 낸 가장
   값진 산출이다.
2. **`B5` 의 산출 경로가 이 회차에서 안 서면** — `#79` 를 안 닫고 *"그 자리는 touch 효과
   측정의 표본으로만 썼다"* 를 적는다. ⚠ **메인이 혼자 발동하지 않는다** — 원 의도의 일부를
   깎으므로 승격이다. ⟨사전부검 `PM1-15` 가 그 경로를 실물로 만들었다 — 제대로 세우면
   `crates/pal-core/src/touch.rs:35 SymbolNode` 와 `schema/graph.toml:45-60` 이 함께
   움직이고 그 자리는 `AGENTS.md` 가 **초석**으로 지목한 곳이다⟩
3. **재는 자리 중 하나가 회차를 삼키면** — 그 자리를 표본에서 빼고 **남은 자리로** 판정한다.
   **하한 1 은 지킨다.** ⚠ 승격이다.
   ★ **그래서 조건 문면을 「세 자리 각각」이 아니라 「재는 자리 각각」으로 적었다**
   ⟨`PM1-10` — 「세 자리」로 적으면 이 차선책을 쓰는 순간 조건 일곱이 문면상 동시에
   깨져서, **등록된 차선책인데도 승격 없이는 못 쓰게** 된다⟩

## 범위 밖

- **R-19 — 자기 저장소 자기적용의 편향.** 이 회차는 **「이 저장소에서 섬」까지만** 주장한다.
  재는 자와 재이는 자가 같은 저장소인 것은 이 회차가 **원리상 못 없앤다.**
- **★ 그보다 좁은 편향 하나가 더 있다 — 사전 등록을 쓰는 손과 실제 변경을 쓰는 손이
  같다** ⟨`PM1-18`⟩. 사전 등록은 **시각**을 봉인하지 **성실성**을 못 봉인한다. 얇게 쓰면
  `C2` 가 구성상 만족되고, 지나치게 두껍게 쓰면 touch 가 더 낼 것이 없어 `C2` 가 구성상
  0 이 된다. **이 회차는 그것을 못 없앤다** — `A3`·`A4` 가 좌표와 조사 기록을 요구해 폭을
  좁히는 것이 할 수 있는 전부다. 종료 보고의 `## 원리상 못 잰 것` 이 이것을 진다.
- **남의 저장소 실사용.** 코퍼스 저장소에는 「실제 작업」이 없어 합격선 문면을 벗어난다.
- **`pal touch` 아닌 표면의 효과.** `pal doctor` · `pal narrative` · 스킬의 효과는 안 잰다.
  ⚠ touch 가 스스로 가리킨 후속 질의는 **그 산출의 연장으로 센다.**
- **「나아졌다」의 판정 기준.** 방향은 `C4` 로 기록하되 합격선이 아니다.
- **`pal touch` 의 멤버 해소(`x.foo()`).** `identity_ceiling` 의 호출자 0 이 거짓 음성인
  까닭이고, touch 자신이 *"이 회차의 범위 밖"* 이라고 적는다. **이 회차도 안 고친다** —
  `C4` 로 그 거짓 음성을 기록만 한다.
- **Markdown 결박(`#69`).** 이 저장소 문서가 미결박인 것은 이 회차가 안 고친다.
- **「추출기가 몇인가」가 저장소 다섯 자리에서 갈린 것.** `D1` 이 `#66` 하나를 정정할
  뿐이다. 나머지는 동결 판정 문서·합격선 파일이고 앞 회차가 이미 `범위밖` 으로 처분했다
  (`2026-08-20-rust-extractor/findings.jsonl:44` `PM2-S14`).
- **소유자가 막은 열넷** — `#136` `#137` `#138` `#139` · `#135` 의 남은 ⓑ 몫 · `#131` ·
  `#133` `#78` `#77` `#57` `#63`~`#65` `#58`.
- **`#102` 의 설계 물음 넷.** 착수 전에 표본에서 뺐다 — 아래 `## 개정`.
- **참조 분모가 갈린 원인**(ⓐ 5767↔5760 · ⓑ 1062↔1061). 관측만 하고 원인은 안 판다.
- **clippy `pedantic` 축.** 착수 관측이 `cargo check` 로만 쟀다. ⚠ **CI 에도 clippy 잡이
  없다** — 사전부검이 확인했고(`ci.yml:121-137`) 그래서 `B4` 와 `E5` 가 안 갈린다.

## 개정

| 언제 | 무엇을 | 왜 |
|---|---|---|
| 2026-09-11 ⟨착수 전⟩ | 재는 자리 ㉠ 을 **`#102` → `#79`** 로 바꿨다 | 메인이 프론티어 한 줄만 보고 `#102` 를 권했는데, 본문에 *"답해야 할 것"* 이 **넷**이고 전부 설계 결정이라 회차를 삼킨다. 메인이 자기 권고를 정정해 올렸고 소유자가 `#79` 를 골랐다 |
| 2026-09-11 ⟨사전부검 R1⟩ | 조건이 **26 → 34**. 더한 것 여덟 — `A1-b`(봉인 blob 해시) · `B1-a`(㉢ RED 관측 자리) · `B5-a`(모집단 정의 선등록) · `B6`(미커밋 회차 처분) · `C2-b`(심볼 고유 줄) · `C2-c`(미끼 선정 규칙) · `C3-a`(팔마다 판정자) · `D4-a`(`#66` 인용 다섯) · `E6`(계획 문서 갱신) | 사전부검 21 시나리오 중 금지역 여섯 · 실패 넷. 처리 방침은 `premortem/r1-raw.md` 와 원장이 진다 |
| 2026-09-11 ⟨사전부검 R1⟩ | `C2` 를 **「귀속된 줄이 심볼 고유 줄이다」로 강화**하고 `C2-a` 의 미끼를 **같은 파일의 다른 심볼**로 다시 세웠다 | 앞 판의 미끼는 두 방향 다 못 잰다 — 진짜와 **38/52 줄 동일**이라 공통 줄 귀속이 미끼에도 나고, `A2` 의 머리가 미끼 심볼을 한 줄에 드러낸다 ⟨`PM1-06`·`PM1-07`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | 조건 문면을 **「세 자리 각각」 → 「재는 자리 각각」** | 차선책 3 을 쓰는 순간 조건 일곱이 문면상 동시에 깨져 **등록된 차선책인데 승격 없이는 못 쓰게** 됐다 ⟨`PM1-10`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | `## 순서` 절을 새로 세웠다 — **㉡ 을 가장 먼저 닫는다** | ㉡ 의 대상이 `E1`·`E4`·`B2` 를 판정하는 검사 그 자체다. §11③ 의 **금지역 예외**에 걸린다 ⟨`PM1-17`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | `## 재는 자리 셋` 에 **결박 0 과 축 명시**를 더했다 | 세 심볼 전부 결박이 0 이라 **목표 1(큐레이션)은 이 표본 위에서 원리상 안 재어진다.** 재는 것은 목표 2(컨설팅)다 ⟨`PM1-02`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | `## 범위 밖` 에 **측정자=피측정자 편향**을 더했다 | R-19 는 「같은 저장소」만 적었는데, 사전 등록을 쓰는 손과 변경을 쓰는 손이 같다. **원리상 못 없앤다** ⟨`PM1-18`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | 착수 관측의 **죽은 링크를 고치고** `## 워킹트리` 문장을 잰 시점에 묶었다 | 내 관측 파일이 `cargo xtask check` 를 **26/28 로 떨어뜨리고 있었다.** 실측으로 확인하고 고쳤다 ⟨`PM1-11`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | 착수 관측에 **§5.8 「메인이 이미 오염됐다」**를 새로 적었다 | 사전부검이 사전 등록 **전에** 세 심볼의 `pal touch` 를 돌려 결과를 메인에 요약해 줬다. 그 목록은 `C2` 의 귀속에서 뺀다 |

## 승격

⟨아직 없다⟩

## 착수 시점 관측

`observations/red.md` 가 원문을 진다. 수를 여기 두 번 적지 않는다 — 갈린다.
