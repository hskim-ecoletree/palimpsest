# 잠긴 의도 — 효과 확인

> 회차 `2026-09-11-effect-confirmation` · 착수 커밋 `8604d62`

## 원문

소유자가 2026-09-11 에 쓴 것 — **압축하지 않는다.**

> [이 회차에서 열 것 — 최단 경로 §4 의 3 단계「효과 확인」]
> 전제는 `#66` 이다. 그 문서가 적는다: *"이 저장소에서 `pal touch` 를 실제 작업 앞에 부르고,
> 그 산출을 본 뒤의 변경이 달라졌는지 잰다"* · 합격선은 *"그 산출을 보고 달라진 변경 1 건
> 이상, 좌표와 함께"*. 그리고 *"이 단계는 한 번도 실행된 적이 없다"*.
>
> ⚠ 이것은 **내(소유자)가 고른 것이다.** 순서표 §2 의 다음은 3 번(결박 반경과 낡음 전파)
> 이고 최단 경로의 다음은 3 단계라 둘이 갈렸고, 위임 조건이 성립하지 않아 내가 정했다.
> 그 사실을 잠긴 의도의 `## 목적 기여` 에 적어라.
>
> [이번엔 열지 마라 — 다음 회차의 몫이다]
> `#136`(엣지가 ⓐ/ⓑ 갈래와 참조 좌표를 안 싣는다) · `#137`(조건 파서가 줄 단위) ·
> `#138`(F04 벤치가 선의 경계) · `#139`(심볼이 가시성을 안 싣는다) ·
> `#135` 의 남은 ⓑ 몫 · `#131`(Kotlin) · 정확도 잔여 `#133 #78 #77 #57 #63~#65 #58`.

인터뷰 세 라운드가 잠근 것 열하나 — **상한 3 에 닿아 닫았다.** 범주 다섯(경계·의도·자율·
종료·재고)이 전부 한 번씩 열렸다.

> 「실제 작업」을 무엇으로 잠그나? → **프론티어의 열린 이슈를 골라 실제로 고친다**
> 인과를 무엇이 증언하나? → **사전 등록 — 보기 전 계획을 커밋으로 봉인한다**
> 몇 건을 재나? → **하한 1 을 지키되 상한을 3 으로 잠근다**
> 장치를 만드나? → **(가) 안 만든다 — 일회성으로 돌리고 수만 남긴다**
> 어느 이슈 위에서 재나? → **`#102` · `#126` · `#129`** ⚠ **`#102` 는 착수 전에 `#79` 로 바뀌었다 — 아래 `## 개정`**
> 「달라진 변경」의 방향을 재나? → **문면대로 「달라졌다」만 재고, 방향은 따로 적는다**
> `#66` 을 어떻게 처분하나? → **이 회차가 닫는다 — 두 물음에 답하고 낡은 전제를 정정한다**
> 어디서 멈추고 올리나? → **세 자리를 다 재고도 차이가 0 이면 멈춘다**
> 사전 등록을 쓰기 전에 얼마나 조사하나? → **평소대로 조사하고, 무엇을 봤는지 기록한다**
> 라운드 상한 → **인터뷰 3 · 사전부검 2 · 조건평가 2 · 정반합 판마다 2 · 독립리뷰 3**
> 이 회차가 무엇까지 주장하나? → **「이 저장소에서 섬」까지만 주장하고 R-19 를 범위 밖에 적는다**

## 목적 기여

[`00-goals.md`](../../../docs/plan/00-goals.md) §2 의 목표 **1(큐레이션)** 과 **2(컨설팅)** —
둘 다 **P1** 이고 그 문서가 *"첫 릴리스의 정의: 1과 2가 **실사용에서 값을 내면** 제품이다"*
로 잠근다. 이 회차가 재는 것이 바로 그 「실사용에서 값을 내나」다.

그리고 §0.1 의 잠긴 문장이 이 회차의 자다:

> **역사·결정을 알게 하는 것은 목적이 아니라 수단이다. 상위 가치는 그 결과로 더 나은
> 코드가 나오는 것이다.**

**왜 지금 그것이 다른 것보다 먼저인가** — `AGENTS.md` 의 소유자 지시가 판정자다
(*"이 순서가 다른 모든 우선순위를 이긴다"*):

> **빠르게 완성한다 → 처음의 의도에 도달한다 → 효과를 본다 → 그 뒤 계속 고친다.**

1·2 단계가 섰으므로(`#130` 2026-09-08 · `#135` 2026-09-10) **「효과를 본다」가 다음 자리**다.
`03-shortest-path.md` §4 의 3 단계가 그 자리이고 그 칸이 스스로 *"이 단계는 한 번도 실행된
적이 없다"* 를 적는다.

★★ **이 자리는 소유자가 골랐다 — 메인이 아니다.** 소유자 원문 그대로:

> ⚠ 이것은 **내(소유자)가 고른 것이다.** 순서표 §2 의 다음은 3 번(결박 반경과 낡음 전파)
> 이고 최단 경로의 다음은 3 단계라 둘이 갈렸고, 위임 조건이 성립하지 않아 내가 정했다.

위임 조건은 `02-order.md` 가 적는다 — *"다음에 첫 자리가 비면 순서표와 최단 경로가 **같은
자리를 가리키는 한** 메인이 지목하고 소유자는 보고를 받는다."* 둘이 갈렸으므로 그 조건이
성립하지 않았고, 그래서 소유자가 정했다.

## 상한

- 인터뷰 **3 라운드** — 소진했다(2026-09-11 · 라운드 3 에서 닫았다).
- 사전부검 **2 라운드** → ⚠ **3 라운드를 썼다. 초과를 적는다** ⟨2026-09-11⟩
  R2 가 `Agent stalled: no progress for 600s` 로 **두 번 죽어**(산출 0) 메인이 셋째를
  띄웠는데, 그 뒤 둘째가 **되살아나 완주했다**(같은 task-id 가 실패 뒤 완료로 다시 알렸다).
  유효한 반환문이 둘이 됐고 **19 건을 버리는 것보다 초과를 적는 쪽**을 골랐다 — 그중
  금지역이 있다. §11① 이 상한을 *"손실 제한이지 안전 보증이 아니다"* 로 적는 그 자를
  이렇게 읽었다. **하네스 실패로 늘어난 것이지 「더 볼 것이 있어서」가 아니다.**
- 완수 조건 설계 평가 **2 라운드**
- 완수 조건 검증 정반합 **판마다 2 라운드**
- 독립 리뷰 **3 라운드**

⚠ 앞 회차는 독립 리뷰가 4 였다. 이 회차는 산출이 코드보다 **측정 기록** 쪽이라 3 으로
줄였다 — 상한은 손실 제한이지 안전 보증이 아니다(§11 ①).

## 금지역

**`.claude/pal/policy.toml` 이 없다** — `.claude/pal/` 디렉터리 자체가 없고
`find . -name 'policy.toml' -not -path './target/*'` 가 빈 출력이다(착수 관측 §5.3).
그러므로 규약의 **기본 다섯**을 쓴다 — 데이터 손실 · **측정이 죽은 가지** ·
**사실이 아닌 것을 사실로** · 인증 · 대표 취약점.

★ 이 회차는 가운데 둘에 특히 가깝다. 효과 측정이 항등식이면 「측정이 죽은 가지」이고,
귀속을 근거 없이 적으면 「사실이 아닌 것을 사실로」다.

⚠ **회차 중에 금지역을 늘리지 않는다.**

## 재는 자리 셋

| # | 이슈 | 무엇 | 왜 이 자리인가 |
|---|---|---|---|
| ㉠ | **`#79`** | `identity_ceiling` 이 `min` 에 삼켜져 「순서에 취약한 것」과 「그냥 L1 인 것」이 같은 글자가 된다 | 제품 코드(`crates/pal-cli/src/ledger.rs:334`)이고 파일을 넘는 자리라 **역방향 색인이 쓰이는** 표본 |
| ㉡ | **`#126`** | 「최근 끝난 회차」를 사전순으로 고르는 자리가 하나 더 있다 — `check_ledger_pair` | **중복된 규칙의 둘째 자리**라 *"여기를 바꾸면 무엇이 깨지나"* 그 자체 |
| ㉢ | **`#129`** | `pal query narrative.unbound` 가 읽기 트랜잭션에서 쓰려 해 실패한다 | **단일 자리**라 *"touch 가 쓸모없었다"* 가 나올 수 있는 **음성 방향의 표본** |

★ **셋의 모양을 일부러 다르게 골랐다.** 하나만 맞춰 둔 표본이면 결과가 구성상 정해진다.

⚠⚠ **세 심볼 전부 결박이 0 이다** ⟨사전부검 R1 · `PM1-02`⟩. `nodes_of` · `identity_ceiling` ·
`check_ledger_pair` 어느 것도 결박 34 건의 대상이 아니다. **그러므로 이 회차가 재는 것은
`00-goals.md` §2 의 목표 2(컨설팅 — *"여기를 바꾸면 무엇이 깨지나"*)이고, 목표 1(큐레이션 —
*"왜 이렇게 되어 있나"*)은 이 표본 위에서 원리상 안 재어진다.** 표본을 안 바꾸고 축을
명시한다 — 소유자가 고른 자리이고, 컨설팅 축은 P1 이라 그 자체로 목적 기여가 선다.

⚠ **이 구별은 「못 읽었다」와 다르다.** 작업 저장소에서 결박 0 은 **진짜 0** 이다 —
격리 사본에서는 같은 명령이 *"못 읽었습니다 — 「0 건」이 아닙니다"* 를 내므로 갈린다.

## 순서 — 못 박는다 ⟨사전부검 R1 `PM1-17` · **R2·R3 가 근거를 정정했다**⟩

**㉡ 을 가장 먼저 닫고, 게이트·종료 문서는 그 뒤에 쓴다.**

⚠ **앞 판이 적은 근거는 과장이었다** ⟨`PM2-13`·`PM3-12`⟩. 앞 판은 *"그 검사가 `E1`·`E4`·`B2`
를 **판정한다**"* 고 적었는데, `#126` 본문이 스스로 부정한다:

> `## 왜 지금은 안 뒤집히나` — *"두 회차가 다 대상이라 **판정이 안 바뀐다**. 바뀌는 것은
> 판정문의 그 한 줄뿐이다 — 읽는 사람이 「가장 최근에 끝난 회차」를 틀리게 읽는다."*

실제 결함은 `xtask/src/main.rs:6254-6258` 의 `.iter().rev().find(…)` 가 **하한 판정문
문자열**을 고르는 자리이고 **통과/불통과 경로에 안 물린다.**

**그래도 ㉡ 을 먼저 닫는다.** 까닭이 바뀌었을 뿐이다 — 그 코드를 고치는 동안 `report.md`·
게이트가 없으면 그 검사의 다른 축(진행중 계수 · 하한)이 이 회차를 두고 발화할 수 있고,
**고치는 손과 판정하는 손이 같은 파일에 있는 것 자체가 §11③ 이 이름 붙인 자리**다.

**고치기 전과 후에 `cargo xtask check` 를 각각 돌려 두 산출을 보존한다**
(`observations/ledger-pair-before.txt` · `-after.txt`). ⚠ **그 둘이 바이트로 같을 수 있다**
⟨`PM2-13` — 지금 저장소에서 **사전순 최대와 커밋시각 최대가 같다**. 둘 다
`2026-09-08-cross-file-references` 라 판정문 문자열이 안 바뀐다⟩. **같으면 같다고 적는다** —
그때 「고친 검사가 자기를 통과시켰나」를 증언하는 것은 그 두 파일이 아니라 **`B6` 의 단위
시험**(합성 입력으로 사전순과 커밋시각이 갈리는 짝을 만든다)이다.

⚠ **`-after.txt` 가 RED 로 날 수 있다** ⟨`PM3-19`⟩ — 이 회차 디렉터리가 이미 커밋됐는데
`report.md` 도 게이트도 없다. **RED 로 나면 그대로 보존하고, 어느 축이 발화했는지 적는다.**
초록을 만들려고 종료 문서를 앞당겨 쓰지 않는다.


## 완수 조건

### 이 절이 쓰는 말 — 먼저 못 박는다 ⟨조건평가 R1 · `CA1-10`·`CA1-20`·`CA1-21`⟩

**재는 자리는 셋이고 착수 시점에 잠겼다** — **㉠ `#79`** · **㉡ `#126`** · **㉢ `#129`**.
회차 중에 **안 바뀐다.** 차선책 3 으로 줄이는 것은 **승격**이다.

★ **이 정의가 여기 없으면 `C5` 가 항등식이 된다** — 집합이 사후에 정해지면 차이 0 이 난
자리를 *"재는 자리가 아니었다"* 로 불러 아무 조건도 안 깨뜨리고 빠져나간다.

**경로의 뿌리는 전부 회차 디렉터리다** — `.palimpsest/rounds/2026-09-11-effect-confirmation/`.
`plan/` · `touch/` · `effect/` · `observations/` 가 그 아래다.

### A — 절차가 실제로 섰나

- [ ] **A1** ⟨결정론적⟩ 재는 자리 각각에 `plan/<이슈>-pre.md` 가 있고, **그 봉인 커밋이 `touch/<이슈>.txt` 커밋의 조상이며 두 SHA 가 다르다** — `git merge-base --is-ancestor <봉인> <touch>` 가 0 이고 `<봉인> != <touch>`. ⟨`CA1-08` — `git log --reverse` 는 SHA 목록만 내고 「앞선다」를 못 낸다. 날짜 비교는 `GIT_COMMITTER_DATE` 로 위조된다. **같은 커밋에 둘을 실으면 불통과다**⟩
- [ ] **A1-b** ⟨결정론적⟩ **봉인이 내용까지 잠근다** — `touch/<이슈>.txt` 머리의 `사전등록-blob` 이 `git rev-parse <봉인커밋>:<경로>` 와 같다
- [ ] **A1-c** ⟨결정론적⟩ **봉인이 판정 대상에 물린다** — `C1`·`C3` 이 실제로 쓴 사전 등록이 그 blob 과 **바이트로 같다**(`git cat-file blob <해시> | diff -`). ⟨`CA1-09` — 봉인 뒤 새 커밋으로 사전 등록을 고쳐도 `A1`·`A1-b` 는 그대로 통과한다⟩
- [ ] **A1-a** ⟨결정론적 · A1·A1-b·A1-c 의 음성 대조⟩ 사본 **셋**에서 판정이 각각 뒤집히는지 재고 산출을 `effect/negative-A1.md` 로 남긴다 — ① 순서를 뒤집은 사본 ② 봉인 뒤 내용을 채운 사본 ③ 둘을 같은 커밋에 실은 사본. 하나라도 안 뒤집히면 그 조건은 항등식이다
- [ ] **A2** ⟨결정론적⟩ 재는 자리 각각에 `pal touch <심볼>` 의 **전 출력**이 `touch/<이슈>.txt` 로 보존됐고, 머리에 **넷**이 적혔다 — 부른 명령 전문 · 그때의 HEAD SHA · **산출 둘째 줄의 심볼 ID**(`palimpsest@<sha>+worktree#<hex>`) · `A1-b` 의 blob 해시
- [ ] **A2-b** ⟨결정론적⟩ **재실행 대조가 선다** — 같은 HEAD·같은 워킹트리에서 그 명령을 다시 돌려 `diff` 가 **바이트로 동일**함을 보이고 산출을 `effect/rerun-<이슈>.txt` 로 남긴다. ⟨`PM2-10`·`PM3-07` — `CA1-17` 이 *"다시 돌려도 바이트가 안 맞는다"* 를 근거로 재실행 대조를 **포기**했는데 **그 근거가 거짓이다.** 두 번 돌려 `diff` 한 결과가 동일했고, `#<hex>` 는 워킹트리 다이제스트가 아니라 **심볼마다 다른 심볼 ID** 다(`crates/pal-core/src/coord.rs:296`). 더 강한 것이 가능한데 더 약한 것을 쓰면서 **강한 것이 불가능하다고 잘못 적었다**⟩
- [ ] **A2-c** ⟨결정론적⟩ ㉢ 의 산출이 **후보 목록**이면 그 사실과 전 출력을 그대로 보존하고, `A2` 의 머리 넷 중 **셋째가 원리상 없다**는 것을 적는다. ⟨`PM3-06` — 후보 목록 답의 근거 줄은 `Snapshot palimpsest@<sha>+worktree` 로 `#<hex>` 가 안 붙는다⟩
- [ ] **A2-a** ⟨결정론적⟩ **철회했다 — 미끼 설계를 버렸다.** 앞 판은 *"미끼 산출도 같은 요건으로 보존됐다"* 였다. `C` 절이 미끼 대신 「내용 시험」으로 갔으므로 보존할 미끼 산출이 없다. **이 조건은 「철회」로 판정한다** ⟨사전부검 R2·R3 · `C` 절 머리의 까닭⟩
- [ ] **A3** ⟨결정론적⟩ 재는 자리 각각의 사전 등록이 **좌표를 적었다** — 바꿀 `파일:줄` 목록과 각 자리에 무엇을 쓸 것인가. 「`#79` 를 고친다」 같은 한 줄은 A3 을 못 채운다
- [ ] **A3-a** ⟨결정론적 · A3 의 음성 대조⟩ 좌표 줄을 지운 사본이 A3 판정을 **통과하는지** 재고 산출을 `effect/negative-A3.md` 로 남긴다. 통과하면 A3 은 아무것도 안 잰다
- [ ] **A4** ⟨결정론적⟩ 재는 자리 각각의 사전 등록에 **touch 전에 무엇을 봤는지**가 적혔다 — 읽은 파일 경로 · 돌린 검색 · 본 이슈 · **그리고 `observations/red.md` §5.8 「메인이 이미 오염됐다」의 표 전문**
- [ ] **A5** ⟨결정론적⟩ ★ **touch 가 실제 작업보다 앞선다** — `touch/<이슈>.txt` 커밋이 **그 자리의 첫 코드 변경 커밋의 조상이고 두 SHA 가 다르다.** 재는 자리와 커밋의 사상은 **사전 등록의 `파일:줄` 목록**이 진다(그 목록의 파일을 건드린 첫 커밋) ⟨`PM3-15` — 대상 선정을 산문으로 남기면 ⟨결정론적⟩ 태그가 거짓이 된다⟩
- [ ] **A5-b** ⟨결정론적⟩ ★★ **커밋 순서만으로는 인과가 안 선다 — 바이트 앵커를 건다.** `touch/<이슈>.txt` 3 줄의 `body <hash>` 가 **봉인 커밋 시점의 그 심볼 본문**에서 나온 값이고 **변경 커밋 뒤의 값과 다르다**는 것을 보인다. ⟨`PM2-03`·`PM3-14` — 격리 사본에서 재현됐다: 코드 변경을 **워킹트리에 먼저 써 놓고** touch 를 돌린 뒤 봉인→touch→변경 순으로 커밋하면 `A1`·`A1-b`·`A1-c`·`A5`·`A6` 이 **전부 통과**하면서 touch 는 변경을 이미 본 상태다. `pal touch` 는 워킹트리를 읽고, 그것을 드러내는 유일한 값이 `body <hash>` 다⟩
- [ ] **A5-c** ⟨결정론적⟩ **`pal touch` 의 「워킹트리 일치」 줄을 증거로 안 쓴다** — 그 줄이 `--at` 없이 부른 모든 산출에서 **구조적으로 참**임을 확인하고 그 사실을 게이트에 적는다. ⟨`PM2-04` — 더러운 워킹트리에서도 `일치` 를 찍는다. 사람은 그 줄을 *"touch 를 돌릴 때 고친 것이 없었다"* 로 읽는다. 제품의 결함이고 이 회차는 **관측만 하고 안 고친다**⟩
- [ ] **A5-a** ⟨결정론적 · A5 의 음성 대조⟩ touch 커밋을 변경 커밋 뒤로 옮긴 사본에서 판정이 **뒤집히는지** 재고 `effect/negative-A5.md` 로 남긴다
- [ ] **A6** ⟨결정론적⟩ **심볼 선정이 봉인됐다** — 재는 자리마다 어느 심볼에 touch 를 걸지를 **사전 등록에 먼저 적고**, 그 뒤 `touch/<이슈>.txt` 로 **보존하는 산출은 심볼마다 하나**다. ⚠ **「한 번만 돈다」가 아니다** ⟨`PM3-08`·`PM3-09` — 착수 관측 §5.8 이 사전부검 R1 이 세 심볼에 이미 돌렸다고 적으므로 「한 번만」은 **착수 전부터 불통과**다. 그리고 ㉢ 은 후보 3 건이라 산출을 얻으려면 한 번 더 돌려야 해서 `A6` 과 원리상 배제됐다. **재실행은 허용하고 보존본을 하나로 잠그는 것**이 `CA1-06`(좋은 것만 남기기)을 막는 자리다 — `A2-b` 의 재실행 대조가 그것을 받친다⟩

### B — 실제 작업이 실제로 섰나

- [ ] **B1** ⟨결정론적⟩ 재는 자리 각각에 **코드 변경 커밋**이 있고, 그 이슈 본문이 요구한 것을 만족한다
- [ ] **B1-a** ⟨결정론적⟩ **㉢ 의 RED 를 작업 저장소에서 관측했다** — `pal query narrative.unbound` 는 `.palimpsest/intent.redb` 가 **있는데도** 실패한다. 산출을 `observations/red-129.txt` 로 남긴다. ⟨`CA1-01` — 앞 판은 *"그 파일이 없을 때만 난다"* 를 전제로 RED 자리를 파일 없는 곳으로 지정했다. **그 전제가 거짓이고**, 원인은 `write()` 가 `Handle::Reading` 과 `Handle::Absent` 를 **둘 다** 거부하는 것이다(`crates/pal-intent/src/store.rs:144-171`). 그 가지만 닫으면 조건은 초록이고 저장소는 빨간 채로 남는다⟩
- [ ] **B1-b** ⟨결정론적⟩ **철회했다 — 메인의 실측이 틀렸다.** 앞 판은 *"그 명령이 `Error:` 를 찍으면서 종료값 0 을 낸다"* 를 ⟨실측⟩ 로 적었다. **거짓이다** — 종료값은 **1** 이다. 사전부검 두 라운드가 각각 네 변형·세 변형으로 재서 전부 1 을 냈고, 메인이 세 형태(맨몸·`if`·`sh -c`)로 다시 재서 전부 1 이었다. **이 조건은 「철회」로 판정한다** ⟨`PM2-01`·`PM3-01` — 뒷문장이 고치기 전부터 참이라 조건이 착수 시점에 항등식이었고, 잠긴 의도에 거짓이 ⟨실측⟩ 태그를 달고 실렸다⟩
- [ ] **B1-c** ⟨결정론적⟩ 재는 자리 각각에 **자동 시험이 섰다** — `#[test]` 또는 `xtask` 검사. ⟨`CA1-11` — 소유자 원문 *"E2E/통합/단위 테스트(결정론적/기계적 검증)가 가능하다면 무조건 작성한다"*⟩
- [ ] **B2** ⟨결정론적⟩ `cargo xtask check` 가 초록이고 **검사 수가 28 이상**이다. ⟨`CA1-15` — *"초록이다"* 만 적으면 **검사 하나를 지워도 통과한다.** 이 저장소가 실제로 겪은 사고의 형태다⟩ ⚠ **착수 값 정정** ⟨`CA1-14`⟩ — 28/28 은 **회차를 열기 전** 값이고, 회차 디렉터리를 커밋한 뒤 `conditions-audit/r1-raw.md` 가 서기 전에는 **27/28** 이었다. 회차가 스스로 만든 RED 다
- [ ] **B3** ⟨결정론적⟩ `cargo test --workspace` 가 초록이고 **통과 수가 1028 이상**이다. **세는 법을 못 박는다** — 전 출력에서 `^test result:` 줄의 `passed` 를 합산한다(48 줄 · doc-test 6 묶음 포함). ⟨`CA1-16` — `#[test]` 는 **806** 이라 셈법이 다르면 값이 갈린다. 착수와 종료가 다른 법으로 세면 회귀 조건이 조용히 통과한다⟩
- [ ] **B4** ⟨결정론적⟩ 컴파일 경고가 **0** 이다 ⟨착수 시점 0⟩. `.rs` 전량 mtime 갱신 후 `cargo build --release --workspace` 와 `cargo check --workspace --all-targets` **각각**으로 잰다 — 증분 빌드는 경고를 다시 안 뱉는다
- [ ] **B5** ⟨결정론적⟩ `#79` 가 요구한 것을 만족한다 — *"닫으려면 그 수를 내는 산출 경로가 필요하다. 손으로 센 수를 판정 표에 실으면 다음 회차가 그것을 못 되짚는다."* 「순서에 취약한 것」과 「그냥 L1 인 것」을 가르는 수가 **명령으로** 난다
- [ ] **B5-a** ⟨결정론적⟩ **B5 의 모집단 정의가 사전 등록에 먼저 적혔다** — 무엇을 분모로 삼는지. ⟨`PM1-21` — 안 적으면 **어떤 수든** B5 를 채운다. `#79` 본문의 「464 · 7,156」은 합이 7,620 이고 지금 좌표 3,306 과 안 맞는다⟩
- [ ] **B6** ⟨결정론적⟩ ㉡ 의 고침이 **커밋이 0 인 회차**를 어떻게 처분하는지 코드와 판정문에 명시돼 있고, **그것을 재는 시험이 섰다** ⟨`PM1-16`·`CA1-11` — 입력을 만들 수 있는 단위 시험 자리다⟩

### C — 차이를 쟀나

★★ **미끼 설계를 버렸다** ⟨사전부검 R2·R3 · `PM2-02`·`PM2-05`·`PM2-06`·`PM2-07`·`PM3-04`·`PM3-10`·`PM3-11`⟩.
앞 판은 귀속을 **「심볼 고유 줄」**로 한정했는데 두 라운드가 독립으로 같은 것을 냈다:

> **「심볼 고유 줄」은 「touch 가 말해 준 것」과 같지 않다.** `pal touch` 산출은 대부분
> 고정 템플릿이라 같은 파일 두 심볼 사이의 차이가 {이름 · 좌표 · 호출 수 · 미해소 목록 ·
> 토큰 크기} 로 **원리상 한정된다.** 그중 앞 둘과 마지막은 **구성상 무조건 고유**라 판정에
> 못 쓰고, 정작 작업을 바꿀 수 있는 줄(`※ 파일 간 해소 — ⓐ 1823/5767` 같은 것)은
> **미끼와 공유된다는 이유로 버려진다.** 그래서 ㉠·㉡ 이 **재기 전에 0** 이 됐다.

**대신 「내용 시험」을 쓴다** — 귀속된 줄이 담은 **사실**이 사전 등록에도 착수 관측 §5.8 에도
없으면 그것이 touch 가 **처음 말해 준 것**이다. 공통 줄도 귀속 대상이고, 미끼가 필요 없다.

- [ ] **C1** ⟨결정론적⟩ 재는 자리 각각에 **사전 등록 ↔ 실제 변경**의 차이 목록이 `effect/<이슈>-delta.md` 로 섰고, 차이마다 **좌표 두 끝**이 붙었다 — `touch/<이슈>.txt:N` 이 원인이고 `파일:줄` 이 결과다
- [ ] **C2** ⟨정반합⟩ 그 차이 중 **touch 산출에 귀속되는 것이 1 건 이상**이다 ← **합격선 본체**
- [ ] **C2-b** ⟨결정론적⟩ **귀속마다 「내용 시험」이 적혔다** — 인용한 `touch/<이슈>.txt:N` 의 줄과, **그 줄이 담은 사실이 `plan/<이슈>-pre.md` 전문에도 `observations/red.md` §5.8 표에도 없다**는 것을 두 문서의 인용으로 보인다. 있으면 그 귀속은 **무효**다
- [ ] **C2-a** ⟨결정론적 · C2·C2-b 의 음성 대조⟩ **사전 등록에 이미 적힌 사실을 담은 줄**을 일부러 귀속으로 제출해 `C2-b` 가 그것을 **무효로 잡는지** 재고, 산출을 `effect/negative-C2.md` 로 남긴다. 안 잡으면 `C2-b` 는 항등식이다
- [ ] **C3** ⟨결정론적⟩ 귀속 판정을 한 것은 **새 에이전트**이고, 프롬프트가 넷을 **인라인으로** 싣는다 — 사전 등록 전문 · touch 산출 전문 · 실제 변경 diff · §5.8 표 전문. **경로로 넘기지 않는다** ⟨`PM2-15` — 경로로 넘기면 판정자가 읽은 바이트가 사후에 복원되지 않아 `A1-c` 가 원리상 관측 불가가 된다⟩. 프롬프트 전문을 `effect/judge-prompt.md` 로 보존한다
- [ ] **C3-a** ⟨결정론적⟩ **판정자에게 답을 안 담았다** — 프롬프트에 *"귀속이 있는지 재라"* 만 있고 *"귀속이 N 건인지 확인해라"* 가 없다. 띄운 인스턴스 수를 `effect/judge-roster.md` 에 적는다
- [ ] **C3-b** ⟨결정론적⟩ **판정 원문이 보존됐다** — `effect/<이슈>-judge.md`. 요약이 아니라 반환문 전문이다 ⟨`CA1-07`⟩
- [ ] **C4** ⟨정반합⟩ 차이마다 **방향**(나아졌다 / 나빠졌다 / 모른다)과 **「touch 가 말한 것이 참이었나」**가 근거와 함께 적혔다. ⚠ 방향은 합격선이 **아니다**
- [ ] **C5** ⟨결정론적⟩ **㉠·㉡·㉢ 셋 전부**에 대해 차이가 기록됐고 **0 인 자리도 그대로 적혔다.** 뺀 자리가 없다. ⚠ **차선책 3 을 쓰면 이 조건이 문면상 깨진다** — 그래서 차선책 3 은 **승격**이라고 적혀 있다 ⟨`PM2-11` — 앞 판은 이 한 자리만 안 고쳐 놓고 「고쳤다」를 개정 표에 적었다⟩

### D — `#66` 의 두 물음

- [ ] **D1** ⟨결정론적⟩ `#66` 본문의 낡은 전제 둘 — 「결박 가능한 좌표 7 개 · 파일 1 개」 · 「추출기는 Kotlin·Java·JavaScript·TypeScript 넷뿐」 — 의 정정문이 **저장소 안에 선다**(`docs/gates/effect-confirmation.md`), 지금 값과 **무슨 명령으로 쟀는지** 함께. 그리고 같은 내용을 이슈에도 남긴다. ⟨`CA1-22` — *"정정했다"* 만 적으면 정정문이 어디 서는지가 없고, 이슈 본문이면 저장소 검사가 못 되짚는다⟩
- [ ] **D2** ⟨정반합⟩ `#66` 의 물음 1 — *"이 도구가 자기 자신을 큐레이션할 수 있어야 하는가"* — 에 답했다. ⚠ **규범 명제라 측정이 혼자 못 답한다** — 이 회차의 측정을 근거로 대고 판정은 정반합이 한다
- [ ] **D3** ⟨결정론적⟩ `#66` 의 물음 2 — *"**Rust 추출기**인가, 아니면 ⑧을 **다른 저장소의 실사용**으로 다시 정의하는가"* — 에 답하되, **두 갈래의 현재 상태를 각각 명령으로 대야 한다.** ⟨`CA1-19` — 앞 판은 조건 문면에 답을 담아 **무엇을 쓰든 참**이었다. 명령이 값을 못 내면 이 조건은 반증된다⟩
- [ ] **D4** ⟨결정론적⟩ `#66` 을 닫았다. ⚠ **`D2` 가 「아직 못 답한다」로 나면 안 닫는다** — 차선책 4 가 그 문이다 ⟨`CA1-18`⟩
- [ ] **D4-a** ⟨결정론적⟩ **`#66` 을 현재형으로 인용하는 자리의 처분이 적혔다** — `corpus/criteria.toml:10530`·`:10886` · `docs/gates/F12.md:312`·`:398` · `scripts/f12-verify.py:559-560`(다섯 자리 실재 확인됨). ⚠ 어느 검사도 이슈 상태를 안 본다. **다섯을 한 규칙으로 덮지 않는다** ⟨`PM3-16`⟩:
  - `docs/gates/F12.md:312`·`:398` 은 **동결 판정 문서**, `corpus/criteria.toml:10530`·`:10886` 은 **합격선 파일** — **안 고치고** 어디가 거짓이 되는지 적는 것이 처분이다
  - `scripts/f12-verify.py:559-560` 은 **문서가 아니라 돌아가는 분류 코드**이고, `D1` 이 정정하려는 바로 그 거짓 전제(*"추출기는 Kotlin·Java·JS·TS 넷뿐이다 … ⑧에 합산하지 않는다"*)를 **분류에 쓴다.** 그 자리의 처분은 **따로** 적는다 — 고칠지 말지를 포함해서. ⚠ `B2` 의 28 검사가 이 스크립트를 안 돌린다

### E — 종료

- [ ] **E1** ⟨결정론적⟩ 게이트 `docs/gates/effect-confirmation.md` 가 절 이름 넷으로 섰고, 표준 표의 **검산이 맞다** — 넷의 합 = 등록 조건 수 · `intent.md` 의 조건 ID 집합과 **집합 같음**
- [ ] **E2** ⟨결정론적⟩ `## 효과` 에 **테스트도 CI 도 아닌 것이 이 회차의 산출을 돌린 출력**이 붙었고, **차이마다 방향이 나란히 적혔다**
- [ ] **E3** ⟨정반합⟩ 종료 보고 `report.md` 가 섰고 **네 이름이 없다**. ⟨`CA1-12` — 앞 판은 ⟨결정론적⟩으로 태그했는데 **규약이 이것을 반대 갈래의 예시로 직접 못 박는다**(`.claude/skills/round/SKILL.md:355` — *"결정론적이 아님 — 해석이 들어간다(… 「네 이름이 없다」…)"*). 결정론적으로 태그하면 정반합이 안 걸리고, 그 형태가 이 저장소에서 이미 실패한 자리다⟩
- [ ] **E4** ⟨결정론적⟩ `findings.jsonl` 의 **열린 발견이 0** 이고 열린 금지역·실패가 없다
- [ ] **E5** ⟨결정론적⟩ 회차의 **마지막 커밋 SHA** 에 `conclusion=success` 런이 붙었다
- [ ] **E6** ⟨결정론적⟩ **`docs/plan/03-shortest-path.md` §4 의 3 단계 행과 `docs/plan/02-order.md` 의 대응 줄이 이 회차의 결과로 갱신됐다** ⟨`PM1-12`⟩

## 차선책

1. **`C2` 가 0 이면** — **반증으로 적고 멈춘다.** 더 재지 않는다. 규약 §9 가 *"반증은
   실패가 아니다"* 를 못 박는다. ★ **「멈춘다」는 `C` 절에만 걸린다** — `D` 절(`#66` 처분)과
   `E` 절(게이트·보고·런)은 **그대로 다 한다.** ⟨`PM3-17` — 앞 판은 그 범위를 안 적어서
   가장 일어나기 쉬운 갈래의 종료 절차가 미정이었다⟩ `D2` 는 그 반증을 **근거로** 답한다.

2. **`B5` 의 산출 경로가 이 회차에서 안 서면** — `#79` 를 안 닫고 *"그 자리는 touch 효과
   측정의 표본으로만 썼다"* 를 적는다. ⚠ **메인이 혼자 발동하지 않는다** — 승격이다.
   ★ **다만 스키마를 먼저 건드리지 마라** ⟨`PM2-14` — `pal ledger --symbols` 가 심볼마다
   `identity` 를 **이미 낸다**(실측 3306 · ordinal 3300). 「순서에 취약한 것」은 그 산출의
   파생으로 선다. `SymbolNode`·`schema/graph.toml` 로 가는 것은 **만들 필요가 없는 것을
   만드는 자리**다⟩

3. **재는 자리 중 하나가 회차를 삼키면** — 그 자리를 표본에서 빼고 남은 자리로 판정한다.
   **하한 1 은 지킨다.** ⚠ **승격이다** — `C5` 가 *"㉠·㉡·㉢ 셋 전부"* 를 요구하므로
   이 차선책을 쓰면 `C5` 가 문면상 깨진다. 그 사실을 `C5` 에 적어 뒀다.

4. **`D2` 가 「아직 못 답한다」로 나면** — `#66` 을 **안 닫고** 그 사유를 적는다. `D4` 는
   그 갈래에서 「닫지 않았다 · 까닭」으로 판정한다.

★ **번호를 등장 순서대로 다시 적었다** ⟨`PM2-12` — 앞 판이 `1·2·4·3` 순으로 적어 놓아서
마크다운이 **등장 순서로 다시 매기면 3 과 4 가 뒤바뀐다.** `D4` 가 *"차선책 4 가 그 문이다"*
로 번호를 참조하는데 그 문이 표본 빼기를 가리키게 된다⟩


## 범위 밖

- **R-19 — 자기 저장소 자기적용의 편향.** 이 회차는 **「이 저장소에서 섬」까지만** 주장한다.
  재는 자와 재이는 자가 같은 저장소인 것은 이 회차가 **원리상 못 없앤다.**
- **★ 그보다 좁은 편향 하나가 더 있다 — 사전 등록을 쓰는 손과 실제 변경을 쓰는 손이
  같다** ⟨`PM1-18`⟩. 사전 등록은 **시각**을 봉인하지 **성실성**을 못 봉인한다. 얇게 쓰면
  `C2` 가 구성상 만족되고, 지나치게 두껍게 쓰면 touch 가 더 낼 것이 없어 `C2` 가 구성상
  0 이 된다. **이 회차는 그것을 못 없앤다** — `A3`·`A4` 가 좌표와 조사 기록을 요구해 폭을
  좁히는 것이 할 수 있는 전부다. 종료 보고의 `## 원리상 못 잰 것` 이 이것을 진다.
- **남의 저장소 실사용.** 코퍼스 저장소에는 「실제 작업」이 없어 합격선 문면을 벗어난다.
- **`pal touch` 아닌 표면의 효과.** `pal doctor` · `pal narrative` · 스킬의 효과는 안 잰다.
  ⚠ touch 가 스스로 가리킨 후속 질의는 **그 산출의 연장으로 센다.**
- **「나아졌다」의 판정 기준.** 방향은 `C4` 로 기록하되 합격선이 아니다.
- **`pal touch` 의 멤버 해소(`x.foo()`).** `identity_ceiling` 의 호출자 0 이 거짓 음성인
  까닭이고, touch 자신이 *"이 회차의 범위 밖"* 이라고 적는다. **이 회차도 안 고친다** —
  `C4` 로 그 거짓 음성을 기록만 한다.
- **Markdown 결박(`#69`).** 이 저장소 문서가 미결박인 것은 이 회차가 안 고친다.
- **「추출기가 몇인가」가 저장소 다섯 자리에서 갈린 것.** `D1` 이 `#66` 하나를 정정할
  뿐이다. 나머지는 동결 판정 문서·합격선 파일이고 앞 회차가 이미 `범위밖` 으로 처분했다
  (`2026-08-20-rust-extractor/findings.jsonl:44` `PM2-S14`).
- **소유자가 막은 열넷** — `#136` `#137` `#138` `#139` · `#135` 의 남은 ⓑ 몫 · `#131` ·
  `#133` `#78` `#77` `#57` `#63`~`#65` `#58`.
- **`#102` 의 설계 물음 넷.** 착수 전에 표본에서 뺐다 — 아래 `## 개정`.
- **참조 분모가 갈린 원인**(ⓐ 5767↔5760 · ⓑ 1062↔1061). 관측만 하고 원인은 안 판다.
- **clippy `pedantic` 축.** 착수 관측이 `cargo check` 로만 쟀다. ⚠ **CI 에도 clippy 잡이
  없다** — 사전부검이 확인했고(`ci.yml:121-137`) 그래서 `B4` 와 `E5` 가 안 갈린다.

## 개정

| 언제 | 무엇을 | 왜 |
|---|---|---|
| 2026-09-11 ⟨착수 전⟩ | 재는 자리 ㉠ 을 **`#102` → `#79`** 로 바꿨다 | 메인이 프론티어 한 줄만 보고 `#102` 를 권했는데, 본문에 *"답해야 할 것"* 이 **넷**이고 전부 설계 결정이라 회차를 삼킨다. 메인이 자기 권고를 정정해 올렸고 소유자가 `#79` 를 골랐다 |
| 2026-09-11 ⟨사전부검 R1⟩ | 조건이 **26 → 34**. 더한 것 여덟 — `A1-b`(봉인 blob 해시) · `B1-a`(㉢ RED 관측 자리) · `B5-a`(모집단 정의 선등록) · `B6`(미커밋 회차 처분) · `C2-b`(심볼 고유 줄) · `C2-c`(미끼 선정 규칙) · `C3-a`(팔마다 판정자) · `D4-a`(`#66` 인용 다섯) · `E6`(계획 문서 갱신) | 사전부검 21 시나리오 중 금지역 여섯 · 실패 넷. 처리 방침은 `premortem/r1-raw.md` 와 원장이 진다 |
| 2026-09-11 ⟨사전부검 R1⟩ | `C2` 를 **「귀속된 줄이 심볼 고유 줄이다」로 강화**하고 `C2-a` 의 미끼를 **같은 파일의 다른 심볼**로 다시 세웠다 | 앞 판의 미끼는 두 방향 다 못 잰다 — 진짜와 **38/52 줄 동일**이라 공통 줄 귀속이 미끼에도 나고, `A2` 의 머리가 미끼 심볼을 한 줄에 드러낸다 ⟨`PM1-06`·`PM1-07`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | 조건 문면을 **「세 자리 각각」 → 「재는 자리 각각」** | 차선책 3 을 쓰는 순간 조건 일곱이 문면상 동시에 깨져 **등록된 차선책인데 승격 없이는 못 쓰게** 됐다 ⟨`PM1-10`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | `## 순서` 절을 새로 세웠다 — **㉡ 을 가장 먼저 닫는다** | ㉡ 의 대상이 `E1`·`E4`·`B2` 를 판정하는 검사 그 자체다. §11③ 의 **금지역 예외**에 걸린다 ⟨`PM1-17`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | `## 재는 자리 셋` 에 **결박 0 과 축 명시**를 더했다 | 세 심볼 전부 결박이 0 이라 **목표 1(큐레이션)은 이 표본 위에서 원리상 안 재어진다.** 재는 것은 목표 2(컨설팅)다 ⟨`PM1-02`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | `## 범위 밖` 에 **측정자=피측정자 편향**을 더했다 | R-19 는 「같은 저장소」만 적었는데, 사전 등록을 쓰는 손과 변경을 쓰는 손이 같다. **원리상 못 없앤다** ⟨`PM1-18`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | 착수 관측의 **죽은 링크를 고치고** `## 워킹트리` 문장을 잰 시점에 묶었다 | 내 관측 파일이 `cargo xtask check` 를 **26/28 로 떨어뜨리고 있었다.** 실측으로 확인하고 고쳤다 ⟨`PM1-11`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | 착수 관측에 **§5.8 「메인이 이미 오염됐다」**를 새로 적었다 | 사전부검이 사전 등록 **전에** 세 심볼의 `pal touch` 를 돌려 결과를 메인에 요약해 줬다. 그 목록은 `C2` 의 귀속에서 뺀다 |
| 2026-09-11 ⟨조건평가 R1⟩ | 조건이 **35 → 43**. `A5`·`A5-a`(touch → 실제 작업) · `A1-c`(봉인이 판정에 물린다) · `A2-a`(미끼 산출 보존) · `A6`(심볼 선정 봉인) · `B1-b`(종료값) · `B1-c`(자동 시험) · `C3-b`(진짜 팔 판정 원문) | 평가가 **걸린 자리 22 · 실패 4** 를 냈다. 원문 전제의 절반(*"실제 작업 **앞에** 부르고"*)을 재는 조건이 **하나도 없었다** ⟨`CA1-02`⟩ |
| 2026-09-11 ⟨조건평가 R1⟩ | 완수 조건 절 머리에 **「재는 자리 셋」의 정의**와 **경로 뿌리**를 못 박았다 | 조건 열둘이 「재는 자리 각각」을 쓰는데 그 집합이 조건 절에 없었다. 사후에 정해지면 `C5` 가 항등식이 된다 ⟨`CA1-10`·`CA1-20`·`CA1-21`⟩ |
| 2026-09-11 ⟨조건평가 R1⟩ | **`B1-a` 의 전제를 뒤집었다** | *"`intent.redb` 가 없을 때만 실패한다"* 가 **거짓**이다. 메인이 직접 재서 확인했다 — 그 파일이 있는데도 실패한다. 사전부검이 `pal narrative`(도는 명령)와 섞었다 ⟨`CA1-01`⟩ |
| 2026-09-11 ⟨조건평가 R1⟩ | `C2-b` 의 정의를 **`comm` → `grep -Fxvn -f`** 로 바꿨다 | `comm` 이 정렬 유무로 **두 값**을 낸다(공통 23 ↔ 38). 정렬하면 줄 번호가 사라져 `C1` 이 요구하는 좌표와 안 맞물린다 ⟨`CA1-04`⟩ |
| 2026-09-11 ⟨조건평가 R1⟩ | `C2` 에 **「§5.8 에 이미 적힌 사실이 아니다」**를 더했다 | 배제 규칙이 관측 파일에만 살았다. **판정 자리에 없으면 없는 것과 같다** ⟨`CA1-03`⟩ |
| 2026-09-11 ⟨조건평가 R1⟩ | 갈래 태그를 **절 → 조건**으로 옮기고 `E3` 을 **정반합**으로 고쳤다 | `E3`(*"네 이름이 없다"*)은 규약이 **반대 갈래의 예시로 직접 못 박은 것**이다(`SKILL.md:355`) ⟨`CA1-12`·`CA1-13`⟩ |
| 2026-09-11 ⟨조건평가 R1⟩ | `B2` 에 **검사 수 하한 28**, `B3` 에 **세는 법**을 적었다 | `B2` 는 검사를 지워도 통과했고, `B3` 의 1028 은 `#[test]` 806 과 셈법이 달랐다 ⟨`CA1-15`·`CA1-16`⟩ |
| 2026-09-11 ⟨조건평가 R1⟩ | 차선책 **4** 를 더했다 — `D2` 가 「못 답한다」면 `#66` 을 안 닫는다 | 앞 판은 `D2` 의 결과와 무관하게 `D4` 가 「닫았다」를 강제했다 ⟨`CA1-18`⟩ |

## 승격

⟨아직 없다⟩

## 착수 시점 관측

`observations/red.md` 가 원문을 진다. 수를 여기 두 번 적지 않는다 — 갈린다.
