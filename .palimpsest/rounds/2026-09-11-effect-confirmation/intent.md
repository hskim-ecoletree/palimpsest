# 잠긴 의도 — 효과 확인

> 회차 `2026-09-11-effect-confirmation` · 착수 커밋 `8604d62`

## 원문

소유자가 2026-09-11 에 쓴 것 — **압축하지 않는다.**

> [이 회차에서 열 것 — 최단 경로 §4 의 3 단계「효과 확인」]
> 전제는 `#66` 이다. 그 문서가 적는다: *"이 저장소에서 `pal touch` 를 실제 작업 앞에 부르고,
> 그 산출을 본 뒤의 변경이 달라졌는지 잰다"* · 합격선은 *"그 산출을 보고 달라진 변경 1 건
> 이상, 좌표와 함께"*. 그리고 *"이 단계는 한 번도 실행된 적이 없다"*.
>
> ⚠ 이것은 **내(소유자)가 고른 것이다.** 순서표 §2 의 다음은 3 번(결박 반경과 낡음 전파)
> 이고 최단 경로의 다음은 3 단계라 둘이 갈렸고, 위임 조건이 성립하지 않아 내가 정했다.
> 그 사실을 잠긴 의도의 `## 목적 기여` 에 적어라.
>
> [이번엔 열지 마라 — 다음 회차의 몫이다]
> `#136`(엣지가 ⓐ/ⓑ 갈래와 참조 좌표를 안 싣는다) · `#137`(조건 파서가 줄 단위) ·
> `#138`(F04 벤치가 선의 경계) · `#139`(심볼이 가시성을 안 싣는다) ·
> `#135` 의 남은 ⓑ 몫 · `#131`(Kotlin) · 정확도 잔여 `#133 #78 #77 #57 #63~#65 #58`.

인터뷰 세 라운드가 잠근 것 열하나 — **상한 3 에 닿아 닫았다.** 범주 다섯(경계·의도·자율·
종료·재고)이 전부 한 번씩 열렸다.

> 「실제 작업」을 무엇으로 잠그나? → **프론티어의 열린 이슈를 골라 실제로 고친다**
> 인과를 무엇이 증언하나? → **사전 등록 — 보기 전 계획을 커밋으로 봉인한다**
> 몇 건을 재나? → **하한 1 을 지키되 상한을 3 으로 잠근다**
> 장치를 만드나? → **(가) 안 만든다 — 일회성으로 돌리고 수만 남긴다**
> 어느 이슈 위에서 재나? → **`#102` · `#126` · `#129`** ⚠ **`#102` 는 착수 전에 `#79` 로 바뀌었다 — 아래 `## 개정`**
> 「달라진 변경」의 방향을 재나? → **문면대로 「달라졌다」만 재고, 방향은 따로 적는다**
> `#66` 을 어떻게 처분하나? → **이 회차가 닫는다 — 두 물음에 답하고 낡은 전제를 정정한다**
> 어디서 멈추고 올리나? → **세 자리를 다 재고도 차이가 0 이면 멈춘다**
> 사전 등록을 쓰기 전에 얼마나 조사하나? → **평소대로 조사하고, 무엇을 봤는지 기록한다**
> 라운드 상한 → **인터뷰 3 · 사전부검 2 · 조건평가 2 · 정반합 판마다 2 · 독립리뷰 3**
> 이 회차가 무엇까지 주장하나? → **「이 저장소에서 섬」까지만 주장하고 R-19 를 범위 밖에 적는다**

## 목적 기여

[`00-goals.md`](../../../docs/plan/00-goals.md) §2 의 목표 **1(큐레이션)** 과 **2(컨설팅)** —
둘 다 **P1** 이고 그 문서가 *"첫 릴리스의 정의: 1과 2가 **실사용에서 값을 내면** 제품이다"*
로 잠근다. 이 회차가 재는 것이 바로 그 「실사용에서 값을 내나」다.

그리고 §0.1 의 잠긴 문장이 이 회차의 자다:

> **역사·결정을 알게 하는 것은 목적이 아니라 수단이다. 상위 가치는 그 결과로 더 나은
> 코드가 나오는 것이다.**

**왜 지금 그것이 다른 것보다 먼저인가** — `AGENTS.md` 의 소유자 지시가 판정자다
(*"이 순서가 다른 모든 우선순위를 이긴다"*):

> **빠르게 완성한다 → 처음의 의도에 도달한다 → 효과를 본다 → 그 뒤 계속 고친다.**

1·2 단계가 섰으므로(`#130` 2026-09-08 · `#135` 2026-09-10 — ⚠ **그 이슈는 아직 OPEN 이고 ⓑ 는 하한 333 에 못 닿아 `E2` 가 반증이다** ⟨`03-shortest-path.md:130`⟩) **「효과를 본다」가 다음 자리**다.
`03-shortest-path.md` §4 의 3 단계가 그 자리이고 그 칸이 스스로 *"이 단계는 한 번도 실행된
적이 없다"* 를 적는다.

★★ **이 자리는 소유자가 골랐다 — 메인이 아니다.** 소유자 원문 그대로:

> ⚠ 이것은 **내(소유자)가 고른 것이다.** 순서표 §2 의 다음은 3 번(결박 반경과 낡음 전파)
> 이고 최단 경로의 다음은 3 단계라 둘이 갈렸고, 위임 조건이 성립하지 않아 내가 정했다.

위임 조건은 `02-order.md` 가 적는다 — *"다음에 첫 자리가 비면 순서표와 최단 경로가 **같은
자리를 가리키는 한** 메인이 지목하고 소유자는 보고를 받는다."* 둘이 갈렸으므로 그 조건이
성립하지 않았고, 그래서 소유자가 정했다.

## 상한

- 인터뷰 **3 라운드** — 소진했다(2026-09-11 · 라운드 3 에서 닫았다).
- 사전부검 **2 라운드** → ⚠ **3 라운드를 썼다. 초과를 적는다** ⟨2026-09-11⟩
  R2 가 `Agent stalled: no progress for 600s` 로 **두 번 죽어**(산출 0) 메인이 셋째를
  띄웠는데, 그 뒤 둘째가 **되살아나 완주했다**(같은 task-id 가 실패 뒤 완료로 다시 알렸다).
  유효한 반환문이 둘이 됐고 **19 건을 버리는 것보다 초과를 적는 쪽**을 골랐다 — 그중
  금지역이 있다. §11① 이 상한을 *"손실 제한이지 안전 보증이 아니다"* 로 적는 그 자를
  이렇게 읽었다. **하네스 실패로 늘어난 것이지 「더 볼 것이 있어서」가 아니다.**
- 완수 조건 설계 평가 **2 라운드**
- 완수 조건 검증 정반합 **판마다 2 라운드** → ⚠ **판 4 가 3 라운드를 쓴다. 초과를 적는다**
  ⟨2026-09-12 · `## 승격` 칸 12⟩ 설계 반증 조항 8 이 라운드 2 에 대해 발화했고
  (판정 대상 파일 자신이 핀 밖으로 나갔다) **소유자가 면제가 아니라 집행을 골랐다.**
  조항 8 의 문면이 *"그 판은 무효이고 다시 돌린다"* 이므로 라운드 2 는 **무효**이고
  라운드 3 이 그 자리를 대신한다. **「더 볼 것이 있어서」가 아니라 설계가 등록해 둔
  무효 조항이 집행된 것**이다 — 사전부검 초과(하네스 실패)와도 종류가 다르다.
- 독립 리뷰 **3 라운드**

⚠ 앞 회차는 독립 리뷰가 4 였다. 이 회차는 산출이 코드보다 **측정 기록** 쪽이라 3 으로
줄였다 — 상한은 손실 제한이지 안전 보증이 아니다(§11 ①).

## 금지역

**`.claude/pal/policy.toml` 이 없다** — `.claude/pal/` 디렉터리 자체가 없고
`find . -name 'policy.toml' -not -path './target/*'` 가 빈 출력이다(착수 관측 §5.3).
그러므로 규약의 **기본 다섯**을 쓴다 — 데이터 손실 · **측정이 죽은 가지** ·
**사실이 아닌 것을 사실로** · 인증 · 대표 취약점.

★ 이 회차는 가운데 둘에 특히 가깝다. 효과 측정이 항등식이면 「측정이 죽은 가지」이고,
귀속을 근거 없이 적으면 「사실이 아닌 것을 사실로」다.

⚠ **회차 중에 금지역을 늘리지 않는다.**

## 재는 자리 셋

| # | 이슈 | 무엇 | 왜 이 자리인가 |
|---|---|---|---|
| ㉠ | **`#79`** | `identity_ceiling` 이 `min` 에 삼켜져 「순서에 취약한 것」과 「그냥 L1 인 것」이 같은 글자가 된다 | 제품 코드(`crates/pal-cli/src/ledger.rs:334`)이고 파일을 넘는 자리라 **역방향 색인이 쓰이는** 표본 |
| ㉡ | **`#126`** | 「최근 끝난 회차」를 사전순으로 고르는 자리가 하나 더 있다 — `check_ledger_pair` | **중복된 규칙의 둘째 자리**라 *"여기를 바꾸면 무엇이 깨지나"* 그 자체 |
| ㉢ | **`#129`** | `pal query narrative.unbound` 가 읽기 트랜잭션에서 쓰려 해 실패한다 | **단일 자리**라 *"touch 가 쓸모없었다"* 가 나올 수 있는 **음성 방향의 표본** |

★ **셋의 모양을 일부러 다르게 골랐다.** 하나만 맞춰 둔 표본이면 결과가 구성상 정해진다.

⚠⚠ **세 심볼 전부 결박이 0 이다** ⟨사전부검 R1 · `PM1-02`⟩. `nodes_of` · `identity_ceiling` ·
`check_ledger_pair` 어느 것도 결박 34 건의 대상이 아니다. **그러므로 이 회차가 재는 것은
`00-goals.md` §2 의 목표 2(컨설팅 — *"여기를 바꾸면 무엇이 깨지나"*)이고, 목표 1(큐레이션 —
*"왜 이렇게 되어 있나"*)은 이 표본 위에서 원리상 안 재어진다.** 표본을 안 바꾸고 축을
명시한다 — 소유자가 고른 자리이고, 컨설팅 축은 P1 이라 그 자체로 목적 기여가 선다.

⚠⚠ **㉢ 은 `C2` 의 귀속 모집단에 착수 시점부터 0 으로 들어간다** ⟨조건 평가 R2 의
`## 합이 의도를 덮나` ②⟩. `pal touch write` 의 답은 **후보 목록**이고, 그 가지
(`crates/pal-cli/src/touch.rs:272-278`)에서는 `※ 파일 간 해소 …` 같은 **사실 줄이 아예
안 찍힌다.** 그러므로 **하한 1 을 실제로 지는 것은 ㉠·㉡ 둘이다** — 소유자가
*"표본에 남기고 「측정 불능」을 그대로 잰다"* 로 정했고, 그 「측정 불능」 자체가 `#66` 의
물음 1 에 대한 값이다.

⚠ **이 구별은 「못 읽었다」와 다르다.** 작업 저장소에서 결박 0 은 **진짜 0** 이다 —
격리 사본에서는 같은 명령이 *"못 읽었습니다 — 「0 건」이 아닙니다"* 를 내므로 갈린다.

## 순서 — 못 박는다 ⟨사전부검 R1 `PM1-17` · **R2·R3 가 근거를 정정했다**⟩

**㉡ 을 가장 먼저 닫고, 게이트·종료 문서는 그 뒤에 쓴다.**

⚠ **앞 판이 적은 근거는 과장이었다** ⟨`PM2-13`·`PM3-12`⟩. 앞 판은 *"그 검사가 `E1`·`E4`·`B2`
를 **판정한다**"* 고 적었는데, `#126` 본문이 스스로 부정한다:

> `## 왜 지금은 안 뒤집히나` — *"두 회차가 다 대상이라 **판정이 안 바뀐다**. 바뀌는 것은
> 판정문의 그 한 줄뿐이다 — 읽는 사람이 「가장 최근에 끝난 회차」를 틀리게 읽는다."*

실제 결함은 `xtask/src/main.rs:6254-6258` 의 `.iter().rev().find(…)` 가 끝난 회차를
**사전순 마지막**으로 고르는 자리라는 것이다. ⚠ **「통과/불통과 경로에 안 물린다」로 적었던
것은 거짓이다** — 거기서 고른 `최근_끝난` 은 `:6262` 의 분기에서 **검사 밖**으로 갈리면 `:6265-6269` 가
`problems` 에 「하한 미충족」을 싣고 `:6294-6295` 가 그것을 `bail!` 한다. 사전순 선택이
틀린 회차를 집으면 검사가 **빨개진다.** ⚠ **민감한 축은 하한 하나뿐이다** — `:6286` 의
빈-모집단 가드는 `최근_끝난` 을 `.is_some()` 과 메시지로만 써서 **어느 회차를 집든 판정이
안 바뀐다**(기록 정합 5 차가 좁혔다 · `:6263` 은 `problems` 가 아니라 **통과 쪽 문자열**이다). `#126` 본문의 *"판정이 안 바뀐다"* 는
**지금 두 회차가 다 대상이라서**이지 코드의 구조적 성질이 아니다 ⟨기록 정합 4 차가
`xtask/src/main.rs` 를 직접 읽어 정정했다 — 이 회차가 `## 금지역` 에 등록한
*「사실이 아닌 것을 사실로」* 에 드는 형태였다⟩.

**㉡ 을 먼저 닫는다 — 까닭이 되돌아왔다.** ⟨사전부검 R1⟩ 의 `PM1-17` 이 적은 *"그 검사가
`E1`·`E4`·`B2` 의 판정자다"* 는 R2·R3 가 「과장」으로 깎았으나, 셋 중 **틀린 것은 `E4`
하나**다(열린 발견은 `check_finding_closure`(`main.rs:645`)라는 다른 검사가 잰다). ⓐ 조건
ID 집합 같음(`:6198`) · ⓑ 하한(`:6262-6269`) · ⓒ 「조건 0 이면 실패」(`:6286`)는 **전부 이
검사 안에 있고 셋 다 `problems` 로 간다.** 그러니 하한은 **「다른 축」이 아니라 ㉡ 이 고칠
`최근_끝난` 이 사는 바로 그 축**이다. 여기에 진행중 계수(`:5964-5982`)가 더해지고,
**고치는 손과 판정하는 손이 같은 파일에 있는 것 자체가 §11③ 이 이름 붙인 자리**다.

**고치기 전과 후에 `cargo xtask check` 를 각각 돌려 두 산출을 보존한다**
(`observations/ledger-pair-before.txt` · `-after.txt`). ⚠ **그 둘이 바이트로 같을 수 있다**
⟨`PM2-13` — 지금 저장소에서 **사전순 최대와 커밋시각 최대가 같다**. 둘 다
`2026-09-08-cross-file-references` 라 판정문 문자열이 안 바뀐다⟩. **같으면 같다고 적는다** —
그때 「고친 검사가 자기를 통과시켰나」를 증언하는 것은 그 두 파일이 아니라 **`B6` 의 단위
시험**(합성 입력으로 사전순과 커밋시각이 갈리는 짝을 만든다)이다.

⚠ **`-after.txt` 가 RED 로 날 수 있다** ⟨`PM3-19`⟩ — 이 회차 디렉터리가 이미 커밋됐는데
`report.md` 도 게이트도 없다. **RED 로 나면 그대로 보존하고, 어느 축이 발화했는지 적는다.**
초록을 만들려고 종료 문서를 앞당겨 쓰지 않는다.


## 완수 조건

### 이 절이 쓰는 말 — 먼저 못 박는다 ⟨조건평가 R1 · `CA1-10`·`CA1-20`·`CA1-21`⟩

**재는 자리는 셋이고 착수 시점에 잠겼다** — **㉠ `#79`** · **㉡ `#126`** · **㉢ `#129`**.
회차 중에 **안 바뀐다.** 차선책 3 으로 줄이는 것은 **승격**이다.

★ **이 정의가 여기 없으면 `C5` 가 항등식이 된다** — 집합이 사후에 정해지면 차이 0 이 난
자리를 *"재는 자리가 아니었다"* 로 불러 아무 조건도 안 깨뜨리고 빠져나간다.

**경로의 뿌리는 전부 회차 디렉터리다** — `.palimpsest/rounds/2026-09-11-effect-confirmation/`.
`plan/` · `touch/` · `effect/` · `observations/` 가 그 아래다.

### A — 절차가 실제로 섰나

- [x] **A1** ⟨결정론적⟩ 재는 자리 각각에 `plan/<이슈>-pre.md` 가 있고, **그 봉인 커밋이 `touch/<이슈>.txt` 커밋의 조상이며 두 SHA 가 다르다** — `git merge-base --is-ancestor <봉인> <touch>` 가 0 이고 `<봉인> != <touch>`. ⟨`CA1-08` — `git log --reverse` 는 SHA 목록만 내고 「앞선다」를 못 낸다. 날짜 비교는 `GIT_COMMITTER_DATE` 로 위조된다. **같은 커밋에 둘을 실으면 불통과다**⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **A1-b** ⟨결정론적⟩ **봉인이 내용까지 잠근다** — `touch/<이슈>.txt` 머리의 `사전등록-blob` 이 `git rev-parse <봉인커밋>:<경로>` 와 같다 · 통과 ⟨전사 2026-09-12⟩
- [x] **A1-c** ⟨결정론적⟩ **봉인이 판정 대상에 물린다** — `C3` 의 프롬프트에 인라인된 사전 등록이 그 blob 과 **바이트로 같다**(`git cat-file blob <해시> | diff -`). ⟨`CA1-09` — 봉인 뒤 새 커밋으로 사전 등록을 고쳐도 `A1`·`A1-b` 는 그대로 통과한다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **A1-a** ⟨결정론적 · A1·A1-b·A1-c 의 음성 대조⟩ 사본 **넷**에서 판정이 각각 뒤집히는지 재고 산출을 `effect/negative-A1.md` 로 남긴다 — ① 순서를 뒤집은 사본(`A1` 을 친다) ② 봉인 뒤 내용을 채운 사본(`A1-b`) ③ 둘을 같은 커밋에 실은 사본(`A1`) ④ `C3` 프롬프트에 인라인된 사전 등록을 **한 바이트 고친** 사본(`A1-c`). **하나라도 안 뒤집히면 그 축의 조건이 항등식이고, 한 축만 초록이어도 `A1-a` 는 불통과다** ⟨④ 는 기록 정합 3 차가 더했다 — 셋 중 `A1-c` 를 빨갛게 만드는 사본이 하나도 없어 그 대조가 **원리상 안 닿았다**. `A5-a` 만 이 규칙을 받고 `A1-a` 는 안 받은 비대칭이었다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **A2** ⟨결정론적⟩ 재는 자리 각각에 `pal touch <심볼>` 의 **전 출력**이 `touch/<이슈>.txt` 로 보존됐고, 머리에 **넷**이 적혔다 — 부른 명령 전문 · 그때의 HEAD SHA · **산출 둘째 줄의 심볼 ID**(`palimpsest@<sha>+worktree#<hex>`) · `A1-b` 의 blob 해시 · 통과 ⟨전사 2026-09-12⟩
- [x] **A2-b** ⟨결정론적⟩ **재실행 대조가 선다** — 같은 HEAD·같은 워킹트리에서 그 명령을 다시 돌려 `diff` 가 **바이트로 동일**함을 보이고 산출을 `effect/rerun-<이슈>.txt` 로 남긴다. ⟨`PM2-10`·`PM3-07` — `CA1-17` 이 *"다시 돌려도 바이트가 안 맞는다"* 를 근거로 재실행 대조를 **포기**했는데 **그 근거가 거짓이다.** 두 번 돌려 `diff` 한 결과가 동일했고, `#<hex>` 는 워킹트리 다이제스트가 아니라 **심볼마다 다른 심볼 ID** 다(`crates/pal-core/src/coord.rs:296`). 더 강한 것이 가능한데 더 약한 것을 쓰면서 **강한 것이 불가능하다고 잘못 적었다**⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **A2-c** ⟨결정론적⟩ ㉢ 의 산출이 **후보 목록**이면 그 사실과 전 출력을 그대로 보존하고, `A2` 의 머리 넷 중 **셋째가 원리상 없다**는 것을 적는다. ⟨`PM3-06` — 후보 목록 답의 근거 줄은 `Snapshot palimpsest@<sha>+worktree` 로 `#<hex>` 가 안 붙는다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **A3** ⟨결정론적⟩ 재는 자리 각각의 사전 등록이 **좌표를 적었다** — 바꿀 `파일:줄` 목록과 각 자리에 무엇을 쓸 것인가. 「`#79` 를 고친다」 같은 한 줄은 A3 을 못 채운다 · 통과 ⟨전사 2026-09-12⟩
- [x] **A3-a** ⟨결정론적 · A3 의 음성 대조⟩ 좌표 줄을 지운 사본이 A3 판정을 **통과하는지** 재고 산출을 `effect/negative-A3.md` 로 남긴다. 통과하면 A3 은 아무것도 안 잰다 · 통과 ⟨전사 2026-09-12⟩
- [x] **A4** ⟨결정론적⟩ 재는 자리 각각의 사전 등록에 **touch 전에 무엇을 봤는지**가 적혔다 — 읽은 파일 경로 · 돌린 검색 · 본 이슈 · **그리고 `observations/red.md` §5.8 「메인이 이미 오염됐다」의 표 전문** · 통과 ⟨전사 2026-09-12⟩
- [x] **A5** ⟨결정론적⟩ ★ **touch 가 실제 작업보다 앞선다** — `touch/<이슈>.txt` 커밋이 **그 자리의 첫 코드 변경 커밋의 조상이고 두 SHA 가 다르다.** 재는 자리와 커밋의 사상은 **사전 등록의 `파일:줄` 목록**이 진다(그 목록의 파일을 건드린 첫 커밋) ⟨`PM3-15` — 대상 선정을 산문으로 남기면 ⟨결정론적⟩ 태그가 거짓이 된다⟩ · 반증 ⟨전사 2026-09-12⟩
- [x] **A5-b** ⟨결정론적⟩ ★★ **커밋 순서만으로는 인과가 안 선다 — 바이트 앵커를 건다.** `touch/<이슈>.txt` 안의 `fun · <파일>:<줄> · … body <hash>` 줄이 **봉인 커밋 시점의 그 심볼 본문**에서 나온 값임을 보인다. ⟨`PM2-03`(격리 사본에서 재현)·`PM3-14`(유효성 **추정** — R3 은 사본을 안 돌렸다) — 재현된 것은 `PM2-03` 이다: 코드 변경을 **워킹트리에 먼저 써 놓고** touch 를 돌린 뒤 봉인→touch→변경 순으로 커밋하면 `A1`·`A1-b`·`A1-c`·`A5`·`A6` 이 **전부 통과**하면서 touch 는 변경을 이미 본 상태다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **A5-d** ⟨결정론적⟩ **touch 건 심볼이 변경 대상인 자리에서는 `body <hash>` 가 변경 전후로 다르다.** 변경 대상이 **아닌** 자리에서는 그 사실과 까닭을 사전 등록에 적고 `A5-b` 를 **대조불가**로 판정한다. ⟨`CA2-01` — ㉠ 의 고침 자리는 `nodes_of`(`crates/pal-cli/src/ledger.rs:334`)인데 `#79` 가 이름으로 지목하는 `identity_ceiling`(`crates/pal-core/src/coord.rs:259`)은 **안 고쳐도 `#79` 가 닫힌다.** 그러면 앵커가 초록이 될 길이 원리상 없다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **A5-e** ⟨결정론적⟩ **앵커가 못 서는 두 자리를 미리 적는다** — ⓐ 답이 **후보 목록**이면 `fun · … body` 줄 자체가 없다(㉢) ⟨`CA2-02`⟩ · ⓑ `body <hash>` 는 **공백·주석 변경에 불변**이라 포매팅만 건드린 변경은 이 앵커가 못 잡는다 ⟨`CA2-03` — 격리 사본에서 재현: 본문을 한 줄로 병합하고 주석을 넣어도 해시가 그대로였고, 조건식을 바꾸자 움직였다⟩. **둘 다 「원리상 못 잼」이지 잔여가 아니다** · 통과 ⟨전사 2026-09-12⟩
- [ ] **A5-c** ⟨정반합⟩ **`pal touch` 의 「워킹트리 일치」 줄을 증거로 안 쓴다** ⟨`CA2-14` — *"모든 산출에서 구조적으로 참"* 은 열거가 아니라 코드 독해 논증이라 ⟨결정론적⟩이 아니다⟩ — 그 줄이 `--at` 없이 부른 모든 산출에서 **구조적으로 참**임을 확인하고 그 사실을 게이트에 적는다. ⟨`PM2-04` — 더러운 워킹트리에서도 `일치` 를 찍는다. 사람은 그 줄을 *"touch 를 돌릴 때 고친 것이 없었다"* 로 읽는다. 제품의 결함이고 이 회차는 **관측만 하고 안 고친다**⟩
- [x] **A5-a** ⟨결정론적 · `A5`·`A5-b` 의 음성 대조⟩ 사본 **둘**에서 판정이 **뒤집히는지** 재고 `effect/negative-A5.md` 에 둘 다 남긴다 — ⓐ touch 커밋을 변경 커밋 뒤로 옮긴 사본에서 `A5` 가 빨개진다 · ⓑ 코드 변경을 **워킹트리에 먼저 써 놓고** touch 를 돌린 사본에서 `A5-b` 의 `body <hash>` 가 봉인 커밋 시점의 본문과 **안 맞는다**. **둘 중 하나라도 안 뒤집히면 그 축의 조건은 항등식이다** — ⓐ 가 안 뒤집히면 `A5`, ⓑ 면 `A5-b` 다. 한 축만 초록이어도 `A5-a` 는 **불통과**다 ⟨`PM2-03` 이 격리 사본에서 재현한 순서가 바로 ⓑ 인데 `A5-b` 에만 대조가 없었다. 조건을 늘리지 않으려고 `A5-a` 에 축을 하나 더 걸었다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **A6** ⟨결정론적⟩ **심볼 선정이 봉인됐다** — 재는 자리마다 어느 심볼에 touch 를 걸지를 **사전 등록에 먼저 적고**, 그 뒤 `touch/<이슈>.txt` 로 **보존하는 산출은 심볼마다 하나**다. ⚠ **「한 번만 돈다」가 아니다** ⟨`PM3-08`·`PM3-09` — 착수 관측 §5.8 이 사전부검 R1 이 세 심볼에 이미 돌렸다고 적으므로 「한 번만」은 **착수 전부터 불통과**다. 그리고 ㉢ 은 후보 3 건이라 산출을 얻으려면 한 번 더 돌려야 해서 `A6` 과 원리상 배제됐다. **재실행은 허용하고 보존본을 하나로 잠그는 것**이 `CA1-06`(좋은 것만 남기기)을 막는 자리다 — `A2-b` 의 재실행 대조가 그것을 받친다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **A7** ⟨결정론적⟩ ★ **「봤다」를 남긴다** — 재는 자리 각각에, `touch/<이슈>.txt` 를 커밋한 **뒤** 코드 변경 **전에** `effect/<이슈>-readnote.md` 를 커밋한다. 그 파일은 **산출의 어느 줄을 읽었고 무엇을 알게 됐는지**를 줄마다 `touch/<이슈>.txt:N` 좌표와 함께 적는다. 커밋 순서는 `A1`·`A5` 와 같은 자로 판정한다 · 반증 ⟨전사 2026-09-12⟩
- [x] **A7-a** ⟨결정론적 · `A7` 의 음성 대조⟩ 사본 **둘**에서 판정이 **뒤집히는지** 재고 `effect/negative-A7.md` 에 둘 다 남긴다 — ⓐ `readnote` 커밋을 **변경 커밋 뒤로** 옮긴 사본에서 `A7` 이 빨개진다 · ⓑ `readnote` 커밋을 **touch 커밋 앞으로** 옮긴 사본에서 `A7` 이 빨개진다(산출이 서기 전에 「봤다」를 적은 것이라 인과가 뒤집힌다). **둘 중 하나라도 안 뒤집히면 그 경계는 항등식이다** — ⓐ 가 안 뒤집히면 뒤쪽 경계(변경 커밋 **전**), ⓑ 면 앞쪽 경계(touch 커밋 **뒤**)다. 한 축만 초록이어도 `A7-a` 는 **불통과**다 ⟨승인 앞 정정 — `A7` 은 경계 **둘**을 걸었는데 대조가 뒤쪽만 뒤집었고, `A1-a`·`A5-a`·`C2-a` 가 받은 「한 축만 초록이어도 불통과」를 `A7-a` 만 못 받고 있었다. **⟨조건평가 R2⟩ 에서 새로 선 여섯 중 하나라 감사를 한 번도 안 거쳤다**⟩ · 통과 ⟨전사 2026-09-12⟩

★★ **`A7` 이 없으면 원문 전제의 「본」이 안 재어진다** ⟨조건 평가 R2 의 `## 합이 의도를 덮나` ①⟩.
원문은 *"그 산출을 **본** 뒤의 변경이 달라졌는지 잰다"* 인데, `A5`·`A5-b` 가 재는 것은
**touch 가 코드를 본 시각**이지 **사람이 touch 산출을 읽었다는 사실**이 아니다. `C2` 의
귀속이 간접 증언이지만 **차선책 1 이 열리는 갈래(`C2` 가 0)에서는 「봤다」를 대는 자리가
하나도 안 남는다.**

### B — 실제 작업이 실제로 섰나

- [x] **B1** ⟨정반합⟩ 재는 자리 각각에 **코드 변경 커밋**이 있고, 그 이슈 본문이 요구한 것을 만족한다. ⚠ **차선책 2 를 쓰면 ㉠ 에 대해 이 조건과 `B5` 가 함께 깨진다** ⟨`CA2-13` — `C5` 에는 차선책 3 의 그 사실을 적어 뒀는데 여기만 안 적혀 있었다⟩. ⟨`CA2-15` — *"이슈 본문이 요구한 것"* 은 `#126` 처럼 갈래가 둘인 이슈에서 해석이라 기계가 못 판정한다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **B1-a** ⟨결정론적⟩ **㉢ 의 RED 를 작업 저장소에서 관측했다** — `pal query narrative.unbound` 는 `.palimpsest/intent.redb` 가 **있는데도** 실패한다. 산출을 `observations/red-129.txt` 로 남긴다. ⟨`CA1-01` — 앞 판은 *"그 파일이 없을 때만 난다"* 를 전제로 RED 자리를 파일 없는 곳으로 지정했다. **그 전제가 거짓이고**, 원인은 `write()` 가 `Handle::Reading` 과 `Handle::Absent` 를 **둘 다** 거부하는 것이다(`crates/pal-intent/src/store.rs:168-170`). 그 가지만 닫으면 조건은 초록이고 저장소는 빨간 채로 남는다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **B1-c** ⟨결정론적⟩ 재는 자리 각각에 **자동 시험이 섰다** — `#[test]` 또는 `xtask` 검사. ⟨`CA1-11` — 소유자 원문 *"E2E/통합/단위 테스트(결정론적/기계적 검증)가 가능하다면 무조건 작성한다"*⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **B1-d** ⟨결정론적⟩ **㉢ 의 GREEN 을 잰다** — 고친 뒤 `pal query narrative.unbound` 가 **오류 없이 값을 내고 종료값이 0** 이다. 산출을 `effect/green-129.txt` 로 남긴다. ⟨`CA2-10` — `B1-b` 를 철회하면서 *"그 명령이 실제로 성공한다"* 를 문면으로 요구하는 조건이 **하나도 안 남았다.** `B1-a` 는 RED 만 재고 `B1-c` 는 시험의 **존재**만 재서 무관한 시험 하나로도 채워진다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **B1-e** ⟨결정론적⟩ **`B1-c` 가 세운 시험이 그 자리의 RED 를 재는 시험이다** — 시험 이름과 그것이 재는 RED 를 사전 등록에 짝지어 적는다 ⟨`CA2-10`⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **B2** ⟨결정론적⟩ `cargo xtask check` 가 초록이고 **검사 수가 28 이상**이다. ⟨`CA1-15` — *"초록이다"* 만 적으면 **검사 하나를 지워도 통과한다.** 이 저장소가 실제로 겪은 사고의 형태다⟩ ⚠ **착수 값 정정** ⟨`CA1-14`⟩ — 28/28 은 **회차를 열기 전** 값이고, 회차 디렉터리를 커밋한 뒤 `conditions-audit/r1-raw.md` 가 서기 전에는 **27/28** 이었다. 회차가 스스로 만든 RED 다 · 통과 ⟨전사 2026-09-12⟩
- [x] **B3** ⟨결정론적⟩ `cargo test --workspace` 가 초록이고 **통과 수가 1028 이상**이다. **세는 법을 못 박는다** — 전 출력에서 `^test result:` 줄의 `passed` 를 합산한다(48 줄 · doc-test 6 묶음 포함). ⟨`CA1-16` — `#[test]` 는 **806** 이라 셈법이 다르면 값이 갈린다. 착수와 종료가 다른 법으로 세면 회귀 조건이 조용히 통과한다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **B4** ⟨결정론적⟩ 컴파일 경고가 **0** 이다 ⟨착수 시점 0⟩. `.rs` 전량 mtime 갱신 후 `cargo build --release --workspace` 와 `cargo check --workspace --all-targets` **각각**으로 잰다 — 증분 빌드는 경고를 다시 안 뱉는다 · 통과 ⟨전사 2026-09-12⟩
- [x] **B5** ⟨결정론적⟩ `#79` 가 요구한 것을 만족한다 — *"닫으려면 그 수를 내는 산출 경로가 필요하다. 손으로 센 수를 판정 표에 실으면 다음 회차가 그것을 못 되짚는다."* 「순서에 취약한 것」과 「그냥 L1 인 것」을 가르는 수가 **명령으로** 난다 · 통과 ⟨전사 2026-09-12⟩
- [x] **B5-a** ⟨결정론적⟩ **B5 의 모집단 정의가 사전 등록에 먼저 적혔다** — 무엇을 분모로 삼는지. ⟨`PM1-21` — 안 적으면 **어떤 수든** B5 를 채운다. `#79` 본문의 「464 · 7,156」은 합이 7,620 이고 지금 좌표 3,306 과 안 맞는다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **B6** ⟨결정론적⟩ ㉡ 의 고침이 `#126` 이 준 **두 갈래 중 어느 것인지**를 적는다 — *"「최근」이 판정에 안 쓰이면 그 낱말을 판정문에서 빼거나, 쓰인다면 커밋 시각으로 고른다."* **커밋 시각 갈래를 고른 경우에만** 「커밋이 0 인 회차」의 처분을 코드와 판정문에 명시하고 **그것을 재는 시험**을 세운다. 낱말을 빼는 갈래를 고르면 그 처분의 모집단이 **없고**, 그 사실을 적는 것이 이 조건이다 ⟨`CA2-11` — 앞 판은 뒤쪽 갈래를 전제해 **구현 선택 하나**를 재고 있었다⟩ ⟨`PM1-16`·`CA1-11` — 입력을 만들 수 있는 단위 시험 자리다⟩ · 통과 ⟨전사 2026-09-12⟩

### C — 차이를 쟀나

★★ **미끼 설계를 버렸다** ⟨사전부검 R2·R3 · `PM2-02`·`PM2-05`·`PM2-06`·`PM2-07`·`PM3-04`·`PM3-10`·`PM3-11`⟩.
앞 판은 귀속을 **「심볼 고유 줄」**로 한정했는데 두 라운드가 독립으로 같은 것을 냈다:

> **「심볼 고유 줄」은 「touch 가 말해 준 것」과 같지 않다.** `pal touch` 산출은 대부분
> 고정 템플릿이라 같은 파일 두 심볼 사이의 차이가 {이름 · 좌표 · 호출 수 · 미해소 목록 ·
> 토큰 크기} 로 **원리상 한정된다.** 그중 앞 둘과 마지막은 **구성상 무조건 고유**라 판정에
> 못 쓰고, 정작 작업을 바꿀 수 있는 줄(`※ 파일 간 해소 — ⓐ 1823/5767` 같은 것)은
> **미끼와 공유된다는 이유로 버려진다.** 그래서 ㉠·㉡ 이 **재기 전에 0** 이 됐다.

**대신 「내용 시험」을 쓴다** — 귀속된 줄이 담은 **사실**이 사전 등록에도 착수 관측 §5.8 에도
없으면 그것이 touch 가 **처음 말해 준 것**이다. 공통 줄도 귀속 대상이고, 미끼가 필요 없다.

- [x] **C1** ⟨결정론적⟩ 재는 자리 각각에 **사전 등록 ↔ 실제 변경**의 차이 목록이 `effect/<이슈>-delta.md` 로 섰고, 차이마다 **좌표 두 끝**이 붙었다 — `touch/<이슈>.txt:N` 이 원인이고 `파일:줄` 이 결과다 · 반증 ⟨전사 2026-09-12⟩
- [ ] **C2** ⟨정반합⟩ 그 차이 중 **touch 산출에 귀속되는 것이 1 건 이상**이다 ← **합격선 본체**
- [ ] **C2-b** ⟨정반합⟩ **귀속마다 「내용 시험」이 적혔다** ⟨`CA2-06` — *"그 줄이 담은 **사실**이 없다"* 는 의미론적 동일성 판단이다. ⟨결정론적⟩으로 두면 규약이 *"기계가 판정한다. **정반합을 안 쓴다**"* 로 못 박은 자리에 단일 에이전트가 들어간다 — `CA1-12` 가 `E3` 에서 잡은 것과 같은 형태다⟩ — 인용한 `touch/<이슈>.txt:N` 의 줄과, **그 줄이 담은 사실이 `plan/<이슈>-pre.md` 전문에도 `observations/red.md` §5.8 표에도 없다**는 것을 두 문서의 인용으로 보인다. 있으면 그 귀속은 **무효**다
- [x] **C2-a** ⟨결정론적 · C2·C2-b 의 음성 대조⟩ **사전 등록에 이미 적힌 사실을 담은 줄**을 일부러 귀속으로 제출해 `C2-b` 가 그것을 **무효로 잡는지** 재고, 산출을 `effect/negative-C2.md` 로 남긴다. 안 잡으면 `C2-b` 는 항등식이다. **그리고 축 하나를 더 건다** — ⓑ **무효로 잡힐 귀속만** 제출한 사본에서 `C2` 가 **빨개지는지** 재고 같은 파일에 남긴다. 안 빨개지면 `C2` 가 항등식이다(귀속이 전부 무효인데도 「1 건 이상」이 선다). **한 축만 초록이어도 `C2-a` 는 불통과다** ⟨기록 정합 4 차 — 태그는 ⟨`C2`·`C2-b` 의 음성 대조⟩ 인데 본문이 `C2-b` 만 쳤다. `A1-a`·`A5-a` 가 받은 규칙을 `C2-a` 만 못 받고 있었다⟩. ⚠ **심는 자와 채점하는 자가 다르다** — 미끼 줄은 메인이 고르고 무효 판정은 `C3` 의 새 에이전트가 한다 ⟨`CA2-07` — 귀속을 쓴 손이 자기 대조를 자기가 채점하면 그 대조는 항등식이다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **C3** ⟨결정론적⟩ 귀속 판정을 한 것은 **새 에이전트**이고, 프롬프트가 넷을 **인라인으로** 싣는다 — 사전 등록 전문 · touch 산출 전문 · 실제 변경 diff · §5.8 표 전문. **경로로 넘기지 않는다** ⟨`PM2-15` — 경로로 넘기면 판정자가 읽은 바이트가 사후에 복원되지 않아 `A1-c` 가 원리상 관측 불가가 된다⟩. 프롬프트 전문을 `effect/judge-prompt.md` 로 보존한다. **넷 다 바이트로 그대로 싣는다** — 자르거나 요약하면 `A1-c` 가 사전 등록 하나만 묶고 나머지 셋은 사후 복원이 안 된다 ⟨`CA2-09`⟩ · 반증 ⟨전사 2026-09-12⟩
- [x] **C3-a** ⟨결정론적⟩ **판정자에게 답을 안 담았다** — 프롬프트에 *"귀속이 있는지 재라"* 만 있고 *"귀속이 N 건인지 확인해라"* 가 없다. 띄운 인스턴스 **전수**를 `effect/judge-roster.md` 에 적고 **반환문을 하나도 안 버린다** ⟨`CA2-08` — 수를 적기만 하면 다섯을 띄워 유리한 하나만 남겨도 `C3-a`·`C3-b` 가 둘 다 통과한다. `A6` 이 touch 쪽에서 막은 것을 판정자 쪽에서는 안 막았다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **C3-b** ⟨결정론적⟩ **판정 원문이 전수로 보존됐다** — `effect/<이슈>-judge-<n>.md` 로 **띄운 만큼**. 요약이 아니라 반환문 전문이고, `judge-roster.md` 의 수와 파일 수가 같다 ⟨`CA1-07`·`CA2-08`⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **C4** ⟨정반합⟩ 차이마다 **방향**(나아졌다 / 나빠졌다 / 모른다)과 **「touch 가 말한 것이 참이었나」**가 근거와 함께 적혔다. ⚠ 방향은 합격선이 **아니다** · 반증 ⟨전사 2026-09-12⟩
- [x] **C5** ⟨결정론적⟩ **㉠·㉡·㉢ 셋 전부**에 대해 차이가 기록됐고 **0 인 자리도 그대로 적혔다.** 뺀 자리가 없다. ⚠ **차선책 3 을 쓰면 이 조건이 문면상 깨진다** — 그래서 차선책 3 은 **승격**이라고 적혀 있다 ⟨`PM2-11` — 앞 판은 이 한 자리만 안 고쳐 놓고 「고쳤다」를 개정 표에 적었다⟩ · 통과 ⟨전사 2026-09-12⟩

### D — `#66` 의 두 물음

- [x] **D1** ⟨결정론적⟩ `#66` 본문의 낡은 전제 둘 — 「결박 가능한 좌표 7 개 · 파일 1 개」 · 「추출기는 Kotlin·Java·JavaScript·TypeScript 넷뿐」 — 의 정정문이 **저장소 안에 선다**(`docs/gates/effect-confirmation.md`), 지금 값과 **무슨 명령으로 쟀는지** 함께. 그리고 같은 내용을 이슈에도 남긴다. ⟨`CA1-22` — *"정정했다"* 만 적으면 정정문이 어디 서는지가 없고, 이슈 본문이면 저장소 검사가 못 되짚는다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **D2** ⟨정반합⟩ `#66` 의 물음 1 — *"이 도구가 자기 자신을 큐레이션할 수 있어야 하는가"* — 에 답했다. ⚠ **규범 명제라 측정이 혼자 못 답한다** — 이 회차의 측정을 근거로 대고 판정은 정반합이 한다 · 통과 ⟨전사 2026-09-12⟩
- [x] **D3** ⟨결정론적⟩ `#66` 의 물음 2 — *"**Rust 추출기**인가, 아니면 ⑧을 **다른 저장소의 실사용**으로 다시 정의하는가"* — 에 답하되, **두 갈래의 현재 상태를 각각 명령으로 대야 한다.** ⟨`CA1-19` — 앞 판은 조건 문면에 답을 담아 **무엇을 쓰든 참**이었다. 명령이 값을 못 내면 이 조건은 반증된다⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **D4** ⟨결정론적⟩ `#66` 을 닫았다. ⚠ **`D2` 가 「아직 못 답한다」로 나면 안 닫는다** — 차선책 4 가 그 문이다 ⟨`CA1-18`⟩ · 통과 ⟨전사 2026-09-12⟩
- [x] **D4-a** ⟨결정론적⟩ **`#66` 을 현재형으로 인용하는 자리의 처분이 적혔다** — `corpus/criteria.toml:10530`·`:10886` · `docs/gates/F12.md:312`·`:398` · `scripts/f12-verify.py:559-560`(다섯 자리 실재 확인됨). ⚠ 어느 검사도 이슈 상태를 안 본다. **다섯을 한 규칙으로 덮지 않는다** ⟨`PM3-16`⟩: · 통과 ⟨전사 2026-09-12⟩
  - `docs/gates/F12.md:312`·`:398` 은 **동결 판정 문서**, `corpus/criteria.toml:10530`·`:10886` 은 **합격선 파일** — **안 고치고** 어디가 거짓이 되는지 적는 것이 처분이다
  - `scripts/f12-verify.py:559-560` 은 **문서가 아니라 돌아가는 분류 코드**이고, `D1` 이 정정하려는 바로 그 거짓 전제(*"추출기는 Kotlin·Java·JS·TS 넷뿐이다 … ⑧에 합산하지 않는다"*)를 **분류에 쓴다.** 그 자리의 처분은 **따로** 적는다 — 고칠지 말지를 포함해서. ⚠ `B2` 의 28 검사가 이 스크립트를 안 돌린다

### E — 종료

- [x] **E1** ⟨결정론적⟩ 게이트 `docs/gates/effect-confirmation.md` 가 절 이름 넷으로 섰고, 표준 표의 **검산이 맞다** — 넷의 합 = 등록 조건 수 · `intent.md` 의 조건 ID 집합과 **집합 같음** · 통과 ⟨전사 2026-09-12⟩
- [x] **E2** ⟨결정론적⟩ `## 효과` 에 **테스트도 CI 도 아닌 것이 이 회차의 산출을 돌린 출력**이 붙었고, **차이마다 방향이 나란히 적혔다** · 통과 ⟨전사 2026-09-12⟩
- [x] **E3** ⟨정반합⟩ 종료 보고 `report.md` 가 섰고 **네 이름이 없다**. ⟨`CA1-12` — 앞 판은 ⟨결정론적⟩으로 태그했는데 **규약이 이것을 반대 갈래의 예시로 직접 못 박는다**(`.claude/skills/round/SKILL.md:355` — *"결정론적이 아님 — 해석이 들어간다(… 「네 이름이 없다」…)"*). 결정론적으로 태그하면 정반합이 안 걸리고, 그 형태가 이 저장소에서 이미 실패한 자리다⟩ · 반증 ⟨전사 2026-09-12⟩
- [x] **E4** ⟨결정론적⟩ `findings.jsonl` 의 **열린 발견이 0** 이고 열린 금지역·실패가 없다 · 통과 ⟨전사 2026-09-12⟩
- [x] **E5** ⟨결정론적⟩ 회차의 **마지막 커밋 SHA** 에 `conclusion=success` 런이 붙었다 · 통과 ⟨전사 2026-09-12⟩
- [x] **E6** ⟨결정론적⟩ **`docs/plan/03-shortest-path.md` §4 의 3 단계 행과 `docs/plan/02-order.md` 의 대응 줄이 이 회차의 결과로 갱신됐다** ⟨`PM1-12`⟩ · 통과 ⟨전사 2026-09-12⟩

## 차선책

1. **`C2` 가 0 이면** — **반증으로 적고 멈춘다.** 더 재지 않는다. 규약 §9 가 *"반증은
   실패가 아니다"* 를 못 박는다. ★ **「멈춘다」는 `C` 절에만 걸린다** — `D` 절(`#66` 처분)과
   `E` 절(게이트·보고·런)은 **그대로 다 한다.** ⟨`PM3-17` — 앞 판은 그 범위를 안 적어서
   가장 일어나기 쉬운 갈래의 종료 절차가 미정이었다⟩ `D2` 는 그 반증을 **근거로** 답한다.

2. **`B5` 의 산출 경로가 이 회차에서 안 서면** — `#79` 를 안 닫고 *"그 자리는 touch 효과
   측정의 표본으로만 썼다"* 를 적는다. ⚠ **메인이 혼자 발동하지 않는다** — 승격이다.
   ★ **다만 스키마를 먼저 건드리지 마라** ⟨`PM2-14` — `pal ledger --symbols` 가 심볼마다
   `identity` 를 **이미 낸다**(실측 3306 · ordinal 3300). 「순서에 취약한 것」은 그 산출의
   파생으로 선다. `SymbolNode`·`schema/graph.toml` 로 가는 것은 **만들 필요가 없는 것을
   만드는 자리**다⟩

3. **재는 자리 중 하나가 회차를 삼키면** — 그 자리를 표본에서 빼고 남은 자리로 판정한다.
   **하한 1 은 지킨다.** ⚠ **승격이다** — `C5` 가 *"㉠·㉡·㉢ 셋 전부"* 를 요구하므로
   이 차선책을 쓰면 `C5` 가 문면상 깨진다. 그 사실을 `C5` 에 적어 뒀다.

4. **`D2` 가 「아직 못 답한다」로 나면** — `#66` 을 **안 닫고** 그 사유를 적는다. `D4` 는
   그 갈래에서 「닫지 않았다 · 까닭」으로 판정한다.

★ **번호를 등장 순서대로 다시 적었다** ⟨`PM2-12` — 앞 판이 `1·2·4·3` 순으로 적어 놓아서
마크다운이 **등장 순서로 다시 매기면 3 과 4 가 뒤바뀐다.** `D4` 가 *"차선책 4 가 그 문이다"*
로 번호를 참조하는데 그 문이 표본 빼기를 가리키게 된다⟩


## 범위 밖

- **R-19 — 자기 저장소 자기적용의 편향.** 이 회차는 **「이 저장소에서 섬」까지만** 주장한다.
  재는 자와 재이는 자가 같은 저장소인 것은 이 회차가 **원리상 못 없앤다.**
- **★ 그보다 좁은 편향 하나가 더 있다 — 사전 등록을 쓰는 손과 실제 변경을 쓰는 손이
  같다** ⟨`PM1-18`⟩. 사전 등록은 **시각**을 봉인하지 **성실성**을 못 봉인한다. 얇게 쓰면
  `C2` 가 구성상 만족되고, 지나치게 두껍게 쓰면 touch 가 더 낼 것이 없어 `C2` 가 구성상
  0 이 된다. **이 회차는 그것을 못 없앤다** — `A3`·`A4` 가 좌표와 조사 기록을 요구해 폭을
  좁히는 것이 할 수 있는 전부다. 종료 보고의 `## 원리상 못 잰 것` 이 이것을 진다.
- **남의 저장소 실사용.** 코퍼스 저장소에는 「실제 작업」이 없어 합격선 문면을 벗어난다.
- **`pal touch` 아닌 표면의 효과.** `pal doctor` · `pal narrative` · 스킬의 효과는 안 잰다.
  ⚠ touch 가 스스로 가리킨 후속 질의는 **그 산출의 연장으로 센다.**
- **「나아졌다」의 판정 기준.** 방향은 `C4` 로 기록하되 합격선이 아니다.
- **`pal touch` 의 멤버 해소(`x.foo()`).** `identity_ceiling` 의 호출자 0 이 거짓 음성인
  까닭이고, touch 자신이 *"이 회차의 범위 밖"* 이라고 적는다. **이 회차도 안 고친다** —
  `C4` 로 그 거짓 음성을 기록만 한다.
- **Markdown 결박(`#69`).** 이 저장소 문서가 미결박인 것은 이 회차가 안 고친다.
- **「추출기가 몇인가」가 저장소에서 갈린 것.** ⚠ **수를 여기 두 번 적지 않는다** — `observations/red.md` ⓑ 가 *「저장소가 **다섯 값**으로 적는다」* 로 적고 전수 표가 그 아래 있다. 「다섯」은 **값의 종수**이고 **자리 수가 아니다**(`D4-a` 의 「다섯 자리」와 다른 수다). `D1` 이 `#66` 하나를 정정할
  뿐이다. 나머지는 동결 판정 문서·합격선 파일이고 앞 회차가 이미 `범위밖` 으로 처분했다
  (`2026-08-20-rust-extractor/findings.jsonl:44` `PM2-S14`).
- **소유자가 막은 열넷** — `#136` `#137` `#138` `#139` · `#135` 의 남은 ⓑ 몫 · `#131` ·
  `#133` `#78` `#77` `#57` `#63`~`#65` `#58`.
- **`#102` 의 설계 물음 넷.** 착수 전에 표본에서 뺐다 — 아래 `## 개정`.
- **참조 분모가 갈린 원인**(ⓐ 5767↔5760 · ⓑ 1062↔1061). 관측만 하고 원인은 안 판다.
- **`pal touch` 의 `크기` 줄이 무엇을 재는가** ⟨`MS-06` · 루프 ㉡ 에서 나왔다⟩.
  산출이 `잰 것: 4108 바이트` 라 적는데 화면 본문은 **3346 바이트**이고,
  `crates/pal-cli/src/evidence.rs:53-55` 의 주석은 *"이 화면처럼 들여쓴 산출은 그보다 크다"*
  라 적어 **방향이 반대**다. `A5-c` 의 「워킹트리 일치」와 같은 부류 — **제품의 표시 결함이고
  이 회차는 관측만 한다.** 고치는 자리는 `pal` 쪽이고 이 회차의 재는 자리 셋 밖이다.
- **`pal touch` 의 「호출자 N」이 파일 간 미해소 몫을 표시 없이 뺀다** ⟨`MS-08` · 루프 ㉠⟩.
  `nodes_of` 의 호출자를 **3** 이라 했고 실제는 **4** 다(`crates/pal-cli/src/defect.rs:326`
  의 `ledger::nodes_of(…)` 가 빠졌다). 같은 화면이 해소율(`ⓐ 1833/5787`)을 싣지만 **그 수
  옆에는 표시가 없다.** `pal` 의 표시는 이 회차가 관측만 한다 — **다만 이것은 이 회차가
  재려는 물음 그 자체에 대한 오답이라 게이트의 `## 효과` 가 진다.**
- **심볼 한 줄의 `identity ordinal` 이 원인을 안 가르는 것** ⟨`MS-09` · 루프 ㉠⟩.
  ㉠ 이 갈래 ⓑ(세어 올린다)를 골랐으므로 **대장의 수는 갈리고 심볼 한 줄은 안 갈린다.**
  2층에 그 값이 없어 갈래 ⓐ(저장한다) 없이는 원리상 못 고친다 — **사전 등록 `plan/79-pre.md`
  §3 에 미리 적고 왔다.**
- **clippy `pedantic` 축.** 착수 관측이 `cargo check` 로만 쟀다. ⚠ **CI 에도 clippy 잡이
  없다** — 사전부검이 확인했고(`ci.yml:121-137`) 그래서 `B4` 와 `E5` 가 안 갈린다.

## 개정

| 언제 | 무엇을 | 왜 |
|---|---|---|
| 2026-09-11 ⟨착수 전⟩ | 재는 자리 ㉠ 을 **`#102` → `#79`** 로 바꿨다 | 메인이 프론티어 한 줄만 보고 `#102` 를 권했는데, 본문에 *"답해야 할 것"* 이 **넷**이고 전부 설계 결정이라 회차를 삼킨다. 메인이 자기 권고를 정정해 올렸고 소유자가 `#79` 를 골랐다 |
| 2026-09-11 ⟨사전부검 R1⟩ | 조건이 **26 → 35**. 더한 것 아홉 — `A1-b`(봉인 blob 해시) · `B1-a`(㉢ RED 관측 자리) · `B5-a`(모집단 정의 선등록) · `B6`(미커밋 회차 처분) · `C2-b`(심볼 고유 줄 — **그 정의는 뒤에 버려졌다**) · `C2-c`(미끼 선정 규칙 — **뒤에 빠졌다**) · `C3-a`(팔마다 판정자) · `D4-a`(`#66` 인용 다섯) · `E6`(계획 문서 갱신) | 사전부검 21 시나리오 중 금지역 **일곱** · 실패 넷 (앞 판이 「여섯」으로 적었다 — 원 반환문과 원장이 둘 다 일곱이다). 처리 방침은 `premortem/r1-raw.md` 와 원장이 진다 |
| 2026-09-11 ⟨사전부검 R1⟩ | `C2` 를 **「귀속된 줄이 심볼 고유 줄이다」로 강화**하고 `C2-a` 의 미끼를 **같은 파일의 다른 심볼**로 다시 세웠다 | 앞 판의 미끼는 두 방향 다 못 잰다 — 진짜와 **38/52 줄 동일**이라 공통 줄 귀속이 미끼에도 나고, `A2` 의 머리가 미끼 심볼을 한 줄에 드러낸다 ⟨`PM1-06`·`PM1-07`⟩. ⚠ **그 뒤 ⟨사전부검 R2·R3⟩ 이 둘 다 뒤집었다 — 지금 `C2` 에 「심볼 고유 줄」이 없고 `C2-a` 의 미끼는 「사전 등록에 이미 적힌 사실을 담은 줄」이다** |
| 2026-09-11 ⟨사전부검 R1⟩ | 조건 문면을 **「세 자리 각각」 → 「재는 자리 각각」** | 차선책 3 을 쓰는 순간 조건 일곱이 문면상 동시에 깨져 **등록된 차선책인데 승격 없이는 못 쓰게** 됐다 ⟨`PM1-10`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | `## 순서` 절을 새로 세웠다 — **㉡ 을 가장 먼저 닫는다** | ㉡ 의 대상이 `E1`·`E4`·`B2` 를 판정하는 검사 그 자체다. §11③ 의 **금지역 예외**에 걸린다 ⟨`PM1-17`⟩. ⚠ **⟨사전부검 R2·R3⟩ 이 「과장」으로 깎았다가 ⟨기록 정합 4·5차⟩ 가 되살렸다.** `PM1-17` 원문의 셋 중 **틀린 것은 `E4` 하나**다 — ⓐ 조건 ID 집합 같음(`:6198`) · ⓑ 하한(`:6262-6269`) · ⓒ 「조건 0 이면 실패」(`:6286`)는 **전부 이 검사에 실재**하고, 열린 발견(`E4`)만 `check_finding_closure`(`main.rs:645`)라는 **다른 검사**가 잰다. ㉡ 을 먼저 닫는 결정은 **근거째 서 있다** |
| 2026-09-11 ⟨사전부검 R1⟩ | `## 재는 자리 셋` 에 **결박 0 과 축 명시**를 더했다 | 세 심볼 전부 결박이 0 이라 **목표 1(큐레이션)은 이 표본 위에서 원리상 안 재어진다.** 재는 것은 목표 2(컨설팅)다 ⟨`PM1-02`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | `## 범위 밖` 에 **측정자=피측정자 편향**을 더했다 | R-19 는 「같은 저장소」만 적었는데, 사전 등록을 쓰는 손과 변경을 쓰는 손이 같다. **원리상 못 없앤다** ⟨`PM1-18`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | 착수 관측의 **죽은 링크를 고치고** 워킹트리 문장(`observations/red.md` 머리 인용구 — **절 이름이 아니다**)을 잰 시점에 묶었다 | 내 관측 파일이 `cargo xtask check` 를 **26/28 로 떨어뜨리고 있었다.** 실측으로 확인하고 고쳤다 ⟨`PM1-11`⟩ |
| 2026-09-11 ⟨사전부검 R1⟩ | 착수 관측에 **§5.8 「메인이 이미 오염됐다」**를 새로 적었다 | 사전부검이 사전 등록 **전에** 세 심볼의 `pal touch` 를 돌려 결과를 메인에 요약해 줬다. 그 목록은 `C2` 의 귀속에서 뺀다 |
| 2026-09-11 ⟨조건평가 R1⟩ | 조건이 **35 → 43**. `A5`·`A5-a`(touch → 실제 작업) · `A1-c`(봉인이 판정에 물린다) · `A2-a`(미끼 산출 보존 — **뒤에 철회·제거됐다**) · `A6`(심볼 선정 봉인) · `B1-b`(종료값 — **뒤에 철회·제거됐다**) · `B1-c`(자동 시험) · `C3-b`(진짜 팔 판정 원문) | 평가가 **걸린 자리 22 · 실패 4** 를 냈다. 원문 전제의 절반(*"실제 작업 **앞에** 부르고"*)을 재는 조건이 **하나도 없었다** ⟨`CA1-02`⟩ |
| 2026-09-11 ⟨조건평가 R1⟩ | 완수 조건 절 머리에 **「재는 자리 셋」의 정의**와 **경로 뿌리**를 못 박았다 | 조건 열둘이 「재는 자리 각각」을 쓰는데(⚠ **감사자 반환문의 수를 그대로 옮겼다 — 실물은 감사 시점 `7b4ddaa` 에 6 · 방침 시점 `b108487` 에 7 · 지금 8 이다. 남의 판정 수라 안 덮고 표시만 단다**) 그 집합이 조건 절에 없었다. 사후에 정해지면 `C5` 가 항등식이 된다 ⟨`CA1-10`·`CA1-20`·`CA1-21`⟩ |
| 2026-09-11 ⟨조건평가 R1⟩ | **`B1-a` 의 전제를 뒤집었다** | *"`intent.redb` 가 없을 때만 실패한다"* 가 **거짓**이다. 메인이 직접 재서 확인했다 — 그 파일이 있는데도 실패한다. 사전부검이 `pal narrative`(도는 명령)와 섞었다 ⟨`CA1-01`⟩ |
| 2026-09-11 ⟨조건평가 R1⟩ | `C2-b` 의 정의를 **`comm` → `grep -Fxvn -f`** 로 바꿨다 | `comm` 이 정렬 유무로 **두 값**을 낸다(공통 23 ↔ 38). 정렬하면 줄 번호가 사라져 `C1` 이 요구하는 좌표와 안 맞물린다 ⟨`CA1-04`⟩. ⚠ **그 뒤 ⟨사전부검 R2·R3⟩ 이 이 정의를 버렸다 — 지금 `C2-b` 는 명령이 아니라 「내용 시험」이다** |
| 2026-09-11 ⟨조건평가 R1⟩ | `C2` 에 **「§5.8 에 이미 적힌 사실이 아니다」**를 더했다 | 배제 규칙이 관측 파일에만 살았다. **판정 자리에 없으면 없는 것과 같다** ⟨`CA1-03`⟩. ⚠ **그 뒤 ⟨사전부검 R2·R3⟩ 이 이 구절을 `C2` 에서 `C2-b` 로 옮겼다 — 지금 `C2` 의 문면에는 §5.8 이 없다** |
| 2026-09-11 ⟨조건평가 R1⟩ | 갈래 태그를 **절 → 조건**으로 옮기고 `E3` 을 **정반합**으로 고쳤다 | `E3`(*"네 이름이 없다"*)은 규약이 **반대 갈래의 예시로 직접 못 박은 것**이다(`SKILL.md:355`) ⟨`CA1-12`·`CA1-13`⟩ |
| 2026-09-11 ⟨조건평가 R1⟩ | `B2` 에 **검사 수 하한 28**, `B3` 에 **세는 법**을 적었다 | `B2` 는 검사를 지워도 통과했고, `B3` 의 1028 은 `#[test]` 806 과 셈법이 달랐다 ⟨`CA1-15`·`CA1-16`⟩ |
| 2026-09-11 ⟨조건평가 R1⟩ | 차선책 **4** 를 더했다 — `D2` 가 「못 답한다」면 `#66` 을 안 닫는다 | 앞 판은 `D2` 의 결과와 무관하게 `D4` 가 「닫았다」를 강제했다 ⟨`CA1-18`⟩ |
| 2026-09-11 ⟨사전부검 R2·R3⟩ | **미끼 설계를 버렸다** — `A2-a` 를 **철회로 판정**하고(목록 제거는 뒤의 ⟨조건평가 R2⟩ 가 했다 · `C2-c` 의 목록 제거는 **아래 `43 → 46` 행**이 진다) `C2-b` 를 「내용 시험」으로 다시 썼다. **`C2-a` 의 미끼도 「같은 파일의 다른 심볼」 → 「사전 등록에 이미 적힌 사실을 담은 줄」로 다시 세웠다** | 「심볼 고유 줄」이 「touch 가 말해 준 것」과 같지 않다. 같은 파일 두 심볼의 차이가 원리상 {이름·좌표·호출 수·미해소 목록·토큰 크기}뿐이라 **㉠·㉡ 이 재기 전에 0** 이었다 |
| 2026-09-11 ⟨사전부검 R2·R3⟩ | **`B1-b` 를 철회로 판정했다** (목록 제거는 뒤의 ⟨조건평가 R2⟩ 가 했다) | *"종료값 0"* 이 **거짓**이다. 두 라운드가 각각 네·세 변형으로, 메인이 세 형태로 다시 재서 전부 **1** 이었다 |
| 2026-09-11 ⟨사전부검 R2·R3⟩ | 조건이 **43 → 46**. 넷을 세우고(`A5-b` 바이트 앵커 · `A2-b` 재실행 대조 · `A2-c` ㉢ 후보 목록 처분 · `A5-c` 워킹트리 줄 안 씀) `C2-c` 하나를 뺐다. `A6` 은 「보존본 하나」로 고쳤다 | 커밋 순서만으로는 인과가 안 선다(격리 사본 재현) · `CA1-17` 의 근거가 거짓이라 재실행 대조가 **실제로 가능**했다 · `A6` 의 「한 번만」은 §5.8 때문에 착수 전부터 불통과였다 |
| 2026-09-11 ⟨조건평가 R2⟩ | 조건이 **46 → 50**. 여섯을 세우고(`A5-d`·`A5-e` 앵커가 못 서는 자리 · **`A7`·`A7-a` 「봤다」를 남긴다** · `B1-d` ㉢ 의 GREEN · `B1-e` 시험이 그 RED 를 잰다) **철회한 둘(`A2-a`·`B1-b`)을 목록에서 뺐다** — 46+6−2=50. 그 제거는 아래 행이 까닭을 진다 | 평가가 **걸린 자리 15 · 실패 6 · 기각 10** 을 냈다. ★ **원문의 「본」을 재는 조건이 하나도 없었다** — `A5`·`A5-b` 가 재는 것은 *touch 가 코드를 본 시각*이지 *사람이 산출을 읽었다는 사실*이 아니다 ⟨합①⟩ |
| 2026-09-11 ⟨조건평가 R2⟩ | **「철회」한 조건 둘을 목록에서 뺐다** | `E1` 의 표준 표는 판정이 **넷뿐**이고 `xtask` 가 게이트 ↔ `intent.md` 의 **양방향 집합 같음**을 강제한다. 「철회」가 갈 칸이 없어 착수 전부터 충돌이었다 ⟨`CA2-12`⟩. **철회한 것은 조건이 아니다** — 이 표가 이력을 진다 |
| 2026-09-11 ⟨조건평가 R2⟩ | `A5-d` 를 세워 `A5-b` 를 **심볼이 변경 대상일 때만** 걸리게 하고, `A5-e` 로 못 서는 두 자리를 미리 적었다 (한정은 `A5-b` 문면이 아니라 `A5-d` 안에 있다) | ㉠ 의 고침 자리는 `nodes_of` 인데 touch 를 건 것은 `identity_ceiling` 이고 **그건 안 고쳐도 `#79` 가 닫힌다** — 앵커가 초록이 될 길이 원리상 없었다 ⟨`CA2-01`⟩. 그리고 `body <hash>` 는 **공백·주석 변경에 불변**이다 ⟨`CA2-03`⟩ |
| 2026-09-11 ⟨조건평가 R2⟩ | `C2-b`·`B1`·`A5-c` 를 **⟨정반합⟩**으로 고치고 `C3-a`·`C3-b` 에 **전수 보존**을 걸었다 | ⚠ **감사자 원문(`conditions-audit/r2-raw.md:16-19`)은 `C2-a` 를 더한 넷이다 — `C2-a` 는 아직 ⟨결정론적⟩이고 왜 안 고쳤는지가 표에도 원장에도 없다**(기록 정합 5 차가 드러냈다 · 처분은 승인 뒤 루프가 진다). 든 셋은 의미론적 판단인데 ⟨결정론적⟩이었다 — `CA1-12` 가 `E3` 에서 잡은 것과 같은 형태 ⟨`CA2-06`·`15`·`14`⟩. 판정자를 다섯 띄워 유리한 하나만 남겨도 통과했다 ⟨`CA2-08`⟩ |
| 2026-09-11 ⟨조건평가 R2⟩ | `## 재는 자리 셋` 에 **㉢ 이 `C2` 모집단에 착수부터 0** 임을 적었다 | `pal touch write` 의 후보 목록 가지에서는 사실 줄이 아예 안 찍힌다. **하한 1 을 실제로 지는 것은 ㉠·㉡ 둘이다** ⟨합②⟩ |
| 2026-09-11 ⟨기록 정합⟩ | `A5-a` 에 **축 ⓑ 를 더했다** — 워킹트리에 먼저 쓴 사본에서 `A5-b` 의 앵커가 빨개지는지 잰다. **조건 수는 50 그대로다** | ★★ 가 붙은 `A5-b` 에만 음성 대조가 없었다. `A5-d`·`A5-e` 는 대조가 아니라 적용 범위를 좁히는 단서다. 규약 `SKILL.md:605` 가 *"가르는 물음: 「사본을 부수면 정말 빨개지는가.」"* 로 못 박고, 이 회차 `state.md` 의 「걸리는 자리」 4 가 *"음성 대조 자신이 항등식일 수 있다 — 세운 뒤 끄면 정말 빨개지는지 돌려 봐라"* 로 받은 자리 |
| 2026-09-11 ⟨기록 정합⟩ | 이 표의 **⟨조건평가 R1⟩ 두 행(`C2-b` 의 `grep` · `C2` 의 §5.8)에 「그 뒤 뒤집힘」을 달았다** (`C2-b` 의 `grep -Fxvn -f` · `C2` 의 §5.8 배제) | 두 행 다 그 시점에는 참이었으나 뒤 라운드가 뒤집었다. 표를 끝까지 안 읽으면 **현재 문면을 틀리게 복원한다** — `comm` 과 `grep -Fxvn` 이라는 낱말이 파일에 남은 자리가 그 한 행뿐이다 |
| 2026-09-11 ⟨기록 정합⟩ | ⟨사전부검 R1⟩ 행의 **「26 → 34 · 더한 것 여덟」을 「26 → 35 · 아홉」으로** 고치고, ⟨사전부검 R2·R3⟩ 행에 **43 → 46** 을 적었다 | 그 행이 열거한 ID 가 실제로 **아홉**이고(`A1-b`·`B1-a`·`B5-a`·`B6`·`C2-b`·`C2-c`·`C3-a`·`D4-a`·`E6`) 26+9=35 다. 다음 행이 **35** 에서 이어받는데 앞 행이 34 로 끝나 계보가 한 칸 끊겨 있었다. 43→46 은 어느 행에도 없었다 |
| 2026-09-11 ⟨기록 정합 · 2차⟩ | **독립 검증이 이 표에서 찾은 어긋남 넷을 더 고쳤다** — `43 → 46` 행의 열거를 셋 → 넷(`A2-c` 누락) · `A2-a`·`B1-b` 제거의 **이중 기장** 해소(철회 판정 ≠ 목록 제거) · `C2-a` 의 미끼 재정의를 표에 처음 적음 · `A5-b` 한정이 `A5-d` 안에 있음을 명시. 그리고 `A5-a` 에 **한 축만 초록일 때의 판정**을 못 박았다 | 앞 회차가 `PM2-11` 로 이름 붙인 것이 *「고쳤다고 적고 안 고침」* 인데, 이 표는 **「고치고 안 적음」과 「적었는데 뒤집힘」** 둘을 더 갖고 있었다. `C2-a` 는 표에 한 번도 안 적혀서 **표만 읽으면 낡은 미끼로 복원된다** — 이번에 고친 두 행과 같은 실패 형태다 |
| 2026-09-11 ⟨기록 정합 · 3차⟩ | **독립 검증 2 차가 찾은 여섯을 고쳤다** — ⟨사전부검 R1⟩ 구간의 **표시 없는 뒤집힘 둘**(`C2`·`C2-a` 강화 · `## 순서` 의 근거)에 ⚠ · 「금지역 **여섯** → **일곱**」(원 반환문·원장이 둘 다 일곱) · `46 → 50` 행에 **제거 둘을 명기**(46+6=52 였다) · `C2-c` **이중 기장** 해소 · `A1-a` 에 **사본 ④ 와 판정 규칙**. **조건 수는 50 그대로다** | 2 차 수정이 ⟨조건평가 R1⟩ 두 행만 보고 **⟨사전부검 R1⟩ 구간을 안 봤다.** 같은 실패형이 앞 구간에 그대로 남아 있었다 — 범위를 좁혀 고치면 고친 자리만 깨끗해진다 |
| 2026-09-11 ⟨기록 정합 · 4차⟩ | **표가 아니라 문면이 코드와 안 맞는 자리를 고쳤다** — `## 순서` 의 *「통과/불통과 경로에 안 물린다」*가 **거짓**이었다(`xtask/src/main.rs` 를 직접 읽었다: `최근_끝난` 이 `:6265-6269` 로 `problems` 에 들어가고 `:6295` 가 `bail!` 한다). 함께: ⟨조건평가 R1⟩ `35 → 43` 행의 `A2-a`·`B1-b` 에 제거 표시 · **`C2-a` 에 축 ⓑ 와 판정 규칙**(태그는 `C2` 도 친다는데 본문이 `C2-b` 만 쳤다) · `touch.rs` 좌표 한 줄 밀림 · `PM3-14` 공동 귀속. **조건 수는 50 그대로다** | 앞의 정합 셋이 전부 *「표가 파일 문면과 맞나」* 만 봤고 *「파일 문면이 코드와 맞나」* 는 **한 번도 안 봤다.** 축이 하나 빠져 있었다 |
| 2026-09-11 ⟨기록 정합 · 5차⟩ | **4 차가 새로 넣은 줄에서 좌표 넷을 되잡았다** — `:6263` 은 `problems` 가 아니라 **통과 쪽 문자열**이고 실제 자리는 `:6265-6269` · 빈-모집단 가드는 `.is_some()` 만 봐서 **「둘 다」가 과했다**(민감한 축은 하한 하나) · `## 범위 밖` → **`## 금지역`** · `store.rs:144-171` → **`:168-170`**. 함께: ⟨사전부검 R1⟩ `## 순서` 행의 「과장」을 **`E4` 하나만 참**으로 갈랐고, ⟨조건평가 R2⟩ 태그 행에 **`C2-a` 가 감사자 원문의 넷째인데 미처분**임을 적었고, `#135` 가 **아직 OPEN** 이며 ⓑ 에 반증이 달렸음을 적었다. **조건 수는 50 그대로다** | 4 차가 *「파일 문면이 코드와 맞나」* 라는 축을 열면서 **그 축에서 자기가 다시 걸렸다.** 코드를 열어 읽고도 `if` 와 `else` 가지를 바꿔 짚었다 — 「확인했다」가 **무엇을 확인했는지**까지 적혀야 하는 자리다 |
| 2026-09-11 ⟨기록 정합 · 6차 · 상한에서 닫음⟩ | **5 차가 넣은 줄의 좌표 셋과 남은 수 셋을 고쳤다** — 감사자 원문 `r2-raw.md:13-18` → **`:16-19`** · **자기참조를 줄 번호에서 ⟨라운드 태그⟩ + 행 이름으로 바꿨다**(표가 길어질 때마다 밀려서 1·4·5 차가 전부 어긋나 있었다) · 규약 인용의 출처를 **`SKILL.md:605` + 이 회차 `state.md`** 로 갈랐다 · 「다섯 자리」는 **값의 종수**이지 자리 수가 아님을 적었다 · 「조건 열둘」에 **감사자 수임(실물 6·7·8)** 을 표시했다. **조건 수는 50 그대로다** | 정합을 다섯 판 돌렸고 **여기서 닫는다** — 규약이 *「끝은 상한이 정한다」* 로 못 박은 자리다. 남은 것(`A7-a` 의 자기 판정값 · `CA2-21` 의 `유효성` · 「조건 열둘」의 실수)은 **승인 뒤 루프가 진다** |
| 2026-09-11 ⟨승인 앞 정정⟩ | **`A7-a` 에 축 ⓑ 를 더하고 자기 판정값을 달았다** — `readnote` 커밋을 touch 커밋 **앞으로** 옮긴 사본을 더하고 「한 축만 초록이어도 불통과」를 걸었다. **조건 수는 50 그대로다** | 앞 판은 *"뒤집히는지 재고 남긴다"* 에서 끝나 **안 뒤집혀도 통과로 적을 수 있었다**(금지역 「측정이 죽은 가지」). 그리고 `A7` 이 건 경계는 둘인데(touch 커밋 **뒤** · 변경 커밋 **전**) 사본은 뒤쪽만 뒤집었다. `A1-a`·`A5-a`·`C2-a` 가 받은 규칙을 이 조건만 못 받은 비대칭이다. **⟨기록 정합 · 6차⟩ 가 「넘기는 것」으로 적어 둔 셋 중 하나이고, 그 계열은 상한에서 닫혔으므로 판을 더 얹지 않고 여기서 닫는다** ⟨`MS-05`⟩ |
| 2026-09-12 ⟨정반합 R1 · 승격⟩ | **조건 문면은 한 글자도 안 바꿨다.** 바뀐 것은 ⑴ ㉠ 의 셈법(버킷 ① 이 그룹 단위가 된다) ⑵ `C2`·`C2-b` 의 판정값이 미측정으로 내려가고 문면 셋이 다음 회차로 ⑶ `C1` 모집단과 `C4` 합격선의 **읽기** ⑷ 판 4 정반합을 `E2` 뒤로. 사유와 원문은 **`## 승격` 이 진다 — 여기 두 번 안 적는다** | 합(合) 셋이 「올린다」를 냈고 소유자가 2026-09-12 에 다섯 칸을 답했다. 조건 수 50 · 문면 `36f0507` 그대로라 §4 의 재승인을 안 부른다 |
| 2026-09-12 ⟨정반합 R2 · 승격⟩ | **조건 문면은 한 글자도 안 바꿨다.** 칸 6~11 여섯 — ⑴ 조항 8 **집행 면제** 둘(칸 6·9) ⑵ `#140` ② 문면 교정(칸 7) ⑶ `A6` 을 기록으로 닫고 판정문에 **성격**을 적는다(칸 8) ⑷ `D2` 를 **답 없이 차선책 4 로** 닫고 `D4` 는 **반증**(칸 10) ⑸ `AT5-14` 는 정정문을 옆에 붙이고 원문 보존(칸 11). 원문은 **`## 승격` 이 진다** | 판 3 라운드 2 의 합·판 5 의 합·취합 보고가 각각 올렸다. 조건 수 50 · 문면 `36f0507` 그대로라 §4 의 재승인을 안 부른다 |
| 2026-09-12 ⟨정반합 라운드 3 · 승격⟩ | **조건 문면은 한 글자도 안 바꿨다.** 칸 13~15 — ⑴ 칸 4 의 사정거리를 **「방향 기입」으로 못 박았다** → `C4` **반증**(칸 13) ⑵ 격리 이탈을 **유효로 확정**하고 대가 둘을 적는다(칸 14) ⑶ ★★ **소유자가 규범형 문장을 썼다** — *「자기 저장소 큐레이션은 바닥까지만 제품 요구다」* → `D2` **통과** · 차선책 4 **안 열림** · `D4` 는 `#66` 을 **닫는다**(칸 15). 원문은 **`## 승격` 이 진다** | 판 4 R3 의 합·종료 판단과 판 5 R2 의 합이 각각 올렸다. ★ **문면은 그대로인데 조건 값 셋이 움직였다** — 새 재료(소유자의 문장)가 들어온 것이지 완화가 아니다. 조건 수 50 · 문면 `36f0507` 그대로라 §4 의 재승인을 안 부른다 |
| 2026-09-12 ⟨정반합 판 4 R2 · 승격⟩ | **조건 문면은 한 글자도 안 바꿨다.** 칸 12 — 설계 반증 **조항 8 을 집행한다.** 판 4 라운드 2 가 **무효**이고 게이트 정정 다섯을 세운 뒤 **라운드 3** 을 돈다. `## 상한` 이 그 초과를 진다 | 판정 대상 파일(`docs/gates/effect-confirmation.md`)이 **핀 밖으로 나갔고**, 칸 6·9 의 면제 근거(*"다시 돌려도 값은 안 움직인다"*)가 이번엔 거짓이다 — 반증 근거 셋 중 둘이 정정 다섯이 고칠 자리다. ★ **칸 9 의 「다섯」이 전수로 재면 여섯**이고 빠진 하나가 그 게이트 파일이다(메인이 핀 40 개를 다시 쟀다) |


## 승인

**소유자가 2026-09-11 에 승인했다.** 조건 **50** 을 그대로 지고 루프에 들어간다.
고른 것은 셋 중 첫째다 — *"승인 — 조건 50 그대로"*. 나머지 둘(독립 리뷰 상한을 4 로 되돌리기 ·
조건 설계 평가를 한 판 더)은 **안 골랐다**. 그러므로 **독립 리뷰 상한은 `## 상한` 의 3 이고**,
감사자가 못 본 문면 **19**(⟨조건평가 R2⟩ 에서 새로 선 여섯 + 강화된 열셋)는 그대로 간다.

승인 앞에 선 것 — 인터뷰 3/3(상한에서 닫음) · 사전부검 **3/2**(초과 · `## 상한` 이 까닭을 진다) ·
완수 조건 설계 평가 2/2 · `cargo xtask check` 28/28 · 원장 124 행 **전부 닫힘**(열린 금지역·실패 0).

⚠ **승인 시점의 조건 문면은 `36f0507` 이다.** 그 뒤 문면이 바뀌면 §4 가 다시 승인을 요구한다
(*"승인은 의도가 잠길 때마다 한 번"*).

## 승격

**정반합이 판 다섯을 돌았고 합(合)·종료 판단이 「소유자에게 올린다」를 거듭 냈다.**
물음 하나에 칸 하나다 — 규약 §5 「승격」. 소유자가 2026-09-12 에 **열다섯**을 답했다
(라운드 1 에서 다섯 · 라운드 2 에서 여섯 · 판 4 라운드 2 의 합이 올린 것 하나 ·
**라운드 3 에서 셋** — 판 4 R3 의 합과 종료 판단, 판 5 R2 의 합).

### 칸 1 ⟨판 1 · `B1` · 합 `dialectic/1-synthesis.md:66`⟩

**물음** — ㉠ 의 새 산출이 찍는 *"순서 위험은 없고"* 가 중복 그룹 첫 선언에 대해 거짓이고
`ledger.rs:876` 의 시험이 그 거짓을 불변식으로 못 박았는데, 이 회차 안에서 문구와 시험을
걷어낼 것인가, 아니면 **수 자체를 그룹 단위로 다시 세어** `#79` 의 닫는 조건을 재정의할 것인가.

**답 — 「그룹 단위로 다시 센다」.**

⚠ **문구와 시험은 `e44a87e` 가 이미 걷어냈다** — 이 답이 더하는 것은 **셈법**이다.
버킷 ① 「순서에 취약」의 술어가 `ordinal > 0` 에서 **「그 (체인·이름·종류) 그룹의 크기가 1 보다
크다」**로 넓어진다. 중복 그룹의 **첫 선언**도 순서가 바뀌면 `SymbolId` 가 움직이므로
(`crates/pal-core/src/coord.rs:126-127` 이 `discriminator.ordinal` 을 해시에 넣는다) 그것이
참인 셈법이다.

★ **`plan/79-pre.md` 를 안 고친다.** 그 파일은 `A1-b` 가 blob 해시로 봉인했고 `A1-c` 가
판정에 물렸다. 사전 등록 §4 의 버킷 표(`:105-110`)는 ① 을 `ordinal > 0` 으로, ② 를
*"순서 위험은 **없다**"* 로 적는데 **그 둘이 지금 틀렸다.** 고치는 대신 **`C1` 의 차이로
싣는다** — 이 회차가 재는 것이 바로 「사전 등록 ↔ 실제 변경의 차이」다. 원인 끝은
`dialectic/1-synthesis.md:37`(정반합)이고 `touch/79.txt:N` 이 아니다.

### 칸 2 ⟨판 3 · `C2`·`C2-b` · 합 `dialectic/3-synthesis.md:72`⟩

**물음** — `C2`·`C2-b` 를 미측정으로 닫고 조건 문면 셋(배제 집합의 정의역 ·
「두 문서의 인용」의 계기 · 사실 자르기의 주체)을 다음 회차로 승격할 것인가, 아니면
이번 회차 안에서 그 셋을 고쳐 다시 잴 것인가.

**답 — 「미측정으로 닫고 문면 셋을 승격」.**

그러므로 게이트 `## 판정` 의 `미측정` 칸에 `C2`·`C2-b` 가 들어가고, 문면 셋은
**다음 회차로 올라간다** — `#140` 이 그 자리다(2026-09-12 · `ready-for-agent`). ⚠ 합이 함께 적은 것 — *"`## 차선책` 1 은
「`C2` 가 0 이면 반증으로 적고 멈춘다」인데 **내 판정은 0 이 아니라 「안 쟀다」다.**
차선책 1 은 안 열린다."* 이 답은 그 판단을 그대로 받는다.

### 칸 3 ⟨판 4 · `C4` · 합 `dialectic/4-synthesis.md:76`⟩

**물음** — `C1` 의 「차이」 모집단을 「사전 등록 ↔ 실제 변경 **전부**」로 할 것인가
「원인이 touch 인 것」으로 할 것인가.

**답 — 「사전 등록 ↔ 실제 변경 **전부**」.**

`C1` 문면(`intent.md:231`)의 앞절을 글자 그대로 읽는다. 뒷절(*"차이마다 좌표 두 끝이
붙었다"*)은 **모집단의 정의가 아니라 기입 형식의 요구**다 — 합이 세운 읽기와 같다.
`C2` 의 *"그 차이 **중** touch 산출에 귀속되는 것"* 이 부분집합을 따로 부르므로 두 조건이
겹치지 않는다. **조건 문면은 안 바뀐다** — 읽기를 못 박는 것이라 재승인을 안 부른다.

### 칸 4 ⟨판 4 · `C4` · 합 `dialectic/4-synthesis.md:76`⟩

**물음** — 방향 기입의 **존재**가 `C4` 의 합격선인가(`intent.md:238` ↔
`dialectic/4-design.md:90-91`).

**답 — 「존재는 합격선, 값은 아니다」.**

`C4` 문면의 두 문장이 이렇게 양립한다: *"적혔다"* 가 합격선이고,
*"⚠ 방향은 합격선이 아니다"* 는 **방향의 값**(나아졌다/나빠졌다/모른다 중 무엇인가)을
안 잰다는 뜻이다. `dialectic/4-design.md:90-91` 의 ⓐ(*"차이 전수에 두 기입이 붙었나"*)가
그대로 선다. **조건 문면은 안 바뀐다.**

### 칸 5 ⟨판 4 · `C4` · 합 `dialectic/4-synthesis.md:76`⟩

**물음** — `C4` 판정을 `E2` 뒤로 미룰 것인가(`intent.md:123` · `state.md` 「남은 것」 4 ↔ 6).

**답 — 「`E2` 뒤로 미뤄 판정한다」.**

게이트 `## 효과`(`E2`)를 먼저 쓰고 그 뒤에 판 4 의 정반합 라운드 2 를 돈다.
조건이 요구하는 산출물이 실제로 선 뒤에 재므로, **아직 안 쓸 차례인 자리의 공백을
반증의 근거로 세는 일**이 원리상 사라진다. `## 순서` 절의 *"게이트·종료 문서는 그 뒤에
쓴다"* 와 안 부딪힌다 — 그 문장이 뒤로 민 것은 **㉡ 을 닫기 전**이고 ㉡ 은 이미 닫혔다.

### 칸 6 ⟨판 3 라운드 2 · 합 `dialectic/3-synthesis-r2.md`⟩

**물음** — 설계문의 **반증 조항 8** 이 발화했다(`dialectic/3-design.md:242-243` —
*"그 판은 **무효이고 다시 돌린다**"*). 합이 기계로 둘을 확인했다: ⑴ `touch/79.txt` ·
`touch/129.txt` · `effect/79-delta.md` 셋의 sha256 이 기준 핀 `05f07ea` 와 다르다
⑵ 초안이 **핀 뒤에 생긴 바이트**(`effect/79-delta.md:23-31`·`:39` — `06e8d63` 가 넣은 줄)에
기대어 값을 세웠다. 조항 8 을 집행할 것인가.

**답 — 「집행 면제 · 드리프트를 기록으로 남긴다」.**

다시 돌려도 값은 안 움직인다 — `C2`·`C2-b` 는 칸 2 로 이미 확정됐고, 밀린 것은 본문이 아니라
머리에 일곱 줄이 들어간 것이다(핀 시점 바이트에서 좌표가 전부 맞았음을 정·반·합 셋이 각각
확인했다). ⚠ **대가를 적는다 — 조항 8 이 이 회차에서 한 번도 이빨을 안 썼다.**
장치를 세워 놓고 발화했을 때 면제하는 것이 이 저장소가 반복해 온 형태이고, 그 사실을
여기 남긴다.

★ **면제는 「안 고친다」가 아니다.** 줄 밀림 자체는 2026-09-12 에 **고쳤다** — ⓓ2 덧붙임을
머리 끝에서 **전 출력 뒤**로 옮겼다. 핀 대비 앞 65 줄(㉠)·38 줄(㉢)이 다시 **바이트로
동일**하고 `touch/79.txt:23`·`:36`·`:56` 과 `touch/129.txt:21`·`:23` 이 핀의 그 줄을 가리킨다.
면제한 것은 **판을 다시 도는 것**이지 결함을 두는 것이 아니다.

### 칸 7 ⟨판 3 라운드 2 · 합 `dialectic/3-synthesis-r2.md`⟩

**물음** — 소유자가 이미 승인해 `#140` ② 로 올린 문장이 **반증됐다.** 그 문장은
*"`C2-b` 가 요구한 「두 문서의 인용」을 판정자 반환 형식이 **원리상 못 낳는다**"* 인데,
합이 실물 반례 둘을 찾았다(`effect/79-judge-1.md:21-22` · `129-judge-2.md:39-40` 가 두 문서를
원문 인용으로 댄다). `#140` ② 의 문면을 이번 회차에서 고칠 것인가.

**답 — 「이번 회차에서 고친다」.**

안 고치면 다음 회차가 거짓 전제 위에서 시작해 **고칠 것이 없는 것을 고치러 간다.**
살아남는 관측으로 바꿔 적는다 — *"반환 형식이 「없다」 쪽에 인용을 **요구하지 않아**
귀속마다 고르지 않게 나왔다"*(양쪽 원문 인용 3 · 한쪽 2 · 맨 「없다」 2).
조건 문면을 안 건드리므로 재승인을 안 부른다.

### 칸 8 ⟨판 3 라운드 2 · 종료 판단 물음 3 · 취합 보고가 다시 올렸다⟩

**물음** — `A6` 의 금지역에 대응하는 칸이 칸 6·7 중에 없다. `A6` 은 *"초안이
`intent.md:412` 를 오인용해 「소유자가 이미 값을 정했다」는 사실을 감췄다"* 이고, 합이
*"이 판의 값 산출은 **판정이 아니라 기록**이다 — 그렇게 안 읽히게 쓴 초안은 원장에
「독립 판정 둘」을 더 얹는다 — 「측정이 죽은 가지」다"* 라 판정했다. 그대로 둘 것인가.

**답 — 「기록으로 닫는다 · 판정문에 성격을 적는다」.**

판을 다시 안 돈다 — 산출물은 이미 있고 빠진 것은 **그 성격을 적는 한 문단**이다.
게이트와 판정 기록에 못 박는다: **`C2`·`C2-b` 의 값은 소유자가 칸 2 에서 정했고, 판 3 의
라운드 2 가 산출한 것은 그 값이 아니라 **근거**다.** 그 문장이 없으면 원장이 같은 값을
「독립 판정 둘」로 세어 미판정 잔액이 거짓이 된다.

### 칸 9 ⟨취합 보고가 전수로 재서 올렸다⟩

**물음** — 칸 6 의 면제 범위가 실제보다 좁다. ⑴ 칸 6 이 센 드리프트는 **셋**인데 preflight
의 핀 목록 **40** 개를 전수로 대면 **다섯**이다 — `intent.md` 와 `findings.jsonl` 이 더 있고
**둘 다 라운드 2 의 판정이 직접 인용한 증거**다. ⑵ **판 1 의 라운드 2 도 핀 밖 바이트**
(`intent.md` 의 `## 승격` 칸 1·3 — `05f07ea` 에 없던 줄)**에 기대어 값과 확장 판단을
세웠는데 판 1 의 어느 산출물도 조항 8 을 부르지 않았다.** 면제가 어디까지 덮나.

**답 — 「면제가 다섯과 판 1 까지 덮는다」.**

칸 6 의 근거(*"다시 돌려도 값은 안 움직인다"*)가 다섯과 판 1 에도 그대로 선다 —
`intent.md` 와 `findings.jsonl` 의 변경은 **이 회차가 자기 결정을 적은 것**이지 증거가
바뀐 것이 아니다. ⚠ **그래도 대가는 칸 6 과 같다 — 조항 8 이 이 회차에서 한 번도 이빨을
안 썼고, 이제 그 사실이 판 1 에까지 걸린다.**

★ **다섯을 이름으로 싣는다** — `touch/79.txt` · `touch/129.txt` · `effect/79-delta.md` ·
`intent.md` · `findings.jsonl`. 그리고 **라운드 2 가 새 preflight 를 안 낸 것**이 이 어긋남의
뿌리다 — 판 1 은 `fc961d3` 을 자기 핀으로 선언했고 판 3 은 `05f07ea` 를 핀으로 잡았다.
**라운드마다 핀을 다시 박는 것**이 다음 회차가 가져갈 교훈이고, 그것은 `#140` 이 아니라
규약(`SKILL.md` §5)의 자리다.

### 칸 10 ⟨판 5 · `D2` · 합 `dialectic/5-synthesis.md`⟩

**물음** — 판 5 의 합이 `#66` 의 물음 1(*"이 도구가 자기 자신을 큐레이션할 수 있어야
하는가"*)에 **「아직 못 답한다」**를 냈다. 근거는 저장소가 가진 권위 재료가 **셋으로 갈리고
어느 것도 명제를 규범형으로 안 말한다**는 것이다 — `corpus/criteria.toml` ⑧(바닥 ≥ 1 ·
그런데 같은 문장이 열 자리에 있는 *"이 저장소의 하한 관례"*) · `docs/adr/0027-…:36`
(Rust 1급 승격) · `docs/plan/00-goals.md:316`(자기 온톨로지는 **비목표**).
합이 `gh` 로 직접 재서 **`#66` 은 지금 OPEN** 이고 마지막 갱신이 **2026-08-19** 다.
지금 한 문장으로 답할 것인가, 답 없이 차선책 4 로 닫을 것인가.

**답 — 「답 없이 차선책 4 로 닫는다」.**

`#66` 을 **열어 둔 채** 이 회차를 닫는다. 차선책 4 는 **등록된 길**이라 승인이 따로 필요
없고(규약: *"등록해 두면 나중에 그것을 쓰는 데 승인이 필요 없다"*), `D4`(*"`#66` 을 닫았다"*)는
**반증**으로 적고 사유를 남긴다.

★ **이것이 축소가 아닌 까닭** — 저장소가 가진 재료로는 그 물음에 답이 안 선다는 것 자체가
**이 회차가 잰 것**이다. `intent.md:105-109` 가 *"목표 1(큐레이션)은 이 표본 위에서 원리상
안 재어진다"* 를 미리 적었고 `:114-116` 이 *"그 「측정 불능」 자체가 `#66` 의 물음 1 에 대한
값이다"* 라 적었다. **능력은 섰는데**(`docs/gates/effect-confirmation.md:69` — Rust L1 141 파일 ·
심볼 3332) **규범은 안 섰다** — 그 둘을 가른 것이 이 회차의 산출이다.

⚠ **남는 것을 적는다** — 답을 낼 수 있는 자는 **소유자**이고, 필요한 것은 새 측정이 아니라
**명제를 규범형으로 적는 문장 하나**다(*"자기 저장소 큐레이션은 제품 요구다 / 바닥까지만
요구다 / 요구가 아니다"* 중 하나). 그 문장이 서면 `#66` 이 닫힌다.

### 칸 11 ⟨판 5 · `D2` · 합의 채택 `AT5-14` · **금지역**⟩

**물음** — 판 5 의 정(正)이 낸 부수 발견 ③(*"`ADR-0027:22` 의 「Kotlin·TypeScript 둘뿐」이
「넷에서 다섯」과 갈린다 — 채택된 ADR 이 낡았거나 틀렸다"*)이 **오독**이다. 합이 핀 안에서
다시 쟀다 — `git ls-tree a1493b5 crates/pal-extract/src/` 는 **추출기 셋**(kotlin·typescript·
rust)을 내고 `crates/pal-core/src/language.rs:24-30` 은 **enum variant 다섯**을 낸다.
**다른 것을 세는 두 수**이고 `ADR-0027:22` 는 맞다. 그런데 그 거짓이 **이미 커밋된 바이트
보존 산출물**(`dialectic/5-thesis.md` · `e14157c`)에 앉아 있다.

**답 — 「정정문을 옆에 붙이고 원문은 안 고친다」.**

`C3-a` 계열이 원 반환문의 바이트 보존을 요구하므로 **`5-thesis.md` 는 한 글자도 안 고친다.**
정정은 ⑴ 원장(`출처=정반합` 으로 `AT5-14` 가 이미 실린다) ⑵ 게이트에 적어 **독자가 만나는
자리**에 세운다. 라운드 1 의 합이 판 3 에서 같은 자리에 쓴 지시와 같다 —
*"고치지 말고 정오표를 옆에 붙여라 — 판정문의 사후 수정은 증거 위조다"*.

### 칸 12 ⟨판 4 라운드 2 · 합 `dialectic/4-synthesis-r2.md` · 종료 판단 `dialectic/r2-referee.md` 판 4 절 · **금지역 `A4`**⟩

**물음** — 설계 반증 조항 8(`dialectic/4-design.md:242-243` — *"판정문이 기준 핀 밖의
바이트에 기댔다 → 그 판은 **무효이고 다시 돌린다**"*)이 판 4 라운드 2 에 대해 **문자 그대로**
발화했다. `dialectic/preflight.md:60` 이 `docs/gates/effect-confirmation.md` 를 **이름으로**
핀에 싣는데(`306ad7ec…`) 지금은 `fb7e5a13…` 이다. 집행할 것인가, 면제할 것인가.

⚠ **함께 올린 것 — 칸 9 의 전제가 틀렸다.** 칸 9 는 면제 범위를 **다섯**으로 이름 붙였는데
메인이 핀 40 개를 전수로 다시 재니 어긋난 것이 **일곱**이고, `dialectic/preflight.md` 는
**구조상 만족 불가능한 자기 핀**(자기 해시를 자기 안에 적을 수 없다)이라 빼면 **여섯**이다.
빠진 하나가 하필 **`C4` 가 재는 게이트 파일**이다.

```
$ awk '/^[0-9a-f]{64}  /{print $1, $2}' dialectic/preflight.md | while read h f; do …; done
DRIFT intent.md · findings.jsonl · touch/129.txt · touch/79.txt · effect/79-delta.md
DRIFT dialectic/preflight.md            ← 자기 핀 · 구조상 만족 불가
DRIFT docs/gates/effect-confirmation.md ← 칸 9 가 빠뜨린 것 · C4 의 판정 대상
```

**답 — 「집행한다 — B 뒤에 다시 돈다」.**

판 4 라운드 2 를 **무효로 한다.** `## 게이트에 남은 거짓과 공백` 의 정정 다섯을 먼저 세우고,
그 위에서 판 4 를 **다시 돈다.** 상한 2/2 를 넘으므로 **라운드 하나 초과**를 `## 상한` 이
사전부검 초과와 같은 형태로 진다.

★ **칸 6·9 와 갈리는 까닭** — 그 둘이 면제의 근거로 쓴 *"다시 돌려도 값은 안 움직인다"* 가
**이번엔 거짓이다.** 정(正)이 든 반증 근거 셋 중 둘(모집단이 12 가 아니라 13 · 죽은 좌표
`effect/negative-79.txt`)이 **B 가 고칠 바로 그 자리**이고, 고치면 그 근거가 없어진다.
그리고 빠진 핀이 **판정 대상 파일 자신**이라 앞 둘과 종류가 다르다.

⚠ **조항 8 이 이 회차에서 처음으로 이빨을 쓴다.** 칸 6 이 적은 대가(*"조항 8 이 이 회차에서
한 번도 이빨을 안 썼다"*)는 **칸 6·9 의 두 번에 대해서만 선다** — 이 칸이 그 문장의 범위를
좁힌다. 세 번 발화하고 세 번 면제하면 그 장치는 이 회차가 등록한 금지역
**「측정이 죽은 가지」**이고, 소유자가 그 길을 안 골랐다.

★ **다시 돌 때 핀을 새로 박는다** — 칸 9 가 *"라운드마다 핀을 다시 박는 것"* 을 교훈으로
적었고, 이 라운드가 그것을 처음 집행한다. 새 preflight 는 `dialectic/preflight-r3.md` 로
선다(앞 것을 안 덮는다 — 무엇이 어떻게 갈렸는지가 증거다).

### 칸 13 ⟨판 4 R3 · `C4` · 합 `dialectic/4-synthesis-r3.md`⟩

**물음** — 칸 4 의 *"존재는 합격선, 값은 아니다"* 가 `C4` 문면(`intent.md:243`)의
**「근거와 함께」**까지 덮나, 아니면 **방향 칸의 값**만 덮나.

**답 — 「안 덮는다 — 반증이 선다」.**

칸 4 는 **「방향 기입의 존재」**를 묻고 답했고 *"조건 문면은 안 바뀐다"* 로 끝난다.
그러므로 `C4` 문면의 **「근거와 함께」는 살아 있고**, 게이트 `## 효과` 표의 「참이었나」 칸
열셋 중 **여덟**이 근거 절 없는 맨 「해당 없음」이라 그것을 못 채운다. **`C4` = 반증.**

★ **반증의 내용을 정확히 적는다 — 「거짓을 실었다」가 아니다.** 그 여덟 행의 「해당 없음」은
**사실로 전부 참이다**(반이 touch 산출 셋에 전수 `grep` 을 걸어 확인했고 합이 받았다).
**「근거를 안 실었다」**가 반증의 전부다.

★ **합이 든 근거** — 게이트 `:180`(*"기입의 존재가 합격선이다"*)과 `MS-17` 은 **같은 손이
같은 커밋(`c807f32`)에서 쓴 같은 의역**이라 독립 권위가 아니다. **도구는 조건 문면을
못 줄인다**(설계문 `dialectic/4-design.md:267-268`).

⚠ **여덟 셀을 이 회차에서 안 고친다.** 칸 12 가 세운 순서(*"정정을 먼저 세우고 그 위에서
다시 돈다 · 다시 돌 때 핀을 새로 박는다"*)가 **판정 중 수리**를 금한다. 수리는 `#140` 이 진다.

⚠ **칸 3·4 의 자기 좌표가 죽어 있었다** ⟨판 4 R3 의 반 R14⟩ — 칸 3 의 *"`intent.md:231`"* 은
§5.8 산문이고 `C1` 은 `:236` · 칸 4 의 *"`intent.md:238`"* 은 `C2-b` 이고 `C4` 는 `:243` 이다.
**이 칸이 그 정정을 진다** — 원문(칸 3·4)은 안 고친다. 표가 길어질 때마다 밀리는 자리이고
⟨기록 정합 6 차⟩ 가 이미 그 형태를 잡았다.

### 칸 14 ⟨판 4 R3 · 종료 판단 `dialectic/r3-referee.md` 판 4 절⟩

**물음** — 메인의 과업문이 설계문 `dialectic/4-design.md:151` 의 「안 주는 것」 칸을 넘겼다
(반(反)에게 `intent.md` 전문과 `findings.jsonl` 을 줬다). 조항 8 처럼 **무효·재실행을 집행할
것인가**, 아니면 **라운드 3 의 산출을 유효로 확정할 것인가.**

**답 — 「유효로 확정한다 · 대가를 적는다」.**

★ **조항 8 을 집행한 칸 12 와 갈리는 까닭** — **설계문에 격리 이탈에 대한 무효 조항이
없다.** 조항 8 은 **핀 이탈**에 대한 것이다. 집행하는 것은 조항을 **집행**하는 것이 아니라
**확장**하는 것이고, 그것은 이 회차가 회차 중에 안 하기로 한 종류다.

★ **합 둘이 오염 없음을 좌표로 보였다** — 판 4 의 합은 그 입력에 기댄 반론 **둘(R1·R3)을
문면으로 기각**하고 **하나(R4)를 `git show` 로 원장 밖에서 다시 세웠다.** 판 5 의 합은
**격리 위반이 정(正)을 도운 것이 아니라 틀리게 만들었다**는 것을 보였다(정이 그 금지 입력을
쓴 자리가 곧 반론 5 가 깨뜨린 자리다).

★ **종료 판단 자신의 문장이 재실행을 반대한다** — *"라운드를 한 번 더 도는 것은 **같은
하네스로 같은 결함을 다시 겪는 것**이지 그 실패를 닫는 행위가 아니다."*

⚠⚠ **대가를 적는다 — 이것이 이 칸의 절반이다.** ⑴ **판 4 에서 하네스가 설계대로 안 돈 것이
두 번째다**(첫째는 조항 8 · 집행했다). ⑵ **메인의 과업문을 보존하는 자리가 회차기록에 없다** —
판 4 의 합이 `## 내가 못 정한 것` 첫 줄로 그것을 적었다: *"정·반에게 간 프롬프트 전문이
회차기록에 보존돼야 정할 수 있다."* 그것이 서면 이 계열의 이탈이 **기계에 보이게** 된다.
**종료 보고의 하네스 실패 항이 이 둘을 진다.**

### 칸 15 ⟨판 5 R2 · `D2` · 합 `dialectic/5-synthesis-r2.md` + 종료 판단⟩ — ★★ **칸 10 이 뒤집힌다**

**물음** — `ADR-0027`(상태 **채택** · 제목 *"자를 자기 바닥에 닿게 한다"* · 파일명
`the-instrument-**must**-reach-its-own-floor` · `:14` 가 「바닥」을 `[f11.pass]` ⑧ =
*"자기 저장소 실사용 · 결박 ≥ 1"* 로 묶으며 **"요구했고"** 라 적는다)이 이미 규범형 문장을
담고 있다. 게이트 `:94-95` 가 등록한 방아쇠가 당겨졌나.

**답 — 「소유자가 지금 규범형 문장을 쓴다」. 그 문장은 —**

> ## **자기 저장소 큐레이션은 바닥까지만 제품 요구다.**
>
> 즉 `[f11.pass]` ⑧ 의 *"자기 저장소 실사용 · 결박 ≥ 1"* 이 **요구**이고, 그 위는 요구가
> 아니다. ⟨소유자 · 2026-09-12⟩

★★ **이것이 `#66` 의 물음 1 에 대한 답이다.** 게이트가 스스로 적어 둔 것이 그대로 집행됐다 —
*"무엇이 있으면 답하나 — 새 측정이 아니라 **명제를 규범형으로 적는 문장 하나**다. 누가 낼 수
있나 — **소유자다**."*

**따라오는 것 넷 — 앞선 칸과 게이트 여러 줄이 뒤집힌다:**

| 무엇 | 앞 | 지금 |
|---|---|---|
| `D2` 의 답 값 | 「아직 못 답한다」 | **「바닥까지만 요구다」** |
| `D2` 의 판정값 | 미측정 ⟨판 5 R2 합⟩ | **통과** |
| **차선책 4** | 발동됐다 ⟨칸 10⟩ | **안 열린다** — 그 문은 *"`D2` 가 「아직 못 답한다」로 나면"* 이다 |
| `D4`(*"`#66` 을 닫았다"*) | 반증 ⟨칸 10⟩ | **`#66` 을 닫는다** → 통과 |

⚠ **칸 10 을 지우지 않는다.** 그때는 그 판단이 참이었다 — 저장소가 가진 재료로는 답이 안
섰고, **그 「측정 불능」 자체가 값이었다.** 이 칸이 그것을 뒤집는 것은 **새 재료가 들어왔기
때문**이고 그 새 재료는 소유자의 문장 하나다. 규약이 *"승격은 잠금으로 되돌아가는 우회로"*
라 적는 그 자리다.

★ **그리고 이 칸이 드러낸 것을 적는다 — 게이트가 자기 반증 검사를 한 번도 안 돌렸다.**
게이트 `:78`·`:119` 가 `docs/adr/0027-rust-first-class.md` 를 가리키는데 **그 파일은
저장소에 없다.** 백틱 참조라 「죽은 링크 부재」 검사가 **원리상 못 본다**(그 검사는 마크다운
링크만 잰다). 경로가 죽어 아무도 그 문서를 안 열었고, 그래서 *"어느 것도 규범형으로 말하지
않는다"* 라는 **전칭 부정**이 한 번도 안 재어진 채 사실로 적혔다. **등록 금지역 둘에 동시에
든다** — 「사실이 아닌 것을 사실로」·「측정이 죽은 가지」. **쓴 것은 메인이다.**

⚠ **종료 판단의 물음을 이 칸에 병합했다 — 까닭을 적는다.** 그 물음은 *"검사를 **실제로
돌려** 뒷받침할 때까지 전칭 부정을 사실에서 내릴 것인가"* 였는데, **그 검사는 판 5 의 합이
이미 돌렸다**(실재 파일을 열어 제목·상태·`:14` 를 읽었다). 남은 결정은 그 산출의 **값**이고
그것이 이 칸이다. 「내릴 것인가」는 결정이 아니라 **정정**이라 메인이 집행한다.
종료 판단이 건 조건(*"두 물음이 같으면 병합하고 다르면 둘로"*)을 그렇게 읽었다.

### 칸 16~21 — **여섯 다 답받았다** ⟨판 6 종료 판단 · 취합 보고 · 답 2026-09-12⟩

★★ **소유자 원문 — 압축하지 않는다.** 세 마디가 순서대로 왔다.

> 네가 스스로 판단해봐. 이 프로젝트의 목적을 보고 말이야

> 난 지금까지 네가 만든걸 몰라 그런데 내가 어떻게 판단하겠어

> 여섯 다 추인한다

⚠⚠ **앞의 둘이 이 회차가 승격에서 한 번 실패했다는 기록이다.** 메인이 여섯을 **갈래 메뉴로만**
올렸고 소유자가 그것을 물렸다 — 규약 §5 「철회」가 못 박은 자리와 같은 병이다:
*"「소유자 결정이다」로 판정 없이 넘기면 소유자는 고를 근거가 없는 채로 고른다."*
**메뉴는 판정이 아니다.** 그리고 둘째 마디가 그 위에 하나를 더 세웠다 — **재료 없이 물으면
승격 자체가 성립하지 않는다.** 그래서 메인이 ⑴ 이 회차가 만든 것(커밋 98 · 코드 11 파일
+808 줄 · 조건 50/50 · 효과 출력 · 닫은 이슈 넷) ⑵ 여섯 물음이 **`E3` 의 값이 아니라 판 6 의
유효성**을 묻는다는 것 ⑶ 열린 30 의 모집단 분포를 먼저 적고, **판정 초안 여섯을 근거와 함께**
낸 뒤에야 추인을 받았다. ⟨이 기록이 `MS-21` 계열의 **아홉째**다 — 형태가 아니라 자리다⟩

★ **판정의 자로 쓴 것 셋** — `00-goals.md` §0.1(*"상위 가치는 그 결과로 더 나은 코드가 나오는
것"*) · `AGENTS.md`(*"빠르게 완성한다 → 처음의 의도에 도달한다 → **효과를 본다** → 그 뒤 계속
고친다"*) · 규약 §11③(*"이 회차가 만든 장치는 이 회차의 먹이가 아니다"*).

⚠ **추인은 여섯을 한 번에 받았지만 칸은 갈라 적는다** — 규약 §5 *"승격은 물음 하나에 칸 하나다.
둘을 묶어 올리면 답 하나가 두 물음을 닫은 것처럼 보인다."* 아래 여섯 칸이 각각 무엇을 닫았는지
따로 적고, 각 칸이 처분하는 발견을 이름으로 단다.

★ **판 6(`E3`)이 상한 2/2 를 다 쓰고 「소유자에게 올린다」로 끝났다.** 설계문 표의 셋째 행이
걸렸다 — *"라운드 2 뒤에도 금지역·실패가 남는다 → 소유자에게 올린다. **「닫혔다」로 안 적는다.**"*
살아남은 **실패 2**(합이 반의 「금지역」에서 내린 값)이고, 취합 보고가 **격리 이탈 일곱**을 더 셌다.

⚠ **`E3` 의 판정값 자체는 섰다 — 「반증」이다.** 이 여섯은 **그 값을 묻는 것이 아니라
「판 6 이 유효한가」와 「조항 ㉮ 를 어떻게 집행하는가」를 묻는다.**

⚠⚠ **회차는 여기서 안 닫힌다.** 원장에 **열린 발견 30**(전부 `DL5-*`)이 있고
`cargo xtask check` 의 **「발견이 닫혔나」가 빨갛다** — 그 검사가 *"끝난 회차에 열린 발견이 있으면
**그 회차가 안 끝난 것**이다"* 로 옳게 말하고 있다. **기계가 사실을 적고 있는 것이지 고장이 아니다.**

#### 칸 16 — 핀 밖 발췌를 여섯 자리가 받은 것이 조항 ㉮ 인가

**물음** — 메인이 격리를 위해 만든 **발췌 넷**(`6-intent-excerpt.md` · `6-design-for-antithesis.md` ·
`6-intent-for-antithesis.md` · `6-intent-for-synthesis.md`)이 **라운드 1·2 의 핀 어디에도 없는데**
정·반·합 여섯 자리가 그것을 과업문으로 받았다. 이것이 조항 ㉮ 의 *"판정문이 기준 핀 밖의
바이트에 기댔다"* 에 해당하여 **라운드 1·2 를 무효로** 만드는가.

★ **함께 알아야 할 것** — 라운드 2 의 합이 더 무거운 것을 댔다: **라운드 1 의 합 자신이
핀 밖 `6-intent-for-synthesis.md` 에 여덟 자리에서 기댔고, 경계 셋과 해악도 강등 하나가
거기 걸려 있다.** 라운드 1 이 지목한 핀 이탈은 **둘 중 가벼운 쪽**이었다.


**답 ⟨추인 · 2026-09-12⟩** — **무효로 안 본다.** 핀 밖 발췌 넷은 **사실로 기록**하고,
조항 ㉮ 의 집행은 **다음 판부터**다. 라운드 1·2 의 판정은 선다.

**근거 셋** — ⑴ 조항 ㉮ 는 **이 회차가 판 6 을 위해 회차 중에 만든 장치**다. 그 장치로 자기
자신을 소급 무효화하면 §11③ 이 이름 붙인 병리(*"장치를 고칠 때마다 새 선언면이 생겨 다음
라운드의 먹이가 된다"* · 15 라운드)를 그대로 되풀이한다. ⑵ **`E3` 의 값(반증)은 어느 갈래에서도
안 움직인다** — 라운드 1 의 잔여 셋 중 둘이 서로 독립이고, 핀 밖 바이트에 기댄 항 21 을 빼도
잔여가 남는다(합 자신이 그렇게 적었다 ⟨`6-synthesis.md:104-105`⟩). ⑶ 원 의도의 산출(효과 확인)은
판 6 과 무관하게 이미 섰다.

**이 칸이 처분하는 것** — `DL5-07`(항 21 이 핀 밖 바이트에만 걸린다) · `DL5-15`(발췌 넷이 전부
핀 밖이고 과업문이 쓰라고 지시한다) · `DL5-27`(격리 3) · `DL5-14`(감사표 ⑥ 의 자기 선언이 거짓) ·
`DL5-22`(정은 전문·반은 발췌) · `DL5-31`(격리 7).

#### 칸 17 — 조항 ㉮ 의 「핀 여섯」 ↔ 실제 「핀 아홉」

**물음** — 조항 ㉮ 의 문면이 **「핀 여섯」**이라 적는데 라운드 2 에 실제로 선 것은 **아홉**이다.
조항의 **오기**로 보아 문면을 고치는가, **핀 구성이 발동 시점과 갈린 것**으로 보아 무효로 보는가.

★ 종료 판단의 문장 — *"검사 규칙 자신이 흔들리는 동안에는 그 검사를 통과했다고도 실패했다고도
적을 수 없다."*


**답 ⟨추인 · 2026-09-12⟩** — **오기다.** 조항 ㉮ 의 문면을 **「그 라운드에 등록된 핀 전부」**로
읽고, 무효 사유로 안 본다.

**근거** — 핀은 여섯에서 아홉으로 **늘었지 줄지 않았다**. 더 좁게 적힌 문면으로 **더 넓은 준수**를
무효라 부를 수 없다. ⚠ **다만 종료 판단의 문장은 지운 것이 아니라 남긴다** — *"검사 규칙 자신이
흔들리는 동안에는 그 검사를 통과했다고도 실패했다고도 적을 수 없다."* 그래서 이 회차는
**「조항 ㉮ 로 검사해서 통과했다」고도 적지 않는다.** 적는 것은 *"그 사유로는 무효가 아니다"* 뿐이다.

**이 칸이 처분하는 것** — `DL5-30`(라운드 2 의 핀 7 이 종료 뒤에 갈렸다 · 칸 21 과 함께).

#### 칸 18 — 「핀 밖」의 정의를 누가 언제 박나

**물음** — 조항 ㉮ 의 문면에 **「핀 밖」의 정의가 없다.** 이 회차 안에서 박는가, 다음 회차의
설계문이 지는가.


**답 ⟨추인 · 2026-09-12⟩** — **다음 회차의 설계문이 진다.** 이 회차는 안 박고 **이슈로 세운다**.

**근거** — 이 회차에서 정의를 박으면 **그 정의를 쓴 손(메인)이 다시 판정 대상**이 된다.
「메인이 낸 것」 계열이 여덟까지 자란 회차에서 아홉째를 스스로 만드는 것이고, 그것이 §11③ 의
*"장치를 고칠 때마다 새 선언면이 생긴다"* 다. **넘김이 아니라 분할이다** — 이슈로 서고
프론티어에 오른다 ⟨§11③ (나)⟩.

**이 칸이 처분하는 것** — `DL5-24`(설계 반증 18 을 절반만 인용) · `DL5-16`(라운드 2 의 순효과 0).

#### 칸 19 — 무효라면 상한과 부딪힌다

**물음** — 칸 16 또는 17 의 답이 **무효**라면, 조항 ㉮ 의 처분은 *"다시 돈다"* 인데
**상한이 2/2 로 찼다.** 다시 돌 **라운드를 열 것인가**, 아니면 무효인 채로 판 6 을 접고
다음 회차로 넘기는가.

★ 종료 판단이 상한을 **안 늘렸다** — *"이 부딪힘은 상한을 진 자리, 곧 **잠긴 의도만** 풀 수 있다."*


**답 ⟨추인 · 2026-09-12⟩** — **불발이다.** 칸 16·17 이 둘 다 「무효 아님」이므로 이 물음은 안 선다.
**라운드를 열지 않고 상한도 안 늘린다.**

**근거** — 상한은 §11① 이 *"손실 제한이지 안전 보증이 아니다"* 로 적고 *"승격에도 리셋하지
않는다"* 를 못 박는다. 무효가 아닌 이상 다시 돌 사유가 없고, 사유 없이 상한을 푸는 선례를
만들면 **상한이 무력해진다.**

**이 칸이 처분하는 것** — 없다. 앞 둘의 귀결이라 이 칸 자신은 발견을 안 닫는다.

#### 칸 20 — 해악도 강등을 추인하는가

**물음** — 합이 반(反)의 **「금지역」을 「실패」로 내렸다**(라운드 1 에서 하나 · 라운드 2 에서 둘).
그 강등을 추인하는가. **뒤집히면 금지역이 살아나** 이 판단의 무게가 달라진다.

★ 종료 판단이 함께 적었다 — *"표가 금지역과 실패를 묶어 세므로 **이 판단의 결론은 그 강등에
걸려 있지 않다**"*. 그러나 회차의 해악 게이트(§11②)는 둘을 **똑같이 교착**으로 센다.


**답 ⟨추인 · 2026-09-12⟩** — **강등을 추인한다.** 합이 「금지역」에서 「실패」로 내린 셋
(라운드 1 에 하나 · 라운드 2 에 둘)이 그대로 선다. **금지역은 살아나지 않는다.**

**근거 — 등록된 금지역 다섯에 하나도 안 닿는다.** 이 회차는 `.claude/pal/policy.toml` 이 없어
규약의 **기본 다섯**을 쓴다 ⟨`## 금지역`⟩. 가장 가까운 것이 **「측정이 죽은 가지」**인데 그
문면은 *"검사·시험이 있는데 실제로는 아무것도 재지 않는다. **통과가 거짓이 된다**"* 이고,
**판 6 은 통과를 안 냈다 — 반증을 냈다.** 나머지 넷(데이터 손실 · 사실이 아닌 것을 사실로 ·
인증 · 대표 취약점)에도 안 닿는다. 걸린 것은 **절차의 유효성**이고 그것은 실패다.

**이 칸이 처분하는 것** — `DL5-25`(격리 1) · `DL5-26`(격리 2) · `DL5-13`(과업문 자리가 안 섰다) ·
`DL5-29`(격리 5). 넷 다 해악도 「실패」로 확정돼 §11② 의 **닫을 수 있다** 쪽에 선다.

#### 칸 21 — 라운드 2 의 핀이 **종료 뒤에** 갈렸다 ⟨취합 보고가 새로 쟀다⟩

**물음** — `6-preflight-r2.md:41` 의 핀 일곱째(`6-prompts.md` · `27ca62f5…`)가 지금 `5ed5cab9…` 다.
**메인이 라운드 2 의 과업문 다섯 절을 19:50:58 에 덧붙였고 그것은 종료 판단 산출(19:49:25)보다
뒤**다. 조항 ㉮ 에 **「종료 시점」의 정의가 없어** 지금 대는 사람은 이것을 **핀 이탈로 읽는다.**
핀 이탈로 볼 것인가, 「판이 닫힌 뒤의 기록 추가」로 볼 것인가.

★★ **이것이 `6-prompts.md` 의 구조적 결함이다** — 그 파일이 스스로 적는다:
*"「과업문을 보존한다」와 「판정 중에 나무를 안 움직인다」가 **같은 파일에서 부딪힌다.**
핀에 넣으면 라운드 중에 못 쓰고, 안 넣으면 그 파일이 핀 밖 바이트가 된다."*
**라운드 1 은 뒤쪽으로, 라운드 2 는 앞쪽으로 실패했다.**


**답 ⟨추인 · 2026-09-12⟩** — **「판이 닫힌 뒤의 기록 추가」로 본다.** 핀 이탈로 안 센다.
「종료 시점」의 정의는 **칸 18 과 함께 다음 회차**가 진다.

**근거** — 덧붙은 것은 **과업문 보존**이고 판정 재료가 아니다. 정·반·합 셋이 잰 시점에는
핀과 파일이 일치했고(취합 보고가 아홉을 다시 대어 확인했다) 나머지 여덟 핀과 `report.md` 는
**지금도 전부 일치한다**. ⚠ **그러나 그 파일의 구조적 결함은 지운 것이 아니라 이슈로 세운다** —
`6-prompts.md` 자신이 적은 *"「과업문을 보존한다」와 「판정 중에 나무를 안 움직인다」가 **같은
파일에서 부딪힌다**"* 가 그대로 남는다. 라운드 1 은 뒤쪽으로, 라운드 2 는 앞쪽으로 실패했다.

**이 칸이 처분하는 것** — `DL5-30` 과 함께 선다(칸 17 참조).

#### ★★ 여섯 다 **메인이 낸 것**이다

칸 16·21 은 메인이 만든 파일과 메인이 움직인 시각이고, 칸 17·18 은 메인이 쓴 설계문의 조항이며,
칸 19 는 그 둘의 귀결이다. 칸 20 만 에이전트(합)의 판단이다.
**이 회차에서 「메인이 낸 것」의 계열이 여기까지 왔다** — `MS-21` 이 이름 붙인 자리다.

### 이 **스물하나**가 조건 문면을 바꾸나 — **안 바꾼다**

칸 3·4 는 **읽기를 못 박은 것**이고, 칸 2·5 는 **판정값과 순서**이고, 칸 1 은 **코드의
셈법**이며, 칸 6·9 는 **절차 면제**이고 칸 12 는 **그 면제의 중단**, 칸 7 은 **이슈 본문**,
칸 8·11 은 **기록의 성격**, 칸 10 은 **등록된 차선책의 발동**이고 **칸 15 가 그 발동을
거둔다**, 칸 13 은 **칸 4 의 사정거리를 못 박은 것**, 칸 14 는 **절차 이탈의 처분**이다.
`## 완수 조건` 의 상자 50 개는 문면도 수도 그대로다 —
**`36f0507` 의 조건 문면이 계속 선다.** 그러므로 §4 의 재승인을 안 부른다.

★ **칸 16~21 도 조건 문면을 안 바꾸고 조건 값도 안 바꾼다.** 여섯은 `E3` 의 **값**이 아니라
**판 6 의 유효성**을 묻는 물음이었고, 답 여섯이 *"그 사유로는 무효가 아니다"* 로 섰으므로
`E3` = **반증**이 그대로 선다. 바뀐 것은 **열린 발견 30 의 처분**뿐이다.

★★ **칸 15 가 조건 **문면**을 안 바꾸면서 조건 **값** 셋을 바꾼다는 것을 따로 적는다** —
`D2`(미측정 → 통과) · `D4`(반증 → 통과) · 차선책 4(발동 → 안 열림). **새 재료가 들어와
값이 움직인 것**이지 문면이 완화된 것이 아니다. 그 구별이 무너지면 「테스트를 약하게 만들어
GREEN 을 얻는 길」이 승격으로 위장한다(규약 §5).

## 착수 시점 관측

`observations/red.md` 가 원문을 진다. 수를 여기 두 번 적지 않는다 — 갈린다.
