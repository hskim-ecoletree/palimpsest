# 사전부검 3 라운드 — 원 반환문

> **왜 3 라운드가 있나 — 상한은 2 였다. 숨기지 않고 적는다.**
> 2 라운드가 `Agent stalled: no progress for 600s` 로 **두 번 죽었다**(task `a700b13e206357fec` ·
> `af1f8160237148fc3`). 산출이 0 이라 메인이 셋째를 띄웠는데, 그 뒤 `af1f8160237148fc3` 이
> **되살아나 완주했다**(같은 task-id 가 실패 뒤 완료로 다시 알렸다). 그래서 유효한 반환문이
> 둘이 됐다. **19 건을 버리는 것보다 상한 초과를 적는 쪽을 골랐다** — 그중 금지역이 있다.
> §11① 이 상한을 *"손실 제한이지 안전 보증이 아니다"* 로 적는 그 자를 이렇게 읽었다.
>
> 이 라운드는 **격리 사본과 `cargo` 를 금지**하고 읽기·`pal` 질의·`git` 만 준 좁힌 판이다.
> 받은 것: 잠긴 의도(조건 43) 뿐. 안 받은 것: 대화 기록 · 앞 라운드 결과.
> 2026-09-11 · HEAD `1a01057`

---

### `B1-b` 가 아무것도 안 재고 착수 시점에 이미 통과한다
- 어떻게 실패하나: 명세는 *"지금 `Error:` 를 찍으면서 종료값 0"* · *"고친 뒤 … 나면 종료값이 0 이 아니다"*. 실측은 **`rc=1`**. 둘째 절이 **고치기 전부터 참**이므로 코드를 한 줄도 안 고쳐도 `B1-b` 가 초록이다. 동시에 `B1-a`(RED 관측)도 참이라 둘이 같은 상태에서 함께 선다
- 어디가 걸리나: `intent.md` `B1-b` · 실측 `pal query narrative.unbound; echo $?` = 1
- 획득: 실측
- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역
- 대상: 계획자신
- 얼마나 아픈가: 합격선 옆 조건이 **거짓 사실 위에 섰고 죽은 가지**다. ⟨실측⟩ 태그가 붙은 자리가 틀렸다

### ㉠ 에서 `C2`(합격선 본체)가 구성상 0
- 어떻게 실패하나: `pal touch identity_ceiling` ↔ `pal touch nodes_of` 의 고유 줄이 **5 줄**: 심볼명+다이제스트 · 좌표 · `호출자 0 · 피호출자 1` · `못 푼 참조가 0 건` · 토큰 크기. 사실을 담은 것은 셋째뿐인데 **§5.8 이 *"`pal touch` 의 호출자 0"* 으로 이미 적었다.** `C2` 의 배제 규칙이 남은 하나를 지운다
- 어디가 걸리나: `intent.md` `C2` · `observations/red.md` §5.8 ㉠ 행
- 획득: 실측
- 모집단: 자기장치
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: ㉠ 은 재기 전에 결과가 정해졌다

### `C2-b` 의 「기계로 정의됐다」가 도구 구현에 따라 두 값을 낸다
- 어떻게 실패하나: `grep` 은 이 하네스에서 **ugrep 7.8.4 셸 함수**다. 같은 두 파일에 ugrep 은 **15 줄**, `/usr/bin/grep`(BSD)은 **11 줄**. 차이는 빈 줄 4 — `-f` 의 빈 패턴을 `-x` 와 묶는 해석이 갈린다. `CA1-04` 가 `comm` 의 정렬 유무(23↔38)에서 고친 그 결함이 **`grep` 으로 그대로 재발**했다
- 어디가 걸리나: `intent.md` `C2-b`
- 획득: 실측
- 모집단: 자기장치
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 「심볼 고유 줄」의 모집단이 판정자마다 갈린다

### `C2-b` 산출의 과반이 판정에 못 쓰는 자기 식별 줄이다
- 어떻게 실패하나: 고유 줄에 `<심볼명> · palimpsest@…#<hex>` 와 `fun · <파일>:<줄> · … body …` 가 **항상** 들어간다 — 심볼이 다르면 반드시 다르므로 **모든 짝에서 무조건 고유**다. `A2` 머리가 미끼 심볼을 드러내는 것을 고쳤다는데, 고유 줄 목록 자신이 심볼 이름을 다시 드러낸다
- 어디가 걸리나: `intent.md` `C2-b` ↔ `## 개정` 의 `PM1-07` 처리
- 획득: 실측
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: `C2` 가 「고유 줄 1 건 이상」을 자동 충족한다 — 판정자가 그 둘을 집으면 통과다

### ㉢ 의 심볼을 `pal touch` 가 원리상 못 집는다
- 어떻게 실패하나: `#129` 의 결함 본체는 `crates/pal-intent/src/store.rs:163 write` 다. `pal touch write` → *"`write` 의 후보가 3건입니다. 하나를 고르지 않습니다"* — 후보 목록만 난다. `pal touch --help` 에 **경로·크레이트 한정 옵션이 없다**(`<NAME>` 하나뿐). `store.rs:write` · `pal_intent::store::IntentStore::write` 둘 다 *"찾지 못했습니다"*
- 어디가 걸리나: `intent.md` ㉢·`A2` · 실측 `pal touch write` / `pal touch --help`
- 획득: 실측
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: ㉢ 에서 `A2`·`A6`·`C2-b`·`C2`·`C5` 가 **동시에** 못 선다. 「음성 방향의 표본」이 아니라 **측정 불능**이다

### ㉢ 의 모호 출력에는 `A2` 가 요구하는 다이제스트가 아예 없다
- 어떻게 실패하나: 단일 심볼 답의 머리는 `palimpsest@<sha>+worktree#<hex>` 인데, 후보 목록 답의 근거 줄은 **`Snapshot palimpsest@<sha>+worktree`** — `#<hex>` 가 안 붙는다. `A2` 의 머리 넷 중 셋째를 적을 원본이 없다
- 어디가 걸리나: `intent.md` `A2` · 실측 `pal touch write` 근거 블록
- 획득: 실측
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: `A2` 가 ㉢ 에서 문면상 불통과

### `CA1-17` 의 근거가 거짓이라 `A2` 의 셋째 항목이 지탱을 잃는다
- 어떻게 실패하나: `CA1-17` 은 *"워킹트리 다이제스트를 실어 **같은 SHA 로 다시 돌려도 바이트가 안 맞는다**"* 를 근거로 재실행 대조를 포기했다. 실측: `pal touch nodes_of` 두 번의 **전 출력이 `diff` 로 완전히 동일**하다. 게다가 같은 HEAD·같은 워킹트리에서 심볼마다 해시가 다르다 — **그 값은 워킹트리 다이제스트가 아니라 답마다 다른 해시**다
- 어디가 걸리나: `intent.md` `A2`
- 획득: 실측
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 재실행 대조를 **쓸 수 있는데 안 쓰기로** 잠갔다. 이름도 틀렸다

### `A6`(한 심볼당 한 번)이 착수 시점에 이미 깨져 있다
- 어떻게 실패하나: §5.8 이 *"사전부검 라운드 1 이 **세 심볼에** `pal touch` 를 돌리고"* 를 전수로 적는다. `A6` 은 *"각 심볼에 한 번씩만 돈다"* 인데, 판정 시점에 각 심볼의 실행 횟수는 이미 ≥1 이다. 조건 문면에 「이 회차가 등록한 실행에 한해」 같은 한정이 **없다**
- 어디가 걸리나: `intent.md` `A6` ↔ `observations/red.md` §5.8
- 획득: 조회
- 모집단: 회차기록
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 문면대로면 착수 전부터 불통과. 해석으로 구제하면 `CA1-06` 이 다시 열린다

### `A6` 과 ㉢ 이 서로를 배제한다
- 어떻게 실패하나: `A6` 을 지키면(한 번) ㉢ 의 산출은 후보 목록이고 `C2-b`·`C2` 가 못 선다. ㉢ 에서 쓸 산출을 얻으려면 다른 이름으로 **한 번 더** 돌려야 하고 그 순간 `A6` 이 깨진다. 해소 문법이 없으므로 「다른 이름」도 실패한다
- 어디가 걸리나: `intent.md` `A6` × ㉢
- 획득: 실측
- 모집단: 원의도
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 하나를 채우면 다른 하나가 원리상 못 선다

### `C2-c` 와 `C2-a` 가 ㉠ 에서 서로를 배제한다
- 어떻게 실패하나: `C2-c` 는 미끼를 *"같은 파일 안의 다른 심볼"* 로 못 박는다. ㉠ 의 **변경 자리**는 `crates/pal-cli/src/ledger.rs:334` 이고 그 줄은 **`nodes_of`(`:301`) 함수 본문 안**이다. 그러니 「같은 파일의 다른 심볼」 미끼는 변경 자리를 **품은** 함수이고, 그 고유 줄이 변경과 직결돼 `C2-a`(미끼 귀속 0)가 깨진다. 반대로 미끼를 정의 파일 `crates/pal-core/src/coord.rs` 에서 고르면 `PM1-06` 이 그대로 재발한다
- 어디가 걸리나: `intent.md` `C2-a`×`C2-c` · `crates/pal-cli/src/ledger.rs:301,334`
- 획득: 실측
- 모집단: 원의도
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 「같은 파일」이 **정의 파일인지 변경 파일인지** 문면에 없고, 어느 쪽을 골라도 조건 하나가 깨진다

### `A4`·`C2`·`C1` 이 같은 사실을 세 번 배제해 `C2` 의 모집단을 비운다
- 어떻게 실패하나: `A4` 는 사전 등록에 **§5.8 표 전문**을 넣게 한다. `C1` 의 차이는 *사전등록 ↔ 실제 변경*이므로 사전 등록에 들어간 것은 차이가 못 된다. `C2` 는 그 위에 *"§5.8 에 이미 적힌 사실이 아니다"* 를 다시 건다. ㉠ 의 실측 고유 줄 5 개는 {§5.8 이 적은 것} ∪ {심볼 식별 메타데이터} 로 **완전히 덮인다**
- 어디가 걸리나: `intent.md` `A4`×`C1`×`C2`
- 획득: 실측
- 모집단: 원의도
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 조건 셋이 각각은 옳은데 **합이 합격선 본체의 모집단을 0 으로** 만든다

### ㉡ 을 첫머리에 못 박은 `## 순서` 절의 근거를 `#126` 자신이 부정한다
- 어떻게 실패하나: 순서 절은 *"그 검사가 `E1`·`E4`·`B2` 를 **판정한다**"* → §11③ 금지역 예외라고 적는다. 그런데 `#126` 본문 `## 왜 지금은 안 뒤집히나` 가 *"두 회차가 다 대상이라 **판정이 안 바뀐다**. 바뀌는 것은 판정문의 그 한 줄뿐"* 이라고 못 박는다. 실제 결함은 **하한 판정문 문자열**을 고르는 자리이고 통과/불통과 경로에 안 물린다
- 어디가 걸리나: `intent.md` `## 순서` ↔ `#126` 본문 · `xtask/src/main.rs:6254-6272`
- 획득: 조회
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 회차의 작업 순서 전체와 두 산출 보존 절차가 **과장된 근거** 위에 섰다

### ㉡ 에서 `C2` 가 구성상 0 에 가깝다
- 어떻게 실패하나: `#126` 이 제시하는 고침은 `.rev()` 와 판정문 문자열 둘이 전부다. `pal touch check_ledger_pair` 는 호출자·피호출자·미해소 참조를 내는데, 변경은 그 어느 것과도 안 맞물린다. `C1` 이 요구하는 *"`touch/<이슈>.txt:N` 이 원인"* 의 원인 끝이 없다
- 어디가 걸리나: `intent.md` `C1`·`C2` · `xtask/src/main.rs:6254-6272`
- 획득: 실측
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 세 자리 중 둘이 재기 전에 0 으로 정해졌고 ㉢ 은 측정 불능 — **차선책 1(반증)이 구성상 예정돼 있다**

### `A5` 가 인과를 안 세우는데 인과를 세운 것으로 적혀 있다
- 어떻게 실패하나: `CA1-02` 는 *"인과가 어디에서도 안 섰다"* 를 고치려고 `A5` 를 세웠다. 그러나 `git merge-base --is-ancestor` 가 대는 것은 **커밋 순서**뿐이다. 실제 변경을 워킹트리에서 먼저 다 쓰고, touch 를 먼저 커밋한 뒤 코드를 커밋하면 `A5` 가 통과한다. `A1-c` 는 사전 등록에 **바이트 앵커**를 걸었지만 `A5` 에는 대응하는 앵커가 **없다**
- 어디가 걸리나: `intent.md` `A5` ↔ `A1-c` 와 대비
- 획득: 추정
- 모집단: 원의도
- 유효성: 추정
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 원문 전제가 여전히 안 재어지는데 재어졌다고 적혔다

### `A5` 의 「그 자리의 첫 코드 변경 커밋」에 기계 정의가 없다
- 어떻게 실패하나: 커밋 → 재는 자리(㉠/㉡/㉢)의 사상이 어디에도 없다. 세 자리가 `crates/pal-core`·`crates/pal-cli`·`xtask`·`crates/pal-intent` 에 걸쳐 있고, 한 커밋이 둘 이상을 건드리면 「첫」이 갈린다. `A1` 은 명령까지 적었는데 `A5` 는 대상 선정을 산문으로 남겼다
- 어디가 걸리나: `intent.md` `A5` ↔ `A1`
- 획득: 추정
- 모집단: 원의도
- 유효성: 추정
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: ⟨결정론적⟩ 태그가 붙었는데 해석이 들어간다 — `CA1-13` 이 잡은 형태와 같다

### `D4-a` 가 실행되는 스크립트를 동결 문서로 취급한다
- 어떻게 실패하나: `D4-a` 는 *"동결 문서와 합격선 파일은 안 고친다"* 로 다섯 자리를 한 규칙으로 덮는다. 그런데 `scripts/f12-verify.py:559-560` 은 문서가 아니라 **런타임 분류 코드**이고, 그 두 줄이 `D1` 이 정정하려는 바로 그 거짓 전제를 **분류에 쓴다**
- 어디가 걸리나: `intent.md` `D4-a`·`D1` · `scripts/f12-verify.py:559-560`
- 획득: 실측
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 게이트 문서에 「거짓이 됐다」를 적는 동안 스크립트는 계속 거짓 분류를 산출한다

### 차선책 1 이 D·E 절의 처분을 안 적는다
- 어떻게 실패하나: *"`C2` 가 0 이면 반증으로 적고 멈춘다. 더 재지 않는다."* 그런데 남은 조건이 `D1`~`D4-a` · `E1`~`E6` 열셋이다. `D2` 는 *"이 회차의 측정을 근거로"* 답하라는데 그 측정이 반증이다. 「멈춘다」가 D 절을 포함하는지가 없고, 포함하면 `D4`·`E1`·`E5` 가 문면상 깨진다
- 어디가 걸리나: `intent.md` 차선책 1 ↔ D·E 절
- 획득: 조회
- 모집단: 원의도
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 실측상 **가장 일어나기 쉬운 갈래**인데 그 갈래의 종료 절차가 미정이다

### `A2` 의 「산출 첫 줄」이 빈 줄이다
- 어떻게 실패하나: `pal touch` 출력의 1 줄은 **빈 줄**이고 다이제스트는 2 줄에 있다. `A2` 는 *"**산출 첫 줄의** 워킹트리 다이제스트"* 를 요구한다
- 어디가 걸리나: `intent.md` `A2`
- 획득: 실측
- 모집단: 자기장치
- 유효성: 참
- 해악도: 미관
- 대상: 계획자신
- 얼마나 아픈가: 문면대로 집으면 빈 줄을 적게 된다

### ㉡ 을 먼저 닫는 동안 `ledger-pair-after.txt` 가 RED 로 난다
- 어떻게 실패하나: 순서 절은 ㉡ 을 먼저 닫고 *"게이트·종료 문서는 그 뒤에"* 라고 적는다. `check_ledger_pair` 의 하한은 「끝난 회차 중 가장 최근」이 검사에 들었는지를 보고, 진행중 계수는 `report.md` 없는 회차가 둘 이상이면 발화한다. 이 회차 디렉터리는 이미 커밋돼 있고 `report.md` 도 게이트도 없다. `-after.txt` 가 RED 로 나면 *"고친 검사가 자기를 통과시켰나"* 를 대는 유일한 증거가 안 읽힌다 — 그 경우의 판독법이 순서 절에 없다
- 어디가 걸리나: `intent.md` `## 순서` · `xtask/src/main.rs:5977`·`6256-6272`
- 획득: 추정
- 모집단: 자기장치
- 유효성: 추정
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 그 RED 가 언제 풀리는지가 순서 절과 안 맞물린다

---

## 내가 기각한 것

### `E1` 의 「절 이름 넷」이 A~E 다섯 절과 안 맞는다
- 어떻게 실패하나: 조건 절이 다섯인데 게이트가 넷을 요구해 검산이 안 선다고 의심했다
- 어디가 걸리나: `.claude/skills/round/SKILL.md` 게이트 절
- 획득: 조회 — 「절 이름 넷」은 게이트 문서의 절 이름(`## 합격선`·`## 판정`·`## 효과`·`## 범위 밖`)이고 조건 묶음과 무관하다
- 모집단: 규약
- 유효성: 거짓
- 해악도: 미관
- 대상: 계획자신
- 얼마나 아픈가: 기각했다

### ㉠ 좌표가 갈린다 — 잠긴 의도와 `pal touch` 의 답이 다르다
- 어떻게 실패하나: `intent.md` 는 `pal-cli/src/ledger.rs:334`, `pal touch` 는 `pal-core/src/coord.rs:259` 를 답한다
- 어디가 걸리나: `observations/red.md` ⓓ
- 획득: 조회 — 착수 관측이 *"`identity_ceiling` 정의 → `crates/pal-core/src/coord.rs:259`"* 로 둘을 이미 적었다. 호출 자리와 정의 자리의 구별이지 오류가 아니다
- 모집단: 회차기록
- 유효성: 거짓
- 해악도: 미관
- 대상: 계획대상
- 얼마나 아픈가: 기각했다

### `#126` 이 이미 고쳐져 있다
- 어떻게 실패하나: `check_ledger_pair` 주석이 *"산문도 사전순도 안 쓴다"* 라고 적는다
- 어디가 걸리나: `xtask/src/main.rs:6254-6258`
- 획득: 실측 — 그 주석은 **진행중 계수** 자리 것이고, 사전순은 하한의 `.iter().rev().find(…)` 에 그대로 남아 있다
- 모집단: 저장소
- 유효성: 거짓
- 해악도: 미관
- 대상: 계획대상
- 얼마나 아픈가: 기각했다

### `B4` 의 mtime 전량 갱신이 `A2` 의 다이제스트를 흔든다
- 어떻게 실패하나: `.rs` 전량 `touch` 가 산출 해시를 바꾼다고 의심했다
- 어디가 걸리나: `intent.md` `B4`×`A2`
- 획득: 실측 — 같은 심볼 재실행이 바이트로 동일하고 해시도 동일하다. 내용 기반으로 보인다
- 모집단: 자기장치
- 유효성: 거짓
- 해악도: 미관
- 대상: 계획자신
- 얼마나 아픈가: 기각했다

### `A1-a` 의 음성 대조 ③(둘을 같은 커밋에)이 안 뒤집힌다
- 어떻게 실패하나: `git merge-base --is-ancestor X X` 가 0 을 내므로 그 사본이 통과한다고 의심했다
- 어디가 걸리나: `intent.md` `A1`
- 획득: 조회 — `A1` 이 `<봉인> != <touch>` 를 **따로** 요구하므로 그 절이 잡는다
- 모집단: 원의도
- 유효성: 거짓
- 해악도: 미관
- 대상: 계획자신
- 얼마나 아픈가: 기각했다

### `pal touch` 가 `.palimpsest/index.redb` 를 써서 저장소를 변이시킨다
- 어떻게 실패하나: 산출을 얻는 행위가 저장소를 더럽힌다고 의심했다
- 어디가 걸리나: `.palimpsest/index.redb`
- 획득: 실측 — mtime 이 갱신되지만 `git status --porcelain` 이 빈 출력이고 산출은 재현된다
- 모집단: 저장소
- 유효성: 거짓
- 해악도: 미관
- 대상: 계획자신
- 얼마나 아픈가: 기각했다

---

## 능력 부재

- `B2`(검사 28)·`B3`(1028)·`B4`(경고 0)은 **`cargo` 금지**라 못 쟀다. 그 셋은 이 라운드가 안 잰다.
- `A1`·`A5` 의 커밋 사슬은 아직 커밋이 없어 실물로 못 뚫어 봤다 — 그 둘은 **추정**으로 적었다.
