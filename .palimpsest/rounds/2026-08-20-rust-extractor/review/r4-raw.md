독립 리뷰 라운드 4/5 — 모든 측정을 직접 돌렸다.

## 합격선 축

**통과 42 · 반증 1 · 대조 불가 1 · 미측정 0 = 44** ✔

주요 판정:

| 조건 | 판정 | 근거 |
|---|---|---|
| A1~A9 | 통과 | `symbol.contains RustExtractor` → 4 · 시험 132 무패 |
| B1·B2·B4 | 통과 | `Cargo.lock` `tree-sitter-rust 0.24.2` · allow-git 3 · deny 4축 ok |
| **B3** | **대조 불가** | 승자 행만 재현됨(오류 6·ERROR 43 / upstream behind **0** — `gh api compare` 실행) |
| C1·C2 | 통과 | 변형 7 · ditto **4578·0** · portal-backend **1340·0** · 대장 동일 |
| D1~D5 | 통과 | 표본이 코드보다 앞 · 380 · 파싱오류 6·43 재현 · syn **268/268·0** · ①15 ②15 |
| E1~E5 | 통과 | Rust L1 · 결박 46 · **접기 끔 73 정확 재현** · 표식 130·도달 69·못 도달 61 **여덟 칸 전부 일치** |
| F1~F5 | 통과 | f01 여덟 · f03-3 · ditto **330** · check 20/20 · test 무패 |
| G1~G3 | 통과 | 질의 재현 · 결박 43→46 |
| H1~H5 | 통과 | 원문 보존 · `grep "1급.*넷" crates/ xtask/` → **0** · 네 이름 0 |
| **H6** | **반증** | 마지막 커밋 `8dc6ec2` **미푸시** — `gh api …/8dc6ec2/check-suites` → **422** · `git log origin/main..HEAD` → **2 커밋** |
| I1~I5 | 통과 | #77~#81 OPEN |

**음성 대조 다섯 중 넷을 직접 재현**했다.

## 미측정 목록

| # | 안 잰 조건 | 모집단 | 유효 | 해악도 | 좌표 | 왜 못 쟀나 |
|---|---|---|---|---|---|---|
| 1 | 없음 | — | — | — | — | 44 를 전부 판정했다 |

## 의도 축 — 빠진 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | 회차의 **마지막 커밋이 원격에 없다** — CI 가 `8dc6ec2` 를 본 적이 없는데 게이트·보고가 H6 을 통과로 셌다. 그 커밋은 **코드 변경**을 담는다 | 원의도 | 참 | **금지역** + 실패 | H6 | `rust-extractor.md:64` · `report.md:14` | `gh api …/8dc6ec2/check-suites` → `422 No commit found` |
| 2 | **접기 미적용(73·137)을 재는 도구가 저장소에 없다** — `이어지는_doc` 에 스위치가 없어 소스를 고쳐야 재진다. R3 이 `coord_collisions` 로 고친 병이 이 축에 그대로 남았다 | 원의도 | 참 | 거짓신호 | E3 | `parse.rs:541,643` | 격리 사본에서 고쳐야 137/73 이 나온다. 저장소 이진으로는 91/46 만 |
| 3 | **문법 축 표의 지는 후보 세 행을 재는 도구가 없다.** 승자 행만 재현된다 | 원의도 | 참 | 거짓신호 | B3 | `rust-extractor.md:129-135` | 승자: 오류 6·ERROR 43 · behind 0. 나머지 셋은 문법 넷을 빌드해야 하고 절차가 없다 |

## 의도 축 — 요구되지 않은 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | 없음 — 도구 셋은 측정에 직접 쓰이고 게이트가 남는다는 사실을 적었다 | — | — | — | — | — | — |

## 의도 축 — 있는데 틀린 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | ★ **반증된 수가 코드 doc 다섯 자리에 「실측」으로 그대로 산다.** 게이트·ADR·criteria 는 정정했는데 **결박이 실제로 걸린 코드 좌표**는 옛 수를 말하고, `symbol.rs` 는 그것을 **결정의 근거**로 든다 | 원의도 | 참 | **금지역** | C1 | `symbol.rs:13,46` · `rust.rs:244` · `classify.rs:234` · `2026-08-20-owner-direction.md:72` | `--example coord_collisions` → `선언 6444 · 충돌 215(3.3%) · 접은 모델 215`. `symbol.rs:46` 원문: `/// 6.1% → 11.2% 로 는다(#66 실측).` |
| 2 | **합격선 정본이 자기 안에서 갈렸다** — 같은 파일이 시험지원을 `25`(④)와 `24`(corpus 블록)로 말한다. 실측은 12+7+5+1 = **25** | 원의도 | 참 | **금지역** | D3 | `criteria.toml:11123` ↔ `:11188` | `grep -c '^crates/cargo-test-support/'` → **12** |
| 3 | **`SymbolKind` 확장의 새 근거가 자기 산출에 안 적용된다** — 「`struct` 를 `class` 라 부르면 거짓」이라 했는데 Rust `fn` 을 **`fun`**(Kotlin 표기)으로 낸다 | 원의도 | 참 | 거짓신호 | C1 | `symbol.rs:64-66` | `pal query symbol.contains RustExtractor` → `fun language …` |

## 이번 라운드의 새 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | **게이트 ⓑ 표의 ② 가 반증된 수를 그대로 진다** — 본문은 *"다중집합으로 고치니 15"* 인데 **판정 표는 13** | 회차기록 | 참 | **금지역** | ⓑ② | `rust-extractor.md:81` ↔ `:256,280` | `rust-recall-verify.py --syn` → `②15` |
| 2 | **게이트와 종료 보고의 44 계수가 갈렸다** — 43/0/1/0 ↔ 42/0/2/0. 마지막 커밋이 게이트만 고치고 보고를 안 옮겼다 | 회차기록 | 참 | **금지역** | 판정 계수 | `rust-extractor.md:59` ↔ `report.md:11` | `git diff 908e41c..8dc6ec2` |
| 3 | **게이트 안에서 D4 판정이 두 자리 대 세 자리로 갈렸다** — 표·본문은 「통과」, 헤딩·머리표·승계 문장 셋은 「대조 불가」 | 회차기록 | 참 | **금지역** | D4 | `:59,230` ↔ `:73,149,219` | `grep -n "대조 불가 둘\|D4 는 아래"` |
| 4 | **`record.py check` 를 인자 없이 부르면 0 개를 훑고 초록** — H4 의 문면을 그대로 실행하면 아무것도 안 재고 통과한다 | 규약 | 참 | 거짓신호 | H4 | `record.py` | `record.py check` → `합계 0행 · 문제 없음` rc=0 |

## 자기 산출에 대한 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | 게이트의 **「영구히 남긴 도구 둘」이 셋이다** — `coord_collisions.rs` 가 목록에 없다. **그 도구를 만든 바로 그 커밋이 절 제목을 안 고쳤다** | 회차기록 | 참 | 거짓신호 | — | `rust-extractor.md:357-366` | `git log --diff-filter=A` |
| 2 | 게이트의 **「아홉 자리」가 낡았다** — 코드 diff 의 `넷` 제거 줄은 **12** 다 | 회차기록 | 참 | 미관 | H2 | `rust-extractor.md:209` | `git diff … \| grep -cE "^-.*넷"` → 12 |
| 3 | **`coord_collisions.rs` 에 단위시험도 음성 대조도 없다** — `cargo xtask test` 는 `--all-targets` 라 **빌드만** 한다 | 자기장치 | 참 | 거짓신호 | — | `coord_collisions.rs:76` | 파일 전문 · `#[test]` 0 |

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효 | 해악도 | 좌표 | 왜 아니었나 |
|---|---|---|---|---|---|---|
| 1 | 「접은 모델」이 `Module`·`Const`·`Static`·`Macro` 를 안 접어 반증이 약하다 | 자기장치 | 거짓 | — | `coord_collisions.rs:104` | 격리 사본에서 넷을 더 접어 다시 쟀다 → **94·215(3.3%) 그대로**. **반증이 더 넓은 모델에서도 선다** |
| 2 | 두 수정(doc 접기 · `impl` 2 패스)이 시험에 안 묶여 「고쳤다」가 말뿐이다 | 원의도 | 거짓 | — | `parse.rs:648`·`rust.rs:145` | 격리 사본에서 둘 다 되돌리니 **정확히 둘만 FAILED**(130 passed; 2 failed) |
| 3 | E5 의 61 분포가 세 번 갈린 수와 같은 병일 것 | 원의도 | 거짓 | — | `rust-extractor.md:198` | 착수 트리를 다시 쟀다 → 표식 130·도달 69·못 도달 61, **여덟 칸 전부 일치** |
| 4 | `syn-oracle/target/` 65 파일이 대장을 오염시킨다 | 자기장치 | 거짓 | — | `.gitignore:14` | `git ls-files scripts/syn-oracle` → **3**. `pal ledger 480` = `git ls-files \| wc -l` **480** |
| 5 | B3 를 「반증」으로 올려야 한다 | 원의도 | 거짓 | — | `criteria.toml:11113` | 축 측정은 등록 뒤에 돌았고 같은 답을 냈다. 「대조 불가」가 문면과 보수성 둘 다에 맞다 |
| 6 | D3 이 「시험지원 24」를 안 실어 반증이다 | 원의도 | 거짓 | — | `intent.md:99` | 실측이 **25** 다. 게이트가 25 를 적은 것이 옳다 |
| 7 | `count_marked` 가 ditto 330 을 못 잰다 | 자기장치 | 거짓 | — | `count_marked.rs` | 정확 재현. **접기 끈 이진으로도 330 불변** — 접기가 TS 로 안 샌 것이 실측으로도 선다 |
| 8 | `Markdown 219/228/232` 갈림이 발견이다 | 원의도 | 거짓 | — | `criteria.toml:11277` | 게이트가 *"변하는 수는 안 적는다"* 를 선언했고 219 는 착수 시점 값이다 |

## 끝내도 되는가

**안 된다** — 금지역 다섯과 실패 하나(H6). 그중 넷은
**「문서에 적힌 수가 산출과 다르다」가 네 번째로 재발한 것**이다.
