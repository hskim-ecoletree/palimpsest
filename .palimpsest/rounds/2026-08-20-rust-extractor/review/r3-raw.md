독립 리뷰 라운드 3/5 — HEAD `908e41c` · 저장소 손대지 않음 · 돌연변이 시험은
`git archive HEAD` 로 뜬 격리 사본에서만.

## 합격선 축

**통과 42 · 반증 1 · 대조 불가 1 · 미측정 0 = 44** ✔
(게이트는 42/0/2/0. 갈린 자리 둘: **D4** 를 나는 통과로, **H6** 를 반증으로 본다.)

주요 판정:

| 조건 | 판정 | 근거 |
|---|---|---|
| A1~A9 | 통과 | `대문자_확장자도_1급으로_인식한다` 통과 · `symbol.contains RustExtractor` → 4 |
| B1·B2·B4 | 통과 | `Cargo.lock:1998` 같은 rev · deny 4 축 ok · allow-git 3 |
| **B3** | **대조 불가** | 축 등록 `c6e5155`(07:30) < 승자 확정 `a2ed4dd`(07:21). 게이트 판정에 동의 |
| C1·C2 | 통과 | 변형 7 · f03-3 움직인 것 0/0 · f01 대조 동일 |
| D1·D2·D3 | 통과 | 표본이 코드보다 앞 · SHA 둘 · 380 재현 · 오류 6·43 재현 (⚠ 「시험지원 24」는 실측 25) |
| **D4** | **통과** | `--syn` 재현: `손 표본 268 · syn 268 · 손에만 0 · syn 에만 0`. 조건 문면(*"돌았고 차이가 판정에 적혔다"*)은 HEAD 에서 충족 |
| D5 | 통과 | 다중집합 `① 15 · ② 15 · 94.40%/94.40%` |
| E1~E5 | 통과 | Rust L1 120 파일 · 결박 46 · 분포 합 61 (⚠ E3 의 69 는 낡았다 — 아래) |
| F1~F5 | 통과 | f01 여덟 · f03-3 넷 · ditto 330 · check 20/20 · test 전량 |
| G1~G3 | 통과 | 질의 재현 · 결박 46 재현 |
| H1~H5 | 통과 | 원문 보존 · 「넷→다섯」 12 자리 · 절 이름 다섯 |
| **H6** | **반증** | 회차의 **마지막 커밋은 `908e41c`** 인데 push 안 됨 — `gh api …/908e41c/check-runs` → `422 No commit found`. `success` 는 `966850b`(마지막 아님) |
| I1~I5 | 통과 | #77~#81 OPEN |

## 미측정 목록

| # | 안 잰 조건 | 모집단 | 유효 | 해악도 | 좌표 | 왜 못 쟀나 |
|---|---|---|---|---|---|---|
| 1 | 없음 — 44 를 전부 판정했다 | — | — | — | — | — |

## 의도 축 — 빠진 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | **회차의 마지막 커밋에 CI 판정이 없다.** `908e41c` 는 push 조차 안 됐는데 게이트 표가 H6 을 통과에 넣었다 | 회차기록 | 참 | **금지역** | H6 | `rust-extractor.md:61-68` | `gh api …/908e41c/check-runs` → `422` · `git rev-parse origin/main` = `966850b` |
| 2 | **접기 미적용 결박 수가 종료 상태에서 재측정되지 않았다.** 게이트의 69 는 doc-접기 빈 줄 버그 수정(`c47328c`) **이전** 값이다 | 회차기록 | 참 | **금지역** | E3 | `rust-extractor.md:168` | 격리 사본에서 접기만 끄고 빌드 → `결박됨 **73**`(켬 46). 퇴로 셋째가 *"69 를 합격선으로 쓴다"* 라 하중을 진다 |

## 의도 축 — 요구되지 않은 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | 잠긴 의도 §범위 밖이 *"고칠 수 없으므로 적는다"* 로 못 박은 동결 문서 둘을 회차가 실제로 고쳤고 **`## 개정` 표에 그 줄이 없다** | 회차기록 | 참 | 거짓신호 | `intent.md:163` | `git diff 56926aa..HEAD -- docs/gates/F11-touch.md F12.md` |
| 2 | `docs/plan/00-stack.md` 수정 — 44 조건 어느 것에도 안 걸린다(H2 는 「코드 doc」) | 저장소 | 참 | 미관 | `00-stack.md:142` | `git diff` |

## 의도 축 — 있는데 틀린 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | ★ **R-16 수치 전부가 실제 추출기 산출로 재현되지 않는다.** *"cargo 7,620 선언 중 464(6.1%) · 접으면 852(11.2%)"* 가 게이트·`criteria.toml`·`report.md`·#78 에 「실측」으로 실렸는데 **그 스파이크는 저장소에 없다** | 회차기록 | 참 | **금지역** | C1 | `criteria.toml [rust].what_was_already_known` ④⑤ · `rust-extractor.md:124,310` | cargo 380 전수 `pal symbols --graph`: **선언 6,444 · `ordinal>0` 121 = 1.9%**(충돌에 낀 심볼 215 = 3.3%). 접은 모델 = 122 = 1.9%. `syn-oracle` 모델 = 6,440 · 86. **어느 셈으로도 7,620·464·6.1%·11.2% 가 안 나온다** |
| 2 | 게이트 음성 대조 표 ③ 의 「실제」가 **85** 인데 HEAD 실측은 **91** 이다. `report.md:66` 은 91 로 적어 두 문서가 갈렸다 | 회차기록 | 참 | **금지역** | 음성대조 | `rust-extractor.md:38` | `count_marked -- . rs` → `표식 주석 91 · 붙은 것 88` |
| 3 | 게이트 E 절 ledger 블록이 **세 번째로** 재현되지 않는다 — 게이트 `477/309/230`, HEAD `478/310/231`. 같은 문서가 *"실행 출력을 그대로 붙였다"* 라 못 박고 **앞의 두 번을 스스로 기록한** 자리다 | 회차기록 | 참 | **금지역** | E1·G1 | `pal ledger` → `파일 478 · unsupported 310 · Markdown 231` · 워킹트리 clean |
| 4 | 게이트가 어긋남을 **28** 로 적는데 같은 문서의 실행 블록은 15+15=**30**, `report.md` 는 세 자리에서 **30** | 회차기록 | 참 | 거짓신호 | D5 | `rust-extractor.md:284` ↔ `:236` | `rust-recall-verify.py` |
| 5 | D3 의 「시험지원 **24**」가 라벨 넷을 문면대로 세면 **25** 다(`cargo-test-support` 11→**12**). 11 이 나오려면 `build.rs` 를 빼야 하는데 그 규칙이 어디에도 없다 | 원의도 | 참 | 미관 | D3 | `rust-recall-sample.tsv:39` | `grep -c cargo-test-support` → **12** |

## 이번 라운드의 새 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | **동결 문서 편집이 마크다운을 깨뜨렸다** — `F11-touch.md` 는 볼드를 열고 인용블록으로 끊은 뒤 `그때의 측정이다..**` 로 닫아 마침표가 둘이고 볼드가 안 닫힌다 | 저장소 | 참 | 미관 | 없음 | `F11-touch.md:314-319` | `sed -n '310,322p'` |
| 2 | 같은 편집이 `F12.md` 에서는 **문장 한복판에** 인용블록을 넣어 문장이 두 동강 났다 | 저장소 | 참 | 미관 | 없음 | `F12.md:302-306` | `sed -n '296,310p'` |
| 3 | ★ **`record.py check` 는 인자 없이 부르면 0 행을 훑고 「문제 없음」 rc=0** — 저장소 규율(*"모집단이 비면 실패다"*)의 정면 반대. 실제 게이트 경로는 경로를 넘기므로 지금은 살아 있으나 **H4 의 문면은 0 행 초록으로도 충족된다** | 규약 | 참 | 거짓신호 | H4 | `record.py:164-191` | `record.py check` → `합계 0행 · 문제 없음` rc=0 |
| 4 | `report.md`·`state.md` 의 「종료」 수가 HEAD 와 다르다 — `parsed 120`·`Rust 119` 인데 실측 `121`·`120` | 회차기록 | 참 | 거짓신호 | 없음 | `report.md:23` · `state.md:52` | `pal ledger` |
| 5 | `state.md` 의 「레코드 **130 행**」이 실제 **141**, 「`rust.rs` 시험 **11**」이 실제 **13** | 회차기록 | 참 | 거짓신호 | 없음 | `state.md:39,56` | `wc -l` · `grep -c '#\[test\]'` |
| 6 | 게이트·`report.md` 범위 밖의 「Markdown **228** 파일」이 HEAD 에서 **231**(의도는 219) | 회차기록 | 참 | 미관 | 없음 | `rust-extractor.md:327` | `pal ledger` |
| 7 | H2 의 잔여 — 시험 이름 `일급_넷은_따로_잡힌다` 가 아직 「1급은 넷」을 현재형으로. **회차가 고친 바로 그 파일 안**이고 착수 전부터 있었다 | 저장소 | 참 | 미관 | H2 | `recognize.rs:209` | `git show 56926aa:…` |

## 자기 산출에 대한 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | **`count_marked` 는 확장자 인자를 잊으면 「파일 0 · 표식 0」을 내고 rc=0** — F3(금지역)의 유일한 관측 장치인데 **빈 모집단을 안 알린다** | 자기장치 | 참 | 거짓신호 | F3 | `count_marked.rs:19-38` | `--example count_marked -- ~/dev/projects/ditto` → `파일 0` |
| 2 | **`rust-recall-verify.py` 를 부르는 자동 자리가 0 이다.** 합격선 ⓑ①② 의 유일한 측정 장치가 `xtask` 에도 CI 에도 없다 | 자기장치 | 참 | 거짓신호 | D5 | `rust-recall-verify.py:1` | `grep -rn "rust-recall-verify"` → 자기 자신과 산문뿐 |
| 3 | `syn-oracle` 은 손 표본 머리말의 규칙을 **같은 저자가 두 번째로 옮겨 쓴 구현**이다. 파서는 다르지만 규칙 해석은 독립이 아니므로 *"두 독립된 방법"* 은 강도가 과하다 | 자기장치 | 추정 | 거짓신호 | D4 | `syn-oracle/src/main.rs:12-60` | 규칙 ①⑤ 가 tsv 머리말과 일대일 |

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효 | 해악도 | 좌표 | 왜 아니었나 |
|---|---|---|---|---|---|---|
| 1 | `.RS` 회귀가 다시 살아 있다 | 저장소 | 거짓 | — | `recognize.rs:142` | 격리 사본에서 되돌리니 시험이 **즉시 FAILED**. 시험이 실제로 문다 |
| 2 | doc 접기 빈 줄 수정에 무는 시험이 없다 | 자기장치 | 거짓 | — | `parse.rs:650` | 줄 번호 판정을 되돌리니 `빈_줄이_doc_접기를_끊는다` FAILED |
| 3 | `impl` 2 패스·모듈 경계 수정이 측정되지 않는다 | 자기장치 | 거짓 | — | `rust.rs:137-176` | 돌연변이 둘 다 물었다 — 부모 일치 제거 → 2 FAILED; 순서 의존 복원 → 1 FAILED |
| 4 | 다중집합 비교가 아직 열쇠 집합이다 | 자기장치 | 거짓 | — | `rust-recall-verify.py:36` | `Counter` 뺄셈이고 열쇠 수를 **둘 다** 보고한다 |
| 5 | `.gitignore` 의 `**/target/` 이 추적돼야 할 파일을 뺀다 | 저장소 | 거짓 | — | `.gitignore:18` | `git ls-files \| grep -c 'target/'` → 0 |
| 6 | `effect.md` 의 ledger 수가 HEAD 와 달라 거짓이다 | 회차기록 | 거짓 | — | `effect.md:20-40` | 같은 문서가 `Snapshot palimpsest@bdd3934+worktree` 로 **시점을 명시**한다. 효과는 시점 산출이므로 정당 |
| 7 | `count_marked`·`syn-oracle` 은 의도에 없는 산출이다 | 자기장치 | 거짓 | — | `rust-extractor.md:332` | 게이트가 「영구히 남긴 도구 둘」 절로 이미 명시 |
| 8 | B3 을 통과로 올릴 근거가 있다 | 회차기록 | 거짓 | — | `rust-extractor.md:205` | 승자가 축 등록보다 9 분 앞선 커밋에 이미 박혀 있다. **대조 불가가 옳고 통과로 올리면 더 나쁘다** |
| 9 | 재현율 94.40% 가 미달이므로 회차가 실패다 | 원의도 | 거짓 | — | `intent.md:146` | 퇴로 첫째가 *"대조 불가로 적고 분할한다. 수를 낮추지 않는다"* 로 사전 등록됐고 그대로 썼다 |
| 10 | `이어지는_doc(first, _source)` 의 미사용 인자가 죽은 가지다 | 자기장치 | 거짓 | — | `parse.rs:642` | 경고도 안 나고 아무것도 안 깨진다. 미관 이하 |

## 끝내도 되는가

**안 된다** — 금지역 다섯(빠진 것 1·2, 있는데 틀린 것 1·2·3)과 반증 하나(H6).

특히 **「있는데 틀린 것 1」** 은 판정·`criteria.toml`·이슈 #78 에 「실측」으로 실린 수가
**어떤 모델로도 재현되지 않는** 자리이고, **이 회차가 D4 에서 고쳤다고 적은 병
(*"스파이크를 저장소에 안 남겼다"*)과 같은 병이 다른 수치에 그대로 남아 있는 것**이다.
