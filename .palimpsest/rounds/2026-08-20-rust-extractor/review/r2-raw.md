독립 리뷰 라운드 2/5 — 전 44 조건을 하나씩 재고, 격리 사본에서 변이 시험 4 건을 돌렸다.

## 합격선 축

**넷의 합 = 44** — 통과 **41** · 반증 **0** · 대조 불가 **2** · 미측정 **1**
(게이트 `## 판정` 표의 41/0/2/1 과 **같다**. 다만 같은 문서의 다른 두 자리는 43·42 를 말한다 — 새 발견 1·2)

주요 판정(전 44 행은 반환문 원본):

| 조건 | 판정 | 근거 |
|---|---|---|
| A1~A9 | 통과 | `pal symbols a.xyz` → *"아는 것은 Kotlin · Java · JavaScript · TypeScript · Rust 다섯 이다"* · 시험 786 통과 |
| B1·B2·B4 | 통과 | `Cargo.toml:137` · `Cargo.lock:1995-1999` · `allow-git` 3 |
| **B3** | **대조 불가** | 축 등록 `c6e5155 07:30:16` · 승자 박힘 `a2ed4dd 07:21:26` — 게이트 판정을 그대로 받는다 |
| C1·C2 | 통과 | 변형 7 추가(총 16) · ditto 4,578 **0 이동** |
| D1·D2·D3·D5 | 통과 | tsv 머리말에 `514c56dd`+`56926aa` 둘 다 · 다중집합 ①15 ②15 |
| **D4** | **대조 불가** | 지금은 재현됨(268/268·0/0). 게이트가 「그때 도구가 없었다」로 남긴 판정을 받는다 |
| E1~E5 | 통과 | Rust **L1 · 120 파일** · **결박 46** · 접기 켬 46 / 끔 73 · 표식 134 중 `cfg(test)` 뒤 13 · 분포 합 61 |
| F1~F5 | 통과 | f01 **여덟 다 통과** · ditto **330·279** · check **20/20**(파이프 없이 진짜 rc=0) · test **786 passed** |
| G1~G3 | 통과 | 질의 둘 재현 · `grep -rln ADR-0027 crates` → 5 |
| H1~H5 | 통과 | 원문 보존 · `grep "닫힌 넷\|추출 대상 넷" crates` → **0** · 네 이름 0 건 · 레코드 106행 rc=0 |
| **H6** | **미측정** | `ahead 5` — push 전 |
| I1~I5 | 통과 | #77~#81 전부 OPEN |

**음성 대조 다섯 중 넷을 재현**했다. ④(손 표본↔syn)는 `--syn` 이 저장소에 있어 재현됨.

## 미측정 목록

| # | 안 잰 조건 | 모집단 | 유효 | 해악도 | 좌표 | 왜 못 쟀나 |
|---|---|---|---|---|---|---|
| 1 | H6 — CI 가 마지막 커밋 SHA 에 `conclusion=success` | 원의도 | 참 | 거짓신호 | `intent.md:132` | 커밋 5 개가 push 안 됨. push 전에는 그 SHA 의 런이 존재하지 않는다 |

## 의도 축 — 빠진 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | **`.RS` 회귀를 잡는 시험이 0 개다.** R1 발견 8 의 코드 수정만 들어가고 방어 장치가 안 들어갔다 — 같은 리뷰의 발견 2 에 쓴 처방을 이 수정에는 안 썼다 | 원의도 | 참 | 거짓신호 | A8 | `recognize.rs:137-142` | 격리 사본에서 되돌림 → **전 시험 초록**. 같은 사본에서 doc 접기·impl 2 패스를 되돌리면 각각 1·2 건이 빨개진다 |
| 2 | **`docs/plan/00-stack.md` 가 「넷이 전부 1급이다」를 현재형으로** 말하고 지시 2026-08-12 §1 을 근거로 인용한다. 동결도 `corpus/` 도 아니라 §범위 밖에 안 들어간다 | 저장소 | 참 | 거짓신호 | 없음 | `00-stack.md:142,174` | `grep -rn "언어 넷\|문법 넷" docs/plan/00-stack.md` |

## 의도 축 — 요구되지 않은 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | **동결된 판정 문서 둘을 고쳤다.** 잠긴 의도 §범위 밖이 *"고칠 수 없으므로 적는다"* 로 등록했고 `docs/gates/README.md` 가 *"사후에 안 고친다"* 로 못 박은 자리다. 그런데 게이트는 지금도 그렇게 적는다 | 저장소 | 참 | 거짓신호 | 범위 밖 선언 | `F11-touch.md:315-317` · `F12.md:302-305` | `git diff 56926aa..HEAD -- docs/gates/F11-touch.md docs/gates/F12.md` |

## 의도 축 — 있는데 틀린 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | ★ **같은 파일에 동명 타입이 둘이면 `impl` 이 엉뚱한 쪽에 붙는다.** 2 패스 해소가 파일 전체의 첫 일치를 잡아 **모듈 경계를 넘는다.** #78 이 등록한 두 원인 어디에도 없다 | 원의도 | 참 | 거짓신호 | A9 | `rust.rs:139-146` | probe: `mod a{struct S;} mod b{impl S{fn f}}` → `f` 가 `a::S` 에. 발생률 **cargo 380 중 11(2.9%)** |
| 2 | **`//!`(내부 doc) 표식이 다음 선언에 붙는다.** `doc_주석인가` 가 `outer`/`inner` 를 안 가른다 | 원의도 | 참 | 미관 | 승격 4 | `parse.rs:624-626` | probe `조각=1 at=Some(26)`. ⚠ **이 저장소 실측 0 건** |
| 3 | **`F11-touch.md` 의 삽입이 문장을 삼켰다** — 닫는 `**` 와 마침표가 사라져 인용 블록이 굵게 표시 안에 들어갔다 | 저장소 | 참 | 거짓신호 | 없음 | `F11-touch.md:315-317` | `sed -n '314,318p'` |
| 4 | **`record.py check` 를 인자 0 개로 부르면 「합계 0행 · 문제 없음」 rc=0** — 0 개를 훑고 초록을 내는 표면 | 규약 | 참 | 미관 | H4 | `record.py` | ⚠ 이 회차의 산출이 아니다 · #74 가 형제 구멍을 등록 |

## 이번 라운드의 새 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | ★ **게이트가 통과 수를 세 자리에서 41·43·42 로 말한다.** 앞 라운드가 잡은 H5 오계상과 **같은 형태** | 회차기록 | 참 | **금지역** | 판정 자체 | `rust-extractor.md:59` vs `:84` vs 절별 | `grep -n '통과'` |
| 2 | ★ **`B. 문법 핀 (4/4)` 인데 B3 는 같은 문서에서 대조 불가다** — 대조 불가를 통과로 계상했다 | 회차기록 | 참 | **금지역** | B3 | `rust-extractor.md:97` vs `:61` | `sed -n '61p;97p'` |
| 3 | ★ **ⓑ①② 의 「실측」 블록이 낡았다** — `②13 · 95.11%` 인데 실행하면 `②15 · 94.40%`. `report.md:71` 은 94.40% 를 **이 회차의 성과로** 적는다 | 회차기록 | 참 | **금지역** | D5 | `rust-extractor.md:212-219`·`:77` | `rust-recall-verify.py` rc=1 |
| 4 | ★ **`parsed 118` 이 두 자리에 남았다** — R1 이 *"산술상 불가"* 로 이미 판정한 값. HEAD 실측은 121 | 회차기록 | 참 | **금지역** | E1 | `rust-extractor.md:36`·`:78` | `pal ledger` → `parsed 121` |
| 5 | **게이트 E 절이 「실행 출력을 그대로 붙였다」고 못 박았는데 HEAD 에서 재현 안 된다** — 붙은 것 `472/120/306`, 실행하면 `542/121/319 · binary 20 · excluded 2` | 회차기록 | 참 | 거짓신호 | E1·G1 | `rust-extractor.md:129-141` | 클론에서 `pal ledger` |
| 6 | 빠진 것 1(`.RS` 회귀 시험 0) | 원의도 | 참 | 거짓신호 | A8 | `recognize.rs:137-142` | 위 표 |
| 7 | 있는데 틀린 것 1(동명 타입 `impl` 오귀속) | 원의도 | 참 | 거짓신호 | A9 | `rust.rs:139-146` | 위 표 |
| 8 | 요구되지 않은 것 1 + 있는데 틀린 것 3(동결 문서 수정·문장 삼킴) | 저장소 | 참 | 거짓신호 | 범위 밖 | `F11-touch.md:315-317` | 위 표 |
| 9 | 빠진 것 2(`00-stack.md`) | 저장소 | 참 | 거짓신호 | 없음 | `00-stack.md:142` | 위 표 |

## 자기 산출에 대한 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | ★ **`scripts/syn-oracle/target/` 빌드 산출 65 파일(17.5 MB)이 커밋됐다.** `.gitignore` 가 `/target/`(루트 고정)만 막는다. **그 결과 이 도구 자신의 대장이 오염됐다** — `unrecognized 45→80` · `binary 1→20` · `excluded 0→2` | 자기장치 | 참 | 거짓신호 | 없음 | `.gitignore:15` · `git ls-files scripts/syn-oracle/target` → 65 | `git log --diff-filter=A` → `c47328c` · 합 17.5 MB |
| 2 | **게이트 §범위 밖이 「동결 문서는 고칠 수 없다」라 하면서 같은 회차가 그 둘을 고쳤다** | 회차기록 | 참 | 거짓신호 | 범위 밖 | `rust-extractor.md:293-295` | `git diff` |
| 3 | **`state.md` 의 수 둘이 틀렸다** — 「레코드 113 행」(실제 106) · 효과·종료 보고가 `☐` 인데 같은 커밋에 있다 | 회차기록 | 참 | 거짓신호 | 없음 | `state.md:36,13,14` | `record.py count` |
| 4 | **「시험 다섯」이 실제로는 넷이다** | 회차기록 | 참 | 미관 | 없음 | `report.md:59`·`state.md:39` | `git diff … \| grep -c '^+    #\[test\]'` → 4 |
| 5 | **종료 보고의 계수 표가 `41/0/2/1` 을 「독립 리뷰」의 수로 적는다** — 리뷰가 낸 것은 `40/1/2/1` 이다 | 회차기록 | 참 | 미관 | 없음 | `report.md:88` vs `:50` | 두 줄 대조 |
| 6 | **#78 본문이 「어긋남 28 이 전부 한 원인」이라 적는다** — 최종 실측은 30 이고 원인은 둘이다. **분할 이슈가 낡은 수를 지고 다음 회차로 간다** | 회차기록 | 참 | 거짓신호 | I2 | `gh issue view 78` | 실행 `①15 ②15` |
| 7 | **`syn` 대조군의 독립성은 「다른 파서」까지다** — 규칙은 손 표본과 같은 사람이 같은 회차에 옮겨 적었다. 268/268 이 말하는 것은 「전사가 정확하다」이지 「규칙이 옳다」가 아니다 | 자기장치 | 추정 | 미관 | D4 | `syn-oracle/src/main.rs:14-56` | 소스 대조 |
| 8 | **`syn-oracle` 이 `cargo xtask test` 모집단 밖이다** — 깨져도 빨개지는 자리가 없다 | 자기장치 | 참 | 미관 | D4 | `Cargo.toml:4` | `xtask test` 출력에 없음 |

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효 | 해악도 | 좌표 | 왜 아니었나 |
|---|---|---|---|---|---|---|
| 1 | `.RS` 가 아직 미인식이다 | 원의도 | 거짓 | — | `recognize.rs:141` | 고쳐졌다. 소문자화 **뒤** 언어를 묻는다 |
| 2 | 접기가 죽은 가지다 | 자기장치 | 거짓 | — | `parse.rs:637` | 격리 빌드로 끄니 **표식 137·붙은 것 134·결박 73**. 켜면 91/88/46 |
| 3 | 골든이 움직였다 | 저장소 | 거짓 | — | `corpus/golden/` | f01 여덟 다 통과·「대조 동일」, f03-3 ditto 4,578·0 |
| 4 | 「Kotlin 골든 둘」 중 하나만 쟀다 | 원의도 | 거짓 | — | `criteria.toml` | f01 이 `.ledger.json`, f03-3 이 `.symbols.tsv` 를 각각 댄다 |
| 5 | `syn-oracle` 이 워크스페이스 밖인 것이 검사를 **우회**한 것이다 | 자기장치 | 거짓 | — | `Cargo.toml:2-4` | 우회가 아니라 **선언된 결정**이고 세 자리에 적혔다 |
| 6 | `xtask check` 의 rc=0 은 판정이 아니다 | 자기장치 | 거짓 | — | — | 파이프 없이 진짜 rc 를 받아 **0** 이었고 출력이 20/20 을 세었다 |
| 7 | 게이트 `## 효과` 에 출력이 안 붙었다 | 회차기록 | 거짓 | — | `rust-extractor.md:261-266` | `effect.md` 가 커밋돼 있고 질의 둘·ADR 결박을 재현했다 |
| 8 | 게이트 판정 표 자체(41/0/2/1)가 틀렸다 | 회차기록 | 거짓 | — | `rust-extractor.md:57-64` | 44 조건을 독립으로 재고 **같은 41/0/2/1** 을 얻었다. 틀린 것은 다른 두 자리다 |

## 끝내도 되는가

**안 된다** — 본 목록에 **금지역 넷**(새 발견 1·2·3·4)이 남았다. 넷 다 같은 병의
다른 얼굴이다: **게이트 문서가 자기 안에서 갈렸고, 갈린 채로 「실측」이라고 말한다.**

전부 **수를 고쳐 적기만 하면 닫히는 것**이고 코드는 안 건드린다.
자기 산출 1(`syn-oracle/target/` 커밋)은 **새 발견 5 의 원인**이다 — 지우면
게이트 E 블록이 다시 재현될 가능성이 크다.
