독립 리뷰 라운드 1/5 — 리뷰 기준 커밋 `e5d7081` · 착수 `56926aa`

> ⚠ 리뷰 중에 워킹트리가 두 번 움직였다. 아래 판정은 **`e5d7081` 시점**이다.
> 금지역 출처 — `.claude/pal/policy.toml` **없음** → `intent.md` §금지역이 등록한 것.

## 합격선 축

**넷의 합 = 44** — 통과 **40** · 반증 **1** · 대조 불가 **2** · 미측정 **1**

주요 판정만 옮긴다(전 44 행은 반환문 원본에 있다):

| 조건 | 판정 | 근거 |
|---|---|---|
| A1~A9 | 통과 | `language.rs:44,54,63` · `extractor.rs:80` · `classify.rs:234` · `shell.rs:42` · `extractor.rs:97`(손 사본 **삭제**) · `main.rs:551` · `recognize.rs:57` · `symbol.contains RustExtractor` → 4 |
| B1·B2·B4 | 통과 | `Cargo.toml:134` · `Cargo.lock:1998` · deny 4 축 ok · `deny.toml:91` |
| **B3 선정 축이 측정 전 등록** | **대조 불가** | 축은 `c6e5155`(07:30), 그런데 **승자 `77a3747` 은 `a2ed4dd`(07:21) 잠긴 의도 B1 에 이미 박혀 있었다.** 축 등록이 선정을 갈랐는지 산출로 대조할 수 없다 |
| C1·C2 | 통과 | `symbol.rs:44-59` 7 변형 · ditto 4578 **0 이동** · portal-backend 1340 **0** |
| D1·D2·D3·D5 | 통과 | `c6e5155` 07:30 → `bdd3934` 07:49 · 380 재현 · 268↔266 · 94.40/95.11 |
| **D4 기계 대조군** | **대조 불가** | 게이트는 `syn` 대조 268/268·0/0 을 적었으나 **그 도구가 저장소에 없다.** 재현 불가 |
| E1~E5 | 통과 | `Rust L1 119 파일` · `결박됨 46` · 43/69 · 증분이 전부 `cfg(test)` 밖 · 61 합 검산함 |
| F1~F5 | 통과 | f01 **여덟 다 통과** · f03-3 · count_marked **330·279** · check 20/20 · test 779 passed |
| G1~G3 | 통과 | 내가 재현: `symbol.resolve extract_detailed` → `rust.rs:212` · `grep ADR-0027 crates` → 6 자리 |
| H1~H4 | 통과 | 지시 문서 6 절 + 갱신 줄 · doc 9~10 자리 · 게이트 이름 · 레코드 290행 |
| **H5 종료 보고** | **반증** | `find .palimpsest -name "report*"` → 다른 회차 셋만. **이 회차 디렉터리에 없다** |
| H6 CI | 미측정 | `ahead 4`. push 전이라 원리상 못 잼 |
| I1~I5 | 통과 | #77~#81 전부 OPEN |

**음성 대조** — 다섯 중 **셋을 내가 재현**했다. 「24→69」와 「손 표본 vs 기계 대조군」은 재현 불가(D4).

## 미측정 목록

| # | 안 잰 조건 | 모집단 | 유효 | 해악도 | 좌표 | 왜 못 쟀나 |
|---|---|---|---|---|---|---|
| 1 | H6 — CI 가 마지막 커밋에 `conclusion=success` | 원의도 | 참 | 거짓신호 | `intent.md:132` | 커밋 4 개가 push 안 됨. push 전에는 원리상 못 잰다 |

## 의도 축

### 빠진 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 1 | **종료 보고 `report.md` 가 없다. 그런데 게이트는 H 를 「5/6」으로 세어 H5 를 통과로 계상했다** | 원의도 | 참 | **금지역** | H5 | `docs/gates/rust-extractor.md:157` | `ls .palimpsest/rounds/2026-08-20-rust-extractor/` → `effect findings.jsonl intent.md premortem state.md` |
| 2 | **`parse.rs` 의 새 두 경로(`건너뛴다`·`이어지는_doc`)에 단위시험이 0 개다.** 음성 대조 ②③ 이 전부 이 둘에 걸려 있는데 회귀가 나면 빨개지는 자리가 없다 | 원의도 | 참 | 거짓신호 | 승격 4 · F3 | `parse.rs:590,637` | `grep -rn marked_comments crates` → 호출자 `narrative.rs:287`·`count_marked.rs:32` 뿐, **시험 0** |
| 3 | **D4 의 기계 대조군(`syn`)이 저장소에 안 남았다.** 손 표본이 나중에 드리프트해도 드러나는 자리가 없다 | 원의도 | 참 | 거짓신호 | D4 | `docs/gates/rust-extractor.md:196-201` | `grep -rln syn scripts/` → 무관 파일뿐 |
| 4 | **`[rust].what_was_already_known` 이 「문법 핀이 이미 정해져 있었다」를 안 적었다.** 그 절은 *"이미 본 것을 **전부** 적는다"* 라고 스스로 못 박았다 | 원의도 | 참 | 거짓신호 | B3 | `criteria.toml:11090-11115` vs `intent.md:86` | `a2ed4dd 07:21:26` < `c6e5155 07:30:16` |

### 요구되지 않은 것

| # | 발견 | 모집단 | 유효 | 해악도 | 좌표 | 근거 |
|---|---|---|---|---|---|---|
| 5 | `examples/count_marked.rs` — 의도가 요구한 적 없는 **새 예제 바이너리**가 크레이트에 영구히 남는 사실이 게이트·ADR 어디에도 안 적혔다 | 자기장치 | 참 | 미관 | `examples/count_marked.rs:1` | `xtask test` 출력에 `Running unittests examples/count_marked.rs` |

### 있는데 틀린 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 6 | ★ **`impl` 이 대상 타입 앞에 오면 같은 파일에 있어도 컨테이너가 안 붙는다.** 게이트는 원인을 *"대상 타입이 같은 파일에 없으면"* **하나**로 적었는데 실제로는 **선언 순서 의존**이라는 둘째 원인이 있다 | 원의도 | 참 | 거짓신호 | A9 · `[rust.pass]`①② | `rust.rs:150-153` | 격리 파일: `impl Foo{fn early}` / `struct Foo;` / `struct Bar;` / `impl Bar{fn late}` → `contains` 가 **`[{parent:2,child:3}]` 하나뿐** |
| 7 | **「1급은 넷」을 현재형으로 말하는 코드 자리 둘이 남았다** | 저장소 | 참 | 거짓신호 | H2 | `pal-core/src/ledger.rs:20,22` · `shell.rs:219` | `grep -rn "닫힌 넷\|추출 대상 넷" crates` |
| 8 | **`.RS`(대문자) 확장자가 이제 「언어 미인식」이 된다.** A8 이 `("rs","Rust")` 를 지우면서 대문자 경로가 떨어졌다 | 원의도 | 참 | 미관 | A8 | `recognize.rs:57,131` | 격리 클론에 `up.RS` → `unrecognized 45→46` |
| 9 | **손 표본 파일 자체에는 「자기 저장소 `56926aa`」가 없다** | 원의도 | 참 | 미관 | D2 | `rust-recall-sample.tsv:25` | tsv 는 `514c56dd` 뿐, `56926aa` 는 `criteria.toml:11141` 에만 |
| 10 | **「동결된 판정 문서」는 원리상 불가가 아니라 잔여다.** 같은 회차가 `2026-08-12-owner-direction.md` 에 「갱신 줄 하나」를 다는 선례를 만들었다 | 저장소 | 참 | 거짓신호 | 범위 밖 선언 | `F11-touch.md:316` · `F12.md:303` | 선례: `git diff 56926aa -- docs/instructions/2026-08-12…` |

## 자기 산출에 대한 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표 | 근거 |
|---|---|---|---|---|---|---|---|
| 11 | ★ **검증 스크립트의 열쇠가 중복을 삼킨다.** 추출기가 낸 것은 **268 행**인데 스크립트는 **266** 으로 보고한다. 삼켜진 둘은 하필 **R-16 이 사는 축**이라 오라클이 자기가 재야 할 축에 눈이 멀어 있다 | 자기장치 | 참 | 거짓신호 | D5 | `rust-recall-verify.py:88-93` | `machine rows 268 machine set 266` · dupe: `(command_prelude.rs,-,flag,function)`·`(parser/mod.rs,-,resolve,function)` |
| 12 | **게이트가 「퇴로 셋을 등록했고 하나도 안 썼다」라고 적었으나 세 절 뒤에서 퇴로 ①을 실행한다** | 회차기록 | 참 | 거짓신호 | 없음 | `rust-extractor.md:42-46` vs `:161-175` | 두 줄 대조 |
| 13 | **게이트 E 절의 `pal ledger` 블록이 실행 출력이 아니다.** 466/118/303 인데 TS 1 도 parsed 이므로 119 가 Rust 면 parsed 는 120 이상이어야 한다. `effect.md` 는 469/120/303, 지금 실행하면 472/120/306 — 셋이 다 다르다 | 회차기록 | 참 | 거짓신호 | E1 · G1 | `rust-extractor.md:120-123` | `./target/release/pal ledger` → 472/120/306 |
| 14 | **`state.md` 가 「사전부검 3/3 진행 중」에서 멈춰 있다.** 지금 교대하면 추출기·오라클·게이트·ADR·이슈 다섯이 전부 안 보인다 | 회차기록 | 참 | 거짓신호 | 없음 | `state.md:7-13,22` | mtime `06:41` — 착수 커밋 `07:21` 보다도 앞 |
| 15 | **의도가 범위 밖에 등록한 「CI 코퍼스 잡을 안 넣는다」가 게이트 §범위 밖에 없다** | 회차기록 | 참 | 미관 | 없음 | `intent.md:161-162` vs `rust-extractor.md:216-217` | 두 목록 대조 |

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효 | 해악도 | 좌표 | 왜 아니었나 |
|---|---|---|---|---|---|---|
| 1 | ADR-0027 이 없는 게이트 문서를 가리킨다 | 회차기록 | 거짓 | — | `0027…md:8` | 08:00 에는 없었으나 `e5d7081` 에 담겨 존재한다 |
| 2 | 게이트 판정 표가 42/0/1/1 인데 절별 합계는 43 이다 | 회차기록 | 거짓 | — | `rust-extractor.md:52-58`(옛 판) | `e5d7081` 이 모집단을 ⓐ44/ⓑ5 로 가르고 고쳤다 |
| 3 | 표식 91 중 88 이 붙었는데 결박은 46 뿐 — 접기가 죽은 가지다 | 자기장치 | 거짓 | — | `count_marked.rs:34` | 둘이 **다른 것을 센다.** `attaches_to_byte.is_some()` 은 `use_declaration` 에 붙어도 참이고 결박은 심볼 span 정확 일치일 때만 선다 |
| 4 | `ArgMatches::flag` 가 산출에 아예 없다 | 자기장치 | 거짓 | — | `rust-recall-verify.py:88` | 산출은 냈다. 최상위 `flag` 와 열쇠가 겹쳐 상쇄됐을 뿐(그 상쇄는 발견 11 로 따로 냄) |
| 5 | 게이트가 든 여섯 타입이 실은 같은 파일에 정의돼 있다 | 회차기록 | 거짓 | — | `rust-extractor.md:167-169` | 전수 확인: 여섯 다 정의 **0**. 게이트 진술은 참이다 |
| 6 | `capable.rs` 가 「추출기는 셋뿐」이라 적는데 다섯이다 | 저장소 | 거짓 | — | `capable.rs:11-13` | 「선언된 1급 다섯 · 만들어진 추출기 셋 · 나머지 둘」이 정확하다 |
| 7 | 손 표본 13 파일 선정이 재현되지 않는다 | 원의도 | 거짓 | — | `rust-recall-sample.tsv:42-46` | `awk 'NR%31==1'` 이 13 파일을 한 자 안 틀리고 재현했다 |
| 8 | `cargo xtask check` 가 rc=0 이니 통과다(내 첫 실행) | 자기장치 | 거짓 | — | — | `timeout` 미설치가 rc=0 을 냈다. **rc 를 판정으로 읽을 뻔했다.** 재실행해 20/20 을 봤다 |

## 끝내도 되는가

**안 된다** — 본 목록에 금지역 하나가 남았다: **발견 1**(`report.md` 가 없는데 게이트가
H5 를 통과로 계상 → 「사실이 아닌 것을 사실로 적음」). 여기에 H5 가 **반증**,
B3·D4 가 **대조 불가**, H6 이 **미측정**이다.

그 밖에 판정에 실려야 할 실질 둘 — **발견 6**(`impl` 선언 순서 의존)과
**발견 2**(새 두 경로에 단위시험 0)는 고치지 않더라도 **게이트에 적히기만 하면**
되는 성질이다. 발견 11~15 는 자기 산출이므로 메인이 처분할 몫이다.
