# 완수 조건 설계 평가 1 라운드 — 원 반환문

> `조건평가` 가 냈다. 2026-09-09 · 회차 `2026-09-08-cross-file-references`
> 조건 24 개(`A1 A2 A3 A4 A5 B1 B2 B3 B4 C1 C2 C3 D1 D2 D3 D4 D5 E1 E1-a E2 E2-a E3 E4 E5`).

## 무엇을 받고 무엇을 안 받았나 — 먼저 적는다

**안 받은 것**: 조건을 쓴 자의 설명 · 대화 기록 · 사전부검/독립리뷰의 판정 · 「이 조건은 이런 뜻이다」라는 해설.

**받으면 안 되는데 받은 것 셋. 그대로 적고 판단에서 뺐다.**

1. 발췌본 `audit-in.md` 의 `## 해소 깊이`·`## 모집단`·`## 범위 밖` 이 **정반합 판 1 의 결과를 그대로 담고 있다** — *"판 1 이 그것을 전환으로 판정해 올렸다"* · *"판 1 이 그 낱말이 … 실패급으로 잡았다"* · *"정(正)이 냈고 1 라운드 반이 못 깼고"* · *"그 배제 근거는 판 1 이 깼다"*. 어느 대목이 이미 잡혔는지를 알려 주는 사전 정보다.
2. 지시가 **항 형식을 `premortem/r1-raw.md` 에 맞추라**고 해서 그 파일을 열었고, 그러면서 **사전부검 1 라운드 원 반환문 전문을 읽었다.** 이 라운드는 사전부검과 독립이 아니다. 아래 발견 중 사전부검이 이미 낸 것과 겹치는 것이 있으면 그것은 독립 검출이 아니다.
3. 발췌본에 `## 목적 기여`·`## 상한`·`## 범위 밖` 이 **두 번씩** 실렸고 `## 차선책` 의 둘째 판은 *"(같이 쓴다)"* 뿐이다.

**받아야 하는데 못 받은 것 둘.** `intent.md` 는 `## 금지역`(44–48 행)과 `## 멈추고 올리는 자리`(49–58 행)를 갖는데, 발췌본은 `## 금지역` 을 **제목만 남기고 본문 0 줄로** 실었고 `## 멈추고 올리는 자리` 를 **통째로 뺐다.** 지시가 `intent.md` 를 열지 말라고 했으므로 **열지 않았다.** 따라서 **조건이 이 회차의 금지역 목록과 멈춤 자리를 덮는가는 이 라운드가 안 쟀다** — 아래 「조건의 합이 의도를 덮나」는 그 둘을 뺀 판정이다.

---

## 조건 갈래 분류

| 조건 | 갈래 | 지금 적힌 형태 | 같나 |
|---|---|---|---|
| A1 | 결정론적 | 단위 시험 | 같다 |
| A2 | 결정론적 | 단위 시험 | 같다 |
| A3 | 결정론적 | 단위 시험 (형태 넷) | 같다 |
| A4 | 결정론적 | 픽스처 시험 | 같다 |
| A5 | 결정론적 | 시험 · 음성 대조 | 같다 |
| B1 | 결정론적 | `cargo xtask check` | 같다 — 그러나 오늘도 초록 |
| B2 | 결정론적 | 값 검사 + 반증 경로 명시 | 같다 |
| B3 | 결정론적 | **서술형** (「값을 찍는다」) | 다르다 |
| B4 | 결정론적 | 스키마 등록 검사 | 같다 |
| C1 | 결정론적 | `pal doctor` 모집단 | 같다 |
| C2 | 결정론적 | `checked > 0` · CI 게이트 | 같다 — 그러나 오늘도 참 |
| C3 | 결정론적 + 의미론 혼합 | **서술형** (「거짓으로 적지 않는다」) | 다르다 |
| D1 | 결정론적 | 총계 대조 | 같다 — 신원 기준선 없음 |
| D2 | 결정론적 | 축 다섯 대조 | 같다 — 넷이 모집단 0 |
| D3 | 결정론적 | 스크립트 검사 | 같다 — 근거 문면이 거짓 |
| D4 | 결정론적 | 서술형 + **탈출구** | 다르다 |
| D5 | 결정론적 | 시험 | 같다 |
| E1 | 결정론적 | 수치 하한 18/26 | 같다 — 모집단 미열거 |
| E1-a | 음성 대조 | **항등식** | 다르다 |
| E2 | 결정론적 | 수치 하한 800 | 같다 — 세는 절차 미등록 |
| E2-a | 음성 대조 | **항등식** | 다르다 |
| E3 | 의미론적 | **합격선 없는 측정** | 다르다 — 조건이 아니다 |
| E4 | 결정론적 | 서술형 (수단은 지정됨) | 대체로 같다 |
| E5 | 결정론적 | **어느 결과든 통과** | 다르다 |

**통과 15 · 걸림 9** — 걸림은 `B1 B3 C2 C3 D2 D3 D4 E1 E1-a E2 E2-a E3 E5` 와 절 밖의 합 걸림 넷이다(항 수는 아래가 진다).

---

## 발견

### `E3` 에 합격선이 없다 — 「잰다」는 판정이 아니고, 파일 간 엣지 800 건이 전부 거짓이어도 조건 24 개가 다 초록이다
- 어떻게 실패하나: `E3` 의 문면은 *"거짓 엣지율을 표본으로 잰다 — 30 건을 뽑아 손으로 대조하고 거짓 엣지 비율을 **산출한다**"* 다. **산출하면 참이다.** 30 건 중 30 건이 거짓이어도 「율을 산출했다」는 충족되고 조건은 통과로 켜진다. 그리고 양(量)을 지는 `E2` 는 *"파일 간 엣지가 800 건 이상"* 이라 **거짓 엣지 800 건으로 채워진다.** `A1`~`A4` 는 손으로 심은 픽스처 위에서만 정확도를 재므로 자기 저장소 산출의 정확도를 지는 조건은 `E3` 하나뿐이고, 그 하나가 판정을 안 낸다. 원문이 증인으로 잠근 *"자기 저장소 + **정확도 표본**"* 중 뒤의 절반이 판정력 0 이다.
- 어디가 걸리나: 잠긴 의도 `## 완수 조건` `E3` · 같은 절 `E2` · `## 원문` 의 *"무엇이 도달을 증언하나? → 자기 저장소 + 정확도 표본"*
- 획득: 조회 — `E3` 문면 직독. 동사가 「산출한다」이고 비교 연산자·임계값이 문장에 없다. `E2`·`E1` 은 각각 `800`·`18` 을 적었으므로 하한을 적는 형식을 이 절이 알고 있는데도 `E3` 만 안 적었다
- 모집단: 원의도
- 유효성: 참
- 해악도: 금지역 (측정이 죽은 가지 — 검사가 있는데 통과가 거짓이 된다)
- 대상: 계획자신
- 얼마나 아픈가: 지금이면 한 낱말이다(하한을 적는 것). 구현 뒤에 알면 표본 30 을 다시 뽑아야 하고, `E3` 이 *"표본 크기 30 을 측정 전에 등록한다"* 를 스스로 요구하므로 **사후에 임계값을 붙이는 것이 그 등록 규칙과 충돌한다**

### `E1` 의 모집단 「26 이름」이 어디에도 열거돼 있지 않다 — 착수 관측 파일은 사용법 오류 출력이다
- 어떻게 실패하나: `E1` 은 *"오늘 `pal query symbol.callers <이름>` 이 `outcome: symbols · symbols: []` 를 돌려주는 **26 이름** 중 18 개 이상"* 을 요구하는데, **그 26 이 무엇인지가 회차 어디에도 적혀 있지 않다.** 착수 관측 `observations/callers-before.txt` 는 인자 없이 돌린 결과라 *"질의 `symbol.callers` 에 인자가 필요하다"* 라는 **오류 문장 한 줄뿐**이다. 그러면 종료 시점에 26 을 **다시 고를 수 있고**, 값이 도는 이름 18 개를 골라 담으면 조건이 100% 통과한다. 잠근 의도가 *"정(正)이 쓴 증인 `symbol.callers RepoPath ≥ 1` 은 폐기했다 — 오늘 이미 4 를 돌려준다"* 라고 적은 것과 **같은 병이 모집단 미열거로 되돌아온다.**
- 어디가 걸리나: `.palimpsest/rounds/2026-09-08-cross-file-references/observations/callers-before.txt` · 잠긴 의도 `E1` · `## 잔여를 여기서 미리 선언한다` 의 「비-0 증인」 절
- 획득: 조회 — ① `callers-before.txt` 전문(3 줄, 전부 오류) ② `grep -rn "26 이름|이름 26" .palimpsest/rounds/2026-09-08-cross-file-references/` → 잠긴 의도의 조건 문장 두 곳과 개정 표 한 곳뿐이고 **이름 목록이 실린 행이 없다**
- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역 (측정이 죽은 가지 — 통과가 사후 선택으로 만들어진다)
- 대상: 계획자신
- 얼마나 아픈가: 지금이면 명령 한 번이다(26 이름을 관측 파일에 박는다). 착수 커밋 `6b6cb6d` 이 지나갔으므로 나중에 뽑으면 「오늘」의 시점이 이미 달라져 있다

### `C2` 의 음성 대조가 오늘 이미 참이다 — `checked > 0` 은 결박 30 건이 이미 채우고 `REFERENCES` 와 무관하다
- 어떻게 실패하나: `C2` 는 *"그 모집단이 0 이 아니다 — `checked > 0` 이고, 0 이면 CI 게이트가 실패로 판정한다 (음성 대조)"* 다. 그런데 착수 관측의 불변식 ① 은 **오늘 이미 `검사 30 · 위반 0`** 이다 — `BOUND_TO` 30 건이 그 30 이다. 즉 `C1`(`REFERENCES` 를 ① 의 모집단에 넣기)이 **완전히 실패해도** `C2` 는 참이다. 음성 대조가 자기가 지키려는 조건과 안 이어져 있다. 게다가 CI 게이트는 이미 그 검사를 하고 있어(`scripts/check-round-doctor.mjs` 가 불변식마다 `checked.checked <= 0` 이면 throw) **이 회차가 아무것도 안 해도 초록**이다.
- 어디가 걸리나: `.palimpsest/rounds/2026-09-08-cross-file-references/observations/doctor-before.txt`(불변식 1 — `검사 30 · 표본 밖 0 · 위반 0`) · `scripts/check-round-doctor.mjs:25-29` · `.github/workflows/ci.yml:135-137` · 잠긴 의도 `C2`
- 획득: 조회 — ① `doctor-before.txt` 직독: ① 이 `검사 30`, 「담지 못하는 자리」에 `REFERENCES(F07)` ② `check-round-doctor.mjs` 직독: `if (!Number.isInteger(checked.checked) || checked.checked <= 0 || …) throw` ③ `ci.yml:137` `cargo run … doctor --full --json | node scripts/check-round-doctor.mjs`
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역 (측정이 죽은 가지 — 음성 대조라 이름 붙인 것이 아무것도 안 잰다)
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 공짜. 걸리는 곳은 조건 문장 하나

### 산출된 엣지가 정말 `exact` 인지 재는 조건이 하나도 없고, 그 자리를 「잔여」 선언이 대신 메웠다
- 어떻게 실패하나: 원문이 인터뷰에서 잠근 일곱 중 하나가 *"엣지에 해소 등급을 싣나? → **exact 만**"* 이다. 그런데 조건 24 개 중 등급을 다루는 것은 `C3` 하나이고, `C3` 은 **`schema/graph.toml` 의 표기**가 거짓을 산출하지 않는 것만 잰다 — **산출된 엣지 하나하나가 후보 유일(=exact)이었나는 아무도 안 잰다.** 그리고 그 유일성을 위협하는 실체가 이미 잠긴 의도 안에 적혀 있는데(*"㉮ 의 43 이름 중 7 개(199 자리)가 오늘 이름만으로는 모호하다"*), 그것이 조건이 아니라 **`## 잔여를 여기서 미리 선언한다`** 로 들어가 *"구현이 있어야 잰다"* 로 닫혔다. 남는 방어는 `E3` 하나인데 위 항대로 `E3` 에는 합격선이 없다. **둘이 겹쳐, 모호한 199 자리에서 임의로 고른 엣지가 「exact 만 실었다」로 보고된다.**
- 어디가 걸리나: 잠긴 의도 `## 원문`(*"exact 만"*) · `## 완수 조건` `C3` · `## 잔여를 여기서 미리 선언한다` 넷째 항 · `schema/graph.toml` `[edge.REFERENCES] grade = "scoped"` · `crates/pal-core/src/projection.rs:79-95`(`RefCounts::ambiguous` — 파일 **안**의 모호성만 가른다)
- 획득: 조회 — ① 조건 24 개에 `exact`·`후보`·`유일`·`ambiguous` 가 나오는 자리를 세었다: `C3` 의 *"등급을 거짓으로 적지 않는다"* 한 곳뿐 ② `schema/graph.toml` `[edge.REFERENCES]` 주석이 *"후보가 둘 이상이면 엣지를 안 만든다 — `RefResolution::Ambiguous` 가 그 자리다"* 라고 파일 **안**의 규칙을 적는데, 파일 **간**에 같은 규칙이 서는가를 요구하는 조건이 없다
- 모집단: 원의도
- 유효성: 참
- 해악도: 금지역 (사실이 아닌 것을 사실로 — 모르는 것을 `exact` 라 적는다)
- 대상: 계획자신
- 얼마나 아픈가: 지금이면 조건 한 줄이다. 구현 뒤면 199 자리를 손으로 되짚어야 하고, 그것을 `E3` 의 표본 30 이 통계적으로 못 잡는다(199/1611 ≈ 12% 이므로 30 표본의 기대 적중이 3.7 건이고 0 건일 확률이 2% 를 넘는다)

### `C3` 의 통과 경로 「등급 칸을 신설하든」이 `## 범위 밖` 이 밖으로 밀어낸 등급 축을 안으로 끌어온다
- 어떻게 실패하나: `## 범위 밖` 이 *"**`candidate` 등급 엣지** — exact 만 싣는다. **등급 축은 `#133` 과 겹친다**"* 라고 등급 축을 이 회차 밖으로 선언했다. 그런데 `C3` 은 통과 경로로 *"**등급 칸을 신설**하든 라벨을 가르든"* 을 연다. 앞의 갈래를 고르면 이 회차가 **밖이라 선언한 축을 짓게 되고**, 그것은 `#133` 과 소유권이 겹친다. 그리고 등급을 `per_edge` 로 바꾸면 `pal doctor` 불변식 ⑤ 의 모집단에 `REFERENCES` 가 새로 들어와 **`D2` 가 지키는 다섯 축 중 하나가 움직인다** — 즉 `C3` 의 한 갈래는 `D2` 를 자동으로 반증하고 「멈추고 올리는 자리」를 발화시킨다. 두 조건이 서로를 배제하는 통과 경로를 갖는다.
- 어디가 걸리나: 잠긴 의도 `C3` · `## 범위 밖` 셋째 항 · `D2` · `crates/pal-core/src/doctor.rs:391-397`(⑤ 모집단이 `GradeRule::PerEdge` 를 담는다) · `schema/graph.toml` `[edge.REFERENCES]`
- 획득: 조회 — `schema.rs` 의 등급 파싱과 `doctor.rs` 의 ⑤ 모집단 결정 직독 · `doctor-before.txt` 의 ⑤ 가 오늘 `(모집단이 없습니다)`
- 모집단: 원의도
- 유효성: 참
- 해악도: 실패 (한 갈래를 고르면 `D2` 가 반증되고 회차가 멈춘다)
- 대상: 계획자신
- 얼마나 아픈가: 지금이면 갈래를 하나로 좁히는 문장 하나. 구현 도중에 알면 `EXTRACTOR_REV` 나 스키마 판이 이미 움직인 뒤다

### `C3` 은 시작 시점에 이미 초록이다 — 「재생성이 양방향 검사를 통과한다」는 오늘 통과한다
- 어떻게 실패하나: `C3` 의 기계로 잴 수 있는 부분은 *"`docs/graph-schema.md` 재생성이 양방향 검사를 통과한다"* 뿐인데, 그 검사는 **오늘 통과한다**(CI 가 초록이고 워킹트리가 clean 이다). 즉 이 조건은 **RED 가 관측되지 않는다.** 정작 조건이 지려던 것 — *"파일 간 엣지의 등급을 **거짓으로 적지 않는다**"* — 는 서술형이고 이를 빨갛게 만들 방법이 조건에 없다. 아무것도 안 만들어도 초록, 거짓 등급으로 만들어도(양방향 검사는 `graph.toml` ↔ `graph-schema.md` 의 일치만 본다) 초록이다.
- 어디가 걸리나: 잠긴 의도 `C3` · `.github/workflows/ci.yml` · `xtask/src/main.rs`(스키마 ↔ 문서 대조)
- 획득: 조회 — 워킹트리 clean(`git status`), 착수 관측 `red.md` 가 *"빌드: `cargo build --release` 초록"* 을 적고 스키마 검사의 빨강을 안 적었다. `red.md` 의 RED 목록 여섯(①~⑥) 어디에도 `C3` 에 대응하는 빨강이 없다
- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 (조건이 켜져도 등급이 참이 된 것은 아니다)
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 공짜

### `B1` 도 RED 가 없다 — 「스키마 정합이 통과한다」는 `UnresolvedRef` 를 하나도 안 세우고도 참이고, `status = "not_built"` 를 뒤집는 것이 조건에 없다
- 어떻게 실패하나: `B1` 은 *"`UnresolvedRef` 가 스키마의 사전 등록된 네 칸 그대로 서고 `cargo xtask check` 의 스키마 정합이 **통과한다**"* 다. 그 검사는 **오늘 통과한다.** 더 나쁜 것은 방향이다 — `schema/graph.toml` 이 `[node.UnresolvedRef] status = "not_built"` 이고 그 절의 주석이 *"`status = "not_built"` 인 노드의 Rust 타입은 **거주 불가**여야 한다(검사가 잰다)"* 라고 적으며, `xtask/src/main.rs:1269-1271` 이 실제로 그것을 강제한다(`if !span.uninhabited { … "`{label}` 은 `not_built`({by}) 인데 `{rust_type}` 에 값을 만들 수 있다" }`). 즉 **`UnresolvedRef` 에 값을 실제로 만들면 `cargo xtask check` 가 빨개진다.** 통과시키려면 `status` 를 뒤집어야 하는데 `B1` 은 그 전환을 안 적었다. 그러면 통과 경로가 둘이 된다 — ㉠ 제대로 세우고 `status` 를 뒤집는다, ㉡ **아무것도 안 세운다**(오늘 그대로 초록).
- 어디가 걸리나: `schema/graph.toml:193-205`(`[node.UnresolvedRef]` · `status = "not_built"` · `built_by = "F08"`) · `xtask/src/main.rs:1269-1271` · 잠긴 의도 `B1`
- 획득: 조회 — ① `schema/graph.toml` 의 `[node.UnresolvedRef]` 절 직독: `key = ["site","name"]` + `attrs` 둘(`reason`·`attempts`)로 「네 칸」은 맞다 ② `xtask/src/main.rs` 의 `uninhabited` 검사 직독 ③ `crates/pal-cli/src/doctor.rs:323` `.absent("UnresolvedRef", CapabilityId::new("F08", "unresolved-refs"))`
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호 (통과 경로 ㉡ 가 열려 있고, 그 경로에서 조건이 켜진다)
- 대상: 계획자신
- 얼마나 아픈가: 지금이면 조건 문장. 구현 뒤면 `status` 전환이 `docs/graph-schema.md` 재생성·`F08` 게이트 문서와 함께 움직인다

### `E1-a`·`E2-a` 는 음성 대조가 아니라 항등식이다 — 이미 적힌 하한을 다시 읽는 조건이다
- 어떻게 실패하나: 둘 다 형태가 *"그 하한을 0 으로 두면 오늘이 만점 통과한다. **하한이 0 이면 이 조건은 실패다**"* 다. 그런데 하한은 `E1`·`E2` 에 **18 과 800 으로 이미 문서에 박혀 있다.** 「하한이 0 인가」는 문서를 읽으면 항상 「아니다」이므로 이 조건은 **원리상 위반될 수 없다.** 음성 대조가 지려던 것은 *"검사가 고장이면 드러난다"* 인데, 이 형태는 검사가 고장나도(예: `symbol.callers` 가 아무 이름에나 값을 돌려주게 되어도) 발화하지 않는다. 진짜 음성 대조는 「착수 커밋의 이진으로 이 조건을 돌리면 빨간가」를 재는 것이고, 그 실행이 조건에 없다.
- 어디가 걸리나: 잠긴 의도 `E1-a` · `E2-a` · `## 잔여를 여기서 미리 선언한다` 의 「비-0 증인」 절 마지막 문단
- 획득: 조회 — 세 조건(`E1`·`E1-a`·`E2`·`E2-a`) 문면 직독. `E1-a`·`E2-a` 의 술어가 「하한이 0 인가」이고 그 값을 정하는 것은 측정이 아니라 같은 문서의 다른 줄이다
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호 (음성 대조가 있다고 사람이 믿게 된다)
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 공짜

### `E5` 는 어느 결과가 나와도 통과한다 — 「유지하든 뒤집든 근거를 적는다」
- 어떻게 실패하나: `E5` 의 앞절은 판정이다(*"`.absent("REFERENCES", F07)` 선언이 **오늘의 사실과 맞는다**"*). 뒷절이 그것을 지운다 — *"**유지하든 뒤집든** 그 판정의 근거를 게이트에 적는다."* 근거를 적기만 하면 참이므로 **선언이 사실과 맞는가는 안 재게 된다.** 그런데 이것은 결정론적으로 잴 수 있다: `crates/pal-cli/src/doctor.rs:316` 의 선언과 `pal doctor --json` 이 산출하는 `REFERENCES` 모집단의 유무가 일치하는지는 기계가 한 줄로 잰다. 결정론적으로 잴 수 있는데 서술형으로 적혔고, 그 서술형이 양쪽 결과를 다 통과시킨다.
- 어디가 걸리나: `crates/pal-cli/src/doctor.rs:315-316`(`.absent("File", F07)` · `.absent("REFERENCES", F07)`) · 잠긴 의도 `E5`
- 획득: 조회 — `grep -rn 'absent(' crates/` 로 선언 자리 확인(`doctor.rs:279-358` 에 14 건) · `doctor-before.txt` 의 ① ② 「담지 못하는 자리」에 `REFERENCES(F07)` 가 실제로 찍힌다
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 공짜

### `D2` 가 지키는 「다섯」 중 넷이 오늘 모집단 0 이다 — 감시력이 실제로는 축 하나다
- 어떻게 실패하나: `D2` 는 *"`pal doctor` 의 판정 축 여덟 중 ①·②·③ 을 뺀 **다섯**의 결과가 안 바뀐다"* 인데, 착수 관측에서 그 다섯(④⑤⑥⑦⑧)이 **전부 `(모집단이 없습니다)`** 다. 「모집단 없음 → 모집단 없음」은 자동으로 참이다. 그중 이 회차의 작업으로 실제로 움직일 수 있는 것은 ⑤ 하나뿐이고(등급을 `per_edge` 로 바꾸면 `REFERENCES` 가 들어온다), 나머지 넷의 모집단은 `NarrativeItem(F12)`·`Residual(F15)`·`BindingIndex(F09)`·`DerivedNode(F17)` 라 이 회차가 원리상 안 건드린다. 「다섯을 지킨다」가 실제로는 「하나를 지킨다」다.
- 어디가 걸리나: `.palimpsest/rounds/2026-09-08-cross-file-references/observations/doctor-before.txt`(④~⑧ 전부 `(모집단이 없습니다)`) · 잠긴 의도 `D2`
- 획득: 조회 — `doctor-before.txt` 직독. ④ `NarrativeItem(F12)` · ⑤ `INTRODUCED_BY(F05)` · ⑥ `Residual(F15) · ScopeReduction(F20)` · ⑦ `BindingIndex(F09)` · ⑧ `DerivedNode(F17)`
- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호 (숫자 「다섯」이 감시 범위를 부풀린다)
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 공짜

### `D3` 이 근거로 적은 사실이 저장소와 반대다 — `f04-verify.py` 는 치환 대상이 없으면 **조용히 꺼지지 않고 FAIL 한다**
- 어떻게 실패하나: `D3` 은 *"오늘 그 스크립트는 리터럴을 찾아 바꾸므로 REV 를 올리면 음성 대조 하나가 **조용히 꺼진다**"* 를 근거로 적는다. 스크립트를 읽으면 정확히 그 경우를 막는 코드와 그 이유를 적은 주석이 **이미 있다**: `if 찾을 not in text: … 어긋남(f"③ {이름} — 치환 대상이 소스에 없다: …"); continue` 이고 주석이 *"**치환 대상이 없으면 멈춘다.** 리팩터가 이 자리를 지워도 대조가 조용히 계속되면 그 대조는 장식이다"* 다. `어긋남` 은 `실패` 목록에 넣고 종료 코드를 비-0 으로 만든다. 조건이 하려는 일(REV 를 올린 뒤 스크립트를 고친다)은 여전히 필요하지만, **근거 문장이 저장소의 사실과 반대**라 원장에 거짓이 들어간다.
- 어디가 걸리나: `scripts/f04-verify.py:235-238`(치환 대상 부재 시 `어긋남`) · `scripts/f04-verify.py:83-85`(`어긋남` → `실패.append`) · `scripts/f04-verify.py:188-189`(리터럴 `"f02-rust-scope"`) · `crates/pal-extract/src/lib.rs:147`(`EXTRACTOR_REV = "f02-rust-scope"`) · 잠긴 의도 `D3`
- 획득: 조회 — 세 좌표 직독 · 코퍼스 실재 확인(`~/dev/projects/ditto` · `~/dev/projects/boxwood/portal-backend` 둘 다 있다 → 이 조건의 모집단은 선다)
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호 (사실이 아닌 것을 근거로 적었다 — 조건 자체는 유효하다)
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 공짜. 문장 한 줄

### `E2` 의 「파일 간 엣지」를 세는 절차가 등록돼 있지 않다 — 착수 관측이 쓴 명령은 파일 간 몫을 안 가른다
- 어떻게 실패하나: `E2` 는 *"파일 간 엣지가 800 건 이상"* 인데, 착수 관측이 기준선으로 쓴 명령은 `pal export --format cypher | grep -c REFERENCES` 이고 그것은 **`REFERENCES` 전량 4259 를 센다**(관측 파일에 `4259` 만 있다). `pal export` 의 Cypher 산출은 엣지 줄에 `MATCH (a {id:…}), (b {id:…}) CREATE (a)-[:REFERENCES]->(b)` 만 찍고 **파일 간인지 안인지를 안 싣는다** — 파일 몫을 가르려면 심볼 노드 줄의 `path` 와 조인해야 하고, 그 조인 스크립트가 어디에도 없다. 그러면 종료 시점에 「800」을 세는 방법이 사람마다 갈리고, 가장 쉬운 길(`grep -c REFERENCES`)은 **오늘 이미 4259 를 돌려주므로 만점 통과한다.**
- 어디가 걸리나: `.palimpsest/rounds/2026-09-08-cross-file-references/observations/references-count.txt`(`4259`) · `crates/pal-cli/src/export.rs:212-220` · 잠긴 의도 `E2`
- 획득: 조회 — ① `references-count.txt` 는 `4259` 다섯 바이트뿐 ② `export.rs:212-220` 직독: 엣지 줄에 실리는 것은 `from`·`to` 의 id 둘뿐이고 파일 표지가 없다. 노드 줄에는 `path` 가 있으므로 **조인은 원리상 가능하다** — 없는 것은 그 절차의 등록이다
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호 (같은 조건이 두 수를 낸다)
- 대상: 계획자신
- 얼마나 아픈가: 지금이면 명령 문자열 한 줄을 조건에 박으면 된다

### `E2` 의 하한 800 이 `## 모집단` 표의 1159 와 안 이어진다 — 그 수가 무슨 회수율을 뜻하는지 어디에도 없다
- 어떻게 실패하나: `## 모집단` 이 잠근 값은 「엣지 **상한** 1159」이고, 같은 절이 *"⚠ **엣지 수는 상한이다.** 별칭 9 건·중첩 목록·동명 임포트가 깎는다"* 와 *"1159~1286 사이에서 움직인다"* 를 함께 적는다. `E2` 의 800 은 그 값의 69% 인데 **왜 69% 인가가 조건에도 모집단 절에도 없다.** 그러면 종료 시 800 을 넘겨도 *"상한의 몇 %를 회수했나"* 를 말할 수 없고, 반대로 799 에서 멈춰도 그것이 「모자란 구현」인지 「깎이는 것이 예상보다 많았다」인지 안 갈린다. 하한과 분모가 같은 절에서 안 이어져 있다.
- 어디가 걸리나: 잠긴 의도 `## 모집단` 표(`엣지(상한) 1159`) · `E2`
- 획득: 조회 — 두 절 직독. `E2` 문면에 `1159`·`%`·`회수` 가 없다
- 모집단: 원의도
- 유효성: 참
- 해악도: 미관
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 공짜

### `D1` 의 기준선이 총계뿐이다 — 어느 결박 넷이 `stale` 이었는지가 착수 관측에 없어 신원 교체를 못 잡는다
- 어떻게 실패하나: `D1` 은 *"결박 30 건의 낡음이 안 움직인다 — 착수 관측이 `fresh` 26 · `stale` 4 였고 종료 시 같다"* 인데, 착수 관측 `binding-before.txt` 는 **21 줄이고 30 건 중 1 건의 상세만 담았다.** `red.md` 도 *"`결박 30건`. 첫 행이 `최신 상태(fresh)`"* 까지만 적는다. 즉 회차 기록에 남은 것은 **총계뿐**이라 종료 시 `fresh` 하나가 `stale` 이 되고 다른 `stale` 하나가 `fresh` 가 되는 **신원 교체를 원리상 못 잡는다** — 26·4 는 그대로다. 이 조건은 원문이 잠근 멈춤 자리(*"결박·판정이 움직이면"*)의 절반을 지므로, 못 잡는 형태가 있는 것이 곧 멈춤 잠금의 구멍이다.
- 어디가 걸리나: `.palimpsest/rounds/2026-09-08-cross-file-references/observations/binding-before.txt`(21 줄) · `observations/red.md` ③ · 잠긴 의도 `D1`
- 획득: 조회 — `wc -l binding-before.txt` → 21 · `grep -c "최신 상태(fresh)"` → **1** · `grep -c "stale"` → **0**. 26·4 라는 수는 이 파일에 없다
- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 지금이면 `pal query binding.status --json > observations/…` 한 번. 착수 커밋이 지나갔지만 워킹트리가 clean 이라 아직 같은 값이 나온다

### `B3` 이 결정론적으로 잴 수 있는데 서술형이고, 「값」의 하한이 없어 한 건이면 참이다
- 어떻게 실패하나: `B3` 은 *"`pal touch` 의 「내가 모르는 것」 줄이 **능력 부재 대신 값**을 찍는다"* 다. 이것은 출력 스냅샷 시험으로 빨강·초록이 나온다 — 오늘의 빨강도 이미 관측돼 있다(`touch-before.txt` 의 `(이 빌드에는 unresolved-refs 능력이 없습니다 — F08 미구축)`). 그런데 조건은 시험을 안 요구하고, **몇 건 이상인지도 안 적었다.** `UnresolvedRef` 를 한 건만 산출해도 「능력 부재 대신 값」이 되어 통과한다. `A1`~`A5` 가 전부 「시험이 단언한다」로 적힌 것과 대비된다 — 같은 절 안에서 형식이 갈렸다.
- 어디가 걸리나: `.palimpsest/rounds/2026-09-08-cross-file-references/observations/touch-before.txt`(「내가 모르는 것」 줄) · `crates/pal-core/src/touch.rs:241, 290` · 잠긴 의도 `B3`
- 획득: 조회 — `touch-before.txt` 직독 · `A1`~`A5` 와 `B3` 의 술어 비교(*"시험이 단언한다"* ×4 vs *"찍는다"* ×1)
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 공짜

### `D4` 에 탈출구가 붙어 있다 — 「원리상 못 잰다면 그 사유를 적는다」가 조건을 서술형으로 되돌린다
- 어떻게 실패하나: `D4` 는 *"`pal export` 의 read-only 경로가 옛 행을 안 읽는다 — 형식 판 불일치를 낡음으로 산출하거나, **원리상 못 잰다면 그 사유를 적는다**"* 다. 뒷절이 있으면 사유를 적는 것으로 통과하므로 **판정이 사람 손에 돌아간다.** 그리고 이 자리는 결정론적으로 잴 수 있다 — `crates/pal-cli/src/export.rs:118` `Projection::open_read_only(&index)` 이고, `RefCounts` 에 칸을 더하면 postcard 가 자리 기반이라 옛 색인이 디코드에서 죽는다는 사실이 이미 코드 주석에 적혀 있다(`projection.rs:81-91`, 구제 절차까지 적혀 있다). 옛 색인을 만들어 두고 새 이진으로 여는 시험이 곧 이 조건이다.
- 어디가 걸리나: `crates/pal-cli/src/export.rs:118` · `crates/pal-core/src/projection.rs:79-95`(`ambiguous` 칸의 판 불일치 주석 + 구제 절차) · 잠긴 의도 `D4` · `## 차선책` 셋째 항
- 획득: 조회 — 두 좌표 직독. `projection.rs` 주석이 *"옛 색인을 새 바이너리로 읽으면 디코드에서 죽는다"* 를 이미 확정 사실로 적는다 → 「원리상 못 잰다」가 성립하기 어렵다
- 모집단: 저장소
- 유효성: 참
- 해악도: 미관
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 공짜

### `A3` 은 손으로 심은 픽스처에서만 형태 넷을 재고, 자기 저장소에서 그 넷 각각이 비-0 인지는 어느 조건도 안 잰다
- 어떻게 실패하나: `A3` 은 *"잠근 축1 의 임포트 형태 **넷 각각**에 대해 엣지가 서는 것을 **시험이** 단언한다"* 인데, 시험은 픽스처 위에서 돈다. 자기 저장소 산출을 지는 것은 `E2` 하나이고 그것은 **총계 800** 이다. 그러면 형태 하나(예: 「형제 크레이트 평평 경로 중 대상이 루트 `lib.rs` 의 최상위 `pub` 인 것」)가 자기 저장소에서 **0 건을 내도** 다른 형태가 800 을 채우면 전부 통과한다. `## 모집단` 표가 총계 1611/1159 만 잠그고 **형태별 분해를 안 잠근 것**이 그 구멍의 짝이다 — 형태별 값이 없으면 종료 시 어느 형태가 안 섰는지 아무도 못 센다.
- 어디가 걸리나: 잠긴 의도 `A3` · `E2` · `## 모집단` 표 · `## 해소 깊이` 축1 표(안 칸 넷)
- 획득: 조회 — `## 모집단` 표는 열이 「참조 자리·엣지(상한)·파일·기여 크레이트」 넷이고 **축1 의 형태별 행이 없다.** `A3` 은 시험을, `E2` 는 총계를 요구하고 그 사이를 잇는 조건이 없다
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 지금이면 형태별 하한 넷을 적으면 된다

### 원문이 잠근 「`ImportSet` 에 이름」이라는 자리를 재는 조건이 하나도 없다 — 결과만 재고 잠근 자리를 안 잰다
- 어떻게 실패하나: 인터뷰가 잠근 일곱 중 하나가 *"임포트가 항목 이름을 실게 하는 자리를 어디로 하나? → **`ImportSet` 에 이름**"* 이다. 조건 24 개에서 `ImportSet` 이 나오는 것은 `D5` 하나이고, 그것은 **TypeScript 쪽 부재 표현의 부작용**을 막는 조건이지 「Rust 임포트의 항목 이름이 `ImportSet` 에 실렸나」를 재지 않는다. `A1`~`A4` 는 결과(엣지)만 잰다. 그러면 구현이 항목 이름을 `ImportSet` 이 아닌 다른 자리(예: 해소 시점에 소스 바이트를 다시 읽기)에 두어도 **조건은 전부 초록이고 잠근 자리가 조용히 버려진 것을 원장이 못 본다.** 이 자리는 결정론적으로 잴 수 있다 — `pal symbols <파일> --graph --json` 의 `imports` 를 단언하면 된다.
- 어디가 걸리나: 잠긴 의도 `## 원문` 넷째 잠금 · `## 완수 조건` 전체(`ImportSet` 이 `D5` 에만 나온다) · `crates/pal-core/src/file_graph.rs:129-137`(`ImportSet.modules`) · `crates/pal-extract/src/rust.rs:378-386`(정렬·중복 제거)
- 획득: 조회 — 조건 24 개에서 `ImportSet` 문자열을 세었다: `D5` 한 곳 · `crates/pal-extract/src/typescript.rs:145,163,416,438` 로 같은 타입이 TS 쪽에서도 쓰이는 것을 확인
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 공짜

### 차선책이 「못 푼 참조」 축을 통째로 지울 수 있는데, 그 발동을 판정할 주체와 시점이 안 적혔다
- 어떻게 실패하나: `## 차선책` 첫 항이 *"`B2` 를 못 채우면 — `UnresolvedRef` 를 세우지 않고 「못 푼 참조」를 **수로만** 산출한다. 그것은 축소이고, 그때 `B1`·`B3`·`B4` 는 **미측정으로 적는다**"* 다. 이것이 발동하면 **B 절 넷 중 셋이 미측정이 되고 `B2` 는 반증**이라 B 절이 통째로 사라진다. 그런데 원문이 잠근 범위는 *"이 회차의 범위를 어디까지로 잠그나? → **경계 + 못 푼 참조**"* 이므로 **잠근 두 축 중 하나가 0 이 된다.** 차선책 자신이 그것을 「축소」라 부르고 소유자 승인 대상인데, **누가 언제 「못 채운다」를 판정하는지가 없다** — `B2` 는 *"원천이 없으면 이 조건은 반증이고"* 라고만 적는다. 구현자가 어렵다고 느낀 시점에 스스로 발동할 수 있는 형태다.
- 어디가 걸리나: 잠긴 의도 `## 차선책` 첫 항 · `B2` · `B1`·`B3`·`B4` · `## 원문` 첫 잠금(*"경계 + 못 푼 참조"*)
- 획득: 조회 — 차선책 세 항의 발동 조건 문면 비교. 셋 다 *"못 채우면"·"안 서면"* 이고 판정자·시점·증거가 없다. 잠긴 의도의 `## 상한` 은 라운드 수를 적지만 차선책 발동 절차는 안 적는다
- 모집단: 원의도
- 유효성: 참
- 해악도: 실패 (발동하면 원문이 잠근 범위의 절반이 사라진 채 회차가 닫힌다)
- 대상: 계획자신
- 얼마나 아픈가: 지금이면 발동 절차 한 줄. 발동 뒤면 이미 소유자 보고가 나간 뒤다

### 감사 입력 발췌본이 `## 금지역` 을 본문 0 줄로 실었고 `## 멈추고 올리는 자리` 를 통째로 뺐다 — 이 라운드가 그 두 절의 덮개를 안 쟀다
- 어떻게 실패하나: 발췌본 `audit-in.md` 는 `## 금지역` 제목 바로 다음 줄에 `## 해소 깊이` 를 이어 붙였고 `## 멈추고 올리는 자리` 절이 없다. `intent.md` 의 제목 목록을 세면 그 두 절이 44–48 행과 49–58 행으로 **실재한다.** 절 추출이 자리 기반이라 이웃 절을 삼킨 것으로 보이며, 같은 결함으로 `## 목적 기여`·`## 상한`·`## 범위 밖` 이 **두 번씩** 실렸고 `## 차선책` 의 둘째 판은 *"(같이 쓴다)"* 뿐이다. 결과로 **금지역과 멈춤 자리를 조건이 덮는가라는 판정이 이 라운드에서 통째로 빠졌다** — 그리고 다음 라운드가 같은 도구를 쓰면 같은 자리가 또 빈다.
- 어디가 걸리나: `/private/tmp/claude-501/-Users-incognito-dev-projects-palimpsest/7680f779-0c18-4212-96fb-dac5472be22e/scratchpad/audit-in.md:42-43`(`## 금지역` 다음 줄이 `## 해소 깊이`) · 같은 파일 17-18 · 29-30 · 166/177 · 126/162 행(중복) · `.palimpsest/rounds/2026-09-08-cross-file-references/intent.md` 의 제목 44 · 49 행
- 획득: 조회 — `grep -n "^## " intent.md` 로 **제목만** 열거해 두 절의 실재를 확인했다(본문은 지시대로 안 읽었다) · 발췌본 전문 직독
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호 (평가가 돌았는데 두 절은 안 재졌고, 산출물만 보면 다 잰 것으로 읽힌다)
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 공짜. 다시 뽑아 2 라운드에 주면 된다

### 감사 입력 발췌본이 정반합 판 1 의 판정 결과를 함께 실었다 — 이 라운드는 해설 없이 읽힌 것이 아니다
- 어떻게 실패하나: 이 자리의 값은 *"해설을 안 받는 것"* 인데, 발췌본의 `## 해소 깊이`·`## 모집단`·`## 범위 밖` 이 **판 1 이 무엇을 어떤 등급으로 잡았는지**를 그대로 담는다 — *"판 1 이 그것을 **전환**으로 판정해 올렸다"* · *"판 1 이 그 낱말이 … **실패급으로** 잡았다"* · *"정(正)이 냈고 1 라운드 반이 못 깼고 1 라운드 합이 통과시킨 값인데"* · *"그 배제 근거는 **판 1 이 깼다**"*. 회차 규약이 개정 표에 대해 경고한 것(*"이건 이미 사전부검이 잡은 것"* 이라는 사전 정보)과 같은 형태가 **잠긴 의도의 본문 절 안에** 들어와 있어, 절만 발췌해도 격리가 안 선다. 여기에 더해 이 라운드는 지시에 따라 `premortem/r1-raw.md` 를 형식 참조로 열어 **사전부검 원 반환문 전문을 읽었다.**
- 어디가 걸리나: `audit-in.md:45-46` · `:62-64` · `:76-79` · `:171` · `.claude/skills/round/SKILL.md:174` · 이 라운드가 연 `.palimpsest/rounds/2026-09-08-cross-file-references/premortem/r1-raw.md`
- 획득: 조회 — 발췌본에서 「판 1」·「정(正)」·「반」·「합」이 판정 등급과 함께 나오는 문장 넷을 직독 · 지시문의 형식 참조 요구 직독
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호 (이 라운드의 독립성이 산출물 문면보다 낮다)
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 없다 — 이 라운드에 한해서는 이미 읽었다. 2 라운드를 다른 세션에 주면 회복된다

---

## 조건의 합이 의도를 덮나

**덮지 않는다. 원문에서 인용해 셋을 적는다.**

**하나.** > *"무엇이 도달을 증언하나? → **자기 저장소 + 정확도 표본**"*

「정확도 표본」을 지는 조건은 `E3` 하나이고 그것에 합격선이 없다. 증인의 절반이 어떤 값이 나와도 통과한다. 그리고 그 구멍이 `E2`(총계 800)와 맞물려, **거짓 엣지 800 건으로 회차 전체가 초록이 되는 경로가 조건 안에 열려 있다.**

**둘.** > *"엣지에 해소 등급을 싣나? → **exact 만**"*

산출된 엣지가 실제로 후보 유일이었나를 재는 조건이 없다. `C3` 은 스키마 표기만 다룬다. 그리고 그 유일성을 위협하는 실체(*"㉮ 의 43 이름 중 7 개(199 자리)가 오늘 이름만으로는 모호하다"*)가 조건이 아니라 `## 잔여를 여기서 미리 선언한다` 로 들어갔다. **잔여 선언이 이 잠금을 지탱하던 유일한 자리를 들어냈다.**

**셋.** > *"이 회차의 범위를 어디까지로 잠그나? → **경계 + 못 푼 참조**"*

`## 차선책` 첫 항이 발동하면 `B1`·`B3`·`B4` 가 미측정, `B2` 가 반증이 되어 **「못 푼 참조」 축이 통째로 사라진다.** 그 발동을 누가 언제 판정하는지가 조건에도 차선책에도 없다.

**넷(약함).** > *"임포트가 항목 이름을 실게 하는 자리를 어디로 하나? → **`ImportSet` 에 이름**"*

잠근 자리를 재는 조건이 없다. 결과(엣지)만 재므로 다른 자리에 두어도 전부 초록이다.

⚠ **`## 금지역` 과 `## 멈추고 올리는 자리` 는 이 판정에서 빠졌다** — 발췌본이 안 줬고, 지시가 원본을 열지 말라고 했다. 그 둘의 덮개는 **안 쟀다.**

## 조건이 의도보다 큰가

**하나 있다.** `C3` 이 통과 경로로 「등급 칸을 신설」을 여는데, `## 범위 밖` 이 *"**`candidate` 등급 엣지** — exact 만 싣는다. 등급 축은 `#133` 과 겹친다"* 로 등급 축을 밖으로 선언했다. 위 발견 항에 적었다.

`D3`·`D4`·`D5` 는 원문에 직접 대응하는 잠금이 없지만 *"이 회차는 어디서 멈추고 올리나? → **결박·판정이 움직이면**"* 의 회귀 방어로 읽히므로 초과로 세지 않았다.

## 내가 기각한 것

### `A5` 가 「엣지 0」을 요구하므로 아무것도 안 만드는 구현으로도 통과한다
- 기각 사유: 그것이 음성 대조의 정상 형태다. 양성 축을 `A1`~`A4` 가 지고 있고, 넷이 초록일 때만 `A5` 의 0 이 뜻을 갖는다. 조건 하나만 떼어 자명 통과를 지적하면 모든 음성 대조가 걸림이 된다
- 모집단: 원의도 · 유효성: 거짓 · 해악도: 미관 · 어디가 걸리나: 잠긴 의도 `A5`

### `A4` 의 「원본 심볼이거나, 못 풀면 미해소로 나간다」가 선언(disjunction)이라 항상 참이 된다
- 기각 사유: 두 갈래 다 조건이 지려는 것(*"거짓 엣지가 안 선다"*)을 만족한다. 미해소 쪽으로 전부 도망가는 퇴화는 `E2`(800 하한)가 막고, 미해소가 조용히 사라지는 것은 `B2`(`attempts` 비지 않음)가 막는다. 항상 참이 아니라 **거짓 엣지가 서면 거짓**이다
- 모집단: 원의도 · 유효성: 거짓 · 해악도: 미관 · 어디가 걸리나: 잠긴 의도 `A4`

### `B4` 가 적은 「오늘 등록된 엣지 여덟」이 틀렸다
- 기각 사유: 세어 보니 정확히 여덟이다 — `BOUND_TO`·`AUTHORED_BY`·`TOUCHES`·`FOLLOWS`·`MANIFESTS_AT`·`INTRODUCED_BY`·`RESOLVED_BY`·`REFERENCES`(`grep -n "^\[edge\." schema/graph.toml`). 조건 문면이 저장소와 맞는다
- 모집단: 저장소 · 유효성: 거짓 · 해악도: 미관 · 어디가 걸리나: `schema/graph.toml:217, 314, 325, 335, 345, 358, 368, 415`

### `D3` 의 모집단이 안 선다 — 외부 코퍼스가 없어 `f04-verify.py` 를 못 돌린다
- 기각 사유: 두 코퍼스가 실재한다(`~/dev/projects/ditto` · `~/dev/projects/boxwood/portal-backend`, `scripts/f04-verify.py:61-62`). 모집단은 선다. `D3` 의 걸림은 모집단이 아니라 근거 문장이고 그것은 위에 따로 적었다
- 모집단: 저장소 · 유효성: 거짓 · 해악도: 미관 · 어디가 걸리나: `scripts/f04-verify.py:61-62`

### `D5` 가 `## 범위 밖` 의 「TypeScript 경계 넘기」와 충돌한다
- 기각 사유: `D5` 는 TypeScript 에서 경계를 넘으라고 요구하지 않는다. `ImportSet` 이 세 언어 공유 타입이라(`crates/pal-extract/src/typescript.rs:145`) Rust 를 위해 필드를 더할 때 TS 쪽 빈 벡터가 「부재」와 「미구축」을 뭉개지 않게 하라는 **부작용 방어**다. 범위 밖 선언과 안 부딪힌다
- 모집단: 원의도 · 유효성: 거짓 · 해악도: 미관 · 어디가 걸리나: 잠긴 의도 `D5` · `## 범위 밖` 둘째 항

### `C1` 이 불변식 ① 만 이름하는데 `.absent` 를 뒤집으면 ②③ 도 함께 움직여 조건이 부작용을 안 적었다
- 기각 사유: 잠긴 의도가 그 부작용을 **이미 적었다** — `D2` 가 *"①·②·③ 을 뺀 다섯 … 앞의 셋은 `C1` 이 의도적으로 바꾸는 자리다"* 라고 셋을 함께 뺐다. 선언은 `crates/pal-cli/src/doctor.rs:315-316` 에서 뷰 라벨 단위라 셋에 동시에 걸리는 것이 맞고, 조건이 그것을 알고 있다
- 모집단: 저장소 · 유효성: 거짓 · 해악도: 미관 · 어디가 걸리나: `crates/pal-cli/src/doctor.rs:315-316` · 잠긴 의도 `C1`·`D2`
