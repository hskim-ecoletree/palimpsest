# 독립 리뷰 라운드 2 — 원 반환문

> 회차 `2026-09-08-cross-file-references` · 2026-09-10 · `pal-independent-reviewer`
>
> **받은 것** — 잠긴 의도 전문 · 산출물 · 라운드 1 의 원 반환문(다시 내지 말 것).
> **안 받은 것** — 대화 기록 · 왜 그렇게 만들었는지의 설명.
>
> 리뷰어가 `00202e0` 에서 실측했고, **격리 사본**(`.git` 포함 · 디렉터리 이름을
> `palimpsest` 로 맞춰 결박이 `orphaned` 로 안 새게 했다)에서 돌렸다.

## 합격선 축 — 리뷰어의 검산

**통과 33 · 반증 4 · 대조 불가 0 · 미측정 2 = 39.**
게이트(통과 35 · 반증 4)와 갈리는 자리는 `A7-a`·`D3` 둘이고, 리뷰어가
*"「회차가 안 쟀다」가 아니라 「내가 안 쟀다」다"* 라고 적었다.

## 미측정 목록

| # | 안 잰 조건 | 모집단 | 유효성 | 해악도 | 좌표 | 왜 못 쟀나 |
|---|---|---|---|---|---|---|
| M1 | `A7-a` — ⓑ 의 분모 893·483 | 원의도 | 추정 | 거짓신호 | `.palimpsest/rounds/2026-09-08-cross-file-references/observations/a7-call-denominator.md` | 등록된 계기 M1~M4 를 다시 돌리려면 이 회차가 쓴 정규식·판정 규칙 C-1~C-4 를 재구현해야 한다 |
| M2 | `D3` — `f04-verify.py` 가 초록인가 | 원의도 | 추정 | 거짓신호 | `scripts/f04-verify.py:188` | 그 스크립트는 **소스를 변이시키고 릴리스 빌드를 넷 돌린다.** 읽기 전용이라 원본에서 못 돌리고 격리 사본에는 `target/` 이 없다. 리터럴 일치만 확인했다 |

## 의도 축 — 빠진 것

| # | 발견 | 모집단 | 유효성 | 해악도 | 좌표 |
|---|---|---|---|---|---|
| 1 | **사정권 전수 표가 「전수」가 아니다 — 조건 39 중 38 만 든다.** 게이트가 *"조건 38 개 × 결박 30"* 이라 적고 안 5 + 밖 18 + 못 쟀다 15 = 38. 한 조건이 어느 칸에도 없고 **어느 것인지 못 짚는다** — 「밖 18」·「못 쟀다 15」가 조건 이름을 안 댄다. 결박도 종료 시점 34 가 아니라 30 으로 쟀고, 이 회차가 새로 건 넷은 `B`·`D4`·`D4-a` 사정권을 실제로 움직인다 | 원의도 | 참 | 금지역 | `docs/gates/cross-file-references.md:170` |

## 의도 축 — 요구되지 않은 것

없음 — 이 라운드에서 산출할 것이 없다.

## 의도 축 — 있는데 틀린 것

| # | 발견 | 모집단 | 유효성 | 해악도 | 좌표 |
|---|---|---|---|---|---|
| 2 | **`super::` 가 인라인 `mod` 안에서 한 칸 높게 풀려 거짓 파일 간 엣지가 선다.** 해소기가 `모듈_자리(from)` 를 **파일 경로만으로** 계산해 `mod tests` 안의 `use super::{…}` 를 부모 모듈 파일로 보낸다. 실측: `install/manifest.rs:483` 의 `use super::{… Rel …}` 가 `install.rs` 로 풀렸고 `Rel::new` 의 꼬리가 그 파일의 **사적** `Report::new` 를 잡아 **거짓 엣지 3 건**이 섰다. 같은 오류가 `no_symbol` 통을 부풀린다 — 못 푼 참조 986 중 **180(18.3%)이 「출발 파일 자신이 정의한 이름」**이다. `A3` 픽스처는 파일 수준 `super::super::…` 만 재서 이 형태를 원리상 안 지난다 | 원의도 | 참 | 금지역 | `crates/pal-core/src/cross_file.rs:344` |
| 3 | **`[node.UnresolvedRef] key = ["site","name"]` 가 산출 데이터에서 키가 아니다.** 4622 행이 **3025** 개의 (site,name) 만 쓴다 — 중복 키 848 · 잉여 1597(34.6%) · 최대 중복 **27** · `reason` 이 서로 다른 키 1. `pal export` 가 같은 키로 노드를 여러 번 내고 이어지는 `MATCH` 가 **중복 노드 전부에 매치**하므로 바깥 도구에서 엣지가 증식한다. 어떤 검사도 이 유일성을 안 잰다 | 원의도 | 참 | 금지역 | `schema/graph.toml:203` |
| 4 | **`pal export` 의 `UnresolvedRef` 행이 자기 스키마와 다르다.** `attempts : attempt[]` 로 적는데 export 는 **길이(정수)** 를 내고, `required = true` 인 다섯째 칸 `at` 을 **아예 안 낸다.** export↔스키마를 대조하는 자가 없다 | 원의도 | 참 | 거짓신호 | `crates/pal-cli/src/export.rs:251` |
| 5 | **`pal doctor` 가 아직 `File` 을 「담지 못하는 자리」로 찍고 능력 줄에 `F05` 를 미구축으로 적는다 — 같은 빌드의 `pal export` 가 `File` 141 건을 낸다.** 이 회차는 능력 id 를 바꾸고 주석 열여덟 줄을 달았을 뿐 **사용자에게 보이는 두 문장은 그대로다.** `E5-a` 는 *"「근거를 적었다」로는 안 닫힌다"* 를 명시한다 | 저장소 | 참 | 거짓신호 | `crates/pal-cli/src/doctor.rs:421` |

## 이번 라운드의 새 발견

| # | 발견 | 모집단 | 유효성 | 해악도 | 좌표 |
|---|---|---|---|---|---|
| 6 | **종료 보고가 「자기 장치에 대한 발견은 하나였고 금지역에 안 닿았다」고 적는데 원장은 25 건이고 그중 여섯이 금지역이다.** 이 문장은 규약 §11 ③ 의 「별도 목록」 판단을 그대로 지므로 다음 회차와 소유자가 *"이 회차는 자기 장치를 거의 안 만졌다"* 로 읽는다 | 회차기록 | 참 | 금지역 | `.palimpsest/rounds/2026-09-08-cross-file-references/report.md:59` |
| 7 | **`state.md` 가 자기 안에서 판정 수를 두 번 다르게 적는다.** `:5` 가 「통과 37 · 반증 2」, `:18` 이 「통과 35 · 반증 4」. 이 파일은 **다음 컨텍스트가 받는 둘 중 하나**라고 자기가 적는다 | 회차기록 | 참 | 금지역 | `.palimpsest/rounds/2026-09-08-cross-file-references/state.md:5` |
| 8 | **잠긴 의도의 「아래 넷은 종료 시점에 관측돼 여기로 들어왔다 … 이슈 셋으로 섰다」가 실제로는 여덟 불릿을 지배한다.** 셋(자기 저장소에서 축1 형태 넷 · 「`ImportSet` 에 이름」을 재는 조건 · `corpus/golden` 축복)은 `## 개정` 표에도 게이트의 「넷이 이슈 셋으로」에도 보고의 「다섯이 더」에도 없다. 세 문서가 같은 사건에 **넷·넷·다섯**을 적는다. 특히 둘째는 인터뷰가 잠근 일곱 중 하나를 **재는 조건이 없다**는 자백인데 어느 요약에도 안 올랐다 | 회차기록 | 참 | 금지역 | `.palimpsest/rounds/2026-09-08-cross-file-references/intent.md:303` |

## 자기 산출에 대한 발견

| # | 발견 | 모집단 | 유효성 | 해악도 | 좌표 |
|---|---|---|---|---|---|
| 9 | 게이트와 잠긴 의도가 **「종료 시점 실측 `fresh` 24 · `stale` 6」**이라 적는데 HEAD 의 실측은 **`fresh` 28 · `stale` 6(총 34)** 이다. 두 시점을 가른 문장은 `state.md` 와 `d1-rebinding.md` 에만 있다 | 회차기록 | 참 | 거짓신호 | `docs/gates/cross-file-references.md:186` |
| 10 | 사정권 표의 **「밖 18」과 「못 쟀다 15」가 조건 이름을 하나도 안 댄다.** 산출한 스크립트도 관측 파일도 없어 **원장이 그 33 을 재검할 길이 없다** | 회차기록 | 참 | 거짓신호 | `docs/gates/cross-file-references.md:174` |

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효성 | 해악도 | 왜 아니었나 |
|---|---|---|---|---|---|
| 11 | `E2` 가 「파일 간 **엣지**」를 요구하는데 게이트가 「참조 자리」를 댔다 | 원의도 | 거짓 | 미관 | `report.a_edges` 는 `edges.push` 한 횟수라 **엣지 단위가 맞다.** 중복 제거 뒤로 재도 ⓐ ≥ 1014 > 800 · ⓑ ≤ 283 < 333 이라 판정이 안 움직인다 |
| 12 | 안 도는 시험이 또 있다(R1 항 6 의 재발) | 자기장치 | 거짓 | 미관 | 전 크레이트 `--list` 964 개를 소스의 시험꼴 함수와 대조했다 — **고아 0.** `cargo test --workspace --all-targets` 1023 통과 · 0 실패 · 재빌드 경고 0 |
| 13 | `E3` 표본이 재현 안 된다 | 회차기록 | 거짓 | 미관 | 정렬이 완전하고 중복 쌍 0 이며 `floor(i·1297/30)` 이 관측 표의 위치 열과 **서른 자리 다 일치**한다 |
| 14 | 이 회차가 만든 표면 중 부르는 자리가 0 인 것이 있다 | 원의도 | 거짓 | 미관 | 새 `pub` 항목을 전수로 세었다 — 0 이 없다 |
| 15 | `cargo xtask check` 가 빨갛다 | 회차기록 | 거짓 | 미관 | **28/28 통과** |
| 16 | 기준선 장치(`doctor-axes.baseline.json`)가 아무것도 안 잰다 | 자기장치 | 거짓 | 미관 | 자기 음성 대조를 모듈 안에 박아 두었고 실제 산출로 `MERGE_BLOCKER_DOCTOR_OK` · rc=0. CI 가 파이프로 잇는다 |

## 끝내도 되는가 — 리뷰어의 답

**안 된다** — 본 목록에 금지역이 여섯 남았다(항 1 · 2 · 3 · 6 · 7 · 8).
그중 **2 와 3 은 산출물 자신의 결함**이다.
