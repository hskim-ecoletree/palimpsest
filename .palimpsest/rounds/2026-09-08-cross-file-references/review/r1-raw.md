# 독립 리뷰 라운드 1 — 원 반환문

> 회차 `2026-09-08-cross-file-references` · 2026-09-10 · `pal-independent-reviewer`
>
> **받은 것** — 잠긴 의도(원문+개정)와 산출물. **안 받은 것** — 대화 기록 · 앞 라운드의
> 논리 · 왜 그렇게 만들었는지의 설명.
>
> 리뷰어가 받은 HEAD 는 `d8d039c` 였으나 회차가 살아 움직여 여섯 커밋이 더 붙었다.
> **아래 판정과 발견은 전부 `649a6f8` 에서 다시 확인한 것**이라고 리뷰어가 적었다.

## 합격선 축 — 리뷰어의 검산

**통과 36 · 반증 2 · 대조불가 0 · 미측정 1 = 39.**
게이트(그때 통과 37 · 반증 2)와 갈리는 자리는 `E3` 하나이고, 리뷰어가
*"그것은 「내가 안 쟀다」이지 「회차가 안 쟀다」가 아니다"* 라고 적었다.

## 의도 축 — 빠진 것

| # | 발견 | 모집단 | 유효성 | 해악도 | 좌표 |
|---|---|---|---|---|---|
| 1 | **소유자 승격 칸 3 이 요구한 「결박 사정권 전수 표」가 게이트에 없다.** 잠긴 의도가 *"등록된 완수 조건 전부에 대해 전수로 재고 그 표를 게이트에 싣는다 — 안 재면 사정권 안의 조건이 조용히 돌아 방아쇠가 다시 발동한다"* 로 못 박았고, 살아남은 실패 `DL3-07` 의 처분도 *"그 사실을 사정권 표에 그대로 적는다"* 였다. 둘 다 실행 안 됐다. 근거 `grep -c 사정권 docs/gates/cross-file-references.md` → **0** | 원의도 | 참 | 금지역 | `.palimpsest/rounds/2026-09-08-cross-file-references/intent.md:68` |

## 의도 축 — 요구되지 않은 것

| # | 발견 | 모집단 | 유효성 | 해악도 | 좌표 |
|---|---|---|---|---|---|
| 2 | `How::SymbolsOnly` 가 이 회차에서 **부르는 자리 0** 이 됐다(`doctor` 가 `Stitching` 으로 옮겨가면서). `pub enum` 변형이라 컴파일러가 안 잡는다. 근거 `grep -rn SymbolsOnly crates xtask` → 생성 자리 0 · 주석 2 · match 팔 1 | 저장소 | 참 | 미관 | `crates/pal-cli/src/attach.rs:119` |

## 의도 축 — 있는데 틀린 것

| # | 발견 | 모집단 | 유효성 | 해악도 | 좌표 |
|---|---|---|---|---|---|
| 3 | **`A5` 가 금한 「재수출만 지나는 이름」이 저장소 산출에서 엣지가 됐다 — 2 건.** `lib.rs:45` 는 사적 `mod traverse;` 이고 `:143` 이 `pub use traverse::{Step, traverse};` 다. 밖에서 `use pal_core::traverse` 는 재수출된 함수만 가리킬 수 있는데 엣지는 사적 `mod` 를 가리킨다. `A5-a` 는 *"대상이 뿌리 파일의 직접 정의인가"* 만 봐서 이 형태를 **원리상 못 잡는다** — 잘못 잡힌 대상은 정의상 언제나 직접 정의다 | 원의도 | 참 | 금지역 | `crates/pal-core/src/lib.rs:45` |
| 4 | **`pal doctor` 가 `File` 을 「담지 못하는 자리(F07)」로 찍는데 같은 빌드가 `File` 노드 141 건을 산출한다.** 같은 화면의 능력 줄은 F07 을 미구축에서 뺐다 — 한 화면 안에서 두 문장이 부딪힌다. 그 선언의 사유 주석 *"이 뷰가 엣지를 안 읽는다"* 는 이 회차의 `C1` 이 뷰에 엣지를 실으면서 거짓이 됐다 | 저장소 | 참 | 거짓신호 | `crates/pal-cli/src/doctor.rs:407` |
| 5 | `RefCounts::total()` 의 문서가 **「일곱의 합」**인데 본체는 **여덟**을 더한다. 이 회차가 `imported` 를 여덟째로 더하면서 주석만 안 고쳤다 | 저장소 | 참 | 거짓신호 | `crates/pal-core/src/projection.rs:120` |

## 이번 라운드의 새 발견

| # | 발견 | 모집단 | 유효성 | 해악도 | 좌표 |
|---|---|---|---|---|---|
| 6 | **이 회차가 기존 음성 대조 시험 하나를 죽였다.** `502b78e` 가 `#[test]` 를 `C4-a` 문서 주석 **위**에 하나 더 붙이면서 원래 그 자리의 `불변식_1_엣지의_양_끝_노드가_존재한다` 가 속성을 잃었다. 그 함수는 한 번도 안 돌고 `c4a_…` 는 두 번 등록돼 두 번 돈다. 근거 `cargo test -p pal-core --lib -- --list \| grep 불변식_1` → 없음 | 저장소 | 참 | 금지역 | `crates/pal-core/src/doctor.rs:1260` |
| 7 | **게이트가 같은 문서 안에서 `C2` 를 「통과」와 「아직 못 쟀다」로 동시에 적는다.** `:164` 가 *"오늘 산출은 `{"checked": 471, …}` 하나뿐이고 라벨별 몫이 없다"* 를 현재형으로 적는다 | 회차기록 | 참 | 금지역 | `docs/gates/cross-file-references.md:164` |
| 8 | **잠긴 의도의 `## 승인` 이 「사전부검 2/2」라 적는데 사전부검은 1 라운드만 돌았다.** 같은 회차의 `report.md` 는 「사전부검 1/2」라 적는다 — 원장 두 문서가 승인 전제에 대해 갈린다 | 회차기록 | 참 | 금지역 | `.palimpsest/rounds/2026-09-08-cross-file-references/intent.md:389` |

## 자기 산출에 대한 발견

| # | 발견 | 모집단 | 유효성 | 해악도 | 좌표 |
|---|---|---|---|---|---|
| 9 | **게이트가 ⓐ/ⓑ 를 두 값으로 싣고 회계 검산 줄이 지금 산출과 안 맞는다.** 판정 절은 ⓐ 1460/5705 · ⓑ 276/1049 이고 효과 절은 ⓐ 1610/5751 · ⓑ 283/1060 이다. `E2` 의 반증 판정 자체는 안 움직인다 | 회차기록 | 참 | 거짓신호 | `docs/gates/cross-file-references.md:252` |
| 10 | **`D1` 이 요구한 셋이 게이트에 안 적혔다.** 조건 문면은 *"그 판정을 게이트에 적는다 … 셋 중 하나라도 없으면 미측정이다"* 인데 게이트는 관측 파일을 가리키기만 한다 | 회차기록 | 참 | 거짓신호 | `docs/gates/cross-file-references.md:97` |
| 11 | **`E3` 표본 30 중 ⓐ 26 건의 좌표가 기록에 없다.** ⓑ 4 건만 표로 실린다 — 「0/30」을 원장이 재검하려면 그 스냅샷의 색인을 다시 세워 정렬을 재현해야 한다 | 회차기록 | 참 | 거짓신호 | `.palimpsest/rounds/2026-09-08-cross-file-references/observations/e3-false-edge-rate.md` |
| 12 | **`A3` 시험의 이름이 재는 것과 다르다.** 단언하는 넷은 「균일·(같은 크레이트) 깊은 경로·별칭·`super::`」이고 잠근 넷의 「형제 크레이트 깊은/평평 경로」는 다른 시험이 진다. 합쳐서 넷은 덮인다 | 자기장치 | 참 | 거짓신호 | `crates/pal-cli/tests/cross_file_references.rs:154` |
| 13 | 잠긴 의도의 `A7` 개정이 *"플래그를 끄면 앞 회차가 통과로 닫은 게이트가 빨개진다"* 를 논거로 쓰는데 **그 예제는 플래그를 켠 지금도 `exit 1`** 이다. ⚠ 착수 커밋에서 빌드해 확인하지는 **않았다** | 회차기록 | 추정 | 거짓신호 | `.palimpsest/rounds/2026-09-08-cross-file-references/intent.md:362` |

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효성 | 해악도 | 왜 아니었나 |
|---|---|---|---|---|---|
| 14 | `cargo test --workspace` 가 `version_is_in_the_binary` 에서 빨갛다 | 저장소 | 거짓 | 미관 | 빌드 캐시 유물이다. 다시 빌드하니 초록이고 이후 전수 두 번 다 실패 0 |
| 15 | `cargo xtask check` 가 「발견이 닫혔나」에서 빨갛다 — 끝난 회차에 열린 발견 127 건 | 회차기록 | 거짓 | 미관 | 측정 시점에는 참이었으나 `fbccfe1` 이 127 건을 닫아 재현 안 된다 |
| 16 | `UnresolvedRef` 의 `attempts` 가 빈 배열일 수 있다(`B2` 반증) | 원의도 | 거짓 | 미관 | 전수로 세니 `attempts: 3` 3272 · `attempts: 2` 1350 이고 **0 이 하나도 없다** |
| 17 | 이 회차가 만든 타입들이 stub 이다 | 원의도 | 거짓 | 미관 | 부르는 자리를 전수로 셌다 — `CrossFileEdge` 2 · `CrossFileReport` 10 · `CallTail` 15 · `PendingImportRef` 7 · `ImportedItem` 29 · `cross_file_edges` 3 · `IntentStorePresence` 12. **0 인 것이 없다** |
| 18 | `Projection::open`(읽기·쓰기)이 형식 판을 안 봐서 `D4-a` 의 「모든 경로」가 안 닫힌다 | 저장소 | 거짓 | 미관 | 조건 문면이 이름 댄 것은 *"옛 색인을 새 바이너리로 **읽는**"* 경로이고, 읽기로 붙는 길이 하나임을 소스 훑기 시험이 강제한다 |

## 끝내도 되는가 — 리뷰어의 답

**안 된다** — 본 목록에 금지역이 넷 남았다(항 1 · 3 · 6 · 7·8).
