
  print_facts  ·  palimpsest@fbccfe1+worktree#f129649fd0fb
  fun · crates/pal-cli/src/touch.rs:457 · identity ordinal · body 1d4a476bb943

■ 이 좌표에 걸린 것 (1)
  [91cdc3add647a3f7] 최신 상태 아님(stale) ← 1 개가 변했습니다  ·  symbol 반경 · 감시 1
      회차 2026-09-07-rust-scope-references — 이 라벨은 두 방향을 다 적어야 한다
      
      **호출이 아닌 것을 담는다** — 실측 엣지 4,209 중 타입 참조 1,197 · 호출·매크로 3,796 ·
      그 밖 1,598. 스키마가 `[edge.REFERENCES]` 에서 *"`CALLS` 가 아니다 … `CALLS` 로 적으면
      타입 참조가 호출로 둔갑한다"* 로 못 박았다.
      
      **호출인데 안 담는 것도 있다** — 효과 관측이 그것을 잡았다. `pal touch resolve_shadowing`
      이 `호출자 0` 을 돌려주는데 바로 옆 `resolve_with` 이 `self.resolve_shadowing(…)` 으로
      부른다. 멤버 호출은 `field_identifier` 라 이 층이 원리상 못 본다(실측 13,951 자리) ·
      경로 호출도 꼬리라 못 본다(4,217 자리).
      
      **둘 중 하나만 적으면 0 이 「아무도 안 부른다」로 읽힌다.** 두 줄이 함께 서야 한다.
      
      이슈 #130 · 조건 D8 · D2
■ 이 좌표를 지켜보는 것 (0)
  아직 없습니다.
■ 이 심볼이 하는 것
  호출자 1 · 피호출자 1
  ※ 「호출자·피호출자」는 **참조 엣지**입니다 — 호출뿐 아니라 타입 참조·구조체 리터럴·경로 머리도 셉니다
  ※ **`x.foo()` 는 아직 안 셉니다** — 멤버 해소(`member-resolution`)는 타입 추론이 필요해 **이 회차의 범위 밖**입니다. 능력 부재가 아니라 안 하기로 정한 자리입니다
  ※ 파일 간 해소 — ⓐ `cross-file-import` 1610/5751 · ⓑ `path-resolution` 283/1060 (선 것/짝)
  ※ ⓐ 가 못 선 까닭 — ambiguous 214 · no_symbol 840 · no_symbol_at_crate_root 1951 · outside_repo 1136
  ※ ⓑ 가 못 선 까닭 — ambiguous 14 · no_symbol 146 · no_symbol_at_crate_root 335 · outside_repo 282
  ※ 못 선 몫이 가는 문 — 이 회차가 안 세우기로 정한 자리와 못 세우는 자리를 가릅니다
      ⚠ 갈래 하나는 **아직 안 갈렸습니다** — 그 자리는 문이 아니라 「미측정」을 적습니다
      `outside_repo` → **경계** — 저장소 밖(`std::*` 등)이라 원리상 못 섭니다
      `no_symbol_at_crate_root` → `A5`·`A5-a` — 재수출 경유는 잠근 축1 의 **밖**입니다
      `no_symbol` → **갈래가 아직 안 갈렸습니다** — `#133`(L2)로 갈 몫과 이 회차의 구현 여지가 섞여 있고 그 크기는 **미측정**입니다
      `no_target_file` → **이 회차의 구현** — 모듈 경로를 파일로 못 폈습니다
      `ambiguous` → **후보 생성의 모호** — 모듈 경로가 여러 파일로 읽힙니다. 하나를 고르면 조용한 오답이라 **안 고르는 것이 설계입니다**
■ 내가 모르는 것
  3 건
  · Capable — no_symbol_at_crate_root · 지난 걸음 import_item(1→1) module_path(4→1) target_symbol(1→0)
  · Capable — no_symbol_at_crate_root · 지난 걸음 import_item(1→1) module_path(4→1) target_symbol(1→0)
  · Capable — no_symbol_at_crate_root · 지난 걸음 import_item(1→1) module_path(4→1) target_symbol(1→0)
■ 효과
  (이 빌드에는 effects 능력이 없습니다 — F13 미구축)
■ 판정
  (이 빌드에는 judgment 능력이 없습니다 — F15 미구축)

■ 이 답의 근거
  Snapshot  palimpsest@fbccfe1+worktree
  대장      parsed 141 · partial 0 · unsupported 760 · unrecognized 322 / 1224 파일
            결박 불가 언어 7개 — 그 파일들에는 좌표가 없습니다
  2층       심볼 3303 색인됨
  워킹트리  일치
  재구축    아님
  생략      없음 (명시)
  이관      1224건 — 본체를 다른 질의로 옮겼습니다. 생략된 것이 아닙니다
            ledger 1224건 → `ledger.snapshot` 로 조회할 수 있습니다
  질의 로그  남았습니다
  크기      약 1317 토큰 **이상** (잰 것: 5269 바이트 · 가정: 4 바이트/토큰)
  능력      ledger.snapshot · symbol.resolve · symbol.contains · symbol.callers · symbol.reaches · graph.dump · binding.status · narrative.unbound · binding.touch · plan.deviation · symbol.references · 미구축 F13 · F15

