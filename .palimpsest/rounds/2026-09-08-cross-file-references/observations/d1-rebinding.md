# 결박 재판정 — `D1`

> 회차 `2026-09-08-cross-file-references` · 2026-09-10 · 커밋 `c9a6bae`
>
> `D1` 원문이 요구한다 — *"움직인 결박마다 셋을 적는다: ⑴ 감시 심볼의 결정 명제 원문
> ⑵ 그 명제를 재는 시험의 이름과 그 시험의 산출(초록·빨강) ⑶ 명제가 아직 참인가의
> 판정. **셋 중 하나라도 없으면 미측정이다.**"*

## 움직인 것 — 둘

| | 착수 관측 | 2026-09-10 |
|---|---|---|
| 총 | 30 | 30 |
| `fresh` | 26 | **24** |
| `stale` | 4 | **6** |

착수 이후 새로 `stale` 이 된 것은 **`1cf63ec5e0e9937a`** 와 **`91cdc3add647a3f7`** 둘이다.
나머지 넷(`39b92bd51bbc82c6`·`4065e80475cf1c72`·`49714573e0fa654c`·`cbd70e3e8dfca3ba`)은
착수 시점에 이미 `stale` 이었고 이 회차가 안 건드렸다.

**재는 방법** — `pal intent import .palimpsest/intent/bindings.jsonl --intent <자리>` 로
파생 저장소를 세운 뒤 `pal query binding.status --intent <그 자리> --json`.

---

## ① `1cf63ec5e0e9937a` — `crates/pal-extract/src/rust.rs` 의 `표면`

### ⑴ 결정 명제 원문

> 회차 2026-09-07-rust-scope-references — `pub` 의 뜻은 「최상위 + 정확히 `pub`」 하나다
>
> `crates/pal-cli/src/ledger.rs` 의 `stitch_of` 가 `n.container.is_empty()` 로 **최상위만**
> EXPORTS 로 옮긴다. 그러므로 `ExportSet` 이 중첩 `pub` 을 담으면 `export_digest` 와
> EXPORTS 가 **서로 다른 모집단**을 재고, 그 어긋남은 화면 어디에도 안 나온다.
>
> `pub(crate)`·`pub(super)`·`pub(in …)` 도 안 담는다 — `ExportSet` 의 뜻이 *"밖에 노출하는
> 것"* 인데 크레이트 안 가시성은 그 뜻과 다르다.
>
> 이 규칙을 넓히면 `a13_pub_의_뜻은_최상위_pub_하나다` 가 빨개진다.
>
> 이슈 #130 · 조건 A13

### ⑵ 그 명제를 재는 시험과 그 산출

**`rust::tests::a13_pub_의_뜻은_최상위_pub_하나다`**(`crates/pal-extract/src/rust.rs:1158`).

```
test rust::tests::a13_pub_의_뜻은_최상위_pub_하나다 ... ok
```

**초록이다.** 명제가 스스로 지목한 반증 조건(*"이 규칙을 넓히면 그 시험이 빨개진다"*)이
안 발화했다.

### ⑶ 명제가 아직 참인가 — **참이다**

이 회차가 `표면` 에 더한 것은 **임포트 항목 축**(`ImportSet::items`)과 그 항목의 선언
바이트(`ImportedItem::at`)이고, **`ExportSet` 의 모집단은 한 자도 안 건드렸다.**
`pub` 의 뜻을 넓히거나 좁히지 않았으므로 `export_digest` 와 `EXPORTS` 가 같은 모집단을
재는 관계도 그대로다.

**낡음이 움직인 까닭은 명제가 깨져서가 아니라 감시 심볼의 본체가 바뀌었기 때문이다** —
결박은 「그 자리를 다시 보라」를 말하지 「그 판단이 틀렸다」를 말하지 않는다.

---

## ② `91cdc3add647a3f7` — `crates/pal-cli/src/touch.rs` 의 `print_facts`

### ⑴ 결정 명제 원문

> 회차 2026-09-07-rust-scope-references — 이 라벨은 두 방향을 다 적어야 한다
>
> **호출이 아닌 것을 담는다** — 실측 엣지 4,209 중 타입 참조 1,197 · 호출·매크로 3,796 ·
> 그 밖 1,598. 스키마가 `[edge.REFERENCES]` 에서 *"`CALLS` 가 아니다 … `CALLS` 로 적으면
> 타입 참조가 호출로 둔갑한다"* 로 못 박았다.
>
> **호출인데 안 담는 것도 있다** — 효과 관측이 그것을 잡았다. `pal touch resolve_shadowing`
> 이 `호출자 0` 을 돌려주는데 바로 옆 `resolve_with` 이 `self.resolve_shadowing(…)` 으로
> 부른다. 멤버 호출은 `field_identifier` 라 이 층이 원리상 못 본다(실측 13,951 자리) ·
> 경로 호출도 꼬리라 못 본다(4,217 자리).
>
> **둘 중 하나만 적으면 0 이 「아무도 안 부른다」로 읽힌다.** 두 줄이 함께 서야 한다.
>
> 이슈 #130 · 조건 D8 · D2

### ⑵ 그 명제를 재는 시험과 그 산출

**`e4_화면이_능력_축_셋을_가르고_회계를_찍는다`**
(`crates/pal-cli/tests/cross_file_references.rs`).

```
test e4_화면이_능력_축_셋을_가르고_회계를_찍는다 ... ok
```

**초록이다.** ⚠ **이 시험은 이 회차가 세웠다** — 결박이 움직인 뒤에 만들었으므로
*"명제를 재던 시험이 여전히 초록이다"* 가 아니라 *"명제를 재는 시험을 세웠고 그것이
초록이다"* 다. 그 사실을 여기 적는다.

### ⑶ 명제가 아직 참인가 — **앞 절반은 참, 뒤 절반은 좁아졌다**

| 명제의 조각 | 판정 | 근거 |
|---|---|---|
| **호출이 아닌 것을 담는다** | **참** | 화면 첫 줄이 그대로 있고 시험이 단언한다 |
| **둘 중 하나만 적으면 0 이 「아무도 안 부른다」로 읽힌다** | **참** | 그래서 두 줄이 함께 선다. 시험이 둘 다 단언한다 |
| **호출인데 안 담는 것 — 멤버 호출**(`field_identifier` · 13,951 자리) | **참** | 오늘도 안 담는다. 타입 추론이 필요하고 이 회차가 **범위 밖**으로 잠갔다 |
| **호출인데 안 담는 것 — 경로 호출**(꼬리 · 4,217 자리) | ★ **더 이상 전부 참이 아니다** | 임포트된 머리에 붙은 경로 호출의 꼬리는 **이제 담는다** — 오늘 276 건 |

★★ **명제 자체는 살아 있고 그 목록이 한 칸 줄었다.** 「두 방향을 다 적어야 한다」는
여전히 참이고, 뒤 방향에 속하던 둘 중 하나(경로 호출)가 앞 방향으로 넘어갔다.

**화면이 그 이동을 말한다.** 앞 판은 `x.foo()` 와 `S::foo()` 를 한 줄에 묶어
*"멤버·경로 해소가 F07 미구축"* 이라 적었는데, 지금은 갈랐다 —

- `x.foo()` 는 **범위 밖**이라 적는다(능력 부재가 아니다)
- `S::foo()` 는 회계 줄의 ⓑ `path-resolution` 이 수로 답한다

⚠ **묶어 둔 채로 두었으면 그것이 거짓이 됐다.** 결박이 움직인 것이 그 자리를 다시 보게
했고, 다시 보니 문면이 이미 낡아 있었다. **결박이 설계대로 일했다.**

---

## 이 판정이 안 답하는 것

- **나머지 넷의 명제** — 착수 시점에 이미 `stale` 이었고 이 회차가 그 본체를 안 건드렸다.
  `D1` 은 *"낡음이 **움직이면**"* 을 조건으로 걸므로 이 회차의 몫이 아니다.
- **`91cdc3add647a3f7` 의 시험이 이 회차가 세운 것이라는 사실이 판정을 약하게 하나** —
  약하게 한다. 명제를 재던 시험이 **앞 회차에 없었다**는 것이 이 자리에서 드러났고,
  그것은 이 회차가 아니라 앞 회차의 잔여다. 여기서는 사실만 적는다.

---

## 방아쇠의 둘째 팔 — 종료 시점 재측정 ⟨2026-09-10 · `DL3-07`⟩

`DL3-07` 이 잡은 것: *"(ㄷ)이 방아쇠의 두 팔 중 하나만 쟀다."*
잠긴 의도의 `## 멈추고 올리는 자리` 는 팔이 **둘**이다 —
⑴ **결박 30 건의 낡음이 움직이나** ⑵ `pal doctor` 의 판정 축 여덟 중 하나라도 **축별
판정 문자열이 바뀌나**. 사정권 전수 표는 ⑵ 만 지고 ⑴ 을 안 쟀다.

**⑴ 을 종료 시점에 다시 쟀다** — `B1`~`B4` 와 `D4` 와 `PM1-06` 을 세운 뒤다.

| | 착수 | `c9a6bae` | **종료 시점** |
|---|---|---|---|
| 총 | 30 | 30 | **30** |
| `fresh` | 26 | 24 | **24** |
| `stale` | 4 | 6 | **6** |

`stale` 여섯의 신원이 `c9a6bae` 때와 **같다** — `1cf63ec5e0e9937a` ·
`39b92bd51bbc82c6` · `4065e80475cf1c72` · `49714573e0fa654c` · `91cdc3add647a3f7` ·
`cbd70e3e8dfca3ba`. **이 교대가 더 움직인 결박은 없다.**

★ **그러므로 이 팔은 이 교대에서 발동하지 않았다.** 이 교대가 만진 자리
(`cross_file.rs` · `projection.rs`(둘) · `touch.rs`(둘) · `doctor.rs`(둘) ·
`export.rs` · `rust_scopes.rs` · `schema/graph.toml`) 가운데 `stale` 을 새로 켠 것이
없다. ⚠ **「안 켜졌다」는 「사정권 밖이다」가 아니다** — `91cdc3add647a3f7` 는
`print_facts` 를 지켜보고 이 교대가 같은 파일을 만졌지만, **이미 `stale` 이라 더 켤
것이 없다.** 낡음은 한 번 켜지면 다시 안 움직이므로 이 팔은 그 결박에 대해 원리상
둔감하다. 그 사실이 이 팔의 한계이고, 여기서는 그것을 적는다.

**재는 방법은 위와 같다** — `pal intent import` 뒤 `pal query binding.status --json`.

⚠ **판정 뒤에 결박이 넷 늘었다** ⟨2026-09-10 · 규약 §11 의 넷째⟩ — 이 회차의 결정을
코드 좌표에 걸었다. **`D1` 이 잰 모집단은 판정 시점의 30 이다.** 늘어난 뒤는 34 ·
`fresh` 28 · `stale` 6 이고 **`stale` 여섯의 신원은 그대로다** — 새 넷은 전부 `fresh` 다.
그러므로 이 덧붙임은 위 판정을 안 움직인다.
