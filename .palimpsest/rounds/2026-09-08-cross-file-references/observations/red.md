# 착수 시점 관측 (RED) — 2026-09-08 · 커밋 `6b6cb6d`

빌드: `cargo build --release` 초록. 이진은 `target/release/pal`.

## ① `pal touch file_edges` — 파일 경계가 안 넘어간다

```
■ 이 심볼이 하는 것
  호출자 6 · 피호출자 3
  ※ **`x.foo()` 와 `S::foo()` 는 아직 안 셉니다** — 멤버·경로 해소가 F07 미구축입니다.
  **파일 안의 관계만입니다** — 파일 경계를 넘는 것은 F07 미구축입니다
■ 내가 모르는 것
  (이 빌드에는 unresolved-refs 능력이 없습니다 — F08 미구축)
```

능력 줄이 `미구축 F07 · F08 · F13 · F15` 를 찍는다. **이 회차가 지는 것은 앞의 둘이다.**

★ **그 줄이 세 갈래를 한 이름으로 묶는다** — ⓐ 파일 경계 넘기 ⓑ `S::foo()` 경로 해소
ⓒ `x.foo()` 멤버 해소. 인터뷰가 잠근 범위는 ⓐ 와, `use` 로 들어온 이름에 한해 ⓑ 다.
**ⓒ 는 타입 추론이 필요해 범위 밖이고, 그것을 안 가르면 「F07 을 했다」가 거짓이 된다.**

## ② 참조 엣지의 지금 수

`pal export --format cypher | grep -c REFERENCES` → **4259**.
전부 파일 **안**의 엣지다.

⚠ **「(헤더 1 행 포함)」은 거짓이었고 2026-09-09 에 지웠다** ⟨실측 `BL2-03`⟩. 같은 명령을
다시 돌려 보니 `REFERENCES` 를 담은 줄은 **전부 `MATCH (a:Symbol` 로 시작하는 엣지 줄**이고
헤더 줄은 0 이다. 그러므로 이 관측이 적은 4259 는 **엣지 4259 건**이지 4258 이 아니다.
`E2` 의 하한(800)은 이 값 위에 서지 않으므로 하한은 안 움직인다.

## ③ 결박 — **30 건이다. 문서가 적은 25 는 낡았다**

`pal query binding.status` → `결박 30건`. 첫 행이 `최신 상태(fresh)`.
⚠ `docs/plan/03-shortest-path.md` 와 이슈 `#133` 이 **25** 로 적는다 — 그 수는 낡았다.
**멈춤 판정의 기준선은 30 이다.**

## ④ `pal doctor` — 축 여덟의 착수 상태

| 축 | 결과 | 담지 못하는 자리 (이 회차와 겹치는 것만) |
|---|---|---|
| ① 엣지 양 끝 노드 존재 | ok · 검사 30 · 위반 0 | **`REFERENCES(F07)`** |
| ② 라벨·필수 속성 | ok · 검사 467 · 표본 밖 2798 | **`File(F07)` · `UnresolvedRef(F08)` · `REFERENCES(F07)`** |
| ③ producer ↔ provenance | ok · 검사 463 · 표본 밖 2772 | **`File(F07)` · `UnresolvedRef(F08)`** |
| ④ inferred evidence_refs | 모집단 없음 | — |
| ⑤ candidate 후보 집합 ≤ K | **모집단 없음** | — |
| ⑥ Residual 결박 | 모집단 없음 | — |
| ⑦ 결박 색인 해소 | 모집단 없음 | — |
| ⑧ 낡음 전파 | 모집단 없음 | — |

잔여 2 건 — 둘 다 표본 밖(2798 · 2772), 해소는 `pal doctor --full`.

★★ **여기가 이 회차의 가장 큰 위험이다.** 축 ①이 `REFERENCES` 를 「담지 못하는 자리」로
적으므로 **지금 있는 엣지 4,259 건이 통째로 doctor 검사 밖**이다. 파일 경계를 넘는 엣지를
더 만들어도 그 엣지의 양 끝 노드가 실재하는지를 아무도 안 잰다 — **금지역 「측정이 죽은
가지」에 닿는다.**

## ⑤ 대장

`parsed 139 · partial 0 · unsupported 706 · unrecognized 309 / 1155 파일` · 심볼 3205 색인.

---

## ⑥ 착수 중 사고 — 하위 에이전트가 원본 스키마를 파괴했다 (2026-09-08)

사전부검·기준선이 도는 동안 `git status` 가 `M schema/graph.toml` 을 찍었고, diff 는
**`[node.Binding]` 절을 포함해 216 줄 삭제**였다. 메인은 그 파일을 안 만졌다.

`git checkout -- schema/graph.toml` 으로 복구했다. 워킹트리는 회차 디렉터리만 남았다.

★ **회차 규약 §7 이 이미 경고한 자리다** — *"격리 사본에서 걸 때 사본이 사본을 재는지 먼저
확인해라"*. 그 문단은 **측정 도구의 뿌리**를 다루는데, 이 사고는 그보다 앞이다. 하위
에이전트가 파괴 실험을 **격리 사본이 아니라 원본에서** 했다.

**처리 방침은 이 회차의 발견 원장이 진다.**
