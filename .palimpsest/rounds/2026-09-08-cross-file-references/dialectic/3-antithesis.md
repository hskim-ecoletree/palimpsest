# 반(反) — 판 3 라운드 1

> 산출: `pal-decision-opponent` · 회차 `2026-09-08-cross-file-references` · 판 3 · 라운드 1 · 2026-09-09 · 커밋 `fe75072`
>
> **반이 적은 입력 경계 (원문)** — *"프롬프트에 대화 기록이나 메인의 사고 과정은 안 섞여 왔다. `dialectic/3-design.md` 는 「안 받는 것」으로 지정돼 열지 않았고, 그래서 **정이 설계문의 갈래 셋 정의를 온전히 다뤘는지는 내가 판정하지 않았다** — 정 자신이 (가)·(나)·(다)를 표로 세우고 (다)를 (다1)/(다2)로 다시 갈랐다는 사실만 적는다."*

## 초안이 서는 자리 — 반이 무너뜨리지 못한 것 넷

- **② 앞 회차 게이트가 (가)에서 빨개진다** — 실재한다. `crates/pal-extract/examples/scope_variants.rs:419` 가 V7 을 「기준에서 끈 것」으로 정의하고, `:454` 가 `if !엣지가_다른가 { 실패 += 1; }`, `:489` 가 `std::process::exit(1)` 이다. `crates/pal-extract/src/rust.rs:985` 의 `a7_매크로_밖_경로_꼬리도_참조가_아니다` 는 `Self::new()` 의 꼬리 자리가 1 임을 단언하고, `docs/gates/rust-scope-references.md` 의 판정표가 `A7` 과 `V7` 을 둘 다 통과에 싣는다. **인용이 다 맞다.**
- **④ 6,961 재현 실패와 `rust.rs:30` 의 4,217** — 인용 정확하다. 특히 정이 붙인 *"134 파일 기준 · 호출 꼬리만"* 이 `rust.rs:19-22` 의 단서와 정확히 맞다.
- **`PendingImportRef` 의 필드 셋** — `crates/pal-core/src/projection.rs:147-155` 가 `from`·`local`·`at` 셋뿐이고, `:342-346` 이 임포트 참조를 `pending` 으로 넘긴다. 맞다.
- **「992 는 enum 변형·연관 상수라 원리상 엣지가 못 선다」** — 반이 독립 정규식으로 잠근 세 크레이트를 다시 셌다(저장소 안 **1,615** · 밖 **482** — 정의 1,565/467 과 거의 같다). 그중 **타입머리+대문자꼬리가 1,005**, 모듈머리+대문자꼬리는 **32** 뿐이다. 정의 귀속이 대체로 맞고, 반이 노린 오분류 후보는 32 자리에 그친다.

## 반론 열둘

| # | 반론 | 좌표 | 유효성 | 해악도 |
|---|---|---|---|---|
| 1 | **(다2)의 꼬리에는 산지가 없다** — `file_edges` 는 `symbols·nodes·scopes·imports·at` 만 받고 소스도 꼬리도 못 본다. 꼬리를 `PendingImportRef` 에 실으려면 그 값이 먼저 `ScopeChain`(=`LocalRef`) 이나 새 `FileGraph` 칸에 들어와야 하고, 그것은 **추출기 변경**이다. 정은 「`PendingImportRef` 에 꼬리 칸 신설」만 비용으로 적었다 | `crates/pal-core/src/projection.rs:301-307` · `crates/pal-core/src/scope.rs:162-169` · `3-thesis.md:79` | 참 | 거짓신호 |
| 2 | **1 의 귀결로 캐시가 반드시 움직인다** — `cached.rs:73` 이 `scopes: Slot<ScopeChain>` 을 싣고 *"F05 의 1패스가 이것을 읽어 파일 내 엣지를 만든다 — 재파싱 없이"* 라 적는다. 곧 꼬리는 **캐시를 통과해야** 하고, 그러면 `EXTRACTOR_REV` 2 차 상향(이 회차가 이미 `f02-rust-scope`→`f07-import-items` 로 한 번 올렸다)과 `scripts/f04-verify.py:188` 리터럴 재수정이 함께 걸린다. 「안 쟀다」 ① 에 이 셋이 없다 | `crates/pal-extract/src/cached.rs:6-23,73` · `crates/pal-extract/src/lib.rs:166` · `scripts/f04-verify.py:188` · `3-thesis.md:126` | 참 | 거짓신호 |
| 3 | **정이 요구한 ⓑ 분모가 1 층에 앉을 자리가 없다** — `E4` 표는 *"잰 자리와 못 이은 자리를 수로 함께 찍는다"* 를 요구하는데, 꼬리는 `refs` 가 아니므로 `RefCounts` 에 넣는 순간 *"일곱의 합 = `refs` 의 길이"* 불변식이 깨진다. 그 수를 어디서 낼지를 판정이 안 정했다 | `crates/pal-core/src/projection.rs:118-131` · `3-thesis.md:104,107` | 참 | 거짓신호 |
| 4 | **사전 등록 규칙을 안 쓰고 판정 계기를 새로 만들었다** — 등록문이 *"값이 나온 뒤 규칙을 손보면 여러 번 뽑고 유리한 판을 남기는 것과 같은 형태"* 로 그 자리를 이미 이름 붙였다. 정의 573·992·294 는 등록 계기의 산출이 아니고, 스크립트가 저장소 밖 scratchpad 라 **원장이 재현할 수 없다.** 그런데 그 수가 새 하한 `E2` ⓑ 150 을 세운다 | `observations/a7-denominator-preregistration.md:9-10` · `3-thesis.md:5,52-60,94` | 참 | 거짓신호 |
| 5 | **150 의 깎기가 한 방향으로만 간다** — `:94` 는 밴드 아래끝 255 에서 다시 40% 를 깎는데, `:127` 은 같은 계기가 **과소일 수도 과대일 수도** 있다고 적는다. 오차가 양방향인데 조정은 합격을 쉽게 하는 쪽으로만 갔고, 총 950 은 800+150 의 산술합일 뿐 독립 근거가 없다. 즉 「측정 전에 정당화된 수」가 아니라 **추정 밴드에서 임의로 깎은 수**다 | `3-thesis.md:93-94` 대 `3-thesis.md:127` | 참 | 거짓신호 |
| 6 | **(다2)의 ⓑ 참조 자리 상한은 1,763 이 아니라 1,722 다** — 정 자신이 `:71` 에서 「임포트된 머리 · `Bound` 41 건은 (다2)에서 아예 안 생긴다」고 적는다. 그런데 `:66`·`:79` 는 41 을 포함한 1,763 을 그대로 ⓑ 자리 수·엣지 환산의 분모로 쓴다. 관측은 (다)의 상한을 **1,722** 로 못 박았다 | `3-thesis.md:66,71,79` · `observations/a7-denominator.md:27,58-63` | 참 | 미관 |
| 7 | **(ㄷ)이 방아쇠의 두 팔 중 하나만 쟀다** — 잠긴 의도의 방아쇠는 *"결박 30 건의 낡음이 **움직이거나** … 축별 판정 문자열이 바뀌면"* 인데, 정은 doctor 팔만 대조하고 결박 팔은 `:135` 에서 「전수로 못 쟀다」고 자백한다. 그 상태의 어휘는 「안 걸린다」가 아니라 **미측정**이다 — 잠긴 의도가 그 어휘를 못 박았다 | `intent.md:57-59,118` · `3-thesis.md:121,135` | 참 | 거짓신호 |
| 8 | **(ㄷ)의 「안 걸린다」는 기계로 반증될 수 없다** — 이 회차 원장이 방금 `BL3-02` 로 *"축별 판정 문자열 변화를 재는 기계가 없다 — 사람이 눈으로 댄다"* 를 등록했다. 곧 (ㄷ)이 틀려도 아무 검사도 안 빨개진다. 그 사실이 (ㄷ) 옆에 안 적혔다 | `findings.jsonl` 의 `BL3-02`(`기준커밋 fe75072`) · `scripts/check-round-doctor.mjs` | 참 | 거짓신호 |
| 9 | **`A7` 문면 개정을 소유자 물음에 안 실었다** — `:21` 은 *"`A7` 문면 자체가 개정 대상이다"* 로 잠긴 의도를 판정자가 고쳐 쓰는데, `:119` 의 (ㄱ) 물음은 ⓑ 의 충족 범위만 묻고 **그 개정을 안 묻는다.** `## 개정` 표의 기존 행은 전부 소유자 승인이나 승격을 거쳤다 | `3-thesis.md:21,119` · `intent.md:131,249-263` | 참 | 거짓신호 |
| 10 | **`A7-a` 가 준 권한은 「`E2` 하한 재등록」뿐인데 조건 무리를 더 넓혔다** — `E2-b` 신설(`:95`) · `E3-b` 를 층화(ⓐ20/ⓑ10)로 재등록(`:113`) · `E4` 축 셋의 화면 문면 확정(`:101-105`). 그리고 이것들이 **(ㄱ)의 답이 오기 전에** 산출됐다 — 답이 「ⓑ 는 `#133` 이 진다」면 950/800/150 이 통째로 폐기된다 | `intent.md:132` · `3-thesis.md:9,95,113` | 참 | 미관 |
| 11 | **좌표 오기** — `:19` 가 `ScopeChain.refs` 의 자리로 `scope.rs:163-169` 를 댄다. 그 범위는 `LocalRef` 의 필드이고 `ScopeChain.refs` 는 `scope.rs:245` 다. 논지에는 안 걸리나 이 문서는 한 글자도 안 고치고 원장에 실린다 | `crates/pal-core/src/scope.rs:162-169` 대 `:242-246` · `3-thesis.md:19` | 참 | 미관 |
| 12 | **정이 「없다」고 한 결박 좌표원이 저장소 안에 하나 더 있다** — `:135` 는 25 건짜리 `bindings-before-export.jsonl.txt` 만 들고 「못 쟀다」로 넘겼는데, 같은 디렉터리의 `intent-export.jsonl.txt` 와 저장소 정본 `.palimpsest/intent/bindings.jsonl` 은 **30 건 전수**를 싣는다. 다만 셋 다 `watch` 에 심볼 해시만 있고 파일 경로가 없어 **사정권 표는 여전히 못 선다** — 결론은 안 바뀌고 「무엇을 봤나」의 기술만 틀렸다 | `.palimpsest/intent/bindings.jsonl`(30 행) · `observations/intent-export.jsonl.txt`(binding 30) · `3-thesis.md:135` | 참 | 미관 |

## 반이 스스로 물린 것

- **「992 는 사실 해소 가능한 자리가 섞였다」를 세우려 했고 실패했다.** 독립 정규식 실측이 정의 귀속을 뒷받침하는 쪽이었다. 남은 것은 *"상위 꼬리 다섯이 195 자리(20%)뿐인데 `:87` 이 992 전체를 단정한다"* 는 문면 강도 문제인데, **반론으로 안 낸다.**
- **「`scope_variants` 는 CI 에 안 걸리는 예제라 ② 의 해악이 과장이다」를 세우려 했고 좌표를 못 댔다.** 워크플로 파일 훑기가 실패했고, 그 계기가 어느 검사에 물려 있는지 확인 못 했다. 근거 없이 낼 수 없어 접는다.
- **「(다2)가 `표면`(`rust.rs:417`)을 건드려 `D1` 결박이 또 움직인다」를 세우려 했고 못 세웠다.** `표면` 은 `ExportSet`·`ImportSet` 산출이고 참조 수집은 `rust_scopes.rs`·`scopes.rs` 라 별개다. 정의 반증 목록 6 이 이미 같은 자리를 열어 두었다.
