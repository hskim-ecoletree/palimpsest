# 반(反) — 판 1 반론표

> `pal-decision-opponent` 가 냈다. **파일은 그 자리가 안 썼다** — 읽기 전용이라 메인이 원 반환문을 그대로 옮겼다.
>
> **안 받은 것**: 대화 기록 · 메인의 사고 과정 · 앞 라운드 반론 · `dialectic/1-design.md`(경계로 안 열었다) · `findings.jsonl` 의 `처분`·`사전처분`·`조건변경` 칸(안 열었다). 이 호출에 사고 과정이나 대화 기록은 섞여 오지 않았다.
> **받은 것**: 판정 초안 전문 · `intent.md` · `premortem/r1-raw.md` · 저장소.
> 측정 시각 2026-09-08 22:24~22:47 · 바이너리 `pal 0.0.0+2ebda8aace23`(22:24 재빌드본 — 초안이 쓴 `6b6cb6ddb837` 과 **다른 바이너리**다) · 워크트리가 측정 중 `6f8ec78` → `99a3630` 으로 움직였다.

측정을 전부 다시 돌렸다. 결과부터: **초안의 수는 한 자리도 안 갈렸다.** 무너진 것은 수가 아니라 처분 논증과 선택지 배제 근거다.

## 초안이 서는 자리

무너뜨리지 못했다 — 그리고 **다른 바이너리로 쟀는데도** 안 갈렸다.

- **M1~M6**: `723 · 266 · 191 · 82 · 51 · 9`. 여섯 다 일치.
- **M16 갈래 표 열 갈래 서른 칸 전부 일치.** 나는 초안의 방법을 안 보고 독립 스크립트로 다시 세웠다(`use` 문 바이트 범위 ↔ `ScopeBinding.declared_at` 포함) — `A1 174/1110/46 · A2 49/0/0 · A3a 21/143/11 · A3b 22/251/16 · B 191/1045/87 · C0 80/1708/50 · C1 2/2/2 · D 26/313/24 · E 51/3/1 · F 107/683/46 · 합계 723/5258`. 갈래끼리 안 겹치는 것도 검산했다(`pub use crate::`·`pub use std::`·`pub use pal_` 이 각각 0 건).
- **균일 경로 26 줄**: 독립 규칙(같은 파일의 `mod <뿌리>;` 선언과 대조)으로 다시 세어 **26 줄 정확히 일치**. 좌표도 같다 — `install.rs:48,49,54` + `tests/` 23 파일. `install.rs:46 pub use doctor::…` 도 균일 경로지만 E 로 가는 것이 맞다.
- **모집단 315**: `D 313 + C1 2 = 315`. `install.rs` 몫이 정확히 **100**, 나머지 213 이 시험 23 파일. **PM1-02 의 머리 주장(「공집합이다」)은 무너진다.** `crates/pal-intent/src/lib.rs:35 pub mod round_condition;` 도 확인 — C1 은 재수출을 안 지난다.
- **`pal query symbol.callers Capable` → `{"outcome":"symbols","symbols":[]}`** 재현. `Capable` 은 `projection.rs` 에서 14 번 참조된다(재현). `symbol.reaches Capable` 도 2(둘 다 `capable.rs`).
- **M8 · M10 완전 재현**: `336/87/55` 와 `outside_file` 이름 14 종 전량 · `doctor` 축 여덟과 「담지 못하는 자리」 문자열 전부.
- **M9/PM1-10 닫힘 검산**: 산 저장소 30(`fresh` 26 · `stale` 4) ↔ `bindings.jsonl` 30, 차집합 **양쪽 다 공집합**. 초안이 옳다.
- **§2.2 의 부분 주장**: `pal symbols crates/pal-core/src/lib.rs --graph --json` 에서 `pub use` 51 줄이 만든 것은 `exports.names` 문자열뿐이고 심볼 노드는 34 개 모듈뿐이다. 확인.

## 반론

| # | 반론 | 좌표 | 유효성 | 해악도 |
|---|---|---|---|---|
| 1 | **초안이 「이미 고쳐져 있다」고 적은 문장이 안 고쳐져 있다.** `intent.md:46` 은 `2ebda8a`·`HEAD`·워크트리 셋 다 *"**결박 25 건**의 낡음이 움직이거나"* 다. 30 이 아니다 — 초안 §0/M9 의 *"`intent.md:46` 은 이미 30 으로 고쳐져 있다"* 는 거짓이고, 같은 문단의 *"PM1-09 는 여전히 참"* 과도 자기모순이다. 그 줄은 회차의 **멈추고 올리는 자리**의 기준선이다 | `intent.md:46` · `git show 2ebda8a:…/intent.md \| sed -n '46p'` · `dialectic/1-thesis.md:92` | 참 | 금지역 |
| 2 | **㉰ 를 뺀 유일한 근거가 실측과 어긋난다.** 초안은 *"재수출 원본을 실을 자리는 `ImportSet` 이 아니라 `ExportSet` 이다 → 한 칸을 넘는다"* 로 ㉰ 를 뺐다. 그러나 `pal symbols crates/pal-core/src/lib.rs --graph --json` 의 `imports.present.modules` 에 재수출 **원본 모듈 34 개**(`capable`·`coord`·`repo`…)가 **오늘 이미 실려 있다.** 빠진 것은 (모듈, 항목) 짝뿐이고 그것은 `intent.md:16` 이 이미 잠근 「`ImportSet` 에 이름」 칸이다. ㉰ 의 모집단은 1708 로 ㉯ 의 1253 보다 크다 | `dialectic/1-thesis.md:290` · `crates/pal-core/src/lib.rs:48-141` · `pal symbols crates/pal-core/src/lib.rs --graph --json` | 참 | 거짓신호 |
| 3 | **㉯ 의 이름표와 ㉯ 의 실측이 다른 것을 잰다.** ㉯ 는 「`crate::`·`super::`·`self::` **경로 해소**」인데 초안은 **`use` 문에 걸린 참조**만 쟀다. 참조 자리의 `crate::`/`super::` 경로는 주석 뺀 코드에서 **270 곳 · 41 파일**이고, 오늘 추출기는 거기서 **참조를 하나도 안 낸다** — `crates/pal-cli/src/doctor.rs:117` `crate::install::checks(…)` 에 `install`·`checks` 어느 ref 도 없고(`refs` 375 개 중 `checks`·`evidence`·`label` 0 개), `:125`·`:185` 의 `install` ref 5 개는 전부 `:116` 의 **지역 변수** `let install =` 에 결박된다. 그 갈래를 넣으면 `ImportSet` 밖이 움직여 ㉯ 의 3 번 칸(「안 움직인다」)이 무너진다 — 그 칸이 ㉯ 를 고른 결정적 근거다 | `dialectic/1-thesis.md:279,353` · `crates/pal-cli/src/doctor.rs:97,117,125,185` · `pal symbols crates/pal-cli/src/doctor.rs --graph --json` | 참 | 거짓신호 |
| 4 | **초안이 잠긴 의도의 한 문단을 한 번도 안 인용한다 — 그 문단이 처분을 바꾼다.** `intent.md:81-83` 은 *"잠근 범위는 ⓐ 와, **`use` 로 들어온 이름에 한한 ⓑ**(`S::foo()` 경로 해소) 다"* 로 **경로 해소의 일부를 이미 범위 안**으로 적는다. 초안은 `intent.md:62`(`## 범위 밖`) 한 줄만 읽고 「확대」를 세웠다. :62 와 :81-83 이 어긋나면 :62 는 **의도를 못 재는 문장**이고, `SKILL.md:322` 로 그것은 확대가 아니라 **정정**이다 | `intent.md:62` vs `intent.md:81-83` · `dialectic/1-thesis.md:382` (인용 목록에 :81-83 없음) | 참 | 거짓신호 |
| 5 | **초안이 규약의 자를 반쪽만 쓴다.** `SKILL.md:329` 는 *"**완수 조건을 고칠 때도** 같은 자로 잰다 — 늘었으면 정정, 줄었으면 완화"* 다. 초안은 ⓐ 완수 조건이 아직 없다는 이유로 **정정을 배제**하면서(:381) ⓑ 같은 문장의 자를 **범위** 변경에 적용해 축소·완화를 배제한다(:372). 자가 서면 결론은 「정정」이고, 자가 안 서면 축소 배제의 근거가 사라진다. 둘 다는 못 취한다. 초안은 자의 전건(늘었다)만 쓰고 후건(정정)을 조용히 버렸다 | `.claude/skills/round/SKILL.md:329` · `dialectic/1-thesis.md:372,381` | 참 | 거짓신호 |
| 6 | **확대의 필요 조건을 초안 자신의 §2.1 이 무너뜨린다.** `SKILL.md:323` 확대 = *"…원 의도 **충족에 필요**하다"*. 그런데 §2.1(:185)이 원 의도의 모집단을 **315 자리 · 25 파일**로 세워 「0 이 아니다」를 증명했다 — 즉 원 의도는 ㉯ 없이 닫힌다. §4.3(:382)이 댄 필요 근거는 *"`Capable` 이 오늘 `[]`"* 인데 그것은 **㉯ 가 여는 자리가 비었다**는 말이지 **원 의도가 못 닫힌다**는 말이 아니다. PM1-02 를 깬 문단이 확대의 전제도 같이 깬다 | `dialectic/1-thesis.md:185` vs `:382` · `.claude/skills/round/SKILL.md:323` | 참 | 거짓신호 |
| 7 | **자의 정당화가 자기를 문다.** §4.2(:359)는 *"`03-shortest-path.md:123` 의 2 단계 **합격선**(「호출자 7 (exact 5 · candidate 2)」…)이 재는 것도 같은 양"* 이라 적는데, 그 문구는 그 행의 **「왜 이 자리인가」 칸**이고 **합격선 칸**은 *"§2 의 표에서 우선순위 1 행이 전부 값을 돌려준다"* 다. 그리고 같은 문단이 「§2 표의 우선순위 1 행 수」를 *"눈금이 두 칸뿐"* 이라며 자로 **기각한다.** 실제 합격선을 자로 못 쓴다고 버리고, 자기 자를 그 합격선으로 정당화한다 | `docs/plan/03-shortest-path.md:123`(4 열 표의 3 열 vs 4 열) · `dialectic/1-thesis.md:359` | 참 | 거짓신호 |
| 8 | **㉮ 의 2 번 칸이 실제로는 안 채워진다.** 초안이 ㉮ 의 합격선으로 제시한 `pal touch pal` 은 오늘 `pal query symbol.callers pal` 이 `{"outcome":"ambiguous"}` 를 돌려준다 — 같은 이름이 `crates/pal-cli/tests/common/mod.rs:128` 과 `crates/pal-cli/tests/install_stays_inside.rs`(container `방`) 등에 여러 개다. 「호출자 ≥ 1」로 판정할 수 없는 이름을 증인으로 세웠다 | `dialectic/1-thesis.md:267` · `pal query symbol.callers pal --json` | 참 | 미관 |
| 9 | **「재빌드는 측정값을 못 바꾼다」가 한 번 반증됐다(비재현).** 초안 §0(:35)은 ④ 미실행을 그 문장으로 정당화한다. 이 자리의 **첫** `pal export --format cypher` 는 같은 워크트리에서 `File 0건 · REFERENCES 0건` 을 산출하면서 동시에 `Snapshot palimpsest@6f8ec78+worktree`·`2층 심볼 3205 색인됨` 을 적었다(바이너리 재빌드 22:24, `.palimpsest/index.redb` mtime 22:39). 두 번째 실행부터 4258 로 안정됐고 **재현하지 못했다** — 병렬 호출과 색인 재구축이 섞인 교란을 못 갈랐다 | `dialectic/1-thesis.md:35` · 반이 돌린 첫 `./target/release/pal export --format cypher \| grep -c REFERENCES` | 추정 | 거짓신호 |
| 10 | **㉯ 만 「모듈 지정자 → 파일 사상」을 새로 요구한다고 적은 것이 틀렸다.** ㉮ 의 D 갈래도 같은 것을 요구한다 — `use common::{git, pal}` 는 `mod common;` → `crates/pal-cli/tests/common/mod.rs` 사상 없이 못 푼다. ㉯ 의 비용이 실제보다 크게, ㉮ 가 작게 적혔다(초안이 고른 값에 **불리한** 쪽 오류다) | `dialectic/1-thesis.md:280` vs `:266` · `crates/pal-cli/tests/round_status.rs:3,8` | 참 | 미관 |
| 11 | **잠긴 의도 자신이 「넷」이라 적는데 초안은 「일곱 칸」을 분모로 쓴다.** `intent.md:11` 은 *"인터뷰에서 잠근 것 **넷**"* 인데 :13-19 는 **일곱 줄**이다. 초안의 *"일곱 칸 중 여섯이 안 움직인다"*(:378)가 어느 수를 소유자가 잠근 것으로 세는지 안 밝힌다 | `intent.md:11` vs `intent.md:13-19` · `dialectic/1-thesis.md:378` | 참 | 미관 |

## 반이 스스로 물린 것

- **M1~M6 이 사전부검(717·273·180·7)과 갈린 이유를 반도 못 밝혔다.** 초안이 §6.2 에서 *"반(反)은 M1~M6 을 그대로 다시 돌리는 것부터 하면 된다"* 고 손잡이를 줬고, 돌렸고, **초안 쪽 수가 나왔다.** 사전부검 쪽이 옳다는 반론을 못 세운다.
- **M16 의 갈래 표를 깎으려고 독립 구현을 썼는데 서른 칸이 전부 일치했다.** 315·1253·1568 을 못 움직였다.
- **「균일 경로 26 줄」을 무너뜨리려 했다 — 못 했다.** 에디션 2024 에서도 균일 경로가 살아 있고, 저장소가 실제로 빌드된다.
- **「`use common::…` 은 통합 시험이라 별개 크레이트이고 파일 간이 아니다」로 세우려다 물렸다.** 사이퍼 산출에 `pal-cli/tests` 가 494 줄, `tests/common/mod.rs` 가 14 줄로 실려 있다 — 그래프 안에서 파일 간이 맞다.
- **PM1-10 닫힘을 뒤집으려 했다 — 못 했다.** 차집합이 양쪽 다 공집합이다.
- **초안이 §6 에서 스스로 적은 자리는 반론으로 안 세운다** — 「소유자 답을 승격 없이 넓힌다」(§6.1) · 「315 가 두 덩어리에 몰려 있다」(§6.5, 실측으로 100 + 213 이었다) · 「A3b 251 을 들여쓰기로만 갈랐다」(§6.4) · 「M16 의 참조는 엣지가 아니다」(§6.3) · 「4,259 vs 4258」(§0·§7). 새롭지 않으므로 유효성 거짓이다.
- **반론 2 는 초안 §6.6 이 가설로 이미 이름 붙인 자리다.** 새로운 것은 그 가설을 **닫는 실측**(`imports.modules` 에 원본 모듈 34 개가 오늘 실려 있다)이다. 그 구별을 적어 둔다.
- **`pal --version` 이 초안과 다른 바이너리(`2ebda8aace23`)였다는 것을 반론으로 안 세운다** — 오히려 초안의 §0 코드 동일성 주장을 **강화하는** 결과가 나왔다(다른 바이너리로 쟀는데 수가 같다).
