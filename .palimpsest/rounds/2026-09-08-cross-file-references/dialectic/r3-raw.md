# 정반합 취합 — 2026-09-08-cross-file-references 판 3 · 라운드 1 · `use` 로 들어온 이름에 한한 `S::foo()` 를 이 회차가 파일 간 엣지로 세우는가

> 산출: `pal-debate-reporter`(취합 보고) · 회차 `2026-09-08-cross-file-references` · **판 3** · 라운드 **1**(상한 2 중 1 을 썼다) · 2026-09-09 · 커밋 `74e3a29`
>
> **내가 받은 것** — 판 3 의 산출물 다섯 전문:
> `dialectic/3-design.md`(설계 · `pal-debate-designer`) ·
> `dialectic/3-thesis.md`(정 · `pal-decision-proposer`) · `dialectic/3-antithesis.md`(반 · `pal-decision-opponent`) ·
> `dialectic/3-synthesis.md`(합 · `pal-decision-synthesizer`) · `dialectic/3-referee.md`(종료 판단 · `pal-debate-referee`).
> 그리고 회차 이름 · 라운드 번호 · 발견 원장 스키마(`python3 .claude/skills/round/bin/record.py --schema`) ·
> 이 판정이 닿는 조건의 원문(`intent.md` 의 `A7`·`A7-a`·`E2`·`E2-a`·`E3-b`·`E4`·`D1`·`D3`).
>
> **내가 안 받은 것** — 대화 기록 · 메인의 사고 과정 · **이 판정을 실행할 계획**(코드를 어떻게 고칠지).
> 지시대로 `1-*.md`·`2-*.md`·`r1-raw.md`·`r2-raw.md`·`premortem/`·`conditions-audit/`·`state.md` 를 **판정 근거로 안 읽었다**
> (`r1-raw.md`·`r2-raw.md` 는 **형식을 맞추기 위해서만** 열었다 — 지시가 그 둘의 형식을 따르라고 했다).
> `intent.md` 와 `findings.jsonl`(`BL3-02` 한 행)과 `observations/a7-denominator-preregistration.md` 머리말은 **계수 대조와 좌표 확인**을 위해 열었다.
>
> ⚠ **나는 읽기 전용이고 판정을 다시 하지 않았다.** 아래 판정 문장 · 반론 문장 · 근거 좌표는 원 산출물에서 **그대로** 옮긴 것이다. 줄이지 않았다.
> **원 산출물이 길어서 이 문서가 길다** — 반(反)이 12 행 표에 반론을 실었고 합(合)이 채택 12 행 · 기각 4 행 · 고침 여덟 · 물음 일곱 답을 각각 여러 줄로 적었다.
>
> ⚠ **모집단 축을 원 산출물이 한 자리도 명시하지 않았다.** 정·반·합·종료 판단 어디에도 `모집단:` 이라는 칸이 없다.
> **그러므로 열다섯 항의 모집단은 전부 취합자인 내가 배정했고, 항마다 그 사실을 적었다.**
>
> ⚠ **해악도는 반(反)의 표와 합(合)의 표에서 그대로 옮겼다.** 다만 **항 13·14·15 는 원 산출물에 해악도 칸이 없어 취합자가 배정했고**, 그 셋은 종료 판단의 분포 표에 **안 들어간다**(그 표는 합(合)의 채택 표 열두 행만 센다).
>
> ⚠ **`경로` 칸의 두 뜻** — 원장의 `경로` 는 「발견이 관측된 자리」와 「처분이 닿는 자리」 둘로 읽힌다. **이 판은 처분이 소유자에게 올라가 판정 반영 커밋이 없다.** 그래서 이 문서의 `어디가 걸리나` 는 전부 **관측된 자리**이고, 회차 안 파일은 저장소 뿌리 기준 전체 경로로 폈다(반(反)이 적은 축약 좌표는 ⟨⟩ 안에 원문 그대로 남겼다).
>
> ★ 저장소 안의 파일은 마크다운 링크가 아니라 코드 표기로 적었다(#134).

**항은 번호로 센다 — 이 문서의 항은 `### [항 N]` 으로 세어 총 15 개다.**

---

## 판정

합(合)(`dialectic/3-synthesis.md`)의 판정 문장 그대로:

> ## 판정
>
> **수정** — 갈래는 **(다2)** 로 서지만 정의 초안은 여덟 자리를 고쳐야 하고, **소유자에게 올린다.**

그리고 같은 문서의 `## 물음 일곱에 답한다` 전문 그대로:

> **1. 갈래 — (다), 그중 (다2).** (가)는 기각한다 — `scope_variants` 가 V7 을 「기준에서 끈 것」으로 정의하고 `실패 += 1` → `exit(1)` 로 가며, `docs/gates/rust-scope-references.md:67` 의 통과 목록에 `A7`·`V7` 이 둘 다 실려 있다. **앞 회차가 닫은 게이트를 이 회차가 자기 손으로 연다.** 게다가 (가)는 잠근 축2 밖의 자리를 함께 들이므로 확대이고, 확대도 승격이다. (나)는 축소이고 승격이다. **반의 ①②는 (다2)의 비용을 바꾸지만 갈래를 안 바꾼다.**
>
> **2. 채운 것인가 조용한 축소인가 — 규칙 층위는 채움, 자리 층위는 「아직 못 정한다」.**
>
> ★★ **그 못 정함의 정체를 정도 반도 안 짚었다.** 정의 근거 ④ 는 세 수를 *"같은 것을 세는 다른 수 셋"* 이라 부르는데 **셋은 같은 것을 안 센다.** `rust.rs:30` 의 4,217 은 「경로 **호출**(`S::new()` 의 `new`)」이고 8,145 는 「경로 **꼬리 전부**」다. 잠근 의도가 ⓑ 를 적을 때 쓴 표기는 `intent.md:94`·`:175`·`:243` 모두 **`S::foo()`** — 곧 호출 꼴이다. 그러므로 **ⓑ 의 분모가 두 뜻으로 읽히고, 어느 뜻이냐에 따라 (다2)가 축2 를 다 채우는지가 갈린다.** 정도 반도 「임포트 머리 · 경로 호출」 몫을 안 쟀다. **이것이 이 판정의 진짜 미결이다.**
>
> 자리 층위의 992 는 그 자체로 축소가 아니다(L1 등급의 한계다). **다만 게이트·화면·잔여 셋 다에 안 적히면 그때 조용한 축소가 된다.**
>
> **3. `A7-a` — ⓐ 는 800 유지, ⓑ 의 수는 등록을 뒤로 미룬다. 150 은 등록하지 않는다.** 반의 ④⑤ 를 받아들인다. 대신 「미룬다」를 빈칸으로 두지 않고 **구조와 마감**을 등록한다:
>
> - `E2` = ⓐ(임포트 이름 자체) **800 이상** — 분모가 안 움직였으므로 옛 값을 그대로 진다.
> - `E2` 에 ⓑ 항을 **신설하되 수는 미정으로 등록한다.** 그 수는 ⑴ 소유자가 ⓑ 의 분모를 잠근 뒤 ⑵ **엣지가 서기 전에** ⑶ **등록된 계기**(`a7-denominator-preregistration.md` 의 M1~M4 축이지 저장소 밖 정규식이 아니다)의 산출로 정한다. **셋 중 하나라도 없이 끝나면 `E2` 는 통과가 아니라 미측정이다.**
> - `E2-b`(음성 대조) 등록 — ⓑ 하한을 안 두면 ⓑ 엣지가 0 이어도 총합으로 통과한다.
>
> **4. `E4` — 축 셋, 그리고 분모의 산지를 못 박는다.** `cross-file-import` = 능력 있음(값과 분모를 함께). `path-resolution` = 부분 능력 — 「`use` 로 들어온 머리에 한해 센다」 · 「임포트 아닌 머리 · 접두는 안 센다」 · 「꼬리가 대상 심볼이 아닌 자리(변형 이름·연관 상수)는 원리상 못 잇는다」를 **수와 함께** 가른다. `member-resolution` = **범위 밖**(「F07 미구축」이라 적으면 안 된다 — `intent.md:245`).
>
> 반의 ③ 처분 — **채택하고 산지를 지정한다.** 그 분모는 `RefCounts` 에 넣지 않는다(`total() == refs.len()` 이 깨진다). **2 층 산출에서 낸다** — 꼬리를 실은 `pending` 건수(시도)와 그중 엣지가 선 수 / 못 선 수. **그 셋을 세는 자리가 안 서면 화면은 수를 찍지 않고 「안 잼」이라 적는다.**
>
> **5. `E3-b` — 이 판정 뒤, 구현 앞. 층화한다.** `intent.md:173` 원문이 *"엣지가 서기 전에"* 이지 *"판정 전에"* 가 아니다. 층화도 채택한다(ⓐ 20 · ⓑ 10, 각 층을 따로 등분). **단 층별 합격선은 신설하지 않는다** — `E3` 판정은 전체 30 건의 비율 하나로 하고 층별 수는 **보고**로 남긴다. 층마다 합격선을 세우는 것은 `A7-a` 의 권한 밖이고 회차가 빨개질 길을 새로 만든다. `E3-c` 의 검정력 문장은 ⓑ 층에 대해 *"이 층 수로는 비율을 못 잰다 — 거짓 건수를 수로만 적는다"* 로 적는다.
>
> **6. 소유자에게 — 올린다.** 아래 절.
>
> **7. ⑨⑩** — ⑨ 채택. ⑩ 부분채택: `E2-b` 는 소유자 확인 목록으로, ⓑ 하한 150 은 폐기, `E3-b`·`E4` 는 기각.

**판정에 딸린 고침 여덟 — 합(合) `## 초안을 어떻게 고치나 — 여덟` 그대로:**

> 1. **판정문에서 「ⓑ 150」과 「총합 950」을 뺀다.** `E2` 재등록은 「ⓐ 800 + ⓑ(수 미정 · 등록된 계기로 · 엣지가 서기 전에 · 소유자가 분모를 잠근 뒤)」 + `E2-b` 로 다시 쓴다. 573·992·294 는 **판정의 근거가 아니라 「반의 독립 실측과 방향이 같았다」는 정황**으로만 남긴다.
> 2. **(ㄷ)의 어휘를 「안 걸린다」에서 「doctor 팔은 이 판정만으로 안 뒤집힌다 · 결박 팔은 미측정」으로 고친다.** 옆에 `BL3-02` 를 적는다.
> 3. **비용표와 「안 쟀다」에 ①②를 넣는다** — `ScopeChain`/`FileGraph` · `cached.rs` 의 `scopes` 슬롯 · `EXTRACTOR_REV` 2 차 상향 · `f04-verify.py:188`(`D3` 사정권).
> 4. **`E4` 의 ⓑ 분모 산지를 못 박는다.** `RefCounts` 는 쓸 수 없다는 근거(`total() == refs.len()`)를 명시한다.
> 5. **(다2)의 상한을 1,763 으로 쓰는 근거를 한 줄 적는다** — 관측은 1,722 를 (다)의 상한으로 적었고, 그 차 41 은 (다1)에서만 파일 안으로 먼저 붙는 자리다.
> 6. **좌표 정정** — `ScopeChain.refs` 는 `crates/pal-core/src/scope.rs:245`.
> 7. **`E3-b`** — 층화 규칙을 등록하되 층별 합격선은 안 세운다.
> 8. **소유자 물음에 `A7` 문면 개정과 `## 모집단` 표의 ⓑ 행 신설을 명시적으로 싣는다** — 후자는 `intent.md:101` 이 ⟨소유자 2026-09-08 승인⟩ 을 진 표라 행을 더하는 것 자체가 승인 사항이다.

⟨정(正)(`dialectic/3-thesis.md:9`)의 판정 문장은 **"(다)로 간다 — `crates/pal-extract/src/rust_scopes.rs:71` 의 `경로_꼬리_배제` 를 `true` 로 둔 채, 임포트된 머리에 붙은 경로 꼬리를 `PendingImportRef` 에 실어 파일 간 해소기가 대상 파일에서만 찾게 하고, 새 하한 「파일 간 엣지 950 이상 · 그중 ⓐ 800 이상 · ⓑ 150 이상」으로 `E2` 를 다시 등록하며, 이 판정을 승격 조건 (ㄱ)으로 소유자에게 올린다."** 였다. 합(合)이 갈래는 그대로 두고 **「950/800/150」을 뺐다.**⟩

---

## 이 판정이 닿는 조건의 원문 ⟨`.palimpsest/rounds/2026-09-08-cross-file-references/intent.md` · 한 글자도 안 고쳤다⟩

> `:131` — - [ ] A7 **축2 의 ⓑ(`S::foo()`)를 이 회차가 세우는지 세우지 않는지를 판정하고 그 판정을 적는다** — 잠근 축2 가 *"`use` 로 들어온 이름에 한한 ⓑ 는 안"* 이라고 적는데 조건 무리가 그것을 한 번도 안 잰다. **세우면** `crates/pal-extract/src/rust_scopes.rs` 의 `경로_꼬리_배제` 를 끄는 것이고 그 주석이 *"끄면 실측 후보 6,961 이 참조가 된다"* 로 적으므로 `## 모집단` 의 1611·1159 가 통째로 다시 서야 한다. **안 세우면** 그것은 잠근 범위의 한 축을 안 채우는 것이므로 **축소이고 승격이다**
>
> `:132` — - [ ] A7-a `A7` 의 판정이 「세운다」면 `E2` 의 하한을 **다시 등록한다** — 옛 하한 800 은 ⓑ 를 안 담은 분모에서 나왔다
>
> `:156` — - [ ] D1 **결박의 낡음이 움직이면 그 결박이 지는 결정 명제를 다시 판정하고 그 판정을 게이트에 적는다** — 움직인 결박마다 셋을 적는다: ⑴ 감시 심볼의 결정 명제 원문 ⑵ 그 명제를 재는 시험의 이름과 그 시험의 산출(초록·빨강) ⑶ 명제가 아직 참인가의 판정. **셋 중 하나라도 없으면 미측정이다.** 착수 관측은 `fresh` 26 · `stale` 4 였고 2026-09-09 실측은 `fresh` 25 · `stale` 5 다 — 움직인 것은 `1cf63ec5e0e9937a`(`crates/pal-extract/src/rust.rs:417` `표면`) 하나다
>
> `:160` — - [ ] D3 `EXTRACTOR_REV` 를 올린 뒤 **`scripts/f04-verify.py` 가 초록이다** — 그 스크립트는 리터럴 `"f02-rust-scope"` 를 찾아 바꾸는 변이 시험을 돌리고, 치환 대상이 없으면 `어긋남` 을 적고 **비-0 으로 끝난다**(`scripts/f04-verify.py:235-238`). 즉 조용히 꺼지는 것이 아니라 **빨개진다** — REV 를 올리면 그 리터럴도 같이 고쳐야 한다
>
> `:169` — - [ ] E2 **파일 간 엣지가 800 건 이상**이다
>
> `:170` — - [ ] E2-a **음성 대조** — 하한을 0 으로 두면 파일 간 엣지 0 인 오늘이 통과한다
>
> `:173` — - [ ] E3-b **표본 추출 규칙을 엣지가 서기 전에 등록한다** — 정렬 키(`from` 심볼 id 오름차순)와 뽑는 위치(전체를 30 등분해 각 구간의 첫 엣지)를 **미리** 적는다. ⚠ 뽑은 뒤에 좌표만 남기는 것은 **여러 번 뽑고 유리한 판을 남기는 것을 못 막는다**
>
> `:175` — - [ ] E4 `pal touch` 산출을 통합 시험이 문자열로 단언하고, 경고 두 줄이 **각자의 근거를 진다** — 파일 경계 넘기가 서도 `x.foo()` 멤버 해소 경고는 참인 채로 남으므로, 능력 축을 **셋**으로 가른다 — `cross-file-import`(파일 경계) · `path-resolution`(`S::foo()` · 축2 의 ⓑ) · `member-resolution`(`x.foo()` · 범위 밖). ⚠ **둘로만 가르면 `S::foo()` 가 갈 자리가 없다** — `A7` 이 「세운다」로 판정되면 그 축이 서야 하고, 「안 세운다」면 그것이 능력 부재가 아니라 **범위 밖**임을 화면이 말해야 한다

⚠ **취합자가 세지 않은 사실 하나** — 지시가 준 여덟 중 `E2-a` 와 `D3` 는 **정·반·합·종료 판단 어느 문서도 처분하지 않았다.** `E2-a` 는 정이 `E2-b` 신설을 제안하며 같은 형태를 참조했을 뿐이고, `D3` 는 반론 2 와 합의 고침 3 에서 **사정권으로만** 언급된다. 그 사실을 여기 적는다 — 처분을 지어내지 않았다.

---

## 반론 처리 — 전부

### [항 1] 반(反) ① — (다2)의 꼬리에는 산지가 없다 — `file_edges` 가 소스도 꼬리도 못 본다

- **모집단:** 저장소 ⟨원 산출물에 없다 — 취합자가 배정했다⟩
- **유효성:** 참 ⟨반(反) 자평 · 합(合)이 *"좌표 확인함"* 으로 유지했다⟩
- **해악도:** 거짓신호 ⟨반(反) 자평 · 합(合) 표도 거짓신호⟩
- **어디가 걸리나:** `crates/pal-core/src/projection.rs:301-307` · `crates/pal-core/src/scope.rs:162-169` · `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-thesis.md:79` ⟨반(反)의 좌표 칸 원문: `crates/pal-core/src/projection.rs:301-307` · `crates/pal-core/src/scope.rs:162-169` · `3-thesis.md:79`⟩
- **반론 원문 그대로:** **(다2)의 꼬리에는 산지가 없다** — `file_edges` 는 `symbols·nodes·scopes·imports·at` 만 받고 소스도 꼬리도 못 본다. 꼬리를 `PendingImportRef` 에 실으려면 그 값이 먼저 `ScopeChain`(=`LocalRef`) 이나 새 `FileGraph` 칸에 들어와야 하고, 그것은 **추출기 변경**이다. 정은 「`PendingImportRef` 에 꼬리 칸 신설」만 비용으로 적었다
- **채택·기각과 근거(합 그대로):** **채택** — 좌표 확인함. `file_edges(symbols, nodes, scopes, imports, at)` 인자에 소스가 없고, `LocalRef`(`crates/pal-core/src/scope.rs:163-169`)는 `name`·`namespace`·`at`·`resolved` 뿐이라 꼬리 문자열이 실릴 칸이 1 층에 없다. 정의 비용표는 「`PendingImportRef` 에 꼬리 칸 신설」로 적어 **추출기 쪽 신설을 안 적었다**
- ⚠ **같은 번호의 한 몫이 기각 표에도 있다(합 그대로):** ①②(일부) *"이 비용이 (다2)를 (다1)로 물러나게 한다"* → **성립 안 한다.** (다1)은 ①②의 비용을 **똑같이** 지고, 그 위에 ⓐ 기준 엣지 집합이 움직이는 것과 ⓑ 관측 ⓓ 의 41 건이 파일 안 가짜 엣지로 서는 것(금지역)을 **더** 진다

### [항 2] 반(反) ② — 1 의 귀결로 캐시가 반드시 움직인다

- **모집단:** 저장소 ⟨원 산출물에 없다 — 취합자가 배정했다⟩
- **유효성:** 참
- **해악도:** 거짓신호 ⟨반(反) 자평 · 합(合) 표도 거짓신호⟩
- **어디가 걸리나:** `crates/pal-extract/src/cached.rs:6-23,73` · `crates/pal-extract/src/lib.rs:166` · `scripts/f04-verify.py:188` · `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-thesis.md:126` ⟨반(反)의 좌표 칸 원문: `crates/pal-extract/src/cached.rs:6-23,73` · `crates/pal-extract/src/lib.rs:166` · `scripts/f04-verify.py:188` · `3-thesis.md:126`⟩
- **반론 원문 그대로:** **1 의 귀결로 캐시가 반드시 움직인다** — `cached.rs:73` 이 `scopes: Slot<ScopeChain>` 을 싣고 *"F05 의 1패스가 이것을 읽어 파일 내 엣지를 만든다 — 재파싱 없이"* 라 적는다. 곧 꼬리는 **캐시를 통과해야** 하고, 그러면 `EXTRACTOR_REV` 2 차 상향(이 회차가 이미 `f02-rust-scope`→`f07-import-items` 로 한 번 올렸다)과 `scripts/f04-verify.py:188` 리터럴 재수정이 함께 걸린다. 「안 쟀다」 ① 에 이 셋이 없다
- **채택·기각과 근거(합 그대로):** **채택** — `crates/pal-extract/src/cached.rs:73` 이 `scopes: Slot<ScopeChain>` 을 싣고 *"F05 의 1패스가 이것을 읽어 파일 내 엣지를 만든다 — 재파싱 없이"* 라 적는다. 꼬리가 `ScopeChain`/`FileGraph` 를 타면 `EXTRACTOR_REV`(`crates/pal-extract/src/lib.rs:166`, 오늘 `"f07-import-items"`)의 **2 차 상향**과 `scripts/f04-verify.py:188` 리터럴 재수정이 따라오고, 그것은 `D3` 의 사정권이다
- ⚠ **같은 번호의 한 몫이 기각 표에도 있다** — 항 1 의 마지막 불릿과 같은 행(①②(일부))이다.

### [항 3] 반(反) ③ — 정이 요구한 ⓑ 분모가 1 층에 앉을 자리가 없다

- **모집단:** 저장소 ⟨원 산출물에 없다 — 취합자가 배정했다⟩
- **유효성:** 참
- **해악도:** 거짓신호 ⟨반(反) 자평 · 합(合) 표도 거짓신호⟩
- **어디가 걸리나:** `crates/pal-core/src/projection.rs:118-131` · `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-thesis.md:104,107` ⟨반(反)의 좌표 칸 원문: `crates/pal-core/src/projection.rs:118-131` · `3-thesis.md:104,107`⟩
- **반론 원문 그대로:** **정이 요구한 ⓑ 분모가 1 층에 앉을 자리가 없다** — `E4` 표는 *"잰 자리와 못 이은 자리를 수로 함께 찍는다"* 를 요구하는데, 꼬리는 `refs` 가 아니므로 `RefCounts` 에 넣는 순간 *"일곱의 합 = `refs` 의 길이"* 불변식이 깨진다. 그 수를 어디서 낼지를 판정이 안 정했다
- **채택·기각과 근거(합 그대로):** **채택** — `RefCounts::total()` 주석이 *"`refs` 의 길이와 같아야 한다 — 다르면 갈래 하나가 샜다"* 로 불변식을 못 박는다. 꼬리는 `refs` 항목이 아니므로 `RefCounts` 에 칸을 더하면 그 불변식이 깨진다. 정은 그 수의 **산지를 안 정했다**
- **처분(합의 물음 4 그대로):** **채택하고 산지를 지정한다.** 그 분모는 `RefCounts` 에 넣지 않는다(`total() == refs.len()` 이 깨진다). **2 층 산출에서 낸다** — 꼬리를 실은 `pending` 건수(시도)와 그중 엣지가 선 수 / 못 선 수. **그 셋을 세는 자리가 안 서면 화면은 수를 찍지 않고 「안 잼」이라 적는다.**

### [항 4] 반(反) ④ — 사전 등록 규칙을 안 쓰고 판정 계기를 새로 만들었다

- **모집단:** 회차기록 ⟨원 산출물에 없다 — 취합자가 배정했다 · 걸린 자리가 이 회차의 사전 등록 관측 문서다⟩
- **유효성:** 참
- **해악도:** **실패** ⟨반(反) 자평은 거짓신호 · **합(合)이 실패로 올렸다**⟩
- **어디가 걸리나:** `.palimpsest/rounds/2026-09-08-cross-file-references/observations/a7-denominator-preregistration.md:9-10` · `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-thesis.md:5,52-60,94` ⟨반(反)의 좌표 칸 원문: `observations/a7-denominator-preregistration.md:9-10` · `3-thesis.md:5,52-60,94`⟩
- **반론 원문 그대로:** **사전 등록 규칙을 안 쓰고 판정 계기를 새로 만들었다** — 등록문이 *"값이 나온 뒤 규칙을 손보면 여러 번 뽑고 유리한 판을 남기는 것과 같은 형태"* 로 그 자리를 이미 이름 붙였다. 정의 573·992·294 는 등록 계기의 산출이 아니고, 스크립트가 저장소 밖 scratchpad 라 **원장이 재현할 수 없다.** 그런데 그 수가 새 하한 `E2` ⓑ 150 을 세운다
- **채택·기각과 근거(합 그대로):** **채택 · 해악도 실패** — 등록문 `:9-10` 이 *"값이 나온 뒤 규칙을 손보면 여러 번 뽑고 유리한 판을 남기는 것과 같은 형태"* 로 그 자리를 **이 회차가 스스로 이름 붙였다.** 정의 573·992·294 는 등록 계기의 산출이 아니고 스크립트가 저장소 밖이라 원장이 재현 못 한다. 그 수가 `E2` 의 ⓑ 하한 150 을 세운다 — **재현 불가능한 계기로 합격선을 세우는 것**
- **처분(합의 고침 1 그대로):** **판정문에서 「ⓑ 150」과 「총합 950」을 뺀다.** `E2` 재등록은 「ⓐ 800 + ⓑ(수 미정 · 등록된 계기로 · 엣지가 서기 전에 · 소유자가 분모를 잠근 뒤)」 + `E2-b` 로 다시 쓴다. 573·992·294 는 **판정의 근거가 아니라 「반의 독립 실측과 방향이 같았다」는 정황**으로만 남긴다

### [항 5] 반(反) ⑤ — 150 의 깎기가 한 방향으로만 간다

- **모집단:** 회차기록 ⟨원 산출물에 없다 — 취합자가 배정했다⟩
- **유효성:** 참
- **해악도:** 거짓신호 ⟨반(反) 자평 · 합(合) 표도 거짓신호 · 합(合)이 *"④ 와 같은 처분으로 흡수"* 했다⟩
- **어디가 걸리나:** `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-thesis.md:93-94` 대 `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-thesis.md:127` ⟨반(反)의 좌표 칸 원문: `3-thesis.md:93-94` 대 `3-thesis.md:127`⟩
- **반론 원문 그대로:** **150 의 깎기가 한 방향으로만 간다** — `:94` 는 밴드 아래끝 255 에서 다시 40% 를 깎는데, `:127` 은 같은 계기가 **과소일 수도 과대일 수도** 있다고 적는다. 오차가 양방향인데 조정은 합격을 쉽게 하는 쪽으로만 갔고, 총 950 은 800+150 의 산술합일 뿐 독립 근거가 없다. 즉 「측정 전에 정당화된 수」가 아니라 **추정 밴드에서 임의로 깎은 수**다
- **채택·기각과 근거(합 그대로):** **채택** — `3-thesis.md:94` 는 밴드 아래끝에서 40% 를 깎는데 `:127` 은 같은 계기가 과소일 수도 과대일 수도 있다고 적는다. 오차가 양방향인데 조정은 합격을 쉽게 하는 쪽으로만 갔다. ④ 와 같은 처분으로 흡수

### [항 6] 반(反) ⑥ — (다2)의 ⓑ 참조 자리 상한은 1,763 이 아니라 1,722 다 ⟨부분채택 · 부분기각⟩

- **모집단:** 회차기록 ⟨원 산출물에 없다 — 취합자가 배정했다⟩
- **유효성:** 참 ⟨반(反) 자평 · 다만 합(合)은 「1,763 이 틀렸다」 몫을 *"원문 오독이다"* 로 기각했다 — 그 몫에 한해서는 안 선다⟩
- **해악도:** 미관 ⟨반(反) 자평 · 합(合) 표도 미관⟩
- **어디가 걸리나:** `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-thesis.md:66,71,79` · `.palimpsest/rounds/2026-09-08-cross-file-references/observations/a7-denominator.md:27,58-63` ⟨반(反)의 좌표 칸 원문: `3-thesis.md:66,71,79` · `observations/a7-denominator.md:27,58-63`⟩
- **반론 원문 그대로:** **(다2)의 ⓑ 참조 자리 상한은 1,763 이 아니라 1,722 다** — 정 자신이 `:71` 에서 「임포트된 머리 · `Bound` 41 건은 (다2)에서 아예 안 생긴다」고 적는다. 그런데 `:66`·`:79` 는 41 을 포함한 1,763 을 그대로 ⓑ 자리 수·엣지 환산의 분모로 쓴다. 관측은 (다)의 상한을 **1,722** 로 못 박았다
- **채택·기각과 근거(합 그대로) — 채택 몫:** ⑥ (**부분**) 관측 ⓒ 는 (다)의 상한을 1,722 로 못 박았다 → 「1,763 이 틀렸다」는 기각. 그러나 등록된 관측이 명시적으로 다른 수를 적었으므로 **왜 (다2)에서 상한이 갈리는지를 문서가 적어야 한다**
- **채택·기각과 근거(합 그대로) — 기각 몫:** ⑥ (다2)의 ⓑ 상한은 1,722 다 — 정이 자기 `:71` 과 모순된다 → **원문 오독이다.** `3-thesis.md:71` 의 「41 건」은 **가짜 엣지**이지 참조 자리가 아니다. (다2)는 그 자리의 꼬리를 `refs` 에 안 넣으므로 파일 안 해소를 안 거치고, 그 자리는 **여전히 임포트 머리를 따라 대상 파일로 가는 후보다.** 곧 1,722 는 (다1)의 상한이고 1,763 이 (다2)의 상한이다

### [항 7] 반(反) ⑦ — (ㄷ)이 방아쇠의 두 팔 중 하나만 쟀다

- **모집단:** 원의도 ⟨원 산출물에 없다 — 취합자가 배정했다 · 걸린 자리가 잠긴 의도의 방아쇠 문장이다⟩
- **유효성:** 참
- **해악도:** **실패** ⟨반(反) 자평은 거짓신호 · **합(合)이 실패로 올렸다**⟩
- **어디가 걸리나:** `.palimpsest/rounds/2026-09-08-cross-file-references/intent.md:57-59,118` · `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-thesis.md:121,135` ⟨반(反)의 좌표 칸 원문: `intent.md:57-59,118` · `3-thesis.md:121,135`⟩
- **반론 원문 그대로:** **(ㄷ)이 방아쇠의 두 팔 중 하나만 쟀다** — 잠긴 의도의 방아쇠는 *"결박 30 건의 낡음이 **움직이거나** … 축별 판정 문자열이 바뀌면"* 인데, 정은 doctor 팔만 대조하고 결박 팔은 `:135` 에서 「전수로 못 쟀다」고 자백한다. 그 상태의 어휘는 「안 걸린다」가 아니라 **미측정**이다 — 잠긴 의도가 그 어휘를 못 박았다
- **채택·기각과 근거(합 그대로):** **채택 · 해악도 실패** — 방아쇠는 *"결박 … 이 움직이**거나** … 축별 판정 문자열이 바뀌면"* 이고, 소유자 답 ㉯ 가 **사정권 전수 표**를 요구한다. 정은 `:135` 에서 그 표를 못 낸다고 자백하면서 `:121` 에서는 「안 걸린다」로 적었다. `intent.md:118` 이 *"안 켜진 상자는 미측정"* 을 못 박았고, 이 회차의 `## 개정` 마지막 행이 같은 형태를 **금지역으로 정정한 전례**다
- **처분(합의 고침 2 그대로):** **(ㄷ)의 어휘를 「안 걸린다」에서 「doctor 팔은 이 판정만으로 안 뒤집힌다 · 결박 팔은 미측정」으로 고친다.** 옆에 `BL3-02` 를 적는다

### [항 8] 반(反) ⑧ — (ㄷ)의 「안 걸린다」는 기계로 반증될 수 없다

- **모집단:** 자기장치 ⟨원 산출물에 없다 — 취합자가 배정했다 · 걸린 자리가 회차 검사 스크립트다⟩
- **유효성:** 참
- **해악도:** 거짓신호 ⟨반(反) 자평 · 합(合) 표도 거짓신호⟩
- **어디가 걸리나:** `scripts/check-round-doctor.mjs` · `.palimpsest/rounds/2026-09-08-cross-file-references/findings.jsonl` 의 `BL3-02`(`기준커밋 fe75072`) ⟨반(反)의 좌표 칸 원문: `findings.jsonl` 의 `BL3-02`(`기준커밋 fe75072`) · `scripts/check-round-doctor.mjs`⟩
- **반론 원문 그대로:** **(ㄷ)의 「안 걸린다」는 기계로 반증될 수 없다** — 이 회차 원장이 방금 `BL3-02` 로 *"축별 판정 문자열 변화를 재는 기계가 없다 — 사람이 눈으로 댄다"* 를 등록했다. 곧 (ㄷ)이 틀려도 아무 검사도 안 빨개진다. 그 사실이 (ㄷ) 옆에 안 적혔다
- **채택·기각과 근거(합 그대로):** **채택** — `findings.jsonl` 의 `BL3-02`(기준커밋 `fe75072`)가 *"회차의 방아쇠를 재는 기계가 없다"* 를 이미 등록했고 `scripts/check-round-doctor.mjs` 가 그 전이를 통과시킨다. (ㄷ) 옆에 그 사실이 안 적히면 사람 눈이 유일한 계기라는 것이 원장에서 사라진다

### [항 9] 반(反) ⑨ — `A7` 문면 개정을 소유자 물음에 안 실었다

- **모집단:** 원의도 ⟨원 산출물에 없다 — 취합자가 배정했다 · 걸린 자리가 잠긴 의도의 조건 문면과 `## 개정` 표다⟩
- **유효성:** 참
- **해악도:** **실패** ⟨반(反) 자평은 거짓신호 · **합(合)이 실패로 올렸다**⟩
- **어디가 걸리나:** `.palimpsest/rounds/2026-09-08-cross-file-references/intent.md:131,249-263` · `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-thesis.md:21,119` ⟨반(反)의 좌표 칸 원문: `3-thesis.md:21,119` · `intent.md:131,249-263`⟩
- **반론 원문 그대로:** **`A7` 문면 개정을 소유자 물음에 안 실었다** — `:21` 은 *"`A7` 문면 자체가 개정 대상이다"* 로 잠긴 의도를 판정자가 고쳐 쓰는데, `:119` 의 (ㄱ) 물음은 ⓑ 의 충족 범위만 묻고 **그 개정을 안 묻는다.** `## 개정` 표의 기존 행은 전부 소유자 승인이나 승격을 거쳤다
- **채택·기각과 근거(합 그대로):** **채택 · 해악도 실패** — `## 개정` 표는 **모든 행이 소유자 승인이나 승격을 지고 있다.** 정의 판정 (다)는 `A7` 원문의 등식을 부정해야만 성립하므로 그 개정이 **판정의 하중을 진다.** 하중을 지는 개정을 소유자 물음에서 뺀 것은 잠금을 판정자가 조용히 고치는 형태다
- **처분(합의 고침 8 그대로):** **소유자 물음에 `A7` 문면 개정과 `## 모집단` 표의 ⓑ 행 신설을 명시적으로 싣는다** — 후자는 `intent.md:101` 이 ⟨소유자 2026-09-08 승인⟩ 을 진 표라 행을 더하는 것 자체가 승인 사항이다

### [항 10] 반(反) ⑩ — `A7-a` 가 준 권한은 「`E2` 하한 재등록」뿐인데 조건 무리를 더 넓혔다 ⟨부분채택 · 부분기각⟩

- **모집단:** 원의도 ⟨원 산출물에 없다 — 취합자가 배정했다 · 걸린 자리가 `A7-a` 가 준 권한의 범위다⟩
- **유효성:** 참
- **해악도:** 거짓신호 ⟨반(反) 자평은 미관 · **합(合)이 거짓신호로 올렸다**⟩
- **어디가 걸리나:** `.palimpsest/rounds/2026-09-08-cross-file-references/intent.md:132` · `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-thesis.md:9,95,113` ⟨반(反)의 좌표 칸 원문: `intent.md:132` · `3-thesis.md:9,95,113`⟩
- **반론 원문 그대로:** **`A7-a` 가 준 권한은 「`E2` 하한 재등록」뿐인데 조건 무리를 더 넓혔다** — `E2-b` 신설(`:95`) · `E3-b` 를 층화(ⓐ20/ⓑ10)로 재등록(`:113`) · `E4` 축 셋의 화면 문면 확정(`:101-105`). 그리고 이것들이 **(ㄱ)의 답이 오기 전에** 산출됐다 — 답이 「ⓑ 는 `#133` 이 진다」면 950/800/150 이 통째로 폐기된다
- **채택·기각과 근거(합 그대로) — 채택 몫:** ⑩ `A7-a` 의 권한을 넘겼다 (**부분**) → `E2-b` 신설과 「(ㄱ)의 답 전에 산출됐다」만 채택한다. ⓑ 하한 150 은 (ㄱ)의 답이 분모를 바꾸면 통째로 폐기되므로 답 앞에 등록하면 안 된다. `E2-b` 는 하한 재등록에 딸린 것으로 보되 **회차가 빨개질 길을 더하는 것**이라 소유자 확인 목록에 올린다
- **채택·기각과 근거(합 그대로) — 기각 몫 둘:** ⑩(일부) `E4` 축 셋의 문면 확정이 권한 밖이다 → `E4` 원문이 *"`A7` 이 「세운다」로 판정되면 그 축이 서야 하고, 「안 세운다」면 … 범위 밖임을 화면이 말해야 한다"* 로 **A7 판정에 조건부로 걸려 있다.** 등록된 갈래의 실행이지 확장이 아니다 · ⑩(일부) `E3-b` 재등록(층화)이 권한 밖이다 → `E3-b` 의 「전체를 30 등분」에서 「전체」는 **ⓐ 만이던 모집단**의 말이다. 모집단에 ⓑ 행이 더해지면 옛 규칙을 그대로 쓰는 것은 **다른 분모에 옛 규칙을 씌우는 것**이다. 재등록은 이 판정이 강제한 것이다

### [항 11] 반(反) ⑪ — 좌표 오기 · `ScopeChain.refs`

- **모집단:** 회차기록 ⟨원 산출물에 없다 — 취합자가 배정했다 · 틀린 좌표가 실린 자리가 `3-thesis.md` 다⟩
- **유효성:** 참
- **해악도:** 미관 ⟨반(反) 자평 · 합(合) 표도 미관⟩
- **어디가 걸리나:** `crates/pal-core/src/scope.rs:162-169` 대 `crates/pal-core/src/scope.rs:242-246` · `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-thesis.md:19` ⟨반(反)의 좌표 칸 원문: `crates/pal-core/src/scope.rs:162-169` 대 `:242-246` · `3-thesis.md:19`⟩
- **반론 원문 그대로:** **좌표 오기** — `:19` 가 `ScopeChain.refs` 의 자리로 `scope.rs:163-169` 를 댄다. 그 범위는 `LocalRef` 의 필드이고 `ScopeChain.refs` 는 `scope.rs:245` 다. 논지에는 안 걸리나 이 문서는 한 글자도 안 고치고 원장에 실린다
- **채택·기각과 근거(합 그대로):** **채택** — `crates/pal-core/src/scope.rs:245` 가 `pub refs: Vec<LocalRef>` 이고 `:163-169` 는 `LocalRef` 의 필드다
- **처분(합의 고침 6 그대로):** **좌표 정정** — `ScopeChain.refs` 는 `crates/pal-core/src/scope.rs:245`

### [항 12] 반(反) ⑫ — 정이 「없다」고 한 결박 좌표원이 저장소 안에 하나 더 있다

- **모집단:** 회차기록 ⟨원 산출물에 없다 — 취합자가 배정했다⟩
- **유효성:** 참
- **해악도:** 미관 ⟨반(反) 자평 · 합(合) 표도 미관⟩
- **어디가 걸리나:** `.palimpsest/intent/bindings.jsonl`(30 행) · `.palimpsest/rounds/2026-09-08-cross-file-references/observations/intent-export.jsonl.txt`(binding 30) · `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-thesis.md:135` ⟨반(反)의 좌표 칸 원문: `.palimpsest/intent/bindings.jsonl`(30 행) · `observations/intent-export.jsonl.txt`(binding 30) · `3-thesis.md:135`⟩
- **반론 원문 그대로:** **정이 「없다」고 한 결박 좌표원이 저장소 안에 하나 더 있다** — `:135` 는 25 건짜리 `bindings-before-export.jsonl.txt` 만 들고 「못 쟀다」로 넘겼는데, 같은 디렉터리의 `intent-export.jsonl.txt` 와 저장소 정본 `.palimpsest/intent/bindings.jsonl` 은 **30 건 전수**를 싣는다. 다만 셋 다 `watch` 에 심볼 해시만 있고 파일 경로가 없어 **사정권 표는 여전히 못 선다** — 결론은 안 바뀌고 「무엇을 봤나」의 기술만 틀렸다
- **채택·기각과 근거(합 그대로):** **채택** — `.palimpsest/intent/bindings.jsonl` 이 전수를 싣는다. 다만 `watch` 에 심볼 다이제스트만 있고 파일 경로가 없어 **사정권 표는 여전히 못 선다** — 결론은 안 바뀌고 「무엇을 봤나」의 기술만 틀렸다

### [항 13] 합(合)이 스스로 낸 미결 — ⓑ 의 분모가 두 뜻으로 읽히고, 어느 뜻이냐에 따라 (다2)가 축2 를 다 채우는지가 갈린다

- **모집단:** 원의도 ⟨원 산출물에 없다 — 취합자가 배정했다 · 걸린 자리가 잠근 축2 의 표기다⟩
- **유효성:** 참
- **해악도:** 실패 ⟨⚠ **원 산출물에 해악도 칸이 없다 — 취합자가 배정했다.** 근거: 합(合)이 *"이것이 이 판정의 진짜 미결이다"* 로 적었고 종료 판단이 이것을 소유자 물음 1 로 세웠다. 재는 자가 두 뜻인 자리를 판 2 는 「실패(재는 자가 정의돼 있지 않다)」로 적었다. **이 항은 종료 판단의 분포 표(금지역 0 · 실패 3 · 거짓신호 6 · 미관 3)에 안 들어간다** — 그 표는 합(合)의 채택 표 열두 행만 센다⟩
- **어디가 걸리나:** `.palimpsest/rounds/2026-09-08-cross-file-references/intent.md:94` · `:175` · `:243` · `crates/pal-extract/src/rust.rs:30` ⟨합(合)이 댄 좌표 원문: `rust.rs:30` 의 4,217 · 사전 등록 계기의 8,145 · `intent.md:94`·`:175`·`:243`⟩
- **원문 그대로(합 `## 물음 일곱에 답한다` 2):** ★★ **그 못 정함의 정체를 정도 반도 안 짚었다.** 정의 근거 ④ 는 세 수를 *"같은 것을 세는 다른 수 셋"* 이라 부르는데 **셋은 같은 것을 안 센다.** `rust.rs:30` 의 4,217 은 「경로 **호출**(`S::new()` 의 `new`)」이고 8,145 는 「경로 **꼬리 전부**」다. 잠근 의도가 ⓑ 를 적을 때 쓴 표기는 `intent.md:94`·`:175`·`:243` 모두 **`S::foo()`** — 곧 호출 꼴이다. 그러므로 **ⓑ 의 분모가 두 뜻으로 읽히고, 어느 뜻이냐에 따라 (다2)가 축2 를 다 채우는지가 갈린다.** 정도 반도 「임포트 머리 · 경로 호출」 몫을 안 쟀다. **이것이 이 판정의 진짜 미결이다.**
- **원문 그대로(합 `## 합이 못 정한 것` 첫 항):** **ⓑ 의 분모 두 뜻 중 어느 것이 잠긴 뜻인지.** 잠근 표기(`S::foo()`)는 호출을 가리키고 사전 등록 계기는 꼬리 전부를 쟀다. 소유자의 답이 있어야 하고, 또는 **등록된 계기로 「임포트 머리 · 경로 호출」 몫을 한 번 더 떠야** 한다(새 측정이라 이 자리가 못 만든다).
- **채택·기각과 근거:** 합(合)이 **스스로 낸 것**이다(정도 반도 안 짚었다고 합이 명시했다). 종료 판단이 이것을 **소유자 물음 1** 로 세웠고 *"설계문의 승격 조건 (ㄱ)이 걸렸다"* 의 내용이 이것이다.

---

## 내가 기각한 것

**내가 새로 기각한 것은 없다.** 나는 판정을 다시 하지 않았고, 위 열세 항 중 어느 것도 내 판단으로 빼거나 낮추지 않았다.
아래는 **원 산출물이 스스로 기각한 것을 그대로 옮긴 것**이다.

⚠ **이 판에서 「완전히 기각된 반론」은 0 이다.** 합(合)의 `## 기각한 반론` 표는 네 행인데 **네 행이 전부 부분 기각**이고, 그 네 행이 가리키는 번호(⑥ · ⑩ 두 몫 · ①② 한 몫)는 **같은 번호가 `## 채택한 반론` 표에도 실려 있다.** 반론 열둘의 번호가 하나도 빠짐없이 채택 표에 있다.

⚠ **아래 넷은 새 항이 아니다 — 이미 번호가 붙은 항을 가리키는 포인터다.** 그래서 표가 아니라 문장으로 적는다(표로 적으면 계수기가 네 항을 더 세고 원장과 갈린다).

항 6 의 **「1,763 이 틀렸다」 몫** — 기각한 자리: 합(合) `## 기각한 반론` — *"**원문 오독이다.** `3-thesis.md:71` 의 「41 건」은 **가짜 엣지**이지 참조 자리가 아니다. … 곧 1,722 는 (다1)의 상한이고 1,763 이 (다2)의 상한이다"* ⟨나머지 몫은 미관으로 부분채택⟩

항 10 의 **`E4` 축 셋 문면 확정 몫** — 기각한 자리: 합(合) — *"`E4` 원문이 … **A7 판정에 조건부로 걸려 있다.** 등록된 갈래의 실행이지 확장이 아니다"*

항 10 의 **`E3-b` 층화 재등록 몫** — 기각한 자리: 합(合) — *"모집단에 ⓑ 행이 더해지면 옛 규칙을 그대로 쓰는 것은 **다른 분모에 옛 규칙을 씌우는 것**이다. 재등록은 이 판정이 강제한 것이다"*

항 1·2 의 **「이 비용이 (다2)를 (다1)로 물러나게 한다」 귀결** — 기각한 자리: 합(合) — *"**성립 안 한다.** (다1)은 ①②의 비용을 **똑같이** 지고, 그 위에 … 41 건이 파일 안 가짜 엣지로 서는 것(금지역)을 **더** 진다"*

그리고 정(正)의 갈래 (가)를 합(合)이 기각했다 — *"(가)는 기각한다 — `scope_variants` 가 V7 을 「기준에서 끈 것」으로 정의하고 `실패 += 1` → `exit(1)` 로 가며 … **앞 회차가 닫은 게이트를 이 회차가 자기 손으로 연다.**"* ⟨이것은 반론이 아니라 갈래의 처분이라 항으로 안 세웠다⟩

### [항 14] 반(反)이 스스로 물린 것 셋

- **모집단:** 회차기록 ⟨원 산출물에 없다 — 취합자가 배정했다⟩
- **유효성:** 참 ⟨물렸다는 사실이 참이다⟩
- **해악도:** 미관 ⟨⚠ 원 산출물에 해악도 칸이 없다 — 취합자가 배정했다. 판 2 의 같은 자리(자기 물림)를 미관으로 실은 전례를 따랐다⟩
- **어디가 걸리나:** `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-antithesis.md:31-35`(`## 반이 스스로 물린 것`)
- **원문 그대로:**
  1. **「992 는 사실 해소 가능한 자리가 섞였다」를 세우려 했고 실패했다.** 독립 정규식 실측이 정의 귀속을 뒷받침하는 쪽이었다. 남은 것은 *"상위 꼬리 다섯이 195 자리(20%)뿐인데 `:87` 이 992 전체를 단정한다"* 는 문면 강도 문제인데, **반론으로 안 낸다.**
  2. **「`scope_variants` 는 CI 에 안 걸리는 예제라 ② 의 해악이 과장이다」를 세우려 했고 좌표를 못 댔다.** 워크플로 파일 훑기가 실패했고, 그 계기가 어느 검사에 물려 있는지 확인 못 했다. 근거 없이 낼 수 없어 접는다.
  3. **「(다2)가 `표면`(`rust.rs:417`)을 건드려 `D1` 결박이 또 움직인다」를 세우려 했고 못 세웠다.** `표면` 은 `ExportSet`·`ImportSet` 산출이고 참조 수집은 `rust_scopes.rs`·`scopes.rs` 라 별개다. 정의 반증 목록 6 이 이미 같은 자리를 열어 두었다.

### [항 15] 종료 판단이 정정한 사실 — 판 3 의 상한 2 는 **등록된 값**이고, 설계문과 `state.md` 의 「미등록」 진술이 사실과 다르다

- **모집단:** 회차기록 ⟨원 산출물에 없다 — 취합자가 배정했다 · 틀린 진술이 실린 자리가 `3-design.md` 와 `state.md` 다⟩
- **유효성:** 참 ⟨종료 판단이 `intent.md` 의 `## 상한` 넷째 줄을 들어 확정했다. 취합자도 그 줄을 열어 확인했다 — 아래⟩
- **해악도:** 거짓신호 ⟨⚠ **원 산출물에 해악도 칸이 없다 — 취합자가 배정했다.** 종료 판단은 이것을 *"⚠ 상한에 대한 사실 정정"* 으로만 적고 해악도를 안 매겼다. **이 항은 종료 판단의 분포 표에 안 들어간다** — 그 표의 「금지역 0」은 이 항을 세지 않은 값이다⟩
- **어디가 걸리나:** `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-design.md:45` · `.palimpsest/rounds/2026-09-08-cross-file-references/intent.md:37-43` · `.palimpsest/rounds/2026-09-08-cross-file-references/state.md` · `.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/3-referee.md:22-24`
- **종료 판단 원문 그대로:**
  > ## ⚠ 상한에 대한 사실 정정
  >
  > 설계문과 `state.md` 는 *"판 3 의 상한 2 는 잠긴 값이 아니다 … 미등록"* 이라 적지만, `intent.md` 의 `## 상한` 넷째 줄이 지금 **「완수 조건 검증 정반합 판마다 2 라운드」⟨소유자 2026-09-09 추인⟩** 이다. **판 3 의 상한 2 는 등록된 값이고, 따라서 이번 승격은 (ㄹ) 소진이 아니다.** 남은 한 라운드는 쓰이지 않은 채 남는다.
- **설계문이 적은 원문 그대로(`3-design.md:45`):** **상한 2 라운드.** ⚠ **근거를 밝힌다 — 이 값은 잠긴 값이 아니다.** `intent.md` 의 `## 상한` 과 `state.md` 의 표 어디에도 **실행 중 판정 정반합 칸이 없다.** 판 1 에서 설계자가 옆칸을 본떠 임의로 세운 것이 실패급으로 잡혔으므로, 여기서도 **2 는 설계자의 임시값이고 미등록**임을 원장에 그대로 적는다.
- **취합자가 확인한 사실:** `.palimpsest/rounds/2026-09-08-cross-file-references/intent.md:37-43` 을 열었다. 여섯 줄은 인터뷰 **3**(소진) · 사전부검 **2** · 범위 재잠금 정반합 **2** ⟨소유자 2026-09-08 추인 · 판 1 이 그 값을 다 썼다⟩ · **완수 조건 검증 정반합 판마다 2 라운드** ⟨소유자 2026-09-09 추인 · 판 2 가 그 값을 다 썼다⟩ · 완수 조건 설계 평가 **2** · 독립 리뷰 **4** 다. **종료 판단의 정정이 맞다** — 넷째 줄이 실재하고 「판마다」라고 적혀 있다. ⟨판 2 의 취합 보고가 원장에 실은 항(「이 판의 상한 2 는 잠긴 의도에 없는 값이고 설계자가 세웠다」)은 **그 시점에는** 참이었고, 소유자 승격 칸 4·6 의 답이 그 뒤에 칸을 세웠다.⟩
- **채택·기각과 근거:** 종료 판단이 **스스로 낸 정정**이다. 설계문이 이 판의 취합 보고에 *"미등록임을 원장에 그대로 적는다"* 는 빚을 지웠는데, **그 빚을 그대로 갚으면 원장에 거짓이 실린다.** 그래서 설계문의 문장이 아니라 **종료 판단의 정정을 싣는다.**

---

## 계수 — 두 원천 대조

★ **맞춰 적지 않았다. 갈린 자리는 갈렸다고 적는다.**

| 잰 것 | 반(反) | 합(合) | 같나 |
|---|---|---|---|
| 반론 수 | **12** (`## 반론 열둘` 표에 번호 1–12) | **12 번호 전부**가 `## 채택한 반론` 표에 있다 | **같다** |
| 처분 표의 행 수 | — | **16 행** = 채택 **12** + 기각 **4** | **갈린다** — 반론은 12 건인데 처분 표는 16 행이다. ⑥ 과 ⑩ 이 두 표에 함께 실리고, ⑩ 은 기각 표에 **두 행**이며, ①② 가 기각 표 한 행에 묶여 있다 |
| 완전히 기각된 반론 | **0** 자평 | **0** — 기각 표 네 행이 전부 **부분**이다 | **같다** |
| 부분채택 | — | **2**(⑥ · ⑩) | 합(合)이 표에 `(**부분**)` 으로 명시했다 |
| 반(反) 자평 해악도 | 거짓신호 **8**(#1·2·3·4·5·7·8·9) · 미관 **4**(#6·10·11·12) | 실패 **3** · 거짓신호 **6** · 미관 **3** | **갈린다 — 네 자리에서** ④⑦⑨ 를 거짓신호→**실패**로, ⑩ 을 미관→**거짓신호**로 **올렸다. 낮춘 것은 없다** |
| 유효성 | **참 12/12** | 표에 **유효성 칸이 없다 — 안 셌다** | **갈린다(구조상)** — 다만 합(合)은 ⑥ 의 「1,763 이 틀렸다」 몫을 *"원문 오독이다"* 로 적었다 |
| 금지역 | **0** | **0** | **같다** |
| 종료 판단이 받아 적은 분포 | — | 금지역 **0** · 실패 **3**(④·⑦·⑨) · 거짓신호 **6**(①②③⑤⑧⑩부분) · 미관 **3**(⑪⑫⑥부분) | 합(合)의 채택 표와 **같다**(종료 판단이 *"`## 채택한 반론` 표의 `해악도` 칸을 그대로 센다"* 라 적었다) |
| **메인이 종료 판단에 준 분포** | — | **열**(실패 3 · 거짓신호 4 · 미관 3) | **갈린다** — 종료 판단이 직접 적었다: *"메인이 준 분포는 **열(실패 3 · 거짓신호 4 · 미관 3)**인데 합의 채택 표는 **열둘**이다. 차이는 부분채택 둘(⑩ · ⑥)과 거짓신호 두 건의 귀속이다. **가르는 값인 실패 셋과 금지역 0 은 두 셈에서 같다** — 답은 이 차에 안 걸린다."* |
| 반(反)이 스스로 물린 것 | **3** | 안 셌다 | **갈린다** — 합(合)의 표에 그 축이 없다 |
| 반(反)이 「무너뜨리지 못한 것」으로 적은 것 | **4** (`## 초안이 서는 자리`) | 안 셌다 | **갈린다** — 합(合)의 표에 그 축이 없다 |
| 합(合)이 스스로 낸 것 | 0 (반은 낼 자리가 아니다) | **1** (항 13 · ⓑ 분모의 두 뜻) | 갈린다(구조상) |
| 초안을 고치라는 항목 수 | — | **8** (`## 초안을 어떻게 고치나 — 여덟`) | — |
| 소유자에게 올릴 물음 | — | 합(合) **4**(분모 하나 + 딸린 셋) · 종료 판단 **5** | **갈린다 — 그리고 종료 판단이 그 차를 스스로 적었다**: *"1~4 는 `3-synthesis.md` 의 `## 소유자에게 — 올린다` 가 이미 실은 물음이다. **5 는 종료 판단의 물음이다**"* |
| 쓴 라운드 / 상한 | — | **1 / 2** — 종료 판단: *"상한은 남아 있으나(1/2 미사용)"* | **설계문은 2 를 「미등록」이라 적었고 종료 판단이 「등록된 값」이라 정정했다 → 갈린다**(항 15) |
| 이 문서의 항 수 | — | — | **15** (항 1–15 · 원장 레코드는 메인이 넣는다) |

⚠ **취합자가 세지 않은 것:** 정(正)의 `## 이 초안이 틀렸다면 무엇이 먼저 드러나나`(**여섯**)와 `## 정이 확인 못 한 것`(**다섯**), 합(合)의 `## 합이 못 정한 것`(**넷**), 반(反)의 `## 초안이 서는 자리`(**넷**)는 **발견이 아니라 미측정 신고이거나 반증 실패 보고**라 항으로 안 세웠다. 아래 절에 옮겨 적는다.

---

## 항이 아닌 것 — 반(反)이 못 무너뜨린 것 · 정(正)이 확인 못 한 것 · 합(合)이 못 정한 것

**발견이 아니라 미측정 신고라 항으로 안 세웠다. 그대로 옮긴다.**

**반(反) `## 초안이 서는 자리 — 반이 무너뜨리지 못한 것 넷`:**

- **② 앞 회차 게이트가 (가)에서 빨개진다** — 실재한다. `crates/pal-extract/examples/scope_variants.rs:419` 가 V7 을 「기준에서 끈 것」으로 정의하고, `:454` 가 `if !엣지가_다른가 { 실패 += 1; }`, `:489` 가 `std::process::exit(1)` 이다. `crates/pal-extract/src/rust.rs:985` 의 `a7_매크로_밖_경로_꼬리도_참조가_아니다` 는 `Self::new()` 의 꼬리 자리가 1 임을 단언하고, `docs/gates/rust-scope-references.md` 의 판정표가 `A7` 과 `V7` 을 둘 다 통과에 싣는다. **인용이 다 맞다.**
- **④ 6,961 재현 실패와 `rust.rs:30` 의 4,217** — 인용 정확하다. 특히 정이 붙인 *"134 파일 기준 · 호출 꼬리만"* 이 `rust.rs:19-22` 의 단서와 정확히 맞다.
- **`PendingImportRef` 의 필드 셋** — `crates/pal-core/src/projection.rs:147-155` 가 `from`·`local`·`at` 셋뿐이고, `:342-346` 이 임포트 참조를 `pending` 으로 넘긴다. 맞다.
- **「992 는 enum 변형·연관 상수라 원리상 엣지가 못 선다」** — 반이 독립 정규식으로 잠근 세 크레이트를 다시 셌다(저장소 안 **1,615** · 밖 **482** — 정의 1,565/467 과 거의 같다). 그중 **타입머리+대문자꼬리가 1,005**, 모듈머리+대문자꼬리는 **32** 뿐이다. 정의 귀속이 대체로 맞고, 반이 노린 오분류 후보는 32 자리에 그친다.

**합(合) `## 합이 못 정한 것`(마지막 라운드이므로 이것이 살아 있는 목록이다):**

- **ⓑ 의 분모 두 뜻 중 어느 것이 잠긴 뜻인지.** 잠근 표기(`S::foo()`)는 호출을 가리키고 사전 등록 계기는 꼬리 전부를 쟀다. 소유자의 답이 있어야 하고, 또는 **등록된 계기로 「임포트 머리 · 경로 호출」 몫을 한 번 더 떠야** 한다(새 측정이라 이 자리가 못 만든다). ⟨이 하나는 항 13 으로 실었다⟩
- **`E2` 의 ⓑ 하한 수.** 등록된 관측은 상한(1,763/1,722)만 주고 실제 해소되는 몫을 안 준다 — 그 몫은 F07 이 서야 나온다.
- **(다2)의 결박 사정권.** `bindings.jsonl` 의 `watch` 가 심볼 다이제스트만 실어 파일 좌표가 없다. **심볼 다이제스트를 좌표로 되푸는 조회**가 서기 전까지 (ㄷ)의 결박 팔은 미측정으로 남는다.
- **`E2` 의 옛 하한 800 이 어떻게 산출됐는지.** 잠긴 의도는 값만 적는다. ⓐ 의 분모가 안 움직였다는 이유로 800 을 유지했으나 800 자체의 근거는 확인 못 했다.

**정(正) `## 정이 확인 못 한 것` 중 위와 겹치지 않는 것:**

- **결박 30 의 감시 심볼 좌표.** `observations/binding-before.txt` 에 파일 경로가 한 줄도 없고 `observations/bindings-before-export.jsonl.txt` 는 심볼 해시만 싣는다(25 건). 그래서 **(다2)가 결박 사정권에 드는지 전수로 못 쟀다.** `## 멈추고 올리는 자리` 가 요구하는 사정권 표는 이 초안이 못 낸다. ⟨⚠ 반(反) ⑫ 가 이 기술을 정정했다 — 항 12⟩
- **사전 등록 계기 자체를 못 돌렸다.** 도구가 저장소 밖이다(관측 머리말). 8,145/6,888/1,763/41 은 **그 관측을 그대로 인용**한 것이고, 정의 수(2,032/1,565/573/294)는 별도의 정규식 근사다. 두 계기를 같은 자로 맞춰 보지 못했다.
- **`E2` 의 옛 하한 800 이 어떻게 나왔는지.** 잠긴 의도는 값만 적는다(`intent.md:169` · `## 비-0 증인`). 1,159 의 69% 라는 것은 역산이지 문서에 적힌 근거가 아니다.
- **`pal` 을 안 돌렸다**(읽기 전용 자리라 색인·파생 저장소를 건드릴 수 있는 명령을 피했다). 그래서 오늘의 `counts.imported` 실측, 파일 간 엣지 0, 축별 판정 문자열은 전부 회차 관측 파일의 값을 인용한 것이다.
- **저장소 밖 대상(`std`·외부 크레이트) 몫 23% 는 정의 계기의 값이다.** 사전 등록 계기는 그 축을 안 갈랐다 — 계기가 재면 다를 수 있고, `E2` 의 ⓑ 하한이 그 비율에 직접 걸린다.

**정(正) `## 이 초안이 틀렸다면 무엇이 먼저 드러나나` 여섯** — 여섯 중 **1 은 반론 ①②가 실제로 쳤고**(항 1·2), **6 은 반(反)이 세우려다 물렸다**(항 14 의 3). 나머지 넷(573 의 정규식 근사 · 잠근 값 1,611 과 정의 실측 불일치 · (ㄱ) 승격이 과하다는 반론 · (가)를 고르고 V7 행을 뒤집으면 된다는 반론)은 **이 판에서 아무도 안 쳤다.** 그 사실만 적는다 — 판정하지 않았다.

---

## 설계문의 `## 무엇이 이 판을 반증하나` 다섯 — 원 산출물이 스스로 댄 것만 옮긴다

⚠ **설계문은 판 2 와 달리 「판이 끝난 뒤 대 본다」를 취합 보고에 지우지 않았다.** 그래서 **취합자는 다섯을 대 보지 않았다** — 대 보는 것은 판정이고, 이 자리는 옮기는 자리다. 아래는 **원 산출물이 그 다섯 중 어디를 스스로 짚었는지**를 그대로 옮긴 것이다.

- **반증 1(수 없는 판정)** — 종료 판단이 직접 적었다: *"설계문의 반증 조건 다섯 중 **첫째(수 없는 판정)**가 부분적으로 서 있고 — `E2` 의 ⓑ 하한이 수 없이 구조와 마감만으로 등록됐다 — 그 수의 산지가 소유자의 분모 잠금이다."*
- **반증 5(라운드 2 가 라운드 1 과 같은 자리를 친다)** — 종료 판단이 직접 적었다: *"반증 조건 **다섯**(*"같은 자리를 다시 치면 라운드를 더 도는 것이 아니라 올린다"*)이 겨냥한 형태가 여기다."*
- **반증 2((다)를 아무도 안 다뤘다)** — 반(反)이 입력 경계에 적었다: *"정 자신이 (가)·(나)·(다)를 표로 세우고 (다)를 (다1)/(다2)로 다시 갈랐다는 사실만 적는다."* 합(合)은 `## 물음 일곱에 답한다` 1 에서 갈래 셋을 각각 처분했다.
- **반증 3(6,961 이 검증 없이 분모로)** — 정(正) 근거 ④ 가 *"`A7` 원문이 인용한 6,961 은 어느 겹에서도 안 나온다 … **판정의 분모는 8,145/6,888 이다**"* 로 적었고, 반(反)이 *"인용 정확하다"* 로 확인했다.
- **반증 4(`E4` 능력 축 셋에 안 닿았다)** — 합(合) `## 물음 일곱에 답한다` 4 가 축 셋의 문면과 분모 산지를 확정했다.

---

## 종료 판단

**답 — 종료 판단자(`dialectic/3-referee.md`)의 문장 그대로: 「소유자에게 올린다.」**

**근거(원문 그대로):**

> **소유자에게 올린다.**
>
> 상한은 남아 있으나(1/2 미사용), 살아남은 **실패 셋이 모두 소유자만 답할 수 있는 한 자리에서 끝난다.** 설계문의 승격 조건 **(ㄱ)이 걸렸다**고 합이 명시했고, (ㄱ)은 *"메인이 혼자 못 닫는다"* 는 조항이다. 라운드를 하나 더 도는 것으로 그 조항이 풀리지 않는다.
>
> 근거 셋:
>
> - **⑨** — 처분이 「`A7` 문면 개정을 소유자 물음에 싣는다」다. `intent.md` 의 `## 개정` 은 모든 행이 소유자 승인·승격을 진다. **자리 안에서 닫히는 항목이 아니다.**
> - **④** — 처분이 「ⓑ 하한 150 폐기 · 수는 미정으로 등록 · **소유자가 분모를 잠근 뒤** 등록된 계기로 정한다」다. **라운드 2 가 낼 수 있는 산출이 아니다.**
> - **⑦** — 처분이 「미측정으로 어휘 정정」이고, 잔여인 결박 팔은 *"심볼 다이제스트를 좌표로 되푸는 조회가 서기 전까지"* 미측정이다. 그 측정이 서서 값이 움직이면 **그때는 (ㄷ)이 발동해 다시 승격이다.**

**라운드 1 뒤 살아남은 해악도 분포(종료 판단자가 `## 채택한 반론` 표의 `해악도` 칸을 그대로 센 표):**

| 해악도 | 수 |
|---|---:|
| 금지역 | 0 |
| **실패** | **3** (④ · ⑦ · ⑨) |
| 거짓신호 | 6 (① ② ③ ⑤ ⑧ ⑩부분) |
| 미관 | 3 (⑪ ⑫ ⑥부분) |

⚠ **이 표는 합(合)의 채택 표 열두 행만 센다** — 이 문서의 **항 13·14·15 는 이 표에 안 들어간다.** 그 셋의 해악도는 취합자가 배정했다(항마다 적었다).

**종료 판단자가 스스로 적은 계수 차(원문 그대로):** 계수 차 하나를 적어 둔다: 메인이 준 분포는 **열(실패 3 · 거짓신호 4 · 미관 3)**인데 합의 채택 표는 **열둘**이다. 차이는 부분채택 둘(⑩ · ⑥)과 거짓신호 두 건의 귀속이다. **가르는 값인 실패 셋과 금지역 0 은 두 셈에서 같다** — 답은 이 차에 안 걸린다.

**상한(원문 그대로):** 설계문과 `state.md` 는 *"판 3 의 상한 2 는 잠긴 값이 아니다 … 미등록"* 이라 적지만, `intent.md` 의 `## 상한` 넷째 줄이 지금 **「완수 조건 검증 정반합 판마다 2 라운드」⟨소유자 2026-09-09 추인⟩** 이다. **판 3 의 상한 2 는 등록된 값이고, 따라서 이번 승격은 (ㄹ) 소진이 아니다.** 남은 한 라운드는 쓰이지 않은 채 남는다.

**가름에 안 쓴 것(원문 그대로):** **어느 갈래가 옳은지에 대한 의견은 안 낸다** — 처분의 귀착점만 읽는다.

---

## 소유자에게 올릴 물음

⚠ **다섯 칸이다. 칸을 유지한 채 올린다 — 묶지 않았다.** 종료 판단자가 표로 냈고, **1~4 는 `3-synthesis.md` 의 `## 소유자에게 — 올린다` 가 이미 실은 물음이고 5 는 종료 판단의 물음이다.**

⚠ **합(合)이 먼저 적은 문장(원문 그대로):** **소유자가 정할 것** — 축2 의 ⓑ 를 「임포트 머리에 붙은 **경로 호출**」로 잠그는가 「임포트 머리에 붙은 **경로 꼬리 전부**」로 잠그는가. 그리고 그 답에 딸린 셋 — `A7` 문면의 「세운다 = `경로_꼬리_배제` 를 끈다」 등식을 개정하는가 · `## 모집단` 표에 ⓑ 행을 더하는가 · 꼬리의 대상이 enum 변형·연관 상수라 심볼이 없는 자리를 이 회차의 미충족으로 세는가 아니면 `#133`(L2)으로 보내는가.

⚠ **합(合)이 「올린다」의 이유로 적은 것(원문 그대로):** **첫째, 살아남은 반론 중 가장 무거운 것이 「실패」다** — ④(재현 불가능한 계기가 합격선을 세운다) · ⑦(방아쇠의 한 팔을 안 잰 채 「안 걸린다」로 적었다) · ⑨(판정의 하중을 지는 잠금 문면 개정이 소유자 물음에서 빠졌다). / **둘째, 설계문의 (ㄱ)이 걸린다.** 결론이 (다)이고, 잠근 축2 를 다 채웠는지가 **ⓑ 의 분모가 두 뜻으로 읽히기 때문에** 오늘 정해지지 않는다. (ㄴ)·(ㄹ)은 안 걸린다. **(ㄷ)은 미측정이다.**

#### 칸 1 ⟨종료 판단 표 1 · 합(合)이 먼저 낸 물음⟩

> 축2 의 ⓑ 를 「임포트 머리에 붙은 **경로 호출**(`S::foo()`)」로 잠그나, 「임포트 머리에 붙은 **경로 꼬리 전부**」로 잠그나?

#### 칸 2 ⟨종료 판단 표 2 · 합(合)이 먼저 낸 물음⟩

> `A7` 문면의 「세운다 = `경로_꼬리_배제` 를 끈다」 등식을 개정하나?

#### 칸 3 ⟨종료 판단 표 3 · 합(合)이 먼저 낸 물음⟩

> `## 모집단` 표⟨2026-09-08 승인⟩에 ⓑ 행을 신설하나?

#### 칸 4 ⟨종료 판단 표 4 · 합(合)이 먼저 낸 물음⟩

> 꼬리의 대상이 enum 변형·연관 상수라 심볼이 없는 자리를 이 회차의 미충족으로 세나, `#133`(L2)으로 보내나?

#### 칸 5 ⟨종료 판단의 물음⟩

> 판 3 의 남은 한 라운드(1/2)를 답 뒤에 쓰게 하나, 판 2 때처럼 라운드를 더 주지 않고 닫나?

**5 는 종료 판단의 물음이다** — 상한이 소진되지 않은 채 승격했으므로 잔여 라운드의 처분이 비어 있다.

⚠ **취합자가 덧붙이는 사실 하나** — 합(合)의 `## 물음 일곱에 답한다` 3 이 `E2` 의 ⓑ 하한을 *"⑴ 소유자가 ⓑ 의 분모를 잠근 뒤 ⑵ 엣지가 서기 전에 ⑶ 등록된 계기의 산출로 정한다. **셋 중 하나라도 없이 끝나면 `E2` 는 통과가 아니라 미측정이다.**"* 로 등록했다. **칸 1 의 답이 그 ⑴ 이다** — 칸 1 이 안 닫히면 `E2` 는 원리상 통과할 수 없다. 두 자리를 묶지 않았다(묶으면 답 하나가 두 물음을 닫은 것처럼 보인다).

---

**저장 위치:** `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-08-cross-file-references/dialectic/r3-raw.md` (취합자가 이 파일을 직접 썼다 — 메인이 옮겨 적으면 글자가 갈리므로. `findings.jsonl` 은 안 건드렸다.)

**추출 확인 요령:** `python3 .claude/skills/round/bin/extract.py 정반합 3 .palimpsest/rounds/2026-09-08-cross-file-references/dialectic/r3-raw.md` 가 **`DL3-01`~`DL3-15` 열다섯 행**을 내야 한다. 이 문서의 `### ` 는 열다섯 개뿐이고, 표는 넷(계수·해악도 분포·조건 인용 없음·처분 인용) 다 `요약` 별칭 열이 없어 항으로 안 세어진다.
