# 상태 — 2026-09-08-cross-file-references

## 지금 단계

**루프(실행) 안이다. 판 2 가 닫혔고 소유자 답을 받아 의도를 다시 잠갔다.** 착수 커밋 `6b6cb6d` 이후 커밋 열여덟 중 코드가 든 것은 둘이다 —
`41f66fa`(검증 스크립트 오타 둘, 착수 기준선을 재기 위한 확대)와 `2cd8f1f`(`ImportSet` 에
임포트 항목 이름을 싣는 자리).

⚠ **이 절은 2026-09-09 에 고쳤다** ⟨실측 `BL2-01`⟩. 앞 판은 *"승인이 끝났고 루프(실행)에
아직 안 들어갔다. 코드는 한 줄도 안 고쳤다"* 로 적혀 있었는데 그 뒤 `2cd8f1f` 이 코드를
넣었고 이 파일만 안 따라왔다. **금지역(사실이 아닌 것을 사실로)이다** — 다음 컨텍스트가
이 문면을 믿으면 이미 선 것을 다시 세운다.

### 지금 서 있는 것과 안 서 있는 것 ⟨2026-09-09 재실측 · 커밋 `fe75072`⟩

⚠ **앞 교대의 문면을 안 믿고 다시 떴다.** 아래 표의 값은 이 교대가 직접 잰 것이고, 앞
교대가 적어 준 수 중 넷이 다시 갈렸다 — `REFERENCES` 4290→**4294** · doctor 축 ② 470→**471**
· ③ 466→**467** · `cargo xtask check` 27→**28**. 갈린 까닭은 아래 ★ 가 적는다.

| | 값 | 어떻게 쟀나 |
|---|---|---|
| 완수 조건 | **38 개 전부 미측정** (`- [x]` 0) ⟨2026-09-09 재잠금으로 `D1-a` 가 늘었다⟩ | `pal round conditions --file <intent.md> --json` → `열림 38 · 닫힘 0 · 형식오류 0` |
| 파일 간 참조 엣지 | **0** — `REFERENCES` 4294 건이 전부 파일 안 | scratchpad 색인으로 `pal export --format cypher` 를 뜨고 `Symbol` 노드의 `path` 를 엣지 양 끝에 조인. **색인을 두 벌 새로 세워 바이트까지 같은 것을 확인했다** — 이 값은 결정적이다 |
| 착수 대비 참조 엣지 | 4259 → **4294** (+35) | 같은 명령 · `observations/references-count.txt` 대조 |
| 결박 | 30 · `fresh` **25** · `stale` **5** — 앞 교대와 같다 | `pal intent import .palimpsest/intent/bindings.jsonl --intent <scratchpad>` 뒤 `pal query binding.status --json` |
| 능력 선언 | `F07 cross-file-resolution` · `F08 unresolved-refs` 가 아직 **못 만든 능력** | `pal query --list` |
| 검사 | `검사 28/28 통과` · `cargo build --workspace` exit 0 | 두 명령의 원문 |
| `pal doctor` 축별 **판정 문자열** | ①②③ `ok` · ④~⑧ `(모집단이 없습니다)` — **착수 관측과 같다. 방아쇠는 안 발동했다** | `pal doctor` 원문 · `observations/doctor-before.txt` 대조 |
| `pal doctor` 축별 **검사 수** | ① 30(착수 30) · ② **471**(467) · ③ **467**(463) · 위반 전부 0 | 같은 명령 |

**이미 선 것 하나** — `2cd8f1f` 이 `crates/pal-core/src/file_graph.rs:130` 의 `ImportSet` 에
`items: Slot<Vec<ImportedItem>>` 를 더했다(`ImportedItem` 은 `module`·`name`·`local` 셋).
그것이 `## 원문` 이 잠근 *"임포트가 항목 이름을 실게 하는 자리 → ImportSet 에 이름"* 이다.
⚠ **그 자체는 아직 아무 조건도 안 닫는다** — 같은 파일 주석이 *"「Rust 임포트가 성립한다」는
**산출까지의 사실**이고 관측 가능한 동작은 아직 없다. 쓰는 것은 파일 간 해소(F07)이고"* 로
적는다. `A1` 은 엣지가 서야 닫힌다.

## 다음 컨텍스트가 받는 것

**잠긴 의도 전문(`intent.md`)과 이 파일뿐이다.** 정반합 산출물·사전부검·조건 감사의
원 반환문을 시드로 주지 마라 — 규약 §5 「교대」가 금한다. 필요하면 발견 원장
(`findings.jsonl`, 157 행 ⟨2026-09-09 실측 · 앞 문면은 123 이었다⟩)을 조회한다.

## 무엇이 끝났나

| 단계 | 상한 | 쓴 것 | 산출물 |
|---|---|---|---|
| 인터뷰 | 3 | 3(소진) | `intent.md` 의 `## 원문` |
| 착수 기준선 | — | 1 | `observations/baseline-start.txt` |
| 사전부검 | 2 | 1 | `premortem/r1-raw.md` (20 항) |
| 범위 재잠금 정반합 | 2 | 2(소진) | `dialectic/` 여덟 · **소유자 승격 한 번** |
| 완수 조건 설계 평가 | 2 | 2(소진) | `conditions-audit/r1-raw.md`·`r2-raw.md` |
| 승인 | — | 완료 | `intent.md` 의 `## 승인` |

`cargo xtask check` **28/28 초록**. 조건 **38 개** · 미측정 38 · 형식 오류 0.

★ **왜 수가 자꾸 갈리는가 — 이 저장소가 자기를 색인하기 때문이다** ⟨2026-09-09 실측⟩.
`REFERENCES` 총량도 `pal doctor` 축 ②③ 의 검사 수도 **이 회차가 코드를 넣을 때마다 함께
움직인다.** 실측이 그것을 그대로 보인다 — `2a7ca9f` 이 `binding.rs` 에 69 줄, 시험에 69 줄을
넣었고 그 뒤 `REFERENCES` 가 4290 → 4294, 축 ② 가 470 → 471, 축 ③ 이 466 → 467 로 **같이**
올랐다. 셋 다 +4 다.

⚠ **그래서 이 수들은 방아쇠로 못 쓴다.** 「축 여덟 중 하나라도 **결과**가 바뀌면 멈추고
올린다」를 검사 수로 읽으면 **코드를 한 줄 넣을 때마다 발동한다.** 소유자가 2026-09-09 에
「결과」의 자를 **축별 판정 문자열**로 좁힌 것(`intent.md` `## 승격` 칸 2)이 이 실측으로
뒷받침된다 — 판정 문자열은 착수 이래 한 번도 안 움직였고 검사 수는 세 번 움직였다.

## 실행에서 먼저 볼 자리 — 실측한 좌표

| 무엇 | 어디 |
|---|---|
| 파일 밖 참조를 표시하는 갈래 | `crates/pal-core/src/scope.rs:185` `RefResolution::OutsideFile` |
| 그 갈래가 버려지는 자리 | `crates/pal-core/src/projection.rs:248` |
| ⚠ **임포트 참조는 그리로 안 온다** | `rust_scopes.rs` 가 `use` 를 모듈 스코프 바인딩으로 선언 → `Bound{NotASymbol}` → `projection.rs:262-265` 의 `counts.locals` |
| 임포트 타입 | `crates/pal-core/src/file_graph.rs:129` `ImportSet` — `modules` **와 `items`**(`2cd8f1f` 가 세웠다). ⚠ 이 줄은 「`modules` 하나뿐이고 항목 이름이 없다」로 적혀 있었다 — 지금은 거짓이다 |
| 익스포트 타입 | 같은 파일 `:76` `ExportSet` — `names`·`star_from`·`has_default` |
| Rust 추출기의 표면 | `crates/pal-extract/src/rust.rs:417` `표면()` · `:468` `재수출을_담는다` |
| 2층 잇기 | `crates/pal-cli/src/ledger.rs:354` `stitch_of` — `EXPORTS` 는 **유일하게 해소되는 최상위 이름만** 담는다 |
| 파일 안 엣지 산출 | `crates/pal-core/src/projection.rs:236` `file_edges` |
| 추출기 판번호 | `crates/pal-extract/src/lib.rs:147` `EXTRACTOR_REV` |

## 돌고 있는 정반합 — 판 둘이 열려 있다

| 판 | 물음 | 어디까지 |
|---|---|---|
| **2** | 결박 `1cf63ec5e0e9937a` 가 움직인 것을 어느 문으로 처분하나 | **닫혔다** ⟨`9862868`⟩. 2 라운드 소진 · 종료 판단이 「올린다」 · 소유자가 여섯 칸에 답했다 |
| **3** | `A7` — 축2 의 ⓑ(`S::foo()`)를 세우나 | **돌고 있다.** 설계문이 요구한 사전 기계 관측을 먼저 떴다(`fe75072`) · 라운드 1 의 정(正)이 도는 중 |

★ **판 3 의 설계자가 갈래를 셋으로 냈다** — (가) 세운다 / (나) 안 세운다 /
**(다) 임포트 이름에 한정해 세운다**. `A7` 원문은 (다)를 안 적으므로 **그 이분법 자체가
판정 대상**이라고 설계문이 적는다.

★ **판 3 의 상한 2 는 잠긴 값이 아니다** — 설계자가 옆칸을 본떠 세운 임시값이고 설계문이
그 사실을 스스로 적는다. 소진되면 그것 자체가 승격 조건 (ㄹ) 이다.

### 사전 기계 관측이 섰다 ⟨`0760fed`(등록) → `fe75072`(값)⟩

설계문이 *"라운드 시작 전에 기계 관측 하나를 먼저 뜬다 … 이것은 판정이 아니라 **분모**다"*
를 요구했다. 규칙을 먼저 등록하고 그 다음에 쟀다.

| | 전체 저장소(138 `.rs`) | 잠근 값 ㉯(3 크레이트 · 124 `.rs`) |
|---|---:|---:|
| `경로_꼬리_배제` 를 끄면 새로 드는 **참조 자리** | 8,145 | 6,888 |
| 그중 머리가 `use` 로 들어온 이름 | 2,042 | **1,763** |
| 그중 오늘 `OutsideFile` — **갈래 (다)의 상한** | 1,991 | **1,722** |
| 그중 오늘 `Bound` — **조용한 오답의 자리** | 51 | **41** |

★★ **`rust_scopes.rs:52` 의 「실측 후보 6,961」은 오늘 어느 겹에서도 재현되지 않는다.**
`A7` 원문이 그 수를 인용하므로 **판 3 은 6,961 을 분모로 못 쓴다.** 자세한 것은
`observations/a7-denominator.md`.

⚠ **8,145 와 `scope_variants` 의 V7 「가짜 88」은 같은 것을 세지 않는다** — 앞은 참조 자리,
뒤는 엣지 신원이다. 자리 단위로 대면 88 에 대응하는 것이 `Bound` 268 이다.

## 실행이 세운 것 ⟨커밋 `a1a4542`⟩

`RefCounts::imported` 갈래와 `PendingImportRef` 짝이 섰다. 임포트 참조가 지역 변수와
같은 칸에서 갈라져 나왔고, 그것이 파일 간 엣지의 **분모**다.
**아직 엣지는 0 이다** — 짝을 파일 경계 너머로 잇는 2 층 패스가 다음 자리다.

## 사정권 전수 표 ⟨2026-09-09 실측 · 승격 칸 3 이 요구한 것⟩

조건 **38** 개 × 결박 30(감시 심볼 27, 셋은 결박 둘씩 진다)을 기계로 교차했다
(`pal round conditions --json` × `pal query binding.status --json` × `pal query graph.dump --json`).

| 판정 | 수 | 조건 |
|---|---:|---|
| **사정권 안** | **5** | `A1`(`표면` `crates/pal-extract/src/rust.rs:417-453` · `1cf63ec5e0e9937a`) · `D4`·`D4-a`(`Projection::files` `crates/pal-store/src/projection.rs:497-509` · `fcd48a9033fd1fb0`) · `E4`·`E5-a`(`print_facts` `crates/pal-cli/src/touch.rs:389-422` · `91cdc3add647a3f7`) |
| 사정권 밖 | 18 | — |
| 못 쟀다 | 15 | `A7`·`A7-a`·`A5-a`·`D1`·`D1-a`·`D2`·`D2-a`·`E1`·`E1-a`·`E2`·`E2-a`·`E3`·`E3-a`·`E3-b`·`E3-c` — 요구가 문서 기입이거나 산출 관측이라 코드 좌표가 없다 |

★ **`B3` 는 밖이고 `E4` 는 안이다** — 「내가 모르는 것」 줄은 `touch.rs:265`(`print_screen`
238-306)이고 감시 본체 389-422 밖이다. 둘을 같은 자리로 읽으면 사정권이 틀린다.

★ **다섯 중 발동한 것은 `A1` 하나다** — `git diff -U0 6b6cb6d..HEAD -- '*.rs'` 를 감시
범위와 교차하면 실제로 건드려진 감시 본체는 `표면`(커밋 `2cd8f1f`) 하나다.
`D4`·`D4-a`·`E4`·`E5-a` 는 아직 안 건드려졌다 — **그것들을 채울 때 방아쇠가 다시 발동한다.**

⚠ **「못 쟀다 15」의 독법이 둘이다.** 「좌표를 확정 못 했다」로 세면 안 5 / 밖 18 / 못 쟀다
15 이고, 「코드 좌표가 없으니 어느 본체에도 안 든다」로 세면 안 5 / 밖 33 / 못 쟀다 0 이다.
**게이트에 실을 때 어느 독법인지 적는다.**

## 잡아 둔 것 — 아직 원장에 안 실림

- **원장의 `경로` 한 칸이 두 뜻을 진다** — 「발견이 관측된 자리」와 「처분이 닿는 자리」다.
  취합 보고는 앞의 뜻으로 적고 `xtask` 의 「발견이 닫혔나」 검사는 **뒤의 뜻으로 읽는다**
  (*"`DL2-11` 가 `5999d44` 로 닫혔다는데 그 커밋이 `observations/doctor-before.txt` 를 안
  만졌다"*). 이 판에서 다섯 건이 그 갈림으로 걸렸고, 좌표를 처분이 닿는 자리로 옮기고
  관측 좌표를 근거 불릿으로 내려 풀었다. **모집단은 규약이다.** `r2-raw.md` 의
  `## 항이 아닌 것` 이 정본이다.

- **`extract.py` 가 반(反)의 자평을 싣고 합(合)의 상향을 안 싣는다.** `DL2-27`(계기의
  침묵)은 원장에 `해악도=거짓신호` 인데 `2-synthesis-2.md` 는 그것을 **금지역**으로
  올렸다. 원 반환문의 항이 두 값을 나란히 적는데 추출기는 앞의 것을 집는다.
  **모집단은 자기 장치다** — 이 회차가 만든 것이 아니라 규약의 도구이므로 저장소 몫이나,
  금지역에 닿는지는 안 쟀다.

## 실패한 접근

- **`use` 직접 임포트만으로 범위를 잠근 것** — 소유자가 2026-09-09 에 전환을 승인해
  `crate::`·`super::`·`self::` 접두가 안으로 들어왔다.
- **`RefResolution::OutsideFile` 을 착지점으로 지목한 것** — 임포트한 이름은 그 갈래에
  안 온다(위 표).
- **모집단을 「315 자리 · 25 파일」로 적은 것** — 정·반·합 셋을 통과한 뒤 2 라운드에서
  깨졌다. 잠근 값 ㉯ 의 모집단은 **1611 자리 · 엣지 상한 1159 · 파일 81 · 크레이트 3**.
- **발견 레코드를 손으로 옮겨 적은 것** — `extract.py` 로 뽑아야 한다. 검사가 잡았다.
- **관측 파일을 `.jsonl` 로 회차 디렉터리에 둔 것** — 원장 검사가 그것을 읽는다.
- **`intent.md` 에 같은 절(`## 차선책`)을 두 번 둔 것** — 감사자가 빈 쪽을 읽고 오판했다.

## 사고 기록

**하위 에이전트가 원본 `schema/graph.toml` 에서 216 줄을 지웠다**(`[node.Binding]` 절 전량).
`git checkout` 으로 복구했다. 격리 사본이 아니라 원본에서 파괴 실험을 한 것으로 보인다.

## 확대 — 이 회차의 종료 게이트가 거짓 0 위에 선다 ⟨2026-09-09 실측 `BL2-02`⟩

**`crates/pal-cli/src/round/status.rs:485-488` 의 `출처` 허용 집합이 넷이다** —
`독립리뷰`·`사전부검`·`인터뷰`·`실측`. 그런데 `.claude/skills/round/bin/record.py:70` 의
같은 축은 **여섯**이고 `정반합`·`조건평가` 가 더 있다. git 이 그 갈림을 이렇게 적는다 —
Rust 쪽 네 값은 `3134a25`(2026-09-02), `record.py` 에 `조건평가` 가 들어온 것은
`7c1f7f4`(2026-09-06). **Rust 가 나중에 생긴 값을 모른다.**

무슨 일이 벌어지나 — 이 회차 원장 123 행 중 **97 행**(`정반합` 46 · `조건평가` 51)이 Rust
집합 밖이다. `findings_state`(`status.rs:441-546`)는 그런 행을 만나면 오류를 내지 않고
`return Ok((false, 0))` 로 **조기 반환**한다(`status.rs:483` 외 일곱 자리가 같은 모양).
그래서 `pal round status --json` 이 `open_harmful_findings: 0` 을 찍는데, 원장을 직접 세면
`상태=열림` 이면서 `해악도∈{금지역,실패}` 인 행이 **39 행**이다. 첫 조기 반환 지점은 파일
28 행 `id=DL1-01`(`출처=정반합`)이고 나머지 96 행은 읽히지도 않는다.

★ **왜 이 회차가 이것을 지는가 — 이 회차가 원리상 못 닫힌다.**
`CompletionState::Complete`(`status.rs:353-384`)의 합격 조건은
`findings_current && open_harmful_findings == 0` 이다(`status.rs:359-360`).
⚠ **거짓 0 이 게이트를 뚫지는 않는다** — 같은 조기 반환이 `findings_current` 도 `false` 로
내리므로 두 값이 함께 움직인다. 걸리는 것은 반대쪽이다: **`findings_current` 가 영원히
`false` 라서 이 회차는 지금 코드로 `Complete` 에 원리상 도달하지 못한다.** 조건 37 개를 다
닫아도 못 닫는다.

그리고 산출된 값 자체가 거짓이다 — `open_harmful_findings: 0` 인데 원장의 실제 값은 39 다.
그 값을 읽는 자리(규약 §5 계기판 ⑨ 해악 게이트, 그리고 사람)가 **「닫을 수 있다」로
오독한다.** 금지역 둘에 닿는다 — **측정이 죽은 가지** · **사실이 아닌 것을 사실로**.

규약 §5 의 판정 기조 원문이 이것을 이번에 하라고 한다 — *"다음 회차에 짐을 지우거나 다음
회차가 이어 받았을때 의도가 변질될 우려가 조금이라도 있다면 무조건 타협없이 이번 회차에서
마무리 되어야 함"*.

⚠ **모집단은 `저장소` 다** — 이 회차가 만든 장치가 아니다. 그래서 §11 ③ 의 「별도 목록」이
아니라 **본 모집단**이고 미룰 수 없다.

## ★★ 멈추고 올리는 자리가 발동했고 **닫혔다** ⟨2026-09-09⟩

**소유자가 2026-09-09 에 여섯 칸에 답했다** — `intent.md` 의 `## 승격` 이 정본이다.
`D1` 을 재잠갔고(`D1-a` 음성 대조 신설), `## 멈추고 올리는 자리` 의 「결과」를 **축별 판정
문자열**로 「멈춘다」를 **결박 사정권**으로 좁혔고, `## 상한` 에 「완수 조건 검증 정반합
판마다 2 라운드」 칸을 세웠고, 계기의 침묵을 **응답에 한 줄 더하는 최소 수정**으로 고치기로
했다. 조건은 37 → **38** 개다.

⚠ **아래 ② 는 거짓으로 판정났다 — 고쳐 적는다.** `pal doctor` 축 ① 은 **안 움직였다.**
파생 저장소 `.palimpsest/intent.redb` 를 커밋된 정본에서 세우면 `검사 30 · 위반 0` 으로
착수 관측과 같다. 모집단 산출 코드는 착수 이후 안 바뀌었다. 발동한 방아쇠는 **결박 하나뿐**
이다. 이 정정은 `intent.md` 의 `## 개정` 마지막 행이 진다.

### 아래는 발동 시점의 기록이다 — ② 는 위 정정이 뒤집었다

`## 멈추고 올리는 자리` 원문 — *"**결박 30 건의 낡음이 움직이거나 `pal doctor` 의 판정 축
여덟 중 하나라도 결과가 바뀌면** 거기서 멈추고 올린다."*

**둘 다 일어났다.**

### ① 결박이 움직였다

| | 착수 (`dialectic/1-thesis.md:87`) | 지금 (`505a0c0`) |
|---|---|---|
| 총 | 30 | 30 |
| `fresh` | 26 | **25** |
| `stale` | 4 | **5** |

움직인 것은 **`1cf63ec5e0e9937a` 하나** — `fresh` → `stale`. 앵커는
`crates/pal-extract/src/rust.rs:417` 의 `표면` 이고, 이 회차가 `A3`·`A4` 를 채우려고
그 함수에 두 줄을 더한 것이 방아쇠다(`triggered_by` = 그 심볼).

⚠ **재는 방법에 함정이 하나 있다.** `pal query binding.status --json` 이 오늘 **`bindings: []`
와 `exit 0`** 을 낸다 — 결박이 없어서가 아니라 파생 저장소 `.palimpsest/intent.redb` 가
없어서다(`.gitignore:34`). 정본은 커밋된 `.palimpsest/intent/bindings.jsonl` 30 행이고,
`pal intent import <그 파일> --intent <자리>` 로 세운 뒤 다시 재야 30 건이 나온다.
**빈 답이 부재로 읽히는 자리다** — 응답 어디에도 「의도 저장소가 없다」가 안 실리고
`coverage.unresolved: 0` 으로 깨끗하게 보인다.

### ② `pal doctor` 축 ① 의 모집단이 사라졌다

| 축 | 착수 (`observations/doctor-before.txt`) | 지금 |
|---|---|---|
| ① edge-ends-exist | `검사 30 · 표본 밖 0 · 위반 0` | ★ **`(모집단이 없습니다)`** |
| ② registered-and-required | `검사 467 · 위반 0` | `검사 471 · 위반 0` ⟨2026-09-09 재실측⟩ |
| ③ producer-fits-provenance | `검사 463 · 위반 0` | `검사 467 · 위반 0` ⟨2026-09-09 재실측⟩ |
| ④⑤⑥⑦⑧ | `(모집단이 없습니다)` | `(모집단이 없습니다)` |

★ **위반 수는 여덟 다 0 이라 `D2` 는 안 깨졌다.** 깨진 것은 `D2-a` 가 경고한 자리다 —
그 조건 원문이 *"모집단이 실제로 있는 ①②③ 이 이 조건의 본체"* 라고 적는데, **그 셋 중
①의 모집단이 30 에서 0 으로 사라졌다.** 0 을 훑고 통과하는 검사가 하나 늘었다.

## 아직 안 정해진 것 — 실행 중에 갈린다

**`A7`** — 축2 의 ⓑ(`S::foo()`)를 세우나. 세우면 `rust_scopes.rs` 의 `경로_꼬리_배제` 를
끄는 것이고 그 주석이 *"끄면 실측 후보 6,961 이 참조가 된다"* 로 적으므로 모집단과
`E2` 의 하한이 다시 서야 한다(`A7-a`). **안 세우면 축소이고 승격이다.**
