# 상태 — 2026-09-08-cross-file-references

## 지금 단계

**루프(실행) 안이고, 정반합 판 4 가 상한 2/2 를 소진했다.** 종료 판단이 **「소유자에게
올린다」**를 냈고 **소유자 칸 넷이 열린 채다.** 다음 자리는 **소유자의 답**이다.

**파일 경계를 넘었다.** 착수 관측이 파일 간 엣지 **0** 이었고 지금은 ⓐ **1460** · ⓑ **276** 이다.

### 지금 값 ⟨2026-09-10 재실측 · 커밋 `5e62c79`⟩

| | 값 | 어떻게 쟀나 |
|---|---|---|
| 완수 조건 | **39 개 중 통과 4 · 미측정 35** | `pal round conditions --file <intent.md> --json` → `열림 35 · 닫힘 4 · 형식오류 0` |
| 파일 간 해소 | ⓐ **1460/5705** · ⓑ **276/1049** | `./target/debug/pal touch file_edges` 의 화면 |
| ⓐ 가 못 선 까닭 | `ambiguous 214 · no_symbol 840 · no_symbol_at_crate_root 1919 · no_target_file 137 · outside_repo 1135` | 같은 화면. **합이 4245 로 분모와 맞는다** |
| ⓑ 가 못 선 까닭 | `ambiguous 14 · no_symbol 146 · no_symbol_at_crate_root 311 · no_target_file 20 · outside_repo 282` | 같은 화면 |
| 결박 | 30 · `fresh` **24** · `stale` **6** | `pal intent import … --intent <scratch>` 뒤 `pal query binding.status --intent <그 자리> --json` |
| `pal doctor` 축별 판정 문자열 | ①②③ `ok` · ④~⑧ `(모집단이 없습니다)` — **착수와 같다** | `pal doctor` 원문 |
| `pal doctor` 검사 수 | ① **471** · ② **498** · ③ **473** · 위반 전부 0 | 같은 명령 |
| 검사 | `검사 28/28 통과` · `cargo test --workspace` 실패 0 | 두 명령의 원문 |

⚠ **방아쇠는 이 교대에서 한 번도 안 발동했다.** 결박 여섯이 착수 교대와 **같은 여섯**이고
축별 판정 문자열도 안 움직였다. 검사 수만 움직였는데 그것은 방아쇠가 아니다(승격 칸 2).

★ **`touch.rs` 의 `print_facts` 를 이 교대가 두 번 만졌다** — 결박 `91cdc3add647a3f7` 의
사정권이다. 그 결박은 **이미 `stale`** 이라 낡음이 더 안 움직인다. 재판정은
[`observations/d1-rebinding.md`](observations/d1-rebinding.md) 가 이미 진다.

## ★★ 다음 자리 — **소유자 칸 넷.** 메인이 혼자 못 닫는다

종료 판단 `dialectic/4-referee-2.md` 가 「올린다」를 냈다. 갈래 셋을 이렇게 갈랐다 —
**「끝났다」가 아니다**(금지역 2 · 실패 1 이 산다) · **「한 라운드 더」가 아니다**(상한 2 는
소유자가 추인한 등록값이고 이 라운드가 소진했다) · **그래서 승격**(ㄷ)(ㅁ).

| 칸 | 물음 |
|---|---|
| 1 | `E2` 의 ⓑ 판정 문자를 **「반증」**으로 박는가 — (ㄷ) |
| 2 | **상한을 열어 한 라운드 더 도는가**, 아니면 금지역 둘을 미해결로 명시한 채 닫는가 — (ㅁ). 실패 1 건(아래)의 경로도 이 칸이 정한다 |
| 3 | 살아남은 **금지역 ①**(「머리가 모듈」의 크기)의 처분 |
| 4 | 살아남은 **금지역 ②**(「≈318 은 상한이 아니다」)의 처분 |

★ **칸 3·4 를 묶지 않은 것은 종료 판단의 판정이다** — 두 금지역이 같은 뿌리인지 판단할
근거를 그 자리가 안 받았고, 근거 없이 묶으면 답 하나가 둘을 닫은 것처럼 보인다.
⚠ **칸 3·4 의 처분이 승인된 수(333·800·893·483·1159)를 교체하면 (ㄴ)이 추가로 성립하므로
교체되는 수마다 칸을 하나 더 세운다.**

### 살아남은 것 — 금지역 둘 · 실패 하나

- **금지역 ① — 「머리가 모듈」은 22 가 아니라 41 이다.** 원장 자신이 그 값을 적어 두었다
  (`intent.md` 의 이관표 행). 정(正)이 그 41 을 인용해 놓고 저장소 밖 계기의 22 로 갈아
  끼웠고, **그 교체는 자기 결론(반증)을 쉽게 만드는 한 방향으로만 갔다.** 정 스스로
  *"22 가 37 을 넘으면 「구현 문을 다 열어도 미달」이 무너진다"* 고 적었다. 좌표 넷이 실재함을
  합이 검산했다 — `crates/pal-cli/src/install/doctor.rs:33`·`:142` ·
  `crates/pal-cli/src/install.rs:36` · `crates/pal-cli/src/install/settings.rs:38`.
- **금지역 ② — 정의 「≈318」은 상한이 아니다.** 296·≈318·≈332 는 **구현이 안 낸 산수**이고
  반(反)이 팔 셋을 실제로 켜 그 상한을 넘겼다(324). ⚠ **반의 대체값도 원장에 상한으로 안
  싣는다** — 실리는 것은 「318 은 상한이 아니다」라는 **반증**뿐이다.
- **실패 — 「반증」이라는 문자가 질 뜻.** 하한 333 의 분자 483 의 규칙이 *"꼬리 이름이
  저장소 **어딘가에** 심볼로 서는가"* 이고, `no_symbol_at_crate_root` 통은 **재수출을 지나야만**
  닿는 자리다. 곧 **하한이 요구하는 몫의 일부는 잠근 축1 이 금한 일**이다. 이 사실이 ⓑ 항에
  함께 안 박히면 원장이 「반증」을 *"구현이 미달했다"* 로만 읽는다.

### ★ 합이 연 자리 — **반의 수도 원장이 재현 못 한다**

반은 자기 계기를 「구현 자신이 찍은 것」이라 열었으나 저장소 밖 복제본에 팔과 계수기를
심어 따로 빌드했고 분모가 원본과 어긋난다 — 폐기된 ⓑ 하한 150 과 같은 형태다.
**다만 반의 계기에는 등록된 음성 대조가 붙어 있고 정의 것에는 없다.** 그래서 반의 수는
**정을 반증하기에는 충분하고 원장에 새 사실로 실리기에는 부족하다.**

### 합이 못 정한 것

**라운드 1 의 반론과 라운드 2 의 반론이 같은 자리를 치는가** — 설계문 반증 목록 일곱째다.
합이 라운드 1 산출을 안 받아 대조하지 못했다. **소유자가 칸 2 에서 상한을 열면 그 대조가
다음 라운드의 첫 일이다.**

## 실행이 세운 것 ⟨커밋 `5e62c79` 까지⟩

`경로_꼬리_배제` 는 **안 껐다.** 잠근 (다2) 그대로다.

| 층 | 무엇 | 어디 |
|---|---|---|
| 1 층 | 꼬리를 참조로 안 만들고 **머리 참조에 싣는다** | `pal_core::CallTail` · `ScopeRules::call_tail` · `rust_scopes.rs` 의 `경로_호출의_꼬리` |
| 1 층 | 임포트 항목이 **선언된 바이트**를 진다 | `pal_core::ImportedItem::at`(목록) |
| 1→2 | 짝에 꼬리를 실어 보낸다 | `PendingImportRef::tail` |
| 2 층 | 모듈 경로를 파일로 펴고 **후보가 유일할 때만** 엣지 | `crates/pal-core/src/cross_file.rs` |
| 스키마 | 파일 간 엣지는 **별도 라벨**이다 | `[edge.REFERENCES_ACROSS]` · `pal_core::CrossFileEdge` |
| 회계 | 갈래별 분모·산출·못 선 까닭 | `CrossFileReport` · `META` 에 저장 · `pal touch` 가 찍는다 |
| 화면 | 능력 축이 **셋** + **「못 선 몫이 가는 문」** 줄 | `touch.rs` 의 `print_facts`·`print_cross`·`print_transfer` |
| doctor | 뷰가 2 층 엣지를 싣고 `REFERENCES`·`REFERENCES_ACROSS` 를 `holding` 으로 진다 | `doctor.rs` 의 `build_view`·`coverage` |
| 시험 | `A1`~`A7` 과 `E4`(이관표 단언 포함) | `crates/pal-cli/tests/cross_file_references.rs` (17 개) |

### 이 교대가 더한 것 ⟨`4c1e0da` → `5e62c79`⟩

- **이관표를 게이트·화면·잔여 셋 다에 적었다** — 종료 조건 게이트 3⑶ 이 요구한 자리다.
  게이트 문서 `docs/gates/cross-file-references.md` 가 **없어서** 새로 세웠다(절 넷 · 표준 표 ·
  조건 ID 39 개가 잠긴 의도와 집합으로 같다).
- **셈을 산문에서 걷어내 회계 열쇠로 올렸다** — `no_symbol` 이 둘로 갈린다
  (`no_symbol_at_crate_root` · `no_symbol`). 앞 판이 주석에 적은 비율 「340 중 298」이 값이
  움직이자 거짓이 된 자리다.
- **합이 잡은 거짓 셋을 고쳤다** — `no_symbol` → `#133` 단일 대응(→ **미측정**) ·
  `ambiguous` → `A6`(→ **후보 생성의 모호**) · **ⓐ 의 회계 결손 316**(공유 단계 실패를 두 통에
  다 헤아린다).

## 판정한 조건 넷 ⟨2026-09-10⟩

| 조건 | 판정 | 산출 |
|---|---|---|
| `E1`·`E1-a` | **통과** | `observations/e1-witness-after.txt` — 증인 26 이 **26/26**(하한 18 · 착수 0/26) |
| `A5-a` | **통과** | `observations/a5a-reexport-edges.md` — 파일 간 엣지 1259 중 재수출을 지난 것 **0** |
| `C1` | **통과** | 불변식 ① 의 `absent` 여섯에 `REFERENCES` 가 없다 |

## 아직 안 한 것 — 조건 39 개 중 35

`A` 계열 대부분 · `B` 계열 전부(`UnresolvedRef`) · `C2`·`C2-a`·`C4`·`C4-a`·`C3` ·
`D` 계열 전부 · `E2` 계열 · `E3` 계열 · `E4` · `E5`·`E5-a`.

★ **`C2` 는 「측정」이 아니라 「계기를 세우는 일」이다** — 불변식 산출이 라벨별 몫을 안 내고
(`{"checked": 471, "skipped": 5177, "violations": 0}` 하나뿐) `C2-a` 가 전체 `checked` 로 재는
길을 막아 뒀다.

★★ **`E5` 가 반증 방향으로 서 있다** ⟨`observations/e5-capability-declarations.md`⟩ —
`pal query --list` 와 `pal touch` 는 `F07` 을 「못 만든 능력」이라 적는데 `pal doctor` 는
안 적는다. **값(ⓐ 1460 · ⓑ 276)이 서 있는 채로 세 표면이 서로 안 맞는다.**
⚠ **고치는 길이 하나가 아니라 상자를 안 켰다** — `F07` 이 세 갈래를 한 이름으로 묶어서
「없다」도 「다 됐다」도 거짓이고, 능력 id 를 가르면 계획 문서 여러 곳이 함께 움직인다.
고칠 자리는 `crates/pal-query/src/lib.rs` 의 `capabilities()` 다.

## 계기판

```bash
PATH="$PWD/target/debug:$PATH" python3 .claude/skills/round/bin/dashboard.py 6b6cb6d \
  .palimpsest/rounds/2026-09-08-cross-file-references/intent.md
```

⚠ **`pal` 이 `PATH` 에 없으면 계기판이 죽는다** — 이 저장소는 `target/debug/pal` 로 돈다.
⑨ 해악 게이트는 앞 판들의 발견 처분이 실행돼야 풀린다.

## 다음 컨텍스트가 받는 것

**잠긴 의도 전문(`intent.md`)과 이 파일뿐이다.** 정반합 산출물·사전부검·조건 감사의
원 반환문을 시드로 주지 마라 — 규약 §5 「교대」가 금한다.

## 무엇이 끝났나

| 단계 | 상한 | 쓴 것 | 산출물 |
|---|---|---|---|
| 인터뷰 | 3 | 3(소진) | `intent.md` 의 `## 원문` |
| 착수 기준선 | — | 1 | `observations/baseline-start.txt` |
| 사전부검 | 2 | 1 | `premortem/r1-raw.md` (20 항) |
| 범위 재잠금 정반합 | 2 | 2(소진) | `dialectic/` · 소유자 승격 한 번 |
| 완수 조건 설계 평가 | 2 | 2(소진) | `conditions-audit/r1-raw.md`·`r2-raw.md` |
| 승인 | — | 완료 | `intent.md` 의 `## 승인` |

## 돌고 있는 정반합

| 판 | 물음 | 어디까지 |
|---|---|---|
| **2** | 결박 `1cf63ec5e0e9937a` 의 처분 | **닫혔다** ⟨`9862868`⟩ · 소유자가 여섯 칸에 답했다 |
| **3** | `A7` — 축2 의 ⓑ 를 세우나 | **닫혔다** ⟨`2feaba8`⟩ · 소유자가 칸 7~10 에 답했다 |
| **4** | `E2` ⓑ 의 어긋남을 어느 문으로 처분하나 | **2/2 소진 · 「올린다」** ⟨`5e62c79`⟩ · **소유자 칸 넷이 열려 있다** |

⚠ **판 4 의 취합 보고(`dialectic/r4-raw.md`)가 아직 없다** — `pal-debate-reporter` 를 안
불렀다. 그것이 발견 원장의 입력이므로 **원장에 판 4 의 발견이 한 건도 안 실려 있다.**

## 실행에서 먼저 볼 자리 — 실측한 좌표

| 무엇 | 어디 |
|---|---|
| 파일 간 해소기 | `crates/pal-core/src/cross_file.rs` 의 `cross_file_edges` |
| 갈래 하나만 막힌 자리 / 공유 단계 | 같은 파일의 `miss` · `miss_shared` |
| 「심볼이 없다」를 두 문으로 가르는 자 | 같은 파일의 `심볼_없음` — `crates_of` 와 같은 자를 쓴다 |
| 화면의 이관표 | `crates/pal-cli/src/touch.rs` 의 `print_transfer` |
| 능력 부재 선언(고칠 자리) | `crates/pal-query/src/lib.rs` 의 `capabilities()` |
| 임포트 타입 | `crates/pal-core/src/file_graph.rs:129` `ImportSet` |
| Rust 추출기의 표면 | `crates/pal-extract/src/rust.rs:417` `표면()` |
| 추출기 판번호 | `crates/pal-extract/src/lib.rs:147` `EXTRACTOR_REV` — 지금 `f09-import-sites` |

## 사정권 전수 표 ⟨2026-09-09 실측 · 승격 칸 3 이 요구한 것⟩

조건 38 개 × 결박 30 을 기계로 교차했다.

| 판정 | 수 | 조건 |
|---|---:|---|
| **사정권 안** | **5** | `A1`(`표면`) · `D4`·`D4-a`(`Projection::files`) · `E4`·`E5-a`(`print_facts`) |
| 사정권 밖 | 18 | — |
| 못 쟀다 | 15 | 요구가 문서 기입이거나 산출 관측이라 코드 좌표가 없다 |

⚠ **「못 쟀다 15」의 독법이 둘이다** — 「좌표를 확정 못 했다」로 세면 안 5 / 밖 18 / 못 쟀다
15 이고, 「코드 좌표가 없으니 어느 본체에도 안 든다」로 세면 안 5 / 밖 33 / 못 쟀다 0 이다.
**게이트에 실을 때 어느 독법인지 적는다.**

★ **`DL3-07` 은 안 닫혔다** — 방아쇠의 결박 팔이 **미측정**이다. `bindings.jsonl` 의 `watch`
가 심볼 다이제스트만 실어 파일 좌표가 없고, 그것을 좌표로 되푸는 조회가 서기 전까지 그렇다.

## 잡아 둔 것 — 아직 원장에 안 실림

- **판 4 의 발견 전부** — 취합 보고를 안 불렀다.
- **원장의 `경로` 한 칸이 두 뜻을 진다** — 「발견이 관측된 자리」와 「처분이 닿는 자리」다.
  `r2-raw.md` 의 `## 항이 아닌 것` 이 정본이다.
- **`extract.py` 가 반의 자평을 싣고 합의 상향을 안 싣는다.**
- **`mod X;` 와 `pub use X::X;` 가 같은 뿌리 파일에 있을 때** `use` 한 줄이 두 이름 공간을
  다 들여오는데 엣지는 **모듈** 쪽으로 선다(`pal_core::traverse` 2 건).
  **거짓 엣지로 셀지는 `E3` 가 판정한다** — `observations/a5a-reexport-edges.md`.

## 실패한 접근

- **`use` 직접 임포트만으로 범위를 잠근 것** — 소유자가 전환을 승인해 접두 셋이 안으로 들어왔다.
- **`RefResolution::OutsideFile` 을 착지점으로 지목한 것** — 임포트한 이름은 그 갈래에 안 온다.
- **모집단을 「315 자리 · 25 파일」로 적은 것** — 정·반·합 셋을 통과한 뒤 깨졌다.
- **발견 레코드를 손으로 옮겨 적은 것** — `extract.py` 로 뽑아야 한다.
- **관측 파일을 `.jsonl` 로 회차 디렉터리에 둔 것** — 원장 검사가 그것을 읽는다.
- **셈을 코드 주석에 비율로 적은 것**(「340 중 298」) — 값이 움직이자 거짓이 됐다.
  **회계 열쇠로 올려야 안 낡는다.**
- **공유 단계 실패를 한 통에만 넣은 것** — ⓐ 의 까닭 합이 자기 분모와 316 어긋났고 화면이
  그것을 안 말했다.

## 사고 기록

**하위 에이전트가 원본 `schema/graph.toml` 에서 216 줄을 지웠다.** `git checkout` 으로
복구했다. 격리 사본이 아니라 원본에서 파괴 실험을 한 것으로 보인다.

**판 4 의 종료 판단이 자기신고를 냈다** — 절을 뽑아 줄 때 경계가 넘어가
`4-design.md` 의 `## 무엇이 이 판을 반증하나` 가 함께 보였다. **메인의 잘못이다.**
답을 바꾸지 않는 방향으로만 썼다고 그 자리가 적었다.

## 확대 — 이 회차의 종료 게이트가 거짓 0 위에 선다 ⟨2026-09-09 실측 `BL2-02`⟩

`crates/pal-cli/src/round/status.rs` 의 `출처` 허용 집합이 넷인데
`.claude/skills/round/bin/record.py` 의 같은 축은 여섯이다(`정반합`·`조건평가` 가 더 있다).
`findings_state` 가 그런 행을 만나면 오류 없이 조기 반환해 `open_harmful_findings: 0` 을
찍는다. **`findings_current` 도 함께 `false` 로 내려가므로 이 회차는 지금 코드로
`Complete` 에 원리상 도달하지 못한다.** 모집단은 **저장소**다 — 미룰 수 없다.
