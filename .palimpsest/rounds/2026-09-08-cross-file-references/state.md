# 상태 — 2026-09-08-cross-file-references

## 지금 단계

**종료 절차 안이다.** 완수 조건 39 개가 전부 판정됐고(통과 37 · 반증 2) 열린 금지역·실패가
0 이며 효과가 게이트에 붙었다. 남은 것은 **열린 발견 전부를 닫는 것**과 **독립 리뷰**와
**push · CI** 다.

⚠ **`report.md` 를 쓰는 순간 검사의 기준이 올라간다** — `cargo xtask check` 의
「발견이 닫혔나」가 끝난 회차에 대해 **금지역·실패만이 아니라 열린 발견 전부**를 요구한다
(*"끝난 회차에 열린 발견이 있으면 그 회차가 안 끝난 것"*). 그리고 **「어색한 표현 부재」의
모집단에 그 회차의 문서와 게이트가 들어온다.** 종료 문서를 쓰기 전에 그 둘을 알고 써라.

### 종료 시점 값 ⟨2026-09-10⟩

| | 값 | 어떻게 쟀나 |
|---|---|---|
| 완수 조건 | **39 개 전부 판정 — 통과 37 · 반증 2 · 대조불가 0 · 미측정 0** | `pal round conditions --file <intent.md> --json` → `열림 0 · 닫힘 39 · 형식오류 0` |
| 반증 둘 | `E2`(ⓑ 가 하한 333 에 못 닿았다 · 276) · `E3-b`(등록된 표본 규칙이 유일한 표본을 안 정한다) | 게이트 `## 판정` |
| 회차 상태 | `terminal: reported` · `findings_current: true` · `open_harmful_findings: **0**` · `completion: unavailable` | `pal round status --round <slug> --json` |
| 파일 간 해소 | ⓐ **1463/5741** · ⓑ **276/1058** | `./target/release/pal touch print_facts` |
| 못 푼 참조 | **값이다** — 이름 · 못 푼 까닭 · 지난 걸음 | 같은 화면의 `■ 내가 모르는 것` |
| 내보내기 | `REFERENCES 4394` · `REFERENCES_ACROSS 1263` · `UnresolvedRef 4744` · `REFERS_UNRESOLVED 4744` | `pal export --format cypher` |
| `pal doctor` 축 ① | `검사 10400 · 위반 0` · 라벨별 `REFERENCES 4394 · REFERENCES_ACROSS 1263 · REFERS_UNRESOLVED 4743` | `pal doctor --full --json` |
| 결박 | 30 · `fresh` **24** · `stale` **6** — 신원이 `c9a6bae` 때와 같다 | `pal intent import` 뒤 `pal query binding.status --json` |
| 검사 | `검사 28/28 통과` · `cargo test --workspace` 실패 0 · `f04-verify.py` exit 0 | 세 명령의 원문 |

★★ **정정 ⟨2026-09-10 · `DL2-13`⟩ — 「축 ① 은 안 움직였다」는 거짓이다.**
합(合) 라운드 1 의 정정문 H1 이 그 문장을 썼고 앞 판의 이 파일이 한 번 더 썼다.
**참인 것은 「축 ① 의 산출은 실제로 바뀌었다」이고, 안 바뀐 것은 축별 판정 문자열
하나다.** 실측: 축 ① 의 `검사` 가 착수 **30** → `39e500b` 뒤 **484** → 종료 **10400**
(표본 상한을 푼 전수)이다. 그 수를 움직인 것은 `doctor.rs` 가 2 층 엣지를 뷰에 실은
것이고(`C1`) 그것은 **의도된 모집단 변경**이다 — `D2` 가 재는 위반 수는 전 구간 0 이다.
**두 절을 갈라 적지 않으면 원장이 「아무것도 안 바뀌었다」를 사실로 싣는다.**

⚠ **이 정정의 뒤 절반은 원리상 못 한다.** `DL2-13` 의 처분이 *"원장 요약 칸에도 두 절을
다 넣는다"* 를 함께 요구하는데, `요약` 은 합계 검산이 **동결된 원 반환문**과 글자로
대조하는 기계 칸이다. 고치면 그 대조가 빨개지고, 원 반환문을 고치면 증거 위조다.

## 이 회차가 세운 것

| 층 | 무엇 | 어디 |
|---|---|---|
| 1 층 | 꼬리를 참조로 안 만들고 **머리 참조에 싣는다** | `pal_core::CallTail` · `rust_scopes.rs` 의 `경로_호출의_꼬리` |
| 1 층 | 임포트 항목이 **선언된 바이트**를 진다 | `pal_core::ImportedItem::at` |
| 2 층 | 모듈 경로를 파일로 펴고 **후보가 유일할 때만** 엣지 | `crates/pal-core/src/cross_file.rs` |
| 2 층 | **못 푼 참조가 값이다** — `site`·`name`·`reason`·`attempts`(+`at`) | 같은 파일의 `UnresolvedRef`·`Attempt` |
| 스키마 | `[edge.REFERENCES_ACROSS]`(`exact`) · `[node.UnresolvedRef]`(Built) · `[edge.REFERS_UNRESOLVED]` | `schema/graph.toml` |
| 저장 | 2 층 행의 **형식 판**과 읽기 관문 하나 | `pal-store` 의 `ROW_FORMAT_REV` · `open_read_only` |
| 화면 | 능력 축 셋 · 이관표 · 「내가 모르는 것」의 값 · **의도 저장소 부재의 표시** | `crates/pal-cli/src/touch.rs` |
| 내보내기 | 라벨을 양 끝의 파일로 가르고 못 푼 참조를 함께 낸다 | `crates/pal-cli/src/export.rs` |
| doctor | 뷰가 못 푼 참조를 싣고 불변식이 **라벨별 몫**을 산출한다 | `doctor.rs` 둘 · `pal-core::Outcome::by_label` |
| 원장 | `경로`(관측된 자리)와 **`처분자리`**(처분이 닿은 자리)를 갈랐다 | `record.py` · `xtask` · `status.rs` |
| 시험 | `A1`~`A7` · `B1`~`B4` · `C2`·`C4-a` · `D4`·`D4-a` · `E4` · 효과가 잡은 것 | `cross_file_references.rs`(22) · `pal-store`·`pal-core` |

## 무엇이 끝났나

| 단계 | 상한 | 쓴 것 | 산출물 |
|---|---|---|---|
| 인터뷰 | 3 | 3(소진) | `intent.md` 의 `## 원문` |
| 착수 기준선 | — | 1 | `observations/baseline-start.txt` |
| 사전부검 | 2 | 1 | `premortem/r1-raw.md` |
| 범위 재잠금 정반합 | 2 | 2(소진) | `dialectic/` 판 1 |
| 완수 조건 검증 정반합 | 판마다 2 | 판 2·3·4 각 2(소진) | `dialectic/` 판 2·3·4 |
| 완수 조건 설계 평가 | 2 | 2(소진) | `conditions-audit/r1-raw.md`·`r2-raw.md` |
| 승인 | — | 완료 | `intent.md` 의 `## 승인` |
| 판정 | — | 완료 | `docs/gates/cross-file-references.md` |
| 효과 | — | 완료 | 게이트 `## 효과` · `observations/effect-touch-print-facts.txt` |
| 종료 보고 | — | 완료 | `report.md` |
| 독립 리뷰 | 4 | 진행 중 | — |

## 다음 컨텍스트가 받는 것

**잠긴 의도 전문(`intent.md`)과 이 파일뿐이다.** 정반합 산출물·사전부검·조건 감사의
원 반환문을 시드로 주지 마라 — 규약 §5 「교대」가 금한다.

## 실패한 접근

- **`use` 직접 임포트만으로 범위를 잠근 것** — 소유자가 전환을 승인해 접두 셋이 들어왔다.
- **`RefResolution::OutsideFile` 을 착지점으로 지목한 것** — 임포트한 이름은 그 갈래에 안 온다.
- **모집단을 「315 자리 · 25 파일」로 적은 것** — 정·반·합 셋을 통과한 뒤 깨졌다.
- **발견 레코드를 손으로 옮겨 적은 것** — `extract.py` 로 뽑아야 한다.
- **관측 파일을 `.jsonl` 로 회차 디렉터리에 둔 것** — 원장 검사가 그것을 읽는다.
- **일괄 치환의 실패를 확인 안 한 것** — 여러 자리를 함께 고치면 고친 뒤 자리마다 다시 세어라.
- **셈을 코드 주석에 적은 것** — 「340 중 298」도 `V1`~`V10` 의 표도 값이 움직이자 거짓이 됐다.
  **회계 열쇠나 계기로 올려야 안 낡는다.**
- **공유 단계 실패를 한 통에만 넣은 것** — ⓐ 의 까닭 합이 316 어긋났다.
- **`경로` 한 칸에 두 뜻을 담은 것** — 「관측된 자리」와 「처분이 닿는 자리」가 갈리는 행에서
  둘은 동시에 못 참이고, 초록으로 만드는 남은 길이 **「닫은 것을 열린 채로 두는 것」**이 됐다.
- **존재하지 않는 커밋을 닫은커밋으로 적은 것**(`f37b194`) — 검사가 그 형태를 이름까지 미리
  적어 두었고 실제로 났다.
- **음성 대조를 이 회차가 안 세운 라벨로 건 것**(`C4-a` 가 `BOUND_TO` 를 심었다).
- **선언을 값보다 먼저 뒤집는 것** — 이 회차는 두 번 다 값을 먼저 세웠다(`C1`·`B1`).

## 사고 기록

- **하위 에이전트가 원본 `schema/graph.toml` 에서 216 줄을 지웠다.** `git checkout` 으로
  복구했다. 격리 사본이 아니라 원본에서 파괴 실험을 한 것으로 보인다.
- **판 4 의 종료 판단이 자기신고를 냈다** — 절을 뽑아 줄 때 경계가 넘어가
  `4-design.md` 의 `## 무엇이 이 판을 반증하나` 가 함께 보였다. **메인의 잘못이다.**
- **측정 중에 다른 에이전트가 같은 기계를 썼다.** `f04-verify.py` 의 ⑨ 가 그때 8.4~8.9 배로
  빨갰고 조용한 기계에서 다시 재니 9.9~14.3 이었다. **벤치를 잴 때는 기계를 비워라.**
