# 결박 시도 — §11 조건 4 (2026-08-19)

## pal narrative — 이 회차의 산출물을 코드 좌표에 건다
```

■ 서술물 인입
  문서 198 · 조각 1909 · 새 개체 209
  결박됨 0 · 후보 있음 7 · 미결박 1902

■ 무엇이 걸었나
  span                   7

■ 이 인입이 못 본 것
  이력 창 400 커밋 · 창 밖에서 마지막으로 바뀐 문서 0
  **그 문서들에는 「같은 커밋」 신호가 없습니다** — 없는 것이지 0 이 아닙니다.

  **아무것도 승인하지 않았습니다.** `inferred` 는 사람의 승인으로만
  `asserted` 가 됩니다 — `pal narrative approve <개체>`.

```

## pal ledger — 무엇을 보았고 무엇을 안 보았나
```
  binary               1
  generated            0

언어      Markdown          L0  결박 불가            197 파일
          Rust              L0  결박 불가            116 파일
          Python            L0  결박 불가             36 파일
          TOML              L0  결박 불가             28 파일
          Shell             L0  결박 불가              6 파일
          YAML              L0  결박 불가              2 파일
          JSON              L0  결박 불가              1 파일
          TypeScript        L2  identity: exact     1 파일

          ← 결박 불가 언어 7개 · 386 파일. 이 파일들에는 좌표가 없습니다

캐시      적중 424 · 빗나감 0

provider  (이 빌드에 provider 포트가 없습니다 — F21 미구축)
조달      (이 빌드에 관측 수용이 없습니다 — F16 미구축)
감지기    추출기 f03-2 · 문법 acb9630 · HEAD 그대로

```
