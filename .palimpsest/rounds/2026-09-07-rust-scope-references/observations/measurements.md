# 착수 시점 대 지금 — **같은 명령으로 잰다**

> 착수 `a774053` 의 값은 잠긴 의도의 `## 착수 시점 관측` 이 진다(원 출력 `baseline-red.txt`).
> 지금 값은 **2026-09-08 · 이 회차의 마지막 산출 상태**에서 잰 것이다.
> 원 출력은 [`b1-export.txt`](b1-export.txt) 다 — 기여한 `.rs` 132 개의 목록이 거기 있다.
> **2층을 지우고 다시 세운 뒤 쟀다** — `rm -rf .palimpsest/index.redb .palimpsest/cache`
> → `pal query graph.dump --json` → `pal export --format cypher`.

| 무엇 | 착수 `a774053` | 지금 | 조건 |
|---|--:|--:|---|
| `pal export --format cypher` 의 `REFERENCES` | **3** | **4,258** | — |
| 그 엣지에 기여한 서로 다른 `.rs` 파일 | **0** | **132** | `B1`(하한 80) |
| 기여 파일 전체(`.rs` + `.ts`) | 1 | 133 | — |
| `File` 노드 | 135 | 139 | — |
| `Symbol` 노드 | 3,110 | 3,205 | — |
| `pal touch file_edges` 의 호출자 · 피호출자 | `0 · 0` | `6 · 3` | `B2` |
| `pal query binding.status` — 착수 시점 결박 25 의 `status` 축 | `fresh 23 · stale 2` | `fresh 23 · stale 2` | `C2` |
| 같은 명령의 전체 결박 | 25 | **30** (이 회차가 §11 조건 4 로 다섯을 걸었다) | — |
| `watch_grades` 축 | `ordinal 25` | `ordinal 30` | — |
| `grade_of(Rust)` | `L1` | `L1` | `C1` |

`File` 과 `Symbol` 이 는 것은 **이 회차가 `.rs` 파일 넷을 새로 썼기 때문**이다 —
`rust_scopes.rs` · `ts_scopes.rs` · `scope_variants.rs` · `rust_references.rs`.

## 변형 대조 장치가 산출한 수 — 같은 저장소, 다른 셈

⚠ **`pal export` 의 4,258 과 장치의 4,255 는 다른 셈이다.** 장치는 파일 시스템을 훑어
`.rs` **138** 개를 보고 엣지를 `(경로, 출발 자리, 도착 자리)` 로 세며, `pal` 은 git 이
추적하는 **134** 개를 보고 `SymbolId` 쌍으로 센다. **둘을 같은 수로 적으면 안 된다.**
아래는 [`v-variants.txt`](v-variants.txt) 의 값이고 **그 파일이 이긴다.**

| 무엇 | 값 |
|---|--:|
| 기준 엣지 쌍 | 4,255 |
| 엣지가 된 참조 | 6,639 |
| 갈래 — 타입 참조 / 호출·매크로 / 그 밖 | 1,198 / 3,843 / 1,598 |
| 참조 일곱 갈래 — 선언 / 엣지 / 지역 / 최상위 / 미해소 / 선언전 / 모호 | 13,517 / 6,639 / 25,782 / 251 / 11,889 / **0** / 47 |

★ **`선언전 0` 이 규칙이 갈렸다는 증거다** — Rust 아이템에는 TDZ 가 없고
`ResolveRule::Shadowing` 이 그것을 표현한다. TypeScript 규칙을 그대로 받았으면
사전부검이 실측한 **52 건**이 「선언 전 참조」로 떨어졌을 것이다.

## `V11` — 사전부검 프로토타입과의 대조

사전부검 R2 의 프로토타입은 타입 1,182 · 호출·매크로 2,305 · 그 밖 2,529(참조 6,016 ·
쌍 3,751)를 냈다. 이 회차의 산출은 타입 **1,198** · 호출·매크로 **3,843** · 그 밖
**1,598**(참조 6,639 · 쌍 4,255)다.

- **타입 참조는 거의 같다**(1,182 → 1,198) — 저장소가 그사이 파일 넷 늘었다.
- **호출·매크로와 그 밖의 경계가 다르다.** 이 회차의 분류는 `token_tree` 안에서
  **뒤에 괄호 열이 오면 호출**로 세고, 프로토타입은 그 규칙을 안 썼다. **엣지 집합의
  차이가 아니라 분류의 차이다.**

## `E3` — 소유자 답의 수를 다시 잰다

소유자 원문은 *"동명 선언 72건이 각기 다른 스코프에 서서 가짜 엣지가 사라진다"* 였다.

| 무엇 | 값 |
|---|--:|
| 같은 스코프의 동명 아이템 재선언 자리 — 기준 | **21** (파일 10) |
| 같은 것 — `impl` 이 스코프를 안 열 때 | **83** (파일 30) |
| 그 자리가 만든 엣지 | **0** — 전부 `RefResolution::Ambiguous` 다 |
| 그중 `cfg` 쌍둥이 | **전부는 아니다** — 아래 |
| 해소하면 늘어나는 엣지 (`V10`) | 31 (그리고 42 가 사라진다) |

⚠ **남은 21 이 전부 `cfg` 쌍둥이는 아니다**(정반합 판 1 의 반(反)이 잡았다).
`crates/pal-cli/src/install/blocks.rs` 는 `pub enum 상태`(`:116`)와 `pub fn 상태`(`:231`)를
함께 갖고 `cfg` 가 없다. `crates/pal-cli/tests/install_boundary.rs` 도 `struct 방`(`:44`)과
`fn 방`(`:50`)이다. 까닭은 이 표가 **타입 아이템을 `Namespace::Value` 에도 묶기**
때문이다 — 값 자리에서 타입 이름이 쓰이는 358 건(`A10`)을 얻는 대가다.

**72 는 어느 판에서도 재현되지 않는다.** 1 차 출처(`premortem/r1-raw.md`)가 그 수를
**문법 스파이크가 134 파일에서** 센 것이라고 적고, 지금 장치의 같은 형태는 83(파일 30)
이며 코퍼스도 138 파일이다.

## D2 — 못 세는 몫 셋과 분모

**모집단은 회차 착수 시점의 추적 `.rs` 134 개다.** ⚠ 이 회차가 파일 넷을 더해
지금은 `git ls-files '*.rs'` 도 변형 대조 장치도 **둘 다 138** 을 산출한다 — 아래
세 수는 그 넷이 없는 134 개에서 쟀고 다시 안 쟀다.

| 무엇 | 못 세는 수 / 후보 |
|---|--:|
| 포맷 문자열 캡처 (`println!("{x}")` 의 `x`) | 987 / 987 |
| 멤버 호출 (`x.foo()` 의 `foo`) | 13,951 / 13,951 |
| 경로 호출 (`S::new()` 의 `new`) | 4,217 / 4,217 |
| ⟨넷째⟩ 동명 아이템이 둘이라 안 고른 참조 | 47 |

비교: 실제로 성립하는 참조가 **6,639**(장치의 셈) · 엣지가 **4,258**(`pal export` 의 셈)이다.
⚠ 두 수는 서로 다른 계수기의 것이다 — 위 「같은 저장소, 다른 셈」 절이 그것을 가른다.
