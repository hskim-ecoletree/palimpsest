E1 층화 표본 — 50 건 (타입 15 · 호출·매크로 20 · 그 밖 15 배분)
  1  [타입] crates/pal-cli/src/attach.rs:81  attach → How
      pub fn attach(index: &Path, report: &LedgerReport, how: How) -> Result<Attached> {
  2  [타입] crates/pal-cli/src/install/layout.rs:82  PAYLOAD → Resource
      Resource {
  3  [타입] crates/pal-cli/src/label.rs:207  잔여_사유 → Label
      Label { 원_표기: "language-grade-below", 사용자_언어: "언어 등급 미달" }
  4  [타입] crates/pal-cli/src/round/ledger.rs:112  Event → EventRef
      thesis: EventRef,
  5  [타입] crates/pal-cli/src/round/verify.rs:204  finalize → FinalizeView
      pub fn finalize(config: &Config<'_>) -> Result<FinalizeView, VerifyError> {
  6  [타입] crates/pal-core/src/cascade.rs:101  cascade → Cascade
      pub fn cascade(view: &GraphView, depth_budget: usize) -> Cascade {
  7  [타입] crates/pal-core/src/doctor.rs:739  inferred_carries_evidence → Outcome
      ) -> Outcome {
  8  [타입] crates/pal-core/src/glob.rs:77  from → Glob
      fn from(g: Glob) -> Self {
  9  [타입] crates/pal-core/src/narrative.rs:682  동점이면_확정하지_않는다 → RawSignals
      RawSignals { spans: vec!["cancel".to_owned()], ..RawSignals::default() },
 10  [타입] crates/pal-core/src/plan.rs:1185  넷이_각각_성립한다 → SnapshotView
      let b = SnapshotView { symbols: &base, files: &files };
 11  [타입] crates/pal-core/src/schema.rs:304  RawEdge → RawCarrier
      carried_by: Option<RawCarrier>,
 12  [타입] crates/pal-core/src/touch.rs:340  값이_있는_자리는_그대로_실린다 → SymbolFacts
      let v: Capable<SymbolFacts> = Capable::Present(SymbolFacts { callers: 9, callees: 14 });
 13  [타입] crates/pal-extract/src/scopes.rs:246  declare_pass → ScopeRules
      fn declare_pass(&mut self, rules: &dyn ScopeRules, node: Node<'_>, scope: ScopeIx) {
 14  [타입] crates/pal-intent/src/store.rs:449  keep_refusal → IntentError
      pub fn keep_refusal(&self, r: &Refusal) -> Result<(), IntentError> {
 15  [타입] crates/pal-store/src/cache.rs:368  lookup → CacheKey
      fn lookup<T: DeserializeOwned>(&self, key: &CacheKey) -> Result<Lookup<T>, CacheError> {
 16  [호출·매크로] crates/pal-cli/build.rs:27  main → git_dir
      if let Some(git) = git_dir(&manifest) {
 17  [호출·매크로] crates/pal-cli/src/install/blocks.rs:236  상태 → 자리
      if 자리(&existing, inserted.as_bytes()).is_some() {
 18  [호출·매크로] crates/pal-cli/src/install/inside.rs:69  join → 실제_경로
      let real = 실제_경로(&candidate, 0)?;
 19  [호출·매크로] crates/pal-cli/src/query.rs:384  print_bindings → 시각
      println!("      {}  ·  {}", b.subject, 시각(b.bound_at_time));
 20  [호출·매크로] crates/pal-cli/src/round/stop.rs:135  command_enable → print_view
      print_view(json, &view, &format!("Stop enabled: {}", activation.round));
 21  [호출·매크로] crates/pal-cli/tests/common/mod.rs:116  저장소 → git
      git(&root, &["init", "-q", "."]);
 22  [호출·매크로] crates/pal-cli/tests/install.rs:539  리소스를_하나도_못_찾으면_실패한다 → 값
      let m = 값(&root.join(".claude/pal/manifest.json"));
 23  [호출·매크로] crates/pal-cli/tests/install_hooks.rs:270  등록된_명령이_돌고_차단을_산출한다 → 프로젝트
      let root = 프로젝트("발화");
 24  [호출·매크로] crates/pal-cli/tests/install_recovery.rs:201  못_지운_디렉터리를_말한다 → 프로젝트
      let root = 프로젝트("남긴다");
 25  [호출·매크로] crates/pal-cli/tests/round_approve_verify.rs:763  실행된_현재_negative_control없이는_주조건도_met이_아니다 → verify
      assert_success(&verify(&repo, &approvals, "A1", &[]));
 26  [호출·매크로] crates/pal-cli/tests/round_status.rs:332  명시_round의_디렉터리와_intent_부재는_resolve_error다 → status
      let out = status(&root, "missing-round");
 27  [호출·매크로] crates/pal-cli/tests/round_stop.rs:788  corrupt_progress와_trailing_partial_crlf를_보수적으로_판정한다 → enable
      enable(&repo, &store);
 28  [호출·매크로] crates/pal-core/src/cascade.rs:285  사슬 → 스냅샷
      GraphView::new(스냅샷(), ViewCoverage::new()).with_nodes(nodes)
 29  [호출·매크로] crates/pal-core/src/doctor.rs:1391  담을_수_없는_불변식은_위반_0_이_아니라_능력_부재다 → 성한
      .with_edges(성한().edges().to_vec())
 30  [호출·매크로] crates/pal-core/src/plan.rs:1045  포매팅만_바뀌면_변경_심볼이_0_이고_본문이_바뀌면_1_이다 → 심볼
      let 같음 = vec![심볼("a", "src/a.ts", 1), 심볼("b", "src/a.ts", 2)];
 31  [호출·매크로] crates/pal-core/src/schema.rs:750  공통_넷_각각이_없으면_엣지가_등록되지_않는다 → 성한
      let 뺀 = 성한.replace(줄, "");
 32  [호출·매크로] crates/pal-extract/src/parse.rs:608  다음_선언 → 벗긴다
      return Some(벗긴다(x, wrappers).start_byte());
 33  [호출·매크로] crates/pal-extract/src/shell.rs:193  껍데기는_추출기의_상수와_같다 → shell_of
      let Capable::Present(kotlin) = shell_of(Language::Kotlin) else {
 34  [호출·매크로] crates/pal-intent/src/store.rs:423  keep_entity → IntentError
      let raw = postcard::to_allocvec(id).map_err(|e| IntentError::Decode(e.to_string()))?;
 35  [호출·매크로] xtask/src/main.rs:625  check → check_intent_untouched
      ("의도 저장소 폐기 경로 부재", check_intent_untouched(root)),
 36  [그밖] crates/pal-cli/src/attach.rs:84  attach → How
      How::Stitching => {
 37  [그밖] crates/pal-cli/src/install/manifest.rs:538  표식_모으기 → 표식
      serde_json::Value::String(s) if s.starts_with(표식) => {
 38  [그밖] crates/pal-cli/src/round/status.rs:313  read_round → ConditionState
      ConditionState::Pending | ConditionState::Unregistered => {
 39  [그밖] crates/pal-cli/tests/round_approve_verify.rs:350  종료직전_전수재실행은_이미_met인_command와_정반합_finding을_같이_닫는다 → SLUG
      json!({"schema_version":3,"종류":"레코드","회차":SLUG}),
 40  [그밖] crates/pal-core/src/binding.rs:649  evaluate → CodeFreshness
      CodeFreshness::Stale { triggered_by: changed }
 41  [그밖] crates/pal-core/src/derived.rs:429  대상_집합이_다르면_다른_노드다 → DerivedId
      DerivedId::compute("F", &둘, Provenance::Inferred, &Producer::Agent, &트리())
 42  [그밖] crates/pal-core/src/envelope.rs:664  사유_넷과_상한_넷이_서로_다른_값이다 → BudgetName
      BudgetName::CandidateSetMax,
 43  [그밖] crates/pal-core/src/ledger.rs:459  칸의_합은_언제나_파일_총수다 → LanguageId
      language: LanguageId::new("Kotlin"),
 44  [그밖] crates/pal-core/src/plan.rs:658  by_name → UnresolvedWhy
      why: UnresolvedWhy::NotAtBaseline,
 45  [그밖] crates/pal-core/src/repo.rs:425  스냅샷은_집합이고_비어_있을_수_없다 → Snapshot
      assert!(Snapshot::of(Vec::new()).is_none());
 46  [그밖] crates/pal-core/src/scope.rs:518  함수_경계를_지나면_뒤에_선_이름도_해소된다 → Namespace
      matches!(c.resolve(body, "LATER", Namespace::Value, 10), RefResolution::Bound { .. }),
 47  [그밖] crates/pal-extract/src/narrative.rs:97  fragment → 자리
      자리::헤딩 => {
 48  [그밖] crates/pal-query/src/lib.rs:489  run → NamedQuery
      NamedQuery::BindingStatus => Ok(QueryResult::Bindings {
 49  [그밖] crates/pal-store/src/projection.rs:317  swap → BY_NAME
      write.delete_multimap_table(BY_NAME).map_err(tx)?;
 50  [그밖] crates/pal-store/src/projection.rs:794  clear_stage → SYMBOL_STAGE
      write.delete_table(SYMBOL_STAGE).map_err(tx)?;
