# `E1` 손 대조 — 층화 표본 50 건

> 잰 자: 메인 · 2026-09-08 · 표본 원 출력: [`e1-sample-50.txt`](e1-sample-50.txt)
> 뽑은 명령: `cargo run -q --release -p pal-extract --example scope_variants -- . --표본`

## 표본 규칙 — **뽑기 전에 고정했다**

잠긴 의도 `E1` 이 적은 것: *"기여 파일에서 파일별 층화로 뽑고, 갈래(타입 참조 /
호출·매크로 / 그 밖)마다 최소 10 건을 채운다"* · 총 50 건.

구현이 고정한 배분은 **타입 15 · 호출·매크로 20 · 그 밖 15** 이고, 갈래마다
`(경로, 줄)` 로 정렬한 뒤 **균등 간격**으로 집는다. 난수를 안 쓴다 — 표본을 다시 뽑아
유리한 것을 고르는 길을 막는다.

⚠ **표본을 두 번 다시 뽑았다.** 두 번 다 **추출기가 바뀌어서**이고 규칙은 한 글자도 안
고쳤다. 같은 명령을 다시 돌렸을 뿐이다 — **바뀐 것은 산출이지 표본 규칙이 아니다.**

| 언제 | 무엇이 바뀌었나 | 누가 잡았나 | 그 판의 손 대조 |
|---|---|---|---|
| 첫째 | 매크로 토큰 열 안의 구조체 리터럴 필드명(`assert_eq!(g, Foo { bar: 2 })` 의 `bar`)이 참조로 세어져 같은 파일 `fn bar` 로 가짜 엣지를 만들었다 | 독립 리뷰 R1 | 가짜 0 |
| 둘째 | `let root = root("x");` 의 초기화식이 **자기 자신**으로 해소돼 참 엣지가 사라졌다 | 정반합 판 1 의 합(合) | 가짜 0 |

세 판 전부 가짜 0 이다. 아래는 **마지막 표본**의 판정이다.

## 무엇을 「가짜」로 판정하나

그 자리의 이름이 **엣지가 가리키는 그 심볼을 실제로 가리키지 않으면** 가짜다. 넷을 본다:

1. 도착 심볼이 그 자리의 Rust 의미상 다른 심볼인가
2. 그 토큰이 애초에 참조가 아닌가 — 선언 이름 · 필드 · 경로 꼬리 · 속성 안 · `use` 절 안
3. 출발 심볼이 그 자리를 담는 가장 안쪽 심볼이 아닌가
4. 참조는 맞지만 이 파일 안에서 해소될 것이 아닌가

## 판정 — **가짜 0 / 50**

| 갈래 | 건수 | 판정 | 대조한 방법 |
|---|--:|---|---|
| 타입 참조 | 15 | **참 15** | 도착 이름이 그 파일에 선언돼 있는지 각각 확인. 엣지는 `BoundSymbol::Symbol` 일 때만 서므로 `use` 로 들어온 이름은 원리상 엣지가 안 된다 — 그 불변을 코드로 확인하고 `RawCarrier`·`Resource`·`How`·`CacheError`(`pal-store/src/cache.rs:36`)·`IntentError`(`pal-intent/src/store.rs:78`)를 직접 열었다 |
| 호출·매크로 | 20 | **참 20** | 부르는 것이 같은 파일에 있는지 각각 확인했다 — `마커가_있나`(`blocks.rs:283`) · `글자로_벗어나나`(`inside.rs:160`) · `disable`(`stop.rs:152`) · `실패`(`install.rs:57`) · `페이로드`(`install_hooks.rs:242`) · `훑기`(`install_recovery.rs:272`) · `assert_success`(`round_approve_verify.rs:179`) · `value`(`round_status.rs:41`) · `blocked`(`round_stop.rs:201`) · `cut_residuals`(`cascade.rs:177`) · `첫_심볼_시작`(`parse.rs:732`) · `거부_열쇠`(`store.rs:795`) · `성한`(`schema.rs:637` 의 `const`) |
| 그 밖 | 15 | **참 15** | 경로 머리 · 구조체 리터럴 · `match` 패턴의 머리. `H0`(`sha256.rs:31` 의 `const`) · `ReproInput`(`derived.rs:73` 의 `pub enum`) · `SLUG`(`round_approve_verify.rs:14` 의 `const`) · `BY_NAME`·`SYMBOL_STAGE`(`projection.rs` 의 `const`) 를 직접 확인 |

**가짜 0 · 참 50.**

## 이 판정이 못 잡는 것

- **누락은 안 잰다.** 표본은 「선 엣지가 참인가」만 묻는다. 안 선 엣지는 `V` 절의 변형
  대조가 잡고, 원리상 못 세는 몫은 `rust.rs` 모듈 주석의 표가 진다.
- **갈래 분류의 정밀도는 안 잰다.** `IntentError::Transaction(…)` 을 「호출」로 센 자리가
  있다(열거 변형 생성이지 함수 호출이 아니다). 표본 **분류**의 흠이지 엣지의 참·거짓이
  아니다.
- **표본의 모집단은 엣지가 아니라 참조다.** `표본()` 이 `chain.refs` 를 중복 제거 없이
  담고 그 총수가 **6,639** 다(엣지 쌍은 4,255). 그러므로 50 건은 **0.75%** 다.
  오류율이 1% 면 놓칠 확률이 60% 다 — **이 표본은 「대량의 가짜가 없다」를 말하고
  「가짜가 없다」를 말하지 않는다.**
- **이 회차가 새로 쓴 네 파일에서 뽑힌 줄은 0 이다.** 균등 간격이 경로 사전순을 따르기
  때문이고, **표본을 고쳐 채우지 않았다** — 규칙을 측정 뒤에 바꾸는 것이 곧 합격선 이동이다.
