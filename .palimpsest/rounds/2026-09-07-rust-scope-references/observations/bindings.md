# 결박 — 이 회차의 결정을 코드 좌표에 못 박았다 (규약 §11 조건 4)

> 2026-09-08 · `pal bind` 다섯 · 의도 저장소는 git 밖이라(R-21) 이 파일이 그 기록이다.

$ ./target/release/pal query binding.status --json | <요약>

결박 30 건 — 착수 시점 25 · 이 회차가 새로 건 5

## 이 회차가 건 다섯

| 결박 | 좌표 | 무엇을 못 박았나 |
|---|---|---|
| `1cf63ec5e0e9937a` | `6b4389c2b186` | `pub` 의 뜻은 「최상위 + 정확히 `pub`」 하나다 |
| `39b92bd51bbc82c6` | `80cd88b02cc2` | Rust 는 「가장 뒤」 팔을 쓴다 |
| `819fb73f9caf7f22` | `2bf66b24410e` | 아이템 호이스팅을 `hoist_home` 으로 표현하지 않는다 |
| `91cdc3add647a3f7` | `f129649fd0fb` | 이 라벨은 두 방향을 다 적어야 한다 |
| `cbd70e3e8dfca3ba` | `f24c2c6201a3` | 패턴에서 가드와 or 갈래를 거른다 |

## 착수 시점 결박 스물다섯 — **안 움직였다**

`status` 축: fresh 23 · stale 2

낡은 둘은 착수 시점과 같은 결박이고 같은 촉발 좌표를 진다:

- `4065e80475cf1c72` ← `bd26d1d8a3ee0246` · #114 — 검출 수단은 사전형이 아니라 활용형으로 잰다. 그리고 자기를 안 잰다
- `49714573e0fa654c` ← `55373aa24b6c4a20` · 회차 2026-09-06-terrain-and-completion-scene 의 정정 — 「어색한 표현 부재」 검사의 대상은 
