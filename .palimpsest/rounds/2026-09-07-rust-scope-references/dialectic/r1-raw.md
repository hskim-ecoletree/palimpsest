# 정반합 취합 — 2026-09-07-rust-scope-references 라운드 1 · `E1`·`E2`·`E3` 이 충족됐나

> `pal-debate-reporter` 가 냈다. **판정을 다시 하지 않는다 — 옮기는 자리다.**
> 실행 2026-09-08. 받은 것: `dialectic/1-design.md` · `1-thesis.md` · `1-antithesis.md` ·
> `1-synthesis.md` · `1-referee.md` 전문 · 회차 이름 · 원장 스키마.
> **안 받은 것**: 대화 기록 · 메인의 사고 과정 · 이 판정을 실행할 계획.
> ⚠ `2-design.md` · `2-thesis.md` 는 **다른 판**이라 안 열었다.

## 이 산출물이 원문에 무엇을 했나 — 먼저 적는다

- **항 제목(`### `)은 반(反)이 자기 반론 칸의 머리에 굵게 적은 절을 그대로 옮긴 것이다.**
  반의 반론 칸은 한 칸이 최대 열두 줄이라 제목 한 줄에 안 들어간다. **그것은 원 산출물이
  긴 사실이고, 줄이지 않기 위해 칸 전문을 항마다 `- 반(反) 원문` 불릿에 그대로 실었다.**
- **`- 어디가 걸리나:` 는 좌표 하나를 먼저 놓고 그 뒤에 반(反)의 좌표 칸 원문을 붙였다.**
  좌표를 먼저 안 놓으면 추출기가 산문 첫 낱말(`git show 3de5e81` 따위)을 경로로 집는다.
  **원문은 한 글자도 안 뺐다.**
- **`- 모집단:` 은 내가 붙였다.** 반(反)도 합(合)도 모집단 축을 안 적었다. 이 회차
  `findings.jsonl` 의 선례를 그대로 썼다 — 제품 크레이트=`저장소` ·
  `crates/pal-extract/examples/scope_variants.rs`=`자기장치` · 회차 디렉터리 안=`회차기록`.
- **`- 해악도:` 는 합(合)이 등급을 매긴 것은 합의 값을, 안 매긴 것은 반(反)의 값을 실었다.**
  갈린 자리 다섯은 항 안과 `## 계수` 에 둘 다 적었다. **맞춰서 적지 않았다.**

---

## 판정

합(合)이 산출한 문장 그대로다.

> **수정.** ⚠ 이 값은 게이트가 아니다 — 「소유자에게」 절이 게이트다.
>
> | 조건 | 이름 | 한 줄 |
> |---|---|---|
> | **`E1`** | **`S1` 통과** | 표본 50 건이 결정론적으로 재생되고, 그 50 줄 전부가 손 대조에서 참이다. 다만 **어느 한 산출물도 혼자서는 지금 표본 50 줄을 다 덮지 않는다** — 덮개는 셋의 합집합이다 |
> | **`E2`** | **`S1` 통과** | `A`~`D` 의 합이 `#130` 의 1·2·4 를 덮는다. **`V9` 는 덮개에서 뺀다** — 도구가 스스로 *"이 줄을 통과로 읽지 마라"* 를 찍는다 |
> | **`E3`** | **`S5` 조건이 그 물음을 안 잰다** | 정의 결론은 살리고 **그 근거는 갈아 끼운다.** 정이 댄 기각 논거(`A17` 충돌)는 반증됐다 |

정(正)이 낸 판정문은 이것이었다 — **합(合)이 `E3` 의 근거를 갈아 끼웠고 이름 셋은 그대로 섰다.**

> **`E1` 을 `S1`(통과)로, `E2` 를 `S1`(통과)로, `E3` 을 `S5`(조건이 그 물음을 안 잰다)로 간다.**

합(合)이 `E3` 에 다시 세운 근거 셋의 머리 문장 그대로:

> 1. **문면이 서로 반대인 두 읽기를 허락한다.** 주어는 *"동명 선언 72건"*(→ ① 자리 수 = **21** → 반증), 술어는 *"가짜 엣지가 사라진다"*(→ ② 가짜 엣지 = **0** → 통과). 어느 쪽을 뜻하는지 문면이 안 정하고, 고름이 **수를 본 뒤에** 일어난다. 이것이 설계문 `S5` 의 정의 그대로다.
> 2. **`S4` 가 아니다.** `S4` 는 「잴 것 하나가 정해져 있는데 증거가 없다」이고, 여기서는 **완벽하게 재도 판정이 안 정해진다.**
> 3. ★ **①이라는 수 자체가 균질하지 않다 — 내가 새로 쟀다.** 도구가 찍는 남은 자리 보기 **12 개**를 전부 열어 보니 **10 개가 `cfg` 쌍둥이이고 2 개는 아니다**

---

## 반론 처리 — 전부

반(反)이 번호를 붙인 것이 **R1~R14 열넷**이다. 아래에 열넷 전부가 선다.

### R1 — 정의 수가 지금 저장소에서 안 나온다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-07-rust-scope-references/dialectic/1-thesis.md` — 반(反)의 좌표 칸 원문: 내 실행 `… --example scope_variants -- .` 3~5 줄: `기준 — 엣지 4211 · 기여 파일 132` / `(참조 6593)` · 대조군 `git show 3de5e81:.palimpsest/…/observations/v-variants.txt` = `엣지 4209 … 참조 6591`
- 반(反) 원문: **정의 수가 지금 저장소에서 안 나온다** — 정은 「엣지 4209 · 참조 6591」로 논증하는데 지금 돌리면 **엣지 4211 · 참조 6593** 이고, 내가 받은 지시문의 「4208 · 6590」도 안 나온다
- 채택·기각과 근거: **합(合)이 채택했다.** 합의 근거 원문 — *"내 실행이 **4211/6593**. 원인은 `git diff 3de5e81 HEAD -- crates/pal-extract/src/rust_scopes.rs` 의 매크로 안 구조체 리터럴 필드 거르기다. ⚠ **정이 틀린 것이 아니라 저장소가 움직였다** — 그러나 회차 산출물 셋이 아직 옛 수를 진다(아래 「고치나」)"*. 등급은 반·합이 같다(거짓신호).

### R2 — 정의 `E1` 50 줄 표가 지금 표본의 33 줄만 덮는다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-07-rust-scope-references/dialectic/1-thesis.md` — 반(反)의 좌표 칸 원문: 위 「나」 표 17 행 · `git show 3de5e81:.palimpsest/…/observations/e1-sample-50.txt` 의 33·37·49 행(`shell.rs:193` · `manifest.rs:538` · `BY_NAME`) 대 내 출력의 같은 번호(`shell.rs:149` · `sha256.rs:71` · `tx`)
- 반(反) 원문: **정의 `E1` 50 줄 표가 지금 표본의 33 줄만 덮는다** — 17 줄(33·34·35·37~50)이 지금 표본에 없는 줄이다. 설계문 `S1` 은 *"그 산출을 저장소에서 다시 확인할 수 있다"* 를 요구하는데 정의 표는 그 절반 남짓만 재확인된다. **다만 내가 그 17 줄을 직접 대서 전부 참이라 「가짜 0」의 실질은 안 깨진다**
- 채택·기각과 근거: **합(合)이 채택했다.** 합의 근거 원문 — *"**diff 로 확정했다.** 갈린 줄은 정확히 33·34·35·37~50 = **17 줄**. 반이 그 17 줄을 전부 독립으로 대서 참이다. 그러므로 「지금 표본 50 줄의 손 대조」는 **정 33 + 반 17 의 합집합**으로만 성립한다"*. 등급은 반·합이 같다(거짓신호).

### R3 — 정의 자기 신고 *"두 출력 모두 … 바이트 단위로 같다(diff 무출력) · 결정론이 실측으로 확인됐다"* 가 지금 재현되지 않는다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관
- 어디가 걸리나: `.palimpsest/rounds/2026-09-07-rust-scope-references/dialectic/1-thesis.md` — 반(反)의 좌표 칸 원문: `1-thesis.md:27-28` 대 `diff <내 표본> observations/e1-sample-50.txt`(내가 09:1x 에 돌렸을 때 17 행 갈림)
- 반(反) 원문: **정의 자기 신고 *"두 출력 모두 … 바이트 단위로 같다(diff 무출력) · 결정론이 실측으로 확인됐다"* 가 지금 재현되지 않는다** — 결정론은 「같은 코드에서 같은 출력」이지 「기록과 같은 출력」이 아닌데 정의 문장은 뒤를 주장한다
- 채택·기각과 근거: **합(合)이 채택했다.** 합의 근거 원문 — *"낱말이 겹쳤다 — 결정론은 「같은 코드에서 같은 출력」이지 「기록과 같은 출력」이 아니다. ⚠ **지금은 둘 다 참이다** — 내가 두 파일에 `diff` 를 걸어 무출력을 봤다. 남는 것은 문장 고치기뿐"*. ⚠ **등급이 갈렸다** — 반(反)은 `거짓신호`, 합(合)은 `미관`.

### R4 — `E3` 대안 사를 기각한 논거가 반증됐다

- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역
- 어디가 걸리나: `crates/pal-extract/examples/scope_variants.rs` — 반(反)의 좌표 칸 원문: 내 실행의 `남은 자리 표본` 줄 · `crates/pal-cli/src/install/blocks.rs:116,231,355` · `crates/pal-cli/tests/install_boundary.rs:44,50` · `crates/pal-extract/src/rust.rs:904-914`(a17 픽스처가 `#[cfg(unix)]`/`#[cfg(windows)]` 뿐)
- 반(反) 원문: **`E3` 대안 사를 기각한 논거가 반증됐다** — 정은 *"①만 뜻이라고 읽으면 `A17` 과 충돌한다 · `A17` 은 그 자리가 남는 것을 옳다고 잠갔다"* 로 기각했다. 그러나 남은 21 자리는 `cfg` 쌍둥이가 아니다. 도구가 스스로 찍는 12 개 보기 중 **둘이 `cfg` 와 무관**하다: `install/blocks.rs:상태(값)×2` 는 `pub enum 상태`(`:116`)와 `pub fn 상태`(`:231`)의 충돌이고 그 파일의 `cfg` 는 `#[cfg(test)]`(`:355`) 하나뿐, `tests/install_boundary.rs:방(값)×2` 는 `struct 방`(`:44`)과 `fn 방`(`:50`)의 충돌이고 그 파일에 `cfg` 가 없다. **`A17` 이 잠근 것은 `cfg` 쌍둥이이고 이 둘은 그것이 아니다 — 충돌 논거가 안 선다**
- 채택·기각과 근거: **합(合)이 채택했고 이 판에서 유일한 금지역이다.** 합의 근거 원문 — *"**직접 확인했다.** 남은 자리 12 중 `blocks.rs:상태`·`install_boundary.rs:방` 둘은 `cfg` 가 아니다(위 표). 그리고 그 거짓 문면이 **아직 코드에 살아 있다** — `crates/pal-extract/examples/scope_variants.rs:225` 의 함수 문서 주석이 여전히 *"남는 것은 `cfg` 쌍둥이뿐이다"* 라고 단언한다. 메인이 고친 것은 **출력 줄(:490)과 그 앞 주석(:479-486)** 이고 :225 는 안 따라갔다 — 같은 파일 안에서 두 문면이 서로 반대다. 안 잰 것을 사실로 적어 판정을 갈랐다"*. 등급은 반·합이 같다(금지역). **메인이 합(合) 뒤에 이 잔여를 고쳤다** — 아래 `## 메인이 반(反)·합(合) 뒤에 실행한 것` 참조.

### R5 — 그 둘을 만든 원인이 코드에 있고 도구의 주석이 사실 아닌 것을 사실로 적는다

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-extract/src/rust_scopes.rs` — 반(反)의 좌표 칸 원문: `crates/pal-extract/src/rust_scopes.rs:239-241` · `crates/pal-extract/examples/scope_variants.rs:225` · 같은 파일 `rust_scopes.rs:334-337` 이 이미 *"`cfg` 쌍둥이가 아닌데도"* 모호가 생길 수 있음을 인정한다
- 반(反) 원문: **그 둘을 만든 원인이 코드에 있고 도구의 주석이 사실 아닌 것을 사실로 적는다** — `rust_scopes.rs:239-241` 이 `struct_item`·`enum_item`·`union_item`·`type_item` 을 **`Namespace::Value` 에도** 묶는다. Rust 에서 중괄호 `struct` 와 변형 있는 `enum` 은 값 이름공간에 안 서는데 이 규칙은 무조건 세운다. 그런데 `scope_variants.rs:225` 는 *"`impl` 이 스코프를 열면 그 72 건이 갈라져야 하고 **남는 것은 `cfg` 쌍둥이뿐**이다"* 라고 단언한다
- 채택·기각과 근거: **합(合)이 채택했다.** 합의 근거 원문 — *"원인 확인: `rust_scopes.rs:239-244` 가 타입 아이템을 `Namespace::Value` 에도 묶는다. `measurements.md:65-70` 과 `scope_variants.rs:479-490` 은 고쳐졌다. **남은 것 둘** — ① `scope_variants.rs:225`(R4 와 같은 자리) ② `crates/pal-extract/src/rust.rs:31-33` 의 *"해소하면 엣지 31 건이 늘지만 **어느 것이 참인지 이 층에서 못 가른다**"*. 뒤엣것은 `상태`·`방` 꼴에 대해 **거짓이다** — 실제 Rust 는 이름공간으로 가른다. 고칠 수 있는 결함을 「원리상 못 함」으로 적었다"*. ⚠ **등급이 갈렸다** — 반(反)은 `금지역`, 합(合)은 `거짓신호`. **메인이 합(合) 뒤에 이 잔여를 고쳤다.**

### R6 — 정의 `E2` ⚠ 절이 딛고 선 저장소 상태가 지금 없다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-07-rust-scope-references/dialectic/1-thesis.md` — 반(反)의 좌표 칸 원문: `docs/gates/rust-scope-references.md:1-60` · 특히 `:49`(*"`B1` 이 80 에 못 미치면 → 128 이라 안 발동"*) 대 `1-thesis.md:160-164`
- 반(反) 원문: **정의 `E2` ⚠ 절이 딛고 선 저장소 상태가 지금 없다** — 정은 *"`docs/gates/` 에 이 회차 이름의 게이트 문서가 없고"* 를 근거로 `B1`·`B2` 를 못 봤다고 적었고 대안 라를 그 위에 세웠다. 지금 `docs/gates/rust-scope-references.md` 가 있고 그 안에 `B1` 산출(`128`)과 음성 대조 표가 있다
- 채택·기각과 근거: **합(合)이 채택했다.** 합의 근거 원문 — *"`docs/gates/rust-scope-references.md` 가 있고 `B1`(132) · `B2`(`호출자 6 · 피호출자 3`)이 그 안에 있다. 정의 *"`B1`·`B2` 의 산출을 나는 못 봤다"* 와 대안 라(`S4`)의 전제가 무너졌다. **결과적으로 `E2` 는 두 읽기 모두에서 `S1` 로 모인다** — 정의 갈림이 없어졌다"*. 등급은 반·합이 같다(거짓신호).

### R7 — `E2` 근거 1 이 증인 `V1`~`V10` 을 덮개로 세는데 `V9` 는 등록된 축에서 안 갈린다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-07-rust-scope-references/dialectic/1-thesis.md` — 반(反)의 좌표 칸 원문: 내 실행 마지막 두 줄 · `crates/pal-extract/examples/scope_variants.rs:484-491` · `1-thesis.md:152`(`증인 V1~V10`)
- 반(反) 원문: **`E2` 근거 1 이 증인 `V1`~`V10` 을 덮개로 세는데 `V9` 는 등록된 축에서 안 갈린다** — 지금 도구가 스스로 *"△ 변형 1 개는 **엣지 집합에서 안 갈렸다** … **이 줄을 통과로 읽지 마라.**"* 를 찍는다. 정은 `V9` 를 「판 2 의 재료」로만 넘겼고 `E2` 덮개 셈에서는 뺀 적이 없다
- 채택·기각과 근거: **합(合)이 채택했고 판정문에 반영했다** — `E2` 의 한 줄이 *"**`V9` 는 덮개에서 뺀다**"* 다. 합의 근거 원문 — *"내 실행 마지막 두 줄이 그대로 찍는다 — *"△ 변형 1 개는 엣지 집합에서 안 갈렸다 … **이 줄을 통과로 읽지 마라.**"* 게이트는 `V` 절을 *"이 절이 완수의 증인이다"* 로 세운다. 증인 하나가 미판정인 채로 덮개에 세면 안 된다"*. 등급은 반·합이 같다(거짓신호).

### R8 — 정의 근거 좌표 여럿이 지금 파일에서 그 줄이 아니다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관
- 어디가 걸리나: `.palimpsest/rounds/2026-09-07-rust-scope-references/dialectic/1-thesis.md` — 반(反)의 좌표 칸 원문: `grep -n '아이템_수 > 1' crates/pal-core/src/scope.rs` → `371` · `grep -n 'counts.ambiguous += 1' crates/pal-core/src/projection.rs` → `250` · `grep -n 'fn rule' crates/pal-extract/src/rust_scopes.rs` → `158`
- 반(反) 원문: **정의 근거 좌표 여럿이 지금 파일에서 그 줄이 아니다** — 설계문 `S1` 이 요구하는 「다시 확인할 수 있다」가 그만큼 깨진다. `scope.rs:358-360`→실제 **:369-371** · `projection.rs:238`→**:250** · `rust_scopes.rs:149-151`→**:158-160** · `:245`→**:254** · `:277`→**:286**. (`scopes.rs:191` · `scope_variants.rs:339·343·347·71-93` 은 맞다)
- 채택·기각과 근거: **합(合)이 사실만 채택하고 결론 절반을 기각했다.** 합의 근거 원문 — *"**사실만 채택한다.** 지금 좌표는 `scope.rs:371` · `projection.rs:250` · `rust_scopes.rs:158·254·286` 이다. ⚠ **「정의 근거가 그만큼 깨진다」는 기각한다** — `git show 3de5e81:crates/pal-core/src/scope.rs` 가 정이 적은 `:358` 에 같은 토큰을 두고 있다. **줄이 밀렸을 뿐 코드는 그대로다**"*. ⚠ **등급이 갈렸다** — 반(反)은 `거짓신호`, 합(合)은 `미관`.

### R9 — `E3` 추가 근거의 수 하나가 틀렸다

- 모집단: 회차기록
- 유효성: 거짓
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-07-rust-scope-references/observations/v-variants.txt` — 반(反)의 좌표 칸 원문: 내 실행 `V3` 행 · 계산 자리는 `scope_variants.rs:429-430`(`집합.difference(&기준_집합)`)
- 반(反) 원문: **`E3` 추가 근거의 수 하나가 틀렸다** — 정은 *"`V3` 의 엣지는 4185 이고 기준(4209) 대비 **가짜 7** · 누락 31"* 이라 적었다. 지금 표는 `V3 … 4185 · 가짜 **5** · 누락 31` 이다
- 채택·기각과 근거: **합(合)이 기각했다.** 합의 기각 근거 원문 — *"**정은 안 틀렸다.** `git show 3de5e81:.palimpsest/…/observations/v-variants.txt` 의 `V3` 행이 `4185 · 가짜 **7** · 누락 31` 이다 — 정이 돌린 시점의 값 그대로다. 5 로 바뀐 것은 그 뒤 커밋의 구조체 리터럴 필드 거르기 때문이고, 그것은 **`R1` 이 이미 세는 드리프트 한 갈래**다. 같은 사실을 「정의 오류」로 다시 세면 반론 하나가 둘이 된다. 그리고 7 이든 5 든 정의 논증 방향(*"impl 이 지우는 것은 엣지가 아니라 모호 자리"*)은 안 움직인다"*. ⚠ **유효성이 갈렸다** — 반(反)은 `참`, 합(合)은 그것을 반증했다. **해악도는 반(反)이 매긴 `거짓신호` 를 그대로 옮겼다 — 합(合)은 기각해서 등급을 안 매겼다.**

### R10 — 정이 `E1` 의 검출력 한계를 「조건의 것」으로 넘기는데 조건 문면이 그 반대를 적었다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `.palimpsest/rounds/2026-09-07-rust-scope-references/dialectic/1-thesis.md` — 반(反)의 좌표 칸 원문: `1-design.md`「판정 대상 문면」의 `E1` 셋째 줄 · `1-thesis.md:216-221` · `observations/e1-hand-check.md` 의 「이 판정이 못 잡는 것」 셋째 항목
- 반(反) 원문: **정이 `E1` 의 검출력 한계를 「조건의 것」으로 넘기는데 조건 문면이 그 반대를 적었다** — `E1` 문면은 *"⚠ 균일 30 건은 사전부검이 잰 실측 오류율에서 고장을 47~66% 놓친다"* 로 **표본 크기를 합격선의 일부로 등록했다**. 그런데 정은 뒤집기 ⓐ 에서 *"이 한계는 조건의 것이지 산출의 것이 아니다"* 로 닫았다. 그리고 회차 자신의 손 대조 문서가 *"표본 50 은 … **0.76%** 다 · 오류율이 1% 면 놓칠 확률이 **60%**"* 라고 적는다 — 30 균일을 물린 바로 그 이유가 50 층화에도 남아 있다
- 채택·기각과 근거: **합(合)이 채택했으나 판정은 안 뒤집혔다.** 합의 근거 원문 — *"`e1-hand-check.md:48-51` 이 스스로 *"50 건은 0.76% · 오류율 1% 면 놓칠 확률 60% · 이 표본은 「가짜가 없다」를 말하지 않는다"* 라고 적는다. `E1` 문면의 ⚠ 절도 표본 크기를 합격선 논거로 등록했다. **판정은 안 뒤집힌다**(문면의 합격선은 「50 건에 가짜 0」이고 그것이 충족됐다) — 그러나 `S1` 을 「가짜가 없다」로 옮겨 적으면 그것이 금지역이다"*. 등급은 반·합이 같다(거짓신호).

### R11 — 표본이 못 재는 축에 실제 누락이 있고 어느 변형도 그것을 안 잡는다

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-core/src/scope.rs` — 반(反)의 좌표 칸 원문: `crates/pal-core/src/scope.rs:338-378`(`resolve_shadowing`) 의 `:359`(`else if b.declared_at <= at`) · `crates/pal-extract/src/rust_scopes.rs:247-252`(`let_declaration` → `패턴을_묶되_자리를_잰다`)와 `:313-321`(그 함수가 `hoisted=false` 로 순회한다) · `crates/pal-cli/tests/round_status.rs:15,94,105,121,145,179,198,223,234,292,305,322,331,345,356` · 세기: 정규식 `let (mut )?(\w+) = \2\(` 전수 **62**
- 반(反) 원문: **표본이 못 재는 축에 실제 누락이 있고 어느 변형도 그것을 안 잡는다** — `let X = X(…)` 꼴에서 `resolve_shadowing` 이 **자기 초기화식 안의 지역**을 먼저 고른다(`b.declared_at <= at` 이면 지역이 이긴다). Rust 는 `let` 이 자기 RHS 에서 안 보이므로 그 `X` 는 아이템을 가리켜야 한다. 저장소에서 이 꼴이 **62 자리**이고(예: `tests/round_status.rs` 의 `let root = root(…)` 15 자리 · `fn root` 는 `:15`) 그만큼 참 엣지가 `locals` 로 죽는다. `V1`~`V11` 중 이 축을 켜고 끄는 변형이 없다
- 채택·기각과 근거: **합(合)이 채택했고 반(反)이 못 댄 실증을 합이 냈다.** 합의 근거 원문 — *"★ **반이 못 댄 실증을 내가 댔다.** 저장소 밖 임시 디렉터리에 한 파일을 심고 돌렸다 — `fn root` · `fn caller(){ let root = root("x"); }` · `fn other(){ let z = root("y"); }`. 산출은 **엣지 1** 이고 표본이 찍는 것은 `other → root` 하나다. **`caller → root` 이 없다.** 게다가 `V2`(패턴 선언을 끈다)는 그 줄이 되살아나는 것을 **「가짜 1」로 센다** — 증인 격자가 이 축에서 부호를 뒤집는다. 저장소 전수는 **62 자리**"*. 등급은 반·합이 같다(거짓신호). **메인이 합(合) 뒤에 이것을 고쳤다.**

### R12 — 「기여 파일」 수가 세 값으로 병존하고 어느 것이 `E1` 문면의 126 인지 아무 데도 안 적혔다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `docs/gates/rust-scope-references.md` — 반(反)의 좌표 칸 원문: `1-design.md`「판정 대상 문면」의 `E1` · 내 실행 3 줄(`기여 파일 132`) · `docs/gates/rust-scope-references.md:49`(`128`)
- 반(反) 원문: **「기여 파일」 수가 세 값으로 병존하고 어느 것이 `E1` 문면의 126 인지 아무 데도 안 적혔다** — 조건 문면 **126** · 도구 출력 **132** · 게이트 문서 **128**. 정은 「126 과 132 의 차를 모른다」까지만 적었고 128 은 못 봤다
- 채택·기각과 근거: **합(合)이 유효로 인정하되 「해소됨」으로 적고 살아남은 반론에서 뺐다.** 합의 원문 — *"유효하다. **메인이 이미 실행했다** — 게이트 `## 합격선` 이 126(문면·프로토타입)·132(장치)·132(`pal export`)를 갈라 적고 *"`B1` 을 판정하는 자는 `pal export` 이고 그 값이 132 다"* 를 못 박았다. **살아남은 반론으로 안 센다**"*. ⚠ **등급이 갈렸다** — 반(反)은 `거짓신호`, 합(合)은 등급 칸에 `해소됨` 을 적었고 그것은 원장 enum 밖이다. 여기 실은 값은 반(反)의 것이다.

### R13 — `E3` 대안 아(→`S4`)를 기각한 논거가 되돌아온다

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-extract/examples/scope_variants.rs` — 반(反)의 좌표 칸 원문: `1-thesis.md:210` · 내 실행의 `남은 자리 표본` 은 21 중 **12 만** 찍는다(`scope_variants.rs:250`, `if 보기.len() < 12`)
- 반(反) 원문: **`E3` 대안 아(→`S4`)를 기각한 논거가 되돌아온다** — 정은 *"`S4` 로 적으면 다시 잴 것이 무엇인지가 정해져 있지 않다"* 를 `S5` 의 이유로 들었다. 그러나 `R4`·`R5` 가 보이듯 **①이 무엇으로 이루어졌는지조차 아직 안 갈렸다**(21 중 몇이 `cfg` 인지 회차 어디에도 없다). *"무엇을 다시 잴지 모른다"* 는 조건이 안 재는 것의 증거가 아니라 **아직 안 잰 것**의 증거로도 똑같이 읽힌다
- 채택·기각과 근거: **합(合)이 이 하나를 둘로 갈라 반은 채택하고 반은 기각했다.** 채택 근거 원문 — *"채택한 것은 이 사실뿐이다 — 도구가 21 중 12 만 찍고(`scope_variants.rs:249`) 회차 어디에도 몫이 없다. 내가 그 12 를 갈랐고 **10/2** 였다. 나머지 9 는 여전히 미측정"*. 기각 근거 원문(`R13`(의 `S4` 부분)) — *"**기각.** `S4` 는 「잴 것이 하나로 정해졌는데 증거가 없다」이다. 여기서는 ①과 ②를 **둘 다 완전히 재도** 어느 것이 조건의 뜻인지가 남는다 — ①=21 이면 반증, ②=0 이면 통과이고 문면이 둘을 다 담는다(주어는 「72건」, 술어는 「가짜 엣지가 사라진다」). 재는 것이 판정을 안 닫으면 그것은 미측정이 아니라 **문면의 미결정**이다. 다만 R13 이 든 사실(①의 구성 미측정)은 위에서 채택했다"*. 등급은 반·합이 같다(거짓신호). ⚠ **이 항 하나가 합(合)의 처분 표에서 행 둘을 차지한다** — `## 계수` 가 그 사실을 센다.

### R14 — 정이 「참」의 기계적 바닥으로 든 불변이 갈래 하나를 안 덮는다

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관
- 어디가 걸리나: `.palimpsest/rounds/2026-09-07-rust-scope-references/dialectic/1-thesis.md` — 반(反)의 좌표 칸 원문: `crates/pal-core/src/scope.rs:376-378`(`ScopeParent::Enclosing(next) => cursor = next`) · `1-thesis.md:130-135` 대 `:247-252`
- 반(反) 원문: **정이 「참」의 기계적 바닥으로 든 불변이 갈래 하나를 안 덮는다** — 정은 *"임포트된 이름은 원리상 엣지가 안 된다"* 로 도착지의 안전을 논증했다. 그런데 표본 28 줄 중 **아홉**(9·12·41·42·43·45·46 과 정 표의 10·33)은 `use` 가 아니라 **중첩 `mod` 에서 바깥으로 걸어 나가는 경로**로 해소된다(`scope.rs:376-378`). 그 경로에는 정이 든 불변이 안 걸린다 — 정도 자기 비판 2 에서 인정했지만 **근거 4 의 결론문(*"표본 50 줄의 도착지가 전부 … 이 불변과 맞는다"*)은 고치지 않았다**
- 채택·기각과 근거: **합(合)이 채택했다.** 합의 근거 원문 — *"아홉 줄은 `use` 불변이 아니라 중첩 `mod` 바깥걷기(`scope.rs:376-378`)로 해소된다. 정도 자기 비판 2 에서 인정했는데 결론문을 안 고쳤다"*. 등급은 반·합이 같다(미관).

---

## 메인이 반(反)·합(合) 뒤에 실행한 것 — 여섯을 저장소에서 대조했다

메인이 사실로 실으라고 준 여섯이다. 나는 그것을 **저장소에서 다시 봤다**(읽기만 했다).

1. **합(合)이 잡은 R4 잔여(장치의 함수 문서 주석이 여전히 *"남는 것은 `cfg` 쌍둥이뿐"* 을 단언)를 고쳤다** — 확인했다. `crates/pal-extract/examples/scope_variants.rs` 의 `E3` 함수 주석이 지금 *"⚠ **남는 것이 `cfg` 쌍둥이뿐은 아니다**(정반합 판 1). … 합(合)이 보기 12 개를 열어 **10 은 `cfg` 쌍둥이 · 2 는 아니다** 로 갈랐다"* 다.
2. **합(合)이 잡은 R5 잔여(`rust.rs` 가 그 겹침을 *"이 층에서 못 가른다"* 로 적음)를 고쳤다** — 확인했다. `crates/pal-extract/src/rust.rs:31-40` 이 지금 *"해소하면 엣지 31 건이 는다"* + *"그 58 건은 한 가지가 아니다"* + *"원리상 한계가 아니라 **고칠 수 있는 결함**이다. 승격은 [#133]"* 다. 커밋 `be86499`.
3. **합(合)이 실증한 R11 을 고쳤다 — `ScopeBinding::visible_from` · 시험 둘 · 엣지 4,211 → 4,255 · 모호 58 → 47** — 확인했다. `crates/pal-core/src/scope.rs:115`(`pub visible_from: usize`) · `:383-384` · `:484` 가 서 있고, `observations/v-variants.txt:3` 이 `기준 — 엣지 4255 · 기여 파일 132`, `:5` 가 `모호 47` 이다.
4. **판 2 의 정(正)이 잡은 것 하나(장치 주석이 `RefCounts::unresolved` 를 `pal touch` 의 「내가 모르는 것」이라 적던 것)도 고쳤다** — 커밋 `be86499` 의 메시지가 그렇게 적고 `crates/pal-core/src/projection.rs` 가 그 커밋에서 4 줄 늘었다. **판 2 의 산출물은 안 열었으므로 그 발견의 원문은 내가 못 봤다.**
5. **합(合)이 요구한 `B1` 원 출력을 `observations/b1-export.txt` 로 남겼다** — 확인했다. 파일이 있고 머리가 `File 139 / Symbol 3205 / REFERENCES 4258 / 기여 파일 133 (.rs 132 · 그 밖 1)` 이며 기여한 `.rs` 파일 전부의 목록이 그 아래 있다.
6. **합(合)이 잡은 수 어긋남 셋을 고쳤다** — 셋 중 **둘만 확인된다.** ① `measurements.md` 의 11,875 → 지금 `:37` 이 `미해소 11,889` 이고 `v-variants.txt:5` 도 `미해소 11889` 다(맞았다). ② 게이트가 두 계수기를 한 줄에 섞은 것 → 지금 게이트가 *"비교: 장치가 센 참조가 **6,639**, `pal export` 가 센 엣지가 **4,258** 이다. ⚠ **두 수는 서로 다른 계수기의 것이다**"* 로 출처를 붙여 갈라 적는다(맞았다). ③ **`rust.rs` 의 옛 값 — 안 고쳐졌다.** 아래 항이 그것이다.

### 합(合)이 지목한 수 어긋남 셋 중 `rust.rs:28` 의 옛 값이 그대로 남아 있다

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `crates/pal-extract/src/rust.rs:28` — 지금 그 줄은 *"비교: 같은 저장소에서 **실제로 서는 참조가 6,584 · 엣지 4,179** 다"* 다. 같은 값이 `crates/pal-extract/src/cached.rs:6` 에도 있다
- 채택·기각과 근거: 합(合)의 「초바뀐다」 7 번 셋째 불릿이 *"`crates/pal-extract/src/rust.rs:28` — *"참조 6,584 · 엣지 4,179"*. 옛 값이다"* 로 이 자리를 지목했고, 메인은 나에게 *"합(合)이 잡은 수 어긋남 셋(… · `rust.rs` 의 옛 값)을 고쳤다"* 를 사실로 실으라고 줬다. **`git show be86499 -- crates/pal-extract/src/rust.rs` 가 고친 것은 `:31-33` 의 문면(R5)이고 `:28` 의 수는 안 건드렸다.** 지금 실측은 참조 6,639 · 엣지 4,255(장치) · 4,258(`pal export`)이다. **판정을 바꾸는 항이 아니다 — 메인이 준 여섯 중 하나가 저장소에서 확인되지 않는다는 사실만 적는다.**

---

## 내가 기각한 것

취합하며 발견처럼 보였으나 내가 산출하지 않기로 한 것 둘이다.

### 합(合)이 반론 하나(`R13`)를 두 행으로 세어 처분 행이 반(反)의 번호보다 하나 많다

- 모집단: 회차기록
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: `.palimpsest/rounds/2026-09-07-rust-scope-references/dialectic/1-synthesis.md` — 채택 표 13 행 + 기각 표 2 행 = 15 행인데 반(反)의 번호는 R1~R14 열넷이다
- 채택·기각과 근거: **기각한다.** 합(合)이 그 갈림을 **숨기지 않고 이름으로 못 박았다** — 기각 표의 행 이름이 `R13`(의 `S4` 부분)이고, 자기 분포 표의 기각 행도 *"`R9` · `R13`(의 `S4` 부분)"* 으로 적는다. 그리고 채택 표의 `R13` 행이 *"채택한 것은 이 사실뿐이다"* 로 무엇을 채택했는지를 좁힌다. **조용히 부풀린 것이 아니라 선언된 분할이다.** 다만 행 수가 갈리는 사실 자체는 `## 계수` 에 수로 적었다.

### 종료 판단의 거짓신호 6 이 합(合)의 8 과 안 맞는다

- 모집단: 회차기록
- 유효성: 거짓
- 해악도: 미관
- 어디가 걸리나: `.palimpsest/rounds/2026-09-07-rust-scope-references/dialectic/1-referee.md` — 종료 판단의 분포는 금지역 0 · 실패 0 · 거짓신호 6 · 미관 3 이고 합(合)의 분포는 금지역 1 · 실패 0 · 거짓신호 8 · 미관 3 이다
- 채택·기각과 근거: **기각한다.** 종료 판단이 스스로 *"메인이 셋을 실행한 뒤의 분포다"* 와 *"★ **금지역이 사라진 경위를 기록한다.** 합(合) 시점 분포는 금지역 1 · 거짓신호 8 · 미관 3 이었다"* 를 적어 두 분포가 다른 시점의 것임을 명시했다. 빠진 셋은 메인이 실행했다고 준 목록의 `R4`(금지역) · `R5`(거짓신호) · `R11`(거짓신호)과 정확히 맞고, 나는 그 셋의 수정을 저장소에서 확인했다(위 절 1·2·3). ⚠ **다만 종료 판단 문서 자체는 빠진 둘의 이름을 안 적는다** — 이름을 안 적어도 게이트 산출이 안 바뀌므로 발견으로 세지 않는다.

---

## 계수 — 두 원천 대조

| 잰 것 | 반(反) | 합(合) | 같나 |
|---|--:|--:|---|
| 반론 번호(고유 ID) | 14 (R1~R14) | 14 (R1~R14) | 같다 |
| 처분 표의 **행 수** | 14 (반론 표 14 행) | **15** (채택 13 + 기각 2) | **갈린다** — `R13` 이 채택·기각 양쪽에 선다 |
| 채택 | (반은 처분을 안 한다) | 13 행 / 12 고유 ID (R1·R2·R3·R4·R5·R6·R7·R8·R10·R11·R12·R13·R14) | — |
| 기각 | (반은 처분을 안 한다) | 2 행 / 2 고유 ID (R9 · R13 의 `S4` 부분) | — |
| 「해소됨」(합이 살아남은 반론에서 뺀 것) | (없는 축) | 1 (R12) | — |
| 살아남은 반론 | (반은 안 센다) | 12 | — |
| 해악도 · 금지역 | **2** (R4 · R5) | **1** (R4) | **갈린다** |
| 해악도 · 실패 | 0 | 0 | 같다 |
| 해악도 · 거짓신호 | **11** (R1·R2·R3·R6·R7·R8·R9·R10·R11·R12·R13) | **8** (R1·R2·R5·R6·R7·R10·R11·R13) | **갈린다** |
| 해악도 · 미관 | **1** (R14) | **3** (R3 · R8 · R14) | **갈린다** |
| 유효성 · 참 | 14 | 13 (합이 R9 를 반증했다) | **갈린다** |
| 유효성 · 거짓 | 0 | 1 (R9) | **갈린다** |
| 반(反)이 스스로 물린 것 | 7 개 불릿(「내가 스스로 물린 것」) | (합의 처분 대상 아님) | — |
| 반(反) 라운드 | 1 / 상한 2 | — | — |

**등급이 갈린 자리 다섯을 이름으로 적는다** — 맞추지 않았다.

| 반론 | 반(反)의 등급 | 합(合)의 등급 | 어느 쪽이 어떻게 갈렸나 |
|---|---|---|---|
| R3 | 거짓신호 | 미관 | 합이 *"지금은 둘 다 참이다 … 남는 것은 문장 고치기뿐"* 으로 내렸다 |
| R5 | **금지역** | 거짓신호 | 합이 R4 와 같은 자리(`scope_variants.rs:225`)를 R4 에 몰아 금지역을 하나로 뒀다 |
| R8 | 거짓신호 | 미관 | 합이 *"줄이 밀렸을 뿐 코드는 그대로다"* 로 결론 절반을 기각하고 내렸다 |
| R9 | 거짓신호 | (등급 없음 · 기각) | 합이 *"정은 안 틀렸다"* 로 반증했다 |
| R12 | 거짓신호 | (등급 없음 · 「해소됨」) | 합이 메인이 이미 실행했다고 보고 살아남은 반론에서 뺐다 |

**세 원천의 해악도 분포**(합은 자기 시점, 종료 판단은 메인 수정 뒤 시점이다 — 문서 둘이 그렇게 적는다).

| 해악도 | 반(反) | 합(合) | 종료 판단 |
|---|--:|--:|--:|
| 금지역 | 2 | 1 | 0 |
| 실패 | 0 | 0 | 0 |
| 거짓신호 | 11 | 8 | 6 |
| 미관 | 1 | 3 | 3 |
| 합계 | **14** | **12** | **9** |

⚠ **종료 판단은 빠진 셋의 이름을 안 적는다.** 나는 메인이 준 실행 목록과 저장소 대조로
그 셋이 `R4`·`R5`·`R11` 임을 확인했으나, **그 이름은 종료 판단 문서에 없다.**

---

## 종료 판단

`pal-debate-referee` 가 산출한 문장 그대로다.

> ## 답
>
> **끝났다.**
>
> ## 근거 — 살아남은 반론의 해악도
>
> 메인이 셋을 실행한 뒤의 분포다.
>
> - **금지역 ①·② 0 · 실패 0** — 교착 사유가 없다. 이 넷만이 「못 닫는다」를 만든다.
> - **거짓신호 6** — 게이트 문면이 *"닫을 수 있다. 판정으로 적고 메인이 고친다"* 다. 남아 있는 것이 닫힘을 막지 않는다. 판정에 적힌 채로 넘어간다.
> - **미관 3** — *"닫을 수 있다."*
>
> ★ **상한이 남았지만 그것이 「한 라운드 더」의 사유가 아니다.** 「한 라운드 더」는
> 살아남은 반론에 금지역이나 실패가 있을 때다. 상한이 남았다는 사실만으로 도는 것은
> 소진을 끝 조건으로 쓰는 것의 거울상이고, 설계문 §11 실측(네 계열 전부 dry 0 회)이
> 막으려던 형태다. 반(反) 라운드 2 는 쓰지 않고 남긴다.
>
> ⚠ **`N4` 반증 신호 점검 — 걸리지 않는다.** *"반론이 전부 「미관」으로 나오면 종료
> 신호가 아니라 반증 신호"* 인데, 살아남은 9 건 중 6 건이 거짓신호다. 미관 단독이 아니다.
>
> ★ **금지역이 사라진 경위를 기록한다.** 합(合) 시점 분포는 금지역 1 · 거짓신호 8 ·
> 미관 3 이었다. 금지역 R4 는 그 자리에서 「교착 또는 승격」이었고, 소진이 아니라
> **메인의 수정으로** 0 이 됐다. 수정이 실제로 그 금지역을 닫았는지는 종료 판단의
> 입력이 아니다 — 그 판단은 합(合)이 이미 했고, 다시 하면 종료 판단이 아니라 또 하나의
> 반(反)이 된다. 종료 판단은 넘겨받은 분포를 게이트에 넣을 뿐이다.

**설계문이 등록한 상한**: 정(正) 1 회 · 반(反) 최대 2 라운드 · 합(合) 1 회 · 종료 판단 1 회.
**쓴 것**: 정 1 · 반 1 · 합 1 · 종료 판단 1. **반(反) 라운드 하나가 안 쓰인 채 남았다.**

---

## 소유자에게 올릴 물음

**물음 하나에 칸 하나다.** 두 원천이 서로 다른 답을 냈으므로 둘 다 옮긴다.

**칸 1 — 합(合)이 올린 것 (원문 그대로)**

> **소유자가 정할 것 한 문장** — *`E3` 을 「가짜 엣지 0」만 재는 조건으로 좁혀 이 회차에서
> 닫을 것인가(축소를 승인), 아니면 남은 21 자리의 구성(진짜 `cfg` 몇 · `A10` 과잉결박 몇)을
> 세어 그 몫을 이슈로 갈라낸 뒤 닫을 것인가.*

합(合)이 그 칸에 붙인 근거 둘, 원문 그대로:

> 1. **살아남은 반론 중 가장 무거운 것이 `R4` — 금지역이다.** 안 잰 사실을 사실로 적어
>    `E3` 의 판정 이름을 갈랐고, 그 거짓 문면이 아직 `scope_variants.rs:225` 에 살아 있다.
> 2. **`E3` 이 `S5` 라서 설계문이 미리 등록한 `P-c` 가 발화한다.** `E3` 은 **소유자 답 자체를
>    다시 재는 조건**이다 … 떼어내면 조건이 재는 양이 줄고, 규약 §5 가 그것을 **축소 = 승격**으로 못 박았다.
>
> ⚠ **한 칸이다.** `R4` 와 `E3` 이 같은 물음의 앞뒤라 칸을 둘로 안 낸다.
> `E1`·`E2` 는 승격 대상이 아니다 — 메인이 위 「고치나」를 실행하고 진행한다.

**칸 2 — 종료 판단이 올린 것 (원문 그대로)**

> ## 소유자에게 올린다면 — 물음 한 문장
>
> 해당 없음. 금지역·실패가 0 이고 상한도 남았으므로 승격 사유가 없다.

⚠ **두 원천이 갈린다.** 합(合)은 「올린다 · 한 칸」이고 종료 판단은 「해당 없음」이다.
종료 판단의 근거는 **메인의 수정으로 금지역이 0 이 된 뒤의 분포**이고, 합(合)의 승격
사유 둘 중 **둘째(`E3` = `S5` → `P-c` 발화 → 축소는 승격)는 금지역 수와 무관하다** —
`E3` 의 판정 이름은 `S5` 로 그대로 서 있고 메인의 수정 어느 것도 `E3` 문면을 안 고쳤다.
**나는 어느 쪽이 옳은지 정하지 않는다. 갈렸다는 사실을 소유자 앞에 그대로 놓는다.**
