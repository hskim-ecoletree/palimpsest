# 사전부검 2라운드 — 원 반환문

> `pal-premortem-sweeper` 가 돌려준 것을 그대로 옮겼다. 요약하지 않는다.
> 원 의도와 **1라운드 처분으로 고친 계획 v2** 만 받았다. 대화 기록도 1라운드 결과도 안 받았다.
> 격리 클론에서 `pal export`·`cargo xtask check --root <사본>`·`record.py gate` 를 실제로 돌렸다.
> ⚠ 공통 전제: 쓴 `pal` 바이너리는 `target/debug/pal`(2026-08-20 빌드, `9f0bf70`)이고 HEAD 는 `930a295` 다.

---

### `pal export` 의 `missing` 은 「인스턴스 0」이 아니다 — 계획 4단계 ⓒ 가 범주 오류다
- 어떻게 실패하나: 계획 4는 `missing`(`not_built`/`not_stored`)을 *"매핑 대상 중 인스턴스가 0 인 것이 무엇인가"* 의 답으로 쓴다. 실제로 `missing` 은 **내보내기가 손으로 정한 라벨 셋(`File`·`Symbol`·`REFERENCES`)의 여집합**이지 인스턴스 수와 무관하다. 실측: 클론에서 `Binding` 이 `not_stored` 로 나왔는데 정본 `.palimpsest/intent/bindings.jsonl` 에는 **결박 18 행**이 산다.
- 어디가 걸리나: `crates/pal-cli/src/export.rs:256-289` `missing_labels()` · `:177` `cypher()` · `.palimpsest/intent/bindings.jsonl`
- 획득: 조회 — HEAD 소스를 읽고, 클론에서 `pal export --json` 을 돌려 `Binding: not_stored` 를 받은 뒤 `wc -l bindings.jsonl` = 19(헤더 1 + 결박 18)로 대조
- 모집단: 저장소 / 유효성: 참 / 해악도: **금지역** (기본 다섯의 「사실이 아닌 것을 사실로」) / 대상: 계획대상
- 얼마나 아픈가: ADR 은 사후에 안 고친다(`docs/adr/README.md:3`). 걸리는 곳은 라벨 18 개 중 `missing` 15 개 전부.

### 같은 커밋에서 `pal export` 가 두 답을 낸다 — 2층이 낡았는지에 달렸다
- 어떻게 실패하나: 같은 바이너리·같은 커밋(`930a295`)인데 **저장소의 기존 `index.redb`(8월 20)로 돌리면 `File 122 · Symbol 2633 · built_for_this_snapshot=false`**, **갓 `pal query` 로 세운 클론에서는 `File 133 · Symbol 3018 · built_for_this_snapshot=true`** 다. `matches_worktree` 는 **양쪽 다 `true`** 라 이 차이를 안 가린다.
- 어디가 걸리나: `crates/pal-cli/src/export.rs:113` `Projection::open_read_only` · `:300-330` `envelope()` · 실물 `.palimpsest/index.redb`(mtime 8월 20)
- 획득: 조회 — 두 번 실행해 봉투를 갈라 비교했다(`--cache-dir` 를 임시로 돌려 저장소를 안 건드렸다. `git status` 로 무변경 확인)
- 모집단: 저장소 / 유효성: 참 / 해악도: **금지역** (「사실이 아닌 것을 사실로」) / 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 다만 **차이가 라벨마다 다르고**, `File` 은 파싱된 파일만 세므로 대장의 1011 중 122~133 이다 — *"이 온톨로지가 담는 파일 수"* 로 읽으면 8 배 과소다.

### `git log` 오라클은 staleness 탐지 밖이다 — 트리 다이제스트가 이력을 안 본다
- 어떻게 실패하나: 계획 10이 「선정 규칙 커밋이 후보 판정 커밋보다 앞선다」를 `oracle` 로 등록한다. 그런데 evidence 의 현재성을 재는 `projected_digest` 는 **tracked 워킹트리 블롭만** 훑는다 — HEAD 도 커밋 그래프도 안 든다. 등록 뒤에 rebase·amend·squash 로 **주장한 순서를 뒤집어도 evidence 는 계속 `met`** 이다. PR 을 squash merge 하면 두 커밋이 하나가 되어 명제 자체가 사라지는데도 원장은 초록이다.
- 어디가 걸리나: `crates/pal-git/src/lib.rs:426-432` `worktree_digest_excluding` · `crates/pal-cli/src/round/verify.rs:424-430` `projected_digest`
- 획득: 조회 — 두 함수 본문(`scan_worktree_with_stat(false)` → tracked 목록만)
- 모집단: 자기장치 / 유효성: 참 / 해악도: **금지역** (「측정이 죽은 가지」) / 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(조건을 다르게 고른다). 계획이 낸 예시 하나가 유일한 결정론 후보라면 이 회차엔 결정론 조건이 사실상 0 이다.

### `## 효과` 에 `pal export` 를 붙이면 §8 이 요구한 것을 안 잰 채 3번 조건이 켜진다
- 어떻게 실패하나: §8 은 *"테스트도 CI 도 아닌 것이 **그 회차의 산출**을 돌린 출력"* 을 요구한다. 이 회차의 산출은 ADR 과 게이트 문서이고 `pal export` 는 이 회차가 만든 것도 고친 것도 아니다. §11 종료 조건 3 이 「효과를 봤다」로 켜지는데 실제로 돌아간 것은 **이 회차와 무관한 기존 명령**이다.
- 어디가 걸리나: `.claude/skills/round/SKILL.md:581-585` (§8) · `:735` (§11 조건 3) · 계획 4단계
- 획득: 조회 — §8 원문과 §11 조건 3 을 읽고 계획의 산출 목록과 댔다
- 모집단: 규약 / 유효성: 참 / 해악도: **금지역** (「측정이 죽은 가지」) / 대상: 계획자신
- 얼마나 아픈가: 되돌리기 쉽다. 다만 **결정만 내는 회차는 효과가 원리상 다음 회차에 있다**는 것이 진짜 문제라, 「원리상 못 잰 것」으로 적는 길이 정직하다.

### 판정 표에 「획득」 열을 더하면 표준 표가 인식되지 않고 끝난 회차가 빨개진다
- 어떻게 실패하나: 계획 3이 *"판정마다 「획득: 조회/추정」을 표기한다"* 를 요구한다. 그것을 `## 판정` 의 표준 표에 세 번째 열로 넣으면 `record.py gate` 가 `표준표: false` 를 내고 **`형식오류` 는 빈 배열**이라 진단도 안 준다. 그러면 `check_ledger_pair` 가 실패한다.
- 어디가 걸리나: `.claude/skills/round/bin/record.py`(`gate` 파서) · `xtask/src/main.rs:5186-5205` · `SKILL.md:626`
- 획득: 조회 — 3열 표와 2열 표를 각각 만들어 `record.py gate` 에 물었다. 3열 → `표준표: false`, 2열 → `표준표: true`
- 모집단: 규약 / 유효성: 참 / 해악도: 실패 / 대상: 계획자신
- 얼마나 아픈가: 되돌리기 쉽다(획득을 표 **아래** 별도 표로). 한 곳.

### 게이트 문서가 「저장소 밖 증거」나 업스트림 경로를 마크다운 링크로 적으면 죽은 링크 검사가 빨개진다
- 어떻게 실패하나: 계획 8의 소문자 근거는 맞다 — 그래서 `docs/gates/openmetadata-decision.md` 는 **모집단 안**이고 그 안의 모든 마크다운 링크가 해소된다. 계획 9는 증거를 저장소 밖 scratchpad 에 두는데 그것을 링크로 적는 순간 죽는다. 업스트림 스펙 경로를 링크로 적어도 같다. 실측: 사본에 그런 게이트 문서를 심으니 **`FAIL 죽은 링크 부재 — 죽은 링크 2건`**.
- 어디가 걸리나: `xtask/src/main.rs:2440-2459` · `:2461-2505` `check_dead_links` · `:2787-2799` `파일_경로인가`
- 획득: 조회 — 격리 사본에서 심고 `xtask check --root <사본>` 으로 재현
- 모집단: 자기장치 / 유효성: 참 / 해악도: 실패 / 대상: 계획자신
- 얼마나 아픈가: 되돌리기 쉽다(코드 표기 + http URL 만 링크로). `.palimpsest/rounds/**/*.md` 도 같은 모집단이다.

### 「증거를 저장소 밖에」와 「judgment 이벤트」가 서로를 막는다
- 어떻게 실패하나: 계획 10은 비결정론 조건을 `judgment` 이벤트로 남긴다. 그 이벤트의 세 참조는 `validate_ref` → `valid_cwd` 를 지나며 **`/` 로 시작하면 거부**된다. 계획 9대로 정·반·합 산출을 scratchpad 에 두면 원장에 못 싣고, 실으려면 저장소 안에 둬야 해서 계획 9가 깨진다.
- 어디가 걸리나: `crates/pal-cli/src/round/ledger.rs:449-460` `validate_ref` · `:470-482` `valid_cwd` · `:288-312` `Event::Judgment`
- 획득: 조회 — 두 함수 본문. ⚠ 실행으로는 안 쟀다
- 모집단: 자기장치 / 유효성: 참 / 해악도: 실패 / 대상: 계획자신
- 얼마나 아픈가: 되돌리기 쉽다. 다만 **둘 중 하나를 반드시 접어야 한다**.

### 「저장소 밖 증거」가 과잉 처방이다 — 직전 회차의 재현 장치는 바로 그 자리에 산다
- 어떻게 실패하나: 계획 9의 근거 셋은 **참**이다(사본에서 `.json` 심어 `FAIL sunset 선언` 재현). 그러나 처방이 「증거 원본을 저장소 밖으로」인 것은 과잉이다 — 실측: 같은 회차 디렉터리에 `export.cypher`(652 KB)와 `notes.txt` 를 넣고 돌리니 **23/23 초록**이다. 직전 회차 `2026-09-03-gate-parser-schema` 는 `verification/mutate.py`·`red/red-observation.md` 를 회차 디렉터리에 두고 게이트가 링크한다. 회차가 끝나면 scratchpad 가 지워지고 남는 것은 다이제스트 한 줄이라, 아무도 근거를 다시 못 본다.
- 어디가 걸리나: `xtask/src/main.rs:3160-3166` `회차_산출`(`jsonl`·`tsv` 만) · `:2631-2712` `check_sunset` · `docs/sunset.toml:29` · `docs/gates/gate-parser-schema.md:44-47`
- 획득: 조회 — 사본에 `.json`(빨강)·`.cypher`·`.txt`(초록)를 각각 심어 갈랐다
- 모집단: 자기장치 / 유효성: 참 / 해악도: 거짓신호 / 대상: 계획자신
- 얼마나 아픈가: 되돌리기 쉽다(확장자만 피한다).

### 봉투 JSON 에 저장소 디렉터리 이름이 박혀 사본의 다이제스트가 재현 불가다
- 어떻게 실패하나: `pal export --json` 의 `snapshot[0][0]` 은 **저장소 디렉터리 이름**이다 — 사본에서는 `"pal-clone-1788608415"`, 원본에서는 `"palimpsest"`. 사본에서 뜬 봉투의 다이제스트는 이 저장소에서 다시 돌려도 **영원히 안 맞는다**. ADR-0023 이 *"공유되는 산출물은 기계 고유의 값을 안 싣는다"* 로 못 박은 자리다.
- 어디가 걸리나: `crates/pal-cli/src/export.rs:300-330` `envelope()` · `docs/adr/0023-...md`
- 획득: 조회 — 사본과 원본에서 각각 봉투를 떠 `snapshot` 을 비교했다
- 모집단: 저장소 / 유효성: 참 / 해악도: 거짓신호 / 대상: 계획자신
- 얼마나 아픈가: 되돌리기 쉽다(다이제스트 대상을 `.cypher` 본문으로 못 박는다). 한 곳.

### 엣지 3 건 위에서 긴장 ⑤(엣지 정밀도)를 판정한다
- 어떻게 실패하나: 이 저장소의 2층은 **심볼 3018 에 `REFERENCES` 3 건**이다(F07 미구축). *"우리 엣지는 정밀하다"* 든 *"이질적 엣지를 감당할 수 있다"* 든 근거가 엣지 3 건이다. 판정문에 그 사실이 안 실리면 읽는 사람은 그래프가 조밀하다고 믿는다.
- 어디가 걸리나: `pal query graph.dump` 실측(노드 3018 · 엣지 3) · `crates/pal-cli/src/export.rs:216-224`
- 획득: 조회 — 사본에서 `graph.dump` 와 `export --json` 둘 다 3 을 냈다
- 모집단: 저장소 / 유효성: 참 / 해악도: 거짓신호 / 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(판정에 「엣지 3 건 위의 추정」을 명시). ⑤ 판정 후보 전부.

### §11 ③ 의 (가)/(나) 를 착수 시점에 안 골랐고, 적용 이슈는 「분할이 아니라 버림」에 걸릴 수 있다
- 어떻게 실패하나: §11 ③ 은 **착수 시점에** (가)/(나) 를 고르라고 요구한다. 계획은 그 선택을 안 적은 채 7번에서 (나)의 문법만 빌려 쓴다. 판정이 「안 쓴다」로 나면 적용 이슈는 **열릴 내용이 없는 채로** 프론티어에 선다. 게다가 적용은 이미 `## 범위 밖` 이고, §5 는 범위 밖을 *"빚이 아니라서 닫을 것이 없다"* 로 못 박는다 — 이슈로 세우면 없던 빚이 생긴다.
- 어디가 걸리나: `SKILL.md:783-790` · `:797` · `:645-651`
- 획득: 조회 — §11 ③ 과 §10 원문
- 모집단: 규약 / 유효성: 참 / 해악도: 거짓신호 / 대상: 계획자신
- 얼마나 아픈가: 되돌리기 쉽다 — **판정이 난 뒤에** 세우고, 「안 쓴다」면 이슈 대신 ADR 한 줄로 닫는다.

### ADR 에서 매핑을 빼고(5) 증거도 저장소 밖에 두면(9) 「무엇에 근거해 채택됐나」가 남지 않는다
- 어떻게 실패하나: 계획 5의 근거는 타당하다. 그러나 계획 9가 증거 원본까지 밖으로 보내면 **매핑은 어디에도 안 남고** ADR 에는 결론과 산문만 남는다. `docs/adr/README.md` 가 *"근거 절을 나중에 손보면 그 결정이 무엇에 근거해 채택됐는지가 사라진다"* 라고 적은 상태를, 이번엔 사후 수정이 아니라 **처음부터** 만든다.
- 어디가 걸리나: `docs/adr/README.md:3-15` · 계획 5·9
- 획득: 조회 — README 원문과 계획 두 항을 댔다
- 모집단: 규약 / 유효성: 참 / 해악도: 거짓신호 / 대상: 계획자신
- 얼마나 아픈가: 되돌리기 어렵다 — ADR 은 사후에 안 고친다. 해법은 매핑을 **게이트 문서**(수정 가능·모집단 안)에 남기고 ADR 이 그것을 가리키는 것이다.

### K=4 가 이미 이름 붙은 넷과 같아 선정 규칙이 아무도 못 떨어뜨린다
- 어떻게 실패하나: 인터뷰가 이미 후보군을 이름으로 넷 고정했다. 거기에 K=4 를 등록하면 **규칙이 산출할 수 있는 답이 하나뿐**이고 「탈락 사유」는 0 건이 된다. 규약이 반복해 경고한 *"이 검사가 실패할 수 있는 길이 있는가"* 에 걸린다.
- 어디가 걸리나: 계획 1·2 · `state.md`(인터뷰 표 「비교 범위」)
- 획득: **추정** — 규칙 문안이 아직 없어 못 물었다
- 모집단: 회차기록 / 유효성: 추정 / 해악도: 거짓신호 / 대상: 계획자신
- 얼마나 아픈가: 되돌리기 쉽다 — **탈락이 최소 하나 나오도록 K 를 후보 모집단보다 작게 잡는다.**

### 비목표 두 행을 「지나기만」 하면 소유자가 「설계할 필요 없다」고 한 로드맵이 옆문으로 결정된다
- 어떻게 실패하나: 「범용 지식 그래프」 행의 사유가 *"노드 타입은 코드 좌표에 결박 가능한 것과, 그것에 결박되는 것뿐이다"* 라, **채팅·문서 엔티티를 담는 어휘를 「차용한다」고만 적어도 그 행이 실질적으로 뒤집힌다.** 계획 6은 「뒤집으려면 흔적을 남긴다」만 적고, **안 뒤집었다고 선언하는 문장**은 요구하지 않는다.
- 어디가 걸리나: `docs/plan/00-goals.md:308-309` · `:3`
- 획득: 조회 — 표 원문 두 행과 사유를 읽었다. 계획이 어느 축을 고를지는 못 물었다
- 모집단: 원의도 / 유효성: 추정 / 해악도: 거짓신호 / 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다 — 게이트에 *"이 판정은 두 비목표 행을 뒤집지 않는다"* 를 ID 붙은 명시 조건으로 등록한다.

### 온톨로지 인스턴스 실사를 하려면 `narrative.unbound` 가 필요한데 그 질의가 에러를 낸다
- 어떻게 실패하나: 갓 뜬 클론에서 `pal query narrative.unbound` 가 **`Error: 개체를 남기지 못했다 / 읽기로 연 의도 저장소에 쓰려 했다`** 로 죽는다. `binding.status` 는 살지만 *"결박이 아직 없습니다"* 를 낸다 — 정본 JSONL 에 18 행이 있는데도. **인스턴스 실사는 기계가 아니라 파일을 손으로 세야 나온다.**
- 어디가 걸리나: `pal query narrative.unbound` · `.palimpsest/intent.redb`(파생·gitignore) · `.gitignore:33-35`
- 획득: 조회 — 사본에서 두 질의를 돌렸다. ⚠ 바이너리가 8월 20 판이라 HEAD 빌드에서 같은지는 안 쟀다
- 모집단: 저장소 / 유효성: 추정 / 해악도: 실패 / 대상: 계획대상
- 얼마나 아픈가: 우회 가능(정본 JSONL 을 직접 센다).

### 이 회차가 도구로 고른 `export.rs` 의 머리 주석이 스키마 수를 틀리게 적고 있다
- 어떻게 실패하나: 고른 기계 자리 바로 위 주석이 **`스키마의 노드 여덟 · 엣지 여덟`** 이다(실물은 10·8). 이 주석은 어느 검사의 모집단에도 안 든다. 회차가 export 를 인용하며 이 문장을 같이 옮기면 세 번째 틀린 수가 생긴다.
- 어디가 걸리나: `crates/pal-cli/src/export.rs:23` · 대조: `docs/graph-schema.md:6`
- 획득: 조회 — 주석 줄과 검사 산출을 댔다
- 모집단: 저장소 / 유효성: 참 / 해악도: 거짓신호 / 대상: 계획대상
- 얼마나 아픈가: 한 줄. 범위 밖이면 「범위 밖」에 적으면 된다.

### 소문자 게이트 이름의 이면 — 동결돼야 할 판정 문서가 영구히 수리 요구를 받는다
- 어떻게 실패하나: 계획 8의 근거는 참이지만 절반이다. 소문자면 모집단에 들어와 **링크가 검사되는 대신, 나중에 링크 대상이 사라질 때마다 이 「동결된 판정 문서」를 고쳐야 초록이 된다.**
- 어디가 걸리나: `xtask/src/main.rs:2452-2459` · `:2411-2418`
- 획득: 조회 — 함수 본문과 그 위 표
- 모집단: 저장소 / 유효성: 참 / 해악도: 미관 / 대상: 계획자신
- 얼마나 아픈가: 저장소가 이미 그렇게 산다. 계획을 바꿀 이유는 없다 — **다만 「빠진다/든다」를 장단으로 함께 적어야** 다음 사람이 소문자를 「검사받으려고 골랐다」로만 읽지 않는다.

---

## 이 라운드가 기각한 것

| 제목 | 어떻게 실패한다고 적었나 | 어디가 걸리나 | 획득 | 모집단 | 유효성 | 대상 |
|---|---|---|---|---|---|---|
| 2층이 안 서 있어 계획 4단계가 통째로 헛돈다 | `pal export` 가 색인 없이는 실패하니 아무것도 못 낸다 | `export.rs:113` | 조회 — 첫 실행은 정말 실패했지만 `pal query graph.dump` **한 번**으로 2층이 서고 그 뒤 652 KB · 18 라벨 전부를 냈다 | 저장소 | 거짓 | 계획대상 |
| 오라클 등록에 blake3 다이제스트를 손으로 계산해야 한다 | `oracle_digest` 를 내는 CLI 가 없다 | `ledger.rs:383-400` | 조회 — 기존 `verification.log` 셋의 `oracle` 줄에 digest 칸이 **없다**. 내부에서 계산된다 | 자기장치 | 거짓 | 계획자신 |
| `pal query` 가 저장소를 더럽혀 회차 커밋을 오염시킨다 | 2층 산출물이 워킹트리에 남는다 | `.gitignore:33-35` | 조회 — 클론에서 돌린 뒤 `git status` 가 **완전히 비었다** | 저장소 | 거짓 | 계획대상 |
| `--root` 를 줘도 원본을 재서 격리 측정이 헛것이다 | `repo_root()` 가 `CARGO_MANIFEST_DIR` 로 박힌다 | `xtask/src/main.rs:114-127` | 조회 — 사본에만 회차를 더하니 판정문이 `회차 16 → 17` 로 바뀌었다. 이 판의 `--root` 는 존중된다 | 자기장치 | 거짓 | 계획자신 |

---

## 이 라운드가 낸 새 범주

**효과가 회차 밖에 있는 회차 유형** — 결정만 내는 회차는 §8 의 「그 회차의 산출을 돌린 출력」을 원리상 자기 안에서 못 낸다. 그 자리를 무관한 기존 명령의 출력으로 채우면 종료 조건 3 이 거짓으로 켜진다.
