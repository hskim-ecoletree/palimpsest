# 잠긴 의도 — 지형과 완성 장면

> 회차 `2026-09-06-terrain-and-completion-scene` · 착수 커밋 `7c41b81`
> 상태는 [`state.md`](state.md) 가 진다.

## 원문

소유자가 인터뷰 네 라운드에서 고른 것. **요약하지 않는다.**

**라운드 1**

- 이 회차가 세우는 지형은 **「코어와 하네스 한 장」**.
- 「완성 장면」의 주인공은 **「둘 다 — 같은 장면의 안쪽과 바깥쪽」**.
- 이 회차는 **「순서표가 서고 그 첫 항목이 지금 이슈로 서 있다」** 가 참이면 끝.
- 초안은 **「정반합으로 초안을 세워 올린다」**.

**라운드 2**

- 순서표의 항목 하나는 **「능력 계약 `C1~C6`」**.
- 재고는 넷 다 만난다 — **「fabricate 의 승계 후보 둘을 읽는다」** · **「걷힌 옛 기능 문서
  스물넷의 순서 근거를 본다」** · **「결박 노드 타입 공백을 다룬다」** ·
  **「P8 생성형 큐레이션의 자리를 지형에 둔다」**.
- 정반합은 **「갈리는 자리만 골라 두세 판」**.
- 상한은 **「인터뷰 4 · 사전부검 3 · 독립 리뷰 5」**.

**라운드 3**

- 하네스는 **「C1~C6 의 소비자다」**.
- 결박 노드 타입 공백은 **「이 회차에서 만든다」**.
- §8 「효과」에는 **「pal 로 이 회차의 결정을 조회해 본다」**.
- 산출은 **「docs/plan/ 새 문서 + AGENTS.md 두 칸은 가리키기만」**.

**라운드 4**

- 결박은 **「스키마 + 저장 + 조회」** 까지.
- 「소비 가능해지는 지점에 가장 가까운 것」은 **「능력마다 지금 무엇이 서 있는지를 실측한다」** 로 잰다.
- P8 은 **「완성 장면의 한 장면으로」**.
- 두께에 대해 소유자가 답한 것은 이것이다 — **"이건 네가 정해줘. 무슨 말인지도 모르겠어"**.

**소유자가 회차 밖에서 정한 것** (같은 대화, 꼬리 정리)

- 낡은 교대 문서는 **「지운다」**.
- 완수 조건 `H2` 의 문면은 **안 고친다** — 앞 회차의 「미측정」을 사후에 통과로
  바꾸지 않는다.

## 목적 기여

**어느 목표에 기여하나** — [`00-goals.md`](../../../docs/plan/00-goals.md) §2 의
**목표 1(큐레이션)·2(컨설팅)**, 즉 §0.1 의 **(a) 역사와 현재 방향성을 반영하는 코드**다.
이 회차의 산출 둘 중 **결박 대상 확장**이 그 목표에 직접 선다 — 지금은 결정을 코드
심볼에만 걸 수 있어서, 문서·절차에 걸린 결정은 그래프가 모른다. 그래서 *"이 결정이 아직
유효한가"* 를 문서에 대해서는 못 묻는다.

**왜 지금 그것이 다른 것보다 먼저인가** — `AGENTS.md` 의 소유자 지시가 판정자다:
*"착수는 소비 가능해지는 지점에 가장 가까운 것을 프론티어에서 고르는 것으로 시작한다."*

**지금 그 거리를 잴 자가 없다.** `./scripts/frontier.sh` 가 열린 이슈 31 건을 **전부**
「착수 가능」으로 산출한다 — 31 건 중 어느 것이 그 지점에 가까운지 저장소가 답하지 않는다.
`AGENTS.md` 의 「어디서부터 읽는가」 표에서 **「무엇을 어느 순서로 만드나」와
「완성되면 실제로 어떻게 쓰이나」 두 칸이 ⏳ 비었다**로 남아 있고, 그 문서가
*"하네스의 완성 장면은 다음 회차가 쓴다"* 로 미뤄 두었다.

⚠ **이 판정은 소유자 지시와 정면으로 부딪힐 수 있다** — 같은 문장이
*"무엇을 먼저 할지 정하는 데 회차를 쓰지 않는다"* 라고도 적는다. **소유자가 그 긴장을
보고 이 회차를 골랐다**(인터뷰 앞 단계의 선택지에 그 충돌을 명시했다). 그리고 이 회차는
계획만 산출하지 않는다 — 결박 대상 확장이 함께 서므로 `AGENTS.md` 의 초석 예외
(*"나중에 바꾸면 그 위의 모든 것이 움직이는 자리는 한 번에 제대로 한다"*)에 걸린다.

## 착수 시점 관측

| 무엇 | 값 | 어떻게 쟀나 |
|---|---|---|
| 착수 커밋 | `7c41b81` | `git rev-parse --short HEAD` |
| 검사 | 26/26 통과 | `cargo xtask check` |
| 시험 | 947 통과 · 0 실패 | `cargo test --workspace` |
| 열린 이슈 | 31 건 · 착수 가능 31 건 · 교착 0 건 | `./scripts/frontier.sh` |
| `docs/` 줄 수 | 28,385 | `find docs -name '*.md' \| xargs wc -l` |
| 결박 가능 개체 | 다섯 — `decision`·`plan`·`label`·`residual`·`scope-reduction` | `crates/pal-core/src/entity.rs:261-265` |
| **결박의 대상** | **심볼만** | [`observations/binding-target-red.txt`](observations/binding-target-red.txt) |
| 승계 후보 | 이 PC 에 **있다** — `~/dev/projects/fabricate/GOAL.md` · `IMPLEMENTATION-ORCHESTRATION.md` | `ls` |
| 저장 형식의 판 표시 | **`intent.redb` 에는 없다** — `Binding` 이 `postcard` 로 통째 직렬화된다. `bindings.jsonl` 에만 `schema_version` 이 있고 지금 `2` 다 | `crates/pal-intent/src/store.rs:193,605,608` · `.palimpsest/intent/bindings.jsonl:1` |
| 이미 선 결박 | **22 건** (`bindings.jsonl` 23행 = 머리 1 + 결박 22) | `wc -l` |

★ **여덟째 줄이 `NEXT-B` 를 정정한다.** 그 문서는 초석 후보를 *"이 회차의 결정을 결박할
노드 타입이 없다"* 로 적었는데, 착수 관측이 그 문면을 반증했다 — 노드 타입(`Binding`)도
개체 종류(`decision`)도 이미 있다. **없는 것은 대상 쪽이다**: `pal bind` 가 `NAME` 을
심볼 이름으로만 받아 `AGENTS.md` 에 걸면 *"이 스냅샷에서 찾지 못했다"* 로 거부한다.

★★ **이슈 [#69] 가 같은 물음에 다른 설계를 이미 등록했다** — *「문서 간 결박이 없다 —
이 문서를 지우면 무엇이 깨지나를 그래프가 못 답한다」*. 그 이슈는 **문서 노드 + 문서→문서
엣지**를 제안하고, 이 회차의 `G` 는 **결박의 대상을 문서 좌표로 넓히는** 다른 설계다.
**`G9`(ADR)가 그 둘을 대조하고 어느 쪽을 왜 골랐는지 적는다.** 안 하면 초석 결정이
열린 이슈 하나와 어긋난 채 정본이 된다(사전부검 R1).

[#69]: https://github.com/hskim-ecoletree/palimpsest/issues/69

## 금지역 — **이 회차가 등록한다**

`/round` §11 이 출처를 ① `.claude/pal/policy.toml` ② 잠긴 의도 ③ 기본 다섯 순서로 정한다.
①은 이 저장소에 **없다**(`ls` 로 확인). 그래서 ②가 이 자리다. **규약이 회차 중에는
늘리지 말라고 했으므로 지금이 유일한 등록 시점이다**(사전부검 R1).

기본 다섯을 그대로 받고, `G` 가 사람의 결박 데이터를 만지므로 **첫째의 경계를 여기서 정한다.**

| 금지역 | 이 회차에서 무엇을 뜻하나 |
|---|---|
| **데이터 손실** | **이미 선 결박 22 건 중 하나라도 못 읽게 되면 손실이다.** `intent.redb` 를 지우고 `bindings.jsonl` 에서 다시 세우는 것은 손실이 **아니다** — 그 파일이 의도층의 진실원이고 git 이 추적한다. 반대로 `bindings.jsonl` 자신이 옛 빌드에서 안 읽히게 되는 것은 **손실이다** |
| **측정이 죽은 가지** | 검사·시험이 있는데 실제로는 아무것도 재지 않는다 |
| **사실이 아닌 것을 사실로** | 모르는 것을 안다고 적는다. 추정을 조회인 척한다. **감시 집합이 비었는데 `Fresh` 를 찍는 것이 이 갈래다** |
| **인증** | 신원·권한 경계 |
| **대표 취약점** | 주입 · 경로 탈출 · 역직렬화 · 비밀 노출 |


⚠ **범위 축소 뒤에는 「데이터 손실」이 이 회차에 안 걸린다** — 결박 데이터를 만지지 않는다.
그래도 목록은 남긴다. 회차 중에 늘리는 것이 금지라 지우면 다시 못 세운다 ⟨개정 R2⟩.

⚠⚠ **그 문면이 사후에 거짓이 됐다** ⟨개정 46 · 독립 리뷰 R3⟩. `/round` §11 조건 4 를 지키려고
이 회차가 결정 둘을 결박했고 `bindings.jsonl` 에 두 줄이 늘었다(22 → 24). **손실은 없다** —
추가만 있고 스물넷 전부 읽힌다(`pal query binding.status` → `결박 24건`). 문면은 그때의 판단
기록이라 안 고치고 이 줄이 정정한다.

## 완수 조건

### A — 완성 장면 (선행 없음)

- [x] **A1** 완성 장면 문서가 `docs/plan/` 에 있고, 장면마다 **누가 · 무엇을 해서 · 무엇을 받는가** 소제목 셋이 있다 · **결정론적** — 장면 하나를 소제목 셋을 가진 절로 정의하고 `xtask` 가 센다 ⟨개정 R2 · 정정⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 앞 문면(*"셋 중 하나라도 빠진 장면은 장면이 아니다"*)은 **항등식이었다** — 셋이 없는 것은 정의상 모집단에서 빠지므로 남은 것은 언제나 셋을 갖는다(조건평가 R1). 지금 문면은 절의 형식을 강제해서 위반이 실제로 나올 수 있다.
- [x] **A1-a** 장면이 **셋 이상**이다 · **결정론적** ⟨개정 R2 · 확대⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 앞 문면에는 하한이 없어 **0 건을 훑고 통과**했다(조건평가 R1). 셋은 `A2` 의 안쪽·바깥쪽과 `A3` 의 `P8` 이 요구하는 최소다.
- [x] **A2** 장면이 **안쪽과 바깥쪽을 둘 다** 그린다 — 남의 프로젝트에 설치한 사용자가 보는 것과, 이 저장소에서 회차를 도는 에이전트가 보는 것 · **정반합 판정** (결정론적이 아님) ⟨개정 R2 · 갈래 표시 추가⟩ · 통과 ⟨전사 2026-09-07⟩
- [x] **A3** **`P8`(생성형 큐레이션은 1급 능력이다)가 장면 하나를 진다** — 모델이 그래프를 읽고 서사를 산출하는 장면 · **정반합 판정** (결정론적이 아님) ⟨개정 R2 · 갈래 표시 추가⟩ · 통과 ⟨전사 2026-09-07⟩
- [x] **A4** 순서표가 장면 문서를 **파일 링크**로 걸고, 죽은 링크 검사가 그 링크를 잰다 · **결정론적** ⟨개정 R2 · 정정⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 앞 문면은 *"장면 하나를 지우면 순서표의 어느 문서 절이 근거를 잃는지"* 였고, 조건평가 R1 이 **낮춘 뒤에도 못 센다**를 냈다 — 죽은 링크 산출의 **왼쪽(근거를 잃는 쪽)이 파일 이름뿐**이라 절 단위가 원본 쪽에 원리상 없다. 그래서 「어느 절인지 기계가 답한다」를 **버리고** 「링크가 실재한다」만 남긴다. **승격 1 은 이 정정으로 철회한다.**
- [x] **A5** **`docs/plan/00-goals.md` §1「완성 장면 — 이 화면이 제품이다」와 새 문서의 관계가 정해진다** — 어느 쪽이 정본이고 다른 쪽이 무엇인지가 두 문서 **양쪽에** 적힌다 ⟨개정 R2 · 확대⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 그 절이 이미 있고 화면 한 장을 그려 두었다(조건평가 R1). 관계를 안 정하면 같은 디렉터리에 같은 이름의 장면 둘이 서고 어느 쪽이 정본인지 저장소가 못 답한다 — **이 회차가 없애려던 상태다.**

### B — 순서표 (선행 A)

- [x] **B1** `C1~C6` 과 **횡단요구 `U`** 일곱마다 **지금 무엇이 서 있는지**를 저장소에서 실측한 표가 있다. 추정이 아니라 조회·실행의 출력이 근거로 붙고, **각 행에 측정 커밋을 박는다** · 통과 ⟨전사 2026-09-07⟩
- [x] **B1-a** 그 표의 머리에 **「서 있다」가 무엇인지**가 적힌다 — 무엇이 참이면 그 능력이 선 것인가 ⟨개정 R2 · 확대⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 정의가 없으면 표가 서고도 무엇을 잰 것인지 다음 컨텍스트가 못 읽는다(조건평가 R1).
- [x] **B2** 순서표의 항목마다 **어느 능력에 매달리나**가 적힌다. 일곱 중 하나도 안 대는 항목은 순서표에 못 들어온다 · 통과 ⟨전사 2026-09-07⟩
- [x] **B2-a** **매단 근거가 항목마다 한 줄씩 적힌다** — *"이것이 돌아가려면 그 능력의 무엇이 필요한가"* ⟨개정 R2 · 확대⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 근거가 없으면 아무 능력이나 붙여도 `B2` 가 언제나 참이다. `B3` 은 **빼는 쪽**의 남용을 `B3-a` 로 막는데 `B2` 는 **넣는 쪽**을 안 막았다(조건평가 R1).
- [ ] **B3** **하네스의 장치가 그 일곱의 소비자로 매달린다.** 매달 데가 없는 하네스 일은 「없음」으로 적고 순서표 밖에 둔다
- [ ] **B3-a** **그 예외의 분모와 문턱을 실측으로 정한다** ⟨개정 R2 · 정정⟩
  ⚠ 앞 문면은 분모를 「하네스 표면 넷」과 「`pal` 상위 명령」으로 적었는데 **둘 다 이 문서 안에 정의가 없었고**, 사전부검 R2 가 실측으로 첫 문턱이 **착수 시점에 이미 발화**함을 보였다(에이전트 정의 13 개와 스킬의 `pal` 호출 40 회가 전부 `pal round …`, 훅은 그래프를 안 읽는다).
  ⚠⚠ **그 「40 회」는 반증됐다** ⟨개정 44 · 정반합 판 1⟩. 착수 커밋에서 다시 재도 리터럴 `pal` 호출은 **여덟**(`pal round` 여섯 · `pal install` 둘, 그리고 뒤의 둘은 호출이 아니라 산문 인용)이다. 잠긴 문면은 그때 무엇을 근거로 삼았는지의 기록이라 **안 고친다** — 대신 이 줄이 그 수가 실측이 아님을 적는다. 다음 컨텍스트가 위의 「40 회」를 실측으로 읽으면 틀린 수를 읽는다. 그리고 `docs/gates/inventory-disposal.md:17` 은 `pal --help` 기준 17 로 세는데 `enum Command` 는 18 이다 — **어느 수가 분모인지도 안 정해졌다.**
  **그러므로 문턱을 미리 못 박지 않는다.** 분모를 실측으로 세우고, 「없음」의 비율을 재고, **그 비율이 얼마면 축이 거짓인지를 그 실측을 보고 정해 게이트에 적는다.**
- [x] **B4** 순서표의 **첫 항목이 착수 시점의 열린 이슈 31 건 중 하나다** · **결정론적** (`gh issue view`) ⟨개정 R2 · 강화⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 앞 문면은 *"31 건 중 하나거나, 이 회차가 새로 연 것이거나"* 였고 뒤 갈래를 타면 **첫 항목이 무엇이든 이슈를 열면 통과**했다(조건평가 R1). 원문의 「**지금** 이슈로 서 있다」가 앞 갈래를 가리킨다.
- [x] **B4-a** `scripts/frontier.sh` 가 **새 순서표 문서를 가리킨다** · **결정론적** ⟨개정 R2 · 강화⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 앞 문면은 *"「지형은 ⏳ 비었다」를 더는 안 찍는다"* 라 **그 문자열을 지우기만 하면 참**이었다(조건평가 R1). 지우는 것이 아니라 가리키게 한다.
- [x] **B5** **음성 대조** — 순서표를 역순으로 놓아도 같은 근거로 정당화되면 그 근거는 순서를 안 정하고 있는 것이다. 근거가 **비대칭**인지 잰다 · **정반합 판정** (결정론적이 아님) · 반증 ⟨전사 2026-09-07⟩
- [x] **B6** **순서가 `B1` 의 실측에서 도출됐다** — 항목마다 *"이것이 왜 그 자리인가"* 가 실측표의 어느 행을 대는지 적힌다 · **정반합 판정** (결정론적이 아님) ⟨개정 R2 · 확대⟩ · 반증 ⟨전사 2026-09-07⟩
  ⚠ 이것이 없으면 표 일곱 행이 서고 **순서가 그 표와 무관하게** 정해져도 `B1`·`B2`·`B5` 가 전부 닫힌다. 이 회차를 고른 근거가 *"거리를 잴 자가 없다"* 인데 조건의 합이 거리 척도를 안 만들었다(조건평가 R1 · 「합이 안 덮는 네 대목」 ④).

### C — 진입점 (선행 A·B)

- [x] **C1** **`AGENTS.md` 의 두 칸이 새 문서를 가리킨다** — 「무엇을 어느 순서로 만드나」와 「완성되면 실제로 어떻게 쓰이나」에서 **⏳ 표기가 사라진다** · **결정론적** ⟨개정 R2 · 확대⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 원문이 산출을 *"`docs/plan/` 새 문서 + `AGENTS.md` 두 칸은 가리키기만"* 으로 적었는데 **그것을 요구하는 조건이 하나도 없었다**(조건평가 R1 · ①). `## 범위 밖` 이 상한만 적고 하한을 안 적어서, 조건이 전부 닫혀도 `AGENTS.md:39,40` 은 ⏳ 인 채로 통과했다.

### D — 재고 (선행 없음)

- [x] **D1** `~/dev/projects/fabricate/` 의 `GOAL.md`(비준)와 `IMPLEMENTATION-ORCHESTRATION.md`(691줄 미비준)를 읽고 **무엇을 승계하고 무엇을 안 하는지**가 사유와 함께 적힌다 · **정반합 판정** (결정론적이 아님). ⚠ 저장소 밖이라 **좌표 면제**가 필요하다 · 통과 ⟨전사 2026-09-07⟩
- [x] **D2** 2026-08-18 에 걷힌 옛 기능 문서 스물넷의 **순서 근거**를 [`disposal-map.md`](../../../docs/plan/disposal-map.md) 로 확인하고, 되살릴 것과 버릴 것이 사유와 함께 적힌다 · **정반합 판정** (결정론적이 아님) · 통과 ⟨전사 2026-09-07⟩
- [x] **D3** `NEXT-B-plan-for-harness.md` 가 **지워진다** · **결정론적** · 통과 ⟨전사 2026-09-07⟩
  ⚠ 이 회차의 발견 레코드는 그 파일을 `경로` 로 안 쓴다 — `## 범위 밖` 이 못 박는다
- [x] **D3-a** **뿌리의 교대 문서 모집단을 세고, 지우는 것과 남기는 것을 사유와 함께 적는다** ⟨개정 R2 · 확대⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 원문은 복수형(*"낡은 교대 문서**는** 지운다"*)인데 `D3` 이 한 건으로 줄었고, **「낡은 교대 문서」의 모집단을 세는 자리가 없었다**(조건평가 R1). 지금 뿌리에 `NEXT-*.md` 셋이 있다.

### E — 회차 규약 (선행 A·B·C·D)

- [x] **E1** `cargo xtask check` **전량 통과 · rc=0**. 검사 수를 여기 안 적는다 · 통과 ⟨전사 2026-09-07⟩
  ⚠ 새 계획 문서는 **「어색한 표현 부재」 검사의 대상 안**이다. 잠긴 의도의 문면을 그대로 옮기면 발화한다
- [x] **E2** `cargo test --workspace` 통과 · 0 실패 · **결정론적** · 통과 ⟨전사 2026-09-07⟩
- [x] **E3** `findings.jsonl` 에 **열린 것 0** · 통과 ⟨전사 2026-09-07⟩
- [x] **E4** 종료 보고 `report.md` 에 **네 이름이 없다.** 그리고 `## 원리상 못 잰 것` 이 **비어 있지 않다** ⟨개정 R2 · 강화⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 앞 문면은 제목 넷의 부재만 재서 **본문을 「없음」으로 채우면 참**이었다(조건평가 R1). 이 회차는 축소를 거쳤으므로 못 잰 것이 반드시 있다.
- [x] **E5** 게이트 `docs/gates/terrain-and-completion-scene.md` 의 `## 판정` 이 **표준 표**로 서고 검산이 맞는다 · **결정론적** · 통과 ⟨전사 2026-09-07⟩
- [x] **E5-a** **「어색한 표현 부재」 검사의 대상이 회차 이름을 손으로 적지 않고 정해진다** · **결정론적** ⟨개정 R2 · 강화 · 금지역⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 앞 문면(*"이 회차의 종결 문서도 잰다"*)은 **파일 이름 두 줄을 더하면 통과**라 다음 회차가 열리는 순간 같은 죽은 가지가 다시 났다(조건평가 R1). 그리고 사전부검 R2 가 실측으로 **일반화하면 1,696 곳이 발화**하고(동결된 옛 기록), 대안인 사전순 선택은 같은 파일의 `check_ledger_pair` 가 **이미 반증해 금지**했음을 보였다.
  **그러므로 「고친다」가 아니라 「세 자리 중 무엇이 이 병을 원리상 못 벗어나는지를 적고, 벗어날 길이 없으면 그 사실을 게이트에 적는다」가 합격선이다.** ⟨차선책 등록⟩
- [x] **E6** **회차 산출의 마지막 커밋에 `conclusion=success` 런이 붙는다.** 「산출」의 경계 — CI 결과를 받아 적는 커밋은 산출이 아니라 관측이다 · **결정론적** · 통과 ⟨전사 2026-09-07⟩
- [x] **E6-a** **그 런이 이 회차의 커밋에 대해 도는 것을 확인한다** ⟨개정 R2 · 확대⟩ · **결정론적** · 통과 ⟨전사 2026-09-07⟩
  ⚠ 사전부검 R2 실측: CI 트리거가 `push: [main]` 과 `pull_request` 둘뿐이라 이 브랜치에서는 PR 이 있어야 돌고, 열려 있는 PR **#115 는 이 회차와 다른 일**이며 `origin/main` 대비 63 커밋 앞이다. 그 PR 의 런에 붙는 `success` 는 「이 회차의 마지막 산출 커밋이 초록이다」와 **같은 문장이 아니다.**
- [x] **E7** **정반합을 돌았고 산출물이 `dialectic/` 에 있다** · **결정론적** ⟨개정 R2 · 확대⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 원문이 *"초안은 「정반합으로 초안을 세워 올린다」"* 인데 **돌았다도 올렸다도 조건이 아니었다**(조건평가 R1 · ②). `## 상한` 의 「2~3 판」은 몇 번까지의 뚜껑이지 했나가 아니라서 0 판을 돌아도 위반이 아니었다.

### F — 효과 (선행 B)

- [x] **F1** **`./scripts/frontier.sh` 가 순서표를 읽어 열린 이슈를 줄 세운 출력**이 게이트 `## 효과` 에 붙는다 ⟨개정 R2 · 전환⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 앞 문면은 *"`pal` 로 이 회차의 결정을 조회한 출력"* 이었고 그것은 **`G` 절(결박 대상 확장)이 서 있을 때만 성립한다.** `G` 를 범위 밖으로 뺐으므로 효과의 자리도 옮긴다. 시험도 CI 도 아닌 것이 돌아야 한다는 요구는 그대로다.
- [x] **F2** 그 출력이 **착수 시점과 다르다** — 착수 때는 31 건이 전부 「착수 가능」이었다 · **결정론적** ⟨개정 R2 · 강화⟩ · 통과 ⟨전사 2026-09-07⟩
  ⚠ 앞 `F1`·`F2` 는 **합집합이 결과 공간 전체**라 실패 경로가 없었다(조건평가 R1). 지금 문면은 착수 관측과의 차이를 재므로 「아무것도 안 달라졌다」가 반증이다.

## 차선책

- **`B1`(능력별 실측)이 일곱 중 어느 능력에서 원리상 못 재면** — 그 능력만 **「못 쟀다」로 적고** 나머지로 순서를 세운다. 못 잰 능력은 **「순위 미정」** 으로 둔다 — 맨 뒤로 미루면 측정 실패가 우선순위 판단으로 둔갑한다.
- **`A3`(P8 장면)이 지금 그래프로는 못 그려지면** — 장면을 **목표 상태**로 적고 무엇이 갖춰지면 서는지를 함께 적는다. 지우지는 않는다.
- **`E5-a` 가 세 자리 어디에서도 회차 이름을 안 적는 형태로 못 서면** — **그 사실과 왜 원리상 못 서는지를 게이트에 적고 「대조 불가」로 판정한다.** 이슈로 세워 다음 회차가 받는다 ⟨개정 R2⟩.
- **`E6-a` 가 이 회차의 커밋에 대해 도는 런을 못 얻으면** — 새 PR 을 연다. 그것도 안 되면 **`E6` 을 「미측정」으로 적는다** — 앞 회차 `H2` 가 그랬듯이, 안 잰 것을 통과로 적지 않는다 ⟨개정 R2⟩.
- **`A5`(§1 과의 관계)에서 어느 쪽이 정본인지 갈리면** — 소유자에게 올린다. 초석에 가까운 결정이다 ⟨개정 R2⟩.

## 범위 밖

- ★★ **결박 대상 확장 전부** — 스키마·저장·조회·낡음·ADR. **소유자가 2026-09-06 에 범위를 문서 둘로 줄였다** ⟨개정 R2 · 축소 · 승인됨⟩.
  이 회차는 **완성 장면과 순서표 두 문서까지**다. 그 자리는 **이슈 [#119] 로 섰다** — 넘김이 아니라 분할이다.

[#119]: https://github.com/hskim-ecoletree/palimpsest/issues/119
  ⚠ **그 이슈에 사전부검 R1·R2 와 조건 평가 R1 이 낸 것을 함께 싣는다** — `postcard` 판 부재 · `bindings.jsonl` 판 롤백 불가 · `WATCH` 색인 32바이트 열쇠 · 감시 집합이 비면 `Orphaned` 를 낸다 · `doctor` 유령 엣지 · 검사 12 의 이름 기반 모집단 · **그리고 `corpus/criteria.toml` `[f12]` 가 「`Binding` 을 한 글자도 안 고친다」를 이미 채택하고 대안 둘을 기각해 두었다는 것.**
- **순서표의 첫 항목을 실제로 구현하는 것.**
- **`C1~C6`·`U` 각 능력을 실제로 올리는 것.** 실측하고 줄 세우는 데까지다.
- **열린 이슈 31 건의 재분류·재라벨.**
- **`AGENTS.md` 의 다른 절을 고치는 것.** 두 칸이 새 문서를 가리키게 하는 것까지다(`C1`).
- **철회한 회차의 재료 `NEXT-E-*.md` 둘.** 그 회차의 발견 기록 세 행이 좌표로 붙들고 있다.
- ★ **이 회차의 발견 레코드에 `NEXT-B-plan-for-harness.md` 를 `경로` 로 안 적는다.**
- ★ **회차 관측을 `.json` 확장자로 놓지 않는다.** `docs/sunset.toml:27` 의 트리거가 그 확장자다. **`.txt` 로 둔다.**
- ★ **`corpus/` 의 사전 등록 상수.** 소유자가 *"범위 밖 — 측정 장치다"* 로 잠갔다.
- ★ **`docs/sunset.toml` 이 유보한 `docs/research` 의 처분.** 이슈 **#68** 이 진다.
- ★★ **완수 조건 `H2` 의 문면을 안 고친다** — 앞 회차의 「미측정」을 사후에 통과로 바꾸지 않는다. **소유자가 회차 밖에서 명시적으로 금했다** ⟨개정 R2⟩.
  ⚠ 조건평가 R1 이 *"소유자가 명시적으로 금한 것인데 조건에도 범위 밖에도 없다"* 를 **금지역**(사실이 아닌 것을 사실로)으로 냈다. 같은 항의 첫째(교대 문서 삭제)는 `D3` 으로 조건화됐는데 둘째만 빠져 있었다.

## 상한

| 자리 | 상한 | 근거 |
|---|---|---|
| 인터뷰 | **4 라운드** | 소유자가 등록했다. 상한에서 닫았다 |
| 사전부검 | **3 라운드** | 소유자가 등록했다. **2 라운드를 썼고, 범위 축소로 R3 을 안 돈다** ⟨개정 R2⟩ |
| 조건 설계 평가 | **2 라운드** | 규약 §3.5 의 기본값. **1 라운드를 썼다** |
| 독립 리뷰 | **5 라운드** | 소유자가 등록했다 — *"독립 리뷰 상한을 다 쓴다"* |
| 정반합 | **2~3 판** | 소유자가 등록했다 — *"갈리는 자리만 골라 두세 판"* |

⚠ **사전부검 R3 과 조건 평가 R2 를 안 도는 것은 소유자 결정이다** ⟨개정 R2⟩. 범위가 절반으로
줄었고 뺀 절(`G`)이 두 라운드의 발견 대부분을 지고 있었다. **상한에 안 닿는 것은 규약이
금하지 않는다** — 상한은 손실 제한이지 하한이 아니다.

## 모집단 분리 — **(가) 로 바꾼다** ⟨개정 R2⟩

앞 판은 (나)였다 — *"장치를 만들되 그것에 대한 발견은 별개 회차"*. **범위 축소로 이 회차가
코드를 안 만지므로 (가)가 성립한다:** 장치를 회차 안에서 만들지 않는다.

산출은 문서 둘과 `AGENTS.md` 두 줄, `frontier.sh` 한 줄, 그리고 `xtask` 의 검사 하나(`E5-a`)다.
**`E5-a` 만 자기 장치이고, 그것에 대한 발견은 금지역에 닿을 때만 본 모집단이다.**

⚠⚠ **그 셈이 하나 모자랐다** ⟨개정 45 · 독립 리뷰 R2⟩. `A1` 이 *"`xtask` 가 헤아린다"* 로 **검사
하나를 더** 요구하고, 이 회차는 그것을 실제로 세웠다(`check_completion_scenes` + 순수 함수 +
시험 셋). 그러므로 자기 장치는 **둘**이다. 문면은 그때의 셈이라 안 고치고 이 줄이 정정한다 —
**두 장치에 대한 발견 모두 「금지역에 닿을 때만 본 모집단」을 따른다.** 실측: 독립 리뷰 R1·R2 가
`E5-a` 의 선택자에서 금지역 둘을 냈고 둘 다 본 목록으로 받아 고쳤다.

## 두께 — **메인이 정했다**

**줄 수 상한을 안 건다.** 줄 수로 재면 내용이 아니라 압축을 유도해서 근거를 지우고 결론만
남기는 쪽으로 간다.

**문서를 둘로 가른다.** 완성 장면은 제품이 무엇인지라 잘 안 바뀌고, 순서표는 회차마다 바뀐다.
`A4` 가 그 둘을 파일 링크로 잇고 죽은 링크 검사가 그 링크를 잰다.

## 개정

| # | 라운드 | 무엇을 왜 바꿨나 | 처분 |
|---|---|---|---|
| 1~20 | 사전부검 R1 | `C`→`G` 개명 · `## 금지역` 신설 · 금지역 여섯을 조건으로 · `A4` 완화 · `B2` 에 `U` · `B3-a`·`B4-a`·`E5-a` 신설 · `E6` 경계 정의 · 범위 밖 다섯 줄 | 위 표는 앞 판에 있었다 |
| 21 | **소유자 결정** | **범위를 문서 둘로 줄인다.** `G` 절 전부(결박 대상 확장)를 범위 밖으로 빼고 이슈로 세운다 | 축소 — **승인됨** |
| 22 | 조건평가 R1 | `A1` 이 **항등식**이었다(*"셋이 빠진 것은 장면이 아니다"* 가 모집단을 정의해 위반이 원리상 안 난다). 절 형식 강제로 바꾸고 갈래를 정반합에서 결정론적으로 옮겼다 | 정정 |
| 23 | 조건평가 R1 | `A1-a` 신설 — 장면 수 하한이 없어 **0 건을 훑고 통과**했다 | 확대 |
| 24 | 사전부검 R2 · 조건평가 R1 | `A4` 를 「링크가 실재한다」로 정정. 죽은 링크 산출의 **왼쪽이 파일 이름뿐**이라 절 단위가 원본 쪽에 원리상 없다 — 낮춰도 못 센다. **승격 1 을 철회한다** | 정정 |
| 25 | 조건평가 R1 | `A5` 신설 — `00-goals.md` §1 이 이미 「완성 장면」이고 관계가 안 정해졌다 | 확대 |
| 26 | 조건평가 R1 | `B1-a` 신설 — 「서 있다」의 정의가 없었다 | 확대 |
| 27 | 조건평가 R1 | `B2-a` 신설 — 넣는 쪽의 남용을 막는 문턱이 없어 `B3` 과 비대칭이었다 | 확대 |
| 28 | 사전부검 R2 | `B3-a` 의 문턱을 **미리 못 박지 않고 실측 뒤에 정한다**로 정정. 분모 둘이 정의되지 않았고 첫 문턱이 착수 시점에 이미 발화했다 | 정정 |
| 29 | 조건평가 R1 | `B4` 를 「31 건 중 하나」로 강화 — 「새로 연 것」 갈래가 **항등식**을 만들었다 | 정정 |
| 30 | 조건평가 R1 | `B4-a` 를 「가리킨다」로 강화 — 「안 찍는다」는 **문자열을 지우면 통과**였다 | 정정 |
| 31 | 조건평가 R1 | `B6` 신설 — **순서가 실측에서 도출됐다**를 재는 자가 없었다. 이 회차를 고른 근거가 그것이다 | 확대 |
| 32 | 조건평가 R1 | `C` 절 신설 — 원문의 「`AGENTS.md` 두 칸」을 요구하는 조건이 하나도 없었다 | 확대 |
| 33 | 조건평가 R1 | `D3-a` 신설 — 원문은 복수형인데 `D3` 이 한 건으로 줄었고 모집단을 세는 자리가 없었다 | 확대 |
| 34 | 조건평가 R1 | `E4` 강화 — 제목 넷의 부재만 재서 본문을 「없음」으로 채우면 참이었다 | 정정 |
| 35 | 사전부검 R2 · 조건평가 R1 | `E5-a` 를 「회차 이름을 손으로 안 적는다」로 강화하고, 못 서면 「대조 불가」로 적는 차선책을 등록. 일반화하면 1,696 곳이 발화하고 대안은 이미 반증됐다 | 정정 |
| 36 | 사전부검 R2 | `E6-a` 신설 — 열린 PR 이 이 회차와 다른 일이라 그 런의 `success` 가 이 회차를 말하지 않는다 | 확대 |
| 37 | 조건평가 R1 | `E7` 신설 — 원문의 「정반합으로 세워 올린다」가 조건이 아니었다 | 확대 |
| 38 | 조건평가 R1 · 소유자 결정 | `F1` 을 `frontier.sh` 출력으로 전환. `pal` 조회는 `G` 가 서야 성립하는데 `G` 를 뺐다 | 전환 |
| 39 | 조건평가 R1 | `F2` 강화 — 앞 `F1`·`F2` 는 **합집합이 결과 공간 전체**라 실패 경로가 없었다 | 정정 |
| 40 | 조건평가 R1 | `## 범위 밖` 에 `H2` 문면 금지를 넣었다. 소유자가 명시적으로 금한 것인데 어디에도 없었다 | 범위밖 |
| 41 | 소유자 결정 | `## 모집단 분리` 를 (나)에서 **(가)** 로 바꿨다. 코드를 안 만지므로 성립한다 | 정정 |
| 42 | 소유자 결정 | 사전부검 R3 과 조건 평가 R2 를 안 돈다. 범위가 절반으로 줄었고 뺀 절이 두 라운드의 발견 대부분을 졌다 | 축소 — **승인됨** |
| 43 | 조건평가 R1 | 갈래 표시를 `A2`·`A3`·`B5`·`B6`·`D1`·`D2` 에 달았다. 37 중 22 에 판정자가 안 정해져 있었다 | 정정 |
| 44 | 정반합 판 1 | `B3-a` 의 각주가 진 「스킬의 `pal` 호출 40 회」가 반증됐다는 것을 그 자리에 적었다. 문면은 안 고친다 — 그때의 근거 기록이다 | 정정 |
| 45 | 독립 리뷰 R2 | `## 모집단 분리` 가 이 회차의 `xtask` 산출을 하나로 셌는데 `A1` 이 요구한 검사가 하나 더 있어 둘이다. 문면은 안 고치고 그 자리에 정정을 적었다 | 정정 |
| 46 | 독립 리뷰 R3 | `## 금지역` 의 「결박 데이터를 만지지 않는다」가 사후에 거짓이 됐다 — 종료 조건 4 를 지키느라 결박 둘을 더했다. 손실은 없고 그 자리에 정정을 적었다 | 정정 |
| 47 | 독립 리뷰 R5 | 조건 32 중 **열넷에 갈래 표시가 없다** — 개정 43 이 여섯에만 달았고 조건 평가 R1 이 낸 「37 중 22 에 판정자가 안 정해져 있었다」가 절반만 처리됐다. 문면은 안 고치고 게이트의 `E5` 행이 그 열넷의 판정자를 자리마다 적는다. ⟨2026-09-07 · push 뒤에 `E6`·`E6-a` 를 판정하면서 그 둘에는 표시를 달았다 — 남은 것은 열둘⟩ | 정정 |

## 승격

### 승격 1 — `A4` 의 해상도 · **철회했다**

물었던 것: `A4` 의 음성 대조를 「어느 항목」에서 「어느 문서 절」로 낮춰도 되나.

**소유자의 답을 안 받고 철회한다.** 사전부검 R2 와 조건 평가 R1 이 같은 것을 냈다 —
**낮춘 뒤에도 못 센다.** 죽은 링크 산출의 왼쪽(근거를 잃는 쪽)이 파일 이름뿐이라 절 단위가
원본 쪽에 원리상 없다. 어느 답이 와도 `A4` 를 대조 가능으로 만들지 못했다. 개정 24 가
조건을 「링크가 실재한다」로 정정했고 그것은 축소가 아니라 **잴 수 있는 것으로 바꾼 정정**이다.

### 승격 2 — 범위 축소 · **승인됨** (2026-09-06)

물은 것: 이 회차를 어떻게 할까.

**소유자의 답: 「범위를 문서 둘로 줄인다」.** 결박 대상 확장(옛 `G` 절 아홉 조건)을
범위 밖으로 빼고 이슈로 세운다. 조건은 37 에서 다시 셈한다.

### 승격 3 — 사전부검 R3 · 조건 평가 R2 를 안 돈다 · **승인됨** (2026-09-06)

같은 답에 딸린 결정이다. 상한에 안 닿고 닫는다.
