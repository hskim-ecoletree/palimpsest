# 잠긴 의도 — 지형과 완성 장면

> 회차 `2026-09-06-terrain-and-completion-scene` · 착수 커밋 `7c41b81`
> 상태는 [`state.md`](state.md) 가 진다.

## 원문

소유자가 인터뷰 네 라운드에서 고른 것. **요약하지 않는다.**

**라운드 1**

- 이 회차가 세우는 지형은 **「코어와 하네스 한 장」**.
- 「완성 장면」의 주인공은 **「둘 다 — 같은 장면의 안쪽과 바깥쪽」**.
- 이 회차는 **「순서표가 서고 그 첫 항목이 지금 이슈로 서 있다」** 가 참이면 끝.
- 초안은 **「정반합으로 초안을 세워 올린다」**.

**라운드 2**

- 순서표의 항목 하나는 **「능력 계약 `C1~C6`」**.
- 재고는 넷 다 만난다 — **「fabricate 의 승계 후보 둘을 읽는다」** · **「걷힌 옛 기능 문서
  스물넷의 순서 근거를 본다」** · **「결박 노드 타입 공백을 다룬다」** ·
  **「P8 생성형 큐레이션의 자리를 지형에 둔다」**.
- 정반합은 **「갈리는 자리만 골라 두세 판」**.
- 상한은 **「인터뷰 4 · 사전부검 3 · 독립 리뷰 5」**.

**라운드 3**

- 하네스는 **「C1~C6 의 소비자다」**.
- 결박 노드 타입 공백은 **「이 회차에서 만든다」**.
- §8 「효과」에는 **「pal 로 이 회차의 결정을 조회해 본다」**.
- 산출은 **「docs/plan/ 새 문서 + AGENTS.md 두 칸은 가리키기만」**.

**라운드 4**

- 결박은 **「스키마 + 저장 + 조회」** 까지.
- 「소비 가능해지는 지점에 가장 가까운 것」은 **「능력마다 지금 무엇이 서 있는지를 실측한다」** 로 잰다.
- P8 은 **「완성 장면의 한 장면으로」**.
- 두께에 대해 소유자가 답한 것은 이것이다 — **"이건 네가 정해줘. 무슨 말인지도 모르겠어"**.

**소유자가 회차 밖에서 정한 것** (같은 대화, 꼬리 정리)

- 낡은 교대 문서는 **「지운다」**.
- 완수 조건 `H2` 의 문면은 **안 고친다** — 앞 회차의 「미측정」을 사후에 통과로
  바꾸지 않는다.

## 목적 기여

**어느 목표에 기여하나** — [`00-goals.md`](../../../docs/plan/00-goals.md) §2 의
**목표 1(큐레이션)·2(컨설팅)**, 즉 §0.1 의 **(a) 역사와 현재 방향성을 반영하는 코드**다.
이 회차의 산출 둘 중 **결박 대상 확장**이 그 목표에 직접 선다 — 지금은 결정을 코드
심볼에만 걸 수 있어서, 문서·절차에 걸린 결정은 그래프가 모른다. 그래서 *"이 결정이 아직
유효한가"* 를 문서에 대해서는 못 묻는다.

**왜 지금 그것이 다른 것보다 먼저인가** — `AGENTS.md` 의 소유자 지시가 판정자다:
*"착수는 소비 가능해지는 지점에 가장 가까운 것을 프론티어에서 고르는 것으로 시작한다."*

**지금 그 거리를 잴 자가 없다.** `./scripts/frontier.sh` 가 열린 이슈 31 건을 **전부**
「착수 가능」으로 산출한다 — 31 건 중 어느 것이 그 지점에 가까운지 저장소가 답하지 않는다.
`AGENTS.md` 의 「어디서부터 읽는가」 표에서 **「무엇을 어느 순서로 만드나」와
「완성되면 실제로 어떻게 쓰이나」 두 칸이 ⏳ 비었다**로 남아 있고, 그 문서가
*"하네스의 완성 장면은 다음 회차가 쓴다"* 로 미뤄 두었다.

⚠ **이 판정은 소유자 지시와 정면으로 부딪힐 수 있다** — 같은 문장이
*"무엇을 먼저 할지 정하는 데 회차를 쓰지 않는다"* 라고도 적는다. **소유자가 그 긴장을
보고 이 회차를 골랐다**(인터뷰 앞 단계의 선택지에 그 충돌을 명시했다). 그리고 이 회차는
계획만 산출하지 않는다 — 결박 대상 확장이 함께 서므로 `AGENTS.md` 의 초석 예외
(*"나중에 바꾸면 그 위의 모든 것이 움직이는 자리는 한 번에 제대로 한다"*)에 걸린다.

## 착수 시점 관측

| 무엇 | 값 | 어떻게 쟀나 |
|---|---|---|
| 착수 커밋 | `7c41b81` | `git rev-parse --short HEAD` |
| 검사 | 26/26 통과 | `cargo xtask check` |
| 시험 | 947 통과 · 0 실패 | `cargo test --workspace` |
| 열린 이슈 | 31 건 · 착수 가능 31 건 · 교착 0 건 | `./scripts/frontier.sh` |
| `docs/` 줄 수 | 28,385 | `find docs -name '*.md' \| xargs wc -l` |
| 결박 가능 개체 | 다섯 — `decision`·`plan`·`label`·`residual`·`scope-reduction` | `crates/pal-core/src/entity.rs:261-265` |
| **결박의 대상** | **심볼만** | [`observations/binding-target-red.txt`](observations/binding-target-red.txt) |
| 승계 후보 | 이 PC 에 **있다** — `~/dev/projects/fabricate/GOAL.md` · `IMPLEMENTATION-ORCHESTRATION.md` | `ls` |
| 저장 형식의 판 표시 | **`intent.redb` 에는 없다** — `Binding` 이 `postcard` 로 통째 직렬화된다. `bindings.jsonl` 에만 `schema_version` 이 있고 지금 `2` 다 | `crates/pal-intent/src/store.rs:193,605,608` · `.palimpsest/intent/bindings.jsonl:1` |
| 이미 선 결박 | **22 건** (`bindings.jsonl` 23행 = 머리 1 + 결박 22) | `wc -l` |

★ **여덟째 줄이 `NEXT-B` 를 정정한다.** 그 문서는 초석 후보를 *"이 회차의 결정을 결박할
노드 타입이 없다"* 로 적었는데, 착수 관측이 그 문면을 반증했다 — 노드 타입(`Binding`)도
개체 종류(`decision`)도 이미 있다. **없는 것은 대상 쪽이다**: `pal bind` 가 `NAME` 을
심볼 이름으로만 받아 `AGENTS.md` 에 걸면 *"이 스냅샷에서 찾지 못했다"* 로 거부한다.

★★ **이슈 [#69] 가 같은 물음에 다른 설계를 이미 등록했다** — *「문서 간 결박이 없다 —
이 문서를 지우면 무엇이 깨지나를 그래프가 못 답한다」*. 그 이슈는 **문서 노드 + 문서→문서
엣지**를 제안하고, 이 회차의 `G` 는 **결박의 대상을 문서 좌표로 넓히는** 다른 설계다.
**`G9`(ADR)가 그 둘을 대조하고 어느 쪽을 왜 골랐는지 적는다.** 안 하면 초석 결정이
열린 이슈 하나와 어긋난 채 정본이 된다(사전부검 R1).

[#69]: https://github.com/hskim-ecoletree/palimpsest/issues/69

## 금지역 — **이 회차가 등록한다**

`/round` §11 이 출처를 ① `.claude/pal/policy.toml` ② 잠긴 의도 ③ 기본 다섯 순서로 정한다.
①은 이 저장소에 **없다**(`ls` 로 확인). 그래서 ②가 이 자리다. **규약이 회차 중에는
늘리지 말라고 했으므로 지금이 유일한 등록 시점이다**(사전부검 R1).

기본 다섯을 그대로 받고, `G` 가 사람의 결박 데이터를 만지므로 **첫째의 경계를 여기서 정한다.**

| 금지역 | 이 회차에서 무엇을 뜻하나 |
|---|---|
| **데이터 손실** | **이미 선 결박 22 건 중 하나라도 못 읽게 되면 손실이다.** `intent.redb` 를 지우고 `bindings.jsonl` 에서 다시 세우는 것은 손실이 **아니다** — 그 파일이 의도층의 진실원이고 git 이 추적한다. 반대로 `bindings.jsonl` 자신이 옛 빌드에서 안 읽히게 되는 것은 **손실이다** |
| **측정이 죽은 가지** | 검사·시험이 있는데 실제로는 아무것도 재지 않는다 |
| **사실이 아닌 것을 사실로** | 모르는 것을 안다고 적는다. 추정을 조회인 척한다. **감시 집합이 비었는데 `Fresh` 를 찍는 것이 이 갈래다** |
| **인증** | 신원·권한 경계 |
| **대표 취약점** | 주입 · 경로 탈출 · 역직렬화 · 비밀 노출 |

## 완수 조건

### A — 완성 장면 (선행 없음)

- [ ] **A1** 완성 장면 문서가 `docs/plan/` 에 선다. 장면마다 **누가 · 무엇을 해서 · 무엇을 받는가** 셋이 다 적힌다 — 셋 중 하나라도 빠진 장면은 장면이 아니다 · **정반합 판정** (결정론적이 아님)
- [ ] **A2** 장면이 **안쪽과 바깥쪽을 둘 다** 그린다 — 남의 프로젝트에 설치한 사용자가 보는 것과, 이 저장소에서 회차를 도는 에이전트가 보는 것. 어느 한쪽만 있으면 반증이다
- [ ] **A3** **`P8`(생성형 큐레이션은 1급 능력이다)가 장면 하나를 진다** — 모델이 그래프를 읽고 서사를 산출하는 장면. `00-goals.md` §2.6 이 `P8` 을 살아 있다고 적는데 만든 것은 계속 반대로 갔다
- [ ] **A4** **음성 대조** — 장면 하나를 지우면 **순서표의 어느 문서 절이 근거를 잃는지**를 죽은 링크 검사가 산출한다. ⚠ **「어느 항목」이 아니라 「어느 문서 절」이다** — 그 검사의 산출이 `"{파일} → {대상}"` 이라 줄 번호도 항목 이름도 없다(사전부검 R1). 항목 단위로 짚으려면 항목마다 유일한 앵커를 강제해야 하고 그것은 이 회차가 안 한다 ⟨개정 R1 · 완화 — 승격 대상⟩

### B — 순서표 (선행 A)

- [ ] **B1** `C1~C6` 과 **횡단요구 `U`** 마다 **지금 무엇이 서 있는지**를 저장소에서 실측한 표가 있다. 추정이 아니라 조회·실행의 출력이 근거로 붙는다. **표의 각 행에 측정 커밋을 박는다** — 안 박으면 같은 회차의 `G` 가 그 값을 바꿔서 종료 시점에 거짓이 된다 ⟨개정 R1⟩
- [ ] **B2** 순서표의 항목마다 **어느 능력에 매달리나**가 적힌다. `C1~C6` 과 `U` **일곱** 중 하나도 안 대는 항목은 순서표에 못 들어온다. ⚠ **`U` 를 빼면 안 된다** — `00-goals.md` §2.5 의 채점 단위가 일곱이고 `docs/gates/inventory-disposal.md:21` 이 그 수를 「7」로 세어 두었다 ⟨개정 R1⟩
- [ ] **B3** **하네스의 장치가 그 일곱의 소비자로 매달린다** — 각 장치에 *"이것이 돌아가려면 어느 능력이 서야 하나"* 가 적힌다. 매달 데가 없는 하네스 일(훅·설치·회차 상태)은 「없음」으로 적고 순서표 밖에 둔다.
- [ ] **B3-a** **그 예외의 문턱을 잰다** — 「없음」으로 뺀 것이 하네스 표면 넷 중 **셋 이상**이거나 `pal` 상위 명령의 **절반 이상**이면 **축이 거짓이다.** 그때는 축을 다시 세우고 그 사실을 게이트에 적는다. ⚠ 사전부검이 실측 없이 추정한 바로는 스킬·에이전트 정의·훅 셋과 `install`·`update`·`uninstall`·`hook`·`round`·`intent`·`cache` 일곱이 후보다 — **그 분류를 이 회차가 실측한다** ⟨개정 R1 · 확대⟩
- [ ] **B4** 순서표의 **첫 항목이 지금 이슈로 서 있다.** 31 건 중 하나거나, 이 회차가 새로 연 것이거나 · **결정론적** (`gh issue view`)
- [ ] **B4-a** `scripts/frontier.sh` 의 마지막 줄이 **「지형은 ⏳ 비었다」를 더는 안 찍는다** — 지형이 섰는데 그 실행 출력이 안 비었다고 말하면 거짓 신호다 ⟨개정 R1 · 확대⟩
- [ ] **B5** **음성 대조** — 순서표를 역순으로 놓아도 같은 근거로 정당화되면 그 근거는 순서를 안 정하고 있는 것이다. 근거가 **비대칭**인지 잰다

### G — 결박 대상 확장 (선행 없음 · **초석**)

⚠ **이 절은 원래 `C` 였다.** 능력 계약 `C1~C6` 과 같은 이름 공간을 써서 `## 차선책` 이
이미 갈렸다(사전부검 R1). `G` 로 개명했다 ⟨개정 R1⟩.

- [ ] **G1** 스키마가 **코드 아닌 좌표**를 결박 대상으로 담는다 — `schema/graph.toml` 에 형식이 서고, 무엇이 좌표가 될 수 있고 무엇이 될 수 없는지가 문면에 있다 · **결정론적**
- [ ] **G2** 그 좌표에 건 결박이 **저장된다** — 되읽어서 같은 값이 나온다 · **결정론적** (왕복 시험)
- [ ] **G2-a** **이미 선 결박 22 건이 코드 변경 뒤에도 읽힌다** · **결정론적** ⟨개정 R1 · 금지역⟩
  ⚠ `Binding` 은 `postcard` 로 통째 직렬화되고 **레코드마다 판 번호가 없다**(`store.rs:193`). `G2` 의 왕복 시험은 **새 형식끼리만** 왕복하므로 이 결함을 **초록인 채로 통과시킨다.**
  **순서를 못 박는다 — 코드를 만지기 전에 `pal intent export` 를 돌려 `bindings.jsonl` 을 최신으로 만든다.** 그 뒤에 고치고, 고친 빌드로 다시 읽어 22 건이 다 서는 것을 잰다.
- [ ] **G2-b** **`bindings.jsonl` 의 판을 안 올린다.** 못 피하면 **올리기 전에 소유자에게 올린다** ⟨개정 R1 · 금지역⟩
  ⚠ `READABLE_SCHEMA_VERSIONS = [1,2]` 이고 export 는 언제나 최신으로 쓴다(`store.rs:605,608,676-687`). 판 3 으로 한 번 내보내면 **롤백한 빌드가 그 파일을 거부**한다 — 되돌릴 경로가 없다.
- [ ] **G2-c** **`WATCH`·`BOUND_BY` 색인의 열쇠 계약이 안 깨진다** · **결정론적** ⟨개정 R1 · 금지역⟩
  ⚠ `watch_index()` 는 열쇠가 32바이트가 아니면 `Decode` 오류를 낸다(`store.rs:283-303`). 문서 경로를 32바이트로 해시해 같은 다중맵에 넣으면 **두 네임스페이스가 섞이고** `SymbolId::from_bytes(raw)` 가 실재하지 않는 심볼 id 를 만들어 `Orphaned` 를 낸다 — *"결정을 다시 해야 한다"* 를 사실이 아닌데 적는 것이다.
- [ ] **G3** 그 좌표에 건 결박이 **조회된다** — *"이 자리에 무엇이 걸려 있나"* 에 답이 온다 · **결정론적**
- [ ] **G3-a** **`pal doctor` 가 문서 좌표 결박에 유령 엣지를 안 만든다** · **결정론적** ⟨개정 R1⟩
  ⚠ `doctor.rs:200,234,253` 이 `BOUND_TO` 를 언제나 `Symbol` 노드로 내고 `schema/graph.toml:218` 이 `to = ["Symbol"]` 이다. 안 고치면 존재하지 않는 노드를 가리키는 엣지가 생기고 불변식이 그 결박들을 위반으로 헤아린다.
- [ ] **G3-b** **`pal export` 가 새 라벨을 「이 빌드의 2층에 없다」로 잘못 적지 않는다** · **결정론적** (`export.rs:289-297` 의 `lives_in`) ⟨개정 R1⟩
- [ ] **G4** **감시 집합을 못 세우는 좌표에 `Fresh` 를 안 찍는다** · **결정론적** ⟨개정 R1 · 강화 · 금지역⟩
  ⚠ `BindingStatus::evaluate`(`binding.rs:613-651`)는 `binding.watch` 만 순회하므로 **빈 벡터면 `changed`·`gone` 이 전부 비어 `Fresh` 가 나온다.** 화면이 *"이 결정은 유효하다"* 를 찍는데 아무것도 안 재고 있다. `Radius` 넷도 전부 심볼 이웃 위에 정의돼 있어(`radius.rs:107-164`) 문서 좌표에는 반경이 원리상 없다.
  **`C4` 였던 「낡음이 계산된다」보다 약하되 금지역은 막는 문면이다** — 낡음을 못 재는 것과 안 재면서 유효하다고 찍는 것은 다른 일이다.
- [ ] **G5** **RED 관측** — 착수 시점에 `pal bind "AGENTS.md"` 가 *"이 스냅샷에서 찾지 못했다"* 로 거부하는 것을 관측했다([`observations/binding-target-red.txt`](observations/binding-target-red.txt)). 그 명령이 성공하는 것이 `G1~G3` 의 GREEN 이다
- [ ] **G6** **음성 대조** — 확장한 뒤에도 **아무 문자열이나 좌표로 받지는 않는다.** 저장소에 없는 경로를 대면 거부하는 것을 관측한다. 거부가 안 오면 확장이 좌표계를 연 것이 아니라 검사를 끈 것이다
- [ ] **G6-a** **새 감시 항목 타입이 「앵커는 신고받지 않는다」 검사의 모집단에 든다** · **결정론적** ⟨개정 R1 · 금지역⟩
  ⚠ 그 검사는 소스에서 문자열 `"WatchEntry {"` 를 찾는다(`xtask/src/main.rs:2034,2054`). 감시 항목을 다른 이름으로 만들면 **검사가 그것을 아예 안 보고 사각도 안 적는다** — 측정이 죽은 가지가 된다. 하한이 「0 이면 실패」뿐이라 **부분 사각을 못 잡는다.**
- [ ] **G7** `G1~G4` 의 결정과 근거가 **ADR 로 발행된다** — 초석이라 나중에 바꾸면 그 위가 전부 움직인다
- [ ] **G7-a** 그 ADR 이 **이슈 [#69] 의 설계(문서 노드 + 문서→문서 엣지)와 이 회차의 설계(결박 대상 확장)를 대조하고 어느 쪽을 왜 골랐는지 적는다** ⟨개정 R1 · 확대⟩

### D — 재고 (선행 없음)

- [ ] **D1** `~/dev/projects/fabricate/` 의 `GOAL.md`(비준)와 `IMPLEMENTATION-ORCHESTRATION.md`(691줄 미비준)를 읽고 **무엇을 승계하고 무엇을 안 하는지**가 사유와 함께 적힌다. ⚠ 그 저장소는 이 저장소 밖이라 **좌표 면제**가 필요하다
- [ ] **D2** 2026-08-18 에 걷힌 옛 기능 문서 스물넷의 **순서 근거**를 [`disposal-map.md`](../../../docs/plan/disposal-map.md) 로 확인하고, 되살릴 것과 버릴 것이 사유와 함께 적힌다
- [ ] **D3** `NEXT-B-plan-for-harness.md` 가 **지워진다** — 그 문서가 *"착수하면 이 파일을 지운다"* 로 적었고 이 회차가 그 착수다 · **결정론적**
  ⚠ **그러므로 이 회차의 발견 레코드는 그 파일을 `경로` 로 안 쓴다** — 쓰면 `check_round_records` 의 좌표 해소가 발화해서 지우는 순간 검사가 실패로 뜬다(사전부검 R1 이 격리 사본에서 재현했다). `## 범위 밖` 이 그것을 못 박는다 ⟨개정 R1⟩

### E — 회차 규약 (선행 A·B·G·D)

- [ ] **E1** `cargo xtask check` **전량 통과 · rc=0**. **검사 수를 여기 안 적는다** — 세는 자리는 `xtask` 의 `checks.len()` 이다
  ⚠ 새 계획 문서는 **「어색한 표현 부재」 검사의 대상 안**이다(`docs/plan` 은 동결 경로가 아니다). 잠긴 의도의 문면을 그대로 옮기면 「~ 선다」·「켜다」가 발화한다 — 사전부검이 격리 사본에서 재현했다 ⟨개정 R1⟩
- [ ] **E2** `cargo test --workspace` 통과 · 0 실패 · **결정론적**
- [ ] **E3** `findings.jsonl` 에 **열린 것 0** — 끝난 회차 0 · 철회한 회차 0 · 닫힘→열림 전환 0
- [ ] **E4** 종료 보고 `report.md` 에 **네 이름이 없다** — 「의도적으로 안 한 것」·「확인 못 한 것」·「추론(확인 아님)」·「다음으로 넘기는 것」
- [ ] **E5** 게이트 `docs/gates/terrain-and-completion-scene.md` 의 `## 판정` 이 **표준 표**로 서고 검산이 맞는다 · **결정론적** (「원장 둘 대조」)
- [ ] **E5-a** **「어색한 표현 부재」 검사가 이 회차의 종결 문서도 잰다** · **결정론적** ⟨개정 R1 · 확대 · 금지역⟩
  ⚠ 그 규칙을 재는 자리가 앞 회차의 파일 이름 **두 문자열로 하드코딩**돼 있다(`xtask/src/main.rs:4534-4537`). `.palimpsest` 는 교정 뿌리 밖이고 `docs/gates` 는 동결이라, **고치지 않으면 새 회차의 `report.md` 와 게이트는 어느 검사에도 안 든다** — 앞 회차가 세운 장치가 다음 회차를 여는 순간 죽은 가지가 된다.
- [ ] **E6** **회차 산출의 마지막 커밋에 `conclusion=success` 런이 붙는다** ⟨개정 R1 · 정정⟩
  **「산출」의 경계를 여기서 정한다 — CI 결과를 받아 적는 커밋은 산출이 아니라 관측이다.** 그 정의가 없으면 앞 회차의 `H2` 와 같은 회귀가 난다: `report.md`·게이트가 산출이므로 CI 결과를 적는 커밋이 하나 더 필요하고, 그 커밋도 산출로 세면 마지막 커밋이 영원히 관측되지 않는다(사전부검 R1).
  ⚠⚠ **push 는 한 번만 한다.** CI 가 `cancel-in-progress: true` 다(`ci.yml:52-54`). 두 번 밀면 첫 런이 `cancelled` 가 되고, 그때 이미 커밋에 적어 둔 *"success"* 는 **사실이 아닌 것을 사실로 적은 것**이 된다.

### F — 효과 (선행 G)

- [ ] **F1** **`pal` 로 이 회차의 결정을 조회한 출력**이 게이트 `## 효과` 에 붙는다. 시험도 CI 도 아닌 것이 돌아야 한다
- [ ] **F2** 그 조회가 **능력 부재**를 내면 그것을 적는다 — 틀린 답이어도 붙인다. `AGENTS.md` 가 *"그래프가 못 답하면 「능력 부재」로 내고, 모델 추정이 그 자리를 메운 사실을 적는다"* 로 요구한다

## 차선책

- **`G4`(감시 집합 없는 좌표의 상태)가 기존 상태 넷으로 표현이 안 되면** — **새 사유를
  더하지 않고** 「낡음을 안 잰다」를 화면에 적는 쪽으로 약하게 선언한다.
  ⚠ `UndeterminableReason::ALL: [Self; 4]` 와 `corpus/criteria.toml:7124` 의
  `undeterminable_reasons_built = 4` 가 그 수를 잠갔고 `corpus/` 는 **범위 밖**이다.
  다섯째 사유가 정말 필요하면 이슈 **#116**(「상태 넷으로 충분한가」)이 그 물음을 진다 ⟨개정 R1⟩.
- **`B1`(능력별 실측)이 일곱 중 어느 능력에서 원리상 못 재면** — 그 능력만
  **「못 쟀다」로 적고** 나머지로 순서를 세운다. 못 잰 능력은 순서표에서 **맨 뒤**가
  아니라 **「순위 미정」** 으로 둔다 — 못 잰 것을 뒤로 미루면 측정 실패가 우선순위 판단으로
  둔갑한다.
- **`A3`(P8 장면)이 지금 그래프로는 못 그려지면** — 장면을 **목표 상태**로 적고 무엇이
  갖춰지면 서는지를 함께 적는다. 지우지는 않는다.
- **`G2-b`(판을 안 올린다)가 안 되면** — 올리기 전에 멈추고 소유자에게 올린다. 되돌릴
  경로가 없는 변경이라 차선책이 아니라 승격이다 ⟨개정 R1⟩.

## 범위 밖

- **순서표의 첫 항목을 실제로 구현하는 것.** 이 회차는 첫 항목이 이슈로 서는 데까지다.
- **`C1~C6`·`U` 각 능력을 실제로 올리는 것.** 실측하고 줄 세우는 데까지다.
- **`pal` 에 결박을 거는 **새 명령**을 만드는 것.** ⚠ **`pal bind` 의 `NAME` 인자가 받는
  것을 넓히는 것은 범위 안이다** — `G5` 가 그 명령의 성공을 GREEN 으로 삼는다. 새 화면·새
  하위 명령이 범위 밖이고, 기존 인자의 의미 확장은 아니다 ⟨개정 R1 · 사전부검이 갈림을 지목했다⟩
- **열린 이슈 31 건의 재분류·재라벨.**
- **`AGENTS.md` 의 다른 절을 고치는 것.** 두 칸이 새 문서를 가리키게 하는 것까지다.
  ⚠ `scripts/frontier.sh:78` 의 한 줄은 **범위 안**이다 — `B4-a` 가 진다 ⟨개정 R1⟩.
- **철회한 회차 `2026-08-23-check-verifies-work` 의 재료 `NEXT-E-*.md` 둘.** 그
  회차의 `findings.jsonl:84,117,144` 이 `NEXT-E-prompt.md` 를 좌표로 결박하고 있어서
  지우면 「회차 레코드」 검사가 실패로 뜬다.
- ★ **이 회차의 발견 레코드에 `NEXT-B-plan-for-harness.md` 를 `경로` 로 안 적는다.**
  `D3` 이 그 파일을 지우므로, 좌표로 적으면 지우는 순간 좌표 해소가 발화한다.
  그 문서를 가리켜야 하는 발견은 **그 문서가 지목한 실제 코드 좌표**를 적는다 ⟨개정 R1⟩.
- ★ **회차 관측을 `.json` 확장자로 놓지 않는다.** `docs/sunset.toml:27` 의 트리거가
  `.palimpsest/rounds/**/*.json` 이라 파일 하나로 트리거가 참이 되고, 선언된
  `docs/research`(3,015줄)·`evidence-map.md` 를 지우라고 검사가 요구한다. **`.txt` 로 둔다** ⟨개정 R1⟩.
- ★ **`corpus/` 의 사전 등록 상수.** 소유자가 *"범위 밖 — 측정 장치다"* 로 잠갔다 ⟨개정 R1⟩.
- ★ **`docs/sunset.toml` 이 유보한 `docs/research` 의 처분.** 그 선언은 유보 사유를
  *"새 지형이 그것을 어디에 둘지 정한 뒤에 간다"* 로 적었고 이 회차가 그 새 지형이다.
  **이 회차는 그 물음에 답하지 않는다** — 이슈 **#68** 이 진다. 사유를 여기 적어 두는 것이
  이 줄의 목적이다: 안 적으면 다음 사람이 그 문장을 읽고 또 미룬다 ⟨개정 R1⟩.

## 상한

| 자리 | 상한 | 근거 |
|---|---|---|
| 인터뷰 | **4 라운드** | 소유자가 등록했다. 상한에서 닫았고 범주 다섯이 전부 열렸다 |
| 사전부검 | **3 라운드** | 소유자가 등록했다. 실측상 소진은 안 온다(12·16·14) — 손실 제한이다 |
| 조건 설계 평가 | **2 라운드** | 규약 §3.5 의 기본값 |
| 독립 리뷰 | **5 라운드** | 소유자가 등록했다. 소유자 결정 — *"독립 리뷰 상한을 다 쓴다"* |
| 정반합 | **2~3 판** | 소유자가 등록했다 — *"갈리는 자리만 골라 두세 판"*. 어느 자리가 갈리는지는 초안을 써 봐야 보이므로 **묶음은 메인이 정하고 근거를 `## 개정` 에 적는다** |

**상한에 닿는 것은 완료가 아니다.** §11 ② 해악 게이트가 통과해야 닫힌다.
**승격은 상한을 리셋하지 않는다.**

## 모집단 분리 — **(나) 를 고른다**

§11 ③ 이 요구하는 착수 시점 선택이다.

**(나) 장치를 만들되, 그것에 대한 발견은 별개 의도로 새 회차를 연다.**

까닭: **`G`(결박 대상 확장)는 장치를 만드는 것 자체가 산출물이다.** (가)「장치를 회차 안에서
만들지 않는다」가 원리상 불가능하다.

★ **별도 목록의 경계를 여기서 좁힌다** ⟨개정 R1⟩. 사전부검이 *"`A`·`B` 의 산출이 같은
회차의 `G`·`D`·`E` 를 판정하는 자로도 쓰인다"* 를 지목했다. 그러므로:

- **`G` 가 만든 코드**(스키마·저장·조회)에 대한 발견 → **자기 장치**. 별도 목록.
- **`A`·`B` 가 만든 문서**(완성 장면·순서표)에 대한 발견 → **원 의도**. 이 회차가 닫는다.
  그 둘은 이 회차의 산출물 자체이지 측정 장치가 아니다.
- **`E5-a` 가 고치는 검사**에 대한 발견 → **자기 장치**. 다만 금지역에 닿으면 본 모집단.

⚠ **예외** — 자기 장치에 대한 발견이 **금지역에 닿으면 본 모집단**이다. 미룰 수 없다.

## 두께 — **메인이 정했다**

소유자가 *"이건 네가 정해줘"* 로 넘긴 자리다. 정한 것과 근거를 여기 적는다.

**줄 수 상한을 안 건다.** 줄 수로 재면 내용이 아니라 압축을 유도해서 근거를 지우고 결론만
남기는 쪽으로 간다 — 그리고 이 저장소는 게이트 문서 크기를 산문으로 적었다가 **세 판 연속
거짓**을 낸 전례가 있다(`/round` §9).

**대신 문서를 둘로 가른다.** 완성 장면은 제품이 무엇인지라 잘 안 바뀌고, 순서표는 회차마다
바뀐다. 한 파일에 두면 순서를 고칠 때마다 장면이 함께 커밋되고 그것이 낡는 캐시의 씨앗이다.

⚠ **가른 것과 `A4` 가 서로를 무르게 한다** ⟨개정 R1⟩. `A4` 가 기계로 서려면 순서표가 장면을
**파일 링크**로 걸어야 하는데, 같은 문서 안이면 `파일_경로인가`(`xtask/src/main.rs:2955`)가
`(#앵커)` 를 링크로 안 세어 **대조 자체가 안 선다.** 가른 덕에 `A4` 가 성립한다 — 대신
장면 문서의 절 이름을 고치면 순서표 쪽이 죽은 링크로 실패한다. **그 결합은 받는다.**

## 개정

| # | 라운드 | 무엇을 왜 바꿨나 | 처분 |
|---|---|---|---|
| 1 | 사전부검 R1 | **완수 조건 `C` 절을 `G` 로 개명.** 능력 계약 `C1~C6` 과 같은 이름 공간이라 `## 차선책` 이 이미 갈렸다 | 정정 |
| 2 | 사전부검 R1 | **`## 금지역` 절 신설.** `.claude/pal/policy.toml` 이 없고 잠긴 의도에도 없어 기본 다섯이 적용되는데, `G` 가 사람의 결박 데이터를 만지므로 「데이터 손실」의 경계를 이 회차가 정해야 한다. 규약이 회차 중 등록을 금해서 지금이 유일한 시점 | 확대 |
| 3 | 사전부검 R1 | **`G2-a`·`G2-b`·`G2-c` 신설** — postcard 판 부재 · `bindings.jsonl` 판 롤백 불가 · 색인 32바이트 열쇠. 셋 다 금지역 | 확대 |
| 4 | 사전부검 R1 | **`C4`(낡음이 계산된다)를 `G4`(감시 집합 없는 좌표에 `Fresh` 를 안 찍는다)로 강화.** 낡음을 못 재는 것과 안 재면서 유효하다고 찍는 것은 다른 일이고, 뒤엣것이 금지역이다 | 정정 |
| 5 | 사전부검 R1 | **`G3-a`·`G3-b`·`G6-a` 신설** — `doctor` 유령 엣지 · `export` 라벨 오기 · 검사 12 의 이름 기반 모집단 | 확대 |
| 6 | 사전부검 R1 | **`G7-a` 신설** — ADR 이 이슈 #69 의 다른 설계를 대조한다 | 확대 |
| 7 | 사전부검 R1 | **`A4` 를 「어느 항목」에서 「어느 문서 절」로 완화.** 죽은 링크 검사의 산출에 줄 번호가 없어 항목 단위로 못 짚는다 | 축소 — **승격 대상** |
| 8 | 사전부검 R1 | **`B2` 에 횡단요구 `U` 추가.** 채점 단위가 일곱인데 여섯만 적어 `U` 에만 매달리는 일이 사유 없이 빠진다 | 정정 |
| 9 | 사전부검 R1 | **`B1` 에 측정 커밋 요구 추가.** 같은 회차의 `G` 가 그 값을 바꿔 표가 회차 안에서 낡는다 | 정정 |
| 10 | 사전부검 R1 | **`B3-a` 신설** — 예외의 문턱. `B3` 이 *"억지로 매달면 축이 거짓"* 이라 적고도 문턱을 안 뒀다 | 확대 |
| 11 | 사전부검 R1 | **`B4-a` 신설** — `frontier.sh` 의 「지형은 ⏳ 비었다」 한 줄 | 확대 |
| 12 | 사전부검 R1 | **`E5-a` 신설** — 「어색한 표현 부재」 검사의 대상이 앞 회차 파일 이름으로 하드코딩돼 이 회차 종결 문서가 사각이다. 측정이 죽은 가지 | 확대 |
| 13 | 사전부검 R1 | **`E6` 에 「산출」의 경계 정의와 push 한 번 제약 추가.** 안 정하면 앞 회차 `H2` 와 같은 회귀가 나고, 두 번 밀면 `cancel-in-progress` 가 첫 런을 죽여 「success」가 거짓이 된다 | 정정 |
| 14 | 사전부검 R1 | **`## 범위 밖` 에 다섯 줄 추가** — `NEXT-B` 를 레코드 좌표로 안 씀 · 관측은 `.txt` · `corpus/` 상수 · `sunset.toml` 의 `docs/research` 처분(#68) · `frontier.sh` 는 범위 안 | 범위밖 |
| 15 | 사전부검 R1 | **`## 범위 밖` 의 「사람용 표면」을 명확히.** `pal bind` 인자 의미 확장은 범위 안, 새 명령·새 화면은 범위 밖 | 정정 |
| 16 | 사전부검 R1 | **`## 차선책` 에 `G2-b` 항 추가**, `G4` 차선책을 「새 사유를 안 더한다」로 고정(`corpus/` 가 범위 밖이라) | 정정 |
| 17 | 사전부검 R1 | **`## 모집단 분리` 의 별도 목록 경계를 좁혔다** — `G` 의 코드는 자기 장치, `A`·`B` 의 문서는 원 의도 | 정정 |
| 18 | 사전부검 R1 | **`## 두께` 에 `A4` 와의 긴장을 적었다** — 가른 덕에 `A4` 가 성립하고, 대신 절 이름 변경이 죽은 링크를 만든다. 그 결합은 받는다 | 수용사유 |
| 19 | 사전부검 R1 | **`## 착수 시점 관측` 에 저장 형식의 판 표시·기존 결박 22 건·이슈 #69 를 추가** | 정정 |
| 20 | 사전부검 R1 | **`E1` 에 새 계획 문서가 「어색한 표현 부재」 검사 대상임을 적었다** | 정정 |

★ **7 번이 유일한 축소다 — 소유자에게 올린다.** `## 승격` 을 본다.

## 승격

### 승격 1 — `A4` 의 해상도를 낮춘다

**물음**: `A4` 의 음성 대조를 *"장면 하나를 지우면 순서표의 **어느 항목**이 근거를 잃는지
기계가 답한다"* 에서 *"**어느 문서 절**이 근거를 잃는지"* 로 낮춰도 되나.

**왜 올리나**: 재는 의도의 양이 줄었으므로 정정이 아니라 축소다. 등록된 차선책이 아니다.

**근거**: 기계로 세울 수 있는 유일한 자가 죽은 링크 검사인데, 그 산출이
`"{파일} → {대상}"` 이라 **줄 번호도 항목 이름도 없다**(`xtask/src/main.rs:2653,2661`).
항목 둘이 같은 장면을 가리키면 두 줄이 글자까지 같아 구별이 안 된다. 항목 단위로 짚으려면
**항목마다 유일한 앵커를 강제**해야 하고, 그것은 순서표의 형식을 정하는 별개의 일이다.

**대안**: ① 낮춘다(제안) ② 항목마다 유일 앵커를 강제한다 — 이 회차가 커진다
③ `A4` 를 「대조 불가」로 판정하고 그 사실을 게이트에 적는다.

**소유자의 답**: *(아직 없다)*
