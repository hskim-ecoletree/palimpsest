# 상태 — 지형과 완성 장면

> 회차 `2026-09-06-terrain-and-completion-scene` · 착수 커밋 `7c41b81`
> 잠긴 의도는 [`intent.md`](intent.md) 가 진다.

## 지금 단계

**판정 기록이 섰고 독립 리뷰가 돈다.** 완수 조건 32 판정 · 게이트 · 종료 보고 · 결박이 끝났고,
남은 것은 리뷰 라운드와 push 다.

## 지나온 것

| 단계 | 결과 | 커밋 |
|---|---|---|
| 인터뷰 | 4 라운드 · 상한에서 닫음 · 범주 다섯 전부 열림 | — |
| 잠금 | 조건 27 | `b01fe49` |
| 사전부검 R1 | 시나리오 28 · 기각 5 · 금지역 6 | — |
| R1 처리 방침 | 개정 20 · 조건 27 → 37 | `fc8bd43` |
| 사전부검 R2 | 시나리오 11 · 기각 4 | — |
| 조건 설계 평가 R1 | 발견 23 · 기각 6 · **조건의 합이 의도를 네 대목에서 안 덮음** | — |
| **범위 축소** | 결박 대상 확장을 이슈 [#119] 로 분할 · 조건 37 → 32 | `0b4a88e` |
| 승인 | 소유자가 범위 축소를 승인했다 | — |
| 루프 | 완성 장면 · 순서표 · `AGENTS.md` 두 칸 · `frontier.sh` · 효과 관측 | `3b5762a`~`2cc703c` |
| 검사 27 | 완성 장면 형식이 `A1`·`A1-a` 를 잰다(음성 대조 포함) | `844427e` |
| **정반합 판 1** | §3 이 축을 지켰나 — **교착.** 상한 두 라운드를 다 썼고 금지역 1 · 실패 4 가 남았다 | `ed97b62` |
| **정반합 판 2** | 완성 장면이 `A2`·`A3` 를 지켰나 — **닫혔다.** 둘 다 문면대로 냈다 | `0051e05` |
| **정반합 판 3** | 순서와 재고가 근거를 지나 — **교착.** 금지역 1 이 남았다 | `f2cfc7a` |
| 문면 정정 | 세 판이 지목한 좌표를 고쳤다 | `b3b7282` |

## 남은 것

- 독립 리뷰 — 상한 5 라운드 중 **2 라운드를 썼다**
- push · 그 뒤에 `E6`·`E6-a` 판정

## 리뷰가 잡은 것 — 라운드 1

| 무엇 | 해악도 | 어떻게 처리했나 |
|---|---|---|
| `E5-a` 가 세운 선택자가 이 회차의 종결 문서를 안 잰다 — 사전순 최대 하나만 골라 같은 날짜의 형제 회차에 가렸다 | 금지역 | **정정.** 선언이 진 하한 이후에 끝난 회차를 **전부** 잰다. 고친 뒤 이 회차의 보고와 게이트에서 다섯 곳이 새로 잡혔고 그것도 고쳤다 |
| 종료 보고가 하지 않은 결박을 「걸었다」로 적었다 | 금지역 | **정정.** 실제로 결박했다 — 코드 심볼 둘. `binding.status` 가 24 건을 산출한다 |
| 순서표 §5.1 의 분모 산술이 한 문장 안에서 어긋난다(13+13 인데 24 라 적었다) | 거짓신호 | **정정.** 24 는 파일 수이고 26 은 처분 행 수라는 것을 적었다 |
| 처분표가 새 지형을 아직 미래형으로 가리킨다 | 거짓신호 | **정정.** 그 `B` 가 이 회차였다는 것과, `B` 처분 35 건 중 다섯만 승계됐다는 것을 함께 적었다 |
| 게이트가 「아래 `E6` 행을 보라」로 가리키는데 그 행이 없었다 | 거짓신호 | **정정.** `E6`·`E6-a` 의 사유와 차선책 처리를 절로 세웠다 |
| 종료 보고의 「원리상 못 잰 것」 한 행이 경계가 아니라 잔여였다 | 거짓신호 | **정정.** 실제로 재서 순서표의 근거 칸을 되짚을 수 있는 줄로 바꾸고 그 행을 뺐다 |
| `check_ledger_pair` 의 「최근 끝난 회차」도 사전순이라 사실과 다르다 | 거짓신호 | **범위밖.** 그 검사에서는 두 회차가 다 대상이라 판정을 안 뒤집는다. 이슈 [#126] 으로 섰다 |

## 리뷰가 잡은 것 — 라운드 2

| 무엇 | 해악도 | 어떻게 처리했나 |
|---|---|---|
| 게이트의 `E5-a` 통과 근거가 **철회된 구현**을 적는다 — 「가장 최근 회차 하나」 | 금지역 | **정정.** 지금 구현(하한 이후 전부)과 두 번의 정정 경위를 그 행에 적었다 |
| 선택자가 fail-open 이다 — 하한을 못 읽으면 대상이 비고 검사가 조용히 초록이 된다. 코드 주석은 반대로 적었다 | 금지역 | **정정.** 하한을 못 읽거나 대상이 비면 실패로 돌린다. 선언을 「선언 목록이 닫혀 있나」의 검증 목록에 넣었고, 음성 대조를 돌려 관측으로 남겼다 |
| 순서표 §4 가 이슈 #77 의 제목을 실제와 다르게 인용한다 | 거짓신호 | **정정.** 인용을 걷고 내용을 적었다 — 제목의 낱말 하나가 검사 대상이라 원문대로 실으면 이 문서가 빨개진다 |
| 처분표에 미래형 「B」가 셋 더 남았고 그중 하나는 이 회차가 이미 한 일이다 | 거짓신호 | **정정.** 셋 다 고쳤고, `B` 처분 35 건 중 무엇이 승계됐는지도 적었다 |
| `fabricate` 의 안 본 절 둘은 이 회차에서 할 수 있었던 잔여다 | 거짓신호 | **정정.** 34 줄을 읽고 처분을 적었다. 이슈 [#125] 를 닫았다 |
| 이 회차가 연 이슈 #126 이 어느 종결 기록에도 안 실렸다 | 거짓신호 | **정정.** 종료 보고와 이 표에 실었다 |
| 잠긴 의도의 `## 모집단 분리` 가 `xtask` 산출을 하나로 센다 — 실제로는 둘이다 | 거짓신호 | **정정.** 개정 45 로 그 자리에 적었다. 문면은 그때의 셈이라 안 고친다 |
| R1 이 고친 선택자에 대한 음성 대조 관측이 없다 | 거짓신호 | **정정.** 두 조건으로 돌려 `observations/negative-control-selector.txt` 에 남기고 게이트가 그것을 댄다 |
| 어느 조건도 안 요구한 계획 문서 편집 둘(`README.md` · `disposal-map.md`) | 미관 | **수용.** 산출 목록과 어긋난다는 사실을 여기 적는다 — 둘 다 낡음을 지운 것이고 `## 범위 밖` 이 금하지 않는다 |

## 소유자에게 올라간 것 — **이 회차는 그 답 없이 못 닫는다**

| # | 물음 | 어디서 나왔나 |
|---|---|---|
| 1 | 순서표를 「제품 능력의 순서표」로 세우는 것을 승인하는가 — 원문의 「하네스는 `C1~C6` 의 소비자다」는 그 순서표 밖에서 산다 | 판 1 · 설계문이 사전 등록한 문면 그대로 |
| 2 | 축 문장을 **전칭**(표면 하나라도 아니면 거짓) · **다수** · **예외 하나 허용** 중 무엇으로 읽는가 | 판 1 · 이 답 없이는 `B3-a` 의 문턱이 실측을 다시 해도 안 선다 |
| 3 | 순서표의 첫 항목이 §2 의 1 번과 다른 축인 것을 어느 갈래로 해소하는가 — ㉠ §4 가 다른 이슈를 댄다 ㉡ §2 의 1 번 문면을 `#77` 에 맞춘다 ㉢ 새 이슈를 세운다 | 판 3 · 종료 판단도 같은 물음을 독립으로 냈다 |
| 4 | 완수 조건 `B5` 의 문면이 항등식인데 고칠 것인가 그대로 둘 것인가 | 판 3 · 설계문이 이 판에서 못 고치게 하고 승격으로 등록했다 |

**셋을 하나로 합치지 않는다** — 판 3 의 종료 판단이 그것을 명시로 금했다.

## 이 회차가 세운 이슈

| 이슈 | 무엇 |
|---|---|
| [#119] | 결박 대상 확장 — 범위 축소로 분할 |
| [#120] · [#121] | `fabricate` 에서 승계한 물음 둘 |
| [#122] | 순서표 §3 에 「소비자다」의 정의 절이 없다 (판 1 · 교착) |
| [#123] | 첫 항목 `#77` 이 §2 의 1 번과 다른 축이다 (판 3 · 금지역) |
| [#124] | `B5` 의 음성 대조가 항등식이다 (판 3) |
| [#125] | `fabricate` 승계에서 안 본 절 둘 (판 3 · 미측정) — **리뷰 R2 뒤에 읽고 처분해 닫았다** |
| [#126] | 「최근 끝난 회차」를 사전순으로 고르는 자리가 하나 더 있다 (리뷰 R1) |

[#119]: https://github.com/hskim-ecoletree/palimpsest/issues/119
[#120]: https://github.com/hskim-ecoletree/palimpsest/issues/120
[#121]: https://github.com/hskim-ecoletree/palimpsest/issues/121
[#122]: https://github.com/hskim-ecoletree/palimpsest/issues/122
[#123]: https://github.com/hskim-ecoletree/palimpsest/issues/123
[#124]: https://github.com/hskim-ecoletree/palimpsest/issues/124
[#125]: https://github.com/hskim-ecoletree/palimpsest/issues/125
[#126]: https://github.com/hskim-ecoletree/palimpsest/issues/126

## 실패한 접근

**반환문 형식이 기계와 갈린 자리 여섯.** 값이 아니라 전부 형식이었다:

1. **기각 절 뒤에 제목 없는 꼬리를 두면 안 된다** — 합계 검산의 기각 절 계수가 `## ` 로만 리셋된다.
2. **기각 표의 첫 열 이름은 추출기의 열 별칭에 있어야 한다** — `발견`·`안 잰 조건`·`기각한 것`·`시나리오`·`제목`·`항목`.
3. **메타 줄을 한 줄에 뭉치면 안 된다** — 세 줄로 나눈다.
4. **좌표에 와일드카드를 쓰면 안 된다.**
5. ★ **취합 보고의 다른 표가 별칭 열 이름을 쓰면 그 표의 행이 전부 발견으로 세어진다** — 이번에 판 2 의 반증 항 표(`항목` · `시나리오(설계문 원문)`)가 그랬고 여덟 행이 원장에 부풀어 들어갈 뻔했다. 열 이름을 별칭 밖으로 바꿔 막았다.
6. ★ **보존 산출물이 인용문 안에 마크다운 링크를 담으면 죽은 링크 검사가 빨개진다** — `.palimpsest/rounds/**` 에서는 `docs/plan/` 상대 경로가 안 풀린다. 링크가 아닌 표기로 바꾸고 그 사실을 파일 머리에 적었다.

★ **에이전트가 적은 자기 계수는 이번에도 갈렸다** — 판 3 의 반(反)이 설계문 좌표 넷을 자기 입력본의 줄 번호로 적었고(정확히 22 줄씩), 합(合)이 원문을 열어 잡았다.

★★ **격리는 이번 판들에서 지켜졌다.** 여섯 자리가 각각 무엇을 안 받았고 무엇을 열었는지를 산출물 머리에 적었고, `git log` 커밋 제목에서 앞 판의 결론 한 조각이 새어 든 것을 셋이 자진 신고했다.
