> `pal-decision-synthesizer` 가 낸 것을 그대로 보존한다. 요약하지 않았다.
> 받은 것: 정의 초안 · 반의 반론 · 판 3 설계문 · 정이 돌린 재측정 관측.
> 안 받은 것: 대화 기록 · 메인의 사고 과정 · 판 1·2 의 산출물 · 재측정 권한.
> 실행 2026-09-07

---

# 판 3 — 합(合)

> 나는 판 3 의 **합(合)** 이다. **읽기 전용으로 돌았다** — 파일을 안 고쳤고 커밋을 안 했다.
> **받은 것**: `dialectic/3-thesis.md` 전문 · `dialectic/3-antithesis.md` 전문(해악도 포함) · `dialectic/3-design.md` 전문 · `observations/order-derivation-recheck.txt`.
> **안 받은 것**: 대화 기록 · 메인의 사고 과정 · 판 1·2 의 산출물 · 재측정 권한. `dialectic/1-*.md` · `2-*.md` · `intent.md` 는 **열지 않았다.**
> **내가 연 것 — 좌표 확인 목적으로만.** `docs/plan/02-order.md`(전문) · `docs/plan/disposal-map.md:34-84` · `docs/plan/00-goals.md:195-210`·`:153` · `docs/plan/01-completion-scenes.md`(`grep`·`wc`) · `scripts/frontier.sh:30-60` · `crates/pal-core/src/projection.rs:214-254` · `crates/pal-cli/src/hook/catalog.rs:10-24` · `crates/pal-core/src/budget.rs:185-195` · `~/dev/projects/fabricate/GOAL.md:55-71`·`:217-232` · `~/dev/projects/fabricate/IMPLEMENTATION-ORCHESTRATION.md:618-640` · `gh issue view 77`. **왜 열었나** — 반(反)이 좌표를 붙여 낸 반론을 그대로 믿지 않기 위해서다. 저장소를 새로 재지 않았다(빌드·인덱스 0 회).

모집단 — `docs/plan/02-order.md` §2 여섯 항목 · §4 첫 항목 지목 · §5.1 다섯 행 · §5.2 승계 셋과 비승계 넷 · 그 문서가 대는 저장소 안팎 원천.
유효성 — 아래에 채택·기각으로 적은 것은 전부 내가 좌표를 직접 열어 확인했다. 확인 못 한 것은 `## 내가 못 정한 것` 에 갈라 적었다.
해악도 — 반(反)이 매긴 것을 그대로 안 쓰고, 내가 확인한 범위에서 유지·하향한다. 상향은 안 했다.

---

## 판정

**수정.** 초안의 산출 목록(가~자)은 대체로 서지만, 초안이 판정을 닫은 **문장**은 두 자리에서 틀렸다 — ① `B6` 를 「읽기 C」로 이름 붙인 것이 측정보다 좁고, ② *"산출은 문면 정정과 근거 보충이지 §2 순서의 변경이 아니다"* 가 **§5 의 처분이 바뀐다는 사실**과 **§4 가 기계에 먹이는 첫 항목이 §2 의 1 번이 아니라는 사실**을 대조항에서 빼놓았다.

---

## ① 갈래 넷의 판정 이름

| 갈래 | 조건 | 초안이 세운 이름 | **내가 세운 이름** | 왜 |
|---|---|---|---|---|
| 갈래 1 | `B6` | 읽기 C — 분업 | **읽기 E — 삼원 분산, 그리고 첫 항목은 넷째 출처가 정한다** | 아래 |
| 갈래 2 | `B5` | 읽기 B — 사후 부착 | **읽기 B — 사후 부착**(유지) | `docs/plan/02-order.md:87-94` 가 §1 을 0 회 댄다. 정·반이 독립으로 같은 값을 냈고 내가 원문을 열어 확인했다 — 그 절이 대는 것은 `C5` 의 **정의**뿐이고, 의존 사슬의 역순은 언제나 무너지므로 위반이 원리상 안 난다 |
| 갈래 3 | `D2` | 읽기 B — 사후 부착 | **읽기 B — 사후 부착**(유지, 모집단 확대) | §5.1 의 다섯은 §2 가 이미 가리킬 수 있는 행이고, 되살림 사유(`:171-176`)는 열세 행 전부에 똑같이 걸려 다섯을 못 가른다. 여기에 §1.1 의 열하나가 **한 번도 안 대어졌다**는 것이 더해진다 |
| 갈래 4 | `D1` | 읽기 B — 사후 부착 | **읽기 B — 사후 부착**(유지, 근거 하나 추가) | 인용은 전부 실재하나(M2) 배분이 한쪽 통에만 자를 댄다. 여기에 **선택 규칙이 문서에 없다**는 것이 더해진다 |

**왜 `B6` 가 C 가 아니라 E 인가 — 넷을 열어서 셌다.**

- §1 이 실제로 지는 것: `C2` 행의 **사실**(참조 엣지 3 · Rust 134 파일 기여 0). 이 행을 지우면 `:83-85` 의 ★ 가 통째로 사라진다 — 초안의 NC-3 이 옳다.
- 그러나 그 사실을 **1 등으로 바꾸는 순위 규칙**(*"0 이 부재보다 소비 가능에 가깝다"*)은 §1 에 없다. §1 은 `:64` 에서 0 과 부재를 **구분**할 뿐 **줄 세우지 않는다.** 규칙은 `AGENTS.md` 의 소유자 지시를 §2 가 적용한 것이다.
- `00-goals.md:203` 의 `C5` 정의(*"진입점 E 에서 지점 S 로 가는 경로"*)가 **6 번이 4 번 뒤**라는 한 자리를 혼자 진다. 나머지 의존 주장(2 는 1 뒤 · 3 은 2 뒤 · 4 는 2 뒤 · 5 는 2·4 뒤)은 §1 에도 정의에도 없고 §2 가 새로 썼다. 3↔4 와 5↔6 은 무엇도 안 금한다.
- **그리고 기계가 읽는 첫 항목은 넷째 출처가 정한다** — `docs/plan/02-order.md:146`·`:149` 가 `#77` 을 지목하고 `scripts/frontier.sh:37-41`·`:55` 가 그것을 프론티어 맨 위에 놓는다.

읽기 C 는 *"뼈대는 능력 정의가, 1 번은 §1 이"* 라고 말한다. 실측은 **정의가 진 것이 여섯 중 한 자리뿐**이고 **1 번조차 §1 + 외부 규칙의 합작**이며 **실제로 착수되는 것은 셋 다와 다른 데서 온다**고 말한다. C 로는 이 셋이 안 담긴다.

---

## ② 채택한 반론

| # | 반론 | 왜 채택했나 | 해악도 |
|---|---|---|---|
| A1 | 순서표의 1 번이 가리키는 `#77` 이 §2 의 1 번이 아닌데 기계가 그 줄을 읽는다 | **내가 직접 확인했다.** `gh issue view 77` 본문은 ① `attribute_item` 이 `body_digest` 밖이라 derive 를 갈아도 `Live` 로 남는다(낡음 = `C6`) ② `parse.rs:248` 의 죽은 가지다. 스코프·임포트·익스포트가 본문에 **없다.** §2 의 1 번은 `C2`(스코프 사슬 → `file_edges`)이고 §1 `:35` 가 대는 막는 것은 `ledger.rs:363-369` 의 `NotBuilt` 다 — `#77` 은 그것을 안 건드린다. `docs/plan/02-order.md:149` 의 *"그 이슈가 지는 것이 §2 의 1 번이다"* 는 **사실이 아니다.** 그리고 `scripts/frontier.sh:40` 이 그 줄을 정규식으로 긁어 `:55` 에서 `← 순서표의 1 번` 을 붙인다 | **금지역** |
| A2 | `:83-85` ★ 의 순위 규칙이 §1 에 없다 (**핵심만 채택**) | §1 의 일곱 행 어디에도 「0 이 부재보다 소비에 가깝다」가 없다는 것을 원문에서 확인했다. `:64` 는 *"낮은 값이 아니라 부재다"* 로 **측정 불가**를 적은 것이지 근접도를 적은 것이 아니다. 이것이 `B6` 의 이름을 C 에서 E 로 옮긴다 | 거짓신호 |
| A3 | NC-2 에서 유일하게 「§1 이 금한다」로 판정한 칸이 좌표를 거꾸로 읽었다 (**범위를 좁혀 채택**) | `crates/pal-core/src/projection.rs:228` 을 열었다 — `RefResolution::OutsideFile => counts.unresolved += 1` 로, 파일 밖 참조는 **세기만 하고 엣지를 안 만든다.** 초안이 `:35` 를 *"스코프 사슬이 없으면 **파일 간** 엣지가 안 만들어진다"* 로 옮긴 것은 오독이다. `:35` 는 파일 **안** 엣지를 말하고 파일 경계는 *"별개로 미구축"* 이라 적는다 | 거짓신호 |
| A4 | 「`00-goals` 에도 없다」가 6 번에서 거짓이다 | `docs/plan/00-goals.md:203` 을 열었다 — `C5` 정의가 *"진입점 E 에서 지점 S 로 가는 경로"* 이고, `02-order.md:81` 의 뒷 문장 *"진입점(`C3`)에서 지점까지의 경로를 요구하므로 4 뒤다"* 는 그 정의의 되읽기다. 초안이 그것을 다섯 항목 전부에 걸어 부정했고, 자기 NC-1(*"근거로 대는 것은 `C5` 의 정의"*)과 어긋난다. 부하의 출처가 둘이 아니라 셋임을 드러낸다 | 거짓신호 |
| A5 | `D2` 의 분모는 스물넷인데 초안이 열셋으로 잡았고, 초안이 세운 대체 규칙이 `F09` 에서 깨진다 | `docs/plan/disposal-map.md:43`(`F02` 파싱·심볼 추출·**파일 내 스코프**)·`:49`(`F09` 결박과 낡음 `C6`)를 열었다. `grep -c F02 docs/plan/02-order.md` → `0`, `F09` → `0`. §5.1 의 제목은 `:159` 에서 **「걷힌 옛 기능 문서 스물넷」** 이고, 자기가 §1.2 만 쓴다는 말을 안 적는다. 초안의 *"승격 4 는 `F23` 에 대해서만 필요하다"* 는 좁다 | 거짓신호 |
| A6 | §5.1 에 쓴 덮음 시험을 §5.2 에 안 걸었고, 걸면 벌어진다 | `~/dev/projects/fabricate/GOAL.md:217-231`(§7 중단 조건 넷)과 `IMPLEMENTATION-ORCHESTRATION.md:620-638`(§8 완료 정의 초안 열 항목)을 열어 실재를 확인했다. 전자는 §5.2 가 그 절 **안**의 `:226` 한 줄만 뽑아 쓰고 절 자체는 승계·비승계 어느 통에도 안 넣었고, 후자는 §5.2 의 첫째 승계 항목(완료 상태)과 같은 주제인데 두 통 어디에도 없다. §5.2 는 **어떻게 골랐는지의 규칙을 한 줄도 안 적는다** — 초안이 §5.1 에서 짚어낸 것과 같은 종류의 공백을 §5.2 에서는 *"무너지는 것은 배분이다"* 한 문장으로 닫았다 | 거짓신호 |
| A7 | 남은 대안 중 둘이 §5 의 **처분 변경**인데 판정문이 §2 하나에만 대조하고 승격 1 을 안 부른다 | 설계문 `dialectic/3-design.md:168` 의 승격 1 문면이 *"§2 의 자리나 **§5 의 처분을 실제로 바꿔야 한다**"* 이고, 초안 `:39` 의 대조항은 *"§2 순서의 변경이 아니다"* 하나뿐이다. 초안 스스로 대안 「사」에 *"금지역 「사실이 아닌 것을 사실로」에 닿는다"* 를 붙여 놓고 승격 절을 안 밟았다. **이 판이 나에게 요구한 ③ 「문면 정정인가 처분 변경인가」의 구분을 초안이 흐렸다는 지적이고, 그것이 맞다** | 거짓신호 |
| A8 | `:161` 의 「그 표를 승계한다」가 §1.1 에 대해서는 승계 자체가 없다 | `docs/plan/disposal-map.md` 에 능력 대응 표가 둘(`:39-53` · `:67-80`)이라는 것을 열어 확인했다. `:161` 의 링크는 문서 전체를 가리키고 「그 표」의 지시 대상이 문서에 없다. A5 와 같은 결함의 다른 얼굴이라 문면 자리로 받는다 | 미관 |

---

## ③ 기각한 것

| 기각한 것 | 왜 기각했나 (근거) |
|---|---|
| A2 의 결론 *"§1 이 부정한다 → 갈래 1 은 읽기 B 다"* | 두 대목이 과하다. (ㄱ) `docs/plan/02-order.md:35` 의 *"파일 경계 넘기는 **별개로** 미구축"* 은 1 번을 고쳤을 때 얻는 것을 **한정**하는 문장이지 *"134 파일이 답하기 시작한다"* 를 **부정**하지 않는다 — 1 번이 서면 Rust 파일이 파일 **안** 관계로는 답하기 시작한다. (ㄴ) 순위 규칙이 §2 의 것이라 해도 그 규칙이 먹이는 **사실**은 §1 `C2` 행이 혼자 준다. 그 행을 지우면 `:83-85` 가 통째로 사라진다(초안 NC-3, 내가 `:83-85` 원문으로 재확인). 따라서 **읽기 B 로 내려가지 않는다** — 읽기 E 로 옮긴다 |
| A3 의 결론 *"NC-2 다섯 쌍 중 §1 이 금하는 것은 0 이다"* | 좌표 독해 정정은 맞으나 결론이 넓다. `crates/pal-core/src/projection.rs:226` 이 참조를 `scopes.refs` 에서만 꺼내므로 스코프 사슬이 없으면 파일 **밖** 참조도 애초에 안 생긴다 — 1 → 2 의존은 **코드에 실재한다.** 다만 그 의존을 **적은 것**이 §1 이 아니라 §2 `:77` 이다. 그러므로 옳은 정정은 「§1 이 금하는 것은 0」이 아니라 **「§1 은 사실을 주고 순서 문장은 §2 가 썼다」**이고, 이것은 A2 와 같은 결론(읽기 E)에 흡수된다 |
| 반(反)이 설계문에 댄 좌표 넷 — `3-design.md:146`(승격 1) · `:192`(스물넷 기각) · `:196`(갈린 셋 기각) · `:206`(§1 실측 참·거짓 제외) | **넷 다 그 줄에 없다.** 내가 `dialectic/3-design.md` 를 열어 대조했다 — 승격 1 은 `:168`, 「스물넷이냐」 기각은 `:214`, 「갈린 셋」 기각은 `:218`, §1 실측 제외는 `:228` 이다. 넷이 **정확히 22 줄씩** 어긋나므로 반(反)이 자기 스크래치패드 입력본의 줄 번호를 `3-design.md` 의 것으로 적은 것이다. **인용한 문면 자체는 전부 원문과 같으므로 A7 의 실질은 안 무너진다.** 기각하는 것은 좌표이지 반론이 아니다 — 반(反)의 *"초안이 설계문에 댄 좌표는 전부 맞는다"* 는 자기 사본으로 대조한 것이라 **확인된 바 없다** |
| 반(反)이 스스로 물린 일곱 | 물린 사유가 근거를 대고 있고 내가 그중 둘을 표본으로 열어 확인했다 — 처분표 §1.1 의 13 은 `P0` 포함이고 `F06b` 하나가 삭제 목록과 어긋난다(설계문 `:214` 가 이미 기각한 자리), `00-goals.md:204` 의 `C6` 행은 단일 저장소 시간축을 먼저 적고 멀티레포를 조건절로 붙여 순서를 못 박지 않는다. 되살릴 이유가 없다 |

---

## 초안을 어떻게 고치나 — 좌표와 성격

| # | 좌표 | 무엇을 | **성격** |
|---|---|---|---|
| 1 | `docs/plan/02-order.md:15-16` | *"순서의 근거는 **전부** §1 의 행을 댄다"* 를 실제 구조로 다시 적는다 — **1 번의 사실은 §1 `C2` 행이 · 6 번이 4 번 뒤인 것은 `00-goals.md:203` 의 `C5` 정의가 · 나머지 의존과 순위 규칙은 §2 가 새로 썼다** | 문면 정정 |
| 2 | `docs/plan/02-order.md:83-85` | ★ 의 (나)가 `AGENTS.md` 지시를 §2 가 적용한 **순위 규칙**임을 명시하고, *"134 파일이 답하기 시작한다"* 를 `:35`·`:77` 과 일관되게 **파일 안 관계에 한해**로 한정한다 | 문면 정정 + 근거 보충 |
| 3 | `docs/plan/02-order.md:81` | *"가장 비싸다"* 를 지우거나 출처를 댄다(`grep -n "비싸"` 가 이 문서에서 이 줄 하나, `docs/plan/00-goals.md` 에서 0 건 — 내가 확인). 뒷 문장의 출처를 `00-goals.md:203` 으로 적는다 | 문면 정정 |
| 4 | `docs/plan/02-order.md:78`·`:79`·`:80`·`:81` | 3↔4 와 5↔6 을 **금하는 것이 없다**는 사실을 적는다. **순서 자체는 안 바꾼다** — 설계문 `:220` 이 「옳은 순서인가」를 이 판 밖으로 뺐다(승격 2 미발화) | 근거 보충(문면) |
| 5 | `docs/plan/02-order.md:144-149` · `scripts/frontier.sh:37-41`·`:55` | `#77` 은 §2 의 1 번이 아니다. 갈래는 셋 — ㉠ §4 가 다른 이슈를 댄다 ㉡ §2 의 1 번 문면을 `#77` 에 맞춘다 ㉢ §2 의 1 번을 지는 이슈를 새로 세운다 | **처분 변경** — 무엇이 다음에 착수되는지가 바뀐다 |
| 6 | `docs/plan/02-order.md:159`·`:161`·`:165-169`·`:178-180` | 분모를 스물넷으로 되돌린다. 최소 `F02`(§2 의 1 번 자리) · `F09`(§2 의 3 번 자리) · `F23`(`C6` 의 공유 → 3 번)에 처분을 적고, §1.1 의 열하나를 「게이트가 정본이라 순서표 밖」으로 명시하든 개별 처분을 적든 **문서가 말하게 한다** | **처분 변경** |
| 7 | `docs/plan/02-order.md:225` → `:214-219` | *"이 저장소가 같은 훅을 안 쓴다"* 를 고친다. `crates/pal-cli/src/hook/catalog.rs:15-24` 의 `EVENTS` 가 `"SubagentStop"` 과 **`"Stop"`** 둘이고, `Stop` 은 `GOAL.md:55-71` 표의 셋 중 하나 그 자체다(내가 양쪽을 열어 대조했다) | **처분 변경**(사유 통 이동) |
| 8 | `docs/plan/02-order.md:206-228` | §5.2 가 **어떻게 골랐는지의 규칙**을 적고, 안 본 절(`~/dev/projects/fabricate/GOAL.md:217-231` · `IMPLEMENTATION-ORCHESTRATION.md:620-638`)을 **미측정**으로 적는다 | 문면 보충 + 미측정 기재 |
| 9 | `docs/plan/02-order.md:87-94` | 이 역순 시험이 **항등식**이고 §1 을 0 회 댄다는 것을 그 자리에 적는다. 조건 `B5` 의 문면 자체는 이 판이 안 고친다 — 설계문 `:229` | 문면 정정 |
| 10 | `docs/plan/02-order.md:34` | `pal symbols --graph` 근거 칸에 **PATH** 를 적는다. 지금 문면으로는 `contains 8` 을 아무도 못 되짚는다(M1) | 문면 정정 |
| 11 | `docs/plan/02-order.md:77` | *"완성 장면의 「호출자 7 (exact 5 · candidate 2)」 줄"* — 그 줄은 `docs/plan/00-goals.md:153` 에 있고 `docs/plan/01-completion-scenes.md` 에는 없다. **내가 A2 의 좌표를 확인하다 찾았다**(`grep -n "호출자" docs/plan/01-completion-scenes.md` → 0 건). 출처를 바로잡는다 | 문면 정정 |

---

## 소유자에게

**올린다.** 살아남은 반론 중 가장 무거운 것이 **금지역**(A1)이고, 그 해소가 **문면 정정만으로 안 된다** — 세 갈래 중 어느 것을 고르든 `docs/plan/02-order.md` §4 가 기계에 먹이는 첫 항목이 바뀌거나 §2 의 1 번 문면이 바뀐다. 설계문 `dialectic/3-design.md:168` 의 승격 1 이 그대로 발화한다. 초안은 이 자리를 §2 하나에만 대조해 닫았다(A7).

**소유자가 정할 것 — 물음 하나에 칸 하나.**

| 물음 | 소유자가 정해야 하는 것 |
|---|---|
| **Q1 — §4 의 첫 항목** | `#77` 을 첫 항목 자리에 그대로 두고 §2 의 1 번 문면을 `#77` 에 맞출 것인가, §4 가 다른 이슈를 대게 할 것인가, §2 의 1 번을 지는 이슈를 새로 세울 것인가 |
| **Q2 — 조건 `B5` 의 문면** | 지금 `B5` 가 요구하는 음성 대조가 항등식이라 아무것도 안 재는데, 조건 문면을 고칠 것인가 그대로 둘 것인가(설계문 `:229` 가 이 판에서 못 고치게 하고 승격 3 으로 올리라고 등록했다) |

**소유자 물음이 아닌 것 — 메인이 실행한다.** 설계문 `:171` 의 승격 4 가 *"「안 하면 다음 회차에 짐이 된다」면 타협 없이 이번에 늘린다"* 로 미리 답했다. `F02`·`F09`·`F23` 은 후자다 — §5.1 을 자로 쓰는 다음 회차가 같은 자로 다른 답을 얻는다.

---

## 라운드 2 를 도나

**안 돈다 — 교착으로 적고 올린다.**

설계문 `:154` 의 「금지역 — 사실이 아닌 것을 사실로」 행이 그 처분을 미리 등록했다(*"교착. 소유자에게 올린다"*). 라운드를 더 도는 것이 여기서 무엇도 안 바꾸는 이유는 셋이다.

1. A1 의 **사실관계에 다툼이 안 남았다.** `#77` 본문과 `scripts/frontier.sh:40`·`:55` 를 반(反)이 아니라 **내가 독립으로 다시 열었고** 같은 것이 나왔다.
2. 남은 갈래 셋 중 어느 것을 고르는가는 **사실 판정이 아니라 결정**이다. 반↔합 라운드는 결정을 못 닫는다.
3. 나머지는 거짓신호·미관이고 고칠 좌표가 위 표에 전부 특정됐다.

상한 2 는 설계문 `:144` 가 *"손실 제한으로 내가 2 를 건다"* 로 적은 **한도**이지 소진 의무가 아니다.

---

## 살아남은 반론의 해악도 분포

| 제목 | 등급 |
|---|---|
| 순서표의 1 번이 가리키는 `#77` 이 §2 의 1 번이 아니고 기계가 그 줄을 읽는다 | 금지역 |
| `:83-85` ★ 의 순위 규칙이 §1 에 없다 | 거짓신호 |
| NC-2 의 「§1 이 금한다」 칸이 `file_edges` 를 파일 간 엣지로 읽었다 | 거짓신호 |
| 6 번의 뒷 문장이 `00-goals.md:203` 의 `C5` 정의에서 나온다 | 거짓신호 |
| `D2` 의 분모가 스물넷이고 §1.1 의 열하나가 한 번도 안 대어졌다 | 거짓신호 |
| §5.2 에 모집단 덮음 시험이 안 걸렸고 선택 규칙이 문서에 없다 | 거짓신호 |
| §5 의 처분 변경을 「문면 정정」으로 다시 이름 붙여 승격 1 이 안 발화했다 | 거짓신호 |
| `:161` 의 「그 표를 승계한다」에 지시 대상이 없다 | 미관 |

---

## 내가 못 정한 것

| 무엇 | 무엇이 있어야 정할 수 있나 |
|---|---|
| §1.1 의 열하나가 §5.1 에 **개별 처분으로** 들어가야 하는가, **한 문장의 범위 선언**으로 닫히는가 | 정(正)이 A5 를 못 받아봤다. 다만 어느 갈래든 **문서가 말하기만 하면 닫히므로** 문서 변경 6 에 두 갈래를 다 적어 두었다 — 이 물음이 안 닫혀도 회차는 막히지 않는다 |
| `#77` 해소 갈래 셋 중 어느 것이 **가장 소비 가능에 가까운가** | `AGENTS.md` 의 판정자가 소유자 지시라서 소유자가 정한다(Q1) |
| §1 실측의 참·거짓 · 「갈렸다」고 적은 셋 중 어느 쪽이 옳은가 | 설계문 `:218`·`:228` 이 이 판 밖으로 뺐다. **미측정으로 남는다** |
| `intent.md` 의 조건 넷 밖(예: `B2-a`)이 §2 의 근거 칸에 무엇을 거는가 | `intent.md` 를 안 받았고 열지 않았다. 조건 넷 밖은 이 판의 물음이 아니다 |
| `fabricate` 의 `decisions`·`contract`·`audit`·`DITTO-PRE-MORTEM.md` | 설계문 `:219` 가 모집단을 두 파일로 잠갔다. 넓히려면 승격이다 |
