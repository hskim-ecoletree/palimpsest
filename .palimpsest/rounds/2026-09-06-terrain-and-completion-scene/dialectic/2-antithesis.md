> `pal-decision-opponent` 가 낸 것을 그대로 보존한다. 요약하지 않았다.
> 받은 것: 정(正)의 산출물 전문 · 설계문 발췌(답의 공간 · 사전 등록 측정 · 음성 대조) · 저장소.
> 안 받은 것: 대화 기록 · 메인의 사고 과정 · 판 1 의 산출물.
> 반이 스스로 신고한 것: 잠긴 의도를 전문이 아니라 세 구간만 열었다.
> 실행 2026-09-07 · 반이 잰 HEAD `d39c4d4`

---

# 반(反) — 판 2 · `A2`·`A3` 판정 초안을 무너뜨린다

## 내가 누구이고 무엇을 받았나

`docs/plan/01-completion-scenes.md` 에 대한 판 2 초안을 무너뜨리는 자리다. **읽기 전용으로 돌았고 저장소를 한 글자도 안 고쳤다.**

| 무엇 | 받았나 |
|---|---|
| 무너뜨릴 초안 | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/dialectic/2-thesis.md` — 받았다 |
| 설계문 발췌(R1~R5 · M1~M8 · ⓐⓑⓒ) | `/private/tmp/claude-501/-Users-incognito-dev-projects-palimpsest/31b2290a-690b-4069-9e46-d452f84219ad/scratchpad/2-antithesis-in.md` — 받았다 |
| 대화 기록 · 메인의 사고 과정 · 메인의 입장 | **안 받았다.** 프롬프트에 섞여 오지도 않았다 |
| 판 1 의 산출물 셋(`1-design.md`·`1-thesis.md`·`1-antithesis.md`) · `dialectic/2-design.md` · `dialectic/3-design.md` | **안 열었다** |

### 내가 연 파일

| 항목 | 경로 |
|---|---|
| 판정 대상 | `/Users/incognito/dev/projects/palimpsest/docs/plan/01-completion-scenes.md` (전문) |
| 좌표 | `docs/plan/00-goals.md` · `docs/plan/02-order.md` · `docs/plan/disposal-map.md` · `AGENTS.md` · `xtask/src/main.rs` · `crates/pal-core/src/doctor.rs` · `crates/pal-core/src/schema.rs` · `docs/graph-schema.md` |
| ★ **금지 목록에 있던 것 중 연 것** | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/intent.md` — **전문이 아니라 세 구간만**: `:118-132`(조건 `A1`~`A5`) · `:188-200`(`## 차선책`) · `:199-220`(`## 범위 밖`). 프롬프트가 *"더 필요하면 열되 적어라"* 로 허락했다. **연 이유는 초안이 `A3` 의 문면을 한 번도 인용하지 않았기 때문이다** — 아래 반론 ①이 그 자리다 |
| 추가로 연 것 | `docs/plan/README.md`(초안이 안 열었다) · `./target/debug/pal`(실행) |

### 내가 잰 환경

```
$ git rev-parse --short HEAD
d39c4d4                      ← 초안이 잰 6af5218 이후 회차 기록 넷만 늘었다
$ git diff --stat 6af5218 HEAD
 dialectic/{1-antithesis,2-design,2-thesis,3-design}.md | 934 +++++   ← docs/plan 무변경
$ cargo xtask check
검사 27/27 통과              ← 죽은 링크 0 · 완성 장면 형식 장면 5개
```

M1·M1-a 는 초안과 같다(빨강이 닫혀 있다). M2·M3·M4·M5·M7·M8 도 내가 다시 돌려 같은 출력을 얻었다. `pal narrative --help` 에 위치 인자도 `--ask` 도 없고, `grep -rn "이 답에 안 들어간 것" crates/` · `grep -rn '\-\-area' crates/pal-cli/src/` · `grep -rn "이 빌드에 없는 능력" crates/` 셋 다 `0` 이다. **초안이 인용한 명령과 출력에 어긋난 것은 없다.**

---

## 초안이 서는 자리

무너뜨리지 못한 것을 먼저 적는다.

- **`A2` = `R1` 은 선다.** 내가 문서를 실제로 깎아 재 봤고(아래 ⓐ), 양방향으로 반응했다. `A2` 판정 자체를 뒤집는 좌표를 못 찾았다.
- **차선책 부속 요구 ⓐ(*"지금 그래프로는 못 그려진다"*)가 참이라는 실측은 옳다.** 내가 같은 명령을 다시 돌려 확인했다.
- **§3 화면이 실제 저장소 개체로 채워져 있다는 근거 ③도 옳다.** `docs/adr/0025-*`·`docs/adr/0034-*` 실재, `NEXT-B-plan-for-harness.md` 부재를 확인했다.
- **`R5` 기각도 옳다.** `xtask/src/main.rs:4479`(`장면_소제목`)·`:4482`(`장면_하한: usize = 3`)뿐이고 안쪽·바깥쪽 라벨도 `P8` 도 모집단에 없다.
- **초안이 자기 「확인 못 한 것」에 `A3` 문면 해석의 갈림을 자진 신고한 것**은 정직하다. 다만 신고한 뒤에 그 갈림을 근거 없이 한쪽으로 닫았고, 아래 ①②③이 그 자리다.

---

## 반론

### ① 초안이 `A3` 의 문면을 한 번도 인용하지 않고 판정했다 — 인용하면 §5 가 문면의 낱말을 그대로 낸다

초안 전문을 훑었는데 `A3` 의 조건 문면이 **한 번도 인용되지 않는다.** 인용된 것은 `## 차선책` 의 `A3` 항뿐이다. 내가 원문을 열었다:

> `intent.md:126` — *"**`P8`(생성형 큐레이션은 1급 능력이다)가 장면 하나를 진다** — 모델이 그래프를 읽고 서사를 **산출하는 장면**"*

그리고 판정 대상의 절 제목이 이것이다:

> `01-completion-scenes.md:167` — *"## 5. **모델이 그래프를 읽고 서사를 만드는 순간**"*

**낱말이 거의 그대로 옮겨져 있다.** 설계문 표(`2-antithesis-in.md:20-21`)가 `R2`·`R3` 를 **둘 다 「문면보다 적다」를 전제로** 정의한다 — `R2` = *"문서가 낸 것이 문면보다 적지만"*, `R3` = *"문면보다 적은데"*. 초안은 §5 가 **안 돈다**는 것을 실측했을 뿐, §5 가 `A3` 문면**보다 적다**는 것을 한 번도 보이지 않았다. 「돈다」는 `A3` 문면에 없는 낱말이다.

그리고 초안 자신이 `A2` 에서 같은 논법을 이미 썼다 — `2-thesis.md:93`: *"`A2` 의 동사는 **「그린다」**이지 「돈다」가 아니다. … §3 `:128` 과 §4 `:159-161` 의 표기는 `A2` 가 요구한 양을 줄이는 것이 아니라 그 위에 얹은 것이다."* `A3` 의 명사는 **「장면」**이다. 같은 규칙을 일관되게 적용하면 §5 의 `:207`(*"이 장면도 목표 상태다"*) 역시 요구한 양을 줄이는 것이 아니라 그 위에 얹은 것이고, `A3` 는 `R1` 이다. **초안은 자기가 세운 규칙을 `A2` 에는 적용하고 `A3` 에는 적용하지 않았다.**

- 좌표: `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/intent.md:126`
- 모집단: 원의도
- 유효성: 참
- 해악도: 실패

### ② 잠긴 의도 자신이 `A3` 를 「장면 한 개」로 셈해 두었다 — 실행 가능성이 아니라 장면의 존재로

`A1-a`(장면 셋 이상 · 결정론적)의 근거절을 열면 이렇게 적혀 있다:

> `intent.md:124` — *"셋은 **`A2` 의 안쪽·바깥쪽과 `A3` 의 `P8` 이 요구하는 최소**다."*

즉 잠긴 의도는 `A2` 를 **장면 둘**(안쪽 하나 · 바깥쪽 하나), `A3` 를 **장면 하나**로 환산해 하한 셋을 산출했다. 그 산술 어디에도 *"그 장면이 돌아야 한다"* 가 안 들어간다 — 들어갔다면 하한이 기계로 셀 수 있는 수가 못 됐을 것이다.

이 문장은 `A2` 와 `A3` 를 **같은 통화로** 센다. 초안은 그 둘에 다른 통화를 매겼다 — `A2` 는 「장면이 있나」, `A3` 는 「장면이 도나」. 의도 안쪽에 두 통화를 가르는 문장이 없다.

- 좌표: `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/intent.md:124`
- 모집단: 원의도
- 유효성: 참
- 해악도: 실패

### ③ `A3`=`R1` 을 막은 「차선책이 죽은 가지가 된다」 논법이 차선책의 마지막 문장을 안 읽었다

초안이 `A3`=`R1` 을 기각한 근거는 이것 하나다:

> `2-thesis.md:156` — *"`A3` 를 「그런 제목의 절을 쓴다」로 읽으면 `## 차선책` 의 `A3` 항이 발동할 경우가 원리상 없어진다 — **절은 언제나 쓸 수 있기 때문이다.**"*

차선책 원문의 **마지막 문장**이 그 논법을 무너뜨린다:

> `intent.md:194` — *"`A3`(P8 장면)이 지금 그래프로는 못 그려지면 — 장면을 목표 상태로 적고 무엇이 갖춰지면 서는지를 함께 적는다. **지우지는 않는다.**"*

차선책이 막는 살아 있는 가지는 **「절을 못 쓴다」가 아니라 「그래프로 못 그려지니 아예 안 싣는다」**다. 그 가지는 가상이 아니라 실재했다 — §5 의 화면은 지금 어느 명령도 못 내고(`pal narrative --help` 에 위치 인자도 `--ask` 도 없다), *"돌지도 않는 것을 완성 장면 문서에 싣지 말자"* 는 자연스러운 선택지다. 차선책은 그 삭제를 금하고 그 위에 표기 요구 둘을 더 얹는다. **읽기 (가)에서도 차선책은 죽은 가지가 아니다.** 초안이 읽기 (나)를 고른 유일한 근거가 사라진다.

초안 자신이 이것을 「내가 확인 못 한 것」 첫 줄에 신고했고(`2-thesis.md:194`), 신고한 뒤 근거 없이 (나)로 닫았다.

- 좌표: `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/intent.md:194`
- 모집단: 원의도
- 유효성: 참
- 해악도: 실패

### ④ *"그 층은 능력 계약 안에 없다"* 는 `P8` 의 단서절에 대해 거짓이다 — 사실/추론을 가르는 장치는 코드에 서 있다

초안 `R3` 의 하중을 지는 문장은 이것이다:

> `2-thesis.md:118` — *"그래프에서 뽑은 사실 위에 **모델이 서사를 엮고 출처·공백·확신도로 사실과 가르는 층**은 계약 안에 없다."*

둘로 갈라야 한다. **가르는 장치**는 이미 코드에 있다 — `crates/pal-core/src/doctor.rs:66` 이 불변식 ④ `InferredCarriesEvidence`(*"`inferred` 노드의 `evidence_refs` 가 비어 있지 않다"*)를 세우고 `:751` 이 `if n.evidence_refs.is_empty()` 로 강제한다. `crates/pal-core/src/schema.rs:512-517` 이 *"`inferred` 로 설 수 있는데 근거를 요구하지 않으면 `P3` 이 문장으로 되돌아간다"* 로 스키마 자신을 잰다. `docs/graph-schema.md:21` 에 `UnresolvedRef` 가 있다. §5 화면이 찍는 `asserted` · `⚠ 추론이다` · `UnresolvedRef` 는 **전부 실재하는 스키마 개념**이다. 그리고 처분표가 그 사실을 이미 적었다 — `disposal-map.md:263`: *"**D10** … 생성형 합성은 1급 능력이되 `evidence_refs` 없는 합성물은 저장되지 않는다 … 처분 **코드** — `crates/pal-core/src/doctor.rs:66`"*.

**엮는 층**은 능력 계약의 항목이 될 수 없다. `00-goals.md:194` 이 계약의 정의 규칙을 적는다 — *"각 항목에 **「없으면 에이전트가 무엇을 추측하게 되는가」**를 붙인다. 그것이 그 능력의 존재 이유다."* `C1~C6` 은 **에이전트가 소비하는 것**의 목록이고, 서사를 엮는 주체가 그 에이전트다. `P8` 을 `C1~C6` 의 행으로 요구하는 것은 소비자를 재료 목록에 넣으라는 것이다. 남는 것은 그래프 기질뿐이고, 그것을 `:207-209` 가 `C1`·`C2`·`C6`·`C5` 로 이름 대어 답했다 — 초안 스스로 *"여기까지는 답이 됐다"*(`2-thesis.md:116`)고 인정한 자리다.

- 좌표: `docs/plan/00-goals.md:194`
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패

### ⑤ ⓑ 가 낸 「한 문장」의 마지막 절이 사실이 아니다 — 그 자리는 초안이 인용한 바로 그 표의 머리에 등록돼 있다

초안이 ⓑ 를 갚으며 낸 문장의 끝이 이것이다:

> `2-thesis.md:171` — *"옛 `F17` 이 그 자리였고 2026-08-18 에 걷혔다. **그 자리는 지금 어디에도 없다.**"*

초안은 `disposal-map.md:75`(`F17` 행)을 인용했다. 그 행이 든 표의 **머리 열다섯 줄 위**가 이것이다:

```
$ sed -n '60p' docs/plan/disposal-map.md
### 1.2 아직 안 만들었다 — **B 가 새 지형에서 다시 정한다** (13)
$ sed -n '120p' docs/plan/disposal-map.md
| **`B`** | 35 | **아직 안 만든 기능의 결정**이다. 병렬 회차 B 가 새 지형에서 다시 정한다 |
```

그리고 `disposal-map.md:263-269` 가 `P8`·`P10` 계열 결정 여섯(`D11`·`D22`·`§9-1`~`§9-4`)의 처분을 **`B` — `F17`** 로 적는다. **그 자리는 「어디에도 없는」 것이 아니라 「B 가 정한다」로 등록돼 있다.**

⚠ **이 반론은 양날이다.** `disposal-map.md:8`(*"그 새 지형은 병렬 회차 B 가 쓴다"*)과 `README.md:3-4`(*"새 지형은 다음 회차가 쓴다"*)를 함께 읽으면 그 `B` 가 바로 이 지형 회차이므로, 등록된 것은 **답이 아니라 숙제**다. 그러니 이 반론은 `R3` 을 뒤집지는 않는다. 뒤집는 것은 **ⓑ 의 통과 근거**다 — 초안이 낸 한 문장이 문면대로 거짓이면 그 문장으로 갚은 ⓑ 도 갚인 것이 아니다.

- 좌표: `docs/plan/disposal-map.md:60`
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호

### ⑥ 근거 ④(`F17` 이 승계표에 없다)는 `P8` 에 관한 증거가 아니다 — 열셋 중 여덟이 없다. 초안이 같은 사실을 두 번 셌다

초안 `2-thesis.md:122` 는 `F17` 이 `02-order.md` 승계표에 없다는 것을 `P8` 축소의 **독립 근거 ④**로 쓴다. 내가 두 표를 전수로 대조했다.

```
$ awk 'NR>=68 && NR<=80 && /^\| \*\*F/' docs/plan/disposal-map.md
F07 F08 F13 F14 F15 F16 F16b F17 F18 F19 F20 F21 F23      ← §1.2 의 열셋
$ sed -n '163,169p' docs/plan/02-order.md
F07 F08 F14 F15 F13                                        ← §5.1 승계표
빠진 것: F16 · F16b · F17 · F18 · F19 · F20 · F21 · F23
```

승계표의 열 이름이 *"옛 기능 | 어느 능력 | §2 의 어느 항목"*(`02-order.md:163`)이다 — **`C1~C6` 에 매달리는 것만 싣는 선택적 표**다. `F17` 의 부재는 「`P8` 이 능력 계약의 항목이 아니다」의 **재진술**이지 새 사실이 아니다. 초안은 근거 ②와 근거 ④로 같은 것을 두 번 셌고, 여덟 중 하나를 골라 `P8` 에 특정한 증거처럼 세웠다.

- 좌표: `docs/plan/02-order.md:163`
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호

### ⑦ `docs/plan/README.md` 가 아직 「지형이 비었다 · 완성 장면이 비었다」로 답한다 — 초안이 이 파일을 안 열었다

초안의 「내가 연 파일」에 `docs/plan/README.md` 가 없다. 계획 폴더의 색인 문서이고, 열면 이렇다:

```
$ grep -n '비었다\|다음 회차가 쓴다' docs/plan/README.md
3:> ⏳ **지형이 비었다.** … **새 지형은 다음 회차가 쓴다**
13:| **완성되면 실제로 어떻게 쓰이나** | ⏳ **비었다** — 옛 완성 장면은 MCP 를 전제해 2026-08-18 에 지웠다
22:## 1. 지형 — ⏳ **비었다**
$ grep -c '01-completion-scenes\|02-order' docs/plan/README.md
0
```

이 회차가 만든 두 문서로 가는 링크가 **한 건도 없다.** 그런데 `AGENTS.md:39` 는 *"`docs/plan/README.md` 에는 **어느 회차에서든 걸리는 것**(상시 검사·판정 명령(oracle)·게이트 소유자·진행 규칙)만 남았다"* 로 적는다 — `README.md:22` 의 「지형 — 비었다」 절은 그 넷 중 어느 것도 아니다. **두 색인 문서가 같은 물음에 반대로 답하고 있고, `cargo xtask check` 27/27 이 이것을 안 잡는다.**

`01-completion-scenes.md` 의 `A2`·`A3` 판정 자체는 안 흔든다. 흔드는 것은 초안 `A2`=`R1` 의 근거가 기대는 「바깥쪽 장면이 이 문서 본문에 있다」의 **도달 가능성**이다 — 계획 폴더를 색인으로 들어온 독자는 그 문서가 없다고 안내받는다.

- 좌표: `docs/plan/README.md:13`
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호

---

## 음성 대조 — 내가 직접 돌렸다. 초안의 결과를 안 옮겼다

문서를 실제로 잘라 재는 스크립트를 스크래치패드에서 돌렸다(저장소 파일은 안 고쳤다).

```
절 경계: 32:§1 바깥쪽 · 56:§2 바깥쪽 · 94:§3 안쪽 · 133:§4 안쪽 · 167:§5

원본           장면5  안쪽2 바깥쪽2  A3문면낱말=True   A1-a하한3=통과
§5 제거        장면4  안쪽2 바깥쪽2  A3문면낱말=False  A1-a하한3=통과
§3§4§5 제거    장면2  안쪽0 바깥쪽2  A3문면낱말=False  A1-a하한3=실패
§1§2 제거      장면3  안쪽2 바깥쪽0  A3문면낱말=True   A1-a하한3=통과
```

**ⓐ — 문서를 깎아도 근거가 사나.**
`A2`=`R1` 에 대해: §3§4§5 를 지우면 안쪽이 `0`, §1§2 를 지우면 바깥쪽이 `0`. **양방향으로 깨진다** — 초안의 ⓐ 결론과 같다. 항등식이 아니다.
★ **내가 세우는 반대 위치(`A3`=`R1`)에도 ⓐ 를 걸었다.** §5 를 지우면 `A3` 문면의 낱말이 사라지고(`A3문면낱말=False`), **그런데 `A1-a` 하한 셋은 여전히 통과한다**(장면 4). 즉 `A3`=`R1` 의 근거는 문서 상태에만 반응하고 M1 을 되풀이하지 않는다. **내 반대 위치도 ⓐ 를 통과한다.**

**ⓑ — 축소라면 무엇이 한 문장 더 있었으면 지킨 것인가.**
나는 `A3` 를 축소로 안 보므로 ⓑ 의 의무가 나에게 안 걸린다. 대신 **초안의 ⓑ 를 검사했다** — 초안이 댄 문장의 마지막 절 *"그 자리는 지금 어디에도 없다"* 가 `disposal-map.md:60`·`:120`·`:263-269` 에 반증된다(반론 ⑤). 초안의 ⓑ 는 **거짓 문장으로 갚였다.**

**ⓒ — 차선책의 대칭성. 초안과 반대 방향으로 돌렸다.**
초안은 *"`A3` 의 `R2` 논법이 `A2` 로 번지나"* 를 물었다. 나는 반대를 물었다 — ***"`A2` 의 `R1` 논법이 `A3` 로 번지나."*** 초안 `A2`=`R1` 의 논법은 셋이다: (i) 문면의 낱말이 문서 절에 그대로 있다 (ii) 동사가 「그린다」이지 「돈다」가 아니다 (iii) 문서 상태에 반응한다. **셋 다 `A3` 로 그대로 옮겨진다** — (i) `intent.md:126` ↔ `01-completion-scenes.md:167` 이 거의 축자 일치, (ii) `A3` 의 명사가 「장면」, (iii) 위 표의 「§5 제거」 행. 초안이 이 전이를 막은 것은 차선책의 존재 하나이고, 반론 ③이 그 막이를 무너뜨렸다.

★ **그러므로 초안이 자기 입으로 적은 자리가 실제로 열렸다** — `2-thesis.md:190`: *"반(反)이 그 기준을 무너뜨리면 `A2`·`A3` 가 같은 이름으로 붙고 이 초안의 갈래가 통째로 무너진다."* 내가 낸 것은 **둘 다 `R1`** 이다.

---

## 내가 스스로 물린 것

| 기각한 것 | 왜 버렸나 |
|---|---|
| **초안이 잰 `HEAD`(`6af5218`)가 지금(`d39c4d4`)과 다르므로 재측정이 무효다** | `git diff --stat 6af5218 HEAD` 가 회차 기록 넷만 낸다. `docs/plan/` 무변경이라 판정 대상에 영향이 없다 |
| **`M1` 의 문서 수가 다르다(초안 462 · 내가 465)** | 그 사이에 회차 기록 셋이 커밋됐다. 링크 616건·죽은 것 0건은 같다. 어긋남이 아니다 |
| **§2 가 표기 없는 미래 화면인데 `A2` 의 바깥쪽 절반을 혼자 진다** | 초안 「발견 ②」가 이미 냈다. **새롭지 않다.** 내가 더한 것은 `00-goals.md:148`(*"이것은 전부 다 만들었을 때의 화면이다"*)이 §1 의 화면을 다른 문서에서만 미래로 표기한다는 비대칭인데, 그 좌표도 초안이 「확인 못 한 것」에 이미 적었다 |
| **`xtask/src/main.rs` 에 상수가 셋이지 둘이 아니다(`완성_장면_문서` `:4476` 이 빠졌다)** | 초안의 `R5` 기각 결론이 안 바뀐다. 그 상수도 안쪽·바깥쪽·`P8` 을 안 잰다. **미관에도 못 미친다** |
| **`A2` 의 바깥쪽이 §1(위임)이지 §2 가 아니라는 읽기** | `intent.md:125` 원문을 열어 *"남의 프로젝트에 설치한 사용자가 보는 것"* 을 확인했고, `01-completion-scenes.md:60` 이 그 사람을 지목한다. 초안의 읽기가 맞다. **좌표로 반증되어 버렸다** |
| **`P8` 의 생성 층이 `intent.md` 의 `## 범위 밖` 에 이미 있다** | `:199-216` 을 전수로 읽었다. **없다.** 초안이 *"훑었고 못 찾았다"* 고 한 것이 맞다. 반론으로 못 낸다 |
| **`02-order.md` 항목 4(`C3`)의 사유가 서사를 지는 항목이라는 읽기** | `:79` 의 「무엇을 만드나」 칸이 *"진입점 인식기"* 다. 서사는 그 항목을 정당화하는 인용이지 산출물이 아니다. 초안의 읽기가 맞다 |
| **`cargo test --workspace` 를 안 돌렸다** | 초안도 안 돌렸다. 안 잰 것을 잰 것처럼 반론에 못 쓴다. **이 자리는 정·반 둘 다 미측정이다** |
