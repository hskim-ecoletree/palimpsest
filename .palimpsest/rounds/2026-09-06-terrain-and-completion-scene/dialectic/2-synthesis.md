> `pal-decision-synthesizer` 가 낸 것을 그대로 보존한다. 요약하지 않았다.
> 받은 것: 정의 초안 · 반의 반론 · 판 2 설계문 · 잠긴 의도 발췌 셋.
> 안 받은 것: 대화 기록 · 메인의 사고 과정 · 판 1 의 산출물 · 저장소 재측정 권한.
> 합이 스스로 신고한 것: `git log --oneline -3` 의 커밋 제목이 판 1 합의 결론 일부를 노출했다.
> 실행 2026-09-07 · 합이 잰 HEAD `c8bc5e9`

---

# 판 2 — 합(合)

> `pal-decision-synthesizer` 가 냈다. 읽기 전용으로 돌았고 저장소를 한 글자도 안 고쳤다.
>
> **내가 누구인가** — 판 2 의 합(合)이다. 초안을 쓴 자가 아니다.
>
> **받은 것** — 정(正)의 초안 전문 · 반(反)의 반론 전문 · 판 2 설계문 전문 · 잠긴 의도 발췌 셋(`## 차선책` · `## 범위 밖` · `## 승격`).
>
> **안 받은 것** — 대화 기록 · 메인의 사고 과정 · 판 1 의 산출물.
>
> **안 연 것** — `dialectic/1-design.md` · `dialectic/1-thesis.md` · `dialectic/1-antithesis.md` · `dialectic/1-synthesis.md` · `dialectic/3-design.md` · `observations/negative-control-scenes.txt`.

## 내가 연 파일 — 인용된 좌표를 직접 확인하려고 열었다

| 항목 | 경로 | 왜 열었나 |
|---|---|---|
| 판정 재료 | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/dialectic/2-design.md` · `2-thesis.md` · `2-antithesis.md` | 받은 것 |
| 잠긴 의도 | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/intent.md` — `:115-135` · `:186-200` · `README` 검색 · `### C —` 절 | 반론 ①②③ 이 댄 `:124`·`:125`·`:126`·`:194` 가 실제로 그런지 |
| 판정 대상 | `docs/plan/01-completion-scenes.md` — `:160-215` | §5 의 문면과 `:207-209` 의 위임 문장 |
| 좌표 | `docs/plan/00-goals.md` — `:190-206` · `:236-242` | 반론 ④ 의 `:194`(계약 정의 규칙)와 `P8` 원문 |
| 좌표 | `docs/plan/02-order.md` — `:70-90` · `:160-182` | 순서 항목 여섯 · 승계표 · 「이 축의 한계다」 문장 |
| 좌표 | `docs/plan/disposal-map.md` — `:58-80` · `:118-122` · `:260-272` | 반론 ⑤⑥ 의 `F17` 행 · `B` 버킷 · `D10`~`§9-4` |
| 좌표 | `docs/plan/README.md` — `:1-30` · `grep -c '01-completion-scenes'` | 반론 ⑦ |
| 좌표 | `AGENTS.md` — `:39`·`:40` | 반론 ⑦ 이 댄 대조 |
| 코드 | `crates/pal-core/src/doctor.rs` — `:60-70` · `:745-755` | 반론 ④ 의 불변식 ④ |
| 실행 | `cargo xtask check` · `git rev-parse HEAD` · `git diff --stat 6af5218 HEAD -- docs/` | 해악 게이트의 「실패」 행이 지금 발화하나 |

### 자진 신고 — 오염 하나

`git log --oneline -3` 을 돌렸다. HEAD 를 확인하려는 것이었는데 **커밋 제목 줄이 판 1 합(合)의 결론 일부를 노출했다.** 파일은 안 열었고 아래 판정은 그 제목에 기대지 않는다 — `A3` 의 이름은 `intent.md:124`·`:126` 과 `intent.md:194` 의 문면, 그리고 내가 직접 연 `01-completion-scenes.md:167` 로만 세웠다. 판 1 이 어느 이름을 세웠는지 나는 모른다.

### 내가 잰 것 — 해악 게이트의 「실패」 행

```
$ git rev-parse --short HEAD
c8bc5e9
$ git diff --stat 6af5218 HEAD -- docs/
(출력 없음)                      ← 판정 대상 문서는 정·반 사이에 안 변했다
$ cargo xtask check
  ok    죽은 링크 부재  — 문서 469개 · 링크 616건 · 죽은 것 0건
  ok    완성 장면 형식  — 장면 5개 · 소제목 셋 전부 갖춤
검사 27/27 통과
```

설계문 `M1-a` 의 빨강은 닫혀 있다. **설계문 §「끝을 정하는 것」의 「실패」 행은 지금 발화하지 않는다.**

---

## 판정

**수정**

`A2` 는 초안대로 선다. **`A3` 는 초안의 이름을 뒤집는다.**

### ① 조건마다 판정 이름 하나씩

| 조건 | 이름 | 근거 |
|---|---|---|
| **`A2`** | **`R1` — 문면대로 냈다** | `intent.md:125` 가 요구한 두 쪽이 위임과 무관한 본문 절로 각각 선다. 정과 반이 **각자 문서를 실제로 잘라 재서 양방향 반응을 얻었고**, 내가 `01-completion-scenes.md:60`·`:98`·`:137` 을 직접 열어 「누가」가 조건의 낱말을 짚는 것을 확인했다. 항등식이 아니다 |
| **`A3`** | **`R1` — 문면대로 냈다** | 아래 |

**여섯째 이름을 세우지 않았다.** `R1` 의 정의(*"조건이 적은 것이 문서에 그대로 있고, 그 문서 상태를 바꾸면 조건이 깨진다"*)가 `A3` 를 그대로 담는다.

**`A3` 를 `R1` 로 뒤집은 근거 셋 — 전부 내가 원문을 열어 확인했다.**

`intent.md:126` — *"**`P8`(생성형 큐레이션은 1급 능력이다)가 장면 하나를 진다** — 모델이 그래프를 읽고 서사를 산출하는 장면"*
`docs/plan/01-completion-scenes.md:167` — *"## 5. 모델이 그래프를 읽고 서사를 만드는 순간"*

- **(가) 문면의 명사는 「장면」이고 「돈다」가 아니다.** 설계문 표가 `R2`·`R3` 를 **둘 다 「문면보다 적다」를 전제로** 정의한다. 초안은 §5 가 **안 돈다**는 것만 실측했고, §5 가 `A3` 문면**보다 적다**는 것을 한 번도 보이지 않았다. 초안 스스로 `A2` 에 대해 *"`A2` 의 동사는 「그린다」이지 「돈다」가 아니다"*(`2-thesis.md:93`)를 썼다 — 같은 규칙을 `A3` 에는 안 썼다.
- **(나) 잠긴 의도 자신이 `A3` 를 「장면 하나」로 셈했다.** `intent.md:124` — *"셋은 **`A2` 의 안쪽·바깥쪽과 `A3` 의 `P8` 이 요구하는 최소**다."* 잠긴 의도가 `A2` 와 `A3` 를 **같은 통화**로 센다. 초안은 그 둘에 다른 통화를 매겼고 의도 안쪽에 통화를 가르는 문장이 없다. ⚠ 이것은 `M1`(기계가 센 장면 수)을 다시 세는 것이 **아니다** — 나는 그 산술을 `A3` 가 무엇을 뜻하는지의 증거로 쓰지, `A3` 가 통과했다는 증거로 안 쓴다.
- **(다) 초안이 `A3`=`R1` 을 막은 유일한 논법이 무너진다.** 초안의 논법은 *"절은 언제나 쓸 수 있으니 그 읽기면 차선책이 죽은 가지가 된다"*(`2-thesis.md:156`)였다. 차선책의 마지막 문장이 그것을 무너뜨린다 — `intent.md:194` *"장면을 목표 상태로 적고 무엇이 갖춰지면 서는지를 함께 적는다. **지우지는 않는다.**"* 차선책이 막는 살아 있는 가지는 「절을 못 쓴다」가 아니라 **「그래프로 못 그려지니 아예 안 싣는다」**다. 그 가지는 실재했다. 낮은 문턱 읽기에서도 차선책은 죽은 가지가 아니다.

초안이 자기 「내가 확인 못 한 것」 첫 줄에 이 갈림을 자진 신고했고(`2-thesis.md:194`), **신고한 뒤 근거 하나로 닫았다. 그 근거가 좌표로 반증된다.**

### ★ `A3`=`R1` 이 삼키지 않는 것 — 함께 적는다

`A3` 의 **조건 문면**은 충족됐다. 그러나 **차선책의 조건절은 사실로 발동돼 있고**(정·반·내가 각각 확인 — `pal narrative --help` 에 위치 인자도 `--ask` 도 없다), 그 부속 요구 ⓑ 가 **생성 표면에 대해 안 갚였다.**

`docs/plan/01-completion-scenes.md:207-209` 가 *"무엇이 갖춰지면 도는지는 순서표가 답한다"* 로 넘기는데 — 그래프 기질(`C1`·`C2`·`C6`·`C5`)에 대해서는 참이고, §5 의 화면이 부르는 **`pal narrative <좌표> --ask` 자체**에 대해서는 `docs/plan/02-order.md` §2 의 항목 여섯 어디에도 자리가 없고 `docs/plan/02-order.md:180` 이 *"**그것이 이 축의 한계다** — 능력 계약에 없는 축은 이 표가 못 줄 세운다"* 로 자기 한계를 적는다. 내가 두 좌표를 다 열어 확인했다.

**이것은 `A3` 의 축소가 아니다**(문면이 요구한 장면은 있다). **차선책 이행의 미기재다.** 판정 이름 안에 숨기지 않고 여기 따로 적는다.

모집단 — `docs/plan/01-completion-scenes.md` §5 의 위임 문장과 `docs/plan/02-order.md` §2·§5.
유효성 — 확인했다. 두 파일을 직접 열었다.
해악도 — **거짓신호.** 독자가 「순서표가 다 답한다」로 믿는다. 안 깨진다.

---

## 채택한 반론

| # | 반론 | 왜 채택했나 | 해악도 |
|---|---|---|---|
| ① | 초안이 `A3` 문면을 한 번도 인용 안 했다 · 인용하면 §5 가 문면 낱말을 그대로 낸다 | `intent.md:126` 과 `docs/plan/01-completion-scenes.md:167` 을 내가 직접 열어 대조했다. 거의 축자 일치다. 그리고 `R2`·`R3` 가 둘 다 「문면보다 적다」를 전제하는데 초안이 그것을 안 세웠다 — 초안 자신이 `A2` 에서 쓴 규칙(`2-thesis.md:93`)을 `A3` 에 안 썼다 | **거짓신호** ⟨반의 자기 등급 「실패」에서 내림 — 근거는 아래 절⟩ |
| ② | 잠긴 의도가 `A3` 를 「장면 하나」로 셈해 두었다 | `intent.md:124` 를 열어 확인했다. `A2` 와 `A3` 를 같은 통화로 세는 산술이고, 초안이 이 좌표를 한 번도 안 다뤘다 | **거짓신호** ⟨같은 이유로 내림⟩ |
| ③ | 「차선책이 죽은 가지가 된다」 논법이 차선책의 마지막 문장을 안 읽었다 | `intent.md:194` 를 열었다. *"지우지는 않는다"* 가 실재하고, 그것이 낮은 문턱 읽기에서도 차선책에 살아 있는 일을 준다. 초안이 (나) 읽기를 고른 **유일한** 근거가 사라진다 | **거짓신호** ⟨같은 이유로 내림⟩ |
| ④ | **절반만** — *"사실과 가르는 층이 계약 안에 없다"* 가 코드에 대해 거짓이다 | `crates/pal-core/src/doctor.rs:66`(`InferredCarriesEvidence`)과 `:751`(`if n.evidence_refs.is_empty()`)을 열어 확인했다. `docs/plan/disposal-map.md:263` 이 같은 결정(`D10`)의 처분을 **코드**로 적고 그 좌표를 댄다. 초안 `2-thesis.md:118` 의 문장은 **가르는 장치에 대해서는 거짓**이다 | **거짓신호** |
| ⑤ | 초안이 ⓑ 로 낸 한 문장의 마지막 절 *"그 자리는 지금 어디에도 없다"* 가 사실이 아니다 | `docs/plan/disposal-map.md:120`(`B` 버킷)과 `:263-269`(`D11`·`D22`·`§9-1`~`§9-4` 가 **`B` — `F17`**)를 열었다. 그 자리는 「어디에도 없는」 것이 아니라 「`B` 가 정한다」로 등록돼 있다. 반이 스스로 *"등록된 것은 답이 아니라 숙제"* 라고 물러선 것까지 옳다 | **거짓신호** |
| ⑥ | `F17` 이 승계표에 없다는 근거 ④ 는 `P8` 특정 증거가 아니다 | `docs/plan/disposal-map.md:66-79`(열셋)와 `docs/plan/02-order.md:163-169`(다섯)를 대조했다. 승계표의 열 이름이 *"어느 능력"* 이라 `C1~C6` 에 매달리는 것만 싣는 **선택적 표**다. `F17` 의 부재는 「`P8` 이 능력 계약 항목이 아니다」의 재진술이지 새 사실이 아니다 | **거짓신호** |
| ⑦ | `docs/plan/README.md` 가 아직 *"지형이 비었다 · 완성 장면이 비었다"* 로 답한다 | 열어서 확인했다 — `:3`·`:13`·`:22` 가 그대로이고 `grep -c '01-completion-scenes' docs/plan/README.md` 가 `0` 이다. `AGENTS.md:39`·`:40` 은 반대로 답한다. 두 색인 문서가 같은 물음에 반대로 답하고 검사가 안 잡는다 | **거짓신호** |

### 해악도를 다시 매긴 근거 — 반의 자기 등급을 그대로 안 썼다

반이 ①②③④ 에 **「실패」**를 매겼다. 설계문 §「끝을 정하는 것」이 「실패」를 **한 뜻으로 못 박아 등록해 두었다** — *"검사·CI 가 빨개진다"*. 나는 `cargo xtask check` 를 다시 돌려 `27/27 통과`를 얻었고 `docs/` 는 `6af5218` 이후 무변경이다. **등록된 뜻으로 「실패」가 아니다.**

반이 쓴 것은 *"초안의 논증이 실패했다"* 는 뜻이고, 그것은 등급이 아니라 채택 사유다. **①②③ 은 실제로 판정을 뒤집었지만**, 뒤집힌 뒤 남는 해악은 「초안대로 갔다면 소유자가 있지도 않은 미승인 축소를 믿었을 것」 — 설계문이 거짓신호에 붙인 정의(*"안 깨지지만 사람이 잘못 믿게 되는 것"*) 그대로다. 등급을 낮춘 것이 반론의 무게를 낮추는 것이 아니다 — **판정은 반의 손을 들었다.**

`금지역 ①`(사실이 아닌 것을 사실로)도 안 매겼다. 그 행의 등록된 예 둘이 *"목표 상태를 지금 서는 능력인 것처럼"* 과 *"안 잰 것을 통과로"* 이고 **둘 다 과대 주장**인데, ①②③ 과 ⑦ 이 지적하는 것은 반대 방향(있는 것을 없다고 적음 · 안 줄어든 것을 줄었다고 적음)이다. `docs/plan/01-completion-scenes.md:169-171` 과 `:207` 이 §5 를 스스로 목표 상태로 못 박고 있어 과대 주장의 자리가 없다.

## 기각한 반론

| 항목 | 반론 | 왜 기각했나 (근거) |
|---|---|---|
| ④ 의 나머지 절반 | *"엮는 층은 능력 계약의 항목이 될 수 없다 — `C1~C6` 은 에이전트가 소비하는 것이고 엮는 주체가 그 에이전트다"* | `docs/plan/00-goals.md:194` 의 정의 규칙은 열어서 확인했고 인용도 정확하다. 그러나 §5 의 화면(`docs/plan/01-completion-scenes.md:187`)이 그리는 것은 **에이전트가 아니라 `pal narrative` 라는 제품 명령이 「■ 답」을 산출하는 장면**이다. 소비자와 산출 표면이 같지 않다. 반은 그 자리를 안 다뤘고, 다루지 않은 채로는 「계약 항목이 될 수 없다」가 안 선다. **그러므로 ⓑ 의 미기재는 이 반론으로 해소되지 않는다** — 위 ★ 절에 살려 두었다 |

### 기각에 딸린 것

반이 「내가 스스로 물린 것」에 올린 여덟 항은 이 자리에서 다시 안 세운다. 그중 둘(`A2` 의 바깥쪽이 §1 이라는 읽기 · `02-order.md` 항목 4 가 서사를 진다는 읽기)은 반 자신이 **좌표로 반증되어 버렸다**고 적었고, 나도 `docs/plan/02-order.md:79` 의 「무엇을 만드나」 칸이 *"진입점 인식기"* 인 것을 확인했다. 초안의 읽기가 맞다.

## 초안을 어떻게 고치나

**고치는 것은 메인의 일이다. 좌표와 방향만 낸다. 새 절을 신설하라고 안 한다.**

| # | 파일 · 절 | 방향 |
|---|---|---|
| 1 | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/dialectic/2-thesis.md` §「판정」 | `A3` = `R3` 를 **`R1`** 로 바꾼다. 뒤집는 근거는 `intent.md:126`·`:124`·`:194` 셋이고 초안이 그 셋을 한 번도 안 인용했다 |
| 2 | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/dialectic/2-thesis.md` §「음성 대조 ⓑ」 | ⓑ 로 낸 한 문장의 마지막 절 *"그 자리는 지금 어디에도 없다"* 를 **`docs/plan/disposal-map.md:120`·`:263-269` 로 정정**한다 — 「`B` 가 정한다」로 등록돼 있고 그 `B` 가 이 회차다 |
| 3 | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/dialectic/2-thesis.md` §「`A3` 가 `R3` 인 근거」 ② | *"가르는 층은 계약 안에 없다"* 를 **`crates/pal-core/src/doctor.rs:66`·`:751` 로 정정**한다. 가르는 장치는 코드에 서 있다. 남는 것은 **엮는 표면**뿐이다 |
| 4 | `docs/plan/01-completion-scenes.md:207-209` | 위임 문장이 **순서표가 답하는 것과 안 답하는 것을 가른다.** 지금은 *"순서표가 답한다"* 하나뿐이라 `docs/plan/02-order.md:180` 의 자기 한계 선언과 어긋난다. ⓑ 를 갚는 자리가 여기다 |
| 5 | `docs/plan/01-completion-scenes.md:12` | *"두 자리를 더한다"* ↔ `docs/plan/00-goals.md:138` 의 *"네 자리를 더한다"*. 관계표(`:14-20`)가 위임 한 행 + 새 자리 네 행이라 `00-goals` 쪽이 맞다. 설계문 `P-d` 가 이미 「안 올린다」로 등록했다 |
| 6 | **게이트 문서를 쓸 때** — `docs/gates/terrain-and-completion-scene.md`(아직 없다) | `A3` 를 「통과」로 적으면 **그 옆에 차선책 ⓑ 의 미이행을 함께 적는다.** 안 적으면 그때 비로소 `금지역 ①`(안 잰 것을 통과로)이 발화한다 |
| 7 | `docs/plan/README.md:3` · `:13` · `:22` | ⚠ **이 판의 판정 대상 밖이다.** 아래 「반론 ⑦ 의 자리」를 보라 |

### 반론 ⑦ 의 자리 — 이 판 밖, 이 회차 안

**밖이라고 정한다. 근거를 댄다.**

- 이 판의 대상은 `A2`·`A3` 이고, 두 조건의 문면(`intent.md:125`·`:126`)에 `docs/plan/README.md` 가 안 들어간다.
- `intent.md` 전체를 `README` 로 검색했다 — **한 건도 안 나온다.** 색인 갱신을 요구하는 조건은 `C1` 하나뿐이고 그 문면은 *"`AGENTS.md` 의 두 칸"* 에 한정된다.
- 그러므로 ⑦ 은 **어떤 완수 조건도 위반하지 않는다.** 위반할 조건이 없다.
- 그리고 `intent.md:185` 가 *"회차 중에 늘리는 것이 금지"* 라 지금 조건을 세울 수도 없다.

**대신 이것은 이미 진단된 병의 재발이다.** `intent.md` 의 `C1` 각주가 조건평가 R1 의 지적을 인용한다 — *"원문이 산출을 「`docs/plan/` 새 문서 + `AGENTS.md` 두 칸은 가리키기만」으로 적었는데 **그것을 요구하는 조건이 하나도 없었다**"*. `AGENTS.md` 는 `C1` 로 메워졌고 **`docs/plan/README.md` 에는 같은 구멍이 그대로 남았다.** 발견 레코드로 넘긴다. 판정은 안 한다.

## 소유자에게

**안 올린다.**

설계문이 결과를 보기 전에 등록한 승격 문턱 표를 그대로 적용한다.

| 항목 | 발화하나 | 왜 |
|---|---|---|
| `P-a` (`R3`) | **안 함** | `A2`·`A3` 둘 다 `R1` 이다. 등록되지 않은 축소가 없다 |
| `P-b` (`R4`) | **안 함** | 항등식이 아니다. 정·반이 각자 문서를 잘라 재서 양방향 반응을 얻었고 나는 그 좌표를 확인했다 |
| `P-c` (정본 다툼) | **안 함** | 판정이 `A5` 로 안 번졌다. `A2` 의 바깥쪽은 위임(`:16`)이 아니라 §2 의 본문으로 서고, 그것은 `intent.md:125` 의 낱말(*"남의 프로젝트에 설치한 사용자"*)을 `docs/plan/01-completion-scenes.md:60` 이 그대로 짚는다 |
| `P-d` (수 불일치류) | **발화 · 「안 올린다」** | 살아남은 것이 전부 거짓신호 등급이다 |
| `P-e` (오염) | **레코드** | 정·반 둘 다 판 1 산출물을 안 열었다고 신고했다. 내 쪽 오염 하나는 머리에 신고했고 판정이 거기 안 기댄다 |
| `P-f` (상한 소진 + 금지역) | **안 함** | 금지역 등급이 살아남지 않았다 |

**그리고 나 자신의 게이트로도 안 올린다** — 살아남은 반론 중 가장 무거운 것이 **거짓신호**다. `실패` 는 `cargo xtask check` `27/27 통과`(내가 다시 돌렸다)로 반증됐고, `금지역` 은 §5 가 스스로를 목표 상태로 못 박아(`docs/plan/01-completion-scenes.md:169-171`·`:207`) 과대 주장의 자리가 없다.

⚠ **한 줄만 조건부다.** 위 「초안을 어떻게 고치나」 6 번 — 게이트가 `A3` 를 「통과」로 적으면서 차선책 ⓑ 의 미이행을 **안 적으면**, 그 순간 `금지역 ①` 이 발화하고 그때는 올려야 한다. 지금은 게이트 문서가 없어서 발화하지 않았다.

## 라운드를 더 도나

**안 돈다. 교착도 아니다.**

설계문의 끝 조건이 남은 반론의 해악도로 가르고, **금지역도 실패도 살아남지 않았다.** 반은 상한 둘 중 한 라운드만 썼고 그 한 라운드가 판정을 실제로 뒤집었다 — 상한에 안 닿고 닫는 것이 설계문 §「상한과 끝 조건」의 *"상한에 닿는 것은 완료가 아니다"* 와 어긋나지 않는다.

**판 3 하나가 남는다.** 아래 둘을 판 3 에 넘긴다 — 이 판은 판정하지 않았다.

- `docs/plan/disposal-map.md:120`·`:263-269` 가 `P8` 계열 결정 여섯을 **`B` — `F17`** 로 등록했고 `docs/plan/disposal-map.md:8` 과 `docs/plan/README.md:3-4` 를 함께 읽으면 그 `B` 가 이 지형 회차다. 그 숙제를 이 회차의 순서표가 받았나 — **`B5`·`B6` 소관이다.**
- `docs/plan/02-order.md:163-169` 의 승계표가 `docs/plan/disposal-map.md` §1.2 의 열셋 중 다섯만 실었다는 것 — **`D1`·`D2` 소관이다.**

## 살아남은 반론의 해악도 분포

| 제목 | 등급 |
|---|---|
| ① `A3` 문면 미인용 — 인용하면 §5 가 문면 낱말을 그대로 낸다 | 거짓신호 |
| ② 잠긴 의도가 `A3` 를 「장면 하나」로 셈해 두었다 | 거짓신호 |
| ③ 「차선책이 죽은 가지」 논법이 *"지우지는 않는다"* 를 안 읽었다 | 거짓신호 |
| ④ (절반) 사실과 가르는 장치는 코드에 서 있다 | 거짓신호 |
| ⑤ ⓑ 의 *"그 자리는 지금 어디에도 없다"* 가 사실이 아니다 | 거짓신호 |
| ⑥ `F17` 승계표 부재는 `P8` 특정 증거가 아니다 | 거짓신호 |
| ⑦ `docs/plan/README.md` 가 두 문서를 아직 「비었다」로 답한다 | 거짓신호 |
| ★ (합이 세웠다) 차선책 ⓑ 가 생성 표면에 대해 안 갚였다 | 거짓신호 |

## 내가 못 정한 것

- **§5 의 「엮는 층」이 제품 표면인가 소비자인 에이전트인가.** 반론 ④ 의 나머지 절반을 기각한 자리다. `docs/plan/01-completion-scenes.md:187` 의 화면은 `pal narrative` 가 「■ 답」을 산출하는 것으로 그리는데, `docs/adr/0025-the-harness-that-reads-the-graph-is-the-same-product.md` 가 하네스를 같은 제품으로 못 박았으므로 그 답을 누가 짓는지가 문서만으로 안 갈린다. **정할 수 있으려면** — `docs/adr/0025-the-harness-that-reads-the-graph-is-the-same-product.md` §3 이 「하네스가 산출하는 것」과 「코어가 산출하는 것」을 가르는 문장을 갖고 있는지, 또는 소유자가 `pal narrative` 의 산출 주체를 한 문장으로 정하는 것. **이 판은 그것 없이도 `A3` 를 정할 수 있었다** — 그 물음의 답이 어느 쪽이든 `A3` 문면의 「장면」은 서 있다.
- **`cargo test --workspace`.** 정도 반도 나도 안 돌렸다. 해악 게이트의 「실패」 행은 `cargo xtask check` 로만 확인했다. 안 잰 것을 잰 것으로 안 센다.
