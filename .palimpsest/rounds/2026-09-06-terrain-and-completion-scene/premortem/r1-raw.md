# 사전부검 R1 — 원 반환문

> `pal-premortem-sweeper` 가 낸 것을 그대로 보존한다. 요약하지 않는다.
> 받은 것: 잠긴 의도 전문 · 착수 재료 · 저장소. 안 받은 것: 대화 기록 · 메인의 사고 과정.
> 실행 2026-09-06 · HEAD `b01fe49`

조회를 마쳤다. 격리 사본(`…/scratchpad/pal-copy-…`)에 심어 **세 개는 실제로 빨갛게 만들어 확인**했고, 나머지는 좌표를 읽어 잰 것과 추정을 구분해 적었다.

---

### D3 이 `NEXT-B-plan-for-harness.md` 를 지우면 이 회차 자신의 발견 좌표가 해소 실패한다
- 어떻게 실패하나: 이 회차의 사전부검·정반합·독립 리뷰가 착수 재료를 `경로` 로 적는다(잠긴 의도의 `## 착수 시점 관측` 이 그 문서를 정정 대상으로 지목하므로 인용은 사실상 강제다). `D3` 이 같은 회차에서 그 파일을 지우면 `check_round_records` 의 ③ 좌표 해소가 발화한다.
- 어디가 걸리나: `xtask/src/main.rs:3980` (`좌표_해소_실패`) · `xtask/src/main.rs:5424` (`좌표가_실재하는가`) · 잠긴 의도 `D3`
- 획득: 조회 + **실험** — 격리 사본에서 `findings.jsonl` 한 행의 `경로` 를 `NEXT-B-plan-for-harness.md` 로 바꾸고 파일을 지운 뒤 `xtask check`: `FAIL 회차 레코드 … 'NEXT-B-plan-for-harness.md' 가 없다`
- 모집단: 원의도
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(파일을 되살리거나 좌표를 바꾼다). ⚠ 잠긴 의도의 `## 범위 밖` 이 **`NEXT-E-*.md` 에 대해서만** 이 함정을 등록했고 `NEXT-B` 에는 안 걸었다 — 같은 병인데 한쪽만 막혔다

### 새 계획 문서가 검사 26(어색한 표현)에 걸린다 — 잠긴 의도의 문면 그대로가 걸린다
- 어떻게 실패하나: `A1` 의 문장 *"완성 장면 문서가 `docs/plan/` 에 선다"* 와 `C4` 의 *"`stale` 을 켠다"* 를 새 문서에 그대로 옮기면 「~ 선다」·「켜다」 둘이 발화한다. `docs/adr`·`docs/gates` 는 동결이지만 **`docs/plan` 은 대상 안**이다.
- 어디가 걸리나: `xtask/src/main.rs:4498` (`어색한_표현`) · `4515` (`교정_뿌리`) · `4528` (`동결_경로`) · 잠긴 의도 `A1`·`C4`
- 획득: 조회 + **실험** — 격리 사본의 `docs/plan/00-goals.md` 끝에 그 두 문장을 심고 `xtask check`: `어색한 표현 부재: … 2곳 … docs/plan/00-goals.md:353 「~ 선다」 / 「켜다」`
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 걸릴 자리가 둘(완성 장면·순서표)이고 산문 전체가 모집단이다

### `.palimpsest/rounds/**/*.json` 하나가 sunset 트리거를 켜고 CI 가 `docs/research` 3,015 줄 삭제를 요구한다
- 어떻게 실패하나: `E6` 이 CI 런 관측을 남기라고 하고, 가장 자연스러운 명령이 `gh run view --json … > observations/…json` 이다. 그 파일 하나로 sunset 트리거가 참이 되고, 선언된 `docs/research`·`docs/evidence-map.md` 가 아직 있으므로 검사 18 이 실패한다. 지금 초록인 것은 회차 산출이 전부 `.jsonl`(확장자 `jsonl ≠ json`)이기 때문일 뿐이다.
- 어디가 걸리나: `docs/sunset.toml:27` (트리거) · `xtask/src/main.rs:2809` (`트리거가_참인가`) · `2975` (`p.extension() == Some("json")`) · 잠긴 의도 `E6`
- 획득: 조회 + **실험** — 사본에 `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/observations/e6-ci.json` 을 놓고 `xtask check`: `FAIL sunset 선언`
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(확장자를 `.txt` 로). 하지만 되돌리지 않고 「트리거가 떴으니 지운다」로 가면 회차 범위가 3,000 줄 삭제로 번진다

### `Binding.target` 의 타입을 바꾸면 `intent.redb` 의 postcard 인코딩이 조용히 깨진다 — 판 표시가 없다
- 어떻게 실패하나: `Binding` 은 `postcard::to_allocvec` 로 통째 직렬화되어 `BINDING` 테이블에 들어가는데 **레코드마다 판 번호가 없다**. `target: SymbolId`(고정 32바이트)를 열거로 바꾸면 판별자 varint 가 앞에 붙어 옛 바이트열이 다른 값으로 읽히거나 `IntentError::Decode` 로 죽는다. JSONL 에는 `schema_version` 이 있지만 redb 에는 없다.
- 어디가 걸리나: `crates/pal-intent/src/store.rs:193,264,331,354,380,407` · `crates/pal-core/src/binding.rs:262` (`pub target: SymbolId`) · 잠긴 의도 `C1`·`C2`
- 획득: 조회 — `grep -n "postcard" crates/pal-intent/src/store.rs` 와 `TableDefinition` 선언을 읽어 판 필드 부재를 확인. 실제 마이그레이션은 안 돌려 봤다
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역 (데이터 손상 · 되돌리기 비용)
- 대상: 계획대상
- 얼마나 아픈가: `C2` 의 「왕복 시험」은 **새 형식끼리만** 왕복하므로 **초록인 채로 통과한다**. 유일한 복구 경로(`pal intent export`)는 코드를 바꾸기 **전에** 돌려야 하는데 조건 어디에도 그 순서가 없다

### `bindings.jsonl` 판을 3 으로 올리면 22 개 결박이 옛 빌드에서 안 읽힌다 — 되돌리기 경로가 없다
- 어떻게 실패하나: `JSONL_SCHEMA_VERSION = 2` · `READABLE_SCHEMA_VERSIONS = [1,2]`. 대상 확장이 판 3 을 부르면 export 는 언제나 최신으로 쓰므로, 다음 export 이후 `.palimpsest/intent/bindings.jsonl`(git 추적 · 22 결박)은 **롤백한 빌드가 "판이 다르다"로 거부**한다. 이 파일이 의도층의 SoT 다.
- 어디가 걸리나: `crates/pal-intent/src/store.rs:605,608,676-687` · `.palimpsest/intent/bindings.jsonl:1` (`{"kind":"header","schema_version":2}`)
- 획득: 조회 — 파일 머리 줄과 상수를 직접 읽음(`head -c 900`, `wc -l` = 23행 = 머리 1 + 결박 22)
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역 (되돌리기 불가한 사람 노동)
- 대상: 계획대상
- 얼마나 아픈가: 22 건. `C1~C3` 조건에 호환성·되돌리기 항목이 하나도 없다

### 문서 좌표 결박의 감시 집합이 비면 상태가 **언제나 `Fresh`** 다 — `[f09.1.pass]` 의 하한과 정면으로 부딪힌다
- 어떻게 실패하나: `BindingStatus::evaluate` 는 `binding.watch` 만 순회한다. 문서 좌표에 감시 집합을 못 세우면(=빈 벡터) `changed`·`gone`·`못_봄` 이 전부 비어 `CodeFreshness::Fresh` 가 나온다 — 화면이 *"이 결정은 유효하다"* 를 찍는다. `차선책` 의 *"낡음 없는 결박으로 약하게 선언"* 을 타입으로 표현할 자리가 없다.
- 어디가 걸리나: `crates/pal-core/src/binding.rs:613-651` (`evaluate`) · `crates/pal-core/src/radius.rs:126-131` · `corpus/criteria.toml:7021` (*"감시 집합 크기의 합이 결박 수 미만이면 멈춘다"*)
- 획득: 조회 — `evaluate` 전문과 `corpus/criteria.toml` `[f09.1.pass]` 의 하한 문장을 읽음. 실제로 문서 결박을 만들어 돌려 보지는 못했다(그 코드가 아직 없다)
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역 (사실이 아닌 것을 사실로 · R-16 이 닫은 자리가 다시 열린다)
- 대상: 계획대상
- 얼마나 아픈가: `C4` 를 반증으로 적어도 이 화면은 남는다 — 반증을 적는 것과 `Fresh` 를 안 찍는 것은 다른 일이다

### 다섯째 `UndeterminableReason` 을 더하면 사전 등록된 `undeterminable_reasons_built = 4` 가 깨진다
- 어떻게 실패하나: 위 문제를 정직하게 고치는 길은 「문서 좌표라 낡음의 자를 모른다」는 새 사유를 더하는 것인데, `UndeterminableReason::ALL: [Self; 4]` 와 코퍼스의 `= 4` 가 그 수를 잠갔다. `corpus/` 는 소유자가 *"범위 밖 — 측정 장치다"* 로 잠근 자리다.
- 어디가 걸리나: `crates/pal-core/src/binding.rs:474-476` (`ALL: [Self; 4]`) · `corpus/criteria.toml:7124` · 이슈 #116(「상태 넷으로 충분한가」)이 이미 그 물음을 진다
- 획득: 조회 — 상수와 코퍼스 값, `gh issue list` 로 #116 확인
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 두 자리(코어 상수 · 코퍼스). 코퍼스를 만지면 잠긴 범위를 깬다

### `pal doctor` 가 `BOUND_TO` 를 언제나 `Symbol` 노드로 낸다 — 문서 결박은 유령 엣지가 된다
- 어디가 걸리나: `crates/pal-cli/src/doctor.rs:200` (`symbol_key`) · `:234` (`Anchor::At(coord(b.target))`) · `:253` (`symbol_key(b.target)`) · `schema/graph.toml:218` (`to = ["Symbol"]`) · `crates/pal-core/src/doctor.rs:862` (`index_resolves`)
- 어떻게 실패하나: 문서 좌표 결박이 있으면 `doctor` 가 존재하지 않는 `Symbol` 노드를 가리키는 엣지를 만들고, 앵커가 `Coordless` 가 되며 불변식이 그 결박들을 위반으로 헤아린다.
- 획득: 조회 — `grep -rn "\.target\b" --include='*.rs'` 로 소비자 전수(11 자리)를 뽑고 각 자리를 읽음
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 걸리는 자리 넷(스키마 `to` · `doctor` 노드/엣지 · `pal-query` 의 `TargetPlace`). 되돌릴 수 있다

### `WATCH`·`BOUND_BY` 색인이 32바이트 열쇠를 강제한다 — 문서 좌표는 오류이거나 `SymbolId` 네임스페이스와 충돌한다
- 어떻게 실패하나: `watch_index()` 는 열쇠가 32바이트가 아니면 `Decode` 오류를 낸다(그리고 `[f09.1.pass]` 가 그것을 읽는다). 반대로 문서 경로를 32바이트로 해시해 넣으면 **같은 다중맵에 두 네임스페이스가 섞이고**, `SymbolId::from_bytes(raw)` 가 실재하지 않는 심볼 id 를 만들어 `projection.symbol()` 이 `None` → `Now::Gone` → `Orphaned` 를 낸다.
- 어디가 걸리나: `crates/pal-intent/src/store.rs:283-303` (`watch_index`) · `:209` (`BOUND_BY` insert) · `:313` (`bound_to`) · `crates/pal-query/src/lib.rs:841-850`
- 획득: 조회 — 세 함수 본문을 읽음. 충돌 확률은 계산 안 했다
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역 (거짓 `Orphaned` = *"결정을 다시 해야 한다"* 를 사실이 아닌데 적는다)
- 대상: 계획대상
- 얼마나 아픈가: 색인 둘 · 조회 셋

### 「앵커는 신고받지 않는다」 검사가 **타입 이름**으로 모집단을 잡는다 — 새 좌표 계열은 이름만 다르면 통째로 사각이다
- 어떻게 실패하나: 검사 12 는 소스에서 문자열 `"WatchEntry {"` 를 찾고 등록된 파일 셋과 댄다. 문서 좌표용 감시 항목을 `DocWatch { … }` 처럼 다른 이름으로 만들면 **검사가 그것을 아예 안 본다** — 발화도 안 하고 사각도 안 적힌다. 앵커가 생산자 신고에서 오는 경로가 그 계열에 대해 조용히 열린다.
- 어디가 걸리나: `xtask/src/main.rs:2034` (`WATCH_ENTRY_SITES`) · `:2054` (`code.contains("WatchEntry {")`) · `:2063` (하한은 「0 이면 실패」뿐이라 **부분 사각을 못 잡는다**)
- 획득: 조회 — 검사 본문과 허용 목록을 읽음
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역 (측정이 죽은 가지가 된다)
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(검사 모집단을 이름이 아니라 필드 형태로 잡으면 된다). 안 고치면 다음 회차가 그것을 못 본다

### `A4` 의 음성 대조가 「어느 항목」을 못 짚는다 — 죽은 링크는 파일만 적고 줄을 안 적는다
- 어떻게 실패하나: `A4` 는 *"장면 하나를 지우면 순서표의 **어느 항목**이 근거를 잃는지 기계가 답한다"* 를 요구한다. 기계로 세울 수 있는 유일한 자가 죽은 링크 검사인데, 그 산출은 `"{파일}  →  {대상}  (그 조각이 없다)"` 로 **줄 번호도 항목 이름도 없다.** 항목 둘이 같은 장면을 가리키면 두 줄이 글자까지 같아 구별이 안 된다.
- 어디가 걸리나: `xtask/src/main.rs:2653,2661` (`죽음.push(format!("{} → {대상}", 상대_경로(…)))`) · 잠긴 의도 `A4`
- 획득: 조회 — `check_dead_links` 본문. 실제로 앵커를 지워 출력을 보지는 않았다
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호 (조건을 「통과」로 적으면 답의 해상도가 조건보다 낮다)
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다 — 조건을 「어느 문서」로 낮추거나 항목마다 유일한 앵커를 강제한다

### 문서를 둘로 가른 근거와 `A4` 가 요구하는 결합이 서로를 무르게 한다
- 어떻게 실패하나: `## 두께` 는 *"순서를 고칠 때마다 장면이 함께 커밋되는 것이 낡는 캐시의 씨앗"* 이라 갈랐다. 그런데 `A4` 가 기계로 서려면 순서표가 장면을 **앵커 링크로** 걸어야 하고, 그러면 장면 제목을 한 글자만 고쳐도 순서표 쪽이 죽은 링크로 빨개진다 — 갈라서 없애려던 결합이 앵커로 되돌아온다. 같은 문서 안이면 `파일_경로인가` 가 `(#앵커)` 를 링크로 안 세므로 **대조 자체가 안 선다**.
- 어디가 걸리나: `xtask/src/main.rs:2955` (`파일_경로인가` — `/` 도 확장자도 없으면 거짓) · `:2658` (`.md` 일 때만 앵커를 본다) · 잠긴 의도 `## 두께`·`A4`
- 획득: 조회 — 두 함수의 조건문을 읽음
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 다만 「가른다」와 「기계가 답한다」 중 하나를 약하게 선언해야 한다

### `B2` 가 횡단요구 `U` 를 이름 없이 떨어뜨린다
- 어떻게 실패하나: `00-goals.md §2.5` 의 채점 단위는 **`C1~C6` + `U`(일곱)** 이고 `docs/gates/inventory-disposal.md:21` 이 그 수를 「7」로 세어 두었다. `B2` 는 *"`C1~C6` 중 하나도 안 대는 항목은 순서표에 못 들어온다"* 라고만 적어 `U`(빌드 없이·임의 커밋에서·균일하게)에만 매달리는 일 — 예: 언어 등급·추출 균일성·`ExtractGrade` 관련 이슈 다수 — 이 **사유 없이** 순서표 밖으로 나간다.
- 어디가 걸리나: `docs/plan/00-goals.md:179,200` · `docs/gates/inventory-disposal.md:21` · 잠긴 의도 `B2`
- 획득: 조회 — `grep -rn "능력 계약"` 로 정의 자리를 찾고 §2.5 전문을 읽음
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호 (범위가 조용히 좁아진다)
- 대상: 계획대상
- 얼마나 아픈가: 문면 한 줄. 지금 고치면 싸다

### `B3` 의 예외가 하네스 표면 넷 중 셋을 통째로 순서표 밖에 둔다 — 순서표가 `## 목적 기여` 의 물음에 안 답하게 된다
- 어떻게 실패하나: ADR-0025 가 정한 표면 넷 중 **스킬·에이전트 정의·훅** 셋은 프롬프트와 강제이고 `C1~C6`(대상 프로젝트의 코드 그래프 능력) 어디에도 안 매달린다. `pal` 상위 명령 17 중 `install`·`update`·`uninstall`·`hook`·`round`·`intent`·`cache` 일곱도 마찬가지다. `B3` 이 그것들을 「없음」으로 적고 순서표 밖에 두면, **순서표는 코어 능력의 순서표가 되고** *"소비 가능해지는 지점에 가장 가까운 것"* 이라는 판정자에 답하지 않는다 — 하네스가 제품이라는 것이 ADR-0025 의 결정인데.
- 어디가 걸리나: `docs/adr/0025-…md` · `docs/plan/00-goals.md:186-196` (`C1~C6` 정의) · `crates/pal-cli/src/main.rs:113-292` (명령 목록) · `.claude/agents/` 13 개 · `.claude/skills/round/` · 잠긴 의도 `B3`·`## 목적 기여`
- 획득: 조회(명령·에이전트·표면의 수는 `ls`·`grep` 로 셌다) + **추정**(어느 것이 `C1~C6` 소비자인지의 분류는 내 판단이다 — 저장소에 그 대응표가 없다)
- 모집단: 원의도
- 유효성: 추정
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있지만 늦게 알면 순서표 전체를 다시 세운다. `B3` 이 스스로 *"억지로 매달면 축이 거짓이 된다"* 라고 적었으므로 **그 예외가 얼마나 커지면 축이 거짓인가**의 문턱이 없는 것이 실제 공백이다

### `E6` 의 순서 문제가 안 없어진다 — 관측을 적는 커밋도 회차 산출이다
- 어떻게 실패하나: `E4` 가 `report.md` 를, `E5` 가 게이트 `## 판정` 을 산출로 요구한다. CI 런의 `conclusion` 은 그 push 가 끝난 **뒤**에만 알 수 있으므로, 관측을 적으려면 `report.md`·게이트를 고치는 커밋이 하나 더 필요하고 **그 커밋도 산출이다.** 그러면 「산출의 마지막 커밋」이 다시 관측되지 않은 커밋이 된다 — 앞 회차가 `H2` 로 만난 것과 같은 회귀다. *"관측을 적는 커밋을 push 에 포함"* 은 아직 일어나지 않은 런의 결과를 커밋하라는 뜻이 되어 성립하지 않는다.
- 어디가 걸리나: 잠긴 의도 `E4`·`E5`·`E6` · `.palimpsest/rounds/2026-09-06-user-surface-vocabulary/report.md:84-91`
- 획득: 조회 — 앞 회차 종결 보고의 같은 자리와 `.github/workflows/ci.yml:46-49`(`on: push[main]` · `pull_request`)를 읽음
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호 (자리표시자를 미리 「통과」로 적으면 **금지역**으로 올라간다)
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다 — 「산출」의 경계를 조건에 명시하거나(예: 관측 커밋은 산출이 아니다) 다시 「미측정」으로 적는다

### 두 번 push 하면 `cancel-in-progress` 가 앞 런을 죽여 `E6` 이 성립 자체를 못 한다
- 어떻게 실패하나: `concurrency.group = "${{ github.workflow }}-${{ github.ref }}"` · `cancel-in-progress: true`. `E6` 을 위해 push 를 두 번 하는데 첫 런이 끝나기 전에 둘째를 밀면 첫 런의 `conclusion` 이 `cancelled` 가 된다 — 그리고 그때 이미 커밋에 적어 둔 *"success"* 는 **사실이 아닌 것을 사실로 적은 것**이 된다.
- 어디가 걸리나: `.github/workflows/ci.yml:52-54` · 잠긴 의도 `E6`
- 획득: 조회 — 워크플로 설정과 `gh run view 34014526416 --json headSha,event,conclusion` (pull_request 런의 `headSha` 가 브랜치 커밋과 같음을 확인)
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역 (사실이 아닌 것을 사실로)
- 대상: 계획대상
- 얼마나 아픈가: 절차로 막을 수 있다(둘째 push 전에 `gh run list` 로 대기). 조건 문면에는 그 대기가 없다

### 완수 조건 `C1~C7` 과 능력 계약 `C1~C6` 이 같은 이름 공간을 쓴다 — `## 차선책` 이 이미 갈렸다
- 어떻게 실패하나: `## 차선책` 이 *"`B1`(능력별 실측)이 **`C5`·`C6`** 중 어느 **능력**에서 원리상 못 재면"* 이라고 적는데, 같은 문서의 `C5` 는 「RED 관측」이고 `C6` 은 「음성 대조」다(능력으로 읽으면 지배관계·시간축). 저장소는 이미 이 충돌을 한 번 겪고 `docs/evidence-map.md:53` 에 **기호 충돌 주의** 규약을 적었는데, 이 회차가 셋째 `C` 계열을 연다.
- 어디가 걸리나: 잠긴 의도 `## 완수 조건 C` 절 · `## 차선책` 둘째 항 · `docs/evidence-map.md:53` · `docs/plan/00-goals.md:179`
- 획득: 조회 — 두 목록을 나란히 놓고 대조. `grep -rn "C1~C6"` 로 기존 충돌 규약을 찾음
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 두 문서에 걸린다. 게이트 `## 판정` 의 조건 ID 집합 대조는 `intent.md` 만 읽으므로 **기계는 안 걸린다** — 그래서 사람만 틀린다

### 이슈 #69 가 같은 물음에 **다른 설계**를 이미 등록해 두었다 — `C` 가 그것을 안 대고 간다
- 어떻게 실패하나: #69(「문서 간 결박이 없다 — 이 문서를 지우면 무엇이 깨지나를 그래프가 못 답한다」)는 **문서 노드 + 문서→문서 엣지**를 만들자고 적었다. `C` 는 **결박의 대상을 문서 좌표로 넓히자**는 다른 설계다. 둘은 같은 물음(`A4` 의 음성 대조가 바로 #69 의 문장이다)에 답하는데, 잠긴 의도가 #69 를 한 번도 안 부른다. `C7` 이 초석 ADR 을 발행하면 열린 이슈 하나와 어긋난 결정이 정본이 된다.
- 어디가 걸리나: `gh issue view 69` · 잠긴 의도 `C1`~`C7`·`A4` · `docs/plan/00-goals.md §3.3`
- 획득: 조회 — `gh issue list --state open --limit 40` 과 `gh issue view 69`
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호 (초석이라 뒤에 오는 것이 다 움직인다)
- 대상: 계획대상
- 얼마나 아픈가: 지금이면 ADR 한 절. 발행 뒤면 되돌리기가 비싸다. `B4`(첫 항목이 이슈로 서 있다)의 후보로도 #69 가 가장 가깝다

### `B1` 의 실측표가 회차 안에서 낡는다 — 같은 회차의 `C` 가 그 값을 바꾼다
- 어떻게 실패하나: `B1` 은 *"추정이 아니라 조회·실행의 출력이 근거로 붙는다"* 를 요구한다. `C6` 의 능력(시간축·낡음)과 `C1`(구조) 칸의 값은 `C` 가 착지하는 순간 바뀐다. 착수 시점 출력을 붙이면 종료 시점에 거짓이고, 종료 시점에 다시 재면 `B` 가 `C` 를 기다려야 해서 `B`(선행 A)라는 선행 관계가 사실과 다르다.
- 어디가 걸리나: 잠긴 의도 `B1`(선행 A) · `C`(선행 없음) · `## 착수 시점 관측` 의 「결박의 대상 = 심볼만」 행
- 획득: 조회 — 선행 관계 표기와 착수 관측표를 대조
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호 (게이트의 1차 증거가 낡는다)
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다 — 실측표에 측정 커밋을 박으면 된다. 앞 회차가 `39f944d` 에서 같은 병을 고쳤다

### 검사 26 의 `이_회차_종결문서` 가 앞 회차 이름으로 못 박혀 있어 **이 회차의 종결 문서는 안 재어진다**
- 어떻게 실패하나: 「이 회차의 종결 문서부터는 교정 표기로 쓴다」는 규칙을 재는 자리가 `[".palimpsest/rounds/2026-09-06-user-surface-vocabulary/report.md", "docs/gates/user-surface-vocabulary.md"]` 두 문자열에 하드코딩돼 있다. `.palimpsest` 는 `교정_뿌리` 밖이고 `docs/gates` 는 동결이므로, **새 회차의 `report.md` 와 게이트는 어느 검사에도 안 들어간다.** 앞 회차가 세운 장치가 다음 회차를 여는 순간 죽은 가지가 된다.
- 어디가 걸리나: `xtask/src/main.rs:4534-4537` (`이_회차_종결문서`) · `:4515` (`교정_뿌리` — `.palimpsest` 없음) · `:4528` (`동결_경로` — `docs/gates` 포함)
- 획득: 조회 — 세 상수를 읽고 이 회차의 산출 경로를 대조
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역 (측정이 죽은 가지가 된다)
- 대상: 계획대상
- 얼마나 아픈가: 두 줄로 고칠 수 있다. 안 고치면 규칙이 회차마다 한 번씩만 살아 있다

### `scripts/frontier.sh` 의 마지막 줄이 「지형은 ⏳ 비었다」로 남는다
- 어떻게 실패하나: 산출은 `AGENTS.md` 두 칸만 새 문서를 가리키게 한다. 그런데 *"지금 어디에 서 있는가"* 에 답하는 실행 출력의 마지막 줄이 `착수는 gh issue edit … (지형은 ⏳ 비었다 — docs/plan/disposal-map.md)` 다. 회차가 지형을 세운 뒤에도 그 문장이 그대로 나온다.
- 어디가 걸리나: `scripts/frontier.sh:78` · `AGENTS.md:44` 위 두 행 · 잠긴 의도 `## 범위 밖`(「`AGENTS.md` 의 다른 절」만 뺐고 스크립트는 언급 없음)
- 획득: 조회 — 스크립트 전문
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 한 줄

### `docs/sunset.toml` 의 유보 사유가 이 회차로 만기가 되는데 범위에 없다
- 어떻게 실패하나: sunset 선언이 `docs/research` 를 지금 안 지우는 이유를 *"**새 지형**이 그것을 어디에 둘지 정한 뒤에 간다"* 로 적었다. 이 회차가 그 새 지형이다. 회차가 그 문장에 답하지 않으면 선언의 사유가 **이미 거짓**인 채로 남고, 다음 사람이 그 문장을 읽고 또 미룬다.
- 어디가 걸리나: `docs/sunset.toml:41-46` · 이슈 #68 · 잠긴 의도 `D2`(24 문서만 다룬다)
- 획득: 조회 — `docs/sunset.toml` 전문과 `gh issue view 68`
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 한 문장(「이 회차는 답하지 않는다」를 범위 밖에 적기만 해도 닫힌다)

### `Radius` 넷이 전부 심볼 이웃 위에 정의돼 있어 문서 좌표에는 반경이 원리상 없다
- 어떻게 실패하나: `expand(target: SymbolId, radius, n: &impl Neighborhood)` 이고 `Neighborhood` 의 두 함수는 `callers_of(SymbolId)` · `symbols_in(RepoPath) -> Vec<SymbolId>` 다. `pal bind` 는 반경 기본값 `symbol` 을 강제하고 감시 항목의 digest 를 **투영에서** 읽는다. 문서 좌표에는 넷 중 어느 것도 뜻이 없고, `files:AGENTS.md` 조차 마크다운에는 심볼이 없어 빈 집합을 준다.
- 어디가 걸리나: `crates/pal-core/src/radius.rs:107-112`(`Neighborhood`) · `:132-164`(`expand`) · `:105`(`NAMES: [&str; 4]`) · `crates/pal-cli/src/bind.rs:105-118` · `crates/pal-cli/src/main.rs:139`(`default_value = "symbol"`)
- 획득: 조회 — 세 파일의 시그니처와 기본값
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: `Radius` 는 postcard 로 저장되므로 변형을 늘리면 위의 인코딩 문제와 같은 자리에 걸린다

### `pal export` 가 새 노드 라벨을 「이 빌드의 2층에 없다」로 잘못 적는다
- 어디가 걸리나: `crates/pal-cli/src/export.rs:289-297` (`lives_in` — `"Binding" | "BOUND_TO"` 만 의도 저장소로 적고 나머지는 `_ =>`)
- 어떻게 실패하나: `C1` 이 스키마에 문서 좌표 노드를 세우면 export 의 「못 산출한 라벨」이 그 노드를 *"이 빌드의 2층에 없다"* 로 적는다 — 실제로는 의도 저장소에 산다.
- 획득: 조회 — 함수 본문
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 한 줄

### `C5` 의 GREEN 이 「사람용 표면」과 부딪힌다
- 어떻게 실패하나: `## 범위 밖` 이 *"`pal` 의 사람용 표면(결박을 거는 새 명령·화면)"* 을 뺐는데, `C5` 는 *"`pal bind "AGENTS.md"` 가 성공하는 것이 `C1~C3` 의 GREEN"* 이라고 적는다. `pal bind` 의 `NAME` 인자 의미를 바꾸는 것은 사람이 부르는 표면을 바꾸는 것이다 — 「새 명령」은 아니라는 읽기로 빠져나갈 수 있지만, 그 읽기가 게이트에 안 적히면 판정자가 갈린다.
- 어디가 걸리나: 잠긴 의도 `C5` · `## 범위 밖` 셋째 항 · `crates/pal-cli/src/main.rs:128-130`(`/// 심볼 이름` `name: String`)
- 획득: 조회(두 문면과 인자 선언) + 추정(어느 읽기가 옳은지는 소유자만 정한다)
- 모집단: 원의도
- 유효성: 추정
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 한 문장

### 「계획과 구현이 한 회차」라서 `## 모집단 분리` 의 (나)가 `B` 산출에 대해 실질적으로 안 선다
- 어떻게 실패하나: (나)는 *"장치를 만들되 그것에 대한 발견은 별개 회차"* 다. 그런데 `A`·`B` 의 산출(완성 장면·순서표)이 **같은 회차의 `C`·`D`·`E` 를 판정하는 자**로도 쓰인다 — 예컨대 `B1` 이 `C` 의 결과를 실측하고, `B4` 가 여는 이슈가 `C` 의 후속이다. 자기 장치가 자기 회차의 판정 입력이 되는 형태이고, 규약이 *"(가)도 (나)도 아닌 것 — 이 회차 안에서 장치까지 완성하고 그 장치에 대한 발견도 다 닫는다 — 이 안 된다"* 로 잠근 자리에 가깝다.
- 어디가 걸리나: `.claude/skills/round/SKILL.md:889-930` · 잠긴 의도 `## 모집단 분리` · `B1`·`B4`
- 획득: 조회(규약 전문) + 추정(「판정 입력이 되는가」는 내 해석이다)
- 모집단: 규약
- 유효성: 추정
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다 — 별도 목록의 경계를 착수 시점에 한 줄로 더 좁히면 된다

### 이 회차의 금지역 목록이 어디에도 등록돼 있지 않다
- 어떻게 실패하나: 규약이 금지역의 출처를 ① `.claude/pal/policy.toml` ② 잠긴 의도 ③ 기본 다섯 순서로 정한다. ①은 없고(`ls` 로 확인) ②도 잠긴 의도에 없다. 그래서 ③ 기본 다섯이 적용되는데, **`C` 가 사람의 결박 데이터를 만지는 회차**라 「데이터 손실」의 경계(예: `intent.redb` 재구축을 손실로 보는가)를 이 회차가 스스로 정해야 한다. 안 정하면 해악 게이트의 판정이 라운드마다 흔들린다.
- 어디가 걸리나: `.claude/skills/round/SKILL.md:862-885` · `.claude/pal/policy.toml`(없음) · 잠긴 의도 전체
- 획득: 조회 — `ls .claude/pal/policy.toml` → 없음, 의도 전문에 `금지역` 등록 없음
- 모집단: 규약
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 한 절. 규약이 *"회차 중에 늘리지 않는다"* 로 잠갔으므로 **지금이 유일한 등록 시점**이다

### `E3`(열린 것 0)과 상한 다섯 라운드가 곱해져 자기 장치 발견이 원장을 지배한다
- 어떻게 실패하나: 앞 회차의 원장이 197 행(사전부검 39 · 독립리뷰 123)이었고, 이 회차는 상한이 사전부검 3 · 독립리뷰 5 다. `C` 가 코어 타입을 건드리므로 발견 대부분이 「이 회차가 만든 장치」에 몰릴 것이고, (나) 를 골랐으므로 그것들은 전부 `처분=범위밖 + 의도 기록` 으로 닫아야 한다. `## 개정` 절이 그만큼 커지고, 검산이 맞으려면 매 라운드 반환문의 항 수와 레코드 행 수가 정확히 같아야 한다.
- 어디가 걸리나: `xtask/src/main.rs:4064-4090`(반환문 항 ↔ 레코드 행 검산) · `.claude/skills/round/bin/record.py --schema` 의 `닫힘축.요구하는자리`(`범위밖 → 의도`) · 잠긴 의도 `E3`·`## 상한`
- 획득: 조회 — `record.py --schema` 를 돌려 `닫힘축` 을 읽음 + 앞 회차 `report.md:55-58` 의 실측 수
- 모집단: 규약
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 없는 것은 아니지만 라운드마다 커밋이 하나씩 는다

---

## 내가 기각한 것

> 첫 열 이름만 「무엇」에서 「제목」으로 메인이 고쳤다 — 추출기의 열 별칭 목록에
> 「무엇」이 없어서 이 표가 통째로 안 읽혔다. 값은 한 칸도 안 바꿨다.

| 제목 | 좌표 | 획득 | 모집단 | 유효성 | 해악도 | 대상 | 왜 아니었나 |
|---|---|---|---|---|---|---|---|
| `D3` 이 `NEXT-B` 를 지우면 죽은 링크 검사가 빨개진다 | `xtask/src/main.rs:2629` · `3128` | 조회 — `grep -rn "NEXT-B"` 전수 12 건 | 저장소 | 거짓 | 미관 | 계획대상 | 남은 인용이 전부 `.palimpsest/` 안이고 그 뿌리는 인용·링크 모집단 밖이며, 형태도 마크다운 링크가 아니라 코드 표기다. 걸리는 것은 **회차 레코드의 `경로`** 뿐이다(살려서 별도 시나리오로 냈다) |
| CI 가 얕은 클론이라 「발견이 닫혔나」가 0 행을 잰다 | `.github/workflows/ci.yml:88-90` | 조회 — 워크플로 · 격리 사본에서 `.git` 없이 재현해 증상은 봤다 | 저장소 | 거짓 | 실패 | 계획대상 | `fetch-depth: 0` 이 이미 걸려 있고 주석이 그 사유를 적었다. 사본에서 난 실패는 내가 `.git` 을 뺀 탓이지 CI 의 사정이 아니다 |
| 지금 `findings.jsonl` 이 이미 sunset 트리거를 켠다 | `docs/sunset.toml:27` · `xtask/src/main.rs:2975` | 조회 + 실험 | 저장소 | 거짓 | 실패 | 계획대상 | 트리거는 확장자 동일성 비교라 `jsonl ≠ json` 이다. **새로 `.json` 을 놓으면** 켜지는 것이 참이고 그쪽만 남겼다 |
| 이 브랜치는 `main` 이 아니라 CI 런이 안 붙어 `E6` 이 원리상 불가다 | `.github/workflows/ci.yml:46-49` | 조회 — `gh pr list` · `gh run view --json headSha` | 저장소 | 거짓 | 금지역 | 계획대상 | PR #115 가 열려 있어 `pull_request` 런이 돈다. 그리고 그 런의 `headSha` 가 **브랜치 커밋 그대로**(`7c41b81`)라 커밋에 런을 붙이는 것이 성립한다 |
| 자기 장치 발견을 별도 목록으로 내면 `E3`(열린 것 0)을 지킬 길이 없다 | `record.py --schema` 의 `닫힘축` | 조회 — `python3 .claude/skills/round/bin/record.py --schema` | 규약 | 거짓 | 실패 | 계획자신 | 처분 `범위밖` 이 enum 에 있고 `요구하는자리 = "의도"` 라 잠긴 의도에 적으면 닫힌다. 경로가 있다 |

---

## 반환문의 꼬리

> 절 제목은 메인이 붙였다 — 원 반환문에는 제목 없이 `---` 뒤에 이어졌고, 그러면 합계
> 검산이 이 아래의 불릿을 「기각한 것」으로 센다. 내용은 한 글자도 안 바꿨다.
> ⚠ 아래 「전체 시나리오 수: 25」는 **에이전트의 자기 계수이고 틀렸다** — `###` 항은
> 28 개다. 규약이 예견한 바로 그 자리(*"사람도 에이전트도 자기 산출을 잘못 잰다"*)라
> 원문 그대로 둔다.

새 범주: **사전 등록된 측정 상수와의 충돌**(`corpus/criteria.toml` 의 `= 4` · 「감시 집합 합 ≥ 결박 수」 같은, 회차가 못 만지는 자리에 박힌 수를 코드 변경이 무르게 하는 갈래)

**전체 시나리오 수: 25**

주요 좌표:
- `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/intent.md`
- `xtask/src/main.rs` (검사 7·12·18·19·21·26)
- `crates/pal-core/src/binding.rs` · `radius.rs`
- `crates/pal-intent/src/store.rs`
- `schema/graph.toml` · `docs/sunset.toml` · `corpus/criteria.toml`
