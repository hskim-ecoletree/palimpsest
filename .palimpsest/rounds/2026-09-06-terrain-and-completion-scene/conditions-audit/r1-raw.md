# 완수 조건 설계 평가 R1 — 원 반환문

> `pal-condition-auditor` 가 낸 것을 그대로 보존한다. 요약하지 않는다.
> 받은 것: 잠긴 의도의 절 여섯(`## 원문`·`## 목적 기여`·`## 완수 조건`·`## 차선책`·
> `## 범위 밖`·`## 상한`) — 개정 태그를 지운 사본. 안 받은 것: 대화 기록 · 조건을 쓴 자의 설명.
> 실행 2026-09-06 · HEAD `95ba2c1`
>
> ⚠⚠ **격리가 부분적으로 깨졌다 — 감사자가 자진 신고했다.** 메인이 첫 호출에서
> 자리표시자를 안 채워 보냈고, 정정이 닿기 전에 감사자가 `intent.md` 를 전문 열었다.
> 그래서 `## 개정` 표 20 행 · `## 승격` · `## 금지역` · `## 모집단 분리` · `## 두께` ·
> `## 착수 시점 관측` 을 봤다. 감사자가 한 완화와 그 때문에 죽인 발견 둘은 아래
> 「내가 기각한 것」에 사유와 함께 적혀 있다. **이 라운드의 격리는 온전하지 않다.**

---

완수 조건 설계 평가 라운드 1 을 돌았다. 먼저 격리에 대한 고지가 있다.

# 완수 조건 설계 평가 — 라운드 1 · `2026-09-06-terrain-and-completion-scene`

## ⚠ 고지 — 내 격리가 부분적으로 깨졌다

앞 메시지가 `INTENT_SECTIONS_PLACEHOLDER` 자리표시자로 왔다. 정정 메시지가 오기 **전에** 나는 저장소에서 `intent.md` 를 전문 열었다. 그래서 **`## 개정` 표(20 행)·`## 승격`·`## 금지역`·`## 모집단 분리`·`## 두께`·`## 착수 시점 관측` 을 봤다.** 「이건 사전부검 R1 이 이미 잡은 것」이라는 사전 정보가 나에게 생겼다.

내가 한 완화: **판정은 `audit-in.md` 의 여섯 절 문면만으로 했다.** `⟨개정 R1⟩` 태그가 붙었는지는 판정에 안 썼고, 앞 절에서 본 것 때문에 죽인 발견은 `## 내가 기각한 것` 에 그 사유로 적었다. 그래도 **이 라운드의 격리는 온전하지 않다.** 라운드 2 를 다른 컨텍스트로 돌릴지는 메인이 정한다.

산출은 조건 37 개 전부를 훑었다. 저장소 조회로 확인한 좌표는 아래의 「유효성: 참」이다.

## 조건 갈래 분류

표시 있음 15 · 없음 22 · 「조건이 아니다」 3(`G2-b`·`G5`·`F2`).

갈래가 어긋난 것: `A1`(결정론적 가능인데 정반합) · `G1`(결정론적 표시인데 뒷 절반이 의미론적) · `G2-b`(조건이 아니라 정책) · `G5`(이미 참인 착수 관측) · `F2`(어떤 결과든 참).

## 발견

### 원문의 「AGENTS.md 두 칸은 가리키기만」이 완수 조건 어디에도 없다

원문 라운드 3 이 산출을 *"`docs/plan/` 새 문서 + `AGENTS.md` 두 칸은 가리키기만"* 으로 적었고, `## 목적 기여` 가 그 두 칸이 ⏳ 로 비어 있는 것을 **이 회차를 고른 근거**로 삼는다. 그런데 37 개 중 그 두 칸이 새 문서를 가리킨다를 요구하는 조건이 **하나도 없다.** `## 범위 밖` 은 상한만 적었지 하한을 안 적었다. 조건이 전부 닫혀도 `AGENTS.md:39,40` 은 ⏳ 인 채로 통과한다.

- 모집단: 원의도
- 유효성: 참
- 해악도: 실패
- 어디가 걸리나: 완수 조건 전체 · `AGENTS.md:39,40`

### 원문의 「정반합으로 초안을 세워 올린다」가 완수 조건이 아니다

원문에는 **두 동작**이 있다 — 정반합으로 세운다, 그리고 **올린다**. `## 상한` 표가 「정반합 2~3 판」을 상한으로 적을 뿐이고, 완수 조건에는 정반합을 돌았다도 초안을 올렸다도 없다. 상한은 「몇 번까지」이지 「했나」가 아니라서 0 판을 돌아도 위반이 아니다.

- 모집단: 원의도
- 유효성: 참
- 해악도: 실패
- 어디가 걸리나: `## 완수 조건` 전체 · `## 상한`

### 원문의 「`H2` 의 문면은 안 고친다」가 조건에도 범위 밖에도 없다

소유자가 명시적으로 금한 것인데 `## 범위 밖` 여섯 항 어디에도 없고 완수 조건에도 없다. 같은 「회차 밖에서 정한 것」의 첫째 항(교대 문서 삭제)은 `D3` 으로 조건화됐다. 둘 중 하나만 조건이 됐다. 이 금지는 결정론적으로 잰다.

- 모집단: 원의도
- 유효성: 참
- 해악도: 금지역
- 어디가 걸리나: `## 완수 조건` 전체 · `## 범위 밖`

### 원문의 「낡은 교대 문서는 지운다」가 `D3` 에서 한 건으로 줄었다

원문은 복수형이고 뿌리에 교대 문서가 **셋** 있다. `D3` 은 `NEXT-B` 하나만 요구하고 `## 범위 밖` 이 `NEXT-E-*` 둘을 뺐다. 그 사유는 「지우면 안 된다」가 아니라 「레코드를 먼저 손봐야 한다」인데 조건은 그것을 범위 밖으로 처분했다. 그리고 **「낡은 교대 문서」의 모집단이 문면 어디에도 정의되지 않았다.**

- 모집단: 원의도
- 유효성: 참
- 해악도: 실패
- 어디가 걸리나: `D3` · `## 범위 밖` 6번째 항

### `docs/plan/` 에 이미 「완성 장면」이 있는데 `A1` 이 그 관계를 안 정한다

`docs/plan/00-goals.md` **§1 의 제목이 그대로 「완성 장면 — 이 화면이 제품이다」**이고 화면 한 장을 이미 그려 두었다. 조건 중 새 문서와 §1 의 관계를 정하는 것이 없다. 조건이 닫혀도 같은 디렉터리에 같은 이름의 장면 둘이 서고 어느 쪽이 정본인지 저장소가 못 답한다 — 이 회차가 없애려던 상태다.

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `A1` · `A2` · `docs/plan/00-goals.md:92-160`

### `A1` 은 문면대로 읽으면 위반이 원리상 안 나온다

*"셋 중 하나라도 빠진 장면은 장면이 아니다"* 가 앞문장의 모집단을 정의한다. 셋이 안 적힌 것은 정의상 모집단에서 빠지고 남은 모집단은 언제나 셋을 다 갖는다. 항등식이다. 그리고 **장면 수의 하한이 `A1` 에 없다.**

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `A1`

### `A1` 은 결정론적으로 잴 수 있는데 「정반합 판정」으로 태그됐다

셋이 적혔나는 형식 검사다. 이 저장소는 이미 그 형태의 검사를 여럿 돌린다. 「결정론적 검증이 가능하면 무조건 시험을 쓴다」가 정본인데 시험을 쓸 수 있는데 정반합으로 넘겼다.

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `A1`

### `A4` 의 음성 대조가 자기가 건 링크 위에 선다 — 항등식이다

순서표를 쓰는 자가 장면 문서로 링크를 걸고, 그 링크가 걸린 뒤에 장면을 지우면 죽은 링크가 뜬다 — **링크를 건 순간 구성상 보장된다.** 재는 것은 「순서표가 장면에 근거한다」가 아니라 「마크다운 링크를 걸었다」다. 관측 절차도 실패 조건도 안 적혔다.

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `A4`

### `B1` 이 표의 존재만 재고, 원문이 요구한 「거리」를 안 잰다

원문은 실측을 **거리의 척도**로 쓰라고 했다. `B1` 은 표가 있다·근거가 붙는다·커밋을 박는다만 요구한다. **「서 있다」가 무엇인지의 정의가 없고**, 그 실측값에서 순서가 도출됐다를 요구하는 조건이 없다. 표 일곱 행이 서고 순서가 그 표와 무관해도 `B1`·`B2`·`B5` 가 전부 닫힌다.

- 모집단: 원의도
- 유효성: 참
- 해악도: 실패
- 어디가 걸리나: `B1`·`B2`·`B5`

### `B3-a` 의 두 문턱이 자기 추정과 어긋나고, 분모 정의가 없다

① 「하네스 표면 넷」이 무엇인지 문면에 정의가 없다. 주석은 후보로 셋만 든다 — 넷째가 없어 **분모가 안 선다**. 주석의 셋을 그대로 쓰면 3/4 로 착수 시점에 이미 「축이 거짓」이다. ② `pal` 상위 명령은 7/18 로 **태어나면서 통과**다. 같은 조건의 두 다리가 같은 추정 위에서 정반대 답을 낸다. 게다가 `docs/gates/inventory-disposal.md:17` 은 17 로 센다 — 분모가 무엇인지도 안 정했다.

- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `B3-a`

### `B2` 에는 「억지로 매단 것」을 잡는 문턱이 없다 — `B3` 과 비대칭이다

`B3` 은 빼는 쪽의 남용을 `B3-a` 로 막는데 `B2` 는 넣는 쪽을 안 막는다. 항목마다 아무 능력이나 붙이면 `B2` 는 언제나 참이다.

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `B2` ↔ `B3`·`B3-a`

### `B4` 가 「이 회차가 새로 연 것」을 허용해 항등식이 된다

원문의 「**지금** 이슈로 서 있다」는 착수 시점의 31 건을 가리키는 독법이 자연스럽다. `B4` 는 두 갈래를 다 허용해서, 뒤 갈래를 타면 첫 항목이 무엇이든 이슈를 열면 통과한다. 종료 조건이 「이슈 하나를 연다」로 줄어든다.

- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `B4`

### `B4-a` 는 한 줄을 지우면 통과한다 — 지형이 섰는지를 안 잰다

그 문자열을 지우기만 하면 참이다. 새 지형 문서를 가리키게 하라고 요구하지 않는다. 「거짓 신호를 지운다」가 아니라 「신호를 지운다」로 닫힌다. 앞의 `AGENTS.md` 두 칸 누락과 합치면 **이 회차의 산출을 가리키는 진입점이 저장소에 하나도 안 남는다.**

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `B4-a`

### `G4` 의 모집단이 착수 시점에 0 이고, 「대신 무엇을 찍나」가 문면에 없다

그런 좌표가 지금 하나도 없다 — `G5` 의 RED 가 그것이다. 0 건을 훑고 참이다. 더 큰 것은 **「안 찍는다」만 말하고 「대신 무엇을 찍나」를 안 정했다**는 것이다. 차선책은 새 사유를 금하고 `corpus/` 는 범위 밖이다. **GREEN 이 무엇인지 모르는 조건이다.**

- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역
- 어디가 걸리나: `G4`

### `G5` 는 완수 조건이 아니라 이미 참인 착수 관측이다

문면이 과거형이고 관측 파일도 이미 있다. 지금 참이고 회차가 끝날 때도 참이다. 뒷문장은 `G1~G3` 의 판정 방법이지 `G5` 자신의 조건이 아니다.

- 모집단: 회차기록
- 유효성: 참
- 해악도: 미관
- 어디가 걸리나: `G5`

### `G6-a` 가 설계 선택 하나로 사라진다

이 회차가 새 감시 항목 타입을 안 만들고 기존 `WatchEntry` 를 재사용하면 — 그것이 `G2-c` 를 지키는 가장 쉬운 길이다 — 모집단이 0 이 되어 vacuous 하게 참이다. 조건이 「**새** 타입」에만 걸려서 설계 선택이 조건을 지운다.

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `G6-a`

### `G1` 이 「결정론적」인데 요구하는 것은 「문면에 있다」다

뒷 절반은 사람이 읽어야 한다. 같은 절의 다른 조건들은 재는 법을 괄호로 달았는데 `G1` 은 없다. 갈래 표시와 실제가 어긋난 조건 위에서 판정하면 「내가 읽었다」가 「결정론적으로 통과」로 기록된다.

- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `G1`

### `G2-a` 의 「22 건」이 이 회차 안에서 낡는다 — `B1` 에 준 방어가 여기엔 없다

`B1` 에는 측정 커밋을 박으라고 달았는데 같은 위험이 `G2-a` 의 「22 건」에도 있고 방어가 없다. `G5` 의 GREEN 이 결박을 늘린다.

- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `G2-a` ↔ `B1`

### `E5-a` 가 하드코딩을 하드코딩으로 고치는 것을 허용한다

이 회차 파일 이름 두 줄을 더하면 통과다. 그러면 **다음 회차가 열리는 순간 같은 죽은 가지가 다시 난다** — 조건 자신이 진단으로 적은 병이다. 조건이 병을 정확히 진단하고 이번 판만 낫는 처방을 합격선으로 삼았다.

- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역
- 어디가 걸리나: `E5-a`

### `E4` 는 제목 넷만 있으면 통과한다

제목 넷을 쓰고 본문을 「없음」으로 채우면 참이다. 이 회차는 금지역 여섯을 지고 `G4` 처럼 GREEN 형태가 미정인 항목을 안는데, 조건이 내용의 하한을 안 준다.

- 모집단: 규약
- 유효성: 추정
- 해악도: 거짓신호
- 어디가 걸리나: `E4`

### `F1`·`F2` 는 어떤 결과가 나와도 통과한다 — 「효과」 절이 효과를 안 잰다

두 조건의 합집합이 결과 공간 전체다. **실패 경로가 없다.** 조회가 **맞는 답을 내는지**를 재는 자리가 F 절에도 G 절에도 없다 — `G3` 은 *"답이 온다"* 이지 *"맞는 답이 온다"* 가 아니다.

- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 어디가 걸리나: `F1`·`F2`·`G3`

### 갈래 표시가 37 중 22 에 없다 — 판정자가 안 정해진 조건이 다수다

22 개는 닫을 때 누가 무엇으로 판정하나가 문면에 없다. 그중 `B2`·`B3-a`·`B4-a`·`G6` 넷은 결정론적으로 잴 수 있는데 표시도 시험도 없다. `A2`·`A3`·`B3`·`B5`·`D1`·`D2`·`G7-a` 일곱은 의미론적인데 검증 방법이 미등록이다.

- 모집단: 규약
- 유효성: 참
- 해악도: 실패
### 조건이 문 좌표 둘이 저장소에서 안 선다

① `## 범위 밖` 이 `scripts/frontier.sh:78` 을 무는데 **그 파일은 63 줄이다.** ② `G3-a` 가 `doctor.rs:200,234,253` 을 무는데 저장소에 `doctor.rs` 가 셋 있고 크레이트 경로가 없다. 이 저장소는 좌표 위생을 검사로 강제하는데 잠긴 의도 자신이 그 규율 밖에 있다.

- 모집단: 저장소
- 유효성: 참
- 해악도: 미관

## 조건의 합이 의도를 덮나

**안 덮인다. 네 대목이다.**

**①** *"산출은 「`docs/plan/` 새 문서 + `AGENTS.md` 두 칸은 가리키기만」."* — 두 칸이 무엇을 가리키게 되는지를 요구하는 조건이 없다.

**②** *"초안은 「정반합으로 초안을 세워 올린다」."* — 돌았다도 올렸다도 조건이 아니다.

**③** *"완수 조건 `H2` 의 문면은 안 고친다."* — 소유자가 명시적으로 금한 것인데 조건에도 범위 밖에도 없다.

**④** *"「소비 가능해지는 지점에 가장 가까운 것」은 「능력마다 지금 무엇이 서 있는지를 실측한다」로 잰다."* — `B1` 이 표의 존재까지만 요구하고 그 실측이 순서를 정했다를 재는 조건이 없다. **이 회차를 고른 근거가 「거리를 잴 자가 없다」인데 조건의 합이 거리 척도를 안 만든다.**

**의도보다 큰 것은 없다.** 원문이 요구하지 않은 것을 요구하는 조건은 못 찾았다.

## 내가 기각한 것

| 제목 | 기각 사유 | 모집단 | 유효성 | 해악도 | 어디가 걸리나 |
|---|---|---|---|---|---|
| `## 범위 밖` 이 sunset 트리거를 잘못 인용했다 | 인용이 글자로는 틀렸고 뜻으로는 맞다 — `아래_전부` 가 재귀로 훑는다. 판정을 안 바꾼다 | 저장소 | 참 | 미관 | `## 범위 밖` 7번째 항 |
| `G2-b` 는 태어나면서 초록이라 아무것도 안 잰다 | 금지역 방어 조건은 성질상 「아무것도 안 하면 참」이다. 그것이 결함이면 금지역 등록 자체가 결함이 된다 | 규약 | 참 | 미관 | `G2-b` |
| `G2-c` 도 설계 선택으로 vacuous 해질 수 있다 | `G6-a` 와 같은 갈래이고 그쪽이 더 강하다. 중복 산출을 안 낸다 | 자기장치 | 추정 | 미관 | `G2-c` |
| `D1` 이 저장소 밖 파일에 걸려 재현이 안 된다 | 조건 자신이 좌표 면제가 필요하다고 적었다. 면제 절차 부재는 조건 설계가 아니라 회차 운영이 진다 | 저장소 | 참 | 미관 | `D1` |
| 「두께를 정한다」가 완수 조건에 없다 | **격리가 깨진 상태에서 `## 두께` 절을 봤다** — 그 자리가 문서에 있다. 받은 여섯 절 밖의 정보로 죽이므로 산출하지 않고 여기 적는다 | 원의도 | 참 | 미관 | `## 완수 조건` |
| `E3`(열린 것 0)이 발견을 억제하는 유인을 만든다 | 같은 사유다 — **격리가 깨진 상태에서 `## 모집단 분리` 절을 봤고** 그 절이 완충을 진다 | 회차기록 | 참 | 미관 | `E3` |

## 형식에 대한 한 줄

`SKILL.md` 는 기각 항도 `### ` 로 쓰라고 하고 코디네이터는 표에 첫 열 `제목` 으로 두라고 했다. **코디네이터의 지시를 따랐다.** 원장에 실을 때 그 갈림을 확인해라.
