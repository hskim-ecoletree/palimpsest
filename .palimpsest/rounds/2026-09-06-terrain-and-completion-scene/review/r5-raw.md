# 독립 리뷰 R5 — 원 반환문

> `pal-independent-reviewer` 가 낸 것을 그대로 보존한다. 요약하지 않는다.
> 받은 것: 잠긴 의도와 산출물. 안 받은 것: 대화 기록 · 앞 라운드의 결과 · 설명.
> 실행 2026-09-07 · HEAD `62e0c98`

---

## 합격선 축

| 조건 | 판정 | 잰 수 | 근거 |
|---|---|---|---|
| A1 | 통과 | 장면 5 · 소제목 결손 0 | `cargo xtask check` → `완성 장면 형식 — 장면 5개 · 소제목 셋 전부 갖춤` |
| A1-a | 통과 | 5 ≥ 하한 3 | 같은 검사 · `xtask/src/main.rs:4481` 의 `장면_하한: usize = 3` |
| A1-a 음성대조 | 통과 | 조건 2/2 발화 | `observations/negative-control-scenes.txt` — 소제목 삭제·번호 삭제 둘 다 FAIL |
| A2 | 통과 | 바깥쪽 2(§1 위임·§2) · 안쪽 2(§3·§4) | `docs/plan/01-completion-scenes.md:34,58,104,143` · 정반합 판 2 R1 |
| A3 | 통과 | P8 장면 1(§5) | `docs/plan/01-completion-scenes.md:177-230` · 정반합 판 2 R1 · 차선책 발동(목표 상태) |
| A4 | 통과 | 링크 1건 실재 · 죽은 링크 0/649 | `docs/plan/02-order.md:4` → `01-completion-scenes.md` · `cargo xtask check` → `문서 488개 · 링크 649건 · 죽은 것 0건` |
| A5 | 통과 | 양쪽 2/2 | `docs/plan/01-completion-scenes.md:6-30` · `docs/plan/00-goals.md:137-143`(둘 다 「00-goals §1 이 정본」) |
| B1 | 통과 | 능력 7행 · 측정커밋 7/7 | `docs/plan/02-order.md:46-54`. 재현: `pal export --format cypher` → `:File` 135 · `:Symbol` 3113 · `REFERENCES` 3(HEAD) · `SymbolKind` 16 |
| B1-a | 통과 | 정의 1절 | `docs/plan/02-order.md:36-42` |
| B2 | 통과 | 항목 6/6 이 능력을 댄다 | `docs/plan/02-order.md:88-95` — `C2`·`C2`·`C6`·`C3`·`C4`·`C5` |
| B2-a | 통과 | 근거 6/6 | 같은 표의 「왜 그 자리인가」 열 |
| B3 | 미측정 | 표 6행 · 자 3종 | 게이트 판정과 같다. `docs/plan/02-order.md:144-157` 이 정의 절 부재를 스스로 적는다 |
| B3-a | 미측정 | 분모 4 · 「없음」 3 = 3/4 · 문턱 미정 | `docs/plan/02-order.md:170-176`. 문턱은 소유자 입력에 매달린다 |
| B4 | 통과 | 1건 | `gh issue view 77` → `OPEN` · `createdAt 2026-08-19T22:58:57Z`; `gh api …/issues/77/timeline` 의 close·reopen 이벤트 **0** → 착수 시점에 열려 있었다 |
| B4-a | 통과 | 첫 항목 1건 · 경고 갈래 2 | `./scripts/frontier.sh` → `#77 … ← 순서표의 1 번` · `scripts/frontier.sh:31-41,80-90` |
| B5 | 반증 | `grep -c '§1'` 원 논증부 → **0** | `awk 'NR>=111 && NR<=116' docs/plan/02-order.md \| grep -c '§1'` → `0`(절 전체는 `3`) |
| B6 | 반증 | 인용 6/6 · 자리를 움직이는 행 1/7 | `docs/plan/02-order.md:88-104` — `C1` 행이 §2 표에서 0회 대어진다 |
| C1 | 통과 | ⏳ 2 → 0 | `git diff 7c41b81..HEAD -- AGENTS.md` — 두 칸이 `02-order.md`·`01-completion-scenes.md` 를 가리킨다 |
| D1 | 통과 | 승계 3 · 이미 있다 5 · 안 승계 3 · 안 본 절 2 처분 | `docs/plan/02-order.md:286-369` |
| D2 | 통과 | 분모 24(파일) / 26(처분 행) · 승계 5 · 지목 3 | `docs/plan/02-order.md:239-284` · `docs/plan/disposal-map.md:66-88`(§1.2 열셋) |
| D3 | 통과 | 1건 삭제 | `git show 7c41b81:NEXT-B-plan-for-harness.md` 존재 · 지금 없음(`3b5762a`) |
| D3-a | 통과 | 모집단 3 · 지움 1 · 남김 2 | `observations/handoff-docs.txt` |
| E1 | 통과 | **27/27 · rc=0** | `cargo xtask check` 전 출력 확인 · 마지막 줄 `검사 27/27 통과` |
| E2 | 통과 | **950 통과 · 0 실패** | `cargo test --workspace` rc=0; `test result: ok` 합산 → passed 950 / failed 0 |
| E3 | 통과 | 열린 행 0 / 데이터 229행 | `findings.jsonl` 파싱 → `상태` 전부 `닫힘` · 검사 「발견이 닫혔나」 초록 |
| E4 | 통과 | 금지 이름 0 · 「원리상 못 잰 것」 3행 | `report.md:55-67` |
| E5 | 통과 | 26+2+0+4 = **32** · 검산 줄 26행 = 판정표 통과 26 (차집합 ∅) | `docs/gates/terrain-and-completion-scene.md:46-55,86-115` |
| E5-a | 통과 | 회차 이름 상수 0 · 종결 문서 **4개** · fail 갈래 2 | `xtask/src/main.rs:4662-4717` · `cargo xtask check` → `파일 247개(종결 문서 4개)` · `observations/negative-control-selector.txt` |
| E6 | 미측정 | 이 회차 커밋에 붙은 런 **0** | `gh run list` 최신 8건이 전부 `7c41b81` 이하 · HEAD `62e0c98` 은 `ahead 44` 로 미푸시 |
| E6-a | 미측정 | 같음 | 같음. 차선책 뒤 갈래(미측정)로 처리됨 |
| E7 | 통과 | `dialectic/` 21파일 · 판 3 · 판 1 은 반–합 2라운드(7파일) | `ls .palimpsest/rounds/…/dialectic/` |
| F1 | 통과 | 출력 1건(테스트·CI 아님) | 게이트 `## 효과` + `observations/effect-frontier.txt` · 재현: `./scripts/frontier.sh` |
| F2 | 통과 | 착수 31 평평 → 지금 첫 항목 표시 1건 | `./scripts/frontier.sh` → `#77 … ← 순서표의 1 번`; 번호는 스크립트에 없고 `02-order.md:208` 에서 읽는다 |

게이트와 갈린 자리 **없음**. 다만 `A4` 근거의 죽은링크 수는 지금과 다르다(아래 자기 산출 3).

## 미측정 목록

| # | 안 잰 조건 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 못 쟀나 |
|---|---|---|---|---|---|---|
| 1 | `B3` — 「소비자다」의 자가 없어 표 여섯 행이 세 자로 재였다 | 원의도 | 참 | 실패 | `docs/plan/02-order.md:144` | 정의 절이 문서에 없고, 그 자의 강도는 소유자만 정한다. 재측정으로 안 나온다 |
| 2 | `B3-a` — 문턱 | 원의도 | 참 | 실패 | `docs/plan/02-order.md:170` | 비율(3/4)은 잤으나 눈금 넷(0·25·50·75)이 선택을 못 낳는다. 소유자 한 칸이 필요하다 |
| 3 | `E6` — 마지막 산출 커밋의 `conclusion=success` | 원의도 | 참 | 거짓신호 | `docs/gates/terrain-and-completion-scene.md:121` | 브랜치가 `ahead 44` 로 미푸시라 이 회차 커밋에 붙은 런이 0 이다 |
| 4 | `E6-a` — 그 런이 이 회차 커밋에 대해 도는지 | 원의도 | 참 | 거짓신호 | `docs/gates/terrain-and-completion-scene.md:122` | 같음. push 뒤에만 잰다 |

## 의도 축

### 빠진 것

없음 — 원문이 요구한 것 중 산출에 없는 것은 결박 대상 확장뿐이고, 그것은 승격 2(소유자 승인)로 이슈 #119 로 분할됐다.

### 요구되지 않은 것

없음 — 착수 커밋 이후 만져진 12 파일이 전부 조건 또는 규약 §11 조건 4 에 걸린다(`docs/plan/README.md`·`disposal-map.md` 둘은 앞 라운드가 이미 수용·기각으로 처리했다).

### 있는데 틀린 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | 실측표 「못 잰 것」의 `narrative.unbound` 행이 *"이미 결함으로 등록된 자리라 더 안 팠다"* 로 안 잰 것을 면제하는데 **그 등록이 없다.** 유일한 등록처인 `docs/gates/F12.md:390` 이 그것을 「F10 후속 [#63]」으로 넘기는데 `#63` 은 「F09 ⑤ 표본기가 코퍼스당 10 으로 자른다」로 전혀 다른 일이다. 잔여가 경계의 외양을 입은 세 번째 자리다(앞 둘은 R3 Kotlin · R4 규모 선형성) | 원의도 | 참 | 금지역 | `B1` | `docs/plan/02-order.md:80` | `./target/debug/pal query narrative.unbound` → `Error: 개체를 남기지 못했다 / 의도 저장소 트랜잭션이 실패했다: 읽기로 연 의도 저장소에 쓰려 했다`; `gh issue list --state all --limit 300 --jq '… test("narrative\\.unbound")'` → `#69`·`#60` 둘뿐이고 **둘 다 이 실패를 안 진다**(`#60` 은 CLOSED · 약한 신호 후보 수); `grep -n '^\[#63\]' docs/gates/F12.md` → `421:…/issues/63`; `gh issue view 63` → `F09 ⑤ 재측정 — 표본기가 코퍼스당 10 으로 자른다` |

## 이번 라운드의 새 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | `docs/plan/README.md` §1 이 **같은 절 안에서 자기를 부정한다** — `:35` 가 *"새 지형은 순서표가 졌다(2026-09-06)"* 인데 아홉 줄 뒤 `:44` 가 *"새 지형이 아직 안 섰으므로 나머지 아홉은 전부 「고칠 것」이다"* 로 남았다. 이 회차가 이 파일을 고치면서 앞 문단만 고쳤다 — R4 의 `AGENTS.md` 「둘/넷」과 같은 형태다 | 저장소 | 참 | 거짓신호 | 없음 | `docs/plan/README.md:35` · `docs/plan/README.md:44` | `grep -n "새 지형" docs/plan/README.md` → `35:**새 지형은 [순서표](02-order.md)가 졌다**(2026-09-06)` · `44:새 지형이 아직 안 섰으므로 나머지 아홉은 전부 …` · `55:새 우선순위는 새 지형이 정한다` |
| 2 | `AGENTS.md:98` 이 가리키는 살아 있는 진입점 문서 `docs/agents/issue-tracker.md` 가 **두 자리에서 「새 지형은 다음 회차가 쓴다」로 남았다.** 같은 문장을 진 `disposal-map.md`·`plan/README.md` 는 R1·R2 가 고쳤고 이 셋째 자리만 안 고쳤다. 그 표의 「무엇을 어느 순서로 → GitHub 이슈」 행은 `AGENTS.md:39`(무엇을 어느 순서로 → `02-order.md`)와 정면으로 갈린다 | 저장소 | 참 | 거짓신호 | 없음 | `docs/agents/issue-tracker.md:79` · `docs/agents/issue-tracker.md:98` | `grep -n "새 지형은 다음 회차가 쓴다" docs/agents/issue-tracker.md` → `79:…`·`98:…`; `grep -n "docs/agents/issue-tracker" AGENTS.md` → `98:… [docs/agents/issue-tracker.md](docs/agents/issue-tracker.md)` |

## 자기 산출에 대한 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | 잠긴 의도 32 조건 중 **14 에 갈래 표시(결정론적·정반합 판정)가 없다**. 그중 일곱(`A5`·`B1`·`B1-a`·`B2`·`B2-a`·`D3-a`·`E4`·`F1`)이 「통과」로 판정됐다 — 판정자가 등록되지 않은 통과다. 이 결함을 낸 레코드 `CA1-22`(유효성 참 · 해악도 실패)는 **「정정·닫힘」**인데 개정 43 은 여섯에만 표시를 달았다 | 회차기록 | 참 | 거짓신호 | 없음 | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/intent.md:134` · `findings.jsonl:71` | 파이썬으로 체크박스 32 를 파싱해 본문에서 `결정론적`·`정반합 판정` 을 찾음 → `갈래 있음 18 · 갈래 없음 14 [A5, B1, B1-a, B2, B2-a, B3, B3-a, D3-a, E1, E3, E4, E6, E6-a, F1]`; `CA1-22` → `"해악도":"실패","처분":"정정","상태":"닫힘","닫은커밋":"0b4a88e"` |
| 2 | 게이트 `## 효과` 의 `pal touch` 블록이 *"위는 실행 출력을 그대로 붙인 것이다"* 라고 적는데 **잘려 있다** — `■ 이 좌표를 지켜보는 것 (0)` 다음 줄 `아직 없습니다.` 와 뒤의 상자 다섯(`이 심볼이 하는 것`·`내가 모르는 것`·`효과`·`판정`·`이 답의 근거`)이 없고 생략 표시도 없다. 같은 문서의 `frontier.sh` 블록은 `…` 로 잘림을 표시한다 | 회차기록 | 참 | 거짓신호 | `F1` | `docs/gates/terrain-and-completion-scene.md:166-178` | `./target/debug/pal touch check_completion_scenes` → 인용된 다섯 줄 뒤에 `  아직 없습니다.` 와 상자 다섯이 더 나온다(머리 두 줄과 결박 줄은 바이트가 일치) |
| 3 | 게이트 `A4` 검산 줄이 죽은 링크 검사 산출을 `문서 480개 · 링크 629건` 으로 적는데 지금은 `문서 488개 · 링크 649건` 이다. 판정(죽은 것 0)은 안 바뀐다 | 회차기록 | 참 | 거짓신호 | `A4` | `docs/gates/terrain-and-completion-scene.md:94` | `cargo xtask check` → `ok  죽은 링크 부재  — 문서 488개 · 링크 649건 · 죽은 것 0건` |

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 아니었나 |
|---|---|---|---|---|---|---|
| 1 | 순서표 §5.1 이 *"그중 **둘**은 §2 의 빈자리를 지목한다"* 라 적고 **세 행**짜리 표를 낸다 | 원의도 | 거짓 | 미관 | `docs/plan/02-order.md:254` | 「그중」의 지시 대상이 바로 앞 문장의 **§1.1 의 열하나**이고, `F02`·`F09` 만 그 열하나 소속이다. `F23` 은 그 표 안에서 *"§1.2 에 있고"* 라고 스스로 소속을 밝힌다. 게이트(셋)·처분표(F23 하나)의 셈과도 어긋나지 않는다 |
| 2 | 완성 장면 §2 와 `AGENTS.md` 가 *"상자 일곱 중 넷이 「능력 없음」을 찍는다"* 로 적는데 실제로 「능력 없음」을 찍는 상자는 셋이다 | 원의도 | 거짓 | 미관 | `docs/plan/01-completion-scenes.md:97` | 실제로 돌려 보니 `pal touch` 의 상자는 정확히 **일곱**(걸린 것·지켜보는 것·하는 것·내가 모르는 것·효과·판정·근거)이고, 능력 공백을 알리는 자리도 **넷**이다 — 셋은 `이 빌드에는 … 능력이 없습니다`, 넷째는 `이 심볼이 하는 것` 안의 `호출자 0 … F07 미구축`. 이름표 하나가 상자 대신 줄을 댄 것뿐이다 |
| 3 | 잠긴 의도 개정 46 이 결박을 `22 → 24` 로 적는데 지금 `bindings.jsonl` 은 **25** 다 | 회차기록 | 거짓 | 미관 | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/intent.md:118` | 개정 46 은 독립 리뷰 R3 시점의 관측 기록이고, 그 뒤 R4 가 `이_회차_종결문서` 에 다시 걸어 하나가 늘었다. `report.md:75` 가 `결박 25건` 으로 지금 값을 적는다. 시점이 다른 것이지 갈린 것이 아니다 |
| 4 | `E5-a` 가 「회차 이름을 손으로 안 적는다」인데 `docs/gates/README.md` 에 하한 `2026-09-06` 을 손으로 적는다 | 자기장치 | 거짓 | 미관 | `docs/gates/README.md:158` | 날짜는 회차 이름이 아니고, 그 선언 자체가 「선언 목록이 닫혀 있나」 검사의 검증 목록에 들어 있다(`xtask/src/main.rs:5063`). 하한이 비면 `이_회차_종결문서` 가 `bail!` 한다 |
| 5 | 「어색한 표현 교정 적용」 선언에 항목이 0 개라 `check_declared_lists` 가 0 개를 훑고 초록을 낸다 | 자기장치 | 거짓 | 미관 | `xtask/src/main.rs:5052-5080` | 그 목록은 방향이 반대인 **적용** 목록이라 항목이 비는 것이 정상이고, 검사가 실제로 지는 것은 **선언과 하한의 존재**다(`선언_목록(root, 제목, 빈항목_허용)?`). 하한 한 줄을 지운 음성 대조가 `FAIL 어색한 표현 부재` 를 냈다(`observations/negative-control-selector.txt`) |

## 끝내도 되는가

**안 된다** — 본 목록에 금지역 1(`02-order.md:80` 의 `narrative.unbound` 등록 주장)이 남았고, 합격선 축에 미측정 4(`B3`·`B3-a`·`E6`·`E6-a`)와 반증 2 가 있으며 `E6`·`E6-a` 는 push 전에는 원리상 못 닫는다.