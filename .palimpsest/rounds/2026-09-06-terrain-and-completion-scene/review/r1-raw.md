# 독립 리뷰 R1 — 원 반환문

> `pal-independent-reviewer` 가 낸 것을 그대로 보존한다. 요약하지 않는다.
> 받은 것: 잠긴 의도와 산출물. 안 받은 것: 대화 기록 · 앞 라운드의 결과 · 왜 그렇게 만들었는지의 설명.
> 실행 2026-09-07 · HEAD `5e673e7`
>
> ⚠ 메인이 고친 글자 하나 — 인용문 안의 마크다운 링크를 링크가 아닌 표기로 바꿨다.
> 이 자리에서는 그 상대 경로가 안 풀려 죽은 링크 검사가 빨개진다(이슈 #87 이 그 갈래다).

---

# 독립 리뷰 R1 — 회차 `2026-09-06-terrain-and-completion-scene`

금지역 출처: `.claude/pal/policy.toml` **없음**(`ls .claude/pal/` 빈 출력) → **잠긴 의도 `## 금지역`**(`intent.md:105-111`)이 정본이다. 다섯 — 데이터 손실 · **측정이 죽은 가지** · **사실이 아닌 것을 사실로** · 인증 · 대표 취약점.

모집단 판정 근거: `git log --diff-filter=A 7c41b81..HEAD --name-only` 와 `git log -S` 로 확인했다. `.claude/` 는 이 회차가 한 줄도 안 고쳤다(`git diff --stat 7c41b81..HEAD`).

## 합격선 축

| 조건 | 판정 | 잰 수 | 근거 |
|---|---|---|---|
| A1 | 통과 | 장면 5 · 위반 0 | `cargo xtask check` → `완성 장면 형식 — 장면 5개 · 소제목 셋 전부 갖춤` |
| A1-a | 통과 | 5 ≥ 하한 3 | `xtask/src/main.rs:4482` `장면_하한: usize = 3` · 같은 출력 |
| A2 | 통과 ⟨정반합 판2 R1⟩ | 바깥쪽 2 · 안쪽 2 | 문면(「안쪽과 바깥쪽을 둘 다」)을 실제로 잰다 — §1·§2 바깥쪽, §3·§4 안쪽이 각각 자기 절로 있다 |
| A3 | 통과 ⟨정반합 판2 R1⟩ | 장면 1(§5) | `01-completion-scenes.md:169-221` 이 `P8` 장면 하나를 진다. 문면을 잰다 |
| A4 | 통과 | 링크 641 · 죽은 것 0 | `02-order.md:4,362` 가 파일 링크 · `죽은 링크 부재 — 문서 482개 · 링크 641건 · 죽은 것 0건` |
| A5 | 통과 | 양쪽 2/2 | `01-completion-scenes.md:8,29` · `00-goals.md:137-139` 둘 다 「§1 이 정본」을 적는다 |
| B1 | 통과 | 능력 7행 · 측정 커밋 7/7 | `02-order.md:46-54` — C1~C6·U 일곱, 근거 열에 명령·출력, 행마다 `19b5877` |
| B1-a | 통과 | 정의 1절 | `02-order.md:36-42` 「「서 있다」가 무엇인가」 |
| B2 | 통과 | 6/6 항목이 능력을 댄다 | `02-order.md:90-95` → `C2`·`C2`·`C6`·`C3`·`C4`·`C5` |
| B2-a | 통과 | 6/6 근거 줄 | 같은 표 4열 「왜 그 자리인가 — §1 의 어느 행」 |
| B3 | **미측정**(정반합 교착) | 정의 절 0 | `02-order.md:144-157` 이 스스로 「정의 절이 없다」를 적는다. 게이트와 일치 |
| B3-a | **미측정**(교착·문턱 미정) | 분모 4 · 「없음」 3 · 비율 3/4 · 문턱 미정 | `02-order.md:170-173`. 분모 넷은 `ADR-0025:62-67` 이 못 박은 표면 넷 — 재현 확인 |
| B4 | 통과 | 1/1 | `gh issue view 77` → `state OPEN` · `createdAt 2026-08-19T22:58:57Z`(착수 `7c41b81` 이전) |
| B4-a | 통과 | 첫 항목 1건 | `scripts/frontier.sh:38-45` 가 `02-order.md` 를 읽는다. 실행 → `#77 … ← 순서표의 1 번` |
| B5 | **반증** ⟨정반합 판3⟩ | `grep -c '§1'` → 0 · 무제약 인접쌍 2 | 문면(「근거가 비대칭인지 잰다」)을 실제로 잰 판정이다. 3↔4·5↔6 을 금하는 것이 없다 |
| B6 | **반증** ⟨정반합 판3⟩ | 인용 6 · 고유 행 5 · 미인용 행 2(`C1`·`U`) · 도출 출처 3 | 문면 앞머리(「순서가 `B1` 의 실측에서 도출됐다」)를 잰다. 뒷절(「행을 대는지 적힌다」)은 참이라 조건 안에서 두 반쪽이 갈린다 |
| C1 | 통과 | ⏳ 0건 | `grep -n '⏳' AGENTS.md` → 0. 착수 시점 `git show 7c41b81:AGENTS.md` 는 두 칸 다 ⏳ 였다 |
| D1 | 통과 ⟨정반합⟩ | 인용 3/3 일치 | `GOAL.md:226`·`:129-136` 원문 대조 일치 · `IMPLEMENTATION-ORCHESTRATION.md` 691줄 일치 |
| D2 | 통과 ⟨정반합⟩ | 승계 5 · 지목 3 | `02-order.md:231-273`. ⚠ 분모 산술이 어긋난다 — 아래 「있는데 틀린 것 1」 |
| D3 | 통과 | 1/1 | `git show 7c41b81:NEXT-B-plan-for-harness.md` 존재 · HEAD 부재(삭제 커밋 `3b5762a`) |
| D3-a | 통과 | 모집단 3 · 붙드는 행 3/0 | `observations/handoff-docs.txt` 의 수를 재현했다(`NEXT-E-prompt` 3행 · `check-verifies` 0행) |
| E1 | 통과 | 27/27 · rc=0 | `cargo xtask check` 전 출력 확인 |
| E2 | 통과 | 950 통과 · 0 실패 | `cargo test --workspace` rc=0. **전 출력**에 `error[E`·`panicked`·`FAILED` **0건** |
| E3 | 통과 | 원장 150행(머리1+149) · 열림 0 | 검사 「발견이 닫혔나」 — `잰 것 917행 · 발화 0 · 열림 0행` |
| E4 | 통과 | 금지 제목 0 · 「원리상 못 잰 것」 4행 | `report.md:48-55`. ⚠ 그중 1행의 분류는 아래 「자기 산출 3」 |
| E5 | 통과 | 26+2+0+4 = 32 | 게이트 `:40-45` · 상자 `[x]`28 / `[ ]`4 와 양방향 일치. 검사 「원장 둘 대조」 통과 |
| E5-a | 통과(문면) | 회차 이름 상수 0 | `xtask/src/main.rs:4656-4688` 에 슬러그 리터럴 없음. ⚠ **대상이 이 회차가 아니다** — 아래 「새 발견 1」 |
| E6 | **미측정** | 이 회차 커밋의 런 **0건** | `gh run list` 최신 3건 전부 `headSha=7c41b81`(착수 커밋). `git status -sb` → `ahead 33` (미push) |
| E6-a | **미측정** | 같음 | 같은 근거. 차선책(「새 PR」)을 쓴 기록이 어디에도 없다 |
| E7 | 통과 | `dialectic/` 21 파일 · 판 3 | 판마다 설계·정·반·합·종료판단 + 취합 3. 판 1 은 반–합 2 라운드 |
| F1 | 통과 | 실행 1회 재현 | `./scripts/frontier.sh` 직접 실행 → `#77 … ← 순서표의 1 번` · 마지막 줄이 두 문서를 가리킴. 테스트도 CI 도 아니다 |
| F2 | 통과 | 착수 31 평평 → 지금 표시 1건 | 착수 관측(31/31 평평)과 지금 출력이 다르다. 번호는 스크립트에 없고 `02-order.md:208` 에서 읽는다 |

게이트가 적은 판정과 **갈리는 것은 없다.**

## 미측정 목록

| # | 안 잰 조건 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 못 쟀나 |
|---|---|---|---|---|---|---|
| 1 | `B3` — 하네스 장치가 일곱의 소비자로 매달리나 | 원의도 | 참 | 거짓신호 | `docs/plan/02-order.md:144` | 「소비자다」의 정의 절이 없어 여섯 행이 세 자로 재였다. 정반합 판 1 이 교착으로 판정했고 소유자 답 없이 안 닫힌다 |
| 2 | `B3-a` — 예외의 분모와 문턱을 실측으로 정한다 | 원의도 | 참 | 거짓신호 | `docs/plan/02-order.md:170` | 분모 넷은 섰으나 눈금(0·25·50·75)이 선택을 못 낳는다. 문턱은 소유자가 축 문장의 읽기 강도를 정해야 선다 |
| 3 | `E6` — 마지막 산출 커밋에 `conclusion=success` 런 | 원의도 | 참 | 거짓신호 | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/intent.md:179` | 회차 33 커밋이 미push 라 런이 애초에 없다(`gh run list` 최신 `headSha`=착수 커밋) |
| 4 | `E6-a` — 그 런이 이 회차 커밋에 대해 도는가 | 원의도 | 참 | 거짓신호 | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/intent.md:180` | 같음. push 뒤에만 잴 수 있다 |

## 의도 축

### 빠진 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | **이 회차의 결정이 어디에도 결박되지 않았다.** `/round` §11 조건 4(*"이 회차의 결정과 의도를 결박하고 그래프를 갱신했다"*)와 `ADR-0025`(*"결박이 회차를 닫는다 … 판정이 문서로만 남으면 그것은 낡을 뿐 낡음이 표시되지 않는다"*)가 요구한다. `state.md:33` 도 「남은 것」에 결박을 적는다 | 회차기록 | 참 | 실패 | 어느 조건에도 안 걸린다 (`G` 절 축소로 결박 조건이 사라졌다) | `.palimpsest/intent/bindings.jsonl:1` | `wc -l .palimpsest/intent/bindings.jsonl` → `23` — 착수 관측(`intent.md:82`)의 23 과 **같다**. `git log 7c41b81..HEAD -- .palimpsest/intent/` → 커밋 0건. `grep -c check_completion_scenes .palimpsest/intent/bindings.jsonl` → `0` |

- 어디가 걸리나: `.palimpsest/intent/bindings.jsonl:1`
- 모집단: 회차기록
- 유효성: 참
- 해악도: 실패

★ **이 회차에서 할 수 있었는가 — 있었다.** 격리 사본에서 실제로 걸어 봤다:

```
$ pal bind "check_completion_scenes" --note "…" --radius symbol
결박했습니다.
  대상    check_completion_scenes · fun · xtask/src/main.rs:4534
  개체    decision/01M1VW4CD2WQAKN90W90AQB4V2
```

### 요구되지 않은 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | `docs/plan/README.md` 를 16 줄 고쳤다. 원문은 산출을 *"`docs/plan/` 새 문서 + `AGENTS.md` 두 칸은 가리키기만"* 으로 적었고(`intent.md:31`), 색인 개정을 요구한 조건은 없다. 낡은 「지형이 비었다」를 없앤 것이라 해는 없다 — 다만 **같은 낡음을 진 `disposal-map.md:8` 은 안 고쳐서 처리가 갈렸다** | 원의도 | 참 | 미관 | 어느 조건에도 안 걸린다 | `docs/plan/README.md:3` | `git diff --stat 7c41b81..HEAD` → `docs/plan/README.md \| 16 +-` |

- 어디가 걸리나: `docs/plan/README.md:3`
- 모집단: 원의도
- 유효성: 참
- 해악도: 미관

### 있는데 틀린 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | **`§5.1` 의 분모 산술이 한 문장 안에서 어긋난다.** 그 줄이 *"처분표에는 능력 대응 표가 **둘**(§1.1 판정이 커밋된 **열셋** · §1.2 아직 안 만든 **열셋**)이고, 이 절의 제목이 가리키는 **스물넷**은 둘을 합친 것이다"* 라고 적는데 **13 + 13 = 26** 이다. 실제로 §5.1 이 세는 24 는 §1.1 에서 **열하나**만 취한 값(11+13)이고, 조용히 빠진 둘은 `P0`·`F06b` 다. 게이트가 `D2` 의 통과 근거로 *"§5.1 이 분모를 스물넷으로 되돌리고"* 를 대므로 통과 근거가 거짓 항등식 위에 얹힌다 | 원의도 | 참 | 거짓신호 | `D2` | `docs/plan/02-order.md:239` | `sed -n '/^### 1\.1/,/^### 1\.2/p' docs/plan/disposal-map.md` → 표 행 **13**(`P0`·`F01`~`F22`) · `### 1.1 … (13)` · `### 1.2 … (13)` · `disposal-map.md:27-28` *"두 표의 행 수 합이 26"* |

- 어디가 걸리나: `docs/plan/02-order.md:239`
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호

## 이번 라운드의 새 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | **`E5-a` 가 세운 선택자가 이 회차의 종결 문서를 안 잰다 — 죽은 가지가 그대로 났다.** `이_회차_종결문서` 는 회차 이름을 **사전순 최대**로 고르는데, 같은 날짜의 형제 회차 `2026-09-06-user-surface-vocabulary` 가 `…-terrain-and-completion-scene` 보다 뒤라 **끝난 지 14 시간 지난 옛 회차**가 뽑힌다. 그래서 이 회차의 `report.md` 와 게이트는 「어색한 표현 부재」 대상 밖이다. 함수의 주석이 *"**여기는 그 위험이 없다** — 종료 보고가 **있는** 것만 고르므로 아직 안 끝난 회차는 애초에 후보가 아니다"* 라고 적는데, 이 회차는 이미 `report.md` 를 가졌으므로 후보이고 그럼에도 진다 — **위험 없음 선언이 사실이 아니다** | 자기장치 | **참** | **금지역** (측정이 죽은 가지 · 사실이 아닌 것을 사실로) | `E5-a` | `xtask/src/main.rs:4666` | 격리 사본에 셋을 심고 돌렸다 — 두 회차의 `report.md` 와 이 회차 게이트. 산출: `소유자가 지목한 표현 패턴이 1곳 남았다: .palimpsest/rounds/2026-09-06-user-surface-vocabulary/report.md:162` — **이 회차 것 둘은 안 잡혔다.** 커밋 시각: terrain `report.md` `Mon Sep 7 02:12:36`, user-surface `Sun Sep 6 11:42:38` |
| 2 | **종료 보고가 하지 않은 결박을 「걸었다」로 적는다.** `report.md:63` 이 *"코드 심볼(`check_completion_scenes`)에 걸었다"* 로 완료형으로 쓴다. 결박은 존재하지 않는다. 같은 회차의 `state.md:33` 은 결박을 「남은 것」으로 적어 **두 산출이 서로 모순**이다 | 회차기록 | **참** | **금지역** (사실이 아닌 것을 사실로) | 어느 조건에도 안 걸린다 | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/report.md:63` | `grep -c "check_completion_scenes" .palimpsest/intent/bindings.jsonl` → `0` · `wc -l` → `23`(착수와 동일) · `git log 7c41b81..HEAD -- .palimpsest/intent/` → 빈 출력 |
| 3 | **`disposal-map.md` 가 새 지형을 아직 미래형으로 가리킨다.** *"무엇을 어느 순서로 만드는지는 「`README.md`」 가 지고, 그 새 지형은 병렬 회차 B 가 쓴다"*. 그런데 ⓐ `README.md:3` 은 이제 *"지형은 [순서표]와 [완성 장면]이 진다"* 로 소유를 넘겼고 ⓑ 이 회차의 산출 `01-completion-scenes.md:217` 이 *"**그 `B` 가 이 회차이고**"* 라고 스스로 선언한다. 처분표를 먼저 읽는 자는 지형 소유자와 시점을 둘 다 틀리게 읽는다. 같은 문서 `:120` 의 `B` 처분 **35 건**도 「병렬 회차 B 가 다시 정한다」로 남아 있는데 이 회차는 그 35 를 안 다뤘고 종료 보고가 그것을 잔여로 안 세운다 | 저장소 | 참 | 거짓신호 | 어느 조건에도 안 걸린다 (`D2` 인접) | `docs/plan/disposal-map.md:8` | `git log --oneline 7c41b81..HEAD -- docs/plan/disposal-map.md` → 빈 출력(이 회차가 안 만졌다). `grep -rn "병렬 회차" --include="*.md" .` |
| 4 | **`check_ledger_pair` 의 「최근 끝난 회차」도 사전순이라 사실과 다르다.** 검사가 `최근 끝난 회차 \`2026-09-06-user-surface-vocabulary\` 가 검사에 들었다` 를 산출하는데, 실제로 가장 최근에 끝난 회차는 `…-terrain-and-completion-scene` 이다. 이 검사에서는 두 회차가 모두 대상이라 판정을 안 뒤집지만, 이 회차의 새 선택자가 **바로 이 코드를 본떴다**(커밋 `30d0152` 메시지: *"「원장 둘 대조」가 이미 쓰는 선택자를 그대로 쓴다"*) | 저장소 | 참 | 거짓신호 | 어느 조건에도 안 걸린다 | `xtask/src/main.rs:6133` | `cargo xtask check` 전 출력 · `git log -1 --format=%ad -- <각 report.md>` |

- 어디가 걸리나: `xtask/src/main.rs:4666`
- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역

⚠ 발견 1 은 잠긴 의도가 **미리 등록한 예외**에 정확히 걸린다 — `intent.md:239`: *"`E5-a` 만 자기 장치이고, 그것에 대한 발견은 **금지역에 닿을 때만 본 모집단**이다."*

- 어디가 걸리나: `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/report.md:63`
- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역

## 자기 산출에 대한 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | 게이트 `## 차선책` 이 *"`E6-a` 의 차선책 … **아래 `E6` 행을 보라**"* 로 가리키는데 **그 행이 문서에 없다.** `grep -n "E6" docs/gates/terrain-and-completion-scene.md` 가 내는 것은 그 포인터 두 줄과 판정 표 한 줄뿐이다. 결과로 **미측정 넷 중 둘(`E6`·`E6-a`)만 사유가 하나도 없다** — `B3`·`B3-a` 는 전용 절을 받았다. 차선책(*"새 PR 을 연다. 그것도 안 되면 미측정"*)의 앞 갈래를 왜 안 탔는지도 기록이 없다 | 회차기록 | 참 | 거짓신호 | `E6`·`E6-a` | `docs/gates/terrain-and-completion-scene.md:34` | `grep -n "E6" docs/gates/terrain-and-completion-scene.md` → `33`·`34`·`43` 세 줄뿐 |
| 2 | `state.md` 가 이 회차의 실제 진행보다 뒤에 멈춰 있다. `:8` 은 *"독립 리뷰와 판정 기록이 남았다"*, `:32-33` 은 「남은 것」에 **완수 조건 32 판정 · 게이트 · 종료 보고**를 적는데 셋 다 커밋 `0fa3851` 로 이미 섰다. 잠긴 의도 `:4` 가 *"상태는 `state.md` 가 진다"* 로 못 박았으므로 상태의 정본이 낡았다 | 회차기록 | 참 | 거짓신호 | 어느 조건에도 안 걸린다 | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/state.md:33` | `git log -1 --format=%h -- …/state.md` → `72a2ecb` · 게이트·보고는 `0fa3851`(뒤) |
| 3 | 종료 보고 `## 원리상 못 잰 것` 셋째 행(`pal symbols --graph` 의 `contains 8` — *"근거 칸이 어느 PATH 인지 안 적었다"*)은 **경계가 아니라 잔여다.** `/round` §10 의 가르는 문장은 *"이 회차에서 그것을 할 수 있었는가"* 이고, `pal symbols --graph <PATH>` 를 아무 경로로나 한 번 돌려 그 칸을 채우는 것은 이 회차에서 가능했다(바이너리가 서 있고 `--help` 가 인자를 명시한다). 원리상 못 되짚는 것은 **옛 실행 하나**이지 그 능력의 측정이 아니다 | 회차기록 | 참 | 거짓신호 | `E4` | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/report.md:54` | `./target/debug/pal symbols --help` → `Usage: pal symbols [OPTIONS] <PATH>` · `--graph` 존재 |

- 어디가 걸리나: `docs/gates/terrain-and-completion-scene.md:34`
- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 아니었나 |
|---|---|---|---|---|---|---|
| 1 | 「`E5-a` 는 **반증**이어야 한다 — 검사가 이 회차의 종결 문서를 안 잰다」 | 자기장치 | 거짓 | 미관 | `.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/intent.md:176` | 개정 35 뒤의 문면은 *"대상이 회차 이름을 손으로 적지 않고 정해진다"* 뿐이다. `xtask/src/main.rs:4656-4688` 에 슬러그 리터럴이 0 개이므로 **문면대로 통과**다. 죽은 가지는 조건 반증이 아니라 별개 발견이라 그쪽으로 옮겼다 |
| 2 | 「게이트 `A4` 행이 `문서 480개 · 링크 629건` 을 적는데 지금은 482·641 이라 낡았다」 | 회차기록 | 거짓 | 미관 | `docs/gates/terrain-and-completion-scene.md:84` | 게이트는 **판정 시점의 관측**을 적는 문서다. 그 뒤 커밋 `5e673e7` 이 파일 셋을 더해 수가 움직인 것이라 낡음이 아니라 시점 차이다. `A4` 의 문면(「죽은 것 0」)은 지금도 참이다 |
| 3 | 「`B4` 가 반증이다 — `#77` 이 §2 의 1 번과 다른 축이라 「첫 항목」이 실측 순서를 안 진다」 | 원의도 | 거짓 | 미관 | `docs/plan/02-order.md:208` | `B4` 의 문면은 *"첫 항목이 착수 시점의 열린 이슈 31 건 중 하나다"* 뿐이고 `gh issue view 77` 이 그것을 만족한다. 축 어긋남은 이미 이슈 `#123` 과 「교착이 남긴 것」 3 번으로 서 있고 `B4` 의 대상이 아니다 |
| 4 | 「`cargo test --workspace` 가 `running 0 tests` 를 찍으므로 `E2` 는 0 개를 훑고 초록이다」 | 저장소 | 거짓 | 미관 | 없음 | 전 출력의 `test result:` 줄을 전부 더하면 **950 통과 · 0 실패**다. `running 0 tests` 는 doc-test 등 빈 타깃의 한 줄이고 모집단 전체가 아니었다 |
| 5 | 「`frontier.sh` 가 `02-order.md` 를 정규식으로만 잡으므로 문서 이름이 바뀌면 조용히 멎는다」 | 원의도 | 거짓 | 미관 | `scripts/frontier.sh:44` | 실제로 멎지 않는다 — `first` 가 비면 `⚠ 순서표가 첫 항목을 안 댄다` 를 찍는다(`:110-112`). 효과 관측이 그 음성 대조를 이미 돌렸다 |

## 끝내도 되는가

**안 된다** — 본 목록에 **금지역 둘**(새 발견 1·2)이 남았다. 하나는 `E5-a` 가 세운 선택자가 이 회차의 종결 문서를 원리상 영영 안 재는 죽은 가지이고(장래 회차 슬러그가 전부 사전순으로 뒤에 서므로 회복되지 않는다), 다른 하나는 종료 보고가 존재하지 않는 결박을 완료형으로 적은 것이다. 뒤의 것은 `state.md` 자신과 모순이고 격리 사본에서 **실제로 걸릴 수 있음**을 확인했으므로 잔여다.

주요 좌표:
- `/Users/incognito/dev/projects/palimpsest/xtask/src/main.rs:4666`
- `/Users/incognito/dev/projects/palimpsest/.palimpsest/rounds/2026-09-06-terrain-and-completion-scene/report.md:63`
- `/Users/incognito/dev/projects/palimpsest/docs/plan/02-order.md:239`
- `/Users/incognito/dev/projects/palimpsest/docs/plan/disposal-map.md:8`
- `/Users/incognito/dev/projects/palimpsest/docs/gates/terrain-and-completion-scene.md:34`
