효과 관측 — frontier.sh 가 순서표를 읽어 줄 세운다
커밋 59801f7 뒤 · 2026-09-06

== 착수 시점 (7c41b81) ==
  ⚠ 아래 세 줄은 바이트 그대로가 아니라 옮겨 적은 것이다 (독립 리뷰 R3 이 잡았다).
    착수 커밋의 스크립트는 마지막 줄을 "(지형은 ⏳ 비었다 — …)" 로 찍는다 — ⏳ 가 빠졌다.
    수(31 · 31 · 0)는 잠긴 의도의 착수 시점 관측과 같으므로 판정은 안 바뀐다.
    원문은 `git show 7c41b81:scripts/frontier.sh` 로 언제든 되짚는다.
  열린 이슈 31건 · 착수 가능 31건 · 교착 0건
  착수는 gh issue edit <번호> --add-assignee @me   (지형은 비었다 — docs/plan/disposal-map.md)
  → 31 건이 전부 평평했다. 무엇이 먼저인지 저장소가 답하지 않았다.

== 회차 중간 (2026-09-06 · 순서표가 #77 을 대던 때) ==
── 지금 착수 가능 ──────────────────────────────
  #77  I1 — Rust 정규화·요약 축 — 속성 변경이 낡음을 안 켠다 · `is_leading_separator` 는 죽은 가지다  ← 순서표의 1 번
  #121  잔여를 착수 시점에 선언한다 — 종료 시점에 적으면 변명이 된다
  #120  완료 상태를 둘로 가른다 — 기계 통과를 완료라고 보고하지 않는다
  ...
열린 이슈 34건 · 착수 가능 34건 · 교착 0건
착수는 gh issue edit <번호> --add-assignee @me   (순서는 docs/plan/02-order.md · 완성 장면은 docs/plan/01-completion-scenes.md)

== 지금 (2026-09-07 · 소유자 갈래 ㉢ 뒤) ==
  소유자가 §2 의 1 번을 지는 이슈를 새로 세우는 갈래를 골랐다. #130 이 그 자리를 진다.
── 지금 착수 가능 ──────────────────────────────
  #130  C2 — Rust·Kotlin 추출기가 스코프·임포트·익스포트를 산출한다 (순서표 §2 의 1 번)  ← 순서표의 1 번
  #129  pal query narrative.unbound 가 읽기 트랜잭션에서 쓰려 해 실패한다
  #128  규모 선형성을 안 쟀다 — 같은 저장소를 복제해 재는 절차가 안 돌았다
  ...
열린 이슈 42건 · 착수 가능 42건 · 교착 0건
착수는 gh issue edit <번호> --add-assignee @me   (순서는 docs/plan/02-order.md · 완성 장면은 docs/plan/01-completion-scenes.md)

== 무엇이 달라졌나 ==
  1. 첫 항목이 맨 위에 뜨고 「← 순서표의 1 번」 이 붙는다. 그 번호가 #77 에서 #130 으로
     옮겨도 스크립트는 안 고쳤다 — 문서만 고쳤고 출력이 따라 움직였다.
  2. 마지막 줄이 순서표와 완성 장면 두 문서를 가리킨다.
  3. 그 번호는 스크립트에 없다 — docs/plan/02-order.md §4 에서 읽는다.
     순서표가 첫 항목을 안 대면 스크립트가 경고를 찍는다.

== 음성 대조 ==
  순서표에서 그 문장을 지우면 경고가 뜬다:
  ⚠ 순서표가 첫 항목을 안 댄다 — docs/plan/02-order.md §4 를 본다
  착수는 gh issue edit <번호> --add-assignee @me   (순서는 docs/plan/02-order.md · 완성 장면은 docs/plan/01-completion-scenes.md)

  되돌린 뒤 다시 돌리면 1 번이 맨 위로 온다.
