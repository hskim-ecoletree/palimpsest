# 교대 상태 — 첫 릴리스를 낸다

> 회차 `2026-09-14-first-release` · 착수 커밋 `ec92b89`

★ **새 컨텍스트가 받는 것은 「`intent.md` 전문 + 이 파일의 요약」이다.** 직전 산출물을
시드로 받지 마라.

## 지금 단계 — **구현** (승인 2026-09-14 · `approval.md` · 레코드 기계 칸 재구성 `7e3091b`)

구현을 워크트리 셋에 나눠 병렬로 돌린다 — ㈎ #127·#139(A1·A1-a·A3·A3-a) · ㈏ #77(A2·A2-a·A2-b) · ㈐ #159·#156(B1·B1-a·B2).
메인은 라이선스 · README · 릴리스 워크플로 · 버전을 맡는다. 병합 뒤 `cargo xtask test`·`check` 전량.

| 갈래 | 상태 |
|---|---|
| ㈎ #127·#139 | **병합** `1d67cb0` — 워크트리 보고: A1 · A1-a · A3 · A3-a 통과 · 파일 간 엣지 1338 → 1336(사라진 둘 = 고정 목록) · 새 사유 없음. ⚠ 남은 위험: `pub mod n;` + `pub use n::n;` 이 함께면 참인 엣지가 못 푼 참조로 갈 수 있다(거짓 엣지는 안 생긴다 · 이 저장소 0 회) |
| ㈏ #77 | 진행 |
| ㈐ #159·#156 | 진행 |
| 메인 | 라이선스 · README · 워크플로(담는다 · 받은 바이너리 touch) · 버전 0.1.0 `4ac7c32` · 순서표 첫 자리 #160 · 최단 경로 §4.3 `797c364` · E2 오라클 `497e2f2` · 표현 수정 `c8cbc1c` |
| 처분 | 사용 기록 이슈 **#160** · R3 39 건 철회(네트워크 끊김 22 건 재시도 · #147 근거 칸 정정) |

| 단계 | 상태 |
|---|---|
| 인터뷰 | **닫혔다** — 1 라운드 · 소유자 답 넷(`intent.md ## 원문`) |
| 착수 관측 | **닫혔다** — `baseline/` 여섯(#159 · #156 · #127 · #77 RED · 결박 착수 판정 42 = fresh 28 · stale 14) |
| 사전부검 | **닫혔다** — R1 시나리오 13 · 기각 5 · 처리 방침 `88f15fb` · 레코드 `3caa8c8` |
| 완수 조건 | **17** · 형식오류 0 · 조건 설계 평가 R1 뒤 문면 정정 |
| 조건 설계 평가 | **닫혔다** — R1 걸림 11 · 기각 6 · 상한 1 소진 |
| #127 ② | 재현 안 됨 — `baseline/06-doctor-full.txt` |
| 승인 | 대기 |
| 구현 · 릴리스 · 효과 · 처분 · 독립 리뷰 · 종료 | 남음 |

## 착수 바이너리

`target/release/pal` = `pal 0.0.0+ec92b899478d`. 세션이 바뀌면 `git worktree add --detach <스크래치>/wt-start ec92b89`
뒤 `cargo build --release -p pal-cli` 로 다시 뜬다. RED 재현 스크래치 클론은 세션 스크래치 `red/` 에 있었다(커밋 안 함).

## 실패한 접근

- `doctor --full` 을 `timeout` 으로 감싸 돌렸다 — macOS 에 `timeout` 이 없어 rc 127. 감싸지 않고 돌린다.

## 구현 입력

코드 지도 요약은 세션 스크래치 `code-map.md` 에 있었다(커밋 안 함). 핵심 — 저장소 식별자가 `SymbolId` 해시 성분이다(`crates/pal-core/src/coord.rs:106-130`).
