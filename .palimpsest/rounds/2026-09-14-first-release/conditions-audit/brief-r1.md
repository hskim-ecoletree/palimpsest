# 조건 설계 평가 R1 브리프 — 잠긴 의도의 여섯 절

> `intent.md` 에서 여섯 절만 떴다. 착수 관측 · 계획 · 사전부검 처리 방침 · 금지역 · 개정 · 승격은 안 준다.

## 원문

소유자가 2026-09-14 에 쓴 것 — **압축하지 않는다.** 전문과 계기는
[`docs/instructions/2026-09-14-owner-direction.md`](../../../docs/instructions/2026-09-14-owner-direction.md) `U25`·`U26`.

> 우선 끝내고 품질을 올리는 방향으로 해. kotlin, java, javascript, python 지원 같은 것들 말이야

인터뷰 1 라운드에서 소유자가 고른 칸:

> 이 회차는 어디서 끝나나요? → **릴리스까지, 사용은 분할**
> 릴리스 뒤 실제로 써 볼 저장소는 어디인가요? → **ditto (TypeScript)**
> R1 5건을 뺀 나머지 이슈 59건은 어떻게 처리할까요? → **R3 철회, R2 유지**
> 회차의 무게를 어떻게 할까요? → **가볍게**

에이전트가 인터뷰 앞에 기본값으로 적고 소유자가 이견을 안 낸 것:

> 범위 — R1 5건 전부(#77 · #127 · #139 · #156 · #159) + 루트 README 설치 안내 + `v0.1.0` 릴리스.
> #156 — 호출자 자리를 몇 줄 싣고 나머지는 `pal query symbol.callers` 로 접는다.
> #159 — `pal install` 이 저장소 식별자를 매니페스트에 적는다. 식별자 없는 옛 설치는 touch 가 드러낸다.
> 멈춰 묻는 자리 — 축소·전환 때만. CI 가 초록이면 릴리스 태그는 묻지 않고 push 한다.
> 범위 밖 — 언어 확장(Kotlin·Java·JavaScript·Python)은 릴리스 뒤 품질 단계.

## 목적 기여

[`00-goals.md` §2](../../../docs/plan/00-goals.md) 의 **첫 릴리스 정의** — *"1(큐레이션)과 2(컨설팅)가
실사용에서 값을 내면 제품이다."* 이 회차는 「실사용」의 앞 조건인 **남이 받아 쓸 수 있는 릴리스**를 세운다.

**왜 지금 이것이 먼저인가** — `AGENTS.md` 의 순서(*"빠르게 완성한다 → … → 효과를 본다"*)에서 소비 가능해지는
지점에 가장 가까운 것이 이것이다. 최단 경로 §4 의 네 단계가 지났고, 남의 저장소에서 장면이 섰으나
(회차 `2026-09-13-first-release-elsewhere`) **받는 길(릴리스·안내)이 없고 · 팀원에게 거짓 0 이 나가고 ·
틀린 사실을 내는 자리가 열려 있다.** 소유자가 `U25` 로 품질(언어 확장)보다 앞에 두었다.

## 완수 조건

### 이 절이 쓰는 말

- **「착수 바이너리」** — `ec92b89` 의 릴리스 빌드 `pal 0.0.0+ec92b899478d`.
- **「새 ditto 복제본」** — `git clone ~/dev/projects/ditto` 로 뜬 `aded7ce` 이고 `.palimpsest/` 가 **없는** 사본. 스크래치에 둔다.
- **「R1 · R2 · R3」** — 착수 때 가른 열린 이슈 64 의 표 `triage.md`.

### A — 틀린 사실을 내지 않는다

- [ ] **A1** ⟨결정론적⟩ 못 푼 참조가 1 이상인 픽스처에서 `pal doctor --json` 과 `pal export` 의 응답 묶음 `coverage` 가 `unresolved 0 · lowest_grade l0` 을 **하드코딩으로 내지 않는다** — 그 명령이 만진 범위에서 센 값이거나 「안 셈」이다. `crates/pal-cli/src` 에 `unresolved: 0,` 문자열 0. **오라클**: `crates/pal-cli/tests/` 통합 시험 + `grep`. **RED**: `baseline/04-coverage-hardcoded.txt`.
- [ ] **A1-a** ⟨결정론적⟩ 음성 대조 — 같은 시험이 픽스처의 못 푼 참조 수가 1 이상임을 **먼저 단언한다**. 그리고 `query_envelope.rs` 의 `범위는_질의마다_다른_값이다` 가 고치지 않은 채 통과한다.
- [ ] **A2** ⟨결정론적⟩ Rust 픽스처에서 결박을 건 뒤 `#[derive]` 인자 · `#[cfg]` 인자 · `#[serde(rename)]` 값 · `#[must_use]` 유무를 **각각** 바꿔 커밋하면 그 결박이 `stale` 이다(네 갈래 각각 단언). **RED**: `baseline/03-derive-change.txt`(`fresh`).
- [ ] **A2-a** ⟨결정론적⟩ 음성 대조 — 같은 픽스처에서 속성 안의 주석·공백만 바꾸면 `fresh` 로 남는다(ADR-0007). 그리고 `&self` ↔ `self` 만 다른 두 메서드의 요약값이 착수 때처럼 같다(`is_leading_separator` 가 산 가지로 남는다).
- [ ] **A2-b** ⟨결정론적⟩ 이 저장소 HEAD 트리에서 새 바이너리의 `pal query binding.status --json` 판정을 착수 판정 `baseline/05-binding-status-start.json` 과 결박 ID 로 대면, **착수에서 `fresh` 였던 결박 중 `stale` 로 바뀐 것이 0** 이다(두 바이너리 모두 착수 커밋 `ec92b89` 의 트리를 댄다).
- [ ] **A3** ⟨결정론적⟩ 크레이트 둘짜리 픽스처 — 크레이트 `a` 의 사적 `mod inner;` + `pub use inner::f;`, 크레이트 `b` 의 `use a::inner::f` — 에서 사적 `mod` 로 가는 파일 간 엣지가 **0** 이고 못 푼 참조가 까닭과 함께 나온다. **RED**: 같은 시험을 착수 커밋 해소기에 대면 엣지가 1 이상이라 실패한다(`oracle/A3-red.txt`).
- [ ] **A3-a** ⟨결정론적⟩ 음성 대조 — 같은 픽스처의 크레이트 `a` 안 `use crate::inner;` → `mod inner;` 엣지는 **남는다.** 이 저장소 전수에서 착수 바이너리 대비 사라진 파일 간 엣지가 **재수출에 가린 사적 모듈을 가리키는 것뿐**이고 새로 생긴 파일 간 엣지는 0 이다(`oracle/A3-repo-diff.txt`).

### B — 팀원에게 거짓 0 을 안 내고, 깨질 곳의 자리를 싣는다

- [ ] **B1** ⟨결정론적⟩ 결박을 가진 픽스처 저장소에서 `pal install` 뒤 `.palimpsest/manifest.toml` 이 `[[repo]] id` 를 지고, 그것을 커밋한 저장소를 **다른 디렉터리 이름**으로 클론해 의도를 들인 `touch` 의 `걸린 것` 수가 원래 이름과 같다. **오라클**: 통합 시험. **RED**: `baseline/01-touch-dir-pal.txt`(`(0)`).
- [ ] **B1-a** ⟨결정론적⟩ 음성 대조 네 갈래 — ⑴ 매니페스트가 **없는** 저장소를 다른 이름으로 클론하면 `touch` 가 「아직 없습니다」를 **안 내고** 다른 식별자에 걸린 결박 수(1 이상)를 낸다 ⑵ 매니페스트가 이미 있으면 `pal install` 이 그 파일을 바이트로 안 바꾼다 ⑶ 결박이 `palimpsest` 로 선 저장소를 `pal/` 에서 install 하면 매니페스트 id 가 `palimpsest` 다 ⑷ install 이 만든 매니페스트를 커밋하지 않고 `uninstall` 하면 `git status --porcelain` 이 비고, 커밋한 뒤 `uninstall` 하면 매니페스트가 남는다.
- [ ] **B2** ⟨결정론적⟩ `touch` 사람 화면이 호출자 자리를 `경로:줄` 로 싣고, 표시 상한보다 많으면 접힌 수와 펴는 명령을 낸다. **그 명령을 그대로 실행한 `--json` 의 자리 수 = 호출자 수**이고, 동명 후보가 둘 이상인 심볼에서도 그렇다. **RED**: `baseline/01-touch-dir-palimpsest.txt`(자리 0).

### C — 받는 길

- [ ] **C1** ⟨결정론적⟩ 루트 `README.md` · `LICENSE-MIT` · `LICENSE-APACHE` 가 있고, README 가 받기(자산 이름 넷) · `pal install` · `pal narrative` · 승인 명령 · `pal touch` · 지원 언어(TypeScript · Rust)를 싣는다. README 의 명령 줄이 **`D1` 의 산출에 실제로 친 명령으로 나온다**(대조 `oracle/C1-readme-vs-d1.txt` · 빠진 것 0). 릴리스 아카이브에 README 와 두 라이선스가 든다.
- [ ] **C2** ⟨결정론적⟩ ⑴ 태그 없는 `workflow_dispatch` 런이 `success` 다 ⑵ 그 뒤 태그 `v0.1.0` 의 런이 `success` 다 — 세운다 넷 · 툴체인 없이 셋(받은 바이너리의 `touch` 한 걸음과 install 왕복의 빈 `porcelain` 포함) · 산출 ⑶ 릴리스에 자산 넷과 `SHA256SUMS` 가 있고 받은 바이너리의 `--version` 이 `0.1.0` 을 싣는다. **RED**: 착수 때 릴리스는 `v0.0.0-f24.1` 하나.

### D — 효과

- [ ] **D1** ⟨결정론적⟩ 새 ditto 복제본에서 **`C2` 의 릴리스 자산**(빌드한 것이 아님)으로 README 걸음을 밟는다 — 승인한 심볼의 `touch` 가 `걸린 것 ≥ 1` · 호출자 자리 1 줄 이상을 싣는다. 매니페스트와 `bindings.jsonl` 을 그 복제본에 커밋하고 그것을 **다른 디렉터리 이름으로 클론**한 `touch` 가 같은 `걸린 것` 수(1 이상)를 낸다. 산출은 `effect/`.
- [ ] **D1-a** ⟨결정론적⟩ 음성 대조 — ⑴ 같은 절차의 둘째 클론을 **착수 바이너리**로 부르면 `걸린 것 (0)` 이다(`effect/negative-start-binary.txt`) ⑵ ditto 원본의 HEAD · `git status --porcelain` · `.palimpsest/` 목록 해시가 착수와 같다(금지역 데이터_손실).

### E — 처분

- [ ] **E1** ⟨결정론적⟩ `triage.md` 의 R3 39 건이 전부 닫혔고 건마다 사유 코멘트(철회 사유 ② · 더 먼저인 것은 첫 릴리스 · 이 회차 경로)가 있다. R1 5 건은 이 회차 커밋을 가리키는 코멘트와 함께 닫혔다(#77 ② · #127 ②③ 의 답 포함). R2 20 건은 열려 있다. **오라클**: `gh` 조회 `oracle/E1-issues.txt`.
- [ ] **E2** ⟨결정론적⟩ 사용 기록 이슈가 섰고 `scripts/frontier.sh` 가 그 번호를 `← 순서표의 1 번` 으로 산출한다. `docs/plan/03-shortest-path.md` 가 끝의 정의(사용 기록)와 릴리스 뒤 순서(언어 확장 Kotlin · Java · JavaScript · Python)를 싣는다.

### F — 회귀

- [ ] **F1** ⟨결정론적⟩ `cargo xtask check` · `cargo xtask test` 가 로컬에서 통과하고, **코드(`crates/` · `xtask/` · `.github/` · `Cargo.*`)를 바꾼 마지막 커밋을 담은 push** 의 SHA 에 CI 런 `success`(세 OS).

## 차선책

- **#139 의 작은 쪽(대상 `mod` 의 가시성을 해소기가 읽는다)이 1 층 산출에 가시성 재료가 없어 안 서면** — 심볼이 가시성을 지는 큰 쪽으로 가고,
  `ROW_FORMAT_REV` · `EXTRACTOR_REV` · `schema/graph.toml` 을 한 커밋에서 옮긴다. 조건은 그대로 잰다.
- **#77 에서 ㉠ ㉡ 둘 다 착수 결박의 판정을 못 지키면** — 속성 변경을 요약에 안 넣고 `touch` 가 결박마다 「속성 변경은 감시 안 함」을 드러내는 데서 멈춘다.
  `A2` 는 반증으로 적고 `A2-b` 는 그대로 잰다.
- **릴리스 워크플로의 한 타깃이 빨갛고 원인이 이 회차 코드가 아니면** — 초록인 타깃만 릴리스 자산으로 내고 README 에 빠진 타깃을 적는다.
  `C2` 의 그 타깃 칸만 반증이다.

## 범위 밖

- **언어 확장** — Kotlin(#131 · #54) · Java · JavaScript · Python. 소유자 `U25` 가 릴리스 뒤 품질 단계로 뒀다.
- **R2 20 건** — 열린 채 둔다(`U26` 셋째 칸). #57 · #78 · #133 · #151 등.
- **사용 기록의 판정** — `U26` 첫 칸이 분할로 정했다. 이 회차는 그 이슈를 세우는 데까지다.
- **서명·공증** — 인증서가 없다. `SHA256SUMS` 를 같이 낸다(릴리스 워크플로가 이미 한다).
- **패키지 관리자 배포**(Homebrew · crates.io · npm 등) — README 의 받기는 릴리스 자산 하나다.
- **`pal install` 이 까는 스킬·에이전트 정의의 내용** — 앞 회차와 같은 판단이고 장면을 안 막는다.
- **`coverage_of` 의 질의별 범위 설계** — `[f05.3.pass]` ⑤ 가 정했고 결함이 아니다.

## 상한

- 인터뷰 **1 라운드** — 소진했다(2026-09-14). 범주 다섯이 한 번씩 열렸다.
- 사전부검 **1 라운드** — 소진했다(2026-09-14).
- 완수 조건 설계 평가 **1 라운드**
- 독립 리뷰 **2 라운드**
- 정반합 — 결정론 조건만 등록했으므로 **없다.** 결정론이 아닌 조건이 생기면 판마다 1 라운드.

