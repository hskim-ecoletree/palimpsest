# 열린 이슈 분류 — 2026-09-14 · main ec92b89 · 64 건

기준: 첫 릴리스 장면(남의 TS/Rust 저장소 · `pal install` → `pal touch`: 결정+신선도 · 호출자 수와 자리 · 못 푼 참조 · 근거 · 다른 디렉터리 이름으로 클론해도 같은 답).
R1 = 장면이 안 선다 / 사용자 화면에 거짓 사실 · R2 = 릴리스 후 제품 품질 · R3 = 하네스·측정·규약·문서 산문.

집계: **R1 5 · R2 20 · R3 39**

| # | 제목(짧게) | 분류 | 근거 한 줄 |
|---|---|---|---|
| 54 | Kotlin 문법이 비ASCII 식별자를 자른다 | R2 | Kotlin 은 첫 릴리스 언어(TS/Rust) 밖. 코퍼스 하중 0 |
| 56 | `pal doctor` 가 검사할 엣지를 지운다 | R3 | 코드는 이미 고쳐졌다 — `doctor.rs:148` 이 `How::Stitching`, `SymbolsOnly` 호출자 0(`attach.rs:19`). 남은 것은 `[f22.4]` 재판정 |
| 57 | `built_for_this_snapshot` 이 워킹트리 드리프트를 못 잡는다 | R2 | 결함 실재(`repo.rs:255` 가 digest 를 버린다). 그러나 `touch.rs:129` 는 매번 Stitching 이라 touch 화면은 안 틀린다 — `--read-only` 소비자만 |
| 58 | callers 반경 거짓 양성률·반경별 비용 벤치 | R2 | 반경 정확도·성능 측정. 화면 사실 아님 |
| 61 | f05 ⑨ 콜드/증분 비율 9.7배 | R3 | 기계 의존 벤치 합격선 문제 |
| 63 | F09 ⑤ 재측정 표본기가 코퍼스당 10 으로 자른다 | R3 | 측정 설계(표본 하한) |
| 64 | 처분 (나)가 떨어뜨린 50 건의 재현율 | R2 | 결박 붙임 재현율 측정 — 없는 엣지 쪽(안전 방향) |
| 65 | 거짓 결박 둘이 구문 트리로 안 갈린다 | R2 | 측정된 잔여 4.0%(상한 5% 아래). 승격은 approve 를 거친다 |
| 68 | sunset — docs/research 처분 | R3 | 문서 재고·xtask 검사 |
| 69 | 문서 간 결박이 없다 | R2 | 새 질의 능력(문서→문서 엣지). 장면 밖 |
| 73 | 발견 레코드에 근거 칸이 없다 | R3 | 회차 레코드 스키마 |
| 74 | 회차 레코드 검사의 구멍 둘 | R3 | xtask 회차 검사 |
| 75 | 독립 리뷰 처분을 손으로 적는다 | R3 | 회차 규약 |
| 77 | Rust 속성 변경이 낡음을 안 켠다 · 죽은 가지 | **R1** | 확인: `rust.rs:352-368` 이 선언 마디만 normalize — `#[derive]`·`#[cfg]`·`#[serde(rename)]` 를 바꿔도 `body_digest` 가 같아 결박이 **거짓 fresh**. Rust 는 첫 릴리스 언어. 빈도는 안 쟀다 |
| 78 | impl 컨테이너가 트레잇을 못 담는다 · 다른 파일 대상 | R2 | ordinal 좌표 뒤바뀜은 stale 로 드러나고 touch 가 `identity ordinal` 을 찍는다(`touch.rs:304`) — 불확실성은 드러난다. 경계 |
| 80 | GRAMMAR_REV 가 Kotlin 하나뿐 | R2 | 캐시 키 결함. 첫 설치에는 옛 캐시가 없다 — **다음 릴리스에서 TS/Rust 문법 핀이 바뀌면** 위험 |
| 81 | 골든 symbols.tsv 가 CI 밖 | R3 | 회귀 탐지 장치 |
| 82 | 에이전트 정의의 Grep·Glob 소실 | R3 | `.claude/agents` 하네스 |
| 83 | 게이트 절 이름 「남기는 빚」 | R3 | 게이트 문서 문면 |
| 84 | round-completion-condition 표준 표 승격 | R3 | 게이트 형식 |
| 87 | 죽은 링크 검사가 코드 스팬 안을 오인 | R3 | xtask 검사 |
| 89 | 계기판 ③ 진자가 target/ 에 지배된다 | R3 | dashboard.py |
| 90 | 표 헤더 한 글자로 원장 대조를 벗어난다 | R3 | xtask 검사 |
| 93 | 리뷰어 「없음」 행이 계기판을 오염 | R3 | 회차 레코드·계기판 |
| 102 | `--schema` 부재가 unwrap_or 로 가려진다 | R3 | xtask 파서 |
| 105 | SARIF·Backstage 비교 | R3 | 온톨로지 조사. 사용자 화면에 안 닿는다 |
| 116 | 상태 넷으로 충분한가 — Undeterminable 분포 | R2 | 상태 설계 측정. overview.md 의 `pending` 표기도 문서 |
| 117 | 사용자 표면 내부 용어(결박·반경·대장…) | R2 | 용어 품질 — 거짓은 아니다 |
| 118 | 과하게 복잡한 표현을 어떻게 잴까 | R3 | 문서 산문 측정 |
| 119 | 결박 대상이 심볼로 한정 | R2 | 문서 좌표 결박은 새 능력. 장면은 심볼 좌표 |
| 120 | 완료 상태를 둘로 가른다 | R3 | 회차 규약 |
| 121 | 잔여를 착수 시점에 선언 | R3 | 회차 규약 |
| 122 | 순서표 §3 「소비자다」 정의 부재 | R3 | 계획 문서 문면 |
| 124 | B5 음성 대조가 항등식 | R3 | 완수 조건 문면 |
| 127 | 같은 인덱스가 두 경로에서 다른 값 | **R1** | 추정 섞임: `doctor.rs:186`·`export.rs:392` 의 봉투가 `unresolved: 0` 을 **하드코딩**. query 는 닿은 파일만 센다(`pal-query/src/lib.rs:674-685`) — ledger.snapshot 은 닿은 것이 없어 「미해소 0」. 모르는 것을 0 으로 낸다. touch 화면 밖이라 경계 |
| 128 | 규모 선형성을 안 쟀다 | R2 | 성능 측정 |
| 131 | Kotlin 추출기 스코프·임포트·익스포트 | R2 | 언어 확장(Kotlin) |
| 132 | 회차 레코드 장치의 자기 모순 | R3 | extract.py·record.py enum |
| 133 | Rust 등급 L1→L2 | R2 | 등급 승격. L1 은 정직하게 표기된다 |
| 134 | 원 반환문의 뿌리 기준 링크 | R3 | 죽은 링크 검사 |
| 135 | F07 파일 경계를 넘는 참조 해소 | R3 | 능력은 섰다 — 게이트 판정 35 통과·4 반증(`cross-file-references.md:39-48`). 반증은 #139·#136 이 진다. 이슈만 안 닫혔다 |
| 136 | 파일 간 엣지가 갈래·줄 번호를 안 싣는다 | R2 | 엣지 속성 추가. #156 과 이웃(호출 자리의 줄) |
| 137 | 완수 조건 파서가 줄 단위 | R3 | round_condition 파서 |
| 138 | F04 벤치 합격선이 경계에서 뒤집힌다 | R3 | 합격선 문제 |
| 139 | 가시성이 없어 사적 모듈이 거짓 엣지를 받는다 | **R1** | 확인: `cross_file.rs:23` — 재수출 미추적, 가시성 칸 없음. `use pal_core::traverse` 가 사적 `mod traverse;` 에 **거짓 엣지** → 호출자 수가 틀린다. 실측 빈도 2/58(자기 저장소) |
| 140 | 「touch 가 처음 말해 준 것」 조건 문면 넷 | R3 | 완수 조건 문면 |
| 141 | 죽은 링크 검사가 백틱 참조를 못 본다 | R3 | xtask 검사 |
| 142 | 추출기와 계수기가 다른 수를 낸다 | R3 | 회차 장치 |
| 143 | 정반합 과업문 보존 자리 | R3 | 회차 규약 |
| 144 | 핀 이탈 조항 정의 부재 | R3 | 회차 규약 |
| 145 | 불변식 ⑦ not_built | R2 | 정직하게 not_built 로 나간다. 능력 부재 |
| 146 | sunset 트리거가 갈래를 안 묶는다 | R3 | xtask 검사 |
| 147 | extract.py 가 `\|` 를 안 받는다 | R3 | 회차 장치 |
| 148 | xtask test 에 --ignored 가 없다 | R3 | CI 구성 |
| 149 | 기계 승격이 ADR 절 전체를 메모로 | R2 | 거짓 양성 stale — 감시 코드는 실제로 바뀌었으니 거짓 사실은 아니다. 정확도 문제 |
| 150 | BindingId 를 진 결박의 메모를 못 고친다 | R2 | 결박 수명 관리. import 불변식 파손은 조용하나 장면 밖 |
| 151 | callers 반경이 impl 멤버를 안 담는다 | R2 | 반경 감도 공백. fresh 는 감시 집합 기준으로 참 |
| 152 | ADR-0024 산문이 없는 크레이트를 부른다 | R3 | ADR 문면 |
| 153 | import_jsonl 이 예산을 비껴간다 | R2 | 성능 방어 경로. 거짓 사실 없음 |
| 154 | 결박 37 건 반경 판정 재측정 | R3 | 앞 회차 판정 재측정 |
| 155 | 기준선 s2②·f22-1⑦ 이 옛 「안 만듦」을 기대 | R3 | 대조 스크립트 |
| 156 | touch 가 호출자 자리를 싣지 않는다 | **R1** | 확인: `touch.rs:243` `SymbolFacts { callers: usize, callees: usize }` — 수만 있다. 장면 ② 「자리」가 화면에 없고 안내도 없다 |
| 158 | first-release-elsewhere 장치 넷 | R3 | 회차 장치 재검토(`pal radius` Windows 잠금 순서 부속 항은 제품 쪽이지만 이슈 본체는 장치) |
| 159 | 결박이 디렉터리 이름에 묶인다 | **R1** | 확인: `ledger.rs:124-127` 매니페스트에 repo id 가 없으면 `repo_name(path)`(`:606`). `install/manifest.rs` 에 repo id 를 쓰는 곳이 없다 → 팀원 클론에서 `(0) 아직 없습니다` 가 **거짓 0** |

## 분류별 목록

- **R1 (5)**: 77 · 127 · 139 · 156 · 159
- **R2 (20)**: 54 · 57 · 58 · 64 · 65 · 69 · 78 · 80 · 116 · 117 · 119 · 128 · 131 · 133 · 136 · 145 · 149 · 150 · 151 · 153
- **R3 (39)**: 56 · 61 · 63 · 68 · 73 · 74 · 75 · 81 · 82 · 83 · 84 · 87 · 89 · 90 · 93 · 102 · 105 · 118 · 120 · 121 · 122 · 124 · 132 · 134 · 135 · 137 · 138 · 140 · 141 · 142 · 143 · 144 · 146 · 147 · 148 · 152 · 154 · 155 · 158

## R2 중 언어 지원(Kotlin/Java/JS/Python)

- Kotlin: **54**, **131**
- 가까운 자리: **80**(문법 핀 캐시 키 — 새 언어가 붙을수록 커진다)
- Java · JavaScript · Python 을 직접 지는 열린 이슈는 **없다**
