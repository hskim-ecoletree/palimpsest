# A1 RED — 시험을 착수 커밋의 doctor · export 에 댄다

회차 2026-09-14-first-release · 조건 A1 · A1-a
HEAD      ec92b899478d31533b4026a0eb2750242bba713f  (crates/ 는 착수 커밋 그대로 · 시험 파일만 더함)
rustc     1.94.1 (rustup 없음)
명령      cargo test -p pal-cli --test coverage_whole_graph --test cross_file_references -- --test-threads=1
exit      101

## 픽스처 (crates/pal-cli/tests/coverage_whole_graph.rs 의 `저장소`)

a.ts        export function 도움() { return console } / export function 부름() { return 도움() }
src/lib.rs  pub fn 러스트() { 없는것(); }

## 결과 발췌

test 전_그래프_명령의_범위가_graph_dump_와_같은_값이다 ... FAILED

thread '전_그래프_명령의_범위가_graph_dump_와_같은_값이다' panicked at crates/pal-cli/tests/coverage_whole_graph.rs:72:5:
assertion `left == right` failed: doctor 의 미해소가 graph.dump 와 다르다
  left: Number(0)
 right: Number(2)

test result: FAILED. 9 passed; 1 failed; 0 ignored; 0 measured; 0 filtered out

- A1-a 전제 단언(graph.dump 의 unresolved ≥ 1 · lowest_grade ≠ l0)은 **통과했다** — 실패는 그 다음 줄의 doctor 대조다.

## 같은 픽스처의 세 산출 — 실패한 시험이 남긴 저장소에서 같은 착수 디버그 바이너리로 떴다

바이너리  target/debug/pal (워크트리 · 착수 커밋 소스로 빌드 · 수정 전)
저장소    $TMPDIR/pal-coverage-whole-39978

pal query graph.dump --json                                  coverage = {"unresolved": 2, "out_of_scope_files": 0, "lowest_grade": "l1", "identity": "ordinal"}
pal doctor --json                                            coverage = {"unresolved": 0, "out_of_scope_files": 0, "lowest_grade": "l0", "identity": "ordinal"}
pal export --format cypher --out <스크래치>/red-a1.cypher --json  coverage = {"unresolved": 0, "out_of_scope_files": 0, "lowest_grade": "l0", "identity": "ordinal"}

## 읽기

- doctor · export 둘 다 `unresolved 0 · lowest_grade l0` — 하드코딩 두 칸이 그대로 나온다.
- 같은 인덱스의 graph.dump 는 `2 · l1` 이다. 시험은 첫 불일치(doctor 미해소)에서 멈추므로 lowest_grade 와
  export 의 불일치는 위 세 산출이 보인다.
