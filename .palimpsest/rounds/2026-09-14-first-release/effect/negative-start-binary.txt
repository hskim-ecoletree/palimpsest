# D1-a ⑴ — 착수 바이너리로 README 걸음을 install 부터 밟는다

- 바이너리: `pal ` (`/Users/incognito/dev/projects/palimpsest/target/release/pal` · 착수 `ec92b89`)
- 스크립트: 세션 스크래치 `d1-walk5.sh` — 친 명령은 `negative-start-binary/commands.txt`, 산출은 같은 디렉터리의 `NN-*.txt`
- ditto 원본 HEAD: `aded7ce7f88feb3c03238c5f9760f3a2ade4a6c1` — `git clone` 으로만 읽었다

## 결과

| 자리 | 산출 |
|---|---|
| 첫 클론(`ditto`) 승인 뒤 touch | `■ 이 좌표에 걸린 것 (1)` |
| 결박 내보내기 | `.palimpsest/intent/bindings.jsonl — 2 줄` |
| 팀원 클론(`ditto-teammate`) 들이기 | `결박 1 · 별칭 0 · 이미 있던 것 0` |
| **팀원 클론 touch** | **`■ 이 좌표에 걸린 것 (0)`** |

## README 에서 어긋난 한 줄

```
README 의 git add 줄이 rc!=0 — 있는 파일만 더한다:
  없음 .palimpsest/manifest.toml
  더함 .palimpsest/intent/bindings.jsonl
```

착수 바이너리는 `.palimpsest/manifest.toml` 을 만들지 않는다(#159 이전). README 의 `git add` 줄이 없는 경로로 rc≠0 이라
**있는 파일만** 더했다 — 그러지 않으면 결박이 안 넘어가 「결박을 안 넘겨서 0」이 되고 대조가 무효다(교대 상태 「실패한 접근」).

## 읽는 법

결박 1 건이 팀원 클론에 **들어갔는데도** `(0)` 이다 — 저장소 식별자가 디렉터리 이름(`ditto` ↔ `ditto-teammate`)이라 좌표가 갈렸다.
같은 걸음을 새 바이너리(`pal 0.1.0+06f6b96153ef` · 로컬 빌드 리허설)로 밟으면 팀원 클론이 `(1)` 이었다. 판정용 `D1` 은 릴리스 자산으로 다시 밟는다.
