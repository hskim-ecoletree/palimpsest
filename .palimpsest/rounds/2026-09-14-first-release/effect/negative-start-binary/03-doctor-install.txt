$ pal doctor --install  (cwd ditto)

■ 설치 검사 6개
  ok   1  설정 파일이 유효한 JSON 인가
      .claude/settings.json 를 읽었다
  ok   2  매니페스트가 실물과 맞는가
      적힌 18개가 실물과 sha256 까지 같다 / ★ **대조 밖**: 남의 에이전트가 함께 사는 자리라 이 검사가 안 보는 것이 4개 있다(설계대로다. 고장이 아니다): .claude/agents/cli-implementer.md · .claude/agents/core-implementer.md · .claude/agents/schema-implementer.md · .claude/skills/pal-round/bin
  ok   3  여기가 설치 루트인가
      여기가 설치 루트다
  빨강 4  `pal` 실행 파일을 찾을 수 있는가
      `PATH` 어디에도 `pal` 이 없다 — 설치된 커맨드가 못 돈다
  ok   5  `.gitignore` 에 파생이 등재됐는가
      파생 3개가 전부 등재됐다
  빨강 6  등록된 훅이 실제로 도는가
      SubagentStop 이 안 돈다 — `PATH` 어디에도 `pal` 이 없다 — 등록은 이 이름을 가리키므로 하네스도 못 찾는다. 하네스는 그 실패를 **완전히 삼키므로** 여기가 유일한 문이다. `pal` 을 `PATH` 에 넣은 뒤 다시 보십시오

rc=0
