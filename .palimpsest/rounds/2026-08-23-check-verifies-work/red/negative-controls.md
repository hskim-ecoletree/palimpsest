# 음성 대조 — 실제로 걸고 관측한 것

> `F2`(전부 걸었다)·`F3`(걸었고 빨개졌다)의 증거. **격리 사본에서 건다.**
>
> ⚠ **사본은 `git clone` 이어야 한다.** 파일 복사본에는 이력이 없어 A 축이
> 아무것도 못 잰다 — 그 사실 자체를 이 자리에서 잡았고 `안 보임 → 실패` 로 고쳤다.
>
> ⚠⚠ **그리고 `--root` 없이는 이 전부가 헛것이다.** 프리빌트 `xtask` 는 뿌리가
> 컴파일 시점에 박혀 **사본으로 `cd` 해도 원본을 잰다.**
>
> 잰 커밋: `84bc53d`

## 기준선 — 사본이 원본과 같은 답을 낸다
```text
검사 23/23 통과
```

## N1 — 실재하지 않는 SHA 를 `닫은커밋` 에 넣는다 (A1 짝)
```text
  FAIL  발견이 닫혔나
발견이 닫혔나: .palimpsest/rounds/2026-08-23-check-verifies-work/findings.jsonl:2: `PM1-01` 의 `닫은커밋` `deadbee` 가 **이 이력에서 안 보인다** — 없는 커밋이거나, 얕은 클론·squash 병합으로 이력 밖이다. CI 라면 체크아웃 깊이를 보라
```

## N2 — 그 처분이 요구하는 자리를 안 만진 커밋으로 바꾼다 (A2 짝)
```text
  FAIL  발견이 닫혔나
발견이 닫혔나: .palimpsest/rounds/2026-08-23-check-verifies-work/findings.jsonl:2: `PM1-01` 가 `b9fbef7` 로 닫혔다는데 그 커밋이 `.palimpsest/rounds/2026-08-23-check-verifies-work/intent.md` 를 안 만졌다
```

## N2-b — 끝난 회차에 열림 행을 만든다 (「열림으로 밀기」 도피로)
```text
  FAIL  발견이 닫혔나
발견이 닫혔나: .palimpsest/rounds/2026-08-23-agent-laziness-behavior/findings.jsonl:3: `PM1-02` 가 **열림**인데 그 회차는 끝났다(`report.md` 가 있다) — 끝난 회차에 열린 발견이 있으면 **그 회차가 안 끝난 것**이다. 「열림으로 밀기」로 이 검사를 비껴가지 않는다
```

## N6 — 새 회차를 「형식 이전」 선언 목록에 넣는다 (C3·C5 짝)
```text
  FAIL  선언 목록이 닫혀 있나
선언 목록이 닫혀 있나: `형식 이전` 목록의 `2026-08-23-check-verifies-work` 이 하한 `2026-08-19` 이후다 — **이 목록은 닫혀 있다.** 새 회차는 못 들어간다
```

## N6-b — 선언 목록에 실재하지 않는 회차를 넣는다
```text
  FAIL  선언 목록이 닫혀 있나
선언 목록이 닫혀 있나: `형식 이전` 목록의 `2026-01-01-없는회차` 은 실재하는 회차가 아니다
```

## 원본은 내내 초록이었다
```text
검사 23/23 통과
```

## N5 — 표 헤더 `조건` → `조건들` (C1 짝)

★ #90 이 실측한 문이다. 앞 판에서는 이 한 글자로 그 회차가 **조용히 검사 밖**으로
빠지고 판정문의 조건 수만 줄어든 채 초록이었다.
```text
  FAIL  원장 둘 대조
원장 둘 대조: 2026-08-23-agent-laziness-behavior: **끝난 회차**(`report.md` 가 있다)인데 게이트 `docs/gates/agent-laziness-behavior.md` 에 표준 표가 없고 `docs/gates/README.md` 의 「형식 이전」 선언 목록에도 없다 — 표 헤더 한 글자로 조용히 검사 밖으로 나가지 않는다
```

## N9 — 종료 보고에 §9 검산 줄을 넣는다 (G1 짝)
```text
  FAIL  원장 둘 대조
원장 둘 대조: .palimpsest/rounds/2026-08-23-agent-laziness-behavior/report.md:6: 종료 보고가 §9 표준 검산 줄을 담는다 — **원장은 둘이다.** 판정은 게이트의 표준 표와 `intent.md` 의 상자가 지고, 종료 보고가 셋째 사본이 되면 그것이 곧 drift 다: `**검산** — 통과 31 · 반증 7 · 대조불가 1 · 미측정 0 = 39`
```

## N9-b — 서술문은 안 잡는다 (거짓 실패를 안 낸다)
```text
검사 23/23 통과
```

## N10 — 종료 보고에 §10 금지 절머리를 넣는다 (G2 짝)
```text
  FAIL  원장 둘 대조
원장 둘 대조: .palimpsest/rounds/2026-08-23-agent-laziness-behavior/report.md:90: 종료 보고에 §10 이 금지한 절 이름이 있다: `## 다음으로 넘기는 것` — 그 이름들은 **할 수 있었는데 안 한 것**(잔여)과 **원리상 못 하는 것**(경계)을 한 칸에 섞는다
```

## N10-b — 규약이 정한 이름은 안 잡는다
```text
검사 23/23 통과
```

## C7 — 끝난 회차의 종료 보고를 지운다

⚠ **이 대조가 처음엔 안 울었다.** 첫 판의 자가 `state.md` 의 「단계」 줄에서
「종료」를 찾았는데 **그 표기가 회차마다 갈린다.** 음성 대조가 그것을 잡았고,
자를 **파일 배치만 보는 것**으로 바꿨다 — 진행 중인 회차는 하나다.

```text
  FAIL  원장 둘 대조
원장 둘 대조: 2026-08-22-agent-laziness: `report.md` 가 없는데 **가장 최근 회차가
  아니다** — 진행 중인 회차는 하나다. §10 이 종료 보고의 자리를 정했고 그것이
  「끝난 회차」의 유일한 기계 정의라, 안 쓰면 표준 표 검사와 종료 보고 검사를
  **한 수로** 비껴간다
```

## K9 — 얕은 클론에서 A 축이 어떻게 되나 (CI 가 그랬다)

`actions/checkout@v4` 의 기본값은 `fetch-depth: 1` 이다. 그 상태에서 A 축은
**닫힘 행 전량을 못 읽는다** — 로컬은 초록이고 push 뒤에야 나므로 **회차 안에서
안 보인다.** 그래서 `check` 잡에 `fetch-depth: 0` 을 박았다.
```text
  FAIL  발견이 닫혔나
발견이 닫혔나: .palimpsest/rounds/2026-08-22-agent-laziness/findings.jsonl:2: `PM1-01` 의 `닫은커밋` `1ea992f` 가 **이 이력에서 안 보인다** — 없는 커밋이거나, 얕은 클론·squash 병합으로 이력 밖이다. CI 라면 체크아웃 깊이를 보라
  … (안 보이는 행 620 개)
```

## N8 — `--schema` 의 정본 키를 고치면 검사 동작이 바뀐다 (E3 짝)

`닫힘축.요구하는자리.정정` 을 `"경로"` → `"의도"` 로 **한 글자** 고친다.
**선언만 고쳤고 코드는 한 줄도 안 고쳤다.**

```text
고치기 전:
  ok    발견이 닫혔나  — 잰 것 476행 · 발화 0 · **감사 대기 발화 41** · …

고친 뒤:
  FAIL  발견이 닫혔나
발견이 닫혔나: …/findings.jsonl:70: `MS1-01` 가 `84bc53d` 로 닫혔다는데
  그 커밋이 `…/2026-08-23-check-verifies-work/intent.md` 를 안 만졌다
  (같은 모양으로 여러 행)
```

★★ **초록이 빨강이 됐다.** 선언이 「정정은 `경로` 를 요구한다」에서 「`의도` 를
요구한다」로 바뀌자 검사가 **다른 것을 재기 시작했다.** 이것이 소비자가 실재한다는
가장 강한 증인이다.

★ 앞 판에서는 이 사전이 **산문**이라 고쳐도 아무 일도 안 났다 — 그것이 #94 다.

## N8-b — 선언을 비우면 검사가 **원리상 안 선다**고 말한다

```text
  FAIL  발견이 닫혔나
발견이 닫혔나: `--schema` 의 `닫힘축.요구하는자리` 가 비었다 — 이 검사의 규칙이 원리상 안 선다
```

## N3 — 추출 산출과 **다른** 레코드를 커밋한다 (B2 짝)

`PM1-05` 의 `요약` 을 딴것으로 바꾼다. **수는 그대로다** — 앞 판의 합계 검산은
행 수만 세므로 **이것을 못 잡았다.**
```text
  FAIL  회차 레코드
회차 레코드: .palimpsest/rounds/2026-08-23-check-verifies-work/premortem/r1-raw.md: 기계 칸이 추출기 산출과 갈린다 (1건) — **손으로 옮기지 않는다.** `python3 .claude/skills/round/bin/extract.py 사전부검 1 <raw>` 로 뽑아라: PM1-05.요약
```

## N3-b — 한 행을 지운다 (합계 검산이 잡는 자리)
```text
  FAIL  회차 레코드
    합계 검산 어긋남 — .palimpsest/rounds/2026-08-23-check-verifies-work/premortem/r1-raw.md: 원 반환문의 항이 22 인데 회차 `2026-08-23-check-verifies-work` 의 `출처=사전부검` · `라운드=1` 레코드는 21 행이다
```

## N4 — 손으로 채운 칸이 있으면 판정문에 수가 난다 (B4 짝)
```text
손으로 채운 칸 — 진행 중 0 · 끝난 회차 2313(보고만)
```

★ **끝난 회차 2313 은 빚이다.** 그 라운드들은 추출기가 없던 때에 손으로
전사됐다. 실패로 안 만들되 **매 실행 수를 낸다** — 조용한 fallback 이 아니다.

## N7 — 표를 아예 안 낸 것이 「없음」을 낸 것과 **다른 답을 받는다** (D2 짝)

### ⓐ 절을 통째로 뺀다 → 「안 냈다」
```text
  FAIL  회차 레코드
회차 레코드: .palimpsest/rounds/2026-08-23-check-verifies-work/premortem/r1-raw.md: `## 내가 기각한 것` 절이 없다 — **「안 냈다」와 「낼 것이 없다」는 다르다.** 낼 것이 없으면 그 절을 두고 **표 밖에 「없음」이라 적어라
```

### ⓑ 절을 두고 표 밖에 「없음」을 적는다 → 「낼 것이 없다」
```text
  FAIL  회차 레코드
    합계 검산 어긋남 — .palimpsest/rounds/2026-08-23-check-verifies-work/premortem/r1-raw.md: 원 반환문의 항이 17 인데 회차 `2026-08-23-check-verifies-work` 의 `출처=사전부검` · `라운드=1` 레코드는 22 행이다
```

★ ⓑ 에서 **합계 검산이 갈리는 것**은 정상이다 — 기각 행 다섯이 실제로 사라졌으니
레코드도 그만큼 없어야 한다. **가르는 것은 절의 유무이지 침묵이 아니다.**
