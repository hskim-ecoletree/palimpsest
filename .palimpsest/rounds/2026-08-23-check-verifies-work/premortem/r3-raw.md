# 사전부검 R3 — 원 반환문

> 보존본. **고치지 않는다** — 합계 검산의 독립된 둘째 원천이다.
> 처분 `75a547f`(조건 50 → 57 · B 축 전면 교체) 위에서 쟀다.
> `pal` 바이너리가 이 저장소에 없어 그래프 조회는 못 썼고, 대신 실제 코드 실행 ·
> `cargo xtask check` 실행 · `gh run view` · git 전수 스캔으로 쟀다.

### `--root` 는 지금 조용히 무시된다 — `A9` 가 서기 전에 쓰면 사본이 아니라 원본이 초록을 낸다

- 어떻게 실패하나: `cargo xtask check --root /tmp` 를 지금 돌리면 에러 없이 21/21 통과하고 판정문은 원본 저장소의 수(회차 8 · 조건 198)를 낸다. `main()` 이 `std::env::args().nth(1)` 만 보고 그 뒤 인자를 읽지도 거부하지도 않기 때문이다. 「모르는 명령이다」 분기는 nth(1) 에만 걸린다. R2 의 ⑱ 은 「cd 해도 원본을 잰다」를 잡았는데, `--root` 는 플래그를 준 것처럼 보이면서 같은 일을 한다 — 더 나쁘다. `A9` 구현 전에 음성 대조 아홉을 쓰거나, 구현 후 프리빌트 바이너리를 재빌드 안 하고 돌리면 열 하나가 전부 「안 빨개졌다」로 읽힌다
- 어디가 걸리나: `xtask/src/main.rs:35-64` · `repo_root()` at `:584`
- 획득: 조회 — 실제로 그 명령을 돌려 21/21 과 원본 수를 관측했고, `main()` 의 인자 분기를 읽었다
- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(알 수 없는 인자를 거부하면 끝). 걸리는 곳은 `A9`·`F2`·`F3`·`N1`·`N3`~`N10` 열 하나 — 즉 이 회차의 효과 확인 전체

### Windows CI 가 착수커밋에서 이미 빨갛다 — `RED-5` 가 「21/21 통과」를 원본으로 적었는데 그것은 macOS 한 대의 수다

- 어떻게 실패하나: 착수커밋 `b9fbef7` 의 CI 런이 failure 다. windows-latest 만 죽었고 원인은 `회차 레코드: .palimpsest/rounds/2026-08-23-agent-laziness-behavior/findings.jsonl:66: /tmp 가 없다` 이다. 그 행의 경로가 절대경로라 `root.join` 이 유닉스에서는 root 를 대체해 존재하고 Windows 에서는 없는 경로가 된다. 직전 세 런이 전부 failure 다. 그런데 RED 표는 「21/21 통과」만 적고 CI 가 빨갛다는 RED 행이 없다. 결과: `K1` 은 이미 거짓이고 `K5` 와 §11 조건 6 이 이 회차가 한 줄도 안 써도 이미 불가능하다. 게다가 고칠 자리는 앞 회차의 레코드라 결정 15 와 정면으로 걸린다
- 어디가 걸리나: `xtask/src/main.rs:3507-3541` · `.palimpsest/rounds/2026-08-23-agent-laziness-behavior/findings.jsonl:66` · `.palimpsest/rounds/2026-08-23-check-verifies-work/intent.md:118`
- 획득: 조회 — `gh run list` 와 `gh run view --log-failed` 로 실패 메시지 원문을 뽑았고, 66 행의 경로를 직접 읽었고, 좌표 검사의 `root.join` 을 읽었다
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(한 행 또는 절대경로 가드 한 줄). 그러나 `K1`·`K5`·효과 확인·`F3` 이 전부 이 하나에 매달려 있고, 플랫폼 갈림이라 ADR-0023 대상이다. 그리고 이 회차가 `A1`·`A2` 로 git 셸아웃과 경로 비교를 더 넣으면 같은 갈림이 늘어난다

### `extract.py` 를 그대로 승격하면 `유효성` 축이 사라지고 `모집단` 이 통째로 거짓이 된다

- 어떻게 실패하나: `B1`·`B2` 가 승격하려는 추출기를 이 회차의 R1 반환문에 실제로 돌렸다. 산출 17 행 대 커밋된 22 행. 갈림 셋. 첫째, 기각 5 행이 통째로 사라진다 — 추출기의 기각 규칙이 최상위 불릿뿐인데 지금 사전부검 반환 형식은 표를 강제한다. 둘째, 유효성이 17/17 전부 참이 된다 — 추출기가 보는 칸 이름이 지금 형식에서 개명됐다. 셋째, 모집단이 두 값으로 뭉개진다 — 추출기가 대상에서 파생하는데 지금 형식은 다섯 값을 사전부검자가 직접 적는다. 커밋된 R1 은 저장소 9 · 자기장치 8 · 회차기록 4 · 규약 1 이고 원의도 0 인데 추출기는 원의도 11 을 낸다. 계기판으로 재면 ⑦ 이 0% 에서 약 65% 로, ⑧ 이 「27% 가 헛것」에서 「0% 가 헛것」으로 바뀐다. 후자가 정확히 #72 가 고치려던 병이다
- 어디가 걸리나: `.palimpsest/rounds/2026-08-19-finding-records/premortem/extract.py:24-56` · `:83-86` · `.claude/agents/pal-premortem-sweeper.md` 반환 형식
- 획득: 조회 — 추출기를 이 회차의 R1 반환문에 돌려 산출을 커밋된 22 행과 필드 단위로 대조했다(갈림 27 건 · 기각 0). 계기판은 실제로 돌려 실측했다
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있으나 ⑲ 의 근거가 무너진다 — "더 작은 표면이 이미 있다" 가 참이려면 추출기를 다시 써야 하고, 그러면 「이미 있는 것을 승격」이 아니라 「새로 만들기」다. 걸리는 곳은 `B1`~`B4` 넷 전부

### `extract.py` 의 기각 규칙과 `반환문_항_수` 의 기각 규칙이 서로를 반증한다 — `B2` 를 켜면 검사 둘이 동시에 못 선다

- 어떻게 실패하나: `반환문_항_수` 는 기각 절에서 불릿이든 표든 센다(2026-08-19 독립 리뷰 3 라운드가 일부러 그렇게 고쳤다). 그래서 이 회차 R1 은 22 대 22 로 초록이다. 추출기는 불릿만 센다 → 17. `B2` 가 「추출 산출 대 커밋된 레코드」를 켜면, 레코드를 22 로 두면 `B2` 가 빨갛고 17 로 줄이면 기존 회차 레코드 검사가 빨갛다. 초록으로 만드는 유일한 길이 추출기를 고치는 것인데, 그러면 그 추출기로 만든 옛 회차의 레코드와도 갈린다. 그 회차는 `B3` 의 하한 밖이라 안 재지만, 하한을 그렇게 그으면 추출기를 실제로 쓴 유일한 회차가 검사 밖이 되어 `B1` 의 근거 문장이 검증되지 않는다
- 어디가 걸리나: `xtask/src/main.rs:3145-3164` · `extract.py:24-56`
- 획득: 조회 — 카운터 로직을 그대로 파이썬으로 재현해 사전부검 반환문 14 개 전부에 돌렸고(전부 커밋된 수와 일치), 같은 파일에 추출기를 돌려 갈림을 관측했다
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳은 `B2`·`B5` 와 회차 레코드 검사 — 검사 하나가 다른 검사를 빨갛게 만드는 구조라 어느 쪽을 고쳐도 다른 쪽 판정문의 뜻이 바뀐다

### `A2` 발화 46 중 44 가 닫힌 앞 회차의 것이고 그중 열이 금지역이다 — `A6` 의 「다시 연다」 가지가 아무것도 안 고치고 검사를 초록으로 만든다

- 어떻게 실패하나: 개정 ① 이 정한 술어를 저장소 전량에 실제로 돌렸다. 닫힘 595 행 중 잰 것 382 · 원리상 못 잼 213 · 발화 46. 발화의 회차별 분포는 2026-08-22 가 2 · 2026-08-23 이 44 · 이 회차 0. 해악도는 거짓신호 27 · 금지역 10 · 실패 4 · 미관 5. 즉 `A5` 는 선다. 그런데 처분 표의 첫 행이 「실제로 안 닫혔다 → 상태를 열림으로」이고, 열림이 되면 그 행은 `A2` 의 모집단에서 빠져 검사가 즉시 초록이 된다. 가장 싼 경로가 「발견을 열어 두고 영원히 방치」이고, 그러면 앞 회차 원장에 금지역 열이 열린 채로 남는데 그 회차의 계기판을 아무도 다시 안 돌린다. ⑳ 이 막으려던 것이 형태만 바꿔 돌아왔다
- 어디가 걸리나: `.palimpsest/rounds/2026-08-23-check-verifies-work/intent.md:319-325` · `.claude/skills/round/bin/record.py:502-514` · `.palimpsest/rounds/2026-08-23-agent-laziness-behavior/findings.jsonl`
- 획득: 조회 — git 을 SHA 28 개에 캐시로 돌려 닫힘 595 행 전수를 판정했고, 열림축 검증이 「열림」 행에 아무것도 요구하지 않음을 읽었다
- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 되돌리기 어렵다 — 44 행을 사람이 한 건씩 판정해야 하고, 그 대상이 "앞 회차의 판정을 다시 열지 마라" 가 지키려는 회차다. `A5`·`A6`·`P1` 이 전부 여기 매달린다. `P1` 은 「열림으로 밀기」로 항상 달성 가능해서 판정 근거가 되지 못한다

### `report.md` 를 안 쓰면 `C1`·`G1`·`G2` 를 한 번에 비껴간다 — 그 문으로 이미 회차 하나가 나갔다

- 어떻게 실패하나: 「끝난 회차」의 유일한 기계 정의가 종료 보고 파일의 존재이고, 그 파일이 있어야 한다고 요구하는 검사는 하나도 없다. 실측: `2026-08-18-round-protocol/state.md:3` 이 "단계: 종료. 완수 조건 전부 닫힘 · 효과 관측 · CI 초록" 이라 적었는데 그 디렉터리에는 의도와 상태뿐이다 — 게이트도 없다. 지금 판정문이 그것을 「게이트 없음」으로만 부르고 오류가 아니다. `C1`·`G1`·`G2` 셋이 전부 이 술어 위에 서므로, 종료 보고를 안 쓰는 한 수가 셋을 동시에 무력화한다. #90 이 말한 「조용히 모집단 밖으로 빠지는 문」의 더 넓은 문짝을 계획이 안 닫는다
- 어디가 걸리나: `xtask/src/main.rs:3663` · `:3826` · `.palimpsest/rounds/2026-08-18-round-protocol/`
- 획득: 조회 — 여덟 회차 디렉터리를 훑어 종료 보고 유무를 셌고(여섯), 그 술어의 사용처 둘을 읽었고, 판정문이 그 회차를 「게이트 없음」으로만 부르는 것을 관측했다
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(「상태가 종료라고 적었으면 종료 보고가 있어야 한다」 한 줄). 걸리는 곳은 `C1`·`C3`·`G1`·`G2`·`G3` 다섯

### `G1` — 옛 종료 보고 둘이 진짜 세 번째 원장을 담고 있다. ㉔ 의 「모집단 0」이 거짓이고 G 축의 소급 정책이 어디에도 없다

- 어떻게 실패하나: ㉔ 은 "엄격하면 모집단 0 — 여섯 보고에 §9 형식 검산 줄이 하나도 없다" 를 근거로 `G1` 을 뒤집었다. 실측하면 두 보고가 §9 표준 표의 검산과 같은 모양의 줄을 담는다 — `2026-08-19-finding-records/report.md:13` 의 「통과 44 · 반증 0 · 대조 불가 2 · 미측정 0 = 46」과 `2026-08-18-completion-condition/report.md:93` 의 「판정: 통과 38 · 반증 2 · 대조 불가 1 · 미측정 0 = 41」. 둘 다 서술문이 아니라 진짜 검산 줄이다. 그래서 구현자가 마주치는 갈림은 둘 다 나쁘다: 접두를 요구하면 모집단이 정말 0 이라 태어나면서 죽은 가지이고, 모양으로 잡으면 끝난 회차 둘이 빨개진다. 그리고 개정 ⑯ 은 소급 정책을 A 축과 B·C·D 축으로만 갈랐고 G 축을 안 적었다
- 어디가 걸리나: `.palimpsest/rounds/2026-08-19-finding-records/report.md:13` · `.palimpsest/rounds/2026-08-18-completion-condition/report.md:93` · `intent.md:245` · `intent.md:407` · `intent.md:391`
- 획득: 조회 — 여섯 종료 보고 전부에 검산 낱말과 수 패턴 grep 을 돌려 두 줄을 원문으로 뽑았고, 개정 ⑯ 이 G 축을 안 다룬 것을 확인했다
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(하한을 명시하면 끝). 그러나 하한을 이 회차 이후로 걸면 `G1` 의 모집단이 이 회차 자기 보고 하나뿐이라 항등식형에 가까워지고, 안 걸면 옛 보고 둘을 고쳐야 한다. 걸리는 곳은 `G1`·`G3`·`N9`

### `B5` 는 이미 존재하지 않는 병을 고치는 조건이다 — 기각 표 헤더는 지금도 안 세어진다

- 어떻게 실패하나: ㉚ 은 "카운터가 기각 표의 헤더 행을 데이터 행으로 센다" 를 실측으로 적었다. 코드는 헤더 접두 하나를 이미 뺀다. 커밋된 R2 반환문의 기각 표 헤더가 그 접두로 시작하고, 그 파일에 규칙을 그대로 돌리면 시나리오 18 더하기 기각 7 은 25 이며, 실제 검사 판정문도 25 대 25 를 낸다. 즉 `B5` 는 무조건 「통과」로 판정되고 아무것도 안 잰다. 조건을 늘려 놓고 항등식으로 채우는 것이 정확히 ㉕ 가 막으려던 퇴출구와 같은 형태다
- 어디가 걸리나: `xtask/src/main.rs:3155-3162` · `intent.md:214` · `intent.md:413`
- 획득: 조회 — 카운터의 분기를 그대로 재현해 반환문 14 개에 돌렸고(전부 커밋 수와 일치), 실제 바이너리의 판정문에서 25 대 25 를 읽었다
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(조건을 지우거나, 헤더 접두가 다른 표로 술어를 바꿔 실제 구멍을 겨눈다). 걸리는 곳은 `B5` 하나 — 다만 이 회차가 자기 조건 하나를 항등식으로 채운 채 「했나」를 가르치는 모양이 된다

### 예외표가 손 전사의 대부분을 그대로 남긴다 — `B4` 의 「손 전사로 남은 라운드 수」가 0 을 내면서 필드의 다수는 여전히 손으로 적힌다

- 어떻게 실패하나: `B2` 가 "사람의 판단은 예외표로 덮는다" 고 적었다. 실물을 세어 보면 리뷰 예외표가 5 라운드에 82 행이고 어떤 라운드는 발견 25 중 20 행에 예외가 붙는다. 예외표가 지는 칸은 처분·조건·해악도·유효성인데 그것이 레코드의 판단 축 전부다. 즉 기계 추출이 없애는 것은 id·라운드·출처·경로·요약이고, 갈릴 수 있는 칸은 그대로 손으로 남는다. 그런데 `B4` 는 「손 전사로 남은 라운드 수」를 노출하므로, 추출기를 돌린 라운드는 예외표 20 행을 손으로 적었어도 0 으로 보고된다
- 어디가 걸리나: `.palimpsest/rounds/2026-08-19-finding-records/review/disposal-overrides.jsonl` · `premortem/disposal-overrides.jsonl` · `intent.md:211-213`
- 획득: 조회 — 예외표 두 파일을 전부 읽어 라운드별 행 수를 셌고, 같은 라운드의 발견 수와 비교했다
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(`B4` 를 「손으로 적은 칸 수」로 바꾸면 정직해진다). 걸리는 곳은 `B2`·`B4` 와 ⑲ 의 "기계 추출은 전사 자체를 없앤다" 라는 문장

### `extract-review.py` 의 ID 채번이 커밋된 레코드와 안 맞는다 — 짝짓기가 이 회차 자신의 리뷰어 여덟에서 먼저 깨진다

- 어떻게 실패하나: 앞 회차 리뷰 8 라운드에 리뷰 추출기를 돌려 커밋된 레코드와 id 로 대조했다. 149 산출 중 50 건이 레코드에 없는 id 이고, 남은 99 건도 경로 99 · 요약 82 · 조건 9 이 갈린다. 원인 하나가 같은 회차 안에서 ID 표기가 갈렸다는 것이다 — 두 자리를 채운 것과 안 채운 것이 같은 파일에 있다. 추출기는 두 자리 채움으로만 낸다. `B2` 가 「추출 산출 대 커밋된 레코드」를 대게 하면, 짝을 무엇으로 잡든 이 회차의 리뷰어 여덟 라운드가 첫 표적이고, 옛 회차를 하한 밖으로 밀어도 이 회차가 자기 레코드를 추출기가 내는 꼴로 강제 정렬해야 한다. RED-4 가 반환문의 표기 갈림만 적었는데, 레코드 원장 자체에도 같은 갈림이 이미 들어와 있다
- 어디가 걸리나: `.palimpsest/rounds/2026-08-19-finding-records/review/extract-review.py:110` · `.palimpsest/rounds/2026-08-23-agent-laziness-behavior/findings.jsonl`
- 획득: 조회 — 추출기를 8 라운드 반환문에 돌려 커밋된 253 행과 필드 단위로 대조했고, 레코드의 id 목록을 직접 뽑았다
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(채번을 한 자리로 못 박는다). 걸리는 곳은 `B2`·`B3`·`N3`·`P2` — 그리고 RED-4 가 「반환문의 표기」로만 적어 이 자리를 안 가리킨다

### `C6` 「뜻마다 정본 한 자리」는 문서에 소비자를 못 주고, 뜻 하나의 자리가 동결된 앞 회차 게이트다

- 어떻게 실패하나: 그 이름은 지금 살아 있는 6 파일에 걸쳐 있고, 전체 74 자리 중 다수는 회차 레코드나 게이트라 동결이다. `C6` 이 "정본 한 자리가 서고 나머지가 거기서 읽는다" 를 요구하는데, 마크다운은 「읽을」 수 없다 — 인용만 가능하고 그것이 ⑨ 가 금지한 「손으로 벤 거울」이다. 그리고 앞 회차 게이트는 끝난 회차의 판정 문서라 "앞 회차의 판정을 다시 열지 마라" 에 걸린다. 남는 길은 코드 둘만 스키마에서 읽게 하고 문서 넷은 손 인용으로 남기거나, 동결 문서를 건드리는 것뿐인데, 앞이면 `C6` 은 선언면 하나를 새로 만든 것이고 뒤는 범위 밖 위반이다
- 어디가 걸리나: `.claude/skills/round/bin/record.py:451` · `:569-572` · `xtask/src/main.rs:3688-3689` · `docs/gates/README.md:90` · `docs/gates/agent-laziness.md`
- 획득: 조회 — 전수 grep(74 자리 · 22 파일 · 살아 있는 6 파일)과, 스키마 함수가 상수를 렌더링만 하고 xtask 는 키 하나만 읽는다는 것을 코드로 확인했다
- 모집단: 규약
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(뜻 셋 중 코드가 실제로 분기하는 둘만 정본으로 세우고 나머지는 안 만든다). 걸리는 곳은 `C6`·`E1`·`E2` — 특히 `E1` 이 소비자 없는 산문 키를 격하하는데 `C6` 은 같은 자리를 정본으로 승격하려 해서 두 조건이 서로를 되돌린다

### `E3` 는 `--schema` 가 상수의 렌더링이라는 사실 위에서만 서고, 산문 열여섯은 소비자가 0 이다

- 어떻게 실패하나: 스키마 출력의 최상위 키는 열둘인데 xtask 가 읽는 것은 하나다. 필드·필수·enum·대응표는 상수를 그대로 실은 것이라 고치면 `record.py check` 의 동작이 바뀌지만, 열림축의 두 키·조건파서의 규칙·게이트파서의 11 키·합계검산의 4 키는 순수 산문이고 어느 코드도 안 읽는다. `E3` 를 만족하는 키와 `E2` 의 격하 금지 목록이 겹치지 않는다 — `E2` 가 지키라는 셋 중 열림축의 둘과 합계검산 전부가 소비자 0 이다. 그래서 `E2` 는 "격하 전에 소비자를 만든다" 를 요구하는데 그 소비자가 이 회차가 새로 만들 장치 셋이 되고, 각각이 다음 라운드의 새 선언면이다
- 어디가 걸리나: `.claude/skills/round/bin/record.py` 스키마 함수 · `xtask/src/main.rs:3215` · `intent.md:234-236`
- 획득: 조회 — 스키마 출력의 키를 뽑고, xtask 소스에서 스키마 첨자 접근을 전수 grep 해 하나뿐임을 확인했고, 스키마 함수가 상수를 참조하는 것을 읽었다
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳은 `E1`·`E2`·`E3`·`N8`·`D3` — `D3` 가 절 목록을 스키마에 두고 「검사가 거기서 읽는다」로 못 박은 것은 정합하지만, `E2` 의 셋에는 그런 못이 없다

### 조건 57 개 · 리뷰 8 라운드 — 실측상 조건이 늘면 리뷰어의 합격선 축 커버리지가 무너진다

- 어떻게 실패하나: 리뷰어 반환문의 합격선 축 데이터 행을 회차별로 셌다. 조건 39 개였던 앞 회차는 8/8 라운드가 39 행을 냈다. 조건 69 개였던 회차는 네 라운드만 69 행이고 나머지는 0 · 2 · 20 · 30 이다 — 절반이 절반 미만을 쟀다. 이 회차는 57 이고 그중 열넷은 격리 사본에서 돌려야만 판정 가능하다. 리뷰어가 못 재면 판정은 미측정인데, 원장 둘 대조는 미측정이 양쪽에서 같기만 하면 초록이다. 결과: 조건 수를 늘려 분모를 지킨 ㉕ 의 효과가 「미측정으로 채운 분모」로 되돌아온다
- 어디가 걸리나: `.palimpsest/rounds/2026-08-22-agent-laziness/review/r3-raw.md` 부터 `r6-raw.md` · `xtask/src/main.rs:3812-3820` · `intent.md:189-286`
- 획득: 조회 — 두 회차 리뷰 반환문 16 개의 합격선 축 표 데이터 행을 세고 절 목록을 뽑았다
- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(리뷰어에게 전량 판정을 요구하거나, 미측정 상한을 조건으로 세운다). 걸리는 곳은 `K2`·`F1`·`F2`·`F3` 와 57 상자 전부

### `D1`·`D2` 가 만드는 「반드시 있어야 하는 절」의 첫 표적은 기각 절이고, 앞 회차 리뷰어 여덟 중 여섯이 그것을 안 냈다

- 어떻게 실패하나: 앞 회차 리뷰 반환문 여덟의 절 목록을 뽑으면 기각 절은 둘뿐, 미측정 목록은 0/8, 절 수는 3~6 이다. 반면 리뷰어 정의는 일곱 절을 요구하고 "기각한 것의 유효성은 거짓이다 — 그것이 「발견의 몇 %가 헛것인가」를 세는 유일한 길" 이라 적는다. `D2` 가 절을 가를 때 기각 절을 「있으면」에 두면 ⑧ 축이 계속 비고, 「반드시」에 두면 이 회차 리뷰어 여덟이 실측 6/8 확률로 형식 오류를 낸다. 그리고 「무엇이 반드시인가」를 정하는 자가 계획 어디에도 없다. 덧붙여 앞 회차 리뷰어들은 정의에 없는 절을 추가했다 — 절 목록을 닫힌 집합으로 검사하면 그것도 전부 걸린다
- 어디가 걸리나: `.claude/agents/pal-independent-reviewer.md:131-165` · 앞 회차의 리뷰 반환문 여덟 · `intent.md:227-229`
- 획득: 조회 — 리뷰 반환문 16 개의 절머리를 전수로 뽑아 정의의 일곱과 대조했다
- 모집단: 규약
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳은 `D1`·`D2`·`D3`·`N7` 과 리뷰어 정의 · 그리고 ⑧ 계기판

### `A2` 의 경로 비교가 Windows 에서 갈릴 자리가 이미 코드에 있다

- 어떻게 실패하나: `A2` 는 git 출력(항상 슬래시 구분)과 레코드의 경로를 대야 한다. 같은 축의 기존 좌표 검사는 이미 역슬래시를 슬래시로 바꾸는 정규화를 손으로 하고 있고, 그럼에도 절대경로에서 플랫폼이 갈려 CI 가 빨갛다. `A2` 가 같은 정규화를 안 하면 같은 갈림이 새 검사에 재생산되고, macOS 에서만 재고 「섰다」로 적는 형태가 된다 — ADR-0023 이 금지한 자리다. 구현 전이라 실측으로 확인하지 못했다
- 어디가 걸리나: `xtask/src/main.rs:3532` · `:3509` · `docs/adr/0023-consistent-method-and-result-across-platforms.md`
- 획득: 추정 — 기존 정규화 코드와 CI 실패 원문은 읽었으나 `A2` 는 아직 없어서 돌려 보지 못했다
- 모집단: 저장소
- 유효성: 추정
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳은 `A1`·`A2`·`K9`

## 내가 기각한 것

| # | 기각한 것 | 왜 아니었나 | 어디가 걸리나 | 획득 | 모집단 | 유효성 | 해악도 | 대상 |
|---|---|---|---|---|---|---|---|---|
| 1 | 예외표를 회차마다 만들면 sunset 트리거가 참이 된다 | 트리거 글롭이 한 겹이고 확장자로 정확히 거른다. 이미 예외표 두 개가 저장소에 있는데 검사는 「트리거 0건」을 낸다 — 실제 실행으로 확인했다 | docs/sunset.toml · xtask/src/main.rs:2554-2583 | 조회 | 저장소 | 거짓 | 미관 | 계획대상 |
| 2 | 조건이 57 개로 늘어 조건 파서나 ID 문법이 깨진다 | 파서에 실제로 걸었다 — 57 개 전부 파싱되고 형식오류 0 · ID 중복 0. 두 자리 숫자도 문법에 든다 | .claude/skills/round/bin/record.py:160-170 | 조회 | 자기장치 | 거짓 | 실패 | 계획자신 |
| 3 | `C2` 의 선언 목록을 README 에 두면 짝 중복으로 `C4` 가 무너진다 | ㉖ 이 이미 회차 이름만 적기로 고쳤고, 짝짓기 열쇠는 경로 문자열뿐이다. README 가 지금도 그 패턴을 산문으로 담고 있는데 짝으로 안 세어진다 — 실행 판정문으로 확인 | docs/gates/README.md:85 · xtask/src/main.rs:3664-3676 | 조회 | 자기장치 | 거짓 | 실패 | 계획자신 |
| 4 | `A2` 가 이 회차 자신의 사전부검 행 다수를 빨갛게 만든다 | 경로 접촉으로 재면 그렇다. 그러나 개정 ① 이 이미 「처분이 요구하는 자리」로 고쳤고, 계획수정 계열을 의도 파일 접촉으로 재니 이 회차 전부가 통과한다. 전수로 다시 돌려 확인했다 | .palimpsest/rounds/2026-08-23-check-verifies-work/findings.jsonl | 조회 | 회차기록 | 거짓 | 실패 | 계획대상 |
| 5 | `G2` 가 규약이 시킨 절 이름을 금지 이름으로 오인한다 | ⑭ 가 절머리 정확 일치로 이미 좁혔고, 두 문자열은 부분 문자열 관계도 아니다. 옛 보고 여섯의 절머리를 전수로 뽑아 대조하니 충돌 0 | .claude/skills/round/SKILL.md:420-445 | 조회 | 자기장치 | 거짓 | 거짓신호 | 계획자신 |
| 6 | 뿌리를 열면 의존 정책 검사나 파이썬 파서 호출이 원본 경로를 계속 쓴다 | 코드를 읽으니 의존 정책은 이미 작업 디렉터리를 뿌리로 잡고, 파서 호출도 뿌리에서 만든 경로를 넘긴다. 실행시점 문서 수집도 뿌리에서 뽑는다 — 뿌리 인자화에 이미 열려 있다 | xtask/src/main.rs:673-698 · :3585-3606 · :2926 | 조회 | 저장소 | 거짓 | 실패 | 계획대상 |

새 범주: 플래그가 조용히 무시되어 「사본을 쟀다」가 문면으로만 참이 되는 자리 (무시되는 인자)
