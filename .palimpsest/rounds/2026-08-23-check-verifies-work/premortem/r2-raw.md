# 사전부검 R2 — 원 반환문

> 보존본. **고치지 않는다** — 합계 검산의 독립된 둘째 원천이다.
> 처분 `40883a6`(조건 35 → 50) 위에서 쟀다.
> 돌린 것: `cargo xtask check`(원본), 격리 사본 재현 4 건(스키마 4 승격 · 버전 상수 분기 ·
> 프리빌트 xtask 의 뿌리 · 얕은 클론), 전량 집계 6 건(570 닫힘 행 × 27 SHA 의 diff 접촉 ·
> report.md 검산 두 갈래 · 리뷰어 반환문 25 개 절 분포 · `--schema` 키 소비자 전수).
> ⚠ **전사 시 표 헤더 하나를 규약 형식으로 맞췄다.** 원 반환문의 기각 표 헤더가
> `| 기각한 것 |` 이었는데, `반환문_항_수` 가 `| #` 로 시작하지 않는 헤더 행을
> **데이터 행으로 센다** — 그래서 항이 하나 부풀었다(25 → 26). 헤더에 `#` 열을 더하고
> 번호를 붙였다. **내용은 한 글자도 안 바꿨다.** 이 카운터 버그는 실측 발견으로 세웠다.


### 격리 사본에서 프리빌트 `xtask` 는 사본이 아니라 원본을 잰다 — 음성 대조 아홉이 통째로 헛것이 된다

- 어떻게 실패하나: `## 어디서 어떻게 거는가` 가 N1·N3~N10 아홉을 「격리 사본」에 건다. 그런데 `repo_root()` 는 `env!("CARGO_MANIFEST_DIR")` 로 컴파일 시점에 박히고 CLI 인자도 환경변수도 없다. 사본으로 `cd` 해서 기존 바이너리를 돌리면 원본 저장소를 잰다. 실제로 심어 봤다: 사본의 `2026-08-22-agent-laziness/findings.jsonl`(297행)을 쓰레기 한 줄로 파괴하고 사본 cwd 에서 돌렸더니 `레코드 1002행` · `21/21 통과` — 원본의 수다. 음성 대조는 「안 빨개졌다」로 읽히고, 그 관측이 `F3`(「걸었고 빨개졌다」)에 그대로 기록된다. 규약은 격리 사본을 쓰라고만 적고 사본 안에서 다시 빌드해야 한다는 말이 어디에도 없다
- 어디가 걸리나: `xtask/src/main.rs:584-589` · `xtask/src/main.rs:35-38` · `.claude/skills/round/SKILL.md:50,97` · `.claude/agents/pal-premortem-sweeper.md:49` · 잠근 의도 `## 어디서 어떻게 거는가`
- 획득: 조회 — 사본에 `target/debug/xtask` 를 복사하고 사본의 레코드를 파괴한 뒤 사본 cwd 에서 실행. 출력이 원본과 바이트 단위로 같음을 확인
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(사본에서 `cargo build` 하거나 `--root` 인자를 연다). 걸리는 곳은 조건 아홉 전부 + `F2`·`F3` + 앞 회차들이 「격리 사본에서 재현했다」고 적은 판정 전부의 신뢰도

### 스키마 4 로 올리는 순간 이 회차 자신의 `findings.jsonl` 이 잠긴다

- 어떻게 실패하나: `B1` 이 스키마 4 를 요구한다. 그런데 이 회차의 `findings.jsonl` 은 이미 존재하고 머리 줄이 3 이다(사전부검 R1 의 22 행). `cmd_add` 는 머리 줄 버전이 최신과 다르면 rc=1 로 거부한다 — "이주하거나 새 파일을 열어라". 격리 사본에서 두 상수를 4 로 올리고 이 회차 디렉터리에 한 줄을 넣어 봤더니 정확히 그 메시지로 죽었다. 「이주」는 22 행에 결박을 손으로 붙이는 것이고, 「새 파일」은 한 회차에 레코드 파일이 둘이 되어 회차 산출의 삼중 열쇠 검산과 계기판 ⑦⑧⑨ 를 동시에 깬다
- 어디가 걸리나: `.claude/skills/round/bin/record.py:600-614` · `.palimpsest/rounds/2026-08-23-check-verifies-work/findings.jsonl:1` · `.claude/skills/round/bin/dashboard.py:215`
- 획득: 조회 — 격리 사본에서 두 상수를 4 로 올리고 `record.py add` 재현
- 모집단: 자기장치
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있으나 이 회차의 기록 흐름이 그 순간 멈춘다. 22 행 + 앞으로 쌓일 사전부검 3 · 독립리뷰 8 회차분 전부가 걸린다

### 버전 상수가 둘인데 쓰는 자는 전역을, 재는 자는 사전을 본다 — 새 레코드가 조용히 「형식 이전」으로 태어난다

- 어떻게 실패하나: 전역 상수와 종류별 사전이 따로 산다. `cmd_add` 는 머리 줄을 전역으로 쓰고 행 검증도 전역을 기본값으로 쓰는데, `cmd_check` 와 `cmd_add` 의 거부 판정은 사전을 본다. `B1` 이 사전만 4 로 올리면 첫 `add` 는 rc=0 으로 머리 줄 3 을 쓰고, `check` 는 그 파일을 형식 이전으로 통과시키고, 두 번째 `add` 에서야 죽는다. 격리 사본에서 그대로 재현했다. 그 사이 그 회차의 레코드는 결박 검사 밖에 있으면서 검사는 초록이다 — `B3`·`B4` 가 세우려는 「조용한 fallback 을 없앤다」가 첫 파일에서 무너진다
- 어디가 걸리나: `.claude/skills/round/bin/record.py:52` · `:58` · `:614-618` · `:562-575`
- 획득: 조회 — 사본에서 사전만 4 로 올려 add(rc=0) → check(rc=0, 형식 이전) → add(rc=1) 순으로 재현
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 한 줄 고침으로 되돌릴 수 있다. 걸리는 곳은 `cmd_add`·검증의 기본 인자·`--schema` 의 버전 키

### `B5` 가 회차 내내 검사를 빨갛게 한다 — 초록으로 만드는 유일한 길이 `## 범위 밖` 이 금한 소급 이주다

- 어떻게 실패하나: `B5` 는 "끝난 회차 중 가장 최근 것이 최신 스키마여야 한다" 다. 지금 「끝난 회차 중 가장 최근」은 2026-08-23-agent-laziness-behavior 이고 그 레코드는 스키마 3 · 253 행이다. 이 회차가 4 로 올리는 커밋부터 자기 종료 보고를 커밋하는 마지막 커밋까지 `B5` 자는 계속 빨갛다. 그 사이 `K1` 이 원리상 못 선다. 빨강을 없애려면 253 행에 결박을 소급해야 하는데 그것이 `## 범위 밖` 의 네 번째 항이다. 퇴로 2 는 여기서 이미 그 하한이 문제의 원인이라 안 듣는다
- 어디가 걸리나: 잠근 의도 `B5` · `## 범위 밖` 4 항 · `xtask/src/main.rs:3831-3853` · `.palimpsest/rounds/2026-08-23-agent-laziness-behavior/findings.jsonl:1`
- 획득: 조회(각 회차의 머리 줄 버전과 종료 보고 유무 전수) + 추정(기존 하한 패턴으로 구현한다는 가정)
- 모집단: 자기장치
- 유효성: 추정
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(`B5` 의 술어를 「이 회차 이후에 연 회차」로 좁힌다). 걸리는 곳은 `B5`·`K1`·CI 전 잡

### `G1` 은 엄격하면 모집단 0, 느슨하면 옛 보고 둘을 거짓 실패시킨다 — 둘 다 실측했다

- 어떻게 실패하나: 엄격 갈래로 기존 `record.py` 의 검산 정규식을 여섯 보고에 대면 히트 0 이다. 유일한 진짜 검산 줄조차 낱말과 공백이 달라 안 걸린다 → 모집단 0 인 자, 퇴로 1 의 경고가 금지한 모양 그대로. 느슨 갈래는 히트 3 중 둘이 서술문이다 — 2026-08-23 보고의 "종료 보고는 통과 29 였고 게이트는 통과 31 이었다", 2026-08-22 보고의 "게이트는 통과 43 이라 적고". 둘 다 「여기에 수를 안 적는다」를 설명하는 문장이다. 초록으로 만드는 유일한 길이 끝난 회차의 종료 보고를 고치는 것인데 `## 범위 밖` 이 그것을 막는다. 개정 ⑭ 는 이 병을 `G2` 쪽에서만 고쳤고 `G1` 은 개정 ⑬ 에서 「있으면 댄다」로 바뀌며 오히려 느슨해졌다
- 어디가 걸리나: `.palimpsest/rounds/2026-08-23-agent-laziness-behavior/report.md:15` · `.palimpsest/rounds/2026-08-22-agent-laziness/report.md:14` · `.palimpsest/rounds/2026-08-19-finding-records/report.md:13` · `.claude/skills/round/bin/record.py:270-274` · 잠근 의도 `G1`·개정 ⑬
- 획득: 조회 — `record.py` 의 검산 함수를 import 해 여섯 보고에 전수로 대고, 느슨 정규식과 나란히 세어 비교
- 모집단: 회차기록
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳은 `G1`·`N9` 와 끝난 보고 여섯

### 처분 `기각` 187 행(닫힘의 33%)에 「요구하는 자리」가 원리상 없다 — `A3` 가 모집단 수를 실으면 안 재는 자가 「쟀다」로 보인다

- 어떻게 실패하나: 닫힘 570 행의 처분 분포는 정정 339 · 기각 187 · 확대 20 · 범위밖 16 · 축소 8 · 전환 0 이다. `A2`·`A3` 는 「그 처분이 요구하는 자리를 만졌나」를 처분마다 잰다. 기각 은 아무것도 안 고치는 처분이라 요구하는 자리가 없다. 후보 하나(그 회차의 레코드 파일을 만졌나)를 실제로 대 보면 187 중 136 이 거짓이다 — 레코드는 처분 커밋보다 나중에 쓰이는 일이 흔하기 때문이다. 그러니 구현은 둘 중 하나가 된다: 항상 참인 술어(모집단 187, 발화 0) 또는 136 건 거짓 실패. 앞을 고르면 `A3` 가 판정문에 「기각 187행」을 실어 안 재는 가지가 「187 행을 쟀다」로 보인다. 개정 ② 는 모집단 0 만 막았고 모집단이 큰데 술어가 항등인 경우는 안 막았다. 축소 8 행은 전부 경로가 없어 실질 모집단 0 이고 전환은 0 행이다
- 어디가 걸리나: 잠근 의도 `A2`·`A3`·퇴로 1 · `.claude/skills/round/bin/record.py:76` · `:110-121`
- 획득: 조회 — 570 닫힘 행 전량에 `git show --name-only` 를 대어 처분과 접촉의 교차표를 냈다 (기각: 접촉 82 · 미접촉 66 · 경로없음 39 / 레코드 파일 접촉 51 · 미접촉 136)
- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(기각·축소·전환을 「원리상 못 재는 처분」으로 판정문에 따로 세운다). 걸리는 곳은 A 축 전부와 `A6`·`A8` 의 잔여 계산

### 결정 5 가 검사를 데이터로 훈련시킨다 — 사람이 지기로 한 단계에 절차도 증거 요구도 없다

- 어떻게 실패하나: 결정 5 는 "빨개진 행은 레코드를 정정한다 — 실제 고침 커밋 SHA 로" 다. 그런데 「그 커밋이 그 발견을 실제로 고쳤나」는 이 계획 자신이 "기계가 원리상 못 안다 · 사람이 진다" 로 적었고, 사람이 무엇을 증거로 대야 하는지는 어디에도 없다. 남은 자연스러운 절차는 「그 경로를 만진 커밋 아무거나 찾아 넣기」이고, 그러면 검사는 언제나 초록이 되며 레코드에는 확인되지 않은 인과가 사실로 적힌다. 게다가 결정 5 의 문장은 `--schema` 가 선언한 `닫은커밋` 의 뜻과 어긋난다 — 개정 ① 이 `A2` 쪽은 그 정의에 맞춰 고쳤으나 결정 5 의 문장은 안 고쳤다
- 어디가 걸리나: 잠근 의도 `## 잠근 결정` 5 행 · `## 기계가 지는 것과 사람이 지는 것` · `## 개정` ① · `.claude/skills/round/bin/record.py:428-431`
- 획득: 조회(세 문서의 정의문을 나란히 대조) · 추정(정정 절차가 실제로 어떻게 굴러갈지)
- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(정정마다 근거를 별 칸에 남기게 하거나, 못 대면 대조불가로 둔다). 걸리는 곳은 A2 가 발화시킬 수십 행 전부

### `B1`(결박·스키마 4)보다 작은 표면이 이미 저장소에 있다 — 기계 추출 + 예외표가 그것이다

- 어떻게 실패하나: `B1` 이 푸는 문제는 "전사자가 요약을 다시 써서 929 쌍 중 337 만 맞는다" 다. 그런데 원 반환문에서 레코드를 기계로 뽑는 스크립트가 이미 두 개 있다 — `premortem/extract.py`(103행)와 `review/extract-review.py`(128행) — 그리고 「메인의 판단(처분)만 손으로 덮는」 예외표 구조까지 갖춰져 있다. 기계로 뽑으면 결박은 원리상 불필요하고 스키마 4 도 불필요하다. 실측: 이 파이프라인을 쓴 회차는 2026-08-19-finding-records 하나뿐이고, 그 뒤 세 회차에는 예외표가 없다 — 손 전사로 돌아갔고 그것이 #92 의 원인이다. 계획은 「도구를 규약으로 승격한다」는 더 작은 선택지를 검토한 흔적 없이 새 필드 + 새 스키마 버전 + 새 검사를 세운다
- 어디가 걸리나: `.palimpsest/rounds/2026-08-19-finding-records/premortem/extract.py` · `.palimpsest/rounds/2026-08-19-finding-records/review/extract-review.py` · 그 회차의 예외표 둘 · 잠근 의도 `B1`·개정 ⑤
- 획득: 조회 — 회차별 산출과 추출 스크립트 전수 목록 + 각 파일의 머리 줄 확인
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기는 싸다(지금 고르면 B 축 다섯이 통째로 줄어든다). 나중에 되돌리면 스키마 4 로 쓰인 레코드가 남아 이주가 필요하다

### `E2` 의 격하 대상에 A 축의 정의문과 `B5` 가 필요로 하는 키가 들어 있다

- 어떻게 실패하나: `--schema` 의 키 중 어느 코드에도 소비자가 없는 것을 전수로 찾으면 버전들·열림축·게이트파서·조건파서·합계검산·대응표 다(xtask 가 읽는 것은 종류 하나뿐). `E2` 는 그것들을 설명 아래로 격하하고 「정본 아님」을 선언한다. 그런데 열림축의 닫는자 는 `닫은커밋` 이 무엇인지를 선언하는 유일한 자리이고 개정 ① 이 A2 를 고친 근거 자체다 — 같은 회차가 자기 A 축의 정의를 「정본 아님」으로 만든다. 버전들 은 `B5` 를 xtask 에서 걸 때 물어봐야 하는 키다 — 격하하면 xtask 가 최신 버전을 손으로 박게 되고 그것이 #94 의 다음 사례다. 합계검산 은 이 회차가 `B1` 으로 다시 쓰는 규칙인데 같은 회차가 「정본 아님」으로 선언하는 모양이 된다
- 어디가 걸리나: `.claude/skills/round/bin/record.py:420-470` · `:422` · `:423-432` · `xtask/src/main.rs:3215-3220` · 잠근 의도 `E1`·`E2`·`B5`
- 획득: 조회 — 버전들·열림축 을 전수로 grep 해 소비자 0 을 확인하고, xtask 의 스키마 접근 자리를 전수로 찾음
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(격하 전에 소비자를 먼저 만든다). 걸리는 곳은 `--schema` 키 여섯

### 「형식 이전」이 한 이름으로 서로 다른 셋을 가리키는데 `C6` 은 「세 자리」만 맞춘다

- 어떻게 실패하나: 저장소에서 그 이름은 5 파일 23 자리에 살고 뜻이 셋이다 — 게이트에 표준 표가 없다 · 레코드 스키마가 옛 버전이다 · 의도의 조건에 형식 오류가 있다. 여기에 이 회차가 넷째를 더한다 — `B3` 의 「옛 회차는 결박을 안 진다」와 `C2` 의 「표준 표 면제 선언 목록」. `C6` 은 "세 자리가 같은 커밋에서 맞춰진다" 인데 세 자리가 아니고 같은 뜻도 아니다. 셋을 맞추는 커밋이 나머지 스무 자리를 낡게 두고 `C6` 은 초록으로 닫힌다
- 어디가 걸리나: `.claude/skills/round/bin/record.py:256,283,313,451,569,572` · `.claude/skills/round/bin/dashboard.py:138,289,295` · `docs/gates/README.md:90` · `xtask/src/main.rs:3576,3688,3868` · `.claude/skills/round/SKILL.md:150,327,412`
- 획득: 조회 — 저장소 전수 grep 후 뜻별로 분류
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(`C6` 을 「뜻마다 한 자리」로 다시 쓴다). 23 자리가 걸린다

### `C2` 의 선언 목록을 의도 파일 경로로 적으면 「원장의 짝은 하나여야 한다」에 걸려 형식 오류가 난다

- 어떻게 실패하나: `check_ledger_pair` 는 `docs/gates/` 의 모든 마크다운(README 포함)을 게이트 후보로 모으고, 본문이 그 회차의 의도 파일 경로를 포함하면 그 회차의 짝으로 센다. 짝이 둘이면 "게이트 N 개가 같은 회차를 가리킨다 — 원장의 짝은 하나여야 한다" 로 형식 오류다. `C2` 는 면제 목록을 README 에 놓는데, 회차를 가리키는 가장 자연스러운 표기가 바로 그 경로다. 그렇게 적는 순간 옛 회차 둘이 각각 짝 둘을 갖고 `C4` 가 같은 커밋에서 무너진다
- 어디가 걸리나: `xtask/src/main.rs:3637-3646` · `:3660-3670` · `docs/gates/README.md` · 잠근 의도 `C2`·`C4`
- 획득: 추정 — 코드의 필터 조건을 읽어 판정했다. 확인하려면 README 를 고쳐야 하는데 이 자리는 읽기 전용이라 안 돌렸다. 사본 검증도 앞의 뿌리 문제 때문에 재빌드 없이는 불가능하다
- 모집단: 자기장치
- 유효성: 추정
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 쉽다(목록을 회차 이름만으로 적거나 README 를 후보에서 뺀다). 걸리는 곳은 `C2`·`C4`·`C5`

### `D1`·`D2` 가 요구하는 절 형식은 실제 반환문 25 개 중 절반 아래에서만 지켜졌다 — 새 자의 첫 표적이 이 회차 자신의 리뷰어 여덟이다

- 어떻게 실패하나: `D2` 는 「안 냈다」와 「낼 것이 없다」를 구별해야 하므로 기대 절 목록이 필요하고 `D3` 가 그것을 `--schema` 에 놓는다. 실제 보존된 리뷰어 반환문 25 개를 세면 최상위 절 수가 3~9 로 갈리고, 절 이름은 16 종이며, 리뷰어 정의가 못 박은 절조차 준수율이 68~76% 다. 가장 최근 회차는 3~5 절로 더 낮다. 옛 것은 하한으로 면제해도 이 회차 자신의 리뷰어 여덟이 첫 표적이 되고, 라운드마다 형식 오류가 나면 축을 무르게 하려는 압력이 그 회차 안에서 생긴다
- 어디가 걸리나: `.claude/agents/pal-independent-reviewer.md:134-160` · 보존된 리뷰 반환문 25 개 · 잠근 의도 `D1`·`D2`·`D3`
- 획득: 조회 — 25 개 파일의 절머리를 전수로 세고 이름별 빈도를 냈다
- 모집단: 회차기록
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(절 목록을 「반드시 있어야 하는 것」과 「있으면 읽는 것」으로 가른다). 걸리는 곳은 이 회차의 반환문 11 개 + 앞으로의 모든 회차

### `A6` 의 소급 정정이 `N2`(유일한 실 저장소 음성 대조)를 소비한다 — 순서가 뒤바뀌면 이 회차의 효과 증거가 사라진다

- 어떻게 실패하나: `## 어디서 어떻게 거는가` 는 N2 하나만 실 저장소에 건다 — "이미 커밋된 행이 그 자체로 음성 대조다". 그런데 `A6` 은 발화한 행을 처분해서 전량 초록으로 만든다. 처분 뒤에는 그 자리에서 다시 발화할 수 없고, 남는 관측은 P1 뿐인데 그것은 `## 양성 대조` 가 스스로 "아무것도 안 재는 검사도 같은 초록을 낸다" 고 적은 항등식이다. 즉 발화 관측을 처분 전에 붙잡아 기록하지 않으면 이 회차의 효과가 사후에 재현 불가능해진다. 계획은 발화 수를 셋으로 해석하라고만 적고 「처분 전에 출력을 판정문·게이트에 고정한다」는 순서 요구를 안 적었다
- 어디가 걸리나: 잠근 의도 `## 어디서 어떻게 거는가` · `A5`·`A6` · `## 양성 대조` P1
- 획득: 조회(계획 문서의 절 구조와 P1 의 정의) · 추정(작업 순서가 실제로 어떻게 흐를지)
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 어렵다 — 처분 커밋이 들어간 뒤에는 옛 트리를 체크아웃해 재빌드해야 한다. 걸리는 곳은 `A5`·`A6`·`F3`·효과

### `## 양성 대조` 절이 등록된 음성 대조의 퇴출구가 되고, 나간 것은 두 원장 어디에도 안 남는다

- 어떻게 실패하나: 개정 ⑫ 가 앞 판의 셋을 「항등식」이라며 P1~P4 로 옮기고 F2·F3 의 모집단에서 뺐다. 그 판단 자체는 옳다. 문제는 경로가 열린 채로 남는다는 것이다 — P 항은 완수 조건 상자가 아니므로 조건 파서에도, 게이트 표준 표에도, 계기판 ② 의 분모에도 안 들어간다. 회차 중에 걸기 어려운 N 을 P 로 옮기면 조건 수가 줄고, 그 사실은 의도와 게이트를 같은 손이 함께 고치므로 「원장 둘 대조」가 못 잡는다(앞 회차가 "조건 하나를 양쪽 원장에서 지워도 21/21 초록" 으로 실측했다). 개정 표에 적는 것이 유일한 방어인데 그것은 사람의 성실성이다
- 어디가 걸리나: 잠근 의도 `## 양성 대조` · 개정 ⑫ · `.claude/skills/round/bin/record.py:154-160` · `xtask/src/main.rs:3760-3800`
- 획득: 조회 — 조건 파서가 완수 조건 절만 읽는 것을 코드로 확인하고, 이 의도를 파서에 걸어 P1~P4 가 조건 50 에 안 든다는 것을 확인
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(P 항에도 ID 를 주고 게이트의 별 표에 판정을 적게 한다). 걸리는 곳은 `F2`·`F3` 와 앞으로의 모든 음성 대조

### 얕은 클론에서 「없는 SHA」와 「이력 밖 SHA」가 구별되지 않는다 — `K9` 는 CI 만 고치고 이력 재작성은 안 막는다

- 어떻게 실패하나: 얕게 클론해 재 보면 27 개 SHA 전부가 안 읽힌다 → 570 행 전량이 빨개진다. `K9` 이 CI 체크아웃을 고쳐 이것을 막는다. 그런데 판정 자체가 두 원인을 구별 못 한다 — 「그런 커밋이 없다」(N1 의 표적)와 「그 커밋이 이 이력 창 밖이다」가 같은 실패를 낸다. 그래서 얕음 여부를 먼저 묻지 않으면 퇴로 5 를 언제 써야 하는지 기계가 모르고, squash merge 나 rebase 로 이력이 다시 쓰이면 570 행이 영구히 빨갛고 정정할 대상도 없다. 지금 27 SHA 는 전부 main 에서 안 닿는다 — 이 브랜치가 fast-forward 로 들어가면 살고 squash 로 들어가면 죽는다
- 어디가 걸리나: `.github/workflows/ci.yml:83` · `:166` · `:215` · 잠근 의도 `K9`·퇴로 5
- 획득: 조회(얕은 클론에서 27/27 실패 재현 · main 에서 27/27 미도달 확인 · main 이 HEAD 의 조상임을 확인) · 추정(앞으로의 병합 방식)
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: CI 쪽은 한 줄(빌드 잡 하나면 족하다). 이력 재작성 쪽은 되돌리기 불가라 판정을 「없다」가 아니라 「이 이력에서 안 보인다」로 적는 것이 유일한 방어다

### A 축의 git 셸아웃이 SHA 캐시 없이 570 회 돌면 macOS 에서 10.3 초가 붙는다 — Windows 는 그 배수다

- 어떻게 실패하나: 닫힘 570 행에 대해 행마다 git 을 부르면 macOS 에서 10.3 초, 서로 다른 SHA 27 개만 부르면 0.5 초다. 지금 전체 검사가 몇 초이므로 체감이 크게 바뀐다. 행 수는 회차마다 200~300 씩 늘어 선형으로 커지고, Windows 는 프로세스 생성 비용이 커서 같은 코드가 몇 배로 걸린다 — ADR-0023 의 「같은 방법 같은 결과」는 지키지만 사람이 로컬에서 검사를 안 돌리게 되는 압력을 만든다. 수용 사유는 ADR-0023 을 위한 것이지 캐시를 요구하지 않는다
- 어디가 걸리나: `xtask/src/main.rs:3183` · 잠근 의도 `## 개정` 의 수용 사유
- 획득: 조회 — 570 회와 27 회를 각각 타이밍
- 모집단: 저장소
- 유효성: 참
- 해악도: 미관
- 대상: 계획대상
- 얼마나 아픈가: 되돌리기 쉽다(SHA 단위 캐시 한 줄). 걸리는 곳은 A 축의 git 호출 함수 하나

### `A5` 가 못 박은 「548 행」이 이미 570 이다 — 개정 ④ 가 세운 원칙을 계획 자신이 어겼다

- 어떻게 실패하나: RED-3 과 `A5` 가 "이미 커밋된 548 행" 을 조건 문면에 박았다. 지금 세면 570 이다(사전부검 R1 이 22 행을 더했다). 이 회차가 진행되며 사전부검 3 · 독립리뷰 8 라운드가 레코드를 쌓으므로 종료 시점에는 800 행대가 된다. 개정 ④ 는 `A8` 을 신설하며 "글은 낡고 수는 안 낡는다" 라 적었는데, 같은 문서가 조건 문면에 수를 박아 판정할 때 「548 행 위에서 발화했다」가 이미 거짓이 된다. 게이트 README 맨 위의 "수를 안 적는다 — 파일이 하나 늘면 즉시 거짓이 되는 캐시다" 와 같은 자다
- 어디가 걸리나: 잠근 의도 RED-3 · `A5` · `## 어디서 어떻게 거는가` N2 · `## 개정` ④
- 획득: 조회 — 전 회차 레코드의 상태 분포를 다시 셈 (닫힘 570 · 상태 없음 432 · 합 1002)
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 쉽다(수를 빼고 세는 자리를 적는다 — 이 저장소가 이미 두 번 그렇게 처분했다). 세 자리가 걸린다

### 잠근 결정 9 가 개정 ⑤·`## 범위 밖` 과 정면으로 부딪힌 채 남아 있다

- 어떻게 실패하나: 결정 9 는 "n 번째 raw 항과 n 번째 레코드를 짝짓고 요약이 겹치는지 본다" 라 적혀 있다. 그런데 개정 ⑤ 는 요약 문자열 대조를 실측으로 기각했고 `## 범위 밖` 이 그것을 명시적으로 뺐다. 결정 표의 9 행은 안 고쳐졌다. 구현자가 잠근 결정을 정본으로 읽으면 기각된 축을 만들고, 개정을 정본으로 읽으면 결정 표가 낡은 채 남는다. 같은 모양이 결정 14 에도 있다 — 개정 ⑯ 이 뒤집었는데 14 행의 글은 그대로다. 이 문서에는 「결정 표와 개정 표 중 무엇이 정본인가」를 정한 축이 없다
- 어디가 걸리나: `.palimpsest/rounds/2026-08-23-check-verifies-work/intent.md:146` · `:151` · `:357` · `:368` · `:331`
- 획득: 조회 — 같은 문서 안의 네 문장을 나란히 읽고 대조
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 쉽다(결정 행에 뒤집힌 사실을 박거나 개정이 정본임을 한 줄로 선언한다). 두 행이 걸린다

## 내가 기각한 것

| # | 기각한 것 | 왜 아니었나 | 어디가 걸리나 | 획득 | 모집단 | 유효성 | 해악도 | 대상 |
|----|---|---|---|---|---|---|---|
| 1 | 조건 50 개가 조건 파서·게이트 표준 표를 깬다 | 파서에 실제로 걸어 봤다 — 50 개 전부 파싱, 형식오류 0, ID 중복 0. ID 꼴이 두 자리 숫자를 받는다 | record.py:160-170 · intent.md 완수 조건 | 조회 | 자기장치 | 거짓 | 실패 | 계획자신 |
| 2 | `A3` 가 판정문을 터뜨린다 | 양성 대조 P4 가 A 축을 독립 검사로 세우라고 이미 못 박았다. 새 검사의 판정문은 비어 있으므로 짧은 줄에 실린다 | xtask/src/main.rs:534-560 · intent.md 양성 대조 P4 | 조회 | 자기장치 | 거짓 | 미관 | 계획자신 |
| 3 | `D3` 와 `E2` 가 서로를 무르게 한다 | `D3` 가 놓는 절 목록은 검사가 거기서 읽는다고 조건에 박혀 있으므로 소비자가 생겨 정본이 된다. 실제 충돌은 E2 와 A 축 정의·B5 쪽이다 | record.py:440-460 · intent.md D3·E1·E2 | 조회 | 자기장치 | 거짓 | 거짓신호 | 계획자신 |
| 4 | `G2` 가 이름만 바꾸면 우회된다 | 사실이지만 `## 범위 밖` 이 명시했고 `G3` 가 그 구멍을 적는 것을 조건으로 세웠다. 이미 처분된 경계에 다시 이의를 다는 것이다 | intent.md 범위 밖 9항 · G3 · SKILL.md:420-440 | 조회 | 자기장치 | 거짓 | 거짓신호 | 계획자신 |
| 5 | `A1` 이 지금 실 저장소에서 발화한다 | 27 개 서로 다른 SHA 를 전수 확인했다. 27/27 실재한다. `A1` 의 실 저장소 발화는 0 이고 `N1` 은 격리 사본에서만 설 수 있다 | 회차별 레코드 · intent.md A1·N1 | 조회 | 회차기록 | 거짓 | 실패 | 계획대상 |
| 6 | 검사 번호 주석이 22 번째에서 낡는다 | 이미 낡아 있다 — 번호가 붙은 주석은 일부뿐이고 넷에는 아예 없다. 거울로 쓰이지 않으므로 새로 깨질 것이 없다 | xtask/src/main.rs:591,660,3544 | 조회 | 저장소 | 거짓 | 미관 | 계획대상 |
| 7 | 예외표를 새로 만들면 sunset 트리거가 발화한다 | 트리거 글롭이 한 겹이고 확장자가 다르라 안 걸린다. 2026-08-19 회차가 정확히 그 이유로 그 확장자를 골랐다고 파일 머리에 적어 뒀다 | docs/sunset.toml · 2026-08-19 회차의 예외표 머리 줄 | 조회 | 저장소 | 거짓 | 실패 | 계획대상 |

새 범주: 측정 도구의 뿌리가 컴파일 시점에 박혀 격리 사본이 원본을 잰다 (「사본에서 잰 것이 사본의 것인가」)
