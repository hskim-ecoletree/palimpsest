# 사전부검 R1 — 원 반환문

> 보존본. **고치지 않는다** — 합계 검산의 독립된 둘째 원천이다.
> 착수커밋 `b9fbef7` · 잠금 `d95dc3f` 위에서 쟀다. 기준선 확인: `cargo xtask check` 21/21.

### `닫은커밋` 은 규약상 「처분한 커밋」인데 A2 가 「고친 커밋」으로 읽는다 — 548 행 중 120 이 빨개진다

- 어떻게 실패하나: A2 를 「닫은커밋 의 diff 가 경로 를 만졌나」로 구현하면, 규약이 정의한 뜻과 다른 자를 옛 데이터에 댄다. 실제로 돌려 봤다 — 2026-08-22 에서 미접촉 45 (경로없음 99 제외), 2026-08-23 에서 미접촉 75. 합 120 행. 2026-08-23 은 248/252 행이 `닫은커밋 == 기준커밋`(= 처분 배치 커밋)이다. 예: `ef072ab` 는 `round(처분): [R1] 사전부검 R1` 이고 intent.md·plan.md·premortem 만 만지는데, 그 커밋으로 닫힌 PM1-01 의 경로는 `xtask/src/main.rs` 다 — 사전부검 발견의 경로는 예측된 파손 지점이지 고침 지점이 아니다
- 어디가 걸리나 (경로): `.claude/skills/round/SKILL.md:313` · `record.py:419` 스키마의 `열림축.닫는자` · `xtask/src/main.rs:3352-3392` 루프
- 획득: 조회 — `git show --name-only` × 26 SHA 를 548 행 전량에 대어 봤고, SKILL.md 와 `--schema` 의 정의문을 직접 읽었다
- 모집단: 규약
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다(코드). 그러나 잠근 결정 5 가 「빨개진 행은 레코드를 정정한다」이므로 120 행에 대해 존재하지 않는 「실제 고침 커밋」을 적게 된다 — 사전부검-계획수정 계열은 원리상 그런 커밋이 없다. 그것이 「사실이 아닌 것을 사실로」다

### 잠근 결정 14(소급 안 함)와 결정 5·A5·A6(레코드 정정)이 정면으로 부딪히고, 퇴로 2 를 쓰면 A5·B2·N2 가 원리상 못 선다

- 어떻게 실패하나: 결정 14 는 "옛 산출물에 소급하지 않는다 — 하한은 언제나 「이 회차 이후」다", 결정 5·A5·A6·N2 는 "이미 커밋된 548 행 위에서 발화를 묻는다". 둘 다 만족하는 구현이 없다. 퇴로 2(하한을 「끝난 회차 중 가장 최근 것」)를 쓰면 A-축 모집단이 2026-08-23 한 회차로 줄고, B2 가 지목한 2026-08-22 는 모집단 밖이 되어 B2 가 원리상 미측정이 된다
- 어디가 걸리나 (경로): `.palimpsest/rounds/2026-08-23-check-verifies-work/intent.md` 잠근 결정 14 · 결정 5 · 완수 조건 A5·A6·B2 · 퇴로 2
- 획득: 조회 — intent.md 전문과 `check_ledger_pair` 의 하한 구현(`main.rs:3800-3826`)을 대어 읽었다
- 모집단: 회차기록
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 되돌리기 쉬움(문서). 걸리는 조건 다섯(A5·A6·B2·B3·N2). 지금 풀지 않으면 회차 중간에 어느 쪽이든 「축소」로 처분되고, 퇴로 2 는 등록돼 있어 승인 없이 쓰인다

### A5 의 표적 둘은 앞 회차의 사후 검증이 이미 고쳤다 — 하나는 초록, 하나는 「정정된 채로」 빨개진다

- 어떻게 실패하나: `516dd8c` (round(사후검증)) 이 "⒟ 거짓 닫힘. IR1-08·IR1-11 … → 닫은커밋 을 실제 고침 커밋으로 고쳤다" 로 둘을 이미 정정했다. 지금 재면 IR1-08 은 접촉(초록)이고, IR1-11 은 `1a4161d` 로 정정됐는데 그 커밋이 `exp/prereg/rules.log` 를 안 만진다(미접촉) — 사전 등록 로그는 설계상 동결이라 어떤 커밋도 그것을 만지지 않는다. 즉 A6(정정하면 초록)이 IR1-11 에 대해 불가능하고, 초록으로 만드는 유일한 길은 깨끗한 세션이 방금 세운 정정을 되돌리는 것이다
- 어디가 걸리나 (경로): `.palimpsest/rounds/2026-08-23-agent-laziness-behavior/findings.jsonl` IR1-08·IR1-11 · 커밋 `516dd8c`
- 획득: 조회 — 두 행의 현재 `닫은커밋` 을 뽑아 `git show --name-only` 로 대어 봤고, `516dd8c` 의 메시지·diff 를 읽었다
- 모집단: 회차기록
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: intent.md 의 N2 는 "발화가 0 건이면 검사가 약한 것이고 그때는 A5 를 반증으로 적는다" 라고 미리 정해 뒀다. 세 번째 설명(표적이 이미 고쳐졌다)이 목록에 없다. 그대로 가면 「검사가 약하다」는 사실이 아닌 판정이 게이트에 박힌다

### B2 의 「다른 20」은 저장소에 커밋된 적이 없다 — 효과 확인의 둘째 축이 원리상 안 선다

- 어떻게 실패하나: B2 는 "2026-08-22 독립리뷰 R4 의 「다른 20」이 빨개진다" 인데, 그 어긋난 20 행은 `fcf4417` 이 같은 커밋 안에서 고쳐 넣었다(커밋 메시지: "처음 적은 20 행이 … 다른 20 이었다 … 고쳤다"). `git log --all -- findings.jsonl` 로 확인하면 R4 행을 도입한 커밋은 `fcf4417` 하나뿐이고 그것이 정정본이다. 현재 트리에서 R4 20 쌍을 순서대로 대면 요약이 전부 맞는다(어긋나는 둘은 「없음」 자리채우기 행이다)
- 어디가 걸리나 (경로): 커밋 `fcf4417` · `.palimpsest/rounds/2026-08-22-agent-laziness/findings.jsonl` IR4-04~IR4-23 · `review/r4-raw.md`
- 획득: 조회 — 파일 이력 전량과 `fcf4417` 메시지를 읽고, 20 쌍을 순서 짝으로 대조했다
- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: B2 를 「격리 사본에 옛 20 을 심어 본다」로 바꾸면 N4 와 같은 것이 되고, intent.md 가 "N2 가 §7 의 항등식 경고를 피하는 자리다" 로 자랑한 「회차가 안 만든 데이터 위의 발화」가 A-축·B-축 양쪽에서 사라진다

### B3 「나머지 회차·라운드 전부가 초록이다」가 거짓 — 2026-08-20 의 레코드는 원문 순서의 순열이다

- 어떻게 실패하나: 순서 짝 + 내용 대조를 실제로 짜서 929 쌍에 돌렸다. `2026-08-20-rust-extractor/review/r2-raw.md` 는 33↔33 로 수가 맞는데 33 쌍 중 32 가 어긋난다 — 원문 1 번(.RS 회귀 시험 0)이 IR2-01(게이트가 41·43·42)과 짝지어지는 식으로 레코드가 범주별로 재정렬돼 있다. r4-raw.md 도 18/23. 잠근 결정 9 는 "앞 회차가 「행 순서까지 어긋남 0」이라 실측상 짝이 이미 선다" 를 근거로 삼았는데, 그 실측은 2026-08-22·2026-08-23 에만 참이고 2026-08-19·2026-08-20 에는 거짓이다
- 어디가 걸리나 (경로): `.palimpsest/rounds/2026-08-20-rust-extractor/findings.jsonl` IR2-01~IR2-16 · `review/r2-raw.md` · `review/r4-raw.md` · `xtask/src/main.rs:3423-3452` 검산 루프
- 획득: 조회 — 파이썬으로 짝을 만들어 929 쌍 전량을 대고 어긋난 74 를 세었으며, r2 의 33 쌍을 눈으로 출력해 순열임을 확인했다
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: B3 는 조건으로 등록돼 있으므로 반증이 난다. 소급 폭발을 막으려면 퇴로 2(하한)를 쓰게 되고, 그러면 위의 시나리오 둘째대로 A5·B2 가 같이 죽는다. 걸리는 라운드 넷, 쌍 74

### A3 처분 여섯 중 `전환` 의 모집단이 저장소 전체에서 0 이고, `축소` 는 v3 회차에서 경로가 전무하다

- 어떻게 실패하나: 네 회차 1,000 행을 세면 처분 분포가 정정 565 · 기각 307 · 범위밖 46 · 확대 46 · 축소 16 · 전환 0 이다. `닫은커밋` 을 지는 v3 두 회차만 보면 축소 8 행이 전부 경로=(경로 없음)이다. 그러므로 A3 이 세우는 자 여섯 중 `전환` 자는 태어나면서 호출자 0 이고, `축소` 자는 경로를 요구하는 순간 모집단 0 이다. 퇴로 1 은 「거짓 실패를 내면」만 다루고 「모집단 0 으로 조용히 초록」은 안 다룬다
- 어디가 걸리나 (경로): 네 회차의 findings.jsonl 전량 · intent.md 완수 조건 A3 · 퇴로 1
- 획득: 조회 — 처분 × 경로유무 교차표를 전량에서 계산했다
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 바로 앞 커밋 `59978c4` 가 "`B7` 이 모집단 0 으로 통과한 것을 게이트에 적는다" 로 정확히 이 병을 기록했다. 같은 회차가 같은 병을 여섯 자 중 둘에 다시 심는다. 「측정이 죽은 가지」는 이 회차가 정본으로 등록한 금지역이다

### CI 는 얕은 클론이라 A1·A2 가 세 플랫폼 전부에서 빨개진다 — 그리고 잠근 결정 8 이 그 이유로 git 축을 이미 기각했다

- 어떻게 실패하나: `.github/workflows/ci.yml` 의 세 잡 전부가 `- uses: actions/checkout@v4` 를 `with:` 없이 쓴다(fetch-depth 선언 0 건). 얕은 체크아웃에서는 `1ea992f`·`ef072ab` 같은 옛 SHA 의 객체가 없어 `git cat-file -e` 도 `git show --name-only` 도 실패한다. `cargo xtask check` 는 job1(ubuntu/macos/windows)에서 돈다. 그리고 잠근 결정 8 이 #90 축을 고르며 든 근거 ⓑ 가 "git log 축은 얕은 클론·이력 창에서 갈린다" 다 — 그 이유로 기각한 것 위에 A-축 전체를 세운다
- 어디가 걸리나 (경로): `.github/workflows/ci.yml:83`·`166`·`215` · `ci.yml:114-115` · intent.md 잠근 결정 8 ⓑ
- 획득: 조회 — ci.yml 의 checkout 스텝 셋을 직접 읽어 `with:` 부재를 확인했다. actions/checkout 의 기본 fetch-depth 가 1 이라는 것은 이 저장소에서 잰 것이 아니라 액션의 기본값 지식이다
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: K1·K5 를 직접 막는다. 로컬은 초록·CI 는 빨강이므로 회차 안에서는 안 보이고 push 후에 난다. 덧붙여 `gix 격리` 검사가 xtask 의 gix 사용을 막으므로 git CLI 셸아웃뿐이고, `파이썬_실행자()` 같은 단일 분기 자리가 git 에는 없다 — ADR-0023 의 「플랫폼 분기는 한 자리」가 새로 걸린다

### G1 이 요구하는 report.md 검산 줄을 앞 회차가 자기 고침으로 방금 지웠다 — 그리고 그것은 세 번째 원장이다

- 어떻게 실패하나: `516dd8c` 가 2026-08-23 종료 보고에서 `통과 29 · 반증 7 · 대조불가 1 · 미측정 2 = 39` 를 삭제하고 그 자리에 "여기에 수를 안 적는다 … 앞 판이 적었고 같은 커밋 안에서 게이트와 갈렸다" 를 넣었다. G1(검산 줄을 게이트·intent.md 와 같은 자로 댄다)을 걸면, 하한 안의 유일한 끝난 회차가 그 줄을 안 가져 빨개지고, 초록으로 만드는 길은 앞 회차가 방금 내린 결정을 되돌리는 것뿐이다. 그리고 AGENTS.md 가 "같은 것을 두 곳에 적으면 그것이 곧 drift" 라 못 박은 자리에 세 번째 사본을 만든다
- 어디가 걸리나 (경로): `.palimpsest/rounds/2026-08-23-agent-laziness-behavior/report.md:7-20` · 커밋 `516dd8c` · `docs/gates/README.md:66-90` · AGENTS.md 「결정과 근거와 실행은 각각 한 자리에 산다」
- 획득: 조회 — `git show 516dd8c -- report.md` 의 diff 를 읽고, 여섯 report.md 전부에서 검산 줄을 grep 했다(2026-08-22 만 서술형 표 행 하나, 나머지 다섯은 없음)
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 되돌리기는 쉽지만 「앞 회차의 판정을 다시 열지 마라」와 부딪힌다. 그리고 K1 이 회차 내내 빨간 채로 남는다 — 이 회차의 report.md 는 마지막에야 생기는데 하한은 매 실행에 걸린다

### G2 를 본문 문자열로 재면 옛 종료 보고 넷이 「네 이름이 있다」로 거짓 실패한다

- 어떻게 실패하나: 2026-08-18(둘)·2026-08-19·2026-08-20 의 report.md 3~4 행이 "규약 §10 이 이 문서의 절 이름을 정한다. 네 이름(「의도적으로 안 한 것」·「확인 못 한 것」·「추론(확인 아님)」·「다음으로 넘기는 것」)은 없다." 라는 인용문을 그대로 싣는다. 금지된 네 낱말이 본문에 글자로 있다. 절머리(`^## `)로 범위를 좁히지 않으면 넷이 전부 빨개진다. 반대로 절머리로 좁히면 `## 다음 회차가 받는 것` 이라는 우회 이름이 이미 보고 넷에 서 있어 G3 이 예고한 구멍이 즉시 실물이 된다
- 어디가 걸리나 (경로): `.palimpsest/rounds/2026-08-18-completion-condition/report.md:3-4` 및 같은 자리 셋 · `.claude/skills/round/SKILL.md:422`
- 획득: 조회 — 네 낱말을 report.md 전량에 grep 하고 절머리 목록을 뽑았다
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 걸리는 파일 넷. 결정 14 의 「이 회차 이후 하한」을 쓰면 모집단이 이 회차 하나로 줄어 G2 가 사실상 자기 산출만 재게 된다 — §7 이 금지한 항등식 형태다

### F1 이 약속하는 「원장 둘 대조가 음성 대조를 댄다」가 거짓이다 — N1~N13 은 완수 조건이 아니다

- 어떻게 실패하나: 잠근 결정 12 는 "음성 대조마다 조건 ID 를 준다. 그러면 안 건 것이 「미측정」으로 판정 표에 서고 「원장 둘 대조」가 댄다" 라고 적는다. 그러나 `record.py` 의 조건 파서는 조건절 = "완수 조건" 절만 읽는다(record.py:145·`cmd_conditions`). 이 회차의 intent.md 를 실제로 파싱하면 조건 35 개(A1~K8)이고 N1~N13 은 하나도 없다 — 음성 대조 표는 별개 절에 산다. N 들을 완수 조건 상자로 등록하지 않는 한 게이트 표준 표에도 안 서고 원장 둘 대조의 모집단이 아니다
- 어디가 걸리나 (경로): `.claude/skills/round/bin/record.py:145`·`154`·`221` · intent.md `## 음성 대조` 표 · 잠근 결정 12
- 획득: 조회 — `record.py conditions` 를 이 회차 intent.md 에 돌려 35 개 ID 를 뽑았고 N 들이 없음을 확인했다
- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역
- 대상: 계획자신
- 얼마나 아픈가: F1 을 「표에 ID 를 적었다」로 판정하면 게이트에 「원장 둘 대조가 댄다」는 거짓 문장이 남는다. 진짜로 대게 하려면 조건이 35 → 48 로 늘고 게이트 표준 표·검산·계기판이 전부 따라 움직인다

### N3·N5·N7 은 「초록이면 통과」형이라 §7 이 금지한 항등식이고, F3(「걸었고 빨개졌다」)을 원리상 못 채운다

- 어떻게 실패하나: 등록된 열셋 중 N3(정정 후 전량 초록) · N5(정상 회차가 안 빨개짐) · N7(옛 게이트 둘이 여전히 초록)은 실패할 수 있는 길이 없는 관측이다 — 아무것도 안 재는 검사도 같은 초록을 낸다. 그런데 F3 는 "건 것이 실제로 빨개졌다 — 「걸었다」가 아니라 「걸었고 빨개졌다」" 라고 전량에 걸린다. 셋은 정의상 안 빨개지므로 F3 는 문자 그대로는 불충족이고, 「그 셋은 예외」로 읽으면 F2·F3 가 재는 모집단이 13 → 10 으로 조용히 좁아진다
- 어디가 걸리나 (경로): intent.md `## 음성 대조` N3·N5·N7 · 완수 조건 F3 · `.claude/skills/round/SKILL.md` §7
- 획득: 조회 — 음성 대조 열셋을 §7 의 항등식 판별 문장에 하나씩 대었다
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 문서 수정으로 되돌릴 수 있다. 그러나 안 고치면 「음성 대조 13/13 걸었고 빨개졌다」가 게이트에 적히고 그중 셋은 발화 자체가 불가능하다

### E1 의 술어(「xtask 가 분기에 쓰는 키」)로 재면 정본이 `종류` 하나뿐이고, record.py 가 진짜로 소비하는 enum·필수가 「정본 아님」으로 선언된다

- 어떻게 실패하나: `--schema` 출력에서 xtask 가 실제로 읽는 것은 `스키마["종류"]` 하나뿐이다(main.rs:3216). `표준표`·`형식오류`·`판정`·`조건` 은 `--schema` 가 아니라 `gate`·`conditions` 명령의 출력이다. E1 을 문면대로 적용하면 정본 집합 = {종류} 이고, `enum`·`필수`·`기본값`(record.py 의 검증 이 매 행에 쓴다)까지 「정본 아님」으로 격하돼 소비되는 것을 안 소비된다고 적는 것이 된다. E3·N10 도 `종류` 값 하나로만 실증 가능해진다
- 어디가 걸리나 (경로): `xtask/src/main.rs:3202-3220` · `.claude/skills/round/bin/record.py:419-467` · `record.py:469-520` · 완수 조건 E1·E2·E3 · 음성 대조 N10
- 획득: 조회 — `--schema` 출력을 덤프하고 xtask 의 스키마 접근 자리를 전수로 찾았으며, record.py 의 소비 지점을 읽었다
- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 술어를 「어느 코드든 분기에 쓰는가」로 고치면 해결되지만, 그러면 조건파서·게이트파서·합계검산·대응표 는 여전히 산문이라 격하 대상이고 — 하필 이 회차가 합계검산 의 규칙을 B1 으로 다시 쓴다. 다시 쓰는 그 규칙을 같은 회차가 「정본 아님」으로 선언하는 모양이 된다

### 「검사가 못 보는 것 2」가 실제로는 v3 두 회차의 27% 다 — 그리고 그중엔 어떤 커밋도 만질 수 없는 좌표가 있다

- 어떻게 실패하나: A2 가 원리상 못 재는 행이 2026-08-22 에서 99/296(33%), 두 회차 합쳐 99/548 이다. 게다가 좌표에 `/tmp` · `.palimpsest/` · `exp/` 같은 디렉터리와 저장소 밖 경로가 섞여 있다(2026-08-23 에서 실측). `/tmp` 를 진 행(PM2-R5)은 어떤 커밋도 그것을 만지지 않으므로 A2 아래서 영구히 빨갛고, 잠근 결정 5 의 「실제 고침 커밋 SHA 로 정정」에도 참인 답이 없다. 판정문이 이 면제 수를 안 실으면 「548 행을 쟀다」로 읽힌다 — 지금 `검산 면제 실측 45행·인터뷰 6행` 을 실어 내는 규율이 이미 서 있는데 새 축은 그것을 안 이어받을 수 있다
- 어디가 걸리나 (경로): `.palimpsest/rounds/2026-08-23-agent-laziness-behavior/findings.jsonl` PM2-R5·PM2-R1·IR1 계열 · `xtask/src/main.rs:3389` · `main.rs:3480-3499`
- 획득: 조회 — 경로 값 분포를 두 회차에서 전수로 뽑았다(distinct 32·42)
- 모집단: 회차기록
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: A4 가 「못 보는 것 넷」을 글로 적게 하지만 수를 판정문에 싣게 하지는 않는다. 글은 낡고 수는 안 낡는다

### D2 는 리뷰어 정의의 절 일곱을 검사 쪽에 손으로 베게 만든다 — 자라는 모집단의 거울이다

- 어떻게 실패하나: 「안 냈다」와 「낼 것이 없다」를 가르려면 검사가 절 목록을 알아야 한다. `pal-independent-reviewer.md:131-165` 의 반환 형식은 표를 일곱 가진다(미측정 목록 · 빠진 것 · 요구되지 않은 것 · 있는데 틀린 것 · 새 발견 · 자기 산출 · 기각). 그 목록은 회차마다 손대는 문서 안에 있고 실제로 자라 왔다. 검사 쪽에 복제하면 절이 하나 늘 때 검사가 조용히 안 요구한다 — 잠근 결정 8 이 「자라는 모집단을 손으로 벤 거울」이라며 피하려 한 바로 그 모양이다. 게다가 사전부검자 정의는 절 구성이 다르므로 두 벌의 거울이 생긴다
- 어디가 걸리나 (경로): `.claude/agents/pal-independent-reviewer.md:131-165` · `.claude/agents/pal-premortem-sweeper.md:76-116` · `xtask/src/main.rs:3113-3168` · `record.py` 스키마의 합계검산
- 획득: 조회 — 두 에이전트 정의의 반환 형식 블록을 읽고 표 수를 세었으며, 현행 카운터가 절 이름을 전혀 안 본다는 것을 코드에서 확인했다
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 목록을 `--schema` 한 자리에 두면 완화되는데, 그것은 E2 가 「정본 아님」으로 격하하려는 산문 사전 자리다 — D 축과 E 축이 서로를 무르게 한다

### C3 의 날짜 하한이 「표준 표가 없을 때만 목록을 읽는」 자연스러운 구현에서 무발화가 된다

- 어떻게 실패하나: 기존 코드 모양(main.rs:3687-3692)을 그대로 따르면 `if !표준표 { 목록에 있으면 형식이전, 없으면 실패 }` 가 된다. 새 회차는 표준 표를 갖고 끝나므로 그 회차를 선언 목록에 넣어도 분기에 도달하지 않는다 — N8("새 회차를 「형식 이전」에 넣었는데 안 빨개진다")이 발화하지 못한다. 목록 자체를 독립으로 검증하는 두 번째 자(항목의 날짜가 하한 이하인가 · 항목이 실재하는 회차인가)를 따로 세워야 N8 이 선다
- 어디가 걸리나 (경로): `xtask/src/main.rs:3672-3692` · `docs/gates/README.md:90` · 완수 조건 C3 · 음성 대조 N8
- 획득: 추정 — 기존 분기 구조는 조회로 읽었으나, 구현이 아직 없어 「어느 모양으로 짜는가」는 못 물었다
- 모집단: 자기장치
- 유효성: 추정
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 실행 전에 「목록 검증자를 따로 세운다」를 완수 조건에 박으면 싸게 막힌다. 안 박으면 N8 이 「걸었다」로만 적히고 실제로는 안 걸린다

### C1 이 docs/gates/README.md 에 이미 적힌 결정문을 거짓으로 만든다

- 어떻게 실패하나: README:90 이 "표준 표가 없는 게이트는 「형식 이전」이고 검사 밖이다 — 오류가 아니다. 위의 「옛 게이트를 옮기지 않는다」가 그대로 산다" 라고 못 박고, 같은 문장이 `xtask/src/main.rs:3689` 주석과 `--schema` 의 게이트파서·형식이전 에도 산다 — 세 자리다. C1 은 끝난 회차에 한해 그것을 오류로 만든다. 세 자리 중 하나만 고치면 나머지 둘이 낡은 채로 남고, 이 회차가 닫으려는 「선언과 코드가 갈린다」(#94)의 새 사례가 된다
- 어디가 걸리나 (경로): `docs/gates/README.md:90`·`:44-46` · `xtask/src/main.rs:3686-3691` · `.claude/skills/round/bin/record.py` 스키마 게이트파서
- 획득: 조회 — 세 자리의 문장을 각각 읽어 같은 뜻임을 확인했다
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 세 자리. 되돌릴 수 있지만, 낡음을 표시할 장치가 없어(문서 산문) 다음 회차가 다시 읽는다

### N13 은 새 자를 기존 함수 안에 넣으면 무발화한다 — 그리고 이 계획은 전부 기존 함수 안에 넣는다

- 어떻게 실패하나: N13 은 "검사 수가 늘었는데 판정문의 수가 안 늘면 손으로 벤 거울이다 → checks.len() 이 세는지 확인한다" 다. `checks.len()` 은 실제로 센다(main.rs:552·총 21). 그러나 이 회차의 A1~A3·B1 은 `check_round_records` 안에, C1~C4·G1~G2 는 `check_ledger_pair` 안에 들어갈 것이 「만질 자리」에 적혀 있다. 그러면 배열 길이는 21 로 그대로이고 N13 의 전건(「검사 수가 늘었는데」)이 한 번도 참이 안 된다 — 공허하게 통과한다
- 어디가 걸리나 (경로): `xtask/src/main.rs:536-556` · intent.md 「만질 자리」 · 음성 대조 N13
- 획득: 조회 — checks 배열과 total 계산을 읽고 21/21 출력을 실제로 확인했으며, 계획의 「만질 자리」 셋과 대었다
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 안쪽 자의 수를 판정문에 싣는 별도 규율(예: 회차 레코드 검사가 「A-축 N행 중 M행 접촉」을 내는 것)이 없으면, 검사가 세 배로 커져도 화면의 수는 21/21 그대로다

## 내가 기각한 것

| # | 제목 | 어떻게 실패한다고 봤나 · 왜 아니었나 | 경로 | 획득 | 모집단 | 유효성 | 해악도 | 대상 |
|---|---|---|---|---|---|---|---|---|
| 1 | 펜스 안의 항·표 줄이 순서 짝을 한 칸씩 민다 | 반환문_항_수 는 코드펜스를 안 본다(check_ledger_pair 는 R8 에서 보게 고쳤는데 여기는 아니다). 순서 짝은 한 항만 어긋나도 뒤가 전부 무너지므로 치명적이라고 봤다. 전 raw 파일 27 개를 펜스 추적으로 훑으니 펜스 안 항·표 시작 줄이 0 건이다 — 잠재적이지 실물이 아니다 | xtask/src/main.rs:3113-3168 | 조회 | 자기장치 | 거짓 | 거짓신호 | 계획대상 |
| 2 | 예외표 123 행이 A1·A2 모집단에 섞여 거짓 실패한다 | 레코드들 에 예외표도 넘긴다(R2 정정)므로 새 자가 예외표 행에도 걸린다고 봤다. 예외표 행에는 경로·닫은커밋 칸이 원리상 없어 접근이 None 이고 기존 좌표 검사와 같은 모양으로 자동 스킵된다 | 2026-08-19-finding-records 의 disposal-overrides · main.rs:3352-3392 | 조회 | 저장소 | 거짓 | 미관 | 계획대상 |
| 3 | C1 의 새 실패 갈래가 모집단 0 이라 태어나면서 죽은 가지다 | 선언 목록이 옛 회차 둘을 100% 덮으므로 「끝난 회차인데 표준 표 없음 + 목록 밖」이 영원히 0 이라고 봤다. N6 이 그것을 정확히 채운다 — 정상 게이트의 헤더 한 글자를 고치면 표준표가 거짓이 되고 그 회차는 목록 밖이라 발화한다 | main.rs:3686-3691 · 음성 대조 N6 | 조회 | 자기장치 | 거짓 | 거짓신호 | 계획자신 |
| 4 | A7 의 스키마 2 회차 둘이 열림축 검증에서 빨개진다 | 08-19·08-20 은 상태·닫은커밋 이 없으니 새 필수 검사에 걸린다고 봤다. record.py:503 이 버전 3 이상에서만 열림축을 재고 두 파일의 머리 줄이 schema_version 2 라 원리상 안 걸린다 | .claude/skills/round/bin/record.py:502-514 | 조회 | 저장소 | 거짓 | 실패 | 계획대상 |
| 5 | 합계 검산이 회차 경계를 넘어 서로를 거짓 실패시킨다 | 라운드 번호가 회차마다 겹치므로(사전부검R1 이 네 번) B1 의 짝짓기가 회차를 섞을 것이라 봤다. 쌍_수 가 이미 회차·출처·라운드 삼중 열쇠이고 반환문도 그 회차 디렉터리 안에서만 찾는다(2026-08-19 R2 정정) | main.rs:3374-3380 · 3437-3441 · 3465-3470 | 조회 | 저장소 | 거짓 | 실패 | 계획대상 |

새 범주: 앞 회차의 고침이 이 회차의 측정 표적을 이미 소거한다 (표적 소실)
