# 독립 리뷰 라운드 2 (등록된 상한) — 원 반환문

> `pal-independent-reviewer` 가 돌려준 것을 그대로 보존한다. 1 라운드의 목록도 논리도 안 줬다.

격리 사본에서 잰다 — `git clone --no-local` → `92cd404` 체크아웃 → 스테이지·미추적 산출물 13 개 복사 → 사본 안에서 `cargo` 빌드. 기준선 초록(`검사 23/23 통과`, rc 0)을 먼저 확인했다. 작업 트리는 안 만졌다(md5 대조 확인).

## 조건 판정

| 조건 | 판정 | 잰 수 | 근거 |
|---|---|---|---|
| A1 | 통과 | 두 사전 모두 변이 모집단에 듦 | 두 케이스가 실제로 돌았다(둘 다 `도달`) |
| A2 | 반증 | 반례 2 — `닫힘축.뜻` rc 0 · `반환형식.역사형식.legacy-2020` rc 0 (둘 다 `설명` 밖) | 재실행. `자동프로필()` 이 낼 수 있는 프로필 넷에 `legacy-2020` 이 없다 |
| B1 | 통과 | 변이 2 케이스 · 검사 rc 0 / 시험 rc 0 / 추출 산출 불변 | 재실행 출력 |
| B2 | 반증 | 반례 — `키삭제` 로 소비되는 키 일곱을 지우면 rc 0 | ★ 게이트가 든 1 차 반례(`열별칭.요약` → rc 0)는 **재현이 안 된다** — 신선 사본에서 rc 1 이다 |
| B3 | 통과 | 새 방어는 교집합 파생, 손 열거 0 | `record.py` |
| C1 | **반증** | 재실행 어긋남 1 / 11 · 스크립트 종료값 **1**(기록은 0) | 신선 클론 둘에서 각각 `{"어긋남": 1}` · rc 1 |
| C2 | 통과 | `cargo xtask check` rc 0 · 23/23 · `cargo xtask test` rc 0 · 통과 시험 908 | 전 출력에 `FAILED`·`panicked`·`error[E` 0 건 |
| D1 | **반증** | 이 검토가 연 금지역 3 · 실패 1 | 아래 6·7·8 이 금지역 |
| D2 | 미측정 | 커밋 0 · CI 실행 0 · `report.md` 0 · #94 OPEN | 산출물이 전부 스테이지 상태 |

**음성 대조** — ①②는 걸렸고 실제로 갈렸다. ③(도달)은 변이 문자열 23 개 중 **9** 에만 걸린다.

## 미측정 목록

| # | 안 잰 조건 | 모집단 | 유효성 | 해악도 | 조건 | 좌표(파일:줄) | 왜 못 쟀나 |
|---|---|---|---|---|---|---|---|
| 1 | D2 — 최종 커밋 SHA 의 세 OS CI 성공 · #94 종료 | 회차기록 | 참 | 거짓신호 | D2 | `.palimpsest/rounds/2026-09-03-gate-parser-schema/intent.md` | 커밋이 아직 없다 |
| 2 | C2 의 Windows·Linux 판 `cargo xtask check` | 저장소 | 참 | 거짓신호 | C2 | `docs/gates/gate-parser-schema.md` | 이 호스트는 darwin 하나다 |

## 빠진 것

| # | 발견 | 모집단 | 유효성 | 해악도 | 조건 | 좌표(파일:줄) | 근거 |
|---|---|---|---|---|---|---|---|
| 3 | 종료 보고가 없다 — 네 이름을 대조할 대상 자체가 부재다 | 회차기록 | 참 | 거짓신호 | D2 | `.palimpsest/rounds/2026-09-03-gate-parser-schema/state.md` | `ls` 에 `report.md` 없음 |
| 4 | RED 에 등록된 음성 대조 ③(도달)이 안 걸렸다 — `red-baseline.txt` 와 `red-mutated.txt` 가 바이트 동일이라 변이가 그 사본의 출력에 닿았음을 보이는 산출물이 없다 | 회차기록 | 참 | 거짓신호 | C1 | `.palimpsest/rounds/2026-09-03-gate-parser-schema/red/red-observation.md` | `diff -q` → 동일 |

## 요구되지 않은 것

| # | 발견 | 모집단 | 유효성 | 해악도 | 조건 | 좌표(파일:줄) | 근거 |
|---|---|---|---|---|---|---|---|
| 5 | `docs/gates/README.md` 에 더한 「동결 규율의 구멍」 문단은 잠긴 경계 밖의 주제다 | 저장소 | 참 | 미관 | B3 | `docs/gates/README.md` | 잠긴 의도의 경계 문장과 대조 |

## 있는데 틀린 것

| # | 발견 | 모집단 | 유효성 | 해악도 | 조건 | 좌표(파일:줄) | 근거 |
|---|---|---|---|---|---|---|---|
| 6 | 회차의 재실행 가능한 대조가 지금 트리에서 스스로 어긋난다 — 신선 클론 둘에서 종료값 1 이다. `review/r1-raw.md` 와 그 레코드를 나중에 넣었는데 장치는 그 전에 마지막으로 돌았다 | 회차기록 | 참 | 금지역 | C1 | `.palimpsest/rounds/2026-09-03-gate-parser-schema/verification/mutate-run.txt` | 신선 사본에서 `{"어긋남": 1}` · rc 1. 산출물은 `{"어긋남": 0}` |
| 7 | 효과 산출물이 지금 코드가 안 내는 출력을 「지금 무엇을 내는가」로 적는다 | 회차기록 | 참 | 금지역 | D1 | `.palimpsest/rounds/2026-09-03-gate-parser-schema/effect/observation.md` | `--schema` 를 실제로 부른 결과와 전사가 다르다 |
| 8 | 게이트 문서의 측정 표와 B2 판정 근거가 실측과 반대다 — 동결 규율이라 그대로 굳는다 | 회차기록 | 참 | 금지역 | B2 | `docs/gates/gate-parser-schema.md` | 위 6 과 같은 실행 |
| 9 | `docs/gates/README.md` 의 「기계 동결」 서술이 거짓이다 — 그 함수는 죽은 링크 검사의 제외 목록이지 동결 강제 장치가 아니다 | 저장소 | 참 | 거짓신호 | B3 | `docs/gates/README.md` | 판정 문서 둘을 실제로 고치고 돌렸더니 23/23 |
| 10 | `record.py` 주석의 「키를 지우거나 개명하면 시험이 빨개진다」가 과일반화다 — 이 회차가 키 넷을 지웠는데 시험은 초록이었다 | 규약 | 참 | 거짓신호 | A2 | `.claude/skills/round/bin/record.py` | 그 시험은 낱말 다섯의 부분 문자열만 본다 |
| 11 | 「같은 일곱을 두 연산자로 쟀다」가 거짓이다 — 값치환 쪽은 여섯이고 `열림값` 이 빠졌다 | 회차기록 | 참 | 거짓신호 | B2 | `.palimpsest/rounds/2026-09-03-gate-parser-schema/effect/observation.md` | 치환쌍 6 개 대 7 개 |
| 12 | 게이트 「종료」 표의 제품 커밋이 불완전하다 — 스테이지된 제품 변경 셋이 아직 다른 커밋으로 간다 | 회차기록 | 참 | 거짓신호 | D2 | `docs/gates/gate-parser-schema.md` | `git diff --cached --stat` |

## 이번 라운드의 새 발견

| # | 발견 | 모집단 | 유효성 | 해악도 | 조건 | 좌표(파일:줄) | 근거 |
|---|---|---|---|---|---|---|---|
| 13 | `xtask` 가 근거로 대는 좌표가 낡았다 — 그 문장은 지금 다른 줄이고 착수 시점에도 달랐다 | 저장소 | 참 | 거짓신호 | B3 | `xtask/src/main.rs` | `grep -n` 으로 현재 줄과 착수 커밋 줄을 각각 확인 |
| 14 | 이 회차가 세운 #102 본문이 같은 거짓 측정을 싣는다 — 다음 회차의 착수 문서라 그 전제 위에서 설계가 시작된다 | 회차기록 | 참 | 거짓신호 | B2 | `.palimpsest/rounds/2026-09-03-gate-parser-schema/intent.md` | 6 과 같은 실행 |

## 자기 산출에 대한 발견

| # | 발견 | 모집단 | 유효성 | 해악도 | 조건 | 좌표(파일:줄) | 근거 |
|---|---|---|---|---|---|---|---|
| 15 | 등록된 음성 대조 ③(도달)이 변이 문자열 23 개 중 9 에만 걸린다 — 케이스마다 경로 하나만 잰다 | 자기장치 | 참 | 거짓신호 | C1 | `.palimpsest/rounds/2026-09-03-gate-parser-schema/verification/mutate.py` | 치환쌍 합 23, 경로 있는 케이스 9 |
| 16 | 일곱 치환을 한 번에 걸고 종료값 하나로 판정한다 — 하나만 빨개도 통과라 개별 키에 대해 아무것도 안 선다 | 자기장치 | 참 | 거짓신호 | B2 | `.palimpsest/rounds/2026-09-03-gate-parser-schema/verification/mutate.py` | 기록된 진단이 한 키에서만 나왔다 |
| 17 | 장치가 살아 있는 추적 파일을 직접 덮어쓴다 — 중단되면 변이가 남는다 | 자기장치 | 참 | 실패 | C1 | `.palimpsest/rounds/2026-09-03-gate-parser-schema/verification/mutate.py` | 실행 중 중단시키니 변이가 파일에 남았다 |
| 18 | 제품 수정의 고유 진단이 기록에 안 남는다 — 첫 매칭 줄만 집어 다른 검사의 진단을 싣는다 | 자기장치 | 참 | 미관 | B3 | `.palimpsest/rounds/2026-09-03-gate-parser-schema/verification/mutate.py` | 모듈 직접 호출로 확인 |
| 19 | 판정이 stdout 에만 있고 남는 산출물에는 없다 — 파일만 읽는 사람은 어긋남을 못 본다 | 자기장치 | 참 | 거짓신호 | C1 | `.palimpsest/rounds/2026-09-03-gate-parser-schema/verification/mutate.py` | 산출물 끝줄이 마지막 변이 행이다 |
| 20 | 게이트의 「손 열거 0」이 문면대로는 안 선다 — 장치가 리터럴 23 개를 복사한다 | 자기장치 | 참 | 미관 | B3 | `docs/gates/gate-parser-schema.md` | 파일 판독 |
| 21 | `state.md` 가 낡았다 — 게이트 문서를 아직 안 쓴 것으로 적는다 | 회차기록 | 참 | 미관 | D2 | `.palimpsest/rounds/2026-09-03-gate-parser-schema/state.md` | `ls` 로 확인 |

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효성 | 해악도 | 조건 | 좌표(파일:줄) | 왜 아니었나 |
|---|---|---|---|---|---|---|---|
| 22 | 지운 상수나 뺀 키 넷을 읽는 자가 남아 회귀가 난다 | 저장소 | 거짓 | 실패 | B3 | `.claude/skills/round/bin/record.py` | 저장소 전량 조회가 남긴 것은 주석과 무관한 산문뿐. 신선 사본 23/23 · 시험 908 통과 |
| 23 | `정본아님` 의 「어느 검사도 값을 안 읽는다」가 거짓이다 | 규약 | 거짓 | 거짓신호 | A2 | `.claude/skills/round/bin/record.py` | 시험은 **키 이름**만 본다. 문면이 「값을」로 좁혀져 있어 참이다 |
| 24 | 새 방어의 조기 반환이 뒤 갈래를 조용히 죽인다 | 저장소 | 거짓 | 실패 | B3 | `.claude/skills/round/bin/record.py` | 앞선 오류와 새 진단이 둘 다 실린다 |
| 25 | 게이트 문서를 고치면 검사가 빨개진다 | 저장소 | 거짓 | 미관 | B3 | `xtask/src/main.rs` | 실제로 고치고 돌렸더니 23/23. 강제 장치가 없다 |
| 26 | 검산 방어 변이가 새 갈래에 안 닿아 죽은 가지다 | 자기장치 | 거짓 | 거짓신호 | B3 | `.claude/skills/round/bin/record.py` | 그 진단이 게이트 아홉에 대해 실린다. 방어를 걷으면 트레이스백이 나온다 |
| 27 | 발견 좌표 중 신선한 체크아웃에 없는 것이 있다 | 회차기록 | 거짓 | 실패 | C2 | `.palimpsest/rounds/2026-09-03-gate-parser-schema/findings.jsonl` | 71 행 전량 존재. 신선 클론에서 초록 |
| 28 | `반환형식.역사형식.legacy-2020` 이 실은 소비되므로 A2 근거가 약하다 | 저장소 | 거짓 | 거짓신호 | A2 | `.claude/skills/round/bin/extract.py` | `자동프로필()` 이 그 프로필을 원리상 안 낸다. 회차 주장이 맞다 |

## 끝내도 되는가

**안 된다.** 본 목록에 금지역 셋(6·7·8)이 열려 있고 실패도 하나(17) 남았다. D1 은 이 사유로 반증이고 D2 는 미측정이다.
