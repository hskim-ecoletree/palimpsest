# 사전부검 라운드 1 — 원 반환문

> `pal-premortem-sweeper` 가 돌려준 것을 그대로 보존한다. 합계 검산의 둘째 원천이다.

원본 저장소는 손대지 않았다(내 변경 0). 다만 **작업 트리에 메인이 진행 중인 미커밋 변경이 있었고**, 그것이 이번 사전부검의 최대 발견원이 됐다 — `record.py --schema` 에 `PAL_MUT` 환경변수 주입 훅이 들어와 있다.

측정은 전부 격리 클론에서 했다: `/tmp/pm94-clone-1788386227` (HEAD `7af5ddc`, 기준선 **23/23 통과**) · `/tmp/pm94-parent` (`8af2240^`) · `/tmp/pm94-1788386200` (git 이력 없는 파일 사본). 좌표는 **저장소 상대**로 적는다 — 시나리오 12 가 그 까닭이다. 저장소 뿌리는 `/Users/incognito/dev/projects/palimpsest-agent-laziness`.

> **메인의 편집 표시 (2026-09-03)** — 원 반환문에서 좌표 **한 자리**를 저장소 상대로 고쳤다:
> 일곱째 시나리오의 `어디가 걸리나` 가 `/tmp/pm94-parent/xtask/src/main.rs:3274` 였다.
> 절대경로 좌표는 A 축에서 영구 면제되어 그 행이 안 재어진다 — **그것을 지적한 것이
> 같은 반환문의 열두째 시나리오다.** 가리키는 파일은 같고 사본의 뿌리만 뗐다.
> 다른 문장은 손대지 않았다.
>
> **정정 2026-09-03 (독립 리뷰 R1 이 잡았다)** — 기각 항 셋의 `어디가 걸리나` 에서
> `__pycache__/` 와 `dashboard.py:29` 의 **순서를 바꿨다.** 추출기가 첫 좌표를 쓰는데
> `__pycache__/` 는 `.gitignore` 대상이라 **신선한 체크아웃에 존재하지 않는다** —
> 그대로 두면 CI 에서 「좌표가 없다」로 빨개진다(로컬은 개발자 트리에 그 디렉터리가
> 있어서 초록이었다). 가리키는 사실은 같고 순서만 바꿨다.
>
> 그리고 `## 내가 기각한 것` 의 **표를 `###` 항으로 폈다.** 내용은 칸 그대로 옮겼고
> 한 줄도 더하거나 빼지 않았다. 까닭은 형식이다 — 추출기가 사전부검 반환문에서
> 항을 `###` 로 세는데 합계 검산은 표 행도 세어, 표로 두면 **항 20 ↔ 추출 16** 으로
> 갈린다. 앞 회차의 반환문도 기각 항을 `###` 로 적었다.

---

### `PAL_MUT` 훅이 정책 검사 전체를 조용히 무력화한다 — 그리고 설치본으로 나간다
- 어떻게 실패하나: 작업 트리의 `record.py --schema` 가 `os.environ["PAL_MUT"]` 로 스키마 출력을 임의 경로에 덮어쓴다. `PAL_MUT='{"path":["닫힘축","요구하는자리"],"value":{"기각":null,"축소":"의도","전환":"의도","범위밖":"의도","정정":null,"확대":null}}' cargo xtask check --root <클론>` → **23/23 통과**, 그런데 A 축 「잰 것」이 **576행 → 35행**(원리상 못 잼 748 → 1289)으로 줄고 판정문은 여전히 「발화 0」이다. `crates/pal-cli/src/install/layout.rs:104` 가 이 파일을 `include_str!` 로 굽으므로 훅은 설치본으로 그대로 나간다 — 설치된 프로젝트에서 환경변수 하나가 정책 스키마를 다시 쓴다.
- 어디가 걸리나: `.claude/skills/round/bin/record.py:752-764` `main` · `crates/pal-cli/src/install/layout.rs:104`
- 획득: 조회 — 격리 클론에 현재 작업 트리 `record.py` 를 넣고 `PAL_MUT` 를 걸어 `cargo xtask check --root` 를 실제로 돌렸다
- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(훅 삭제). 그러나 삭제하면 C1 산출물이 재실행 불가가 되므로 **C1 과 정면 충돌**한다 — 걸리는 곳은 `record.py` 한 자리지만 C1·B1·B2 세 조건이 매달린다

### `PAL_MUT` 로 돌린 B1 은 항등식이고, 틀린 사실을 기록으로 남긴다
- 어떻게 실패하나: B1 은 「변이가 실제 `--schema` 출력에 도달했음을 함께 확인한다」로 조용한 무시를 막으려 했는데, 변이 수단이 **출력 프린터 자신**이면 그 가드는 원리상 언제나 참이다. 실측: `PAL_MUT` 로 `설명.게이트파서.절` 을 바꾸면 값이 출력에 도달하고 **23/23 통과** → 「비정본이라 안 바뀐다」는 결론이 나온다. 그런데 같은 값을 **선언 자리에서** 바꾸면(`판정절 = "판정"` → `"판정 결과"`) 「원장 둘 대조」가 **FAIL** 이다. 같은 키에 대해 두 방법이 반대 답을 낸다
- 어디가 걸리나: `.claude/skills/round/bin/record.py:194` `판정절` · `:427` `설명.게이트파서.절` · `:752-764`
- 획득: 조회 — 두 변이를 각각 돌려 초록/빨강을 관측
- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역 (사실이 아닌 것을 사실로 적는다)
- 대상: 계획자신
- 얼마나 아픈가: B1 판정과 그 근거 산출물 전부를 다시 내야 한다

### 「비정본」 설명의 값 절반이 살아 있는 상수의 별칭이라 A2 가 원리상 안 선다
- 어떻게 실패하나: `설명.게이트파서.절` 은 `판정절`, `설명.게이트파서.판정`·`설명.조건파서.판정` 은 `판정값` 을 **그대로 실은 것**이고 그 둘은 `게이트판정()` 이 분기에 쓴다(`:242`,`:274`,`:302`,`:329`,`:331`). 실측: `판정값` 에 `"보류"` 를 더하면 「원장 둘 대조」 FAIL. 반면 `조건절` 은 진짜 죽은 상수라 바꿔도 23/23 초록. **`--schema` 출력만 보면 이 셋이 구별되지 않는다** — 셋 다 `설명` 아래 평범한 문자열이다. A2 를 「출력만으로 구별된다」로 판정하려면 판정자가 「`설명` 아래면 비정본」이라는 규칙을 스스로 도입해야 하고, 그 규칙은 값의 지위에 대해 거짓이다
- 어디가 걸리나: `.claude/skills/round/bin/record.py:140-141`·`:194`·`:418-429`
- 획득: 조회 — 세 상수를 각각 변이해 초록/빨강 관측 + `grep -n "조건절\|판정절\|판정값"`
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: A2 문면을 「위치로 가른다」로 고치면 닫히지만, 그러면 A2 가 재는 것이 대폭 줄어든다. 걸리는 자리 3

### 「정본 = 소비되는 키」가 `--schema` 출력에 대해 거짓이다 — 열둘 중 여섯이 소비자 0
- 어떻게 실패하나: `--schema` **출력**을 읽는 자는 `xtask`(`합계검산`·`종류`·`반환형식`·`닫힘축`·`열림축`)와 `extract.py`(`반환형식`)뿐이다. `schema_version`·`버전들`·`필드`·`필수`·`기본값`·`enum` 여섯은 **출력 소비자가 0** 이고 `record.py` 안에서 같은 파이썬 객체가 쓰일 뿐이다. 그것은 `설명.게이트파서.절`(=`판정절`)이 소비되는 것과 **정확히 같은 종류의 소비**다. 그런데 `record.py:404-409` 주석은 앞의 여섯을 「정본(소비됨)」에, 뒤를 「정본 아님」에 넣는다 — 같은 잣대를 선 위아래에 다르게 댄다. B2 가 예시로 드는 넷(`열림축`·`닫힘축`·`합계검산`·`종류`)만 실제로 출력 경로에서 소비되므로, B2 를 다 통과해도 「정본 키는 소비된다」는 문장은 열둘 중 넷에만 참이다
- 어디가 걸리나: `.claude/skills/round/bin/record.py:404-409` · `xtask/src/main.rs:3613,3615,3833,3869,4513,4514,4977` · `.claude/skills/round/bin/extract.py:53`
- 획득: 조회 — `grep -n '스키마\["' xtask/src/main.rs` 로 소비 지점 전수 + `--schema` 최상위 키 전수 대조
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 보고에 「소비되는 키가 정본」이라 적으면 그 문장이 절반 거짓이다. 되돌리기 쉽다(문면)

### 격리 사본에 git 이력이 없으면 음성 대조 **전체**가 무효가 된다
- 어떻게 실패하나: `rsync` 로 `.git` 없이 뜬 사본에 `cargo xtask check --root` 를 걸면 **변이와 무관하게** 「발견이 닫혔나」가 FAIL 이다 — 실측: **잰 것 0 · 이 이력에서 안 보임 892 · 형식 이전 432**. 이 상태에서 B1 을 재면 「변이 전후 결과 같음(둘 다 빨강)」이 되고 B2 를 재면 「변이했더니 빨강」이 되어 **둘 다 항등식으로 통과**한다. 이 저장소의 `.git` 은 워크트리 포인터(4K)라 단순 디렉터리 복사가 기본 실패 모드다
- 어디가 걸리나: `xtask/src/main.rs:4294-4310` `커밋이_만진_것` · `:4614-4630` 안_보임 갈래
- 획득: 조회 — `/tmp/pm94-1788386200` (rsync 사본)에서 실제로 돌렸다
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역 (측정이 죽은 가지)
- 대상: 계획대상
- 얼마나 아픈가: `git clone` 으로 뜨면 23/23 초록이 되어 대조가 산다. 실행 계획 2~4 단계 전부가 걸린다

### `8af2240^` 를 현재 `xtask` 로 재면 나오는 RED 는 #94 의 결함이 아니라 버전 스큐다
- 어떻게 실패하나: `cargo xtask check --root /tmp/pm94-parent`(=`8af2240^`) → **3 FAIL**. 그런데 발화는 「`반환형식.반드시있어야하는절` 이 비었다」·「`반환형식.종료보고금지절` 이 비었다」·「`닫힘축.요구하는자리` 가 비었다」다. 그 트리의 `--schema` 최상위 키는 열둘이고 `닫힘축`·`설명`·`반환형식` 이 아예 없다 — **새 바이너리가 옛 선언을 못 찾은 것**이지 「비소비 선언이 검사를 안 바꾼다」가 아니다. 이 출력을 `red/` 산출물로 남기면 RED 관측의 귀속이 통째로 틀린다. 진짜 RED 를 보려면 사본에서 `8af2240^` 로 **재빌드**해야 한다
- 어디가 걸리나: `xtask/src/main.rs:3838`·`4977`·`4521` (세 bail) · `.palimpsest/rounds/2026-09-03-gate-parser-schema/intent.md` RED 관측 계획
- 획득: 조회 — `8af2240^` 를 별도 클론에 체크아웃하고 현재 바이너리로 실제 실행
- 모집단: 원의도
- 유효성: 참
- 해악도: 금지역 (사실이 아닌 것을 사실로 적음)
- 대상: 계획대상
- 얼마나 아픈가: 재빌드하면 되지만 실행 계획에 그 문장이 없다. RED 산출물 하나가 통째로 걸린다

### #94 의 결함은 원리상 RED 로 관측되지 않는다 — 서명이 「초록 유지」다
- 어떻게 실패하나: 계획은 「RED 관측 계획: 비소비 선언이 실제 검사 결과를 바꾸지 않는데도 정본처럼 노출되던 결함을 재현한다」라 적는다. 그런데 그 결함의 관측 가능한 서명은 **「망가뜨렸는데 초록」**이다. 실측: `8af2240^` 의 `xtask` 는 스키마에서 `스키마["종류"]` **하나만** 읽는다(`스키마를_읽는다` 는 존재조차 안 한다). 그러므로 `게이트파서` 를 통째로 파괴해도 그 판의 검사는 초록이다. 「RED 를 냈다」고 적으려면 다른 무언가를 빨갛게 만들어야 하고, 그것이 앞 시나리오의 버전 스큐다
- 어디가 걸리나: `xtask/src/main.rs:3274`(그 판의 사본) (그 판의 유일한 스키마 소비) · `intent.md` **RED 관측 계획**
- 획득: 조회 — `8af2240^` 트리에서 `grep -c 스키마를_읽는다` → 0, `grep -n '스키마\['` → `종류` 한 줄
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 문면 정정(“GREEN 관측”)으로 닫힌다. 되돌리기 쉽다

### 닫힌 게이트 둘이 `--schema` 의 `게이트파서` 를 근거로 대는데 그 경로가 지금 없다
- 어떻게 실패하나: `8af2240` 이 키를 `설명.` 아래로 내리면서 최상위 `게이트파서` 가 사라졌다. 실측: 현재 `--schema` 출력에 `.게이트파서` 는 **없고**(`False`) `.설명.게이트파서.대조밖` 만 있다. 그런데 `docs/gates/rust-extractor.md:73` 은 C5 통과의 근거로 *"그 경계가 파서에도 적혀 있다(`--schema` 의 `게이트파서.대조밖`)"* 를, `docs/gates/agent-laziness.md:118` 은 묶음 C 의 「무엇을 돌렸나」로 *"`record.py --schema` 의 `게이트파서`"* 를 댄다. 둘 다 **닫힌 판정**이고, 지시한 대로 조회하면 `null` 이 나온다. 어떤 검사도 게이트의 근거 명령을 안 돌린다
- 어디가 걸리나: `docs/gates/rust-extractor.md:73` · `docs/gates/agent-laziness.md:118` · `.claude/skills/round/bin/record.py:426`
- 획득: 조회 — `--schema` 출력에서 `.게이트파서` 존재 여부를 실제로 물었다
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 두 줄 정정. 지금 계획의 완수 조건 어디에도 이 자리가 없다

### `C2-b`「스키마의 유일한 자리」의 지위가 조용히 줄었는데 완수 조건이 그것을 안 잰다
- 어떻게 실패하나: #94 본문이 스스로 적는다 — *"사전을 없애면 C2-b 의 「스키마의 유일한 자리」가 이 장치에 대해 성립 안 한다"*. 3안(격하)도 같은 효과다. 그런데 `docs/gates/round-finding-records.md:44` 의 표준 표는 `C2-b` 를 **통과**로 닫아 두었고, 이 회차의 완수 조건 A1~D2 어디에도 「C2-b 의 범위가 줄었음을 기록한다」가 없다. #94 를 닫으면 「스키마가 유일한 자리다」라는 닫힌 판정과 「게이트/조건 파서에 대해서는 아니다」라는 새 사실이 같이 서 있게 된다
- 어디가 걸리나: `docs/gates/round-finding-records.md:44`·`:63` · `.palimpsest/rounds/2026-09-03-gate-parser-schema/intent.md` 완수 조건
- 획득: 조회 — `gh issue view 94` 본문과 게이트 표준 표를 읽고 완수 조건 목록과 대조
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 게이트 한 줄 추가로 닫힌다. 안 하면 닫힌 판정 하나가 과장된 채 남는다

### `B3`「두 번째 정본을 만들지 않는다」는 이미 있는 손 열거 앞에서 판정 불가다
- 어떻게 실패하나: `설명.게이트파서` 의 열두 키 중 `빈모집단`·`전량0`·`미측정노출`·`짝중복`·`게이트없음`·`하한`·`대조밖` 일곱은 `record.py` 의 파서가 아니라 **`xtask` 의 `check_ledger_pair` 갈래**를 손으로 옮겨 적은 것이다. 즉 「두 파서의 코드 갈래를 손으로 열거한 두 번째 사전」이 **파이썬 파일 안에 이미 있다.** B3 은 「만들지 않는다」이므로 아무것도 안 하면 자동 통과하고, 「정본」의 정의가 「누가 읽느냐」라 라벨만으로 면제된다 — 반증할 입력이 원리상 없다
- 어디가 걸리나: `.claude/skills/round/bin/record.py:426-441` · `xtask/src/main.rs:4934` `check_ledger_pair`
- 획득: 조회 — 열두 키를 `check_ledger_pair` 갈래와 눈으로 대조(코드 읽기). 실행 대조는 안 했다
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: B3 을 「기존 열거를 늘리지 않는다 + 늘어난 것을 세는 자를 남긴다」로 좁히면 잴 수 있다

### 변이를 안 되돌려도 어떤 검사도 못 잡는다 — `설명` 이 CI 사각지대다
- 어떻게 실패하나: 실행 계획 3 단계가 `설명.조건파서`·`설명.게이트파서` 를 실제로 변이한다. 실측: `설명.게이트파서` 의 `표`·`형식이전`·`대조밖` 세 값을 엉터리로 바꿔 놓아도 `cargo xtask check` 는 **23/23 통과**다. `cargo test -p pal-cli` 의 `설치본의_record_가_스키마를_낸다` 도 `["schema_version","필수","enum","대응표","합계검산"]` 부분 문자열만 본다. 되돌리는 유일한 장치가 사람의 `git diff` 다 — 그리고 이 회차는 그 변이를 정상 절차로 시킨다
- 어디가 걸리나: `.claude/skills/round/bin/record.py:411-441` · `crates/pal-cli/tests/round_scripts_run.rs:125`
- 획득: 조회 — 변이한 채로 `cargo xtask check --root` 를 돌려 23/23 관측
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳 1 — 다만 잡아 줄 자가 없다는 것이 요점이다

### 격리 사본의 절대경로를 발견 `경로` 에 적으면 그 행은 A 축에서 영구 면제된다
- 어떻게 실패하나: `저장소_밖_절대경로()` 는 `/` 로 시작하는 모든 문자열을 면제한다. 이 계획은 2·3·4 단계를 전부 격리 사본에서 돌리므로 발견 좌표가 `/tmp/pm94-…/record.py` 꼴이 되기 쉽고, 그러면 그 행은 「원리상 못 잼」으로 빠져 `발견이 닫혔나` 가 **영원히 안 잰다**. 현재 트리에도 이미 그런 행이 하나 있다(판정문: 「좌표 면제 1행 (저장소 밖 절대경로)」)
- 어디가 걸리나: `xtask/src/main.rs:4730-4741` `저장소_밖_절대경로` · `:4646` 이후 면제 갈래
- 획득: 조회 — 함수 본문과 시험(`저장소_밖_절대경로("/tmp")`)을 읽고, 현재 판정문의 「좌표 면제 1행」을 확인
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역 (측정이 죽은 가지)
- 대상: 계획대상
- 얼마나 아픈가: 레코드를 쓸 때 상대 좌표로 적으면 막힌다. 되돌리기 쉽지만 놓치면 조용하다

### `판정값` 이 늘면 게이트 파서가 진단이 아니라 `IndexError` 로 죽는다
- 어떻게 실패하나: `검산 = {k2: int(m.group(k2)) for k2 in 판정값}` 이 enum 목록을 정규식 그룹 이름과 1:1 로 묶는다. `판정값` 에 다섯째를 더하면 `re` 가 `IndexError: no such group` 을 던지고, `xtask` 는 「원장 둘 대조」를 파이썬 트레이스백과 함께 FAIL 로 낸다 — 형식 오류 진단이 아니다. 실측으로 재현했다
- 어디가 걸리나: `.claude/skills/round/bin/record.py:329` (`게이트판정` 검산 갈래)
- 획득: 조회 — `판정값` 에 `"보류"` 를 더해 `cargo xtask check --root` 실행
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 한 줄로 막힌다(`판정값 ∩ 그룹`). #94 가 지목한 파서 안이라 범위 안이다

### `설명.조건파서` 는 Rust 파서를 서술하는데 값은 Python 상수다 — 두 번째 정본이 언어 경계를 넘어 산다
- 어떻게 실패하나: 조건 문법의 정본은 `crates/pal-intent/src/round_condition.rs` 다(`record.py:조건파일` 은 `pal round conditions` 로 위임한다). 그런데 `설명.조건파서.절` 은 파이썬의 죽은 상수 `조건절 = "완수 조건"` 이고 Rust 쪽에는 별도로 `const SECTION: &str = "완수 조건"` 이 있다 — **같은 문자열 두 벌**. `설명.조건파서.판정` 은 더 나쁘다: 값이 `판정값`(게이트 파서의 상수)이라, Rust 의 `Verdict` 가 바뀌어도 이 설명은 **엉뚱한 파서에 용접된 채 그대로 옳아 보인다.** 「비정본」 라벨은 갈리는 것을 허용할 뿐 이 오배선을 안 고친다
- 어디가 걸리나: `.claude/skills/round/bin/record.py:140-141`·`:418-424` · `crates/pal-intent/src/round_condition.rs:9`(`SECTION`)·`:38-59`(`Verdict`)
- 획득: 조회 — `조건절` 변이 후 23/23 초록 관측 + 두 파일의 상수 대조
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: `조건파서` 를 지우거나 Rust 에서 뽑아 오면 닫힌다. 걸리는 곳 2

### 검증 단계가 `--schema` 출력을 실제로 재는 유일한 시험을 안 돈다
- 어떻게 실패하나: 실행 계획 6 은 `cargo xtask check` + `cargo test -p xtask` 만 돈다. `xtask` 의 시험 42 개는 순수 함수(`표준_검산_줄인가`·`요구하는_자리`·`펜스_밖`…)이고 `record.py` 를 안 부른다. `--schema` **출력 내용**과 **설치본의 record.py** 를 재는 것은 `crates/pal-cli/tests/round_scripts_run.rs:114-128` 하나뿐인데 계획에 없다. C2 의 「관련 시험」이 무엇인지 정의가 없어 판정자가 스스로 고른다
- 어디가 걸리나: `crates/pal-cli/tests/round_scripts_run.rs:114-128` · `intent.md` 실행 계획 6 · 완수 조건 C2
- 획득: 조회 — `grep -rn '\-\-schema' --include="*.rs"` 로 소비 시험 전수 확인
- 모집단: 원의도
- 유효성: 참
- 해악도: 실패
- 대상: 계획대상
- 얼마나 아픈가: 명령 한 줄 추가. `PAL_MUT` 훅이 남아 있으면 이 시험이 그것을 통과시킨다는 점이 더 아프다

### 만들 필요가 없는 것을 만들고 있다 — 새 장치가 #94 가 이름 붙인 루프의 네 번째 먹이다
- 어떻게 실패하나: #94 본문이 규약 §11 ③ 을 인용해 *"장치를 고칠 때마다 새 선언면이 생겨 다음 라운드의 먹이가 된다"* 를 적고, 세 라운드가 그것을 실증했다. 그런데 이 계획은 판정만으로 끝날 수 있는 자리에 `PAL_MUT` 라는 **새 선언면**(어디에도 선언 안 된 환경변수 계약)을 이미 하나 만들었다. 「정본 표식 키」나 「소비되는 키를 열거하는 검사」를 더하면 그것도 같은 자리다 — 기계는 「코드 갈래」를 셀 축이 없으므로 그런 검사는 반드시 손으로 벤 거울이 된다. 퇴로가 *"새 제품 코드를 만들지 않고 검증·판정만"* 이라 적혀 있는데 그 퇴로가 이미 깨졌다. 더 작은 표면: **선언 자리에서 직접 변이 → `git stash` 로 원복**이면 새 코드가 0 이고 결론도 더 강하다(실제로 이 사전부검이 그렇게 재서 `PAL_MUT` 보다 더 많은 것을 봤다)
- 어디가 걸리나: `.claude/skills/round/bin/record.py:752-764` (새 장치) · `intent.md` **퇴로**
- 획득: 추정 — 「이것이 루프의 네 번째 먹이인가」는 다음 라운드가 물어야 확정된다. 다만 훅의 존재와 퇴로 문면의 충돌은 조회로 확인했다
- 모집단: 자기장치
- 유효성: 추정
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 지금 걷으면 비용 0. 커밋되면 설치본까지 따라간다

---

## 내가 기각한 것

### 프리빌트 `xtask` 가 `CARGO_MANIFEST_DIR` 로 박혀 격리 사본을 재도 원본을 잰다

- 어디가 걸리나: `xtask/src/main.rs:670` `repo_root`
- 획득: 조회 — `--root <클론>` 과 `--root <이력없는 사본>` 이 서로 **다른 수**를 냈다(576행 vs 0행)
- 모집단: 저장소
- 유효성: 거짓
- 해악도: 금지역
- 대상: 계획대상
- 왜 아니었나: 이미 닫혀 있다 — `뿌리를_고른다()`(`:114`)와 `명령을_고른다()`(`:127`)가 `--root` 를 받고 모르는 인자를 거부한다. 앞 회차가 실측으로 막아 놓았다

### `설명.대응표` 는 실제로 `검증()` 이 소비하므로 「비정본」이 거짓이다

- 어디가 걸리나: `.claude/skills/round/bin/record.py:121`·`:416`·`:610-614`
- 획득: 조회 — `grep -n 대응표` 전수
- 모집단: 저장소
- 유효성: 거짓
- 해악도: 거짓신호
- 대상: 계획대상
- 왜 아니었나: `:612`·`:614` 는 진단 **문자열 안에** `(대응표)` 라 적을 뿐 사전을 안 읽는다. `대응표` 는 진짜 소비자 0 이다

### `__pycache__` 의 스테일 `.pyc`(8/23 자 `record.cpython-313.pyc`)가 변이를 가려 음성 대조가 헛돈다

- 어디가 걸리나: `.claude/skills/round/bin/dashboard.py:29` `from record import 조건파일` · 같은 디렉터리의 `__pycache__/`
- 획득: 조회 — `record.py` 는 `xtask`·`extract.py` 모두 **서브프로세스**로 부르고 import 하는 것은 `dashboard.py` 하나뿐이며, 그 경로는 이 회차의 대조에 안 든다
- 모집단: 저장소
- 유효성: 거짓
- 해악도: 실패
- 대상: 계획대상
- 왜 아니었나: 소비 경로가 전부 `python3 record.py` 실행이라 `.pyc` 를 안 탄다. 게다가 무효화는 mtime·size 기반이다

### `include_str!` 때문에 `record.py` 변이가 설치본 시험에 안 닿는다(컴파일 시점 박힘)

- 어디가 걸리나: `crates/pal-cli/src/install/layout.rs:104`
- 획득: 추정 — 재빌드 실측은 안 했다. 다만 `include_str!` 은 rustc 가 dep-info 에 싣고 cargo 가 그것으로 재빌드한다
- 모집단: 저장소
- 유효성: 거짓
- 해악도: 실패
- 대상: 계획대상
- 왜 아니었나: 박히는 것은 맞지만 **자동 재빌드된다.** 진짜 위험은 이쪽이 아니라 시나리오 1(훅이 그대로 설치본에 실린다)이다

새 범주: 시험 주입 훅이 제품 표면이 되는 자리 — 「음성 대조를 위해 만든 우회로가 배포되어 검사 자체의 우회로가 된다」
