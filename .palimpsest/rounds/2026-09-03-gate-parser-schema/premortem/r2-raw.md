# 사전부검 라운드 2 — 원 반환문

> `pal-premortem-sweeper` 가 돌려준 것을 그대로 보존한다. 1 라운드의 처분으로 바뀐 계획을 줬다.
>
> **메인의 편집 표시 (2026-09-03)** — 내용은 손대지 않았고 둘만 손봤다.
> ① `## 내가 기각한 것` 의 표를 `###` 항으로 폈다(추출기가 항을 `###` 로 센다 · 1 라운드와 같은 사유).
> ② HTML 이스케이프(`&amp;`·`&lt;`·`&gt;`)를 원래 글자로 되돌렸다.

측정은 `/tmp/premortem-r2-20260903/repo` (격리 클론 · HEAD `7af5ddc`)에서 했고, 사본 안에서 `cargo xtask check` 를 **빌드해** 돌렸다. 기준선 **23/23 통과 · exit 0** 확인 후 시작했고, 모든 변이는 `git checkout --` 로 복원해 마지막 `git status` 가 깨끗함을 확인했다. 사용자 작업 트리는 만지지 않았다.

---

### `설명.대응표` 에는 살아 있는 소비자가 있다 — 수정 3 의 문면이 거짓이 된다
- 어떻게 실패하나: `설명.대응표` 를 `설명.규약대응` 으로 **개명만** 하고 `cargo test -p pal-cli --test round_scripts_run` → `설치본의_record_가_스키마를_낸다` **FAILED** (14 passed / 1 failed). 1 라운드 측정은 `cargo xtask check` 로만 쟀고, 그 검사는 초록이라 `설명` 아래가 전부 죽은 것으로 보였다. 수정 3 이 「이 아래는 어느 코드도/검사도 안 읽는다」 계열의 문장을 남기면 **C2 가 요구하는 시험이 반증하는 문장을** 이 회차가 새로 적는다.
- 어디가 걸리나: `.claude/skills/round/bin/record.py:411-412`(`정본아님`) · `:416`(`대응표`) · `crates/pal-cli/tests/round_scripts_run.rs:125`
- 획득: 조회 — 격리 사본에서 개명 후 `cargo test -p pal-cli --test round_scripts_run`
- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(문면 한 줄). 걸리는 곳 둘 — 문면과 그 시험. 다만 **판정 근거가 바뀐다**: `설명` 이 「전부 죽었다」가 아니라 「검사에는 안 걸리고 시험 하나에는 걸린다」다.

### `조건절` 은 죽은 상수이고 Rust 정본의 그림자다 — 수정 2 가 그것을 최상위 정본으로 승격한다
- 어떻게 실패하나: `조건절 = "완수 조건"` 을 `"완전히 틀린 절 이름"` 으로 바꾸고 `cargo xtask check` → **23/23 통과**. 조건 문법의 실제 정본은 `crates/pal-intent/src/round_condition.rs:9 const SECTION: &str = "완수 조건"` 이고, Python 쪽은 `pal round conditions` 호환 래퍼로 내려가 이 상수를 아무도 안 쓴다(유일한 참조가 `스키마()` 자신이다). 수정 2 가 이것을 `파서.조건.절` 로 최상위에 올리면 **낡은 사본을 「정본」이라 선언**하는 것이고, Rust 가 절 이름을 바꿔도 아무것도 안 운다.
- 어디가 걸리나: `.claude/skills/round/bin/record.py:140` · `crates/pal-intent/src/round_condition.rs:9`
- 획득: 조회 — 변이 후 `cargo xtask check`; `grep -n 조건절 .claude/skills/round/bin/*.py` → 정의 1 · 스키마 1
- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 1 라운드 측정표는 `판정절`·`판정값` 만 쟀고 `조건절` 은 **안 쟀다** — 수정 2 가 옮기는 셋 중 하나가 미측정이다.

### 새 최상위 `파서` 키에도 소비자가 0 이고, 위치 규약을 아무것도 강제하지 않는다
- 어떻게 실패하나: 최상위에 `"허수선언": {"이것은": "아무도 안 읽는다", "그런데": "최상위다"}` 를 통째로 더하고 `cargo xtask check` → **23/23 통과**. 수정 1~3 을 다 해도 「최상위 = 정본 · `설명` 아래 = 비정본」은 **사람이 지키는 관례**일 뿐이고, 다음 커밋이 죽은 키를 최상위에 하나 더 놓아도 검사가 침묵한다. A2(「출력만으로 지위가 구별된다」)를 기계가 재는 수단이 이 계획 뒤에도 0 이다 — 이것이 #94 가 이름 붙인 순환의 다음 바퀴다.
- 어디가 걸리나: `.claude/skills/round/bin/record.py:353-562`(`스키마()`) · `xtask/src/main.rs:3613`·`3615`·`3833`·`4513-4514`·`4977`
- 획득: 조회 — 격리 사본에서 키 삽입 후 `cargo xtask check`
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 그러나 A2 판정이 「출력을 사람이 보고 납득했다」로만 닫히고, 그 판정자가 자기 규칙(위치 관례)을 도입하는 자리가 바로 여기다.

### 「정본」키를 **지우면** 초록이다 — `unwrap_or` 가 같은 값을 하드코딩해 둔 두 번째 정본이다
- 어떻게 실패하나: 열 개를 한꺼번에 **삭제**하고 `cargo xtask check` → **23/23 통과**. 지운 것: `열림축.닫힘값`·`열림축.열림값`·`열림축.부터`·`닫힘축.의도파일`·`닫힘축.경로없음`·`닫힘축.사전처분없음`·`반환형식.없음표시`·`합계검산.사전부검항`·`합계검산.사전부검기각절`·`합계검산.독립리뷰표머리`. 소비자가 전부 `…as_str().unwrap_or("<같은 문자열>")` 이라 **부재가 안 보인다.** 1 라운드 표는 이 키들을 「값을 바꾸면 FAIL」로 재서 정본으로 분류했는데, 삭제(B2 가 말하는 「유효한 구조 안에서」의 또 다른 형태)에는 죽어 있다. 즉 **「소비되는가」라는 술어가 변이 연산자에 상대적**이고, 판정자가 어느 연산자를 쓰느냐로 정본 목록이 갈린다.
- 어디가 걸리나: `xtask/src/main.rs:3218-3221` · `:3834` · `:4515-4522` · `:4399`·`:4405`·`:4415`
- 획득: 조회 — 열 키 일괄 삭제 후 `cargo xtask check`
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳 열 자리 이상. **A2·B2 를 이 계획대로 닫으면 「정본이다」가 조건부로만 참이다.**

### 수정 5 의 한 줄 방어는 크래시를 `IndexError` → `KeyError` 로 옮길 뿐이다
- 어떻게 실패하나: `판정값` 에 `"보류"` 를 더하고 `python3 record.py gate docs/gates/agent-laziness.md` → `IndexError: no such group` (record.py:329). 계획이 말하는 한 줄 방어를 `{k2: int(m.group(k2)) for k2 in 판정값 if k2 in m.groupdict()}` 로 넣고 다시 돌리면 → `KeyError: '보류'` (record.py:332). **형식 오류 진단으로 바뀌지 않는다.** 그리고 현 게이트 전량(`docs/gates/*.md`)에 대해 방어 전/후 출력은 **완전히 동일**하다 — 즉 그 갈래는 어떤 시험도 안 지나고 관측되지 않는다.
- 어디가 걸리나: `.claude/skills/round/bin/record.py:329` · `:332` · `:199-203`
- 획득: 조회 — 두 변이를 실제로 돌렸고, 게이트 전량 출력 diff 0
- 모집단: 자기장치
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 진짜 원인은 `판정값`(record.py:141)과 `_검산` 의 이름 그룹 넷(`:199-203`)이 **같은 집합을 두 번 손으로 벤 것**이다. 한 줄 방어는 그 둘의 갈림을 **시끄러운 크래시에서 조용한 오답으로** 바꾸는 방향이다.

### 판정 넷은 이미 세 곳에 손으로 벴다 — 수정 2 는 그중 하나를 「정본」으로 못 박는다 (B3 위반)
- 어떻게 실패하나: `record.py:141 판정값` · `record.py:199-203 _검산` 의 이름 그룹 · `crates/pal-intent/src/round_condition.rs:45-57`(`Verdict` enum, 조건 파서 쪽 정본). 수정 2 가 `파서.조건.판정`·`파서.게이트.판정` 으로 이 값을 최상위에 놓으면 **조건 파서의 정본이 아닌 사본**이 조건 파서의 정본이라 선언된다. B3 는 「두 파서의 코드 갈래를 다시 손으로 열거한 두 번째 정본을 만들지 않는다」인데, 계획 자신이 그것을 만든다.
- 어디가 걸리나: `.claude/skills/round/bin/record.py:141`·`:199-203` · `crates/pal-intent/src/round_condition.rs:45-57`
- 획득: 조회 — `grep -n "대조불가\|미측정" crates/pal-intent/src/round_condition.rs`
- 모집단: 자기장치
- 유효성: 참
- 해악도: 금지역
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. **B3 정정문(「늘어난 항의 수를 세어 보인다」)이 이것을 못 잡는다** — 키 수는 4 → 4 로 그대로이고, 는 것은 「정본이라 주장하는 손 열거」 0 → 2 다. 세는 양이 틀렸다.

### `xtask` 쪽에는 `판정인절` 의 죽은 매개변수와 자기 파일 안에서 서로 반대인 두 주석이 남는다
- 어떻게 실패하나: `반환형식.판정인절` 을 `설명` 아래로 내려도 `xtask/src/main.rs:3836` 이 `Vec::new()` 를 만들어 `:3869` 로 넘기고 `:3217` 이 `let _ = 발견아닌절;` 로 버린다. 그 바로 위 `:3212` 주석은 *"판정이지 발견이 아닌 절은 안 센다"* 라 적혀 있고, 같은 파일 `:3317` 시험 주석은 *"판정이지 발견이 아닌 절도 센다"* 라 적혀 있으며 `:3321` 시험이 후자를 강제한다. 이 회차가 「선언의 지위를 정리했다」로 닫으면 **거짓인 주석 하나와 죽은 매개변수 하나를 그대로 두고** 그 자리를 지나간다.
- 어디가 걸리나: `xtask/src/main.rs:3202` · `:3212-3215` · `:3217` · `:3836` · `:3317-3321`
- 획득: 조회 — 파일 직독 + 시험 본문
- 모집단: 저장소
- 유효성: 참
- 해악도: 금지역
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳 셋. 계획이 이 키를 만지므로 **범위 밖이라 말하기 어렵다.**

### 수정 1 의 「죽은 리터럴 셋」이 좁다 — `역사기계칸.legacy-2020` 은 서브트리 통째로 죽었다
- 어떻게 실패하나: `반환형식.역사기계칸.legacy-2020` 블록을 통째로 지우고 `반환형식.역사형식.legacy-2020` 을 명백한 거짓 문장으로 바꾼 뒤 `cargo xtask check` → **23/23 통과**. 까닭은 `extract.py:326-341 자동프로필()` 이 원리상 `"legacy-2020"` 을 **못 낸다** — 반환값은 `legacy-2019`·`legacy-2022`·`legacy-behavior`·`current` 넷뿐이다. 수정 1 이 `역사형식`(설명문)만 내리고 `역사기계칸.legacy-2020`(최상위, 똑같이 죽음)을 남기면, 이 회차가 「죽은 것을 내렸다」고 적은 뒤에도 최상위에 죽은 정본이 남는다.
- 어디가 걸리나: `.claude/skills/round/bin/record.py:457` · `:539-542` · `.claude/skills/round/bin/extract.py:326-341`
- 획득: 조회 — 삭제·오염 후 `cargo xtask check`
- 모집단: 저장소
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 걸리는 곳 둘.

### `설명.정본아님` 위의 정본 키 열거가 이미 둘을 빠뜨렸다 — 수정 3 이 JSON 문자열만 고치면 그대로 남는다
- 어떻게 실패하나: `record.py:408-409` 주석이 정본 키를 여덟으로 센다(`종류`·`필드`·`필수`·`enum`·`기본값`·`버전들`·`열림축`·`닫힘축`). 그런데 `xtask` 는 `스키마["반환형식"]`(`main.rs:3833`, `:4977`)과 `스키마["합계검산"]`(`:3613`, `:3869`)도 읽는다 — **둘이 빠졌다.** 수정 3 이 「`설명.정본아님` 문면을 측정한 것에 맞춘다」로만 적혀 있어, 바로 위 주석의 손으로 벤 여덟 항은 계획의 사정거리 밖이다.
- 어디가 걸리나: `.claude/skills/round/bin/record.py:408-409` · `xtask/src/main.rs:3613`·`3833`·`3869`·`4977`
- 획득: 조회 — `grep -n '스키마\["' xtask/src/main.rs`
- 모집단: 자기장치
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 한 자리.

### 이 회차가 쓸 게이트가 다른 회차의 `intent.md` 경로를 한 번만 인용해도 「원장 둘 대조」가 죽는다
- 어떻게 실패하나: `docs/gates/rust-extractor.md` 끝에 산문 한 줄로 `.palimpsest/rounds/2026-08-22-agent-laziness/intent.md` 를 인용하고 `cargo xtask check` → **`FAIL 원장 둘 대조 — 게이트 2 개가 같은 회차를 가리킨다`**. 짝짓기는 `본문.contains("{회차_뿌리}/{회차}/intent.md")` 라 링크든 산문이든 코드 스팬이든 다 걸린다. 이 회차의 게이트는 성질상 **앞 회차들의 판정·조건을 근거로 인용**할 유인이 크다(C1 이 「재실행 가능한 산출물」을 요구한다).
- 어디가 걸리나: `xtask/src/main.rs:5063-5065` · `:5138-5146` · 새로 쓸 게이트 문서
- 획득: 조회 — 격리 사본에서 한 줄 추가 후 `cargo xtask check` 로 재현
- 모집단: 저장소
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다(한 줄). 진단문이 원인을 안 가리킨다 — 「게이트 2 개」라고만 말하고 어느 줄이 그랬는지는 안 낸다.

### 사전부검 발견의 `닫은커밋` 은 이 회차의 `intent.md` 를 만진 커밋이어야 한다 — 제품 커밋으로 닫으면 실패
- 어떻게 실패하나: `닫힘축.의도를요구하는출처 = ["사전부검"]` 이므로 `요구하는_자리()` 가 출처=사전부검 행 전량에 대해 이 회차의 `intent.md` 를 요구한다(처분이 `기각` 이 아닌 한). 그런데 이 회차의 자연스러운 커밋 모양은 `record.py` + 게이트 문서를 고치는 제품 커밋이고, 그 커밋은 `intent.md` 를 안 만진다. 그러면 「발견이 닫혔나」가 발화한다. `퇴로`(기존 구현이 충족하면 제품 코드를 안 만든다)를 타면 **모든 사전부검 발견이 `기각` 이 아닌 처분을 받기 어려워지고**, 반대로 `기각` 으로 몰면 `닫힘축` 이 그것을 「원리상 못 잼」으로 빼서 A 축이 아무것도 안 잰다.
- 어디가 걸리나: `xtask/src/main.rs:4394-4426` · `:4462-4468` · `.claude/skills/round/bin/record.py:381-386`
- 획득: 조회 — 코드·시험 직독. 커밋을 실제로 쌓아 재현하지는 않았다
- 모집단: 규약
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있으나 **커밋 이력을 다시 만들어야 한다.** `intent.md` 의 `## 개정` 절을 처분 커밋마다 함께 만지는 것이 유일한 정상 경로다.

### 이 회차 자신의 사전부검·리뷰 반환문이 「진행 중」 모집단에 들어 기계 칸 전량 일치를 요구한다
- 어떻게 실패하나: `check_round_records` 는 끝난 회차의 전사 갈림은 **보고만** 하고, **진행 중 회차는 실패로 낸다.** 그러므로 이 반환문의 `요약`·`경로`·`모집단`·`유효성`·`해악도` 다섯 칸이 `extract.py` 산출과 **글자 단위로** 같아야 하고, 항 수가 레코드 행 수와 같아야 한다. 손으로 한 칸이라도 다듬으면 `cargo xtask check` 가 빨개진다.
- 어디가 걸리나: `xtask/src/main.rs:3926-3958` · `:3199-3270` · `.claude/skills/round/bin/extract.py:377-430`
- 획득: 조회 — 코드 직독 + 기준선 판정문의 「진행 중 0」
- 모집단: 규약
- 유효성: 참
- 해악도: 실패
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 회차 안에서 반복적으로 문다.

### 수정 4 는 「동결」 규율과 부딪히는데, 규율의 모집단이 코드에서 조용히 좁아져 있다
- 어떻게 실패하나: `docs/gates/README.md:1-4` 는 *"이 디렉터리는 **동결된 기록**이다 … 사후에 안 고친다"* 라 적으면서 모집단을 `F* · G* · S* · preflight` 로 든다. 코드는 그것을 그대로 박았다 — `xtask/src/main.rs:2452-2459 동결된_판정_문서인가()` 가 `F|G|S` 접두 또는 `preflight` 만 동결로 본다. 그래서 `rust-extractor.md`·`agent-laziness.md`·`round-*.md` 는 **원리상 동결 밖**이고, 실제로 수정 4 를 적용하고 `cargo xtask check` 를 돌리면 23/23 초록이다. 문제는 판정자가 어느 문장을 읽느냐로 답이 갈린다는 것이다: 제목(「이 디렉터리는」)을 읽으면 위반, 괄호 안 글로브를 읽으면 위반 아님. **규율 자신이 낡았다** — 이름 규칙이 `F/G/S` 에서 기능명으로 바뀐 뒤 그 글로브가 대부분의 게이트를 놓쳤다.
- 어디가 걸리나: `docs/gates/README.md:1-18` · `xtask/src/main.rs:2452-2459` · `:2520`
- 획득: 조회 — 두 문서 직독 + 수정 4 를 실제로 적용해 `cargo xtask check` 23/23 확인
- 모집단: 규약
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 「이 디렉터리」 문장을 믿는 다음 리뷰어가 수정 4 를 **동결 위반**으로 판정할 수 있고, 그 논쟁의 정본이 없다.

### 수정 2 뒤 `파서.게이트` 와 `설명.게이트파서` 가 이름으로 겹친다 — 수정 4 가 어느 쪽을 대야 하는지가 사람 판단이다
- 어떻게 실패하나: 수정 2 는 `절`·`판정` 을 `파서.게이트` 로 올리고 나머지 열은 `설명.게이트파서` 에 남긴다. 그런데 `docs/gates/rust-extractor.md:73` 이 대는 것은 `게이트파서.대조밖`(→ `설명.게이트파서.대조밖`)이고, `docs/gates/agent-laziness.md:118` 이 대는 것은 한정 없는 `게이트파서`(→ 어느 쪽?)다. 후자를 `파서.게이트` 로 고치면 **거짓**이고, `설명.게이트파서` 로 고치면 그 게이트의 C 묶음 근거가 *"정본 아님"* 이라 명시된 자리를 가리킨다. 둘 다 닫힌 **통과** 판정의 근거 칸이다.
- 어디가 걸리나: `docs/gates/rust-extractor.md:73` · `docs/gates/agent-laziness.md:118` · `.claude/skills/round/bin/record.py:418`·`:426`
- 획득: 추정 — 이름 겹침과 두 줄의 문면은 조회했으나, 어느 쪽으로 고칠지는 계획에 안 적혀 있어 잘못 고를 확률은 못 쟀다
- 모집단: 회차기록
- 유효성: 추정
- 해악도: 금지역
- 대상: 계획자신
- 얼마나 아픈가: 되돌릴 수 있다. 두 줄. 계획이 「두 줄 정정」이라고만 적고 **무엇으로** 정정할지를 안 잠갔다.

### A2 를 전수로 확인할 수단이 없어 표본 판정으로 닫힌다
- 어떻게 실패하나: `--schema` 출력의 잎은 **218** 이고 그중 `설명` 아래가 **35**, 밖이 **183** 이다. 1 라운드가 실제로 변이한 자리는 열셋 남짓이고, 이번 라운드가 더 잰 것을 합쳐도 밖 183 중 열댓뿐이다. 「`설명` 아래에 소비되는 값이 0 이다」와 「`설명` 밖은 전부 소비된다」 둘 다 **표본 진술**이고, 위의 「지우면 초록」 열 건과 「`대응표` 는 시험이 읽는다」 한 건이 이미 양쪽 방향으로 반례를 냈다. 전수 확인은 잎 하나씩 변이·삭제해 `cargo xtask check` + `cargo xtask test` 를 218×2 회 돌리는 것뿐인데, 계획에 그 장치가 없다.
- 어디가 걸리나: `.claude/skills/round/bin/record.py:353-562` · 이 회차 `intent.md` 의 A2·B1·B2
- 획득: 조회 — `--schema` 출력을 재귀로 펼쳐 잎을 셌다(218 / 35 / 183)
- 모집단: 원의도
- 유효성: 참
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 없다 — **A2 를 「확인했다」로 닫으면 그 문장이 게이트에 동결된다.** 「표본 N 자리를 이 연산자로 쟀다」로 적는 것이 유일한 정직한 출구다.

### `record.py gate` 의 exit 1 이 「형식 오류」와 「파이썬이 죽었다」를 안 가른다
- 어떻게 실패하나: `판정값` 변이로 `IndexError` 가 난 실행의 종료값은 **1** 이고, 형식 오류만 있을 때의 `cmd_gate` 반환값도 **1** 이다. `xtask` 는 `파서에_묻는다()` 에서 **rc 를 안 보고** stdout 이 비었는지로만 가른다(`main.rs:4867-4874`) — 이번엔 우연히 stdout 이 비어 진단이 났지만, 표준출력에 부분 JSON 이 나가는 크래시 형태에서는 **크래시가 형식 오류로 위장**된다. 수정 5 는 그 갈래를 손보면서도 이 구별을 안 세운다.
- 어디가 걸리나: `.claude/skills/round/bin/record.py:344-350` · `xtask/src/main.rs:4857-4877`
- 획득: 조회 — `python3 record.py gate …; echo $?` 로 둘 다 1 임을 확인. 부분 JSON 을 내는 크래시 형태는 못 만들어 봤다
- 모집단: 저장소
- 유효성: 추정
- 해악도: 거짓신호
- 대상: 계획대상
- 얼마나 아픈가: 되돌릴 수 있다. 한 자리.

---

## 내가 기각한 것

### 수정 1 이 `닫힘축.뜻` 을 빼면 `닫힘축` 을 읽는 Rust 경로가 깨진다
- 어디가 걸리나: `.claude/skills/round/bin/record.py:377` · `xtask/src/main.rs:4513`
- 획득: 조회 — 삭제 후 `cargo xtask check` 23/23. `요구하는_자리_시험` 의 픽스처에 애초에 `뜻` 이 없다
- 모집단: 저장소
- 유효성: 거짓
- 해악도: 미관
- 대상: 계획대상
- 왜 아니었나: 안 아프다

### 수정 1 이 `반환형식` 에서 `역사형식`·`판정인절` 을 빼면 `extract.py` 가 깨진다
- 어디가 걸리나: `.claude/skills/round/bin/extract.py:377-378`·`:410`·`:425`
- 획득: 조회 — 둘 다 `설명` 아래로 옮기고 `cargo xtask check` 23/23
- 모집단: 저장소
- 유효성: 거짓
- 해악도: 미관
- 대상: 계획대상
- 왜 아니었나: `extract.py` 는 `열별칭`·`사전부검불릿`·`역사불릿별칭`·`기계칸`·`역사기계칸`·`없음표시` 만 읽는다

### CI 의 checkout 이 얕은 클론이라 `닫은커밋` SHA 를 못 봐 전량 빨개진다
- 어디가 걸리나: `.github/workflows/ci.yml:88-91`
- 획득: 조회 — 워크플로 직독. `fetch-depth: 0` 이 이미 박혀 있고 주석이 그 사유를 적는다
- 모집단: 저장소
- 유효성: 거짓
- 해악도: 미관
- 대상: 계획대상
- 왜 아니었나: 이미 닫힌 문이다

### 새 최상위 `파서` 키가 설치본 시험을 깨뜨린다
- 어디가 걸리나: `crates/pal-cli/tests/round_scripts_run.rs:113-128`
- 획득: 조회 — 수정 2 적용 후 `cargo test -p pal-cli --test round_scripts_run` 15 passed
- 모집단: 자기장치
- 유효성: 거짓
- 해악도: 미관
- 대상: 계획자신
- 왜 아니었나: 안 아프다

### 수정 4 의 게이트 두 줄 편집이 게이트 표준 표 파싱을 깨뜨린다
- 어디가 걸리나: `docs/gates/rust-extractor.md:73` · `docs/gates/agent-laziness.md:118` · `xtask/src/main.rs:5175-5205`
- 획득: 조회 — 실제로 두 줄을 고치고 `cargo xtask check` 23/23, `record.py gate` 출력 동일
- 모집단: 회차기록
- 유효성: 거짓
- 해악도: 미관
- 대상: 계획자신
- 왜 아니었나: 표는 잇닿은 행만 읽고 검산 줄은 표 바로 뒤 6 줄만 본다

### 수정 2 가 `dashboard.py` 의 import 를 깨뜨린다
- 어디가 걸리나: `.claude/skills/round/bin/dashboard.py:29`
- 획득: 조회 — 수정 1+2 적용 후 계기판 시험 셋 전부 통과
- 모집단: 저장소
- 유효성: 거짓
- 해악도: 미관
- 대상: 계획자신
- 왜 아니었나: 안 아프다

### `record.py gate` 의 IndexError 가 `xtask` 에서 조용히 삼켜진다
- 어디가 걸리나: `xtask/src/main.rs:4867-4874`
- 획득: 조회 — 크래시 시 stdout 이 비어 시끄럽게 죽는다
- 모집단: 저장소
- 유효성: 거짓
- 해악도: 미관
- 대상: 계획대상
- 왜 아니었나: 위 시나리오는 부분 JSON 형태로 좁혀 남겼다

새 범주: **술어가 측정 연산자에 상대적이다** — 같은 키가 값-변이에는 빨갛고 키-삭제에는 초록이라, 「소비되는가」라는 판정이 판정자가 고른 변이 연산자에 좌우된다. 계획은 연산자를 사전 등록하지 않았다.
