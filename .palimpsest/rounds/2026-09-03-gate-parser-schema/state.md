# 상태 — `--schema` 파서 설명의 지위

## 지금 단계

실행 루프 — 소유자가 2026-09-03 세션 요청으로 `intent.md` 초안을 승인했고 의도가 잠겼다.
사전부검 → RED 관측 → 변이 판정 순으로 간다.

## 착수 기준

- 브랜치: `round/94-gate-parser-schema`
- 기준 커밋: `6668bfda4bbdd2517ee841382f9295613c32d29e` (`origin/main`)
- 이슈: #94, `hskim-ecoletree`에게 할당됨, OPEN
- 프론티어: `./scripts/frontier.sh` 재실행에서 #94가 착수 가능 목록에 있음
- 작업 트리: 회차 교대 문서 작성 전 깨끗했음

## 확인한 재고

- `origin/main` 이력에 `8af2240 feat(#94)`가 이미 있다.
- 그 커밋은 `record.py --schema`의 `조건파서`와 `게이트파서`를 `설명.정본아님` 아래로
  옮겨 사람이 읽는 비정본 요약으로 격하했다.
- `gh issue view 94 --comments`의 소유자 댓글은 #94 범위를 `게이트파서`뿐 아니라
  `조건파서`까지 확장한다.
- 그래프는 `xtask/src/main.rs`의 `check_ledger_pair` 좌표를 찾았으나 Python의
  `게이트파서` 설명은 심볼로 찾지 못했다. cross-file-resolution 능력도 아직 없다.

## 다음 실행 순서

1. ~~승인 확인과 잠금~~ — 끝났다. `intent.md` 머리가 승인 원문을 진다.
2. 사전부검을 정해 둔 상한 안에서 실행하고 원 반환문과 발견 레코드를 남긴다.
3. `8af2240`의 부모 또는 격리 변이에서 원 결함의 RED를 관측한다.
4. 현재 구현에서 두 비정본 설명과 소비되는 정본 키를 각각 독립 변이해 A1~B3을 판정한다.
5. 반증된 조건만 최소 수정하고, C1~D2의 검증·효과·독립 검토·결박·CI·이슈 종료까지 마친다.

## 실패한 접근

- 새 회차를 시작했다는 이유만으로 기존 `feat(#94)` 구현을 무시하고 다시 구현하려 했다.
  현재 브랜치에는 그 커밋이 이미 있으므로 검증이 먼저다.
- 승인받지 않은 의도 초안을 잠긴 의도라고 부르지 않는다.

## 능력 부재

- 그래프가 Python 설명 사전에서 Rust 검사로 이어지는 소비 관계를 답하지 못했다.
  다음 세션은 이 한계를 조회 결과와 코드 판독으로 분리해 기록한다.
