# 합(合) — 판 `p1-binary-nul` · 라운드 R1 · 회차 `2026-09-13-first-release-elsewhere`

> **낸 자리:** 합(合) — 별도 서브에이전트(Claude Opus 5). 초안을 쓴 자도, 반론을 쓴 자도 아니다. 읽기 전용이라 저장소 파일은 하나도 안 바꿨다.
> **받은 것:** 초안 `R/dialectic/p1-thesis.md` · 반론표 `R/dialectic/p1-antithesis.md` · 규약 발췌 `R/dialectic/p1-brief-synthesis.md`. 저장소와 스크래치는 초안·반론이 인용한 좌표를 확인할 때만 열었다.
> **안 받은 것 · 안 연 것:** 설계문 `R/dialectic/p1-design.md` · 대화 기록 · 메인의 입장(초안 안 요약 말고) · 종료 판단 · `R/state.md` · `R/findings.jsonl` · `R/premortem/` · `R/conditions-audit/` · `R/effect/` · `e3-*`. `R/oracle/` 을 `ls` 할 때 `B4-timing.*` 같은 이름이 목록에 찍혔지만 열지 않았다.
> **내가 돌린 것:** `sed -n` 으로 좌표 확인. 연 것은 `crates/pal-extract/src/classify.rs` · `crates/pal-cli/src/ledger.rs:105-215` · `crates/pal-store/src/cache.rs:55-70` · `crates/pal-extract/src/lib.rs:60-205` · `scripts/f04-verify.py` · `scripts/f03-3-verify.py:40-115` · `crates/pal-cli/src/touch.rs:520-535` · `R/intent.md` 의 인용 줄 · `docs/gates/F03-1-identity.md:128-152`, `:244-249` · `docs/gates/F03.md:203-207` · `.claude/skills/round/SKILL.md` 의 인용 절과 `:440-450`, `:852-862` · `R/oracle/A5-ditto.txt` · `R/oracle/p1-M2/`. 명령은 `grep -rn 'ditto.symbols.tsv' …` · `git show a0010fe` · `ls <스크래치>/ditto-effect/.palimpsest/` · `gh issue list --state open --search NUL|binary`(출력 없음).
> **옮겨 적은 자:** 메인. 합(合) 에이전트는 읽기 전용이라 본문을 반환문으로만 냈다 — ```markdown 블록을 스크립트로 뽑아 한 글자도 안 바꾸고 옮겼다.

## 판정

**수정** — 갈래는 초안대로 **확대**다. 다만 초안의 근거 하나(⑴-2 의 금지역 연결)와 대안표 첫 행의 규약 적용은 고쳐야 한다. 그리고 회귀표에 빠진 **캐시 무효화 축**을 실행 조건으로 넣어야 한다. 이 칸은 게이트가 아니다 — 아래 「소유자에게」가 게이트다.

### 물음에 대한 답 — 안 고치면 다음 회차가 짐을 지나

**진다.** 근거는 셋이다.

1. `A5` ⑷ 는 이 회차 목표 2 의 **등록된 결정론적 측정**이다. 이 조건은 「빠진 것 0」을 요구하고 「손으로 분류해 빼는 몫은 없다」(`R/intent.md:335`)고 적는다.
   - 모집단 밖 몫은 이름 붙은 셋(71 · 216 · 10)뿐이다. 59 는 모집단 **안**에 있다(`R/oracle/A5-ditto.txt:61-64`).
   - 반증으로 닫으면 다음 회차는 원인·자리·고친 형태가 이미 재어진 결함을 넘겨받는다. 목표 2 의 TS 몫을 닫으려면 그 회차가 같은 일을 다시 해야 한다.
   - 이것은 「하면 좋아진다」가 아니다. 의도가 스스로 등록한 자다(발췌 「②의 기본값」).
2. 고친 형태가 이미 재어져 있다.
   - `textsrc` 변형이 ⑶ 불일치 0, ⑵ 저장소 안 0, ⑷ 2915 중 빠진 것 0 을 냈다(`R/oracle/p1-M2/textsrc.txt` 의 `## A5 ⑷` 절 — 직접 열어 확인).
   - 그러니 「검증 가능」 요건은 선다. 남는 것은 회귀 요건이다. 반론 1·2·6 이 그 자리를 짚었고 전부 받았다.
3. 옛 처분 「범위를 늘리지 않는다」(`docs/gates/F03-1-identity.md:150-151`)는 파일 간 엣지가 없던 때 내려졌다(`R/intent.md:33` 의 `0/11010`). 그리고 그 전제의 일부가 틀렸다(반론 5 기각 사유 참조).
   - 이 회차가 엣지를 세우면서 같은 규칙이 **이 회차 산출물의 결함**이 됐다.
   - 발췌의 「착수 때 못 본 것이 나왔고 원 의도 충족에 필요하다」에 맞는다.

⚠ 메인의 셋째 근거(효과 장면 `E*` 산출에 다섯 파일이 0 번 나온다)는 사실이다. 초안과 반론이 둘 다 확인했다. 그러나 효과 장면은 표본이고 `A5` 는 전수다. 표본이 결함을 안 밟았다고 해서 전수 측정의 반증이 「원 의도의 완결에 필요하지 않다」가 되지는 않는다.

## 채택한 반론

| # | 반론 | 왜 채택했나 | 해악도 |
|---|---|---|---|
| 1 | 분류 결과는 캐시에 실리는데 초안은 `EXTRACTOR_REV` 승급을 안 적었다. 데워진 캐시는 옛 `binary{nul_byte}` 를 돌려주고, ⑵ 초록은 새 복제본에서만 잰다 | 좌표를 열어 확인했다. `crates/pal-cli/src/ledger.rs:170-176` 은 캐시 적중 시 `FileOutcome` 을 그대로 쓰고 `:212-213` 은 분류 결과를 `put` 한다. 키 성분은 blob · `version`(= `GRAMMAR_REV` + `EXTRACTOR_REV`, `crates/pal-extract/src/lib.rs:200-204`) · 경로 · 선언 언어 · 능력 축이라, 규칙 변경은 키에 안 실린다. 기본 캐시 자리는 `crates/pal-cli/src/ledger.rs:111` 이다. `R/oracle/p1-M2/README.md` 는 「복제본은 매번 새로 떴다」고 적었으니 M2 는 데워진 가지를 재지 않았다. 데워진 실물도 있다: `ls <스크래치>/ditto-effect/.palimpsest/` 가 `cache` 를 낸다 | 거짓신호 |
| 2 | 승급하면 `scripts/f04-verify.py:188` 의 리터럴이 같이 움직여야 하고, 안 움직이면 비-0 으로 끝난다. 1층 캐시도 다섯 언어 전부 무효화된다. 초안 ⑶ 에 둘 다 없다 | 리터럴 `'pub const EXTRACTOR_REV: &str = "f09-import-sites";'` 가 `scripts/f04-verify.py:188` 에 그대로 있다. `crates/pal-extract/src/lib.rs:161-163` 은 「찾을 것이 없으면 어긋남을 적고 비-0 으로 끝난다」고 적는다. 전례도 있다: `git show a0010fe` 는 이 스크립트가 낡은 자 때문에 비-0 을 낸 것을 한 회차가 따로 고친 기록이다. 규약 정의로 실패는 「검사·CI 가 빨개진다」(`.claude/skills/round/SKILL.md:859`)이고 이것이 그 형태다. ⚠ 둘째 몫(전량 무효화 비용)은 실패가 아니다 — `crates/pal-extract/src/lib.rs:62-64`, `:69-70` 이 「과잉 무효화는 느릴 뿐 틀리지 않는다 · 일회성」이라 적는다. 해악도는 리터럴 몫이 정한다 | 실패 |
| 3 | ⑴-2 가 금지역을 잘못 읽었다. 금지역은 「틀린 파일을 잡으면 거짓 수」(정밀도)이지 빠진 호출자가 아니다. 화면의 「하한」은 참으로 남는다 | `R/intent.md:274-275` 원문이 「파일 간 해소가 **틀린 파일을 잡으면**」이다. `A7`(`R/intent.md:337`)은 이름 붙은 두 사실만 요구하고 목록의 완전성은 요구하지 않는다. `crates/pal-cli/src/touch.rs:531` 의 문장도 「하한」이라 빠짐이 있어도 거짓이 아니다. ⑴-2 에 남는 것은 「세지 않는 자리 목록이 `binary` 파일 몫을 안 적는다」는 거짓신호뿐이다 | 거짓신호 |
| 4 | 대안표 첫 행이 「반증 = 재는 의도의 양이 준다 = 축소 → 승격」으로 모는데, 그 자(`SKILL.md:329`)는 **완수 조건을 고칠 때**의 자다. 반증은 조건의 글자를 안 바꾼다 | 발췌 원문이 「완수 조건을 고칠 때도 같은 자로 잰다」이고, 초안 ⑷ 도 같은 자로 「글자를 안 바꾸니 정정이 아니다」라고 쟀다. 한 자를 두 갈래에 다르게 댄 것은 사실이다. `SKILL.md:111`, `:116-118` 은 반증을 판정이 난 상태로 정의한다. ⚠ 채택 범위를 적는다: 이 반론이 무너뜨리는 것은 **「반증 = 완화」라는 규약 적용**뿐이다. 반증+이슈 갈래가 **축소(깎는다)** 인지는 「원 의도 완결에 필요한가」에 달렸고, 이 판정은 그것을 위 「물음에 대한 답」 1 로 따로 쟀다 | 거짓신호 |
| 6 | 골든이 움직이면 「`ditto.symbols.tsv` (4,578) 안 움직인다」가 초안이 이름 댄 두 곳 말고도 박혀 있다 | `grep -rn 'ditto.symbols.tsv' --exclude-dir=.palimpsest --exclude-dir=target .` 을 돌렸다. `corpus/criteria.toml` 에 4033 말고도 4593 · 5006 · 5842 · 8139 · 9064 · 9481 · 10053 · 10816 · 11242 가 있다. `docs/gates/` 에는 F04 · F05 · F06 · F09 · F10 · F10-5 · F10-6 · F11-touch · F12 · G50 · `rust-scope-references.md` 가, 코드에는 `crates/pal-extract/src/ts_scopes.rs:5` 가 걸린다. 대부분 닫힌 게이트의 **기록**이라 그 시점에 참이고 고치면 증거 위조다. 그래서 깨지는 것은 없고 뒤에서 옮겨 쓰는 사람이 잘못 읽는 몫만 남는다 | 미관 |

## 기각한 반론

| # | 반론 | 왜 기각했나 (근거 필수) |
|---|---|---|
| 5 | 옛 처분의 근거 「대장은 거짓말하지 않는다 — `binary{nul_byte}` 로 적힌다」가 엣지가 생긴 뒤에도 그대로라 반증 갈래를 방어한다. 초안이 그것을 안 다뤘다 | 그 근거는 원문에서 이미 스스로 무너져 있다. ① 같은 문서가 바로 앞 문장에서 그 분류가 옳다는 받침으로 「git 도 이 파일들을 binary 로 본다」(`docs/gates/F03-1-identity.md:142-143`)를 댔다. 그런데 반론 자신이 `git diff --no-index --numstat` 으로 다섯 중 둘(`memory-query.ts` `637 0` · `mode-doctor.ts` `327 0`)에서 틀렸음을 확인했다(반론 「초안이 서는 자리」). ② 같은 절이 「`binary{nul_byte}` 라는 이름은 사용자에게 *이미지 파일* 로 읽힌다」(`:146-148`), 빚 표가 「실코드를 이미지처럼 읽히게 한다」(`:247`, `docs/gates/F03.md:205`)고 적는다. 적힌 것이 사람을 잘못 믿게 한다고 원문이 인정한 것이다. ③ 이 회차의 산출면은 대장이 아니라 `touch` 의 호출자 수다. 그 단서(`crates/pal-cli/src/touch.rs:527-532`)는 `binary` 몫을 말하지 않는다. 그러니 「보지 않음이 산출에 남는다」는 `touch` 화면에서는 성립하지 않는다. `A5` 산출이 59 를 늘어놓는다는 것(`R/oracle/A5-ditto.txt:65-123`)은 대조 스크립트의 산출이지 제품 화면이 아니다 |

### 해악도 분포 — 살아남은 반론

| 금지역 | 실패 | 거짓신호 | 미관 | 근거 없는 기각 |
|---|---|---|---|---|
| 0 | 1 | 3 | 1 | 0 |

### 갈래와 처리 범주

| 항목 | 값 | 발췌의 자 |
|---|---|---|
| 고른 갈래 | **확대** | — |
| 처리 범주 | **확대** | 「착수 때 못 본 것이 나왔고 원 의도 충족에 필요하다 → 편입해서 이 회차에서 닫는다」. 「착수 때 못 본 것」: 엣지로 번진 효과는 `0/11010` 시점에 원리상 안 보였다(반론이 스스로 물린 넷째 항목과 같다). 「필요하다」: `A5` ⑷ 는 등록된 조건이고 59 는 모집단 안에 있다. **정정이 아니다** — `A5` ⑷ 의 글자를 안 바꾼다. **`물음 분기` 아님** — 모집단 정의를 안 건드린다 |

## 초안을 어떻게 고치나

**근거 절**

1. ⑴-2 에서 금지역(`R/intent.md:274-275`) 연결을 뺀다. 남기는 것은 「세지 않는 자리 목록에 `binary` 몫이 없다 — 거짓신호」와 장면 문장(`R/intent.md:49`)뿐이다. 중심 근거는 ⑴-3(등록 조건 `A5` ⑷ · 모집단 안 59 · `F1` 이 그 수를 `#135` 에 싣는다)으로 옮긴다. (반론 3)
2. 대안표 첫 행의 빠지는 근거에서 「반증으로 닫으면 재는 의도의 양이 준다 → 축소 → 승격」 절을 지운다. 그 자리에 「`A5` ⑷ 가 등록한 의도를 이 회차가 안 채우고 다음 회차로 넘긴다 — 원 의도 완결에 필요하므로 안 늘릴 사유가 서지 않는다(발췌 「②의 기본값」)」로 적는다. (반론 4)

**실행 조건 — 확대가 닫히려면 전부 서야 한다**

| # | 조건 | 받치는 반론 |
|---|---|---|
| X1 | **`EXTRACTOR_REV` 를 올린다.** `crates/pal-extract/src/lib.rs` 의 승급 기록 절을 앞선 여덟과 같은 형태로 더한다: 무엇이 바뀌었나 · 안 올리면 무엇이 되살아나나 · `body_digest` 가 움직이나 | 1 |
| X2 | **같은 커밋에서 `scripts/f04-verify.py:188` 의 리터럴을 새 값으로 옮긴다.** 그 스크립트의 ③(변이)을 돌려 `어긋남` 0 · 종료 코드 0 을 산출로 남긴다 | 2 |
| X3 | **데워진 캐시 가지를 잰다.** 규칙 변경 **전** 바이너리로 `.palimpsest/cache` 를 데운 `aded7ce` 복제본에 새 바이너리를 대고 `R/oracle/ts-oracle.mjs --unresolved --recall` 을 돌린다. ⑷ 빠진 것 0 이어야 한다. **음성 대조**: 같은 입력에서 X1 을 뺀 빌드는 ⑷ 가 0 이 아니어야 한다. 0 이면 이 측정이 캐시 가지를 안 탄 것이다. 2층 인덱스(`index.redb`)가 옛 산출을 들고 있는지도 이 측정이 드러내야 한다 | 1 |
| X4 | **새 복제본에서 초안 ⑵ 의 초록 셋을 동시에** 세운다: ⑷ 2915 중 빠진 것 0 · ⑶ 불일치 0 · ⑵ 저장소 안 0. **커밋된 바이너리**로 잰다(M2 는 커밋 안 된 빌드였다) | 초안 ⑵ |
| X5 | **골든 회귀를 목록으로.** `scripts/f03-3-verify.py` 재축복 산출이 `ditto.symbols.tsv` 에서 새로 78 · 사라짐 0 · 좌표 이동 0 · 요약 이동 0 이어야 한다. `portal-backend.ledger.json` · `portal-backend.symbols.tsv` · `s0-reference-vector.tsv` 는 움직인 것 0 · `R/oracle/A6-rust.sh` 1319 그대로 · `cargo xtask check` · `cargo xtask test` 초록. 초안이 `미측정` 이라 적은 M6 · `A6` · 시험 칸이 여기서 실측이 된다 | 2 · 초안 ⑶ |
| X6 | **낡는 문장.** 살아 있는 코드 주석 `crates/pal-extract/src/classify.rs:10` · `crates/pal-core/src/ledger.rs:148`(「git 이 쓰는 것과 같은 판정」) · `crates/pal-extract/src/lib.rs:74`(「4,578 줄」)을 규칙과 함께 고친다. `lib.rs:74` 를 고칠 때 더한 줄에 `ditto` 가 들어가면 `G1`(`R/intent.md:373`)이 빨개진다 — 골든을 경로나 이름 없이 가리킨다. 닫힌 게이트 기록과 `corpus/criteria.toml` 의 옛 합격선은 **고치지 않는다**. 대신 이 회차 게이트 기록에 「`ditto.symbols.tsv` 가 이 회차에서 78 행 움직여 새 행 수가 N 이다 — 이후 음성 대조는 N 을 댄다」를 한 줄 적는다 | 6 |
| X7 | **픽스처는 결합 금지를 지킨다.** 1급 확장자 + 문자열 안 NUL 은 `Parsed`, 기존 `.png` + NUL 단언(`classify.rs:295`)은 `Binary` 그대로. ditto 이름·경로·`~/` 별칭은 쓰지 않는다(`R/intent.md:313`) | 초안 ⑵ |

## 소유자에게

**올린다.** 살아남은 반론 가운데 반론 2 가 **실패**(`scripts/f04-verify.py` 가 빨개진다)에 닿는다. 게이트(`.claude/skills/round/SKILL.md:447`)에 따라 메인이 혼자 정할 수 없다. 실패는 X2 로 피할 수 있고 절차도 앞선 승급들에 적혀 있다. 그래도 이 확대는 세 가지를 함께 움직인다: 1층 캐시 전량 무효화, 열다섯 곳 넘는 기록이 「안 움직인다」고 박아 둔 골든의 재축복, 다섯 언어 공통 분류 규칙.

**소유자가 정할 것:** `A5` ⑷ 를 이 회차에서 닫기 위해 분류 규칙을 고칠지 정한다. 이 변경은 `EXTRACTOR_REV` 승급(캐시 전량 무효화 · `f04-verify.py` 리터럴 이동)과 ditto 기호 골든 78 행 재축복을 동반한다. 받으면 X1-X7 을 조건으로 진행하고, 안 받으면 `A5` ⑷ 를 반증으로 적고 축소로 처리한다.

## 내가 못 정한 것

| 무엇 | 왜 못 정했나 | 무엇이 있어야 정하나 |
|---|---|---|
| 1급 확장자를 달았지만 소스가 아닌 파일(예: `.ts` MPEG 전송 스트림)이 새 규칙에서 무슨 상태로 적히나 | 초안 틀림 조건 4 가 적었고 두 코퍼스에 표본이 없다. 반론도 표본을 못 댔다. `textsrc` 형태면 `binary` 가 아니라 추출기로 가서 `Partial`/`GrammarDefeated` 로 적힐 수 있다. 그것이 대장이 거짓말하는 것인지는 규칙의 형태에 달렸고, 형태는 이 판이 안 고른다 | 규칙 형태를 고를 때, 그 파일이 어떤 상태를 받아야 하는지를 시험 하나로 못박은 산출. 또는 「잰 적 없음」을 게이트 기록에 이름으로 남긴 줄 |
| `EXTRACTOR_REV` 승급이 이 회차에서 **이미 판정이 난 조건** 가운데 데워진 캐시에 기댄 측정을 낡게 하나 | `R/oracle/` 목록에 시간 측정 산출이 있었지만 인용 좌표 밖이라 열지 않았다. 캐시 적중을 전제한 수가 있으면 승급 뒤 재측정이 필요할 수 있다 | 이 회차 조건 목록에서 캐시 상태에 기대는 조건을 훑은 결과. 메인이 X1 착수 전에 확인한다 |
| 반론 1 의 인용 `R/oracle/ts-oracle.mjs:50` | 그 줄은 `pal ledger` 가 아니라 `pal query graph.dump` 호출이다. 쿼리가 복제본의 기본 캐시·인덱스를 거치는 경로를 끝까지 읽지 않았다. 반론 1 의 채택은 캐시 키 구성(`ledger.rs:170-176`, `:212-213`)만으로 선다. X3 이 그 경로를 실측으로 드러낸다 | X3 의 산출 |
