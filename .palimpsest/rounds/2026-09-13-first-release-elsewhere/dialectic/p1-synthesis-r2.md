# 합(合) R2 — 판 `p1-binary-nul` · 회차 `2026-09-13-first-release-elsewhere`

> **낸 자리:** 합(合), 라운드 R2(합2). 별도 서브에이전트(Claude Opus 5)다. 초안(합1)도 R2 반론표도 내가 쓰지 않았다. 읽기 전용이라 저장소 파일은 하나도 안 바꿨다.
> **받은 것:** 초안 `R/dialectic/p1-synthesis.md`(합1) · 반론표 `R/dialectic/p1-antithesis-r2.md` · 규약 발췌 `R/dialectic/p1-brief-synthesis.md`.
> **안 받은 것 · 안 연 것:** R1 정 `p1-thesis.md` · R1 반론표 `p1-antithesis.md` · 설계문 `p1-design.md` · 종료 판단 `p1-referee.md` · 대화 기록 · `R/state.md` · `R/findings.jsonl` · `R/premortem/` · `R/conditions-audit/` · `R/effect/` · `e3-*`. `ls` 로 목록만 본 파일(`R/oracle/` 의 다른 산출, 스크래치의 다른 파일)은 열지 않았다. 예외는 아래 「내가 돌린 것」에 적은 파일뿐이다.
> **내가 돌린 것:**
> - **좌표 확인에 쓴 명령:** `sed -n` · `grep -n` · `cat`.
> - **저장소에서 연 것:** `scripts/f04-verify.py:180-200`, `:212-252`, `:600-658` · `docs/gates/F04.md:25-40` · `crates/pal-extract/src/classify.rs:95-200`, `:280-300` · `crates/pal-extract/src/lib.rs` 의 `EXTRACTOR_REV` 줄 · `crates/pal-cli/src/query.rs:90-115` · `crates/pal-cli/src/attach.rs:95-132` · `crates/pal-store/src/projection.rs:330-370` · `crates/pal-cli/src/export.rs:110-125` · `crates/pal-cli/build.rs:1-45`, `:85-105` · `scripts/f03-3-verify.py:138-170` · `docs/gates/F03.md:200-208` · `docs/gates/F03-1-identity.md:240-250` · `.claude/skills/round/SKILL.md:320-335`, `:440-450`, `:852-862` · `R/intent.md` 의 인용 줄(`:25-55`, `:270-380`).
> - **`R/oracle/` 에서 연 것:** `ts-oracle.mjs:40-60`, `:175-195` · `A5-ditto.txt:40-50` · `A6-rust.txt` · `p1-M2/`(`m2.sh` · `README.md` · `textsrc.txt`).
> - **스크래치에서 연 것:** `baseline-20260913-1935.md:55-85` · `xtask-check-ms25b.txt` · `m2-run.txt` · `a5-shape.txt` · `p1-thesis-sym.jsonl` 머리. cargo 체크아웃은 `tree-sitter-typescript-…/75b3874/common/scanner.h:1-60` 을 열었다.
> - **★ 실행한 측정 하나:** 반론 2 를 재려고 스크래치 워크트리 `wt-m2` 에 남은 빌드(`m2.sh` 가 마지막에 빌드한 `textsrc` 변형, `pal 0.0.0+1d7b3b8a9dcb`)로 스크래치 복제본 `ditto-m2-textsrc` 에서 `pal ledger --json --cache-dir <스크래치>/synth-r2-p1/cache` 를 돌렸다. 새로 쓴 것은 스크래치 안 새 디렉터리 `synth-r2-p1/`(캐시 · `ledger.json` · `ledger.err`)뿐이고 복제본의 `.palimpsest/` 는 캐시 자리를 따로 줘서 안 건드렸다. 같은 복제본의 `.ts` 에서 NUL 이 든 줄도 `python3` 로 뽑았다.
> **옮겨 적는 자:** 메인(발주문이 그렇게 적었다).
> **옮겨 적은 자:** 메인. 합(合) R2 에이전트는 읽기 전용이라 본문을 반환문으로만 냈다 — ```markdown 블록을 스크립트로 뽑아 한 글자도 안 바꾸고 옮겼다. 그 에이전트가 스크래치 `synth-r2-p1/` 에 남긴 대장은 `oracle/p1-M2/synth-r2-ledger.json.gz` 로 옮겼다.

## 판정

**수정** — 갈래는 합1 대로 **확대**다. R2 반론 일곱 중 여섯을 받는다. 그중 **실패 둘**(X2 의 종료 코드 · X7 의 `Parsed`)은 실행 조건이 적힌 대로는 **서지 않는** 자리라서 조건 표를 다시 적는다. 이 칸은 게이트가 아니다 — 아래 「소유자에게」가 게이트다.

### 물음에 대한 답 — 안 고치면 다음 회차가 짐을 지나

**진다.** 합1 의 근거 1(등록된 결정론적 조건 `A5` ⑷ · 「손으로 분류해 빼는 몫은 없다」 `R/intent.md:335` · 59 는 모집단 안)은 R2 반론 어느 것도 무너뜨리지 않았다. 반론 7 이 가장 가까이 갔지만 스스로 「확대를 무너뜨리지 못한다」고 적었다(기각 사유 참조).

이번에 새로 잰 사실이 「검증 가능」 요건을 더 좁게 세운다.
- `textsrc` 변형에서 다섯 파일은 **`binary` 가 아니라 `partial`** 로 적힌다(측정 결과 아래 반론 2 칸).
- 다섯 다 `grade: l2` 이고 복구 자리가 1~2 다.
- 대장 전체는 `partial 5 · binary 6 · parsed 491` 이다.
- 그래프가 실리니 ⑷ 0 과 맞는다. 이것은 「일부는 읽었다」는 `Partial` 의 뜻(`classify.rs:176-181` 주석)과 맞는 **참인 적음**이다.
- 따라서 고친 뒤 대장이 새로 거짓말하는 자리는 이 표본에서 안 보인다. 다만 그 형태가 합1 의 X7 이 적은 `Parsed` 와 다르다.

## 채택한 반론

| # | 반론 | 왜 채택했나 | 해악도 |
|---|---|---|---|
| 1 | X2 의 「종료 코드 0」은 이 기계에서 원리상 안 선다. f04 는 ③ 만 도는 옵션이 없고 실패를 모아 1 을 돌려준다. 착수 기준선에서 이미 ⑨ 로 1 이다 | **좌표 확인:** `scripts/f04-verify.py:619-620` 의 옵션은 `--skip-mutation` 하나뿐이다. `:652-655` 는 `if 실패: … return 1` 이다. `:216` 은 ③ 을 건너뛰면 그것도 실패로 넣는다. 그러니 ③ 만 떼어 종료 코드로 잴 길이 없다. **기준선 확인:** 스크래치 `baseline-20260913-1935.md:62` 에 「f04 · 1 · … ⑨ ditto 고정비 뺀 8.8배 (선 10)」이 있고, `:80` 은 이것을 기존 빚으로 분류한다. 닫힌 게이트 `docs/gates/F04.md:32` 에도 ⑨ 가 「⚠ 어긋났다」로 남아 있다. **등급 유지:** 합1 이 X2 를 「확대가 닫히려면 전부 서야 한다」에 넣었으니, 적힌 대로는 확대가 닫힐 길이 없다. 비-0 을 「환경 탓」으로 넘기면 규약이 교착으로 정한 실패(`SKILL.md:858`)를 손으로 푸는 꼴이다. 등급을 내릴 근거가 없다. **재는 방법:** ③ 의 산출 줄은 `ok ③ {이름} — 적중 0` / `FAIL ③ …`(`:238`, `:245`, `:247`, 형식 `:79-85`)이라 줄로 잴 수 있다 | 실패 |
| 2 | X7 의 「1급 확장자 + 문자열 안 NUL 은 `Parsed`」는 잰 적 없는 전제이고, 문법 원문이 반대를 가리킨다 | **직접 쟀다 — 반론이 `추정` 으로 올린 것이 참이다.** `textsrc` 빌드의 대장에서 다섯의 상태는 이렇다. `src/acg/boundary/codeql-edges.ts` `{"partial":{…,"recovery_sites":1}}` · `src/acg/icl/static-check.ts` `…2` · `src/acg/impact/codeql-analyzer.ts` `…1` · `src/core/memory-query.ts` `…2` · `src/core/mode-doctor.ts` `…2`. `Parsed` 는 하나도 없다. **문법 원문:** `scanner.h:28-29` 가 템플릿 조각에서 `case '\0': return false;` 다. **NUL 자리:** 넷은 템플릿 리터럴 안, `mode-doctor.ts:208,210` 은 홑따옴표 `'\x00'` 안이다. ⚠ 반론이 「스스로 물린 것」 다섯째 줄로 뺀 홑따옴표 문자열(`mode-doctor.ts`)도 실측에서 `partial` 이다. 물린 쪽이 틀렸고 반론의 원래 범위가 맞았다. **안 고치면:** X7 을 적힌 대로 시험으로 쓰면 `cargo xtask test`(`A6` ⑴)가 빨개진다. 초록을 맞추려고 NUL 을 문자열 밖으로 옮기면 ditto 의 형태를 안 재는 시험이 된다. 규약 정의로 실패다 | 실패 |
| 3 | X3 의 「2층 인덱스(`index.redb`)가 옛 산출을 들고 있는지도 이 측정이 드러내야 한다」는 이 측정으로는 원리상 못 드러난다 | **왜 옛 2층이 안 남나:** `R/oracle/ts-oracle.mjs:50` 이 먼저 `pal query graph.dump` 를 부른다. `crates/pal-cli/src/query.rs:105-109` 은 기본이 `How::Stitching` 이다. `crates/pal-cli/src/attach.rs:105-117` 의 Stitching 갈래는 `built_for` 를 비교하지 않고 매번 `stitch_in` 을 부른다. `crates/pal-store/src/projection.rs:350-352` 는 무조건 `prepare_stage()` 로 무대를 비운다. 읽기 전용 `pal export`(`crates/pal-cli/src/export.rs:118`, 대조 스크립트 `:184`)는 그 **뒤**에 붙는다. **결과:** 옛 2층 내용은 재기 전에 덮이고, 그 절은 실패할 수 없다. 게이트 기록에 「2층도 쟀다」로 남으면 거짓신호다 | 거짓신호 |
| 4 | X4 의 「커밋된 바이너리로 잰다」는 산출에서 확인할 수단이 없다. 버전 도장이 작업 트리가 더러운지를 안 싣는다 | **도장이 담는 것:** `crates/pal-cli/build.rs:92-102` 는 `git rev-parse --short=12 HEAD` 만 담는다. 파일 머리 `:1-45` 에도 dirty 표식이 없다. **실물로 확인했다:** 커밋 안 된 변경을 얹은 M2 빌드의 산출이 `pal 0.0.0+1d7b3b8a9dcb`(`R/oracle/p1-M2/textsrc.txt` 머리)이고, 그 값은 워크트리 `wt-m2` 의 HEAD `1d7b3b8` 그대로다. 그러니 X4 가 M2 와 갈라 세우려는 차이가 산출에 안 남는다 | 거짓신호 |
| 5 | 처분 범주 칸의 「엣지로 번진 효과는 `0/11010` 시점에 원리상 안 보였다」는 사실이 아니다 | **기록에 있었다:** 다섯이 `binary` 이고 심볼 78 이 대장 밖이라는 것이 열린 빚으로 적혀 있었다(`docs/gates/F03.md:205`, `docs/gates/F03-1-identity.md:247`). **코드가 이미 적었다:** 「그래프가 없는 파일은 2층에 파일 노드도 성립하지 않는다」(`crates/pal-extract/src/classify.rs:101-102`). 그러니 「그 다섯은 파일 간 엣지 0」은 착수 때 유도된다. 안 보인 것은 **그 빚이 등록 조건 `A5` ⑷ 를 59 로 깬다는 수**뿐이다. **갈래는 안 뒤집힌다:** 의도를 향한 변경은 자유다(`SKILL.md:327`). 원장 근거 문장만 거짓이다 | 거짓신호 |
| 6 | X5 의 「`cargo xtask check` 초록」은 지금 p1 과 무관한 죽은 링크로 빨갛다. 순서나 제외 조건이 없다 | **죽은 링크 확인:** 스크래치 `xtask-check-ms25b.txt` 끝에 「죽은 링크 1건: `docs/gates/first-release-elsewhere.md → ../../.palimpsest/rounds/2026-09-13-first-release-elsewhere/report.md`」가 있다. 회차 시작 git 상태에서 그 게이트 문서는 아직 추적되지 않았다. **같은 원인이 X5 의 다른 검사도 빨갛게 한다:** X5 가 부르는 `scripts/f03-3-verify.py` 의 ④ 가 안에서 `cargo xtask check` 를 돌려 비-0 이면 `FAIL ④` 를 모은다(`:147-153`). 기준선 `baseline-20260913-1935.md:61` 도 f03-3 을 ④ 로 1 이라 적었다. **등급 유지:** `report.md` 를 쓴 뒤 풀리는 순서 문제이고 영구히 안 서는 조건은 아니다. 등급을 올릴 근거는 못 찾았다 | 미관 |

## 기각한 반론

| # | 반론 | 왜 기각했나 (근거 필수) |
|---|---|---|
| 7 | 버린 갈래(반증+이슈)를 받치는 가장 강한 근거 — 의도 스스로의 범위 자 「장면을 막나」 — 를 합1 이 안 댔다 | **① 범위 자는 여기에 안 걸린다.** 그 자는 `## 범위 밖` 절(`R/intent.md:282-298`)에 **이름으로 올라간 항목**을 빼는 자다. NUL 규칙은 그 목록에 없다. 반면 `A5` ⑷ 는 `## 완수 조건` 절에서 「빠진 것 0 · 손으로 분류해 빼는 몫은 없다」(`R/intent.md:335`)로 등록됐다. 발췌 ② 「이 의도가 답해야 할 물음인가」에 대한 답을 의도가 스스로 적었다. **② 합1 은 이것을 댔다.** 근거 1(`R/dialectic/p1-synthesis.md:17-20`)이 「이것은 「하면 좋아진다」가 아니다. 의도가 스스로 등록한 자다」로 범위 자 대신 등록 조건을 저울에 올렸다. 그러니 「안 댔다」는 사실이 아니다. **③ 선례도 반론 편이 아니다.** 재수출 70 건(`R/intent.md:294-295`)은 `A5` ⑷ 의 모집단 밖 몫(71)으로 정의에서 이미 빠져 있다. 59 는 모집단 **안**이다(`R/oracle/A5-ditto.txt:61-64`, 합1 인용). **④ 반론 스스로 적었다:** 「이 근거는 확대를 무너뜨리지 못한다」. 소유자가 볼 저울 무게는 아래 「소유자에게」 칸이 싣는다 |

## 초안을 어떻게 고치나

**근거 절**

1. **처분 범주 칸의 「착수 때 못 본 것」(반론 5).** 「엣지로 번진 효과는 `0/11010` 시점에 원리상 안 보였다」를 지운다.
   - 새로 적는다: 「다섯이 `binary` 라 파일 간 엣지가 0 이라는 것은 착수 때 유도됐다(`docs/gates/F03.md:205` · `classify.rs:101-102`). 착수 때 못 본 것은 그 빚이 등록 조건 `A5` ⑷ 를 **59 로 깬다는 수**다」.
   - 갈래와 범주는 그대로 **확대**다.
2. **「물음에 대한 답」 2 에 한 줄을 더한다(반론 2).** 「고친 형태(`textsrc`)에서 다섯은 `Parsed` 가 아니라 `partial`(복구 자리 1~2)로 적힌다 — 합(合) R2 실측」.

**실행 조건 — 고친 표 전체. 확대가 닫히려면 전부 서야 한다**

| # | 조건 | 받치는 반론 | R2 에서 바뀐 것 |
|---|---|---|---|
| X1 | **`EXTRACTOR_REV` 를 올린다.** `crates/pal-extract/src/lib.rs` 의 승급 기록 절(마지막 절 `:181` 의 형태)을 앞선 것들과 같은 꼴로 더한다: 무엇이 바뀌었나 · 안 올리면 무엇이 되살아나나 · `body_digest` 가 움직이나 | R1 1 | 없음 |
| X2 | **같은 커밋에서 `scripts/f04-verify.py:188` 의 리터럴을 새 값으로 옮긴다.** ⑴ **p1 코드 변경 전에** 같은 스크립트를 옵션 없이 돌린 산출을 `R/oracle/` 에 남긴다. ⑵ 변경 뒤 한 번 더 돌려 남긴다. **판정은 종료 코드가 아니라 줄로 한다:** ⒜ `ok    ③ 추출기 코드 버전 — 적중 0` 줄이 있다 ⒝ `FAIL  ③` 으로 시작하는 줄이 0 이다 ⒞ `어긋난 것` 목록의 **검사 번호 집합**이 ⑴ 과 같다(기준선에서는 ⑨ 하나). 종료 코드는 판정에 안 쓴다 — `:652-655` 가 모든 검사의 실패를 한 코드로 모으고, ⑨ 는 기존 빚이다. **음성 대조는 스크립트에 이미 있다:** 리터럴을 안 옮기면 `:238` 이 `FAIL  ③ 추출기 코드 버전 — 치환 대상이 소스에 없다` 를 낸다 | R1 2 · R2 1 | 「종료 코드 0」 → 줄 판정 셋과 착수 전 산출 대조 |
| X3 | **데워진 캐시 가지를 잰다.** 규칙 변경 **전** 바이너리로 `.palimpsest/cache` 를 데운 `aded7ce` 복제본에 새 바이너리를 대고 `R/oracle/ts-oracle.mjs --unresolved --recall` 을 돌린다. ⑷ 빠진 것이 0 이어야 한다. **음성 대조:** 같은 입력에서 X1 을 뺀 빌드는 ⑷ 가 0 이 아니어야 한다. 0 이면 이 측정이 캐시 가지를 안 탄 것이다. **2층은 이 측정이 재지 않는다** — 쓰기 붙기가 매번 무대를 비우고 다시 쓴다(`crates/pal-cli/src/attach.rs:105-117` · `crates/pal-store/src/projection.rs:350-352`). 게이트 기록에 그 문장과 좌표를 한 줄로 적는다. 「2층도 쟀다」로 적지 않는다 | R1 1 · R2 3 | 실패할 수 없는 2층 절을 뺐다. 뺀 까닭을 기록에 적는 줄로 바꿨다 |
| X4 | **새 복제본에서 초록 셋을 동시에** 세운다: ⑷ 2915 중 빠진 것 0 · ⑶ 불일치 0 · ⑵ 저장소 안 0. **커밋된 바이너리임을 산출에 싣는다:** 빌드 직전 `git status --porcelain --untracked-files=no -- crates/` 의 출력(빈 줄)과 `git rev-parse --short=12 HEAD` 를 산출 머리에 싣고, 그 SHA 가 대조 산출의 `pal 0.0.0+<sha>` 와 같다(도장이 dirty 를 안 싣는다 — `crates/pal-cli/build.rs:92-102`). **다섯 파일의 대장 상태를 싣는다:** `pal ledger --json` 에서 다섯의 상태를 뽑아 적는다. 합(合) R2 가 `textsrc` 형태에서 잰 값은 다섯 다 `partial`(복구 자리 1~2)이다. 규칙 형태가 달라 값이 다르면 그 값을 적고 X7 을 같은 값에 맞춘다 | 초안 ⑵ · R2 4 · R2 2 | 「커밋된 바이너리」의 확인 수단 둘과 다섯 상태 적기를 더했다 |
| X5 | **골든 회귀를 목록으로.** ⑴ `scripts/f03-3-verify.py` 재축복 산출에서 `ditto.symbols.tsv` 가 새로 78 · 사라짐 0 · 좌표 이동 0 · 요약 이동 0 이다. ⑵ `portal-backend.ledger.json` · `portal-backend.symbols.tsv` · `s0-reference-vector.tsv` 는 움직인 것 0 이다. ⑶ `R/oracle/A6-rust.sh` 의 1319 가 그대로다. ⑷ `cargo xtask test` 가 초록이다. ⑸ `cargo xtask check` 는 `FAIL` 줄의 **검사 이름 집합**이 p1 코드 변경 전 산출과 같다(지금은 `죽은 링크 부재` 하나 · 원인 `docs/gates/first-release-elsewhere.md → …/report.md`). `report.md` 를 쓴 뒤라면 초록이다. `f03-3-verify.py` 는 ④ 가 같은 원인으로 `FAIL` 을 낼 수 있으니(`:147-153`) 종료 코드가 아니라 ③ 줄로 판정한다. 초안이 `미측정` 이라 적은 M6 · `A6` · 시험 칸이 여기서 실측이 된다 | R1 2 · 초안 ⑶ · R2 6 | 「`xtask check` 초록」 → 착수 전 FAIL 집합과 같음 / `report.md` 뒤 초록. f03-3 을 줄로 판정한다 |
| X6 | **낡는 문장.** 살아 있는 코드 주석 `crates/pal-extract/src/classify.rs:10` · `crates/pal-core/src/ledger.rs:148`(「git 이 쓰는 것과 같은 판정」) · `crates/pal-extract/src/lib.rs:74`(「4,578 줄」)을 규칙과 함께 고친다. `lib.rs:74` 에 더한 줄에 `ditto` 가 들어가면 `G1`(`R/intent.md:373`, 범위 `crates/` · `xtask/`)이 빨개지니 골든을 경로나 이름 없이 가리킨다. 닫힌 게이트 기록과 `corpus/criteria.toml` 의 옛 합격선은 **고치지 않는다.** 대신 이 회차 게이트 기록에 「`ditto.symbols.tsv` 가 이 회차에서 78 행 움직여 새 행 수가 N 이다 — 이후 음성 대조는 N 을 댄다」를 한 줄 적는다(`docs/gates/` 는 `G1` 범위 밖이다 — 반(反) R2 가 물린 셋째 줄) | R1 6 | 없음 |
| X7 | **픽스처는 잰 형태를 단언하고 결합 금지를 지킨다.** 1급 확장자(`.ts`) 픽스처 둘을 만든다: ⒜ **템플릿 리터럴 안 NUL** ⒝ **홑따옴표 문자열 안 NUL**. 둘 다 이것을 단언한다: 상태가 `Binary` 가 **아니고** `Partial`(복구 자리 ≥ 1)이며, 그래프가 실려 픽스처가 선언한 이름이 심볼로 나온다. **RED:** 착수 규칙에서 둘 다 `Binary` 로 실패한다 — 관측을 남긴다. 기존 `.png` + NUL 단언(`classify.rs:298-300`)은 `Binary` 그대로다. ditto 이름·경로·`~/` 별칭은 쓰지 않는다(`R/intent.md:313`). ⚠ X4 에 실린 다섯 상태가 `partial` 이 아니면 이 단언을 그 값에 맞춘다. **NUL 을 문자열 밖으로 옮겨 초록을 맞추지 않는다** | 초안 ⑵ · R2 2 | `Parsed` → `Partial` 과 심볼 실림(실측). 픽스처를 문자열 형태 둘로 나눴고 RED 를 더했다 |

## 소유자에게

**올린다.** 채택한 R2 반론 가운데 반론 1 · 2 가 **실패**다. 반론 1 은 X2 가 적힌 대로는 원리상 안 선다는 것이고, 반론 2 는 X7 을 적힌 대로 시험으로 쓰면 `cargo xtask test` 가 빨개진다는 것이다(합(合) R2 실측). 게이트(`.claude/skills/round/SKILL.md:447`)에 따라 메인이 혼자 못 정한다.

두 실패는 위 고친 표(X2 · X7)로 서는 꼴이 됐다. 그리고 이번 실측이 소유자가 볼 무게 하나를 바꿨다: **다섯은 `parsed` 가 아니라 `partial` 로 적힌다.** 반대편 무게(반론 7 이 댄 「장면을 막나」와 다섯 언어 공통 규칙 · 1층 캐시 전량 · 골든 78 행 재축복)도 함께 올린다.

| 물음 | 소유자가 정할 것 |
|---|---|
| `A5` ⑷ 를 이 회차에서 닫기 위해 NUL 분류 규칙을 고치는가 | **받으면:** 고친 X1–X7 을 조건으로 진행한다. 결과는 다섯 `.ts` 가 `binary` → `partial` · `EXTRACTOR_REV` 승급(1층 캐시 전량 무효화 · `f04-verify.py` 리터럴 이동) · ditto 기호 골든 78 행 재축복이다. **안 받으면:** `A5` ⑷ 를 반증으로 적고, 「원 의도 완결에 필요한 것을 안 한다」는 축소로 처리한다 |

## 살아남은 반론의 해악도 분포

| 금지역 | 실패 | 거짓신호 | 미관 | 근거 없는 기각 |
|---|---|---|---|---|
| 0 | 2 | 3 | 1 | 0 |

**합1 에서 살아남았고 이번 수정으로 풀린 것**

- **R1 반론 2**(`f04-verify.py:188` 리터럴 · 실패): 합1 은 X2 로 막았다고 적었지만 X2 가 스스로 서지 않았다. 고친 X2 의 줄 판정으로 **이번에 서는 꼴이 됐다.**
- **합1 「내가 못 정한 것」 셋째 줄**(`ts-oracle.mjs:50` 의 쿼리가 기본 캐시를 지나나): `crates/pal-cli/src/query.rs:97` 이 `ledger::compute(a.repo, a.rev, a.cache_dir.clone())` 를 부른다 — 반(反) R2 「초안이 서는 자리」, 내가 열어 확인. **풀렸다.**
- **R1 반론 1**(캐시 무효화 · 거짓신호)은 X1 · X3 가 계속 진다. 이번 수정은 X3 에서 실패할 수 없는 절 하나만 뺐고, 캐시 가지 측정과 음성 대조는 그대로다.

## 갈래와 처리 범주

| 항목 | 값 | 발췌의 자 |
|---|---|---|
| 고른 갈래 | **확대** | — |
| 처리 범주 | **확대** | 「착수 때 못 본 것이 나왔고 원 의도 충족에 필요하다 → 편입해서 이 회차에서 닫는다」. **「못 본 것」**(반론 5 로 고침): 빚 자체는 기록에 있었다. 못 본 것은 그것이 등록 조건 `A5` ⑷ 를 59 로 깬다는 수다. **「필요하다」:** `A5` ⑷ 는 등록된 결정론적 조건이고 59 는 모집단 안이다. 「하면 좋아진다」가 아니라 「안 하면 다음 회차가 같은 측정을 다시 진다」(발췌 「②의 기본값」). **정정이 아니다** — `A5` ⑷ 의 글자를 안 바꾼다. **`물음 분기` 아님** — 모집단 정의를 안 건드린다. X2 · X5 를 종료 코드에서 줄 판정으로 바꾼 것도 **완화가 아니다** — 재는 대상(리터럴 이동 · 골든)은 그대로 다 재고, 착수 전 FAIL 집합과 같다는 대조를 더했다 |

## 내가 못 정한 것

| 무엇 | 왜 못 정했나 | 무엇이 있어야 정하나 |
|---|---|---|
| 합1 이 올린 같은 물음에 소유자가 이미 답했나 | 발주문이 `R/state.md` 와 종료 판단 `p1-referee.md` 를 안 줬다. 커밋 제목 「종료 판단 R1 — 실패 1 이 남아 R2 를 돈다」만 보인다 | **답이 있으면:** 그 답 위에서 고친 X 표와 `partial` 사실만 덧붙여 보인다. **없으면:** 위 칸 하나를 그대로 올린다 |
| 1급 확장자를 달았지만 소스가 아닌 파일(예: `.ts` MPEG 전송 스트림)이 새 규칙에서 무슨 상태로 적히나 | 합1 「못 정한 것」 첫째 줄 그대로다. 이번 대장에도 그런 표본이 없다(`binary 6` 은 1급 확장자가 아니었다 — 확장자는 안 뽑아 봤다) | 규칙 형태를 고를 때 그 파일이 받을 상태를 시험 하나로 못박은 산출. 또는 「잰 적 없음」을 게이트 기록에 이름으로 남긴 줄 |
| 승급 뒤 읽기 전용 표면(`pal export` · `--read-only` 질의)이 한 번도 다시 안 쓰인 옛 2층을 읽을 때 무엇을 보이나 | 스냅샷 이름(`attach.rs:98`)에 추출기 rev 가 들어가는지 안 쟀다. 이 판의 물음(⑷)과 떨어진 자리라 반론으로 세우지 않았다 | `crates/pal-core` 의 스냅샷 `Display` 를 읽은 결과. 또는 데운 옛 인덱스에 새 바이너리로 `pal export` 만 먼저 부른 산출 |
| 규칙 변경이 종료 기준선(스물넷)의 ditto 상태 수를 세는 대조에 새 어긋남을 만드나 | 대장 상태 분포가 `binary 11 → 6`(착수 대장의 수는 안 쟀다) · `partial +5` 로 움직인다. 그 수를 합격선에 박은 스크립트가 있는지 안 훑었다 | 종료 기준선을 착수 기준선과 줄 단위로 댄 결과. 새 어긋남이 있으면 X5 의 목록에 더한다 |
