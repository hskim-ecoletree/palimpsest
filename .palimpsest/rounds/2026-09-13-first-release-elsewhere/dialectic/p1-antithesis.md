# 반(反) — 판 `p1-binary-nul` · 라운드 R1 · 회차 `2026-09-13-first-release-elsewhere`

> **낸 자리:** 반(反) — Claude Opus 5. 읽기 전용이다. 저장소 파일은 하나도 안 바꿨다.
> **받은 것:** 초안 `R/dialectic/p1-thesis.md` · 발췌 `R/dialectic/p1-brief-opponent.md` · 초안이 인용한 좌표를 확인하는 데 한한 저장소 읽기와 스크래치 읽기.
> **안 받은 것 · 안 연 것:** 설계문 `R/dialectic/p1-design.md` · 대화 기록 · 메인의 사고 과정 · `R/state.md` · `R/findings.jsonl` · `R/premortem/` · `R/conditions-audit/` · `R/effect/` · `e3-*` 산출물. `ls` 로 `dialectic/` 을 볼 때 `e3-design.md` 라는 이름이 목록에 찍혔지만 열지 않았다. 프롬프트에 대화 기록은 섞여 오지 않았다. 메인의 입장은 초안 본문 안에 요약된 것만 봤다.
> **내가 돌린 것:** `git diff 1d7b3b8..HEAD -- crates xtask` · 스크래치 복제본 `ditto-a5` 에서 다섯 파일에 `git diff --no-index --numstat` 과 NUL 오프셋 계산 · 스크래치 `p1-thesis-sym*.jsonl` 의 파일별 행 수 세기 · `A5-ditto.txt:65-123` 을 스크래치 `anti-59.txt` 로 떠서 들여오는 쪽과 대상 쪽 세기 · `grep`. 스크래치에 `anti-59.txt` 하나를 썼다. 그 밖에는 읽기만 했다.
> **옮겨 적은 자:** 메인. 반(反) 에이전트는 읽기 전용이라 본문을 반환문으로만 냈다 — ```markdown 블록을 스크립트로 뽑아 한 글자도 안 바꾸고 옮겼다.

## 초안이 서는 자리

- **⑴-1 의 59 = 33 + 26 은 사실이다.** `R/oracle/A5-ditto.txt:65-123` 은 59 줄이다. 들여오는 쪽이 다섯 파일인 줄이 33, 대상이 다섯 파일인 줄이 26 이고, 양쪽 다 다섯 파일인 줄은 0 이다.
- **⑴-4 의 git 대조는 사실이다.** 스크래치 `ditto-a5`(`aded7ce7`)에서 `--numstat` 결과는 이렇다.
  - `codeql-edges.ts` · `static-check.ts` · `codeql-analyzer.ts` 셋은 `- -` 다.
  - `memory-query.ts` 는 `637 0`, `mode-doctor.ts` 는 `327 0` 이다.
  - 첫 NUL 오프셋은 2974 · 1239 · 7337 · 21905 · 9429 다.
  - 그래서 `docs/gates/F03-1-identity.md:142-143` 의 「git 도 이 파일들을 binary 로 본다」는 다섯 중 둘에서 틀렸다.
- **⑶ M5 행의 수는 사실이다.**
  - 스크래치 `p1-thesis-sym.jsonl` 은 4,656 행이다. 다섯 파일 몫이 5 · 3 · 7 · 43 · 20 이다.
  - `p1-thesis-sym-head.jsonl` 은 4,578 행이고, 다섯 파일 몫은 0 이다.
- **「M2 바탕에서 HEAD 까지 차이는 별칭·주석뿐」은 사실이다.** `git diff 1d7b3b8..HEAD -- crates xtask` 는 네 파일 · 9 줄이다. 시험 고정물의 `~/*` → `@app/*` 교체와 `budget.rs:160` 주석 한 줄뿐이다.
- **`wt-m2` 바이너리가 `textsrc` 라는 추론은 정황과 맞는다.**
  - `wt-m2/target/release/pal` 의 수정 시각은 23:06 이다. `m2-git8000.txt` 는 23:06, `m2-textsrc.txt` 는 23:07 이다.
  - `m2.sh:8` 은 `git8000` 다음에 `textsrc` 를 빌드한다.
  - `wt-m2` 의 HEAD 는 `1d7b3b8` 이다.
- **메인의 셋째 근거에 대한 초안의 확인은 사실이다.** `R/oracle/E*.txt` 에서 다섯 파일 이름을 `grep -l` 하면 일치가 없다(rc=1).
- **`classify.rs:132-134` · `:10` · `ledger.rs:148` · `lib.rs:68` · `:74` · `touch.rs:527-532` 의 인용은 원문 그대로다.**

## 반론

| # | 반론 | 좌표 | 유효성 | 해악도 |
|---|---|---|---|---|
| 1 | **분류 결과는 캐시에 실린다. 그런데 초안의 ⑶ 회귀표에도, ⑵ 초록 조건에도 `EXTRACTOR_REV` 를 올린다는 말이 없다.** 규칙만 고치면 캐시가 데워진 복제본은 옛 `binary{nul_byte}` 를 계속 돌려받는다. 캐시가 빈 복제본만 `parsed` 를 받으니, 같은 커밋이 캐시 상태에 따라 다른 답을 낸다. ⑵ 의 초록은 **「새 `aded7ce` 복제본」** 에서만 잰다. 그래서 캐시가 데워진 가지는 안 재어지고, 초록이 떠도 그 가지가 고쳐졌다는 뜻이 아니다. 이 회차 안에도 그 가지의 실물이 있다(효과 장면용 복제본에 캐시가 이미 있다) | `crates/pal-cli/src/ledger.rs:170-176`(키로 조회하고, 적중하면 `FileOutcome` 을 그대로 쓴다) · `:203`(분류) · `:212-213`(분류 결과를 `cache.put`) · `crates/pal-store/src/cache.rs:60-66`(「캐시가 담는 것은 `FileOutcome` 이고 그것은 **분류 결과**다」, 키는 blob · 추출기 버전 · 경로 · 선언 언어 · 능력 축) · `crates/pal-extract/src/lib.rs:97-98`(안 올리면 「같은 커밋이 캐시 상태에 따라 다른 답」) · `R/oracle/ts-oracle.mjs:50`(`--cache-dir` 없이 부르므로 복제본의 `.palimpsest/cache` 를 쓴다. 기본값은 `crates/pal-cli/src/ledger.rs:111`) · `ls <스크래치>/ditto-effect/.palimpsest/cache` 가 있다 | 참 | 거짓신호 |
| 2 | **반론 1 을 막으려고 `EXTRACTOR_REV` 를 올리면, 초안의 ⑶ 에 없는 회귀가 둘 생긴다.** ① `scripts/f04-verify.py:188` 이 그 상수 줄을 리터럴로 찾는다. 이 줄을 같이 안 고치면 스크립트가 「어긋남」을 적고 비-0 으로 끝난다. ② 1층 캐시가 다섯 언어 전부에서 무효화된다. 초안 ⑶ 은 「`A6` 안 움직인다 · `cargo xtask test` 초록」만 적었고, 이 비용과 스크립트 자리는 표에 없다 | `crates/pal-extract/src/lib.rs:161-163`(「`scripts/f04-verify.py:188` 의 리터럴도 같이 움직인다 … 비-0 으로 끝난다」) · `scripts/f04-verify.py:188` · `crates/pal-extract/src/lib.rs:200-204` | 참 | 실패 |
| 3 | **⑴-2 가 금지역을 잘못 읽었다.** 초안이 받침으로 인용한 금지역 문장은 「파일 간 해소가 **틀린 파일을 잡으면** `호출자 N` 이 거짓 수」다. 이것은 정밀도, 곧 틀린 대상을 지목한다. 빠진 호출자는 가리키지 않는다. 화면은 이미 「호출자 수는 **하한**」이라고 말하고, 빠진 호출자가 있어도 하한이라는 문장은 참으로 남는다. `A7` 이 요구하는 것도 이름 붙은 두 사실(하한 · 파일 최상위와 문자열 자리)뿐이다. 목록이 완전해야 한다는 요구는 없다. 그러니 ⑴-2 는 금지역에 닿지 않는다. 남는 것은 **목록이 불완전하다는 미관이나 거짓신호** 정도다. 초안은 틀림 조건 6 에서 「과장일 수 있다」고만 했고, 인용한 금지역 문장이 오히려 반대를 말한다는 점은 안 다뤘다 | `R/intent.md:274-275` · `crates/pal-cli/src/touch.rs:531` · `R/intent.md:337` · 초안 ⑴-2 가 인용한 좌표가 같은 두 줄(`R/intent.md:274-275`) | 참 | 거짓신호 |
| 4 | **(버린 갈래를 받치는 가장 강한 근거) 「반증 + 이슈」를 「축소 → 승격」으로 모는 대안표 첫 행은 규약의 범주를 잘못 적용했다.** 규약이 축소·완화를 가르는 자는 「**완수 조건을 고칠 때** 고친 뒤 그 검사가 재는 의도의 양」이다. 반증을 적는 것은 조건의 글자를 안 바꾼다. 초안도 ⑷ 에서 같은 자(`:329`)로 「글자를 안 바꾸니 정정이 아니다」라고 쟀다. 그렇다면 반증 쪽에도 같은 이유로 **완화가 아니다**가 나와야 한다. 상자 규약은 반증을 판정이 난 상태로 정의하고, 「반증은 실패가 아니다」라고 적는다. 초안은 이 문장들과의 긴장을 「합이 잰다」로 넘겼다. **자를 두 갈래에 다르게 댄 것**은 짚지 않았다. 이 반론이 서면 대안표 첫 행의 「빠지는 근거」 중 규약 경로(승격) 절이 무너진다. 남는 것은 ⑴-3(다음 회차가 반증을 재해석 없이 받나) 하나다 | `.claude/skills/round/SKILL.md:329` · `:111`(「상자 켜짐 — **판정이 났다** — 통과 · 반증 · 대조불가」) · `:116-118` · `:774` · 초안 대안표 첫 행과 ⑷ 「정교가 아닌 까닭」 | 참 | 거짓신호 |
| 5 | **(버린 갈래 쪽 근거 둘째) 이 빚은 한 번 「범위를 늘리지 않는다」로 명시 처분됐고, 그 뒤 여러 게이트에서 「열림」인 채 넘어왔다.** 초안의 ⑴-1 은 「이번에 엣지 59 로 번졌다」를 새 사실로 세운다. 그런데 옛 처분의 근거는 「대장이 `binary{nul_byte}` 로 적으므로 거짓말하지 않는다」였고, 그 근거는 엣지가 생긴 뒤에도 그대로다. `A5` 산출도 59 를 이름으로 늘어놓아 숨기지 않는다. 초안은 이 옛 처분 **문장**을 메인의 입장 확인(1·2 확인)으로만 인용했다. 그 **근거**(대장이 안 봤다는 사실이 산출에 남는다)가 반증 갈래의 「사실이 아닌 것을 사실로」 방어로 그대로 쓰일 수 있다는 점은 다루지 않았다 | `docs/gates/F03-1-identity.md:143-144`(「대장은 거짓말하지 않는다 — `binary{nul_byte}` 로 적히므로 "보지 않음" 이 산출에 남는다」) · `:150-151` · `docs/gates/F03.md:205` · `R/oracle/A5-ditto.txt:61`, `:65-123` | 참 | 거짓신호 |
| 6 | **골든을 움직이면 낡는 자리가 초안 ⑶ 이 이름 댄 두 곳(`criteria.toml:4033` · `lib.rs:74`) 말고도 더 있다.** 「`ditto.symbols.tsv` (4,578) 안 움직인다」가 뒤 기능들의 합격선과 게이트 기록에 음성 대조로 반복 박혀 있다. 가장 최근 것은 「이 회차가 등록한 금지역」이라는 절이다. 골든이 4,656 이 되면 이 문장들을 그대로 옮겨 쓰는 뒤 게이트가 「4,578 · 안 움직인다」를 대며 빨개지거나, 사람이 옛 수를 지금 값으로 잘못 읽는다 | `corpus/criteria.toml:4593` · `:11242` · `crates/pal-extract/src/ts_scopes.rs:5`(「골든 `ditto.symbols.tsv` 가 그 불변을 잰다」) · `docs/gates/F11-touch.md:335` · `docs/gates/F12.md:323` · 명령 `grep -rn 'ditto.symbols.tsv' --exclude-dir=.palimpsest --exclude-dir=target .` | 참 | 미관 |

## 내가 스스로 물린 것

- **「`.ts` 확장자의 MPEG 전송 스트림이 TS 추출기로 간다」.** 초안의 틀림 조건 4 가 이미 적었다. 새롭지 않아 뺐다.
- **「기록을 다시 축복하려고 `scripts/f03-3-verify.py` 를 원본 `~/dev/projects/ditto` 에 대면 `G2`(원본 불가침)가 깨진다」.** 확인해 보니 그 스크립트는 `--cache-dir` 을 넘긴다(`scripts/f03-3-verify.py:65`). 기본 캐시 자리 `repo/.palimpsest/cache`(`crates/pal-cli/src/ledger.rs:111`)는 안 쓴다. `G2` 박제 항목(`R/oracle/G2-snapshot.sh`)이 원본 안에서 움직인다는 좌표를 대지 못했다. `pal ledger` 가 원본에 `manifest.toml` 을 읽기 말고 쓰는지는 안 쟀다(`ledger.rs:566` 은 경로 조립뿐이다).
- **「`EXTRACTOR_REV` 를 올리면 `Coord.extractor` 가 움직여 이 저장소 결박이나 골든이 흔들린다」.** `grep -c 'f09-import-sites'` 결과가 `corpus/golden/*` 셋과 `.palimpsest/intent/bindings.jsonl` 모두 0 이다. 움직인다는 좌표를 대지 못해 반론 2 에서 뺐다.
- **「59 는 착수 때 볼 수 있었으니 확대의 전제 "착수 때 못 본 것"이 거짓이다」.** 빚 자체는 공개돼 있었다(`docs/gates/F03.md:205`). 그러나 엣지로 번진 효과는 TS 해소가 `0/11010` 이던 착수 시점(`R/intent.md:33`)에는 원리상 볼 수 없었다. 초안의 ⑴-1 이 그 구별을 이미 한다. 물렸다.
- **「⑵ 의 초록에 음성 대조가 없다」.** 현재 산출의 59(`R/oracle/A5-ditto.txt:61`)가 이미 RED 다. 물렸다.
- **「`linguist-language` 선언으로 1급이 된 진짜 바이너리가 `textsrc` 규칙에서 텍스트로 샌다」.** 두 코퍼스에서 실물 표본을 못 댔다. `portal-backend` 쪽은 초안이 이미 「`linguist-language` 없음」으로 다뤘다.
- **`gh issue` 로 열린 NUL 빚 이슈가 정말 없는지.** 안 돌렸다. 초안의 「못 찾았다」를 반박할 좌표가 없다.
