# 인터뷰 R2 — 2026-09-13 · 상한 2 의 마지막

> 열린 범주: **종료**(증인 · 판정자) · **재고**(열린 이슈와 어떻게 만나나) · 「이것이 전부인가」

## 물음 앞에 붙인 재료

- ditto 의 열린 이슈에서 변경 과제 후보를 찾았다. **ditto#70 「Codex 표면 제거」** 가 장면 조건 넷을 한 과제에서 태운다:
  `src/core/hosts/codex.ts` 의 `mcpServersFromToml` 에 ADR-0003 후보 · `HostAdapter`·`resolveRepoRoot` 에 ADR-0016 후보
  (ADR-0016 은 ditto 가 `ADR-20260722-claude-code-only-host` 로 폐기했다) · `loadPermissions`·`HostAdapter` 는 동명 2.
- ditto TS 재수출 70 건 — 저장소 안 임포트 1,701 에 비해 작아 재수출 축(#135 의 반증 `A5` · #139)은 장면을 안 막는다.

## 물음과 답

| # | 물음 | 선택지(추천 먼저) | 답 |
|---|---|---|---|
| 1 | 효과를 재는 실제 변경 과제는? (복제본에서만 · 원본·원격에 안 보낸다) | ① ditto#70 의 한 조각 — codex.ts 제거 · ② ditto#71 — work done --changed crash · ③ ditto#94 — work start --issue 플래그 무시 | **① ditto#70 의 한 조각** |
| 2 | 「화면을 본 뒤 그 변경에서 무엇이 달라졌나」는 누가 판정하나? | ① 정반합 판 하나 · ② 소유자가 직접 읽는다 | **① 정반합 판 하나** |
| 3 | 후보 이슈 넷 — #135 는 TS 몫을 세우고 열린 채, #139·#133(Rust 전용)·#117(제품 용어)은 사유와 함께 범위 밖 | ① 그대로 · ② #135 를 이 회차에서 닫는다 | **① 그대로** |
| 4 | 이것이 전부인가? 잠글 것: TS 모듈 해소 · touch 의 승인 대기 후보와 안내 정정 · touch 의 동명 후보 고르기 · 사용자 화면의 회차·이슈·기능 코드 제거 · ditto#70 조각으로 효과 확인 | ① 없다 — 잠그러 가라 · ② 있다 | **① 없다** |
