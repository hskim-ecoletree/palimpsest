# M2 — 분류 규칙 두 변형에서 `A5` 대조를 다시 돌린 산출

> 판 `p1-binary-nul` 설계문의 M2 칸(「규칙만 바꾸면 ⑷ 가 59→0 이 되고 ⑶ 은 그대로인가」)을 잰 것.
> **코드는 커밋하지 않았다** — 스크래치 워크트리에서 `crates/pal-extract/src/classify.rs` 의 ② 한 줄만 바꿔 빌드했다(`m2.sh`).
> 복제본은 매번 `aded7ce` 로 새로 떴다. 대조 스크립트는 `oracle/ts-oracle.mjs --unresolved --recall` 그대로다.

| 변형 | ② 의 조건 | 정밀도(잰 엣지 · 불일치) | ⑵ outside_repo 중 TS 가 저장소 안으로 푸는 것 | ⑷ 모집단 · 빠진 것 | 산출 |
|---|---|---|---|---|---|
| 지금(`A5-ditto.txt`) | `source.contains(&0)` | 5003 · 0 | 0 | 2915 · **59** | `../A5-ditto.txt` |
| `git8000` | 앞 8,000 바이트에 NUL | 5054 · 0 | 0 | 2915 · **22** | `git8000.txt` |
| `textsrc` | NUL 이 있고 **1급 언어로 인식되지 않을 때만** | 5082 · 0 | 0 | 2915 · **0** | `textsrc.txt` |

⚠ `m2.sh` 가 찍은 `symbols <경로>` 줄은 **경로를 잘못 줬다**(다섯 파일은 `src/acg/…` 아래다) — 그 수는 인용하지 않는다. `m2-run` 로그는 싣지 않았다.
⚠ 이 측정은 M6(portal-backend 골든의 진짜 바이너리 8 건)과 M5(ditto 기호 골든)와 `A6` 을 **재지 않았다.**
