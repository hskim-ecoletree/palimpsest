# D2 — 옛 단언을 지운 커밋에 새 단언이 함께 있나

- 명령: `git log -p 6ee9eb3..HEAD -- crates/pal-cli/tests/cross_file_references.rs`
- 이 파일을 고친 커밋: 9db40b7 

## 9db40b7 — feat(cli): 사용자 화면에서 이 저장소의 작업 기록 어휘를 뺀다 — 회차 · 조건 ID · 이슈 번호 · 기능 코드 [㈃]
```diff
+    //   `D2`). 앞 판은 문구 「범위 밖」을 찾았고, 그 말이 이 저장소의 회차 어휘라 화면에서
-        화면.contains("범위 밖"),
+        화면.contains("안 하기로 정한 자리입니다"),
+        화면.contains("원리상 풀 수 없는 자리입니다"),
```
- 옛 「범위 밖」 지운 줄 1 · 새 두 문자열 더한 줄 2

## 음성 대조 — 한 커밋이 아닌 경우를 가르나
- 이 파일을 고친 커밋이 여럿이면 각 커밋이 따로 셈을 가진다 — 옛 것을 지운 커밋의 새 셈이 0 이면 반증이다

## 판정: 통과 — 9db40b7 한 커밋
