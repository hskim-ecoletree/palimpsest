# G1 — ditto 결합 금지 (최종 커밋까지 다시 잼)

- 범위: `git diff 6ee9eb3..264d967 -- crates/ xtask/` 의 더한 줄
- 앞 측정은 `6ee9eb3..6145849`(R2 가 잡음) → `6ee9eb3..c9e31a3`(R3 가 잡음) 까지였다. 첫 측정은 `oracle/G1-first-red.txt`
- 걸린 줄 **0**

## 음성 대조 — 같은 명령에 `+ // ditto` 한 줄을 심는다
- 걸린 줄 **1** (본 셈 + 1 이어야 한다)

## 판정: 통과
