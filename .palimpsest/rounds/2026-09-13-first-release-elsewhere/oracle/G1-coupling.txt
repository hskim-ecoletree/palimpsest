# G1 — ditto 결합 금지

- 범위: `git diff 6ee9eb3 -- crates/ xtask/` (작업 트리 포함) 의 더한 줄
- 첫 측정은 `oracle/G1-first-red.txt` — 시험 고정물 둘이 `~/*` 별칭을, 시험 문자열 하나와 주석 하나가 `ditto` 를 실었다
- 걸린 줄 **0**

## 음성 대조 — 같은 명령에 `+ // ditto` 한 줄을 심는다
- 걸린 줄 **1** (본 셈 + 1 이어야 한다)

## 판정: 통과
