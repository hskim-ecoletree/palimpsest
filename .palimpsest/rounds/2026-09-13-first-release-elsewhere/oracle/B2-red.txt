# B2 RED — 착수 바이너리의 `pal narrative` 가 안내한 명령은 인자 파서가 거부한다

- 바이너리: `pal 0.0.0+6ee9eb30f2dc` (워크트리 `6ee9eb3`)
- 픽스처: `crates/pal-cli/tests/pending_and_pick.rs` 의 `대기_저장소` 와 같은 파일 셋

## `pal narrative` 의 안내 줄
```
15:  `asserted` 가 됩니다 — `pal narrative approve <개체>`.
```

## 안내를 그대로 돌린다 — 자리표시 `<개체>` 에 실제 개체를 넣어도 하위 명령 `approve` 가 없다
```
$ pal narrative approve 01M2DEXNH614HQGPPE1DYMY9SV
error: unexpected argument 'approve' found

Usage: pal narrative [OPTIONS]

For more information, try '--help'.
rc=2
```

## 그 화면에 개체 이름이 한 줄도 없다 — 사람이 승인할 대상을 찾을 길이 `--json` 뿐이다
```
0
```
