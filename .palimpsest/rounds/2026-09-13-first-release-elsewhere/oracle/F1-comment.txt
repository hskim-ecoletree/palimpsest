# F1 — #135 에 TS 몫이 섰다는 코멘트

- 코멘트: https://github.com/hskim-ecoletree/palimpsest/issues/135#issuecomment-5657189371 (독립 리뷰 R1 발견 4 로 수를 고쳐 편집했다)
- 확인 명령: `gh api repos/hskim-ecoletree/palimpsest/issues/comments/5657189371` (2026-09-14T00:12:44Z)

```
작성 2026-09-13T23:57:36Z · 편집 2026-09-14T00:12:43Z
## TypeScript 몫이 섰다 — 회차 `2026-09-13-first-release-elsewhere`

남의 TS 저장소(ditto 복제본 `aded7ce`)에서 파일 간 해소를 TypeScript 컴파일러와 댔다.

| 잰 것 | 착수(`6ee9eb3`) | 종료(이 회차 최종 바이너리) |
|---|---|---|
| `cross-file-import` 선 것 / 짝 | `0/11010` (전부 「저장소 밖」) | `7199/11099` |
| 선 엣지의 대상 파일 — TypeScript 와 불일치 | 잴 엣지가 없다 | **0** (잰 엣지 5082) |
| `outside_repo` 로 남은 것 중 TypeScript 가 저장소 안으로 푸는 것 · 지정자가 `./` · `../` · 별칭 꼴인 것 | — | **0** · **0** |
| 재현율 — TypeScript 가 푸는 저장소 안 이름 임포트 중 `pal` 엣지가 빠진 것 | 잴 엣지가 없다 | **0** (모집단 2915) |

- 재현율의 중간값: TS 해소가 선 뒤 · NUL 분류 수정 전 규칙에서 59 가 빠졌다(NUL 바이트가 든 TS 파일 다섯이 `binary`). 분류 규칙 변경(`ba0a983`)으로 0 이 됐고 짝이 11010 → 11099 로 는 것은 그 다섯이 추출되기 때문이다.
- ⚠ **정정(2026-09-14)** — 이 코멘트의 첫 판은 종료 칸에 효과 실행 때(NUL 수정 전) 바이너리의 `7116/11010` 을 적고 재현율 59 를 착수 칸에 적었다. 두 시점을 섞은 것이라 고쳤다.

- 게이트: `docs/gates/first-release-elsewhere.md` (`A5`)
- 산출: `.palimpsest/rounds/2026-09-13-first-release-elsewhere/oracle/A5-ditto.txt` · `A5-2-shape.txt` · `p1-X/`

**이 이슈는 열어 둔다** — 남은 반증은 앞 회차 `2026-09-08-cross-file-references` 의 재수출 경유 조건 둘(Rust 축)이다. TS 의 재수출 경유 이름은 따라가지 않되 「재수출을 지나는 이름」 까닭으로 적는다.

```

## 판정
통과 — 코멘트가 최종 바이너리의 A5 수(7199/11099 · 불일치 0 · 재현율 빠진 것 0)와 게이트 경로를 싣는다
