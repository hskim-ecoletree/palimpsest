설명문이 한 언어의 이름 크레이트 을 싣는다:
  ※ 「호출자·피호출자」는 **참조 엣지**입니다 — 호출뿐 아니라 타입 참조·구조체 리터럴·경로 머리도 셉니다
  ※ **`x.foo()` 는 아직 안 셉니다** — 멤버 해소(`member-resolution`)는 타입 추론이 필요합니다. 능력이 없는 것이 아니라 **안 하기로 정한 자리입니다**
  ※ **호출자 수는 하한입니다** — 파일 최상위의 참조와 문자열로 찾는 자리는 세지 않습니다
  ※ 파일 간 해소 — ⓐ `cross-file-import` 1/2 · ⓑ `path-resolution` 0/0 (선 것/짝)
  ※ ⓐ 가 못 선 까닭 — no_target_file 1
  ※ 못 선 까닭의 성격 — 안 하기로 정한 자리와 원리상 풀 수 없는 자리를 가릅니다
 저장소 밖(표준 라이브러리 · 런타임 모듈)입니다 — **원리상 풀 수 없는 자리입니다**
 패키지 이름입니다 — 설치된 패키지인지 워크스페이스인지 가리지 않았습니다. **안 하기로 정한 자리입니다**
 재수출을 지나는 이름입니다 — 원 정의까지 따라가지 않습니다. **안 하기로 정한 자리입니다**
 크레이트 뿌리의 재수출을 지나는 이름입니다 — 원 정의까지 따라가지 않습니다. **안 하기로 정한 자리입니다**
 이 파일의 모듈 해소 모드(`classic`)를 맞추지 않습니다 — **안 하기로 정한 자리입니다**
 설정 파일(`tsconfig` 의 `extends` 등)을 끝까지 못 읽어 별칭인지 모릅니다
 대상 파일에 그 이름의 선언이 없습니다 — 열거형 변형 · 연관 상수처럼 심볼로 안 서는 이름이 여기 듭니다
 모듈 경로를 저장소의 파일로 못 폈습니다
 후보가 여럿입니다 — 하나를 고르면 조용한 오답이라 **고르지 않습니다**
note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace


failures:
    화면의_설명문은_한_언어의_이름을_싣지_않는다

test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 18 filtered out; finished in 0.36s

error: test failed, to rerun pass `-p pal-cli --test ts_cross_file`
