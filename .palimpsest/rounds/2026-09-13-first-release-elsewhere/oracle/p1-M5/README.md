# 판 `p1-binary-nul` 의 정(正) 이 스크래치에 남긴 대장 산출 둘

> 정(正) 에이전트가 `pal ledger --symbols` 로 떠서 스크래치에 둔 파일이다. 초안 `dialectic/p1-thesis.md` ⑶ 표 M5 행의 근거라 **옮겨 보존한다** — 내용은 안 바꾸고 `gzip -9` 로만 줄였다.

| 파일 | 무엇 |
|---|---|
| `p1-thesis-sym.jsonl.gz` | 스크래치 `wt-m2/target/release/pal`(정(正)의 추론으로 `textsrc` 변형) · 복제본 `ditto-m2-textsrc` |
| `p1-thesis-sym-head.jsonl.gz` | 저장소 `target/release/pal`(`0.0.0+db6c366564e3`) · 같은 복제본 |
