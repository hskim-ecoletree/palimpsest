# 변경 — 복제본의 커밋 둘

- touch 때 HEAD `5ec95eb2f6c6575a366a3e7bab4a3c0b09b8c971`
- A `898a479c8f55af5ecc41796b6672f25c61a814e7` · 부모 `5ec95eb2f6c6575a366a3e7bab4a3c0b09b8c971` · touch 때 HEAD 와 같다: 예
- B `d0eff0d0275b62301dd014436a7f2fc8962ed032` · 부모 `898a479c8f55af5ecc41796b6672f25c61a814e7`

## A — 파일 삭제만
```
898a479c codex.ts 제거 (A) — ditto#70 의 한 조각

 src/core/hosts/codex.ts | 239 ------------------------------------------------
 1 file changed, 239 deletions(-)
```

## B — 깨진 곳 고치기
```
d0eff0d0 codex.ts 제거의 뒷정리 (B) — 등록·참조를 걷고 Codex 갈래는 시끄럽게 실패 · 주인 잃은 parseToml 제거 · ADR-0003 상태 줄

 .ditto/knowledge/adr/ADR-0003-toml-parser.md |  2 +-
 src/cli/commands/setup.ts                    | 40 ++++------------------------
 src/core/hosts/index.ts                      |  4 +--
 src/core/hosts/shared.ts                     |  5 ----
 src/core/setup.ts                            | 14 +++-------
 5 files changed, 10 insertions(+), 55 deletions(-)
```

## tsc --noEmit

| 시점 | 오류 |
|---|--:|
| 전(`5ec95eb2f6c6575a366a3e7bab4a3c0b09b8c971`) | 0 |
| A 뒤 | 4 |
| B 뒤 | 0 |

## B 의 diff
```diff
diff --git a/.ditto/knowledge/adr/ADR-0003-toml-parser.md b/.ditto/knowledge/adr/ADR-0003-toml-parser.md
index 6e3604cb..468e78fc 100644
--- a/.ditto/knowledge/adr/ADR-0003-toml-parser.md
+++ b/.ditto/knowledge/adr/ADR-0003-toml-parser.md
@@ -1,6 +1,6 @@
 # ADR-0003: Codex 설정용 TOML 파서
 
-- 상태: accepted
+- 상태: accepted — ⚠ 2026-09-13 사용 지점(`src/core/hosts/codex.ts` 의 `loadPermissions` · `mcpServersFromToml`)이 Codex 표면 제거(#70 · ADR-20260722-claude-code-only-host)로 사라졌고 `parseToml` wrapper 도 함께 지웠다. `smol-toml` 의존 제거는 잠금 파일을 움직여 따로 한다.
 - 결정 일자: 2026-05-24
 - 결정자: hskim, claude (claude-opus-4-7)
 - 관련: ADR-0001 (런타임 스택), wi_v02doctor (자체 파서 도입), wi_v02harden (실제 환경 정확도)
diff --git a/src/cli/commands/setup.ts b/src/cli/commands/setup.ts
index 3a5dd02c..53294a2f 100644
--- a/src/cli/commands/setup.ts
+++ b/src/cli/commands/setup.ts
@@ -5,7 +5,6 @@ import { type AgentVariant, writeAgentVariants } from '~/core/agent-variants';
 import { seedGithubConfigIfAbsent } from '~/core/ditto-config';
 import { resolveRepoRootForCreate } from '~/core/fs';
 import { claudeCodeHostAdapter } from '~/core/hosts/claude-code';
-import { codexHostAdapter } from '~/core/hosts/codex';
 import { fileExists } from '~/core/hosts/shared';
 import { detectLspLanguages } from '~/core/provision/lsp-detect';
 import {
@@ -248,35 +247,6 @@ async function discoverClaudeAgents(
   return out;
 }
 
-/**
- * 프로젝트 `.codex/agents`에서 사용자 커스텀 codex agent를 발견한다(codex 호스트, agent only).
- * claude와 달리 codex는 ditto 자체 agent를 프로젝트 `.codex/agents`에 직접 복사하므로
- * (installCodexAgents), provenance 헤더(`ditto agent-projection`)가 있는 번들 agent는 제외해
- * 사용자가 직접 쓴 agent만 surface한다. description은 TOML `description = "..."` 한 줄을
- * best-effort로 읽는다(heuristic은 name만으로도 동작) — 멀티라인/이스케이프는 미파싱, 빈 문자열.
- */
-export async function discoverCodexAgents(
-  projectRoot: string,
-): Promise<{ name: string; description: string }[]> {
-  const inventory = await codexHostAdapter.loadSurfaceInventory(projectRoot);
-  const agents = inventory.localSurfaces.filter((s) => s.kind === 'agent');
-  const out: { name: string; description: string }[] = [];
-  for (const a of agents) {
-    let text = '';
-    try {
-      text = await Bun.file(a.path).text();
-    } catch {
-      // 파일 읽기 실패는 무시 — name만으로 추천.
-    }
-    if (/ditto agent-projection/.test(text)) continue; // ditto 자체 번들 agent 제외
-    let description = '';
-    const m = /^description\s*=\s*"([^"]*)"\s*$/m.exec(text);
-    if (m) description = (m[1] ?? '').trim();
-    out.push({ name: a.id, description });
-  }
-  return out;
-}
-
 /**
  * 설치 대상 host에 맞는 프로젝트 agent를 발견한다 — claude-code는 `.claude/agents`, codex는
  * `.codex/agents`, both는 둘을 합치되 이름 충돌은 claude 우선(먼저 발견)으로 dedupe한다.
@@ -285,11 +255,11 @@ export async function discoverProjectAgents(
   projectRoot: string,
   host: SetupHost,
 ): Promise<{ name: string; description: string }[]> {
-  const claude =
-    host === 'claude-code' || host === 'both' ? await discoverClaudeAgents(projectRoot) : [];
-  const codex = host === 'codex' || host === 'both' ? await discoverCodexAgents(projectRoot) : [];
-  const seen = new Set(claude.map((a) => a.name));
-  return [...claude, ...codex.filter((a) => !seen.has(a.name))];
+  // Codex 호스트 표면은 제거됐다(ADR-20260722-claude-code-only-host · #70) — 조용히 빈 목록을 내지 않고 시끄럽게 실패한다.
+  if (host === 'codex' || host === 'both') {
+    throw new Error('codex host surface was removed (ADR-20260722-claude-code-only-host); use --host claude-code');
+  }
+  return discoverClaudeAgents(projectRoot);
 }
 
 /** 대화형 wizard 흐름: 의존을 실제 구현으로 묶어 runSetupWizard를 돌리고 요약을 출력한다. */
diff --git a/src/core/hosts/index.ts b/src/core/hosts/index.ts
index 9de1e355..4de0e173 100644
--- a/src/core/hosts/index.ts
+++ b/src/core/hosts/index.ts
@@ -1,9 +1,7 @@
 import { claudeCodeHostAdapter } from './claude-code';
-import { codexHostAdapter } from './codex';
 import { registerHostAdapter } from './types';
 
-registerHostAdapter(codexHostAdapter);
 registerHostAdapter(claudeCodeHostAdapter);
 
 export * from './types';
-export { claudeCodeHostAdapter, codexHostAdapter };
+export { claudeCodeHostAdapter };
diff --git a/src/core/hosts/shared.ts b/src/core/hosts/shared.ts
index 74a0f3f5..f3f9ab51 100644
--- a/src/core/hosts/shared.ts
+++ b/src/core/hosts/shared.ts
@@ -1,6 +1,5 @@
 import { readdir, stat } from 'node:fs/promises';
 import { join, resolve } from 'node:path';
-import { parse as parseTomlText } from 'smol-toml';
 import { parse as parseYamlText } from 'yaml';
 
 /**
@@ -29,10 +28,6 @@ export async function readJsonIfExists(path: string): Promise<unknown | null> {
   return JSON.parse(text);
 }
 
-export function parseToml(text: string): Record<string, unknown> {
-  return parseTomlText(text) as Record<string, unknown>;
-}
-
 /** YAML 텍스트를 파싱하는 wrapper(`yaml` 패키지). 사람이 손으로 쓰는 산출물용. */
 export function parseYaml(text: string): unknown {
   return parseYamlText(text);
diff --git a/src/core/setup.ts b/src/core/setup.ts
index 88c3b266..1c294dd9 100644
--- a/src/core/setup.ts
+++ b/src/core/setup.ts
@@ -4,7 +4,6 @@ import { basename, dirname, isAbsolute, join, resolve } from 'node:path';
 import type { RecipePushGate } from '~/schemas/recipe';
 import { refreshCharterRegion } from './charter-region';
 import { atomicWriteText, ensureDir } from './fs';
-import { codexHostAdapter } from './hosts/codex';
 import { fileExists, listDirectories, listFiles, readJsonIfExists } from './hosts/shared';
 import { type InitScaffoldResult, initScaffold } from './init-scaffold';
 import { applyManagedFile, unwrapManagedBlock, writeBackupOnce } from './managed-resource';
@@ -288,16 +287,9 @@ async function rewriteCodexInstalledReferences(installedPluginDir: string): Prom
   }
 }
 
-async function writeCodexSurfaceCatalog(projectRoot: string): Promise<string> {
-  const surfaceCatalogPath = join(projectRoot, '.ditto', 'local', 'surfaces.codex.json');
-  await ensureDir(join(projectRoot, '.ditto', 'local'));
-  const catalog = await generateSurfaceCatalog(
-    [codexHostAdapter],
-    projectRoot,
-    'surfaces.codex.json',
-  );
-  await atomicWriteText(surfaceCatalogPath, `${JSON.stringify(catalog, null, 2)}\n`);
-  return surfaceCatalogPath;
+async function writeCodexSurfaceCatalog(_projectRoot: string): Promise<string> {
+  // Codex 호스트 어댑터는 제거됐다(ADR-20260722-claude-code-only-host · #70) — 시끄럽게 실패한다.
+  throw new Error('codex host surface was removed (ADR-20260722-claude-code-only-host)');
 }
 
 async function writeCodexPluginStatus(projectRoot: string): Promise<{
```
