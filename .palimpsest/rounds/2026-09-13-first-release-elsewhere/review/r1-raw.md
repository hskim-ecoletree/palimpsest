# 독립 리뷰 라운드 1 — 원문

> 회차 `2026-09-13-first-release-elsewhere` · 잰 날 2026-09-14 · HEAD `62377ed`(리뷰 중 `792a4da` → `96d99b5` → `be2ccb5` → `ab5369b` → `ed27c9b` → `ded2aec` → `62377ed` 로 움직였다 — 판정은 마지막 HEAD 기준)
> **읽기 전용이다.** 코드·회차 파일을 고치지 않았다. 원본 `~/dev/projects/ditto` 에는 `git clone --no-hardlinks`(읽기)만 댔다.
> **옮겨 적은 자:** 메인. 독립 리뷰 에이전트가 스크래치 `review-r1/out.md`(md5 `e3e322c697170a74c2901a7015a5d16c`)에 쓴 같은 바이트를 파일째 복사했다 — 이 인용 줄 하나만 더했다.

돌린 것 — `cargo xtask check`(세 번 · 마지막 HEAD 에서 **FAIL 1: 죽은 링크 부재** · rc=1) ·
`cargo test -p pal-cli --test ts_cross_file --test pending_and_pick --test user_vocabulary --test cross_file_references`
(`test result` 넷 · 통과 25+14+18+13 = **70** · 실패 0 · 무시 1(`d1_scan` · `PAL_VOCAB_SCAN` 전용)) ·
스크래치 새 복제본(`aded7ce` · `.palimpsest/` 없음)에 HEAD 크레이트와 같은 `target/release/pal`(`0.0.0+da017847967a` · `fee673b..HEAD -- crates/ xtask/` 변경 0)로
`pal touch codexHostAdapter --cache-dir/--index/--intent <스크래치>` · G1 명령을 `6ee9eb3..HEAD` 로 다시 · `gh issue view 135/155/156` ·
`git log --diff-filter=A 6ee9eb3..HEAD --name-only` · `git branch -vv` · `pal round conditions --json`.

---

## 합격선 축

「잼」은 이번 라운드에 내가 돌린 것 · 「문면」은 산출 파일을 읽고 좌표만 확인한 것이다.

| 조건 | 판정 | 잰 수 | 근거 |
|---|---|---|---|
| `A1` | 통과 | 잼 1/1 | `ts_cross_file::a1_지정자_여섯_꼴이_다른_파일의_심볼로_가는_엣지가_된다 ... ok` · RED `oracle/A1-red.txt` 는 문면 |
| `A1-a` | 통과 | 잼 1 + 화면 1 | 같은 시험 · 새 복제본 `touch` 화면에서 `bare_specifier` 가 「안 하기로 정한 자리입니다」 갈래(「원리상 풀 수 없는」 아래가 아님) |
| `A2` | 통과 | 잼 6/6 | `a2_1`~`a2_6` 전부 ok |
| `A2-a` | 통과 | 잼 1/1 | `a2a_패키지_extends_에서만_오는_별칭은_…` ok |
| `A3` | 통과 | 문면 8 픽스처 | `oracle/A3-fixture.txt` — 픽스처 여덟 불일치 0 · 합산 가지 표본 확장자 없음 8 · .js 대응 3 · 명시 .ts 1 · index 2 · 별칭 4 · 상속 별칭 2 · .tsx 1 · .d.ts 1 · bundler 19 · nodenext 1 → 0 인 가지 없음. 재실행 안 함 |
| `A3-a` | 통과 | 문면 1 | 같은 파일 `--corrupt` 절 불일치 **1** · rc=1 |
| `A4` | 통과 | 잼 1/1 | `a4_재수출_배럴로_펴지는_임포트는_재수출을_지나는_이름이다` ok |
| `A5` | 통과 | 문면 ⑵⑶⑷ · 잼 ⑴ | `oracle/A5-ditto.txt`(바이너리 `ba0a983`) ⑵ 0 · ⑶ 5082 중 불일치 0 · ⑷ 모집단 2915 빠진 것 0. ⑴ 은 HEAD 바이너리로 새 복제본에서 `cross-file-import 7199/11099` 를 내가 봤다. ⚠ `ba0a983` 뒤 `d734c04`(style) 가 `cross_file.rs` 를 고쳤고 ⑵⑶⑷ 는 그 뒤 재측정 기록이 없다 |
| `A6` | 미측정 | ⑴ 부분 · ⑵ 0 | 게이트와 같다. ⑴ 은 `runner-post.md` 가 `fee673b`(HEAD 와 크레이트 같음)에서 1123/0/3 을 적었고 내 부분 시험 70/0. ⑵ 는 `oracle/A6-rust.txt` 가 `ba0a983` 바이너리(1319=1319 · 음성 1294)이고 `d734c04` 이후 재측정이 없다 — 미측정이 맞다 |
| `A7` | 통과 | 잼 시험 25/25 + 화면 1 | `cross_file_references` 전량 ok · 내 `touch` 화면에 「호출자 수는 하한입니다 — 파일 최상위의 참조와 문자열로 찾는 자리는 세지 않습니다」 |
| `B1` | 통과 | 잼 1/1 | `pending_and_pick::b1_…` ok |
| `B1-a` | 통과 | 잼(b1 안) | 같은 시험 |
| `B2` | 통과 | 잼 1/1 | `b2_화면이_안내한_명령을_그대로_돌리면_통한다` ok · RED `oracle/B2-red.txt` 문면 |
| `B3` | 통과 | 잼 1/1 + 실저장소 ⑴ | `b3_…` ok · 새 복제본 `touch` 가 「아직 만들지 않았습니다 — `pal narrative` 를 먼저 돌리면 만들어집니다」 |
| `B4` | 통과 | 문면 2 표본 | `oracle/B4-timing.txt` — 표본 ① p95 2.2 ms · ② p95 2.2 ms · ② 대기 구역 50/50 · 후보 화면 0. 재실행 안 함 |
| `C1` | 통과 | 잼 1/1 | `c1_후보_화면의_지목_문자열을_그대로_주면_하나가_나온다` ok |
| `C1-a` | 통과 | 잼 1/1 | `c1a_…` ok |
| `D1` | 통과 | 잼 1/1 | `d1_장면_명령의_사람_화면에_작업_기록_어휘가_없다` ok · RED `oracle/D1-red.txt` 문면 |
| `D1-a` | 통과 | 잼 3/3 | `d1a_*` 셋 ok |
| `D2` | 통과 | 잼 + 문면 | 두 문자열이 내 `touch` 화면에 그대로 · `oracle/D2-commit.txt`(`9db40b7` 한 커밋 · 지운 줄 1 · 더한 줄 2) |
| `E1` | 통과 | 문면 6 산출 | `oracle/E1-order.txt` — 이웃 쌍 다섯 ✓ · 앵커 ✓. 복제본이 세션 스크래치라 재실행 못 함 |
| `E2` | 통과 | 문면 | `oracle/E2-scene.txt` ⑴~⑷ — **⟨개정 p2⟩ 뒤 문면**으로. 옛 문면대로는 반증(`oracle/E2-literal-touch.txt`)이고 개정은 두 결과를 본 뒤 썼다고 `intent.md ## 개정` 1 행이 스스로 적는다 · 소유자 `## 승격` 8 |
| `E3` | 대조 불가 | 기록 확인 | 판 `e3-effect` 취합 `dialectic/r2-raw.md` 의 합(合) r2 판정 「대조 불가」 · 소유자 `## 승격` 4~6 이 선다. 뒤집지 않는다 |
| `E4` | 통과 | 문면 | `oracle/E4-breakage.txt` P∖(C∪D) = `src/core/hosts/index.ts` · 오류 자리 최상위 1 · 선언 안 0. ⟨개정 p2⟩ 뒤 문면 |
| `F1` | 통과 | 잼 1 | `gh issue view 135 --json comments` — 2026-09-13T23:57:36Z 코멘트가 `A5` 의 수와 `docs/gates/first-release-elsewhere.md` 를 싣는다. ⚠ 그 수의 일부가 최종 바이너리의 값이 아니다 — 발견 4 |
| `F2` | 미측정 | 0 | `git branch -vv` → `main … [origin/main: ahead 80]`(그 뒤 커밋 더 늘었다) · 이 HEAD 에 런이 있을 수 없다 |
| `F4` | **미측정** (게이트는 통과) | 1/2 | `oracle/F4-g5.txt` `total_count 0` 은 섰다. 그 갈래의 요구 *"이 회차 `report.md` 에 `G5` 와 `Actions` 를 함께 담은 줄이 있다"* 는 `ls …/report.md` → `No such file or directory` — 발견 5 |
| `G1` | 통과 | 잼 1 + 음성 1 | `git diff 6ee9eb3..HEAD -- crates/ xtask/` 의 더한 줄에서 금지 낱말 → **0** · `+ // ditto` 심은 입력 → **1**. 오라클 기록(`6ee9eb3..6145849`)보다 넓게 HEAD 까지 쟀다 |
| `G2` | 반증 | 문면 | `oracle/G2-violation.txt` · before/after `palimpsest_dir_listing_sha256` 이 다르다. 원본 재박제는 안 돌렸다(원본 `git status` 가 `.git/index` 를 갱신할 수 있다) |

### 요약

- 내 판정: 통과 **24** · 반증 **1**(`G2`) · 대조 불가 **1**(`E3`) · 미측정 **3**(`A6` · `F2` · `F4`) = **29**
- 게이트(`62377ed`: 통과 25 · 반증 1 · 대조불가 1 · 미측정 2)와 **갈린 자리는 하나** — `F4`(게이트 통과 ↔ 내 판정 미측정).
- `E2` · `E4` 의 통과는 **사후에 고친 문면** 위의 통과다. 게이트 `### 개정 셋` 이 그 사실을 적고 있어 숨김은 아니다.
- **음성 대조가 걸린 것을 산출로 본 자리**: `A3-a`(불일치 1) · `A6`(1294≠1319) · `D1-a`(시험) · `E2`(`oracle/E2-negative-count.txt`) · `E4` 셋 · `G1`(내가 1 을 봤다).
- **RED 가 실제로 빨갰던 관측**: `A1`(0/10) · `A5`(0/11010 · `baseline/04-touch-resolveRepoRootForCreate.txt`) · `B2`(rc 2) · `D1`(64) · `G1`(4). 파일을 읽었고 재실행은 안 했다.
- **연결 확인(stub)**: 승인 대기(`crates/pal-cli/src/pending.rs`)와 TS 해소(`crates/pal-core/src/ts_module.rs`)는 **실행한 화면에 값이 실렸다** — 새 복제본에서 대기 구역 문구 · `7199/11099`. 부르는 자리 0 인 표면은 못 찾았다.
- **결박**: `.palimpsest/intent/bindings.jsonl` 에 이 회차 행 셋(`:4` NUL 분류 · `:11` 대기 목록 열쇠 · `:30` TS 해소)이 있다. `:30` 의 수가 낡았다 — 발견 8.
- **종료 보고**: `report.md` 가 없어 네 이름을 볼 수 없다(아직 종료 전) — 발견 6.

## 미측정 목록

이번 라운드에 **내가 직접 안 잰 것**이다.

| # | 안 잰 조건 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 못 쟀나 |
|---|---|---|---|---|---|---|
| 1 | `A3` · `A3-a` — TypeScript 컴파일러 대조를 다시 안 돌렸다 | 원의도 | 참 | 미관 | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/oracle/A3-fixture.txt` | 대조가 원본의 `node_modules/typescript` 를 부르고 픽스처 여덟을 다시 세워야 한다. 산출만 읽었다 |
| 2 | `A5` ⑵⑶⑷ — 대조 전량을 다시 안 돌렸다(⑴ 만 잼) | 원의도 | 참 | 미관 | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/oracle/A5-ditto.txt` | 같은 까닭. `d734c04` 뒤 재측정이 없다는 것만 확인했다 |
| 3 | `A6` ⑵ — 착수 워크트리 빌드와 엣지 집합 대조 | 원의도 | 참 | 미관 | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/oracle/A6-rust.sh` | `git worktree add` 가 이 저장소에 쓴다(읽기 전용) |
| 4 | `B4` — 타이밍 표본 둘 | 원의도 | 참 | 미관 | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/oracle/B4-timing.txt` | 대기 목록을 만들려면 `pal narrative`(수십 초)와 표본 100 호출이 필요했다 |
| 5 | `E1` · `E2` · `E4` — 효과 복제본 위 오라클 | 원의도 | 참 | 미관 | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/oracle/E1-order.sh` | 복제본 `ditto-effect` 가 앞 세션 스크래치에 있어 없다. 산출만 읽었다 |
| 6 | `G2` — 원본 재박제 | 원의도 | 참 | 미관 | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/oracle/G2-snapshot.sh` | 스크립트의 `git -C ~/dev/projects/ditto status` 는 원본 `.git/index` 의 stat 캐시를 갱신할 수 있다 — 원본에 쓰는 명령 금지 |
| 7 | `F2` — CI 런 | 원의도 | 참 | 미관 | `docs/gates/first-release-elsewhere.md` | push 전이다(`origin/main` 보다 앞섬) |
| 8 | `E3` — 방향 판정 자체 | 원의도 | 참 | 미관 | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/dialectic/r2-raw.md` | 결정론이 아니다 — 판과 소유자 기록이 서는지만 봤다 |

## 의도 축

### 빠진 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 1 | 원문의 목적 「1과 2가 실사용에서 값을 내면 제품이다」를 판정하는 자리가 게이트에 없다 — 효과는 `E3` 대조 불가 · 화면 몫 「걸음을 없앴다」 0 인데, 게이트는 조건 통과 25 만 표로 싣고 「첫 릴리스가 섰나」를 한 줄도 판정하지 않는다 | 원의도 | 참 | 거짓신호 | `E3` | `docs/gates/first-release-elsewhere.md` | 게이트 `## 판정` 표와 `## 효과` 절을 읽음 — 효과 절이 「화면 몫으로 「걸음을 없앴다」는 0 이다」를 적고, 제목·이슈 코멘트(「TypeScript 몫이 섰다」)는 섰다고 읽힌다 |

#### 발견 1 — 원문의 목적을 판정하는 자리가 없다

원문 `## 왜 이것인가` 는 무게를 *"실사용에서"* 에 두고, `## 목적 기여` 는 첫 릴리스의 정의를 *"1과 2가 실사용에서 값을 내면 제품이다"* 로 인용한다. 잠긴 조건 29 는 **장면의 부품**(해소 · 대기 · 지목 · 어휘 · 효과 기록)을 재고, 「값을 냈나」는 `E3` 하나가 지는데 그것이 대조 불가다. 게이트 `## 효과` 는 정직하게 0 을 적는다. 그러나 게이트에는 **「그러므로 첫 릴리스가 섰나 · 안 섰나」의 판정 줄이 없고**, 표의 통과 25 와 제목 「첫 릴리스를 남의 저장소에서 세운다」가 섰다고 읽힌다. 종료 보고가 이것을 「릴리스가 섰다」로 적으면 사실과 다르다.

- 모집단: 원의도
- 유효성: 참 — 게이트 전문을 읽었다. 「첫 릴리스」 판정 문장이 없다
- 해악도: 거짓신호
- 어디가 걸리나: `E3` · `docs/gates/first-release-elsewhere.md` `## 효과`

### 요구되지 않은 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 2 | 레코드 추출기의 좌표 선택 규칙을 이 회차가 바꿨다 — 반환문이 처음 들어온 커밋의 트리에 있는 첫 후보만 `경로` 로 적는다. 원문·인터뷰·조건 어디에도 없는 산출이고 이 리뷰의 레코드 추출에도 걸린다 | 규약 | 참 | 미관 | 없음 | `.claude/skills/round/bin/extract.py:421-492` | `git diff 6ee9eb3..HEAD -- .claude/skills/round/bin/extract.py` → `반환문_트리` · `트리에_있나` · `절대경로_모양` 추가(+75 −21). 원문 *"새 측정 장치 · 새 게이트 계열을 만들지 않는다. 규약이 요구하는 것만 만든다."* |

#### 발견 2 — 추출기 좌표 규칙 변경은 요구되지 않았다

해악은 작다 — 남의 저장소 좌표(`codex.ts`)가 `경로` 에 들어가던 것을 막는 방향이다. 그러나 **규약 도구**이고 다음 회차 전체의 레코드 `경로` 칸을 바꾼다. 원문의 범위 규칙은 *"위 장면을 막는 것만"* 이고, 이 변경은 장면(`touch`)을 막지 않는다. 원장에 「범위 밖에서 들인 것」으로도 적히지 않았다(`intent.md ## 범위 밖` 에 없음).

- 모집단: 규약
- 유효성: 참 — diff 를 읽었다
- 해악도: 미관
- 어디가 걸리나: 어느 조건에도 안 걸린다 · 원문 `## 범위를 정하는 규칙` 첫 줄과 끝 줄

### 있는데 틀린 것

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 3 | 남의 TS 저장소의 `touch` 화면이 Rust 의 뜻을 싣는다 — 「크레이트 뿌리의 재수출을 지나는 이름」 · 「열거형 변형 · 연관 상수」 · 「구조체 리터럴·경로 머리」. 계획의 세부가 *"`std::*`·크레이트 뿌리 같은 Rust 의 뜻을 TS 화면에 싣지 않는다"* 로 잠갔다 | 원의도 | 참 | 거짓신호 | 없음 | `crates/pal-cli/src/touch.rs:592` | 새 복제본에서 HEAD 바이너리 `pal touch codexHostAdapter` 출력에 `no_symbol_at_crate_root → 크레이트 뿌리의 재수출을 지나는 이름입니다` 줄이 찍힌다 · `print_transfer` 가 언어와 무관하게 아홉 줄을 늘 찍는다(`:586-601`) · `:516` · `:595` |
| 4 | `#135` 코멘트의 「종료 `7116/11010`」은 최종 제품의 값이 아니고 「착수 59」는 착수 값이 아니다 — 7116 은 NUL 분류 수정 전 바이너리(`db6c366` · partial 0 · 심볼 4578)의 효과 화면 수이고, HEAD 바이너리는 같은 복제본에서 `7199/11099` 를 낸다. 착수는 `0/11010` 이었고 59 는 ㈀ 뒤 NUL 수정 전 규칙의 값이다 | 원의도 | 참 | 거짓신호 | `F1` | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/effect/03-touch/05-codexHostAdapter.txt:17` | `gh issue view 135 --json comments` 표 첫 행 「`0/11010` → `7116/11010`」 · 넷째 행 「59 → 0」 · 내 실행 `※ 파일 간 해소 — ⓐ cross-file-import 7199/11099` · `대장 parsed 491 · partial 5 … / 2451 파일` · `2층 심볼 4656` |

#### 발견 3 — TS 화면에 Rust 의 뜻이 남았다

`intent.md ## 계획` `### 계획의 세부` 넷째 줄(사전부검 R1 ④⑤)이 *"화면의 「못 선 몫이 가는 문」 문구는 언어 중립이다"* 를 계획으로 잠갔다. 이관표는 회차 어휘를 걷어냈지만(`D1` 통과) **열쇠 아홉을 언어와 무관하게 전부** 찍고, 그중 `no_symbol_at_crate_root` 의 뜻이 「크레이트 뿌리」다. `no_symbol` 의 예시(「열거형 변형 · 연관 상수」)와 참조 엣지 설명(「구조체 리터럴·경로 머리」)도 Rust 용어다. TS 사용자는 자기 저장소에 없는 개념을 보고 그 까닭이 자기에게 해당하는지 판단할 수 없다. 이 줄을 재는 조건이 없어(`D1` 은 금지 패턴, `D2` 는 두 문자열) 초록인 채 남았다.

- 모집단: 원의도
- 유효성: 참 — 스크래치 새 복제본에서 HEAD 크레이트의 release 바이너리로 실행해 그 줄을 봤다
- 해악도: 거짓신호
- 어디가 걸리나: 어느 완수 조건에도 안 걸린다 · `intent.md` `### 계획의 세부` 「㈀ 화면의 「못 선 몫이 가는 문」 문구는 언어 중립이다」

#### 발견 4 — `#135` 코멘트의 수가 두 시점을 섞었다

`F1` 의 글자(「`A5` 의 수와 게이트 경로를 싣는다」)는 충족한다. 그러나 표의 「종료」 칸은 **효과 실행(`db6c366`) 시점**의 `touch` 수 7116/11010 이고, 같은 표의 재현율 0 · 잰 엣지 5082 는 **NUL 수정 뒤(`ba0a983`)** 의 값이다. HEAD 바이너리는 짝 11099 · 선 것 7199 를 낸다(파일 496 이 되며 짝이 89 늘었다). 「착수」 칸의 59 도 착수 기준선(0/11010 — 재현율을 잴 엣지가 없다)이 아니라 ㈀ 뒤의 중간값이다. 밖으로 나간 글이라 원장 안에서 고쳐지지 않는다.

- 모집단: 원의도
- 유효성: 참 — 코멘트 본문과 내 실행 출력을 댔다
- 해악도: 거짓신호
- 어디가 걸리나: `F1`

## 이번 라운드의 새 발견

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 5 | 게이트가 `F4` 를 통과로 적었는데 그 갈래의 요구(이 회차 `report.md` 에 `G5` · `Actions` 줄)가 아직 없다 — 사실이 아닌 판정이 원장 둘에 전사됐다 | 회차기록 | 참 | 금지역 | `F4` | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/intent.md:371` | `ls .palimpsest/rounds/2026-09-13-first-release-elsewhere/report.md` → `No such file or directory` · `oracle/F4-g5.txt` `{"runs":[],"total_count":0}` · 게이트 `## 판정` 통과 행에 `F4` · `intent.md:371` `· 통과 ⟨전사 2026-09-14⟩` |

#### 발견 5 — `F4` 통과는 아직 사실이 아니다

`F4` 는 `total_count` 에 묶였고 값이 0 이다. 그러면 조건이 요구하는 것은 *"이 회차 `report.md` 에 `G5` 와 `Actions` 를 함께 담은 줄이 있다"* 하나다. 그 파일이 없다. 게이트의 근거 행도 「`total_count 0` → 종료 보고에 `G5` · `Actions` 줄」이라 적어 **아직 없는 산출을 근거로 든다.** 게이트 주석은 종료 걸음에서 확정할 것으로 `A6` · `F2` 만 적고 `F4` 는 뺐다. 그대로 두면 종료 보고가 그 줄을 빠뜨려도 `원장 둘 대조` 는 두 원장이 같아 초록이다 — 그 경우 이 칸을 다시 재는 장치가 없다.

금지역 출처: `.claude/pal/policy.toml` 없음 → `intent.md ## 금지역` 이 규약 §11 기본 다섯을 등록 → **사실이_아닌_것을_사실로**. 모집단은 회차기록이지만 금지역이라 본 목록에 둔다.

- 모집단: 회차기록
- 유효성: 참 — `ls` 와 게이트 · `intent.md` 전사 줄을 읽었다
- 해악도: 금지역
- 어디가 걸리나: `F4` · `docs/gates/first-release-elsewhere.md` `## 판정`

## 자기 산출에 대한 발견

메인이 처리 방침을 정한다. 이 회차가 반드시 닫아야 하는 것이 아니다.

| # | 발견 | 모집단 | 유효 | 해악도 | 조건 | 좌표(파일:줄) | 근거(명령·출력) |
|---|---|---|---|---|---|---|---|
| 6 | 게이트 머리가 없는 `report.md` 로 링크해 `cargo xtask check` 가 빨갛다 — push 하면 `F2` 의 CI 가 같은 자리에서 빨개질 수 있다. 종료 보고가 없어 네 이름도 아직 볼 수 없다 | 회차기록 | 참 | 실패 | `F2` | `docs/gates/first-release-elsewhere.md:7` | HEAD `62377ed` 에서 `cargo xtask check` → `FAIL  죽은 링크 부재` · `docs/gates/first-release-elsewhere.md  →  ../../.palimpsest/rounds/2026-09-13-first-release-elsewhere/report.md` · `Error: 1개 검사가 실패했다` |
| 7 | 종료 기준선 기록이 「ditto 원본에 쓰는 스크립트 — 없음」이라 적는데, 바로 그 러너 창(07:21~08:38) 안의 `f06` 이 08:26 에 원본 캐시 2,451 개를 썼다. 뒤 커밋이 `G2` 반증을 세웠지만 기준선 파일은 주석 없이 남았다 | 회차기록 | 참 | 거짓신호 | `G2` | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/baseline/runner-post.md` | `runner-post.md` 환경 표 「ditto 원본에 쓰는 스크립트 · 없음」 · `oracle/G2-violation.txt` 「시각 분포 `2451 09-14 08:26`」 · `git log -- baseline/runner-post.md` 가 `3c1e36f` 한 커밋 |
| 8 | TS 해소 결박의 노트가 「지금 같은 복제본에서 선 것 7116」이라 적는다 — 결박 시점(`3c1e36f` 워크트리 · NUL 수정 뒤)의 제품은 7199/11099 를 낸다 | 회차기록 | 참 | 거짓신호 | `A5` | `.palimpsest/intent/bindings.jsonl:30` | 노트 원문 「지금 같은 복제본에서 선 것 7116 · … 불일치 0 · 재현율 빠진 것 0(모집단 2915)」 · 내 실행 `7199/11099` · 7116 이 나오는 산출은 `effect/03-touch/*`(바이너리 `db6c366`)뿐 |
| 9 | 잠긴 의도의 착수 관측 표에 자리표시가 채워지지 않고 남았다 — 「⟨기준선 러너가 `baseline/runner-pre.md` 에 적는다⟩」 | 회차기록 | 참 | 미관 | 없음 | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/intent.md:125` | 해당 줄을 읽음 · `baseline/runner-pre.md` 는 있다 |
| 10 | 게이트 머리 「판정일 2026-09-13」인데 판정 전사는 전부 2026-09-14 이고, 절 제목 「개정 셋」인데 `intent.md ## 개정` 은 2 행이다 | 회차기록 | 참 | 미관 | 없음 | `docs/gates/first-release-elsewhere.md:4` | 게이트 머리 · `intent.md` 조건 줄 29 개 모두 `⟨전사 2026-09-14⟩` · `## 개정` 표 데이터 행 2 |

## 내가 기각한 것

| # | 기각한 것 | 모집단 | 유효 | 해악도 | 좌표(파일:줄) | 왜 아니었나 |
|---|---|---|---|---|---|---|
| 11 | 원본 캐시 격리 수정(`--cache-dir`)이 워킹트리에만 있어 HEAD 의 `f06` 이 다음 기준선에서 또 원본에 쓴다 | 저장소 | 거짓 | 금지역 | `scripts/f06-verify.py` | 리뷰 중 `be2ccb5` 가 커밋했다 — `git show HEAD:scripts/f06-verify.py` 에서 `cache-dir` 2 회 · `git status --short` 비었다 |
| 12 | `f06` 의 `pal()` 이 `--intent` 를 안 줘 원본에 `intent.redb` 를 새로 만든다 | 저장소 | 거짓 | 금지역 | `scripts/f06-verify.py` | `ls -la ~/dev/projects/ditto/.palimpsest/` → `cache` · `index.redb`(8월 16일) 뿐이다. 질의가 의도 저장소를 만들지 않았다 |
| 13 | `어색한 표현 부재` 가 `f06:320` · `f09:268` 의 「~를 낸다」로 빨갛다 | 저장소 | 거짓 | 실패 | `scripts/f09-verify.py:268` | 첫 실행이 커밋 사이 트리를 쟀다. HEAD 두 파일에 `낸다` 0 회 · 재실행에서 그 검사는 FAIL 이 아니다 |
| 14 | `원장 둘 대조` 가 게이트 `F1` 미측정 · `intent.md` 통과 갈림(`ded2aec`)을 초록으로 넘겼다 — 검사가 죽은 가지다 | 저장소 | 거짓 | 금지역 | `xtask/src/main.rs:6495` | `check_ledger_pair` 는 `의 != 게` 면 problems 에 민다. 내 초록 실행은 `62377ed`(08:58:27) 커밋 뒤에 돈 경합이었다 |
| 15 | 효과 산출이 NUL 수정 전 바이너리(`db6c366`)라 `E1` · `E2` 가 최종 제품을 안 쟀다 | 원의도 | 거짓 | 거짓신호 | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/effect/00-seal.md` | 의도는 *"이 회차가 만든 바이너리"* 를 요구한다 — `db6c366` 은 ㈀~㈃ 이 든 이 회차 커밋이다. HEAD 바이너리로 새 복제본에서 `codexHostAdapter` 가 같은 `호출자 2` · 같은 이관표를 낸다 |
| 16 | 원문 「깨질 곳을 받는다」가 빠졌다 — `touch` 가 호출자 자리를 안 싣는다 | 원의도 | 거짓 | 거짓신호 | `crates/pal-cli/src/touch.rs:505` | 소유자가 `## 승격` 7 에서 「수가 의도」로 답했고 `#156` 이 열려 있으며 게이트 `## 효과` 가 그 사실을 적는다. 숨긴 축소가 아니다 |
| 17 | `A6` 은 시험이 초록이니 통과로 적어야 한다 | 원의도 | 거짓 | 미관 | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/oracle/A6-rust.txt` | ⑵ 는 `ba0a983` 바이너리로 쟀고 `d734c04` 가 뒤에 `cross_file.rs` 를 고쳤다(`git log ba0a983..HEAD -- crates/ xtask/`). 미측정이 맞다 |
| 18 | `G1` 오라클이 `6145849` 까지만 재서 뒤의 코드 변경을 놓쳤다 | 원의도 | 거짓 | 거짓신호 | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/oracle/G1-coupling.txt` | `6145849..HEAD -- crates/ xtask/` 변경 0 · 내가 `6ee9eb3..HEAD` 로 다시 재서 0 · 음성 1 |
| 19 | `xtask` 의 「이력 면제」는 원문이 금지한 새 측정 장치다 | 저장소 | 거짓 | 미관 | `xtask/src/main.rs:4189` | 기존 검사의 면제 한 갈래이고, 착수 첫 걸음 2(`NEXT-E-*` 를 지운다)의 직접 귀결이며 음성 대조 시험 `기준커밋에_있다가_지워진_좌표만_면제한다` 가 있다 |
| 20 | `pal export` 화면의 `F19` · `R-21` 이 `D1` 위반이다 | 원의도 | 거짓 | 거짓신호 | `.palimpsest/rounds/2026-09-13-first-release-elsewhere/oracle/A5-ditto.txt` | `D1` 모집단은 장면 명령(`install` · `ledger` · `narrative` · `touch` · `query` · 최상위 `--help`)이고 계획 ㈃ 가 「장면의 명령의 사람 화면」으로 잠갔다. `export` 는 밖이다 |
| 21 | 결박 노트의 7116 과 `A5` 의 잰 엣지 5082 가 서로 모순이다 | 회차기록 | 거짓 | 미관 | `.palimpsest/intent/bindings.jsonl:30` | 단위가 다르다 — 7116 은 선 참조 수(짝 대비), 5082 는 파일 간 엣지 수다. 효과 화면에서 7116 + 못 선 3894 = 11010 으로 검산된다(낡음은 발견 8 이 따로 진다) |

## 이 반환문이 낸 항의 총수

**8 + 1 + 1 + 2 + 1 + 5 + 11 = 29.**
미측정 목록 8 · 빠진 것 1 · 요구되지 않은 것 1 · 있는데 틀린 것 2 · 이번 라운드의 새 발견 1 · 자기 산출 5 · 기각 11.
그중 유효성 `거짓` 은 **11**(전부 기각)이다. `## 합격선 축` 의 29 행은 조건 판정이고 항이 아니다.

## 끝내도 되는가

**안 된다.** 본 목록에 **금지역 1**(발견 5 — `F4` 를 아직 없는 종료 보고 줄로 통과 전사)이 남았다. 실패 1(발견 6 · 죽은 링크)은 자기 산출 절에 있고, 종료 보고를 쓰면 둘이 함께 닫힐 수 있다.
