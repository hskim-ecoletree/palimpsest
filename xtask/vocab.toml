# `pal-core` 소스에 나타나면 CI 실패 — stack §4.2
#
# **허용 목록에 줄이 느는 것 자체가 관측 대상이다.** 예외를 넣을 때는 왜인지 적는다.
# 금지 목록의 정본은 stack §4.2 이고 xtask/src/main.rs 가 그것을 옮겨 담는다.

# 지금 허용된 예외는 없다. 하나도 없는 것이 정상 상태다.
allow = []
