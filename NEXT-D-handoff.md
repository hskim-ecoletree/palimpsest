# 회차 D — 착수 프롬프트

> **임시 파일이다. 착수하면 이 파일을 지워라.**
> **다른 세션에서 여는 회차다.** 먼저 `git pull` 로 최신을 받아라 —
> 앞 회차(`216d3ea..5e83493`)는 `#70` 을 닫았다.

---

## 0. 먼저 할 것

```
/round
```

절차를 요약해서 흉내내지 마라. **스킬을 열어 그대로 밟아라. 인터뷰부터다.**

★ **규약이 바뀌었다.** 「소진」은 없어졌고 **끝을 정하는 자가 셋**이다 —
**상한 · 해악 게이트 · 모집단 분리**(§11 「끝을 정하는 자」). 앞 회차가 그것을 만들었고
**자기 자신에게 적용해 닫았다.** 이 회차는 **그 조건으로 도는 두 번째 회차**다.

## 1. 착수 전에 반드시 읽을 것

| 무엇 | 어디 |
|---|---|
| **바뀐 규약** | `.claude/skills/round/SKILL.md` §11 「끝을 정하는 자」 · §1 · §2 · §5 · §10 |
| **앞 회차의 종료 보고** | `.palimpsest/rounds/2026-08-18-completion-condition/report.md` |
| **그 판정** | `docs/gates/round-completion-condition.md` |
| **회고 전량** | 같은 회차의 `retro/` (22 파일) — **필요할 때만 열어라. 크다** |

## 2. 앞 회차가 남긴 수 — 이 회차가 기대야 할 것

| 무엇 | 수 |
|---|---|
| **원 의도에 관한 발견** | **3 / 108 (2.8%)** — 97% 가 회차 자신·저장소·규약 |
| **해로운 발견 중 자기 장치** | **20 / 27** |
| 상한 5 로 닫을 때 놓치는 해로운 발견 | 16 — **자기 장치를 빼면 2, 10 라운드를 더 돌아도 2** |
| 연쇄(앞 수정이 뒤 발견을 낳음) | **61 / 109 (56%)** — 진자는 6 |
| 실제로 뭔가 깨뜨린 것 | **6 / 109** — 67% 는 「사람이 잘못 믿게 되는 것」 |
| **dry 가 난 계열** | **0 / 다섯** (독립 리뷰 15 · 사전부검 3 · 인터뷰 8 · 사전부검 3 · 독립 리뷰 3) |

★ **소진을 기다리지 마라. 안 온다.**

## 3. 새 도구 — 실제로 써라

### 계기판

```bash
.claude/skills/round/bin/dashboard.py <착수커밋> <의도파일> [종료커밋]
```

**규약 §5 의 「검증 → 수정 착수」 사이**에서 부른다. 여섯 칸이 나온다 —
**자기 비율 · 미판정 잔액 · 진자 · 연쇄 깊이 · 라운드 · 승격.**

★ **판정 도구가 아니다. 당기는 물건이다.** 수가 이상하면 들여다봐라.
⚠ **커밋 제목에 `[R<n>]` 과 `[승격]` 을 달아야** ④⑤⑥ 이 선다. 안 달면 「못 잰다」고 말한다.

### 에이전트 둘

`pal-independent-reviewer` · `pal-premortem-sweeper` — 발견마다 **모집단 · 유효성 ·
해악도**를 붙이고 **자기 장치에 대한 발견은 별도 절**로 낸다. 판정값에 **「미측정」**이 있다.

## 4. 지금 어디에 서 있나

```bash
./scripts/frontier.sh
```

**상태는 이슈에만 산다.** 문서를 읽지 말고 실행해서 알아라.

**앞 회차가 분할로 세운 둘** (§11 ③ — 넘김이 아니라 분할):

- **#71** 회차 산출의 **기계 원천**을 담는 자리가 없다 — 회고의 분류가 어느 검사도 안 지난다
- **#72** 발견을 **구조화된 레코드**로 남기는 자리가 없다 — 기각된 발견이 사라져 유효성을 못 잰다

★ **둘은 짝이다.** #72 가 서면 계기판의 칸 둘(원 의도 비율 · 발견 유효성)이 서고,
정반합 실증의 표본이 8 에서 늘어난다. **#71 이 그 레코드가 살 자리를 정한다.**

⚠ **`.json` 은 못 쓴다** — `docs/sunset.toml` 의 트리거가 `.palimpsest/rounds/*/*.json` 이라
`cargo xtask check` 가 `docs/research` 삭제를 요구한다(#68). 형식을 정할 때 함께 봐라.

## 5. 앞 회차가 못 잰 것 — 이 회차가 잴 수 있다

### 계기판이 「당기는가」

앞 회차의 `## 원리상 못 잰 것` 이 조건을 적었다:

> **잴 수 있게 되는 조건**: 이 저장소에서 도는 다음 회차 하나. 소유자가 계기판의 수를 보고
> **스스로 들여다보거나 설명을 요구하면** 선 것이다.

★ **이 회차가 그 다음 회차다.** ⚠ **그 사건을 기록하는 자리가 아직 없다** —
조건이 판정되려면 그 자리부터 있어야 한다(#71·#72 와 같은 자리다).

### 모집단 분리가 값을 내는가

앞 회차는 (나)를 골랐는데 **강하게 시험하지 못했다** — 자기 산출 발견이 **금지역에 닿거나
작아서** 거의 다 그 회차에서 닫혔다. **분리가 값을 내는 것은 장치에 대한 발견이 크고
금지역이 아닐 때**인데 그런 표본이 안 나왔다.

## 6. 걸리는 것 — 이 저장소의 규율

- **저장소가 PUBLIC 이다. force push 금지. 태그·릴리스 생성 금지.**
- 실험은 `/tmp/<이름>-<타임스탬프>/` 하나로 고정하고 **절대경로**로 박아라
- ⚠ **검증 출력을 파이프로 잘라 `error[E`·`panicked` 를 날리지 마라.** 전 출력을 파일로 받고 세라
- ⚠ **`rc=0` 을 판정으로 읽지 마라**
- **음성 대조는 반드시 격리 사본에서** — 워킹트리에 심으면 커밋에 딸려 들어간다
- **독립 리뷰가 도는 동안 `git add -A` 를 쓰지 마라.** 경로를 지정해 담아라
- **CI 는 `cancel-in-progress: true`** — 커밋은 여럿 내되 **push 는 한 번**
- **새 이슈를 열 때는 「만들 것」만**
- `cargo fmt` 는 이 저장소의 게이트가 아니다

★★ **앞 회차가 가장 많이 물린 것 하나** — **「손으로 베낀 거울」이 여섯 번 나왔다.**
검사의 모집단·조건 수·라운드 표기·매니페스트 항목 수를 **문서나 주석에 베껴 놓으면
놓은 날부터 갈린다.** 그중 하나는 **CI 만 잡았다**(로컬은 그 시험을 안 돌렸다).

> **세는 자리는 하나다. 베끼지 말고 돌려라.**

⚠ 그리고 **베낀 것을 고치면서 또 베꼈다** — 한 번에 안 끝난다. **고친 검출식을 그 자리에서
곧바로 돌려 봐라.**
