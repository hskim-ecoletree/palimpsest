# 회차 C — 재고 처분을 회고하고, 「소진」을 대체할 완료 조건을 찾는다

> **임시 파일이다. 착수하면 이 파일을 지워라.**
> **다른 PC 에서 여는 회차다.** 먼저 `git pull` 로 최신을 받아라 — 이 문서가 가리키는 회차는 `7b683d0` 에서 닫혔다.
> 짝: `NEXT-B-plan-for-harness.md` — B 가 지운다. **C 는 B 를 안 만진다.**

---

## 0. 먼저 할 것

```
/round
```

절차를 요약해서 흉내내지 말고 **스킬을 열어 그대로 밟아라. 인터뷰부터다.**

⚠ **다만 이 회차는 그 스킬의 §11 조건 2 를 의심하는 회차다.** 규약을 밟되, 밟는 동안 그 규약이 어디서 안 서는지를 **관측 대상으로 삼아라.** 규약을 고치는 것이 이 회차의 산출이다.

## 1. 소유자 지시 (2026-08-18)

> 소진을 대체할 새로운 완료조건을 찾는다.
> 지금 닫고, 새 세션에서 이번 회차에 대해 회고한 뒤에 문제 해결방식부터 찾는다.

★ **순서가 지시의 일부다.** ① 회고 → ② 문제 해결 방식 → ③ 그 뒤에 조건 설계.
**조건을 먼저 고안하지 마라.** 회고 없이 만든 조건은 지금 반증된 것과 같은 종이 된다.

이것을 지는 이슈: [#70](https://github.com/hskim-ecoletree/palimpsest/issues/70)

## 2. 무엇이 반증됐나 — 착수 시점의 사실

재고 처분 회차(`2989527..7b683d0`)에서 `/round` §11 조건 2 **「소진 = 새 발견이 연속 두 라운드 0」**이 종료를 판정하지 못한다는 것이 실측으로 드러났다.

- **독립 리뷰 15 라운드 · dry 0 회.** 라운드별 발견: 14 · 13 · 10 · 8 · 13 · 5 · 12 · 6 · 7 · 7 · 3 · 3 · 4 · 1 · 2
- **8 라운드부터 발견의 거의 전부가 그 회차가 만든 검사 자신**에 관한 것이다. 발견을 닫은 커밋이 만진 파일에 `xtask/src/main.rs` 가 거의 매번 있다 — **장치를 고칠 때마다 새 선언면이 생겨 다음 라운드의 먹이가 된다.**
- **진자 셋** — 「계획 §」 토큰(넣었다 빼고 거짓 표기 7 개를 되돌렸다) · `f10-5-verify.py`(고쳤다 되돌려 순변경 0) · **`f12-verify.py` 는 7 라운드의 수정이 14 라운드의 결함을 만들었다**(검사를 통과시키려 「옛」을 경로 리터럴에 박아 측정이 죽은 가지가 됐다).
- **까닭**: 「새 발견 0」은 **열린 생성 과정에게 음성을 요구한다.** 리뷰어에게 *"문제를 찾아라"* 라고 물으면 무엇이든 그럴듯한 것이 나온다.

⚠ **반대 방향도 사실이다 — 여기서 균형을 잃지 마라.** 13~15 라운드의 발견은 지어낸 것이 **아니었다.** 착수 전에 전부 실측으로 확인했다(표기 없는 인용 **62 곳** · CI 판별 `grep` 이 오분류하는 파일 부류 **넷** · `docs/sunset.toml` 이 검사 모집단 밖 — 셋 다 격리 사본에 심어 재현). 약 100 건 중 회귀를 만든 것은 **둘**, 순변경 0 이 **하나**다.

★ **그러니 「리뷰가 헛것을 낸다」로 결론을 미리 정하지 마라.** 그것을 **재는 것**이 ① 회고다.

## 3. 회고 — 무엇을 재나

**발견 약 100 건을 실제로 분류한다.** 인상이 아니라 수로.

근거 자리:
- `git log --oneline 2989527..7b683d0` 의 `fix(round):` 커밋 — 각 라운드가 무엇을 냈고 어느 커밋이 닫았는지가 제목과 본문에 있다
- `.palimpsest/rounds/2026-08-18-inventory-disposal/state.md` — **실패한 접근 열다섯**
- `.palimpsest/rounds/2026-08-18-inventory-disposal/report.md` — 종료 보고
- `docs/gates/inventory-disposal.md` — 합격선·판정·효과·범위 밖 + 음성 대조 **28 행**

가를 축(예시지 상한이 아니다):
- **진짜였나** — 실측으로 재현됐나, 아니면 그럴듯했을 뿐인가
- **무엇을 낳았나** — 장치(검사·시험)를 낳았나, 산문만 고쳤나
- **부호** — 개선인가, 회귀인가, 순변경 0 인가
- **대상** — 원 의도(재고)인가, 그 회차가 만든 장치인가
- **비용** — 라운드당 토큰·시간 대비 얻은 것

## 4. 그 다음 — 문제 해결 방식

회고가 끝나기 전에 이 절을 열지 마라. 물음만 미리 적어 둔다:

- 종료를 **누가** 판정하나 — 열린 생성 과정인가, 사전 등록된 유한 목록인가
- 「자기 장치에 대한 발견」을 어떻게 다루나 — 무한 편입인가, 한 번만인가, 새 회차인가
- 리뷰어에게 **무엇을 묻나** — *"문제를 찾아라"* 는 음성을 못 낸다. 무엇을 물으면 음성이 가능한가
- 회차가 **커지는 것** 자체를 어떻게 막나 — 이 회차는 15 라운드를 돌았다

## 5. 그 뒤 — 규약을 고친다

- `.claude/skills/round/SKILL.md` §11 조건 2
- ★ *"끝은 소진이 정한다"* 는 **세 자리**에 있다 — §1 인터뷰 · §2 사전부검 · §7 검증. **같은 병을 앓는지 함께 봐라.** 하나만 고치면 나머지 둘이 남는다.

## 6. 걸리는 것 — 이 저장소의 규율

- **저장소가 PUBLIC 이다. force push 금지. 태그·릴리스 생성 금지.**
- 실험은 `/tmp/<이름>-<타임스탬프>/` 하나로 고정하고 **절대경로**로 박아라
- ⚠ **검증 출력을 파이프로 잘라 `error[E`·`panicked` 를 날리지 마라.** 전 출력을 파일로 받고 거기서 세라
- ⚠ **`rc=0` 을 판정으로 읽지 마라**
- **음성 대조는 반드시 격리 사본에서 돌려라** — 워킹트리에 심으면 커밋에 딸려 들어간다(실제로 한 번 CI 를 빨갛게 만들었다)
- **독립 리뷰가 도는 동안 `git add -A` 를 쓰지 마라.** 경로를 지정해 담고, 커밋 뒤 `git diff <직전>..HEAD --stat` 으로 무엇이 들어갔는지 봐라
- **CI 는 `cancel-in-progress: true` 다.** push 전에 `gh run list` 로 진행 중인 회차를 봐라. 커밋은 여럿 내되 **push 는 한 번**
- **새 이슈를 열 때는 「만들 것」만**
- `cargo fmt` 는 이 저장소의 게이트가 아니다

## 7. 지금 어디에 서 있나

```bash
./scripts/frontier.sh
```

**상태는 이슈에만 산다.** 문서를 읽지 말고 이것을 실행해서 알아라.
