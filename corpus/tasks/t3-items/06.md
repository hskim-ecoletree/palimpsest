# 6 / 42

**boxwood-packages/boxwood-commons/src/main/kotlin/kr/co/ecoletree/boxwood/commons/audit/aspect/AuditLogAspect.kt:118** · 심볼 `AuditLog`

```kotlin
  110   
  111           // Extract resource ID if needed for before state
  112           val resourceId = extractResourceId(auditLog, args, signature)
  113           logger.info("[CHANGE_TRACKING] resourceId extracted: $resourceId (resourceIdParam=${auditLog.resourceIdParam}, isSuspend=$isSuspendFunction)")
  114   
  115           // Determine if change tracking is needed
  116           val needsChangeTracking = auditLog.captureBeforeState || auditLog.captureAfterState
  117   
  118 ▶         // Push domain context for change tracking (supports nested @AuditLog methods)
  119           // Use .code (e.g., "ServiceInterfaceVersion") for consistency with database target_domain column
  120           if (needsChangeTracking) {
  121               AuditTrackingContext.pushDomain(auditLog.targetDomain.code)
  122           }
  123   
  124           // Execute the method with proper cleanup in finally block
  125           var result: Any? = null
  126           var exception: Throwable? = null
  127           var beforeStateRaw: Any? = null
  128           var requestPayload: String? = null
  129   
  130           try {
  131               // Capture before state (inside try block for proper cleanup)
  132               logger.info("[CHANGE_TRACKING] captureBeforeState=${auditLog.captureBeforeState}, resourceId=$resourceId")
  133               beforeStateRaw = if (auditLog.captureBeforeState && resourceId != null) {
  134                   try {
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
