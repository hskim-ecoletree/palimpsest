# 35 / 42

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/auth/web/controller/AuthenticationController.kt:451** · 심볼 `TenantTransactional`

```kotlin
  443           setupTenantContextFromRefreshToken(request.refreshToken)
  444   
  445           val refreshResponse = authenticationService.refreshToken(request.refreshToken, httpRequest, httpResponse)
  446           TokenRefreshResponseDto.from(refreshResponse)
  447       }
  448   
  449       /**
  450        * Refresh Token에서 tenantId를 추출하여 TenantContext 설정
  451 ▶      * @TenantTransactional AOP가 실행되기 전에 호출되어야 함
  452        */
  453       private fun setupTenantContextFromRefreshToken(refreshToken: String) {
  454           try {
  455               val tenantId = jwtTokenService.extractCompanyId(refreshToken)
  456               if (!tenantId.isNullOrBlank()) {
  457                   val tenant = boxwoodTenantRepository.findByTenantKey(tenantId)
  458                   if (tenant != null) {
  459                       TenantContext.setCurrentTenant(tenant.tenantSchema)
  460                       log.debug("TenantContext set from refresh token in controller: tenantId={}, schema={}",
  461                           tenantId, tenant.tenantSchema)
  462                   } else {
  463                       log.warn("Tenant not found for tenantId in refresh token: {}", tenantId)
  464                   }
  465               } else {
  466                   log.warn("No tenantId found in refresh token")
  467               }
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
