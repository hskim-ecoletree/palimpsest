# 5 / 42

**boxwood-packages/boxwood-commons/src/main/kotlin/kr/co/ecoletree/boxwood/commons/audit/aspect/AuditLogAspect.kt:36** · 심볼 `AuditLog`

```kotlin
   28   import org.springframework.expression.spel.support.StandardEvaluationContext
   29   import org.springframework.web.bind.annotation.PathVariable
   30   import org.springframework.web.bind.annotation.RequestBody
   31   import org.springframework.web.context.request.RequestContextHolder
   32   import org.springframework.web.context.request.ServletRequestAttributes
   33   import kotlin.coroutines.Continuation
   34   
   35   /**
   36 ▶  * AOP aspect that intercepts methods annotated with @AuditLog (v1.3.1).
   37    *
   38    * Enhanced features:
   39    * - SpEL expression evaluation for target extraction and summary args
   40    * - Value Object construction (ActorInfo, TargetInfo, ChangeInfo, ErrorInfo)
   41    * - i18n summary support (summaryKey, summaryArgs)
   42    * - Sensitive data masking
   43    * - MDC traceId extraction for distributed tracing
   44    * - TTL calculation based on event category
   45    *
   46    * ## Processing Flow:
   47    * 1. Extract annotation configuration and handler
   48    * 2. Build SpEL evaluation context with method parameters
   49    * 3. Capture before state if required (via handler.loadBeforeState())
   50    * 4. Execute the target method
   51    * 5. Capture after state if required (via handler.loadAfterState())
   52    * 6. Evaluate SpEL expressions (targetId, targetName, summaryArgs)
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
