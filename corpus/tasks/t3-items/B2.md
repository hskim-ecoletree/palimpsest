# 묶음 2 — 8건을 한 번에 판정한다 (시퀀스 21–28 / 42)

**개별 확인이 아니라 표본 확인이다.** 전부 열어 볼 필요는 없다 — 몇 건을 보고 나머지를 함께 판정해도 된다. 그것이 이 장치의 형태다.

판정은 **8건 전부에 대해 한 번에** 준다. 답과 함께 **실제로 열어 본 건수**를 적어 달라.


---


## 1 / 8

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/common/util/ValidationUtil.kt:85** · 심볼 `require`

```kotlin
   77    * <h2>리스트 요소 필드 null 검사</h2>
   78    * <p>리스트의 모든 요소에서 지정된 필드가 null이면 예외를 발생시킵니다.</p>
   79    *
   80    * @param collection   검사할 컬렉션
   81    * @param fieldName    검사할 필드 이름
   82    * @param errorMessage 에러 메시지
   83    * @throws ETServiceFailureException 필드 접근 오류 또는 필드가 null인 경우 발생
   84    */
   85 ▶ fun requireNonNullFields(collection: Collection<*>?, fieldName: String, errorMessage: String) {
   86       if (!isNotNullOrEmpty(collection)) {
   87           throw ETServiceFailureException.badRequestWithFailureMessage(errorMessage)
   88       }
   89   
   90       try {
   91           for (item in collection!!) {
   92               if (item == null) {
   93                   throw ETServiceFailureException.badRequestWithFailureMessage(errorMessage)
   94               }
   95   
   96               val field = item::class.java.getDeclaredField(fieldName)
   97               field.isAccessible = true
   98               if (field.get(item) == null) {
   99                   throw ETServiceFailureException.badRequestWithFailureMessage(errorMessage)
  100               }
  101           }
```


## 2 / 8

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/common/util/ValidationUtil.kt:223** · 심볼 `require`

```kotlin
  215   /**
  216    * <h2>전화번호 형식 검증</h2>
  217    * <p>문자열이 전화번호 형식인지 검증합니다.</p>
  218    *
  219    * @param phoneNumber  검증할 전화번호
  220    * @param errorMessage 에러 메시지
  221    * @throws ETServiceFailureException 문자열이 null이거나 전화번호 형식이 아닌 경우
  222    */
  223 ▶ fun requirePhoneNumber(phoneNumber: String?, errorMessage: String) {
  224       requirePattern(phoneNumber, PHONE_PATTERN, errorMessage)
  225   }
  226   
  227   /**
  228    * <h2>숫자 범위 검증</h2>
  229    * <p>숫자가 지정된 범위 내에 있는지 검증합니다.</p>
  230    *
  231    * @param number       검증할 숫자
  232    * @param min          최소값
  233    * @param max          최대값
  234    * @param errorMessage 에러 메시지
  235    * @throws ETServiceFailureException 숫자가 범위를 벗어나는 경우
  236    */
  237   fun requireNumberInRange(number: Number?, min: Number, max: Number, errorMessage: String) {
  238       if (number == null || number.toDouble() < min.toDouble() || number.toDouble() > max.toDouble()) {
  239           throw ETServiceFailureException.badRequestWithFailureMessage(errorMessage)
```


## 3 / 8

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/config/FlywayConfig.kt:206** · 심볼 `validate`

```kotlin
  198        */
  199       fun info(schemaName: String) = createFlyway(schemaName).info()
  200   
  201       /**
  202        * 특정 스키마의 마이그레이션 검증
  203        *
  204        * @param schemaName 스키마명
  205        */
  206 ▶     fun validate(schemaName: String) {
  207           logger.info { "Validating migrations for schema: $schemaName" }
  208           createFlyway(schemaName).validate()
  209           logger.info { "Validation completed for schema: $schemaName" }
  210       }
  211   
  212       /**
  213        * 특정 스키마의 Baseline 설정
  214        *
  215        * 기존 스키마에 Flyway를 처음 적용할 때 사용
  216        *
  217        * @param schemaName 스키마명
  218        */
  219       fun baseline(schemaName: String) {
  220           logger.info { "Setting baseline for schema: $schemaName" }
  221           createFlyway(schemaName).baseline()
  222           logger.info { "Baseline set for schema: $schemaName" }
```


## 4 / 8

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/dwp/rest/controller/DwpAuditLogController.kt:68** · 심볼 `validate`

```kotlin
   60       private lateinit var configuredPatToken: String
   61   
   62       /**
   63        * Validate PAT token from Authorization header.
   64        *
   65        * @param authToken The Authorization header value
   66        * @throws ETAuthorizationException if token is invalid or missing
   67        */
   68 ▶     private fun validatePatToken(authToken: String?) {
   69           if (authToken.isNullOrBlank()) {
   70               throw ETAuthorizationException("Authorization header is required")
   71           }
   72   
   73           val expectedToken = if (configuredPatToken.isNotBlank()) {
   74               "Bearer $configuredPatToken"
   75           } else {
   76               // Fallback for development
   77               "Bearer bxwd_pat_0200000001_k3mQ8xPv2nL9wR4tY6hJ1sZ7cB5aD0fG2eN4iM6oP8qR2sT4uV6wX8yZ0aZ8"
   78           }
   79   
   80           if (authToken != expectedToken) {
   81               throw ETAuthorizationException("PAT invalid")
   82           }
   83       }
   84   
```


## 5 / 8

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/permission/aspect/IntegratedPermissionCheckAspect.kt:58** · 심볼 `check`

```kotlin
   50           private const val SCOPE_AUTHORITY_PREFIX = "SCOPE_"
   51           private const val PAT_AUTH_ROLE = "ROLE_PAT_AUTH"
   52       }
   53   
   54       /**
   55        * @RequiresIntegratedPermission 애노테이션 처리
   56        */
   57       @Before("@annotation(kr.co.ecoletree.boxwood.permission.annotation.RequiresIntegratedPermission)")
   58 ▶     fun checkIntegratedPermission(joinPoint: JoinPoint) {
   59           logger.debug { "Checking integrated permission for method: ${joinPoint.signature.name}" }
   60   
   61           try {
   62               val auth = SecurityContextHolder.getContext().authentication
   63               if (auth == null || !auth.isAuthenticated) {
   64                   throw AccessDeniedException("Authentication required")
   65               }
   66   
   67               val annotation = getAnnotation(joinPoint)
   68               val isPatAuth = isPatAuthentication(auth)
   69               val userId = SecurityContextHelper.getCurrentUserId()
   70               logger.debug { "IntegratedPermissionCheck - method: ${joinPoint.signature.name}, isPatAuth: $isPatAuth, userId: $userId, authorities: ${auth.authorities.map { it.authority }}" }
   71   
   72               // 1. permissions 체크 (RBAC + PAT 공통) - value와 permissions 둘 다 지원
   73               val permissions = annotation.value.ifEmpty { annotation.permissions }
   74               if (permissions.isNotEmpty()) {
```


## 6 / 8

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/permission/aspect/IntegratedPermissionCheckAspect.kt:205** · 심볼 `check`

```kotlin
  197                   throw PermissionDeniedException(userId, resource.resource, null, resource.action)
  198               }
  199           }
  200       }
  201   
  202       /**
  203        * @RequiresPATScope 배열 체크 (기존 PatScopeCheckAspect 로직 재사용)
  204        */
  205 ▶     private fun checkPatScopes(auth: Authentication, patScopes: Array<RequiresPATScope>) {
  206           val userScopes = extractScopes(auth)
  207   
  208           for (patScope in patScopes) {
  209               val requiredScopes = patScope.value
  210               val mode = patScope.mode
  211   
  212               val hasRequired = when (mode) {
  213                   ScopeCheckMode.ALL_OF -> requiredScopes.all { userScopes.contains(it) }
  214                   ScopeCheckMode.ANY_OF -> requiredScopes.any { userScopes.contains(it) }
  215               }
  216   
  217               if (!hasRequired) {
  218                   val scopeList = requiredScopes.joinToString(", ")
  219                   val modeStr = if (mode == ScopeCheckMode.ALL_OF) "all of" else "any of"
  220                   logger.warn { "PAT scope check failed: required $modeStr [$scopeList], actual: $userScopes" }
  221                   throw AccessDeniedException("Insufficient scope. Required $modeStr: [$scopeList]")
```


## 7 / 8

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/permission/aspect/PermissionCheckAspect.kt:166** · 심볼 `check`

```kotlin
  158                   }
  159               }
  160           }
  161       }
  162   
  163       /**
  164        * 복합 권한들을 체크합니다.
  165        */
  166 ▶     private fun checkComplexPermissions(userId: String, annotation: RequiresPermissions): Boolean {
  167           val operator = annotation.operator
  168   
  169           // Feature 권한들 체크
  170           var featureResult = true
  171           if (annotation.features.isNotEmpty()) {
  172               featureResult = when (operator) {
  173                   RequiresPermissions.LogicalOperator.OR -> {
  174                       // OR 조건: Feature 권한 중 하나라도 만족하면 됨
  175                       annotation.features.any { featureAnnotation ->
  176                           checkFeaturePermissions(userId, featureAnnotation)
  177                       }
  178                   }
  179                   RequiresPermissions.LogicalOperator.AND -> {
  180                       // AND 조건: 모든 Feature 권한을 만족해야 함
  181                       annotation.features.all { featureAnnotation ->
  182                           checkFeaturePermissions(userId, featureAnnotation)
```


## 8 / 8

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/role/service/impl/RoleGroupServiceImpl.kt:403** · 심볼 `validate`

```kotlin
  395               )
  396           }
  397       }
  398   
  399       /**
  400        * 역할 그룹 생성 시 비즈니스 규칙 검증
  401        * @throws IllegalArgumentException 검증 실패 시
  402        */
  403 ▶     private fun validateRoleGroupCreation(request: CreateRoleGroupRequest) {
  404           // 1. 역할명 유효성 검증
  405           if (request.groupName.isBlank() || request.groupName.length < 2 || request.groupName.length > 100) {
  406               throw IllegalArgumentException("역할명은 2-100자 사이여야 합니다")
  407           }
  408   
  409           // 2. 역할명 중복 검증
  410           if (roleGroupRepository.existsByGroupName(request.groupName)) {
  411               throw IllegalArgumentException("이미 존재하는 역할명입니다: ${request.groupName}")
  412           }
  413   
  414           // 3. 유효기간 검증
  415           if (request.validFrom != null && request.validTo != null) {
  416               if (request.validFrom.isAfter(request.validTo)) {
  417                   throw IllegalArgumentException("유효 시작일은 종료일보다 이전이어야 합니다")
  418               }
  419           }
```


---

**8건 각각: 인가 가드인가 · 근거 좌표는 어디인가**

형식 예 — `1 예 (파일:심볼) · 2 아니오 · 3 예 (…) · …` + `열어 본 건수: n`
