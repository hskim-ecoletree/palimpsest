# 8 / 42

**boxwood-packages/boxwood-commons/src/main/kotlin/kr/co/ecoletree/boxwood/commons/audit/aspect/AuditLogAspect.kt:263** · 심볼 `AuditLog`

```kotlin
  255   
  256               // Publish asynchronously
  257               try {
  258                   auditPublisher.publish(event, handler)
  259               } catch (e: Exception) {
  260                   logger.error("Failed to publish audit event", e)
  261               }
  262   
  263 ▶             // Pop domain from change tracking context (supports nested @AuditLog methods)
  264               if (needsChangeTracking) {
  265                   AuditTrackingContext.popDomain()
  266               }
  267           }
  268   
  269           return result
  270       }
  271   
  272       /**
  273        * Build SpEL evaluation context with method parameters
  274        */
  275       private fun buildSpelContext(args: Array<Any?>, signature: MethodSignature): StandardEvaluationContext {
  276           val context = StandardEvaluationContext()
  277           val paramNames = signature.parameterNames
  278   
  279           // Add each parameter by name
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
