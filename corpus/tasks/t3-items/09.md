# 9 / 42

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/code/controller/CodeController.kt:28** · 심볼 `RequiresIntegratedPermission`

```kotlin
   20    */
   21   @RestController
   22   @RequestMapping("/api/v1/code-categories")
   23   class CodeCategoryController(
   24       private val codeCategoryService: CodeCategoryService,
   25       exceptionConverter: ExceptionToResultConverter
   26   ) : BaseController(exceptionConverter) {
   27   
   28 ▶ //    @RequiresIntegratedPermission(["code:view"])
   29       @GetMapping
   30       fun getAllCategories(
   31           @RequestParam(defaultValue = "false") activeOnly: Boolean
   32       ): ResponseEntity<Results> = list {
   33           codeCategoryService.getAll(activeOnly)
   34       }
   35   
   36   //    @RequiresIntegratedPermission(["code:view"])
   37       @GetMapping("/{code}")
   38       fun getCategory(@PathVariable code: String): ResponseEntity<Results> = single {
   39           codeCategoryService.getByCode(code)
   40       }
   41   
   42   //    @RequiresIntegratedPermission(["code:edit"])
   43       @PostMapping
   44       @ResponseStatus(HttpStatus.CREATED)
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
