# 34 / 42

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/common/util/ResultsDsl.kt:59** · 심볼 `validate`

```kotlin
   51   
   52       /**
   53        * 조건을 검증하고 실패 시 FailureResult를 반환합니다.
   54        *
   55        * @param condition 검증 조건
   56        * @param errorBlock 실패 시 에러 정보 제공
   57        * @throws ResultReturnException 조건이 false일 때
   58        */
   59 ▶     inline fun validate(condition: Boolean, errorBlock: () -> Pair<String, String>) {
   60           if (!condition) {
   61               val (code, message) = errorBlock()
   62               failure(code, message, HttpStatus.BAD_REQUEST)
   63               throw ResultReturnException()
   64           }
   65       }
   66   
   67       /**
   68        * 조건을 검증하고 실패 시 커스텀 HttpStatus로 FailureResult를 반환합니다.
   69        */
   70       inline fun validate(condition: Boolean, httpStatus: HttpStatus, errorBlock: () -> Pair<String, String>) {
   71           if (!condition) {
   72               val (code, message) = errorBlock()
   73               failure(code, message, httpStatus)
   74               throw ResultReturnException()
   75           }
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
