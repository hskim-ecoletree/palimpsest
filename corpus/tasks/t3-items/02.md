# 2 / 42

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/auth/pat/service/BoxwoodPatUserResolver.kt:62** · 심볼 `TenantTransactional`

```kotlin
   54   
   55           return UsernamePasswordAuthenticationToken(
   56               authUser,
   57               token.tokenHash,
   58               authUser.authorities
   59           )
   60       }
   61   
   62 ▶     @TenantTransactional(readOnly = true)
   63       override fun isUserActive(token: IPersonalAccessToken): Boolean {
   64           val tenantUser = tenantUserService.findTenantUserById(token.userId)
   65           return tenantUser?.isActive == true && tenantUser.isDeleted == false
   66       }
   67   
   68       override fun setupTenantContext(token: IPersonalAccessToken) {
   69           val currentSchema = TenantContext.getCurrentTenant()
   70           logger.debug("setupTenantContext called - TenantContext already set: {}", currentSchema)
   71       }
   72   
   73       override fun clearTenantContext() {
   74           TenantContext.clear()
   75           logger.debug("TenantContext cleared")
   76       }
   77   
   78       override fun setupTenantContextBeforeValidation(request: HttpServletRequest) {
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
