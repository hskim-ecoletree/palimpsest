# 1 / 42

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/auth/admin/controller/AdminLoginController.kt:65** · 심볼 `AuditLog`

```kotlin
   57       /**
   58        * 관리자 로그인
   59        *
   60        * @param request 로그인 요청 (username, password)
   61        * @param httpRequest HTTP 요청 (IP, UserAgent 추출용)
   62        * @return 로그인 응답 (토큰 포함)
   63        */
   64       @PostMapping("/login")
   65 ▶     @AuditLog(
   66           eventType = AuditEventType.LOGIN_SUCCESS,
   67           targetDomain = AuditTargetDomain.SESSION,
   68           summaryKey = "audit.admin.login_success",
   69           summaryArgs = ["request.username"],
   70           targetNameExpression = "#request.username",
   71           captureRequest = true,
   72           captureResponse = true,
   73           sensitiveFields = ["password", "accessToken", "refreshToken"],
   74           handler = DefaultAuditHandler::class
   75       )
   76       fun login(
   77           @RequestBody request: AdminLoginRequestDto,
   78           httpRequest: HttpServletRequest
   79       ): ResponseEntity<Results> = single {
   80           val clientIp = getClientIpAddress(httpRequest)
   81           val userAgent = httpRequest.getHeader("User-Agent")
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
