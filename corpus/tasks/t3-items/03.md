# 3 / 42

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/auth/config/AccountSecurityProperties.kt:102** · 심볼 `validate`

```kotlin
   94                   "autoUnlockEnabled=$autoUnlockEnabled, " +
   95                   "passwordExpirationDays=$passwordExpirationDays"
   96           }
   97       }
   98   
   99       /**
  100        * 설정값 검증
  101        */
  102 ▶     private fun validate() {
  103           require(maxFailedAttempts >= 1) { "maxFailedAttempts must be at least 1" }
  104           require(lockoutDurationMinutes >= 0) { "lockoutDurationMinutes cannot be negative" }
  105           require(passwordExpirationDays >= 0) { "passwordExpirationDays cannot be negative" }
  106           require(passwordExpirationWarningDays >= 0) { "passwordExpirationWarningDays cannot be negative" }
  107   
  108           if (passwordExpirationDays > 0 && passwordExpirationWarningDays >= passwordExpirationDays) {
  109               logger.warn {
  110                   "passwordExpirationWarningDays ($passwordExpirationWarningDays) should be less than " +
  111                   "passwordExpirationDays ($passwordExpirationDays)"
  112               }
  113           }
  114       }
  115   
  116       override fun toString(): String {
  117           return "AccountSecurityProperties(" +
  118               "maxFailedAttempts=$maxFailedAttempts, " +
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
