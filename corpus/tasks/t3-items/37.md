# 37 / 42

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/auth/core/service/impl/DwpAuthServiceImpl.kt:77** · 심볼 `verify`

```kotlin
   69               .executeWithBodyAsync(request, DwpTokenGenerationResponse::class.java)
   70               .block()
   71           return throwIfFailed(response)
   72       }
   73   
   74       /**
   75        * DWP 토큰 검증
   76        */
   77 ▶     override fun verifyDwpToken(request: DwpTokenVerifyRequest): DwpTokenVerifyResponse {
   78           val fullUrl = "$baseUrl$tokenValidateUrl"
   79           log.debug("=== DWP Token Verify Request ===")
   80           log.debug("Target URL: {}", fullUrl)
   81           log.debug("Token (first 20 chars): {}", request.token.take(20))
   82   
   83           return try {
   84               val response = KotlinHttpClients.post<Unit>(obtainWebClient())
   85                   .url(tokenValidateUrl)
   86                   .build()
   87                   .executeWithBodyAsync(request, DwpTokenVerifyResponse::class.java)
   88                   .block()
   89   
   90               log.debug("=== DWP Token Verify Response ===")
   91               log.debug("Response: {}", response)
   92               throwIfFailed(response)
   93           } catch (e: Exception) {
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
