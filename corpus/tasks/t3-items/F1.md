# F1 — 단건 (시퀀스 41–42 / 42 중 하나)

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/config/TenantStateValidationAspect.kt:23** · 심볼 `TenantTransactional`

```kotlin
   15   import org.aspectj.lang.annotation.Aspect
   16   import org.aspectj.lang.reflect.MethodSignature
   17   import org.springframework.core.Ordered
   18   import org.springframework.core.annotation.AnnotationUtils
   19   import org.springframework.core.annotation.Order
   20   import org.springframework.stereotype.Component
   21   
   22   /**
   23 ▶  * @TenantTransactional의 allowedStates 속성을 처리하는 AOP Aspect
   24    *
   25    * ## 동작 원리
   26    * 1. Spring TransactionInterceptor보다 먼저 실행 (@Order(Ordered.HIGHEST_PRECEDENCE))
   27    * 2. @TenantTransactional 어노테이션에서 allowedStates 추출
   28    * 3. TenantContext에 allowedStates 설정
   29    * 4. 메서드 실행 (Spring TransactionInterceptor → TenantSpringTransactionManager)
   30    * 5. TenantContext에서 allowedStates 정리
   31    *
   32    * ## 실행 순서
   33    * ```
   34    * TenantStateValidationAspect (@Order(HIGHEST_PRECEDENCE))
   35    *   ↓ (allowedStates → TenantContext)
   36    * Spring TransactionInterceptor
   37    *   ↓
   38    * TenantSpringTransactionManager.doBegin()
   39    *   ↓ (TenantContext.getAllowedStates() 사용)
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
