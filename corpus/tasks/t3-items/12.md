# 12 / 42

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/code/controller/CodeController.kt:45** · 심볼 `TenantTransactional`

```kotlin
   37       @GetMapping("/{code}")
   38       fun getCategory(@PathVariable code: String): ResponseEntity<Results> = single {
   39           codeCategoryService.getByCode(code)
   40       }
   41   
   42   //    @RequiresIntegratedPermission(["code:edit"])
   43       @PostMapping
   44       @ResponseStatus(HttpStatus.CREATED)
   45 ▶     @TenantTransactional
   46       @AuditLog(
   47           eventType = AuditEventType.CREATE,
   48           targetDomain = AuditTargetDomain.CODE_CATEGORY,
   49           summaryKey = "audit.code_category.created",
   50           summaryArgs = ["request.name"],
   51           targetIdExpression = "#result.body.data.code",
   52           targetNameExpression = "#request.name",
   53           captureRequest = true,
   54           captureResponse = true,
   55           handler = DefaultAuditHandler::class
   56       )
   57       fun createCategory(@RequestBody request: CreateCodeCategoryRequest): ResponseEntity<Results> = created {
   58           codeCategoryService.create(request)
   59       }
   60   
   61   //    @RequiresIntegratedPermission(["code:edit"])
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
