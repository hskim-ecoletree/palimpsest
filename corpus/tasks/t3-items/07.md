# 7 / 42

**boxwood-packages/boxwood-commons/src/main/kotlin/kr/co/ecoletree/boxwood/commons/audit/aspect/AuditLogAspect.kt:173** · 심볼 `TenantTransactional`

```kotlin
  165               exception = e
  166               throw e
  167           } finally {
  168               // For suspend functions, we need to capture after state in a callback
  169               // because the method hasn't completed yet when we reach this finally block.
  170               // For now, we capture after state synchronously but note this limitation.
  171               //
  172               // IMPORTANT: For suspend functions with change tracking, the service layer's
  173 ▶             // @TenantTransactional must complete BEFORE the controller method returns.
  174               // This typically works for non-suspend service methods called from suspend controllers.
  175   
  176               val durationMs = System.currentTimeMillis() - startTime
  177   
  178               // Add result to SpEL context for post-execution expressions
  179               spelContext.setVariable("result", result)
  180   
  181               // Capture after state
  182               // For suspend functions with Spring AOP, proceed() should wait for completion,
  183               // but the service transaction might not have committed yet.
  184               logger.info("[CHANGE_TRACKING] Capturing after state - result type: ${result?.javaClass?.simpleName}, isSuspend=$isSuspendFunction")
  185   
  186               val afterStateRaw = if (auditLog.captureAfterState && exception == null && resourceId != null) {
  187                   try {
  188                       // First, try to extract from the result (works for both suspend and non-suspend)
  189                       var afterState: Any? = null
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
