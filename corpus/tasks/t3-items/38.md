# 38 / 42

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/auth/core/service/impl/TokenStorageServiceImpl.kt:191** · 심볼 `validate`

```kotlin
  183                   "(processed: tokens=$totalTokensDeleted, blacklist=$totalBlacklistDeleted)",
  184                   e
  185               )
  186               throw e
  187           }
  188       }
  189   
  190       @TenantTransactional(readOnly = true)
  191 ▶     override fun validateToken(token: String): Boolean {
  192           if (isTokenBlacklisted(token)) {
  193               return false
  194           }
  195   
  196           val tokenHash = resolveAndHash(token)
  197   
  198           if (userTokenRepository.isTokenRevoked(tokenHash)) {
  199               return false
  200           }
  201   
  202           val resolvedToken = JwtUtils.resolveBearerToken(token) ?: token
  203           return JwtUtils.isTokenValid(resolvedToken, jwtSecret)
  204       }
  205   
  206       @TenantTransactional(readOnly = true)
  207       override fun getUserActiveTokens(userId: Long, tokenType: TokenType): List<UserToken> {
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
