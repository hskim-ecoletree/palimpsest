# 36 / 42

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/automation/dashboard/service/DashboardService.kt:255** · 심볼 `TenantTransactional`

```kotlin
  247               topByExecution = topByExecution,
  248               topBySlowest = topBySlowest
  249           )
  250       }
  251   
  252       /**
  253        * External Task Queue 현황 조회
  254        */
  255 ▶     @TenantTransactional
  256       fun getExternalTaskQueue(): List<ExternalTaskQueueDto> {
  257           return dashboardRepository.getExternalTaskQueue()
  258       }
  259   
  260       /**
  261        * 내 대시보드 조회
  262        */
  263       @TenantTransactional
  264       fun getMyDashboard(startDate: LocalDate, endDate: LocalDate): MyDashboardDto {
  265           val userId = AuditContextHolder.getCurrentUserId()
  266               ?: throw IllegalStateException("User ID not found in context")
  267   
  268           val summary = dashboardRepository.getMyDashboardSummary(userId, startDate, endDate)
  269           val topProcesses = dashboardRepository.getMyTopProcesses(userId, startDate, endDate, 5)
  270   
  271           // Incident 목록과 총 개수 조회
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
