# 29 / 42

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/automation/llm/service/McpToolConfigService.kt:59** · 심볼 `TenantTransactional`

```kotlin
   51   
   52       @TenantTransactional(readOnly = true)
   53       fun getConfigById(id: Long): McpToolConfigResponseDto {
   54           val data = mcpToolConfigRepository.findById(id)
   55               ?: throw ETNotFoundException("MCP Tool Config not found with ID: $id")
   56           return toConfigResponseDto(data)
   57       }
   58   
   59 ▶     @TenantTransactional(readOnly = true)
   60       fun getConfigWithTools(id: Long): McpToolConfigResponseDto {
   61           val (config, tools) = mcpToolConfigRepository.findWithTools(id)
   62           if (config == null) {
   63               throw ETNotFoundException("MCP Tool Config not found with ID: $id")
   64           }
   65           return toConfigResponseDto(config, tools.map { toToolResponseDto(it) })
   66       }
   67   
   68       @TenantTransactional(readOnly = true)
   69       fun findConfigs(search: McpToolConfigSearchDto): List<McpToolConfigResponseDto> {
   70           return mcpToolConfigRepository.findAll(search).map { toConfigResponseDto(it) }
   71       }
   72   
   73       @TenantTransactional
   74       fun syncTools(configId: Long, tools: List<McpToolSyncDto>): List<McpToolResponseDto> {
   75           mcpToolConfigRepository.findById(configId)
```

---

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다.*

**이것이 인가 가드인가 · 근거 좌표는 어디인가**
(가드 예/아니오 + 근거 좌표(파일·심볼) 또는 "없음")
