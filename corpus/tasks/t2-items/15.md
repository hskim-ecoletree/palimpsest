# 15 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/auth/systempat/filter/SystemPatAuthenticationFilter.kt:42** · 심볼 `SystemPatAuthenticationFilter`

```kotlin
   34    * ## 특징
   35    * - **테넌트 컨텍스트 불필요**: X-TENANT-ID 헤더 없이 동작
   36    * - **Shared Schema 사용**: 시스템 PAT는 Shared Schema에 저장됨
   37    * - **OWNER 전용**: TenantAdmin 중 OWNER 역할만 사용 가능
   38    *
   39    * @see SystemPatUserResolver
   40    * @see PatTokenValidator
   41    */
   42 ▶ class SystemPatAuthenticationFilter(
   43       private val systemPatValidator: PatTokenValidator<SystemPersonalAccessToken>,
   44       private val systemPatUserResolver: SystemPatUserResolver
   45   ) : OncePerRequestFilter() {
   46
   47       private val log = LoggerFactory.getLogger(SystemPatAuthenticationFilter::class.java)
   48
   49       companion object {
   50           private const val AUTHORIZATION_HEADER = "Authorization"
   51           private const val BEARER_PREFIX = "Bearer "
   52       }
   53
   54       override fun doFilterInternal(
   55           request: HttpServletRequest,
   56           response: HttpServletResponse,
   57           filterChain: FilterChain
   58       ) {
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/auth/systempat/filter/SystemPatAuthenticationFilter.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **15행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`16.md`](16.md)
