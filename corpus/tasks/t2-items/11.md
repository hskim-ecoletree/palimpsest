# 11 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/permission/aspect/IntegratedPermissionCheckAspect.kt:185** · 심볼 `checkResourcePermissions`

```kotlin
  177                   throw PermissionDeniedException(userId, permissionList)
  178               }
  179           }
  180       }
  181
  182       /**
  183        * @HasResourcePermission 배열 체크 (기존 PermissionCheckAspect 로직 재사용)
  184        */
  185 ▶     private fun checkResourcePermissions(userId: String, resources: Array<HasResourcePermission>) {
  186           for (resource in resources) {
  187               val hasPermission = permissionChecker.hasResourceTypePermission(
  188                   userId,
  189                   resource.resource,
  190                   resource.action
  191               )
  192
  193               if (!hasPermission) {
  194                   logger.warn {
  195                       "Resource permission denied for user '$userId': ${resource.resource}:${resource.action}"
  196                   }
  197                   throw PermissionDeniedException(userId, resource.resource, null, resource.action)
  198               }
  199           }
  200       }
  201
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/permission/aspect/IntegratedPermissionCheckAspect.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **11행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`12.md`](12.md)
