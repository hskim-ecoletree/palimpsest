# 29 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/code/tracking/CodeChangeTrackingProvider.kt:64** · 심볼 `TenantTransactional`

```kotlin
   56    *     captureAfterState = true,
   57    *     // ...
   58    * )
   59    * fun updateCode(@PathVariable code: String, ...)
   60    * ```
   61    *
   62    * Note: Uses MultiTenantTransactionManager to create transaction context
   63    * because this provider is called from AOP aspect BEFORE the service's
   64 ▶  * @TenantTransactional is entered.
   65    *
   66    * @property codeRepository Repository for fetching code data
   67    * @property transactionManager Transaction manager for database operations
   68    *
   69    * @see ChangeTrackingProvider
   70    */
   71   @Component
   72   class CodeChangeTrackingProvider(
   73       private val codeRepository: CodeRepository,
   74       private val transactionManager: MultiTenantTransactionManager
   75   ) : ChangeTrackingProvider {
   76
   77       /**
   78        * Domain name: "Code"
   79        *
   80        * This must match the `targetDomain.code` value in @AuditLog annotations.
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/code/tracking/CodeChangeTrackingProvider.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **29행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`30.md`](30.md)
