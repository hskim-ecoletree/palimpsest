# 22 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/auth/pat/repository/ExposedPatTokenRepository.kt:199** · 심볼 `TenantTransactional`

```kotlin
  191                   this[PersonalAccessTokenScopeTable.tokenId] = savedToken.id!!
  192                   this[PersonalAccessTokenScopeTable.permCode] = scope
  193               }
  194           }
  195
  196           return savedToken.withScopes(scopes)
  197       }
  198
  199 ▶     @TenantTransactional
  200       override fun updateLastUsed(tokenId: Long, lastUsedAt: Instant, lastUsedIp: String?) {
  201           PersonalAccessTokenTable.update({ PersonalAccessTokenTable.id eq tokenId }) {
  202               it[PersonalAccessTokenTable.lastUsedAt] = lastUsedAt
  203               it[PersonalAccessTokenTable.lastUsedIp] = lastUsedIp
  204           }
  205       }
  206
  207       // ============================================================
  208       // 폐기 메서드
  209       // ============================================================
  210
  211       @TenantTransactional
  212       override fun revoke(tokenId: Long, reason: String?, revokedAt: Instant) {
  213           PersonalAccessTokenTable.update({ PersonalAccessTokenTable.id eq tokenId }) {
  214               it[isRevoked] = true
  215               it[PersonalAccessTokenTable.revokedAt] = revokedAt
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/auth/pat/repository/ExposedPatTokenRepository.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **22행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`23.md`](23.md)
