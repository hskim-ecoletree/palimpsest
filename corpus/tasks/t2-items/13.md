# 13 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/auth/filter/RateLimitFilter.kt:55** · 심볼 `RateLimitFilter`

```kotlin
   47    * - X-RateLimit-Remaining: 남은 요청 횟수
   48    * - Retry-After: 다음 요청까지 대기 시간 (초)
   49    *
   50    * @see RateLimitService
   51    * @see RateLimitProperties
   52    */
   53   @Component
   54   @Order(Ordered.HIGHEST_PRECEDENCE + 10) // JwtAuthenticationFilter 이후에 실행
   55 ▶ class RateLimitFilter(
   56       private val rateLimitService: RateLimitService,
   57       private val rateLimitProperties: RateLimitProperties,
   58       private val securityEventService: SecurityEventService,
   59       private val internalAuditService: InternalAuditService,
   60       private val boxwoodTenantRepository: BoxwoodTenantRepository
   61   ) : OncePerRequestFilter() {
   62
   63       companion object {
   64           const val HEADER_RATE_LIMIT_REMAINING = "X-RateLimit-Remaining"
   65           const val HEADER_RATE_LIMIT_LIMIT = "X-RateLimit-Limit"
   66           const val HEADER_RETRY_AFTER = "Retry-After"
   67       }
   68
   69       override fun doFilterInternal(
   70           request: HttpServletRequest,
   71           response: HttpServletResponse,
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/auth/filter/RateLimitFilter.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **13행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`14.md`](14.md)
