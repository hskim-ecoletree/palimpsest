# 16 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/common/util/ModernDateUtil.kt:284** · 심볼 `checkExcludeStartEndDate`

```kotlin
  276    * @param fmtDate
  277    * @param formatter
  278    * @return
  279    */
  280   fun parse2LocalDateTime(fmtDate: String, formatter: DateTimeFormatter): LocalDateTime {
  281       return LocalDateTime.parse(fmtDate, formatter)
  282   }
  283
  284 ▶ private fun checkExcludeStartEndDate(option: BetweenOption): BooleanArray {
  285       // Kotlin 2.2.0+: when 표현식을 직접 반환하여 간결성 향상
  286       return when (option) {
  287           BetweenOption.INCLUDE_START_DATE_AND_INCLUDE_END_DATE -> booleanArrayOf(false, false)
  288           BetweenOption.INCLUDE_START_DATE_AND_EXCLUDE_END_DATE -> booleanArrayOf(false, true)
  289           BetweenOption.EXCLUDE_START_DATE_AND_INCLUDE_END_DATE -> booleanArrayOf(true, false)
  290           BetweenOption.EXCLUDE_START_DATE_AND_EXCLUDE_END_DATE -> booleanArrayOf(true, true)
  291       }
  292   }
  293
  294   enum class BetweenOption {
  295       INCLUDE_START_DATE_AND_INCLUDE_END_DATE,
  296       INCLUDE_START_DATE_AND_EXCLUDE_END_DATE,
  297       EXCLUDE_START_DATE_AND_INCLUDE_END_DATE,
  298       EXCLUDE_START_DATE_AND_EXCLUDE_END_DATE
  299   }
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/common/util/ModernDateUtil.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **16행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`17.md`](17.md)
