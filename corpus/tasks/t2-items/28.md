# 28 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/code/controller/CodeController.kt:288** · 심볼 `TenantTransactional`

```kotlin
  280       @GetMapping("/children")
  281       fun getRootCodes(): ResponseEntity<Results> = list {
  282           codeService.getByParentCode(null)
  283       }
  284
  285   //    @RequiresIntegratedPermission(["code:edit"])
  286       @PostMapping
  287       @ResponseStatus(HttpStatus.CREATED)
  288 ▶     @TenantTransactional
  289       @AuditLog(
  290           eventType = AuditEventType.CREATE,
  291           targetDomain = AuditTargetDomain.CODE,
  292           summaryKey = "audit.code.created",
  293           summaryArgs = ["request.name"],
  294           targetIdExpression = "#result.body.data.code",
  295           targetNameExpression = "#request.name",
  296           captureRequest = true,
  297           captureResponse = true,
  298           handler = DefaultAuditHandler::class
  299       )
  300       fun createCode(@RequestBody request: CreateCodeRequest): ResponseEntity<Results> = created {
  301           codeService.create(request)
  302       }
  303
  304   //    @RequiresIntegratedPermission(["code:edit"])
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/code/controller/CodeController.kt`*

---

1. **무엇인가?**
   · `가드(인가)` — 권한 보유 여부를 검사해 없으면 거부한다 (403)
   · `가드(인증)` — 신원·세션·토큰을 확립하거나 검증한다. 실패하면 거부한다 (401)
   · `아니오` — 둘 다 아니다 (형식 검증 · 로깅 · 트랜잭션 · 성능 …)
   · `모름` — 결론을 못 내리겠다. **이것도 1급 답이다**
2. **근거 좌표는?**  실제로 그 판정이 내려지는 파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **28행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`29.md`](29.md)
