# 32 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/rbac/annotation/HasResourcePermission.kt:22** · 심볼 `HasResourcePermission`

```kotlin
   14    * 지정된 액션 권한을 가지고 있는지 확인합니다.
   15    *
   16    * ## 사용 예시
   17    *
   18    * ```kotlin
   19    * @HasResourcePermission(resource = "PROCESS", action = "READ")
   20    * fun getProcess(id: Long): ProcessResponse
   21    *
   22 ▶  * @HasResourcePermission(resource = "PROCESS", action = "DELETE")
   23    * fun deleteProcess(id: Long)
   24    * ```
   25    *
   26    * ## Resource Types
   27    * - PROCESS: 프로세스 리소스
   28    * - TASK: 태스크 리소스
   29    * - FORM: 폼 리소스
   30    * - DOCUMENT: 문서 리소스
   31    * - IT_INFRA: IT 인프라 리소스
   32    *
   33    * ## Actions
   34    * - READ: 조회 권한
   35    * - WRITE: 생성/수정 권한
   36    * - DELETE: 삭제 권한
   37    * - EXECUTE: 실행 권한
   38    *
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/rbac/annotation/HasResourcePermission.kt`*

---

1. **무엇인가?**
   · `가드(인가)` — 권한 보유 여부를 검사해 없으면 거부한다 (403)
   · `가드(인증)` — 신원·세션·토큰을 확립하거나 검증한다. 실패하면 거부한다 (401)
   · `아니오` — 둘 다 아니다 (형식 검증 · 로깅 · 트랜잭션 · 성능 …)
   · `모름` — 결론을 못 내리겠다. **이것도 1급 답이다**
2. **근거 좌표는?**  실제로 그 판정이 내려지는 파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **32행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`33.md`](33.md)
