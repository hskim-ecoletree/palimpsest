# 18 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/common/filter/LoginTenantFilter.kt:53** · 심볼 `LoginTenantFilter`

```kotlin
   45    *
   46    * ## Note
   47    * - 이 필터는 MDCLoggingFilter보다 먼저 실행되어야 함 (Order.HIGHEST_PRECEDENCE + 1)
   48    * - request body를 캐싱하여 여러 번 읽을 수 있게 함
   49    * - RefreshTokenTenantFilter와 동일한 Order를 사용하여 일관성 유지
   50    */
   51   @Component
   52   @Order(Ordered.HIGHEST_PRECEDENCE + 1)
   53 ▶ class LoginTenantFilter(
   54       private val boxwoodTenantRepository: BoxwoodTenantRepository
   55   ) : OncePerRequestFilter() {
   56
   57       private val log = LoggerFactory.getLogger(LoginTenantFilter::class.java)
   58       private val objectMapper = jacksonObjectMapper()
   59
   60       companion object {
   61           private val LOGIN_PATH_PATTERNS = listOf(
   62               "/authenticate/v1/login",
   63               "/api/v1/auth/login"
   64           )
   65       }
   66
   67       /**
   68        * Filter initialization logging for debugging filter registration.
   69        */
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/common/filter/LoginTenantFilter.kt`*

---

1. **무엇인가?**
   · `가드(인가)` — 권한 보유 여부를 검사해 없으면 거부한다 (403)
   · `가드(인증)` — 신원·세션·토큰을 확립하거나 검증한다. 실패하면 거부한다 (401)
   · `아니오` — 둘 다 아니다 (형식 검증 · 로깅 · 트랜잭션 · 성능 …)
   · `모름` — 결론을 못 내리겠다. **이것도 1급 답이다**
2. **근거 좌표는?**  실제로 그 판정이 내려지는 파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **18행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`19.md`](19.md)
