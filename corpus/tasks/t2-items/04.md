# 4 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/auth/pat/service/PersonalAccessTokenService.kt:83** · 심볼 `validateScopes`

```kotlin
   75
   76       /**
   77        * 요청된 스코프가 사용자 권한 범위 내인지 검증
   78        *
   79        * @param userId 사용자 ID
   80        * @param requestedScopes 요청 스코프
   81        * @return 유효한 스코프 목록
   82        */
   83 ▶     fun validateScopes(userId: Long, requestedScopes: Set<String>): Set<String>
   84
   85       /**
   86        * 시스템 토큰 생성 (스코프 검증 없음)
   87        *
   88        * 테넌트 부트스트랩, 시스템 초기화 등 권한 검증을 우회해야 하는 경우에 사용합니다.
   89        * 요청된 모든 스코프가 그대로 적용됩니다.
   90        *
   91        * @param userId 사용자 ID
   92        * @param tenantId 테넌트 ID
   93        * @param request 토큰 생성 요청
   94        * @return 생성된 토큰 (원본 토큰은 이 응답에서만 반환)
   95        */
   96       fun createSystemToken(userId: Long, tenantId: String, request: CreatePatRequest): CreatePatResponse
   97
   98       /**
   99        * 사전 정의된 토큰 생성 (외부 서비스 연동용)
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/auth/pat/service/PersonalAccessTokenService.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **4행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`05.md`](05.md)
