# 20 / 33

**boxwood-packages/boxwood-commons/src/main/kotlin/kr/co/ecoletree/boxwood/commons/audit/annotation/AuditLog.kt:24** · 심볼 `AuditLog`

```kotlin
   16   /**
   17    * Annotation for declarative audit logging on controller methods.
   18    *
   19    * When applied to a method, the AuditLogAspect will intercept the method
   20    * execution and publish an audit event asynchronously.
   21    *
   22    * ## Basic Usage (v1.3.1+)
   23    * ```kotlin
   24 ▶  * @AuditLog(
   25    *     eventType = AuditEventType.CREATE,
   26    *     targetDomain = AuditTargetDomain.PROCESS_VERSION,
   27    *     summaryKey = "audit.process.created",
   28    *     summaryArgs = ["request.name", "response.id"],
   29    *     targetIdExpression = "#result.body.data.id",
   30    *     targetNameExpression = "#request.managementKey",
   31    *     captureRequest = true,
   32    *     captureResponse = true,
   33    *     handler = ProcessVersionAuditHandler::class
   34    * )
   35    * @PostMapping("/process")
   36    * suspend fun createProcess(@RequestBody request: CreateProcessRequest): ResponseEntity<Results>
   37    * ```
   38    *
   39    * ## Before/After State Capture (for UPDATE operations)
   40    * ```kotlin
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/boxwood-packages show 2e9198716796:boxwood-commons/src/main/kotlin/kr/co/ecoletree/boxwood/commons/audit/annotation/AuditLog.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **20행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`21.md`](21.md)
