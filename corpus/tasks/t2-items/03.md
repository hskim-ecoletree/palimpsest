# 3 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/auth/core/service/impl/DwpAuthServiceImpl.kt:298** · 심볼 `validateDwpTokenFromRequest`

```kotlin
  290           } catch (e: Exception) {
  291               null
  292           }
  293       }
  294
  295       /**
  296        * HTTP 요청에서 DWP_TOKEN 쿠키를 추출하고 검증
  297        */
  298 ▶     override fun validateDwpTokenFromRequest(request: HttpServletRequest): DwpTokenValidationResult {
  299           val dwpToken = extractDwpTokenFromCookie(request)
  300               ?: throw DomainException(UnauthorizedError(messageArgs = arrayOf("DWP token missing in cookies")))
  301
  302           return validateDwpTokenString(dwpToken)
  303       }
  304
  305       /**
  306        * DWP 토큰 검증 (토큰 문자열만으로)
  307        */
  308       override fun validateDwpTokenString(token: String): DwpTokenValidationResult {
  309           if (token.isBlank()) {
  310               throw DomainException(UnauthorizedError(messageArgs = arrayOf("DWP token is empty")))
  311           }
  312
  313           val dwpVerifyResponse = verifyDwpToken(DwpTokenVerifyRequest(token))
  314
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/auth/core/service/impl/DwpAuthServiceImpl.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **3행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`04.md`](04.md)
