# 7 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/common/util/ValidationUtil.kt:140** · 심볼 `requireNonNullValue`

```kotlin
  132    * <h2>맵 요소 키/값 null 검사 및 예외 발생</h2>
  133    * <p>맵에서 특정 키에 해당하는 값이 null이면 예외를 발생시킵니다.</p>
  134    *
  135    * @param map          검사할 맵
  136    * @param key          검사할 키
  137    * @param errorMessage 에러 메시지
  138    * @throws ETServiceFailureException 값이 null인 경우
  139    */
  140 ▶ fun requireNonNullValue(map: Map<*, *>?, key: Any, errorMessage: String) {
  141       if (!isNotNullOrEmpty(map) || !map!!.containsKey(key) || map[key] == null) {
  142           throw ETServiceFailureException.badRequestWithFailureMessage(errorMessage)
  143       }
  144   }
  145
  146   /**
  147    * <h2>리스트 모든 요소 null 검사 및 예외 발생</h2>
  148    * <p>리스트의 요소 중 하나라도 null이면 예외를 발생시킵니다.</p>
  149    *
  150    * @param collection   검사할 컬렉션
  151    * @param errorMessage 에러 메시지
  152    * @throws ETServiceFailureException 하나라도 null인 경우
  153    */
  154   fun requireNoNullElements(collection: Collection<*>?, errorMessage: String) {
  155       if (!isNotNullOrEmpty(collection) || collection!!.any { it == null }) {
  156           throw ETServiceFailureException.badRequestWithFailureMessage(errorMessage)
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/common/util/ValidationUtil.kt`*

---

1. **무엇인가?**
   · `가드(인가)` — 권한 보유 여부를 검사해 없으면 거부한다 (403)
   · `가드(인증)` — 신원·세션·토큰을 확립하거나 검증한다. 실패하면 거부한다 (401)
   · `아니오` — 둘 다 아니다 (형식 검증 · 로깅 · 트랜잭션 · 성능 …)
   · `모름` — 결론을 못 내리겠다. **이것도 1급 답이다**
2. **근거 좌표는?**  실제로 그 판정이 내려지는 파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **7행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`08.md`](08.md)
