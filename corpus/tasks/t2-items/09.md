# 9 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/config/MultiTenantTransactionManager.kt:206** · 심볼 `validateTenantState`

```kotlin
  198        * ACTIVE 또는 INACTIVE 상태의 테넌트만 허용
  199        * SUSPENDED, DELETED 상태는 TenantNotActiveException 발생
  200        *
  201        * 참고: INACTIVE 상태에서도 관리자 관리 등의 작업은 가능합니다.
  202        *
  203        * @param schema 테넌트 스키마명
  204        * @throws TenantNotActiveException 테넌트가 SUSPENDED 또는 DELETED 상태인 경우
  205        */
  206 ▶     private fun validateTenantState(schema: String) {
  207           val tenant = tenantProvisioningService.findByTenantSchemaAnyState(schema) ?: run {
  208               logger.warn("테넌트를 찾을 수 없음: schema={}", schema)
  209               return
  210           }
  211
  212           // ACTIVE 또는 INACTIVE 상태는 허용
  213           val allowedStates = listOf(TenantState.ACTIVE.code, TenantState.INACTIVE.code)
  214           if (tenant.tenantState !in allowedStates) {
  215               logger.error(
  216                   "차단된 테넌트 상태 접근 시도: tenantKey={}, schema={}, state={}",
  217                   tenant.tenantKey, schema, tenant.tenantState
  218               )
  219
  220               // Phase B: 메트릭 - 검증 실패 기록
  221               tenantMetrics.recordValidationFailure(schema, "invalid_state")
  222
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/config/MultiTenantTransactionManager.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **9행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`10.md`](10.md)
