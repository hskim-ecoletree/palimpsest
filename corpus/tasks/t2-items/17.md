# 17 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/automation/dashboard/model/dto/DashboardDto.kt:22** · 심볼 `ProcessSummaryDto`

```kotlin
   14
   15   // ============================================
   16   // 1. KPI Summary (상단 카드)
   17   // ============================================
   18
   19   /**
   20    * Process 현황 요약
   21    */
   22 ▶ data class ProcessSummaryDto(
   23       /** 총 Process 수 */
   24       val totalCount: Long,
   25       /** DEPLOYED Process 수 */
   26       val deployedCount: Long,
   27       /** SAVED Process 수 */
   28       val savedCount: Long,
   29       /** ACTIVE Process 수 */
   30       val activeCount: Long,
   31       /** INACTIVE Process 수 */
   32       val inactiveCount: Long
   33   )
   34
   35   /**
   36    * ProcessInstance 실행 현황 요약
   37    */
   38   data class ProcessInstanceSummaryDto(
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/automation/dashboard/model/dto/DashboardDto.kt`*

---

1. **무엇인가?**
   · `가드(인가)` — 권한 보유 여부를 검사해 없으면 거부한다 (403)
   · `가드(인증)` — 신원·세션·토큰을 확립하거나 검증한다. 실패하면 거부한다 (401)
   · `아니오` — 둘 다 아니다 (형식 검증 · 로깅 · 트랜잭션 · 성능 …)
   · `모름` — 결론을 못 내리겠다. **이것도 1급 답이다**
2. **근거 좌표는?**  실제로 그 판정이 내려지는 파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **17행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`18.md`](18.md)
