# 23 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/automation/adaptive/cards/service/AdaptiveCardService.kt:31** · 심볼 `TenantTransactional`

```kotlin
   23               name = request.name,
   24               description = request.description,
   25               cardPayload = request.cardPayload,
   26               sampleData = request.sampleData,
   27               userId = null
   28           )
   29       }
   30
   31 ▶     @TenantTransactional
   32       fun updateAdaptiveCard(id: Long, request: AdaptiveCardCreateRequestDto): AdaptiveCardResponseDto {
   33           return adaptiveCardRepository.updateAdaptiveCard(
   34               id = id,
   35               name = request.name,
   36               description = request.description,
   37               cardPayload = request.cardPayload,
   38               sampleData = request.sampleData,
   39               userId = null
   40           ) ?: throw ETNotFoundException("Adaptive card not found with ID: $id")
   41       }
   42
   43       @TenantTransactional
   44       fun getAdaptiveCardById(id: Long): AdaptiveCardResponseDto {
   45           return adaptiveCardRepository.findById(id)
   46               ?: throw ETNotFoundException("Adaptive card not found with ID: $id")
   47       }
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/automation/adaptive/cards/service/AdaptiveCardService.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **23행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`24.md`](24.md)
