# 33 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/shared/i18n/controller/I18nMessageController.kt:130** · 심볼 `AuthenticationPrincipal`

```kotlin
  122       fun updateMessage(
  123           @PathVariable id: Long,
  124           @RequestBody request: UpdateMessageResourceRequest
  125       ): ResponseEntity<Results> {
  126           return single {
  127               service.update(
  128                   id = id,
  129                   request = request,
  130 ▶                 updatedBy = null // TODO: @AuthenticationPrincipal로 현재 사용자 ID 가져오기
  131               )
  132           }
  133       }
  134
  135       /**
  136        * 개별 메시지 삭제 API (Soft Delete)
  137        *
  138        * ID로 특정 메시지를 삭제합니다 (논리 삭제).
  139        *
  140        * @param id 메시지 ID
  141        */
  142       @DeleteMapping("/messages/{id}")
  143       @AuditLog(
  144           eventType = AuditEventType.DELETE,
  145           targetDomain = AuditTargetDomain.I18N_MESSAGE,
  146           summaryKey = "audit.i18n_message.deleted",
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/shared/i18n/controller/I18nMessageController.kt`*

---

1. **무엇인가?**
   · `가드(인가)` — 권한 보유 여부를 검사해 없으면 거부한다 (403)
   · `가드(인증)` — 신원·세션·토큰을 확립하거나 검증한다. 실패하면 거부한다 (401)
   · `아니오` — 둘 다 아니다 (형식 검증 · 로깅 · 트랜잭션 · 성능 …)
   · `모름` — 결론을 못 내리겠다. **이것도 1급 답이다**
2. **근거 좌표는?**  실제로 그 판정이 내려지는 파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **33행**에 적는다.
앞 건으로 돌아가지 않는다.

**마지막이다.** 판정표를 저장하고 `docs/gates/preflight.md` §T2 에 결과를 적는다.
