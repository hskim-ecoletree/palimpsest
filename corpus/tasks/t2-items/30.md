# 30 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/dwp/statistics/service/DwpDailyUsageStatisticsService.kt:52** · 심볼 `TenantTransactional`

```kotlin
   44       companion object {
   45           private val DATE_FORMATTER = DateTimeFormatter.ofPattern("yyyyMMdd")
   46       }
   47
   48       /**
   49        * 일별 시스템 사용량 통계를 조회합니다.
   50        *
   51        * TenantHeaderInjectionFilter가 RequestBody의 company를 X-Company 헤더로 주입하고,
   52 ▶      * BoxwoodPatUserResolver가 TenantContext를 설정하므로, @TenantTransactional만 사용합니다.
   53        *
   54        * @param criteria 조회 조건 (targetDate, company, page)
   55        * @return 페이징된 일별 사용량 통계
   56        */
   57       @TenantTransactional(readOnly = true)
   58       fun getDailyUsageStatistics(criteria: DwpBoxwoodDailyUsageCriteria): PageData<DwpBoxwoodDailyUsageStatistics> {
   59           logger.info { "일별 사용량 통계 조회 요청: targetDate=${criteria.targetDate}, company=${criteria.company}" }
   60
   61           // 1. targetDate 파싱
   62           val targetDate = try {
   63               LocalDate.parse(criteria.targetDate, DATE_FORMATTER)
   64           } catch (e: Exception) {
   65               logger.error(e) { "날짜 파싱 오류: targetDate=${criteria.targetDate}" }
   66               return PageData.empty()
   67           }
   68
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/dwp/statistics/service/DwpDailyUsageStatisticsService.kt`*

---

1. **무엇인가?**
   · `가드(인가)` — 권한 보유 여부를 검사해 없으면 거부한다 (403)
   · `가드(인증)` — 신원·세션·토큰을 확립하거나 검증한다. 실패하면 거부한다 (401)
   · `아니오` — 둘 다 아니다 (형식 검증 · 로깅 · 트랜잭션 · 성능 …)
   · `모름` — 결론을 못 내리겠다. **이것도 1급 답이다**
2. **근거 좌표는?**  실제로 그 판정이 내려지는 파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **30행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`31.md`](31.md)
