# 5 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/auth/web/controller/AuthenticationController.kt:477** · 심볼 `validateToken`

```kotlin
  469               log.error("Failed to setup TenantContext from refresh token: {}", e.message)
  470           }
  471       }
  472
  473       /**
  474        * 토큰 검증
  475        */
  476       @PostMapping("/validate")
  477 ▶     fun validateToken(
  478           @RequestHeader("Authorization") token: String
  479       ): ResponseEntity<Results> = single {
  480           val isValid = authenticationService.validateToken(token)
  481           mapOf("valid" to isValid)
  482       }
  483
  484       /**
  485        * 현재 사용자 정보 조회
  486        * Spring Security 인증 기반 (JWT만 사용)
  487        * 조직/역할/테넌트/권한 정보 포함
  488        *
  489        * JWT 클레임 최소화 정책에 따라 featurePermissions와 isAdmin은 이 엔드포인트에서 반환됩니다.
  490        */
  491       @GetMapping("/me")
  492       fun getCurrentUser(httpRequest: HttpServletRequest): ResponseEntity<Results> = single {
  493           // 1. Spring Security 인증 확인 (JWT 기반)
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/auth/web/controller/AuthenticationController.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **5행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`06.md`](06.md)
