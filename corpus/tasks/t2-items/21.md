# 21 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/common/util/ValidationUtil.kt:268** · 심볼 `requireCollectionSize`

```kotlin
  260    * <p>컬렉션의 크기가 지정된 범위 내에 있는지 검증합니다.</p>
  261    *
  262    * @param collection   검증할 컬렉션
  263    * @param minSize      최소 크기
  264    * @param maxSize      최대 크기
  265    * @param errorMessage 에러 메시지
  266    * @throws ETServiceFailureException 컬렉션이 null이거나 크기가 범위를 벗어나는 경우
  267    */
  268 ▶ fun requireCollectionSize(collection: Collection<*>?, minSize: Int, maxSize: Int, errorMessage: String) {
  269       if (collection == null || collection.size < minSize || collection.size > maxSize) {
  270           throw ETServiceFailureException.badRequestWithFailureMessage(errorMessage)
  271       }
  272   }
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/common/util/ValidationUtil.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **21행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`22.md`](22.md)
