# 14 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/auth/jwt/filter/JwtAuthenticationFilter.kt:50** · 심볼 `JwtAuthenticationFilter`

```kotlin
   42    * DWP 통합 환경에서 JWT의 dwpTokenHash와 쿠키의 DWP_TOKEN 해시를 비교합니다.
   43    * - disabled 모드: 검증 건너뜀
   44    * - optional 모드: 불일치 시 경고 로그만 기록
   45    * - required 모드: 불일치 시 401 응답
   46    *
   47    * @see DwpValidationProperties
   48    * @see DwpTokenHashService
   49    */
   50 ▶ class JwtAuthenticationFilter(
   51       private val authenticationService: AuthenticationService,
   52       private val jwtTokenService: JwtTokenService,
   53       private val boxwoodTenantRepository: BoxwoodTenantRepository,
   54       private val tokenStorageService: TokenStorageService,
   55       private val dwpTokenHashService: DwpTokenHashService,
   56       private val dwpValidationProperties: DwpValidationProperties,
   57       private val dwpAuthService: DwpAuthService,
   58       private val dwpTokenSessionService: DwpTokenSessionService
   59   ) : OncePerRequestFilter() {
   60
   61       private var excludeUrls: Array<String> = emptyArray()
   62
   63       fun setExcludeUrls(urls: Array<String>) {
   64           this.excludeUrls = urls
   65       }
   66
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/auth/jwt/filter/JwtAuthenticationFilter.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **14행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`15.md`](15.md)
