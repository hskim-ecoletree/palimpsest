# 10 / 33

**portal-backend/src/main/kotlin/kr/co/ecoletree/boxwood/llmtask/infrastructure/mcp/tools/form/ValidateFormTool.kt:76** · 심볼 `validateForm`

```kotlin
   68           } catch (e: Exception) {
   69               MCPToolResult.Error(
   70                   code = "VALIDATION_ERROR",
   71                   message = "Failed to validate form: ${e.message}"
   72               )
   73           }
   74       }
   75
   76 ▶     private fun validateForm(
   77           schema: Map<String, Any>,
   78           variables: Map<String, Any?>,
   79           targetFields: List<String>?
   80       ): List<ValidationError> {
   81           val errors = mutableListOf<ValidationError>()
   82
   83           // required 필드 검증
   84           @Suppress("UNCHECKED_CAST")
   85           val requiredFields = schema["required"] as? List<String> ?: emptyList()
   86
   87           @Suppress("UNCHECKED_CAST")
   88           val properties = schema["properties"] as? Map<String, Any> ?: emptyMap()
   89
   90           // 대상 필드 결정 (targetFields가 null이면 전체)
   91           val fieldsToValidate = targetFields ?: properties.keys.toList()
   92
```

*`▶` 가 후보 줄이다. 앞뒤는 문맥이다. 더 봐야 하면 코퍼스를 열어도 된다 —
`git -C ~/dev/projects/boxwood/portal-backend show a29cad0bf6a8:src/main/kotlin/kr/co/ecoletree/boxwood/llmtask/infrastructure/mcp/tools/form/ValidateFormTool.kt`*

---

1. **인가 가드인가?**  `예` / `아니오` / `모름`
2. **근거 좌표는?**  파일·심볼, 또는 `없음`

답과 소요 초를 [`../label-judgment-sheet.md`](../label-judgment-sheet.md) 의 **10행**에 적는다.
앞 건으로 돌아가지 않는다.

다음 → [`11.md`](11.md)
