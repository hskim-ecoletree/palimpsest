# IDE
.idea/
.vscode/

# OS
.DS_Store
.t2-stamp.json
.t6-stamp.json
.t3-stamp.json

# Rust
/target/

# 파이썬 바이트코드 — 대조 스크립트가 서로를 import 하면서 생긴다
__pycache__/
*.pyc

# 1층 추출 캐시 — 도구가 만든다. `.palimpsest/` 아래에서 **`cache/` 만** 뺀다
# (F04 게이트 · DESIGN §12.8: `intent/*.jsonl` · `manifest.toml` 은 커밋 대상이다)
/.palimpsest/cache/
