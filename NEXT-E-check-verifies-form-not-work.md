# 다음 회차가 받는 것 — **검사가 「했나」를 안 본다**

> 작성 2026-08-23 · 앞 회차 `2026-08-23-agent-laziness-behavior` 종료 + 사후 검증 직후
> 앞 회차의 게이트: [`docs/gates/agent-laziness-behavior.md`](docs/gates/agent-laziness-behavior.md)
> 앞 회차의 사후 검증: [`exp/post-verify.md`](.palimpsest/rounds/2026-08-23-agent-laziness-behavior/exp/post-verify.md)

**이 문서는 잠긴 의도가 아니다.** 착수 재료다. **착수하면 지운다** — `NEXT-C`·`NEXT-D` 가 그랬듯이.

---

## 1. 지금 상태의 정확한 진단

> **검사는 「형식이 맞나」를 보고 「했나」를 안 본다.
> 그리고 초록은 「어긋남 0」이 아니라 「검사 모집단 안에서 어긋남 0」이다.**

흩어진 이슈 넷이 각자 말하던 것이 **한 문장**이다 —
[#92](https://github.com/hskim-ecoletree/palimpsest/issues/92)(합계 검산이 행 수만 센다) ·
[#90](https://github.com/hskim-ecoletree/palimpsest/issues/90)(표 헤더 한 글자로 조용히 빠진다) ·
[#93](https://github.com/hskim-ecoletree/palimpsest/issues/93)(「없음」 행이 레코드가 된다) ·
[#97](https://github.com/hskim-ecoletree/palimpsest/issues/97)(음성 대조를 등록만 하고 안 걸었다).

## 2. ★ 앞 회차가 **자기 몸으로** 증명했다 — 이것이 이 재료의 핵심

앞 회차는 *"에이전트가 완료를 거짓 선언하는가"* 를 재려고 열렸다.
**재는 도중에 자기가 그것을 했다.**

| | 실험이 잰 것 | 회차가 자기에게 한 것 |
|---|---|---|
| 자기신고 | 완료 선언이 매 라운드 났다 | 완수 조건 상자를 다 켰다 |
| 실제 | 절반 넘게가 거짓 | **넷이 안 한 것이었다**(독립 리뷰 R1) |
| 반대 방향 | — | **셋을 서 있는데 반증으로 적었다**(R6) |
| 잡은 것 | 오라클 | **독립 리뷰와 사후 검증** |
| 자기 장치가 잡았나 | **못 잡았다** — 장치는 스펙을 모른다 | **못 잡았다** — `cargo xtask check` 는 그때도 **21/21** 이었다 |

그리고 **닫은 뒤에** 깨끗한 세션의 사후 검증이 어긋남 **일곱**을 더 냈다.
그중 둘이 이 회차의 주제다:

- **거짓 닫힘** — 레코드 두 행이 `닫은커밋` 을 한 커밋으로 적었는데 **그 커밋에 고침이 없다.**
  실제 수정은 각각 다섯·다섯 라운드 뒤에 났고 **그 사이 셋이 같은 것을 다시 냈다.**
- **검사 모집단 밖의 원장** — 종료 보고의 검산이 게이트와 **같은 커밋 안에서** 갈렸는데,
  「원장 둘 대조」는 그것을 **안 본다.**

★ **그리고 고치는 도중에 한 번 더 났다** — 정정 한 줄이 앵커를 못 찾아 안 들어갔는데
**커밋이 그 부분 실패를 삼켰고 검사는 그때도 21/21 이었다.**

## 3. RED — **돌려서 확인해라.** 수는 여기 안 적는다

```bash
# ① 합계 검산이 「닫은커밋」을 한 번이라도 읽나
grep -c '닫은커밋' xtask/src/main.rs

# ② `report.md` 로 무엇을 하나 — **존재 여부만** 보나 내용도 보나
grep -n 'report\.md' xtask/src/main.rs

# ③ 「원장 둘 대조」가 대는 두 원장이 무엇인가
grep -n 'fn check_ledger_pair' xtask/src/main.rs

# ④ 앞 회차의 레코드에 「닫힘」이 몇이고 그중 몇이 실제로 고쳐졌나
python3 .claude/skills/round/bin/record.py count \
  .palimpsest/rounds/2026-08-23-agent-laziness-behavior

# ⑤ 잘 도는 것 — 건드리기 전에 봐라
cargo xtask check
```

★ **①·② 가 이 회차의 출발선이다.** ① 이 `0` 이면 **검사가 그 칸을 한 번도 안 읽는 것**이고,
② 가 `is_file()` 뿐이면 **종료 보고의 내용은 어느 검사의 모집단에도 없는 것**이다.

## 4. 착수 시점에 정할 것 — 넷

**(1) §11 ③ 의 (가)/(나).** 이 회차는 **저장소의 검사를 고치는 회차**다 —
자기 장치에 대한 발견이 반드시 난다. **(가)는 원리상 못 고른다**(장치가 곧 산출이다).
**(나)를 고르고 잠근 의도 머리에 박아라.** 앞 회차는 (가)를 골랐고 그것이 맞았다 —
**이번엔 다르다.**

**(2) 범위.** 이슈 넷이 한 문장이지만 **한 회차에 다 넣으면 크다.**
⚠ 그리고 [#94](https://github.com/hskim-ecoletree/palimpsest/issues/94)(스키마 선언이 코드
갈래를 못 따라간다)가 **세 라운드 연속 같은 자리**로 서 있다 — 같은 계열이다.
**명시적으로 고르고 잠가라.**

**(3) 「검사가 「했나」를 본다」의 조작적 정의.** 안 정하면 회차 안에서 태어나고 갈린다.
⚠ **이것이 이 회차의 급소다** — 「했나」를 기계가 어떻게 아나.
후보: 좌표가 실재하나 · `닫은커밋` 의 diff 가 그 파일을 만졌나 ·
선언한 것이 그 커밋에 있나. **어느 것도 완전하지 않다. 무엇을 못 보는지 함께 적어라.**

**(4) 상한 셋.** ⚠ **독립 리뷰 상한은 다 쓴다** — 앞 회차에서 **여덟 라운드가 전부
무언가를 잡았고, R6 이 판정 셋을 뒤집었고, R8 이 마지막까지 금지역 둘을 냈다.**
그리고 **닫은 뒤의 사후 검증이 일곱을 더 냈다** — 그것도 상한 안에 넣을지 정해라.

## 5. ⚠ 부딪히는 자리 둘 — 착수 때 풀어라

**① 「고칠 것」이 「완성」을 밀어낸다.**
소유자의 기록: *착수 가능 열하나 중 아홉이 「고칠 것」이었고 「효과 확인」은 실행된 적 없다.*
**이 회차는 그 아홉째다.** 착수 때 **무엇이 「효과 확인」인지**를 함께 정해라 —
고친 검사가 **실제로 무언가를 빨갛게 만드는 것을 봐야** 한다(§8).

**② 검사가 「했나」를 보게 만들면 못 아는 것에 대해 빨개진다.**
`닫은커밋` 의 diff 가 그 파일을 만졌는지는 볼 수 있지만
**「그것이 그 발견을 고쳤나」는 기계가 원리상 못 안다.**
**어디까지 기계가 지고 어디부터 사람이 지는지를 잠근 의도에 적어라.**

## 6. 하지 말 것

- **앞 회차의 실험 하네스를 건드리지 마라.** `exp/` 아래는 **기록**이고
  저장소의 선언면이 아니다(§11③ (가)로 그렇게 닫혔다).
- **앞 회차의 판정을 다시 열지 마라.** 게이트는 닫혔고 사후 검증까지 받았다.
  **거기서 나온 것은 이 회차의 착수 재료이지 그 회차의 미결이 아니다.**
- **「검사를 더 엄격하게」로 가지 마라.** #90 이 실측한 것 —
  **엄격한 파서와 안 적힌 문법이 같이 가면 옛 회차가 통째로 형식 오류가 된다.**
- **`report.md` 를 검사 모집단에 넣을 때 조심해라.** 옛 회차 셋 중 하나만 그것을 가졌다
  (2026-08-19 실측). **하한을 「끝난 회차 중 가장 최근 것」으로 거는 기존 패턴을 봐라.**

## 7. ★★ 앞 회차의 별도 목록 — **두 회차 연속 안 열렸다**

#90 · #92 · #93 · #94 · #95 가 **2026-08-22 회차의 분할**인데
그 뒤 두 회차가 **하나도 안 열었다.**

§11 음성 대조: *"「별도 목록」이 다음 회차에서 한 번도 안 열리면 그것은 **분할이 아니라 버림**이다."*

> ★ **그 음성 대조가 울었다. 이 회차가 그것을 먼저 처분해야 한다.**
> 다행히 **이 회차의 주제가 그 목록의 대부분**이다 — #90·#92·#93 이 한 문장 안에 있다.
> #94·#95 는 다르다. **열든 「버림」으로 판정하든 명시적으로 해라.**

## 8. 앞 회차가 실측한 함정 — 측정 회차가 아니어도 무는 것

1. ★★ **원 반환문의 분류를 옮겨 적지 마라. 기계로 뽑아라.**
   앞 회차가 그 자리에서 **세 번** 조용히 행을 떨어뜨렸다 —
   굵게 표기(`**거짓**`) · 기각 행의 비정수 번호 · 셀 안의 `\|` 이스케이프.
   **세 번 중 두 번이 기각 행에 몰렸고**, 그러면 계기판 ⑧ 이 **낮게** 나온다.
2. ★★ **에이전트에게 반환 형식의 규율을 프롬프트에 박아라** —
   모집단·유효성·해악도 칸에 **굵게·백틱 금지** · 셀 안 `\|` 금지 ·
   **마크다운 링크 문법 금지**(백틱 안이라도 죽은 링크 검사가 읽는다) ·
   `#` 칸에 숫자만.
3. ★ **사전부검자는 `^### ` 항 + 기각 절**, **리뷰어는 `| # |` 표**다. 갈리면 검산이 0 을 낸다.
4. ★ **`.palimpsest/rounds/**` 아래에 `.json`·`.jsonl` 을 두지 마라.**
   `.json` 하나가 sunset 트리거를 참으로 만들어 **문서 두 뭉치 삭제를 요구**한다(실측).
5. ★ **커밋이 부분 실패를 삼킨다.** 여러 정정을 한 스크립트로 넣을 때
   **하나가 앵커를 못 찾아도 나머지가 커밋되고 검사는 초록이다.**
   **적용된 수를 세서 기대와 대라.**
6. ★ **`round/*` 는 `push` 로 CI 가 안 돈다.** PR 이 유일한 길이고 **소유자 판단**이다([#95]).

## 9. 잘 돌고 있는 것 — 건드리지 마라

돌려서 확인해라. **수는 여기 안 적는다.**

```bash
cargo xtask check              # 21 번째 검사(원장 둘 대조)와 회차 레코드 검사
python3 .claude/skills/round/bin/dashboard.py fa727ec \
  .palimpsest/rounds/2026-08-23-agent-laziness-behavior/intent.md HEAD
gh issue list --state open
```

앞 회차가 **레코드 252 행을 원 반환문과 행 순서까지 어긋남 0** 으로 세웠고
사후 검증이 그것을 **독립으로 재확인**했다. **그 자리는 지금 잘 돈다.**
