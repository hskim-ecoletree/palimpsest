---
description: 설치 상태와 저장된 그래프를 함께 본다
---

`pal doctor` 를 돌린다. 설치만 보려면 `pal doctor --install`.

읽는 순서:

1. **설치 검사 다섯** — 하나라도 빨가면 그것부터 고친다.
2. **불변식** — `모집단이 없습니다` 는 통과가 아니다.
3. **잔여** — 검사하지 못한 것이지 이상 없음이 아니다.
