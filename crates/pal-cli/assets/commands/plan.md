---
description: 계획 문서를 지금 스냅샷에 대 본다 — 그리고 갈린 것을 센다
argument-hint: <계획 문서 경로>
---

먼저 `pal plan $ARGUMENTS` 로 계획이 어느 좌표를 가리키는지 본다.
기준선이 문서의 프론트매터에 있으면 이어서 `pal deviation $ARGUMENTS` 를 돌린다.

- **이탈은 실패가 아니다.** 계획과 실제가 갈린 자리를 세는 것이다.
- 못 잰 것은 이탈 0 이 아니다 — 산출이 그 둘을 갈라 낸다.
