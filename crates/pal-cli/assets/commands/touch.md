---
description: 좌표 하나를 만진다 — 그 자리에 결박된 것과 그 낡음
argument-hint: <심볼 이름>
---

`pal touch $ARGUMENTS` 를 돌리고 그 산출을 **봉투째** 전한다.

- 결박이 0 건이면 그 사실을 그대로 적는다. **빈 답은 실패가 아니다.**
- `elision` 이 있으면 무엇이 잘렸는지 함께 적는다.
- 낡음 등급을 근거 없이 「유효함」으로 바꿔 적지 않는다.
