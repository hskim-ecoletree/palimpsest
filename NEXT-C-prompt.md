# 새 세션 착수 프롬프트

아래를 **그대로 복사해서** 새 세션 첫 메시지로 붙인다.

---

```
/round

에이전트 게으름 회차를 연다. 착수 재료가 `NEXT-C-agent-laziness.md` 에 있다 — 먼저 읽어라.
앞 세션이 unlazy(https://github.com/Leonxlnx/unlazy) 를 실측 대조하고 실험 둘을 돌린 결과다.

## 하려는 것

**우리 규약의 금지역 하나를 닫고, unlazy 에서 가져올 것을 실측으로 정한다.**

금지역: **완수 조건 체크박스가 죽은 가지다.** 최근 두 회차가 완수 조건 90 개를 열린 채로
끝냈고(46 + 44), 계기판 ② 는 그것을 읽어 「미판정 44/44」라는 거짓 신호를 낸다 —
같은 회차의 게이트 판정 표는 「통과 43」이다. 판정 원장이 두 자리에 있고 한 자리가 죽었다.

## 착수 시점에 정할 것 둘

**(1) §11③ 의 (가)/(나).** 만들 것이 전부 「이 회차가 만드는 장치」다.
앞 세션의 권고는 **(나)** — 장치를 만들되 그것에 대한 발견은 별개 의도로 보고 이슈로 세운다.

**(2) 범위.** 재료 문서의 5 절이 재조사 셋(R1 문면 대조 · R2 구축 과제 A/B · R3 길이 비용)을
설계해 뒀다. 앞 세션의 권고는 **R1 을 이 회차에 넣고 R2·R3 은 다음 회차로 분할**하는 것이다 —
R1 은 문서 대조뿐이라 값싸고, R2 는 구축 과제 하네스를 새로 만들어야 해서 그 자체가 회차 하나다.
다르게 판단해도 된다. 다만 **인터뷰에서 명시적으로 고르고 잠근 의도에 적어라.**

## 잠글 때 걸리는 것

- **RED 관측 후보**: 열린 체크박스 90 · 계기판 44/44 · 훅 `EVENTS` 에 `Stop` 0 개 ·
  완수 조건의 `CHECK` 형식 0 개. 착수 시점에 실제로 관측해서 적어라.
- **음성 대조는 재료 문서 4 절에 후보가 있다.** 특히 이것 —
  *"검사를 붙였는데 어느 회차에서도 한 번도 실패하지 않으면 CHECK 가 자기충족이다."*
  실측으로 확인됐다: `CHECK: echo "품질 확인함"` / `EXPECT: 확인` 이 한 줄도 안 짜고 `ALL MET` 을 낸다.
- **`EXPECT` 작성 규율을 함께 등록하라.** 절대값이 아니라 관계·상태로 쓴다.
  실측: `EXPECT: 117 파일` 은 회차가 끝난 지 이틀 만에 낡아 FAIL 을 내고 `EXPECT: /Rust\s+L1/` 는 참이다.
  이 규율이 없으면 붙인 게이트가 「완화해서 초록을 얻는」 길만 넓힌다.
- **퇴로**: 훅 `Stop` 정책이 위험하다고 판단되면 그것만 빼고 배선(검사 실행 + 막힘 표기)을 세운다.

## 하지 말 것

- **unlazy 의 스크립트를 베끼지 마라.** 실측된 결함 15 건이 재료 문서 3 절에 있다.
  특히 진행 해시 방식(빈 줄 하나에 카운터가 리셋된다) · 파서 경계(산문 한 줄에 차단이 무력화된다) ·
  들여쓰기(공백 두 칸에 게이트가 사라진다). **설계만 가져오고 구현은 다시 한다.**
- **Depth Tree · tree N 을 들이지 마라.** 저자가 효과 배수를 스스로 반증했고,
  우리는 회차·교대·에이전트 분업으로 그 자리를 이미 채운다. 어휘를 더하면 그것이 drift 다.
- **「개선 없는 패스 = 완료」를 들이지 마라.** 우리가 네 계열에서 반증한 조건이다.
  다만 앞 세션이 그 **범위**를 실측했다 — **모집단이 닫힌 일에서는 dry 도 유효한 신호다**
  (열린 과제 dry 0/12 라운드 · 닫힌 과제 3/3 조건 R2). 재료 문서 참조.

## 잘 돌고 있는 것 — 건드리지 마라

`cargo xtask check` 20/20 · 회차 레코드 검산 16 쌍 전부 일치 · 종료 보고 다섯 절 규약 4/4 준수 ·
넘긴 다섯이 5/5 실재 이슈. 실행해서 확인했다.
```

---

## 착수 직후 실행할 것 (RED 관측용)

```bash
# ① 열린 체크박스
for d in .palimpsest/rounds/*/; do
  printf "%-40s 열림 %3s / 닫힘 %3s\n" "$(basename $d)" \
    "$(grep -c '^- \[ \]' $d/intent.md)" "$(grep -c '^- \[[xX]\]' $d/intent.md)"
done

# ② 계기판이 죽은 쪽을 읽는다
python3 .claude/skills/round/bin/dashboard.py 56926aa \
  .palimpsest/rounds/2026-08-20-rust-extractor/intent.md HEAD | head -4

# ③ 훅에 Stop 이 없다
grep -n 'EVENTS' crates/pal-cli/src/hook/policy.rs

# ④ 완수 조건에 CHECK 형식이 없다
grep -c 'CHECK:' .palimpsest/rounds/*/intent.md

# ⑤ 잘 도는 것 (건드리지 말 것 확인)
cargo xtask check 2>&1 | tail -2
```
