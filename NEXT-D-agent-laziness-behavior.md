# 다음 회차가 받는 것 — 에이전트 게으름 ②: **문면이 행동을 바꾸는가**

> 작성 2026-08-23 · 앞 회차 `2026-08-22-agent-laziness` 종료 직후 · 아직 회차가 안 열렸다
> 조사 대상: [Leonxlnx/unlazy](https://github.com/Leonxlnx/unlazy) `ed9e8d2` (v2.0.0)
> 앞 회차의 잠긴 의도·판정·레코드: [`.palimpsest/rounds/2026-08-22-agent-laziness/`](.palimpsest/rounds/2026-08-22-agent-laziness/)
> 앞 회차의 게이트: [`docs/gates/agent-laziness.md`](docs/gates/agent-laziness.md)

**이 문서는 잠긴 의도가 아니다.** 착수 재료다. 의도는 `/round` 가 잠근다.
**착수하면 지운다** — `NEXT-A`·`NEXT-C` 가 그랬듯이.

---

## 1. 앞 회차가 무엇을 닫았고 무엇을 안 닫았나

### 닫은 것 — **하네스가 완성도에 대해 거짓말하던 통로 하나**

판정 원장이 두 자리에 있고 한 자리가 죽어 있었다. 이제 `cargo xtask check` 의
**21 번째 검사**가 게이트 표준 표 ↔ `intent.md` 상자를 **양방향 집합 같음**으로 댄다.
상자를 안 켜면 CI 가 빨개진다.

★ **그것은 「강제」지 「행동 변화」가 아니다.** 안 적으면 벌을 받게 된 것이고,
에이전트가 80% 에서 멈추는 것 자체를 겨눈 장치는 아니다.

### 안 닫은 것 — **가져오기로 고른 다섯을 안 적었다**

앞 회차가 `unlazy/SKILL.md` 를 문장 단위로 전수 대조해 **가져올 다섯**을 골랐다.
목록과 근거는 [#88](https://github.com/hskim-ecoletree/palimpsest/issues/88) 전문에 있고,
전수 대조표는 [`review/r1-unlazy-line-by-line.md`](.palimpsest/rounds/2026-08-22-agent-laziness/review/r1-unlazy-line-by-line.md) 다.

**「고르는 것까지」가 승격 5 의 결정이었다.** 기입은 다음 일이다.

### ★ 그리고 아무도 안 잰 것 — **가져오면 실제로 덜 게을러지는가**

앞 세션의 재조사 설계에서 **R1(문면 대조)만 했다.** `R2`(구축 과제 A/B) 와
`R3`(길이 비용)은 **범위 밖**이었다. 그러니 지금 상태는 이렇다:

> **무엇을 가져올지는 정해졌고, 가져오면 값이 있는지는 아무도 안 쟀다.**

---

## 2. ★ 앞 회차가 우연히 낸 증거 — **문면은 안 먹었고 구조가 먹었다**

이것이 이 재료 문서의 핵심이다. 그리고 **앞 회차가 겨눈 것이 아니라 부산물이다.**

앞 회차는 *"수를 안 적는다"* 를 주제로 삼았다. 규약에도, 게이트에도, 계획 문서에도
그 규율을 적었다. **그런데 독립 리뷰가 같은 병을 여섯 번 넘게 잡았다** —
산문이 잰 적 없거나 낡은 것을 사실처럼 적는 것.

**같은 문장 하나가 세 번 연달아 거짓이었다.** 「0 줄」 → 고치면서 「한 자릿수 분의
일」 → 또 고치면서 「한 자릿수로」. **고치는 손이 매번 새 어림수를 넣었다.**

★ **막힌 자리는 딱 하나였다 — 그 문서에서 「수를 안 드는 구조」로 바꾼 자리.**
`state.md` 는 *"매 라운드 갱신한다"* 를 자기 머리에 적은 채로 **세 번** 낡았고,
갱신 규율을 아무리 세게 적어도 안 막혔다. 수를 통째로 빼자 그 뒤로 안 낡았다.

이것은 **unlazy v2 자신의 논지와 같다:**

> v1 of this skill fought these with instructions. A controlled six-run test showed the
> limit of that … So v2 moves enforcement out of your goodwill and into files and checks.

⚠ **그러므로 다음 회차의 물음이 R2/R3 의 원래 형보다 날카로울 수 있다.**
원래 축은 `P`(현행 규약) 대 `P+`(현행 + 고른 다섯 문면)였다. 그런데 앞 회차의 실측은
**「문면을 더하는 것」 자체의 값을 의심하게 만든다.** 셋째 팔을 생각해 볼 만하다 —
`P+장치`(같은 규율을 문면이 아니라 검사·훅으로). **이것은 권고이지 결론이 아니다.
인터뷰에서 고르고 잠근 의도에 적어라.**

---

## 3. 재조사 설계 — 앞 세션이 남긴 것 (그대로 유효하다)

### R2 — 구축 과제로 A/B

**발견 과제가 아니라 구축 과제로 잰다.** unlazy 가 겨냥하는 것이 그쪽이다.

- 과제: 스펙이 있는 작은 것을 처음부터 만든다 (오라클 = 스펙 충족을 기계가 재는 것)
- 조건: `P`(현행 규약) · `P+`(현행 + 고른 다섯) · `B`(없음) — **셋째 팔은 위 2 절 참조**
- 측정: 스펙 충족률 · **플레이스홀더/TODO 수** · 자기 발견 결함 수 · 라운드별 개선량
- ★ **드리프트를 재려면 라운드를 여섯 이상으로.** 넷은 짧다(실측)

### R3 — 붙였을 때 손해가 없나

문면을 더하면 규약이 길어진다. **길이가 비용**이다.
`P` 대 `P+` 로 **같은 과제**를 재고, **개선이 없으면 안 넣는다.**

⚠⚠ **그러므로 [#88](https://github.com/hskim-ecoletree/palimpsest/issues/88)
(「기입한다」)과 R3 이 부딪힌다.** R3 의 규율은 **재고 나서 넣는다**이고 #88 의 제목은
**넣는다**이다. **착수 시점에 순서를 정하고 잠근 의도에 적어라** — 후보 규약 `P+` 를
만들되 커밋하지 않고 재는 쪽이 R3 을 지킨다.

### ⚠ 앞 세션이 실측한 설계 함정 다섯 — 되풀이하지 마라

1. **정답표를 에이전트가 닿는 곳에 두지 마라.** 한 실험에서 조건 하나가 정답 파일을
   찾아냈고 **막힘으로 닫아야 했다.**
2. **과제문이 답을 정하지 않게 하라.** 과제문에 수를 박으면 그 축이 미측정이 된다.
3. **길이는 줄 수로 재지 마라 — 바이트와 낱말로 재라.** 한국어 줄이 짧아 척도가 뒤집힌다.
4. **오라클의 각 항목이 실제로 발화하는지 측정 전에 확인하라.** 앞 실험에서 오라클
   항목 여럿이 「동작 결함 아님」으로 옳게 판정됐다 — **오라클이 틀렸던 것**이다.
5. **채점기도 장치다.** 마크다운 셀 안의 `|` 로 셀이 쪼개져 거짓 판정이 났다.
   **자동 계수는 손 검산과 대야 한다.**

### ⚠ 그 조사가 못 잰 것 (다음 회차가 이어받는 미측정)

조건마다 세션 하나라 **통계가 아니라 관측**이다 · **드리프트 미관측**(라운드가 짧았다) ·
**조기 포기 미측정**(과제문이 강제하거나 셋 다 완주) · **정밀도 미측정**(거짓 양성이
세 조건 다 0) · 한 실험은 오라클 노출로 **라운드 하나가 취소**돼 조건 간 비교가 깨졌다.

---

## 4. ★ 앞 회차가 새로 실측한 함정 — **측정 회차라면 이것들이 문다**

앞 회차의 [`state.md`](.palimpsest/rounds/2026-08-22-agent-laziness/state.md) 「실패한 접근」에
전문이 있다. 측정을 하는 회차에 특히 걸리는 것만 추린다.

1. ★★ **격리 사본에서 재려면 그 안에서 다시 빌드해라.** `xtask` 의 `repo_root()` 가
   `env!("CARGO_MANIFEST_DIR")` 라 **컴파일 시점에 원본 경로가 박힌다.** 사본을 복사만
   하고 원본 바이너리로 돌리면 **사본의 흠집을 안 보고 원본을 재서 초록이 난다** —
   독립 리뷰어가 실제로 그 초록을 한 번 받았다.
   확인: `strings target/debug/xtask | grep <사본경로>` 가 비면 원본을 재고 있다.
2. ★★ **에이전트에게 원 반환문을 직접 쓰게 해라** (`Bash` 로 `cat > … <<'EOF'`).
   메인이 옮겨 적으면 합계 검산이 **독립된 둘째 원천을 못 댄다** — *"자기가 쓴 것을
   자기가 세면 검산이 아니라 항등식"* 이다.
3. ★★ **레코드에 옮길 때 원문의 분류를 뽑아서 적어라.** 앞 회차에서 **수는 맞고
   내용이 어긋난 일**이 두 번 났고, 한 번은 **리뷰어가 문장으로 거부한 판정을 뒤집었다.**
   합계 검산은 **행 수만** 센다 —
   [#92](https://github.com/hskim-ecoletree/palimpsest/issues/92) 가 그 자리를 진다.
   그리고 **표 안의 `|` 는 `\|` 로 이스케이프하게 요청해라** — 셀이 깨져 분류를 잘못 읽는다.
4. ★ **산문에 수를 적지 마라.** 위 2 절. 세는 명령을 적어라.
5. ★ **원 반환문과 그 레코드는 같은 커밋에 실어라.** 반환문만 먼저 커밋하면 합계 검산이
   `항 N ↔ 레코드 0` 으로 **그 커밋을 빨갛게** 만든다. 처분은 그 앞 커밋이 지고
   `닫은커밋` 이 그 SHA 를 받는다.
6. ★ **`round/*` 브랜치는 `push` 로 CI 가 안 돈다.** 트리거가 `push: branches: [main]` 과
   `pull_request` 뿐이라 **PR 이 유일한 길**이고, 그것은 **소유자의 판단**이다.
7. ★ **`target/` 이 낡으면 `버전에_커밋이_실려_있다` 가 거짓 실패한다.**
   `touch crates/pal-cli/build.rs` 로 재빌드해라. 제품 결함이 아니다.
8. ★ **`K9`(CI 초록)는 상자로 못 켠다.** 켜는 행위가 새 마지막 커밋을 만들고 그 SHA 의
   런은 쓰는 시점에 없다. 앞 회차는 **「대조 불가」**로 판정했다 —
   [#95](https://github.com/hskim-ecoletree/palimpsest/issues/95) 가 구조를 진다.
   **같은 조건을 또 등록하려면 그 이슈를 먼저 읽어라.**

---

## 5. 착수 시점에 정할 것 — 넷

**(1) §11 ③ 의 (가)/(나).** 앞 회차는 **행동은 (나)였는데 등록을 빠뜨렸고** 독립 리뷰가
그것을 잡았다. **잠긴 의도 머리에 박아라.**

**(2) 범위.** 후보 셋이 있고 한 회차에 다 넣으면 크다:
- **문면 효과 측정**(R2·R3) — 하네스를 새로 만들어야 해서 그 자체가 회차 하나다
- **기입**([#88](https://github.com/hskim-ecoletree/palimpsest/issues/88)) — R3 의 규율상
  **측정 뒤**여야 한다
- **Stop 정책과 자기 상한**([#85](https://github.com/hskim-ecoletree/palimpsest/issues/85))
  — 게으름을 실제로 **막는** 자리. 앞 회차는 *"막는 것이 먹는다"* 만 관측했다

**(3) 「덜 게으르다」의 조작적 정의.** 이것을 안 정하면 무엇을 재는지가 회차 안에서
태어나고, **회차 안에서 태어난 표기는 갈린다**(§5 실측).

**(4) 상한 셋.** 인터뷰 · 사전부검 · 독립 리뷰.
⚠ **독립 리뷰 상한은 다 쓴다** — 소유자가 2026-08-23 에 그렇게 골랐고, 앞 회차에서
**여덟 라운드가 전부 무언가를 잡았으며 마지막 둘이 판정 자체를 뒤집었다.**

---

## 6. 잘 돌고 있는 것 — 건드리지 마라

돌려서 확인해라. **수는 여기 안 적는다.**

```bash
cargo xtask check                      # 21 번째 검사가 원장 둘을 댄다
cargo test --workspace                 # 낡은 target 이면 build.rs 를 touch
python3 .claude/skills/round/bin/record.py conditions \
  .palimpsest/rounds/2026-08-22-agent-laziness/intent.md
python3 .claude/skills/round/bin/dashboard.py e45e822 \
  .palimpsest/rounds/2026-08-22-agent-laziness/intent.md HEAD
gh issue list --state open             # 앞 회차의 별도 목록이 전부 살아 있다
```

---

## 7. 원자료 — 어디에 있나

- **`unlazy` 원문**: `git clone https://github.com/Leonxlnx/unlazy` (핀 `ed9e8d2` · v2.0.0).
  ⚠ 앞 회차의 전수 대조는 **`SKILL.md` 하나만** 모집단으로 삼았다 —
  `references/`·`templates/`·`scripts/` 는 **안 봤다.** R2 를 설계할 때 그 파일들이 필요할 수 있다.
- **전수 대조표**: [`review/r1-unlazy-line-by-line.md`](.palimpsest/rounds/2026-08-22-agent-laziness/review/r1-unlazy-line-by-line.md)
  — 그 문서가 `SKILL.md` 의 문장을 **전부 원문 인용으로 싣는다.** 클론 없이도 대조는 재도출된다.
- **고른 다섯과 기각한 열하나**: [#88](https://github.com/hskim-ecoletree/palimpsest/issues/88)
  본문 + 앞 회차 게이트의 `### I3` 절
- **베끼면 안 되는 unlazy 결함**: 앞 회차의 사전부검·재료가 열다섯을 실측했다.
  요지는 셋이다 — **진행 해시**(빈 줄 하나에 카운터가 리셋된다) ·
  **파서 경계**(산문 한 줄에 차단이 무력화된다) · **들여쓰기**(공백 두 칸에 게이트가 사라진다).
  **설계만 가져오고 구현은 다시 한다.**
- **브리핑 만드는 법**: `P` 는 `/round` 규약의 잠금·발견의 처분·막힘·검증·종료 보고·
  끝을 정하는 자 절 발췌. ⚠ **링크를 치환해 참조를 끊지 마라** — 앞 실험에서 형식 참조가
  끊겨 한 조건이 스펙과 다른 형식을 썼고 도구가 전부 거부했다.
