# 새 세션 착수 프롬프트

아래 펜스 안을 **그대로 복사해서** 새 세션 첫 메시지로 붙인다.

---

```
/round

에이전트 게으름 **둘째 회차**를 연다. 새 회차다 — 앞 회차는 닫혔다.
착수 재료가 `NEXT-D-agent-laziness-behavior.md` 에 있다 — **먼저 읽어라.**

앞 회차(`.palimpsest/rounds/2026-08-22-agent-laziness/`)는 **판정 원장이 두 자리에 있고
한 자리가 죽어 있던 금지역**을 닫았고, `unlazy` 에서 **가져올 다섯을 실측으로 골랐다.**
그 회차의 잠긴 의도·게이트·레코드를 시드로 받지 마라 — 필요하면 직접 읽어라.

## 하려는 것

**고른 다섯이 실제로 행동을 바꾸는지 재고, 값이 있는 것만 규약에 넣는다.**

지금 상태는 이렇다: **무엇을 가져올지는 정해졌고, 가져오면 값이 있는지는 아무도 안 쟀다.**
앞 세션의 재조사 설계에서 R1(문면 대조)만 했고 R2(구축 과제 A/B)·R3(길이 비용)은 범위 밖이었다.

## ★ 인터뷰에서 반드시 다뤄야 할 것 — 앞 회차가 우연히 낸 증거가 축을 흔든다

앞 회차는 *"수를 안 적는다"* 를 주제로 삼고 그 규율을 규약·게이트·계획에 다 적었다.
**그런데 독립 리뷰가 같은 병을 여섯 번 넘게 잡았고, 같은 문장 하나가 세 번 연달아
거짓이었다.** 「0 줄」 → 「한 자릿수 분의 일」 → 「한 자릿수로」 — 고치는 손이 매번
새 어림수를 넣었다. **막힌 자리는 딱 하나, 「수를 안 드는 구조」로 바꾼 자리였다.**

그러니 R2 의 원래 축(`P` 대 `P+문면`)이 물음을 다 못 잡을 수 있다.
**셋째 팔** — `P+장치`(같은 규율을 문면이 아니라 검사·훅으로) — 을 넣을지 인터뷰에서
고르고 **잠근 의도에 적어라.** 이것은 권고이지 결론이 아니다.

## 착수 시점에 정할 것 — 넷

1. **§11 ③ 의 (가)/(나).** 앞 회차는 행동은 (나)였는데 **등록을 빠뜨렸고** 리뷰가 잡았다.
   **잠긴 의도 머리에 박아라.**
2. **범위.** 후보 셋이고 한 회차에 다 넣으면 크다 —
   문면 효과 측정(R2·R3, 하네스를 새로 만들어야 한다) ·
   기입(#88, **R3 의 규율상 측정 뒤여야 한다**) ·
   Stop 정책과 자기 상한(#85, 게으름을 실제로 **막는** 자리).
   **다르게 판단해도 된다. 다만 명시적으로 고르고 잠가라.**
3. **「덜 게으르다」의 조작적 정의.** 안 정하면 그 표기가 회차 안에서 태어나고,
   **회차 안에서 태어난 표기는 갈린다**(§5 실측).
4. **상한 셋.** ⚠ **독립 리뷰 상한은 다 쓴다** — 소유자가 2026-08-23 에 그렇게 골랐고,
   앞 회차에서 **여덟 라운드가 전부 무언가를 잡았으며 마지막 둘이 판정 자체를 뒤집었다.**

## ⚠ 부딪히는 자리 하나 — 착수 때 풀어라

**#88 의 제목은 「기입한다」이고 R3 의 규율은 「재고 나서 넣는다」다.**
후보 규약 `P+` 를 만들되 **커밋하지 않고** 재는 쪽이 R3 을 지킨다.
어느 쪽이든 **잠근 의도에 적어라.**

## 하지 말 것

- **unlazy 의 스크립트를 베끼지 마라.** 실측된 결함 요지 셋 —
  진행 해시(빈 줄 하나에 카운터가 리셋된다) · 파서 경계(산문 한 줄에 차단이 무력화된다) ·
  들여쓰기(공백 두 칸에 게이트가 사라진다). **설계만 가져오고 구현은 다시 한다.**
- **Depth Tree · tree N · 「개선 없는 패스 = 완료」를 들이지 마라.**
  앞 회차가 근거와 함께 기각했고 마지막 것은 **우리가 네 계열에서 실측으로 반증했다.**
- **정답표를 에이전트가 닿는 곳에 두지 마라.** 앞 실험이 그것으로 라운드 하나를 잃었다.
- **길이를 줄 수로 재지 마라** — 바이트와 낱말로 재라. 한국어 줄이 짧아 척도가 뒤집힌다.

## 측정 회차라서 특히 무는 것 — 재료 문서 4 절 전문을 읽어라

- ★★ **격리 사본에서 재려면 그 안에서 `cargo build -p xtask` 를 다시 해라.**
  `repo_root()` 가 컴파일 시점 경로라 **사본의 흠집을 안 보고 원본을 재서 초록이 난다.**
  확인: `strings target/debug/xtask | grep <사본경로>` 가 비면 원본을 재고 있다.
- ★★ **에이전트에게 원 반환문을 직접 쓰게 해라**(`Bash` 로 `cat > … <<'EOF'`).
  메인이 옮겨 적으면 합계 검산이 **독립된 둘째 원천을 못 댄다.**
  그리고 **표 안의 `|` 는 `\|` 로 이스케이프하게 요청해라.**
- ★★ **레코드에 옮길 때 원문의 분류를 뽑아서 적어라.** 합계 검산은 **행 수만** 센다 —
  앞 회차에서 **수는 맞고 내용이 어긋난 일**이 두 번 났고 한 번은 **리뷰어가 문장으로
  거부한 판정을 뒤집었다.**
- ★ **산문에 수를 적지 마라. 세는 명령을 적어라.**
- ★ **`K9`(CI 초록)를 또 등록하려면 #95 를 먼저 읽어라.** 앞 회차는 그것을
  **「대조 불가」**로 판정했다 — 상자를 켜는 행위가 새 마지막 커밋을 만든다.

## 잘 돌고 있는 것 — 건드리지 마라

돌려서 확인해라. `cargo xtask check` 의 21 번째 검사(**원장 둘 대조**)와 계기판 ⑨
(**해악 게이트**)가 앞 회차의 산출이다. 그 둘에 대한 발견은 **별도 목록**이다 —
#90 · #92 · #93 · #94 · #95 가 이미 서 있다.
```

---

## 착수 직후 실행할 것 (RED 관측용)

```bash
# ① 고른 다섯이 규약에 안 들어갔다 — 이것이 이 회차의 출발선이다
grep -c '공격선 전환\|끝난 느낌\|적대적으로 재검\|전수 훑기\|값싼 모델' .claude/skills/round/SKILL.md

# ② 「덜 게으르다」를 재는 장치가 0 이다
#    ⚠ `corpus/tasks/` 는 **추출기 코퍼스**다 — 게으름 과제가 아니다. 헷갈리지 마라.
grep -rn 'A/B\|조건 P+\|플레이스홀더\|스펙 충족' .claude/skills/round/ scripts/ | wc -l
find .palimpsest/rounds -maxdepth 2 -name 'exp*' | wc -l   # 앞 실험의 원자료는 저장소에 없다

# ③ Stop 정책이 없다 — 관측만 했다
grep -n 'EVENTS' crates/pal-cli/src/hook/policy.rs
cat .palimpsest/rounds/2026-08-22-agent-laziness/effect/stop-hook-observed.txt | head -20

# ④ 잘 도는 것 (건드리지 말 것 확인)
cargo xtask check 2>&1 | tail -2
python3 .claude/skills/round/bin/record.py conditions \
  .palimpsest/rounds/2026-08-22-agent-laziness/intent.md | tail -5

# ⑤ 앞 회차의 별도 목록이 살아 있나
gh issue list --state open --limit 30
```
