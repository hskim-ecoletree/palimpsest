# 회차 A — 재고 처분

> **임시 파일이다.** 착수하면 이 파일을 지운다.
> 짝: [`NEXT-B-plan-for-harness.md`](NEXT-B-plan-for-harness.md) — 다른 PC 에서 **병렬로** 돈다. §5 를 반드시 읽어라.

---

## 0. 먼저 할 것

```
/round
```

**이 회차는 새 규약의 첫 소비자다.** 규약 자신이 「원리상 못 잰 것」으로 적어 둔 물음이 하나 있고, 그 답이 이 회차다:

> **이 규약이 남은 일을 실제로 줄이는가.** 잴 수 있게 되는 조건: `/round` 로 도는 회차 하나.

그러니 절차를 요약해서 흉내내지 말고 **스킬을 열어 그대로 밟아라.** 인터뷰부터다.

## 1. 무엇을 하나

**2026-08-18 회차가 방향을 틀었다.** palimpsest 는 이제 지식 그래프 **위에 하네스가 얹힌** 제품이고 Claude Code 전용이다([ADR-0025](docs/adr/0025-the-harness-that-reads-the-graph-is-the-same-product.md)).

그 결정 앞에서 **기존 재고 전부를 한 번 훑어 처분한다.** 소유자 지시(2026-08-18): *"재고도 한 번 훑어 처분한다."*

처분은 셋 중 하나다 — **지운다 · 고친다 · 남긴다.** 「나중에」가 없다(규약).

## 2. 대상 — 이 기계에서 잰 값 (2026-08-18 · 재확인 대상)

| 무엇 | 규모 | 왜 의심스러운가 |
|---|---|---|
| 열린 이슈 | **22 건** (착수 가능 11 · 그중 **9 건이 「고칠 것」**) | 전부 KG·투영 시절에 열렸다. 하네스 방향에서 여전히 「만들 것」인가 |
| `docs/` 전체 | **28,314 줄** | |
| `docs/gates/` | **12,818 줄** · 파일 19 | 판정 기록. 발견이 **여덟 가지 절 이름**으로 흩어져 있다 |
| `docs/plan/` | **5,949 줄** · 파일 **29** (기능 문서 F01~F23 · P0) | **지형이 KG·투영 중심이고 하네스를 안 담는다** |
| `docs/DESIGN.md` | **2,138 줄** | ★ 자기 지위가 *"권위: 아직 없다. **이 문서는 ADR 후보의 묶음이다**"* — 이제 ADR 이 **25 개**다 |
| `WHITEPAPER.md` | **480 줄** | 자기 삭제 조건이 *"논거가 전부 ADR 과 코드로 흡수되어 요약 이상을 못 할 때"* |
| `docs/plan/00-goals.md` | **242 줄** | §4 비목표 표에서 한 줄을 이미 옮겼다. 나머지는 |
| `docs/how-it-works.md` | **973 줄** | 완성 장면이 MCP 를 전제하는가 |
| ADR | **25 개** | 호스트 중립을 전제한 것이 있는가 |

★ **소유자가 「DESIGN.md 와 GOAL.md — 이것들은 원래 지울 목적이었음」이라고 말했다.**
그런데 **palimpsest 에 `GOAL.md` 는 없다.** 후보 둘이다:
- `docs/plan/00-goals.md` (이 저장소)
- `~/dev/projects/fabricate/GOAL.md` (비준된 인터뷰 하네스 · 개발 중단 · 자산 승계 미결)

**인터뷰에서 어느 쪽인지 확인해라.** 추측해서 지우지 마라.

## 3. 판정하는 자

각 문서·이슈에 대해 셋을 묻는다:

1. **낡았는가** — ADR-0025 이후로 사실이 아닌 것을 적고 있는가
2. **방향을 잘못 가리키는가** — 읽는 사람을 옛 방향으로 데려가는가
3. **흡수됐는가** — 그 내용이 ADR·코드·규약으로 이미 옮겨갔는가

**「낡은 문서의 문제는 쓸모없어지는 것이 아니라 거짓 신호가 되는 것」**([WHITEPAPER §1](WHITEPAPER.md))이 이 저장소 자신의 논지다. 지금 그 논지가 자기 문서에 적용되는 자리다.

⚠ **지우는 것이 기본값이 아니다.** 흡수됐으면 지우고, 낡았으면 고치고, 아직 유효하면 남긴다. 「전부 지운다」도 「전부 남긴다」도 판정이 아니다.

## 4. 이 회차가 특별히 봐야 할 것

- **F06b 가 남긴 MCP 자산** — `pal-mcp` 크레이트 · `serve` 서브커맨드 · feature `mcp` · CI 의 어댑터 부재 갈림. ADR-0025 가 *"MCP 는 이 결정으로 값을 잃는다"* 고 적었지만 **처분은 이 회차로 넘겼다.** 지우면 서브커맨드가 18 → 17 이 되고 CI 잡 구성이 바뀐다.
- **게이트 문서의 여덟 이름** — 새 규약은 절 이름을 넷으로 고정했다(`합격선`·`판정`·`효과`·`범위 밖`). 기존 19 개 파일을 그 형식으로 옮길지, 옛 게이트는 그대로 둘지 정해야 한다.
- **`.claude/` 가 이번에 처음 추적에 들어갔다** — 에이전트 정의 여섯과 스킬 하나.

## 5. ★ 병렬 세션과의 경계 — 반드시 지킨다

`NEXT-B` 가 **다른 PC 에서 동시에 돈다.** 같은 `main` 에 push 한다.

| | A (이 회차) | B (병렬) |
|---|---|---|
| **소유** | 기존 파일의 **처분**(삭제·정정) · 이슈 · 게이트 | **새 계획 문서**(새 경로에만) |
| **금지** | 새 계획을 쓰지 마라 — B 의 몫이다 | 기존 파일을 고치지 마라 |
| **겹치는 자리** | `docs/plan/README.md` · `00-goals.md` 는 **A 소유**다 | B 는 그것들을 읽되 안 고친다 |

- **push 전에 `git pull --rebase` 한다.** 충돌하면 상대 것을 살리고 자기 것을 다시 얹는다.
- ⚠ **CI 는 `cancel-in-progress: true` 다**(`.github/workflows/ci.yml:52`). **push 하기 전에 `gh run list --limit 3` 으로 진행 중인 회차가 있는지 본다** — 있으면 기다린다. 안 그러면 상대 회차를 죽인다(2026-08-18 에 실제로 일어났다).

## 6. 걸리는 자리

- **새 이슈를 열 때는 「만들 것」만.** 발견은 이 회차에서 닫는다(규약).
- **저장소가 PUBLIC 이다.** force push 금지. 태그·릴리스 생성 금지.
- 실험은 `/tmp/<이름>-<타임스탬프>/` 하나로 고정하고 절대경로로 박아라.
- ⚠ 검증 출력을 파이프로 잘라 `error[E`·`panicked` 를 날리지 마라. 전 출력을 파일로 받고 거기서 세라.
- ⚠ `rc=0` 을 판정으로 읽지 마라.
- `cargo fmt` 는 이 저장소의 게이트가 아니다.
- **이 파일과 `NEXT-B-plan-for-harness.md` 를 착수 시 지운다.**

## 7. 시작 상태

- 브랜치 `main` · `HEAD == origin/main`. 마지막 회차의 CI 는 7 잡 전부 success.
- 규약: [`AGENTS.md`](AGENTS.md)(3.5k자) → 절차 정본은 `/round` 스킬.
- 이번 회차 기록: `.palimpsest/rounds/2026-08-18-round-protocol/` (`intent.md` · `state.md` — **실패한 접근 여섯**이 거기 있다. 읽어라).
- 기계: Darwin arm64 · rustc/cargo 1.94.1 (Homebrew) · rustup 없음 · `timeout` 없음.
