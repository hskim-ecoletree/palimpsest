# 회차 B — 하네스의 계획을 세운다

> **임시 파일이다.** 착수하면 이 파일을 지운다.
> 짝: `NEXT-A-inventory-disposal.md` — 다른 PC 에서 **병렬로** 돈다. §5 를 반드시 읽어라.
> ★ **A 는 2026-08-18 에 착수했고 그 파일을 지웠다**(커밋 `57db375`). 회차 기록은 `.palimpsest/rounds/2026-08-18-inventory-disposal/` 에, 처분 결과는 `docs/plan/disposal-map.md` 에 있다.
> **다른 PC 에서 여는 회차다.** 저장소를 클론했으면 먼저 `git pull` 로 최신을 받아라.

---

## 0. 먼저 할 것

```
/round
```

**이 회차는 새 규약의 첫 소비자 둘 중 하나다.** 절차를 요약해서 흉내내지 말고 **스킬을 열어 그대로 밟아라.** 인터뷰부터다 — 계획을 세우기 전에 소유자에게 물어야 할 것이 많다.

## 1. 무엇을 하나

**2026-08-18 회차가 방향을 틀었다.** palimpsest 는 이제 **지식 그래프 위에 하네스가 얹힌** 제품이다 — 사용자 프로젝트에 설치되어 회차를 돌고, Claude Code 전용이다([ADR-0025](docs/adr/0025-the-harness-that-reads-the-graph-is-the-same-product.md)).

**그런데 계획에 그 지형이 없다.** `docs/plan/` 의 기능 문서 24 개(F01~F23 · P0)는 전부 KG·투영·질의 중심이고, **하네스를 만드는 일이 하나도 없다.**

이 회차는 그 빈자리를 채운다 — **무엇을 어느 순서로 만들어야 하네스가 서는가.**

## 2. 무엇이 이미 정해졌나 — 계획이 이것을 만족해야 한다

전문은 [ADR-0025](docs/adr/0025-the-harness-that-reads-the-graph-is-the-same-product.md) 와 `/round` 스킬. 계획에 직접 걸리는 것만 옮긴다.

**표면은 넷이고 그 이상 없다.**

| 표면 | 매체 | 하는 일 |
|---|---|---|
| 스킬 | 프롬프트 | 회차를 어떻게 도는가 |
| 에이전트 정의 | 프롬프트 | 역할·경계·반환 형식 |
| 훅 | 강제 | 우회하면 피해가 큰 경계 |
| `pal` CLI | 코드 | **상태 관리자 — 대화하지 않는다** |

**역할은 다섯이고 단계에 배치되지 않는다** — 사관(결박) · 사서(조회) · 큐레이터(선별) · 서사자(엮음) · 동료(판단). **필요한 어느 자리에서든 부르는 도구**이고, 가장 자주 걸리는 자리는 **`grep` 을 집으려는 순간**이다.

**그래프가 답해야 하는 것** — 소유자 지시(2026-08-18): 정적 분석으로 **데이터 흐름 · 함수와 인터페이스 호출 · 이벤트 전파 · 라이브러리 사용**을 미리 계산해 **영향 범위 파악 · pre-mortem · 코드 작성 계획**에 쓴다. *"이게 meta 의 pre-compute 개념이잖아."*

**판단은 모델이, 검사는 기계가.** `pal` 은 절차가 밟혔는지와 산출물의 형상만 본다.

## 3. 계획이 답해야 하는 것

**바로 답하려 하지 마라 — 인터뷰가 먼저다.** 아래는 계획이 끝났을 때 답이 서 있어야 하는 것들이다.

1. **회차 하나를 돌리는 데 `pal` 이 무엇을 해야 하나** — 지금은 스킬(프롬프트)만으로 돈다. 어디까지가 프롬프트이고 어디부터가 코드인가. 규약이 *"끝을 셈이 정한다"* 고 했는데 그 셈을 누가 세나.
2. **잠긴 의도와 상태를 `pal` 이 소유하나** — 지금은 `.palimpsest/rounds/<slug>/` 의 마크다운이다. redb 로 갈 것인가, 파일로 남을 것인가. `intent.redb` 가 이미 있다.
3. **훅이 무엇을 강제하나** — 표면 넷 중 유일하게 지금 하나도 없다. *"우회하면 피해가 큰 경계"* 가 구체적으로 무엇인가.
4. **그래프가 답할 질의 목록** — 「영향 범위」가 실제로 어떤 질의인가. `queries.toml` 이 이미 있다.
5. **설치** — 사용자 프로젝트에 어떻게 얹히나. `pal install` 이 이미 있는데 스킬·에이전트·훅까지 놓아야 한다.
6. **기존 F 기능과의 순서** — F07(참조 해소) · F23(git 결합) 같은 것이 하네스보다 먼저인가 나중인가.

★ **가장 중요한 물음 하나** — 규약 자신이 「능력 부재」로 이미 적어 둔 것이다:

> **이 회차의 결정을 결박할 노드 타입이 없다.** 결박 대상이 코드가 아니라 **문서·절차**라서 지금 그래프가 담지 못한다.

**하네스가 자기 회차를 결박하려면 그래프가 「결정」과 「의도」를 코드 밖에서도 붙들어야 한다.** 이것이 계획의 초석 후보다 — 나중에 바꾸면 그 위가 전부 움직인다.

## 4. 산출물

**새 파일에만 쓴다**(§5). 형태는 인터뷰가 정하되, 이 저장소의 계획 문서가 이미 지키는 것 둘은 지킨다:

- **완성 장면이 있어야 한다** — *"이 화면이 제품이다"*([00-goals §1](docs/plan/00-goals.md) 형식). 게이트 언어만으로는 안 그려진다.
- **각 기능에 반증 조건이 있어야 한다** — *"안 되면 약하게 선언하고 X 에 집중한다"*. 퇴로를 미리 등록하면 나중에 쓰는 데 승인이 필요 없다(규약).

⚠ **문서를 두껍게 만들지 마라.** 이 저장소의 `docs/` 는 이미 **28,314 줄**이고 그것이 진단된 병의 일부다. 상세도는 구현 품질이 아니다.

## 5. ★ 병렬 세션과의 경계 — 반드시 지킨다

`NEXT-A` (재고 처분)가 **다른 PC 에서 동시에 돈다.** 같은 `main` 에 push 한다.

| | B (이 회차) | A (병렬) |
|---|---|---|
| **소유** | **새 계획 문서**(새 경로에만) | 기존 파일의 **처분**(삭제·정정) · 이슈 · 게이트 |
| **금지** | **기존 파일을 고치지 마라** — 읽기만 | 새 계획을 쓰지 않는다 |
| **겹치는 자리** | `docs/plan/README.md` · `00-goals.md` 는 **A 소유**다. 읽되 안 고친다 | |

⚠ **A 가 문서를 지우고 있다.** 네가 읽는 기존 문서가 회차 도중에 사라지거나 바뀔 수 있다. **인용할 때 커밋 해시를 함께 적어라.**

- **push 전에 `git pull --rebase` 한다.**
- ⚠ **CI 는 `cancel-in-progress: true` 다**(`.github/workflows/ci.yml:52`). **push 하기 전에 `gh run list --limit 3` 으로 진행 중인 회차가 있는지 본다** — 있으면 기다린다. 안 그러면 상대 회차를 죽인다(2026-08-18 에 실제로 일어났다).

## 6. 걸리는 자리

- **기능을 구현하지 마라.** 이 회차의 산출물은 계획이다.
- **새 이슈를 열 때는 「만들 것」만.** 발견은 이 회차에서 닫는다(규약).
- **저장소가 PUBLIC 이다.** force push 금지. 태그·릴리스 생성 금지.
- ⚠ `rc=0` 을 판정으로 읽지 마라. 전 출력을 파일로 받고 거기서 세라.
- **이 파일과 `NEXT-A-inventory-disposal.md` 를 착수 시 지운다.**

## 7. 먼저 읽을 것

| | |
|---|---|
| 규약 | [`AGENTS.md`](AGENTS.md) → 절차 정본은 `/round` 스킬 |
| 방향 전환 | [`docs/adr/0025-…`](docs/adr/0025-the-harness-that-reads-the-graph-is-the-same-product.md) |
| 소유자 지시 원문 | [`docs/instructions/2026-08-18-owner-direction.md`](docs/instructions/2026-08-18-owner-direction.md) — **§2 정규화 13 개가 요구사항이다** |
| 앞 회차의 실패 | `.palimpsest/rounds/2026-08-18-round-protocol/state.md` — **실패한 접근 여섯** |
| 기존 지형 | [`docs/plan/README.md`](docs/plan/README.md) — 읽되 고치지 마라 |
| 승계 후보 | `~/dev/projects/fabricate/IMPLEMENTATION-ORCHESTRATION.md`(691줄, 미비준 초안) · `GOAL.md`(비준) — **다른 PC 에는 없을 수 있다.** 없으면 능력 부재로 적어라 |
