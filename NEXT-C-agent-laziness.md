# 다음 회차가 받는 것 — 에이전트 게으름

> 작성 2026-08-22 · 브랜치 `round/agent-laziness` · 아직 회차가 안 열렸다
> 조사 대상: [Leonxlnx/unlazy](https://github.com/Leonxlnx/unlazy) `ed9e8d2` (v2.0.0)
> 대조 보고서: https://claude.ai/code/artifact/aa4a8e73-b05c-4fbb-8d52-9f3871fb1506

**이 문서는 잠긴 의도가 아니다.** 착수 재료다. 의도는 `/round` 가 잠근다.

---

## 1. 무엇이 실측됐나

### ① 완수 조건 체크박스가 죽은 가지다 — **금지역**

```
2026-08-18-completion-condition   열림   0 / 닫힘  41
2026-08-18-inventory-disposal     열림   0 / 닫힘  37
2026-08-18-round-protocol         열림   0 / 닫힘   7
2026-08-19-finding-records        열림  46 / 닫힘   0   ←
2026-08-20-rust-extractor         열림  44 / 닫힘   0   ←
```

최근 두 회차가 완수 조건을 **한 개도 안 켜고** 끝났다. 판정을 안 낸 것이 아니다 —
`docs/gates/rust-extractor.md` 의 `## 판정` 은 「통과 43 · 반증 0 · 대조 불가 1 · 미측정 0 = 44」를
적고 *"이 표가 유일한 계수 자리다"* 라고 못 박는다.

**판정 원장이 두 자리에 있고 한 자리가 죽었다.** 그리고 계기판은 죽은 쪽을 읽는다:

```
$ python3 .claude/skills/round/bin/dashboard.py 56926aa \
    .palimpsest/rounds/2026-08-20-rust-extractor/intent.md HEAD
② 미판정 잔액    44 / 44        ← 게이트 판정은 「통과 43」이라 적혀 있다
```

해악도: 「검사·시험이 있는데 실제로는 아무것도 재지 않는다. 통과가 거짓이 된다」 =
**금지역 「측정이 죽은 가지」** → 해악 게이트에서 **막힘** 등급이다.

### ② 완수 조건에 실행 가능한 검사가 0 개다

`intent.md` 어디에도 `CHECK:` 형식이 없다. 상자는 **사람이 손으로 켜야** 하고, 그래서 안 켜진다.
한 회차의 완수 조건 44 를 손으로 분류하면:

| 유형 | 수 | 기계가 무엇을 재나 |
|---|--:|---|
| 코드·파일·명령이 답한다 | **36** | `CHECK`/`EXPECT` 로 그대로 표현된다 |
| 「판정·효과에 적혔다」 (`B4·D3·D4·E3·E5·G1·G2·G3`) | **8** | 「적혔다」만 재고 「옳게 적혔다」는 못 잰다 |

**82% 지 100% 가 아니다.** 나머지 8 은 독립 리뷰가 계속 진다.

### ③ 사전 등록값이 낡는다 — 기계를 붙이면 즉시 드러난다

완수 조건 `E1` 은 *"`pal ledger` 가 Rust 를 L1 로 보이고 **117** 파일이 빠진다"* 이고
게이트는 **통과**로 적었다. 지금 돌리면:

```
FAIL E1   EXPECT: 117 파일
     Rust   L1  identity: ordinal   121 파일
PASS E1b  EXPECT: /Rust\s+L1/
```

**117 은 틀린 것이 아니라 낡은 것이다.** 그리고 `docs/gates/README.md` 는 이미
*"수를 안 적는다 — 파일이 하나 늘면 즉시 거짓이 되는 캐시다"* 로 갔다.
⚠ **그런데 unlazy 의 `EXPECT` 는 수를 박으라고 요구한다.** 두 규율이 그대로는 부딪힌다.
`E1b` 가 답이다 — **`EXPECT` 를 절대값이 아니라 관계·상태로 쓴다.**

### ④ 훅 `EVENTS` 에 `Stop` 이 없다

`crates/pal-cli/src/hook/policy.rs:36` — `EVENTS = &["SubagentStop"]` 하나이고
정책은 「돌려준 말이 비어 있는 서브에이전트」 하나다. **의도된 최소**이고 파일 머리가
*"배관이 먼저 서고 정책은 나중에 소유자가 정한다"* 라고 적는다.
**메인 세션의 조기 종료를 막는 것이 없다.**

⚠ 그리고 같은 파일이 실측을 적어 뒀다: *"매 라운드 도구를 부르는 서브에이전트는 100 회까지
한 번도 안 멈췄다 — 하네스가 반복 차단을 못 멈춘다."* **자기 상한이 선택이 아니다.**

### ⑤ 「막힘」의 기계 표기가 없다

규약은 막힘을 개념으로 갖고 있고 *"축소로 위장해 완료를 선언하지 않는다"* 까지 적었다.
그런데 **산문이라 기계가 못 읽는다** — `findings.jsonl` 처분 enum 은
정정·확대·축소·전환이고 **막힘이 없다.**
훅이 턴을 막기 시작하면 이 표기가 **반드시 있어야 한다.** 없으면 정직하게 막힌 회차와
게으른 회차를 구별 못 하고, 유일한 탈출구가 「조건을 지우는 것」이 된다.

### ⑥ 잘 돌고 있는 것 — 건드리지 마라

- `cargo xtask check` **20/20 통과** · 회차 레코드 **검산 16 쌍 전부 일치**(29↔29 … 33↔33)
  + 원천이 원리상 없는 두 자리를 **면제라고 말하고 면제**
- 종료 보고 다섯 절 규약: **4/4 회차 준수**, 금지된 넷 0 건, 음성 대조 미발화
- 넘긴 다섯이 **5/5 실재하는 열린 이슈**(#77~#81) — 「분할이지 넘김이 아니다」가 참
- 「원리상 못 잰 것」에 규약이 요구한 세 성분이 실제로 다 붙어 있다

---

## 2. unlazy 에서 가져올 것 — **배선 셋은 판정됐고, 문면은 미조사다**

### 배선 셋 (판정 완료 · 착수 가능)

1. **완수 조건에 `CHECK`/`EXPECT`/`EVIDENCE` 형식을 붙이고 기계가 실행한다.**
   `scripts/f*-verify.py` 20+ 와 `cargo xtask check` 20 검사가 이미 있으니 **새로 만들 검사는 거의 없다** —
   없는 것은 *완수 조건 하나하나를 그것들에 잇는 줄*이다.
   실행자는 `record.py` 옆에 서거나 `cargo xtask` 의 21 번째 검사가 된다.
   **`EXPECT` 작성 규율을 함께 등록한다** (③ 참조).

2. **`Stop` 이벤트 + 진행 인지형 자기 상한.**
   `hook/policy.rs` 의 `EVENTS` 에 `"Stop"` 을 더하고 정책 하나:
   *잠긴 의도에 열린 완수 조건이 있고 그 회차의 판정 커밋이 없으면 차단.*
   상한 설계는 unlazy 의 `stop-hook.mjs` 가 형태를 보여 준다 —
   게이트 파일 내용 해시가 바뀌면 카운터 리셋, 진전 없이 N 회 막히면 **가두지 않고 경고와 함께 해제.**
   ⚠ **그 해시 방식에 결함이 있다**(아래 3-③ 참조). 설계만 가져오고 구현은 다시 한다.

3. **「막힘」의 기계 표기** — `findings.jsonl` 처분 enum 또는 별도 줄.

### ★ 문면 — **미조사다. 이것이 다음 회차의 진짜 물음이다**

이번 대조는 *"가져올 것은 배선 하나"* 로 결론지었는데, **그 결론의 근거가 약하다:**

- 실험 둘 다 **발견 과제**였다. unlazy 는 **구축**을 겨냥한다(*"build the landing page"*).
- 그리고 조건 U 는 **행동이 실제로 바뀌었다** — `GATES.md` 를 만들고 게이트 31 개를 세웠다.
  결과(재현율)가 같았을 뿐이다.
- **④ 드리프트는 4 라운드가 짧아서 미관측**이었다.

**palimpsest 규약에 대응물이 없거나 약한 unlazy 문면 후보:**

| unlazy 문면 | palimpsest 대응물 |
|---|---|
| **네 패스** (구현 완료 → 전문가 재독 → 결함 사냥 → 무료 광택) | **없다.** 규약은 처분·판정을 말하고 *작업 방식*을 안 말한다 |
| *"When you feel finished, check instead of concluding"* | 종료 조건 여섯은 있으나 **「느낌」을 트리거로 쓰는 문장이 없다** |
| *"Do not simulate work you can do"* (싸고 되돌릴 수 있으면 실행하고 관측하라) | 「추정을 조회인 척하지 않는다」가 가장 가깝지만 **실행 유도가 아니다** |
| *"Ignore resource anxiety"* + 정직한 인계 경로 | 이월 사유 넷에 있으나 **대응 행동이 없다** |
| *"Full files, full lists, full sweeps — 80 files면 연 수가 80이고 그 수를 말한다"* | 모집단 정의는 있으나 **「세어서 말한다」가 없다** |
| **Report audit** (보고 시점에 모든 수를 재측정하거나 「미확인」이라 붙인다) | 「수를 안 적는다」·합 검산이 더 강하지만 **보고 시점 재측정 규칙은 없다** |
| **토큰 경제** (검사를 서브프로세스로 · 증거 상한 · 리프 브리프 최소화) | 오케스트레이션 표에 암시적. **명문화 안 됨** |

**이 일곱이 실제로 효과가 있는지가 미측정이다.**

---

## 3. unlazy 를 베끼면 안 되는 것 — 실측한 결함 15 건

세 조건이 4 라운드 동안 unlazy 의 `scripts/`+`templates/` 451 줄에서 낸 것이다.
**가져오는 형식·설계에 그대로 따라오면 안 된다:**

1. `gate-check.mjs:23` — `--timeout` 없으면 **첫 인자가 항상 버려진다** → 지정한 파일을 무시하고 `exit 0`
2. `install-hooks.mjs:43-46` — 자기 항목을 **디렉터리 이름**으로 식별 → 이름이 다르면 제거 불능
3. **③ `stop-hook.mjs` 의 진행 해시** — 게이트 의미와 무관한 편집(빈 줄 하나)에도 해시가 바뀌어
   **카운터가 영원히 1** → 「6 회 무진전이면 해제」 안전판이 안 돈다
4. `stop-hook.mjs` — 상태 파일을 못 쓰는 환경에서 **영원히 block** (실패 모드가 위험 쪽으로 열림)
5. **파서 경계** — 게이트 뒤 무관한 산문에 들여쓴 `EVIDENCE:` 한 줄이면 **Stop 훅이 차단을 완전히 포기**
6. **들여쓴 게이트를 파서가 못 본다** — `ALL MET` + 화면에는 `- [ ]` 가 남아 있고,
   `gate-check` 가 **자기 규칙(`[x]` + `EVIDENCE: pending` = UNMET)을 어기는 파일을 스스로 만든다**
7. `tail()` 이 EXPECT 매칭 줄이 아니라 **마지막 2 줄**을 증거로 박는다 → 증거가 판정을 재현 못 함
8. ABANDON ID 에 쉼표가 붙으면 매칭 실패 · 같은 ID 두 개면 하나가 둘 다 처리
9. EVIDENCE 줄이 없으면 `gate-check` 는 `ALL MET`, `stop-hook` 은 `block` — **두 도구가 반대 판정**
10. `expectMatches` 가 `/…/` 꼴 리터럴 경로를 정규식으로 오인 · CRLF 를 조용히 LF 로 · `--timeout 0` 이 falsy 로 무시됨 · `gates/` 에 `.md` 로 끝나는 디렉터리가 있으면 EISDIR 로 전체 크래시 · id 폴백 규칙이 두 스크립트에서 다름

★ **그리고 형식 이탈이 「형식 오류」가 아니라 「N 개 미충족」으로 보고된다.**
실험 2 의 조건 U 가 게이트 31 개를 만들고 증거까지 다 적었는데
`gate-check` 가 `UNMET: 31 (met: 0)`, `stop-hook` 이 차단했다 —
`EVIDENCE:` 를 별도 줄이 아니라 같은 줄에 썼다는 이유만으로.
**우리 구현은 형식 이탈을 형식 오류로 보고해야 한다.**

---

## 4. 착수 시 걸리는 것

- **§11③ 에 정면으로 걸린다.** 가져올 셋이 전부 「이 회차가 만드는 장치」다.
  **(가)/(나) 를 착수 시점에 고르는 것이 첫 결정**이고, 장치를 만들어야 하므로 **(나)** 가 맞다.
- **RED 관측 후보**: 열린 체크박스 90 · 계기판 44/44 · 훅 `Stop` 0 개 · 완수 조건의 `CHECK` 0 개
- **음성 대조 후보**:
  - 검사를 붙였는데 **어느 회차에서도 한 번도 실패하지 않으면** — `CHECK` 가 자기충족이다
    (실측: `CHECK: echo "품질 확인함"` / `EXPECT: 확인` 이 한 줄도 안 짜고 `ALL MET`)
  - 훅이 **한 번도 안 막으면** 너무 느슨하고, **매번 막고 상한으로만 풀리면** 너무 빡빡하다
  - `EVIDENCE` 가 **판정 근거를 재현 못 하면** 증거가 아니라 장식이다
- **퇴로**: 훅 정책이 위험하면 `Stop` 은 빼고 배선(⑤ 막힘 표기 + ① 검사 실행)만 세운다

---

## 5. 재조사 설계 — 문면 효과를 재려면

이번 두 실험이 **못 잰 것**을 겨냥한다.

### R1 — 대응물 대조 (값싸다 · 먼저 한다)

`unlazy/SKILL.md` 104 줄을 **문장 단위로 갈라** 각각에 대해 `/round` 규약에 대응물이 있나 대조한다.
2 절의 표가 손으로 만든 초안이다 — **전수로 다시 한다.** 산출: 「대응물 없음」 목록.

### R2 — 구축 과제로 A/B

**발견 과제가 아니라 구축 과제로 잰다.** unlazy 가 겨냥하는 것이 그쪽이다.

- 과제: 스펙이 있는 작은 것을 처음부터 만든다 (오라클 = 스펙 충족 여부를 기계가 재는 것)
- 조건: `P`(현행 규약) · `P+`(현행 + R1 이 고른 문면) · `B`(없음)
- 측정: 스펙 충족률 · **플레이스홀더/TODO 수** · 자기 발견 결함 수 · 라운드별 개선량
- ★ **④ 드리프트를 재려면 라운드를 6 이상으로.** 4 는 짧다(실측)

### R3 — 붙였을 때 손해가 없나

문면을 더하면 규약이 길어진다. **길이가 비용**이다.
`P` 대 `P+` 로 **같은 과제**를 재고, 개선이 없으면 **안 넣는다.**

### ⚠ 이번 실험에서 배운 설계 함정 — 되풀이하지 마라

1. **정답표를 에이전트가 닿는 곳에 두지 마라.** 실험 2 라운드 3 에서 조건 U 가
   `answers.json`·사전 등록 문서를 찾아냈고 **막힘으로 닫아야 했다.**
2. **과제문이 답을 정하지 않게 하라.** 실험 1 은 *"재현 증거를 대라"* 가 ⑦ 을 미측정으로 만들었고,
   실험 2 는 *"30 개"* 라는 수를 뺐기 때문에 ⑧ 이 관측됐다.
3. **길이는 줄 수로 재지 마라.** 한국어 줄이 짧아 1.65 배로 보였는데 낱말로는 오히려 적었다.
4. **오라클의 각 항목이 실제로 발화하는지 측정 전에 확인하라.** L1 다섯 중 셋이
   「스타일 위험」이었고 세 조건 다 옳게 「동작 결함 아님」으로 판정했다 — **내 오라클이 틀렸다.**
5. **채점기도 장치다.** 마크다운 셀 안의 `||` 로 셀이 쪼개져 거짓 「미재현」이 났다.
   **자동 계수는 손 검산과 대야 한다.**

---

## 6. 이 조사가 못 잰 것

- **조건마다 세션 하나** — 통계가 아니라 관측이다
- **④ 드리프트**: 두 실험 다 미관측. 4 라운드가 짧다
- **⑦ 조기 포기**: 실험 1 은 과제문이 강제, 실험 2 는 셋 다 L3 6/6 → **둘 다 미측정**
- **정밀도**: 실험 2 에서 거짓 양성이 세 조건 다 0 → **미측정**
- **⑧ 전수**: 셋 다 30/30 → **안 갈렸다**
- 실험 2 의 오라클 노출로 **라운드 4 취소** — 조건 간 비교가 원리상 불가능해졌다

★ **그리고 이 조사를 하는 동안 내가 여섯 번 틀렸다** — 여러 줄 조건 분류 · 셀 파싱 ·
불안어 오검출 둘 · 브리핑 길이 척도 · 오라클의 L1 셋 · 정답표 노출.
**전부 실측이 잡았고 산문 비교는 하나도 못 잡았다.** 다음 회차도 같은 자를 쓰길.

---

## 7. 원자료 — 어디에 있나

앞 세션의 조사 산출은 **세션 스크래치패드**에 있고 **세션이 끝나면 사라질 수 있다.**
아래는 재생성 방법이다.

```
scratchpad/unlazy/                  # git clone https://github.com/Leonxlnx/unlazy
scratchpad/exp/                     # 실험 1 — 열린 과제
  00-preregistration.md             #   사전 등록 + 정정 2 건
  brief-U.md · brief-P.md           #   조건별 브리핑
  returns/{U,P,B}{1..4}.md          #   원 반환문 12 편
  score.py                          #   채점기
scratchpad/exp2/                    # 실험 2 — 닫힌 과제
  00-preregistration.md             #   사전 등록 + 정정 2 건 + 막힘 기록
  build.py · answers.json           #   코퍼스 생성기 = 정답표
  corpus-{U,P,B}/mod_01..30.py      #   모듈 30 개
  returns/                          #   원 반환문 8 편 (+ U3-OILED.md)
  score2.py                         #   오라클 대조 채점기
scratchpad/report/unlazy-round.html # 대조 보고서 원본
```

**재생성:**

- **R1(문면 대조)에 필요한 것은 두 파일뿐이다** — `unlazy/SKILL.md`(클론하면 된다)와
  `.claude/skills/round/SKILL.md`. 원자료 없이 바로 할 수 있다.
- **브리핑 만드는 법**: U 는 `SKILL.md` 의 frontmatter 이후 본문.
  P 는 `/round` 의 잠금·발견의 처분·막힘·검증·종료 보고·끝을 정하는 자 절 발췌.
  ⚠ **링크를 치환해 참조를 끊지 마라** — 실험 2 에서 형식 참조가 끊겨 조건 U 가
  스펙과 다른 게이트 형식을 썼고 도구가 전부 거부했다.
- **길이는 바이트와 낱말로 맞춘다. 줄 수로 재지 마라.**
