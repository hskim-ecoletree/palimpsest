---
description: 좌표 하나를 만지기 전에 무엇이 걸려 있는지 본다
---

`pal touch $ARGUMENTS` 를 돌리고 그 산출을 그대로 읽어라.

답에는 **걸린 것**과 **지켜보는 것**이 함께 온다. 그리고 봉투에 이 빌드가 **못 만든
자리**가 실린다 — 그 자리는 「없다」가 아니라 「안 봤다」이므로, 비어 있다고 해서
문제가 없다고 읽지 마라.
