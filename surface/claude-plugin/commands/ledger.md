---
description: 이 저장소에서 무엇을 보았고 무엇을 보지 않았는지 본다
---

`pal ledger` 를 돌리고 그 표를 그대로 읽어라.

`unsupported` 와 `unrecognized` 에 있는 파일들에는 **좌표가 없다.** 앞으로 어떤 답에도
안 나오고, 그것은 「그 파일에 아무 일도 없다」가 아니라 **「우리가 안 봤다」**다.
