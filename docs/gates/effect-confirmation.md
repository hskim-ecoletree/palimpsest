# 효과 확인 — 판정 기록

> 회차 [`2026-09-11-effect-confirmation`](../../.palimpsest/rounds/2026-09-11-effect-confirmation/intent.md)
> · 착수 커밋 `8604d62`
> **이 문서는 회차가 건드린 코드보다 작다.** 절 이름은 넷으로 고정한다.

## 합격선

측정 **전에** 등록한 것은 잠긴 의도의 `## 완수 조건` **50 개**이고, 이 문서는 그것을
옮겨 적지 않는다 — **원장이 둘이면 갈린다.** 여기서 지는 것은 **판정**이다.

- **RED 관측** — ㉢ 은 `observations/red-129.txt`(`rc=1` · 의도 저장소가 **있는데도** 실패).
  ㉡ 은 「사전순 최대를 고른다」가 코드에 실재했고 시험 셋이 그것을 잰다. ㉠ 은 「둘이 같은
  글자다」가 `min` 한 줄로 실재했다.
- **음성 대조** — 자리마다 **끄고 돌렸다.** ㉡ 둘 FAILED · ㉠ 셋 FAILED(처음엔 둘이었고
  그 사실이 시험 하나가 항등식이었음을 드러냈다) · ㉢ 둘 FAILED, 그리고 **버린 갈래로
  고친 사본에서 첫 시험은 통과하고 둘째가 잡았다.**
- **차선책** — 넷을 등록했고 **하나도 안 썼다.** 문마다 왜 안 열렸는지를 적는다:
  **1** 은 *"`C2` 가 **0** 이면"* 인데 `C2` 의 판정값은 0 이 아니라 **미측정**이다 ⟨칸 2⟩ ·
  **2**(`B5` 산출 경로)와 **3**(자리 빼기)은 발동 사유가 안 났다 ·
  **4** 는 *"`D2` 가 「아직 못 답한다」로 나면"* 인데 **`D2` 가 통과했다** ⟨칸 15⟩.
  ⚠ **이 줄이 회차 중에 두 번 움직였다.** 집필 시점 *"하나도 안 썼다"* → 칸 10 이 차선책 4 를
  발동해 *"하나를 썼다"* → **칸 15 가 그 발동을 거둬 다시 *"하나도 안 썼다"***. 처음 값으로
  돌아왔지만 **같은 문장이 아니다** — 지금은 문마다 안 열린 까닭이 적혀 있고, 그 사이에
  ⟨칸 12⟩ 가 이 줄의 미갱신을 `MS-20` 으로 원장에 실었다.

## 판정

| 판정 | 조건 |
|---|---|
| 통과 | A1 A1-b A1-c A1-a A2 A2-b A2-c A3 A3-a A4 A5-b A5-d A5-e A5-a A6 A7-a B1 B1-a B1-c B1-d B1-e B2 B3 B4 B5 B5-a B6 C2-a C3-a C3-b C5 D1 D2 D3 D4 D4-a E1 E2 E4 E6 |
| 반증 | A5 A7 C1 C3 C4 |
| 대조불가 | — |
| 미측정 | A5-c C2 C2-b E3 E5 |

**검산** — 통과 40 · 반증 5 · 대조불가 0 · 미측정 5 = 50

### 반증 다섯 — **무엇이 반증됐는지 정확히 적는다**

**반증은 실패가 아니다** ⟨규약 §9⟩. 다섯 다 **「거짓을 실었다」가 아니라 「문면이 지정한 것이
안 섰다」**이고, 다섯 중 둘은 **회차가 스스로 모집단을 넓히면서 생긴 자리**다.

⚠⚠ **`A5` 는 독립 리뷰 라운드 1 이 뒤집었다** — 이 표가 한때 `A5` 를 **통과**로 싣고 있었다.
메인의 실측이 `git log <touch>..HEAD` 로 범위를 좁혀 첫 코드 변경 커밋을 뽑았고, **그 범위에서는
조상 관계가 구성상 언제나 참**이라 검사가 원리상 못 빨개졌다. 리뷰어가 문면의 사상 규칙
(*"그 목록의 파일을 건드린 첫 커밋"*)대로 다시 재서 잡았고, 메인이 명령으로 재현했다.

| 조건 | 무엇이 안 섰나 | 이 판정을 뒤집는 것 |
|---|---|---|
| **`A5`** | 문면은 *"`touch/<이슈>.txt` 커밋이 **그 자리의 첫 코드 변경 커밋**의 조상이고 두 SHA 가 다르다"* 이고, 사상 규칙을 괄호로 **못 박았다** — *"(그 목록의 파일을 건드린 첫 커밋)"* ⟨`PM3-15` — 산문으로 남기면 ⟨결정론적⟩ 태그가 거짓이 된다⟩. ㉡ 의 사전 등록 목록은 `xtask/src/main.rs` 이고, `8604d62..HEAD` 에서 **그 파일을 건드린 첫 커밋은 `8644835`**(*"무시 대상 좌표가 CI 를 죽이던 자리를 고치고 `A7-a` 에 판정값을 단다"* · 22:07)다. `git merge-base --is-ancestor 14f0f92 8644835` → **`rc=1`**. ㉠·㉢ 은 `rc=0` 이다 | 사상 규칙을 *"그 자리의 **작업**으로 그 파일을 건드린 첫 커밋"* 으로 읽으면 ㉡ 은 `398d233` 이고 통과다. **그 읽기는 산문이라 `PM3-15` 가 금한 자리**로 되돌아간다 — 같은 자로 `C1`·`C4` 를 반증으로 판정했으므로 여기서만 다르게 읽지 않는다 |
| **`A7`** | 문면이 *"커밋 순서는 **`A1`·`A5` 와 같은 자**로 판정한다"* 라 적는다. 그 자로 ㉡ 을 재면 `git merge-base --is-ancestor 3637ea9 8644835` → **`rc=1`** 이다. ㉠·㉢ 은 `rc=0` | `A5` 와 같이 움직인다 — `A5` 가 통과로 읽히는 어떤 독해에서도 `A7` 은 통과다. **둘을 다르게 읽는 것만 안 된다** |
| **`C1`** | 문면은 *"차이마다 **좌표 두 끝**이 붙었다 — `touch/<이슈>.txt:N` 이 원인이고 `파일:줄` 이 결과다"* 인데, 소유자 칸 3 이 모집단을 **「사전 등록 ↔ 실제 변경 전부」**로 넓히면서 원인 끝이 touch 좌표가 **아닌** 행이 들어왔다. 전수로 셌다 — **13 행 중 3 행**만 touch 좌표이고 나머지 **10** 의 원인은 저장소 검사·컴파일러·정반합이다. delta 파일들은 그 사실을 정직하게 적었다(`**touch 아님** — …`) | 칸 3 의 *"뒷절은 모집단의 정의가 아니라 **기입 형식의 요구**"* 를 *"원인 끝은 원인이 touch 인 행에만 요구된다"* 로 읽으면 통과다. **도구는 조건 문면을 못 줄인다** ⟨칸 13⟩ 를 같은 자로 적용해 그 읽기를 안 골랐다 ⟨`MS-23`⟩ |
| **`C3`** | 두 요구가 각각 안 섰다. ⑴ *"프롬프트 전문을 `effect/judge-prompt.md` 로 보존한다"* — 띄운 인스턴스는 **다섯**인데 그 파일이 싣는 판은 **넷**이다(`grep '판 [0-9]'` 가 1·2·3·4 만 낸다). `judge-roster.md` 5 행과 `effect/129-judge-2.md:5` 가 *"판 5"* 를 부르는데 **그 판이 없다.** ⑵ *"넷 다 바이트로 그대로 싣는다 — 자르거나 요약하면…"* — 4 번 인스턴스가 받은 ③ 이 네 파일 중 앞의 둘뿐이었고 `effect/129-judge-1.md:3-8` 이 **스스로** *"조건은 문면대로 못 지켰다"* 라 적는다 | **되돌릴 수 없다** — 판정자가 실제로 읽은 바이트를 지금 지어내면 증거 위조다. 뒤집는 길은 없고, `C3` 이 존재하는 까닭(*"판정자가 읽은 바이트가 사후에 복원된다"*)이 두 자리에서 깨진 것을 판정으로 적는 것이 전부다 ⟨`MS-24`⟩ |
| **`C4`** | 문면의 **「근거와 함께」**가 살아 있고(칸 13), `## 효과` 표의 「참이었나」 칸 **열셋 중 여덟**이 근거 절 없는 맨 「해당 없음」이라 그것을 못 채운다. ★ **그 여덟은 사실로 전부 참이다** — 판 4 R3 의 반이 touch 산출 셋에 전수 `grep` 을 걸어 확인했고 합이 받았다 | 칸 4 의 *"존재는 합격선, 값은 아니다"* 가 **「근거와 함께」까지 덮는다**로 읽히면 통과다. 소유자가 칸 13 에서 **「안 덮는다」**를 골랐다 |

★★ **`A5` 가 반증인 까닭이 §11③ 이 이름 붙인 자리다.** `8644835` 는 **이 회차가 자기 장치를
고친 커밋**(회차 레코드 검사의 무시 좌표 면제 ⟨`MS-04`⟩)이고 ㉡ 의 작업과 무관한데,
**㉡ 이 고칠 파일과 같은 파일에 산다.** `intent.md` `## 순서` 가 그것을 착수 때 미리 적었다 —
*"고치는 손과 판정하는 손이 같은 파일에 있는 것 자체가 §11③ 이 이름 붙인 자리다."*
그 문장이 예고한 사고가 **조건 하나를 실제로 깨뜨렸다.**

⚠ **`A5-a` 의 음성 대조가 이 축을 안 덮는다** ⟨독립 리뷰 R1 · **금지역**⟩. 그 대조는 변경
커밋을 **인자로 받아** 순서를 뒤집었고, 실측은 그 커밋을 **스스로 뽑았다.** 그러므로
대조가 뒤집은 축과 실측이 쓴 축이 **다르다** — 대조는 *"순서가 뒤집히면 빨개지나"* 를 쟀고
실측이 틀린 자리는 *"어느 커밋을 첫 커밋으로 뽑나"* 였다. **음성 대조가 실측 절차를 안 덮는
자리**이고, 그것을 잡은 것은 대조가 아니라 독립 리뷰다.

⚠⚠ **`A7` 은 독립 리뷰 라운드 2 가 잡았다 — 라운드 1 이 `A5` 를 뒤집었는데 그 자가 `A7` 에
전파되지 않았다.** 이 표가 `A5` 에서 *"여기서만 다르게 읽지 않는다"* 를 못 박아 놓고
**바로 그 완화 독해를 `A7` 에만 쓰고 있었다.** `grep -rn "8644835"` 가 회차 문서 여덟에
적중하는데 `A7` 을 다시 잰 자리는 **0** 이었다. ★ **한 조건을 뒤집으면 그 조건의 자를
인용하는 조건을 전수로 다시 재야 한다** — 그것이 이 라운드가 산 교훈이다.

⚠ **다섯 다 이 회차에서 안 고친다.** `C1`·`C3` 은 **원리상 못 고치고**(모집단은 소유자가
정했고 잃어버린 바이트는 못 되살린다), `C4` 의 여덟 셀은 **칸 12 가 세운 순서가 판정 중
수리를 금했고** 판정이 난 지금 고치면 그것은 반증을 사후에 지우는 것이다. 수리는 `#140` 이 진다.

### 미측정 다섯 — **「안 쟀다」이지 「됐다」가 아니다**

| 조건 | 왜 안 쟀나 |
|---|---|
| **`A5-c`** | 판 2 가 라운드 1 에서 **미측정**으로 닫았다 |
| **`C2`·`C2-b`** | 소유자 칸 2 가 **미측정으로 닫고** 조건 문면 셋을 `#140` 으로 승격했다. ⚠ 합이 함께 적었다 — *"`## 차선책` 1 은 「`C2` 가 **0** 이면」인데 내 판정은 0 이 아니라 **「안 쟀다」**다. 차선책 1 은 안 열린다"* |
| **`E3`** | 종료 보고가 아직 안 섰다. ⟨정반합⟩ 이라 정반합이 판정한다 |
| **`E5`** | CI 는 회차의 **마지막 커밋**에 붙어야 재어진다. 그 커밋이 아직 없다 |

★ **`E5` 는 원리상 자기를 못 담는다** — 마지막 커밋의 런 결론을 그 커밋 안에 적을 수 없다.
이 회차는 앞 회차와 같은 형태로 푼다: 판정 표를 밀고, 런이 초록인 것을 보고, **그 사실을
적는 커밋이 회차의 마지막 커밋이 된다.** 그 커밋의 런은 **다음 회차의 착수 기준선**이다
(이 회차가 `8604d62` 를 그렇게 받았다).

### 살아남은 반론의 해악도 — **판정값과 함께 싣는다** ⟨`DL3-41`⟩

두 설계문이 요구한 자리다(*"판정값이 게이트 표에 실릴 때 살아남은 반론의 해악도가 같이 안
실리면"*). 판 4 R3 **금지역 1 · 실패 1 · 거짓신호 8 · 미관 3 = 13** ·
판 5 R2 **금지역 1 · 실패 0 · 거짓신호 10 · 미관 2 = 13** ·
판 5 R1 **금지역 1 · 실패 0 · 거짓신호 11 · 미관 4 = 16**.

## 효과

⚠ **`e2-run.txt` 의 sha256 은 재현 앵커가 아니다** ⟨독립 리뷰 R1⟩. 그 파일은 전 출력
**8550 줄 · 466960 바이트**의 해시를 적고 **머리 40 줄**만 싣는데, **그 해시를 댈 대상이
저장소에 없다.** 그리고 그 해시는 **HEAD 에 묶인 값**이다 — 같은 명령을 `39b0fa3` 에서 다시
돌리니 **27350 줄 · 1624470 바이트**(`21e6263c6868a4d6…`)가 나왔다. 산출이 저장소의 문서 수에
따라 자라기 때문이다. **그러므로 그 해시는 「그때 그 바이트였다」는 무결성 기록이지
「다시 돌리면 같은 것이 나온다」는 앵커가 아니고**, 이 줄이 그 구별을 적는다.

**돌린 것**: 사람(메인)이 셸에서 `pal` 을 직접 불렀다 — `cargo test` 도 `cargo xtask check`
도 CI 도 아니다. 산출은 `.palimpsest/rounds/2026-09-11-effect-confirmation/effect/e2-run.txt`
에 바이트로 있다(HEAD `9e092e3`).

### 이 회차의 산출이 실제로 돌았다

| 명령 | 무엇이 달라졌나 | 종료값 |
|---|---|---|
| `pal query narrative.unbound` | ㉢ 의 **RED 를 관측한 바로 그 명령**이다. `observations/red-129.txt` 에서 `Error: 개체를 남기지 못했다` · `rc=1` 이었고 **`.palimpsest/intent.redb` 가 있는데도 그랬다**. 지금 그 파일은 그대로 있고(1351680 바이트) 명령이 **선다**. 새 줄 `이름이 아직 없어 뺀 조각 4733` 이 이 회차가 세운 것이다 | `1` → **`0`** |
| `pal ledger` | ㉠ 의 **정체성 블록**이 산출한다 — `순서에 취약 20 · 등급이 낮음 3306 · 정확 6 · 불가 0 · 합 3332`. 착수 시점에는 이 블록이 **없었다**(`observations/identity-before.txt`) | `0` |
| `pal symbols /dev/null` | 아는 언어를 이름으로 산출한다 — `#66` 전제 ② 를 재는 자리 | `1`(인자가 틀렸을 때의 정상 경로) |
| `pal doctor` | 검사 여섯 중 넷이 「잔여」로, 못 잰 것을 **못 쟀다고** 적는다 | `0` |

### `#66` 의 낡은 전제 둘 — 정정문 ⟨`D1`⟩

원 이슈가 전제로 삼은 두 문장이 **지금 거짓이다.** 무엇으로 쟀는지 함께 적는다.

| `#66` 이 적은 것 | 지금 값 | 잰 명령 |
|---|---|---|
| *"결박 가능한 좌표 **7 개** · 파일 **1 개**"* | **심볼 3332 · 파일 142**(`parsed`). 결박 가능한 언어는 Rust(L1 · 141 파일)와 TypeScript(L2 · 1 파일) 둘이고, 결박 **불가**가 언어 7 개 · 812 파일이다 — *"7 개"* 가 가리키던 것이 그 사이에 **반대쪽**이 됐다 | `pal ledger` |
| *"추출기는 Kotlin·Java·JavaScript·TypeScript **넷뿐**"* | **다섯이다** — `Kotlin · Java · JavaScript · TypeScript · Rust`. 명령이 그 목록을 오류문에 그대로 싣는다 | `pal symbols /dev/null` |

⚠ **둘 다 이 회차가 만든 변화가 아니다** — Rust 추출기는 `2026-08-20-rust-extractor` 회차가
세웠다. 이 회차가 한 것은 **그 전제가 낡았다는 사실을 명령으로 재어 적은 것**이다.
같은 내용을 이슈 `#66` 에도 남긴다.

### `#66` 의 물음 1 — **「바닥까지만 요구다」** ⟨`D2` = 통과⟩

물음 원문: *"이 도구가 **자기 자신을 큐레이션할 수 있어야 하는가**."*
⟨정반합⟩ 조건이고 판 5 가 **두 라운드**를 돌았다 — 산출은
`dialectic/5-{design,preflight,thesis,antithesis,synthesis,referee}.md` 와
`dialectic/5-{preflight-r2,thesis-r2,antithesis-r2,synthesis-r2}.md` · `dialectic/r3-referee.md`.
⚠ **`dialectic/5-thesis.md` 에는 정정문이 붙어 있다** — 아래
`### 정반합 산출물 — 정오표와 성격` 을 함께 읽어라.

**답 값 — 소유자가 2026-09-12 에 쓴 규범형 문장 하나다** ⟨`intent.md` `## 승격` 칸 15⟩:

> ## **자기 저장소 큐레이션은 바닥까지만 제품 요구다.**
>
> 즉 `[f11.pass]` ⑧ 의 *"자기 저장소 실사용 · 결박 ≥ 1"* 이 **요구**이고, 그 위는 요구가
> 아니다.

**`D2` 는 통과다.** 조건 문면이 *"…에 **답했다**"* 이고 답이 섰다.

#### 어떻게 그 문장에 닿았나 — ★ **게이트가 자기 반증 검사를 안 돌린 것이 드러나서다**

라운드 1 은 **「아직 못 답한다」**를 냈다. 근거는 *"권위 재료 셋이 갈리고 **어느 것도**
명제를 규범형으로 말하지 않는다"* 였고, 이 절이 그 자리에 **자기 반증 조건을 등록**했다 —
*"셋 중 어느 하나가 규범형 문장을 이미 담고 있음이 드러나는 것."*

**그 검사가 한 번도 안 돌았다.** 판 5 라운드 2 의 반(反)이 그것을 잡았고 합(合)이 1 차
원문에서 확인했다:

| 잰 것 | 값 |
|---|---|
| 이 절이 재료 둘째로 적던 경로 | `docs/adr/0027-rust-first-class.md` — **저장소에 없는 파일** |
| 실재하는 파일 | `docs/adr/0027-the-instrument-must-reach-its-own-floor.md` |
| 그 파일의 제목 `:1` | **「자를 자기 바닥에 닿게 한다.」** — 명령형이고 파일명이 `must` 를 진다 |
| 상태 `:3` | **채택** (2026-08-20) |
| `:14` | *"`[f11.pass]` ⑧ 은 「자기 저장소 실사용 · 결박 ≥ 1」을 **요구했고** 그 하한은 넘었다"* |

★★ **경로가 죽어 있어 아무도 그 문서를 안 열었고, 그래서 「어느 것도」라는 전칭 부정이
한 번도 안 재어진 채 사실로 적혔다.** 이 회차가 `intent.md` 에 등록한 금지역 **둘에 동시에**
든다 — 「사실이 아닌 것을 사실로」(*모르는 것을 안다고 적는다*)와 「측정이 죽은 가지」
(*검사가 있는데 실제로는 아무것도 재지 않는다*). **쓴 것은 메인이다.**

⚠ **`cargo xtask check` 의 「죽은 링크 부재」가 이것을 원리상 못 잡는다** — 그 검사는
마크다운 링크(`[…](…)`)를 재고 이 자리는 **백틱 코드 참조**였다. 그 사실을 여기 적는다.

#### 권위 재료 셋 — 다시 잰 값

| 재료 | 무엇을 말하나 | 규범형 문장을 담나 |
|---|---|---|
| `corpus/criteria.toml` ⑧ | 바닥 **≥ 1** | **아니다** — *"이 저장소의 하한 관례"* 라는 같은 문장이 열 자리에 있다. 관례의 진술이지 요구의 선언이 아니다 |
| [`ADR-0027`](../adr/0027-the-instrument-must-reach-its-own-floor.md) `:1`·`:3`·`:14` | **자를 자기 바닥에 닿게 한다** · 채택 · ⑧ 을 *"요구했고"* | ★ **담는다.** 제목이 명령형이고 파일명이 `must` 이며 `:14` 가 그 「바닥」을 ⑧ 로 묶는다. **이것이 등록된 방아쇠를 당긴 재료다** |
| `docs/plan/00-goals.md:316` | 자기 온톨로지는 **비목표** | **아니다** — 인접하지만 다른 명제다. 온톨로지를 안 만드는 것과 자기를 큐레이션하지 않는 것이 같지 않다 |

⚠ **`ADR-0027` 하나로 `D2` 를 닫지 않았다.** 그 문서는 `:3` 에서 *"`#66` 종료 시점에 발행"*
이라 적어 **`#66` 의 하류 산물일 수 있다** — 하류 문서로 상류 물음을 답하면 순환이다.
판 5 의 합이 *"저장소 문면만으로는 못 가른다 — ADR 상태를 정한 소유자의 한 문장이 필요하다"*
로 **소유자에게 올렸고**, 소유자가 그 문장을 썼다. **순환을 끊은 것이 소유자의 발화다.**

#### 「아직」이 요구했던 둘 — 둘 다 충족됐다

라운드 1 이 *"안 적으면 「못 답한다」가 교착과 구별되지 않는다"* 며 등록한 둘이다.

- **무엇이 있으면 답하나** — *"새 측정이 아니라 **명제를 규범형으로 적는 문장 하나**"* →
  **소유자가 썼다** ⟨2026-09-12 · 칸 15⟩.
- **누가 낼 수 있나** — *"**소유자다**"* → **소유자가 냈다.**

★ **그 등록이 이 회차에서 실제로 값을 냈다.** 「아직」을 적으면서 무엇이 그것을 끝내는지를
함께 적었고, 적힌 그대로 끝났다. **교착이 아니었다**는 것이 사후로 확인된다.

★★ **능력과 규범이 갈렸던 것도 그대로 산출이다.** **능력은 섰고**(위 `### #66 의 물음 2` 가
`pal ledger` 로 Rust L1 141 파일 · 심볼 3332 를 낸다) **규범은 안 서 있었다.** 잠긴 의도가
착수 시점에 *"목표 1(큐레이션)은 이 표본 위에서 원리상 안 재어진다"* 와 *"그 「측정 불능」
자체가 `#66` 의 물음 1 에 대한 값이다"* 를 미리 적었고 ⟨`intent.md:105-116`⟩, **그 측정
불능이 소유자의 한 문장으로 끝났다.**

**이 판정을 뒤집는 것** — 소유자가 그 문장을 철회하거나 다르게 다시 쓰는 것. 저장소 재료는
이제 이 값을 뒤집지 못한다 — 규범 명제라 측정이 혼자 못 답하기 때문이고, 그것이 `D2` 문면의
⚠ 가 처음부터 적은 것이다.

⚠ **그래서 `#66` 을 닫는다** — **차선책 4 는 안 열린다.** 그 문은 *"`D2` 가 「아직 못 답한다」로
나면"* 이고 그 갈래가 아니게 됐다. 처분은 아래 `### #66 을 닫는다` 가 진다.

### `#66` 의 물음 2 — 두 갈래의 현재 상태 ⟨`D3`⟩

물음 원문: *"**Rust 추출기**인가, 아니면 ⑧을 **다른 저장소의 실사용**으로 다시 정의하는가."*
⚠ **답을 문면에 담지 않는다** — 갈래마다 **명령이 값을 낸다**(`CA1-19`).

| 갈래 | 지금 상태 | 잰 명령 | 값 |
|---|---|---|---|
| ⓐ **Rust 추출기** | **섰다.** 자기 저장소가 L1 로 파싱되고 심볼이 3332 개 선다 | `pal ledger` | `Rust L1 ordinal 141 파일` · `정체성 심볼 3332` |
| ⓑ **다른 저장소의 실사용** | **안 섰다.** 대장이 보는 저장소가 **하나**이고 그것이 자기 자신이다 | `pal ledger` 머리 | `저장소 선언 1 · 본 것 1` · `Snapshot palimpsest@9e092e3+worktree` |

**ⓐ 가 값을 내고 ⓑ 는 안 낸다** — 그것이 이 두 줄이 산출한 것이고, **그 값 자체가 `D3` 의
답이다.**

⚠ **앞 판은 이 자리에 *"어느 갈래를 고를지는 `D2` 의 정반합이 판정한다"* 라고 적었고 그것이
틀렸다** ⟨2026-09-12 정정 · 판 5 라운드 1 의 정(正) 부수 발견 ②⟩. 갈래 물음은 **`D3` 의
것**이고 `D2` 가 아니다. 둘이 그렇게 갈린다:

- [`ADR-0027`](../adr/0027-the-instrument-must-reach-its-own-floor.md) 의 **「대안과 왜 아닌가」** 표가 2026-08-20 에 갈래 ⓑ
  (*"⑧ 을 「다른 저장소의 실사용」으로 재정의"*)를 **이미 기각했다** — *"하한을 낮추는 문이다.
  물음은 「그 1 이 무엇 위에 서야 하는가」였고 재정의는 그것을 안 답한다."*
- 판 5 의 설계문 `dialectic/5-design.md` 의 `## 이 판이 안 다루는 것` 첫 항이 이 물음을
  **`D3` 의 것**으로 못 박고 *"이 판은 그 산출을 사실로 받는다"* 라 적는다. 그러므로 `D2` 가
  이 갈래를 판정할 자리가 **원리상 없었다.**

`D2` 가 판정하는 것은 물음 **1**(*"이 도구가 자기 자신을 큐레이션할 수 있어야 하는가"*)이고
그 답은 **위** `### #66 의 물음 1` 이 진다.

⚠ **그 절이 이 절보다 앞에 있는 것이 맞다** — 물음 1 이 규범이고 물음 2 가 능력이라,
*"능력은 섰고 규범은 안 서 있었다"* 는 이 회차의 산출이 그 순서로 읽힌다.

### `#66` 을 **닫는다** ⟨`D4` = **통과**⟩

`D4` 의 문면은 *"`#66` 을 닫았다"* 이고 **이 회차가 닫는다.** 그러므로 `D4` 는 **통과**다.

⚠⚠ **이 값이 회차 중에 한 번 뒤집혔다 — 그 이력을 지우지 않는다.**

| 언제 | `D4` | 무엇이 그렇게 만들었나 |
|---|---|---|
| 2026-09-12 ⟨칸 10⟩ | **반증** | `D2` 가 「아직 못 답한다」로 나왔고 **차선책 4**(등록된 길)가 열렸다. 소유자가 *"답 없이 차선책 4 로 닫는다"* 를 골랐다 |
| 2026-09-12 ⟨칸 15⟩ | **통과** | 판 5 라운드 2 가 **게이트 자신이 등록한 반증 검사가 한 번도 안 돌았음**을 드러냈고, 소유자가 규범형 문장을 써서 `D2` 를 통과시켰다. **차선책 4 는 안 열린다** — 그 문은 *"「아직 못 답한다」로 나면"* 이다 |

★ **칸 10 이 그때 틀렸던 것이 아니다.** 저장소가 가진 재료로는 답이 안 섰고 **그 「측정
불능」 자체가 값이었다.** 뒤집은 것은 **새 재료**이고, 그 재료는 저장소에서 나오지 않았다 —
소유자의 문장 하나다. 규약이 *"승격은 나가는 것이 아니라 잠금으로 되돌아가는 우회로"* 라
적는 그 자리다.

★ **`#66` 이 이 회차에서 받은 것 넷:**

| `#66` 이 물은 것 | 이 회차가 낸 것 |
|---|---|
| 물음 1 — 자기 큐레이션이 **요구인가** | **「바닥까지만 요구다」** ⟨위 `### #66 의 물음 1` · 소유자 2026-09-12⟩ |
| 물음 2 — Rust 추출기인가, ⑧ 재정의인가 | **ⓐ 가 값을 내고 ⓑ 는 안 낸다** ⟨위 `### #66 의 물음 2`⟩ |
| 본문의 **낡은 전제 둘** | 명령으로 다시 재어 정정했다 ⟨위 `### #66 의 낡은 전제 둘`⟩ |
| 본문을 **현재형으로 인용하는 자리들** | 다섯을 한 규칙으로 안 덮고 따로 처분했다 ⟨아래⟩ |

**이슈에 남긴 것** — 위 넷을 댓글로 실었다:
[`#66` 댓글 · 2026-09-12T05:02:14Z](https://github.com/hskim-ecoletree/palimpsest/issues/66#issuecomment-5643614829)
⟨`D1` 이 *"같은 내용을 이슈에도 남긴다"* 를 요구한다⟩. **닫는 댓글이 그 뒤에 실렸다** —
[`#66` 닫는 댓글 · 2026-09-12T06:05:04Z](https://github.com/hskim-ecoletree/palimpsest/issues/66#issuecomment-5644071744) ·
규범형 문장과 닫는 사유를 진다. **이슈는 `CLOSED` 이고 `closedAt` 이 `2026-09-12T06:05:04Z` 다**(`gh issue view 66`).

⚠ **그 첫 댓글이 이 문서의 한 줄을 죽였다** ⟨판 5 R2 반론 10⟩. 앞 판이 `D2` 절에
*"`#66` … 마지막 갱신이 **2026-08-19**"* 를 판정 근거로 실었는데, `6e58dfa`(`D1` 집행)가
그 값을 `2026-09-12T05:02:14Z` 로 바꿨다. **`D1` 을 집행하면서 `D2` 의 근거를 깬 것**이고,
지금은 그 줄이 `D2` 절에서 없어졌다(답이 이슈 쪽 상태에 안 기대게 됐다).

### `#66` 을 **현재형으로** 인용하는 다섯 자리의 처분 ⟨`D4-a`⟩

어느 검사도 이슈 상태를 안 본다 — `#66` 이 닫혀도 이 다섯은 *"소유자에게 묻고 있다"* 를
계속 적는다. **다섯을 한 규칙으로 덮지 않는다** ⟨`PM3-16`⟩.

| 자리 | 무엇인가 | **처분** | 어디가 거짓이 되나 |
|---|---|---|---|
| `docs/gates/F12.md:312` | **동결 판정 문서** | **안 고친다** | *"[#66] 이 소유자에게 묻고 있다 (「Rust 추출기를 만드는가」)"* — **묻던 것이 이미 섰다.** 추출기는 `2026-08-20-rust-extractor` 회차에 들어왔고 지금 `pal ledger` 가 `Rust L1 ordinal 141 파일` 을 낸다 |
| `docs/gates/F12.md:398` | 같음(결함 표의 행선지 칸) | **안 고친다** | *"자기 적용이 구조적으로 못 선다 — 코어가 Rust 다 → **[#66]**(소유자에게 묻는 중)"* — **「구조적으로 못 선다」가 거짓이다.** 자기 저장소가 파싱되고 심볼 3332 이 선다 |
| `corpus/criteria.toml:10530` | **합격선 파일** | **안 고친다** | *"이 자리의 처분은 [#66] 이 소유자에게 묻고 있다"* — 같은 까닭 |
| `corpus/criteria.toml:10886` | 같음 | **안 고친다** | *"「이 도구가 자기 계획을 잰다」는 **Rust 추출기가 선 뒤**의 문장이고"* — **그 뒤가 왔다.** 조건절이 충족됐는데 문면이 아직 미래형이다 |
| `scripts/f12-verify.py:556-570` | **돌아가는 분류 코드** | ★ **고쳤다** | 앞 판은 *"추출기는 Kotlin·Java·JS·TS 넷뿐이다"* 를 **현재형 주장으로** 산출에 실었다. 문서가 아니라 **실행되는 코드가 거짓을 주장하는 자리**라 회차의 금지역 「사실이 아닌 것을 사실로」에 직접 든다 |

★ **넷과 하나를 가르는 자**: *"이 문장이 **판정의 기록**인가, **지금 돌아가는 주장**인가."*
동결 판정 문서와 합격선 파일은 **그때 그렇게 판정했다**는 기록이라 고치면 증거 위조다 —
거짓이 되는 자리를 **여기** 적는 것이 처분이다. 돌아가는 코드는 기록이 아니라 **지금
산출을 만든다.**

⚠ **`B2` 의 28 검사가 `scripts/f12-verify.py` 를 안 돌린다** — 고친 것이 검사로 안 잡힌다.
그 사실을 여기 적는다. 고친 뒤 문면은 `⑧ 합산 여부는 F12 판정 시점의 결정이므로 그대로
두고, 그 결정이 선 전제가 지금은 다르다`이고, 산출은 `docs/gates/effect-confirmation.md`
(이 문서)를 가리킨다.

### 차이마다 — 방향과 「touch 가 말한 것이 참이었나」 ⟨`E2`·`C4`⟩

모집단은 **사전 등록 ↔ 실제 변경의 차이 전부**다 ⟨소유자 결정 2026-09-12 · `intent.md`
`## 승격` 칸 3⟩. 세 자리 합 **열셋**이고 전문은 `effect/{126,79,129}-delta.md` §2 가 진다.
★ **방향의 값은 합격선이 아니다. 기입의 존재가 합격선이다** ⟨같은 곳 칸 4⟩.

| 자리 | # | 차이 | 원인이 touch 인가 | **방향** | **「touch 가 말한 것이 참이었나」** · 근거 |
|---|---|---|---|---|---|
| ㉡ `#126` | ㉮ | 사전 등록 표기(`&회차들`)와 실물(`&끝난`)이 어긋났다 | 아니다 | **나아졌다** — 사전 등록 §4 ⑴⑵ 의 내부 어긋남이 실물에서 ⑵ 쪽으로 풀렸다 | **해당 없음** — 대상이 없다 |
| ㉡ | ㉯ | `%ct` 를 고른 까닭을 주석에 적었다 | 아니다 | **나아졌다** — 근거가 코드에 섰다(`xtask/src/main.rs:6432-6433`) | **해당 없음** |
| ㉡ | ㉰ | 새 함수를 `check_ledger_pair` 뒤에 두는 **근거**가 바뀌었다 | **그렇다** ⟨`touch/126.txt:17`⟩ | **나아졌다** — 「보기 좋다」가 「정체성이 선언 순서에 안 걸린다」로 바뀌었다 | **참이었다.** `identity ordinal` 은 합친 값이고 그 값이 실제로 `ordinal` 이다. ⚠ **원인은 안 가른다** — 판별자 상한은 `Exact` 이고 낮춘 것은 추출기 등급이다(`MS-07`·`MS-09` · `ledger.rs` 의 `check_ledger_pair_는_순서에_취약하지_않다`) |
| ㉡ | ㉱ | **호출자를 찾는 걸음이 없어졌다** | **그렇다** ⟨`touch/126.txt:24`⟩ | **나아졌다** — 걸음 하나가 사라졌다 | **참이었다.** `effect/126-readnote.md:14` 가 `grep -n "check_ledger_pair" xtask/src/main.rs` 로 다시 확인했고 `:643` 한 자리뿐이었다 |
| ㉠ `#79` | ㉮ | 세는 메서드 이름 `센다` → `헤아린다` | 아니다 | **나아졌다** — 저장소 검사가 잡은 13 곳이 없어졌다 | **해당 없음** |
| ㉠ | ㉯ | 화면에 「그룹마다 첫 선언은 빠진다」를 더했다 | 아니다 | **나아졌다** — 그때 화면이 싣던 거짓이 줄었다. ⚠ **그 줄은 ㉲ 가 걷어냈다** — 셈법 자체가 바뀌어 더 안 참이 됐다 | **해당 없음** |
| ㉠ | ㉰ | 셋째 시험에 **짝**을 달았다 | 아니다 | **나아졌다** — 항등식이던 시험이 실제로 재게 됐다(`effect/79-delta.md:70-88` §4 가 1 차·2 차 산출을 나란히 싣는다 — 1 차의 끈 판에서 `check_ledger_pair_는_순서에_취약하지_않다` 가 **안 빨개졌고**, 짝을 단 2 차에서 셋 다 FAILED 다) | **해당 없음** |
| ㉠ | ㉱ | ⑶ 의 호출자 목록을 **`grep` 이 지었다** | **그렇다** ⟨`touch/79.txt:23`⟩ | **나빠졌다** — 제품이 이 물음에 **덜 답했다**. 사전 등록에 `grep` 결과를 먼저 안 적었으면 `defect.rs` 를 안 고쳤다 | **참이지만 덜 말했다.** 「호출자 3」은 **참조 엣지 기준**으로 참이고(`effect/79-readnote.md:39-41` — *"touch 가 거짓말을 한 것은 아니다"*), 파일 간 미해소 몫이 표시 없이 빠진다(`MS-08`) |
| ㉠ | ㉲ | 버킷 ① 의 술어가 `ordinal > 0` → **그룹 크기 > 1**(`순서에 취약 10 → 20`) | 아니다 ⟨**정반합**⟩ | **나아졌다** — 산출이 참이 됐다. 중복 그룹의 첫 선언도 순서가 바뀌면 `SymbolId` 가 움직인다 | **해당 없음** |
| ㉠ | ㉳ | `IdentityTally` 의 doc 주석 **세 자리**에서 ㉲ 뒤에 거짓이 된 문장을 걷었다 | 아니다 ⟨**정반합**⟩ | **나아졌다** — ㉲ 가 셈법을 바꾸면서 주석 넷(*"이 버킷은 「이름이 유일한 것」과 「그룹의 첫 선언」을 함께 담는다"* · *"셈법을 그룹 단위로 바꾸는 것은 … 이 자리에서 안 한다"* · *"둘째가 ①이다"* · *"판별자 상한은 `Exact` 이므로"*)이 거짓이 됐고 그것이 없어졌다. **㉲ 없이는 이 차이가 없다** ⟨`effect/79-delta.md:40`⟩ | **해당 없음** — 대상이 없다. 원인 끝이 `dialectic/1-synthesis-r2.md` 이고 `touch/79.txt:N` 이 아니다 |
| ㉢ `#129` | ㉮ | `Ingested::비어_있다()` 를 세웠다 | 아니다 ⟨컴파일러⟩ | **나아졌다** — *"안 물었다"* 와 *"물었는데 0"* 이 갈렸다 | **해당 없음** |
| ㉢ | ㉯ | `QueryCtx` 에 `narrative_unminted: 0` 을 더했다 | 아니다 ⟨컴파일러 `E0063`⟩ | **나아졌다** — 빠진 칸이 타입으로 드러났다 | **해당 없음** |
| ㉢ | ㉰ | 세는 낱말 `센다` → `헤아린다` | 아니다 ⟨저장소 검사⟩ | **나아졌다** — 「어색한 표현 부재」가 `narrative.rs:85` 를 잡았고 그 한 자리가 없어졌다. **㉠㉮ 에 이어 두 번째다** ⟨`effect/129-delta.md:31`⟩ | **해당 없음** |

**검산** — 차이 **13** = ㉡ 4 + ㉠ **6** + ㉢ 3. 원인이 touch 인 것 **3** · 아닌 것 **10**.
방향 기입 **13/13** · 「참이었나」 기입 **13/13**(해당 없음 10 · 참 2 · 참이지만 덜 말했다 1).
**「참이었나」에 근거 절이 붙은 셀 5 · 안 붙은 셀 8** = 13.

★★ **근거 축을 세는 줄이 없었다** ⟨`DL3-18` · 판 4 R3 의 정(正)이 잡았다⟩. 위 표의 칸
이름은 **「기입 · 근거」** 인데 검산은 **기입만** 셌다. 세는 줄을 더하니 **여덟이 비어 있다** —
그 여덟이 `C4` 반증의 전부다 ⟨칸 13⟩. **비어 있는 여덟은 사실로 전부 참이다**(판 4 R3 의
반이 touch 산출 셋에 전수 `grep` 을 걸어 확인했고 합이 받았다). 반증의 내용은
**「거짓을 실었다」가 아니라 「근거를 안 실었다」**다.

⚠ **여덟 셀을 이 회차에서 안 고친다** ⟨칸 13⟩ — 칸 12 가 세운 순서(*"정정을 먼저 세우고
그 위에서 다시 돈다"*)가 **판정 중 수리**를 금했고, 판정이 끝난 지금 고치면 그것은 반증을
사후에 지우는 것이다. 수리는 `#140` 이 진다.

⚠ **이 수가 한 번 틀렸다** ⟨2026-09-12 정정 · `intent.md` `## 승격` 칸 12⟩. 집필 시점
(`0aabb1c` 11:33)에 ㉠ 은 다섯이었고 여섯째 행 ㉳ 가 **그 뒤**(`017e5f1` 12:03)에
`effect/79-delta.md` 에 들어왔는데 이 표가 앞에서 멈춰 **㉠ 5 · 합 12** 로 적고 있었다.
`effect/79-delta.md:23` 은 그때도 스스로 **여섯**이라 적었으므로 두 문서가 갈려 있었다 —
회차가 `intent.md` `## 금지역` 에 등록한 **「사실이 아닌 것을 사실로」**에 드는 형태다.
`C1` 이 **전수**를 요구하고 소유자 칸 3 이 모집단을 「전부」로 못 박았으므로 **13 이 맞다.**

★ **한 문장으로**: touch 가 ㉡ 에서는 **걸음 하나를 없앴고**, ㉠ 에서는 **덜 말했으며**,
㉢ 에서는 **안 닿았다.** 셋이 다른 방향이고 그것이 표본을 다르게 고른 값이다.

### 정반합 산출물 — 정오표와 성격 ⟨칸 8·11⟩

**바이트 보존 산출물은 한 글자도 안 고친다** — `C3-a` 계열이 그것을 요구한다.
그래서 **정오표를 옆에 붙인다.**

⚠ **앞 판이 이 자리에서 출처를 틀리게 댔다** ⟨2026-09-12 정정 · 판 5 R2 부수 발견 ①⟩ —
*"**규약이** 「판정문의 사후 수정은 증거 위조다」로 못 박는다"* 라 적었는데
`grep -rn "판정문의 사후 수정" .claude/skills/round/SKILL.md` 의 **적중이 0** 이다.
그 문장의 출처는 **이 회차의 에이전트 산출물** `dialectic/3-synthesis.md:48`(판 3 라운드 1 의
합)이고, 규약이 실제로 적는 것은 `SKILL.md:494`·`ADR-0034:81` 의 *"고치면 증거 위조다"* 로
**회차 종결 기록**에 대한 것이다. **뜻은 받쳐지지만 인용은 1 차 원문에 없었다** — 하필
정정문 절의 머리말에 앉아 있었고, 이 회차의 금지역 「사실이 아닌 것을 사실로」와 같은
형태다. **쓴 것은 메인이다.**
독자가 그 판정문을 만나는 자리가 여기이므로 정정이 여기 선다.

#### 정오표 — `dialectic/5-thesis.md` 의 부수 발견 ③ ⟨`AT5-14` · 금지역⟩

그 초안은 *"`ADR-0027:22` 의 「Kotlin·TypeScript 둘뿐」이 「넷에서 다섯」과 갈린다 —
채택된 ADR **의 맥락 절이** 낡았거나 틀렸다"* 라 적었다. **오독이다.** 합(合)이 핀 안에서
다시 쟀다:

⚠ **이 인용이 한 번 넓어져 있었다** ⟨2026-09-12 정정 · 판 5 R2 부수 발견 ②⟩ — 앞 판이
*"채택된 **ADR 이**"* 로 세 글자를 빼고 실어 **원문보다 넓은 진술**을 반박하고 있었다.
원문 `dialectic/5-thesis.md:314` 대로 되돌렸다.

| 센 것 | 값 | 명령 |
|---|---|---|
| 추출기 모듈 | **셋** — `kotlin.rs` · `typescript.rs` · `rust.rs` | `git ls-tree a1493b5 --name-only crates/pal-extract/src/` ⚠ **그 명령의 산출은 15 항이다** — 「셋」은 **언어 이름을 파일명으로 지는 것만** 거른 수다(`cached`·`classify`·`extractor`·`lib`·`narrative`·`parse`·`plan`·`recognize`·`scopes`·`shell` 은 언어가 아니고, `rust_scopes`·`ts_scopes` 는 같은 언어의 둘째 파일이라 안 센다). **거르는 규칙을 여기 적는다** ⟨2026-09-12 정정 · 판 5 R2 부수 발견 ③ — 안 적으면 재현하려는 독자가 15 를 보고 멈춘다⟩ |
| `Language` enum variant | **다섯** — Kotlin · Java · JavaScript · TypeScript · Rust | `crates/pal-core/src/language.rs:24-30` |

**다른 것을 세는 두 수**이고 `ADR-0027:22` 는 **맞다** — 그 문장은 *"추출기는
Kotlin·TypeScript 둘뿐**이었다**"* 로 **과거형**이라 ADR 이 고친 **앞의 상태**를 적는다.
채택된 ADR 은 낡지도 틀리지도 않았다.

⚠ **이것이 금지역에 드는 까닭** — 회차가 등록한 「사실이 아닌 것을 사실로」다. 낸 것이
서브에이전트이고 합이 같은 판 안에서 잡았다.

#### 정오표 — 한 원천뿐이라 반·합이 안 다툰 셋 ⟨`DL3-12`·`DL3-17`·`DL3-39`⟩

- **`DL3-12` — `effect/129-delta.md:33` 의 「차이는 0 이다」가 낱말 드리프트다.** 소유자 칸 3 이
  「차이」를 **전부**로 다시 정의한 뒤 `effect/79-delta.md:25-31` 은 머리글을 고쳤고 **이 파일은
  안 따라갔다.** 같은 줄이 곧바로 *"위 셋은 전부 도구가 시킨 것이고 touch 산출에 귀속되는 것이
  하나도 없다"* 로 자기 뜻을 정의하므로 **거짓 주장은 아니다.** ⚠ **파일은 안 고친다** — 그
  파일이 `C1` 의 증거물이고, 판정이 난 뒤 증거를 고치면 판정의 근거가 움직인다.
- **`DL3-17` — 위 표의 시험 좌표가 밀렸다.** `effect/79-delta.md:116` 이
  `check_ledger_pair_는_순서에_취약하지_않다` 를 `ledger.rs:923` 이라 적는데 지금은 `:955` 다.
  그 파일 머리(`:6`)가 *"줄 번호는 변경 커밋 `9f993cc` 에서 실측했다"* 로 미리 적었으므로
  **거짓 주장은 아니다** — 밀린 것이 사실이라는 기록이 이 줄이다.
- **`DL3-39` — 같은 커밋 쌍에서 한쪽만 원장에 실렸다.** `c807f32`(게이트 정정) 직후
  `8457ac1` 이 **같은 게이트의 거짓 여섯**을 `MS-15`~`MS-20`(`출처=실측`)으로 실었는데
  `AT5-14`(`출처=정반합`)는 안 실렸다. **이중 계수의 위험**이 거기 있었고, 라운드 3 의 취합이
  전수로 재서 *"`MS-15`~`MS-20` 여섯과 `DL3-*` 사이에 요약이 같은 항은 없다"* 를 확인했다.
  가장 가까운 셋(`MS-18`·`MS-17`·`MS-19`)은 **인용 대상**이지 재적재가 아니다.

#### 정오표 — 판정문의 정정 **스물여섯** ⟨라운드 1·2 · `DL1-*`·`DL2-*`⟩

정반합의 초안(정·반)은 **바이트 보존**이라 한 글자도 안 고친다. 그 초안의 틀린 문장은
합(合)이 `## 초안을 어떻게 고치나` 로 정정했고 그 정정은 **커밋돼 있다.** 빠져 있던 것은
**독자가 그 초안을 만나는 자리의 정오표**이고, 이 표가 그 자리다.

⚠ **이 표 없이 닫으면 `DL1-17` 이 자기 처분을 안 지킨 것이 된다** — 그 행의 합 지시가
(`dialectic/3-synthesis.md:48`) *"고치지 말고 정오표를 옆에 붙여라 — 판정문의 사후 수정은
증거 위조다"* 자체다.

| 발견 | 초안의 어느 문장이 틀렸나 | 정정이 선 자리 |
|---|---|---|
| `DL1-04` | 초안의 ★(*"이 고침이 실제로 다른 회차를 집는다"*)가 과장이다 | `dialectic/1-synthesis.md` ⟨`af981d1`⟩ |
| `DL1-14` | 팔 ③ 의 유일한 측정 행위가 *"전문(42 줄) 읽었다"* 인데 그 파일은 41 줄이다 | `dialectic/2-synthesis.md` ⟨`e44a87e`⟩ |
| `DL1-15` | ⓐ·ⓒ 의 셈이 자기 목록과 안 맞는다 | `dialectic/2-synthesis.md` ⟨`e44a87e`⟩ |
| `DL1-16` | ⓓ 가 셋이 아니다 — A2-b 의 전제를 건 조건 문면 좌표를 안 짚었다 | `dialectic/2-synthesis.md` ⟨`e44a87e`⟩ |
| `DL1-17` | 초안의 가장 무거운 문단(손잡이 1)이 든 좌표 셋이 전부 한 줄씩 어긋난다 | `dialectic/3-synthesis.md` ⟨`af981d1`⟩ |
| `DL1-22` | 초안의 세는 규칙이 ㉡ 과 ㉠ 에 비대칭으로 걸렸다 | `dialectic/3-synthesis.md` ⟨`af981d1`⟩ |
| `DL1-23` | 분자 둘의 「결과 좌표」가 둘 다 「안 바뀐 자리」다 | `dialectic/3-synthesis.md` ⟨`af981d1`⟩ |
| `DL1-24` | 초안이 음성 대조가 「실행됐다」고 기댄 근거가 같은 회차에서 반증돼 있다 | `dialectic/3-synthesis.md` ⟨`af981d1`⟩ |
| `DL1-27` | **금지역** · 초안의 ★뼈대(§5)가 사실이 아니다 — 「호출자 1 이 참이었나」는 이미 회차에 적혀 있다 | `dialectic/4-synthesis.md` ⟨`af981d1`⟩ |
| `DL1-28` | **금지역** · 초안은 readnote 셋을 한 번도 안 다뤘다 | `dialectic/4-synthesis.md` ⟨`af981d1`⟩ |
| `DL1-30` | 초안이 자기가 「해당 없음」이라 적은 행을 실패 분모에 넣었다 | `dialectic/4-synthesis.md` ⟨`af981d1`⟩ |
| `DL1-31` | 「라벨」 요건은 초안이 새로 만든 것이다 | `dialectic/4-synthesis.md` ⟨`af981d1`⟩ |
| `DL1-32` | §3 의 「전수 확인」이 사실과 다르다 | `dialectic/4-synthesis.md` ⟨`af981d1`⟩ |
| `DL1-33` | 어긋남 판정을 증거를 안 보고 내렸고, 실제로 어긋난 자리가 있다 | `dialectic/4-synthesis.md` ⟨`af981d1`⟩ |
| `DL1-36` | 초안의 검산 인용이 한 자리 틀렸다 | `dialectic/4-synthesis.md` ⟨`af981d1`⟩ |
| `DL2-03` | 초안 ① 의 귀속이 git blame 과 어긋난다 | `dialectic/1-synthesis-r2.md` ⟨`017e5f1`⟩ |
| `DL2-05` | 초안 ③ 은 #79 를 스스로에게 맞세워 읽으면서 그 사실을 안 적는다 | `dialectic/1-synthesis-r2.md` ⟨`017e5f1`⟩ |
| `DL2-06` | 초안 ② 가 대는 「줄 번호 일치」는 ③ 의 산출에 원리상 안 닿는다 | `dialectic/1-synthesis-r2.md` ⟨`017e5f1`⟩ |
| `DL2-07` | 초안의 「101 줄」이 틀렸다 | `dialectic/1-synthesis-r2.md` ⟨`017e5f1`⟩ |
| `DL2-11` | 초안의 좌표 셋이 어긋났고 반의 「전수 대조했고 어긋난 좌표가 하나도 없다」가 그 자리에서 틀렸다 | `dialectic/1-synthesis-r2.md` ⟨`017e5f1`⟩ |
| `DL2-13` | 초안이 「내용 시험이 적혔다」를 판정자 반환문 한 곳에서만 찾았고 readnote 의 네 번째 칸을 한 번도 안 봤다 | `dialectic/3-synthesis-r2.md` ⟨`017e5f1`⟩ |
| `DL2-14` | 판정 대상은 조건 문면인데 초안은 ## 원문 의 낱말로 조건을 바꿔 읽었다 | `dialectic/3-synthesis-r2.md` ⟨`017e5f1`⟩ |
| `DL2-15` | *"칸 3 은 C1 의 모집단만 정했다"* 가 자기 인용 구간 안의 :428 로 반박된다 | `dialectic/3-synthesis-r2.md` ⟨`017e5f1`⟩ |
| `DL2-19` | 초안이 「내가 전수 재검으로 새로 세웠다」로 내놓은 오염 중 둘은 회차가 봉인 직후 스스로 이미 적어 둔 것이다 | `dialectic/3-synthesis-r2.md` ⟨`017e5f1`⟩ |
| `DL2-20` | 초안이 effect/79-judge-1.md:23 을 「세상에 대해 거짓인 문장」이라 적고 덧붙임을 요구한 것이 과하다 | `dialectic/3-synthesis-r2.md` ⟨`017e5f1`⟩ |
| `DL2-21` | 분모가 초안 안에서 안 정해진다(§3 일곱 · §4 여섯 · 명단 여덟) | `dialectic/3-synthesis-r2.md` ⟨`017e5f1`⟩ |

★ **「정정이 선 자리」의 뜻** — 그 파일의 `## 초안을 어떻게 고치나` 절에 정정 문장이 실려
있다는 것이지, 초안이 고쳐졌다는 뜻이 **아니다.** 초안은 안 고친다.

#### 정오표 — 그 밖의 여섯

- **`MS-12` · `DL2-23` — 설계 반증 조항 6 이 두 라운드 연속 걸렸다.** 판 3 설계문
  `dialectic/3-design.md:238` 이 *"`C2` 와 `C2-b` 의 판정값이 언제나 같이 움직이면 두 조건이
  실은 한 조건이고, 그러면 `C2-a` 의 ⓑ 축이 재는 것도 없다"* 를 미리 적어 뒀다. 라운드 1 에서
  둘 다 통과→미측정으로 **같이** 움직였고 라운드 2 에서 **또** 같이 미측정으로 남았다.
  **그 사실이 이 회차의 어느 문서에도 안 적혀 있었다.** 여기 적는다 — 조항이 발화했고,
  「두 조건이 한 조건인가」는 `#140` 의 문면 셋과 같은 자리로 다음 회차가 진다.
  ⚠ 그러나 `C2-a` 의 ⓑ 축은 **실제로 값을 냈다**(`effect/negative-C2.md` — 미끼만 제출한
  사본에서 유효 0). 조항이 예고한 「재는 것이 없다」는 **안 일어났다.**
- **`DL1-02` — 두 수의 4 배 어긋남.** 앞 회차(`2026-08-20-rust-extractor`)가 적은
  `44 건(1.6%)` 과 이 회차의 `20/3306` 은 **모집단도 계수기도 다르다** — 앞은 추출 대상
  파일의 비율이고 뒤는 2 층 심볼 중 「순서에 취약」 버킷의 수다. **어긋남이 아니라 다른 것을
  센 두 수**이고, 그 사실이 어디에도 안 적혀 있었다.
- **`DL1-25` — 판정자가 `touch/<이슈>.txt:N` 의 `N` 을 한 번도 검증하지 않았다.**
  `C2-b` 는 *"인용한 `touch/<이슈>.txt:N` 의 줄"* 을 요구하는데, 판정자 다섯 중 그 줄 번호가
  실제로 그 내용을 가리키는지 **댄 인스턴스가 없다.** 이 회차는 안 고친다 — `C2`·`C2-b` 가
  미측정으로 닫혔고(칸 2) 문면 수리는 `#140` 이 진다. **`#140` 의 네 번째 항이다.**
- **`DL2-26` — 같은 코드 자리를 원장 두 행이 친다.** `DL1-03`(라운드 1 `R3`)과
  `DL2-04`(라운드 2 `AT2-04`)가 **같은 자리**(`xtask` 의 커밋 시각 선택)를 치므로 원장이
  둘을 독립 발견으로 세면 이중 계수다. **둘 다 이 회차가 고쳤고**(시험 하나 · 판정문 문면
  하나) 서로 다른 것을 고쳤으므로 행 둘이 맞다. 그 사실을 여기 적는다.
- **`DL2-10` — 「합 = 분모」 축을 아무도 안 껐다.** 껐다 — `effect/negative-B5-sum.txt`.
  `ledger.rs` 의 `그룹_크기 > 1` 가지에서 `return` 한 줄을 지운 사본에서
  `합이_분모와_같다` 가 **`left: 6 · right: 4` 로 빨개졌다.** 라운드 2 의 `AT2-01`
  (*"이 검산은 항등식이라 원리상 못 빨개진다"*)이 **실측으로 반증됐고 합의 기각이 옳았다.**
- **`DL3-19` · `DL4-01` — 핀 목록의 공백.** `dialectic/preflight-r3.md` 의 마흔 항에
  `observations/identity-group.txt` 가 **없는데** 판 4 의 판정이 그 파일에 기댄다. 그리고
  판 5 라운드 1 의 합이 지시한 「핀 기록 정정」이 `dialectic/5-preflight.md` 에 **안 섰다**.
  **핀은 라운드마다 새로 박는 것**이 이 회차가 얻은 규칙이고(칸 9·12), 그 규칙이 서기 전에
  박힌 핀의 공백은 **되돌려 채우지 않는다** — 채우면 그것이 사후 수정이다.

#### 정오표 — 취합이 잰 것 ⟨`DL3-41`·`DL3-42`·`DL4-15`⟩

- **`DL3-41` — 게이트가 판정값을 실으면서 살아남은 반론의 해악도를 안 실었다.** 두 설계문이
  그것을 요구한다(*"판정값이 게이트 표에 실릴 때 살아남은 반론의 해악도가 같이 안 실리면"*).
  이제 싣는다 — **판 4 R3**: 금지역 1 · 실패 1 · 거짓신호 8 · 미관 3 = 13 ·
  **판 5 R2**: 금지역 1 · 실패 0 · 거짓신호 10 · 미관 2 = 13 ·
  **판 5 R1**: 금지역 1 · 실패 0 · 거짓신호 11 · 미관 4 = 16.
- **`DL3-42` — 판 4 판정문이 인용한 이 문서의 줄 번호가 `cc703dd` 에서 +62 줄 밀렸다.**
  판정문은 핀 안의 바이트에만 기댔으므로 조항 8 은 안 발화한다. 그러나 **원장으로 옮겨진
  좌표는 지금 이 파일의 다른 줄을 가리킨다.** 판 4 의 `:180`·`:182`·`:184`~`:196`·`:199` 는
  지금 `:242`·`:244`·`:246`~`:258`·`:260-261` 이다.
- **`DL4-15` — `783` 과 `812` 를 가르는 한 줄이 안 섰다.** 판 5 라운드 1 의 초안이
  「783 파일」을 들었고 이 게이트는 **812** 만 적는다. 둘은 다른 시점·다른 모집단의 수이고,
  **어느 쪽도 틀리지 않았다.** 가르는 줄이 없던 것이 발견이고 이 줄이 그것이다.

#### 정오표 — 「워킹트리」가 표면 셋에서 서로 다른 것을 뜻한다 ⟨`MS-11`⟩

`crates/pal-cli/src/ledger.rs` 의 주석이 셋을 가른다 — ⑴ `Snapshot … (워킹트리)`(어느 시점을
보고 있나) ⑵ `워킹트리 <sha> 와 다른 파일 N개`(더러움) ⑶ `pal touch` 근거 상자의
`워킹트리 일치/다름`(**더러운 트리에서도 「일치」를 찍는다** ⟨`PM2-04`·`A5-c`⟩). 셋이 같은
낱말이고 **같은 것을 안 잰다.** 고치는 자리는 표면 어휘 쪽이라 이 회차의 재는 자리 밖이다.

#### 성격 — 판 3 라운드 2 가 산출한 것은 **판정이 아니라 근거다** ⟨칸 8⟩

**`C2`·`C2-b` 의 값(미측정)은 소유자가 `intent.md` `## 승격` 칸 2 에서 정했다.** 판 3 의
라운드 2 가 산출한 것은 **그 값이 아니라 그 값을 받치는 근거**다.

★ **이 문장이 없으면 원장이 같은 값을 「독립 판정 둘」로 센다** — 미판정 잔액이 거짓이 되고,
그것이 이 회차가 등록한 금지역 **「측정이 죽은 가지」**다. 합(合) 자신이 그렇게 판정했고
소유자가 *"기록으로 닫는다 · 판정문에 성격을 적는다"* 로 답했다.

#### 무효 — 판 4 라운드 2 ⟨칸 12⟩

설계 반증 **조항 8**(`dialectic/4-design.md:242-243` — *"판정문이 기준 핀 밖의 바이트에
기댔다 → 그 판은 **무효이고 다시 돌린다**"*)이 판 4 라운드 2 에 대해 발화했다.
`dialectic/preflight.md:60` 이 **이 문서를 이름으로** 핀에 싣는데(`306ad7ec…`) 판정 시점의
값이 `fb7e5a13…` 이었다. **소유자가 면제가 아니라 집행을 골랐다.**

그러므로 `dialectic/4-{thesis,antithesis,synthesis}-r2.md` 는 **바이트로 남되 판정으로 안
쓰인다.** `C4` 의 판정은 이 문서의 정정이 선 뒤 **라운드 3** 이 낸다.

⚠ **앞선 면제 둘(칸 6·9)과 갈리는 까닭** — 그 둘이 면제 근거로 쓴 *"다시 돌려도 값은 안
움직인다"* 가 이번엔 거짓이다. 라운드 2 의 초안이 든 반증 근거 셋 중 **둘**(모집단이 12 가
아니라 13 · 죽은 좌표 `effect/negative-79.txt`)이 **이 정정이 고친 바로 그 자리**다.

### ★★ 원 의도의 합격선이 실제로 기대는 것 ⟨독립 리뷰 R2⟩

원문의 합격선은 *"그 산출을 보고 달라진 변경 **1 건 이상**, 좌표와 함께"* 이고 그 본체가
`C2` 다. **`C2` 는 미측정이다** — 「안 쟀다」이지 「됐다」가 아니다. 그러면 이 회차가 실제로
무엇을 보였는지를 여기서 정확히 적는다.

**원인 끝이 touch 인 차이는 셋**이다 — `effect/126-delta.md:26`(㉡㉰) · `:27`(㉡㉱) ·
`effect/79-delta.md:38`(㉠㉱). 그 셋이 **유효한** 귀속인지는 `C2-b` 가 정하고 그것도 미측정이다.
셋을 코드에서 다시 대면:

| 차이 | 코드에 남은 흔적 | 방향 |
|---|---|---|
| ㉡㉰ | **없다** — 바뀐 것이 함수를 두는 자리의 **근거**이고 코드는 안 움직였다(`grep -c "선언 순서" xtask/src/main.rs` = **0**) | 나아졌다 |
| ㉡㉱ | **없다** — *"호출자를 찾는 걸음이 없어졌다"* 이고 `:643` 은 `398d233` 의 hunk 밖이다. **안 건드린 것**이 차이다 | 나아졌다 |
| ㉠㉱ | **있다** — `crates/pal-cli/src/defect.rs:326` 한 줄 | **나빠졌다** |

★★ **그러므로 「touch 를 보고 코드가 실제로 달라진 자리」는 하나이고, 그 하나는 touch 가
「호출자 3」이라 했는데 실제가 넷이어서 난 것**이다 ⟨`MS-08`⟩. 이 표의 방향 칸이 그것을
**「나빠졌다」**로 적는다 — *"제품이 이 물음에 덜 답했다."*

⚠ **이것이 「효과가 없었다」는 뜻은 아니다.** ㉡ 의 둘은 **걸음을 없앴고**(호출자 1 을 보고
등록 표를 안 건드리기로 확정) 그것은 코드에 흔적이 안 남는 종류의 효과다. **코드 diff 로는
안 보이는 효과를 이 회차가 못 재는 것**이고, 그 사실을 여기 적는 것이 할 수 있는 전부다.

⚠ **그리고 이 문장 자체가 `C2` 의 판정이 아니다** — `C2` 는 미측정으로 닫혔고 이 절은
**그 미측정이 무엇을 가리고 있는지**를 적는다.

### 이 회차가 세운 이슈와 닫은 이슈 ⟨독립 리뷰 R1⟩

**닫았다** — `#66`(전제 · 물음 둘에 답이 섰다) · `#79`·`#126`·`#129`(재는 자리 셋).
⚠ 셋은 리뷰가 짚기 전까지 **열린 채였다** — `B1`·`B5`·`B6`·`B1-d` 가 통과이고 차선책 2 가
안 열렸으므로 문면이 가리키는 갈래는 「닫는다」인데 안 닫혀 있었다. `frontier.sh` 가 셋을
계속 할 일로 내고 있었다.

**세웠다** — 셋 다 `ready-for-agent` 이고 **넘김이 아니라 분할**이다 ⟨규약 §11③ (나)⟩.

| 이슈 | 무엇 |
|---|---|
| [`#140`](https://github.com/hskim-ecoletree/palimpsest/issues/140) | `C2`·`C2-b` 의 조건 문면 **넷** — 배제 집합의 정의역 · 인용을 낳는 계기 · 사실 자르기의 주체 · **판정자가 `touch/<이슈>.txt:N` 의 `N` 을 한 번도 검증 안 했다** ⟨`DL1-25`⟩ |
| [`#141`](https://github.com/hskim-ecoletree/palimpsest/issues/141) | **「죽은 링크 부재」가 백틱 코드 참조를 원리상 못 본다.** 이 회차가 그 값을 치렀다 — 죽은 경로 하나 때문에 전칭 부정이 안 재어진 채 사실로 적혔고, 같은 형태가 회차 안에서 **세 번** 났다. 규약이 그 표기를 **지시**하므로 「조심하면 된다」가 처방이 아니다 |
| [`#142`](https://github.com/hskim-ecoletree/palimpsest/issues/142) | **발견 추출기와 합계 계수기가 같은 파일에서 다른 수를 낸다.** 규칙이 두 벌이고, 이 회차에서 **세 번** 갈렸다(49↔72 · 26↔38 · 22↔14). 세 번 다 보고자가 자기 열 이름을 바꿔서 넘겼고 **그것은 처방이 아니다** |

## 범위 밖

이 게이트가 답하지 않기로 한 물음은 잠긴 의도의 `## 범위 밖` 이 진다. **여기 옮겨 적지
않는다** — 같은 것을 두 곳에 적으면 그것이 곧 drift 다.
