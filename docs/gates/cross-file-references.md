# 게이트 — 파일 경계를 넘는 참조 해소

> 회차 `.palimpsest/rounds/2026-09-08-cross-file-references/intent.md` · 착수 커밋 `6b6cb6d`
>
> ⚠ **앞 판은 여기에 「이 회차는 아직 안 끝났다」를 적었다.** 그때 `## 판정` 의 대부분이
> **미측정**이었고, 상자가 안 채워진
> 조건에 판정을 적지 않는 것이 그 뜻이다. 합격선은 **측정 앞에** 여기 등록한다 —
> 규약 §9 가 요구하는 순서이고, 판정이 난 뒤에 합격선을 손보면 그것이 사전 등록이 아니게 된다.
> 판정은 **재는 대로** 채운다.

## 합격선

등록된 완수 조건 **39 개**가 합격선이고 원문은 잠긴 의도가 진다. 여기 옮겨 적지 않는다 —
같은 것을 두 곳에 적으면 그것이 곧 drift 다. 아래는 그 조건들에 딸린 **셋**이다.

### RED 관측 ⟨착수 시점⟩

`observations/red.md` 가 원문을 진다. 요지 넷:

1. `pal touch file_edges` 가 *"파일 안의 관계만입니다 — 파일 경계를 넘는 것은 F07
   미구축입니다"* 를 찍었다.
2. 참조 엣지 4,259 행이 **전부 파일 안**이었다.
3. 결박 30 건 — 멈춤 판정의 기준선이다.
4. `pal doctor` 축 ① 이 `REFERENCES(F07)` 를 「담지 못하는 자리」로 적어 엣지 4,259 건이
   통째로 검사 밖이었다.

### 음성 대조

조건 자체가 짝으로 지고 있다 — `C2-a`·`C4-a`·`D1-a`·`E1-a`·`E2-a`·`E2-b`·`E3-a`·`E5-a`.
그 여덟이 *"검사가 고장이면 이렇게 드러난다"* 를 각자 적는다. `A5` 는 **범위 밖의 음성
대조**다 — 재수출만 지나는 이름에 엣지가 서면 잠근 축1 을 넘은 것이다.

### 차선책

셋을 등록했다 — `B2` 의 원천이 없을 때 · `C3` 의 등급 축이 안 설 때 · `D4` 의 형식 판이
안 설 때. 원문은 잠긴 의도의 `## 차선책` 이 진다. **앞의 것은 등록돼 있어도 메인이 혼자
발동하지 않는다** — 원 의도의 절반을 깎으므로 승격이다.

## 판정

| 판정 | 조건 |
|---|---|
| 통과 | A1 A2 A3 A4 A6 A7 A7-a B1 B2 B3 B4 C1 C2 C2-a C3 C4 C4-a D1 D1-a D2 D2-a D3 D4 D4-a D5 E1 E1-a E2-a E2-b E3 E3-a E3-c E4 E5 E5-a |
| 반증 | A5 A5-a E2 E3-b |
| 대조불가 | — |
| 미측정 | — |

**검산** — 통과 35 · 반증 4 · 대조불가 0 · 미측정 0 = 39

### `A5`·`A5-a` — **반증** ⟨2026-09-10 · 독립 리뷰 라운드 1 이 잡았다⟩

`A5` 는 *"재수출만 지나는 이름에 대해 엣지가 0"* 을 요구하고 `A5-a` 는 *"산출된 파일 간
엣지 중 그 경로를 지난 것이 0 임을 저장소 산출에서 확인한다"* 를 요구한다.
**픽스처 시험은 통과했고 저장소 산출에는 그 형태가 둘 섰다.**

`crates/pal-core/src/lib.rs` 는 `:45` 가 **사적** `mod traverse;` 이고 `:143` 이
`pub use traverse::{Step, traverse};` 다. 사적 모듈은 크레이트 밖에서 이름을 못 대므로
**밖에서 `use pal_core::traverse` 가 닿는 것은 재수출된 함수뿐**이다. 그런데 엣지는
**사적 `mod` 선언**을 가리킨다 — 출발은 `crates/pal-query/src/lib.rs` 와
`crates/pal-query/tests/bench.rs` 다.

★★ **`A5-a` 의 자가 이 형태를 원리상 못 잡는다.** 그 조건이 잰 것은
*"대상이 뿌리 파일의 직접 정의인가"* 인데, **잘못 잡힌 대상은 정의상 언제나 직접
정의다.** 그래서 0 이 나왔고 그 0 이 참으로 읽혔다 — 금지역 「측정이 죽은 가지」다.

**전수로 다시 쟀다** — 도착이 `mod` 선언인 파일 간 엣지는 **58** 이고, 그중 같은 파일이
같은 이름을 재수출해 밖에서 재수출만 닿는 것은 **둘**뿐이다. 나머지 56 은 크레이트 안의
모듈 참조(`use crate::ledger;` → `mod ledger;`)라 **참인 엣지**다.

⚠ **이 회차가 안 고친다 — 고칠 수단이 없다.** 가르려면 심볼이 **가시성**을 져야 하는데
`SymbolNode` 에 그 칸이 없다. 칸을 더하는 것은 `#136` 과 같은 축이고 판정이 이미 났다.
**조건을 사후에 고쳐 통과로 만들지 않는다.** 이슈로 세웠다.

★ **반증은 실패가 아니다.** 둘 다 이 회차가 **재고 나서** 얻은 것이다 —
`E2` 는 ⓑ 가 하한 333 에 못 닿았다는 사실이고, `E3-b` 는 등록된 표본 규칙이 유일한
표본을 안 정한다는 사실이다. 조건을 사후에 고쳐 통과로 만들지 않았다.

### `E3-b` — **반증** ⟨2026-09-10⟩

규칙은 엣지가 서기 전에 등록됐다(`8e0259b`). **그 절반은 참이다.** 그러나 그 조건이
막으려던 것은 *"여러 번 뽑고 유리한 판을 남기는 것"* 이고, **유일한 표본을 안 정하는
규칙은 그것을 못 막는다.** 파일 간 엣지 **1262** 는 30 으로 안 나눠떨어지고(42.0667)
「30 등분」의 렌더링이 셋으로 갈린다 — 실제 엣지로 A↔C 가 30 중 **28 자리** 다르다.

⚠ **그리고 정반합 판 3 의 합이 채택한 재등록이 조건에 안 닿았다** —
`dialectic/3-synthesis.md:58` 이 `E3-b` 를 **ⓐ20·ⓑ10 층화**로 다시 등록하라고 채택했는데
`intent.md` 의 `E3-b` 는 `8c3eae3` 이후 안 바뀌었다. **채택이 원장에만 살았다.**

★ **지금 고치지 않는다.** 엣지가 선 뒤에 추출 규칙을 다시 등록하는 것은 `E3-b` 자신이
금지한 형태다. 전문은 [`observations/e3-false-edge-rate.md`](../../.palimpsest/rounds/2026-09-08-cross-file-references/observations/e3-false-edge-rate.md).

### `E3`·`E3-a`·`E3-c` — 거짓 엣지 **0 / 30 · 0%** (상한 10%)

표본은 위 반증이 남긴 세 해석 중 **A**(`floor(i·N/30)`)로 뽑았고, **엣지를 하나도 보기
전에** 골랐다. 근거는 문면뿐이다 — 「등분」은 조각의 폭이 같다는 뜻이고 `B` 는 마지막
구간이 64 라 그 뜻에 안 맞는다. ⚠ **고를 자리가 있었다는 것 자체가 `E3-b` 반증의
내용이다.**

ⓐ 는 `use` 줄과 도착 정의를 대어 확인했고, ⓑ 는 `use` 줄에 도착 이름이 없는 것이
**정상**이라 함수 본문에서 참조 자리를 직접 읽어 확인했다. 거짓으로 판정한 것이 하나도
없다.

★★ **두 번 뽑았다.** 첫 표본은 `REFERENCES_ACROSS` **1262** 에서 뽑았고(ⓐ 24 · ⓑ 6 ·
거짓 0), 그 뒤 `9e04334` 가 `DL4-11` 을 고쳐 **모집단이 1297 로 늘었다.** 늘어난 35 건은
첫 표본에 원리상 없으므로 **같은 규칙·같은 해석으로 다시 뽑았다**(ⓐ 26 · ⓑ 4 · 거짓 0 ·
앞 표본과 겹치는 것 **7/30**). 규칙을 새로 정하지 않았다.

⚠ **`state.md` 가 잡아 둔 모호한 형태는 두 표본 어디에도 안 들었다** — `mod X;` 와
`pub use X::X;` 가 같은 뿌리 파일에 있어 `use` 한 줄이 두 이름 공간을 다 들여오는 자리
(`pal_core::traverse` 2 건). **이 표본은 그 형태에 대해 아무것도 말하지 않는다.**

**`E3-c` — 이 표본의 검정력은 낮다. 그 사실을 여기 적는다.**
n=30 에서 참 오류율이 15% 여도 관측 거짓이 3 건 이하일 확률이 절반을 넘는다.
0/30 의 95% 상한은 규칙 3/n 의 어림으로 약 **11.6%** 라 **상한 10% 를 표본만으로는 못
배제한다.** 그러므로 이 표본은 「10% 이하」를 **입증하지 못하고** 「10% 를 크게 넘지는
않는다」만 말한다. 그리고 `E3-b` 가 반증됐으므로 **이 표본은 유일하게 정해진 표본이
아니다** — 다른 해석(B·C)의 표본은 안 쟀다.

### `B1` — 실물의 칸은 **다섯**이다

조건 문면은 사전 등록된 넷(`site`·`name`·`reason`·`attempts`)을 적고 그 넷은 그대로
섰다. 다섯째 `at` 는 `B4` 의 `REFERS_UNRESOLVED` 가 요구하는 **공통 넷의 넷째**이고,
없는 필드 이름을 스키마에 적는 것이 곧 거짓이라 더했다. 잠긴 의도의 `## 개정` 이 그
사실을 진다.

### `D1` — 문면의 중간 값과 종료 실측이 갈렸다

조건 문면이 「2026-09-09 실측 `fresh` 25 · `stale` 5 — 움직인 것은 하나」였는데 종료
시점 실측은 **`fresh` 24 · `stale` 6 · 움직인 것 둘**이다. `observations/d1-rebinding.md`
는 처음부터 둘을 적고 있었다 — **문면만 중간 값에 멈춰 있었다.** `## 개정` 이 정정을 진다.
움직인 둘 각각에 ⑴명제 원문 ⑵시험 이름과 산출 ⑶명제가 아직 참인가가 다 있다.

### `D1` 의 셋 — **여기 적는다** ⟨정정 2026-09-10 · 독립 리뷰가 잡았다⟩

조건 문면이 *"그 판정을 **게이트에 적는다** … 셋 중 하나라도 없으면 미측정이다"* 이고,
앞 판은 관측 파일을 **가리키기만** 했다. 가리키는 것은 적는 것이 아니다.

#### ① `1cf63ec5e0e9937a` — `crates/pal-extract/src/rust.rs` 의 `표면`

⑴ **결정 명제 원문** — *"회차 2026-09-07-rust-scope-references — `pub` 의 뜻은 「최상위 +
정확히 `pub`」 하나다. … 이 규칙을 넓히면 `a13_pub_의_뜻은_최상위_pub_하나다` 가
빨개진다."*

⑵ **그 명제를 재는 시험과 산출** —
`rust::tests::a13_pub_의_뜻은_최상위_pub_하나다`(`crates/pal-extract/src/rust.rs:1158`) →
`ok`. **초록이다.** 명제가 스스로 지목한 반증 조건이 안 발화했다.

⑶ **명제가 아직 참인가 — 참이다.** 이 회차가 `표면` 에 더한 것은 임포트 항목 축
(`ImportSet::items`)과 그 항목의 선언 바이트(`ImportedItem::at`)이고 **`ExportSet` 의
모집단은 한 자도 안 건드렸다.**

#### ② `91cdc3add647a3f7` — `crates/pal-cli/src/touch.rs` 의 `print_facts`

⑴ **결정 명제 원문** — *"회차 2026-09-07-rust-scope-references — 이 라벨은 두 방향을 다
적어야 한다. **호출이 아닌 것을 담는다** … **호출인데 안 담는 것도 있다** …
**둘 중 하나만 적으면 0 이 「아무도 안 부른다」로 읽힌다.**"*

⑵ **그 명제를 재는 시험과 산출** —
`e4_화면이_능력_축_셋을_가르고_회계를_찍는다`(`crates/pal-cli/tests/cross_file_references.rs`)
→ `ok`. ⚠ **그 시험은 이 회차가 세웠다** — 명제를 재던 시험이 앞 회차에 없었다는 것이
이 자리에서 드러났고, 그것은 앞 회차의 잔여다.

⑶ **명제가 아직 참인가 — 앞 절반은 참, 뒤 절반은 좁아졌다.** 「호출이 아닌 것을 담는다」와
「둘 중 하나만 적으면 0 이 잘못 읽힌다」는 그대로 참이다. 「호출인데 안 담는 것」 중
**경로 호출의 꼬리는 이제 담는다**(오늘 283 건) — 멤버 호출(`x.foo()`)만 남았고 그것은
**능력 부재가 아니라 이 회차가 범위 밖으로 정한 자리**다. 화면이 그 둘을 갈라 적는다.

전문과 재는 방법: [`observations/d1-rebinding.md`](../../.palimpsest/rounds/2026-09-08-cross-file-references/observations/d1-rebinding.md)

### 사정권 전수 표 — **소유자 승격 칸 3 이 요구했다** ⟨2026-09-10⟩

조건 38 개 × 결박 30 을 기계로 교차한 결과다.

| 판정 | 수 | 조건 |
|---|---:|---|
| **사정권 안** | **5** | `A1`(`표면`) · `D4`·`D4-a`(`Projection::files`) · `E4`·`E5-a`(`print_facts`) |
| 사정권 밖 | 18 | — |
| 못 쟀다 | 15 | 요구가 문서 기입이거나 산출 관측이라 **코드 좌표가 없다** |

⚠ **「못 쟀다 15」의 독법이 둘이다.** 「좌표를 확정 못 했다」로 세면 **안 5 / 밖 18 / 못
쟀다 15** 이고, 「코드 좌표가 없으니 어느 본체에도 안 든다」로 세면 **안 5 / 밖 33 / 못
쟀다 0** 이다. **이 게이트는 앞의 독법으로 적는다** — 뒤의 독법은 「안 잰 것」을 「밖」으로
합쳐서 그 15 가 안 보이게 만든다.

★★ **그리고 방아쇠의 두 팔 중 결박 팔을 종료 시점에 다시 쟀다**(`DL3-07` 의 처분) —
`fresh` 24 · `stale` 6 이고 여섯의 신원이 `c9a6bae` 때와 같다. **이 교대가 더 움직인
결박은 없다.** ⚠ 그 팔은 이미 `stale` 인 결박에 대해 **원리상 둔감하다** — 낡음은 한 번
그렇게 되면 다시 안 움직인다. 그 한계를 종료 보고의 `## 원리상 못 잰 것` 이 진다.

### `D2`·`D2-a` — 축 여덟의 위반이 안 늘었고 다섯은 종료 시에도 모집단 0 이다

전수: ① `checked 10416 · violations 0` · ② `18375 · 0` · ③ `7959 · 0`.
축 ④⑤⑥⑦⑧ 은 종료 시점에도 `not_built` — **모집단 0 이다.** `D2-a` 가 요구한 대로
그 사실을 판정에 적는다. 그러므로 `D2` 의 본체는 ①②③ 이다.

### `C2`·`C2-a` — 라벨별 몫이 서고 나서야 잴 자가 생겼다

조건은 *"전체 `checked` 가 아니라 **`REFERENCES` 몫**"* 인데 산출에 그 값이 없었다.
`Outcome.by_label` 을 더했다 — 종료 시점 축 ① `BOUND_TO 34 · REFERENCES 4463 ·
REFERENCES_ACROSS 1297 · REFERS_UNRESOLVED 4622`(합 10416 = `checked`). 음성 대조는
시험이 진다: 그 라벨이 없는 픽스처에서 `checked > 0` 이면서 `REFERENCES` 몫이 **0** 이다.

⚠ **`BOUND_TO 34` 가 든 것은 `.palimpsest/intent.redb` 가 서 있기 때문이다** — 이 회차가
결박을 걸며 세웠다. 그 파생물이 없으면 그 라벨의 몫은 0 이고, **그것이 「0 건」이 아니라
「못 읽었다」**임을 `pal touch` 의 화면이 이제 말한다(`## 효과`).

### `D3` — 초록이다. **그러나 스크립트의 종료 코드는 안정적이지 않다**

`EXTRACTOR_REV` 는 `"f09-import-sites"` 이고 `scripts/f04-verify.py:188` 의 치환 대상이
그것과 글자로 같다. 변이 시험 세 줄은 반복 실행에서 전부 초록이다. 이 조건이 이름 댄
축(*"REV 를 올리면 그 리터럴도 같이 고쳐야 한다"*)은 그래서 통과다.

⚠ **같은 스크립트의 ⑨(벤치)가 선의 경계에 있다.** 조용한 기계에서 다섯 번 재서 `ditto`
가 `9.9 · 10.3 · 10.3 · 10.3 · 14.3`(선 10)이다. 그 선은 `corpus/criteria.toml` 의
`bench_ratio_correction` 이 **F04 의 합격선**으로 등록한 것이고 이 회차의 조건이 아니다.
다시 등록하는 것은 `[f04].self_judged` 가 금한 사후 조정이라 **안 건드리고 범위 밖에
적었다.** ★ **「초록이었다」로만 적으면 그것이 거짓 신호다** — 그래서 분포를 함께 싣는다.

### 판정의 근거 — 잰 것마다

**`E1`·`E1-a` ⟨2026-09-10 · 통과⟩** — `observations/e1-witness-after.txt` 가 산출을 진다.
착수 시점에 못 박은 이름 26 개에 `pal query symbol.callers` 를 다시 돌렸다. 하한은
**18** 이고 오늘 값을 돌려준 이름은 **26** 이다. 이름은 다시 고르지 않았다 —
`observations/witness-26.txt` 의 목록 그대로다.

`E1-a` 는 그 하한이 0 이 아니어야 성립한다. **착수 시점 산출이 26/26 빈 답이므로 하한
18 은 그때를 빨갛게 한다** — 대조가 실패할 수 있는 길이 있고, 그래서 항등식이 아니다.

**`A5-a` ⟨2026-09-10 · 통과⟩** — `observations/a5a-reexport-edges.md` 가 산출을 진다.
픽스처가 아니라 **저장소 산출**에서 쟀다. ⚠ **그 측정이 반증됐다 — 위 `A5`·`A5-a` 절을 보라.** 그때 파일 간 엣지 1259 중 대상이 크레이트 뿌리
`lib.rs` 인 것이 40 이고, **그 40 이 전부 뿌리 파일의 직접 정의**다 — 재수출을 지난 것은
**0**. 구조가 그것을 보증한다: 해소기는 대상 파일의 `SymbolNode` 만 보고 `pub use` 는
심볼을 안 만든다.

⚠ **같은 측정에서 `E3` 의 표본에 들어갈 형태가 하나 나왔다** — `mod X;` 와
`pub use X::X;` 가 같은 뿌리 파일에 있으면 `use` 한 줄이 두 이름 공간을 다 들여오는데
오늘 엣지는 **모듈** 쪽으로 간다. `A6`(후보 유일)은 안 걸린다 — 함수 심볼이 그 파일에
없어서 후보가 정말로 하나다. **거짓 엣지로 셀지는 `E3` 가 판정한다.**

**`C1` ⟨2026-09-10 · 통과⟩** — `pal doctor --json` 의 불변식 ① 산출에서 `absent`
목록이 여섯이고(`AUTHORED_BY`·`FOLLOWS`·`INTRODUCED_BY`·`MANIFESTS_AT`·`RESOLVED_BY`·
`TOUCHES`, 전부 `F05`) **`REFERENCES` 가 없다.** 착수 관측은 그 목록에 `REFERENCES(F07)`
를 싣고 있었고 엣지 4,259 건이 통째로 검사 밖이었다.

⚠ **여기 있던 문단은 2026-09-09 의 상태였다** ⟨정정 2026-09-10 · 독립 리뷰가 잡았다⟩.
그때 `C2` 는 *"아직 못 쟀다 — 자가 없다"* 였고 산출이
`{"checked": 471, "skipped": 5177, "violations": 0}` 하나뿐이었다. **그 문장이 현재형으로
남아 있어 같은 문서가 `C2` 를 「통과」와 「아직 못 쟀다」로 동시에 적었다** — 금지역
「사실이 아닌 것을 사실로」다. 지금 산출에는 `by_label` 이 있고 위 절이 그 수를 진다.

**`E2` ⟨2026-09-10 · 반증⟩ · `E2-a`·`E2-b` ⟨통과⟩** — 소유자가 2026-09-10 에 정했다
(승격 칸 11). ⓐ 는 종료 시점에 **1610/5751** 로 하한 800 을 넘겼고 ⓑ 는 **283/1060** 으로 하한 **333** 에
못 닿았다. 조건이 둘을 다 요구하므로 `E2` 는 **반증**이다.

⚠ **단서 둘을 같은 상자에 박는다.** 안 박으면 원장이 이 반증을 *"구현이 미달했다"* 로만
읽는다.

- **㉠ 하한의 분자 483 이 잠근 축1 이 금한 몫을 담고 그 크기가 미측정이다.** 483 의 규칙은
  *"꼬리 이름이 저장소 **어딘가에** 심볼로 서는가"* 인데(`intent.md` 가 그 자의 어긋남을
  하한을 세울 때 이미 적었다), `no_symbol_at_crate_root` 통(ⓑ **311** 건)은 **재수출을
  지나야만** 닿는 자리이고 재수출 추적은 `A5` 가 잠근 축1 의 **밖**이다. 곧 **하한이
  요구하는 몫의 일부는 이 회차가 안 하기로 정한 일**이다.
- **㉡ 원장이 「이 회차의 구현」으로 등록한 문 하나가 열린 채다** — 이관표의
  「머리가 모듈」 **41** 행이다.

`E2-a`·`E2-b` 는 **둘 다 성립한다.** 두 하한이 0 이 아니고 착수 상태(ⓐ 0 · ⓑ 0)를 지금도
빨갛게 두며, **ⓑ 의 하한이 실제로 물어서 `E2` 가 ⓐ 만으로 통과하지 못했다** — 그것이
`E2-b` 가 막으려던 형태 그 자체다.

### 금지역 둘의 처분 ⟨소유자 2026-09-10 · 승격 칸 13·14⟩

- **금지역 ① — 「머리가 모듈」은 41 이다.** 이관표의 41 이 정본이고 정(正)이 갈아 끼운
  **22 는 폐기한다.** 정 스스로 *"22 가 37 을 넘으면 「구현 문을 다 열어도 미달」이
  무너진다"* 고 적었으므로 그 논거는 조각 하나를 잃는다. ⚠ **41 도 이 회차가 다시 잰 값이
  아니다** — 앞 판이 적은 값이고 그 사실을 여기 적는다.
- **금지역 ② — 정의 「≈318 은 상한」은 상한이 아니다.** 그 반증만 싣고 **반(反)이 낸
  324 는 상한으로 안 싣는다** — 저장소 밖 복제본에서 잰 수라 원장이 재현 못 한다. 폐기된
  ⓑ 하한 150 과 같은 형태다. **이 회차는 ⓑ 의 상한을 안 가진 채 끝난다.**

### 이관표 — 못 닿는 몫이 가는 문

정본은 판 4 의 합(合)이 산출한 표이고 잠긴 의도의 `## 잔여를 여기서 미리 선언한다` 가 같은
표를 진다. **셋째 자리가 여기다** — 잠긴 의도가 *"셋 중 하나라도 빠지면 그것이 조용한
축소다"* 로 게이트·화면·잔여 셋 다를 요구한다.

| 몫 | 합이 적은 수 | 가는 문 |
|---|---:|---|
| 대상이 크레이트 루트 `lib.rs`(재수출 경유) | 201 (전체 300) | `A5`·`A5-a` — 잠근 축1 의 **밖** |
| 꼬리가 enum 변형·연관 상수 | 37 | `#133`(L2) 잔여 |
| 저장소 밖(`std::*` 등) | 245 (전체 322) | 경계 — 원리상 못 잇는다 |
| 균일 경로 형제 모듈(`use inside::{…}`) | 22 | 이 회차의 구현 |
| 머리가 모듈(`use super::{settings,…}`) | 41 | 이 회차의 구현 — `A6` 의 축2 물음이 붙는다 |
| `pal-cli` 크레이트 뿌리(`main.rs`) | 20 | 이 회차의 구현 |
| 비-`Root` `use` 중 축1 안 | 101 (중 엣지 후보 83, 더 엄한 자로 69) | 이 회차의 구현 — `A2` 가 진다 |

⚠ **그 수들은 판 4 라운드 1 시점(ⓑ 169)의 것이다.** 그 뒤 「구현」 문의 넷 중 셋이 서서
ⓑ 가 169 → 276, ⓐ 가 1133 → 1460 이 됐다. **「머리가 모듈」 41 은 안 건드렸다.**

★ **셈은 산문이 아니라 회계 열쇠가 진다** ⟨2026-09-10⟩. `pal touch` 가 갈래마다 오늘의
수를 찍고 그 아래 「못 선 몫이 가는 문」 줄이 열쇠와 문의 대응을 찍는다.

| 회계 열쇠 | 가는 문 |
|---|---|
| `outside_repo` | 경계 |
| `no_symbol_at_crate_root` | `A5`·`A5-a` |
| `no_symbol` | **갈래가 아직 안 갈렸다 — 미측정** |
| `no_target_file` | 이 회차의 구현 |
| `ambiguous` | **후보 생성의 모호** — 모듈 경로가 여러 파일로 읽힌다 |

⚠ **뒤의 둘은 2026-09-10 에 고쳤다** ⟨정반합 판 4 라운드 2 의 합⟩. `4c1e0da` 는
`no_symbol` 을 `#133`(L2) 하나로, `ambiguous` 를 `A6` 로 보냈고 **합이 둘 다 거짓으로
판정했다** — `no_symbol` 안에는 `#133` 몫과 이 회차의 구현 여지가 섞여 있고 그 크기가
미측정이며, ⓑ 의 `ambiguous` 는 전부 **짝 찾기 단계**의 모호라 꼬리 유일성을 재는 `A6`
가 아니다. 합의 문장: *"거짓 대응을 미측정으로 되돌리는 것은 축소가 아니라 금지역
해소다."*

★ **그리고 ⓐ 의 회계 결손 316 을 고쳤다.** 공유 단계(임포트 항목 찾기·모듈 경로 펴기)의
실패는 ⓐ·ⓑ 를 **둘 다** 넘어뜨리는데 앞 판은 꼬리가 있는 짝의 그 실패를 ⓑ 통에만 넣었다.
그래서 ⓐ 의 까닭 합이 ⓐ 의 못 선 수보다 316 적었고, **화면이 그 결손을 안 말했다.**
이제 두 통에 다 헤아린다. 종료 시점 값으로 — `5751 − 1610 = 4141` 이고 까닭의 합도
`214 + 840 + 1951 + 1136 = 4141` 이다. ⚠ **수를 여기 적었으므로 낡는다** — 회차가 끝나 값이 안 움직이는 자리라 적었고, 재는 자리는 `pal touch` 의 화면이다.

## 효과

**물음 하나에 답을 받았고 그 답이 틀렸다.** 틀렸다는 것이 이 회차가 여기서 얻은 것이다.

돌린 것 — `./target/release/pal touch print_facts` (2026-09-10). 시험도 CI 도 아니다.
물음 — *"이 회차가 두 번 만진 `print_facts` 에 걸린 결정이 있나. 지켜보는 결정은 있나."*

첫 답:

```text
■ 이 좌표에 걸린 것 (0)
  아직 없습니다.
■ 이 좌표를 지켜보는 것 (0)
  아직 없습니다.
```

**거짓이다.** `.palimpsest/intent/bindings.jsonl` 의 결박 `91cdc3add647a3f7` 이
`target` 과 `watch` 로 심볼 `f129649fd0fb…` 를 지키고, 그것이 같은 화면의 머리가 찍은
좌표다. 파생 저장소 `.palimpsest/intent.redb` 가 없어서 못 읽은 것이고 —
`.gitignore` 가 그것을 지우므로 **새로 받은 사람에게는 언제나 그 상태다** — 화면은 그것을
「0 건」으로 적었다. 금지역 「사실이 아닌 것을 사실로」다.

★ **가르는 타입이 이미 있었다.** `IntentStorePresence::unread` 의 문서가 *"빈 목록을
「0 건」으로 읽으면 안 되는 자리인가"* 라고 적고 `binding.status` 는 그 값을 싣는다.
**`pal touch` 의 답만 그것을 안 싣고 있었다.** 고친 뒤의 답:

```text
■ 이 좌표에 걸린 것 (0)
  **못 읽었습니다 — 「0 건」이 아닙니다.** 파생 저장소가 없습니다: ./.palimpsest/intent.redb
  정본은 있습니다: ./.palimpsest/intent/bindings.jsonl
  세우려면 — `pal intent import ./.palimpsest/intent/bindings.jsonl`
```

같은 실행이 이 회차의 산출도 함께 답한다 — `호출자 1 · 피호출자 1` ·
`파일 간 해소 ⓐ 1610/5751 · ⓑ 283/1060` · `■ 내가 모르는 것 3 건` 과 못 푼 까닭과
지난 걸음. **착수 시점 그 자리는 *"이 빌드에는 unresolved-refs 능력이 없습니다"* 였다.**

★★ **그리고 고친 화면이 종료 시점에 실제로 답을 바꿨다.** 결박을 걸며
`.palimpsest/intent.redb` 를 세운 뒤 같은 명령의 산출이 이렇게 바뀐다:

```text
■ 이 좌표에 걸린 것 (1)
  [91cdc3add647a3f7] 최신 상태 아님(stale) ← 1 개가 변했습니다  ·  symbol 반경 · 감시 1
      회차 2026-09-07-rust-scope-references — 이 라벨은 두 방향을 다 적어야 한다
```

**「0 건」이었던 자리에 결박이 나타났다.** 그 결박은 내내 정본에 있었고 화면만 못 읽고
있었다. 전문: [`observations/effect-touch-print-facts-after.txt`](../../.palimpsest/rounds/2026-09-08-cross-file-references/observations/effect-touch-print-facts-after.txt)

전문: [`observations/effect-touch-print-facts.txt`](../../.palimpsest/rounds/2026-09-08-cross-file-references/observations/effect-touch-print-facts.txt)

★ **둘째 효과 — `D4` 의 관문이 야생에서 발동했다.** 표본을 뽑던 자리에서
`pal export --format cypher` 가 *"2층의 행 형식이 낡았다 — 색인 `판 표시 없음` · 이 빌드
`f09-imported-and-unresolved`"* 로 거절했다. **이 회차가 세운 장치가 이 회차 안에서 실제
사용자를 막아 세웠고, 구제를 이름으로 줬다.**

## 범위 밖

잠긴 의도의 `## 범위 밖` 이 정본이다. 착수 시점에 일곱을 등록했다 — Kotlin 몫(`#131`) ·
TypeScript 경계 넘기 · `candidate` 등급 엣지 · `pub use` 재수출 추적 ·
추출 등급 L1→L2 승격(`#133`) · 효과 확인(다음 회차) · 저장소를 건너는 잇기.

★ **종료 시점에 넷이 더 들어갔다** ⟨2026-09-10⟩ — 산출물이 ⓐ/ⓑ 를 안 가른다 ·
엣지가 참조 줄 번호를 안 싣는다 · `f04-verify.py` 의 ⑨ 가 선의 경계에서 흔들린다 ·
완수 조건 파서가 줄 단위다. 넷 다 이 회차의 물음이 아니고 **넷이 이슈 셋으로 섰다** — [#136](https://github.com/hskim-ecoletree/palimpsest/issues/136)
(ⓐ/ⓑ 미구분과 참조 줄 번호 부재는 **같은 축**이라 하나로 묶었다) ·
[#138](https://github.com/hskim-ecoletree/palimpsest/issues/138)(⑨ 의 경계) ·
[#137](https://github.com/hskim-ecoletree/palimpsest/issues/137)(조건 파서).
사유는 잠긴 의도가 진다. ★ **이슈로 서야 「분할」이다** — 규약 §11 ③ 의 음성 대조가
*"「별도 목록」이 다음 회차에서 한 번도 안 열리면 그것은 분할이 아니라 버림이다"* 로
못 박았다.

⚠ **`crate::`·`super::`·`self::` 접두는 2026-09-08 에 안으로 들어왔다** — 소유자가 전환을
승인했다. 재수출은 그대로 밖이다.
