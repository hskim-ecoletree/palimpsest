# F22-3 게이트 — 사슬의 마디 · 판정 기록

**판정: 대조 불가 (2026-08-12) → 재판정 통과 (2026-08-13).**

> **2026-08-13 · [F02-1(#46)](F02-1-extractor.md) 이 TypeScript 추출기를 세워 대조 불가가
> 닫혔다.** 등록된 오라클이 재어졌고 **등록값과 일치한다** — 아래 「재판정」 절.
>
> 같은 날 **이 게이트가 자기에 대해 거짓말한 자리 하나를 정정한다** — 아래 표의 음성
> 대조 「5/5」는 참이 아니었다. 다섯째는 시험되지 않았고 스크립트가 `–` 를 찍었는데
> 게이트가 그것을 통과로 적었다.
>
> **아래 2026-08-12 의 기록은 그대로 둔다.** 그때는 참이었고, 무엇이 왜 닫혔는지는
> 그 기록이 남아 있어야 읽힌다.

---

## 재판정 — 2026-08-13 · **통과**

`scripts/f22-3-verify.py` 를 **고친 뒤** 재실행했다(`2e2eb3f`). 고치기 전 수치는 쓰지
않았다 — 그것이 `[f02.1.pass]` ⑥ 의 넷째 항목이다.

### A. 등록된 오라클 — **재어졌다. 그리고 맞았다**

```
add91871  bound   발현  10 · 도입 7d6451a5 (9/10 = 90%) · 나머지 후보 1   (테스트 1 · 실코드 5)
2550118a  bound   발현   1 · 도입 5616f46e (1/1 = 100%)
afcfefab  no_semantic_change                                ⚠ 만진 코드가 테스트뿐이다
7f6b0a58  bound   발현   2 · 도입 59fb3d8f (1/2 = 50%) · 나머지 후보 1   (테스트 1 · 실코드 2)
9dc1af1e  bound   발현   4 · 도입 d2940f7f (2/4 = 50%) · 나머지 후보 2   (테스트 3 · 실코드 4)
```

| | 등록값(T10) | 관측 |
|---|---|---|
| 발현 좌표 | ≥ 4/5 | **4/5** |
| 도입 변경 (과반 `>=50%`) | ≥ 4/5 | **4/5** |
| 도입 변경 (엄격 `>50%`) | ≥ 2/5 | **2/5** |
| 담기지 않는 결함 계수 | 1 | **1** (`afcfefab`) |

**셋이 등록값과 정확히 같다. 그것을 성취로 읽지 않는다.** 이 게이트 자신이 아래
「줄이 아니라 심볼 위에서 결박한다」 에서 *"T10 은 삭제된 줄을 blame 했고 이 구현은
심볼을 본다 — 같은 질문에 다른 도구로 답한다"* 라고 적었다. **다른 도구가 같은 수를
냈다는 것은 흥미로운 사실이지 설계된 합격이 아니다.** 표본이 5 건이라 일치가 우연일
여지도 그만큼 크다.

그리고 **이 수치는 F02 의 합격선이 아니다**(`[f02.1.pass]` ⑥). 이것이 재는 것은
추출기 품질이 아니라 결함 계보 알고리즘이다.

### B. 비판정 관측 — **모집단이 고정 SHA 로 돌아왔다**

`^fix` **477 건 @ `a29cad0bf6a8`**. 재실행 전에는 HEAD 를 보고 있어 **654** 였다.
표본과 수치는 2026-08-12 기록과 같다(발현 3/5 · 과반 3/5 · 엄격 3/5 · 담기지 않음 2/5).

### C. 음성 대조 — **다섯이 전부 시험됐다. 그전에는 넷이었다**

```
✓ 추출기 부재가 `no_semantic_change` 로 뭉개지지 않는다 — 8566c827 → no_extractor
✓ 코드 밖 변경만 있는 커밋 4629dd81 → outside_code
✓ 같은 커밋을 두 번 → 산출 동일
✓ 예산 2 에서도 지목됨 (1/1)
✓ 최빈 아닌 후보를 실은 자리를 찾았다 — 등간격 표본 안 (cb7fc927 · add91871 · 7f6b0a58 · 9dc1af1e)
```

**첫째 대조가 오늘 낡았고, 지우지 않고 옮겼다.** 원래 그것은 *"T10 표본 5 건이 전부
`no_extractor` 인가"* 를 물었다. TypeScript 추출기가 서면서 전제가 사라져 `✗` 를 찍기
시작했다 — 결함이 아니라 대조가 낡은 것이다. 명제(*추출기 부재가 '변한 것 없음' 으로
뭉개지지 않는다*)는 여전히 참이어야 하므로, **추출기가 실제로 없는 언어**로 옮겼다:
ditto 의 고정 모집단에서 `.mjs`/`.js` 만 만진 첫 `fix` 커밋을 결정적으로 찾는다.

### 예언이 빗나갔다 — **틀린 결박이 아니라 입자 부재였다**

2026-08-12 의 이 게이트가 적었다:

> TypeScript 추출기가 서면 그 커밋(`afcfefab`)은 *"테스트 심볼이 발현 좌표"* 로
> 결박될 것이고 그것은 **틀린 결박이다.**

**그렇게 되지 않았다.** `afcfefab` 은 `bound` 가 아니라 **`no_semantic_change`** 로
나왔다. 왜인지 파고들었고, 그것이 이 재판정에서 가장 값진 산출이다.

그 커밋이 만진 것은 `agents/reviewer.md` · `agents/security-reviewer.md` ·
`tests/host/claude/agents.surface.test.ts` 셋이다. 그 테스트 파일에는 심볼이 **4 개
있다**(`REPO` · `AGENTS_DIR` · `AGENTS` · `frontmatter`). 그런데 커밋이 더한 것은
`describe(…)` 콜백 **안**의 새 `test.each(…)` 블록 전체다.

**`describe`·`test` 에 넘긴 화살표 함수는 익명이라 심볼이 아니다**(F02 §3.3 —
*"익명 함수는 가장 가까운 이름 있는 조상에 귀속된다"*). 그런데 **그 조상이 없다.**
모듈 스코프의 호출 인자로 들어간 함수라 담아 줄 이름 있는 선언이 위에 없다. 그래서
그 코드는 **어느 심볼에도 속하지 않고**, 거기서 일어난 변경은 산출에서 사라진다.

산출은 이렇게 적는다:

```
■ **담기지 않았다** — 세어서 표시한다
    코드는 변했는데 의미가 변한 심볼이 없다 (포매팅·주석·신규 파일)
```

**이 문장은 이 경우에 거짓이다.** 코드는 의미까지 변했고, 우리 **입자가 그것을 담지
못했을** 뿐이다. 이 게이트가 첫 구현에서 고발한 것과 **같은 병의 다른 형태다** —
그때는 *능력 부재*(`Capable` 로 고쳤다)였고 이번은 **입자 부재**다.

**고치지 않았다.** 세는 규칙은 `corpus/tasks/f02-recall-sample.tsv` 에 코드보다 먼저
등록됐고, 이 발견을 보고 규칙을 바꾸는 것은 대조를 사후 조정하는 일이다. 처분은 열려
있고 그 자리를 지목해 둔다:

- `no_semantic_change` 를 **하나로 두지 않는다** — *"변한 심볼이 없다"* 와 *"변경이
  어느 심볼에도 담기지 않았다"* 는 다른 사실이고, 후자는 `Uncapturable` 의 자리다
- 조상 없는 익명 함수를 **파일 단위 잔여**로 남길지 — F02 §3.4(`partial`)의 이웃이고
  소유는 **#47** 이 가깝다

### ⚠ 테스트와 실코드를 **구별하지 않는다**

`[f02.1.pass]` ⑦ 이 요구한 기록이다. 위 표의 괄호가 그것이고, 재실행 산출에서
**결박된 넷 중 셋이 테스트 파일을 함께 만졌다**(`add91871` · `7f6b0a58` · `9dc1af1e`).
발현 좌표에 테스트 심볼이 섞였을 수 있고, **이 빌드는 그것을 가려내지 않는다.**

무엇이 테스트인가를 정하는 것은 **F15** 다. 여기서 진 것은 **그 부재가 값으로 남는
것**이고, `scripts/f22-3-verify.py` 가 커밋마다 그 사실을 찍는다.

**정확히 말하면 발현 좌표의 파일을 직접 세지 못했다** — `manifests_at` 은 `SymbolId`
목록이고 경로가 실려 있지 않다. 위 괄호는 **커밋이 만진 파일**로 센 것이라 상한이다.
좌표에서 경로로 되짚는 것은 2층 조회이고 이 조각이 하지 않았다.

---

## 이하 2026-08-12 의 기록 — **그대로 둔다**

**판정: 대조 불가 (2026-08-12).**

**등록된 오라클을 이 빌드로 잴 수 없다.** [T10](preflight.md#t10--여정결함의-올라탈-곳)이
잰 표본은 ditto 의 커밋 5 건이고 ditto 는 **TypeScript** 다. 이 빌드의 추출기는
**Kotlin 하나**다(`pal_extract::capability` — Java · JavaScript · TypeScript 는 전부
`NotBuilt{F02}`). 그래서 발현 좌표를 하나도 세울 수 없고, 등록값
**발현 4/5 · 도입 4/5 · 엄격 하한 2/5** 는 재어지지 않는다.

| | 등록값 | 관측 |
|---|---|---|
| ① 결정론 | 두 번 돌리면 같음 | **통과** — 산출 동일 |
| ② 발현 좌표 | ≥ 4/5 | **대조 불가** — 추출기 없음 5/5 |
| ② 도입 변경 (과반 `>=50%`) | ≥ 4/5 | **대조 불가** |
| ② 도입 변경 (엄격 `>50%`) | ≥ 2/5 | **대조 불가** |
| ③ 신뢰도가 산출에 실린다 | true | **통과** — `agreeing/total` · 백분율 · 나머지 후보 |
| ④ 담기지 않는 결함 계수 | **1** | **대조 불가** — T10 표본에서는 5/5 가 `NoExtractor` 다 |
| ⑤ `NotBuilt` 경로 | 빈 목록이 아님 | **통과** — `Journey` 거주 불가 · `NoExtractor` 가 `CapabilityId` 를 싣는다 |

이슈 [#44](https://github.com/hskim-ecoletree/palimpsest/issues/44) (부모 [#3](https://github.com/hskim-ecoletree/palimpsest/issues/3)) ·
합격선 정본 [`corpus/criteria.toml`](../../corpus/criteria.toml) `[f22.3]` ·
오라클 [T10](preflight.md#t10--여정결함의-올라탈-곳) · [`corpus/tasks/outcomes.toml`](../../corpus/tasks/outcomes.toml)

**합격선도 판정도 에이전트가 한다** — [R-18](../plan/00-risks.md#r-18)은 닫히지 않는다.

---

## 대조 불가가 드러낸 것 — **계획의 두 자리가 겹치지 않는다**

이것이 이 게이트에서 가장 중요한 산출이다.

> **P0-preflight 의 오라클(ditto · TypeScript)과 P0 가 실제로 만든 능력(Kotlin 추출기)이
> 겹치지 않는다.**

T10 은 코드 없이 손으로 측정했으므로 언어가 상관없었다. 그런데 [stack §3.1 의 착수
순서 변경](../plan/00-stack.md)(2026-08-12)이 **Kotlin 을 첫 언어로** 세웠고, 그 결정은
적용 대상(boxwood)을 보고 내려진 것이다. 두 결정 다 옳았고 **아무도 그 둘이 만나는
자리를 보지 않았다.**

결과: **T10 이 등록한 수치는 F02 가 TypeScript 추출기를 세울 때까지 재어질 수 없다.**
이것은 F22-3 의 실패가 아니라 계획의 사실이고, 그래서 판정이 반증이 아니라 대조 불가다.

같은 종류의 자리가 더 있는지는 세어 보지 않았다 — **P0-preflight 의 다른 오라클들이
어느 언어의 저장소에서 측정됐는지**를 확인하는 것은 이 게이트 밖이고, 빚으로 남긴다.

---

## 첫 구현이 이 도구가 고발한 문제를 저질렀다

**음성 대조가 아니라 실행 자체가 잡았다.** 첫 실행에서 다섯 건이 전부 이렇게 나왔다:

```
■ **담기지 않았다** — 세어서 표시한다
    코드는 변했는데 의미가 변한 심볼이 없다 (포매팅·주석·신규 파일)
```

**거짓이다.** 코드는 의미까지 변했고, 우리가 **읽지 못했을** 뿐이다.
`digests_at` 이 `Capable::NotBuilt` 를 받아 빈 표로 바꾸고 있었고, 그 빈 표가
*"변한 심볼 0 개"* 로 세어졌다 — **`Finding 0` 과 "감사를 안 만들었음"이 같은 출력이
되는 것**([목표 §3.1](../plan/00-goals.md) · S2 합격선 ②)의 정확한 재현이다.

고친 형태는 [`Uncapturable::NoExtractor`](../../crates/pal-core/src/chain.rs) 이고
**능력 부재가 값으로 실린다**:

```
■ **담기지 않았다** — 세어서 표시한다
    이 빌드에 그 언어의 추출기가 없습니다 — F02 미구축 (typescript-extraction)
    **변한 것이 없다는 뜻이 아닙니다.** 읽지 못했다는 뜻입니다.
```

그리고 `DefectReport.not_extracted` 가 **읽지 못한 파일을 산출에 남긴다** — 일부만 읽은
답이 전부 읽은 답처럼 보이지 않게.

---

## 기계가 도는가 — Kotlin 에서의 비판정 관측

**이 값은 합격선이 아니다.** 등록된 오라클이 대조 불가여서, *"경로 자체가 서 있는가"* 를
따로 봤다. 모집단이 다르므로 T10 의 표와 비교하지 않는다.

선정 규칙은 **T10 의 것을 그대로 옮겼다** — 시각 오름차순 등간격 5.
모집단은 `boxwood/portal-backend` 의 `^fix` 커밋 **477 건**.

```
4ad79de7  bound               발현   1 · 도입 f370960d (1/1 = 100%)
cb7fc927  bound               발현   8 · 도입 c7ecdbdb (7/8 = 87%) · 나머지 후보 1
d79a0c5a  no_semantic_change
f2aa232f  outside_code
91445143  bound               발현   1 · 도입 86610001 (1/1 = 100%)

발현 좌표 3/5 · 도입(과반) 3/5 · 도입(엄격) 3/5 · 담기지 않음 2/5
```

`cb7fc927` 이 이 표에서 가장 값진 줄이다 — 발현 좌표 8 개 중 7 개가 한 커밋을 가리키고
**하나는 다른 커밋을 가리킨다.** 그 하나가 `others` 에 남았다. 최빈만 저장했다면 그것이
곧 조용한 절단이었을 것이다(stack §5.4).

## 음성 대조 — 5/5

```
✓ 추출기 부재가 `no_semantic_change` 로 뭉개지지 않는다   ← 위의 결함이 되돌아오면 잡힌다
✓ 코드 밖 변경만 있는 커밋 4629dd81 → outside_code        ← 계수기가 실제로 센다
✓ 같은 커밋을 두 번 → 산출 동일                            ← `extracted` 배정의 조건
✓ 예산 2 에서도 지목됨 (1/1)                               ← 예산과 부재가 갈린다
✓ 최빈 아닌 후보를 실은 건수 1/3                           ← 조용한 절단 금지가 실물에서 시험됐다
```

> **2026-08-13 정정 — 위 다섯째 줄은 참이 아니었다.**
>
> 스크립트는 `–` 를 찍었다(*"이번 표본에서는 전부 만장일치라 시험되지 않았다"*). 즉
> **시험되지 않았는데 게이트가 `✓ 1/3` 이라고 적었다.** 원인은 B 부 모집단이 코퍼스
> HEAD 에 묶여 `^fix` 가 477 → 654 로 자라면서 표본이 통째로 바뀐 것이다 —
> **변이가 자라는 값에 묶여 있었다**(`7fe6b62` 가 잡은 것과 같은 형태).
>
> `2e2eb3f` 가 셋을 고쳤다: 모집단을 고정 SHA 에 묶고, 시험되지 않은 대조를 `–` 가
> 아니라 **실패**로 세고, 다섯째를 표본의 운에 맡기는 대신 모집단을 결정적으로 훑는다.
> 재실행 결과는 위 「재판정 · C」 에 있다.
>
> **이것이 규율 7(*게이트가 자기에 대해 거짓말하지 않게 하라*)의 두 번째 실물 사례다** —
> 첫째는 `d1f9a09`(F01 이 *"정의만 했다"* 라고 적었는데 정의도 안 했던 것)다.

---

## 줄이 아니라 심볼 위에서 결박한다 — **T10 과 방법이 다르다**

T10 은 **삭제된 줄을 blame** 했다. 이 구현은 **`body_digest` 가 마지막으로 변한 조상**을
본다. 그래서 두 측정은 같은 질문에 다른 도구로 답하고, 그 사실을 여기 적는다.

| | T10 (손) | 이 구현 |
|---|---|---|
| 단위 | 삭제된 줄 | **심볼(`symbol_id`)** |
| 신뢰도 | 최빈 줄의 비율 | 최빈 커밋에 동의한 **좌표**의 비율 |
| 포매팅 커밋 | 도입 커밋으로 지목될 수 있다 | **지목되지 않는다** — 정규화가 이미 지웠다 |

**바꾼 이유는 좌표계다.** [`Coord`](../DESIGN.md#21-d1--3축-좌표)에 `span` 이 없고 줄
번호는 포매팅으로 움직인다. 줄 위에서 결박하면 [R-07](../plan/00-risks.md#r-07)의 거짓
양성이 결함 계보 안에서 재생산된다.

### 그리고 이 방법이 못 하는 것 하나

T10 은 `afcfefab` 을 *"발현 좌표 ✗"* 로 적었다 — `src/**` 변경이 0 줄이고 결함이 에이전트
지시 문서에 있었기 때문이다. 그런데 그 커밋은 **테스트 파일(`.ts`) 하나를 함께 바꿨고**,
이 구현은 **테스트와 실코드를 구별하지 않는다.** TypeScript 추출기가 서면 그 커밋은
*"테스트 심볼이 발현 좌표"* 로 결박될 것이고 그것은 **틀린 결박이다.**

지금은 재어지지 않아 드러나지 않았다. **F02 가 서는 날 이 자리가 다시 열린다** — 빚으로 적는다.

> **2026-08-13 후속 — 이 예언은 빗나갔다.** `afcfefab` 은 *"테스트 심볼이 발현 좌표"*
> 로 결박되지 않았다. **아예 결박되지 않았다**(`no_semantic_change`). 이유는 위
> 「재판정 · 예언이 빗나갔다」 에 있고, 드러난 것은 예상보다 무거운 자리였다 —
> 틀린 결박이 아니라 **입자 부재**다. 구별하지 않는다는 사실 자체는 참이고, 다른 셋
> (`add91871` · `7f6b0a58` · `9dc1af1e`)에서 실제로 하중을 진다.

---

## 코드를 스키마에 맞춘 자리와 설계를 정정한 자리

### J2. `Actor` 의 `종류(human|agent|tool)` 를 노드에서 뺐다

[DESIGN §1.1](../DESIGN.md)은 `Actor` 를 `{종류, 안정 식별자, 표시 이름}` 으로 적고
출처를 `asserted`(등록)라 했다. **그 셋은 한 노드에 설 수 없다.**

- git 커밋의 이름·이메일 → `(재현 입력, 추출기 버전)` 만으로 재현된다 → 배정 규칙 1 →
  **`extracted`**
- *"이 식별자는 사람이 아니라 에이전트다"* → 코드에서 나오지 않는다 → **`asserted`**

[§3.4](../DESIGN.md)의 속성 출처 동질성이 깨지고, 그 규칙의 처방은
*"섞으려면 노드를 쪼개고 엣지로 잇는다"* 이다. 그래서 `Actor` 는 **git 정체성만** 진다.
종류를 싣는 등록 노드는 **저작할 기능이 없어 만들지 않았다** — 없는 것을 미리 선언하면
스키마가 만들 수 없는 것을 실은 채 자란다.

**[DESIGN §1.1](../DESIGN.md)의 `Actor` 행을 정정했다.**

### J3. `Defect` 의 `원인 좌표[]` 를 두지 않았다

소급 결박에서 **원인은 발현과 구별되지 않는다.** 우리가 아는 것은 *"이 심볼이 수정
커밋에서 변했다"* 와 *"그 전에 마지막으로 변한 커밋이 이것이다"* 뿐이고 둘 다 같은
좌표를 가리킨다. 구별하려면 인과 판정이 필요하고 그것은 F15 다.

지금 넣으면 발현의 복사본이 되고, [§3.1](../DESIGN.md)이 금지한
*"이 시스템은 이것을 다룬다는 거짓 신호"* 가 된다.

### J4. `Defect` 의 출처가 `extracted` 다

F22 §2.2 는 `asserted`(사람) 또는 `observed`(도구)라 적었다. **그것은 인입(F16)으로
들어오는 결함**을 두고 쓴 것이고, 여기 있는 것은 git 이력에서 결정론적으로 나온다
(배정 규칙 1 · 재현 입력이 `History`). 같은 결함을 도구가 다시 보고해도
**파생 id 규칙에 따라 다른 노드로 선다**([F22-2](F22-2-derived-id.md)) — 붕괴하지 않는다.

**한 라벨이 여러 출처에 서는 문제는 열린 채 남는다.** 지금 스키마는 노드마다 출처
하나이고, F16 이 `observed` 결함을 들일 때 그 자리가 다시 열린다.

### `blame` 을 켜지 않았다 — 검토하고 기각

`gix` 의 `blame` 기능을 켜면 `gix-diff/blob` → `gix-filter` → **`encoding_rs`
(BSD-3-Clause)** 가 트리에 돌아온다. S1 이 정확히 그 크레이트를 features 를 좁혀
몰아냈고 [`deny.toml`](../../deny.toml)은 *"예외를 늘리기 전에 의존을 줄이는 것이
순서다 — 정책은 느슨해지면 되돌아오지 않는다"* 라고 적었다.

줄여 보려 했으나 `gix-diff/blob` 이 `gix-filter` 를 무조건 끌고 온다(직접 `gix-blame` 을
써도 같다). **그리고 위에서 적은 대로 줄 단위 blame 은 애초에 이 좌표계가 아니다.**
그래서 예외를 늘리지 않고 방법을 바꿨다. `cargo deny` 는 그대로 통과한다.

---

## 이 게이트가 판정하지 않는 것

- **T10 의 수치가 반증된 것이 아니다.** 재지 못했을 뿐이다. 반증하려면 TypeScript
  추출기가 있어야 한다.
- **결함 인입(F16)이 아니다.** `observed` 결함을 도구에서 받는 경로는 없다.
- **`Journey` 저작이 아니다.** T10ⓑ 가 진입점 좌표 0/3 으로 반증했고, 여기서는
  **자리와 거주 불가능성**뿐이다.
- **`pal defect` 가 `Envelope` 를 돌려주지 않는다.** `pal touch` 만 그렇다.
  커버리지의 구멍은 `not_extracted` 가 대신 싣는다 — 임시 형태다.
- **테스트와 실코드가 구별되지 않는다.** 위 참조.

## 실측

| | |
|---|---|
| `cargo xtask check` | **7/7** — 노드 라벨 **7** · 엣지 타입 **7** (3·1 에서 늘었다) |
| `cargo test --workspace` | **82** |
| `cargo clippy --all-targets` | 경고 **0** |
| `cargo deny check` | advisories ok · bans ok · **licenses ok** · sources ok |
| `scripts/f22-3-verify.py` | 음성 대조 **5/5** · A 부 **대조 불가** |

## 이 게이트를 닫으면서 남기는 빚

| 빚 | 누가 |
|---|---|
| **T10 의 수치를 재려면 TypeScript 추출기가 있어야 한다** — 그때 이 게이트를 다시 돌린다 | F02 |
| **테스트 파일이 발현 좌표로 결박된다** — `afcfefab` 이 그 자리다. F02 가 서는 날 열린다 | F02 · F15 |
| P0-preflight 의 **다른 오라클들이 어느 언어에서 측정됐는지** 세어 보지 않았다 | 열린 채 |
| `Actor` 의 종류를 싣는 등록 노드가 없다 | F18 · F01(매니페스트) |
| 한 라벨이 여러 출처에 서는 스키마 형태 | [#45](https://github.com/hskim-ecoletree/palimpsest/issues/45) · F16 |
| `pal defect` 가 `Envelope` 를 돌려주지 않는다 | F05 · F06 |
| `PROVISIONAL_HISTORY_BUDGET = 400` 은 자리표시다 — 어느 측정도 정하지 않았다 | F05 (예산 회귀) |
