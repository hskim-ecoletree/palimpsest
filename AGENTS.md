# palimpsest

환경에 종속되지 않는 코드 이해의 큐레이터. **코드가 서 있다** — `pal` 바이너리(서브커맨드 **18** — `symbols`·`bind`·`narrative`·`defect`·`plan`·`deviation`·`touch`·`doctor`·`install`·`update`·`uninstall`·`hook`·`intent`·`cache`·`query`·**`serve`**·`export`·`ledger`. `crates/pal-cli/src/main.rs` 의 `enum Command` 선언 순서다. ⚠ **`serve` 는 feature `mcp` 뒤에 있다** — 기본으로 켜지고, 끄면 **17** 이 된다. 그 갈림을 CI 가 잰다)와 크레이트 **여덟**(`crates/` 아래 디렉터리 수 — 워크스페이스 멤버는 `xtask` 를 더해 아홉이다). 무엇이 실제로 서 있고 무엇이 자리뿐인지는 **문서가 아니라 산출이 말한다**: `pal doctor` 는 검사하지 못한 것을 `Residual` 로, 담을 수 없는 것을 능력 부재로 낸다.

## 지금 어디에 서 있는가

```bash
./scripts/frontier.sh
```

착수 가능한 이슈를 센다. **상태는 이슈에만 있고 문서는 그것을 복제하지 않는다** — 어디까지 왔는지 알고 싶으면 문서를 읽지 말고 이것을 실행할 것.

## 어디서부터 읽는가

| 알고 싶은 것 | 문서 |
|---|---|
| 무엇을 어느 순서로 만드나 (지형) | [docs/plan/README.md](docs/plan/README.md) — **전체를 관장한다** |
| 완성되면 실제로 어떻게 쓰이나 | [docs/how-it-works.md](docs/how-it-works.md) |
| 왜 만드나 / 모든 기능의 채점 기준 | [docs/plan/00-goals.md](docs/plan/00-goals.md) |
| 무슨 언어·라이브러리·구조로 만드나 | [docs/plan/00-stack.md](docs/plan/00-stack.md) |
| 무엇이 우리를 막을 것인가 | [docs/plan/00-risks.md](docs/plan/00-risks.md) |
| 설계 결정과 그 근거 | [docs/DESIGN.md](docs/DESIGN.md) · [docs/evidence-map.md](docs/evidence-map.md) |
| 왜 이런 제품이어야 하나 | [WHITEPAPER.md](WHITEPAPER.md) |

## 운영 순서 — **완성이 완벽보다 앞선다**

**소유자 지시 (2026-08-16).** 이 순서가 다른 모든 우선순위를 이긴다:

> **빠르게 완성한다 → 처음의 의도에 도달한다 → 효과를 본다 → 그 뒤 계속 고친다.**
> *"완벽한 프로그램을 만들 수 있다면 좋겠지만 그런 건 없잖아."*

**네 마디의 순서가 곧 규칙이다. 수정은 마지막이고 앞의 셋보다 먼저 오면 안 된다** —
완벽에 수렴하면 완성에 못 가고, 완성하지 않으면 **효과를 못 보며, 효과를 못 보면 무엇을
고쳐야 하는지도 모른다.**

1. ★ **발견을 새 이슈로 열지 않는다.** 게이트 문서의 「다음으로 넘기는 것」이 발견의
   단일 진실이다. 이슈로 또 열면 같은 것이 두 곳에 적히고(진행 규칙 4), 목록만 늘어
   완성이 멀어 보인다. **이슈는 「만들 것」만 담는다.**
   실측(2026-08-16): 게이트 하나가 닫힐 때마다 후속 이슈 1~4개가 났고 **아홉 중 하나도
   안 닫혔다** — 전부 `needs-triage` 로 방치됐다.
2. ★ **「무엇을 먼저 할까」를 정하느라 회차를 쓰지 않는다.** 착수 가능한 것 중에서
   **소비 가능해지는 지점에 가장 가까운 것**을 고르고 시작한다.
3. ★ **기능 문서가 열어 둔 퇴로를 쓰는 것은 설계대로 가는 것이다.** 반증 게이트가
   *"안 되면 약하게 선언하고 X에 집중한다"* 를 적어 뒀으면 그것을 쓴다. 포기가 아니다.
4. ★ **완벽에 수렴하는 장치를 새로 만들지 않는다** — 추가 측정 · 추가 후속 절 ·
   합격선 상향. **등록된 합격선을 재고 판정하면 끝이다.**

★ **Windows 는 지원 대상이고, 이제 실측됐다.** 소유자 지시 (2026-08-16): *"그럼 windows 를 대응한다는 가정하에 앞으로 모든 설계와 개발이 되어야 해."* 판정은 [F24-Windows 게이트](docs/gates/F24-windows-parity.md) — **통과**(2026-08-17). 라이프사이클 넷과 `doctor` 여섯 검사가 그 플랫폼에서 선다.

★ **그리고 이제 세 플랫폼이다.** [F24-크로스플랫폼 게이트](docs/gates/F24-cross-platform.md) — **통과**(2026-08-17). 같은 소스가 `ubuntu`·`macos`·`windows` 에서 서고 같은 답을 내며, 한쪽이 낸 산출물을 다른 쪽이 그대로 쓴다. **각 플랫폼 사용자가 받아서 쓸 수 있다**(릴리스 · `.github/workflows/release.yml`).

**앞으로 모든 설계·구현이 Windows 를 가정한다.** 그 가정이 코드에서 뜻하는 것은 [ADR-0023](docs/adr/0023-consistent-method-and-result-across-platforms.md) 이 정한다:

> ★ **같은 상황에서 같은 방법과 같은 결과.** 소유자 지시(2026-08-17): *"동일한 하네스가 서로 다른 플랫폼에서 일관된 방법과 결과를 주지 않는다면 그 하네스를 쓰는 프로젝트에서는 품질이 들쭉날쭉할 텐데."* **이 층의 소비자는 공유되는 프로젝트다** — 플랫폼별 예외는 그 프로젝트의 품질 편차가 된다.

- **「이 플랫폼에는 방어가 없다」를 주석으로 남기지 않는다.** 적어 둔 주석은 아무것도 안 막는다(실측: 표시가 있던 자리 셋 중 **둘이 뚫려 있었다**).
- **차이를 없앨 수 있으면 없앤다.** 플랫폼이 무언가를 못 볼 때 고를 축은 「볼 수 있는 쪽」이 아니라 **양쪽이 할 수 있는 것**이다. `rc` 가 갈리면 팀에게 **런북이 둘**이 된다. 없앨 수 없는 것만 남기고, 그때 남는 것이 무엇인지 정확히 적는다.
- **공유되는 산출물(`settings.json`·매니페스트)에 기계 고유의 값을 안 싣는다** — 절대 경로도, 그 기계의 줄바꿈도. 실으면 받는 쪽에서 **반드시** 틀리고 추적 파일이 핑퐁한다.
- 같은 물음의 플랫폼 분기는 **한 자리**에 산다. 두 곳에서 각자 답하면 한쪽이 조용히 낡는다.
- `#[cfg(unix)]` 시험에는 **다른 플랫폼에서 시끄럽게 실패하는 짝**을 단다. 그 실패는 **까닭과 함께 `xtask` 의 `외침` 에 등록**하고, `cargo xtask test` 가 *"실패 집합 = 등록 집합"* 을 양방향으로 판정한다 — **승격하고 등록을 안 지워도 걸린다.**

`cargo xtask check` 와 `cargo xtask test` 가 **`ubuntu-latest`·`macos-latest`·`windows-latest` 셋에서** 매 커밋마다 돈다(`.github/workflows/ci.yml`). ★ **CI 가 실제로 돌았고 세 플랫폼에서 초록이다**(2026-08-17). 그리고 같은 워크플로가 **양방향 상호운용**을 잰다 — 한 플랫폼이 설치·커밋한 저장소를 다른 플랫폼이 `git clone` 으로 받아 **아무것도 안 하고** `doctor` 를 돌린다.

★ **`xtask` 의 `외침` 목록은 비어 있다**(2026-08-17). 어느 플랫폼에도 안 재지는 것이 없다. 거기 들어올 자격은 「이 플랫폼에서 **원리상** 못 잰다」뿐이고, 「아직 안 했다」는 자격이 없다 — `등록은_원리상_불가능한_것만_담는다` 가 그것을 판정한다.

⚠ **유닉스 쪽은 이 기계에서 컴파일할 수 없다.** `zstd-sys` 와 tree-sitter 문법들이 C 컴파일러를 타고 크로스 gcc·WSL·docker·zig 가 없다(실측 2026-08-17). **유닉스의 컴파일과 동작은 CI 가 유일한 문이다** — 「Windows 에서 서니까 된다」로 적지 마라. 그 형태가 이미 한 번 물었다(`c19920e`).

**완성의 기준은 체크리스트가 아니라 「누군가 실제로 쓸 수 있게 되는 지점」이다.**
그 마지막 한 칸이 없으면 앞의 모든 기능이 값을 못 낸다.

⚠ **[초석](docs/plan/README.md)만 예외다** — 좌표계·정체성·타입 불변식·합격선의 사전
등록처럼 **나중에 바꾸면 그 위의 모든 것이 움직이는 자리**는 한 번에 제대로 한다.
가르는 문장 하나: *"지금 대충 하면 나중에 이 위의 것들이 전부 움직이는가."*

## 진행 규칙

전문은 [계획 §7](docs/plan/README.md)에 있다. 산출물을 쓰기 전에 걸리는 것 넷:

1. **게이트는 판정 기록을 커밋으로 남긴다** — `docs/gates/<기능>.md`에 **통과 · 반증 · 대조 불가** 셋 중 하나. 생략하고 다음으로 가는 것이 이 계획의 가장 조용한 실패 경로다.
2. **일정을 두지 않는다.** 순서는 게이트가 정하고 작업에는 상대 규모(S/M/L/XL)만 붙는다.
3. **반증은 실패가 아니다.** 기능이 취소되면 계획이 작동한 것이다.
4. **결정은 설계 문서에, 근거는 근거 대장에, 실행은 이슈에.** 같은 것을 두 곳에 적으면 그것이 곧 drift다.

**ADR은 기능 착수가 아니라 종료 시점에 발행한다.** 착수 시점의 ADR은 결정이 아니라 계획의 복사본이다.

## Agent skills

### Issue tracker

이슈는 `hskim-ecoletree/palimpsest`의 GitHub 이슈로 산다. 이 설치본의 `gh`는 2.90이라 하위 이슈와 blocking을 `gh api`로 걸어야 한다. `docs/agents/issue-tracker.md` 참조.

### Triage labels

정본 다섯 상태(`needs-triage`·`needs-info`·`ready-for-agent`·`ready-for-human`·`wontfix`)를 이름 그대로 쓴다. 라벨은 트래커에 이미 생성되어 있다. `docs/agents/triage-labels.md` 참조.

### Domain docs

단일 컨텍스트 — 루트의 `CONTEXT.md` + `docs/adr/`. **`CONTEXT.md` 는 아직 없고 없는 것이 정상 상태다.** ADR 은 **스물넷** 있다(F22·F01·F02·F03·F05·F06·F09·F10·F10-5·F10-6·F11·F12·**F24**·**F24-Windows**·**F06b** 종료 시 발행). `docs/agents/domain.md` 참조.
