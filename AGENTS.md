# palimpsest

프로젝트의 결정·설계·구현 의도를 **코드 좌표에 결박**해 두고, 코드를 만지려는 순간에 **무엇이 걸려 있고 무엇이 낡았는지** 답하는 지식 그래프. 그리고 그 그래프를 쓰는 **하네스** — 사용자 프로젝트에 설치되어 회차를 돈다.

역할은 다섯이다: **사관**(결박) · **사서**(조회) · **큐레이터**(선별) · **서사자**(엮음) · **동료**(판단). 정체성은 [ADR-0025](docs/adr/0025-the-harness-that-reads-the-graph-is-the-same-product.md) 가 정한다.

★ **이 역할들은 단계에 배치되지 않는다 — 필요하다고 판단되는 어느 자리에서든 부르는 도구다.** 가장 자주 걸리는 자리 하나: **`grep` 을 집으려는 순간에 그래프에 먼저 묻는다.** `grep` 은 문자열을 맞히고 그래프는 **관계를 안다** — 부르는 자리 · 흘러드는 데이터 · 걸린 결정 · 낡은 것. 구현 계획을 세울 때 · 리뷰할 때 · 질문을 만들 때 · 의도의 진위를 볼 때 전부 같은 자리다.

무엇이 실제로 서 있는지는 **문서가 아니라 산출이 말한다** — `pal doctor` 는 검사하지 못한 것을 `Residual` 로, 담을 수 없는 것을 **능력 부재**로 낸다.

## 지금 어디에 서 있는가

```bash
./scripts/frontier.sh
```

**상태는 이슈에만 산다.** 어디까지 왔는지는 문서를 읽지 말고 이것을 실행해서 안다.

## 일을 시작할 때

**`/round` 를 연다.** 회차의 절차 전문이 거기 있다 — 인터뷰 · 사전부검 · 완수 조건 · 승인 · 루프 · 검증 · 효과 · 종료.

무엇을 하든 그 안에서 한다. 이슈를 집을 때도, 소유자가 새 것을 요구할 때도, 버그 하나를 고칠 때도.

★ **회차가 끝날 때 「나중에」가 0 이다.** 그것이 이 규약의 합격선이고, 나머지는 전부 그것을 세우기 위한 장치다.

## 어디서부터 읽는가

| 알고 싶은 것 | 문서 |
|---|---|
| 회차를 어떻게 도나 | **`/round`** — 절차의 정본 |
| 무엇을 어느 순서로 만드나 (지형) | [docs/plan/README.md](docs/plan/README.md) — **전체를 관장한다** |
| 완성되면 실제로 어떻게 쓰이나 | [docs/how-it-works.md](docs/how-it-works.md) |
| 왜 만드나 / 모든 기능의 채점 기준 | [docs/plan/00-goals.md](docs/plan/00-goals.md) |
| 무슨 언어·라이브러리·구조로 만드나 | [docs/plan/00-stack.md](docs/plan/00-stack.md) |
| 무엇이 우리를 막을 것인가 | [docs/plan/00-risks.md](docs/plan/00-risks.md) |
| 설계 결정과 그 근거 | [docs/DESIGN.md](docs/DESIGN.md) · [docs/evidence-map.md](docs/evidence-map.md) |
| 왜 이런 제품이어야 하나 | [WHITEPAPER.md](WHITEPAPER.md) |
| 소유자가 실제로 무엇을 요구했나 | [docs/instructions/](docs/instructions/) — 원문이 산다 |

---

# 걸리는 것

절차는 `/round` 가 지고, 여기 남는 것은 **어느 회차에서든 걸리는 것**이다.

## 순서 — 완성이 완벽보다 앞선다

**소유자 지시 (2026-08-16).** 이 순서가 다른 모든 우선순위를 이긴다:

> **빠르게 완성한다 → 처음의 의도에 도달한다 → 효과를 본다 → 그 뒤 계속 고친다.**

완성하지 않으면 효과를 못 보고, 효과를 못 보면 무엇을 고쳐야 하는지도 모른다. 착수는 **소비 가능해지는 지점에 가장 가까운 것**을 프론티어에서 고르는 것으로 시작한다 — 무엇을 먼저 할지 정하는 데 회차를 쓰지 않는다.

⚠ **[초석](docs/plan/README.md)만 예외다** — 좌표계·정체성·타입 불변식·합격선의 사전 등록처럼 나중에 바꾸면 그 위의 모든 것이 움직이는 자리는 한 번에 제대로 한다. 가르는 문장: *"지금 대충 하면 나중에 이 위의 것들이 전부 움직이는가."*

## 플랫폼 셋에서 같은 방법과 같은 결과

`ubuntu`·`macos`·`windows` 셋 다 지원 대상이고 CI 가 매 커밋 잰다. 전문은 [ADR-0023](docs/adr/0023-consistent-method-and-result-across-platforms.md).

- **차이를 없앨 수 있으면 없앤다.** 고를 축은 「볼 수 있는 쪽」이 아니라 **양쪽이 할 수 있는 것**이다.
- **표시 대신 장치를 남긴다.** 「이 플랫폼에는 방어가 없다」는 주석은 아무것도 안 막는다 (실측: 표시가 있던 자리 셋 중 둘이 뚫려 있었다).
- **공유되는 산출물**은 기계 고유의 값을 안 싣는다 — 절대 경로도, 그 기계의 줄바꿈도.
- 같은 물음의 플랫폼 분기는 **한 자리**에 산다.
- `#[cfg(unix)]` 시험에는 다른 플랫폼에서 시끄럽게 실패하는 짝을 달고, 그 실패를 까닭과 함께 `xtask` 의 **외침**에 등록한다. **들어올 자격은 「원리상 못 잰다」뿐이고 「아직 안 했다」는 자격이 없다.**

## 표면은 넷이고 그 이상 없다

| 표면 | 하는 일 |
|---|---|
| 스킬 | 회차를 어떻게 도는가 |
| 에이전트 정의 | 역할·경계·반환 형식 |
| 훅 | 우회하면 피해가 큰 경계 |
| `pal` CLI | 상태 관리자 — **대화하지 않는다** |

**표면을 더할 때 목록을 더하지 않는다.** 목록은 언제나 코드가 이미 아는 것에서 렌더링된다 ([ADR-0024](docs/adr/0024-an-adapter-that-can-diverge-is-a-second-core.md)).

## 결정과 근거와 실행은 각각 한 자리에 산다

**결정은 설계 문서에, 근거는 근거 대장에, 실행은 이슈에.** 같은 것을 두 곳에 적으면 그것이 곧 drift 다.

**환경도 진실원이다** — `--help` · 설정 파일 · 디렉터리 배치가 답하는 것을 문서가 복제하면 그 문서는 낡는 캐시다.

**일정을 두지 않는다.** 순서는 게이트가 정하고 작업에는 상대 규모(S/M/L/XL)만 붙는다.

---

# 도구

**이슈 트래커** — `hskim-ecoletree/palimpsest` 의 GitHub 이슈. 이 설치본의 `gh` 는 2.90 이라 하위 이슈와 blocking 을 `gh api` 로 건다. **이슈는 「만들 것」을 담는다.** [docs/agents/issue-tracker.md](docs/agents/issue-tracker.md)

**라벨** — [docs/agents/triage-labels.md](docs/agents/triage-labels.md)

**도메인 문서** — 단일 컨텍스트: 루트의 `CONTEXT.md` + `docs/adr/`. [docs/agents/domain.md](docs/agents/domain.md)
